/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $TypeError = $util.global.TypeError, $Number = $util.global.Number, $parseInt = $util.global.parseInt, $String = $util.global.String, $BigInt = $util.global.BigInt, $Boolean = $util.global.Boolean, $Array = $util.global.Array, $isFinite = $util.global.isFinite;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const lnrpc = $root.lnrpc = (() => {

    /**
     * Namespace lnrpc.
     * @exports lnrpc
     * @namespace
     */
    const lnrpc = {};

    lnrpc.Lightning = (function() {

        /**
         * Constructs a new Lightning service.
         * @memberof lnrpc
         * @classdesc Represents a Lightning
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const Lightning = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (Lightning.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = Lightning;

        /**
         * Creates new Lightning service using the specified rpc implementation.
         * @function create
         * @memberof lnrpc.Lightning
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {Lightning} RPC service. Useful where requests and/or responses are streamed.
         */
        Lightning.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link lnrpc.Lightning#walletBalance}.
         * @memberof lnrpc.Lightning
         * @typedef WalletBalanceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.WalletBalanceResponse} [response] WalletBalanceResponse
         */

        /**
         * Calls WalletBalance.
         * @memberof lnrpc.Lightning
         * @typedef WalletBalance
         * @type {{
         *   (request: lnrpc.IWalletBalanceRequest, callback: lnrpc.Lightning.WalletBalanceCallback): void;
         *   (request: lnrpc.IWalletBalanceRequest): Promise<lnrpc.WalletBalanceResponse>;
         *   readonly name: "WalletBalance";
         *   readonly path: "/lnrpc.Lightning/WalletBalance";
         *   readonly requestType: "WalletBalanceRequest";
         *   readonly responseType: "WalletBalanceResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls WalletBalance.
         * @name lnrpc.Lightning#walletBalance
         * @type {lnrpc.Lightning.WalletBalance}
         */
        $Object.defineProperties(Lightning.prototype.walletBalance = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.walletBalance, $root.lnrpc.WalletBalanceRequest, $root.lnrpc.WalletBalanceResponse, request, callback);
        }, {
            name: { value: "WalletBalance" },
            path: { value: "/lnrpc.Lightning/WalletBalance" },
            requestType: { value: "WalletBalanceRequest" },
            responseType: { value: "WalletBalanceResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#channelBalance}.
         * @memberof lnrpc.Lightning
         * @typedef ChannelBalanceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChannelBalanceResponse} [response] ChannelBalanceResponse
         */

        /**
         * Calls ChannelBalance.
         * @memberof lnrpc.Lightning
         * @typedef ChannelBalance
         * @type {{
         *   (request: lnrpc.IChannelBalanceRequest, callback: lnrpc.Lightning.ChannelBalanceCallback): void;
         *   (request: lnrpc.IChannelBalanceRequest): Promise<lnrpc.ChannelBalanceResponse>;
         *   readonly name: "ChannelBalance";
         *   readonly path: "/lnrpc.Lightning/ChannelBalance";
         *   readonly requestType: "ChannelBalanceRequest";
         *   readonly responseType: "ChannelBalanceResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ChannelBalance.
         * @name lnrpc.Lightning#channelBalance
         * @type {lnrpc.Lightning.ChannelBalance}
         */
        $Object.defineProperties(Lightning.prototype.channelBalance = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.channelBalance, $root.lnrpc.ChannelBalanceRequest, $root.lnrpc.ChannelBalanceResponse, request, callback);
        }, {
            name: { value: "ChannelBalance" },
            path: { value: "/lnrpc.Lightning/ChannelBalance" },
            requestType: { value: "ChannelBalanceRequest" },
            responseType: { value: "ChannelBalanceResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#getTransactions}.
         * @memberof lnrpc.Lightning
         * @typedef GetTransactionsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.TransactionDetails} [response] TransactionDetails
         */

        /**
         * Calls GetTransactions.
         * @memberof lnrpc.Lightning
         * @typedef GetTransactions
         * @type {{
         *   (request: lnrpc.IGetTransactionsRequest, callback: lnrpc.Lightning.GetTransactionsCallback): void;
         *   (request: lnrpc.IGetTransactionsRequest): Promise<lnrpc.TransactionDetails>;
         *   readonly name: "GetTransactions";
         *   readonly path: "/lnrpc.Lightning/GetTransactions";
         *   readonly requestType: "GetTransactionsRequest";
         *   readonly responseType: "TransactionDetails";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetTransactions.
         * @name lnrpc.Lightning#getTransactions
         * @type {lnrpc.Lightning.GetTransactions}
         */
        $Object.defineProperties(Lightning.prototype.getTransactions = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.getTransactions, $root.lnrpc.GetTransactionsRequest, $root.lnrpc.TransactionDetails, request, callback);
        }, {
            name: { value: "GetTransactions" },
            path: { value: "/lnrpc.Lightning/GetTransactions" },
            requestType: { value: "GetTransactionsRequest" },
            responseType: { value: "TransactionDetails" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#estimateFee}.
         * @memberof lnrpc.Lightning
         * @typedef EstimateFeeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.EstimateFeeResponse} [response] EstimateFeeResponse
         */

        /**
         * Calls EstimateFee.
         * @memberof lnrpc.Lightning
         * @typedef EstimateFee
         * @type {{
         *   (request: lnrpc.IEstimateFeeRequest, callback: lnrpc.Lightning.EstimateFeeCallback): void;
         *   (request: lnrpc.IEstimateFeeRequest): Promise<lnrpc.EstimateFeeResponse>;
         *   readonly name: "EstimateFee";
         *   readonly path: "/lnrpc.Lightning/EstimateFee";
         *   readonly requestType: "EstimateFeeRequest";
         *   readonly responseType: "EstimateFeeResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls EstimateFee.
         * @name lnrpc.Lightning#estimateFee
         * @type {lnrpc.Lightning.EstimateFee}
         */
        $Object.defineProperties(Lightning.prototype.estimateFee = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.estimateFee, $root.lnrpc.EstimateFeeRequest, $root.lnrpc.EstimateFeeResponse, request, callback);
        }, {
            name: { value: "EstimateFee" },
            path: { value: "/lnrpc.Lightning/EstimateFee" },
            requestType: { value: "EstimateFeeRequest" },
            responseType: { value: "EstimateFeeResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#sendCoins}.
         * @memberof lnrpc.Lightning
         * @typedef SendCoinsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.SendCoinsResponse} [response] SendCoinsResponse
         */

        /**
         * Calls SendCoins.
         * @memberof lnrpc.Lightning
         * @typedef SendCoins
         * @type {{
         *   (request: lnrpc.ISendCoinsRequest, callback: lnrpc.Lightning.SendCoinsCallback): void;
         *   (request: lnrpc.ISendCoinsRequest): Promise<lnrpc.SendCoinsResponse>;
         *   readonly name: "SendCoins";
         *   readonly path: "/lnrpc.Lightning/SendCoins";
         *   readonly requestType: "SendCoinsRequest";
         *   readonly responseType: "SendCoinsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SendCoins.
         * @name lnrpc.Lightning#sendCoins
         * @type {lnrpc.Lightning.SendCoins}
         */
        $Object.defineProperties(Lightning.prototype.sendCoins = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.sendCoins, $root.lnrpc.SendCoinsRequest, $root.lnrpc.SendCoinsResponse, request, callback);
        }, {
            name: { value: "SendCoins" },
            path: { value: "/lnrpc.Lightning/SendCoins" },
            requestType: { value: "SendCoinsRequest" },
            responseType: { value: "SendCoinsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#listUnspent}.
         * @memberof lnrpc.Lightning
         * @typedef ListUnspentCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ListUnspentResponse} [response] ListUnspentResponse
         */

        /**
         * Calls ListUnspent.
         * @memberof lnrpc.Lightning
         * @typedef ListUnspent
         * @type {{
         *   (request: lnrpc.IListUnspentRequest, callback: lnrpc.Lightning.ListUnspentCallback): void;
         *   (request: lnrpc.IListUnspentRequest): Promise<lnrpc.ListUnspentResponse>;
         *   readonly name: "ListUnspent";
         *   readonly path: "/lnrpc.Lightning/ListUnspent";
         *   readonly requestType: "ListUnspentRequest";
         *   readonly responseType: "ListUnspentResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListUnspent.
         * @name lnrpc.Lightning#listUnspent
         * @type {lnrpc.Lightning.ListUnspent}
         */
        $Object.defineProperties(Lightning.prototype.listUnspent = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.listUnspent, $root.lnrpc.ListUnspentRequest, $root.lnrpc.ListUnspentResponse, request, callback);
        }, {
            name: { value: "ListUnspent" },
            path: { value: "/lnrpc.Lightning/ListUnspent" },
            requestType: { value: "ListUnspentRequest" },
            responseType: { value: "ListUnspentResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#subscribeTransactions}.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeTransactionsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.Transaction} [response] Transaction
         */

        /**
         * Calls SubscribeTransactions.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeTransactions
         * @type {{
         *   (request: lnrpc.IGetTransactionsRequest, callback: lnrpc.Lightning.SubscribeTransactionsCallback): void;
         *   (request: lnrpc.IGetTransactionsRequest): Promise<lnrpc.Transaction>;
         *   readonly name: "SubscribeTransactions";
         *   readonly path: "/lnrpc.Lightning/SubscribeTransactions";
         *   readonly requestType: "GetTransactionsRequest";
         *   readonly responseType: "Transaction";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribeTransactions.
         * @name lnrpc.Lightning#subscribeTransactions
         * @type {lnrpc.Lightning.SubscribeTransactions}
         */
        $Object.defineProperties(Lightning.prototype.subscribeTransactions = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.subscribeTransactions, $root.lnrpc.GetTransactionsRequest, $root.lnrpc.Transaction, request, callback);
        }, {
            name: { value: "SubscribeTransactions" },
            path: { value: "/lnrpc.Lightning/SubscribeTransactions" },
            requestType: { value: "GetTransactionsRequest" },
            responseType: { value: "Transaction" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#sendMany}.
         * @memberof lnrpc.Lightning
         * @typedef SendManyCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.SendManyResponse} [response] SendManyResponse
         */

        /**
         * Calls SendMany.
         * @memberof lnrpc.Lightning
         * @typedef SendMany
         * @type {{
         *   (request: lnrpc.ISendManyRequest, callback: lnrpc.Lightning.SendManyCallback): void;
         *   (request: lnrpc.ISendManyRequest): Promise<lnrpc.SendManyResponse>;
         *   readonly name: "SendMany";
         *   readonly path: "/lnrpc.Lightning/SendMany";
         *   readonly requestType: "SendManyRequest";
         *   readonly responseType: "SendManyResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SendMany.
         * @name lnrpc.Lightning#sendMany
         * @type {lnrpc.Lightning.SendMany}
         */
        $Object.defineProperties(Lightning.prototype.sendMany = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.sendMany, $root.lnrpc.SendManyRequest, $root.lnrpc.SendManyResponse, request, callback);
        }, {
            name: { value: "SendMany" },
            path: { value: "/lnrpc.Lightning/SendMany" },
            requestType: { value: "SendManyRequest" },
            responseType: { value: "SendManyResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#newAddress}.
         * @memberof lnrpc.Lightning
         * @typedef NewAddressCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.NewAddressResponse} [response] NewAddressResponse
         */

        /**
         * Calls NewAddress.
         * @memberof lnrpc.Lightning
         * @typedef NewAddress
         * @type {{
         *   (request: lnrpc.INewAddressRequest, callback: lnrpc.Lightning.NewAddressCallback): void;
         *   (request: lnrpc.INewAddressRequest): Promise<lnrpc.NewAddressResponse>;
         *   readonly name: "NewAddress";
         *   readonly path: "/lnrpc.Lightning/NewAddress";
         *   readonly requestType: "NewAddressRequest";
         *   readonly responseType: "NewAddressResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls NewAddress.
         * @name lnrpc.Lightning#newAddress
         * @type {lnrpc.Lightning.NewAddress}
         */
        $Object.defineProperties(Lightning.prototype.newAddress = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.newAddress, $root.lnrpc.NewAddressRequest, $root.lnrpc.NewAddressResponse, request, callback);
        }, {
            name: { value: "NewAddress" },
            path: { value: "/lnrpc.Lightning/NewAddress" },
            requestType: { value: "NewAddressRequest" },
            responseType: { value: "NewAddressResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#signMessage}.
         * @memberof lnrpc.Lightning
         * @typedef SignMessageCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.SignMessageResponse} [response] SignMessageResponse
         */

        /**
         * Calls SignMessage.
         * @memberof lnrpc.Lightning
         * @typedef SignMessage
         * @type {{
         *   (request: lnrpc.ISignMessageRequest, callback: lnrpc.Lightning.SignMessageCallback): void;
         *   (request: lnrpc.ISignMessageRequest): Promise<lnrpc.SignMessageResponse>;
         *   readonly name: "SignMessage";
         *   readonly path: "/lnrpc.Lightning/SignMessage";
         *   readonly requestType: "SignMessageRequest";
         *   readonly responseType: "SignMessageResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SignMessage.
         * @name lnrpc.Lightning#signMessage
         * @type {lnrpc.Lightning.SignMessage}
         */
        $Object.defineProperties(Lightning.prototype.signMessage = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.signMessage, $root.lnrpc.SignMessageRequest, $root.lnrpc.SignMessageResponse, request, callback);
        }, {
            name: { value: "SignMessage" },
            path: { value: "/lnrpc.Lightning/SignMessage" },
            requestType: { value: "SignMessageRequest" },
            responseType: { value: "SignMessageResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#verifyMessage}.
         * @memberof lnrpc.Lightning
         * @typedef VerifyMessageCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.VerifyMessageResponse} [response] VerifyMessageResponse
         */

        /**
         * Calls VerifyMessage.
         * @memberof lnrpc.Lightning
         * @typedef VerifyMessage
         * @type {{
         *   (request: lnrpc.IVerifyMessageRequest, callback: lnrpc.Lightning.VerifyMessageCallback): void;
         *   (request: lnrpc.IVerifyMessageRequest): Promise<lnrpc.VerifyMessageResponse>;
         *   readonly name: "VerifyMessage";
         *   readonly path: "/lnrpc.Lightning/VerifyMessage";
         *   readonly requestType: "VerifyMessageRequest";
         *   readonly responseType: "VerifyMessageResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls VerifyMessage.
         * @name lnrpc.Lightning#verifyMessage
         * @type {lnrpc.Lightning.VerifyMessage}
         */
        $Object.defineProperties(Lightning.prototype.verifyMessage = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.verifyMessage, $root.lnrpc.VerifyMessageRequest, $root.lnrpc.VerifyMessageResponse, request, callback);
        }, {
            name: { value: "VerifyMessage" },
            path: { value: "/lnrpc.Lightning/VerifyMessage" },
            requestType: { value: "VerifyMessageRequest" },
            responseType: { value: "VerifyMessageResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#connectPeer}.
         * @memberof lnrpc.Lightning
         * @typedef ConnectPeerCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ConnectPeerResponse} [response] ConnectPeerResponse
         */

        /**
         * Calls ConnectPeer.
         * @memberof lnrpc.Lightning
         * @typedef ConnectPeer
         * @type {{
         *   (request: lnrpc.IConnectPeerRequest, callback: lnrpc.Lightning.ConnectPeerCallback): void;
         *   (request: lnrpc.IConnectPeerRequest): Promise<lnrpc.ConnectPeerResponse>;
         *   readonly name: "ConnectPeer";
         *   readonly path: "/lnrpc.Lightning/ConnectPeer";
         *   readonly requestType: "ConnectPeerRequest";
         *   readonly responseType: "ConnectPeerResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ConnectPeer.
         * @name lnrpc.Lightning#connectPeer
         * @type {lnrpc.Lightning.ConnectPeer}
         */
        $Object.defineProperties(Lightning.prototype.connectPeer = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.connectPeer, $root.lnrpc.ConnectPeerRequest, $root.lnrpc.ConnectPeerResponse, request, callback);
        }, {
            name: { value: "ConnectPeer" },
            path: { value: "/lnrpc.Lightning/ConnectPeer" },
            requestType: { value: "ConnectPeerRequest" },
            responseType: { value: "ConnectPeerResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#disconnectPeer}.
         * @memberof lnrpc.Lightning
         * @typedef DisconnectPeerCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.DisconnectPeerResponse} [response] DisconnectPeerResponse
         */

        /**
         * Calls DisconnectPeer.
         * @memberof lnrpc.Lightning
         * @typedef DisconnectPeer
         * @type {{
         *   (request: lnrpc.IDisconnectPeerRequest, callback: lnrpc.Lightning.DisconnectPeerCallback): void;
         *   (request: lnrpc.IDisconnectPeerRequest): Promise<lnrpc.DisconnectPeerResponse>;
         *   readonly name: "DisconnectPeer";
         *   readonly path: "/lnrpc.Lightning/DisconnectPeer";
         *   readonly requestType: "DisconnectPeerRequest";
         *   readonly responseType: "DisconnectPeerResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DisconnectPeer.
         * @name lnrpc.Lightning#disconnectPeer
         * @type {lnrpc.Lightning.DisconnectPeer}
         */
        $Object.defineProperties(Lightning.prototype.disconnectPeer = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.disconnectPeer, $root.lnrpc.DisconnectPeerRequest, $root.lnrpc.DisconnectPeerResponse, request, callback);
        }, {
            name: { value: "DisconnectPeer" },
            path: { value: "/lnrpc.Lightning/DisconnectPeer" },
            requestType: { value: "DisconnectPeerRequest" },
            responseType: { value: "DisconnectPeerResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#listPeers}.
         * @memberof lnrpc.Lightning
         * @typedef ListPeersCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ListPeersResponse} [response] ListPeersResponse
         */

        /**
         * Calls ListPeers.
         * @memberof lnrpc.Lightning
         * @typedef ListPeers
         * @type {{
         *   (request: lnrpc.IListPeersRequest, callback: lnrpc.Lightning.ListPeersCallback): void;
         *   (request: lnrpc.IListPeersRequest): Promise<lnrpc.ListPeersResponse>;
         *   readonly name: "ListPeers";
         *   readonly path: "/lnrpc.Lightning/ListPeers";
         *   readonly requestType: "ListPeersRequest";
         *   readonly responseType: "ListPeersResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListPeers.
         * @name lnrpc.Lightning#listPeers
         * @type {lnrpc.Lightning.ListPeers}
         */
        $Object.defineProperties(Lightning.prototype.listPeers = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.listPeers, $root.lnrpc.ListPeersRequest, $root.lnrpc.ListPeersResponse, request, callback);
        }, {
            name: { value: "ListPeers" },
            path: { value: "/lnrpc.Lightning/ListPeers" },
            requestType: { value: "ListPeersRequest" },
            responseType: { value: "ListPeersResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#subscribePeerEvents}.
         * @memberof lnrpc.Lightning
         * @typedef SubscribePeerEventsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.PeerEvent} [response] PeerEvent
         */

        /**
         * Calls SubscribePeerEvents.
         * @memberof lnrpc.Lightning
         * @typedef SubscribePeerEvents
         * @type {{
         *   (request: lnrpc.IPeerEventSubscription, callback: lnrpc.Lightning.SubscribePeerEventsCallback): void;
         *   (request: lnrpc.IPeerEventSubscription): Promise<lnrpc.PeerEvent>;
         *   readonly name: "SubscribePeerEvents";
         *   readonly path: "/lnrpc.Lightning/SubscribePeerEvents";
         *   readonly requestType: "PeerEventSubscription";
         *   readonly responseType: "PeerEvent";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribePeerEvents.
         * @name lnrpc.Lightning#subscribePeerEvents
         * @type {lnrpc.Lightning.SubscribePeerEvents}
         */
        $Object.defineProperties(Lightning.prototype.subscribePeerEvents = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.subscribePeerEvents, $root.lnrpc.PeerEventSubscription, $root.lnrpc.PeerEvent, request, callback);
        }, {
            name: { value: "SubscribePeerEvents" },
            path: { value: "/lnrpc.Lightning/SubscribePeerEvents" },
            requestType: { value: "PeerEventSubscription" },
            responseType: { value: "PeerEvent" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#getInfo}.
         * @memberof lnrpc.Lightning
         * @typedef GetInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.GetInfoResponse} [response] GetInfoResponse
         */

        /**
         * Calls GetInfo.
         * @memberof lnrpc.Lightning
         * @typedef GetInfo
         * @type {{
         *   (request: lnrpc.IGetInfoRequest, callback: lnrpc.Lightning.GetInfoCallback): void;
         *   (request: lnrpc.IGetInfoRequest): Promise<lnrpc.GetInfoResponse>;
         *   readonly name: "GetInfo";
         *   readonly path: "/lnrpc.Lightning/GetInfo";
         *   readonly requestType: "GetInfoRequest";
         *   readonly responseType: "GetInfoResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetInfo.
         * @name lnrpc.Lightning#getInfo
         * @type {lnrpc.Lightning.GetInfo}
         */
        $Object.defineProperties(Lightning.prototype.getInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.getInfo, $root.lnrpc.GetInfoRequest, $root.lnrpc.GetInfoResponse, request, callback);
        }, {
            name: { value: "GetInfo" },
            path: { value: "/lnrpc.Lightning/GetInfo" },
            requestType: { value: "GetInfoRequest" },
            responseType: { value: "GetInfoResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#getDebugInfo}.
         * @memberof lnrpc.Lightning
         * @typedef GetDebugInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.GetDebugInfoResponse} [response] GetDebugInfoResponse
         */

        /**
         * Calls GetDebugInfo.
         * @memberof lnrpc.Lightning
         * @typedef GetDebugInfo
         * @type {{
         *   (request: lnrpc.IGetDebugInfoRequest, callback: lnrpc.Lightning.GetDebugInfoCallback): void;
         *   (request: lnrpc.IGetDebugInfoRequest): Promise<lnrpc.GetDebugInfoResponse>;
         *   readonly name: "GetDebugInfo";
         *   readonly path: "/lnrpc.Lightning/GetDebugInfo";
         *   readonly requestType: "GetDebugInfoRequest";
         *   readonly responseType: "GetDebugInfoResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetDebugInfo.
         * @name lnrpc.Lightning#getDebugInfo
         * @type {lnrpc.Lightning.GetDebugInfo}
         */
        $Object.defineProperties(Lightning.prototype.getDebugInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.getDebugInfo, $root.lnrpc.GetDebugInfoRequest, $root.lnrpc.GetDebugInfoResponse, request, callback);
        }, {
            name: { value: "GetDebugInfo" },
            path: { value: "/lnrpc.Lightning/GetDebugInfo" },
            requestType: { value: "GetDebugInfoRequest" },
            responseType: { value: "GetDebugInfoResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#getRecoveryInfo}.
         * @memberof lnrpc.Lightning
         * @typedef GetRecoveryInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.GetRecoveryInfoResponse} [response] GetRecoveryInfoResponse
         */

        /**
         * lncli: `getrecoveryinfo`
         * GetRecoveryInfo returns information concerning the recovery mode including
         * whether it's in a recovery mode, whether the recovery is finished, and the
         * progress made so far.
         * @memberof lnrpc.Lightning
         * @typedef GetRecoveryInfo
         * @type {{
         *   (request: lnrpc.IGetRecoveryInfoRequest, callback: lnrpc.Lightning.GetRecoveryInfoCallback): void;
         *   (request: lnrpc.IGetRecoveryInfoRequest): Promise<lnrpc.GetRecoveryInfoResponse>;
         *   readonly name: "GetRecoveryInfo";
         *   readonly path: "/lnrpc.Lightning/GetRecoveryInfo";
         *   readonly requestType: "GetRecoveryInfoRequest";
         *   readonly responseType: "GetRecoveryInfoResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * lncli: `getrecoveryinfo`
         * GetRecoveryInfo returns information concerning the recovery mode including
         * whether it's in a recovery mode, whether the recovery is finished, and the
         * progress made so far.
         * @name lnrpc.Lightning#getRecoveryInfo
         * @type {lnrpc.Lightning.GetRecoveryInfo}
         */
        $Object.defineProperties(Lightning.prototype.getRecoveryInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.getRecoveryInfo, $root.lnrpc.GetRecoveryInfoRequest, $root.lnrpc.GetRecoveryInfoResponse, request, callback);
        }, {
            name: { value: "GetRecoveryInfo" },
            path: { value: "/lnrpc.Lightning/GetRecoveryInfo" },
            requestType: { value: "GetRecoveryInfoRequest" },
            responseType: { value: "GetRecoveryInfoResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#pendingChannels}.
         * @memberof lnrpc.Lightning
         * @typedef PendingChannelsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.PendingChannelsResponse} [response] PendingChannelsResponse
         */

        /**
         * Calls PendingChannels.
         * @memberof lnrpc.Lightning
         * @typedef PendingChannels
         * @type {{
         *   (request: lnrpc.IPendingChannelsRequest, callback: lnrpc.Lightning.PendingChannelsCallback): void;
         *   (request: lnrpc.IPendingChannelsRequest): Promise<lnrpc.PendingChannelsResponse>;
         *   readonly name: "PendingChannels";
         *   readonly path: "/lnrpc.Lightning/PendingChannels";
         *   readonly requestType: "PendingChannelsRequest";
         *   readonly responseType: "PendingChannelsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls PendingChannels.
         * @name lnrpc.Lightning#pendingChannels
         * @type {lnrpc.Lightning.PendingChannels}
         */
        $Object.defineProperties(Lightning.prototype.pendingChannels = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.pendingChannels, $root.lnrpc.PendingChannelsRequest, $root.lnrpc.PendingChannelsResponse, request, callback);
        }, {
            name: { value: "PendingChannels" },
            path: { value: "/lnrpc.Lightning/PendingChannels" },
            requestType: { value: "PendingChannelsRequest" },
            responseType: { value: "PendingChannelsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#listChannels}.
         * @memberof lnrpc.Lightning
         * @typedef ListChannelsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ListChannelsResponse} [response] ListChannelsResponse
         */

        /**
         * Calls ListChannels.
         * @memberof lnrpc.Lightning
         * @typedef ListChannels
         * @type {{
         *   (request: lnrpc.IListChannelsRequest, callback: lnrpc.Lightning.ListChannelsCallback): void;
         *   (request: lnrpc.IListChannelsRequest): Promise<lnrpc.ListChannelsResponse>;
         *   readonly name: "ListChannels";
         *   readonly path: "/lnrpc.Lightning/ListChannels";
         *   readonly requestType: "ListChannelsRequest";
         *   readonly responseType: "ListChannelsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListChannels.
         * @name lnrpc.Lightning#listChannels
         * @type {lnrpc.Lightning.ListChannels}
         */
        $Object.defineProperties(Lightning.prototype.listChannels = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.listChannels, $root.lnrpc.ListChannelsRequest, $root.lnrpc.ListChannelsResponse, request, callback);
        }, {
            name: { value: "ListChannels" },
            path: { value: "/lnrpc.Lightning/ListChannels" },
            requestType: { value: "ListChannelsRequest" },
            responseType: { value: "ListChannelsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#subscribeChannelEvents}.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeChannelEventsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChannelEventUpdate} [response] ChannelEventUpdate
         */

        /**
         * Calls SubscribeChannelEvents.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeChannelEvents
         * @type {{
         *   (request: lnrpc.IChannelEventSubscription, callback: lnrpc.Lightning.SubscribeChannelEventsCallback): void;
         *   (request: lnrpc.IChannelEventSubscription): Promise<lnrpc.ChannelEventUpdate>;
         *   readonly name: "SubscribeChannelEvents";
         *   readonly path: "/lnrpc.Lightning/SubscribeChannelEvents";
         *   readonly requestType: "ChannelEventSubscription";
         *   readonly responseType: "ChannelEventUpdate";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribeChannelEvents.
         * @name lnrpc.Lightning#subscribeChannelEvents
         * @type {lnrpc.Lightning.SubscribeChannelEvents}
         */
        $Object.defineProperties(Lightning.prototype.subscribeChannelEvents = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.subscribeChannelEvents, $root.lnrpc.ChannelEventSubscription, $root.lnrpc.ChannelEventUpdate, request, callback);
        }, {
            name: { value: "SubscribeChannelEvents" },
            path: { value: "/lnrpc.Lightning/SubscribeChannelEvents" },
            requestType: { value: "ChannelEventSubscription" },
            responseType: { value: "ChannelEventUpdate" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#closedChannels}.
         * @memberof lnrpc.Lightning
         * @typedef ClosedChannelsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ClosedChannelsResponse} [response] ClosedChannelsResponse
         */

        /**
         * Calls ClosedChannels.
         * @memberof lnrpc.Lightning
         * @typedef ClosedChannels
         * @type {{
         *   (request: lnrpc.IClosedChannelsRequest, callback: lnrpc.Lightning.ClosedChannelsCallback): void;
         *   (request: lnrpc.IClosedChannelsRequest): Promise<lnrpc.ClosedChannelsResponse>;
         *   readonly name: "ClosedChannels";
         *   readonly path: "/lnrpc.Lightning/ClosedChannels";
         *   readonly requestType: "ClosedChannelsRequest";
         *   readonly responseType: "ClosedChannelsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ClosedChannels.
         * @name lnrpc.Lightning#closedChannels
         * @type {lnrpc.Lightning.ClosedChannels}
         */
        $Object.defineProperties(Lightning.prototype.closedChannels = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.closedChannels, $root.lnrpc.ClosedChannelsRequest, $root.lnrpc.ClosedChannelsResponse, request, callback);
        }, {
            name: { value: "ClosedChannels" },
            path: { value: "/lnrpc.Lightning/ClosedChannels" },
            requestType: { value: "ClosedChannelsRequest" },
            responseType: { value: "ClosedChannelsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#openChannelSync}.
         * @memberof lnrpc.Lightning
         * @typedef OpenChannelSyncCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChannelPoint} [response] ChannelPoint
         */

        /**
         * Calls OpenChannelSync.
         * @memberof lnrpc.Lightning
         * @typedef OpenChannelSync
         * @type {{
         *   (request: lnrpc.IOpenChannelRequest, callback: lnrpc.Lightning.OpenChannelSyncCallback): void;
         *   (request: lnrpc.IOpenChannelRequest): Promise<lnrpc.ChannelPoint>;
         *   readonly name: "OpenChannelSync";
         *   readonly path: "/lnrpc.Lightning/OpenChannelSync";
         *   readonly requestType: "OpenChannelRequest";
         *   readonly responseType: "ChannelPoint";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls OpenChannelSync.
         * @name lnrpc.Lightning#openChannelSync
         * @type {lnrpc.Lightning.OpenChannelSync}
         */
        $Object.defineProperties(Lightning.prototype.openChannelSync = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.openChannelSync, $root.lnrpc.OpenChannelRequest, $root.lnrpc.ChannelPoint, request, callback);
        }, {
            name: { value: "OpenChannelSync" },
            path: { value: "/lnrpc.Lightning/OpenChannelSync" },
            requestType: { value: "OpenChannelRequest" },
            responseType: { value: "ChannelPoint" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#openChannel}.
         * @memberof lnrpc.Lightning
         * @typedef OpenChannelCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.OpenStatusUpdate} [response] OpenStatusUpdate
         */

        /**
         * Calls OpenChannel.
         * @memberof lnrpc.Lightning
         * @typedef OpenChannel
         * @type {{
         *   (request: lnrpc.IOpenChannelRequest, callback: lnrpc.Lightning.OpenChannelCallback): void;
         *   (request: lnrpc.IOpenChannelRequest): Promise<lnrpc.OpenStatusUpdate>;
         *   readonly name: "OpenChannel";
         *   readonly path: "/lnrpc.Lightning/OpenChannel";
         *   readonly requestType: "OpenChannelRequest";
         *   readonly responseType: "OpenStatusUpdate";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls OpenChannel.
         * @name lnrpc.Lightning#openChannel
         * @type {lnrpc.Lightning.OpenChannel}
         */
        $Object.defineProperties(Lightning.prototype.openChannel = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.openChannel, $root.lnrpc.OpenChannelRequest, $root.lnrpc.OpenStatusUpdate, request, callback);
        }, {
            name: { value: "OpenChannel" },
            path: { value: "/lnrpc.Lightning/OpenChannel" },
            requestType: { value: "OpenChannelRequest" },
            responseType: { value: "OpenStatusUpdate" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#batchOpenChannel}.
         * @memberof lnrpc.Lightning
         * @typedef BatchOpenChannelCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.BatchOpenChannelResponse} [response] BatchOpenChannelResponse
         */

        /**
         * Calls BatchOpenChannel.
         * @memberof lnrpc.Lightning
         * @typedef BatchOpenChannel
         * @type {{
         *   (request: lnrpc.IBatchOpenChannelRequest, callback: lnrpc.Lightning.BatchOpenChannelCallback): void;
         *   (request: lnrpc.IBatchOpenChannelRequest): Promise<lnrpc.BatchOpenChannelResponse>;
         *   readonly name: "BatchOpenChannel";
         *   readonly path: "/lnrpc.Lightning/BatchOpenChannel";
         *   readonly requestType: "BatchOpenChannelRequest";
         *   readonly responseType: "BatchOpenChannelResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls BatchOpenChannel.
         * @name lnrpc.Lightning#batchOpenChannel
         * @type {lnrpc.Lightning.BatchOpenChannel}
         */
        $Object.defineProperties(Lightning.prototype.batchOpenChannel = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.batchOpenChannel, $root.lnrpc.BatchOpenChannelRequest, $root.lnrpc.BatchOpenChannelResponse, request, callback);
        }, {
            name: { value: "BatchOpenChannel" },
            path: { value: "/lnrpc.Lightning/BatchOpenChannel" },
            requestType: { value: "BatchOpenChannelRequest" },
            responseType: { value: "BatchOpenChannelResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#fundingStateStep}.
         * @memberof lnrpc.Lightning
         * @typedef FundingStateStepCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.FundingStateStepResp} [response] FundingStateStepResp
         */

        /**
         * Calls FundingStateStep.
         * @memberof lnrpc.Lightning
         * @typedef FundingStateStep
         * @type {{
         *   (request: lnrpc.IFundingTransitionMsg, callback: lnrpc.Lightning.FundingStateStepCallback): void;
         *   (request: lnrpc.IFundingTransitionMsg): Promise<lnrpc.FundingStateStepResp>;
         *   readonly name: "FundingStateStep";
         *   readonly path: "/lnrpc.Lightning/FundingStateStep";
         *   readonly requestType: "FundingTransitionMsg";
         *   readonly responseType: "FundingStateStepResp";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls FundingStateStep.
         * @name lnrpc.Lightning#fundingStateStep
         * @type {lnrpc.Lightning.FundingStateStep}
         */
        $Object.defineProperties(Lightning.prototype.fundingStateStep = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.fundingStateStep, $root.lnrpc.FundingTransitionMsg, $root.lnrpc.FundingStateStepResp, request, callback);
        }, {
            name: { value: "FundingStateStep" },
            path: { value: "/lnrpc.Lightning/FundingStateStep" },
            requestType: { value: "FundingTransitionMsg" },
            responseType: { value: "FundingStateStepResp" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#channelAcceptor}.
         * @memberof lnrpc.Lightning
         * @typedef ChannelAcceptorCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChannelAcceptRequest} [response] ChannelAcceptRequest
         */

        /**
         * Calls ChannelAcceptor.
         * @memberof lnrpc.Lightning
         * @typedef ChannelAcceptor
         * @type {{
         *   (request: lnrpc.IChannelAcceptResponse, callback: lnrpc.Lightning.ChannelAcceptorCallback): void;
         *   (request: lnrpc.IChannelAcceptResponse): Promise<lnrpc.ChannelAcceptRequest>;
         *   readonly name: "ChannelAcceptor";
         *   readonly path: "/lnrpc.Lightning/ChannelAcceptor";
         *   readonly requestType: "ChannelAcceptResponse";
         *   readonly responseType: "ChannelAcceptRequest";
         *   readonly requestStream: true;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls ChannelAcceptor.
         * @name lnrpc.Lightning#channelAcceptor
         * @type {lnrpc.Lightning.ChannelAcceptor}
         */
        $Object.defineProperties(Lightning.prototype.channelAcceptor = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.channelAcceptor, $root.lnrpc.ChannelAcceptResponse, $root.lnrpc.ChannelAcceptRequest, request, callback);
        }, {
            name: { value: "ChannelAcceptor" },
            path: { value: "/lnrpc.Lightning/ChannelAcceptor" },
            requestType: { value: "ChannelAcceptResponse" },
            responseType: { value: "ChannelAcceptRequest" },
            requestStream: { value: true },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#closeChannel}.
         * @memberof lnrpc.Lightning
         * @typedef CloseChannelCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.CloseStatusUpdate} [response] CloseStatusUpdate
         */

        /**
         * Calls CloseChannel.
         * @memberof lnrpc.Lightning
         * @typedef CloseChannel
         * @type {{
         *   (request: lnrpc.ICloseChannelRequest, callback: lnrpc.Lightning.CloseChannelCallback): void;
         *   (request: lnrpc.ICloseChannelRequest): Promise<lnrpc.CloseStatusUpdate>;
         *   readonly name: "CloseChannel";
         *   readonly path: "/lnrpc.Lightning/CloseChannel";
         *   readonly requestType: "CloseChannelRequest";
         *   readonly responseType: "CloseStatusUpdate";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls CloseChannel.
         * @name lnrpc.Lightning#closeChannel
         * @type {lnrpc.Lightning.CloseChannel}
         */
        $Object.defineProperties(Lightning.prototype.closeChannel = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.closeChannel, $root.lnrpc.CloseChannelRequest, $root.lnrpc.CloseStatusUpdate, request, callback);
        }, {
            name: { value: "CloseChannel" },
            path: { value: "/lnrpc.Lightning/CloseChannel" },
            requestType: { value: "CloseChannelRequest" },
            responseType: { value: "CloseStatusUpdate" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#abandonChannel}.
         * @memberof lnrpc.Lightning
         * @typedef AbandonChannelCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.AbandonChannelResponse} [response] AbandonChannelResponse
         */

        /**
         * Calls AbandonChannel.
         * @memberof lnrpc.Lightning
         * @typedef AbandonChannel
         * @type {{
         *   (request: lnrpc.IAbandonChannelRequest, callback: lnrpc.Lightning.AbandonChannelCallback): void;
         *   (request: lnrpc.IAbandonChannelRequest): Promise<lnrpc.AbandonChannelResponse>;
         *   readonly name: "AbandonChannel";
         *   readonly path: "/lnrpc.Lightning/AbandonChannel";
         *   readonly requestType: "AbandonChannelRequest";
         *   readonly responseType: "AbandonChannelResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls AbandonChannel.
         * @name lnrpc.Lightning#abandonChannel
         * @type {lnrpc.Lightning.AbandonChannel}
         */
        $Object.defineProperties(Lightning.prototype.abandonChannel = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.abandonChannel, $root.lnrpc.AbandonChannelRequest, $root.lnrpc.AbandonChannelResponse, request, callback);
        }, {
            name: { value: "AbandonChannel" },
            path: { value: "/lnrpc.Lightning/AbandonChannel" },
            requestType: { value: "AbandonChannelRequest" },
            responseType: { value: "AbandonChannelResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#sendPayment}.
         * @memberof lnrpc.Lightning
         * @typedef SendPaymentCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.SendResponse} [response] SendResponse
         */

        /**
         * Calls SendPayment.
         * @memberof lnrpc.Lightning
         * @typedef SendPayment
         * @type {{
         *   (request: lnrpc.ISendRequest, callback: lnrpc.Lightning.SendPaymentCallback): void;
         *   (request: lnrpc.ISendRequest): Promise<lnrpc.SendResponse>;
         *   readonly name: "SendPayment";
         *   readonly path: "/lnrpc.Lightning/SendPayment";
         *   readonly requestType: "SendRequest";
         *   readonly responseType: "SendResponse";
         *   readonly requestStream: true;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SendPayment.
         * @name lnrpc.Lightning#sendPayment
         * @type {lnrpc.Lightning.SendPayment}
         */
        $Object.defineProperties(Lightning.prototype.sendPayment = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.sendPayment, $root.lnrpc.SendRequest, $root.lnrpc.SendResponse, request, callback);
        }, {
            name: { value: "SendPayment" },
            path: { value: "/lnrpc.Lightning/SendPayment" },
            requestType: { value: "SendRequest" },
            responseType: { value: "SendResponse" },
            requestStream: { value: true },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#sendPaymentSync}.
         * @memberof lnrpc.Lightning
         * @typedef SendPaymentSyncCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.SendResponse} [response] SendResponse
         */

        /**
         * Calls SendPaymentSync.
         * @memberof lnrpc.Lightning
         * @typedef SendPaymentSync
         * @type {{
         *   (request: lnrpc.ISendRequest, callback: lnrpc.Lightning.SendPaymentSyncCallback): void;
         *   (request: lnrpc.ISendRequest): Promise<lnrpc.SendResponse>;
         *   readonly name: "SendPaymentSync";
         *   readonly path: "/lnrpc.Lightning/SendPaymentSync";
         *   readonly requestType: "SendRequest";
         *   readonly responseType: "SendResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SendPaymentSync.
         * @name lnrpc.Lightning#sendPaymentSync
         * @type {lnrpc.Lightning.SendPaymentSync}
         */
        $Object.defineProperties(Lightning.prototype.sendPaymentSync = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.sendPaymentSync, $root.lnrpc.SendRequest, $root.lnrpc.SendResponse, request, callback);
        }, {
            name: { value: "SendPaymentSync" },
            path: { value: "/lnrpc.Lightning/SendPaymentSync" },
            requestType: { value: "SendRequest" },
            responseType: { value: "SendResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#sendToRoute}.
         * @memberof lnrpc.Lightning
         * @typedef SendToRouteCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.SendResponse} [response] SendResponse
         */

        /**
         * Calls SendToRoute.
         * @memberof lnrpc.Lightning
         * @typedef SendToRoute
         * @type {{
         *   (request: lnrpc.ISendToRouteRequest, callback: lnrpc.Lightning.SendToRouteCallback): void;
         *   (request: lnrpc.ISendToRouteRequest): Promise<lnrpc.SendResponse>;
         *   readonly name: "SendToRoute";
         *   readonly path: "/lnrpc.Lightning/SendToRoute";
         *   readonly requestType: "SendToRouteRequest";
         *   readonly responseType: "SendResponse";
         *   readonly requestStream: true;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SendToRoute.
         * @name lnrpc.Lightning#sendToRoute
         * @type {lnrpc.Lightning.SendToRoute}
         */
        $Object.defineProperties(Lightning.prototype.sendToRoute = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.sendToRoute, $root.lnrpc.SendToRouteRequest, $root.lnrpc.SendResponse, request, callback);
        }, {
            name: { value: "SendToRoute" },
            path: { value: "/lnrpc.Lightning/SendToRoute" },
            requestType: { value: "SendToRouteRequest" },
            responseType: { value: "SendResponse" },
            requestStream: { value: true },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#sendToRouteSync}.
         * @memberof lnrpc.Lightning
         * @typedef SendToRouteSyncCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.SendResponse} [response] SendResponse
         */

        /**
         * Calls SendToRouteSync.
         * @memberof lnrpc.Lightning
         * @typedef SendToRouteSync
         * @type {{
         *   (request: lnrpc.ISendToRouteRequest, callback: lnrpc.Lightning.SendToRouteSyncCallback): void;
         *   (request: lnrpc.ISendToRouteRequest): Promise<lnrpc.SendResponse>;
         *   readonly name: "SendToRouteSync";
         *   readonly path: "/lnrpc.Lightning/SendToRouteSync";
         *   readonly requestType: "SendToRouteRequest";
         *   readonly responseType: "SendResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SendToRouteSync.
         * @name lnrpc.Lightning#sendToRouteSync
         * @type {lnrpc.Lightning.SendToRouteSync}
         */
        $Object.defineProperties(Lightning.prototype.sendToRouteSync = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.sendToRouteSync, $root.lnrpc.SendToRouteRequest, $root.lnrpc.SendResponse, request, callback);
        }, {
            name: { value: "SendToRouteSync" },
            path: { value: "/lnrpc.Lightning/SendToRouteSync" },
            requestType: { value: "SendToRouteRequest" },
            responseType: { value: "SendResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#addInvoice}.
         * @memberof lnrpc.Lightning
         * @typedef AddInvoiceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.AddInvoiceResponse} [response] AddInvoiceResponse
         */

        /**
         * Calls AddInvoice.
         * @memberof lnrpc.Lightning
         * @typedef AddInvoice
         * @type {{
         *   (request: lnrpc.IInvoice, callback: lnrpc.Lightning.AddInvoiceCallback): void;
         *   (request: lnrpc.IInvoice): Promise<lnrpc.AddInvoiceResponse>;
         *   readonly name: "AddInvoice";
         *   readonly path: "/lnrpc.Lightning/AddInvoice";
         *   readonly requestType: "Invoice";
         *   readonly responseType: "AddInvoiceResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls AddInvoice.
         * @name lnrpc.Lightning#addInvoice
         * @type {lnrpc.Lightning.AddInvoice}
         */
        $Object.defineProperties(Lightning.prototype.addInvoice = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.addInvoice, $root.lnrpc.Invoice, $root.lnrpc.AddInvoiceResponse, request, callback);
        }, {
            name: { value: "AddInvoice" },
            path: { value: "/lnrpc.Lightning/AddInvoice" },
            requestType: { value: "Invoice" },
            responseType: { value: "AddInvoiceResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#listInvoices}.
         * @memberof lnrpc.Lightning
         * @typedef ListInvoicesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ListInvoiceResponse} [response] ListInvoiceResponse
         */

        /**
         * Calls ListInvoices.
         * @memberof lnrpc.Lightning
         * @typedef ListInvoices
         * @type {{
         *   (request: lnrpc.IListInvoiceRequest, callback: lnrpc.Lightning.ListInvoicesCallback): void;
         *   (request: lnrpc.IListInvoiceRequest): Promise<lnrpc.ListInvoiceResponse>;
         *   readonly name: "ListInvoices";
         *   readonly path: "/lnrpc.Lightning/ListInvoices";
         *   readonly requestType: "ListInvoiceRequest";
         *   readonly responseType: "ListInvoiceResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListInvoices.
         * @name lnrpc.Lightning#listInvoices
         * @type {lnrpc.Lightning.ListInvoices}
         */
        $Object.defineProperties(Lightning.prototype.listInvoices = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.listInvoices, $root.lnrpc.ListInvoiceRequest, $root.lnrpc.ListInvoiceResponse, request, callback);
        }, {
            name: { value: "ListInvoices" },
            path: { value: "/lnrpc.Lightning/ListInvoices" },
            requestType: { value: "ListInvoiceRequest" },
            responseType: { value: "ListInvoiceResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#lookupInvoice}.
         * @memberof lnrpc.Lightning
         * @typedef LookupInvoiceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.Invoice} [response] Invoice
         */

        /**
         * Calls LookupInvoice.
         * @memberof lnrpc.Lightning
         * @typedef LookupInvoice
         * @type {{
         *   (request: lnrpc.IPaymentHash, callback: lnrpc.Lightning.LookupInvoiceCallback): void;
         *   (request: lnrpc.IPaymentHash): Promise<lnrpc.Invoice>;
         *   readonly name: "LookupInvoice";
         *   readonly path: "/lnrpc.Lightning/LookupInvoice";
         *   readonly requestType: "PaymentHash";
         *   readonly responseType: "Invoice";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls LookupInvoice.
         * @name lnrpc.Lightning#lookupInvoice
         * @type {lnrpc.Lightning.LookupInvoice}
         */
        $Object.defineProperties(Lightning.prototype.lookupInvoice = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.lookupInvoice, $root.lnrpc.PaymentHash, $root.lnrpc.Invoice, request, callback);
        }, {
            name: { value: "LookupInvoice" },
            path: { value: "/lnrpc.Lightning/LookupInvoice" },
            requestType: { value: "PaymentHash" },
            responseType: { value: "Invoice" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#subscribeInvoices}.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeInvoicesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.Invoice} [response] Invoice
         */

        /**
         * Calls SubscribeInvoices.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeInvoices
         * @type {{
         *   (request: lnrpc.IInvoiceSubscription, callback: lnrpc.Lightning.SubscribeInvoicesCallback): void;
         *   (request: lnrpc.IInvoiceSubscription): Promise<lnrpc.Invoice>;
         *   readonly name: "SubscribeInvoices";
         *   readonly path: "/lnrpc.Lightning/SubscribeInvoices";
         *   readonly requestType: "InvoiceSubscription";
         *   readonly responseType: "Invoice";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribeInvoices.
         * @name lnrpc.Lightning#subscribeInvoices
         * @type {lnrpc.Lightning.SubscribeInvoices}
         */
        $Object.defineProperties(Lightning.prototype.subscribeInvoices = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.subscribeInvoices, $root.lnrpc.InvoiceSubscription, $root.lnrpc.Invoice, request, callback);
        }, {
            name: { value: "SubscribeInvoices" },
            path: { value: "/lnrpc.Lightning/SubscribeInvoices" },
            requestType: { value: "InvoiceSubscription" },
            responseType: { value: "Invoice" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#deleteCanceledInvoice}.
         * @memberof lnrpc.Lightning
         * @typedef DeleteCanceledInvoiceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.DelCanceledInvoiceResp} [response] DelCanceledInvoiceResp
         */

        /**
         * Calls DeleteCanceledInvoice.
         * @memberof lnrpc.Lightning
         * @typedef DeleteCanceledInvoice
         * @type {{
         *   (request: lnrpc.IDelCanceledInvoiceReq, callback: lnrpc.Lightning.DeleteCanceledInvoiceCallback): void;
         *   (request: lnrpc.IDelCanceledInvoiceReq): Promise<lnrpc.DelCanceledInvoiceResp>;
         *   readonly name: "DeleteCanceledInvoice";
         *   readonly path: "/lnrpc.Lightning/DeleteCanceledInvoice";
         *   readonly requestType: "DelCanceledInvoiceReq";
         *   readonly responseType: "DelCanceledInvoiceResp";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DeleteCanceledInvoice.
         * @name lnrpc.Lightning#deleteCanceledInvoice
         * @type {lnrpc.Lightning.DeleteCanceledInvoice}
         */
        $Object.defineProperties(Lightning.prototype.deleteCanceledInvoice = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.deleteCanceledInvoice, $root.lnrpc.DelCanceledInvoiceReq, $root.lnrpc.DelCanceledInvoiceResp, request, callback);
        }, {
            name: { value: "DeleteCanceledInvoice" },
            path: { value: "/lnrpc.Lightning/DeleteCanceledInvoice" },
            requestType: { value: "DelCanceledInvoiceReq" },
            responseType: { value: "DelCanceledInvoiceResp" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#decodePayReq}.
         * @memberof lnrpc.Lightning
         * @typedef DecodePayReqCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.PayReq} [response] PayReq
         */

        /**
         * Calls DecodePayReq.
         * @memberof lnrpc.Lightning
         * @typedef DecodePayReq
         * @type {{
         *   (request: lnrpc.IPayReqString, callback: lnrpc.Lightning.DecodePayReqCallback): void;
         *   (request: lnrpc.IPayReqString): Promise<lnrpc.PayReq>;
         *   readonly name: "DecodePayReq";
         *   readonly path: "/lnrpc.Lightning/DecodePayReq";
         *   readonly requestType: "PayReqString";
         *   readonly responseType: "PayReq";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DecodePayReq.
         * @name lnrpc.Lightning#decodePayReq
         * @type {lnrpc.Lightning.DecodePayReq}
         */
        $Object.defineProperties(Lightning.prototype.decodePayReq = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.decodePayReq, $root.lnrpc.PayReqString, $root.lnrpc.PayReq, request, callback);
        }, {
            name: { value: "DecodePayReq" },
            path: { value: "/lnrpc.Lightning/DecodePayReq" },
            requestType: { value: "PayReqString" },
            responseType: { value: "PayReq" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#listPayments}.
         * @memberof lnrpc.Lightning
         * @typedef ListPaymentsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ListPaymentsResponse} [response] ListPaymentsResponse
         */

        /**
         * Calls ListPayments.
         * @memberof lnrpc.Lightning
         * @typedef ListPayments
         * @type {{
         *   (request: lnrpc.IListPaymentsRequest, callback: lnrpc.Lightning.ListPaymentsCallback): void;
         *   (request: lnrpc.IListPaymentsRequest): Promise<lnrpc.ListPaymentsResponse>;
         *   readonly name: "ListPayments";
         *   readonly path: "/lnrpc.Lightning/ListPayments";
         *   readonly requestType: "ListPaymentsRequest";
         *   readonly responseType: "ListPaymentsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListPayments.
         * @name lnrpc.Lightning#listPayments
         * @type {lnrpc.Lightning.ListPayments}
         */
        $Object.defineProperties(Lightning.prototype.listPayments = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.listPayments, $root.lnrpc.ListPaymentsRequest, $root.lnrpc.ListPaymentsResponse, request, callback);
        }, {
            name: { value: "ListPayments" },
            path: { value: "/lnrpc.Lightning/ListPayments" },
            requestType: { value: "ListPaymentsRequest" },
            responseType: { value: "ListPaymentsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#deletePayment}.
         * @memberof lnrpc.Lightning
         * @typedef DeletePaymentCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.DeletePaymentResponse} [response] DeletePaymentResponse
         */

        /**
         * Calls DeletePayment.
         * @memberof lnrpc.Lightning
         * @typedef DeletePayment
         * @type {{
         *   (request: lnrpc.IDeletePaymentRequest, callback: lnrpc.Lightning.DeletePaymentCallback): void;
         *   (request: lnrpc.IDeletePaymentRequest): Promise<lnrpc.DeletePaymentResponse>;
         *   readonly name: "DeletePayment";
         *   readonly path: "/lnrpc.Lightning/DeletePayment";
         *   readonly requestType: "DeletePaymentRequest";
         *   readonly responseType: "DeletePaymentResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DeletePayment.
         * @name lnrpc.Lightning#deletePayment
         * @type {lnrpc.Lightning.DeletePayment}
         */
        $Object.defineProperties(Lightning.prototype.deletePayment = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.deletePayment, $root.lnrpc.DeletePaymentRequest, $root.lnrpc.DeletePaymentResponse, request, callback);
        }, {
            name: { value: "DeletePayment" },
            path: { value: "/lnrpc.Lightning/DeletePayment" },
            requestType: { value: "DeletePaymentRequest" },
            responseType: { value: "DeletePaymentResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#deleteAllPayments}.
         * @memberof lnrpc.Lightning
         * @typedef DeleteAllPaymentsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.DeleteAllPaymentsResponse} [response] DeleteAllPaymentsResponse
         */

        /**
         * Calls DeleteAllPayments.
         * @memberof lnrpc.Lightning
         * @typedef DeleteAllPayments
         * @type {{
         *   (request: lnrpc.IDeleteAllPaymentsRequest, callback: lnrpc.Lightning.DeleteAllPaymentsCallback): void;
         *   (request: lnrpc.IDeleteAllPaymentsRequest): Promise<lnrpc.DeleteAllPaymentsResponse>;
         *   readonly name: "DeleteAllPayments";
         *   readonly path: "/lnrpc.Lightning/DeleteAllPayments";
         *   readonly requestType: "DeleteAllPaymentsRequest";
         *   readonly responseType: "DeleteAllPaymentsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DeleteAllPayments.
         * @name lnrpc.Lightning#deleteAllPayments
         * @type {lnrpc.Lightning.DeleteAllPayments}
         */
        $Object.defineProperties(Lightning.prototype.deleteAllPayments = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.deleteAllPayments, $root.lnrpc.DeleteAllPaymentsRequest, $root.lnrpc.DeleteAllPaymentsResponse, request, callback);
        }, {
            name: { value: "DeleteAllPayments" },
            path: { value: "/lnrpc.Lightning/DeleteAllPayments" },
            requestType: { value: "DeleteAllPaymentsRequest" },
            responseType: { value: "DeleteAllPaymentsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#describeGraph}.
         * @memberof lnrpc.Lightning
         * @typedef DescribeGraphCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChannelGraph} [response] ChannelGraph
         */

        /**
         * Calls DescribeGraph.
         * @memberof lnrpc.Lightning
         * @typedef DescribeGraph
         * @type {{
         *   (request: lnrpc.IChannelGraphRequest, callback: lnrpc.Lightning.DescribeGraphCallback): void;
         *   (request: lnrpc.IChannelGraphRequest): Promise<lnrpc.ChannelGraph>;
         *   readonly name: "DescribeGraph";
         *   readonly path: "/lnrpc.Lightning/DescribeGraph";
         *   readonly requestType: "ChannelGraphRequest";
         *   readonly responseType: "ChannelGraph";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DescribeGraph.
         * @name lnrpc.Lightning#describeGraph
         * @type {lnrpc.Lightning.DescribeGraph}
         */
        $Object.defineProperties(Lightning.prototype.describeGraph = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.describeGraph, $root.lnrpc.ChannelGraphRequest, $root.lnrpc.ChannelGraph, request, callback);
        }, {
            name: { value: "DescribeGraph" },
            path: { value: "/lnrpc.Lightning/DescribeGraph" },
            requestType: { value: "ChannelGraphRequest" },
            responseType: { value: "ChannelGraph" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#getNodeMetrics}.
         * @memberof lnrpc.Lightning
         * @typedef GetNodeMetricsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.NodeMetricsResponse} [response] NodeMetricsResponse
         */

        /**
         * Calls GetNodeMetrics.
         * @memberof lnrpc.Lightning
         * @typedef GetNodeMetrics
         * @type {{
         *   (request: lnrpc.INodeMetricsRequest, callback: lnrpc.Lightning.GetNodeMetricsCallback): void;
         *   (request: lnrpc.INodeMetricsRequest): Promise<lnrpc.NodeMetricsResponse>;
         *   readonly name: "GetNodeMetrics";
         *   readonly path: "/lnrpc.Lightning/GetNodeMetrics";
         *   readonly requestType: "NodeMetricsRequest";
         *   readonly responseType: "NodeMetricsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetNodeMetrics.
         * @name lnrpc.Lightning#getNodeMetrics
         * @type {lnrpc.Lightning.GetNodeMetrics}
         */
        $Object.defineProperties(Lightning.prototype.getNodeMetrics = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.getNodeMetrics, $root.lnrpc.NodeMetricsRequest, $root.lnrpc.NodeMetricsResponse, request, callback);
        }, {
            name: { value: "GetNodeMetrics" },
            path: { value: "/lnrpc.Lightning/GetNodeMetrics" },
            requestType: { value: "NodeMetricsRequest" },
            responseType: { value: "NodeMetricsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#getChanInfo}.
         * @memberof lnrpc.Lightning
         * @typedef GetChanInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChannelEdge} [response] ChannelEdge
         */

        /**
         * Calls GetChanInfo.
         * @memberof lnrpc.Lightning
         * @typedef GetChanInfo
         * @type {{
         *   (request: lnrpc.IChanInfoRequest, callback: lnrpc.Lightning.GetChanInfoCallback): void;
         *   (request: lnrpc.IChanInfoRequest): Promise<lnrpc.ChannelEdge>;
         *   readonly name: "GetChanInfo";
         *   readonly path: "/lnrpc.Lightning/GetChanInfo";
         *   readonly requestType: "ChanInfoRequest";
         *   readonly responseType: "ChannelEdge";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetChanInfo.
         * @name lnrpc.Lightning#getChanInfo
         * @type {lnrpc.Lightning.GetChanInfo}
         */
        $Object.defineProperties(Lightning.prototype.getChanInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.getChanInfo, $root.lnrpc.ChanInfoRequest, $root.lnrpc.ChannelEdge, request, callback);
        }, {
            name: { value: "GetChanInfo" },
            path: { value: "/lnrpc.Lightning/GetChanInfo" },
            requestType: { value: "ChanInfoRequest" },
            responseType: { value: "ChannelEdge" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#getNodeInfo}.
         * @memberof lnrpc.Lightning
         * @typedef GetNodeInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.NodeInfo} [response] NodeInfo
         */

        /**
         * Calls GetNodeInfo.
         * @memberof lnrpc.Lightning
         * @typedef GetNodeInfo
         * @type {{
         *   (request: lnrpc.INodeInfoRequest, callback: lnrpc.Lightning.GetNodeInfoCallback): void;
         *   (request: lnrpc.INodeInfoRequest): Promise<lnrpc.NodeInfo>;
         *   readonly name: "GetNodeInfo";
         *   readonly path: "/lnrpc.Lightning/GetNodeInfo";
         *   readonly requestType: "NodeInfoRequest";
         *   readonly responseType: "NodeInfo";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetNodeInfo.
         * @name lnrpc.Lightning#getNodeInfo
         * @type {lnrpc.Lightning.GetNodeInfo}
         */
        $Object.defineProperties(Lightning.prototype.getNodeInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.getNodeInfo, $root.lnrpc.NodeInfoRequest, $root.lnrpc.NodeInfo, request, callback);
        }, {
            name: { value: "GetNodeInfo" },
            path: { value: "/lnrpc.Lightning/GetNodeInfo" },
            requestType: { value: "NodeInfoRequest" },
            responseType: { value: "NodeInfo" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#queryRoutes}.
         * @memberof lnrpc.Lightning
         * @typedef QueryRoutesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.QueryRoutesResponse} [response] QueryRoutesResponse
         */

        /**
         * Calls QueryRoutes.
         * @memberof lnrpc.Lightning
         * @typedef QueryRoutes
         * @type {{
         *   (request: lnrpc.IQueryRoutesRequest, callback: lnrpc.Lightning.QueryRoutesCallback): void;
         *   (request: lnrpc.IQueryRoutesRequest): Promise<lnrpc.QueryRoutesResponse>;
         *   readonly name: "QueryRoutes";
         *   readonly path: "/lnrpc.Lightning/QueryRoutes";
         *   readonly requestType: "QueryRoutesRequest";
         *   readonly responseType: "QueryRoutesResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls QueryRoutes.
         * @name lnrpc.Lightning#queryRoutes
         * @type {lnrpc.Lightning.QueryRoutes}
         */
        $Object.defineProperties(Lightning.prototype.queryRoutes = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.queryRoutes, $root.lnrpc.QueryRoutesRequest, $root.lnrpc.QueryRoutesResponse, request, callback);
        }, {
            name: { value: "QueryRoutes" },
            path: { value: "/lnrpc.Lightning/QueryRoutes" },
            requestType: { value: "QueryRoutesRequest" },
            responseType: { value: "QueryRoutesResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#getNetworkInfo}.
         * @memberof lnrpc.Lightning
         * @typedef GetNetworkInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.NetworkInfo} [response] NetworkInfo
         */

        /**
         * Calls GetNetworkInfo.
         * @memberof lnrpc.Lightning
         * @typedef GetNetworkInfo
         * @type {{
         *   (request: lnrpc.INetworkInfoRequest, callback: lnrpc.Lightning.GetNetworkInfoCallback): void;
         *   (request: lnrpc.INetworkInfoRequest): Promise<lnrpc.NetworkInfo>;
         *   readonly name: "GetNetworkInfo";
         *   readonly path: "/lnrpc.Lightning/GetNetworkInfo";
         *   readonly requestType: "NetworkInfoRequest";
         *   readonly responseType: "NetworkInfo";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetNetworkInfo.
         * @name lnrpc.Lightning#getNetworkInfo
         * @type {lnrpc.Lightning.GetNetworkInfo}
         */
        $Object.defineProperties(Lightning.prototype.getNetworkInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.getNetworkInfo, $root.lnrpc.NetworkInfoRequest, $root.lnrpc.NetworkInfo, request, callback);
        }, {
            name: { value: "GetNetworkInfo" },
            path: { value: "/lnrpc.Lightning/GetNetworkInfo" },
            requestType: { value: "NetworkInfoRequest" },
            responseType: { value: "NetworkInfo" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#stopDaemon}.
         * @memberof lnrpc.Lightning
         * @typedef StopDaemonCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.StopResponse} [response] StopResponse
         */

        /**
         * Calls StopDaemon.
         * @memberof lnrpc.Lightning
         * @typedef StopDaemon
         * @type {{
         *   (request: lnrpc.IStopRequest, callback: lnrpc.Lightning.StopDaemonCallback): void;
         *   (request: lnrpc.IStopRequest): Promise<lnrpc.StopResponse>;
         *   readonly name: "StopDaemon";
         *   readonly path: "/lnrpc.Lightning/StopDaemon";
         *   readonly requestType: "StopRequest";
         *   readonly responseType: "StopResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls StopDaemon.
         * @name lnrpc.Lightning#stopDaemon
         * @type {lnrpc.Lightning.StopDaemon}
         */
        $Object.defineProperties(Lightning.prototype.stopDaemon = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.stopDaemon, $root.lnrpc.StopRequest, $root.lnrpc.StopResponse, request, callback);
        }, {
            name: { value: "StopDaemon" },
            path: { value: "/lnrpc.Lightning/StopDaemon" },
            requestType: { value: "StopRequest" },
            responseType: { value: "StopResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#subscribeChannelGraph}.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeChannelGraphCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.GraphTopologyUpdate} [response] GraphTopologyUpdate
         */

        /**
         * Calls SubscribeChannelGraph.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeChannelGraph
         * @type {{
         *   (request: lnrpc.IGraphTopologySubscription, callback: lnrpc.Lightning.SubscribeChannelGraphCallback): void;
         *   (request: lnrpc.IGraphTopologySubscription): Promise<lnrpc.GraphTopologyUpdate>;
         *   readonly name: "SubscribeChannelGraph";
         *   readonly path: "/lnrpc.Lightning/SubscribeChannelGraph";
         *   readonly requestType: "GraphTopologySubscription";
         *   readonly responseType: "GraphTopologyUpdate";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribeChannelGraph.
         * @name lnrpc.Lightning#subscribeChannelGraph
         * @type {lnrpc.Lightning.SubscribeChannelGraph}
         */
        $Object.defineProperties(Lightning.prototype.subscribeChannelGraph = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.subscribeChannelGraph, $root.lnrpc.GraphTopologySubscription, $root.lnrpc.GraphTopologyUpdate, request, callback);
        }, {
            name: { value: "SubscribeChannelGraph" },
            path: { value: "/lnrpc.Lightning/SubscribeChannelGraph" },
            requestType: { value: "GraphTopologySubscription" },
            responseType: { value: "GraphTopologyUpdate" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#debugLevel}.
         * @memberof lnrpc.Lightning
         * @typedef DebugLevelCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.DebugLevelResponse} [response] DebugLevelResponse
         */

        /**
         * Calls DebugLevel.
         * @memberof lnrpc.Lightning
         * @typedef DebugLevel
         * @type {{
         *   (request: lnrpc.IDebugLevelRequest, callback: lnrpc.Lightning.DebugLevelCallback): void;
         *   (request: lnrpc.IDebugLevelRequest): Promise<lnrpc.DebugLevelResponse>;
         *   readonly name: "DebugLevel";
         *   readonly path: "/lnrpc.Lightning/DebugLevel";
         *   readonly requestType: "DebugLevelRequest";
         *   readonly responseType: "DebugLevelResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DebugLevel.
         * @name lnrpc.Lightning#debugLevel
         * @type {lnrpc.Lightning.DebugLevel}
         */
        $Object.defineProperties(Lightning.prototype.debugLevel = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.debugLevel, $root.lnrpc.DebugLevelRequest, $root.lnrpc.DebugLevelResponse, request, callback);
        }, {
            name: { value: "DebugLevel" },
            path: { value: "/lnrpc.Lightning/DebugLevel" },
            requestType: { value: "DebugLevelRequest" },
            responseType: { value: "DebugLevelResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#feeReport}.
         * @memberof lnrpc.Lightning
         * @typedef FeeReportCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.FeeReportResponse} [response] FeeReportResponse
         */

        /**
         * Calls FeeReport.
         * @memberof lnrpc.Lightning
         * @typedef FeeReport
         * @type {{
         *   (request: lnrpc.IFeeReportRequest, callback: lnrpc.Lightning.FeeReportCallback): void;
         *   (request: lnrpc.IFeeReportRequest): Promise<lnrpc.FeeReportResponse>;
         *   readonly name: "FeeReport";
         *   readonly path: "/lnrpc.Lightning/FeeReport";
         *   readonly requestType: "FeeReportRequest";
         *   readonly responseType: "FeeReportResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls FeeReport.
         * @name lnrpc.Lightning#feeReport
         * @type {lnrpc.Lightning.FeeReport}
         */
        $Object.defineProperties(Lightning.prototype.feeReport = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.feeReport, $root.lnrpc.FeeReportRequest, $root.lnrpc.FeeReportResponse, request, callback);
        }, {
            name: { value: "FeeReport" },
            path: { value: "/lnrpc.Lightning/FeeReport" },
            requestType: { value: "FeeReportRequest" },
            responseType: { value: "FeeReportResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#updateChannelPolicy}.
         * @memberof lnrpc.Lightning
         * @typedef UpdateChannelPolicyCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.PolicyUpdateResponse} [response] PolicyUpdateResponse
         */

        /**
         * Calls UpdateChannelPolicy.
         * @memberof lnrpc.Lightning
         * @typedef UpdateChannelPolicy
         * @type {{
         *   (request: lnrpc.IPolicyUpdateRequest, callback: lnrpc.Lightning.UpdateChannelPolicyCallback): void;
         *   (request: lnrpc.IPolicyUpdateRequest): Promise<lnrpc.PolicyUpdateResponse>;
         *   readonly name: "UpdateChannelPolicy";
         *   readonly path: "/lnrpc.Lightning/UpdateChannelPolicy";
         *   readonly requestType: "PolicyUpdateRequest";
         *   readonly responseType: "PolicyUpdateResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls UpdateChannelPolicy.
         * @name lnrpc.Lightning#updateChannelPolicy
         * @type {lnrpc.Lightning.UpdateChannelPolicy}
         */
        $Object.defineProperties(Lightning.prototype.updateChannelPolicy = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.updateChannelPolicy, $root.lnrpc.PolicyUpdateRequest, $root.lnrpc.PolicyUpdateResponse, request, callback);
        }, {
            name: { value: "UpdateChannelPolicy" },
            path: { value: "/lnrpc.Lightning/UpdateChannelPolicy" },
            requestType: { value: "PolicyUpdateRequest" },
            responseType: { value: "PolicyUpdateResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#forwardingHistory}.
         * @memberof lnrpc.Lightning
         * @typedef ForwardingHistoryCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ForwardingHistoryResponse} [response] ForwardingHistoryResponse
         */

        /**
         * Calls ForwardingHistory.
         * @memberof lnrpc.Lightning
         * @typedef ForwardingHistory
         * @type {{
         *   (request: lnrpc.IForwardingHistoryRequest, callback: lnrpc.Lightning.ForwardingHistoryCallback): void;
         *   (request: lnrpc.IForwardingHistoryRequest): Promise<lnrpc.ForwardingHistoryResponse>;
         *   readonly name: "ForwardingHistory";
         *   readonly path: "/lnrpc.Lightning/ForwardingHistory";
         *   readonly requestType: "ForwardingHistoryRequest";
         *   readonly responseType: "ForwardingHistoryResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ForwardingHistory.
         * @name lnrpc.Lightning#forwardingHistory
         * @type {lnrpc.Lightning.ForwardingHistory}
         */
        $Object.defineProperties(Lightning.prototype.forwardingHistory = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.forwardingHistory, $root.lnrpc.ForwardingHistoryRequest, $root.lnrpc.ForwardingHistoryResponse, request, callback);
        }, {
            name: { value: "ForwardingHistory" },
            path: { value: "/lnrpc.Lightning/ForwardingHistory" },
            requestType: { value: "ForwardingHistoryRequest" },
            responseType: { value: "ForwardingHistoryResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#exportChannelBackup}.
         * @memberof lnrpc.Lightning
         * @typedef ExportChannelBackupCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChannelBackup} [response] ChannelBackup
         */

        /**
         * Calls ExportChannelBackup.
         * @memberof lnrpc.Lightning
         * @typedef ExportChannelBackup
         * @type {{
         *   (request: lnrpc.IExportChannelBackupRequest, callback: lnrpc.Lightning.ExportChannelBackupCallback): void;
         *   (request: lnrpc.IExportChannelBackupRequest): Promise<lnrpc.ChannelBackup>;
         *   readonly name: "ExportChannelBackup";
         *   readonly path: "/lnrpc.Lightning/ExportChannelBackup";
         *   readonly requestType: "ExportChannelBackupRequest";
         *   readonly responseType: "ChannelBackup";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ExportChannelBackup.
         * @name lnrpc.Lightning#exportChannelBackup
         * @type {lnrpc.Lightning.ExportChannelBackup}
         */
        $Object.defineProperties(Lightning.prototype.exportChannelBackup = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.exportChannelBackup, $root.lnrpc.ExportChannelBackupRequest, $root.lnrpc.ChannelBackup, request, callback);
        }, {
            name: { value: "ExportChannelBackup" },
            path: { value: "/lnrpc.Lightning/ExportChannelBackup" },
            requestType: { value: "ExportChannelBackupRequest" },
            responseType: { value: "ChannelBackup" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#exportAllChannelBackups}.
         * @memberof lnrpc.Lightning
         * @typedef ExportAllChannelBackupsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChanBackupSnapshot} [response] ChanBackupSnapshot
         */

        /**
         * Calls ExportAllChannelBackups.
         * @memberof lnrpc.Lightning
         * @typedef ExportAllChannelBackups
         * @type {{
         *   (request: lnrpc.IChanBackupExportRequest, callback: lnrpc.Lightning.ExportAllChannelBackupsCallback): void;
         *   (request: lnrpc.IChanBackupExportRequest): Promise<lnrpc.ChanBackupSnapshot>;
         *   readonly name: "ExportAllChannelBackups";
         *   readonly path: "/lnrpc.Lightning/ExportAllChannelBackups";
         *   readonly requestType: "ChanBackupExportRequest";
         *   readonly responseType: "ChanBackupSnapshot";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ExportAllChannelBackups.
         * @name lnrpc.Lightning#exportAllChannelBackups
         * @type {lnrpc.Lightning.ExportAllChannelBackups}
         */
        $Object.defineProperties(Lightning.prototype.exportAllChannelBackups = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.exportAllChannelBackups, $root.lnrpc.ChanBackupExportRequest, $root.lnrpc.ChanBackupSnapshot, request, callback);
        }, {
            name: { value: "ExportAllChannelBackups" },
            path: { value: "/lnrpc.Lightning/ExportAllChannelBackups" },
            requestType: { value: "ChanBackupExportRequest" },
            responseType: { value: "ChanBackupSnapshot" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#verifyChanBackup}.
         * @memberof lnrpc.Lightning
         * @typedef VerifyChanBackupCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.VerifyChanBackupResponse} [response] VerifyChanBackupResponse
         */

        /**
         * Calls VerifyChanBackup.
         * @memberof lnrpc.Lightning
         * @typedef VerifyChanBackup
         * @type {{
         *   (request: lnrpc.IChanBackupSnapshot, callback: lnrpc.Lightning.VerifyChanBackupCallback): void;
         *   (request: lnrpc.IChanBackupSnapshot): Promise<lnrpc.VerifyChanBackupResponse>;
         *   readonly name: "VerifyChanBackup";
         *   readonly path: "/lnrpc.Lightning/VerifyChanBackup";
         *   readonly requestType: "ChanBackupSnapshot";
         *   readonly responseType: "VerifyChanBackupResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls VerifyChanBackup.
         * @name lnrpc.Lightning#verifyChanBackup
         * @type {lnrpc.Lightning.VerifyChanBackup}
         */
        $Object.defineProperties(Lightning.prototype.verifyChanBackup = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.verifyChanBackup, $root.lnrpc.ChanBackupSnapshot, $root.lnrpc.VerifyChanBackupResponse, request, callback);
        }, {
            name: { value: "VerifyChanBackup" },
            path: { value: "/lnrpc.Lightning/VerifyChanBackup" },
            requestType: { value: "ChanBackupSnapshot" },
            responseType: { value: "VerifyChanBackupResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#restoreChannelBackups}.
         * @memberof lnrpc.Lightning
         * @typedef RestoreChannelBackupsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.RestoreBackupResponse} [response] RestoreBackupResponse
         */

        /**
         * Calls RestoreChannelBackups.
         * @memberof lnrpc.Lightning
         * @typedef RestoreChannelBackups
         * @type {{
         *   (request: lnrpc.IRestoreChanBackupRequest, callback: lnrpc.Lightning.RestoreChannelBackupsCallback): void;
         *   (request: lnrpc.IRestoreChanBackupRequest): Promise<lnrpc.RestoreBackupResponse>;
         *   readonly name: "RestoreChannelBackups";
         *   readonly path: "/lnrpc.Lightning/RestoreChannelBackups";
         *   readonly requestType: "RestoreChanBackupRequest";
         *   readonly responseType: "RestoreBackupResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls RestoreChannelBackups.
         * @name lnrpc.Lightning#restoreChannelBackups
         * @type {lnrpc.Lightning.RestoreChannelBackups}
         */
        $Object.defineProperties(Lightning.prototype.restoreChannelBackups = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.restoreChannelBackups, $root.lnrpc.RestoreChanBackupRequest, $root.lnrpc.RestoreBackupResponse, request, callback);
        }, {
            name: { value: "RestoreChannelBackups" },
            path: { value: "/lnrpc.Lightning/RestoreChannelBackups" },
            requestType: { value: "RestoreChanBackupRequest" },
            responseType: { value: "RestoreBackupResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#subscribeChannelBackups}.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeChannelBackupsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChanBackupSnapshot} [response] ChanBackupSnapshot
         */

        /**
         * Calls SubscribeChannelBackups.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeChannelBackups
         * @type {{
         *   (request: lnrpc.IChannelBackupSubscription, callback: lnrpc.Lightning.SubscribeChannelBackupsCallback): void;
         *   (request: lnrpc.IChannelBackupSubscription): Promise<lnrpc.ChanBackupSnapshot>;
         *   readonly name: "SubscribeChannelBackups";
         *   readonly path: "/lnrpc.Lightning/SubscribeChannelBackups";
         *   readonly requestType: "ChannelBackupSubscription";
         *   readonly responseType: "ChanBackupSnapshot";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribeChannelBackups.
         * @name lnrpc.Lightning#subscribeChannelBackups
         * @type {lnrpc.Lightning.SubscribeChannelBackups}
         */
        $Object.defineProperties(Lightning.prototype.subscribeChannelBackups = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.subscribeChannelBackups, $root.lnrpc.ChannelBackupSubscription, $root.lnrpc.ChanBackupSnapshot, request, callback);
        }, {
            name: { value: "SubscribeChannelBackups" },
            path: { value: "/lnrpc.Lightning/SubscribeChannelBackups" },
            requestType: { value: "ChannelBackupSubscription" },
            responseType: { value: "ChanBackupSnapshot" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#bakeMacaroon}.
         * @memberof lnrpc.Lightning
         * @typedef BakeMacaroonCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.BakeMacaroonResponse} [response] BakeMacaroonResponse
         */

        /**
         * Calls BakeMacaroon.
         * @memberof lnrpc.Lightning
         * @typedef BakeMacaroon
         * @type {{
         *   (request: lnrpc.IBakeMacaroonRequest, callback: lnrpc.Lightning.BakeMacaroonCallback): void;
         *   (request: lnrpc.IBakeMacaroonRequest): Promise<lnrpc.BakeMacaroonResponse>;
         *   readonly name: "BakeMacaroon";
         *   readonly path: "/lnrpc.Lightning/BakeMacaroon";
         *   readonly requestType: "BakeMacaroonRequest";
         *   readonly responseType: "BakeMacaroonResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls BakeMacaroon.
         * @name lnrpc.Lightning#bakeMacaroon
         * @type {lnrpc.Lightning.BakeMacaroon}
         */
        $Object.defineProperties(Lightning.prototype.bakeMacaroon = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.bakeMacaroon, $root.lnrpc.BakeMacaroonRequest, $root.lnrpc.BakeMacaroonResponse, request, callback);
        }, {
            name: { value: "BakeMacaroon" },
            path: { value: "/lnrpc.Lightning/BakeMacaroon" },
            requestType: { value: "BakeMacaroonRequest" },
            responseType: { value: "BakeMacaroonResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#listMacaroonIDs}.
         * @memberof lnrpc.Lightning
         * @typedef ListMacaroonIDsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ListMacaroonIDsResponse} [response] ListMacaroonIDsResponse
         */

        /**
         * Calls ListMacaroonIDs.
         * @memberof lnrpc.Lightning
         * @typedef ListMacaroonIDs
         * @type {{
         *   (request: lnrpc.IListMacaroonIDsRequest, callback: lnrpc.Lightning.ListMacaroonIDsCallback): void;
         *   (request: lnrpc.IListMacaroonIDsRequest): Promise<lnrpc.ListMacaroonIDsResponse>;
         *   readonly name: "ListMacaroonIDs";
         *   readonly path: "/lnrpc.Lightning/ListMacaroonIDs";
         *   readonly requestType: "ListMacaroonIDsRequest";
         *   readonly responseType: "ListMacaroonIDsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListMacaroonIDs.
         * @name lnrpc.Lightning#listMacaroonIDs
         * @type {lnrpc.Lightning.ListMacaroonIDs}
         */
        $Object.defineProperties(Lightning.prototype.listMacaroonIDs = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.listMacaroonIDs, $root.lnrpc.ListMacaroonIDsRequest, $root.lnrpc.ListMacaroonIDsResponse, request, callback);
        }, {
            name: { value: "ListMacaroonIDs" },
            path: { value: "/lnrpc.Lightning/ListMacaroonIDs" },
            requestType: { value: "ListMacaroonIDsRequest" },
            responseType: { value: "ListMacaroonIDsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#deleteMacaroonID}.
         * @memberof lnrpc.Lightning
         * @typedef DeleteMacaroonIDCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.DeleteMacaroonIDResponse} [response] DeleteMacaroonIDResponse
         */

        /**
         * Calls DeleteMacaroonID.
         * @memberof lnrpc.Lightning
         * @typedef DeleteMacaroonID
         * @type {{
         *   (request: lnrpc.IDeleteMacaroonIDRequest, callback: lnrpc.Lightning.DeleteMacaroonIDCallback): void;
         *   (request: lnrpc.IDeleteMacaroonIDRequest): Promise<lnrpc.DeleteMacaroonIDResponse>;
         *   readonly name: "DeleteMacaroonID";
         *   readonly path: "/lnrpc.Lightning/DeleteMacaroonID";
         *   readonly requestType: "DeleteMacaroonIDRequest";
         *   readonly responseType: "DeleteMacaroonIDResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DeleteMacaroonID.
         * @name lnrpc.Lightning#deleteMacaroonID
         * @type {lnrpc.Lightning.DeleteMacaroonID}
         */
        $Object.defineProperties(Lightning.prototype.deleteMacaroonID = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.deleteMacaroonID, $root.lnrpc.DeleteMacaroonIDRequest, $root.lnrpc.DeleteMacaroonIDResponse, request, callback);
        }, {
            name: { value: "DeleteMacaroonID" },
            path: { value: "/lnrpc.Lightning/DeleteMacaroonID" },
            requestType: { value: "DeleteMacaroonIDRequest" },
            responseType: { value: "DeleteMacaroonIDResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#listPermissions}.
         * @memberof lnrpc.Lightning
         * @typedef ListPermissionsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ListPermissionsResponse} [response] ListPermissionsResponse
         */

        /**
         * Calls ListPermissions.
         * @memberof lnrpc.Lightning
         * @typedef ListPermissions
         * @type {{
         *   (request: lnrpc.IListPermissionsRequest, callback: lnrpc.Lightning.ListPermissionsCallback): void;
         *   (request: lnrpc.IListPermissionsRequest): Promise<lnrpc.ListPermissionsResponse>;
         *   readonly name: "ListPermissions";
         *   readonly path: "/lnrpc.Lightning/ListPermissions";
         *   readonly requestType: "ListPermissionsRequest";
         *   readonly responseType: "ListPermissionsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListPermissions.
         * @name lnrpc.Lightning#listPermissions
         * @type {lnrpc.Lightning.ListPermissions}
         */
        $Object.defineProperties(Lightning.prototype.listPermissions = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.listPermissions, $root.lnrpc.ListPermissionsRequest, $root.lnrpc.ListPermissionsResponse, request, callback);
        }, {
            name: { value: "ListPermissions" },
            path: { value: "/lnrpc.Lightning/ListPermissions" },
            requestType: { value: "ListPermissionsRequest" },
            responseType: { value: "ListPermissionsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#checkMacaroonPermissions}.
         * @memberof lnrpc.Lightning
         * @typedef CheckMacaroonPermissionsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.CheckMacPermResponse} [response] CheckMacPermResponse
         */

        /**
         * Calls CheckMacaroonPermissions.
         * @memberof lnrpc.Lightning
         * @typedef CheckMacaroonPermissions
         * @type {{
         *   (request: lnrpc.ICheckMacPermRequest, callback: lnrpc.Lightning.CheckMacaroonPermissionsCallback): void;
         *   (request: lnrpc.ICheckMacPermRequest): Promise<lnrpc.CheckMacPermResponse>;
         *   readonly name: "CheckMacaroonPermissions";
         *   readonly path: "/lnrpc.Lightning/CheckMacaroonPermissions";
         *   readonly requestType: "CheckMacPermRequest";
         *   readonly responseType: "CheckMacPermResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls CheckMacaroonPermissions.
         * @name lnrpc.Lightning#checkMacaroonPermissions
         * @type {lnrpc.Lightning.CheckMacaroonPermissions}
         */
        $Object.defineProperties(Lightning.prototype.checkMacaroonPermissions = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.checkMacaroonPermissions, $root.lnrpc.CheckMacPermRequest, $root.lnrpc.CheckMacPermResponse, request, callback);
        }, {
            name: { value: "CheckMacaroonPermissions" },
            path: { value: "/lnrpc.Lightning/CheckMacaroonPermissions" },
            requestType: { value: "CheckMacPermRequest" },
            responseType: { value: "CheckMacPermResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#registerRPCMiddleware}.
         * @memberof lnrpc.Lightning
         * @typedef RegisterRPCMiddlewareCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.RPCMiddlewareRequest} [response] RPCMiddlewareRequest
         */

        /**
         * Calls RegisterRPCMiddleware.
         * @memberof lnrpc.Lightning
         * @typedef RegisterRPCMiddleware
         * @type {{
         *   (request: lnrpc.IRPCMiddlewareResponse, callback: lnrpc.Lightning.RegisterRPCMiddlewareCallback): void;
         *   (request: lnrpc.IRPCMiddlewareResponse): Promise<lnrpc.RPCMiddlewareRequest>;
         *   readonly name: "RegisterRPCMiddleware";
         *   readonly path: "/lnrpc.Lightning/RegisterRPCMiddleware";
         *   readonly requestType: "RPCMiddlewareResponse";
         *   readonly responseType: "RPCMiddlewareRequest";
         *   readonly requestStream: true;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls RegisterRPCMiddleware.
         * @name lnrpc.Lightning#registerRPCMiddleware
         * @type {lnrpc.Lightning.RegisterRPCMiddleware}
         */
        $Object.defineProperties(Lightning.prototype.registerRPCMiddleware = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.registerRPCMiddleware, $root.lnrpc.RPCMiddlewareResponse, $root.lnrpc.RPCMiddlewareRequest, request, callback);
        }, {
            name: { value: "RegisterRPCMiddleware" },
            path: { value: "/lnrpc.Lightning/RegisterRPCMiddleware" },
            requestType: { value: "RPCMiddlewareResponse" },
            responseType: { value: "RPCMiddlewareRequest" },
            requestStream: { value: true },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#sendCustomMessage}.
         * @memberof lnrpc.Lightning
         * @typedef SendCustomMessageCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.SendCustomMessageResponse} [response] SendCustomMessageResponse
         */

        /**
         * Calls SendCustomMessage.
         * @memberof lnrpc.Lightning
         * @typedef SendCustomMessage
         * @type {{
         *   (request: lnrpc.ISendCustomMessageRequest, callback: lnrpc.Lightning.SendCustomMessageCallback): void;
         *   (request: lnrpc.ISendCustomMessageRequest): Promise<lnrpc.SendCustomMessageResponse>;
         *   readonly name: "SendCustomMessage";
         *   readonly path: "/lnrpc.Lightning/SendCustomMessage";
         *   readonly requestType: "SendCustomMessageRequest";
         *   readonly responseType: "SendCustomMessageResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SendCustomMessage.
         * @name lnrpc.Lightning#sendCustomMessage
         * @type {lnrpc.Lightning.SendCustomMessage}
         */
        $Object.defineProperties(Lightning.prototype.sendCustomMessage = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.sendCustomMessage, $root.lnrpc.SendCustomMessageRequest, $root.lnrpc.SendCustomMessageResponse, request, callback);
        }, {
            name: { value: "SendCustomMessage" },
            path: { value: "/lnrpc.Lightning/SendCustomMessage" },
            requestType: { value: "SendCustomMessageRequest" },
            responseType: { value: "SendCustomMessageResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#subscribeCustomMessages}.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeCustomMessagesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.CustomMessage} [response] CustomMessage
         */

        /**
         * Calls SubscribeCustomMessages.
         * @memberof lnrpc.Lightning
         * @typedef SubscribeCustomMessages
         * @type {{
         *   (request: lnrpc.ISubscribeCustomMessagesRequest, callback: lnrpc.Lightning.SubscribeCustomMessagesCallback): void;
         *   (request: lnrpc.ISubscribeCustomMessagesRequest): Promise<lnrpc.CustomMessage>;
         *   readonly name: "SubscribeCustomMessages";
         *   readonly path: "/lnrpc.Lightning/SubscribeCustomMessages";
         *   readonly requestType: "SubscribeCustomMessagesRequest";
         *   readonly responseType: "CustomMessage";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribeCustomMessages.
         * @name lnrpc.Lightning#subscribeCustomMessages
         * @type {lnrpc.Lightning.SubscribeCustomMessages}
         */
        $Object.defineProperties(Lightning.prototype.subscribeCustomMessages = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.subscribeCustomMessages, $root.lnrpc.SubscribeCustomMessagesRequest, $root.lnrpc.CustomMessage, request, callback);
        }, {
            name: { value: "SubscribeCustomMessages" },
            path: { value: "/lnrpc.Lightning/SubscribeCustomMessages" },
            requestType: { value: "SubscribeCustomMessagesRequest" },
            responseType: { value: "CustomMessage" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#listAliases}.
         * @memberof lnrpc.Lightning
         * @typedef ListAliasesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ListAliasesResponse} [response] ListAliasesResponse
         */

        /**
         * Calls ListAliases.
         * @memberof lnrpc.Lightning
         * @typedef ListAliases
         * @type {{
         *   (request: lnrpc.IListAliasesRequest, callback: lnrpc.Lightning.ListAliasesCallback): void;
         *   (request: lnrpc.IListAliasesRequest): Promise<lnrpc.ListAliasesResponse>;
         *   readonly name: "ListAliases";
         *   readonly path: "/lnrpc.Lightning/ListAliases";
         *   readonly requestType: "ListAliasesRequest";
         *   readonly responseType: "ListAliasesResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListAliases.
         * @name lnrpc.Lightning#listAliases
         * @type {lnrpc.Lightning.ListAliases}
         */
        $Object.defineProperties(Lightning.prototype.listAliases = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.listAliases, $root.lnrpc.ListAliasesRequest, $root.lnrpc.ListAliasesResponse, request, callback);
        }, {
            name: { value: "ListAliases" },
            path: { value: "/lnrpc.Lightning/ListAliases" },
            requestType: { value: "ListAliasesRequest" },
            responseType: { value: "ListAliasesResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.Lightning#lookupHtlcResolution}.
         * @memberof lnrpc.Lightning
         * @typedef LookupHtlcResolutionCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.LookupHtlcResolutionResponse} [response] LookupHtlcResolutionResponse
         */

        /**
         * Calls LookupHtlcResolution.
         * @memberof lnrpc.Lightning
         * @typedef LookupHtlcResolution
         * @type {{
         *   (request: lnrpc.ILookupHtlcResolutionRequest, callback: lnrpc.Lightning.LookupHtlcResolutionCallback): void;
         *   (request: lnrpc.ILookupHtlcResolutionRequest): Promise<lnrpc.LookupHtlcResolutionResponse>;
         *   readonly name: "LookupHtlcResolution";
         *   readonly path: "/lnrpc.Lightning/LookupHtlcResolution";
         *   readonly requestType: "LookupHtlcResolutionRequest";
         *   readonly responseType: "LookupHtlcResolutionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls LookupHtlcResolution.
         * @name lnrpc.Lightning#lookupHtlcResolution
         * @type {lnrpc.Lightning.LookupHtlcResolution}
         */
        $Object.defineProperties(Lightning.prototype.lookupHtlcResolution = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Lightning.prototype.lookupHtlcResolution, $root.lnrpc.LookupHtlcResolutionRequest, $root.lnrpc.LookupHtlcResolutionResponse, request, callback);
        }, {
            name: { value: "LookupHtlcResolution" },
            path: { value: "/lnrpc.Lightning/LookupHtlcResolution" },
            requestType: { value: "LookupHtlcResolutionRequest" },
            responseType: { value: "LookupHtlcResolutionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return Lightning;
    })();

    lnrpc.LookupHtlcResolutionRequest = (function() {

        /**
         * Properties of a LookupHtlcResolutionRequest.
         * @typedef {Object} lnrpc.LookupHtlcResolutionRequest.$Properties
         * @property {Long|null} [chan_id] LookupHtlcResolutionRequest chan_id
         * @property {Long|null} [htlc_index] LookupHtlcResolutionRequest htlc_index
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LookupHtlcResolutionRequest.
         * @memberof lnrpc
         * @interface ILookupHtlcResolutionRequest
         * @augments lnrpc.LookupHtlcResolutionRequest.$Properties
         * @deprecated Use lnrpc.LookupHtlcResolutionRequest.$Properties instead.
         */

        /**
         * Shape of a LookupHtlcResolutionRequest.
         * @typedef {lnrpc.LookupHtlcResolutionRequest.$Properties} lnrpc.LookupHtlcResolutionRequest.$Shape
         */

        /**
         * Constructs a new LookupHtlcResolutionRequest.
         * @memberof lnrpc
         * @classdesc Represents a LookupHtlcResolutionRequest.
         * @constructor
         * @param {lnrpc.LookupHtlcResolutionRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LookupHtlcResolutionRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LookupHtlcResolutionRequest chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @instance
         */
        LookupHtlcResolutionRequest.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * LookupHtlcResolutionRequest htlc_index.
         * @member {Long} htlc_index
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @instance
         */
        LookupHtlcResolutionRequest.prototype.htlc_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new LookupHtlcResolutionRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @static
         * @param {lnrpc.LookupHtlcResolutionRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.LookupHtlcResolutionRequest} LookupHtlcResolutionRequest instance
         * @type {{
         *   (properties: lnrpc.LookupHtlcResolutionRequest.$Shape): lnrpc.LookupHtlcResolutionRequest & lnrpc.LookupHtlcResolutionRequest.$Shape;
         *   (properties?: lnrpc.LookupHtlcResolutionRequest.$Properties): lnrpc.LookupHtlcResolutionRequest;
         * }}
         */
        LookupHtlcResolutionRequest.create = function(properties) {
            return new LookupHtlcResolutionRequest(properties);
        };

        /**
         * Encodes the specified LookupHtlcResolutionRequest message. Does not implicitly {@link lnrpc.LookupHtlcResolutionRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @static
         * @param {lnrpc.LookupHtlcResolutionRequest.$Properties} message LookupHtlcResolutionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LookupHtlcResolutionRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.chan_id);
            if (message.htlc_index != null && $Object.hasOwnProperty.call(message, "htlc_index") && (typeof message.htlc_index === "object" ? message.htlc_index.low || message.htlc_index.high : message.htlc_index !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.htlc_index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LookupHtlcResolutionRequest message, length delimited. Does not implicitly {@link lnrpc.LookupHtlcResolutionRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @static
         * @param {lnrpc.LookupHtlcResolutionRequest.$Properties} message LookupHtlcResolutionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LookupHtlcResolutionRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LookupHtlcResolutionRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.LookupHtlcResolutionRequest & lnrpc.LookupHtlcResolutionRequest.$Shape} LookupHtlcResolutionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LookupHtlcResolutionRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.LookupHtlcResolutionRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_index = value;
                        else
                            delete message.htlc_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LookupHtlcResolutionRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.LookupHtlcResolutionRequest & lnrpc.LookupHtlcResolutionRequest.$Shape} LookupHtlcResolutionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LookupHtlcResolutionRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LookupHtlcResolutionRequest message.
         * @function verify
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LookupHtlcResolutionRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.htlc_index != null && $Object.hasOwnProperty.call(message, "htlc_index"))
                if (!$util.isInteger(message.htlc_index) && !(message.htlc_index && $util.isInteger(message.htlc_index.low) && $util.isInteger(message.htlc_index.high)))
                    return "htlc_index: integer|Long expected";
            return null;
        };

        /**
         * Creates a LookupHtlcResolutionRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.LookupHtlcResolutionRequest} LookupHtlcResolutionRequest
         */
        LookupHtlcResolutionRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.LookupHtlcResolutionRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.LookupHtlcResolutionRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.LookupHtlcResolutionRequest();
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.htlc_index != null)
                if (typeof object.htlc_index === "object" ? object.htlc_index.low || object.htlc_index.high : $Number(object.htlc_index) !== 0)
                    if ($util.Long)
                        message.htlc_index = $util.Long.fromValue(object.htlc_index, true);
                    else if (typeof object.htlc_index === "string")
                        message.htlc_index = $parseInt(object.htlc_index, 10);
                    else if (typeof object.htlc_index === "number")
                        message.htlc_index = object.htlc_index;
                    else if (typeof object.htlc_index === "object")
                        message.htlc_index = new $util.LongBits(object.htlc_index.low >>> 0, object.htlc_index.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a LookupHtlcResolutionRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @static
         * @param {lnrpc.LookupHtlcResolutionRequest} message LookupHtlcResolutionRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LookupHtlcResolutionRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.htlc_index != null && $Object.hasOwnProperty.call(message, "htlc_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_index = typeof message.htlc_index === "number" ? $BigInt(message.htlc_index) : $util.Long.fromBits(message.htlc_index.low >>> 0, message.htlc_index.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_index === "number")
                    object.htlc_index = options.longs === $String ? $String(message.htlc_index) : message.htlc_index;
                else
                    object.htlc_index = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_index) : options.longs === $Number ? new $util.LongBits(message.htlc_index.low >>> 0, message.htlc_index.high >>> 0).toNumber(true) : message.htlc_index;
            return object;
        };

        /**
         * Converts this LookupHtlcResolutionRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LookupHtlcResolutionRequest.prototype.toJSON = function() {
            return LookupHtlcResolutionRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LookupHtlcResolutionRequest
         * @function getTypeUrl
         * @memberof lnrpc.LookupHtlcResolutionRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LookupHtlcResolutionRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.LookupHtlcResolutionRequest";
        };

        return LookupHtlcResolutionRequest;
    })();

    lnrpc.LookupHtlcResolutionResponse = (function() {

        /**
         * Properties of a LookupHtlcResolutionResponse.
         * @typedef {Object} lnrpc.LookupHtlcResolutionResponse.$Properties
         * @property {boolean|null} [settled] LookupHtlcResolutionResponse settled
         * @property {boolean|null} [offchain] LookupHtlcResolutionResponse offchain
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LookupHtlcResolutionResponse.
         * @memberof lnrpc
         * @interface ILookupHtlcResolutionResponse
         * @augments lnrpc.LookupHtlcResolutionResponse.$Properties
         * @deprecated Use lnrpc.LookupHtlcResolutionResponse.$Properties instead.
         */

        /**
         * Shape of a LookupHtlcResolutionResponse.
         * @typedef {lnrpc.LookupHtlcResolutionResponse.$Properties} lnrpc.LookupHtlcResolutionResponse.$Shape
         */

        /**
         * Constructs a new LookupHtlcResolutionResponse.
         * @memberof lnrpc
         * @classdesc Represents a LookupHtlcResolutionResponse.
         * @constructor
         * @param {lnrpc.LookupHtlcResolutionResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LookupHtlcResolutionResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LookupHtlcResolutionResponse settled.
         * @member {boolean} settled
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @instance
         */
        LookupHtlcResolutionResponse.prototype.settled = false;

        /**
         * LookupHtlcResolutionResponse offchain.
         * @member {boolean} offchain
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @instance
         */
        LookupHtlcResolutionResponse.prototype.offchain = false;

        /**
         * Creates a new LookupHtlcResolutionResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @static
         * @param {lnrpc.LookupHtlcResolutionResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.LookupHtlcResolutionResponse} LookupHtlcResolutionResponse instance
         * @type {{
         *   (properties: lnrpc.LookupHtlcResolutionResponse.$Shape): lnrpc.LookupHtlcResolutionResponse & lnrpc.LookupHtlcResolutionResponse.$Shape;
         *   (properties?: lnrpc.LookupHtlcResolutionResponse.$Properties): lnrpc.LookupHtlcResolutionResponse;
         * }}
         */
        LookupHtlcResolutionResponse.create = function(properties) {
            return new LookupHtlcResolutionResponse(properties);
        };

        /**
         * Encodes the specified LookupHtlcResolutionResponse message. Does not implicitly {@link lnrpc.LookupHtlcResolutionResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @static
         * @param {lnrpc.LookupHtlcResolutionResponse.$Properties} message LookupHtlcResolutionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LookupHtlcResolutionResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.settled != null && $Object.hasOwnProperty.call(message, "settled") && message.settled !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.settled);
            if (message.offchain != null && $Object.hasOwnProperty.call(message, "offchain") && message.offchain !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.offchain);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LookupHtlcResolutionResponse message, length delimited. Does not implicitly {@link lnrpc.LookupHtlcResolutionResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @static
         * @param {lnrpc.LookupHtlcResolutionResponse.$Properties} message LookupHtlcResolutionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LookupHtlcResolutionResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LookupHtlcResolutionResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.LookupHtlcResolutionResponse & lnrpc.LookupHtlcResolutionResponse.$Shape} LookupHtlcResolutionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LookupHtlcResolutionResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.LookupHtlcResolutionResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.settled = value;
                        else
                            delete message.settled;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.offchain = value;
                        else
                            delete message.offchain;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LookupHtlcResolutionResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.LookupHtlcResolutionResponse & lnrpc.LookupHtlcResolutionResponse.$Shape} LookupHtlcResolutionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LookupHtlcResolutionResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LookupHtlcResolutionResponse message.
         * @function verify
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LookupHtlcResolutionResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.settled != null && $Object.hasOwnProperty.call(message, "settled"))
                if (typeof message.settled !== "boolean")
                    return "settled: boolean expected";
            if (message.offchain != null && $Object.hasOwnProperty.call(message, "offchain"))
                if (typeof message.offchain !== "boolean")
                    return "offchain: boolean expected";
            return null;
        };

        /**
         * Creates a LookupHtlcResolutionResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.LookupHtlcResolutionResponse} LookupHtlcResolutionResponse
         */
        LookupHtlcResolutionResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.LookupHtlcResolutionResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.LookupHtlcResolutionResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.LookupHtlcResolutionResponse();
            if (object.settled != null)
                if (object.settled)
                    message.settled = $Boolean(object.settled);
            if (object.offchain != null)
                if (object.offchain)
                    message.offchain = $Boolean(object.offchain);
            return message;
        };

        /**
         * Creates a plain object from a LookupHtlcResolutionResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @static
         * @param {lnrpc.LookupHtlcResolutionResponse} message LookupHtlcResolutionResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LookupHtlcResolutionResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.settled = false;
                object.offchain = false;
            }
            if (message.settled != null && $Object.hasOwnProperty.call(message, "settled"))
                object.settled = message.settled;
            if (message.offchain != null && $Object.hasOwnProperty.call(message, "offchain"))
                object.offchain = message.offchain;
            return object;
        };

        /**
         * Converts this LookupHtlcResolutionResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LookupHtlcResolutionResponse.prototype.toJSON = function() {
            return LookupHtlcResolutionResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LookupHtlcResolutionResponse
         * @function getTypeUrl
         * @memberof lnrpc.LookupHtlcResolutionResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LookupHtlcResolutionResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.LookupHtlcResolutionResponse";
        };

        return LookupHtlcResolutionResponse;
    })();

    lnrpc.SubscribeCustomMessagesRequest = (function() {

        /**
         * Properties of a SubscribeCustomMessagesRequest.
         * @typedef {Object} lnrpc.SubscribeCustomMessagesRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SubscribeCustomMessagesRequest.
         * @memberof lnrpc
         * @interface ISubscribeCustomMessagesRequest
         * @augments lnrpc.SubscribeCustomMessagesRequest.$Properties
         * @deprecated Use lnrpc.SubscribeCustomMessagesRequest.$Properties instead.
         */

        /**
         * Shape of a SubscribeCustomMessagesRequest.
         * @typedef {lnrpc.SubscribeCustomMessagesRequest.$Properties} lnrpc.SubscribeCustomMessagesRequest.$Shape
         */

        /**
         * Constructs a new SubscribeCustomMessagesRequest.
         * @memberof lnrpc
         * @classdesc Represents a SubscribeCustomMessagesRequest.
         * @constructor
         * @param {lnrpc.SubscribeCustomMessagesRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SubscribeCustomMessagesRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new SubscribeCustomMessagesRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @static
         * @param {lnrpc.SubscribeCustomMessagesRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SubscribeCustomMessagesRequest} SubscribeCustomMessagesRequest instance
         * @type {{
         *   (properties: lnrpc.SubscribeCustomMessagesRequest.$Shape): lnrpc.SubscribeCustomMessagesRequest & lnrpc.SubscribeCustomMessagesRequest.$Shape;
         *   (properties?: lnrpc.SubscribeCustomMessagesRequest.$Properties): lnrpc.SubscribeCustomMessagesRequest;
         * }}
         */
        SubscribeCustomMessagesRequest.create = function(properties) {
            return new SubscribeCustomMessagesRequest(properties);
        };

        /**
         * Encodes the specified SubscribeCustomMessagesRequest message. Does not implicitly {@link lnrpc.SubscribeCustomMessagesRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @static
         * @param {lnrpc.SubscribeCustomMessagesRequest.$Properties} message SubscribeCustomMessagesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeCustomMessagesRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SubscribeCustomMessagesRequest message, length delimited. Does not implicitly {@link lnrpc.SubscribeCustomMessagesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @static
         * @param {lnrpc.SubscribeCustomMessagesRequest.$Properties} message SubscribeCustomMessagesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeCustomMessagesRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SubscribeCustomMessagesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SubscribeCustomMessagesRequest & lnrpc.SubscribeCustomMessagesRequest.$Shape} SubscribeCustomMessagesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeCustomMessagesRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SubscribeCustomMessagesRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SubscribeCustomMessagesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SubscribeCustomMessagesRequest & lnrpc.SubscribeCustomMessagesRequest.$Shape} SubscribeCustomMessagesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeCustomMessagesRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SubscribeCustomMessagesRequest message.
         * @function verify
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SubscribeCustomMessagesRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a SubscribeCustomMessagesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SubscribeCustomMessagesRequest} SubscribeCustomMessagesRequest
         */
        SubscribeCustomMessagesRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SubscribeCustomMessagesRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SubscribeCustomMessagesRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.SubscribeCustomMessagesRequest();
        };

        /**
         * Creates a plain object from a SubscribeCustomMessagesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @static
         * @param {lnrpc.SubscribeCustomMessagesRequest} message SubscribeCustomMessagesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SubscribeCustomMessagesRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this SubscribeCustomMessagesRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SubscribeCustomMessagesRequest.prototype.toJSON = function() {
            return SubscribeCustomMessagesRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SubscribeCustomMessagesRequest
         * @function getTypeUrl
         * @memberof lnrpc.SubscribeCustomMessagesRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SubscribeCustomMessagesRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SubscribeCustomMessagesRequest";
        };

        return SubscribeCustomMessagesRequest;
    })();

    lnrpc.CustomMessage = (function() {

        /**
         * Properties of a CustomMessage.
         * @typedef {Object} lnrpc.CustomMessage.$Properties
         * @property {Uint8Array|null} [peer] CustomMessage peer
         * @property {number|null} [type] CustomMessage type
         * @property {Uint8Array|null} [data] CustomMessage data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CustomMessage.
         * @memberof lnrpc
         * @interface ICustomMessage
         * @augments lnrpc.CustomMessage.$Properties
         * @deprecated Use lnrpc.CustomMessage.$Properties instead.
         */

        /**
         * Shape of a CustomMessage.
         * @typedef {lnrpc.CustomMessage.$Properties} lnrpc.CustomMessage.$Shape
         */

        /**
         * Constructs a new CustomMessage.
         * @memberof lnrpc
         * @classdesc Represents a CustomMessage.
         * @constructor
         * @param {lnrpc.CustomMessage.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CustomMessage = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CustomMessage peer.
         * @member {Uint8Array} peer
         * @memberof lnrpc.CustomMessage
         * @instance
         */
        CustomMessage.prototype.peer = $util.newBuffer([]);

        /**
         * CustomMessage type.
         * @member {number} type
         * @memberof lnrpc.CustomMessage
         * @instance
         */
        CustomMessage.prototype.type = 0;

        /**
         * CustomMessage data.
         * @member {Uint8Array} data
         * @memberof lnrpc.CustomMessage
         * @instance
         */
        CustomMessage.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new CustomMessage instance using the specified properties.
         * @function create
         * @memberof lnrpc.CustomMessage
         * @static
         * @param {lnrpc.CustomMessage.$Properties=} [properties] Properties to set
         * @returns {lnrpc.CustomMessage} CustomMessage instance
         * @type {{
         *   (properties: lnrpc.CustomMessage.$Shape): lnrpc.CustomMessage & lnrpc.CustomMessage.$Shape;
         *   (properties?: lnrpc.CustomMessage.$Properties): lnrpc.CustomMessage;
         * }}
         */
        CustomMessage.create = function(properties) {
            return new CustomMessage(properties);
        };

        /**
         * Encodes the specified CustomMessage message. Does not implicitly {@link lnrpc.CustomMessage.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.CustomMessage
         * @static
         * @param {lnrpc.CustomMessage.$Properties} message CustomMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomMessage.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.peer != null && $Object.hasOwnProperty.call(message, "peer") && message.peer.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.peer);
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.type);
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CustomMessage message, length delimited. Does not implicitly {@link lnrpc.CustomMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.CustomMessage
         * @static
         * @param {lnrpc.CustomMessage.$Properties} message CustomMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomMessage.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CustomMessage message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.CustomMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.CustomMessage & lnrpc.CustomMessage.$Shape} CustomMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomMessage.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.CustomMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.peer = value;
                        else
                            delete message.peer;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CustomMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.CustomMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.CustomMessage & lnrpc.CustomMessage.$Shape} CustomMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomMessage.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CustomMessage message.
         * @function verify
         * @memberof lnrpc.CustomMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CustomMessage.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.peer != null && $Object.hasOwnProperty.call(message, "peer"))
                if (!(message.peer && typeof message.peer.length === "number" || $util.isString(message.peer)))
                    return "peer: buffer expected";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        /**
         * Creates a CustomMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.CustomMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.CustomMessage} CustomMessage
         */
        CustomMessage.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.CustomMessage)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.CustomMessage: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.CustomMessage();
            if (object.peer != null)
                if (object.peer.length)
                    if (typeof object.peer === "string")
                        $util.base64.decode(object.peer, message.peer = $util.newBuffer($util.base64.length(object.peer)), 0);
                    else if (object.peer.length >= 0)
                        message.peer = object.peer;
            if (object.type != null)
                if ($Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a CustomMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.CustomMessage
         * @static
         * @param {lnrpc.CustomMessage} message CustomMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CustomMessage.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.peer = "";
                else {
                    object.peer = [];
                    if (options.bytes !== $Array)
                        object.peer = $util.newBuffer(object.peer);
                }
                object.type = 0;
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.peer != null && $Object.hasOwnProperty.call(message, "peer"))
                object.peer = options.bytes === $String ? $util.base64.encode(message.peer, 0, message.peer.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.peer) : message.peer;
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this CustomMessage to JSON.
         * @function toJSON
         * @memberof lnrpc.CustomMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CustomMessage.prototype.toJSON = function() {
            return CustomMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CustomMessage
         * @function getTypeUrl
         * @memberof lnrpc.CustomMessage
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CustomMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.CustomMessage";
        };

        return CustomMessage;
    })();

    lnrpc.SendCustomMessageRequest = (function() {

        /**
         * Properties of a SendCustomMessageRequest.
         * @typedef {Object} lnrpc.SendCustomMessageRequest.$Properties
         * @property {Uint8Array|null} [peer] SendCustomMessageRequest peer
         * @property {number|null} [type] SendCustomMessageRequest type
         * @property {Uint8Array|null} [data] SendCustomMessageRequest data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendCustomMessageRequest.
         * @memberof lnrpc
         * @interface ISendCustomMessageRequest
         * @augments lnrpc.SendCustomMessageRequest.$Properties
         * @deprecated Use lnrpc.SendCustomMessageRequest.$Properties instead.
         */

        /**
         * Shape of a SendCustomMessageRequest.
         * @typedef {lnrpc.SendCustomMessageRequest.$Properties} lnrpc.SendCustomMessageRequest.$Shape
         */

        /**
         * Constructs a new SendCustomMessageRequest.
         * @memberof lnrpc
         * @classdesc Represents a SendCustomMessageRequest.
         * @constructor
         * @param {lnrpc.SendCustomMessageRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendCustomMessageRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendCustomMessageRequest peer.
         * @member {Uint8Array} peer
         * @memberof lnrpc.SendCustomMessageRequest
         * @instance
         */
        SendCustomMessageRequest.prototype.peer = $util.newBuffer([]);

        /**
         * SendCustomMessageRequest type.
         * @member {number} type
         * @memberof lnrpc.SendCustomMessageRequest
         * @instance
         */
        SendCustomMessageRequest.prototype.type = 0;

        /**
         * SendCustomMessageRequest data.
         * @member {Uint8Array} data
         * @memberof lnrpc.SendCustomMessageRequest
         * @instance
         */
        SendCustomMessageRequest.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new SendCustomMessageRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.SendCustomMessageRequest
         * @static
         * @param {lnrpc.SendCustomMessageRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SendCustomMessageRequest} SendCustomMessageRequest instance
         * @type {{
         *   (properties: lnrpc.SendCustomMessageRequest.$Shape): lnrpc.SendCustomMessageRequest & lnrpc.SendCustomMessageRequest.$Shape;
         *   (properties?: lnrpc.SendCustomMessageRequest.$Properties): lnrpc.SendCustomMessageRequest;
         * }}
         */
        SendCustomMessageRequest.create = function(properties) {
            return new SendCustomMessageRequest(properties);
        };

        /**
         * Encodes the specified SendCustomMessageRequest message. Does not implicitly {@link lnrpc.SendCustomMessageRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SendCustomMessageRequest
         * @static
         * @param {lnrpc.SendCustomMessageRequest.$Properties} message SendCustomMessageRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendCustomMessageRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.peer != null && $Object.hasOwnProperty.call(message, "peer") && message.peer.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.peer);
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.type);
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendCustomMessageRequest message, length delimited. Does not implicitly {@link lnrpc.SendCustomMessageRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SendCustomMessageRequest
         * @static
         * @param {lnrpc.SendCustomMessageRequest.$Properties} message SendCustomMessageRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendCustomMessageRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendCustomMessageRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SendCustomMessageRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SendCustomMessageRequest & lnrpc.SendCustomMessageRequest.$Shape} SendCustomMessageRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendCustomMessageRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SendCustomMessageRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.peer = value;
                        else
                            delete message.peer;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendCustomMessageRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SendCustomMessageRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SendCustomMessageRequest & lnrpc.SendCustomMessageRequest.$Shape} SendCustomMessageRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendCustomMessageRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendCustomMessageRequest message.
         * @function verify
         * @memberof lnrpc.SendCustomMessageRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendCustomMessageRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.peer != null && $Object.hasOwnProperty.call(message, "peer"))
                if (!(message.peer && typeof message.peer.length === "number" || $util.isString(message.peer)))
                    return "peer: buffer expected";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        /**
         * Creates a SendCustomMessageRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SendCustomMessageRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SendCustomMessageRequest} SendCustomMessageRequest
         */
        SendCustomMessageRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SendCustomMessageRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SendCustomMessageRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SendCustomMessageRequest();
            if (object.peer != null)
                if (object.peer.length)
                    if (typeof object.peer === "string")
                        $util.base64.decode(object.peer, message.peer = $util.newBuffer($util.base64.length(object.peer)), 0);
                    else if (object.peer.length >= 0)
                        message.peer = object.peer;
            if (object.type != null)
                if ($Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a SendCustomMessageRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SendCustomMessageRequest
         * @static
         * @param {lnrpc.SendCustomMessageRequest} message SendCustomMessageRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendCustomMessageRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.peer = "";
                else {
                    object.peer = [];
                    if (options.bytes !== $Array)
                        object.peer = $util.newBuffer(object.peer);
                }
                object.type = 0;
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.peer != null && $Object.hasOwnProperty.call(message, "peer"))
                object.peer = options.bytes === $String ? $util.base64.encode(message.peer, 0, message.peer.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.peer) : message.peer;
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this SendCustomMessageRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.SendCustomMessageRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendCustomMessageRequest.prototype.toJSON = function() {
            return SendCustomMessageRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendCustomMessageRequest
         * @function getTypeUrl
         * @memberof lnrpc.SendCustomMessageRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendCustomMessageRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SendCustomMessageRequest";
        };

        return SendCustomMessageRequest;
    })();

    lnrpc.SendCustomMessageResponse = (function() {

        /**
         * Properties of a SendCustomMessageResponse.
         * @typedef {Object} lnrpc.SendCustomMessageResponse.$Properties
         * @property {string|null} [status] SendCustomMessageResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendCustomMessageResponse.
         * @memberof lnrpc
         * @interface ISendCustomMessageResponse
         * @augments lnrpc.SendCustomMessageResponse.$Properties
         * @deprecated Use lnrpc.SendCustomMessageResponse.$Properties instead.
         */

        /**
         * Shape of a SendCustomMessageResponse.
         * @typedef {lnrpc.SendCustomMessageResponse.$Properties} lnrpc.SendCustomMessageResponse.$Shape
         */

        /**
         * Constructs a new SendCustomMessageResponse.
         * @memberof lnrpc
         * @classdesc Represents a SendCustomMessageResponse.
         * @constructor
         * @param {lnrpc.SendCustomMessageResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendCustomMessageResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendCustomMessageResponse status.
         * @member {string} status
         * @memberof lnrpc.SendCustomMessageResponse
         * @instance
         */
        SendCustomMessageResponse.prototype.status = "";

        /**
         * Creates a new SendCustomMessageResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.SendCustomMessageResponse
         * @static
         * @param {lnrpc.SendCustomMessageResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SendCustomMessageResponse} SendCustomMessageResponse instance
         * @type {{
         *   (properties: lnrpc.SendCustomMessageResponse.$Shape): lnrpc.SendCustomMessageResponse & lnrpc.SendCustomMessageResponse.$Shape;
         *   (properties?: lnrpc.SendCustomMessageResponse.$Properties): lnrpc.SendCustomMessageResponse;
         * }}
         */
        SendCustomMessageResponse.create = function(properties) {
            return new SendCustomMessageResponse(properties);
        };

        /**
         * Encodes the specified SendCustomMessageResponse message. Does not implicitly {@link lnrpc.SendCustomMessageResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SendCustomMessageResponse
         * @static
         * @param {lnrpc.SendCustomMessageResponse.$Properties} message SendCustomMessageResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendCustomMessageResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendCustomMessageResponse message, length delimited. Does not implicitly {@link lnrpc.SendCustomMessageResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SendCustomMessageResponse
         * @static
         * @param {lnrpc.SendCustomMessageResponse.$Properties} message SendCustomMessageResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendCustomMessageResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendCustomMessageResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SendCustomMessageResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SendCustomMessageResponse & lnrpc.SendCustomMessageResponse.$Shape} SendCustomMessageResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendCustomMessageResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SendCustomMessageResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendCustomMessageResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SendCustomMessageResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SendCustomMessageResponse & lnrpc.SendCustomMessageResponse.$Shape} SendCustomMessageResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendCustomMessageResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendCustomMessageResponse message.
         * @function verify
         * @memberof lnrpc.SendCustomMessageResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendCustomMessageResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a SendCustomMessageResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SendCustomMessageResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SendCustomMessageResponse} SendCustomMessageResponse
         */
        SendCustomMessageResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SendCustomMessageResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SendCustomMessageResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SendCustomMessageResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a SendCustomMessageResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SendCustomMessageResponse
         * @static
         * @param {lnrpc.SendCustomMessageResponse} message SendCustomMessageResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendCustomMessageResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this SendCustomMessageResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.SendCustomMessageResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendCustomMessageResponse.prototype.toJSON = function() {
            return SendCustomMessageResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendCustomMessageResponse
         * @function getTypeUrl
         * @memberof lnrpc.SendCustomMessageResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendCustomMessageResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SendCustomMessageResponse";
        };

        return SendCustomMessageResponse;
    })();

    lnrpc.Utxo = (function() {

        /**
         * Properties of an Utxo.
         * @typedef {Object} lnrpc.Utxo.$Properties
         * @property {lnrpc.AddressType|null} [address_type] Utxo address_type
         * @property {string|null} [address] Utxo address
         * @property {Long|null} [amount_sat] Utxo amount_sat
         * @property {string|null} [pk_script] Utxo pk_script
         * @property {lnrpc.OutPoint.$Properties|null} [outpoint] Utxo outpoint
         * @property {Long|null} [confirmations] Utxo confirmations
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an Utxo.
         * @memberof lnrpc
         * @interface IUtxo
         * @augments lnrpc.Utxo.$Properties
         * @deprecated Use lnrpc.Utxo.$Properties instead.
         */

        /**
         * Shape of an Utxo.
         * @typedef {lnrpc.Utxo.$Properties} lnrpc.Utxo.$Shape
         */

        /**
         * Constructs a new Utxo.
         * @memberof lnrpc
         * @classdesc Represents an Utxo.
         * @constructor
         * @param {lnrpc.Utxo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Utxo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Utxo address_type.
         * @member {lnrpc.AddressType} address_type
         * @memberof lnrpc.Utxo
         * @instance
         */
        Utxo.prototype.address_type = 0;

        /**
         * Utxo address.
         * @member {string} address
         * @memberof lnrpc.Utxo
         * @instance
         */
        Utxo.prototype.address = "";

        /**
         * Utxo amount_sat.
         * @member {Long} amount_sat
         * @memberof lnrpc.Utxo
         * @instance
         */
        Utxo.prototype.amount_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Utxo pk_script.
         * @member {string} pk_script
         * @memberof lnrpc.Utxo
         * @instance
         */
        Utxo.prototype.pk_script = "";

        /**
         * Utxo outpoint.
         * @member {lnrpc.OutPoint.$Properties|null|undefined} outpoint
         * @memberof lnrpc.Utxo
         * @instance
         */
        Utxo.prototype.outpoint = null;

        /**
         * Utxo confirmations.
         * @member {Long} confirmations
         * @memberof lnrpc.Utxo
         * @instance
         */
        Utxo.prototype.confirmations = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new Utxo instance using the specified properties.
         * @function create
         * @memberof lnrpc.Utxo
         * @static
         * @param {lnrpc.Utxo.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Utxo} Utxo instance
         * @type {{
         *   (properties: lnrpc.Utxo.$Shape): lnrpc.Utxo & lnrpc.Utxo.$Shape;
         *   (properties?: lnrpc.Utxo.$Properties): lnrpc.Utxo;
         * }}
         */
        Utxo.create = function(properties) {
            return new Utxo(properties);
        };

        /**
         * Encodes the specified Utxo message. Does not implicitly {@link lnrpc.Utxo.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Utxo
         * @static
         * @param {lnrpc.Utxo.$Properties} message Utxo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Utxo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type") && message.address_type !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.address_type);
            if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat") && (typeof message.amount_sat === "object" ? message.amount_sat.low || message.amount_sat.high : message.amount_sat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.amount_sat);
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script") && message.pk_script !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.pk_script);
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                $root.lnrpc.OutPoint.encode(message.outpoint, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.confirmations != null && $Object.hasOwnProperty.call(message, "confirmations") && (typeof message.confirmations === "object" ? message.confirmations.low || message.confirmations.high : message.confirmations !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.confirmations);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Utxo message, length delimited. Does not implicitly {@link lnrpc.Utxo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Utxo
         * @static
         * @param {lnrpc.Utxo.$Properties} message Utxo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Utxo.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Utxo message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Utxo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Utxo & lnrpc.Utxo.$Shape} Utxo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Utxo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Utxo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.address_type = value;
                        else
                            delete message.address_type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.address = value;
                        else
                            delete message.address;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount_sat = value;
                        else
                            delete message.amount_sat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pk_script = value;
                        else
                            delete message.pk_script;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.outpoint = $root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.outpoint);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.confirmations = value;
                        else
                            delete message.confirmations;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Utxo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Utxo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Utxo & lnrpc.Utxo.$Shape} Utxo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Utxo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Utxo message.
         * @function verify
         * @memberof lnrpc.Utxo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Utxo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                if (typeof message.address_type !== "number" || (message.address_type | 0) !== message.address_type)
                    return "address_type: enum value expected";
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat"))
                if (!$util.isInteger(message.amount_sat) && !(message.amount_sat && $util.isInteger(message.amount_sat.low) && $util.isInteger(message.amount_sat.high)))
                    return "amount_sat: integer|Long expected";
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                if (!$util.isString(message.pk_script))
                    return "pk_script: string expected";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint")) {
                let error = $root.lnrpc.OutPoint.verify(message.outpoint, _depth + 1);
                if (error)
                    return "outpoint." + error;
            }
            if (message.confirmations != null && $Object.hasOwnProperty.call(message, "confirmations"))
                if (!$util.isInteger(message.confirmations) && !(message.confirmations && $util.isInteger(message.confirmations.low) && $util.isInteger(message.confirmations.high)))
                    return "confirmations: integer|Long expected";
            return null;
        };

        /**
         * Creates an Utxo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Utxo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Utxo} Utxo
         */
        Utxo.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Utxo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Utxo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Utxo();
            if (object.address_type !== 0 && (typeof object.address_type !== "string" || $root.lnrpc.AddressType[object.address_type] !== 0))
                switch (object.address_type) {
                case "WITNESS_PUBKEY_HASH":
                case 0:
                    message.address_type = 0;
                    break;
                case "NESTED_PUBKEY_HASH":
                case 1:
                    message.address_type = 1;
                    break;
                case "UNUSED_WITNESS_PUBKEY_HASH":
                case 2:
                    message.address_type = 2;
                    break;
                case "UNUSED_NESTED_PUBKEY_HASH":
                case 3:
                    message.address_type = 3;
                    break;
                case "TAPROOT_PUBKEY":
                case 4:
                    message.address_type = 4;
                    break;
                case "UNUSED_TAPROOT_PUBKEY":
                case 5:
                    message.address_type = 5;
                    break;
                default:
                    if (typeof object.address_type === "number" && (object.address_type | 0) === object.address_type)
                        message.address_type = object.address_type;
                }
            if (object.address != null)
                if (typeof object.address !== "string" || object.address.length)
                    message.address = $String(object.address);
            if (object.amount_sat != null)
                if (typeof object.amount_sat === "object" ? object.amount_sat.low || object.amount_sat.high : $Number(object.amount_sat) !== 0)
                    if ($util.Long)
                        message.amount_sat = $util.Long.fromValue(object.amount_sat, false);
                    else if (typeof object.amount_sat === "string")
                        message.amount_sat = $parseInt(object.amount_sat, 10);
                    else if (typeof object.amount_sat === "number")
                        message.amount_sat = object.amount_sat;
                    else if (typeof object.amount_sat === "object")
                        message.amount_sat = new $util.LongBits(object.amount_sat.low >>> 0, object.amount_sat.high >>> 0).toNumber();
            if (object.pk_script != null)
                if (typeof object.pk_script !== "string" || object.pk_script.length)
                    message.pk_script = $String(object.pk_script);
            if (object.outpoint != null) {
                if (!$util.isObject(object.outpoint))
                    throw $TypeError(".lnrpc.Utxo.outpoint: object expected");
                message.outpoint = $root.lnrpc.OutPoint.fromObject(object.outpoint, _depth + 1);
            }
            if (object.confirmations != null)
                if (typeof object.confirmations === "object" ? object.confirmations.low || object.confirmations.high : $Number(object.confirmations) !== 0)
                    if ($util.Long)
                        message.confirmations = $util.Long.fromValue(object.confirmations, false);
                    else if (typeof object.confirmations === "string")
                        message.confirmations = $parseInt(object.confirmations, 10);
                    else if (typeof object.confirmations === "number")
                        message.confirmations = object.confirmations;
                    else if (typeof object.confirmations === "object")
                        message.confirmations = new $util.LongBits(object.confirmations.low >>> 0, object.confirmations.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an Utxo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Utxo
         * @static
         * @param {lnrpc.Utxo} message Utxo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Utxo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.address_type = options.enums === $String ? "WITNESS_PUBKEY_HASH" : 0;
                object.address = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.pk_script = "";
                object.outpoint = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.confirmations = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.confirmations = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                object.address_type = options.enums === $String ? $root.lnrpc.AddressType[message.address_type] === $undefined ? message.address_type : $root.lnrpc.AddressType[message.address_type] : message.address_type;
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                object.address = message.address;
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount_sat = typeof message.amount_sat === "number" ? $BigInt(message.amount_sat) : $util.Long.fromBits(message.amount_sat.low >>> 0, message.amount_sat.high >>> 0, false).toBigInt();
                else if (typeof message.amount_sat === "number")
                    object.amount_sat = options.longs === $String ? $String(message.amount_sat) : message.amount_sat;
                else
                    object.amount_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.amount_sat) : options.longs === $Number ? new $util.LongBits(message.amount_sat.low >>> 0, message.amount_sat.high >>> 0).toNumber() : message.amount_sat;
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                object.pk_script = message.pk_script;
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = $root.lnrpc.OutPoint.toObject(message.outpoint, options, _depth + 1);
            if (message.confirmations != null && $Object.hasOwnProperty.call(message, "confirmations"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.confirmations = typeof message.confirmations === "number" ? $BigInt(message.confirmations) : $util.Long.fromBits(message.confirmations.low >>> 0, message.confirmations.high >>> 0, false).toBigInt();
                else if (typeof message.confirmations === "number")
                    object.confirmations = options.longs === $String ? $String(message.confirmations) : message.confirmations;
                else
                    object.confirmations = options.longs === $String ? $util.Long.prototype.toString.call(message.confirmations) : options.longs === $Number ? new $util.LongBits(message.confirmations.low >>> 0, message.confirmations.high >>> 0).toNumber() : message.confirmations;
            return object;
        };

        /**
         * Converts this Utxo to JSON.
         * @function toJSON
         * @memberof lnrpc.Utxo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Utxo.prototype.toJSON = function() {
            return Utxo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Utxo
         * @function getTypeUrl
         * @memberof lnrpc.Utxo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Utxo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Utxo";
        };

        return Utxo;
    })();

    /**
     * OutputScriptType enum.
     * @name lnrpc.OutputScriptType
     * @enum {number}
     * @property {number} SCRIPT_TYPE_PUBKEY_HASH=0 SCRIPT_TYPE_PUBKEY_HASH value
     * @property {number} SCRIPT_TYPE_SCRIPT_HASH=1 SCRIPT_TYPE_SCRIPT_HASH value
     * @property {number} SCRIPT_TYPE_WITNESS_V0_PUBKEY_HASH=2 SCRIPT_TYPE_WITNESS_V0_PUBKEY_HASH value
     * @property {number} SCRIPT_TYPE_WITNESS_V0_SCRIPT_HASH=3 SCRIPT_TYPE_WITNESS_V0_SCRIPT_HASH value
     * @property {number} SCRIPT_TYPE_PUBKEY=4 SCRIPT_TYPE_PUBKEY value
     * @property {number} SCRIPT_TYPE_MULTISIG=5 SCRIPT_TYPE_MULTISIG value
     * @property {number} SCRIPT_TYPE_NULLDATA=6 SCRIPT_TYPE_NULLDATA value
     * @property {number} SCRIPT_TYPE_NON_STANDARD=7 SCRIPT_TYPE_NON_STANDARD value
     * @property {number} SCRIPT_TYPE_WITNESS_UNKNOWN=8 SCRIPT_TYPE_WITNESS_UNKNOWN value
     * @property {number} SCRIPT_TYPE_WITNESS_V1_TAPROOT=9 SCRIPT_TYPE_WITNESS_V1_TAPROOT value
     */
    lnrpc.OutputScriptType = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "SCRIPT_TYPE_PUBKEY_HASH"] = 0;
        values[valuesById[1] = "SCRIPT_TYPE_SCRIPT_HASH"] = 1;
        values[valuesById[2] = "SCRIPT_TYPE_WITNESS_V0_PUBKEY_HASH"] = 2;
        values[valuesById[3] = "SCRIPT_TYPE_WITNESS_V0_SCRIPT_HASH"] = 3;
        values[valuesById[4] = "SCRIPT_TYPE_PUBKEY"] = 4;
        values[valuesById[5] = "SCRIPT_TYPE_MULTISIG"] = 5;
        values[valuesById[6] = "SCRIPT_TYPE_NULLDATA"] = 6;
        values[valuesById[7] = "SCRIPT_TYPE_NON_STANDARD"] = 7;
        values[valuesById[8] = "SCRIPT_TYPE_WITNESS_UNKNOWN"] = 8;
        values[valuesById[9] = "SCRIPT_TYPE_WITNESS_V1_TAPROOT"] = 9;
        return values;
    })();

    lnrpc.OutputDetail = (function() {

        /**
         * Properties of an OutputDetail.
         * @typedef {Object} lnrpc.OutputDetail.$Properties
         * @property {lnrpc.OutputScriptType|null} [output_type] OutputDetail output_type
         * @property {string|null} [address] OutputDetail address
         * @property {string|null} [pk_script] OutputDetail pk_script
         * @property {Long|null} [output_index] OutputDetail output_index
         * @property {Long|null} [amount] OutputDetail amount
         * @property {boolean|null} [is_our_address] OutputDetail is_our_address
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an OutputDetail.
         * @memberof lnrpc
         * @interface IOutputDetail
         * @augments lnrpc.OutputDetail.$Properties
         * @deprecated Use lnrpc.OutputDetail.$Properties instead.
         */

        /**
         * Shape of an OutputDetail.
         * @typedef {lnrpc.OutputDetail.$Properties} lnrpc.OutputDetail.$Shape
         */

        /**
         * Constructs a new OutputDetail.
         * @memberof lnrpc
         * @classdesc Represents an OutputDetail.
         * @constructor
         * @param {lnrpc.OutputDetail.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const OutputDetail = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OutputDetail output_type.
         * @member {lnrpc.OutputScriptType} output_type
         * @memberof lnrpc.OutputDetail
         * @instance
         */
        OutputDetail.prototype.output_type = 0;

        /**
         * OutputDetail address.
         * @member {string} address
         * @memberof lnrpc.OutputDetail
         * @instance
         */
        OutputDetail.prototype.address = "";

        /**
         * OutputDetail pk_script.
         * @member {string} pk_script
         * @memberof lnrpc.OutputDetail
         * @instance
         */
        OutputDetail.prototype.pk_script = "";

        /**
         * OutputDetail output_index.
         * @member {Long} output_index
         * @memberof lnrpc.OutputDetail
         * @instance
         */
        OutputDetail.prototype.output_index = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * OutputDetail amount.
         * @member {Long} amount
         * @memberof lnrpc.OutputDetail
         * @instance
         */
        OutputDetail.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * OutputDetail is_our_address.
         * @member {boolean} is_our_address
         * @memberof lnrpc.OutputDetail
         * @instance
         */
        OutputDetail.prototype.is_our_address = false;

        /**
         * Creates a new OutputDetail instance using the specified properties.
         * @function create
         * @memberof lnrpc.OutputDetail
         * @static
         * @param {lnrpc.OutputDetail.$Properties=} [properties] Properties to set
         * @returns {lnrpc.OutputDetail} OutputDetail instance
         * @type {{
         *   (properties: lnrpc.OutputDetail.$Shape): lnrpc.OutputDetail & lnrpc.OutputDetail.$Shape;
         *   (properties?: lnrpc.OutputDetail.$Properties): lnrpc.OutputDetail;
         * }}
         */
        OutputDetail.create = function(properties) {
            return new OutputDetail(properties);
        };

        /**
         * Encodes the specified OutputDetail message. Does not implicitly {@link lnrpc.OutputDetail.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.OutputDetail
         * @static
         * @param {lnrpc.OutputDetail.$Properties} message OutputDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OutputDetail.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.output_type != null && $Object.hasOwnProperty.call(message, "output_type") && message.output_type !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.output_type);
            if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script") && message.pk_script !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.pk_script);
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index") && (typeof message.output_index === "object" ? message.output_index.low || message.output_index.high : message.output_index !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.output_index);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.amount);
            if (message.is_our_address != null && $Object.hasOwnProperty.call(message, "is_our_address") && message.is_our_address !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.is_our_address);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified OutputDetail message, length delimited. Does not implicitly {@link lnrpc.OutputDetail.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.OutputDetail
         * @static
         * @param {lnrpc.OutputDetail.$Properties} message OutputDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OutputDetail.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an OutputDetail message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.OutputDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.OutputDetail & lnrpc.OutputDetail.$Shape} OutputDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OutputDetail.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.OutputDetail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.output_type = value;
                        else
                            delete message.output_type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.address = value;
                        else
                            delete message.address;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pk_script = value;
                        else
                            delete message.pk_script;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.output_index = value;
                        else
                            delete message.output_index;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_our_address = value;
                        else
                            delete message.is_our_address;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an OutputDetail message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.OutputDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.OutputDetail & lnrpc.OutputDetail.$Shape} OutputDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OutputDetail.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OutputDetail message.
         * @function verify
         * @memberof lnrpc.OutputDetail
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OutputDetail.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.output_type != null && $Object.hasOwnProperty.call(message, "output_type"))
                if (typeof message.output_type !== "number" || (message.output_type | 0) !== message.output_type)
                    return "output_type: enum value expected";
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                if (!$util.isString(message.pk_script))
                    return "pk_script: string expected";
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index"))
                if (!$util.isInteger(message.output_index) && !(message.output_index && $util.isInteger(message.output_index.low) && $util.isInteger(message.output_index.high)))
                    return "output_index: integer|Long expected";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.is_our_address != null && $Object.hasOwnProperty.call(message, "is_our_address"))
                if (typeof message.is_our_address !== "boolean")
                    return "is_our_address: boolean expected";
            return null;
        };

        /**
         * Creates an OutputDetail message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.OutputDetail
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.OutputDetail} OutputDetail
         */
        OutputDetail.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.OutputDetail)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.OutputDetail: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.OutputDetail();
            if (object.output_type !== 0 && (typeof object.output_type !== "string" || $root.lnrpc.OutputScriptType[object.output_type] !== 0))
                switch (object.output_type) {
                case "SCRIPT_TYPE_PUBKEY_HASH":
                case 0:
                    message.output_type = 0;
                    break;
                case "SCRIPT_TYPE_SCRIPT_HASH":
                case 1:
                    message.output_type = 1;
                    break;
                case "SCRIPT_TYPE_WITNESS_V0_PUBKEY_HASH":
                case 2:
                    message.output_type = 2;
                    break;
                case "SCRIPT_TYPE_WITNESS_V0_SCRIPT_HASH":
                case 3:
                    message.output_type = 3;
                    break;
                case "SCRIPT_TYPE_PUBKEY":
                case 4:
                    message.output_type = 4;
                    break;
                case "SCRIPT_TYPE_MULTISIG":
                case 5:
                    message.output_type = 5;
                    break;
                case "SCRIPT_TYPE_NULLDATA":
                case 6:
                    message.output_type = 6;
                    break;
                case "SCRIPT_TYPE_NON_STANDARD":
                case 7:
                    message.output_type = 7;
                    break;
                case "SCRIPT_TYPE_WITNESS_UNKNOWN":
                case 8:
                    message.output_type = 8;
                    break;
                case "SCRIPT_TYPE_WITNESS_V1_TAPROOT":
                case 9:
                    message.output_type = 9;
                    break;
                default:
                    if (typeof object.output_type === "number" && (object.output_type | 0) === object.output_type)
                        message.output_type = object.output_type;
                }
            if (object.address != null)
                if (typeof object.address !== "string" || object.address.length)
                    message.address = $String(object.address);
            if (object.pk_script != null)
                if (typeof object.pk_script !== "string" || object.pk_script.length)
                    message.pk_script = $String(object.pk_script);
            if (object.output_index != null)
                if (typeof object.output_index === "object" ? object.output_index.low || object.output_index.high : $Number(object.output_index) !== 0)
                    if ($util.Long)
                        message.output_index = $util.Long.fromValue(object.output_index, false);
                    else if (typeof object.output_index === "string")
                        message.output_index = $parseInt(object.output_index, 10);
                    else if (typeof object.output_index === "number")
                        message.output_index = object.output_index;
                    else if (typeof object.output_index === "object")
                        message.output_index = new $util.LongBits(object.output_index.low >>> 0, object.output_index.high >>> 0).toNumber();
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.is_our_address != null)
                if (object.is_our_address)
                    message.is_our_address = $Boolean(object.is_our_address);
            return message;
        };

        /**
         * Creates a plain object from an OutputDetail message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.OutputDetail
         * @static
         * @param {lnrpc.OutputDetail} message OutputDetail
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OutputDetail.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.output_type = options.enums === $String ? "SCRIPT_TYPE_PUBKEY_HASH" : 0;
                object.address = "";
                object.pk_script = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.output_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.output_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.is_our_address = false;
            }
            if (message.output_type != null && $Object.hasOwnProperty.call(message, "output_type"))
                object.output_type = options.enums === $String ? $root.lnrpc.OutputScriptType[message.output_type] === $undefined ? message.output_type : $root.lnrpc.OutputScriptType[message.output_type] : message.output_type;
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                object.address = message.address;
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                object.pk_script = message.pk_script;
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.output_index = typeof message.output_index === "number" ? $BigInt(message.output_index) : $util.Long.fromBits(message.output_index.low >>> 0, message.output_index.high >>> 0, false).toBigInt();
                else if (typeof message.output_index === "number")
                    object.output_index = options.longs === $String ? $String(message.output_index) : message.output_index;
                else
                    object.output_index = options.longs === $String ? $util.Long.prototype.toString.call(message.output_index) : options.longs === $Number ? new $util.LongBits(message.output_index.low >>> 0, message.output_index.high >>> 0).toNumber() : message.output_index;
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.is_our_address != null && $Object.hasOwnProperty.call(message, "is_our_address"))
                object.is_our_address = message.is_our_address;
            return object;
        };

        /**
         * Converts this OutputDetail to JSON.
         * @function toJSON
         * @memberof lnrpc.OutputDetail
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OutputDetail.prototype.toJSON = function() {
            return OutputDetail.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for OutputDetail
         * @function getTypeUrl
         * @memberof lnrpc.OutputDetail
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        OutputDetail.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.OutputDetail";
        };

        return OutputDetail;
    })();

    lnrpc.Transaction = (function() {

        /**
         * Properties of a Transaction.
         * @typedef {Object} lnrpc.Transaction.$Properties
         * @property {string|null} [tx_hash] Transaction tx_hash
         * @property {Long|null} [amount] Transaction amount
         * @property {number|null} [num_confirmations] Transaction num_confirmations
         * @property {string|null} [block_hash] Transaction block_hash
         * @property {number|null} [block_height] Transaction block_height
         * @property {Long|null} [time_stamp] Transaction time_stamp
         * @property {Long|null} [total_fees] Transaction total_fees
         * @property {Array.<string>|null} [dest_addresses] Transaction dest_addresses
         * @property {Array.<lnrpc.OutputDetail.$Properties>|null} [output_details] Transaction output_details
         * @property {string|null} [raw_tx_hex] Transaction raw_tx_hex
         * @property {string|null} [label] Transaction label
         * @property {Array.<lnrpc.PreviousOutPoint.$Properties>|null} [previous_outpoints] Transaction previous_outpoints
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Transaction.
         * @memberof lnrpc
         * @interface ITransaction
         * @augments lnrpc.Transaction.$Properties
         * @deprecated Use lnrpc.Transaction.$Properties instead.
         */

        /**
         * Shape of a Transaction.
         * @typedef {lnrpc.Transaction.$Properties} lnrpc.Transaction.$Shape
         */

        /**
         * Constructs a new Transaction.
         * @memberof lnrpc
         * @classdesc Represents a Transaction.
         * @constructor
         * @param {lnrpc.Transaction.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Transaction = function (properties) {
            this.dest_addresses = [];
            this.output_details = [];
            this.previous_outpoints = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Transaction tx_hash.
         * @member {string} tx_hash
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.tx_hash = "";

        /**
         * Transaction amount.
         * @member {Long} amount
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Transaction num_confirmations.
         * @member {number} num_confirmations
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.num_confirmations = 0;

        /**
         * Transaction block_hash.
         * @member {string} block_hash
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.block_hash = "";

        /**
         * Transaction block_height.
         * @member {number} block_height
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.block_height = 0;

        /**
         * Transaction time_stamp.
         * @member {Long} time_stamp
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.time_stamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Transaction total_fees.
         * @member {Long} total_fees
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.total_fees = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Transaction dest_addresses.
         * @member {Array.<string>} dest_addresses
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.dest_addresses = $util.emptyArray;

        /**
         * Transaction output_details.
         * @member {Array.<lnrpc.OutputDetail.$Properties>} output_details
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.output_details = $util.emptyArray;

        /**
         * Transaction raw_tx_hex.
         * @member {string} raw_tx_hex
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.raw_tx_hex = "";

        /**
         * Transaction label.
         * @member {string} label
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.label = "";

        /**
         * Transaction previous_outpoints.
         * @member {Array.<lnrpc.PreviousOutPoint.$Properties>} previous_outpoints
         * @memberof lnrpc.Transaction
         * @instance
         */
        Transaction.prototype.previous_outpoints = $util.emptyArray;

        /**
         * Creates a new Transaction instance using the specified properties.
         * @function create
         * @memberof lnrpc.Transaction
         * @static
         * @param {lnrpc.Transaction.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Transaction} Transaction instance
         * @type {{
         *   (properties: lnrpc.Transaction.$Shape): lnrpc.Transaction & lnrpc.Transaction.$Shape;
         *   (properties?: lnrpc.Transaction.$Properties): lnrpc.Transaction;
         * }}
         */
        Transaction.create = function(properties) {
            return new Transaction(properties);
        };

        /**
         * Encodes the specified Transaction message. Does not implicitly {@link lnrpc.Transaction.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Transaction
         * @static
         * @param {lnrpc.Transaction.$Properties} message Transaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Transaction.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tx_hash != null && $Object.hasOwnProperty.call(message, "tx_hash") && message.tx_hash !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.tx_hash);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amount);
            if (message.num_confirmations != null && $Object.hasOwnProperty.call(message, "num_confirmations") && message.num_confirmations !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.num_confirmations);
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash") && message.block_hash !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.block_hash);
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height") && message.block_height !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.block_height);
            if (message.time_stamp != null && $Object.hasOwnProperty.call(message, "time_stamp") && (typeof message.time_stamp === "object" ? message.time_stamp.low || message.time_stamp.high : message.time_stamp !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.time_stamp);
            if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees") && (typeof message.total_fees === "object" ? message.total_fees.low || message.total_fees.high : message.total_fees !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.total_fees);
            if (message.dest_addresses != null && message.dest_addresses.length)
                for (let i = 0; i < message.dest_addresses.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.dest_addresses[i]);
            if (message.raw_tx_hex != null && $Object.hasOwnProperty.call(message, "raw_tx_hex") && message.raw_tx_hex !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.raw_tx_hex);
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.label);
            if (message.output_details != null && message.output_details.length)
                for (let i = 0; i < message.output_details.length; ++i)
                    $root.lnrpc.OutputDetail.encode(message.output_details[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.previous_outpoints != null && message.previous_outpoints.length)
                for (let i = 0; i < message.previous_outpoints.length; ++i)
                    $root.lnrpc.PreviousOutPoint.encode(message.previous_outpoints[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Transaction message, length delimited. Does not implicitly {@link lnrpc.Transaction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Transaction
         * @static
         * @param {lnrpc.Transaction.$Properties} message Transaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Transaction.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Transaction message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Transaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Transaction & lnrpc.Transaction.$Shape} Transaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Transaction.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Transaction(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.tx_hash = value;
                        else
                            delete message.tx_hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.num_confirmations = value;
                        else
                            delete message.num_confirmations;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.block_hash = value;
                        else
                            delete message.block_hash;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.block_height = value;
                        else
                            delete message.block_height;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.time_stamp = value;
                        else
                            delete message.time_stamp;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_fees = value;
                        else
                            delete message.total_fees;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.dest_addresses && message.dest_addresses.length))
                            message.dest_addresses = [];
                        message.dest_addresses.push(reader.stringVerify());
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.output_details && message.output_details.length))
                            message.output_details = [];
                        message.output_details.push($root.lnrpc.OutputDetail.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.raw_tx_hex = value;
                        else
                            delete message.raw_tx_hex;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.previous_outpoints && message.previous_outpoints.length))
                            message.previous_outpoints = [];
                        message.previous_outpoints.push($root.lnrpc.PreviousOutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Transaction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Transaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Transaction & lnrpc.Transaction.$Shape} Transaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Transaction.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Transaction message.
         * @function verify
         * @memberof lnrpc.Transaction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Transaction.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tx_hash != null && $Object.hasOwnProperty.call(message, "tx_hash"))
                if (!$util.isString(message.tx_hash))
                    return "tx_hash: string expected";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.num_confirmations != null && $Object.hasOwnProperty.call(message, "num_confirmations"))
                if (!$util.isInteger(message.num_confirmations))
                    return "num_confirmations: integer expected";
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash"))
                if (!$util.isString(message.block_hash))
                    return "block_hash: string expected";
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height"))
                if (!$util.isInteger(message.block_height))
                    return "block_height: integer expected";
            if (message.time_stamp != null && $Object.hasOwnProperty.call(message, "time_stamp"))
                if (!$util.isInteger(message.time_stamp) && !(message.time_stamp && $util.isInteger(message.time_stamp.low) && $util.isInteger(message.time_stamp.high)))
                    return "time_stamp: integer|Long expected";
            if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees"))
                if (!$util.isInteger(message.total_fees) && !(message.total_fees && $util.isInteger(message.total_fees.low) && $util.isInteger(message.total_fees.high)))
                    return "total_fees: integer|Long expected";
            if (message.dest_addresses != null && $Object.hasOwnProperty.call(message, "dest_addresses")) {
                if (!$Array.isArray(message.dest_addresses))
                    return "dest_addresses: array expected";
                for (let i = 0; i < message.dest_addresses.length; ++i)
                    if (!$util.isString(message.dest_addresses[i]))
                        return "dest_addresses: string[] expected";
            }
            if (message.output_details != null && $Object.hasOwnProperty.call(message, "output_details")) {
                if (!$Array.isArray(message.output_details))
                    return "output_details: array expected";
                for (let i = 0; i < message.output_details.length; ++i) {
                    let error = $root.lnrpc.OutputDetail.verify(message.output_details[i], _depth + 1);
                    if (error)
                        return "output_details." + error;
                }
            }
            if (message.raw_tx_hex != null && $Object.hasOwnProperty.call(message, "raw_tx_hex"))
                if (!$util.isString(message.raw_tx_hex))
                    return "raw_tx_hex: string expected";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.previous_outpoints != null && $Object.hasOwnProperty.call(message, "previous_outpoints")) {
                if (!$Array.isArray(message.previous_outpoints))
                    return "previous_outpoints: array expected";
                for (let i = 0; i < message.previous_outpoints.length; ++i) {
                    let error = $root.lnrpc.PreviousOutPoint.verify(message.previous_outpoints[i], _depth + 1);
                    if (error)
                        return "previous_outpoints." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Transaction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Transaction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Transaction} Transaction
         */
        Transaction.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Transaction)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Transaction: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Transaction();
            if (object.tx_hash != null)
                if (typeof object.tx_hash !== "string" || object.tx_hash.length)
                    message.tx_hash = $String(object.tx_hash);
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.num_confirmations != null)
                if ($Number(object.num_confirmations) !== 0)
                    message.num_confirmations = object.num_confirmations | 0;
            if (object.block_hash != null)
                if (typeof object.block_hash !== "string" || object.block_hash.length)
                    message.block_hash = $String(object.block_hash);
            if (object.block_height != null)
                if ($Number(object.block_height) !== 0)
                    message.block_height = object.block_height | 0;
            if (object.time_stamp != null)
                if (typeof object.time_stamp === "object" ? object.time_stamp.low || object.time_stamp.high : $Number(object.time_stamp) !== 0)
                    if ($util.Long)
                        message.time_stamp = $util.Long.fromValue(object.time_stamp, false);
                    else if (typeof object.time_stamp === "string")
                        message.time_stamp = $parseInt(object.time_stamp, 10);
                    else if (typeof object.time_stamp === "number")
                        message.time_stamp = object.time_stamp;
                    else if (typeof object.time_stamp === "object")
                        message.time_stamp = new $util.LongBits(object.time_stamp.low >>> 0, object.time_stamp.high >>> 0).toNumber();
            if (object.total_fees != null)
                if (typeof object.total_fees === "object" ? object.total_fees.low || object.total_fees.high : $Number(object.total_fees) !== 0)
                    if ($util.Long)
                        message.total_fees = $util.Long.fromValue(object.total_fees, false);
                    else if (typeof object.total_fees === "string")
                        message.total_fees = $parseInt(object.total_fees, 10);
                    else if (typeof object.total_fees === "number")
                        message.total_fees = object.total_fees;
                    else if (typeof object.total_fees === "object")
                        message.total_fees = new $util.LongBits(object.total_fees.low >>> 0, object.total_fees.high >>> 0).toNumber();
            if (object.dest_addresses) {
                if (!$Array.isArray(object.dest_addresses))
                    throw $TypeError(".lnrpc.Transaction.dest_addresses: array expected");
                message.dest_addresses = $Array(object.dest_addresses.length);
                for (let i = 0; i < object.dest_addresses.length; ++i)
                    message.dest_addresses[i] = $String(object.dest_addresses[i]);
            }
            if (object.output_details) {
                if (!$Array.isArray(object.output_details))
                    throw $TypeError(".lnrpc.Transaction.output_details: array expected");
                message.output_details = $Array(object.output_details.length);
                for (let i = 0; i < object.output_details.length; ++i) {
                    if (!$util.isObject(object.output_details[i]))
                        throw $TypeError(".lnrpc.Transaction.output_details: object expected");
                    message.output_details[i] = $root.lnrpc.OutputDetail.fromObject(object.output_details[i], _depth + 1);
                }
            }
            if (object.raw_tx_hex != null)
                if (typeof object.raw_tx_hex !== "string" || object.raw_tx_hex.length)
                    message.raw_tx_hex = $String(object.raw_tx_hex);
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.previous_outpoints) {
                if (!$Array.isArray(object.previous_outpoints))
                    throw $TypeError(".lnrpc.Transaction.previous_outpoints: array expected");
                message.previous_outpoints = $Array(object.previous_outpoints.length);
                for (let i = 0; i < object.previous_outpoints.length; ++i) {
                    if (!$util.isObject(object.previous_outpoints[i]))
                        throw $TypeError(".lnrpc.Transaction.previous_outpoints: object expected");
                    message.previous_outpoints[i] = $root.lnrpc.PreviousOutPoint.fromObject(object.previous_outpoints[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Transaction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Transaction
         * @static
         * @param {lnrpc.Transaction} message Transaction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Transaction.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.dest_addresses = [];
                object.output_details = [];
                object.previous_outpoints = [];
            }
            if (options.defaults) {
                object.tx_hash = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.num_confirmations = 0;
                object.block_hash = "";
                object.block_height = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.time_stamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.time_stamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_fees = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_fees = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.raw_tx_hex = "";
                object.label = "";
            }
            if (message.tx_hash != null && $Object.hasOwnProperty.call(message, "tx_hash"))
                object.tx_hash = message.tx_hash;
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.num_confirmations != null && $Object.hasOwnProperty.call(message, "num_confirmations"))
                object.num_confirmations = message.num_confirmations;
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash"))
                object.block_hash = message.block_hash;
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height"))
                object.block_height = message.block_height;
            if (message.time_stamp != null && $Object.hasOwnProperty.call(message, "time_stamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.time_stamp = typeof message.time_stamp === "number" ? $BigInt(message.time_stamp) : $util.Long.fromBits(message.time_stamp.low >>> 0, message.time_stamp.high >>> 0, false).toBigInt();
                else if (typeof message.time_stamp === "number")
                    object.time_stamp = options.longs === $String ? $String(message.time_stamp) : message.time_stamp;
                else
                    object.time_stamp = options.longs === $String ? $util.Long.prototype.toString.call(message.time_stamp) : options.longs === $Number ? new $util.LongBits(message.time_stamp.low >>> 0, message.time_stamp.high >>> 0).toNumber() : message.time_stamp;
            if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_fees = typeof message.total_fees === "number" ? $BigInt(message.total_fees) : $util.Long.fromBits(message.total_fees.low >>> 0, message.total_fees.high >>> 0, false).toBigInt();
                else if (typeof message.total_fees === "number")
                    object.total_fees = options.longs === $String ? $String(message.total_fees) : message.total_fees;
                else
                    object.total_fees = options.longs === $String ? $util.Long.prototype.toString.call(message.total_fees) : options.longs === $Number ? new $util.LongBits(message.total_fees.low >>> 0, message.total_fees.high >>> 0).toNumber() : message.total_fees;
            if (message.dest_addresses && message.dest_addresses.length) {
                object.dest_addresses = $Array(message.dest_addresses.length);
                for (let j = 0; j < message.dest_addresses.length; ++j)
                    object.dest_addresses[j] = message.dest_addresses[j];
            }
            if (message.raw_tx_hex != null && $Object.hasOwnProperty.call(message, "raw_tx_hex"))
                object.raw_tx_hex = message.raw_tx_hex;
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.output_details && message.output_details.length) {
                object.output_details = $Array(message.output_details.length);
                for (let j = 0; j < message.output_details.length; ++j)
                    object.output_details[j] = $root.lnrpc.OutputDetail.toObject(message.output_details[j], options, _depth + 1);
            }
            if (message.previous_outpoints && message.previous_outpoints.length) {
                object.previous_outpoints = $Array(message.previous_outpoints.length);
                for (let j = 0; j < message.previous_outpoints.length; ++j)
                    object.previous_outpoints[j] = $root.lnrpc.PreviousOutPoint.toObject(message.previous_outpoints[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this Transaction to JSON.
         * @function toJSON
         * @memberof lnrpc.Transaction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Transaction.prototype.toJSON = function() {
            return Transaction.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Transaction
         * @function getTypeUrl
         * @memberof lnrpc.Transaction
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Transaction.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Transaction";
        };

        return Transaction;
    })();

    lnrpc.GetTransactionsRequest = (function() {

        /**
         * Properties of a GetTransactionsRequest.
         * @typedef {Object} lnrpc.GetTransactionsRequest.$Properties
         * @property {number|null} [start_height] GetTransactionsRequest start_height
         * @property {number|null} [end_height] GetTransactionsRequest end_height
         * @property {string|null} [account] GetTransactionsRequest account
         * @property {number|null} [index_offset] GetTransactionsRequest index_offset
         * @property {number|null} [max_transactions] GetTransactionsRequest max_transactions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetTransactionsRequest.
         * @memberof lnrpc
         * @interface IGetTransactionsRequest
         * @augments lnrpc.GetTransactionsRequest.$Properties
         * @deprecated Use lnrpc.GetTransactionsRequest.$Properties instead.
         */

        /**
         * Shape of a GetTransactionsRequest.
         * @typedef {lnrpc.GetTransactionsRequest.$Properties} lnrpc.GetTransactionsRequest.$Shape
         */

        /**
         * Constructs a new GetTransactionsRequest.
         * @memberof lnrpc
         * @classdesc Represents a GetTransactionsRequest.
         * @constructor
         * @param {lnrpc.GetTransactionsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetTransactionsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetTransactionsRequest start_height.
         * @member {number} start_height
         * @memberof lnrpc.GetTransactionsRequest
         * @instance
         */
        GetTransactionsRequest.prototype.start_height = 0;

        /**
         * GetTransactionsRequest end_height.
         * @member {number} end_height
         * @memberof lnrpc.GetTransactionsRequest
         * @instance
         */
        GetTransactionsRequest.prototype.end_height = 0;

        /**
         * GetTransactionsRequest account.
         * @member {string} account
         * @memberof lnrpc.GetTransactionsRequest
         * @instance
         */
        GetTransactionsRequest.prototype.account = "";

        /**
         * GetTransactionsRequest index_offset.
         * @member {number} index_offset
         * @memberof lnrpc.GetTransactionsRequest
         * @instance
         */
        GetTransactionsRequest.prototype.index_offset = 0;

        /**
         * GetTransactionsRequest max_transactions.
         * @member {number} max_transactions
         * @memberof lnrpc.GetTransactionsRequest
         * @instance
         */
        GetTransactionsRequest.prototype.max_transactions = 0;

        /**
         * Creates a new GetTransactionsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.GetTransactionsRequest
         * @static
         * @param {lnrpc.GetTransactionsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GetTransactionsRequest} GetTransactionsRequest instance
         * @type {{
         *   (properties: lnrpc.GetTransactionsRequest.$Shape): lnrpc.GetTransactionsRequest & lnrpc.GetTransactionsRequest.$Shape;
         *   (properties?: lnrpc.GetTransactionsRequest.$Properties): lnrpc.GetTransactionsRequest;
         * }}
         */
        GetTransactionsRequest.create = function(properties) {
            return new GetTransactionsRequest(properties);
        };

        /**
         * Encodes the specified GetTransactionsRequest message. Does not implicitly {@link lnrpc.GetTransactionsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GetTransactionsRequest
         * @static
         * @param {lnrpc.GetTransactionsRequest.$Properties} message GetTransactionsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTransactionsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.start_height != null && $Object.hasOwnProperty.call(message, "start_height") && message.start_height !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.start_height);
            if (message.end_height != null && $Object.hasOwnProperty.call(message, "end_height") && message.end_height !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.end_height);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account") && message.account !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.account);
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset") && message.index_offset !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.index_offset);
            if (message.max_transactions != null && $Object.hasOwnProperty.call(message, "max_transactions") && message.max_transactions !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.max_transactions);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetTransactionsRequest message, length delimited. Does not implicitly {@link lnrpc.GetTransactionsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GetTransactionsRequest
         * @static
         * @param {lnrpc.GetTransactionsRequest.$Properties} message GetTransactionsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTransactionsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetTransactionsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GetTransactionsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GetTransactionsRequest & lnrpc.GetTransactionsRequest.$Shape} GetTransactionsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTransactionsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GetTransactionsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.start_height = value;
                        else
                            delete message.start_height;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.end_height = value;
                        else
                            delete message.end_height;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index_offset = value;
                        else
                            delete message.index_offset;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_transactions = value;
                        else
                            delete message.max_transactions;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetTransactionsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GetTransactionsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GetTransactionsRequest & lnrpc.GetTransactionsRequest.$Shape} GetTransactionsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTransactionsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetTransactionsRequest message.
         * @function verify
         * @memberof lnrpc.GetTransactionsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetTransactionsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.start_height != null && $Object.hasOwnProperty.call(message, "start_height"))
                if (!$util.isInteger(message.start_height))
                    return "start_height: integer expected";
            if (message.end_height != null && $Object.hasOwnProperty.call(message, "end_height"))
                if (!$util.isInteger(message.end_height))
                    return "end_height: integer expected";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset"))
                if (!$util.isInteger(message.index_offset))
                    return "index_offset: integer expected";
            if (message.max_transactions != null && $Object.hasOwnProperty.call(message, "max_transactions"))
                if (!$util.isInteger(message.max_transactions))
                    return "max_transactions: integer expected";
            return null;
        };

        /**
         * Creates a GetTransactionsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GetTransactionsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GetTransactionsRequest} GetTransactionsRequest
         */
        GetTransactionsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GetTransactionsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GetTransactionsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.GetTransactionsRequest();
            if (object.start_height != null)
                if ($Number(object.start_height) !== 0)
                    message.start_height = object.start_height | 0;
            if (object.end_height != null)
                if ($Number(object.end_height) !== 0)
                    message.end_height = object.end_height | 0;
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = $String(object.account);
            if (object.index_offset != null)
                if ($Number(object.index_offset) !== 0)
                    message.index_offset = object.index_offset >>> 0;
            if (object.max_transactions != null)
                if ($Number(object.max_transactions) !== 0)
                    message.max_transactions = object.max_transactions >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a GetTransactionsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GetTransactionsRequest
         * @static
         * @param {lnrpc.GetTransactionsRequest} message GetTransactionsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetTransactionsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.start_height = 0;
                object.end_height = 0;
                object.account = "";
                object.index_offset = 0;
                object.max_transactions = 0;
            }
            if (message.start_height != null && $Object.hasOwnProperty.call(message, "start_height"))
                object.start_height = message.start_height;
            if (message.end_height != null && $Object.hasOwnProperty.call(message, "end_height"))
                object.end_height = message.end_height;
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = message.account;
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset"))
                object.index_offset = message.index_offset;
            if (message.max_transactions != null && $Object.hasOwnProperty.call(message, "max_transactions"))
                object.max_transactions = message.max_transactions;
            return object;
        };

        /**
         * Converts this GetTransactionsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.GetTransactionsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetTransactionsRequest.prototype.toJSON = function() {
            return GetTransactionsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetTransactionsRequest
         * @function getTypeUrl
         * @memberof lnrpc.GetTransactionsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetTransactionsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GetTransactionsRequest";
        };

        return GetTransactionsRequest;
    })();

    lnrpc.TransactionDetails = (function() {

        /**
         * Properties of a TransactionDetails.
         * @typedef {Object} lnrpc.TransactionDetails.$Properties
         * @property {Array.<lnrpc.Transaction.$Properties>|null} [transactions] TransactionDetails transactions
         * @property {Long|null} [last_index] TransactionDetails last_index
         * @property {Long|null} [first_index] TransactionDetails first_index
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TransactionDetails.
         * @memberof lnrpc
         * @interface ITransactionDetails
         * @augments lnrpc.TransactionDetails.$Properties
         * @deprecated Use lnrpc.TransactionDetails.$Properties instead.
         */

        /**
         * Shape of a TransactionDetails.
         * @typedef {lnrpc.TransactionDetails.$Properties} lnrpc.TransactionDetails.$Shape
         */

        /**
         * Constructs a new TransactionDetails.
         * @memberof lnrpc
         * @classdesc Represents a TransactionDetails.
         * @constructor
         * @param {lnrpc.TransactionDetails.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TransactionDetails = function (properties) {
            this.transactions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TransactionDetails transactions.
         * @member {Array.<lnrpc.Transaction.$Properties>} transactions
         * @memberof lnrpc.TransactionDetails
         * @instance
         */
        TransactionDetails.prototype.transactions = $util.emptyArray;

        /**
         * TransactionDetails last_index.
         * @member {Long} last_index
         * @memberof lnrpc.TransactionDetails
         * @instance
         */
        TransactionDetails.prototype.last_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TransactionDetails first_index.
         * @member {Long} first_index
         * @memberof lnrpc.TransactionDetails
         * @instance
         */
        TransactionDetails.prototype.first_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new TransactionDetails instance using the specified properties.
         * @function create
         * @memberof lnrpc.TransactionDetails
         * @static
         * @param {lnrpc.TransactionDetails.$Properties=} [properties] Properties to set
         * @returns {lnrpc.TransactionDetails} TransactionDetails instance
         * @type {{
         *   (properties: lnrpc.TransactionDetails.$Shape): lnrpc.TransactionDetails & lnrpc.TransactionDetails.$Shape;
         *   (properties?: lnrpc.TransactionDetails.$Properties): lnrpc.TransactionDetails;
         * }}
         */
        TransactionDetails.create = function(properties) {
            return new TransactionDetails(properties);
        };

        /**
         * Encodes the specified TransactionDetails message. Does not implicitly {@link lnrpc.TransactionDetails.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.TransactionDetails
         * @static
         * @param {lnrpc.TransactionDetails.$Properties} message TransactionDetails message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionDetails.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transactions != null && message.transactions.length)
                for (let i = 0; i < message.transactions.length; ++i)
                    $root.lnrpc.Transaction.encode(message.transactions[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.last_index != null && $Object.hasOwnProperty.call(message, "last_index") && (typeof message.last_index === "object" ? message.last_index.low || message.last_index.high : message.last_index !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.last_index);
            if (message.first_index != null && $Object.hasOwnProperty.call(message, "first_index") && (typeof message.first_index === "object" ? message.first_index.low || message.first_index.high : message.first_index !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.first_index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionDetails message, length delimited. Does not implicitly {@link lnrpc.TransactionDetails.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.TransactionDetails
         * @static
         * @param {lnrpc.TransactionDetails.$Properties} message TransactionDetails message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionDetails.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TransactionDetails message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.TransactionDetails
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.TransactionDetails & lnrpc.TransactionDetails.$Shape} TransactionDetails
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionDetails.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.TransactionDetails(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.transactions && message.transactions.length))
                            message.transactions = [];
                        message.transactions.push($root.lnrpc.Transaction.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.last_index = value;
                        else
                            delete message.last_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.first_index = value;
                        else
                            delete message.first_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionDetails message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.TransactionDetails
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.TransactionDetails & lnrpc.TransactionDetails.$Shape} TransactionDetails
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionDetails.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionDetails message.
         * @function verify
         * @memberof lnrpc.TransactionDetails
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionDetails.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.transactions != null && $Object.hasOwnProperty.call(message, "transactions")) {
                if (!$Array.isArray(message.transactions))
                    return "transactions: array expected";
                for (let i = 0; i < message.transactions.length; ++i) {
                    let error = $root.lnrpc.Transaction.verify(message.transactions[i], _depth + 1);
                    if (error)
                        return "transactions." + error;
                }
            }
            if (message.last_index != null && $Object.hasOwnProperty.call(message, "last_index"))
                if (!$util.isInteger(message.last_index) && !(message.last_index && $util.isInteger(message.last_index.low) && $util.isInteger(message.last_index.high)))
                    return "last_index: integer|Long expected";
            if (message.first_index != null && $Object.hasOwnProperty.call(message, "first_index"))
                if (!$util.isInteger(message.first_index) && !(message.first_index && $util.isInteger(message.first_index.low) && $util.isInteger(message.first_index.high)))
                    return "first_index: integer|Long expected";
            return null;
        };

        /**
         * Creates a TransactionDetails message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.TransactionDetails
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.TransactionDetails} TransactionDetails
         */
        TransactionDetails.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.TransactionDetails)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.TransactionDetails: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.TransactionDetails();
            if (object.transactions) {
                if (!$Array.isArray(object.transactions))
                    throw $TypeError(".lnrpc.TransactionDetails.transactions: array expected");
                message.transactions = $Array(object.transactions.length);
                for (let i = 0; i < object.transactions.length; ++i) {
                    if (!$util.isObject(object.transactions[i]))
                        throw $TypeError(".lnrpc.TransactionDetails.transactions: object expected");
                    message.transactions[i] = $root.lnrpc.Transaction.fromObject(object.transactions[i], _depth + 1);
                }
            }
            if (object.last_index != null)
                if (typeof object.last_index === "object" ? object.last_index.low || object.last_index.high : $Number(object.last_index) !== 0)
                    if ($util.Long)
                        message.last_index = $util.Long.fromValue(object.last_index, true);
                    else if (typeof object.last_index === "string")
                        message.last_index = $parseInt(object.last_index, 10);
                    else if (typeof object.last_index === "number")
                        message.last_index = object.last_index;
                    else if (typeof object.last_index === "object")
                        message.last_index = new $util.LongBits(object.last_index.low >>> 0, object.last_index.high >>> 0).toNumber(true);
            if (object.first_index != null)
                if (typeof object.first_index === "object" ? object.first_index.low || object.first_index.high : $Number(object.first_index) !== 0)
                    if ($util.Long)
                        message.first_index = $util.Long.fromValue(object.first_index, true);
                    else if (typeof object.first_index === "string")
                        message.first_index = $parseInt(object.first_index, 10);
                    else if (typeof object.first_index === "number")
                        message.first_index = object.first_index;
                    else if (typeof object.first_index === "object")
                        message.first_index = new $util.LongBits(object.first_index.low >>> 0, object.first_index.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a TransactionDetails message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.TransactionDetails
         * @static
         * @param {lnrpc.TransactionDetails} message TransactionDetails
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionDetails.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.transactions = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.last_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.last_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.first_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.first_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.transactions && message.transactions.length) {
                object.transactions = $Array(message.transactions.length);
                for (let j = 0; j < message.transactions.length; ++j)
                    object.transactions[j] = $root.lnrpc.Transaction.toObject(message.transactions[j], options, _depth + 1);
            }
            if (message.last_index != null && $Object.hasOwnProperty.call(message, "last_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.last_index = typeof message.last_index === "number" ? $BigInt(message.last_index) : $util.Long.fromBits(message.last_index.low >>> 0, message.last_index.high >>> 0, true).toBigInt();
                else if (typeof message.last_index === "number")
                    object.last_index = options.longs === $String ? $String(message.last_index) : message.last_index;
                else
                    object.last_index = options.longs === $String ? $util.Long.prototype.toString.call(message.last_index) : options.longs === $Number ? new $util.LongBits(message.last_index.low >>> 0, message.last_index.high >>> 0).toNumber(true) : message.last_index;
            if (message.first_index != null && $Object.hasOwnProperty.call(message, "first_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.first_index = typeof message.first_index === "number" ? $BigInt(message.first_index) : $util.Long.fromBits(message.first_index.low >>> 0, message.first_index.high >>> 0, true).toBigInt();
                else if (typeof message.first_index === "number")
                    object.first_index = options.longs === $String ? $String(message.first_index) : message.first_index;
                else
                    object.first_index = options.longs === $String ? $util.Long.prototype.toString.call(message.first_index) : options.longs === $Number ? new $util.LongBits(message.first_index.low >>> 0, message.first_index.high >>> 0).toNumber(true) : message.first_index;
            return object;
        };

        /**
         * Converts this TransactionDetails to JSON.
         * @function toJSON
         * @memberof lnrpc.TransactionDetails
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionDetails.prototype.toJSON = function() {
            return TransactionDetails.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionDetails
         * @function getTypeUrl
         * @memberof lnrpc.TransactionDetails
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionDetails.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.TransactionDetails";
        };

        return TransactionDetails;
    })();

    lnrpc.FeeLimit = (function() {

        /**
         * Properties of a FeeLimit.
         * @typedef {Object} lnrpc.FeeLimit.$Properties
         * @property {Long|null} [fixed] FeeLimit fixed
         * @property {Long|null} [fixed_msat] FeeLimit fixed_msat
         * @property {Long|null} [percent] FeeLimit percent
         * @property {"fixed"|"fixed_msat"|"percent"} [limit] FeeLimit limit
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FeeLimit.
         * @memberof lnrpc
         * @interface IFeeLimit
         * @augments lnrpc.FeeLimit.$Properties
         * @deprecated Use lnrpc.FeeLimit.$Properties instead.
         */

        /**
         * Narrowed shape of a FeeLimit.
         * @typedef {{
         *   fixed?: Long|null;
         *   fixed_msat?: Long|null;
         *   percent?: Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ limit?: undefined; fixed?: null; fixed_msat?: null; percent?: null }|{ limit?: "fixed"; fixed: Long; fixed_msat?: null; percent?: null }|{ limit?: "fixed_msat"; fixed?: null; fixed_msat: Long; percent?: null }|{ limit?: "percent"; fixed?: null; fixed_msat?: null; percent: Long })
         * )} lnrpc.FeeLimit.$Shape
         */

        /**
         * Constructs a new FeeLimit.
         * @memberof lnrpc
         * @classdesc Represents a FeeLimit.
         * @constructor
         * @param {lnrpc.FeeLimit.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FeeLimit = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FeeLimit fixed.
         * @member {Long|null|undefined} fixed
         * @memberof lnrpc.FeeLimit
         * @instance
         */
        FeeLimit.prototype.fixed = null;

        /**
         * FeeLimit fixed_msat.
         * @member {Long|null|undefined} fixed_msat
         * @memberof lnrpc.FeeLimit
         * @instance
         */
        FeeLimit.prototype.fixed_msat = null;

        /**
         * FeeLimit percent.
         * @member {Long|null|undefined} percent
         * @memberof lnrpc.FeeLimit
         * @instance
         */
        FeeLimit.prototype.percent = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * FeeLimit limit.
         * @member {"fixed"|"fixed_msat"|"percent"|undefined} limit
         * @memberof lnrpc.FeeLimit
         * @instance
         */
        $Object.defineProperty(FeeLimit.prototype, "limit", {
            get: $util.oneOfGetter($oneOfFields = ["fixed", "fixed_msat", "percent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new FeeLimit instance using the specified properties.
         * @function create
         * @memberof lnrpc.FeeLimit
         * @static
         * @param {lnrpc.FeeLimit.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FeeLimit} FeeLimit instance
         * @type {{
         *   (properties: lnrpc.FeeLimit.$Shape): lnrpc.FeeLimit & lnrpc.FeeLimit.$Shape;
         *   (properties?: lnrpc.FeeLimit.$Properties): lnrpc.FeeLimit;
         * }}
         */
        FeeLimit.create = function(properties) {
            return new FeeLimit(properties);
        };

        /**
         * Encodes the specified FeeLimit message. Does not implicitly {@link lnrpc.FeeLimit.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FeeLimit
         * @static
         * @param {lnrpc.FeeLimit.$Properties} message FeeLimit message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeLimit.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fixed != null && $Object.hasOwnProperty.call(message, "fixed"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.fixed);
            if (message.percent != null && $Object.hasOwnProperty.call(message, "percent"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.percent);
            if (message.fixed_msat != null && $Object.hasOwnProperty.call(message, "fixed_msat"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.fixed_msat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FeeLimit message, length delimited. Does not implicitly {@link lnrpc.FeeLimit.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FeeLimit
         * @static
         * @param {lnrpc.FeeLimit.$Properties} message FeeLimit message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeLimit.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FeeLimit message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FeeLimit
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FeeLimit & lnrpc.FeeLimit.$Shape} FeeLimit
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeLimit.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FeeLimit();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        message.fixed = reader.int64();
                        message.limit = "fixed";
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        message.fixed_msat = reader.int64();
                        message.limit = "fixed_msat";
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        message.percent = reader.int64();
                        message.limit = "percent";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FeeLimit message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FeeLimit
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FeeLimit & lnrpc.FeeLimit.$Shape} FeeLimit
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeLimit.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeeLimit message.
         * @function verify
         * @memberof lnrpc.FeeLimit
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeeLimit.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.fixed != null && $Object.hasOwnProperty.call(message, "fixed")) {
                properties.limit = 1;
                if (!$util.isInteger(message.fixed) && !(message.fixed && $util.isInteger(message.fixed.low) && $util.isInteger(message.fixed.high)))
                    return "fixed: integer|Long expected";
            }
            if (message.fixed_msat != null && $Object.hasOwnProperty.call(message, "fixed_msat")) {
                if (properties.limit === 1)
                    return "limit: multiple values";
                properties.limit = 1;
                if (!$util.isInteger(message.fixed_msat) && !(message.fixed_msat && $util.isInteger(message.fixed_msat.low) && $util.isInteger(message.fixed_msat.high)))
                    return "fixed_msat: integer|Long expected";
            }
            if (message.percent != null && $Object.hasOwnProperty.call(message, "percent")) {
                if (properties.limit === 1)
                    return "limit: multiple values";
                properties.limit = 1;
                if (!$util.isInteger(message.percent) && !(message.percent && $util.isInteger(message.percent.low) && $util.isInteger(message.percent.high)))
                    return "percent: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a FeeLimit message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FeeLimit
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FeeLimit} FeeLimit
         */
        FeeLimit.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FeeLimit)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FeeLimit: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.FeeLimit();
            if (object.fixed != null)
                if ($util.Long)
                    message.fixed = $util.Long.fromValue(object.fixed, false);
                else if (typeof object.fixed === "string")
                    message.fixed = $parseInt(object.fixed, 10);
                else if (typeof object.fixed === "number")
                    message.fixed = object.fixed;
                else if (typeof object.fixed === "object")
                    message.fixed = new $util.LongBits(object.fixed.low >>> 0, object.fixed.high >>> 0).toNumber();
            if (object.fixed_msat != null)
                if ($util.Long)
                    message.fixed_msat = $util.Long.fromValue(object.fixed_msat, false);
                else if (typeof object.fixed_msat === "string")
                    message.fixed_msat = $parseInt(object.fixed_msat, 10);
                else if (typeof object.fixed_msat === "number")
                    message.fixed_msat = object.fixed_msat;
                else if (typeof object.fixed_msat === "object")
                    message.fixed_msat = new $util.LongBits(object.fixed_msat.low >>> 0, object.fixed_msat.high >>> 0).toNumber();
            if (object.percent != null)
                if ($util.Long)
                    message.percent = $util.Long.fromValue(object.percent, false);
                else if (typeof object.percent === "string")
                    message.percent = $parseInt(object.percent, 10);
                else if (typeof object.percent === "number")
                    message.percent = object.percent;
                else if (typeof object.percent === "object")
                    message.percent = new $util.LongBits(object.percent.low >>> 0, object.percent.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a FeeLimit message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FeeLimit
         * @static
         * @param {lnrpc.FeeLimit} message FeeLimit
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeeLimit.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.fixed != null && $Object.hasOwnProperty.call(message, "fixed")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fixed = typeof message.fixed === "number" ? $BigInt(message.fixed) : $util.Long.fromBits(message.fixed.low >>> 0, message.fixed.high >>> 0, false).toBigInt();
                else if (typeof message.fixed === "number")
                    object.fixed = options.longs === $String ? $String(message.fixed) : message.fixed;
                else
                    object.fixed = options.longs === $String ? $util.Long.prototype.toString.call(message.fixed) : options.longs === $Number ? new $util.LongBits(message.fixed.low >>> 0, message.fixed.high >>> 0).toNumber() : message.fixed;
                if (options.oneofs)
                    object.limit = "fixed";
            }
            if (message.percent != null && $Object.hasOwnProperty.call(message, "percent")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.percent = typeof message.percent === "number" ? $BigInt(message.percent) : $util.Long.fromBits(message.percent.low >>> 0, message.percent.high >>> 0, false).toBigInt();
                else if (typeof message.percent === "number")
                    object.percent = options.longs === $String ? $String(message.percent) : message.percent;
                else
                    object.percent = options.longs === $String ? $util.Long.prototype.toString.call(message.percent) : options.longs === $Number ? new $util.LongBits(message.percent.low >>> 0, message.percent.high >>> 0).toNumber() : message.percent;
                if (options.oneofs)
                    object.limit = "percent";
            }
            if (message.fixed_msat != null && $Object.hasOwnProperty.call(message, "fixed_msat")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fixed_msat = typeof message.fixed_msat === "number" ? $BigInt(message.fixed_msat) : $util.Long.fromBits(message.fixed_msat.low >>> 0, message.fixed_msat.high >>> 0, false).toBigInt();
                else if (typeof message.fixed_msat === "number")
                    object.fixed_msat = options.longs === $String ? $String(message.fixed_msat) : message.fixed_msat;
                else
                    object.fixed_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.fixed_msat) : options.longs === $Number ? new $util.LongBits(message.fixed_msat.low >>> 0, message.fixed_msat.high >>> 0).toNumber() : message.fixed_msat;
                if (options.oneofs)
                    object.limit = "fixed_msat";
            }
            return object;
        };

        /**
         * Converts this FeeLimit to JSON.
         * @function toJSON
         * @memberof lnrpc.FeeLimit
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeeLimit.prototype.toJSON = function() {
            return FeeLimit.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FeeLimit
         * @function getTypeUrl
         * @memberof lnrpc.FeeLimit
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FeeLimit.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FeeLimit";
        };

        return FeeLimit;
    })();

    lnrpc.SendRequest = (function() {

        /**
         * Properties of a SendRequest.
         * @typedef {Object} lnrpc.SendRequest.$Properties
         * @property {Uint8Array|null} [dest] SendRequest dest
         * @property {string|null} [dest_string] SendRequest dest_string
         * @property {Long|null} [amt] SendRequest amt
         * @property {Long|null} [amt_msat] SendRequest amt_msat
         * @property {Uint8Array|null} [payment_hash] SendRequest payment_hash
         * @property {string|null} [payment_hash_string] SendRequest payment_hash_string
         * @property {string|null} [payment_request] SendRequest payment_request
         * @property {number|null} [final_cltv_delta] SendRequest final_cltv_delta
         * @property {lnrpc.FeeLimit.$Properties|null} [fee_limit] SendRequest fee_limit
         * @property {Long|null} [outgoing_chan_id] SendRequest outgoing_chan_id
         * @property {Uint8Array|null} [last_hop_pubkey] SendRequest last_hop_pubkey
         * @property {number|null} [cltv_limit] SendRequest cltv_limit
         * @property {Object.<string,Uint8Array>|null} [dest_custom_records] SendRequest dest_custom_records
         * @property {boolean|null} [allow_self_payment] SendRequest allow_self_payment
         * @property {Array.<lnrpc.FeatureBit>|null} [dest_features] SendRequest dest_features
         * @property {Uint8Array|null} [payment_addr] SendRequest payment_addr
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendRequest.
         * @memberof lnrpc
         * @interface ISendRequest
         * @augments lnrpc.SendRequest.$Properties
         * @deprecated Use lnrpc.SendRequest.$Properties instead.
         */

        /**
         * Shape of a SendRequest.
         * @typedef {{
         *   dest?: Uint8Array|null;
         *   dest_string?: string|null;
         *   amt?: Long|null;
         *   amt_msat?: Long|null;
         *   payment_hash?: Uint8Array|null;
         *   payment_hash_string?: string|null;
         *   payment_request?: string|null;
         *   final_cltv_delta?: number|null;
         *   fee_limit?: lnrpc.FeeLimit.$Shape|null;
         *   outgoing_chan_id?: Long|null;
         *   last_hop_pubkey?: Uint8Array|null;
         *   cltv_limit?: number|null;
         *   dest_custom_records?: Object.<string,Uint8Array>|null;
         *   allow_self_payment?: boolean|null;
         *   dest_features?: Array.<lnrpc.FeatureBit>|null;
         *   payment_addr?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.SendRequest.$Shape
         */

        /**
         * Constructs a new SendRequest.
         * @memberof lnrpc
         * @classdesc Represents a SendRequest.
         * @constructor
         * @param {lnrpc.SendRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendRequest = function (properties) {
            this.dest_custom_records = {};
            this.dest_features = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendRequest dest.
         * @member {Uint8Array} dest
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.dest = $util.newBuffer([]);

        /**
         * SendRequest dest_string.
         * @member {string} dest_string
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.dest_string = "";

        /**
         * SendRequest amt.
         * @member {Long} amt
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.amt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendRequest amt_msat.
         * @member {Long} amt_msat
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.amt_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendRequest payment_hash.
         * @member {Uint8Array} payment_hash
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.payment_hash = $util.newBuffer([]);

        /**
         * SendRequest payment_hash_string.
         * @member {string} payment_hash_string
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.payment_hash_string = "";

        /**
         * SendRequest payment_request.
         * @member {string} payment_request
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.payment_request = "";

        /**
         * SendRequest final_cltv_delta.
         * @member {number} final_cltv_delta
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.final_cltv_delta = 0;

        /**
         * SendRequest fee_limit.
         * @member {lnrpc.FeeLimit.$Properties|null|undefined} fee_limit
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.fee_limit = null;

        /**
         * SendRequest outgoing_chan_id.
         * @member {Long} outgoing_chan_id
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.outgoing_chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * SendRequest last_hop_pubkey.
         * @member {Uint8Array} last_hop_pubkey
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.last_hop_pubkey = $util.newBuffer([]);

        /**
         * SendRequest cltv_limit.
         * @member {number} cltv_limit
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.cltv_limit = 0;

        /**
         * SendRequest dest_custom_records.
         * @member {Object.<string,Uint8Array>} dest_custom_records
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.dest_custom_records = $util.emptyObject;

        /**
         * SendRequest allow_self_payment.
         * @member {boolean} allow_self_payment
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.allow_self_payment = false;

        /**
         * SendRequest dest_features.
         * @member {Array.<lnrpc.FeatureBit>} dest_features
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.dest_features = $util.emptyArray;

        /**
         * SendRequest payment_addr.
         * @member {Uint8Array} payment_addr
         * @memberof lnrpc.SendRequest
         * @instance
         */
        SendRequest.prototype.payment_addr = $util.newBuffer([]);

        /**
         * Creates a new SendRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.SendRequest
         * @static
         * @param {lnrpc.SendRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SendRequest} SendRequest instance
         * @type {{
         *   (properties: lnrpc.SendRequest.$Shape): lnrpc.SendRequest & lnrpc.SendRequest.$Shape;
         *   (properties?: lnrpc.SendRequest.$Properties): lnrpc.SendRequest;
         * }}
         */
        SendRequest.create = function(properties) {
            return new SendRequest(properties);
        };

        /**
         * Encodes the specified SendRequest message. Does not implicitly {@link lnrpc.SendRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SendRequest
         * @static
         * @param {lnrpc.SendRequest.$Properties} message SendRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.dest != null && $Object.hasOwnProperty.call(message, "dest") && message.dest.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.dest);
            if (message.dest_string != null && $Object.hasOwnProperty.call(message, "dest_string") && message.dest_string !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.dest_string);
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt") && (typeof message.amt === "object" ? message.amt.low || message.amt.high : message.amt !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.amt);
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.payment_hash);
            if (message.payment_hash_string != null && $Object.hasOwnProperty.call(message, "payment_hash_string") && message.payment_hash_string !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.payment_hash_string);
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request") && message.payment_request !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.payment_request);
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta") && message.final_cltv_delta !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.final_cltv_delta);
            if (message.fee_limit != null && $Object.hasOwnProperty.call(message, "fee_limit"))
                $root.lnrpc.FeeLimit.encode(message.fee_limit, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id") && (typeof message.outgoing_chan_id === "object" ? message.outgoing_chan_id.low || message.outgoing_chan_id.high : message.outgoing_chan_id !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.outgoing_chan_id);
            if (message.cltv_limit != null && $Object.hasOwnProperty.call(message, "cltv_limit") && message.cltv_limit !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.cltv_limit);
            if (message.dest_custom_records != null && $Object.hasOwnProperty.call(message, "dest_custom_records"))
                for (let keys = $Object.keys(message.dest_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 11, wireType 2 =*/90).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.dest_custom_records[keys[i]]).ldelim();
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat") && (typeof message.amt_msat === "object" ? message.amt_msat.low || message.amt_msat.high : message.amt_msat !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.amt_msat);
            if (message.last_hop_pubkey != null && $Object.hasOwnProperty.call(message, "last_hop_pubkey") && message.last_hop_pubkey.length)
                writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.last_hop_pubkey);
            if (message.allow_self_payment != null && $Object.hasOwnProperty.call(message, "allow_self_payment") && message.allow_self_payment !== false)
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.allow_self_payment);
            if (message.dest_features != null && message.dest_features.length)
                writer.uint32(/* id 15, wireType 2 =*/122).int32s(message.dest_features);
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr") && message.payment_addr.length)
                writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.payment_addr);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendRequest message, length delimited. Does not implicitly {@link lnrpc.SendRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SendRequest
         * @static
         * @param {lnrpc.SendRequest.$Properties} message SendRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SendRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SendRequest & lnrpc.SendRequest.$Shape} SendRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SendRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.dest = value;
                        else
                            delete message.dest;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.dest_string = value;
                        else
                            delete message.dest_string;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt = value;
                        else
                            delete message.amt;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_msat = value;
                        else
                            delete message.amt_msat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_hash_string = value;
                        else
                            delete message.payment_hash_string;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_request = value;
                        else
                            delete message.payment_request;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.final_cltv_delta = value;
                        else
                            delete message.final_cltv_delta;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.fee_limit = $root.lnrpc.FeeLimit.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fee_limit);
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.outgoing_chan_id = value;
                        else
                            delete message.outgoing_chan_id;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.last_hop_pubkey = value;
                        else
                            delete message.last_hop_pubkey;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.cltv_limit = value;
                        else
                            delete message.cltv_limit;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (message.dest_custom_records === $util.emptyObject)
                            message.dest_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.dest_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.allow_self_payment = value;
                        else
                            delete message.allow_self_payment;
                        continue;
                    }
                case 15: {
                        if (wireType === 2) {
                            if (!(message.dest_features && message.dest_features.length))
                                message.dest_features = [];
                            reader.int32s(message.dest_features);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.dest_features && message.dest_features.length))
                            message.dest_features = [];
                        message.dest_features.push(reader.int32());
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_addr = value;
                        else
                            delete message.payment_addr;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SendRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SendRequest & lnrpc.SendRequest.$Shape} SendRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendRequest message.
         * @function verify
         * @memberof lnrpc.SendRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dest != null && $Object.hasOwnProperty.call(message, "dest"))
                if (!(message.dest && typeof message.dest.length === "number" || $util.isString(message.dest)))
                    return "dest: buffer expected";
            if (message.dest_string != null && $Object.hasOwnProperty.call(message, "dest_string"))
                if (!$util.isString(message.dest_string))
                    return "dest_string: string expected";
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt"))
                if (!$util.isInteger(message.amt) && !(message.amt && $util.isInteger(message.amt.low) && $util.isInteger(message.amt.high)))
                    return "amt: integer|Long expected";
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (!$util.isInteger(message.amt_msat) && !(message.amt_msat && $util.isInteger(message.amt_msat.low) && $util.isInteger(message.amt_msat.high)))
                    return "amt_msat: integer|Long expected";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            if (message.payment_hash_string != null && $Object.hasOwnProperty.call(message, "payment_hash_string"))
                if (!$util.isString(message.payment_hash_string))
                    return "payment_hash_string: string expected";
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                if (!$util.isString(message.payment_request))
                    return "payment_request: string expected";
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta"))
                if (!$util.isInteger(message.final_cltv_delta))
                    return "final_cltv_delta: integer expected";
            if (message.fee_limit != null && $Object.hasOwnProperty.call(message, "fee_limit")) {
                let error = $root.lnrpc.FeeLimit.verify(message.fee_limit, _depth + 1);
                if (error)
                    return "fee_limit." + error;
            }
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id"))
                if (!$util.isInteger(message.outgoing_chan_id) && !(message.outgoing_chan_id && $util.isInteger(message.outgoing_chan_id.low) && $util.isInteger(message.outgoing_chan_id.high)))
                    return "outgoing_chan_id: integer|Long expected";
            if (message.last_hop_pubkey != null && $Object.hasOwnProperty.call(message, "last_hop_pubkey"))
                if (!(message.last_hop_pubkey && typeof message.last_hop_pubkey.length === "number" || $util.isString(message.last_hop_pubkey)))
                    return "last_hop_pubkey: buffer expected";
            if (message.cltv_limit != null && $Object.hasOwnProperty.call(message, "cltv_limit"))
                if (!$util.isInteger(message.cltv_limit))
                    return "cltv_limit: integer expected";
            if (message.dest_custom_records != null && $Object.hasOwnProperty.call(message, "dest_custom_records")) {
                if (!$util.isObject(message.dest_custom_records))
                    return "dest_custom_records: object expected";
                let key = $Object.keys(message.dest_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "dest_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.dest_custom_records[key[i]] && typeof message.dest_custom_records[key[i]].length === "number" || $util.isString(message.dest_custom_records[key[i]])))
                        return "dest_custom_records: buffer{k:uint64} expected";
                }
            }
            if (message.allow_self_payment != null && $Object.hasOwnProperty.call(message, "allow_self_payment"))
                if (typeof message.allow_self_payment !== "boolean")
                    return "allow_self_payment: boolean expected";
            if (message.dest_features != null && $Object.hasOwnProperty.call(message, "dest_features")) {
                if (!$Array.isArray(message.dest_features))
                    return "dest_features: array expected";
                for (let i = 0; i < message.dest_features.length; ++i)
                    if (typeof message.dest_features[i] !== "number" || (message.dest_features[i] | 0) !== message.dest_features[i])
                        return "dest_features: enum value[] expected";
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                if (!(message.payment_addr && typeof message.payment_addr.length === "number" || $util.isString(message.payment_addr)))
                    return "payment_addr: buffer expected";
            return null;
        };

        /**
         * Creates a SendRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SendRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SendRequest} SendRequest
         */
        SendRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SendRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SendRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SendRequest();
            if (object.dest != null)
                if (object.dest.length)
                    if (typeof object.dest === "string")
                        $util.base64.decode(object.dest, message.dest = $util.newBuffer($util.base64.length(object.dest)), 0);
                    else if (object.dest.length >= 0)
                        message.dest = object.dest;
            if (object.dest_string != null)
                if (typeof object.dest_string !== "string" || object.dest_string.length)
                    message.dest_string = $String(object.dest_string);
            if (object.amt != null)
                if (typeof object.amt === "object" ? object.amt.low || object.amt.high : $Number(object.amt) !== 0)
                    if ($util.Long)
                        message.amt = $util.Long.fromValue(object.amt, false);
                    else if (typeof object.amt === "string")
                        message.amt = $parseInt(object.amt, 10);
                    else if (typeof object.amt === "number")
                        message.amt = object.amt;
                    else if (typeof object.amt === "object")
                        message.amt = new $util.LongBits(object.amt.low >>> 0, object.amt.high >>> 0).toNumber();
            if (object.amt_msat != null)
                if (typeof object.amt_msat === "object" ? object.amt_msat.low || object.amt_msat.high : $Number(object.amt_msat) !== 0)
                    if ($util.Long)
                        message.amt_msat = $util.Long.fromValue(object.amt_msat, false);
                    else if (typeof object.amt_msat === "string")
                        message.amt_msat = $parseInt(object.amt_msat, 10);
                    else if (typeof object.amt_msat === "number")
                        message.amt_msat = object.amt_msat;
                    else if (typeof object.amt_msat === "object")
                        message.amt_msat = new $util.LongBits(object.amt_msat.low >>> 0, object.amt_msat.high >>> 0).toNumber();
            if (object.payment_hash != null)
                if (object.payment_hash.length)
                    if (typeof object.payment_hash === "string")
                        $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                    else if (object.payment_hash.length >= 0)
                        message.payment_hash = object.payment_hash;
            if (object.payment_hash_string != null)
                if (typeof object.payment_hash_string !== "string" || object.payment_hash_string.length)
                    message.payment_hash_string = $String(object.payment_hash_string);
            if (object.payment_request != null)
                if (typeof object.payment_request !== "string" || object.payment_request.length)
                    message.payment_request = $String(object.payment_request);
            if (object.final_cltv_delta != null)
                if ($Number(object.final_cltv_delta) !== 0)
                    message.final_cltv_delta = object.final_cltv_delta | 0;
            if (object.fee_limit != null) {
                if (!$util.isObject(object.fee_limit))
                    throw $TypeError(".lnrpc.SendRequest.fee_limit: object expected");
                message.fee_limit = $root.lnrpc.FeeLimit.fromObject(object.fee_limit, _depth + 1);
            }
            if (object.outgoing_chan_id != null)
                if (typeof object.outgoing_chan_id === "object" ? object.outgoing_chan_id.low || object.outgoing_chan_id.high : $Number(object.outgoing_chan_id) !== 0)
                    if ($util.Long)
                        message.outgoing_chan_id = $util.Long.fromValue(object.outgoing_chan_id, true);
                    else if (typeof object.outgoing_chan_id === "string")
                        message.outgoing_chan_id = $parseInt(object.outgoing_chan_id, 10);
                    else if (typeof object.outgoing_chan_id === "number")
                        message.outgoing_chan_id = object.outgoing_chan_id;
                    else if (typeof object.outgoing_chan_id === "object")
                        message.outgoing_chan_id = new $util.LongBits(object.outgoing_chan_id.low >>> 0, object.outgoing_chan_id.high >>> 0).toNumber(true);
            if (object.last_hop_pubkey != null)
                if (object.last_hop_pubkey.length)
                    if (typeof object.last_hop_pubkey === "string")
                        $util.base64.decode(object.last_hop_pubkey, message.last_hop_pubkey = $util.newBuffer($util.base64.length(object.last_hop_pubkey)), 0);
                    else if (object.last_hop_pubkey.length >= 0)
                        message.last_hop_pubkey = object.last_hop_pubkey;
            if (object.cltv_limit != null)
                if ($Number(object.cltv_limit) !== 0)
                    message.cltv_limit = object.cltv_limit >>> 0;
            if (object.dest_custom_records) {
                if (!$util.isObject(object.dest_custom_records))
                    throw $TypeError(".lnrpc.SendRequest.dest_custom_records: object expected");
                message.dest_custom_records = {};
                for (let keys = $Object.keys(object.dest_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.dest_custom_records, keys[i]);
                    if (typeof object.dest_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.dest_custom_records[keys[i]], message.dest_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.dest_custom_records[keys[i]])), 0);
                    else if (object.dest_custom_records[keys[i]].length >= 0)
                        message.dest_custom_records[keys[i]] = object.dest_custom_records[keys[i]];
                }
            }
            if (object.allow_self_payment != null)
                if (object.allow_self_payment)
                    message.allow_self_payment = $Boolean(object.allow_self_payment);
            if (object.dest_features) {
                if (!$Array.isArray(object.dest_features))
                    throw $TypeError(".lnrpc.SendRequest.dest_features: array expected");
                message.dest_features = [];
                for (let i = 0; i < object.dest_features.length; ++i)
                    switch (object.dest_features[i]) {
                    case "DATALOSS_PROTECT_REQ":
                    case 0:
                        message.dest_features[message.dest_features.length] = 0;
                        break;
                    case "DATALOSS_PROTECT_OPT":
                    case 1:
                        message.dest_features[message.dest_features.length] = 1;
                        break;
                    case "INITIAL_ROUING_SYNC":
                    case 3:
                        message.dest_features[message.dest_features.length] = 3;
                        break;
                    case "UPFRONT_SHUTDOWN_SCRIPT_REQ":
                    case 4:
                        message.dest_features[message.dest_features.length] = 4;
                        break;
                    case "UPFRONT_SHUTDOWN_SCRIPT_OPT":
                    case 5:
                        message.dest_features[message.dest_features.length] = 5;
                        break;
                    case "GOSSIP_QUERIES_REQ":
                    case 6:
                        message.dest_features[message.dest_features.length] = 6;
                        break;
                    case "GOSSIP_QUERIES_OPT":
                    case 7:
                        message.dest_features[message.dest_features.length] = 7;
                        break;
                    case "TLV_ONION_REQ":
                    case 8:
                        message.dest_features[message.dest_features.length] = 8;
                        break;
                    case "TLV_ONION_OPT":
                    case 9:
                        message.dest_features[message.dest_features.length] = 9;
                        break;
                    case "EXT_GOSSIP_QUERIES_REQ":
                    case 10:
                        message.dest_features[message.dest_features.length] = 10;
                        break;
                    case "EXT_GOSSIP_QUERIES_OPT":
                    case 11:
                        message.dest_features[message.dest_features.length] = 11;
                        break;
                    case "STATIC_REMOTE_KEY_REQ":
                    case 12:
                        message.dest_features[message.dest_features.length] = 12;
                        break;
                    case "STATIC_REMOTE_KEY_OPT":
                    case 13:
                        message.dest_features[message.dest_features.length] = 13;
                        break;
                    case "PAYMENT_ADDR_REQ":
                    case 14:
                        message.dest_features[message.dest_features.length] = 14;
                        break;
                    case "PAYMENT_ADDR_OPT":
                    case 15:
                        message.dest_features[message.dest_features.length] = 15;
                        break;
                    case "MPP_REQ":
                    case 16:
                        message.dest_features[message.dest_features.length] = 16;
                        break;
                    case "MPP_OPT":
                    case 17:
                        message.dest_features[message.dest_features.length] = 17;
                        break;
                    case "WUMBO_CHANNELS_REQ":
                    case 18:
                        message.dest_features[message.dest_features.length] = 18;
                        break;
                    case "WUMBO_CHANNELS_OPT":
                    case 19:
                        message.dest_features[message.dest_features.length] = 19;
                        break;
                    case "ANCHORS_REQ":
                    case 20:
                        message.dest_features[message.dest_features.length] = 20;
                        break;
                    case "ANCHORS_OPT":
                    case 21:
                        message.dest_features[message.dest_features.length] = 21;
                        break;
                    case "ANCHORS_ZERO_FEE_HTLC_REQ":
                    case 22:
                        message.dest_features[message.dest_features.length] = 22;
                        break;
                    case "ANCHORS_ZERO_FEE_HTLC_OPT":
                    case 23:
                        message.dest_features[message.dest_features.length] = 23;
                        break;
                    case "ROUTE_BLINDING_REQUIRED":
                    case 24:
                        message.dest_features[message.dest_features.length] = 24;
                        break;
                    case "ROUTE_BLINDING_OPTIONAL":
                    case 25:
                        message.dest_features[message.dest_features.length] = 25;
                        break;
                    case "AMP_REQ":
                    case 30:
                        message.dest_features[message.dest_features.length] = 30;
                        break;
                    case "AMP_OPT":
                    case 31:
                        message.dest_features[message.dest_features.length] = 31;
                        break;
                    default:
                        if (typeof object.dest_features[i] === "number" && (object.dest_features[i] | 0) === object.dest_features[i])
                            message.dest_features[message.dest_features.length] = object.dest_features[i];
                    }
            }
            if (object.payment_addr != null)
                if (object.payment_addr.length)
                    if (typeof object.payment_addr === "string")
                        $util.base64.decode(object.payment_addr, message.payment_addr = $util.newBuffer($util.base64.length(object.payment_addr)), 0);
                    else if (object.payment_addr.length >= 0)
                        message.payment_addr = object.payment_addr;
            return message;
        };

        /**
         * Creates a plain object from a SendRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SendRequest
         * @static
         * @param {lnrpc.SendRequest} message SendRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.dest_features = [];
            if (options.objects || options.defaults)
                object.dest_custom_records = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.dest = "";
                else {
                    object.dest = [];
                    if (options.bytes !== $Array)
                        object.dest = $util.newBuffer(object.dest);
                }
                object.dest_string = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.payment_hash = "";
                else {
                    object.payment_hash = [];
                    if (options.bytes !== $Array)
                        object.payment_hash = $util.newBuffer(object.payment_hash);
                }
                object.payment_hash_string = "";
                object.payment_request = "";
                object.final_cltv_delta = 0;
                object.fee_limit = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.outgoing_chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.outgoing_chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.cltv_limit = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.last_hop_pubkey = "";
                else {
                    object.last_hop_pubkey = [];
                    if (options.bytes !== $Array)
                        object.last_hop_pubkey = $util.newBuffer(object.last_hop_pubkey);
                }
                object.allow_self_payment = false;
                if (options.bytes === $String)
                    object.payment_addr = "";
                else {
                    object.payment_addr = [];
                    if (options.bytes !== $Array)
                        object.payment_addr = $util.newBuffer(object.payment_addr);
                }
            }
            if (message.dest != null && $Object.hasOwnProperty.call(message, "dest"))
                object.dest = options.bytes === $String ? $util.base64.encode(message.dest, 0, message.dest.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.dest) : message.dest;
            if (message.dest_string != null && $Object.hasOwnProperty.call(message, "dest_string"))
                object.dest_string = message.dest_string;
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt = typeof message.amt === "number" ? $BigInt(message.amt) : $util.Long.fromBits(message.amt.low >>> 0, message.amt.high >>> 0, false).toBigInt();
                else if (typeof message.amt === "number")
                    object.amt = options.longs === $String ? $String(message.amt) : message.amt;
                else
                    object.amt = options.longs === $String ? $util.Long.prototype.toString.call(message.amt) : options.longs === $Number ? new $util.LongBits(message.amt.low >>> 0, message.amt.high >>> 0).toNumber() : message.amt;
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
            if (message.payment_hash_string != null && $Object.hasOwnProperty.call(message, "payment_hash_string"))
                object.payment_hash_string = message.payment_hash_string;
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                object.payment_request = message.payment_request;
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta"))
                object.final_cltv_delta = message.final_cltv_delta;
            if (message.fee_limit != null && $Object.hasOwnProperty.call(message, "fee_limit"))
                object.fee_limit = $root.lnrpc.FeeLimit.toObject(message.fee_limit, options, _depth + 1);
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_chan_id = typeof message.outgoing_chan_id === "number" ? $BigInt(message.outgoing_chan_id) : $util.Long.fromBits(message.outgoing_chan_id.low >>> 0, message.outgoing_chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_chan_id === "number")
                    object.outgoing_chan_id = options.longs === $String ? $String(message.outgoing_chan_id) : message.outgoing_chan_id;
                else
                    object.outgoing_chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_chan_id) : options.longs === $Number ? new $util.LongBits(message.outgoing_chan_id.low >>> 0, message.outgoing_chan_id.high >>> 0).toNumber(true) : message.outgoing_chan_id;
            if (message.cltv_limit != null && $Object.hasOwnProperty.call(message, "cltv_limit"))
                object.cltv_limit = message.cltv_limit;
            let keys2;
            if (message.dest_custom_records && (keys2 = $Object.keys(message.dest_custom_records)).length) {
                object.dest_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.dest_custom_records, keys2[j]);
                    object.dest_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.dest_custom_records[keys2[j]], 0, message.dest_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.dest_custom_records[keys2[j]]) : message.dest_custom_records[keys2[j]];
                }
            }
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_msat = typeof message.amt_msat === "number" ? $BigInt(message.amt_msat) : $util.Long.fromBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_msat === "number")
                    object.amt_msat = options.longs === $String ? $String(message.amt_msat) : message.amt_msat;
                else
                    object.amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_msat) : options.longs === $Number ? new $util.LongBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0).toNumber() : message.amt_msat;
            if (message.last_hop_pubkey != null && $Object.hasOwnProperty.call(message, "last_hop_pubkey"))
                object.last_hop_pubkey = options.bytes === $String ? $util.base64.encode(message.last_hop_pubkey, 0, message.last_hop_pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.last_hop_pubkey) : message.last_hop_pubkey;
            if (message.allow_self_payment != null && $Object.hasOwnProperty.call(message, "allow_self_payment"))
                object.allow_self_payment = message.allow_self_payment;
            if (message.dest_features && message.dest_features.length) {
                object.dest_features = $Array(message.dest_features.length);
                for (let j = 0; j < message.dest_features.length; ++j)
                    object.dest_features[j] = options.enums === $String ? $root.lnrpc.FeatureBit[message.dest_features[j]] === $undefined ? message.dest_features[j] : $root.lnrpc.FeatureBit[message.dest_features[j]] : message.dest_features[j];
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                object.payment_addr = options.bytes === $String ? $util.base64.encode(message.payment_addr, 0, message.payment_addr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_addr) : message.payment_addr;
            return object;
        };

        /**
         * Converts this SendRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.SendRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendRequest.prototype.toJSON = function() {
            return SendRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendRequest
         * @function getTypeUrl
         * @memberof lnrpc.SendRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SendRequest";
        };

        return SendRequest;
    })();

    lnrpc.SendResponse = (function() {

        /**
         * Properties of a SendResponse.
         * @typedef {Object} lnrpc.SendResponse.$Properties
         * @property {string|null} [payment_error] SendResponse payment_error
         * @property {Uint8Array|null} [payment_preimage] SendResponse payment_preimage
         * @property {lnrpc.Route.$Properties|null} [payment_route] SendResponse payment_route
         * @property {Uint8Array|null} [payment_hash] SendResponse payment_hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendResponse.
         * @memberof lnrpc
         * @interface ISendResponse
         * @augments lnrpc.SendResponse.$Properties
         * @deprecated Use lnrpc.SendResponse.$Properties instead.
         */

        /**
         * Shape of a SendResponse.
         * @typedef {lnrpc.SendResponse.$Properties} lnrpc.SendResponse.$Shape
         */

        /**
         * Constructs a new SendResponse.
         * @memberof lnrpc
         * @classdesc Represents a SendResponse.
         * @constructor
         * @param {lnrpc.SendResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendResponse payment_error.
         * @member {string} payment_error
         * @memberof lnrpc.SendResponse
         * @instance
         */
        SendResponse.prototype.payment_error = "";

        /**
         * SendResponse payment_preimage.
         * @member {Uint8Array} payment_preimage
         * @memberof lnrpc.SendResponse
         * @instance
         */
        SendResponse.prototype.payment_preimage = $util.newBuffer([]);

        /**
         * SendResponse payment_route.
         * @member {lnrpc.Route.$Properties|null|undefined} payment_route
         * @memberof lnrpc.SendResponse
         * @instance
         */
        SendResponse.prototype.payment_route = null;

        /**
         * SendResponse payment_hash.
         * @member {Uint8Array} payment_hash
         * @memberof lnrpc.SendResponse
         * @instance
         */
        SendResponse.prototype.payment_hash = $util.newBuffer([]);

        /**
         * Creates a new SendResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.SendResponse
         * @static
         * @param {lnrpc.SendResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SendResponse} SendResponse instance
         * @type {{
         *   (properties: lnrpc.SendResponse.$Shape): lnrpc.SendResponse & lnrpc.SendResponse.$Shape;
         *   (properties?: lnrpc.SendResponse.$Properties): lnrpc.SendResponse;
         * }}
         */
        SendResponse.create = function(properties) {
            return new SendResponse(properties);
        };

        /**
         * Encodes the specified SendResponse message. Does not implicitly {@link lnrpc.SendResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SendResponse
         * @static
         * @param {lnrpc.SendResponse.$Properties} message SendResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment_error != null && $Object.hasOwnProperty.call(message, "payment_error") && message.payment_error !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.payment_error);
            if (message.payment_preimage != null && $Object.hasOwnProperty.call(message, "payment_preimage") && message.payment_preimage.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.payment_preimage);
            if (message.payment_route != null && $Object.hasOwnProperty.call(message, "payment_route"))
                $root.lnrpc.Route.encode(message.payment_route, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.payment_hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendResponse message, length delimited. Does not implicitly {@link lnrpc.SendResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SendResponse
         * @static
         * @param {lnrpc.SendResponse.$Properties} message SendResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SendResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SendResponse & lnrpc.SendResponse.$Shape} SendResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SendResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_error = value;
                        else
                            delete message.payment_error;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_preimage = value;
                        else
                            delete message.payment_preimage;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.payment_route = $root.lnrpc.Route.decode(reader, reader.uint32(), $undefined, _depth + 1, message.payment_route);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SendResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SendResponse & lnrpc.SendResponse.$Shape} SendResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendResponse message.
         * @function verify
         * @memberof lnrpc.SendResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_error != null && $Object.hasOwnProperty.call(message, "payment_error"))
                if (!$util.isString(message.payment_error))
                    return "payment_error: string expected";
            if (message.payment_preimage != null && $Object.hasOwnProperty.call(message, "payment_preimage"))
                if (!(message.payment_preimage && typeof message.payment_preimage.length === "number" || $util.isString(message.payment_preimage)))
                    return "payment_preimage: buffer expected";
            if (message.payment_route != null && $Object.hasOwnProperty.call(message, "payment_route")) {
                let error = $root.lnrpc.Route.verify(message.payment_route, _depth + 1);
                if (error)
                    return "payment_route." + error;
            }
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            return null;
        };

        /**
         * Creates a SendResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SendResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SendResponse} SendResponse
         */
        SendResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SendResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SendResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SendResponse();
            if (object.payment_error != null)
                if (typeof object.payment_error !== "string" || object.payment_error.length)
                    message.payment_error = $String(object.payment_error);
            if (object.payment_preimage != null)
                if (object.payment_preimage.length)
                    if (typeof object.payment_preimage === "string")
                        $util.base64.decode(object.payment_preimage, message.payment_preimage = $util.newBuffer($util.base64.length(object.payment_preimage)), 0);
                    else if (object.payment_preimage.length >= 0)
                        message.payment_preimage = object.payment_preimage;
            if (object.payment_route != null) {
                if (!$util.isObject(object.payment_route))
                    throw $TypeError(".lnrpc.SendResponse.payment_route: object expected");
                message.payment_route = $root.lnrpc.Route.fromObject(object.payment_route, _depth + 1);
            }
            if (object.payment_hash != null)
                if (object.payment_hash.length)
                    if (typeof object.payment_hash === "string")
                        $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                    else if (object.payment_hash.length >= 0)
                        message.payment_hash = object.payment_hash;
            return message;
        };

        /**
         * Creates a plain object from a SendResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SendResponse
         * @static
         * @param {lnrpc.SendResponse} message SendResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.payment_error = "";
                if (options.bytes === $String)
                    object.payment_preimage = "";
                else {
                    object.payment_preimage = [];
                    if (options.bytes !== $Array)
                        object.payment_preimage = $util.newBuffer(object.payment_preimage);
                }
                object.payment_route = null;
                if (options.bytes === $String)
                    object.payment_hash = "";
                else {
                    object.payment_hash = [];
                    if (options.bytes !== $Array)
                        object.payment_hash = $util.newBuffer(object.payment_hash);
                }
            }
            if (message.payment_error != null && $Object.hasOwnProperty.call(message, "payment_error"))
                object.payment_error = message.payment_error;
            if (message.payment_preimage != null && $Object.hasOwnProperty.call(message, "payment_preimage"))
                object.payment_preimage = options.bytes === $String ? $util.base64.encode(message.payment_preimage, 0, message.payment_preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_preimage) : message.payment_preimage;
            if (message.payment_route != null && $Object.hasOwnProperty.call(message, "payment_route"))
                object.payment_route = $root.lnrpc.Route.toObject(message.payment_route, options, _depth + 1);
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
            return object;
        };

        /**
         * Converts this SendResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.SendResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendResponse.prototype.toJSON = function() {
            return SendResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendResponse
         * @function getTypeUrl
         * @memberof lnrpc.SendResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SendResponse";
        };

        return SendResponse;
    })();

    lnrpc.SendToRouteRequest = (function() {

        /**
         * Properties of a SendToRouteRequest.
         * @typedef {Object} lnrpc.SendToRouteRequest.$Properties
         * @property {Uint8Array|null} [payment_hash] SendToRouteRequest payment_hash
         * @property {string|null} [payment_hash_string] SendToRouteRequest payment_hash_string
         * @property {lnrpc.Route.$Properties|null} [route] SendToRouteRequest route
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendToRouteRequest.
         * @memberof lnrpc
         * @interface ISendToRouteRequest
         * @augments lnrpc.SendToRouteRequest.$Properties
         * @deprecated Use lnrpc.SendToRouteRequest.$Properties instead.
         */

        /**
         * Shape of a SendToRouteRequest.
         * @typedef {lnrpc.SendToRouteRequest.$Properties} lnrpc.SendToRouteRequest.$Shape
         */

        /**
         * Constructs a new SendToRouteRequest.
         * @memberof lnrpc
         * @classdesc Represents a SendToRouteRequest.
         * @constructor
         * @param {lnrpc.SendToRouteRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendToRouteRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendToRouteRequest payment_hash.
         * @member {Uint8Array} payment_hash
         * @memberof lnrpc.SendToRouteRequest
         * @instance
         */
        SendToRouteRequest.prototype.payment_hash = $util.newBuffer([]);

        /**
         * SendToRouteRequest payment_hash_string.
         * @member {string} payment_hash_string
         * @memberof lnrpc.SendToRouteRequest
         * @instance
         */
        SendToRouteRequest.prototype.payment_hash_string = "";

        /**
         * SendToRouteRequest route.
         * @member {lnrpc.Route.$Properties|null|undefined} route
         * @memberof lnrpc.SendToRouteRequest
         * @instance
         */
        SendToRouteRequest.prototype.route = null;

        /**
         * Creates a new SendToRouteRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.SendToRouteRequest
         * @static
         * @param {lnrpc.SendToRouteRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SendToRouteRequest} SendToRouteRequest instance
         * @type {{
         *   (properties: lnrpc.SendToRouteRequest.$Shape): lnrpc.SendToRouteRequest & lnrpc.SendToRouteRequest.$Shape;
         *   (properties?: lnrpc.SendToRouteRequest.$Properties): lnrpc.SendToRouteRequest;
         * }}
         */
        SendToRouteRequest.create = function(properties) {
            return new SendToRouteRequest(properties);
        };

        /**
         * Encodes the specified SendToRouteRequest message. Does not implicitly {@link lnrpc.SendToRouteRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SendToRouteRequest
         * @static
         * @param {lnrpc.SendToRouteRequest.$Properties} message SendToRouteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendToRouteRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.payment_hash);
            if (message.payment_hash_string != null && $Object.hasOwnProperty.call(message, "payment_hash_string") && message.payment_hash_string !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.payment_hash_string);
            if (message.route != null && $Object.hasOwnProperty.call(message, "route"))
                $root.lnrpc.Route.encode(message.route, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendToRouteRequest message, length delimited. Does not implicitly {@link lnrpc.SendToRouteRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SendToRouteRequest
         * @static
         * @param {lnrpc.SendToRouteRequest.$Properties} message SendToRouteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendToRouteRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendToRouteRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SendToRouteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SendToRouteRequest & lnrpc.SendToRouteRequest.$Shape} SendToRouteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendToRouteRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SendToRouteRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_hash_string = value;
                        else
                            delete message.payment_hash_string;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.route = $root.lnrpc.Route.decode(reader, reader.uint32(), $undefined, _depth + 1, message.route);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendToRouteRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SendToRouteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SendToRouteRequest & lnrpc.SendToRouteRequest.$Shape} SendToRouteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendToRouteRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendToRouteRequest message.
         * @function verify
         * @memberof lnrpc.SendToRouteRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendToRouteRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            if (message.payment_hash_string != null && $Object.hasOwnProperty.call(message, "payment_hash_string"))
                if (!$util.isString(message.payment_hash_string))
                    return "payment_hash_string: string expected";
            if (message.route != null && $Object.hasOwnProperty.call(message, "route")) {
                let error = $root.lnrpc.Route.verify(message.route, _depth + 1);
                if (error)
                    return "route." + error;
            }
            return null;
        };

        /**
         * Creates a SendToRouteRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SendToRouteRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SendToRouteRequest} SendToRouteRequest
         */
        SendToRouteRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SendToRouteRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SendToRouteRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SendToRouteRequest();
            if (object.payment_hash != null)
                if (object.payment_hash.length)
                    if (typeof object.payment_hash === "string")
                        $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                    else if (object.payment_hash.length >= 0)
                        message.payment_hash = object.payment_hash;
            if (object.payment_hash_string != null)
                if (typeof object.payment_hash_string !== "string" || object.payment_hash_string.length)
                    message.payment_hash_string = $String(object.payment_hash_string);
            if (object.route != null) {
                if (!$util.isObject(object.route))
                    throw $TypeError(".lnrpc.SendToRouteRequest.route: object expected");
                message.route = $root.lnrpc.Route.fromObject(object.route, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SendToRouteRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SendToRouteRequest
         * @static
         * @param {lnrpc.SendToRouteRequest} message SendToRouteRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendToRouteRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.payment_hash = "";
                else {
                    object.payment_hash = [];
                    if (options.bytes !== $Array)
                        object.payment_hash = $util.newBuffer(object.payment_hash);
                }
                object.payment_hash_string = "";
                object.route = null;
            }
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
            if (message.payment_hash_string != null && $Object.hasOwnProperty.call(message, "payment_hash_string"))
                object.payment_hash_string = message.payment_hash_string;
            if (message.route != null && $Object.hasOwnProperty.call(message, "route"))
                object.route = $root.lnrpc.Route.toObject(message.route, options, _depth + 1);
            return object;
        };

        /**
         * Converts this SendToRouteRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.SendToRouteRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendToRouteRequest.prototype.toJSON = function() {
            return SendToRouteRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendToRouteRequest
         * @function getTypeUrl
         * @memberof lnrpc.SendToRouteRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendToRouteRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SendToRouteRequest";
        };

        return SendToRouteRequest;
    })();

    lnrpc.ChannelAcceptRequest = (function() {

        /**
         * Properties of a ChannelAcceptRequest.
         * @typedef {Object} lnrpc.ChannelAcceptRequest.$Properties
         * @property {Uint8Array|null} [node_pubkey] ChannelAcceptRequest node_pubkey
         * @property {Uint8Array|null} [chain_hash] ChannelAcceptRequest chain_hash
         * @property {Uint8Array|null} [pending_chan_id] ChannelAcceptRequest pending_chan_id
         * @property {Long|null} [funding_amt] ChannelAcceptRequest funding_amt
         * @property {Long|null} [push_amt] ChannelAcceptRequest push_amt
         * @property {Long|null} [dust_limit] ChannelAcceptRequest dust_limit
         * @property {Long|null} [max_value_in_flight] ChannelAcceptRequest max_value_in_flight
         * @property {Long|null} [channel_reserve] ChannelAcceptRequest channel_reserve
         * @property {Long|null} [min_htlc] ChannelAcceptRequest min_htlc
         * @property {Long|null} [fee_per_kw] ChannelAcceptRequest fee_per_kw
         * @property {number|null} [csv_delay] ChannelAcceptRequest csv_delay
         * @property {number|null} [max_accepted_htlcs] ChannelAcceptRequest max_accepted_htlcs
         * @property {number|null} [channel_flags] ChannelAcceptRequest channel_flags
         * @property {lnrpc.CommitmentType|null} [commitment_type] ChannelAcceptRequest commitment_type
         * @property {boolean|null} [wants_zero_conf] ChannelAcceptRequest wants_zero_conf
         * @property {boolean|null} [wants_scid_alias] ChannelAcceptRequest wants_scid_alias
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelAcceptRequest.
         * @memberof lnrpc
         * @interface IChannelAcceptRequest
         * @augments lnrpc.ChannelAcceptRequest.$Properties
         * @deprecated Use lnrpc.ChannelAcceptRequest.$Properties instead.
         */

        /**
         * Shape of a ChannelAcceptRequest.
         * @typedef {lnrpc.ChannelAcceptRequest.$Properties} lnrpc.ChannelAcceptRequest.$Shape
         */

        /**
         * Constructs a new ChannelAcceptRequest.
         * @memberof lnrpc
         * @classdesc Represents a ChannelAcceptRequest.
         * @constructor
         * @param {lnrpc.ChannelAcceptRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelAcceptRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelAcceptRequest node_pubkey.
         * @member {Uint8Array} node_pubkey
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.node_pubkey = $util.newBuffer([]);

        /**
         * ChannelAcceptRequest chain_hash.
         * @member {Uint8Array} chain_hash
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.chain_hash = $util.newBuffer([]);

        /**
         * ChannelAcceptRequest pending_chan_id.
         * @member {Uint8Array} pending_chan_id
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.pending_chan_id = $util.newBuffer([]);

        /**
         * ChannelAcceptRequest funding_amt.
         * @member {Long} funding_amt
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.funding_amt = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptRequest push_amt.
         * @member {Long} push_amt
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.push_amt = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptRequest dust_limit.
         * @member {Long} dust_limit
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.dust_limit = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptRequest max_value_in_flight.
         * @member {Long} max_value_in_flight
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.max_value_in_flight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptRequest channel_reserve.
         * @member {Long} channel_reserve
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.channel_reserve = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptRequest min_htlc.
         * @member {Long} min_htlc
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.min_htlc = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptRequest fee_per_kw.
         * @member {Long} fee_per_kw
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.fee_per_kw = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptRequest csv_delay.
         * @member {number} csv_delay
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.csv_delay = 0;

        /**
         * ChannelAcceptRequest max_accepted_htlcs.
         * @member {number} max_accepted_htlcs
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.max_accepted_htlcs = 0;

        /**
         * ChannelAcceptRequest channel_flags.
         * @member {number} channel_flags
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.channel_flags = 0;

        /**
         * ChannelAcceptRequest commitment_type.
         * @member {lnrpc.CommitmentType} commitment_type
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.commitment_type = 0;

        /**
         * ChannelAcceptRequest wants_zero_conf.
         * @member {boolean} wants_zero_conf
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.wants_zero_conf = false;

        /**
         * ChannelAcceptRequest wants_scid_alias.
         * @member {boolean} wants_scid_alias
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         */
        ChannelAcceptRequest.prototype.wants_scid_alias = false;

        /**
         * Creates a new ChannelAcceptRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelAcceptRequest
         * @static
         * @param {lnrpc.ChannelAcceptRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelAcceptRequest} ChannelAcceptRequest instance
         * @type {{
         *   (properties: lnrpc.ChannelAcceptRequest.$Shape): lnrpc.ChannelAcceptRequest & lnrpc.ChannelAcceptRequest.$Shape;
         *   (properties?: lnrpc.ChannelAcceptRequest.$Properties): lnrpc.ChannelAcceptRequest;
         * }}
         */
        ChannelAcceptRequest.create = function(properties) {
            return new ChannelAcceptRequest(properties);
        };

        /**
         * Encodes the specified ChannelAcceptRequest message. Does not implicitly {@link lnrpc.ChannelAcceptRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelAcceptRequest
         * @static
         * @param {lnrpc.ChannelAcceptRequest.$Properties} message ChannelAcceptRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelAcceptRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.node_pubkey != null && $Object.hasOwnProperty.call(message, "node_pubkey") && message.node_pubkey.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.node_pubkey);
            if (message.chain_hash != null && $Object.hasOwnProperty.call(message, "chain_hash") && message.chain_hash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.chain_hash);
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id") && message.pending_chan_id.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.pending_chan_id);
            if (message.funding_amt != null && $Object.hasOwnProperty.call(message, "funding_amt") && (typeof message.funding_amt === "object" ? message.funding_amt.low || message.funding_amt.high : message.funding_amt !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.funding_amt);
            if (message.push_amt != null && $Object.hasOwnProperty.call(message, "push_amt") && (typeof message.push_amt === "object" ? message.push_amt.low || message.push_amt.high : message.push_amt !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.push_amt);
            if (message.dust_limit != null && $Object.hasOwnProperty.call(message, "dust_limit") && (typeof message.dust_limit === "object" ? message.dust_limit.low || message.dust_limit.high : message.dust_limit !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.dust_limit);
            if (message.max_value_in_flight != null && $Object.hasOwnProperty.call(message, "max_value_in_flight") && (typeof message.max_value_in_flight === "object" ? message.max_value_in_flight.low || message.max_value_in_flight.high : message.max_value_in_flight !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.max_value_in_flight);
            if (message.channel_reserve != null && $Object.hasOwnProperty.call(message, "channel_reserve") && (typeof message.channel_reserve === "object" ? message.channel_reserve.low || message.channel_reserve.high : message.channel_reserve !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.channel_reserve);
            if (message.min_htlc != null && $Object.hasOwnProperty.call(message, "min_htlc") && (typeof message.min_htlc === "object" ? message.min_htlc.low || message.min_htlc.high : message.min_htlc !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.min_htlc);
            if (message.fee_per_kw != null && $Object.hasOwnProperty.call(message, "fee_per_kw") && (typeof message.fee_per_kw === "object" ? message.fee_per_kw.low || message.fee_per_kw.high : message.fee_per_kw !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.fee_per_kw);
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay") && message.csv_delay !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.csv_delay);
            if (message.max_accepted_htlcs != null && $Object.hasOwnProperty.call(message, "max_accepted_htlcs") && message.max_accepted_htlcs !== 0)
                writer.uint32(/* id 12, wireType 0 =*/96).uint32(message.max_accepted_htlcs);
            if (message.channel_flags != null && $Object.hasOwnProperty.call(message, "channel_flags") && message.channel_flags !== 0)
                writer.uint32(/* id 13, wireType 0 =*/104).uint32(message.channel_flags);
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type") && message.commitment_type !== 0)
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.commitment_type);
            if (message.wants_zero_conf != null && $Object.hasOwnProperty.call(message, "wants_zero_conf") && message.wants_zero_conf !== false)
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.wants_zero_conf);
            if (message.wants_scid_alias != null && $Object.hasOwnProperty.call(message, "wants_scid_alias") && message.wants_scid_alias !== false)
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.wants_scid_alias);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelAcceptRequest message, length delimited. Does not implicitly {@link lnrpc.ChannelAcceptRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelAcceptRequest
         * @static
         * @param {lnrpc.ChannelAcceptRequest.$Properties} message ChannelAcceptRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelAcceptRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelAcceptRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelAcceptRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelAcceptRequest & lnrpc.ChannelAcceptRequest.$Shape} ChannelAcceptRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelAcceptRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelAcceptRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.node_pubkey = value;
                        else
                            delete message.node_pubkey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.chain_hash = value;
                        else
                            delete message.chain_hash;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pending_chan_id = value;
                        else
                            delete message.pending_chan_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.funding_amt = value;
                        else
                            delete message.funding_amt;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.push_amt = value;
                        else
                            delete message.push_amt;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.dust_limit = value;
                        else
                            delete message.dust_limit;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_value_in_flight = value;
                        else
                            delete message.max_value_in_flight;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.channel_reserve = value;
                        else
                            delete message.channel_reserve;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.min_htlc = value;
                        else
                            delete message.min_htlc;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_per_kw = value;
                        else
                            delete message.fee_per_kw;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.csv_delay = value;
                        else
                            delete message.csv_delay;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_accepted_htlcs = value;
                        else
                            delete message.max_accepted_htlcs;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.channel_flags = value;
                        else
                            delete message.channel_flags;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.commitment_type = value;
                        else
                            delete message.commitment_type;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.wants_zero_conf = value;
                        else
                            delete message.wants_zero_conf;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.wants_scid_alias = value;
                        else
                            delete message.wants_scid_alias;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelAcceptRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelAcceptRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelAcceptRequest & lnrpc.ChannelAcceptRequest.$Shape} ChannelAcceptRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelAcceptRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelAcceptRequest message.
         * @function verify
         * @memberof lnrpc.ChannelAcceptRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelAcceptRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.node_pubkey != null && $Object.hasOwnProperty.call(message, "node_pubkey"))
                if (!(message.node_pubkey && typeof message.node_pubkey.length === "number" || $util.isString(message.node_pubkey)))
                    return "node_pubkey: buffer expected";
            if (message.chain_hash != null && $Object.hasOwnProperty.call(message, "chain_hash"))
                if (!(message.chain_hash && typeof message.chain_hash.length === "number" || $util.isString(message.chain_hash)))
                    return "chain_hash: buffer expected";
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                if (!(message.pending_chan_id && typeof message.pending_chan_id.length === "number" || $util.isString(message.pending_chan_id)))
                    return "pending_chan_id: buffer expected";
            if (message.funding_amt != null && $Object.hasOwnProperty.call(message, "funding_amt"))
                if (!$util.isInteger(message.funding_amt) && !(message.funding_amt && $util.isInteger(message.funding_amt.low) && $util.isInteger(message.funding_amt.high)))
                    return "funding_amt: integer|Long expected";
            if (message.push_amt != null && $Object.hasOwnProperty.call(message, "push_amt"))
                if (!$util.isInteger(message.push_amt) && !(message.push_amt && $util.isInteger(message.push_amt.low) && $util.isInteger(message.push_amt.high)))
                    return "push_amt: integer|Long expected";
            if (message.dust_limit != null && $Object.hasOwnProperty.call(message, "dust_limit"))
                if (!$util.isInteger(message.dust_limit) && !(message.dust_limit && $util.isInteger(message.dust_limit.low) && $util.isInteger(message.dust_limit.high)))
                    return "dust_limit: integer|Long expected";
            if (message.max_value_in_flight != null && $Object.hasOwnProperty.call(message, "max_value_in_flight"))
                if (!$util.isInteger(message.max_value_in_flight) && !(message.max_value_in_flight && $util.isInteger(message.max_value_in_flight.low) && $util.isInteger(message.max_value_in_flight.high)))
                    return "max_value_in_flight: integer|Long expected";
            if (message.channel_reserve != null && $Object.hasOwnProperty.call(message, "channel_reserve"))
                if (!$util.isInteger(message.channel_reserve) && !(message.channel_reserve && $util.isInteger(message.channel_reserve.low) && $util.isInteger(message.channel_reserve.high)))
                    return "channel_reserve: integer|Long expected";
            if (message.min_htlc != null && $Object.hasOwnProperty.call(message, "min_htlc"))
                if (!$util.isInteger(message.min_htlc) && !(message.min_htlc && $util.isInteger(message.min_htlc.low) && $util.isInteger(message.min_htlc.high)))
                    return "min_htlc: integer|Long expected";
            if (message.fee_per_kw != null && $Object.hasOwnProperty.call(message, "fee_per_kw"))
                if (!$util.isInteger(message.fee_per_kw) && !(message.fee_per_kw && $util.isInteger(message.fee_per_kw.low) && $util.isInteger(message.fee_per_kw.high)))
                    return "fee_per_kw: integer|Long expected";
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay"))
                if (!$util.isInteger(message.csv_delay))
                    return "csv_delay: integer expected";
            if (message.max_accepted_htlcs != null && $Object.hasOwnProperty.call(message, "max_accepted_htlcs"))
                if (!$util.isInteger(message.max_accepted_htlcs))
                    return "max_accepted_htlcs: integer expected";
            if (message.channel_flags != null && $Object.hasOwnProperty.call(message, "channel_flags"))
                if (!$util.isInteger(message.channel_flags))
                    return "channel_flags: integer expected";
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                if (typeof message.commitment_type !== "number" || (message.commitment_type | 0) !== message.commitment_type)
                    return "commitment_type: enum value expected";
            if (message.wants_zero_conf != null && $Object.hasOwnProperty.call(message, "wants_zero_conf"))
                if (typeof message.wants_zero_conf !== "boolean")
                    return "wants_zero_conf: boolean expected";
            if (message.wants_scid_alias != null && $Object.hasOwnProperty.call(message, "wants_scid_alias"))
                if (typeof message.wants_scid_alias !== "boolean")
                    return "wants_scid_alias: boolean expected";
            return null;
        };

        /**
         * Creates a ChannelAcceptRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelAcceptRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelAcceptRequest} ChannelAcceptRequest
         */
        ChannelAcceptRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelAcceptRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelAcceptRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelAcceptRequest();
            if (object.node_pubkey != null)
                if (object.node_pubkey.length)
                    if (typeof object.node_pubkey === "string")
                        $util.base64.decode(object.node_pubkey, message.node_pubkey = $util.newBuffer($util.base64.length(object.node_pubkey)), 0);
                    else if (object.node_pubkey.length >= 0)
                        message.node_pubkey = object.node_pubkey;
            if (object.chain_hash != null)
                if (object.chain_hash.length)
                    if (typeof object.chain_hash === "string")
                        $util.base64.decode(object.chain_hash, message.chain_hash = $util.newBuffer($util.base64.length(object.chain_hash)), 0);
                    else if (object.chain_hash.length >= 0)
                        message.chain_hash = object.chain_hash;
            if (object.pending_chan_id != null)
                if (object.pending_chan_id.length)
                    if (typeof object.pending_chan_id === "string")
                        $util.base64.decode(object.pending_chan_id, message.pending_chan_id = $util.newBuffer($util.base64.length(object.pending_chan_id)), 0);
                    else if (object.pending_chan_id.length >= 0)
                        message.pending_chan_id = object.pending_chan_id;
            if (object.funding_amt != null)
                if (typeof object.funding_amt === "object" ? object.funding_amt.low || object.funding_amt.high : $Number(object.funding_amt) !== 0)
                    if ($util.Long)
                        message.funding_amt = $util.Long.fromValue(object.funding_amt, true);
                    else if (typeof object.funding_amt === "string")
                        message.funding_amt = $parseInt(object.funding_amt, 10);
                    else if (typeof object.funding_amt === "number")
                        message.funding_amt = object.funding_amt;
                    else if (typeof object.funding_amt === "object")
                        message.funding_amt = new $util.LongBits(object.funding_amt.low >>> 0, object.funding_amt.high >>> 0).toNumber(true);
            if (object.push_amt != null)
                if (typeof object.push_amt === "object" ? object.push_amt.low || object.push_amt.high : $Number(object.push_amt) !== 0)
                    if ($util.Long)
                        message.push_amt = $util.Long.fromValue(object.push_amt, true);
                    else if (typeof object.push_amt === "string")
                        message.push_amt = $parseInt(object.push_amt, 10);
                    else if (typeof object.push_amt === "number")
                        message.push_amt = object.push_amt;
                    else if (typeof object.push_amt === "object")
                        message.push_amt = new $util.LongBits(object.push_amt.low >>> 0, object.push_amt.high >>> 0).toNumber(true);
            if (object.dust_limit != null)
                if (typeof object.dust_limit === "object" ? object.dust_limit.low || object.dust_limit.high : $Number(object.dust_limit) !== 0)
                    if ($util.Long)
                        message.dust_limit = $util.Long.fromValue(object.dust_limit, true);
                    else if (typeof object.dust_limit === "string")
                        message.dust_limit = $parseInt(object.dust_limit, 10);
                    else if (typeof object.dust_limit === "number")
                        message.dust_limit = object.dust_limit;
                    else if (typeof object.dust_limit === "object")
                        message.dust_limit = new $util.LongBits(object.dust_limit.low >>> 0, object.dust_limit.high >>> 0).toNumber(true);
            if (object.max_value_in_flight != null)
                if (typeof object.max_value_in_flight === "object" ? object.max_value_in_flight.low || object.max_value_in_flight.high : $Number(object.max_value_in_flight) !== 0)
                    if ($util.Long)
                        message.max_value_in_flight = $util.Long.fromValue(object.max_value_in_flight, true);
                    else if (typeof object.max_value_in_flight === "string")
                        message.max_value_in_flight = $parseInt(object.max_value_in_flight, 10);
                    else if (typeof object.max_value_in_flight === "number")
                        message.max_value_in_flight = object.max_value_in_flight;
                    else if (typeof object.max_value_in_flight === "object")
                        message.max_value_in_flight = new $util.LongBits(object.max_value_in_flight.low >>> 0, object.max_value_in_flight.high >>> 0).toNumber(true);
            if (object.channel_reserve != null)
                if (typeof object.channel_reserve === "object" ? object.channel_reserve.low || object.channel_reserve.high : $Number(object.channel_reserve) !== 0)
                    if ($util.Long)
                        message.channel_reserve = $util.Long.fromValue(object.channel_reserve, true);
                    else if (typeof object.channel_reserve === "string")
                        message.channel_reserve = $parseInt(object.channel_reserve, 10);
                    else if (typeof object.channel_reserve === "number")
                        message.channel_reserve = object.channel_reserve;
                    else if (typeof object.channel_reserve === "object")
                        message.channel_reserve = new $util.LongBits(object.channel_reserve.low >>> 0, object.channel_reserve.high >>> 0).toNumber(true);
            if (object.min_htlc != null)
                if (typeof object.min_htlc === "object" ? object.min_htlc.low || object.min_htlc.high : $Number(object.min_htlc) !== 0)
                    if ($util.Long)
                        message.min_htlc = $util.Long.fromValue(object.min_htlc, true);
                    else if (typeof object.min_htlc === "string")
                        message.min_htlc = $parseInt(object.min_htlc, 10);
                    else if (typeof object.min_htlc === "number")
                        message.min_htlc = object.min_htlc;
                    else if (typeof object.min_htlc === "object")
                        message.min_htlc = new $util.LongBits(object.min_htlc.low >>> 0, object.min_htlc.high >>> 0).toNumber(true);
            if (object.fee_per_kw != null)
                if (typeof object.fee_per_kw === "object" ? object.fee_per_kw.low || object.fee_per_kw.high : $Number(object.fee_per_kw) !== 0)
                    if ($util.Long)
                        message.fee_per_kw = $util.Long.fromValue(object.fee_per_kw, true);
                    else if (typeof object.fee_per_kw === "string")
                        message.fee_per_kw = $parseInt(object.fee_per_kw, 10);
                    else if (typeof object.fee_per_kw === "number")
                        message.fee_per_kw = object.fee_per_kw;
                    else if (typeof object.fee_per_kw === "object")
                        message.fee_per_kw = new $util.LongBits(object.fee_per_kw.low >>> 0, object.fee_per_kw.high >>> 0).toNumber(true);
            if (object.csv_delay != null)
                if ($Number(object.csv_delay) !== 0)
                    message.csv_delay = object.csv_delay >>> 0;
            if (object.max_accepted_htlcs != null)
                if ($Number(object.max_accepted_htlcs) !== 0)
                    message.max_accepted_htlcs = object.max_accepted_htlcs >>> 0;
            if (object.channel_flags != null)
                if ($Number(object.channel_flags) !== 0)
                    message.channel_flags = object.channel_flags >>> 0;
            if (object.commitment_type !== 0 && (typeof object.commitment_type !== "string" || $root.lnrpc.CommitmentType[object.commitment_type] !== 0))
                switch (object.commitment_type) {
                case "UNKNOWN_COMMITMENT_TYPE":
                case 0:
                    message.commitment_type = 0;
                    break;
                case "LEGACY":
                case 1:
                    message.commitment_type = 1;
                    break;
                case "STATIC_REMOTE_KEY":
                case 2:
                    message.commitment_type = 2;
                    break;
                case "ANCHORS":
                case 3:
                    message.commitment_type = 3;
                    break;
                case "SCRIPT_ENFORCED_LEASE":
                case 4:
                    message.commitment_type = 4;
                    break;
                case "SIMPLE_TAPROOT":
                case 5:
                    message.commitment_type = 5;
                    break;
                case "SIMPLE_TAPROOT_OVERLAY":
                case 6:
                    message.commitment_type = 6;
                    break;
                default:
                    if (typeof object.commitment_type === "number" && (object.commitment_type | 0) === object.commitment_type)
                        message.commitment_type = object.commitment_type;
                }
            if (object.wants_zero_conf != null)
                if (object.wants_zero_conf)
                    message.wants_zero_conf = $Boolean(object.wants_zero_conf);
            if (object.wants_scid_alias != null)
                if (object.wants_scid_alias)
                    message.wants_scid_alias = $Boolean(object.wants_scid_alias);
            return message;
        };

        /**
         * Creates a plain object from a ChannelAcceptRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelAcceptRequest
         * @static
         * @param {lnrpc.ChannelAcceptRequest} message ChannelAcceptRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelAcceptRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.node_pubkey = "";
                else {
                    object.node_pubkey = [];
                    if (options.bytes !== $Array)
                        object.node_pubkey = $util.newBuffer(object.node_pubkey);
                }
                if (options.bytes === $String)
                    object.chain_hash = "";
                else {
                    object.chain_hash = [];
                    if (options.bytes !== $Array)
                        object.chain_hash = $util.newBuffer(object.chain_hash);
                }
                if (options.bytes === $String)
                    object.pending_chan_id = "";
                else {
                    object.pending_chan_id = [];
                    if (options.bytes !== $Array)
                        object.pending_chan_id = $util.newBuffer(object.pending_chan_id);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.funding_amt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.funding_amt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.push_amt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.push_amt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.dust_limit = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.dust_limit = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.max_value_in_flight = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.max_value_in_flight = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.channel_reserve = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.channel_reserve = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.min_htlc = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min_htlc = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.fee_per_kw = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_per_kw = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.csv_delay = 0;
                object.max_accepted_htlcs = 0;
                object.channel_flags = 0;
                object.commitment_type = options.enums === $String ? "UNKNOWN_COMMITMENT_TYPE" : 0;
                object.wants_zero_conf = false;
                object.wants_scid_alias = false;
            }
            if (message.node_pubkey != null && $Object.hasOwnProperty.call(message, "node_pubkey"))
                object.node_pubkey = options.bytes === $String ? $util.base64.encode(message.node_pubkey, 0, message.node_pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.node_pubkey) : message.node_pubkey;
            if (message.chain_hash != null && $Object.hasOwnProperty.call(message, "chain_hash"))
                object.chain_hash = options.bytes === $String ? $util.base64.encode(message.chain_hash, 0, message.chain_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.chain_hash) : message.chain_hash;
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                object.pending_chan_id = options.bytes === $String ? $util.base64.encode(message.pending_chan_id, 0, message.pending_chan_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pending_chan_id) : message.pending_chan_id;
            if (message.funding_amt != null && $Object.hasOwnProperty.call(message, "funding_amt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.funding_amt = typeof message.funding_amt === "number" ? $BigInt(message.funding_amt) : $util.Long.fromBits(message.funding_amt.low >>> 0, message.funding_amt.high >>> 0, true).toBigInt();
                else if (typeof message.funding_amt === "number")
                    object.funding_amt = options.longs === $String ? $String(message.funding_amt) : message.funding_amt;
                else
                    object.funding_amt = options.longs === $String ? $util.Long.prototype.toString.call(message.funding_amt) : options.longs === $Number ? new $util.LongBits(message.funding_amt.low >>> 0, message.funding_amt.high >>> 0).toNumber(true) : message.funding_amt;
            if (message.push_amt != null && $Object.hasOwnProperty.call(message, "push_amt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.push_amt = typeof message.push_amt === "number" ? $BigInt(message.push_amt) : $util.Long.fromBits(message.push_amt.low >>> 0, message.push_amt.high >>> 0, true).toBigInt();
                else if (typeof message.push_amt === "number")
                    object.push_amt = options.longs === $String ? $String(message.push_amt) : message.push_amt;
                else
                    object.push_amt = options.longs === $String ? $util.Long.prototype.toString.call(message.push_amt) : options.longs === $Number ? new $util.LongBits(message.push_amt.low >>> 0, message.push_amt.high >>> 0).toNumber(true) : message.push_amt;
            if (message.dust_limit != null && $Object.hasOwnProperty.call(message, "dust_limit"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.dust_limit = typeof message.dust_limit === "number" ? $BigInt(message.dust_limit) : $util.Long.fromBits(message.dust_limit.low >>> 0, message.dust_limit.high >>> 0, true).toBigInt();
                else if (typeof message.dust_limit === "number")
                    object.dust_limit = options.longs === $String ? $String(message.dust_limit) : message.dust_limit;
                else
                    object.dust_limit = options.longs === $String ? $util.Long.prototype.toString.call(message.dust_limit) : options.longs === $Number ? new $util.LongBits(message.dust_limit.low >>> 0, message.dust_limit.high >>> 0).toNumber(true) : message.dust_limit;
            if (message.max_value_in_flight != null && $Object.hasOwnProperty.call(message, "max_value_in_flight"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.max_value_in_flight = typeof message.max_value_in_flight === "number" ? $BigInt(message.max_value_in_flight) : $util.Long.fromBits(message.max_value_in_flight.low >>> 0, message.max_value_in_flight.high >>> 0, true).toBigInt();
                else if (typeof message.max_value_in_flight === "number")
                    object.max_value_in_flight = options.longs === $String ? $String(message.max_value_in_flight) : message.max_value_in_flight;
                else
                    object.max_value_in_flight = options.longs === $String ? $util.Long.prototype.toString.call(message.max_value_in_flight) : options.longs === $Number ? new $util.LongBits(message.max_value_in_flight.low >>> 0, message.max_value_in_flight.high >>> 0).toNumber(true) : message.max_value_in_flight;
            if (message.channel_reserve != null && $Object.hasOwnProperty.call(message, "channel_reserve"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.channel_reserve = typeof message.channel_reserve === "number" ? $BigInt(message.channel_reserve) : $util.Long.fromBits(message.channel_reserve.low >>> 0, message.channel_reserve.high >>> 0, true).toBigInt();
                else if (typeof message.channel_reserve === "number")
                    object.channel_reserve = options.longs === $String ? $String(message.channel_reserve) : message.channel_reserve;
                else
                    object.channel_reserve = options.longs === $String ? $util.Long.prototype.toString.call(message.channel_reserve) : options.longs === $Number ? new $util.LongBits(message.channel_reserve.low >>> 0, message.channel_reserve.high >>> 0).toNumber(true) : message.channel_reserve;
            if (message.min_htlc != null && $Object.hasOwnProperty.call(message, "min_htlc"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min_htlc = typeof message.min_htlc === "number" ? $BigInt(message.min_htlc) : $util.Long.fromBits(message.min_htlc.low >>> 0, message.min_htlc.high >>> 0, true).toBigInt();
                else if (typeof message.min_htlc === "number")
                    object.min_htlc = options.longs === $String ? $String(message.min_htlc) : message.min_htlc;
                else
                    object.min_htlc = options.longs === $String ? $util.Long.prototype.toString.call(message.min_htlc) : options.longs === $Number ? new $util.LongBits(message.min_htlc.low >>> 0, message.min_htlc.high >>> 0).toNumber(true) : message.min_htlc;
            if (message.fee_per_kw != null && $Object.hasOwnProperty.call(message, "fee_per_kw"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_per_kw = typeof message.fee_per_kw === "number" ? $BigInt(message.fee_per_kw) : $util.Long.fromBits(message.fee_per_kw.low >>> 0, message.fee_per_kw.high >>> 0, true).toBigInt();
                else if (typeof message.fee_per_kw === "number")
                    object.fee_per_kw = options.longs === $String ? $String(message.fee_per_kw) : message.fee_per_kw;
                else
                    object.fee_per_kw = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_per_kw) : options.longs === $Number ? new $util.LongBits(message.fee_per_kw.low >>> 0, message.fee_per_kw.high >>> 0).toNumber(true) : message.fee_per_kw;
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay"))
                object.csv_delay = message.csv_delay;
            if (message.max_accepted_htlcs != null && $Object.hasOwnProperty.call(message, "max_accepted_htlcs"))
                object.max_accepted_htlcs = message.max_accepted_htlcs;
            if (message.channel_flags != null && $Object.hasOwnProperty.call(message, "channel_flags"))
                object.channel_flags = message.channel_flags;
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                object.commitment_type = options.enums === $String ? $root.lnrpc.CommitmentType[message.commitment_type] === $undefined ? message.commitment_type : $root.lnrpc.CommitmentType[message.commitment_type] : message.commitment_type;
            if (message.wants_zero_conf != null && $Object.hasOwnProperty.call(message, "wants_zero_conf"))
                object.wants_zero_conf = message.wants_zero_conf;
            if (message.wants_scid_alias != null && $Object.hasOwnProperty.call(message, "wants_scid_alias"))
                object.wants_scid_alias = message.wants_scid_alias;
            return object;
        };

        /**
         * Converts this ChannelAcceptRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelAcceptRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelAcceptRequest.prototype.toJSON = function() {
            return ChannelAcceptRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelAcceptRequest
         * @function getTypeUrl
         * @memberof lnrpc.ChannelAcceptRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelAcceptRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelAcceptRequest";
        };

        return ChannelAcceptRequest;
    })();

    lnrpc.ChannelAcceptResponse = (function() {

        /**
         * Properties of a ChannelAcceptResponse.
         * @typedef {Object} lnrpc.ChannelAcceptResponse.$Properties
         * @property {boolean|null} [accept] ChannelAcceptResponse accept
         * @property {Uint8Array|null} [pending_chan_id] ChannelAcceptResponse pending_chan_id
         * @property {string|null} [error] ChannelAcceptResponse error
         * @property {string|null} [upfront_shutdown] ChannelAcceptResponse upfront_shutdown
         * @property {number|null} [csv_delay] ChannelAcceptResponse csv_delay
         * @property {Long|null} [reserve_sat] ChannelAcceptResponse reserve_sat
         * @property {Long|null} [in_flight_max_msat] ChannelAcceptResponse in_flight_max_msat
         * @property {number|null} [max_htlc_count] ChannelAcceptResponse max_htlc_count
         * @property {Long|null} [min_htlc_in] ChannelAcceptResponse min_htlc_in
         * @property {number|null} [min_accept_depth] ChannelAcceptResponse min_accept_depth
         * @property {boolean|null} [zero_conf] ChannelAcceptResponse zero_conf
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelAcceptResponse.
         * @memberof lnrpc
         * @interface IChannelAcceptResponse
         * @augments lnrpc.ChannelAcceptResponse.$Properties
         * @deprecated Use lnrpc.ChannelAcceptResponse.$Properties instead.
         */

        /**
         * Shape of a ChannelAcceptResponse.
         * @typedef {lnrpc.ChannelAcceptResponse.$Properties} lnrpc.ChannelAcceptResponse.$Shape
         */

        /**
         * Constructs a new ChannelAcceptResponse.
         * @memberof lnrpc
         * @classdesc Represents a ChannelAcceptResponse.
         * @constructor
         * @param {lnrpc.ChannelAcceptResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelAcceptResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelAcceptResponse accept.
         * @member {boolean} accept
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.accept = false;

        /**
         * ChannelAcceptResponse pending_chan_id.
         * @member {Uint8Array} pending_chan_id
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.pending_chan_id = $util.newBuffer([]);

        /**
         * ChannelAcceptResponse error.
         * @member {string} error
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.error = "";

        /**
         * ChannelAcceptResponse upfront_shutdown.
         * @member {string} upfront_shutdown
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.upfront_shutdown = "";

        /**
         * ChannelAcceptResponse csv_delay.
         * @member {number} csv_delay
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.csv_delay = 0;

        /**
         * ChannelAcceptResponse reserve_sat.
         * @member {Long} reserve_sat
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.reserve_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptResponse in_flight_max_msat.
         * @member {Long} in_flight_max_msat
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.in_flight_max_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptResponse max_htlc_count.
         * @member {number} max_htlc_count
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.max_htlc_count = 0;

        /**
         * ChannelAcceptResponse min_htlc_in.
         * @member {Long} min_htlc_in
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.min_htlc_in = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelAcceptResponse min_accept_depth.
         * @member {number} min_accept_depth
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.min_accept_depth = 0;

        /**
         * ChannelAcceptResponse zero_conf.
         * @member {boolean} zero_conf
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         */
        ChannelAcceptResponse.prototype.zero_conf = false;

        /**
         * Creates a new ChannelAcceptResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelAcceptResponse
         * @static
         * @param {lnrpc.ChannelAcceptResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelAcceptResponse} ChannelAcceptResponse instance
         * @type {{
         *   (properties: lnrpc.ChannelAcceptResponse.$Shape): lnrpc.ChannelAcceptResponse & lnrpc.ChannelAcceptResponse.$Shape;
         *   (properties?: lnrpc.ChannelAcceptResponse.$Properties): lnrpc.ChannelAcceptResponse;
         * }}
         */
        ChannelAcceptResponse.create = function(properties) {
            return new ChannelAcceptResponse(properties);
        };

        /**
         * Encodes the specified ChannelAcceptResponse message. Does not implicitly {@link lnrpc.ChannelAcceptResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelAcceptResponse
         * @static
         * @param {lnrpc.ChannelAcceptResponse.$Properties} message ChannelAcceptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelAcceptResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accept != null && $Object.hasOwnProperty.call(message, "accept") && message.accept !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.accept);
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id") && message.pending_chan_id.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.pending_chan_id);
            if (message.error != null && $Object.hasOwnProperty.call(message, "error") && message.error !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.error);
            if (message.upfront_shutdown != null && $Object.hasOwnProperty.call(message, "upfront_shutdown") && message.upfront_shutdown !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.upfront_shutdown);
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay") && message.csv_delay !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.csv_delay);
            if (message.reserve_sat != null && $Object.hasOwnProperty.call(message, "reserve_sat") && (typeof message.reserve_sat === "object" ? message.reserve_sat.low || message.reserve_sat.high : message.reserve_sat !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.reserve_sat);
            if (message.in_flight_max_msat != null && $Object.hasOwnProperty.call(message, "in_flight_max_msat") && (typeof message.in_flight_max_msat === "object" ? message.in_flight_max_msat.low || message.in_flight_max_msat.high : message.in_flight_max_msat !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.in_flight_max_msat);
            if (message.max_htlc_count != null && $Object.hasOwnProperty.call(message, "max_htlc_count") && message.max_htlc_count !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.max_htlc_count);
            if (message.min_htlc_in != null && $Object.hasOwnProperty.call(message, "min_htlc_in") && (typeof message.min_htlc_in === "object" ? message.min_htlc_in.low || message.min_htlc_in.high : message.min_htlc_in !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.min_htlc_in);
            if (message.min_accept_depth != null && $Object.hasOwnProperty.call(message, "min_accept_depth") && message.min_accept_depth !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.min_accept_depth);
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf") && message.zero_conf !== false)
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.zero_conf);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelAcceptResponse message, length delimited. Does not implicitly {@link lnrpc.ChannelAcceptResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelAcceptResponse
         * @static
         * @param {lnrpc.ChannelAcceptResponse.$Properties} message ChannelAcceptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelAcceptResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelAcceptResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelAcceptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelAcceptResponse & lnrpc.ChannelAcceptResponse.$Shape} ChannelAcceptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelAcceptResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelAcceptResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.accept = value;
                        else
                            delete message.accept;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pending_chan_id = value;
                        else
                            delete message.pending_chan_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.error = value;
                        else
                            delete message.error;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.upfront_shutdown = value;
                        else
                            delete message.upfront_shutdown;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.csv_delay = value;
                        else
                            delete message.csv_delay;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.reserve_sat = value;
                        else
                            delete message.reserve_sat;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.in_flight_max_msat = value;
                        else
                            delete message.in_flight_max_msat;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_htlc_count = value;
                        else
                            delete message.max_htlc_count;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.min_htlc_in = value;
                        else
                            delete message.min_htlc_in;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.min_accept_depth = value;
                        else
                            delete message.min_accept_depth;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zero_conf = value;
                        else
                            delete message.zero_conf;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelAcceptResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelAcceptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelAcceptResponse & lnrpc.ChannelAcceptResponse.$Shape} ChannelAcceptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelAcceptResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelAcceptResponse message.
         * @function verify
         * @memberof lnrpc.ChannelAcceptResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelAcceptResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accept != null && $Object.hasOwnProperty.call(message, "accept"))
                if (typeof message.accept !== "boolean")
                    return "accept: boolean expected";
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                if (!(message.pending_chan_id && typeof message.pending_chan_id.length === "number" || $util.isString(message.pending_chan_id)))
                    return "pending_chan_id: buffer expected";
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            if (message.upfront_shutdown != null && $Object.hasOwnProperty.call(message, "upfront_shutdown"))
                if (!$util.isString(message.upfront_shutdown))
                    return "upfront_shutdown: string expected";
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay"))
                if (!$util.isInteger(message.csv_delay))
                    return "csv_delay: integer expected";
            if (message.reserve_sat != null && $Object.hasOwnProperty.call(message, "reserve_sat"))
                if (!$util.isInteger(message.reserve_sat) && !(message.reserve_sat && $util.isInteger(message.reserve_sat.low) && $util.isInteger(message.reserve_sat.high)))
                    return "reserve_sat: integer|Long expected";
            if (message.in_flight_max_msat != null && $Object.hasOwnProperty.call(message, "in_flight_max_msat"))
                if (!$util.isInteger(message.in_flight_max_msat) && !(message.in_flight_max_msat && $util.isInteger(message.in_flight_max_msat.low) && $util.isInteger(message.in_flight_max_msat.high)))
                    return "in_flight_max_msat: integer|Long expected";
            if (message.max_htlc_count != null && $Object.hasOwnProperty.call(message, "max_htlc_count"))
                if (!$util.isInteger(message.max_htlc_count))
                    return "max_htlc_count: integer expected";
            if (message.min_htlc_in != null && $Object.hasOwnProperty.call(message, "min_htlc_in"))
                if (!$util.isInteger(message.min_htlc_in) && !(message.min_htlc_in && $util.isInteger(message.min_htlc_in.low) && $util.isInteger(message.min_htlc_in.high)))
                    return "min_htlc_in: integer|Long expected";
            if (message.min_accept_depth != null && $Object.hasOwnProperty.call(message, "min_accept_depth"))
                if (!$util.isInteger(message.min_accept_depth))
                    return "min_accept_depth: integer expected";
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf"))
                if (typeof message.zero_conf !== "boolean")
                    return "zero_conf: boolean expected";
            return null;
        };

        /**
         * Creates a ChannelAcceptResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelAcceptResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelAcceptResponse} ChannelAcceptResponse
         */
        ChannelAcceptResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelAcceptResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelAcceptResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelAcceptResponse();
            if (object.accept != null)
                if (object.accept)
                    message.accept = $Boolean(object.accept);
            if (object.pending_chan_id != null)
                if (object.pending_chan_id.length)
                    if (typeof object.pending_chan_id === "string")
                        $util.base64.decode(object.pending_chan_id, message.pending_chan_id = $util.newBuffer($util.base64.length(object.pending_chan_id)), 0);
                    else if (object.pending_chan_id.length >= 0)
                        message.pending_chan_id = object.pending_chan_id;
            if (object.error != null)
                if (typeof object.error !== "string" || object.error.length)
                    message.error = $String(object.error);
            if (object.upfront_shutdown != null)
                if (typeof object.upfront_shutdown !== "string" || object.upfront_shutdown.length)
                    message.upfront_shutdown = $String(object.upfront_shutdown);
            if (object.csv_delay != null)
                if ($Number(object.csv_delay) !== 0)
                    message.csv_delay = object.csv_delay >>> 0;
            if (object.reserve_sat != null)
                if (typeof object.reserve_sat === "object" ? object.reserve_sat.low || object.reserve_sat.high : $Number(object.reserve_sat) !== 0)
                    if ($util.Long)
                        message.reserve_sat = $util.Long.fromValue(object.reserve_sat, true);
                    else if (typeof object.reserve_sat === "string")
                        message.reserve_sat = $parseInt(object.reserve_sat, 10);
                    else if (typeof object.reserve_sat === "number")
                        message.reserve_sat = object.reserve_sat;
                    else if (typeof object.reserve_sat === "object")
                        message.reserve_sat = new $util.LongBits(object.reserve_sat.low >>> 0, object.reserve_sat.high >>> 0).toNumber(true);
            if (object.in_flight_max_msat != null)
                if (typeof object.in_flight_max_msat === "object" ? object.in_flight_max_msat.low || object.in_flight_max_msat.high : $Number(object.in_flight_max_msat) !== 0)
                    if ($util.Long)
                        message.in_flight_max_msat = $util.Long.fromValue(object.in_flight_max_msat, true);
                    else if (typeof object.in_flight_max_msat === "string")
                        message.in_flight_max_msat = $parseInt(object.in_flight_max_msat, 10);
                    else if (typeof object.in_flight_max_msat === "number")
                        message.in_flight_max_msat = object.in_flight_max_msat;
                    else if (typeof object.in_flight_max_msat === "object")
                        message.in_flight_max_msat = new $util.LongBits(object.in_flight_max_msat.low >>> 0, object.in_flight_max_msat.high >>> 0).toNumber(true);
            if (object.max_htlc_count != null)
                if ($Number(object.max_htlc_count) !== 0)
                    message.max_htlc_count = object.max_htlc_count >>> 0;
            if (object.min_htlc_in != null)
                if (typeof object.min_htlc_in === "object" ? object.min_htlc_in.low || object.min_htlc_in.high : $Number(object.min_htlc_in) !== 0)
                    if ($util.Long)
                        message.min_htlc_in = $util.Long.fromValue(object.min_htlc_in, true);
                    else if (typeof object.min_htlc_in === "string")
                        message.min_htlc_in = $parseInt(object.min_htlc_in, 10);
                    else if (typeof object.min_htlc_in === "number")
                        message.min_htlc_in = object.min_htlc_in;
                    else if (typeof object.min_htlc_in === "object")
                        message.min_htlc_in = new $util.LongBits(object.min_htlc_in.low >>> 0, object.min_htlc_in.high >>> 0).toNumber(true);
            if (object.min_accept_depth != null)
                if ($Number(object.min_accept_depth) !== 0)
                    message.min_accept_depth = object.min_accept_depth >>> 0;
            if (object.zero_conf != null)
                if (object.zero_conf)
                    message.zero_conf = $Boolean(object.zero_conf);
            return message;
        };

        /**
         * Creates a plain object from a ChannelAcceptResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelAcceptResponse
         * @static
         * @param {lnrpc.ChannelAcceptResponse} message ChannelAcceptResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelAcceptResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.accept = false;
                if (options.bytes === $String)
                    object.pending_chan_id = "";
                else {
                    object.pending_chan_id = [];
                    if (options.bytes !== $Array)
                        object.pending_chan_id = $util.newBuffer(object.pending_chan_id);
                }
                object.error = "";
                object.upfront_shutdown = "";
                object.csv_delay = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.reserve_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.reserve_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.in_flight_max_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.in_flight_max_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.max_htlc_count = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.min_htlc_in = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min_htlc_in = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.min_accept_depth = 0;
                object.zero_conf = false;
            }
            if (message.accept != null && $Object.hasOwnProperty.call(message, "accept"))
                object.accept = message.accept;
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                object.pending_chan_id = options.bytes === $String ? $util.base64.encode(message.pending_chan_id, 0, message.pending_chan_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pending_chan_id) : message.pending_chan_id;
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                object.error = message.error;
            if (message.upfront_shutdown != null && $Object.hasOwnProperty.call(message, "upfront_shutdown"))
                object.upfront_shutdown = message.upfront_shutdown;
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay"))
                object.csv_delay = message.csv_delay;
            if (message.reserve_sat != null && $Object.hasOwnProperty.call(message, "reserve_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.reserve_sat = typeof message.reserve_sat === "number" ? $BigInt(message.reserve_sat) : $util.Long.fromBits(message.reserve_sat.low >>> 0, message.reserve_sat.high >>> 0, true).toBigInt();
                else if (typeof message.reserve_sat === "number")
                    object.reserve_sat = options.longs === $String ? $String(message.reserve_sat) : message.reserve_sat;
                else
                    object.reserve_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.reserve_sat) : options.longs === $Number ? new $util.LongBits(message.reserve_sat.low >>> 0, message.reserve_sat.high >>> 0).toNumber(true) : message.reserve_sat;
            if (message.in_flight_max_msat != null && $Object.hasOwnProperty.call(message, "in_flight_max_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.in_flight_max_msat = typeof message.in_flight_max_msat === "number" ? $BigInt(message.in_flight_max_msat) : $util.Long.fromBits(message.in_flight_max_msat.low >>> 0, message.in_flight_max_msat.high >>> 0, true).toBigInt();
                else if (typeof message.in_flight_max_msat === "number")
                    object.in_flight_max_msat = options.longs === $String ? $String(message.in_flight_max_msat) : message.in_flight_max_msat;
                else
                    object.in_flight_max_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.in_flight_max_msat) : options.longs === $Number ? new $util.LongBits(message.in_flight_max_msat.low >>> 0, message.in_flight_max_msat.high >>> 0).toNumber(true) : message.in_flight_max_msat;
            if (message.max_htlc_count != null && $Object.hasOwnProperty.call(message, "max_htlc_count"))
                object.max_htlc_count = message.max_htlc_count;
            if (message.min_htlc_in != null && $Object.hasOwnProperty.call(message, "min_htlc_in"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min_htlc_in = typeof message.min_htlc_in === "number" ? $BigInt(message.min_htlc_in) : $util.Long.fromBits(message.min_htlc_in.low >>> 0, message.min_htlc_in.high >>> 0, true).toBigInt();
                else if (typeof message.min_htlc_in === "number")
                    object.min_htlc_in = options.longs === $String ? $String(message.min_htlc_in) : message.min_htlc_in;
                else
                    object.min_htlc_in = options.longs === $String ? $util.Long.prototype.toString.call(message.min_htlc_in) : options.longs === $Number ? new $util.LongBits(message.min_htlc_in.low >>> 0, message.min_htlc_in.high >>> 0).toNumber(true) : message.min_htlc_in;
            if (message.min_accept_depth != null && $Object.hasOwnProperty.call(message, "min_accept_depth"))
                object.min_accept_depth = message.min_accept_depth;
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf"))
                object.zero_conf = message.zero_conf;
            return object;
        };

        /**
         * Converts this ChannelAcceptResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelAcceptResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelAcceptResponse.prototype.toJSON = function() {
            return ChannelAcceptResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelAcceptResponse
         * @function getTypeUrl
         * @memberof lnrpc.ChannelAcceptResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelAcceptResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelAcceptResponse";
        };

        return ChannelAcceptResponse;
    })();

    lnrpc.ChannelPoint = (function() {

        /**
         * Properties of a ChannelPoint.
         * @typedef {Object} lnrpc.ChannelPoint.$Properties
         * @property {Uint8Array|null} [funding_txid_bytes] ChannelPoint funding_txid_bytes
         * @property {string|null} [funding_txid_str] ChannelPoint funding_txid_str
         * @property {number|null} [output_index] ChannelPoint output_index
         * @property {"funding_txid_bytes"|"funding_txid_str"} [funding_txid] ChannelPoint funding_txid
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelPoint.
         * @memberof lnrpc
         * @interface IChannelPoint
         * @augments lnrpc.ChannelPoint.$Properties
         * @deprecated Use lnrpc.ChannelPoint.$Properties instead.
         */

        /**
         * Narrowed shape of a ChannelPoint.
         * @typedef {{
         *   funding_txid_bytes?: Uint8Array|null;
         *   funding_txid_str?: string|null;
         *   output_index?: number|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ funding_txid?: undefined; funding_txid_bytes?: null; funding_txid_str?: null }|{ funding_txid?: "funding_txid_bytes"; funding_txid_bytes: Uint8Array; funding_txid_str?: null }|{ funding_txid?: "funding_txid_str"; funding_txid_bytes?: null; funding_txid_str: string })
         * )} lnrpc.ChannelPoint.$Shape
         */

        /**
         * Constructs a new ChannelPoint.
         * @memberof lnrpc
         * @classdesc Represents a ChannelPoint.
         * @constructor
         * @param {lnrpc.ChannelPoint.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelPoint = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelPoint funding_txid_bytes.
         * @member {Uint8Array|null|undefined} funding_txid_bytes
         * @memberof lnrpc.ChannelPoint
         * @instance
         */
        ChannelPoint.prototype.funding_txid_bytes = null;

        /**
         * ChannelPoint funding_txid_str.
         * @member {string|null|undefined} funding_txid_str
         * @memberof lnrpc.ChannelPoint
         * @instance
         */
        ChannelPoint.prototype.funding_txid_str = null;

        /**
         * ChannelPoint output_index.
         * @member {number} output_index
         * @memberof lnrpc.ChannelPoint
         * @instance
         */
        ChannelPoint.prototype.output_index = 0;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ChannelPoint funding_txid.
         * @member {"funding_txid_bytes"|"funding_txid_str"|undefined} funding_txid
         * @memberof lnrpc.ChannelPoint
         * @instance
         */
        $Object.defineProperty(ChannelPoint.prototype, "funding_txid", {
            get: $util.oneOfGetter($oneOfFields = ["funding_txid_bytes", "funding_txid_str"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ChannelPoint instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelPoint
         * @static
         * @param {lnrpc.ChannelPoint.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelPoint} ChannelPoint instance
         * @type {{
         *   (properties: lnrpc.ChannelPoint.$Shape): lnrpc.ChannelPoint & lnrpc.ChannelPoint.$Shape;
         *   (properties?: lnrpc.ChannelPoint.$Properties): lnrpc.ChannelPoint;
         * }}
         */
        ChannelPoint.create = function(properties) {
            return new ChannelPoint(properties);
        };

        /**
         * Encodes the specified ChannelPoint message. Does not implicitly {@link lnrpc.ChannelPoint.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelPoint
         * @static
         * @param {lnrpc.ChannelPoint.$Properties} message ChannelPoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelPoint.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.funding_txid_bytes != null && $Object.hasOwnProperty.call(message, "funding_txid_bytes"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.funding_txid_bytes);
            if (message.funding_txid_str != null && $Object.hasOwnProperty.call(message, "funding_txid_str"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.funding_txid_str);
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index") && message.output_index !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.output_index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelPoint message, length delimited. Does not implicitly {@link lnrpc.ChannelPoint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelPoint
         * @static
         * @param {lnrpc.ChannelPoint.$Properties} message ChannelPoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelPoint.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelPoint message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelPoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelPoint & lnrpc.ChannelPoint.$Shape} ChannelPoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelPoint.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelPoint(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.funding_txid_bytes = reader.bytes();
                        message.funding_txid = "funding_txid_bytes";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.funding_txid_str = reader.stringVerify();
                        message.funding_txid = "funding_txid_str";
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.output_index = value;
                        else
                            delete message.output_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelPoint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelPoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelPoint & lnrpc.ChannelPoint.$Shape} ChannelPoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelPoint.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelPoint message.
         * @function verify
         * @memberof lnrpc.ChannelPoint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelPoint.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.funding_txid_bytes != null && $Object.hasOwnProperty.call(message, "funding_txid_bytes")) {
                properties.funding_txid = 1;
                if (!(message.funding_txid_bytes && typeof message.funding_txid_bytes.length === "number" || $util.isString(message.funding_txid_bytes)))
                    return "funding_txid_bytes: buffer expected";
            }
            if (message.funding_txid_str != null && $Object.hasOwnProperty.call(message, "funding_txid_str")) {
                if (properties.funding_txid === 1)
                    return "funding_txid: multiple values";
                properties.funding_txid = 1;
                if (!$util.isString(message.funding_txid_str))
                    return "funding_txid_str: string expected";
            }
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index"))
                if (!$util.isInteger(message.output_index))
                    return "output_index: integer expected";
            return null;
        };

        /**
         * Creates a ChannelPoint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelPoint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelPoint} ChannelPoint
         */
        ChannelPoint.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelPoint)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelPoint: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelPoint();
            if (object.funding_txid_bytes != null)
                if (typeof object.funding_txid_bytes === "string")
                    $util.base64.decode(object.funding_txid_bytes, message.funding_txid_bytes = $util.newBuffer($util.base64.length(object.funding_txid_bytes)), 0);
                else if (object.funding_txid_bytes.length >= 0)
                    message.funding_txid_bytes = object.funding_txid_bytes;
            if (object.funding_txid_str != null)
                message.funding_txid_str = $String(object.funding_txid_str);
            if (object.output_index != null)
                if ($Number(object.output_index) !== 0)
                    message.output_index = object.output_index >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ChannelPoint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelPoint
         * @static
         * @param {lnrpc.ChannelPoint} message ChannelPoint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelPoint.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.output_index = 0;
            if (message.funding_txid_bytes != null && $Object.hasOwnProperty.call(message, "funding_txid_bytes")) {
                object.funding_txid_bytes = options.bytes === $String ? $util.base64.encode(message.funding_txid_bytes, 0, message.funding_txid_bytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.funding_txid_bytes) : message.funding_txid_bytes;
                if (options.oneofs)
                    object.funding_txid = "funding_txid_bytes";
            }
            if (message.funding_txid_str != null && $Object.hasOwnProperty.call(message, "funding_txid_str")) {
                object.funding_txid_str = message.funding_txid_str;
                if (options.oneofs)
                    object.funding_txid = "funding_txid_str";
            }
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index"))
                object.output_index = message.output_index;
            return object;
        };

        /**
         * Converts this ChannelPoint to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelPoint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelPoint.prototype.toJSON = function() {
            return ChannelPoint.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelPoint
         * @function getTypeUrl
         * @memberof lnrpc.ChannelPoint
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelPoint.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelPoint";
        };

        return ChannelPoint;
    })();

    lnrpc.OutPoint = (function() {

        /**
         * Properties of an OutPoint.
         * @typedef {Object} lnrpc.OutPoint.$Properties
         * @property {Uint8Array|null} [txid_bytes] OutPoint txid_bytes
         * @property {string|null} [txid_str] OutPoint txid_str
         * @property {number|null} [output_index] OutPoint output_index
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an OutPoint.
         * @memberof lnrpc
         * @interface IOutPoint
         * @augments lnrpc.OutPoint.$Properties
         * @deprecated Use lnrpc.OutPoint.$Properties instead.
         */

        /**
         * Shape of an OutPoint.
         * @typedef {lnrpc.OutPoint.$Properties} lnrpc.OutPoint.$Shape
         */

        /**
         * Constructs a new OutPoint.
         * @memberof lnrpc
         * @classdesc Represents an OutPoint.
         * @constructor
         * @param {lnrpc.OutPoint.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const OutPoint = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OutPoint txid_bytes.
         * @member {Uint8Array} txid_bytes
         * @memberof lnrpc.OutPoint
         * @instance
         */
        OutPoint.prototype.txid_bytes = $util.newBuffer([]);

        /**
         * OutPoint txid_str.
         * @member {string} txid_str
         * @memberof lnrpc.OutPoint
         * @instance
         */
        OutPoint.prototype.txid_str = "";

        /**
         * OutPoint output_index.
         * @member {number} output_index
         * @memberof lnrpc.OutPoint
         * @instance
         */
        OutPoint.prototype.output_index = 0;

        /**
         * Creates a new OutPoint instance using the specified properties.
         * @function create
         * @memberof lnrpc.OutPoint
         * @static
         * @param {lnrpc.OutPoint.$Properties=} [properties] Properties to set
         * @returns {lnrpc.OutPoint} OutPoint instance
         * @type {{
         *   (properties: lnrpc.OutPoint.$Shape): lnrpc.OutPoint & lnrpc.OutPoint.$Shape;
         *   (properties?: lnrpc.OutPoint.$Properties): lnrpc.OutPoint;
         * }}
         */
        OutPoint.create = function(properties) {
            return new OutPoint(properties);
        };

        /**
         * Encodes the specified OutPoint message. Does not implicitly {@link lnrpc.OutPoint.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.OutPoint
         * @static
         * @param {lnrpc.OutPoint.$Properties} message OutPoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OutPoint.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.txid_bytes != null && $Object.hasOwnProperty.call(message, "txid_bytes") && message.txid_bytes.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.txid_bytes);
            if (message.txid_str != null && $Object.hasOwnProperty.call(message, "txid_str") && message.txid_str !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.txid_str);
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index") && message.output_index !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.output_index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified OutPoint message, length delimited. Does not implicitly {@link lnrpc.OutPoint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.OutPoint
         * @static
         * @param {lnrpc.OutPoint.$Properties} message OutPoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OutPoint.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an OutPoint message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.OutPoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.OutPoint & lnrpc.OutPoint.$Shape} OutPoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OutPoint.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.OutPoint(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.txid_bytes = value;
                        else
                            delete message.txid_bytes;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.txid_str = value;
                        else
                            delete message.txid_str;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.output_index = value;
                        else
                            delete message.output_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an OutPoint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.OutPoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.OutPoint & lnrpc.OutPoint.$Shape} OutPoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OutPoint.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OutPoint message.
         * @function verify
         * @memberof lnrpc.OutPoint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OutPoint.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.txid_bytes != null && $Object.hasOwnProperty.call(message, "txid_bytes"))
                if (!(message.txid_bytes && typeof message.txid_bytes.length === "number" || $util.isString(message.txid_bytes)))
                    return "txid_bytes: buffer expected";
            if (message.txid_str != null && $Object.hasOwnProperty.call(message, "txid_str"))
                if (!$util.isString(message.txid_str))
                    return "txid_str: string expected";
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index"))
                if (!$util.isInteger(message.output_index))
                    return "output_index: integer expected";
            return null;
        };

        /**
         * Creates an OutPoint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.OutPoint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.OutPoint} OutPoint
         */
        OutPoint.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.OutPoint)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.OutPoint: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.OutPoint();
            if (object.txid_bytes != null)
                if (object.txid_bytes.length)
                    if (typeof object.txid_bytes === "string")
                        $util.base64.decode(object.txid_bytes, message.txid_bytes = $util.newBuffer($util.base64.length(object.txid_bytes)), 0);
                    else if (object.txid_bytes.length >= 0)
                        message.txid_bytes = object.txid_bytes;
            if (object.txid_str != null)
                if (typeof object.txid_str !== "string" || object.txid_str.length)
                    message.txid_str = $String(object.txid_str);
            if (object.output_index != null)
                if ($Number(object.output_index) !== 0)
                    message.output_index = object.output_index >>> 0;
            return message;
        };

        /**
         * Creates a plain object from an OutPoint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.OutPoint
         * @static
         * @param {lnrpc.OutPoint} message OutPoint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OutPoint.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.txid_bytes = "";
                else {
                    object.txid_bytes = [];
                    if (options.bytes !== $Array)
                        object.txid_bytes = $util.newBuffer(object.txid_bytes);
                }
                object.txid_str = "";
                object.output_index = 0;
            }
            if (message.txid_bytes != null && $Object.hasOwnProperty.call(message, "txid_bytes"))
                object.txid_bytes = options.bytes === $String ? $util.base64.encode(message.txid_bytes, 0, message.txid_bytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.txid_bytes) : message.txid_bytes;
            if (message.txid_str != null && $Object.hasOwnProperty.call(message, "txid_str"))
                object.txid_str = message.txid_str;
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index"))
                object.output_index = message.output_index;
            return object;
        };

        /**
         * Converts this OutPoint to JSON.
         * @function toJSON
         * @memberof lnrpc.OutPoint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OutPoint.prototype.toJSON = function() {
            return OutPoint.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for OutPoint
         * @function getTypeUrl
         * @memberof lnrpc.OutPoint
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        OutPoint.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.OutPoint";
        };

        return OutPoint;
    })();

    lnrpc.PreviousOutPoint = (function() {

        /**
         * Properties of a PreviousOutPoint.
         * @typedef {Object} lnrpc.PreviousOutPoint.$Properties
         * @property {string|null} [outpoint] PreviousOutPoint outpoint
         * @property {boolean|null} [is_our_output] PreviousOutPoint is_our_output
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PreviousOutPoint.
         * @memberof lnrpc
         * @interface IPreviousOutPoint
         * @augments lnrpc.PreviousOutPoint.$Properties
         * @deprecated Use lnrpc.PreviousOutPoint.$Properties instead.
         */

        /**
         * Shape of a PreviousOutPoint.
         * @typedef {lnrpc.PreviousOutPoint.$Properties} lnrpc.PreviousOutPoint.$Shape
         */

        /**
         * Constructs a new PreviousOutPoint.
         * @memberof lnrpc
         * @classdesc Represents a PreviousOutPoint.
         * @constructor
         * @param {lnrpc.PreviousOutPoint.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PreviousOutPoint = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PreviousOutPoint outpoint.
         * @member {string} outpoint
         * @memberof lnrpc.PreviousOutPoint
         * @instance
         */
        PreviousOutPoint.prototype.outpoint = "";

        /**
         * PreviousOutPoint is_our_output.
         * @member {boolean} is_our_output
         * @memberof lnrpc.PreviousOutPoint
         * @instance
         */
        PreviousOutPoint.prototype.is_our_output = false;

        /**
         * Creates a new PreviousOutPoint instance using the specified properties.
         * @function create
         * @memberof lnrpc.PreviousOutPoint
         * @static
         * @param {lnrpc.PreviousOutPoint.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PreviousOutPoint} PreviousOutPoint instance
         * @type {{
         *   (properties: lnrpc.PreviousOutPoint.$Shape): lnrpc.PreviousOutPoint & lnrpc.PreviousOutPoint.$Shape;
         *   (properties?: lnrpc.PreviousOutPoint.$Properties): lnrpc.PreviousOutPoint;
         * }}
         */
        PreviousOutPoint.create = function(properties) {
            return new PreviousOutPoint(properties);
        };

        /**
         * Encodes the specified PreviousOutPoint message. Does not implicitly {@link lnrpc.PreviousOutPoint.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PreviousOutPoint
         * @static
         * @param {lnrpc.PreviousOutPoint.$Properties} message PreviousOutPoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreviousOutPoint.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint") && message.outpoint !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.outpoint);
            if (message.is_our_output != null && $Object.hasOwnProperty.call(message, "is_our_output") && message.is_our_output !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.is_our_output);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PreviousOutPoint message, length delimited. Does not implicitly {@link lnrpc.PreviousOutPoint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PreviousOutPoint
         * @static
         * @param {lnrpc.PreviousOutPoint.$Properties} message PreviousOutPoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreviousOutPoint.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PreviousOutPoint message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PreviousOutPoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PreviousOutPoint & lnrpc.PreviousOutPoint.$Shape} PreviousOutPoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreviousOutPoint.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PreviousOutPoint(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.outpoint = value;
                        else
                            delete message.outpoint;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_our_output = value;
                        else
                            delete message.is_our_output;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PreviousOutPoint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PreviousOutPoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PreviousOutPoint & lnrpc.PreviousOutPoint.$Shape} PreviousOutPoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreviousOutPoint.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PreviousOutPoint message.
         * @function verify
         * @memberof lnrpc.PreviousOutPoint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PreviousOutPoint.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                if (!$util.isString(message.outpoint))
                    return "outpoint: string expected";
            if (message.is_our_output != null && $Object.hasOwnProperty.call(message, "is_our_output"))
                if (typeof message.is_our_output !== "boolean")
                    return "is_our_output: boolean expected";
            return null;
        };

        /**
         * Creates a PreviousOutPoint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PreviousOutPoint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PreviousOutPoint} PreviousOutPoint
         */
        PreviousOutPoint.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PreviousOutPoint)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PreviousOutPoint: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PreviousOutPoint();
            if (object.outpoint != null)
                if (typeof object.outpoint !== "string" || object.outpoint.length)
                    message.outpoint = $String(object.outpoint);
            if (object.is_our_output != null)
                if (object.is_our_output)
                    message.is_our_output = $Boolean(object.is_our_output);
            return message;
        };

        /**
         * Creates a plain object from a PreviousOutPoint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PreviousOutPoint
         * @static
         * @param {lnrpc.PreviousOutPoint} message PreviousOutPoint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PreviousOutPoint.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.outpoint = "";
                object.is_our_output = false;
            }
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = message.outpoint;
            if (message.is_our_output != null && $Object.hasOwnProperty.call(message, "is_our_output"))
                object.is_our_output = message.is_our_output;
            return object;
        };

        /**
         * Converts this PreviousOutPoint to JSON.
         * @function toJSON
         * @memberof lnrpc.PreviousOutPoint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PreviousOutPoint.prototype.toJSON = function() {
            return PreviousOutPoint.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PreviousOutPoint
         * @function getTypeUrl
         * @memberof lnrpc.PreviousOutPoint
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PreviousOutPoint.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PreviousOutPoint";
        };

        return PreviousOutPoint;
    })();

    lnrpc.LightningAddress = (function() {

        /**
         * Properties of a LightningAddress.
         * @typedef {Object} lnrpc.LightningAddress.$Properties
         * @property {string|null} [pubkey] LightningAddress pubkey
         * @property {string|null} [host] LightningAddress host
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LightningAddress.
         * @memberof lnrpc
         * @interface ILightningAddress
         * @augments lnrpc.LightningAddress.$Properties
         * @deprecated Use lnrpc.LightningAddress.$Properties instead.
         */

        /**
         * Shape of a LightningAddress.
         * @typedef {lnrpc.LightningAddress.$Properties} lnrpc.LightningAddress.$Shape
         */

        /**
         * Constructs a new LightningAddress.
         * @memberof lnrpc
         * @classdesc Represents a LightningAddress.
         * @constructor
         * @param {lnrpc.LightningAddress.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LightningAddress = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LightningAddress pubkey.
         * @member {string} pubkey
         * @memberof lnrpc.LightningAddress
         * @instance
         */
        LightningAddress.prototype.pubkey = "";

        /**
         * LightningAddress host.
         * @member {string} host
         * @memberof lnrpc.LightningAddress
         * @instance
         */
        LightningAddress.prototype.host = "";

        /**
         * Creates a new LightningAddress instance using the specified properties.
         * @function create
         * @memberof lnrpc.LightningAddress
         * @static
         * @param {lnrpc.LightningAddress.$Properties=} [properties] Properties to set
         * @returns {lnrpc.LightningAddress} LightningAddress instance
         * @type {{
         *   (properties: lnrpc.LightningAddress.$Shape): lnrpc.LightningAddress & lnrpc.LightningAddress.$Shape;
         *   (properties?: lnrpc.LightningAddress.$Properties): lnrpc.LightningAddress;
         * }}
         */
        LightningAddress.create = function(properties) {
            return new LightningAddress(properties);
        };

        /**
         * Encodes the specified LightningAddress message. Does not implicitly {@link lnrpc.LightningAddress.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.LightningAddress
         * @static
         * @param {lnrpc.LightningAddress.$Properties} message LightningAddress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LightningAddress.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey") && message.pubkey !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pubkey);
            if (message.host != null && $Object.hasOwnProperty.call(message, "host") && message.host !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.host);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LightningAddress message, length delimited. Does not implicitly {@link lnrpc.LightningAddress.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.LightningAddress
         * @static
         * @param {lnrpc.LightningAddress.$Properties} message LightningAddress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LightningAddress.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LightningAddress message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.LightningAddress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.LightningAddress & lnrpc.LightningAddress.$Shape} LightningAddress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LightningAddress.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.LightningAddress(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pubkey = value;
                        else
                            delete message.pubkey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.host = value;
                        else
                            delete message.host;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LightningAddress message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.LightningAddress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.LightningAddress & lnrpc.LightningAddress.$Shape} LightningAddress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LightningAddress.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LightningAddress message.
         * @function verify
         * @memberof lnrpc.LightningAddress
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LightningAddress.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                if (!$util.isString(message.pubkey))
                    return "pubkey: string expected";
            if (message.host != null && $Object.hasOwnProperty.call(message, "host"))
                if (!$util.isString(message.host))
                    return "host: string expected";
            return null;
        };

        /**
         * Creates a LightningAddress message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.LightningAddress
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.LightningAddress} LightningAddress
         */
        LightningAddress.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.LightningAddress)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.LightningAddress: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.LightningAddress();
            if (object.pubkey != null)
                if (typeof object.pubkey !== "string" || object.pubkey.length)
                    message.pubkey = $String(object.pubkey);
            if (object.host != null)
                if (typeof object.host !== "string" || object.host.length)
                    message.host = $String(object.host);
            return message;
        };

        /**
         * Creates a plain object from a LightningAddress message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.LightningAddress
         * @static
         * @param {lnrpc.LightningAddress} message LightningAddress
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LightningAddress.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.pubkey = "";
                object.host = "";
            }
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                object.pubkey = message.pubkey;
            if (message.host != null && $Object.hasOwnProperty.call(message, "host"))
                object.host = message.host;
            return object;
        };

        /**
         * Converts this LightningAddress to JSON.
         * @function toJSON
         * @memberof lnrpc.LightningAddress
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LightningAddress.prototype.toJSON = function() {
            return LightningAddress.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LightningAddress
         * @function getTypeUrl
         * @memberof lnrpc.LightningAddress
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LightningAddress.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.LightningAddress";
        };

        return LightningAddress;
    })();

    /**
     * CoinSelectionStrategy enum.
     * @name lnrpc.CoinSelectionStrategy
     * @enum {number}
     * @property {number} STRATEGY_USE_GLOBAL_CONFIG=0 STRATEGY_USE_GLOBAL_CONFIG value
     * @property {number} STRATEGY_LARGEST=1 STRATEGY_LARGEST value
     * @property {number} STRATEGY_RANDOM=2 STRATEGY_RANDOM value
     */
    lnrpc.CoinSelectionStrategy = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "STRATEGY_USE_GLOBAL_CONFIG"] = 0;
        values[valuesById[1] = "STRATEGY_LARGEST"] = 1;
        values[valuesById[2] = "STRATEGY_RANDOM"] = 2;
        return values;
    })();

    lnrpc.EstimateFeeRequest = (function() {

        /**
         * Properties of an EstimateFeeRequest.
         * @typedef {Object} lnrpc.EstimateFeeRequest.$Properties
         * @property {Object.<string,Long>|null} [AddrToAmount] EstimateFeeRequest AddrToAmount
         * @property {number|null} [target_conf] EstimateFeeRequest target_conf
         * @property {number|null} [min_confs] EstimateFeeRequest min_confs
         * @property {boolean|null} [spend_unconfirmed] EstimateFeeRequest spend_unconfirmed
         * @property {lnrpc.CoinSelectionStrategy|null} [coin_selection_strategy] EstimateFeeRequest coin_selection_strategy
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an EstimateFeeRequest.
         * @memberof lnrpc
         * @interface IEstimateFeeRequest
         * @augments lnrpc.EstimateFeeRequest.$Properties
         * @deprecated Use lnrpc.EstimateFeeRequest.$Properties instead.
         */

        /**
         * Shape of an EstimateFeeRequest.
         * @typedef {lnrpc.EstimateFeeRequest.$Properties} lnrpc.EstimateFeeRequest.$Shape
         */

        /**
         * Constructs a new EstimateFeeRequest.
         * @memberof lnrpc
         * @classdesc Represents an EstimateFeeRequest.
         * @constructor
         * @param {lnrpc.EstimateFeeRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const EstimateFeeRequest = function (properties) {
            this.AddrToAmount = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * EstimateFeeRequest AddrToAmount.
         * @member {Object.<string,Long>} AddrToAmount
         * @memberof lnrpc.EstimateFeeRequest
         * @instance
         */
        EstimateFeeRequest.prototype.AddrToAmount = $util.emptyObject;

        /**
         * EstimateFeeRequest target_conf.
         * @member {number} target_conf
         * @memberof lnrpc.EstimateFeeRequest
         * @instance
         */
        EstimateFeeRequest.prototype.target_conf = 0;

        /**
         * EstimateFeeRequest min_confs.
         * @member {number} min_confs
         * @memberof lnrpc.EstimateFeeRequest
         * @instance
         */
        EstimateFeeRequest.prototype.min_confs = 0;

        /**
         * EstimateFeeRequest spend_unconfirmed.
         * @member {boolean} spend_unconfirmed
         * @memberof lnrpc.EstimateFeeRequest
         * @instance
         */
        EstimateFeeRequest.prototype.spend_unconfirmed = false;

        /**
         * EstimateFeeRequest coin_selection_strategy.
         * @member {lnrpc.CoinSelectionStrategy} coin_selection_strategy
         * @memberof lnrpc.EstimateFeeRequest
         * @instance
         */
        EstimateFeeRequest.prototype.coin_selection_strategy = 0;

        /**
         * Creates a new EstimateFeeRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.EstimateFeeRequest
         * @static
         * @param {lnrpc.EstimateFeeRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.EstimateFeeRequest} EstimateFeeRequest instance
         * @type {{
         *   (properties: lnrpc.EstimateFeeRequest.$Shape): lnrpc.EstimateFeeRequest & lnrpc.EstimateFeeRequest.$Shape;
         *   (properties?: lnrpc.EstimateFeeRequest.$Properties): lnrpc.EstimateFeeRequest;
         * }}
         */
        EstimateFeeRequest.create = function(properties) {
            return new EstimateFeeRequest(properties);
        };

        /**
         * Encodes the specified EstimateFeeRequest message. Does not implicitly {@link lnrpc.EstimateFeeRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.EstimateFeeRequest
         * @static
         * @param {lnrpc.EstimateFeeRequest.$Properties} message EstimateFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EstimateFeeRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.AddrToAmount != null && $Object.hasOwnProperty.call(message, "AddrToAmount"))
                for (let keys = $Object.keys(message.AddrToAmount), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.AddrToAmount[keys[i]]).ldelim();
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf") && message.target_conf !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.target_conf);
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.min_confs);
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed") && message.spend_unconfirmed !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.spend_unconfirmed);
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy") && message.coin_selection_strategy !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.coin_selection_strategy);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EstimateFeeRequest message, length delimited. Does not implicitly {@link lnrpc.EstimateFeeRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.EstimateFeeRequest
         * @static
         * @param {lnrpc.EstimateFeeRequest.$Properties} message EstimateFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EstimateFeeRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EstimateFeeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.EstimateFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.EstimateFeeRequest & lnrpc.EstimateFeeRequest.$Shape} EstimateFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EstimateFeeRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.EstimateFeeRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (message.AddrToAmount === $util.emptyObject)
                            message.AddrToAmount = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, false) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.AddrToAmount, key);
                        message.AddrToAmount[key] = value;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.target_conf = value;
                        else
                            delete message.target_conf;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.spend_unconfirmed = value;
                        else
                            delete message.spend_unconfirmed;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.coin_selection_strategy = value;
                        else
                            delete message.coin_selection_strategy;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EstimateFeeRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.EstimateFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.EstimateFeeRequest & lnrpc.EstimateFeeRequest.$Shape} EstimateFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EstimateFeeRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EstimateFeeRequest message.
         * @function verify
         * @memberof lnrpc.EstimateFeeRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EstimateFeeRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.AddrToAmount != null && $Object.hasOwnProperty.call(message, "AddrToAmount")) {
                if (!$util.isObject(message.AddrToAmount))
                    return "AddrToAmount: object expected";
                let key = $Object.keys(message.AddrToAmount);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.AddrToAmount[key[i]]) && !(message.AddrToAmount[key[i]] && $util.isInteger(message.AddrToAmount[key[i]].low) && $util.isInteger(message.AddrToAmount[key[i]].high)))
                        return "AddrToAmount: integer|Long{k:string} expected";
            }
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                if (!$util.isInteger(message.target_conf))
                    return "target_conf: integer expected";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                if (typeof message.spend_unconfirmed !== "boolean")
                    return "spend_unconfirmed: boolean expected";
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                if (typeof message.coin_selection_strategy !== "number" || (message.coin_selection_strategy | 0) !== message.coin_selection_strategy)
                    return "coin_selection_strategy: enum value expected";
            return null;
        };

        /**
         * Creates an EstimateFeeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.EstimateFeeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.EstimateFeeRequest} EstimateFeeRequest
         */
        EstimateFeeRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.EstimateFeeRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.EstimateFeeRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.EstimateFeeRequest();
            if (object.AddrToAmount) {
                if (!$util.isObject(object.AddrToAmount))
                    throw $TypeError(".lnrpc.EstimateFeeRequest.AddrToAmount: object expected");
                message.AddrToAmount = {};
                for (let keys = $Object.keys(object.AddrToAmount), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.AddrToAmount, keys[i]);
                    if ($util.Long)
                        message.AddrToAmount[keys[i]] = $util.Long.fromValue(object.AddrToAmount[keys[i]], false);
                    else if (typeof object.AddrToAmount[keys[i]] === "string")
                        message.AddrToAmount[keys[i]] = $parseInt(object.AddrToAmount[keys[i]], 10);
                    else if (typeof object.AddrToAmount[keys[i]] === "number")
                        message.AddrToAmount[keys[i]] = object.AddrToAmount[keys[i]];
                    else if (typeof object.AddrToAmount[keys[i]] === "object")
                        message.AddrToAmount[keys[i]] = new $util.LongBits(object.AddrToAmount[keys[i]].low >>> 0, object.AddrToAmount[keys[i]].high >>> 0).toNumber();
                }
            }
            if (object.target_conf != null)
                if ($Number(object.target_conf) !== 0)
                    message.target_conf = object.target_conf | 0;
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            if (object.spend_unconfirmed != null)
                if (object.spend_unconfirmed)
                    message.spend_unconfirmed = $Boolean(object.spend_unconfirmed);
            if (object.coin_selection_strategy !== 0 && (typeof object.coin_selection_strategy !== "string" || $root.lnrpc.CoinSelectionStrategy[object.coin_selection_strategy] !== 0))
                switch (object.coin_selection_strategy) {
                case "STRATEGY_USE_GLOBAL_CONFIG":
                case 0:
                    message.coin_selection_strategy = 0;
                    break;
                case "STRATEGY_LARGEST":
                case 1:
                    message.coin_selection_strategy = 1;
                    break;
                case "STRATEGY_RANDOM":
                case 2:
                    message.coin_selection_strategy = 2;
                    break;
                default:
                    if (typeof object.coin_selection_strategy === "number" && (object.coin_selection_strategy | 0) === object.coin_selection_strategy)
                        message.coin_selection_strategy = object.coin_selection_strategy;
                }
            return message;
        };

        /**
         * Creates a plain object from an EstimateFeeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.EstimateFeeRequest
         * @static
         * @param {lnrpc.EstimateFeeRequest} message EstimateFeeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EstimateFeeRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.AddrToAmount = {};
            if (options.defaults) {
                object.target_conf = 0;
                object.min_confs = 0;
                object.spend_unconfirmed = false;
                object.coin_selection_strategy = options.enums === $String ? "STRATEGY_USE_GLOBAL_CONFIG" : 0;
            }
            let keys2;
            if (message.AddrToAmount && (keys2 = $Object.keys(message.AddrToAmount)).length) {
                object.AddrToAmount = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.AddrToAmount, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.AddrToAmount[keys2[j]] = typeof message.AddrToAmount[keys2[j]] === "number" ? $BigInt(message.AddrToAmount[keys2[j]]) : $util.Long.fromBits(message.AddrToAmount[keys2[j]].low >>> 0, message.AddrToAmount[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.AddrToAmount[keys2[j]] === "number")
                        object.AddrToAmount[keys2[j]] = options.longs === $String ? $String(message.AddrToAmount[keys2[j]]) : message.AddrToAmount[keys2[j]];
                    else
                        object.AddrToAmount[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.AddrToAmount[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.AddrToAmount[keys2[j]].low >>> 0, message.AddrToAmount[keys2[j]].high >>> 0).toNumber() : message.AddrToAmount[keys2[j]];
                }
            }
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                object.target_conf = message.target_conf;
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                object.spend_unconfirmed = message.spend_unconfirmed;
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                object.coin_selection_strategy = options.enums === $String ? $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] === $undefined ? message.coin_selection_strategy : $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] : message.coin_selection_strategy;
            return object;
        };

        /**
         * Converts this EstimateFeeRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.EstimateFeeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EstimateFeeRequest.prototype.toJSON = function() {
            return EstimateFeeRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EstimateFeeRequest
         * @function getTypeUrl
         * @memberof lnrpc.EstimateFeeRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EstimateFeeRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.EstimateFeeRequest";
        };

        return EstimateFeeRequest;
    })();

    lnrpc.EstimateFeeResponse = (function() {

        /**
         * Properties of an EstimateFeeResponse.
         * @typedef {Object} lnrpc.EstimateFeeResponse.$Properties
         * @property {Long|null} [fee_sat] EstimateFeeResponse fee_sat
         * @property {Long|null} [feerate_sat_per_byte] EstimateFeeResponse feerate_sat_per_byte
         * @property {Long|null} [sat_per_vbyte] EstimateFeeResponse sat_per_vbyte
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an EstimateFeeResponse.
         * @memberof lnrpc
         * @interface IEstimateFeeResponse
         * @augments lnrpc.EstimateFeeResponse.$Properties
         * @deprecated Use lnrpc.EstimateFeeResponse.$Properties instead.
         */

        /**
         * Shape of an EstimateFeeResponse.
         * @typedef {lnrpc.EstimateFeeResponse.$Properties} lnrpc.EstimateFeeResponse.$Shape
         */

        /**
         * Constructs a new EstimateFeeResponse.
         * @memberof lnrpc
         * @classdesc Represents an EstimateFeeResponse.
         * @constructor
         * @param {lnrpc.EstimateFeeResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const EstimateFeeResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * EstimateFeeResponse fee_sat.
         * @member {Long} fee_sat
         * @memberof lnrpc.EstimateFeeResponse
         * @instance
         */
        EstimateFeeResponse.prototype.fee_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EstimateFeeResponse feerate_sat_per_byte.
         * @member {Long} feerate_sat_per_byte
         * @memberof lnrpc.EstimateFeeResponse
         * @instance
         */
        EstimateFeeResponse.prototype.feerate_sat_per_byte = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EstimateFeeResponse sat_per_vbyte.
         * @member {Long} sat_per_vbyte
         * @memberof lnrpc.EstimateFeeResponse
         * @instance
         */
        EstimateFeeResponse.prototype.sat_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new EstimateFeeResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.EstimateFeeResponse
         * @static
         * @param {lnrpc.EstimateFeeResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.EstimateFeeResponse} EstimateFeeResponse instance
         * @type {{
         *   (properties: lnrpc.EstimateFeeResponse.$Shape): lnrpc.EstimateFeeResponse & lnrpc.EstimateFeeResponse.$Shape;
         *   (properties?: lnrpc.EstimateFeeResponse.$Properties): lnrpc.EstimateFeeResponse;
         * }}
         */
        EstimateFeeResponse.create = function(properties) {
            return new EstimateFeeResponse(properties);
        };

        /**
         * Encodes the specified EstimateFeeResponse message. Does not implicitly {@link lnrpc.EstimateFeeResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.EstimateFeeResponse
         * @static
         * @param {lnrpc.EstimateFeeResponse.$Properties} message EstimateFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EstimateFeeResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fee_sat != null && $Object.hasOwnProperty.call(message, "fee_sat") && (typeof message.fee_sat === "object" ? message.fee_sat.low || message.fee_sat.high : message.fee_sat !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.fee_sat);
            if (message.feerate_sat_per_byte != null && $Object.hasOwnProperty.call(message, "feerate_sat_per_byte") && (typeof message.feerate_sat_per_byte === "object" ? message.feerate_sat_per_byte.low || message.feerate_sat_per_byte.high : message.feerate_sat_per_byte !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.feerate_sat_per_byte);
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte") && (typeof message.sat_per_vbyte === "object" ? message.sat_per_vbyte.low || message.sat_per_vbyte.high : message.sat_per_vbyte !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.sat_per_vbyte);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EstimateFeeResponse message, length delimited. Does not implicitly {@link lnrpc.EstimateFeeResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.EstimateFeeResponse
         * @static
         * @param {lnrpc.EstimateFeeResponse.$Properties} message EstimateFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EstimateFeeResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EstimateFeeResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.EstimateFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.EstimateFeeResponse & lnrpc.EstimateFeeResponse.$Shape} EstimateFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EstimateFeeResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.EstimateFeeResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_sat = value;
                        else
                            delete message.fee_sat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.feerate_sat_per_byte = value;
                        else
                            delete message.feerate_sat_per_byte;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_vbyte = value;
                        else
                            delete message.sat_per_vbyte;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EstimateFeeResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.EstimateFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.EstimateFeeResponse & lnrpc.EstimateFeeResponse.$Shape} EstimateFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EstimateFeeResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EstimateFeeResponse message.
         * @function verify
         * @memberof lnrpc.EstimateFeeResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EstimateFeeResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fee_sat != null && $Object.hasOwnProperty.call(message, "fee_sat"))
                if (!$util.isInteger(message.fee_sat) && !(message.fee_sat && $util.isInteger(message.fee_sat.low) && $util.isInteger(message.fee_sat.high)))
                    return "fee_sat: integer|Long expected";
            if (message.feerate_sat_per_byte != null && $Object.hasOwnProperty.call(message, "feerate_sat_per_byte"))
                if (!$util.isInteger(message.feerate_sat_per_byte) && !(message.feerate_sat_per_byte && $util.isInteger(message.feerate_sat_per_byte.low) && $util.isInteger(message.feerate_sat_per_byte.high)))
                    return "feerate_sat_per_byte: integer|Long expected";
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (!$util.isInteger(message.sat_per_vbyte) && !(message.sat_per_vbyte && $util.isInteger(message.sat_per_vbyte.low) && $util.isInteger(message.sat_per_vbyte.high)))
                    return "sat_per_vbyte: integer|Long expected";
            return null;
        };

        /**
         * Creates an EstimateFeeResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.EstimateFeeResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.EstimateFeeResponse} EstimateFeeResponse
         */
        EstimateFeeResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.EstimateFeeResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.EstimateFeeResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.EstimateFeeResponse();
            if (object.fee_sat != null)
                if (typeof object.fee_sat === "object" ? object.fee_sat.low || object.fee_sat.high : $Number(object.fee_sat) !== 0)
                    if ($util.Long)
                        message.fee_sat = $util.Long.fromValue(object.fee_sat, false);
                    else if (typeof object.fee_sat === "string")
                        message.fee_sat = $parseInt(object.fee_sat, 10);
                    else if (typeof object.fee_sat === "number")
                        message.fee_sat = object.fee_sat;
                    else if (typeof object.fee_sat === "object")
                        message.fee_sat = new $util.LongBits(object.fee_sat.low >>> 0, object.fee_sat.high >>> 0).toNumber();
            if (object.feerate_sat_per_byte != null)
                if (typeof object.feerate_sat_per_byte === "object" ? object.feerate_sat_per_byte.low || object.feerate_sat_per_byte.high : $Number(object.feerate_sat_per_byte) !== 0)
                    if ($util.Long)
                        message.feerate_sat_per_byte = $util.Long.fromValue(object.feerate_sat_per_byte, false);
                    else if (typeof object.feerate_sat_per_byte === "string")
                        message.feerate_sat_per_byte = $parseInt(object.feerate_sat_per_byte, 10);
                    else if (typeof object.feerate_sat_per_byte === "number")
                        message.feerate_sat_per_byte = object.feerate_sat_per_byte;
                    else if (typeof object.feerate_sat_per_byte === "object")
                        message.feerate_sat_per_byte = new $util.LongBits(object.feerate_sat_per_byte.low >>> 0, object.feerate_sat_per_byte.high >>> 0).toNumber();
            if (object.sat_per_vbyte != null)
                if (typeof object.sat_per_vbyte === "object" ? object.sat_per_vbyte.low || object.sat_per_vbyte.high : $Number(object.sat_per_vbyte) !== 0)
                    if ($util.Long)
                        message.sat_per_vbyte = $util.Long.fromValue(object.sat_per_vbyte, true);
                    else if (typeof object.sat_per_vbyte === "string")
                        message.sat_per_vbyte = $parseInt(object.sat_per_vbyte, 10);
                    else if (typeof object.sat_per_vbyte === "number")
                        message.sat_per_vbyte = object.sat_per_vbyte;
                    else if (typeof object.sat_per_vbyte === "object")
                        message.sat_per_vbyte = new $util.LongBits(object.sat_per_vbyte.low >>> 0, object.sat_per_vbyte.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from an EstimateFeeResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.EstimateFeeResponse
         * @static
         * @param {lnrpc.EstimateFeeResponse} message EstimateFeeResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EstimateFeeResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.feerate_sat_per_byte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.feerate_sat_per_byte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sat_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.fee_sat != null && $Object.hasOwnProperty.call(message, "fee_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_sat = typeof message.fee_sat === "number" ? $BigInt(message.fee_sat) : $util.Long.fromBits(message.fee_sat.low >>> 0, message.fee_sat.high >>> 0, false).toBigInt();
                else if (typeof message.fee_sat === "number")
                    object.fee_sat = options.longs === $String ? $String(message.fee_sat) : message.fee_sat;
                else
                    object.fee_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_sat) : options.longs === $Number ? new $util.LongBits(message.fee_sat.low >>> 0, message.fee_sat.high >>> 0).toNumber() : message.fee_sat;
            if (message.feerate_sat_per_byte != null && $Object.hasOwnProperty.call(message, "feerate_sat_per_byte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.feerate_sat_per_byte = typeof message.feerate_sat_per_byte === "number" ? $BigInt(message.feerate_sat_per_byte) : $util.Long.fromBits(message.feerate_sat_per_byte.low >>> 0, message.feerate_sat_per_byte.high >>> 0, false).toBigInt();
                else if (typeof message.feerate_sat_per_byte === "number")
                    object.feerate_sat_per_byte = options.longs === $String ? $String(message.feerate_sat_per_byte) : message.feerate_sat_per_byte;
                else
                    object.feerate_sat_per_byte = options.longs === $String ? $util.Long.prototype.toString.call(message.feerate_sat_per_byte) : options.longs === $Number ? new $util.LongBits(message.feerate_sat_per_byte.low >>> 0, message.feerate_sat_per_byte.high >>> 0).toNumber() : message.feerate_sat_per_byte;
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_vbyte = typeof message.sat_per_vbyte === "number" ? $BigInt(message.sat_per_vbyte) : $util.Long.fromBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.sat_per_vbyte === "number")
                    object.sat_per_vbyte = options.longs === $String ? $String(message.sat_per_vbyte) : message.sat_per_vbyte;
                else
                    object.sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0).toNumber(true) : message.sat_per_vbyte;
            return object;
        };

        /**
         * Converts this EstimateFeeResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.EstimateFeeResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EstimateFeeResponse.prototype.toJSON = function() {
            return EstimateFeeResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EstimateFeeResponse
         * @function getTypeUrl
         * @memberof lnrpc.EstimateFeeResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EstimateFeeResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.EstimateFeeResponse";
        };

        return EstimateFeeResponse;
    })();

    lnrpc.SendManyRequest = (function() {

        /**
         * Properties of a SendManyRequest.
         * @typedef {Object} lnrpc.SendManyRequest.$Properties
         * @property {Object.<string,Long>|null} [AddrToAmount] SendManyRequest AddrToAmount
         * @property {number|null} [target_conf] SendManyRequest target_conf
         * @property {Long|null} [sat_per_vbyte] SendManyRequest sat_per_vbyte
         * @property {Long|null} [sat_per_byte] SendManyRequest sat_per_byte
         * @property {string|null} [label] SendManyRequest label
         * @property {number|null} [min_confs] SendManyRequest min_confs
         * @property {boolean|null} [spend_unconfirmed] SendManyRequest spend_unconfirmed
         * @property {lnrpc.CoinSelectionStrategy|null} [coin_selection_strategy] SendManyRequest coin_selection_strategy
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendManyRequest.
         * @memberof lnrpc
         * @interface ISendManyRequest
         * @augments lnrpc.SendManyRequest.$Properties
         * @deprecated Use lnrpc.SendManyRequest.$Properties instead.
         */

        /**
         * Shape of a SendManyRequest.
         * @typedef {lnrpc.SendManyRequest.$Properties} lnrpc.SendManyRequest.$Shape
         */

        /**
         * Constructs a new SendManyRequest.
         * @memberof lnrpc
         * @classdesc Represents a SendManyRequest.
         * @constructor
         * @param {lnrpc.SendManyRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendManyRequest = function (properties) {
            this.AddrToAmount = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendManyRequest AddrToAmount.
         * @member {Object.<string,Long>} AddrToAmount
         * @memberof lnrpc.SendManyRequest
         * @instance
         */
        SendManyRequest.prototype.AddrToAmount = $util.emptyObject;

        /**
         * SendManyRequest target_conf.
         * @member {number} target_conf
         * @memberof lnrpc.SendManyRequest
         * @instance
         */
        SendManyRequest.prototype.target_conf = 0;

        /**
         * SendManyRequest sat_per_vbyte.
         * @member {Long} sat_per_vbyte
         * @memberof lnrpc.SendManyRequest
         * @instance
         */
        SendManyRequest.prototype.sat_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * SendManyRequest sat_per_byte.
         * @member {Long} sat_per_byte
         * @memberof lnrpc.SendManyRequest
         * @instance
         */
        SendManyRequest.prototype.sat_per_byte = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendManyRequest label.
         * @member {string} label
         * @memberof lnrpc.SendManyRequest
         * @instance
         */
        SendManyRequest.prototype.label = "";

        /**
         * SendManyRequest min_confs.
         * @member {number} min_confs
         * @memberof lnrpc.SendManyRequest
         * @instance
         */
        SendManyRequest.prototype.min_confs = 0;

        /**
         * SendManyRequest spend_unconfirmed.
         * @member {boolean} spend_unconfirmed
         * @memberof lnrpc.SendManyRequest
         * @instance
         */
        SendManyRequest.prototype.spend_unconfirmed = false;

        /**
         * SendManyRequest coin_selection_strategy.
         * @member {lnrpc.CoinSelectionStrategy} coin_selection_strategy
         * @memberof lnrpc.SendManyRequest
         * @instance
         */
        SendManyRequest.prototype.coin_selection_strategy = 0;

        /**
         * Creates a new SendManyRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.SendManyRequest
         * @static
         * @param {lnrpc.SendManyRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SendManyRequest} SendManyRequest instance
         * @type {{
         *   (properties: lnrpc.SendManyRequest.$Shape): lnrpc.SendManyRequest & lnrpc.SendManyRequest.$Shape;
         *   (properties?: lnrpc.SendManyRequest.$Properties): lnrpc.SendManyRequest;
         * }}
         */
        SendManyRequest.create = function(properties) {
            return new SendManyRequest(properties);
        };

        /**
         * Encodes the specified SendManyRequest message. Does not implicitly {@link lnrpc.SendManyRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SendManyRequest
         * @static
         * @param {lnrpc.SendManyRequest.$Properties} message SendManyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendManyRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.AddrToAmount != null && $Object.hasOwnProperty.call(message, "AddrToAmount"))
                for (let keys = $Object.keys(message.AddrToAmount), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.AddrToAmount[keys[i]]).ldelim();
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf") && message.target_conf !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.target_conf);
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte") && (typeof message.sat_per_vbyte === "object" ? message.sat_per_vbyte.low || message.sat_per_vbyte.high : message.sat_per_vbyte !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.sat_per_vbyte);
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte") && (typeof message.sat_per_byte === "object" ? message.sat_per_byte.low || message.sat_per_byte.high : message.sat_per_byte !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.sat_per_byte);
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.label);
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.min_confs);
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed") && message.spend_unconfirmed !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.spend_unconfirmed);
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy") && message.coin_selection_strategy !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.coin_selection_strategy);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendManyRequest message, length delimited. Does not implicitly {@link lnrpc.SendManyRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SendManyRequest
         * @static
         * @param {lnrpc.SendManyRequest.$Properties} message SendManyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendManyRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendManyRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SendManyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SendManyRequest & lnrpc.SendManyRequest.$Shape} SendManyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendManyRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SendManyRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (message.AddrToAmount === $util.emptyObject)
                            message.AddrToAmount = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, false) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.AddrToAmount, key);
                        message.AddrToAmount[key] = value;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.target_conf = value;
                        else
                            delete message.target_conf;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_vbyte = value;
                        else
                            delete message.sat_per_vbyte;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_byte = value;
                        else
                            delete message.sat_per_byte;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.spend_unconfirmed = value;
                        else
                            delete message.spend_unconfirmed;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.coin_selection_strategy = value;
                        else
                            delete message.coin_selection_strategy;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendManyRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SendManyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SendManyRequest & lnrpc.SendManyRequest.$Shape} SendManyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendManyRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendManyRequest message.
         * @function verify
         * @memberof lnrpc.SendManyRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendManyRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.AddrToAmount != null && $Object.hasOwnProperty.call(message, "AddrToAmount")) {
                if (!$util.isObject(message.AddrToAmount))
                    return "AddrToAmount: object expected";
                let key = $Object.keys(message.AddrToAmount);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.AddrToAmount[key[i]]) && !(message.AddrToAmount[key[i]] && $util.isInteger(message.AddrToAmount[key[i]].low) && $util.isInteger(message.AddrToAmount[key[i]].high)))
                        return "AddrToAmount: integer|Long{k:string} expected";
            }
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                if (!$util.isInteger(message.target_conf))
                    return "target_conf: integer expected";
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (!$util.isInteger(message.sat_per_vbyte) && !(message.sat_per_vbyte && $util.isInteger(message.sat_per_vbyte.low) && $util.isInteger(message.sat_per_vbyte.high)))
                    return "sat_per_vbyte: integer|Long expected";
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (!$util.isInteger(message.sat_per_byte) && !(message.sat_per_byte && $util.isInteger(message.sat_per_byte.low) && $util.isInteger(message.sat_per_byte.high)))
                    return "sat_per_byte: integer|Long expected";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                if (typeof message.spend_unconfirmed !== "boolean")
                    return "spend_unconfirmed: boolean expected";
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                if (typeof message.coin_selection_strategy !== "number" || (message.coin_selection_strategy | 0) !== message.coin_selection_strategy)
                    return "coin_selection_strategy: enum value expected";
            return null;
        };

        /**
         * Creates a SendManyRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SendManyRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SendManyRequest} SendManyRequest
         */
        SendManyRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SendManyRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SendManyRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SendManyRequest();
            if (object.AddrToAmount) {
                if (!$util.isObject(object.AddrToAmount))
                    throw $TypeError(".lnrpc.SendManyRequest.AddrToAmount: object expected");
                message.AddrToAmount = {};
                for (let keys = $Object.keys(object.AddrToAmount), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.AddrToAmount, keys[i]);
                    if ($util.Long)
                        message.AddrToAmount[keys[i]] = $util.Long.fromValue(object.AddrToAmount[keys[i]], false);
                    else if (typeof object.AddrToAmount[keys[i]] === "string")
                        message.AddrToAmount[keys[i]] = $parseInt(object.AddrToAmount[keys[i]], 10);
                    else if (typeof object.AddrToAmount[keys[i]] === "number")
                        message.AddrToAmount[keys[i]] = object.AddrToAmount[keys[i]];
                    else if (typeof object.AddrToAmount[keys[i]] === "object")
                        message.AddrToAmount[keys[i]] = new $util.LongBits(object.AddrToAmount[keys[i]].low >>> 0, object.AddrToAmount[keys[i]].high >>> 0).toNumber();
                }
            }
            if (object.target_conf != null)
                if ($Number(object.target_conf) !== 0)
                    message.target_conf = object.target_conf | 0;
            if (object.sat_per_vbyte != null)
                if (typeof object.sat_per_vbyte === "object" ? object.sat_per_vbyte.low || object.sat_per_vbyte.high : $Number(object.sat_per_vbyte) !== 0)
                    if ($util.Long)
                        message.sat_per_vbyte = $util.Long.fromValue(object.sat_per_vbyte, true);
                    else if (typeof object.sat_per_vbyte === "string")
                        message.sat_per_vbyte = $parseInt(object.sat_per_vbyte, 10);
                    else if (typeof object.sat_per_vbyte === "number")
                        message.sat_per_vbyte = object.sat_per_vbyte;
                    else if (typeof object.sat_per_vbyte === "object")
                        message.sat_per_vbyte = new $util.LongBits(object.sat_per_vbyte.low >>> 0, object.sat_per_vbyte.high >>> 0).toNumber(true);
            if (object.sat_per_byte != null)
                if (typeof object.sat_per_byte === "object" ? object.sat_per_byte.low || object.sat_per_byte.high : $Number(object.sat_per_byte) !== 0)
                    if ($util.Long)
                        message.sat_per_byte = $util.Long.fromValue(object.sat_per_byte, false);
                    else if (typeof object.sat_per_byte === "string")
                        message.sat_per_byte = $parseInt(object.sat_per_byte, 10);
                    else if (typeof object.sat_per_byte === "number")
                        message.sat_per_byte = object.sat_per_byte;
                    else if (typeof object.sat_per_byte === "object")
                        message.sat_per_byte = new $util.LongBits(object.sat_per_byte.low >>> 0, object.sat_per_byte.high >>> 0).toNumber();
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            if (object.spend_unconfirmed != null)
                if (object.spend_unconfirmed)
                    message.spend_unconfirmed = $Boolean(object.spend_unconfirmed);
            if (object.coin_selection_strategy !== 0 && (typeof object.coin_selection_strategy !== "string" || $root.lnrpc.CoinSelectionStrategy[object.coin_selection_strategy] !== 0))
                switch (object.coin_selection_strategy) {
                case "STRATEGY_USE_GLOBAL_CONFIG":
                case 0:
                    message.coin_selection_strategy = 0;
                    break;
                case "STRATEGY_LARGEST":
                case 1:
                    message.coin_selection_strategy = 1;
                    break;
                case "STRATEGY_RANDOM":
                case 2:
                    message.coin_selection_strategy = 2;
                    break;
                default:
                    if (typeof object.coin_selection_strategy === "number" && (object.coin_selection_strategy | 0) === object.coin_selection_strategy)
                        message.coin_selection_strategy = object.coin_selection_strategy;
                }
            return message;
        };

        /**
         * Creates a plain object from a SendManyRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SendManyRequest
         * @static
         * @param {lnrpc.SendManyRequest} message SendManyRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendManyRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.AddrToAmount = {};
            if (options.defaults) {
                object.target_conf = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sat_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sat_per_byte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_byte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.label = "";
                object.min_confs = 0;
                object.spend_unconfirmed = false;
                object.coin_selection_strategy = options.enums === $String ? "STRATEGY_USE_GLOBAL_CONFIG" : 0;
            }
            let keys2;
            if (message.AddrToAmount && (keys2 = $Object.keys(message.AddrToAmount)).length) {
                object.AddrToAmount = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.AddrToAmount, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.AddrToAmount[keys2[j]] = typeof message.AddrToAmount[keys2[j]] === "number" ? $BigInt(message.AddrToAmount[keys2[j]]) : $util.Long.fromBits(message.AddrToAmount[keys2[j]].low >>> 0, message.AddrToAmount[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.AddrToAmount[keys2[j]] === "number")
                        object.AddrToAmount[keys2[j]] = options.longs === $String ? $String(message.AddrToAmount[keys2[j]]) : message.AddrToAmount[keys2[j]];
                    else
                        object.AddrToAmount[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.AddrToAmount[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.AddrToAmount[keys2[j]].low >>> 0, message.AddrToAmount[keys2[j]].high >>> 0).toNumber() : message.AddrToAmount[keys2[j]];
                }
            }
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                object.target_conf = message.target_conf;
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_vbyte = typeof message.sat_per_vbyte === "number" ? $BigInt(message.sat_per_vbyte) : $util.Long.fromBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.sat_per_vbyte === "number")
                    object.sat_per_vbyte = options.longs === $String ? $String(message.sat_per_vbyte) : message.sat_per_vbyte;
                else
                    object.sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0).toNumber(true) : message.sat_per_vbyte;
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_byte = typeof message.sat_per_byte === "number" ? $BigInt(message.sat_per_byte) : $util.Long.fromBits(message.sat_per_byte.low >>> 0, message.sat_per_byte.high >>> 0, false).toBigInt();
                else if (typeof message.sat_per_byte === "number")
                    object.sat_per_byte = options.longs === $String ? $String(message.sat_per_byte) : message.sat_per_byte;
                else
                    object.sat_per_byte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_byte) : options.longs === $Number ? new $util.LongBits(message.sat_per_byte.low >>> 0, message.sat_per_byte.high >>> 0).toNumber() : message.sat_per_byte;
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                object.spend_unconfirmed = message.spend_unconfirmed;
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                object.coin_selection_strategy = options.enums === $String ? $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] === $undefined ? message.coin_selection_strategy : $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] : message.coin_selection_strategy;
            return object;
        };

        /**
         * Converts this SendManyRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.SendManyRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendManyRequest.prototype.toJSON = function() {
            return SendManyRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendManyRequest
         * @function getTypeUrl
         * @memberof lnrpc.SendManyRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendManyRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SendManyRequest";
        };

        return SendManyRequest;
    })();

    lnrpc.SendManyResponse = (function() {

        /**
         * Properties of a SendManyResponse.
         * @typedef {Object} lnrpc.SendManyResponse.$Properties
         * @property {string|null} [txid] SendManyResponse txid
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendManyResponse.
         * @memberof lnrpc
         * @interface ISendManyResponse
         * @augments lnrpc.SendManyResponse.$Properties
         * @deprecated Use lnrpc.SendManyResponse.$Properties instead.
         */

        /**
         * Shape of a SendManyResponse.
         * @typedef {lnrpc.SendManyResponse.$Properties} lnrpc.SendManyResponse.$Shape
         */

        /**
         * Constructs a new SendManyResponse.
         * @memberof lnrpc
         * @classdesc Represents a SendManyResponse.
         * @constructor
         * @param {lnrpc.SendManyResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendManyResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendManyResponse txid.
         * @member {string} txid
         * @memberof lnrpc.SendManyResponse
         * @instance
         */
        SendManyResponse.prototype.txid = "";

        /**
         * Creates a new SendManyResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.SendManyResponse
         * @static
         * @param {lnrpc.SendManyResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SendManyResponse} SendManyResponse instance
         * @type {{
         *   (properties: lnrpc.SendManyResponse.$Shape): lnrpc.SendManyResponse & lnrpc.SendManyResponse.$Shape;
         *   (properties?: lnrpc.SendManyResponse.$Properties): lnrpc.SendManyResponse;
         * }}
         */
        SendManyResponse.create = function(properties) {
            return new SendManyResponse(properties);
        };

        /**
         * Encodes the specified SendManyResponse message. Does not implicitly {@link lnrpc.SendManyResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SendManyResponse
         * @static
         * @param {lnrpc.SendManyResponse.$Properties} message SendManyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendManyResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid") && message.txid !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.txid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendManyResponse message, length delimited. Does not implicitly {@link lnrpc.SendManyResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SendManyResponse
         * @static
         * @param {lnrpc.SendManyResponse.$Properties} message SendManyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendManyResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendManyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SendManyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SendManyResponse & lnrpc.SendManyResponse.$Shape} SendManyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendManyResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SendManyResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.txid = value;
                        else
                            delete message.txid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendManyResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SendManyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SendManyResponse & lnrpc.SendManyResponse.$Shape} SendManyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendManyResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendManyResponse message.
         * @function verify
         * @memberof lnrpc.SendManyResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendManyResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                if (!$util.isString(message.txid))
                    return "txid: string expected";
            return null;
        };

        /**
         * Creates a SendManyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SendManyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SendManyResponse} SendManyResponse
         */
        SendManyResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SendManyResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SendManyResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SendManyResponse();
            if (object.txid != null)
                if (typeof object.txid !== "string" || object.txid.length)
                    message.txid = $String(object.txid);
            return message;
        };

        /**
         * Creates a plain object from a SendManyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SendManyResponse
         * @static
         * @param {lnrpc.SendManyResponse} message SendManyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendManyResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.txid = "";
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                object.txid = message.txid;
            return object;
        };

        /**
         * Converts this SendManyResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.SendManyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendManyResponse.prototype.toJSON = function() {
            return SendManyResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendManyResponse
         * @function getTypeUrl
         * @memberof lnrpc.SendManyResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendManyResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SendManyResponse";
        };

        return SendManyResponse;
    })();

    lnrpc.SendCoinsRequest = (function() {

        /**
         * Properties of a SendCoinsRequest.
         * @typedef {Object} lnrpc.SendCoinsRequest.$Properties
         * @property {string|null} [addr] SendCoinsRequest addr
         * @property {Long|null} [amount] SendCoinsRequest amount
         * @property {number|null} [target_conf] SendCoinsRequest target_conf
         * @property {Long|null} [sat_per_vbyte] SendCoinsRequest sat_per_vbyte
         * @property {Long|null} [sat_per_byte] SendCoinsRequest sat_per_byte
         * @property {boolean|null} [send_all] SendCoinsRequest send_all
         * @property {string|null} [label] SendCoinsRequest label
         * @property {number|null} [min_confs] SendCoinsRequest min_confs
         * @property {boolean|null} [spend_unconfirmed] SendCoinsRequest spend_unconfirmed
         * @property {lnrpc.CoinSelectionStrategy|null} [coin_selection_strategy] SendCoinsRequest coin_selection_strategy
         * @property {Array.<lnrpc.OutPoint.$Properties>|null} [outpoints] SendCoinsRequest outpoints
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendCoinsRequest.
         * @memberof lnrpc
         * @interface ISendCoinsRequest
         * @augments lnrpc.SendCoinsRequest.$Properties
         * @deprecated Use lnrpc.SendCoinsRequest.$Properties instead.
         */

        /**
         * Shape of a SendCoinsRequest.
         * @typedef {lnrpc.SendCoinsRequest.$Properties} lnrpc.SendCoinsRequest.$Shape
         */

        /**
         * Constructs a new SendCoinsRequest.
         * @memberof lnrpc
         * @classdesc Represents a SendCoinsRequest.
         * @constructor
         * @param {lnrpc.SendCoinsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendCoinsRequest = function (properties) {
            this.outpoints = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendCoinsRequest addr.
         * @member {string} addr
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.addr = "";

        /**
         * SendCoinsRequest amount.
         * @member {Long} amount
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendCoinsRequest target_conf.
         * @member {number} target_conf
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.target_conf = 0;

        /**
         * SendCoinsRequest sat_per_vbyte.
         * @member {Long} sat_per_vbyte
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.sat_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * SendCoinsRequest sat_per_byte.
         * @member {Long} sat_per_byte
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.sat_per_byte = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendCoinsRequest send_all.
         * @member {boolean} send_all
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.send_all = false;

        /**
         * SendCoinsRequest label.
         * @member {string} label
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.label = "";

        /**
         * SendCoinsRequest min_confs.
         * @member {number} min_confs
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.min_confs = 0;

        /**
         * SendCoinsRequest spend_unconfirmed.
         * @member {boolean} spend_unconfirmed
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.spend_unconfirmed = false;

        /**
         * SendCoinsRequest coin_selection_strategy.
         * @member {lnrpc.CoinSelectionStrategy} coin_selection_strategy
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.coin_selection_strategy = 0;

        /**
         * SendCoinsRequest outpoints.
         * @member {Array.<lnrpc.OutPoint.$Properties>} outpoints
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         */
        SendCoinsRequest.prototype.outpoints = $util.emptyArray;

        /**
         * Creates a new SendCoinsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.SendCoinsRequest
         * @static
         * @param {lnrpc.SendCoinsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SendCoinsRequest} SendCoinsRequest instance
         * @type {{
         *   (properties: lnrpc.SendCoinsRequest.$Shape): lnrpc.SendCoinsRequest & lnrpc.SendCoinsRequest.$Shape;
         *   (properties?: lnrpc.SendCoinsRequest.$Properties): lnrpc.SendCoinsRequest;
         * }}
         */
        SendCoinsRequest.create = function(properties) {
            return new SendCoinsRequest(properties);
        };

        /**
         * Encodes the specified SendCoinsRequest message. Does not implicitly {@link lnrpc.SendCoinsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SendCoinsRequest
         * @static
         * @param {lnrpc.SendCoinsRequest.$Properties} message SendCoinsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendCoinsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr") && message.addr !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.addr);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amount);
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf") && message.target_conf !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.target_conf);
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte") && (typeof message.sat_per_vbyte === "object" ? message.sat_per_vbyte.low || message.sat_per_vbyte.high : message.sat_per_vbyte !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.sat_per_vbyte);
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte") && (typeof message.sat_per_byte === "object" ? message.sat_per_byte.low || message.sat_per_byte.high : message.sat_per_byte !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.sat_per_byte);
            if (message.send_all != null && $Object.hasOwnProperty.call(message, "send_all") && message.send_all !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.send_all);
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.label);
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.min_confs);
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed") && message.spend_unconfirmed !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.spend_unconfirmed);
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy") && message.coin_selection_strategy !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.coin_selection_strategy);
            if (message.outpoints != null && message.outpoints.length)
                for (let i = 0; i < message.outpoints.length; ++i)
                    $root.lnrpc.OutPoint.encode(message.outpoints[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendCoinsRequest message, length delimited. Does not implicitly {@link lnrpc.SendCoinsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SendCoinsRequest
         * @static
         * @param {lnrpc.SendCoinsRequest.$Properties} message SendCoinsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendCoinsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendCoinsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SendCoinsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SendCoinsRequest & lnrpc.SendCoinsRequest.$Shape} SendCoinsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendCoinsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SendCoinsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.addr = value;
                        else
                            delete message.addr;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.target_conf = value;
                        else
                            delete message.target_conf;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_vbyte = value;
                        else
                            delete message.sat_per_vbyte;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_byte = value;
                        else
                            delete message.sat_per_byte;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.send_all = value;
                        else
                            delete message.send_all;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.spend_unconfirmed = value;
                        else
                            delete message.spend_unconfirmed;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.coin_selection_strategy = value;
                        else
                            delete message.coin_selection_strategy;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.outpoints && message.outpoints.length))
                            message.outpoints = [];
                        message.outpoints.push($root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendCoinsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SendCoinsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SendCoinsRequest & lnrpc.SendCoinsRequest.$Shape} SendCoinsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendCoinsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendCoinsRequest message.
         * @function verify
         * @memberof lnrpc.SendCoinsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendCoinsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                if (!$util.isString(message.addr))
                    return "addr: string expected";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                if (!$util.isInteger(message.target_conf))
                    return "target_conf: integer expected";
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (!$util.isInteger(message.sat_per_vbyte) && !(message.sat_per_vbyte && $util.isInteger(message.sat_per_vbyte.low) && $util.isInteger(message.sat_per_vbyte.high)))
                    return "sat_per_vbyte: integer|Long expected";
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (!$util.isInteger(message.sat_per_byte) && !(message.sat_per_byte && $util.isInteger(message.sat_per_byte.low) && $util.isInteger(message.sat_per_byte.high)))
                    return "sat_per_byte: integer|Long expected";
            if (message.send_all != null && $Object.hasOwnProperty.call(message, "send_all"))
                if (typeof message.send_all !== "boolean")
                    return "send_all: boolean expected";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                if (typeof message.spend_unconfirmed !== "boolean")
                    return "spend_unconfirmed: boolean expected";
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                if (typeof message.coin_selection_strategy !== "number" || (message.coin_selection_strategy | 0) !== message.coin_selection_strategy)
                    return "coin_selection_strategy: enum value expected";
            if (message.outpoints != null && $Object.hasOwnProperty.call(message, "outpoints")) {
                if (!$Array.isArray(message.outpoints))
                    return "outpoints: array expected";
                for (let i = 0; i < message.outpoints.length; ++i) {
                    let error = $root.lnrpc.OutPoint.verify(message.outpoints[i], _depth + 1);
                    if (error)
                        return "outpoints." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SendCoinsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SendCoinsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SendCoinsRequest} SendCoinsRequest
         */
        SendCoinsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SendCoinsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SendCoinsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SendCoinsRequest();
            if (object.addr != null)
                if (typeof object.addr !== "string" || object.addr.length)
                    message.addr = $String(object.addr);
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.target_conf != null)
                if ($Number(object.target_conf) !== 0)
                    message.target_conf = object.target_conf | 0;
            if (object.sat_per_vbyte != null)
                if (typeof object.sat_per_vbyte === "object" ? object.sat_per_vbyte.low || object.sat_per_vbyte.high : $Number(object.sat_per_vbyte) !== 0)
                    if ($util.Long)
                        message.sat_per_vbyte = $util.Long.fromValue(object.sat_per_vbyte, true);
                    else if (typeof object.sat_per_vbyte === "string")
                        message.sat_per_vbyte = $parseInt(object.sat_per_vbyte, 10);
                    else if (typeof object.sat_per_vbyte === "number")
                        message.sat_per_vbyte = object.sat_per_vbyte;
                    else if (typeof object.sat_per_vbyte === "object")
                        message.sat_per_vbyte = new $util.LongBits(object.sat_per_vbyte.low >>> 0, object.sat_per_vbyte.high >>> 0).toNumber(true);
            if (object.sat_per_byte != null)
                if (typeof object.sat_per_byte === "object" ? object.sat_per_byte.low || object.sat_per_byte.high : $Number(object.sat_per_byte) !== 0)
                    if ($util.Long)
                        message.sat_per_byte = $util.Long.fromValue(object.sat_per_byte, false);
                    else if (typeof object.sat_per_byte === "string")
                        message.sat_per_byte = $parseInt(object.sat_per_byte, 10);
                    else if (typeof object.sat_per_byte === "number")
                        message.sat_per_byte = object.sat_per_byte;
                    else if (typeof object.sat_per_byte === "object")
                        message.sat_per_byte = new $util.LongBits(object.sat_per_byte.low >>> 0, object.sat_per_byte.high >>> 0).toNumber();
            if (object.send_all != null)
                if (object.send_all)
                    message.send_all = $Boolean(object.send_all);
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            if (object.spend_unconfirmed != null)
                if (object.spend_unconfirmed)
                    message.spend_unconfirmed = $Boolean(object.spend_unconfirmed);
            if (object.coin_selection_strategy !== 0 && (typeof object.coin_selection_strategy !== "string" || $root.lnrpc.CoinSelectionStrategy[object.coin_selection_strategy] !== 0))
                switch (object.coin_selection_strategy) {
                case "STRATEGY_USE_GLOBAL_CONFIG":
                case 0:
                    message.coin_selection_strategy = 0;
                    break;
                case "STRATEGY_LARGEST":
                case 1:
                    message.coin_selection_strategy = 1;
                    break;
                case "STRATEGY_RANDOM":
                case 2:
                    message.coin_selection_strategy = 2;
                    break;
                default:
                    if (typeof object.coin_selection_strategy === "number" && (object.coin_selection_strategy | 0) === object.coin_selection_strategy)
                        message.coin_selection_strategy = object.coin_selection_strategy;
                }
            if (object.outpoints) {
                if (!$Array.isArray(object.outpoints))
                    throw $TypeError(".lnrpc.SendCoinsRequest.outpoints: array expected");
                message.outpoints = $Array(object.outpoints.length);
                for (let i = 0; i < object.outpoints.length; ++i) {
                    if (!$util.isObject(object.outpoints[i]))
                        throw $TypeError(".lnrpc.SendCoinsRequest.outpoints: object expected");
                    message.outpoints[i] = $root.lnrpc.OutPoint.fromObject(object.outpoints[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SendCoinsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SendCoinsRequest
         * @static
         * @param {lnrpc.SendCoinsRequest} message SendCoinsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendCoinsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.outpoints = [];
            if (options.defaults) {
                object.addr = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.target_conf = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sat_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sat_per_byte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_byte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.send_all = false;
                object.label = "";
                object.min_confs = 0;
                object.spend_unconfirmed = false;
                object.coin_selection_strategy = options.enums === $String ? "STRATEGY_USE_GLOBAL_CONFIG" : 0;
            }
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                object.addr = message.addr;
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                object.target_conf = message.target_conf;
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_vbyte = typeof message.sat_per_vbyte === "number" ? $BigInt(message.sat_per_vbyte) : $util.Long.fromBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.sat_per_vbyte === "number")
                    object.sat_per_vbyte = options.longs === $String ? $String(message.sat_per_vbyte) : message.sat_per_vbyte;
                else
                    object.sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0).toNumber(true) : message.sat_per_vbyte;
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_byte = typeof message.sat_per_byte === "number" ? $BigInt(message.sat_per_byte) : $util.Long.fromBits(message.sat_per_byte.low >>> 0, message.sat_per_byte.high >>> 0, false).toBigInt();
                else if (typeof message.sat_per_byte === "number")
                    object.sat_per_byte = options.longs === $String ? $String(message.sat_per_byte) : message.sat_per_byte;
                else
                    object.sat_per_byte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_byte) : options.longs === $Number ? new $util.LongBits(message.sat_per_byte.low >>> 0, message.sat_per_byte.high >>> 0).toNumber() : message.sat_per_byte;
            if (message.send_all != null && $Object.hasOwnProperty.call(message, "send_all"))
                object.send_all = message.send_all;
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                object.spend_unconfirmed = message.spend_unconfirmed;
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                object.coin_selection_strategy = options.enums === $String ? $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] === $undefined ? message.coin_selection_strategy : $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] : message.coin_selection_strategy;
            if (message.outpoints && message.outpoints.length) {
                object.outpoints = $Array(message.outpoints.length);
                for (let j = 0; j < message.outpoints.length; ++j)
                    object.outpoints[j] = $root.lnrpc.OutPoint.toObject(message.outpoints[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this SendCoinsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.SendCoinsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendCoinsRequest.prototype.toJSON = function() {
            return SendCoinsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendCoinsRequest
         * @function getTypeUrl
         * @memberof lnrpc.SendCoinsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendCoinsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SendCoinsRequest";
        };

        return SendCoinsRequest;
    })();

    lnrpc.SendCoinsResponse = (function() {

        /**
         * Properties of a SendCoinsResponse.
         * @typedef {Object} lnrpc.SendCoinsResponse.$Properties
         * @property {string|null} [txid] SendCoinsResponse txid
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendCoinsResponse.
         * @memberof lnrpc
         * @interface ISendCoinsResponse
         * @augments lnrpc.SendCoinsResponse.$Properties
         * @deprecated Use lnrpc.SendCoinsResponse.$Properties instead.
         */

        /**
         * Shape of a SendCoinsResponse.
         * @typedef {lnrpc.SendCoinsResponse.$Properties} lnrpc.SendCoinsResponse.$Shape
         */

        /**
         * Constructs a new SendCoinsResponse.
         * @memberof lnrpc
         * @classdesc Represents a SendCoinsResponse.
         * @constructor
         * @param {lnrpc.SendCoinsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendCoinsResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendCoinsResponse txid.
         * @member {string} txid
         * @memberof lnrpc.SendCoinsResponse
         * @instance
         */
        SendCoinsResponse.prototype.txid = "";

        /**
         * Creates a new SendCoinsResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.SendCoinsResponse
         * @static
         * @param {lnrpc.SendCoinsResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SendCoinsResponse} SendCoinsResponse instance
         * @type {{
         *   (properties: lnrpc.SendCoinsResponse.$Shape): lnrpc.SendCoinsResponse & lnrpc.SendCoinsResponse.$Shape;
         *   (properties?: lnrpc.SendCoinsResponse.$Properties): lnrpc.SendCoinsResponse;
         * }}
         */
        SendCoinsResponse.create = function(properties) {
            return new SendCoinsResponse(properties);
        };

        /**
         * Encodes the specified SendCoinsResponse message. Does not implicitly {@link lnrpc.SendCoinsResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SendCoinsResponse
         * @static
         * @param {lnrpc.SendCoinsResponse.$Properties} message SendCoinsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendCoinsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid") && message.txid !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.txid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendCoinsResponse message, length delimited. Does not implicitly {@link lnrpc.SendCoinsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SendCoinsResponse
         * @static
         * @param {lnrpc.SendCoinsResponse.$Properties} message SendCoinsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendCoinsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendCoinsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SendCoinsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SendCoinsResponse & lnrpc.SendCoinsResponse.$Shape} SendCoinsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendCoinsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SendCoinsResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.txid = value;
                        else
                            delete message.txid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendCoinsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SendCoinsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SendCoinsResponse & lnrpc.SendCoinsResponse.$Shape} SendCoinsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendCoinsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendCoinsResponse message.
         * @function verify
         * @memberof lnrpc.SendCoinsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendCoinsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                if (!$util.isString(message.txid))
                    return "txid: string expected";
            return null;
        };

        /**
         * Creates a SendCoinsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SendCoinsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SendCoinsResponse} SendCoinsResponse
         */
        SendCoinsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SendCoinsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SendCoinsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SendCoinsResponse();
            if (object.txid != null)
                if (typeof object.txid !== "string" || object.txid.length)
                    message.txid = $String(object.txid);
            return message;
        };

        /**
         * Creates a plain object from a SendCoinsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SendCoinsResponse
         * @static
         * @param {lnrpc.SendCoinsResponse} message SendCoinsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendCoinsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.txid = "";
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                object.txid = message.txid;
            return object;
        };

        /**
         * Converts this SendCoinsResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.SendCoinsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendCoinsResponse.prototype.toJSON = function() {
            return SendCoinsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendCoinsResponse
         * @function getTypeUrl
         * @memberof lnrpc.SendCoinsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendCoinsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SendCoinsResponse";
        };

        return SendCoinsResponse;
    })();

    lnrpc.ListUnspentRequest = (function() {

        /**
         * Properties of a ListUnspentRequest.
         * @typedef {Object} lnrpc.ListUnspentRequest.$Properties
         * @property {number|null} [min_confs] ListUnspentRequest min_confs
         * @property {number|null} [max_confs] ListUnspentRequest max_confs
         * @property {string|null} [account] ListUnspentRequest account
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListUnspentRequest.
         * @memberof lnrpc
         * @interface IListUnspentRequest
         * @augments lnrpc.ListUnspentRequest.$Properties
         * @deprecated Use lnrpc.ListUnspentRequest.$Properties instead.
         */

        /**
         * Shape of a ListUnspentRequest.
         * @typedef {lnrpc.ListUnspentRequest.$Properties} lnrpc.ListUnspentRequest.$Shape
         */

        /**
         * Constructs a new ListUnspentRequest.
         * @memberof lnrpc
         * @classdesc Represents a ListUnspentRequest.
         * @constructor
         * @param {lnrpc.ListUnspentRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListUnspentRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListUnspentRequest min_confs.
         * @member {number} min_confs
         * @memberof lnrpc.ListUnspentRequest
         * @instance
         */
        ListUnspentRequest.prototype.min_confs = 0;

        /**
         * ListUnspentRequest max_confs.
         * @member {number} max_confs
         * @memberof lnrpc.ListUnspentRequest
         * @instance
         */
        ListUnspentRequest.prototype.max_confs = 0;

        /**
         * ListUnspentRequest account.
         * @member {string} account
         * @memberof lnrpc.ListUnspentRequest
         * @instance
         */
        ListUnspentRequest.prototype.account = "";

        /**
         * Creates a new ListUnspentRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListUnspentRequest
         * @static
         * @param {lnrpc.ListUnspentRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListUnspentRequest} ListUnspentRequest instance
         * @type {{
         *   (properties: lnrpc.ListUnspentRequest.$Shape): lnrpc.ListUnspentRequest & lnrpc.ListUnspentRequest.$Shape;
         *   (properties?: lnrpc.ListUnspentRequest.$Properties): lnrpc.ListUnspentRequest;
         * }}
         */
        ListUnspentRequest.create = function(properties) {
            return new ListUnspentRequest(properties);
        };

        /**
         * Encodes the specified ListUnspentRequest message. Does not implicitly {@link lnrpc.ListUnspentRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListUnspentRequest
         * @static
         * @param {lnrpc.ListUnspentRequest.$Properties} message ListUnspentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListUnspentRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.min_confs);
            if (message.max_confs != null && $Object.hasOwnProperty.call(message, "max_confs") && message.max_confs !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.max_confs);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account") && message.account !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.account);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListUnspentRequest message, length delimited. Does not implicitly {@link lnrpc.ListUnspentRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListUnspentRequest
         * @static
         * @param {lnrpc.ListUnspentRequest.$Properties} message ListUnspentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListUnspentRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListUnspentRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListUnspentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListUnspentRequest & lnrpc.ListUnspentRequest.$Shape} ListUnspentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListUnspentRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListUnspentRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.max_confs = value;
                        else
                            delete message.max_confs;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListUnspentRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListUnspentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListUnspentRequest & lnrpc.ListUnspentRequest.$Shape} ListUnspentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListUnspentRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListUnspentRequest message.
         * @function verify
         * @memberof lnrpc.ListUnspentRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListUnspentRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            if (message.max_confs != null && $Object.hasOwnProperty.call(message, "max_confs"))
                if (!$util.isInteger(message.max_confs))
                    return "max_confs: integer expected";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            return null;
        };

        /**
         * Creates a ListUnspentRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListUnspentRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListUnspentRequest} ListUnspentRequest
         */
        ListUnspentRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListUnspentRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListUnspentRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListUnspentRequest();
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            if (object.max_confs != null)
                if ($Number(object.max_confs) !== 0)
                    message.max_confs = object.max_confs | 0;
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = $String(object.account);
            return message;
        };

        /**
         * Creates a plain object from a ListUnspentRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListUnspentRequest
         * @static
         * @param {lnrpc.ListUnspentRequest} message ListUnspentRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListUnspentRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.min_confs = 0;
                object.max_confs = 0;
                object.account = "";
            }
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            if (message.max_confs != null && $Object.hasOwnProperty.call(message, "max_confs"))
                object.max_confs = message.max_confs;
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = message.account;
            return object;
        };

        /**
         * Converts this ListUnspentRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ListUnspentRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListUnspentRequest.prototype.toJSON = function() {
            return ListUnspentRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListUnspentRequest
         * @function getTypeUrl
         * @memberof lnrpc.ListUnspentRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListUnspentRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListUnspentRequest";
        };

        return ListUnspentRequest;
    })();

    lnrpc.ListUnspentResponse = (function() {

        /**
         * Properties of a ListUnspentResponse.
         * @typedef {Object} lnrpc.ListUnspentResponse.$Properties
         * @property {Array.<lnrpc.Utxo.$Properties>|null} [utxos] ListUnspentResponse utxos
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListUnspentResponse.
         * @memberof lnrpc
         * @interface IListUnspentResponse
         * @augments lnrpc.ListUnspentResponse.$Properties
         * @deprecated Use lnrpc.ListUnspentResponse.$Properties instead.
         */

        /**
         * Shape of a ListUnspentResponse.
         * @typedef {lnrpc.ListUnspentResponse.$Properties} lnrpc.ListUnspentResponse.$Shape
         */

        /**
         * Constructs a new ListUnspentResponse.
         * @memberof lnrpc
         * @classdesc Represents a ListUnspentResponse.
         * @constructor
         * @param {lnrpc.ListUnspentResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListUnspentResponse = function (properties) {
            this.utxos = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListUnspentResponse utxos.
         * @member {Array.<lnrpc.Utxo.$Properties>} utxos
         * @memberof lnrpc.ListUnspentResponse
         * @instance
         */
        ListUnspentResponse.prototype.utxos = $util.emptyArray;

        /**
         * Creates a new ListUnspentResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListUnspentResponse
         * @static
         * @param {lnrpc.ListUnspentResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListUnspentResponse} ListUnspentResponse instance
         * @type {{
         *   (properties: lnrpc.ListUnspentResponse.$Shape): lnrpc.ListUnspentResponse & lnrpc.ListUnspentResponse.$Shape;
         *   (properties?: lnrpc.ListUnspentResponse.$Properties): lnrpc.ListUnspentResponse;
         * }}
         */
        ListUnspentResponse.create = function(properties) {
            return new ListUnspentResponse(properties);
        };

        /**
         * Encodes the specified ListUnspentResponse message. Does not implicitly {@link lnrpc.ListUnspentResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListUnspentResponse
         * @static
         * @param {lnrpc.ListUnspentResponse.$Properties} message ListUnspentResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListUnspentResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.utxos != null && message.utxos.length)
                for (let i = 0; i < message.utxos.length; ++i)
                    $root.lnrpc.Utxo.encode(message.utxos[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListUnspentResponse message, length delimited. Does not implicitly {@link lnrpc.ListUnspentResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListUnspentResponse
         * @static
         * @param {lnrpc.ListUnspentResponse.$Properties} message ListUnspentResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListUnspentResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListUnspentResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListUnspentResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListUnspentResponse & lnrpc.ListUnspentResponse.$Shape} ListUnspentResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListUnspentResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListUnspentResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.utxos && message.utxos.length))
                            message.utxos = [];
                        message.utxos.push($root.lnrpc.Utxo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListUnspentResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListUnspentResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListUnspentResponse & lnrpc.ListUnspentResponse.$Shape} ListUnspentResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListUnspentResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListUnspentResponse message.
         * @function verify
         * @memberof lnrpc.ListUnspentResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListUnspentResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.utxos != null && $Object.hasOwnProperty.call(message, "utxos")) {
                if (!$Array.isArray(message.utxos))
                    return "utxos: array expected";
                for (let i = 0; i < message.utxos.length; ++i) {
                    let error = $root.lnrpc.Utxo.verify(message.utxos[i], _depth + 1);
                    if (error)
                        return "utxos." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListUnspentResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListUnspentResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListUnspentResponse} ListUnspentResponse
         */
        ListUnspentResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListUnspentResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListUnspentResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListUnspentResponse();
            if (object.utxos) {
                if (!$Array.isArray(object.utxos))
                    throw $TypeError(".lnrpc.ListUnspentResponse.utxos: array expected");
                message.utxos = $Array(object.utxos.length);
                for (let i = 0; i < object.utxos.length; ++i) {
                    if (!$util.isObject(object.utxos[i]))
                        throw $TypeError(".lnrpc.ListUnspentResponse.utxos: object expected");
                    message.utxos[i] = $root.lnrpc.Utxo.fromObject(object.utxos[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListUnspentResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListUnspentResponse
         * @static
         * @param {lnrpc.ListUnspentResponse} message ListUnspentResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListUnspentResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.utxos = [];
            if (message.utxos && message.utxos.length) {
                object.utxos = $Array(message.utxos.length);
                for (let j = 0; j < message.utxos.length; ++j)
                    object.utxos[j] = $root.lnrpc.Utxo.toObject(message.utxos[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ListUnspentResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ListUnspentResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListUnspentResponse.prototype.toJSON = function() {
            return ListUnspentResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListUnspentResponse
         * @function getTypeUrl
         * @memberof lnrpc.ListUnspentResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListUnspentResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListUnspentResponse";
        };

        return ListUnspentResponse;
    })();

    /**
     * AddressType enum.
     * @name lnrpc.AddressType
     * @enum {number}
     * @property {number} WITNESS_PUBKEY_HASH=0 WITNESS_PUBKEY_HASH value
     * @property {number} NESTED_PUBKEY_HASH=1 NESTED_PUBKEY_HASH value
     * @property {number} UNUSED_WITNESS_PUBKEY_HASH=2 UNUSED_WITNESS_PUBKEY_HASH value
     * @property {number} UNUSED_NESTED_PUBKEY_HASH=3 UNUSED_NESTED_PUBKEY_HASH value
     * @property {number} TAPROOT_PUBKEY=4 TAPROOT_PUBKEY value
     * @property {number} UNUSED_TAPROOT_PUBKEY=5 UNUSED_TAPROOT_PUBKEY value
     */
    lnrpc.AddressType = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "WITNESS_PUBKEY_HASH"] = 0;
        values[valuesById[1] = "NESTED_PUBKEY_HASH"] = 1;
        values[valuesById[2] = "UNUSED_WITNESS_PUBKEY_HASH"] = 2;
        values[valuesById[3] = "UNUSED_NESTED_PUBKEY_HASH"] = 3;
        values[valuesById[4] = "TAPROOT_PUBKEY"] = 4;
        values[valuesById[5] = "UNUSED_TAPROOT_PUBKEY"] = 5;
        return values;
    })();

    lnrpc.NewAddressRequest = (function() {

        /**
         * Properties of a NewAddressRequest.
         * @typedef {Object} lnrpc.NewAddressRequest.$Properties
         * @property {lnrpc.AddressType|null} [type] NewAddressRequest type
         * @property {string|null} [account] NewAddressRequest account
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NewAddressRequest.
         * @memberof lnrpc
         * @interface INewAddressRequest
         * @augments lnrpc.NewAddressRequest.$Properties
         * @deprecated Use lnrpc.NewAddressRequest.$Properties instead.
         */

        /**
         * Shape of a NewAddressRequest.
         * @typedef {lnrpc.NewAddressRequest.$Properties} lnrpc.NewAddressRequest.$Shape
         */

        /**
         * Constructs a new NewAddressRequest.
         * @memberof lnrpc
         * @classdesc Represents a NewAddressRequest.
         * @constructor
         * @param {lnrpc.NewAddressRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NewAddressRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NewAddressRequest type.
         * @member {lnrpc.AddressType} type
         * @memberof lnrpc.NewAddressRequest
         * @instance
         */
        NewAddressRequest.prototype.type = 0;

        /**
         * NewAddressRequest account.
         * @member {string} account
         * @memberof lnrpc.NewAddressRequest
         * @instance
         */
        NewAddressRequest.prototype.account = "";

        /**
         * Creates a new NewAddressRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.NewAddressRequest
         * @static
         * @param {lnrpc.NewAddressRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NewAddressRequest} NewAddressRequest instance
         * @type {{
         *   (properties: lnrpc.NewAddressRequest.$Shape): lnrpc.NewAddressRequest & lnrpc.NewAddressRequest.$Shape;
         *   (properties?: lnrpc.NewAddressRequest.$Properties): lnrpc.NewAddressRequest;
         * }}
         */
        NewAddressRequest.create = function(properties) {
            return new NewAddressRequest(properties);
        };

        /**
         * Encodes the specified NewAddressRequest message. Does not implicitly {@link lnrpc.NewAddressRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NewAddressRequest
         * @static
         * @param {lnrpc.NewAddressRequest.$Properties} message NewAddressRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NewAddressRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account") && message.account !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.account);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NewAddressRequest message, length delimited. Does not implicitly {@link lnrpc.NewAddressRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NewAddressRequest
         * @static
         * @param {lnrpc.NewAddressRequest.$Properties} message NewAddressRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NewAddressRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NewAddressRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NewAddressRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NewAddressRequest & lnrpc.NewAddressRequest.$Shape} NewAddressRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NewAddressRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NewAddressRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NewAddressRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NewAddressRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NewAddressRequest & lnrpc.NewAddressRequest.$Shape} NewAddressRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NewAddressRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NewAddressRequest message.
         * @function verify
         * @memberof lnrpc.NewAddressRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NewAddressRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            return null;
        };

        /**
         * Creates a NewAddressRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NewAddressRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NewAddressRequest} NewAddressRequest
         */
        NewAddressRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NewAddressRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NewAddressRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NewAddressRequest();
            if (object.type !== 0 && (typeof object.type !== "string" || $root.lnrpc.AddressType[object.type] !== 0))
                switch (object.type) {
                case "WITNESS_PUBKEY_HASH":
                case 0:
                    message.type = 0;
                    break;
                case "NESTED_PUBKEY_HASH":
                case 1:
                    message.type = 1;
                    break;
                case "UNUSED_WITNESS_PUBKEY_HASH":
                case 2:
                    message.type = 2;
                    break;
                case "UNUSED_NESTED_PUBKEY_HASH":
                case 3:
                    message.type = 3;
                    break;
                case "TAPROOT_PUBKEY":
                case 4:
                    message.type = 4;
                    break;
                case "UNUSED_TAPROOT_PUBKEY":
                case 5:
                    message.type = 5;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = $String(object.account);
            return message;
        };

        /**
         * Creates a plain object from a NewAddressRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NewAddressRequest
         * @static
         * @param {lnrpc.NewAddressRequest} message NewAddressRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NewAddressRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.type = options.enums === $String ? "WITNESS_PUBKEY_HASH" : 0;
                object.account = "";
            }
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === $String ? $root.lnrpc.AddressType[message.type] === $undefined ? message.type : $root.lnrpc.AddressType[message.type] : message.type;
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = message.account;
            return object;
        };

        /**
         * Converts this NewAddressRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.NewAddressRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NewAddressRequest.prototype.toJSON = function() {
            return NewAddressRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NewAddressRequest
         * @function getTypeUrl
         * @memberof lnrpc.NewAddressRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NewAddressRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NewAddressRequest";
        };

        return NewAddressRequest;
    })();

    lnrpc.NewAddressResponse = (function() {

        /**
         * Properties of a NewAddressResponse.
         * @typedef {Object} lnrpc.NewAddressResponse.$Properties
         * @property {string|null} [address] NewAddressResponse address
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NewAddressResponse.
         * @memberof lnrpc
         * @interface INewAddressResponse
         * @augments lnrpc.NewAddressResponse.$Properties
         * @deprecated Use lnrpc.NewAddressResponse.$Properties instead.
         */

        /**
         * Shape of a NewAddressResponse.
         * @typedef {lnrpc.NewAddressResponse.$Properties} lnrpc.NewAddressResponse.$Shape
         */

        /**
         * Constructs a new NewAddressResponse.
         * @memberof lnrpc
         * @classdesc Represents a NewAddressResponse.
         * @constructor
         * @param {lnrpc.NewAddressResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NewAddressResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NewAddressResponse address.
         * @member {string} address
         * @memberof lnrpc.NewAddressResponse
         * @instance
         */
        NewAddressResponse.prototype.address = "";

        /**
         * Creates a new NewAddressResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.NewAddressResponse
         * @static
         * @param {lnrpc.NewAddressResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NewAddressResponse} NewAddressResponse instance
         * @type {{
         *   (properties: lnrpc.NewAddressResponse.$Shape): lnrpc.NewAddressResponse & lnrpc.NewAddressResponse.$Shape;
         *   (properties?: lnrpc.NewAddressResponse.$Properties): lnrpc.NewAddressResponse;
         * }}
         */
        NewAddressResponse.create = function(properties) {
            return new NewAddressResponse(properties);
        };

        /**
         * Encodes the specified NewAddressResponse message. Does not implicitly {@link lnrpc.NewAddressResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NewAddressResponse
         * @static
         * @param {lnrpc.NewAddressResponse.$Properties} message NewAddressResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NewAddressResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NewAddressResponse message, length delimited. Does not implicitly {@link lnrpc.NewAddressResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NewAddressResponse
         * @static
         * @param {lnrpc.NewAddressResponse.$Properties} message NewAddressResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NewAddressResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NewAddressResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NewAddressResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NewAddressResponse & lnrpc.NewAddressResponse.$Shape} NewAddressResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NewAddressResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NewAddressResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.address = value;
                        else
                            delete message.address;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NewAddressResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NewAddressResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NewAddressResponse & lnrpc.NewAddressResponse.$Shape} NewAddressResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NewAddressResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NewAddressResponse message.
         * @function verify
         * @memberof lnrpc.NewAddressResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NewAddressResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            return null;
        };

        /**
         * Creates a NewAddressResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NewAddressResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NewAddressResponse} NewAddressResponse
         */
        NewAddressResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NewAddressResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NewAddressResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NewAddressResponse();
            if (object.address != null)
                if (typeof object.address !== "string" || object.address.length)
                    message.address = $String(object.address);
            return message;
        };

        /**
         * Creates a plain object from a NewAddressResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NewAddressResponse
         * @static
         * @param {lnrpc.NewAddressResponse} message NewAddressResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NewAddressResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.address = "";
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                object.address = message.address;
            return object;
        };

        /**
         * Converts this NewAddressResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.NewAddressResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NewAddressResponse.prototype.toJSON = function() {
            return NewAddressResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NewAddressResponse
         * @function getTypeUrl
         * @memberof lnrpc.NewAddressResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NewAddressResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NewAddressResponse";
        };

        return NewAddressResponse;
    })();

    lnrpc.SignMessageRequest = (function() {

        /**
         * Properties of a SignMessageRequest.
         * @typedef {Object} lnrpc.SignMessageRequest.$Properties
         * @property {Uint8Array|null} [msg] SignMessageRequest msg
         * @property {boolean|null} [single_hash] SignMessageRequest single_hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignMessageRequest.
         * @memberof lnrpc
         * @interface ISignMessageRequest
         * @augments lnrpc.SignMessageRequest.$Properties
         * @deprecated Use lnrpc.SignMessageRequest.$Properties instead.
         */

        /**
         * Shape of a SignMessageRequest.
         * @typedef {lnrpc.SignMessageRequest.$Properties} lnrpc.SignMessageRequest.$Shape
         */

        /**
         * Constructs a new SignMessageRequest.
         * @memberof lnrpc
         * @classdesc Represents a SignMessageRequest.
         * @constructor
         * @param {lnrpc.SignMessageRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignMessageRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignMessageRequest msg.
         * @member {Uint8Array} msg
         * @memberof lnrpc.SignMessageRequest
         * @instance
         */
        SignMessageRequest.prototype.msg = $util.newBuffer([]);

        /**
         * SignMessageRequest single_hash.
         * @member {boolean} single_hash
         * @memberof lnrpc.SignMessageRequest
         * @instance
         */
        SignMessageRequest.prototype.single_hash = false;

        /**
         * Creates a new SignMessageRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.SignMessageRequest
         * @static
         * @param {lnrpc.SignMessageRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SignMessageRequest} SignMessageRequest instance
         * @type {{
         *   (properties: lnrpc.SignMessageRequest.$Shape): lnrpc.SignMessageRequest & lnrpc.SignMessageRequest.$Shape;
         *   (properties?: lnrpc.SignMessageRequest.$Properties): lnrpc.SignMessageRequest;
         * }}
         */
        SignMessageRequest.create = function(properties) {
            return new SignMessageRequest(properties);
        };

        /**
         * Encodes the specified SignMessageRequest message. Does not implicitly {@link lnrpc.SignMessageRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SignMessageRequest
         * @static
         * @param {lnrpc.SignMessageRequest.$Properties} message SignMessageRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg") && message.msg.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.msg);
            if (message.single_hash != null && $Object.hasOwnProperty.call(message, "single_hash") && message.single_hash !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.single_hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignMessageRequest message, length delimited. Does not implicitly {@link lnrpc.SignMessageRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SignMessageRequest
         * @static
         * @param {lnrpc.SignMessageRequest.$Properties} message SignMessageRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignMessageRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SignMessageRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SignMessageRequest & lnrpc.SignMessageRequest.$Shape} SignMessageRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SignMessageRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.msg = value;
                        else
                            delete message.msg;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.single_hash = value;
                        else
                            delete message.single_hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignMessageRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SignMessageRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SignMessageRequest & lnrpc.SignMessageRequest.$Shape} SignMessageRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignMessageRequest message.
         * @function verify
         * @memberof lnrpc.SignMessageRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignMessageRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                if (!(message.msg && typeof message.msg.length === "number" || $util.isString(message.msg)))
                    return "msg: buffer expected";
            if (message.single_hash != null && $Object.hasOwnProperty.call(message, "single_hash"))
                if (typeof message.single_hash !== "boolean")
                    return "single_hash: boolean expected";
            return null;
        };

        /**
         * Creates a SignMessageRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SignMessageRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SignMessageRequest} SignMessageRequest
         */
        SignMessageRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SignMessageRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SignMessageRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SignMessageRequest();
            if (object.msg != null)
                if (object.msg.length)
                    if (typeof object.msg === "string")
                        $util.base64.decode(object.msg, message.msg = $util.newBuffer($util.base64.length(object.msg)), 0);
                    else if (object.msg.length >= 0)
                        message.msg = object.msg;
            if (object.single_hash != null)
                if (object.single_hash)
                    message.single_hash = $Boolean(object.single_hash);
            return message;
        };

        /**
         * Creates a plain object from a SignMessageRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SignMessageRequest
         * @static
         * @param {lnrpc.SignMessageRequest} message SignMessageRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignMessageRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.msg = "";
                else {
                    object.msg = [];
                    if (options.bytes !== $Array)
                        object.msg = $util.newBuffer(object.msg);
                }
                object.single_hash = false;
            }
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                object.msg = options.bytes === $String ? $util.base64.encode(message.msg, 0, message.msg.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.msg) : message.msg;
            if (message.single_hash != null && $Object.hasOwnProperty.call(message, "single_hash"))
                object.single_hash = message.single_hash;
            return object;
        };

        /**
         * Converts this SignMessageRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.SignMessageRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignMessageRequest.prototype.toJSON = function() {
            return SignMessageRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignMessageRequest
         * @function getTypeUrl
         * @memberof lnrpc.SignMessageRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignMessageRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SignMessageRequest";
        };

        return SignMessageRequest;
    })();

    lnrpc.SignMessageResponse = (function() {

        /**
         * Properties of a SignMessageResponse.
         * @typedef {Object} lnrpc.SignMessageResponse.$Properties
         * @property {string|null} [signature] SignMessageResponse signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignMessageResponse.
         * @memberof lnrpc
         * @interface ISignMessageResponse
         * @augments lnrpc.SignMessageResponse.$Properties
         * @deprecated Use lnrpc.SignMessageResponse.$Properties instead.
         */

        /**
         * Shape of a SignMessageResponse.
         * @typedef {lnrpc.SignMessageResponse.$Properties} lnrpc.SignMessageResponse.$Shape
         */

        /**
         * Constructs a new SignMessageResponse.
         * @memberof lnrpc
         * @classdesc Represents a SignMessageResponse.
         * @constructor
         * @param {lnrpc.SignMessageResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignMessageResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignMessageResponse signature.
         * @member {string} signature
         * @memberof lnrpc.SignMessageResponse
         * @instance
         */
        SignMessageResponse.prototype.signature = "";

        /**
         * Creates a new SignMessageResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.SignMessageResponse
         * @static
         * @param {lnrpc.SignMessageResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SignMessageResponse} SignMessageResponse instance
         * @type {{
         *   (properties: lnrpc.SignMessageResponse.$Shape): lnrpc.SignMessageResponse & lnrpc.SignMessageResponse.$Shape;
         *   (properties?: lnrpc.SignMessageResponse.$Properties): lnrpc.SignMessageResponse;
         * }}
         */
        SignMessageResponse.create = function(properties) {
            return new SignMessageResponse(properties);
        };

        /**
         * Encodes the specified SignMessageResponse message. Does not implicitly {@link lnrpc.SignMessageResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SignMessageResponse
         * @static
         * @param {lnrpc.SignMessageResponse.$Properties} message SignMessageResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.signature);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignMessageResponse message, length delimited. Does not implicitly {@link lnrpc.SignMessageResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SignMessageResponse
         * @static
         * @param {lnrpc.SignMessageResponse.$Properties} message SignMessageResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignMessageResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SignMessageResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SignMessageResponse & lnrpc.SignMessageResponse.$Shape} SignMessageResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SignMessageResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignMessageResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SignMessageResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SignMessageResponse & lnrpc.SignMessageResponse.$Shape} SignMessageResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignMessageResponse message.
         * @function verify
         * @memberof lnrpc.SignMessageResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignMessageResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                if (!$util.isString(message.signature))
                    return "signature: string expected";
            return null;
        };

        /**
         * Creates a SignMessageResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SignMessageResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SignMessageResponse} SignMessageResponse
         */
        SignMessageResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SignMessageResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SignMessageResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SignMessageResponse();
            if (object.signature != null)
                if (typeof object.signature !== "string" || object.signature.length)
                    message.signature = $String(object.signature);
            return message;
        };

        /**
         * Creates a plain object from a SignMessageResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SignMessageResponse
         * @static
         * @param {lnrpc.SignMessageResponse} message SignMessageResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignMessageResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.signature = "";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                object.signature = message.signature;
            return object;
        };

        /**
         * Converts this SignMessageResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.SignMessageResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignMessageResponse.prototype.toJSON = function() {
            return SignMessageResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignMessageResponse
         * @function getTypeUrl
         * @memberof lnrpc.SignMessageResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignMessageResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SignMessageResponse";
        };

        return SignMessageResponse;
    })();

    lnrpc.VerifyMessageRequest = (function() {

        /**
         * Properties of a VerifyMessageRequest.
         * @typedef {Object} lnrpc.VerifyMessageRequest.$Properties
         * @property {Uint8Array|null} [msg] VerifyMessageRequest msg
         * @property {string|null} [signature] VerifyMessageRequest signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a VerifyMessageRequest.
         * @memberof lnrpc
         * @interface IVerifyMessageRequest
         * @augments lnrpc.VerifyMessageRequest.$Properties
         * @deprecated Use lnrpc.VerifyMessageRequest.$Properties instead.
         */

        /**
         * Shape of a VerifyMessageRequest.
         * @typedef {lnrpc.VerifyMessageRequest.$Properties} lnrpc.VerifyMessageRequest.$Shape
         */

        /**
         * Constructs a new VerifyMessageRequest.
         * @memberof lnrpc
         * @classdesc Represents a VerifyMessageRequest.
         * @constructor
         * @param {lnrpc.VerifyMessageRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const VerifyMessageRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * VerifyMessageRequest msg.
         * @member {Uint8Array} msg
         * @memberof lnrpc.VerifyMessageRequest
         * @instance
         */
        VerifyMessageRequest.prototype.msg = $util.newBuffer([]);

        /**
         * VerifyMessageRequest signature.
         * @member {string} signature
         * @memberof lnrpc.VerifyMessageRequest
         * @instance
         */
        VerifyMessageRequest.prototype.signature = "";

        /**
         * Creates a new VerifyMessageRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.VerifyMessageRequest
         * @static
         * @param {lnrpc.VerifyMessageRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.VerifyMessageRequest} VerifyMessageRequest instance
         * @type {{
         *   (properties: lnrpc.VerifyMessageRequest.$Shape): lnrpc.VerifyMessageRequest & lnrpc.VerifyMessageRequest.$Shape;
         *   (properties?: lnrpc.VerifyMessageRequest.$Properties): lnrpc.VerifyMessageRequest;
         * }}
         */
        VerifyMessageRequest.create = function(properties) {
            return new VerifyMessageRequest(properties);
        };

        /**
         * Encodes the specified VerifyMessageRequest message. Does not implicitly {@link lnrpc.VerifyMessageRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.VerifyMessageRequest
         * @static
         * @param {lnrpc.VerifyMessageRequest.$Properties} message VerifyMessageRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg") && message.msg.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.msg);
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.signature);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VerifyMessageRequest message, length delimited. Does not implicitly {@link lnrpc.VerifyMessageRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.VerifyMessageRequest
         * @static
         * @param {lnrpc.VerifyMessageRequest.$Properties} message VerifyMessageRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VerifyMessageRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.VerifyMessageRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.VerifyMessageRequest & lnrpc.VerifyMessageRequest.$Shape} VerifyMessageRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.VerifyMessageRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.msg = value;
                        else
                            delete message.msg;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a VerifyMessageRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.VerifyMessageRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.VerifyMessageRequest & lnrpc.VerifyMessageRequest.$Shape} VerifyMessageRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VerifyMessageRequest message.
         * @function verify
         * @memberof lnrpc.VerifyMessageRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VerifyMessageRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                if (!(message.msg && typeof message.msg.length === "number" || $util.isString(message.msg)))
                    return "msg: buffer expected";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                if (!$util.isString(message.signature))
                    return "signature: string expected";
            return null;
        };

        /**
         * Creates a VerifyMessageRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.VerifyMessageRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.VerifyMessageRequest} VerifyMessageRequest
         */
        VerifyMessageRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.VerifyMessageRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.VerifyMessageRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.VerifyMessageRequest();
            if (object.msg != null)
                if (object.msg.length)
                    if (typeof object.msg === "string")
                        $util.base64.decode(object.msg, message.msg = $util.newBuffer($util.base64.length(object.msg)), 0);
                    else if (object.msg.length >= 0)
                        message.msg = object.msg;
            if (object.signature != null)
                if (typeof object.signature !== "string" || object.signature.length)
                    message.signature = $String(object.signature);
            return message;
        };

        /**
         * Creates a plain object from a VerifyMessageRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.VerifyMessageRequest
         * @static
         * @param {lnrpc.VerifyMessageRequest} message VerifyMessageRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VerifyMessageRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.msg = "";
                else {
                    object.msg = [];
                    if (options.bytes !== $Array)
                        object.msg = $util.newBuffer(object.msg);
                }
                object.signature = "";
            }
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                object.msg = options.bytes === $String ? $util.base64.encode(message.msg, 0, message.msg.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.msg) : message.msg;
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                object.signature = message.signature;
            return object;
        };

        /**
         * Converts this VerifyMessageRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.VerifyMessageRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VerifyMessageRequest.prototype.toJSON = function() {
            return VerifyMessageRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for VerifyMessageRequest
         * @function getTypeUrl
         * @memberof lnrpc.VerifyMessageRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        VerifyMessageRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.VerifyMessageRequest";
        };

        return VerifyMessageRequest;
    })();

    lnrpc.VerifyMessageResponse = (function() {

        /**
         * Properties of a VerifyMessageResponse.
         * @typedef {Object} lnrpc.VerifyMessageResponse.$Properties
         * @property {boolean|null} [valid] VerifyMessageResponse valid
         * @property {string|null} [pubkey] VerifyMessageResponse pubkey
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a VerifyMessageResponse.
         * @memberof lnrpc
         * @interface IVerifyMessageResponse
         * @augments lnrpc.VerifyMessageResponse.$Properties
         * @deprecated Use lnrpc.VerifyMessageResponse.$Properties instead.
         */

        /**
         * Shape of a VerifyMessageResponse.
         * @typedef {lnrpc.VerifyMessageResponse.$Properties} lnrpc.VerifyMessageResponse.$Shape
         */

        /**
         * Constructs a new VerifyMessageResponse.
         * @memberof lnrpc
         * @classdesc Represents a VerifyMessageResponse.
         * @constructor
         * @param {lnrpc.VerifyMessageResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const VerifyMessageResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * VerifyMessageResponse valid.
         * @member {boolean} valid
         * @memberof lnrpc.VerifyMessageResponse
         * @instance
         */
        VerifyMessageResponse.prototype.valid = false;

        /**
         * VerifyMessageResponse pubkey.
         * @member {string} pubkey
         * @memberof lnrpc.VerifyMessageResponse
         * @instance
         */
        VerifyMessageResponse.prototype.pubkey = "";

        /**
         * Creates a new VerifyMessageResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.VerifyMessageResponse
         * @static
         * @param {lnrpc.VerifyMessageResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.VerifyMessageResponse} VerifyMessageResponse instance
         * @type {{
         *   (properties: lnrpc.VerifyMessageResponse.$Shape): lnrpc.VerifyMessageResponse & lnrpc.VerifyMessageResponse.$Shape;
         *   (properties?: lnrpc.VerifyMessageResponse.$Properties): lnrpc.VerifyMessageResponse;
         * }}
         */
        VerifyMessageResponse.create = function(properties) {
            return new VerifyMessageResponse(properties);
        };

        /**
         * Encodes the specified VerifyMessageResponse message. Does not implicitly {@link lnrpc.VerifyMessageResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.VerifyMessageResponse
         * @static
         * @param {lnrpc.VerifyMessageResponse.$Properties} message VerifyMessageResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid") && message.valid !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.valid);
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey") && message.pubkey !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.pubkey);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VerifyMessageResponse message, length delimited. Does not implicitly {@link lnrpc.VerifyMessageResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.VerifyMessageResponse
         * @static
         * @param {lnrpc.VerifyMessageResponse.$Properties} message VerifyMessageResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VerifyMessageResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.VerifyMessageResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.VerifyMessageResponse & lnrpc.VerifyMessageResponse.$Shape} VerifyMessageResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.VerifyMessageResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.valid = value;
                        else
                            delete message.valid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pubkey = value;
                        else
                            delete message.pubkey;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a VerifyMessageResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.VerifyMessageResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.VerifyMessageResponse & lnrpc.VerifyMessageResponse.$Shape} VerifyMessageResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VerifyMessageResponse message.
         * @function verify
         * @memberof lnrpc.VerifyMessageResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VerifyMessageResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid"))
                if (typeof message.valid !== "boolean")
                    return "valid: boolean expected";
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                if (!$util.isString(message.pubkey))
                    return "pubkey: string expected";
            return null;
        };

        /**
         * Creates a VerifyMessageResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.VerifyMessageResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.VerifyMessageResponse} VerifyMessageResponse
         */
        VerifyMessageResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.VerifyMessageResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.VerifyMessageResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.VerifyMessageResponse();
            if (object.valid != null)
                if (object.valid)
                    message.valid = $Boolean(object.valid);
            if (object.pubkey != null)
                if (typeof object.pubkey !== "string" || object.pubkey.length)
                    message.pubkey = $String(object.pubkey);
            return message;
        };

        /**
         * Creates a plain object from a VerifyMessageResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.VerifyMessageResponse
         * @static
         * @param {lnrpc.VerifyMessageResponse} message VerifyMessageResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VerifyMessageResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.valid = false;
                object.pubkey = "";
            }
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid"))
                object.valid = message.valid;
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                object.pubkey = message.pubkey;
            return object;
        };

        /**
         * Converts this VerifyMessageResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.VerifyMessageResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VerifyMessageResponse.prototype.toJSON = function() {
            return VerifyMessageResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for VerifyMessageResponse
         * @function getTypeUrl
         * @memberof lnrpc.VerifyMessageResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        VerifyMessageResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.VerifyMessageResponse";
        };

        return VerifyMessageResponse;
    })();

    lnrpc.ConnectPeerRequest = (function() {

        /**
         * Properties of a ConnectPeerRequest.
         * @typedef {Object} lnrpc.ConnectPeerRequest.$Properties
         * @property {lnrpc.LightningAddress.$Properties|null} [addr] ConnectPeerRequest addr
         * @property {boolean|null} [perm] ConnectPeerRequest perm
         * @property {Long|null} [timeout] ConnectPeerRequest timeout
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ConnectPeerRequest.
         * @memberof lnrpc
         * @interface IConnectPeerRequest
         * @augments lnrpc.ConnectPeerRequest.$Properties
         * @deprecated Use lnrpc.ConnectPeerRequest.$Properties instead.
         */

        /**
         * Shape of a ConnectPeerRequest.
         * @typedef {lnrpc.ConnectPeerRequest.$Properties} lnrpc.ConnectPeerRequest.$Shape
         */

        /**
         * Constructs a new ConnectPeerRequest.
         * @memberof lnrpc
         * @classdesc Represents a ConnectPeerRequest.
         * @constructor
         * @param {lnrpc.ConnectPeerRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ConnectPeerRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ConnectPeerRequest addr.
         * @member {lnrpc.LightningAddress.$Properties|null|undefined} addr
         * @memberof lnrpc.ConnectPeerRequest
         * @instance
         */
        ConnectPeerRequest.prototype.addr = null;

        /**
         * ConnectPeerRequest perm.
         * @member {boolean} perm
         * @memberof lnrpc.ConnectPeerRequest
         * @instance
         */
        ConnectPeerRequest.prototype.perm = false;

        /**
         * ConnectPeerRequest timeout.
         * @member {Long} timeout
         * @memberof lnrpc.ConnectPeerRequest
         * @instance
         */
        ConnectPeerRequest.prototype.timeout = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new ConnectPeerRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ConnectPeerRequest
         * @static
         * @param {lnrpc.ConnectPeerRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ConnectPeerRequest} ConnectPeerRequest instance
         * @type {{
         *   (properties: lnrpc.ConnectPeerRequest.$Shape): lnrpc.ConnectPeerRequest & lnrpc.ConnectPeerRequest.$Shape;
         *   (properties?: lnrpc.ConnectPeerRequest.$Properties): lnrpc.ConnectPeerRequest;
         * }}
         */
        ConnectPeerRequest.create = function(properties) {
            return new ConnectPeerRequest(properties);
        };

        /**
         * Encodes the specified ConnectPeerRequest message. Does not implicitly {@link lnrpc.ConnectPeerRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ConnectPeerRequest
         * @static
         * @param {lnrpc.ConnectPeerRequest.$Properties} message ConnectPeerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConnectPeerRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                $root.lnrpc.LightningAddress.encode(message.addr, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.perm != null && $Object.hasOwnProperty.call(message, "perm") && message.perm !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.perm);
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout") && (typeof message.timeout === "object" ? message.timeout.low || message.timeout.high : message.timeout !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.timeout);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConnectPeerRequest message, length delimited. Does not implicitly {@link lnrpc.ConnectPeerRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ConnectPeerRequest
         * @static
         * @param {lnrpc.ConnectPeerRequest.$Properties} message ConnectPeerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConnectPeerRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ConnectPeerRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ConnectPeerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ConnectPeerRequest & lnrpc.ConnectPeerRequest.$Shape} ConnectPeerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConnectPeerRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ConnectPeerRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.addr = $root.lnrpc.LightningAddress.decode(reader, reader.uint32(), $undefined, _depth + 1, message.addr);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.perm = value;
                        else
                            delete message.perm;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.timeout = value;
                        else
                            delete message.timeout;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConnectPeerRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ConnectPeerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ConnectPeerRequest & lnrpc.ConnectPeerRequest.$Shape} ConnectPeerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConnectPeerRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConnectPeerRequest message.
         * @function verify
         * @memberof lnrpc.ConnectPeerRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConnectPeerRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr")) {
                let error = $root.lnrpc.LightningAddress.verify(message.addr, _depth + 1);
                if (error)
                    return "addr." + error;
            }
            if (message.perm != null && $Object.hasOwnProperty.call(message, "perm"))
                if (typeof message.perm !== "boolean")
                    return "perm: boolean expected";
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                if (!$util.isInteger(message.timeout) && !(message.timeout && $util.isInteger(message.timeout.low) && $util.isInteger(message.timeout.high)))
                    return "timeout: integer|Long expected";
            return null;
        };

        /**
         * Creates a ConnectPeerRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ConnectPeerRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ConnectPeerRequest} ConnectPeerRequest
         */
        ConnectPeerRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ConnectPeerRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ConnectPeerRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ConnectPeerRequest();
            if (object.addr != null) {
                if (!$util.isObject(object.addr))
                    throw $TypeError(".lnrpc.ConnectPeerRequest.addr: object expected");
                message.addr = $root.lnrpc.LightningAddress.fromObject(object.addr, _depth + 1);
            }
            if (object.perm != null)
                if (object.perm)
                    message.perm = $Boolean(object.perm);
            if (object.timeout != null)
                if (typeof object.timeout === "object" ? object.timeout.low || object.timeout.high : $Number(object.timeout) !== 0)
                    if ($util.Long)
                        message.timeout = $util.Long.fromValue(object.timeout, true);
                    else if (typeof object.timeout === "string")
                        message.timeout = $parseInt(object.timeout, 10);
                    else if (typeof object.timeout === "number")
                        message.timeout = object.timeout;
                    else if (typeof object.timeout === "object")
                        message.timeout = new $util.LongBits(object.timeout.low >>> 0, object.timeout.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ConnectPeerRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ConnectPeerRequest
         * @static
         * @param {lnrpc.ConnectPeerRequest} message ConnectPeerRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConnectPeerRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.addr = null;
                object.perm = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.timeout = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.timeout = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                object.addr = $root.lnrpc.LightningAddress.toObject(message.addr, options, _depth + 1);
            if (message.perm != null && $Object.hasOwnProperty.call(message, "perm"))
                object.perm = message.perm;
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.timeout = typeof message.timeout === "number" ? $BigInt(message.timeout) : $util.Long.fromBits(message.timeout.low >>> 0, message.timeout.high >>> 0, true).toBigInt();
                else if (typeof message.timeout === "number")
                    object.timeout = options.longs === $String ? $String(message.timeout) : message.timeout;
                else
                    object.timeout = options.longs === $String ? $util.Long.prototype.toString.call(message.timeout) : options.longs === $Number ? new $util.LongBits(message.timeout.low >>> 0, message.timeout.high >>> 0).toNumber(true) : message.timeout;
            return object;
        };

        /**
         * Converts this ConnectPeerRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ConnectPeerRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConnectPeerRequest.prototype.toJSON = function() {
            return ConnectPeerRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConnectPeerRequest
         * @function getTypeUrl
         * @memberof lnrpc.ConnectPeerRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConnectPeerRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ConnectPeerRequest";
        };

        return ConnectPeerRequest;
    })();

    lnrpc.ConnectPeerResponse = (function() {

        /**
         * Properties of a ConnectPeerResponse.
         * @typedef {Object} lnrpc.ConnectPeerResponse.$Properties
         * @property {string|null} [status] ConnectPeerResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ConnectPeerResponse.
         * @memberof lnrpc
         * @interface IConnectPeerResponse
         * @augments lnrpc.ConnectPeerResponse.$Properties
         * @deprecated Use lnrpc.ConnectPeerResponse.$Properties instead.
         */

        /**
         * Shape of a ConnectPeerResponse.
         * @typedef {lnrpc.ConnectPeerResponse.$Properties} lnrpc.ConnectPeerResponse.$Shape
         */

        /**
         * Constructs a new ConnectPeerResponse.
         * @memberof lnrpc
         * @classdesc Represents a ConnectPeerResponse.
         * @constructor
         * @param {lnrpc.ConnectPeerResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ConnectPeerResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ConnectPeerResponse status.
         * @member {string} status
         * @memberof lnrpc.ConnectPeerResponse
         * @instance
         */
        ConnectPeerResponse.prototype.status = "";

        /**
         * Creates a new ConnectPeerResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ConnectPeerResponse
         * @static
         * @param {lnrpc.ConnectPeerResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ConnectPeerResponse} ConnectPeerResponse instance
         * @type {{
         *   (properties: lnrpc.ConnectPeerResponse.$Shape): lnrpc.ConnectPeerResponse & lnrpc.ConnectPeerResponse.$Shape;
         *   (properties?: lnrpc.ConnectPeerResponse.$Properties): lnrpc.ConnectPeerResponse;
         * }}
         */
        ConnectPeerResponse.create = function(properties) {
            return new ConnectPeerResponse(properties);
        };

        /**
         * Encodes the specified ConnectPeerResponse message. Does not implicitly {@link lnrpc.ConnectPeerResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ConnectPeerResponse
         * @static
         * @param {lnrpc.ConnectPeerResponse.$Properties} message ConnectPeerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConnectPeerResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConnectPeerResponse message, length delimited. Does not implicitly {@link lnrpc.ConnectPeerResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ConnectPeerResponse
         * @static
         * @param {lnrpc.ConnectPeerResponse.$Properties} message ConnectPeerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConnectPeerResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ConnectPeerResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ConnectPeerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ConnectPeerResponse & lnrpc.ConnectPeerResponse.$Shape} ConnectPeerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConnectPeerResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ConnectPeerResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConnectPeerResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ConnectPeerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ConnectPeerResponse & lnrpc.ConnectPeerResponse.$Shape} ConnectPeerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConnectPeerResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConnectPeerResponse message.
         * @function verify
         * @memberof lnrpc.ConnectPeerResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConnectPeerResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a ConnectPeerResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ConnectPeerResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ConnectPeerResponse} ConnectPeerResponse
         */
        ConnectPeerResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ConnectPeerResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ConnectPeerResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ConnectPeerResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a ConnectPeerResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ConnectPeerResponse
         * @static
         * @param {lnrpc.ConnectPeerResponse} message ConnectPeerResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConnectPeerResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this ConnectPeerResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ConnectPeerResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConnectPeerResponse.prototype.toJSON = function() {
            return ConnectPeerResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConnectPeerResponse
         * @function getTypeUrl
         * @memberof lnrpc.ConnectPeerResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConnectPeerResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ConnectPeerResponse";
        };

        return ConnectPeerResponse;
    })();

    lnrpc.DisconnectPeerRequest = (function() {

        /**
         * Properties of a DisconnectPeerRequest.
         * @typedef {Object} lnrpc.DisconnectPeerRequest.$Properties
         * @property {string|null} [pub_key] DisconnectPeerRequest pub_key
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DisconnectPeerRequest.
         * @memberof lnrpc
         * @interface IDisconnectPeerRequest
         * @augments lnrpc.DisconnectPeerRequest.$Properties
         * @deprecated Use lnrpc.DisconnectPeerRequest.$Properties instead.
         */

        /**
         * Shape of a DisconnectPeerRequest.
         * @typedef {lnrpc.DisconnectPeerRequest.$Properties} lnrpc.DisconnectPeerRequest.$Shape
         */

        /**
         * Constructs a new DisconnectPeerRequest.
         * @memberof lnrpc
         * @classdesc Represents a DisconnectPeerRequest.
         * @constructor
         * @param {lnrpc.DisconnectPeerRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DisconnectPeerRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DisconnectPeerRequest pub_key.
         * @member {string} pub_key
         * @memberof lnrpc.DisconnectPeerRequest
         * @instance
         */
        DisconnectPeerRequest.prototype.pub_key = "";

        /**
         * Creates a new DisconnectPeerRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.DisconnectPeerRequest
         * @static
         * @param {lnrpc.DisconnectPeerRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DisconnectPeerRequest} DisconnectPeerRequest instance
         * @type {{
         *   (properties: lnrpc.DisconnectPeerRequest.$Shape): lnrpc.DisconnectPeerRequest & lnrpc.DisconnectPeerRequest.$Shape;
         *   (properties?: lnrpc.DisconnectPeerRequest.$Properties): lnrpc.DisconnectPeerRequest;
         * }}
         */
        DisconnectPeerRequest.create = function(properties) {
            return new DisconnectPeerRequest(properties);
        };

        /**
         * Encodes the specified DisconnectPeerRequest message. Does not implicitly {@link lnrpc.DisconnectPeerRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DisconnectPeerRequest
         * @static
         * @param {lnrpc.DisconnectPeerRequest.$Properties} message DisconnectPeerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisconnectPeerRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key") && message.pub_key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pub_key);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DisconnectPeerRequest message, length delimited. Does not implicitly {@link lnrpc.DisconnectPeerRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DisconnectPeerRequest
         * @static
         * @param {lnrpc.DisconnectPeerRequest.$Properties} message DisconnectPeerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisconnectPeerRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DisconnectPeerRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DisconnectPeerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DisconnectPeerRequest & lnrpc.DisconnectPeerRequest.$Shape} DisconnectPeerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisconnectPeerRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DisconnectPeerRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pub_key = value;
                        else
                            delete message.pub_key;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DisconnectPeerRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DisconnectPeerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DisconnectPeerRequest & lnrpc.DisconnectPeerRequest.$Shape} DisconnectPeerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisconnectPeerRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DisconnectPeerRequest message.
         * @function verify
         * @memberof lnrpc.DisconnectPeerRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DisconnectPeerRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                if (!$util.isString(message.pub_key))
                    return "pub_key: string expected";
            return null;
        };

        /**
         * Creates a DisconnectPeerRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DisconnectPeerRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DisconnectPeerRequest} DisconnectPeerRequest
         */
        DisconnectPeerRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DisconnectPeerRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DisconnectPeerRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DisconnectPeerRequest();
            if (object.pub_key != null)
                if (typeof object.pub_key !== "string" || object.pub_key.length)
                    message.pub_key = $String(object.pub_key);
            return message;
        };

        /**
         * Creates a plain object from a DisconnectPeerRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DisconnectPeerRequest
         * @static
         * @param {lnrpc.DisconnectPeerRequest} message DisconnectPeerRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DisconnectPeerRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.pub_key = "";
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                object.pub_key = message.pub_key;
            return object;
        };

        /**
         * Converts this DisconnectPeerRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.DisconnectPeerRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DisconnectPeerRequest.prototype.toJSON = function() {
            return DisconnectPeerRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DisconnectPeerRequest
         * @function getTypeUrl
         * @memberof lnrpc.DisconnectPeerRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DisconnectPeerRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DisconnectPeerRequest";
        };

        return DisconnectPeerRequest;
    })();

    lnrpc.DisconnectPeerResponse = (function() {

        /**
         * Properties of a DisconnectPeerResponse.
         * @typedef {Object} lnrpc.DisconnectPeerResponse.$Properties
         * @property {string|null} [status] DisconnectPeerResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DisconnectPeerResponse.
         * @memberof lnrpc
         * @interface IDisconnectPeerResponse
         * @augments lnrpc.DisconnectPeerResponse.$Properties
         * @deprecated Use lnrpc.DisconnectPeerResponse.$Properties instead.
         */

        /**
         * Shape of a DisconnectPeerResponse.
         * @typedef {lnrpc.DisconnectPeerResponse.$Properties} lnrpc.DisconnectPeerResponse.$Shape
         */

        /**
         * Constructs a new DisconnectPeerResponse.
         * @memberof lnrpc
         * @classdesc Represents a DisconnectPeerResponse.
         * @constructor
         * @param {lnrpc.DisconnectPeerResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DisconnectPeerResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DisconnectPeerResponse status.
         * @member {string} status
         * @memberof lnrpc.DisconnectPeerResponse
         * @instance
         */
        DisconnectPeerResponse.prototype.status = "";

        /**
         * Creates a new DisconnectPeerResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.DisconnectPeerResponse
         * @static
         * @param {lnrpc.DisconnectPeerResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DisconnectPeerResponse} DisconnectPeerResponse instance
         * @type {{
         *   (properties: lnrpc.DisconnectPeerResponse.$Shape): lnrpc.DisconnectPeerResponse & lnrpc.DisconnectPeerResponse.$Shape;
         *   (properties?: lnrpc.DisconnectPeerResponse.$Properties): lnrpc.DisconnectPeerResponse;
         * }}
         */
        DisconnectPeerResponse.create = function(properties) {
            return new DisconnectPeerResponse(properties);
        };

        /**
         * Encodes the specified DisconnectPeerResponse message. Does not implicitly {@link lnrpc.DisconnectPeerResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DisconnectPeerResponse
         * @static
         * @param {lnrpc.DisconnectPeerResponse.$Properties} message DisconnectPeerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisconnectPeerResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DisconnectPeerResponse message, length delimited. Does not implicitly {@link lnrpc.DisconnectPeerResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DisconnectPeerResponse
         * @static
         * @param {lnrpc.DisconnectPeerResponse.$Properties} message DisconnectPeerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisconnectPeerResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DisconnectPeerResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DisconnectPeerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DisconnectPeerResponse & lnrpc.DisconnectPeerResponse.$Shape} DisconnectPeerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisconnectPeerResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DisconnectPeerResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DisconnectPeerResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DisconnectPeerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DisconnectPeerResponse & lnrpc.DisconnectPeerResponse.$Shape} DisconnectPeerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisconnectPeerResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DisconnectPeerResponse message.
         * @function verify
         * @memberof lnrpc.DisconnectPeerResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DisconnectPeerResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a DisconnectPeerResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DisconnectPeerResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DisconnectPeerResponse} DisconnectPeerResponse
         */
        DisconnectPeerResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DisconnectPeerResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DisconnectPeerResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DisconnectPeerResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a DisconnectPeerResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DisconnectPeerResponse
         * @static
         * @param {lnrpc.DisconnectPeerResponse} message DisconnectPeerResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DisconnectPeerResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this DisconnectPeerResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.DisconnectPeerResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DisconnectPeerResponse.prototype.toJSON = function() {
            return DisconnectPeerResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DisconnectPeerResponse
         * @function getTypeUrl
         * @memberof lnrpc.DisconnectPeerResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DisconnectPeerResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DisconnectPeerResponse";
        };

        return DisconnectPeerResponse;
    })();

    lnrpc.HTLC = (function() {

        /**
         * Properties of a HTLC.
         * @typedef {Object} lnrpc.HTLC.$Properties
         * @property {boolean|null} [incoming] HTLC incoming
         * @property {Long|null} [amount] HTLC amount
         * @property {Uint8Array|null} [hash_lock] HTLC hash_lock
         * @property {number|null} [expiration_height] HTLC expiration_height
         * @property {Long|null} [htlc_index] HTLC htlc_index
         * @property {Long|null} [forwarding_channel] HTLC forwarding_channel
         * @property {Long|null} [forwarding_htlc_index] HTLC forwarding_htlc_index
         * @property {boolean|null} [locked_in] HTLC locked_in
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a HTLC.
         * @memberof lnrpc
         * @interface IHTLC
         * @augments lnrpc.HTLC.$Properties
         * @deprecated Use lnrpc.HTLC.$Properties instead.
         */

        /**
         * Shape of a HTLC.
         * @typedef {lnrpc.HTLC.$Properties} lnrpc.HTLC.$Shape
         */

        /**
         * Constructs a new HTLC.
         * @memberof lnrpc
         * @classdesc Represents a HTLC.
         * @constructor
         * @param {lnrpc.HTLC.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const HTLC = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HTLC incoming.
         * @member {boolean} incoming
         * @memberof lnrpc.HTLC
         * @instance
         */
        HTLC.prototype.incoming = false;

        /**
         * HTLC amount.
         * @member {Long} amount
         * @memberof lnrpc.HTLC
         * @instance
         */
        HTLC.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * HTLC hash_lock.
         * @member {Uint8Array} hash_lock
         * @memberof lnrpc.HTLC
         * @instance
         */
        HTLC.prototype.hash_lock = $util.newBuffer([]);

        /**
         * HTLC expiration_height.
         * @member {number} expiration_height
         * @memberof lnrpc.HTLC
         * @instance
         */
        HTLC.prototype.expiration_height = 0;

        /**
         * HTLC htlc_index.
         * @member {Long} htlc_index
         * @memberof lnrpc.HTLC
         * @instance
         */
        HTLC.prototype.htlc_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HTLC forwarding_channel.
         * @member {Long} forwarding_channel
         * @memberof lnrpc.HTLC
         * @instance
         */
        HTLC.prototype.forwarding_channel = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HTLC forwarding_htlc_index.
         * @member {Long} forwarding_htlc_index
         * @memberof lnrpc.HTLC
         * @instance
         */
        HTLC.prototype.forwarding_htlc_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HTLC locked_in.
         * @member {boolean} locked_in
         * @memberof lnrpc.HTLC
         * @instance
         */
        HTLC.prototype.locked_in = false;

        /**
         * Creates a new HTLC instance using the specified properties.
         * @function create
         * @memberof lnrpc.HTLC
         * @static
         * @param {lnrpc.HTLC.$Properties=} [properties] Properties to set
         * @returns {lnrpc.HTLC} HTLC instance
         * @type {{
         *   (properties: lnrpc.HTLC.$Shape): lnrpc.HTLC & lnrpc.HTLC.$Shape;
         *   (properties?: lnrpc.HTLC.$Properties): lnrpc.HTLC;
         * }}
         */
        HTLC.create = function(properties) {
            return new HTLC(properties);
        };

        /**
         * Encodes the specified HTLC message. Does not implicitly {@link lnrpc.HTLC.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.HTLC
         * @static
         * @param {lnrpc.HTLC.$Properties} message HTLC message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HTLC.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.incoming != null && $Object.hasOwnProperty.call(message, "incoming") && message.incoming !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.incoming);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amount);
            if (message.hash_lock != null && $Object.hasOwnProperty.call(message, "hash_lock") && message.hash_lock.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.hash_lock);
            if (message.expiration_height != null && $Object.hasOwnProperty.call(message, "expiration_height") && message.expiration_height !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.expiration_height);
            if (message.htlc_index != null && $Object.hasOwnProperty.call(message, "htlc_index") && (typeof message.htlc_index === "object" ? message.htlc_index.low || message.htlc_index.high : message.htlc_index !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.htlc_index);
            if (message.forwarding_channel != null && $Object.hasOwnProperty.call(message, "forwarding_channel") && (typeof message.forwarding_channel === "object" ? message.forwarding_channel.low || message.forwarding_channel.high : message.forwarding_channel !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.forwarding_channel);
            if (message.forwarding_htlc_index != null && $Object.hasOwnProperty.call(message, "forwarding_htlc_index") && (typeof message.forwarding_htlc_index === "object" ? message.forwarding_htlc_index.low || message.forwarding_htlc_index.high : message.forwarding_htlc_index !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.forwarding_htlc_index);
            if (message.locked_in != null && $Object.hasOwnProperty.call(message, "locked_in") && message.locked_in !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.locked_in);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HTLC message, length delimited. Does not implicitly {@link lnrpc.HTLC.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.HTLC
         * @static
         * @param {lnrpc.HTLC.$Properties} message HTLC message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HTLC.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HTLC message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.HTLC
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.HTLC & lnrpc.HTLC.$Shape} HTLC
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HTLC.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.HTLC(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.incoming = value;
                        else
                            delete message.incoming;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hash_lock = value;
                        else
                            delete message.hash_lock;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.expiration_height = value;
                        else
                            delete message.expiration_height;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_index = value;
                        else
                            delete message.htlc_index;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.forwarding_channel = value;
                        else
                            delete message.forwarding_channel;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.forwarding_htlc_index = value;
                        else
                            delete message.forwarding_htlc_index;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.locked_in = value;
                        else
                            delete message.locked_in;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HTLC message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.HTLC
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.HTLC & lnrpc.HTLC.$Shape} HTLC
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HTLC.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HTLC message.
         * @function verify
         * @memberof lnrpc.HTLC
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HTLC.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.incoming != null && $Object.hasOwnProperty.call(message, "incoming"))
                if (typeof message.incoming !== "boolean")
                    return "incoming: boolean expected";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.hash_lock != null && $Object.hasOwnProperty.call(message, "hash_lock"))
                if (!(message.hash_lock && typeof message.hash_lock.length === "number" || $util.isString(message.hash_lock)))
                    return "hash_lock: buffer expected";
            if (message.expiration_height != null && $Object.hasOwnProperty.call(message, "expiration_height"))
                if (!$util.isInteger(message.expiration_height))
                    return "expiration_height: integer expected";
            if (message.htlc_index != null && $Object.hasOwnProperty.call(message, "htlc_index"))
                if (!$util.isInteger(message.htlc_index) && !(message.htlc_index && $util.isInteger(message.htlc_index.low) && $util.isInteger(message.htlc_index.high)))
                    return "htlc_index: integer|Long expected";
            if (message.forwarding_channel != null && $Object.hasOwnProperty.call(message, "forwarding_channel"))
                if (!$util.isInteger(message.forwarding_channel) && !(message.forwarding_channel && $util.isInteger(message.forwarding_channel.low) && $util.isInteger(message.forwarding_channel.high)))
                    return "forwarding_channel: integer|Long expected";
            if (message.forwarding_htlc_index != null && $Object.hasOwnProperty.call(message, "forwarding_htlc_index"))
                if (!$util.isInteger(message.forwarding_htlc_index) && !(message.forwarding_htlc_index && $util.isInteger(message.forwarding_htlc_index.low) && $util.isInteger(message.forwarding_htlc_index.high)))
                    return "forwarding_htlc_index: integer|Long expected";
            if (message.locked_in != null && $Object.hasOwnProperty.call(message, "locked_in"))
                if (typeof message.locked_in !== "boolean")
                    return "locked_in: boolean expected";
            return null;
        };

        /**
         * Creates a HTLC message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.HTLC
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.HTLC} HTLC
         */
        HTLC.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.HTLC)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.HTLC: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.HTLC();
            if (object.incoming != null)
                if (object.incoming)
                    message.incoming = $Boolean(object.incoming);
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.hash_lock != null)
                if (object.hash_lock.length)
                    if (typeof object.hash_lock === "string")
                        $util.base64.decode(object.hash_lock, message.hash_lock = $util.newBuffer($util.base64.length(object.hash_lock)), 0);
                    else if (object.hash_lock.length >= 0)
                        message.hash_lock = object.hash_lock;
            if (object.expiration_height != null)
                if ($Number(object.expiration_height) !== 0)
                    message.expiration_height = object.expiration_height >>> 0;
            if (object.htlc_index != null)
                if (typeof object.htlc_index === "object" ? object.htlc_index.low || object.htlc_index.high : $Number(object.htlc_index) !== 0)
                    if ($util.Long)
                        message.htlc_index = $util.Long.fromValue(object.htlc_index, true);
                    else if (typeof object.htlc_index === "string")
                        message.htlc_index = $parseInt(object.htlc_index, 10);
                    else if (typeof object.htlc_index === "number")
                        message.htlc_index = object.htlc_index;
                    else if (typeof object.htlc_index === "object")
                        message.htlc_index = new $util.LongBits(object.htlc_index.low >>> 0, object.htlc_index.high >>> 0).toNumber(true);
            if (object.forwarding_channel != null)
                if (typeof object.forwarding_channel === "object" ? object.forwarding_channel.low || object.forwarding_channel.high : $Number(object.forwarding_channel) !== 0)
                    if ($util.Long)
                        message.forwarding_channel = $util.Long.fromValue(object.forwarding_channel, true);
                    else if (typeof object.forwarding_channel === "string")
                        message.forwarding_channel = $parseInt(object.forwarding_channel, 10);
                    else if (typeof object.forwarding_channel === "number")
                        message.forwarding_channel = object.forwarding_channel;
                    else if (typeof object.forwarding_channel === "object")
                        message.forwarding_channel = new $util.LongBits(object.forwarding_channel.low >>> 0, object.forwarding_channel.high >>> 0).toNumber(true);
            if (object.forwarding_htlc_index != null)
                if (typeof object.forwarding_htlc_index === "object" ? object.forwarding_htlc_index.low || object.forwarding_htlc_index.high : $Number(object.forwarding_htlc_index) !== 0)
                    if ($util.Long)
                        message.forwarding_htlc_index = $util.Long.fromValue(object.forwarding_htlc_index, true);
                    else if (typeof object.forwarding_htlc_index === "string")
                        message.forwarding_htlc_index = $parseInt(object.forwarding_htlc_index, 10);
                    else if (typeof object.forwarding_htlc_index === "number")
                        message.forwarding_htlc_index = object.forwarding_htlc_index;
                    else if (typeof object.forwarding_htlc_index === "object")
                        message.forwarding_htlc_index = new $util.LongBits(object.forwarding_htlc_index.low >>> 0, object.forwarding_htlc_index.high >>> 0).toNumber(true);
            if (object.locked_in != null)
                if (object.locked_in)
                    message.locked_in = $Boolean(object.locked_in);
            return message;
        };

        /**
         * Creates a plain object from a HTLC message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.HTLC
         * @static
         * @param {lnrpc.HTLC} message HTLC
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HTLC.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.incoming = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.hash_lock = "";
                else {
                    object.hash_lock = [];
                    if (options.bytes !== $Array)
                        object.hash_lock = $util.newBuffer(object.hash_lock);
                }
                object.expiration_height = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.forwarding_channel = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.forwarding_channel = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.forwarding_htlc_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.forwarding_htlc_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.locked_in = false;
            }
            if (message.incoming != null && $Object.hasOwnProperty.call(message, "incoming"))
                object.incoming = message.incoming;
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.hash_lock != null && $Object.hasOwnProperty.call(message, "hash_lock"))
                object.hash_lock = options.bytes === $String ? $util.base64.encode(message.hash_lock, 0, message.hash_lock.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash_lock) : message.hash_lock;
            if (message.expiration_height != null && $Object.hasOwnProperty.call(message, "expiration_height"))
                object.expiration_height = message.expiration_height;
            if (message.htlc_index != null && $Object.hasOwnProperty.call(message, "htlc_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_index = typeof message.htlc_index === "number" ? $BigInt(message.htlc_index) : $util.Long.fromBits(message.htlc_index.low >>> 0, message.htlc_index.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_index === "number")
                    object.htlc_index = options.longs === $String ? $String(message.htlc_index) : message.htlc_index;
                else
                    object.htlc_index = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_index) : options.longs === $Number ? new $util.LongBits(message.htlc_index.low >>> 0, message.htlc_index.high >>> 0).toNumber(true) : message.htlc_index;
            if (message.forwarding_channel != null && $Object.hasOwnProperty.call(message, "forwarding_channel"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.forwarding_channel = typeof message.forwarding_channel === "number" ? $BigInt(message.forwarding_channel) : $util.Long.fromBits(message.forwarding_channel.low >>> 0, message.forwarding_channel.high >>> 0, true).toBigInt();
                else if (typeof message.forwarding_channel === "number")
                    object.forwarding_channel = options.longs === $String ? $String(message.forwarding_channel) : message.forwarding_channel;
                else
                    object.forwarding_channel = options.longs === $String ? $util.Long.prototype.toString.call(message.forwarding_channel) : options.longs === $Number ? new $util.LongBits(message.forwarding_channel.low >>> 0, message.forwarding_channel.high >>> 0).toNumber(true) : message.forwarding_channel;
            if (message.forwarding_htlc_index != null && $Object.hasOwnProperty.call(message, "forwarding_htlc_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.forwarding_htlc_index = typeof message.forwarding_htlc_index === "number" ? $BigInt(message.forwarding_htlc_index) : $util.Long.fromBits(message.forwarding_htlc_index.low >>> 0, message.forwarding_htlc_index.high >>> 0, true).toBigInt();
                else if (typeof message.forwarding_htlc_index === "number")
                    object.forwarding_htlc_index = options.longs === $String ? $String(message.forwarding_htlc_index) : message.forwarding_htlc_index;
                else
                    object.forwarding_htlc_index = options.longs === $String ? $util.Long.prototype.toString.call(message.forwarding_htlc_index) : options.longs === $Number ? new $util.LongBits(message.forwarding_htlc_index.low >>> 0, message.forwarding_htlc_index.high >>> 0).toNumber(true) : message.forwarding_htlc_index;
            if (message.locked_in != null && $Object.hasOwnProperty.call(message, "locked_in"))
                object.locked_in = message.locked_in;
            return object;
        };

        /**
         * Converts this HTLC to JSON.
         * @function toJSON
         * @memberof lnrpc.HTLC
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HTLC.prototype.toJSON = function() {
            return HTLC.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HTLC
         * @function getTypeUrl
         * @memberof lnrpc.HTLC
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HTLC.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.HTLC";
        };

        return HTLC;
    })();

    /**
     * CommitmentType enum.
     * @name lnrpc.CommitmentType
     * @enum {number}
     * @property {number} UNKNOWN_COMMITMENT_TYPE=0 UNKNOWN_COMMITMENT_TYPE value
     * @property {number} LEGACY=1 LEGACY value
     * @property {number} STATIC_REMOTE_KEY=2 STATIC_REMOTE_KEY value
     * @property {number} ANCHORS=3 ANCHORS value
     * @property {number} SCRIPT_ENFORCED_LEASE=4 SCRIPT_ENFORCED_LEASE value
     * @property {number} SIMPLE_TAPROOT=5 SIMPLE_TAPROOT value
     * @property {number} SIMPLE_TAPROOT_OVERLAY=6 SIMPLE_TAPROOT_OVERLAY value
     */
    lnrpc.CommitmentType = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN_COMMITMENT_TYPE"] = 0;
        values[valuesById[1] = "LEGACY"] = 1;
        values[valuesById[2] = "STATIC_REMOTE_KEY"] = 2;
        values[valuesById[3] = "ANCHORS"] = 3;
        values[valuesById[4] = "SCRIPT_ENFORCED_LEASE"] = 4;
        values[valuesById[5] = "SIMPLE_TAPROOT"] = 5;
        values[valuesById[6] = "SIMPLE_TAPROOT_OVERLAY"] = 6;
        return values;
    })();

    lnrpc.ChannelConstraints = (function() {

        /**
         * Properties of a ChannelConstraints.
         * @typedef {Object} lnrpc.ChannelConstraints.$Properties
         * @property {number|null} [csv_delay] ChannelConstraints csv_delay
         * @property {Long|null} [chan_reserve_sat] ChannelConstraints chan_reserve_sat
         * @property {Long|null} [dust_limit_sat] ChannelConstraints dust_limit_sat
         * @property {Long|null} [max_pending_amt_msat] ChannelConstraints max_pending_amt_msat
         * @property {Long|null} [min_htlc_msat] ChannelConstraints min_htlc_msat
         * @property {number|null} [max_accepted_htlcs] ChannelConstraints max_accepted_htlcs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelConstraints.
         * @memberof lnrpc
         * @interface IChannelConstraints
         * @augments lnrpc.ChannelConstraints.$Properties
         * @deprecated Use lnrpc.ChannelConstraints.$Properties instead.
         */

        /**
         * Shape of a ChannelConstraints.
         * @typedef {lnrpc.ChannelConstraints.$Properties} lnrpc.ChannelConstraints.$Shape
         */

        /**
         * Constructs a new ChannelConstraints.
         * @memberof lnrpc
         * @classdesc Represents a ChannelConstraints.
         * @constructor
         * @param {lnrpc.ChannelConstraints.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelConstraints = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelConstraints csv_delay.
         * @member {number} csv_delay
         * @memberof lnrpc.ChannelConstraints
         * @instance
         */
        ChannelConstraints.prototype.csv_delay = 0;

        /**
         * ChannelConstraints chan_reserve_sat.
         * @member {Long} chan_reserve_sat
         * @memberof lnrpc.ChannelConstraints
         * @instance
         */
        ChannelConstraints.prototype.chan_reserve_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelConstraints dust_limit_sat.
         * @member {Long} dust_limit_sat
         * @memberof lnrpc.ChannelConstraints
         * @instance
         */
        ChannelConstraints.prototype.dust_limit_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelConstraints max_pending_amt_msat.
         * @member {Long} max_pending_amt_msat
         * @memberof lnrpc.ChannelConstraints
         * @instance
         */
        ChannelConstraints.prototype.max_pending_amt_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelConstraints min_htlc_msat.
         * @member {Long} min_htlc_msat
         * @memberof lnrpc.ChannelConstraints
         * @instance
         */
        ChannelConstraints.prototype.min_htlc_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelConstraints max_accepted_htlcs.
         * @member {number} max_accepted_htlcs
         * @memberof lnrpc.ChannelConstraints
         * @instance
         */
        ChannelConstraints.prototype.max_accepted_htlcs = 0;

        /**
         * Creates a new ChannelConstraints instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelConstraints
         * @static
         * @param {lnrpc.ChannelConstraints.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelConstraints} ChannelConstraints instance
         * @type {{
         *   (properties: lnrpc.ChannelConstraints.$Shape): lnrpc.ChannelConstraints & lnrpc.ChannelConstraints.$Shape;
         *   (properties?: lnrpc.ChannelConstraints.$Properties): lnrpc.ChannelConstraints;
         * }}
         */
        ChannelConstraints.create = function(properties) {
            return new ChannelConstraints(properties);
        };

        /**
         * Encodes the specified ChannelConstraints message. Does not implicitly {@link lnrpc.ChannelConstraints.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelConstraints
         * @static
         * @param {lnrpc.ChannelConstraints.$Properties} message ChannelConstraints message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelConstraints.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay") && message.csv_delay !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.csv_delay);
            if (message.chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "chan_reserve_sat") && (typeof message.chan_reserve_sat === "object" ? message.chan_reserve_sat.low || message.chan_reserve_sat.high : message.chan_reserve_sat !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.chan_reserve_sat);
            if (message.dust_limit_sat != null && $Object.hasOwnProperty.call(message, "dust_limit_sat") && (typeof message.dust_limit_sat === "object" ? message.dust_limit_sat.low || message.dust_limit_sat.high : message.dust_limit_sat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.dust_limit_sat);
            if (message.max_pending_amt_msat != null && $Object.hasOwnProperty.call(message, "max_pending_amt_msat") && (typeof message.max_pending_amt_msat === "object" ? message.max_pending_amt_msat.low || message.max_pending_amt_msat.high : message.max_pending_amt_msat !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.max_pending_amt_msat);
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat") && (typeof message.min_htlc_msat === "object" ? message.min_htlc_msat.low || message.min_htlc_msat.high : message.min_htlc_msat !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.min_htlc_msat);
            if (message.max_accepted_htlcs != null && $Object.hasOwnProperty.call(message, "max_accepted_htlcs") && message.max_accepted_htlcs !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.max_accepted_htlcs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelConstraints message, length delimited. Does not implicitly {@link lnrpc.ChannelConstraints.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelConstraints
         * @static
         * @param {lnrpc.ChannelConstraints.$Properties} message ChannelConstraints message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelConstraints.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelConstraints message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelConstraints
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelConstraints & lnrpc.ChannelConstraints.$Shape} ChannelConstraints
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelConstraints.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelConstraints(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.csv_delay = value;
                        else
                            delete message.csv_delay;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_reserve_sat = value;
                        else
                            delete message.chan_reserve_sat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.dust_limit_sat = value;
                        else
                            delete message.dust_limit_sat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_pending_amt_msat = value;
                        else
                            delete message.max_pending_amt_msat;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.min_htlc_msat = value;
                        else
                            delete message.min_htlc_msat;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_accepted_htlcs = value;
                        else
                            delete message.max_accepted_htlcs;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelConstraints message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelConstraints
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelConstraints & lnrpc.ChannelConstraints.$Shape} ChannelConstraints
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelConstraints.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelConstraints message.
         * @function verify
         * @memberof lnrpc.ChannelConstraints
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelConstraints.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay"))
                if (!$util.isInteger(message.csv_delay))
                    return "csv_delay: integer expected";
            if (message.chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "chan_reserve_sat"))
                if (!$util.isInteger(message.chan_reserve_sat) && !(message.chan_reserve_sat && $util.isInteger(message.chan_reserve_sat.low) && $util.isInteger(message.chan_reserve_sat.high)))
                    return "chan_reserve_sat: integer|Long expected";
            if (message.dust_limit_sat != null && $Object.hasOwnProperty.call(message, "dust_limit_sat"))
                if (!$util.isInteger(message.dust_limit_sat) && !(message.dust_limit_sat && $util.isInteger(message.dust_limit_sat.low) && $util.isInteger(message.dust_limit_sat.high)))
                    return "dust_limit_sat: integer|Long expected";
            if (message.max_pending_amt_msat != null && $Object.hasOwnProperty.call(message, "max_pending_amt_msat"))
                if (!$util.isInteger(message.max_pending_amt_msat) && !(message.max_pending_amt_msat && $util.isInteger(message.max_pending_amt_msat.low) && $util.isInteger(message.max_pending_amt_msat.high)))
                    return "max_pending_amt_msat: integer|Long expected";
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat"))
                if (!$util.isInteger(message.min_htlc_msat) && !(message.min_htlc_msat && $util.isInteger(message.min_htlc_msat.low) && $util.isInteger(message.min_htlc_msat.high)))
                    return "min_htlc_msat: integer|Long expected";
            if (message.max_accepted_htlcs != null && $Object.hasOwnProperty.call(message, "max_accepted_htlcs"))
                if (!$util.isInteger(message.max_accepted_htlcs))
                    return "max_accepted_htlcs: integer expected";
            return null;
        };

        /**
         * Creates a ChannelConstraints message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelConstraints
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelConstraints} ChannelConstraints
         */
        ChannelConstraints.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelConstraints)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelConstraints: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelConstraints();
            if (object.csv_delay != null)
                if ($Number(object.csv_delay) !== 0)
                    message.csv_delay = object.csv_delay >>> 0;
            if (object.chan_reserve_sat != null)
                if (typeof object.chan_reserve_sat === "object" ? object.chan_reserve_sat.low || object.chan_reserve_sat.high : $Number(object.chan_reserve_sat) !== 0)
                    if ($util.Long)
                        message.chan_reserve_sat = $util.Long.fromValue(object.chan_reserve_sat, true);
                    else if (typeof object.chan_reserve_sat === "string")
                        message.chan_reserve_sat = $parseInt(object.chan_reserve_sat, 10);
                    else if (typeof object.chan_reserve_sat === "number")
                        message.chan_reserve_sat = object.chan_reserve_sat;
                    else if (typeof object.chan_reserve_sat === "object")
                        message.chan_reserve_sat = new $util.LongBits(object.chan_reserve_sat.low >>> 0, object.chan_reserve_sat.high >>> 0).toNumber(true);
            if (object.dust_limit_sat != null)
                if (typeof object.dust_limit_sat === "object" ? object.dust_limit_sat.low || object.dust_limit_sat.high : $Number(object.dust_limit_sat) !== 0)
                    if ($util.Long)
                        message.dust_limit_sat = $util.Long.fromValue(object.dust_limit_sat, true);
                    else if (typeof object.dust_limit_sat === "string")
                        message.dust_limit_sat = $parseInt(object.dust_limit_sat, 10);
                    else if (typeof object.dust_limit_sat === "number")
                        message.dust_limit_sat = object.dust_limit_sat;
                    else if (typeof object.dust_limit_sat === "object")
                        message.dust_limit_sat = new $util.LongBits(object.dust_limit_sat.low >>> 0, object.dust_limit_sat.high >>> 0).toNumber(true);
            if (object.max_pending_amt_msat != null)
                if (typeof object.max_pending_amt_msat === "object" ? object.max_pending_amt_msat.low || object.max_pending_amt_msat.high : $Number(object.max_pending_amt_msat) !== 0)
                    if ($util.Long)
                        message.max_pending_amt_msat = $util.Long.fromValue(object.max_pending_amt_msat, true);
                    else if (typeof object.max_pending_amt_msat === "string")
                        message.max_pending_amt_msat = $parseInt(object.max_pending_amt_msat, 10);
                    else if (typeof object.max_pending_amt_msat === "number")
                        message.max_pending_amt_msat = object.max_pending_amt_msat;
                    else if (typeof object.max_pending_amt_msat === "object")
                        message.max_pending_amt_msat = new $util.LongBits(object.max_pending_amt_msat.low >>> 0, object.max_pending_amt_msat.high >>> 0).toNumber(true);
            if (object.min_htlc_msat != null)
                if (typeof object.min_htlc_msat === "object" ? object.min_htlc_msat.low || object.min_htlc_msat.high : $Number(object.min_htlc_msat) !== 0)
                    if ($util.Long)
                        message.min_htlc_msat = $util.Long.fromValue(object.min_htlc_msat, true);
                    else if (typeof object.min_htlc_msat === "string")
                        message.min_htlc_msat = $parseInt(object.min_htlc_msat, 10);
                    else if (typeof object.min_htlc_msat === "number")
                        message.min_htlc_msat = object.min_htlc_msat;
                    else if (typeof object.min_htlc_msat === "object")
                        message.min_htlc_msat = new $util.LongBits(object.min_htlc_msat.low >>> 0, object.min_htlc_msat.high >>> 0).toNumber(true);
            if (object.max_accepted_htlcs != null)
                if ($Number(object.max_accepted_htlcs) !== 0)
                    message.max_accepted_htlcs = object.max_accepted_htlcs >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ChannelConstraints message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelConstraints
         * @static
         * @param {lnrpc.ChannelConstraints} message ChannelConstraints
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelConstraints.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.csv_delay = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_reserve_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_reserve_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.dust_limit_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.dust_limit_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.max_pending_amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.max_pending_amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.min_htlc_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min_htlc_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.max_accepted_htlcs = 0;
            }
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay"))
                object.csv_delay = message.csv_delay;
            if (message.chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "chan_reserve_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_reserve_sat = typeof message.chan_reserve_sat === "number" ? $BigInt(message.chan_reserve_sat) : $util.Long.fromBits(message.chan_reserve_sat.low >>> 0, message.chan_reserve_sat.high >>> 0, true).toBigInt();
                else if (typeof message.chan_reserve_sat === "number")
                    object.chan_reserve_sat = options.longs === $String ? $String(message.chan_reserve_sat) : message.chan_reserve_sat;
                else
                    object.chan_reserve_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_reserve_sat) : options.longs === $Number ? new $util.LongBits(message.chan_reserve_sat.low >>> 0, message.chan_reserve_sat.high >>> 0).toNumber(true) : message.chan_reserve_sat;
            if (message.dust_limit_sat != null && $Object.hasOwnProperty.call(message, "dust_limit_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.dust_limit_sat = typeof message.dust_limit_sat === "number" ? $BigInt(message.dust_limit_sat) : $util.Long.fromBits(message.dust_limit_sat.low >>> 0, message.dust_limit_sat.high >>> 0, true).toBigInt();
                else if (typeof message.dust_limit_sat === "number")
                    object.dust_limit_sat = options.longs === $String ? $String(message.dust_limit_sat) : message.dust_limit_sat;
                else
                    object.dust_limit_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.dust_limit_sat) : options.longs === $Number ? new $util.LongBits(message.dust_limit_sat.low >>> 0, message.dust_limit_sat.high >>> 0).toNumber(true) : message.dust_limit_sat;
            if (message.max_pending_amt_msat != null && $Object.hasOwnProperty.call(message, "max_pending_amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.max_pending_amt_msat = typeof message.max_pending_amt_msat === "number" ? $BigInt(message.max_pending_amt_msat) : $util.Long.fromBits(message.max_pending_amt_msat.low >>> 0, message.max_pending_amt_msat.high >>> 0, true).toBigInt();
                else if (typeof message.max_pending_amt_msat === "number")
                    object.max_pending_amt_msat = options.longs === $String ? $String(message.max_pending_amt_msat) : message.max_pending_amt_msat;
                else
                    object.max_pending_amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.max_pending_amt_msat) : options.longs === $Number ? new $util.LongBits(message.max_pending_amt_msat.low >>> 0, message.max_pending_amt_msat.high >>> 0).toNumber(true) : message.max_pending_amt_msat;
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min_htlc_msat = typeof message.min_htlc_msat === "number" ? $BigInt(message.min_htlc_msat) : $util.Long.fromBits(message.min_htlc_msat.low >>> 0, message.min_htlc_msat.high >>> 0, true).toBigInt();
                else if (typeof message.min_htlc_msat === "number")
                    object.min_htlc_msat = options.longs === $String ? $String(message.min_htlc_msat) : message.min_htlc_msat;
                else
                    object.min_htlc_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.min_htlc_msat) : options.longs === $Number ? new $util.LongBits(message.min_htlc_msat.low >>> 0, message.min_htlc_msat.high >>> 0).toNumber(true) : message.min_htlc_msat;
            if (message.max_accepted_htlcs != null && $Object.hasOwnProperty.call(message, "max_accepted_htlcs"))
                object.max_accepted_htlcs = message.max_accepted_htlcs;
            return object;
        };

        /**
         * Converts this ChannelConstraints to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelConstraints
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelConstraints.prototype.toJSON = function() {
            return ChannelConstraints.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelConstraints
         * @function getTypeUrl
         * @memberof lnrpc.ChannelConstraints
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelConstraints.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelConstraints";
        };

        return ChannelConstraints;
    })();

    lnrpc.Channel = (function() {

        /**
         * Properties of a Channel.
         * @typedef {Object} lnrpc.Channel.$Properties
         * @property {boolean|null} [active] Channel active
         * @property {string|null} [remote_pubkey] Channel remote_pubkey
         * @property {string|null} [channel_point] Channel channel_point
         * @property {Long|null} [chan_id] Channel chan_id
         * @property {Long|null} [capacity] Channel capacity
         * @property {Long|null} [local_balance] Channel local_balance
         * @property {Long|null} [remote_balance] Channel remote_balance
         * @property {Long|null} [commit_fee] Channel commit_fee
         * @property {Long|null} [commit_weight] Channel commit_weight
         * @property {Long|null} [fee_per_kw] Channel fee_per_kw
         * @property {Long|null} [unsettled_balance] Channel unsettled_balance
         * @property {Long|null} [total_satoshis_sent] Channel total_satoshis_sent
         * @property {Long|null} [total_satoshis_received] Channel total_satoshis_received
         * @property {Long|null} [num_updates] Channel num_updates
         * @property {Array.<lnrpc.HTLC.$Properties>|null} [pending_htlcs] Channel pending_htlcs
         * @property {number|null} [csv_delay] Channel csv_delay
         * @property {boolean|null} ["private"] Channel private
         * @property {boolean|null} [initiator] Channel initiator
         * @property {string|null} [chan_status_flags] Channel chan_status_flags
         * @property {Long|null} [local_chan_reserve_sat] Channel local_chan_reserve_sat
         * @property {Long|null} [remote_chan_reserve_sat] Channel remote_chan_reserve_sat
         * @property {boolean|null} [static_remote_key] Channel static_remote_key
         * @property {lnrpc.CommitmentType|null} [commitment_type] Channel commitment_type
         * @property {Long|null} [lifetime] Channel lifetime
         * @property {Long|null} [uptime] Channel uptime
         * @property {string|null} [close_address] Channel close_address
         * @property {Long|null} [push_amount_sat] Channel push_amount_sat
         * @property {number|null} [thaw_height] Channel thaw_height
         * @property {lnrpc.ChannelConstraints.$Properties|null} [local_constraints] Channel local_constraints
         * @property {lnrpc.ChannelConstraints.$Properties|null} [remote_constraints] Channel remote_constraints
         * @property {Array.<Long>|null} [alias_scids] Channel alias_scids
         * @property {boolean|null} [zero_conf] Channel zero_conf
         * @property {Long|null} [zero_conf_confirmed_scid] Channel zero_conf_confirmed_scid
         * @property {string|null} [peer_alias] Channel peer_alias
         * @property {Long|null} [peer_scid_alias] Channel peer_scid_alias
         * @property {string|null} [memo] Channel memo
         * @property {Uint8Array|null} [custom_channel_data] Channel custom_channel_data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Channel.
         * @memberof lnrpc
         * @interface IChannel
         * @augments lnrpc.Channel.$Properties
         * @deprecated Use lnrpc.Channel.$Properties instead.
         */

        /**
         * Shape of a Channel.
         * @typedef {lnrpc.Channel.$Properties} lnrpc.Channel.$Shape
         */

        /**
         * Constructs a new Channel.
         * @memberof lnrpc
         * @classdesc Represents a Channel.
         * @constructor
         * @param {lnrpc.Channel.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Channel = function (properties) {
            this.pending_htlcs = [];
            this.alias_scids = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Channel active.
         * @member {boolean} active
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.active = false;

        /**
         * Channel remote_pubkey.
         * @member {string} remote_pubkey
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.remote_pubkey = "";

        /**
         * Channel channel_point.
         * @member {string} channel_point
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.channel_point = "";

        /**
         * Channel chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Channel capacity.
         * @member {Long} capacity
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.capacity = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel local_balance.
         * @member {Long} local_balance
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.local_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel remote_balance.
         * @member {Long} remote_balance
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.remote_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel commit_fee.
         * @member {Long} commit_fee
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.commit_fee = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel commit_weight.
         * @member {Long} commit_weight
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.commit_weight = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel fee_per_kw.
         * @member {Long} fee_per_kw
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.fee_per_kw = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel unsettled_balance.
         * @member {Long} unsettled_balance
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.unsettled_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel total_satoshis_sent.
         * @member {Long} total_satoshis_sent
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.total_satoshis_sent = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel total_satoshis_received.
         * @member {Long} total_satoshis_received
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.total_satoshis_received = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel num_updates.
         * @member {Long} num_updates
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.num_updates = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Channel pending_htlcs.
         * @member {Array.<lnrpc.HTLC.$Properties>} pending_htlcs
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.pending_htlcs = $util.emptyArray;

        /**
         * Channel csv_delay.
         * @member {number} csv_delay
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.csv_delay = 0;

        /**
         * Channel private.
         * @member {boolean} private
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype["private"] = false;

        /**
         * Channel initiator.
         * @member {boolean} initiator
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.initiator = false;

        /**
         * Channel chan_status_flags.
         * @member {string} chan_status_flags
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.chan_status_flags = "";

        /**
         * Channel local_chan_reserve_sat.
         * @member {Long} local_chan_reserve_sat
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.local_chan_reserve_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel remote_chan_reserve_sat.
         * @member {Long} remote_chan_reserve_sat
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.remote_chan_reserve_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel static_remote_key.
         * @member {boolean} static_remote_key
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.static_remote_key = false;

        /**
         * Channel commitment_type.
         * @member {lnrpc.CommitmentType} commitment_type
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.commitment_type = 0;

        /**
         * Channel lifetime.
         * @member {Long} lifetime
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.lifetime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel uptime.
         * @member {Long} uptime
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.uptime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Channel close_address.
         * @member {string} close_address
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.close_address = "";

        /**
         * Channel push_amount_sat.
         * @member {Long} push_amount_sat
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.push_amount_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Channel thaw_height.
         * @member {number} thaw_height
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.thaw_height = 0;

        /**
         * Channel local_constraints.
         * @member {lnrpc.ChannelConstraints.$Properties|null|undefined} local_constraints
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.local_constraints = null;

        /**
         * Channel remote_constraints.
         * @member {lnrpc.ChannelConstraints.$Properties|null|undefined} remote_constraints
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.remote_constraints = null;

        /**
         * Channel alias_scids.
         * @member {Array.<Long>} alias_scids
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.alias_scids = $util.emptyArray;

        /**
         * Channel zero_conf.
         * @member {boolean} zero_conf
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.zero_conf = false;

        /**
         * Channel zero_conf_confirmed_scid.
         * @member {Long} zero_conf_confirmed_scid
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.zero_conf_confirmed_scid = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Channel peer_alias.
         * @member {string} peer_alias
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.peer_alias = "";

        /**
         * Channel peer_scid_alias.
         * @member {Long} peer_scid_alias
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.peer_scid_alias = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Channel memo.
         * @member {string} memo
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.memo = "";

        /**
         * Channel custom_channel_data.
         * @member {Uint8Array} custom_channel_data
         * @memberof lnrpc.Channel
         * @instance
         */
        Channel.prototype.custom_channel_data = $util.newBuffer([]);

        /**
         * Creates a new Channel instance using the specified properties.
         * @function create
         * @memberof lnrpc.Channel
         * @static
         * @param {lnrpc.Channel.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Channel} Channel instance
         * @type {{
         *   (properties: lnrpc.Channel.$Shape): lnrpc.Channel & lnrpc.Channel.$Shape;
         *   (properties?: lnrpc.Channel.$Properties): lnrpc.Channel;
         * }}
         */
        Channel.create = function(properties) {
            return new Channel(properties);
        };

        /**
         * Encodes the specified Channel message. Does not implicitly {@link lnrpc.Channel.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Channel
         * @static
         * @param {lnrpc.Channel.$Properties} message Channel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Channel.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.active != null && $Object.hasOwnProperty.call(message, "active") && message.active !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.active);
            if (message.remote_pubkey != null && $Object.hasOwnProperty.call(message, "remote_pubkey") && message.remote_pubkey !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.remote_pubkey);
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point") && message.channel_point !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.channel_point);
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.chan_id);
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity") && (typeof message.capacity === "object" ? message.capacity.low || message.capacity.high : message.capacity !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.capacity);
            if (message.local_balance != null && $Object.hasOwnProperty.call(message, "local_balance") && (typeof message.local_balance === "object" ? message.local_balance.low || message.local_balance.high : message.local_balance !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.local_balance);
            if (message.remote_balance != null && $Object.hasOwnProperty.call(message, "remote_balance") && (typeof message.remote_balance === "object" ? message.remote_balance.low || message.remote_balance.high : message.remote_balance !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.remote_balance);
            if (message.commit_fee != null && $Object.hasOwnProperty.call(message, "commit_fee") && (typeof message.commit_fee === "object" ? message.commit_fee.low || message.commit_fee.high : message.commit_fee !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.commit_fee);
            if (message.commit_weight != null && $Object.hasOwnProperty.call(message, "commit_weight") && (typeof message.commit_weight === "object" ? message.commit_weight.low || message.commit_weight.high : message.commit_weight !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.commit_weight);
            if (message.fee_per_kw != null && $Object.hasOwnProperty.call(message, "fee_per_kw") && (typeof message.fee_per_kw === "object" ? message.fee_per_kw.low || message.fee_per_kw.high : message.fee_per_kw !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.fee_per_kw);
            if (message.unsettled_balance != null && $Object.hasOwnProperty.call(message, "unsettled_balance") && (typeof message.unsettled_balance === "object" ? message.unsettled_balance.low || message.unsettled_balance.high : message.unsettled_balance !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.unsettled_balance);
            if (message.total_satoshis_sent != null && $Object.hasOwnProperty.call(message, "total_satoshis_sent") && (typeof message.total_satoshis_sent === "object" ? message.total_satoshis_sent.low || message.total_satoshis_sent.high : message.total_satoshis_sent !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.total_satoshis_sent);
            if (message.total_satoshis_received != null && $Object.hasOwnProperty.call(message, "total_satoshis_received") && (typeof message.total_satoshis_received === "object" ? message.total_satoshis_received.low || message.total_satoshis_received.high : message.total_satoshis_received !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.total_satoshis_received);
            if (message.num_updates != null && $Object.hasOwnProperty.call(message, "num_updates") && (typeof message.num_updates === "object" ? message.num_updates.low || message.num_updates.high : message.num_updates !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.num_updates);
            if (message.pending_htlcs != null && message.pending_htlcs.length)
                for (let i = 0; i < message.pending_htlcs.length; ++i)
                    $root.lnrpc.HTLC.encode(message.pending_htlcs[i], writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay") && message.csv_delay !== 0)
                writer.uint32(/* id 16, wireType 0 =*/128).uint32(message.csv_delay);
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private") && message["private"] !== false)
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message["private"]);
            if (message.initiator != null && $Object.hasOwnProperty.call(message, "initiator") && message.initiator !== false)
                writer.uint32(/* id 18, wireType 0 =*/144).bool(message.initiator);
            if (message.chan_status_flags != null && $Object.hasOwnProperty.call(message, "chan_status_flags") && message.chan_status_flags !== "")
                writer.uint32(/* id 19, wireType 2 =*/154).string(message.chan_status_flags);
            if (message.local_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "local_chan_reserve_sat") && (typeof message.local_chan_reserve_sat === "object" ? message.local_chan_reserve_sat.low || message.local_chan_reserve_sat.high : message.local_chan_reserve_sat !== 0))
                writer.uint32(/* id 20, wireType 0 =*/160).int64(message.local_chan_reserve_sat);
            if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat") && (typeof message.remote_chan_reserve_sat === "object" ? message.remote_chan_reserve_sat.low || message.remote_chan_reserve_sat.high : message.remote_chan_reserve_sat !== 0))
                writer.uint32(/* id 21, wireType 0 =*/168).int64(message.remote_chan_reserve_sat);
            if (message.static_remote_key != null && $Object.hasOwnProperty.call(message, "static_remote_key") && message.static_remote_key !== false)
                writer.uint32(/* id 22, wireType 0 =*/176).bool(message.static_remote_key);
            if (message.lifetime != null && $Object.hasOwnProperty.call(message, "lifetime") && (typeof message.lifetime === "object" ? message.lifetime.low || message.lifetime.high : message.lifetime !== 0))
                writer.uint32(/* id 23, wireType 0 =*/184).int64(message.lifetime);
            if (message.uptime != null && $Object.hasOwnProperty.call(message, "uptime") && (typeof message.uptime === "object" ? message.uptime.low || message.uptime.high : message.uptime !== 0))
                writer.uint32(/* id 24, wireType 0 =*/192).int64(message.uptime);
            if (message.close_address != null && $Object.hasOwnProperty.call(message, "close_address") && message.close_address !== "")
                writer.uint32(/* id 25, wireType 2 =*/202).string(message.close_address);
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type") && message.commitment_type !== 0)
                writer.uint32(/* id 26, wireType 0 =*/208).int32(message.commitment_type);
            if (message.push_amount_sat != null && $Object.hasOwnProperty.call(message, "push_amount_sat") && (typeof message.push_amount_sat === "object" ? message.push_amount_sat.low || message.push_amount_sat.high : message.push_amount_sat !== 0))
                writer.uint32(/* id 27, wireType 0 =*/216).uint64(message.push_amount_sat);
            if (message.thaw_height != null && $Object.hasOwnProperty.call(message, "thaw_height") && message.thaw_height !== 0)
                writer.uint32(/* id 28, wireType 0 =*/224).uint32(message.thaw_height);
            if (message.local_constraints != null && $Object.hasOwnProperty.call(message, "local_constraints"))
                $root.lnrpc.ChannelConstraints.encode(message.local_constraints, writer.uint32(/* id 29, wireType 2 =*/234).fork(), _depth + 1).ldelim();
            if (message.remote_constraints != null && $Object.hasOwnProperty.call(message, "remote_constraints"))
                $root.lnrpc.ChannelConstraints.encode(message.remote_constraints, writer.uint32(/* id 30, wireType 2 =*/242).fork(), _depth + 1).ldelim();
            if (message.alias_scids != null && message.alias_scids.length)
                writer.uint32(/* id 31, wireType 2 =*/250).uint64s(message.alias_scids);
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf") && message.zero_conf !== false)
                writer.uint32(/* id 32, wireType 0 =*/256).bool(message.zero_conf);
            if (message.zero_conf_confirmed_scid != null && $Object.hasOwnProperty.call(message, "zero_conf_confirmed_scid") && (typeof message.zero_conf_confirmed_scid === "object" ? message.zero_conf_confirmed_scid.low || message.zero_conf_confirmed_scid.high : message.zero_conf_confirmed_scid !== 0))
                writer.uint32(/* id 33, wireType 0 =*/264).uint64(message.zero_conf_confirmed_scid);
            if (message.peer_alias != null && $Object.hasOwnProperty.call(message, "peer_alias") && message.peer_alias !== "")
                writer.uint32(/* id 34, wireType 2 =*/274).string(message.peer_alias);
            if (message.peer_scid_alias != null && $Object.hasOwnProperty.call(message, "peer_scid_alias") && (typeof message.peer_scid_alias === "object" ? message.peer_scid_alias.low || message.peer_scid_alias.high : message.peer_scid_alias !== 0))
                writer.uint32(/* id 35, wireType 0 =*/280).uint64(message.peer_scid_alias);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 36, wireType 2 =*/290).string(message.memo);
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data") && message.custom_channel_data.length)
                writer.uint32(/* id 37, wireType 2 =*/298).bytes(message.custom_channel_data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Channel message, length delimited. Does not implicitly {@link lnrpc.Channel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Channel
         * @static
         * @param {lnrpc.Channel.$Properties} message Channel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Channel.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Channel message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Channel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Channel & lnrpc.Channel.$Shape} Channel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Channel.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Channel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.active = value;
                        else
                            delete message.active;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.remote_pubkey = value;
                        else
                            delete message.remote_pubkey;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.channel_point = value;
                        else
                            delete message.channel_point;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.capacity = value;
                        else
                            delete message.capacity;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.local_balance = value;
                        else
                            delete message.local_balance;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.remote_balance = value;
                        else
                            delete message.remote_balance;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.commit_fee = value;
                        else
                            delete message.commit_fee;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.commit_weight = value;
                        else
                            delete message.commit_weight;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_per_kw = value;
                        else
                            delete message.fee_per_kw;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.unsettled_balance = value;
                        else
                            delete message.unsettled_balance;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_satoshis_sent = value;
                        else
                            delete message.total_satoshis_sent;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_satoshis_received = value;
                        else
                            delete message.total_satoshis_received;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.num_updates = value;
                        else
                            delete message.num_updates;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pending_htlcs && message.pending_htlcs.length))
                            message.pending_htlcs = [];
                        message.pending_htlcs.push($root.lnrpc.HTLC.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.csv_delay = value;
                        else
                            delete message.csv_delay;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message["private"] = value;
                        else
                            delete message["private"];
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.initiator = value;
                        else
                            delete message.initiator;
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.chan_status_flags = value;
                        else
                            delete message.chan_status_flags;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.local_chan_reserve_sat = value;
                        else
                            delete message.local_chan_reserve_sat;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.remote_chan_reserve_sat = value;
                        else
                            delete message.remote_chan_reserve_sat;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.static_remote_key = value;
                        else
                            delete message.static_remote_key;
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.commitment_type = value;
                        else
                            delete message.commitment_type;
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.lifetime = value;
                        else
                            delete message.lifetime;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.uptime = value;
                        else
                            delete message.uptime;
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.close_address = value;
                        else
                            delete message.close_address;
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.push_amount_sat = value;
                        else
                            delete message.push_amount_sat;
                        continue;
                    }
                case 28: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.thaw_height = value;
                        else
                            delete message.thaw_height;
                        continue;
                    }
                case 29: {
                        if (wireType !== 2)
                            break;
                        message.local_constraints = $root.lnrpc.ChannelConstraints.decode(reader, reader.uint32(), $undefined, _depth + 1, message.local_constraints);
                        continue;
                    }
                case 30: {
                        if (wireType !== 2)
                            break;
                        message.remote_constraints = $root.lnrpc.ChannelConstraints.decode(reader, reader.uint32(), $undefined, _depth + 1, message.remote_constraints);
                        continue;
                    }
                case 31: {
                        if (wireType === 2) {
                            if (!(message.alias_scids && message.alias_scids.length))
                                message.alias_scids = [];
                            reader.uint64s(message.alias_scids);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.alias_scids && message.alias_scids.length))
                            message.alias_scids = [];
                        message.alias_scids.push(reader.uint64());
                        continue;
                    }
                case 32: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zero_conf = value;
                        else
                            delete message.zero_conf;
                        continue;
                    }
                case 33: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.zero_conf_confirmed_scid = value;
                        else
                            delete message.zero_conf_confirmed_scid;
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.peer_alias = value;
                        else
                            delete message.peer_alias;
                        continue;
                    }
                case 35: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.peer_scid_alias = value;
                        else
                            delete message.peer_scid_alias;
                        continue;
                    }
                case 36: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 37: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.custom_channel_data = value;
                        else
                            delete message.custom_channel_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Channel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Channel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Channel & lnrpc.Channel.$Shape} Channel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Channel.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Channel message.
         * @function verify
         * @memberof lnrpc.Channel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Channel.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.active != null && $Object.hasOwnProperty.call(message, "active"))
                if (typeof message.active !== "boolean")
                    return "active: boolean expected";
            if (message.remote_pubkey != null && $Object.hasOwnProperty.call(message, "remote_pubkey"))
                if (!$util.isString(message.remote_pubkey))
                    return "remote_pubkey: string expected";
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                if (!$util.isString(message.channel_point))
                    return "channel_point: string expected";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (!$util.isInteger(message.capacity) && !(message.capacity && $util.isInteger(message.capacity.low) && $util.isInteger(message.capacity.high)))
                    return "capacity: integer|Long expected";
            if (message.local_balance != null && $Object.hasOwnProperty.call(message, "local_balance"))
                if (!$util.isInteger(message.local_balance) && !(message.local_balance && $util.isInteger(message.local_balance.low) && $util.isInteger(message.local_balance.high)))
                    return "local_balance: integer|Long expected";
            if (message.remote_balance != null && $Object.hasOwnProperty.call(message, "remote_balance"))
                if (!$util.isInteger(message.remote_balance) && !(message.remote_balance && $util.isInteger(message.remote_balance.low) && $util.isInteger(message.remote_balance.high)))
                    return "remote_balance: integer|Long expected";
            if (message.commit_fee != null && $Object.hasOwnProperty.call(message, "commit_fee"))
                if (!$util.isInteger(message.commit_fee) && !(message.commit_fee && $util.isInteger(message.commit_fee.low) && $util.isInteger(message.commit_fee.high)))
                    return "commit_fee: integer|Long expected";
            if (message.commit_weight != null && $Object.hasOwnProperty.call(message, "commit_weight"))
                if (!$util.isInteger(message.commit_weight) && !(message.commit_weight && $util.isInteger(message.commit_weight.low) && $util.isInteger(message.commit_weight.high)))
                    return "commit_weight: integer|Long expected";
            if (message.fee_per_kw != null && $Object.hasOwnProperty.call(message, "fee_per_kw"))
                if (!$util.isInteger(message.fee_per_kw) && !(message.fee_per_kw && $util.isInteger(message.fee_per_kw.low) && $util.isInteger(message.fee_per_kw.high)))
                    return "fee_per_kw: integer|Long expected";
            if (message.unsettled_balance != null && $Object.hasOwnProperty.call(message, "unsettled_balance"))
                if (!$util.isInteger(message.unsettled_balance) && !(message.unsettled_balance && $util.isInteger(message.unsettled_balance.low) && $util.isInteger(message.unsettled_balance.high)))
                    return "unsettled_balance: integer|Long expected";
            if (message.total_satoshis_sent != null && $Object.hasOwnProperty.call(message, "total_satoshis_sent"))
                if (!$util.isInteger(message.total_satoshis_sent) && !(message.total_satoshis_sent && $util.isInteger(message.total_satoshis_sent.low) && $util.isInteger(message.total_satoshis_sent.high)))
                    return "total_satoshis_sent: integer|Long expected";
            if (message.total_satoshis_received != null && $Object.hasOwnProperty.call(message, "total_satoshis_received"))
                if (!$util.isInteger(message.total_satoshis_received) && !(message.total_satoshis_received && $util.isInteger(message.total_satoshis_received.low) && $util.isInteger(message.total_satoshis_received.high)))
                    return "total_satoshis_received: integer|Long expected";
            if (message.num_updates != null && $Object.hasOwnProperty.call(message, "num_updates"))
                if (!$util.isInteger(message.num_updates) && !(message.num_updates && $util.isInteger(message.num_updates.low) && $util.isInteger(message.num_updates.high)))
                    return "num_updates: integer|Long expected";
            if (message.pending_htlcs != null && $Object.hasOwnProperty.call(message, "pending_htlcs")) {
                if (!$Array.isArray(message.pending_htlcs))
                    return "pending_htlcs: array expected";
                for (let i = 0; i < message.pending_htlcs.length; ++i) {
                    let error = $root.lnrpc.HTLC.verify(message.pending_htlcs[i], _depth + 1);
                    if (error)
                        return "pending_htlcs." + error;
                }
            }
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay"))
                if (!$util.isInteger(message.csv_delay))
                    return "csv_delay: integer expected";
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                if (typeof message["private"] !== "boolean")
                    return "private: boolean expected";
            if (message.initiator != null && $Object.hasOwnProperty.call(message, "initiator"))
                if (typeof message.initiator !== "boolean")
                    return "initiator: boolean expected";
            if (message.chan_status_flags != null && $Object.hasOwnProperty.call(message, "chan_status_flags"))
                if (!$util.isString(message.chan_status_flags))
                    return "chan_status_flags: string expected";
            if (message.local_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "local_chan_reserve_sat"))
                if (!$util.isInteger(message.local_chan_reserve_sat) && !(message.local_chan_reserve_sat && $util.isInteger(message.local_chan_reserve_sat.low) && $util.isInteger(message.local_chan_reserve_sat.high)))
                    return "local_chan_reserve_sat: integer|Long expected";
            if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat"))
                if (!$util.isInteger(message.remote_chan_reserve_sat) && !(message.remote_chan_reserve_sat && $util.isInteger(message.remote_chan_reserve_sat.low) && $util.isInteger(message.remote_chan_reserve_sat.high)))
                    return "remote_chan_reserve_sat: integer|Long expected";
            if (message.static_remote_key != null && $Object.hasOwnProperty.call(message, "static_remote_key"))
                if (typeof message.static_remote_key !== "boolean")
                    return "static_remote_key: boolean expected";
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                if (typeof message.commitment_type !== "number" || (message.commitment_type | 0) !== message.commitment_type)
                    return "commitment_type: enum value expected";
            if (message.lifetime != null && $Object.hasOwnProperty.call(message, "lifetime"))
                if (!$util.isInteger(message.lifetime) && !(message.lifetime && $util.isInteger(message.lifetime.low) && $util.isInteger(message.lifetime.high)))
                    return "lifetime: integer|Long expected";
            if (message.uptime != null && $Object.hasOwnProperty.call(message, "uptime"))
                if (!$util.isInteger(message.uptime) && !(message.uptime && $util.isInteger(message.uptime.low) && $util.isInteger(message.uptime.high)))
                    return "uptime: integer|Long expected";
            if (message.close_address != null && $Object.hasOwnProperty.call(message, "close_address"))
                if (!$util.isString(message.close_address))
                    return "close_address: string expected";
            if (message.push_amount_sat != null && $Object.hasOwnProperty.call(message, "push_amount_sat"))
                if (!$util.isInteger(message.push_amount_sat) && !(message.push_amount_sat && $util.isInteger(message.push_amount_sat.low) && $util.isInteger(message.push_amount_sat.high)))
                    return "push_amount_sat: integer|Long expected";
            if (message.thaw_height != null && $Object.hasOwnProperty.call(message, "thaw_height"))
                if (!$util.isInteger(message.thaw_height))
                    return "thaw_height: integer expected";
            if (message.local_constraints != null && $Object.hasOwnProperty.call(message, "local_constraints")) {
                let error = $root.lnrpc.ChannelConstraints.verify(message.local_constraints, _depth + 1);
                if (error)
                    return "local_constraints." + error;
            }
            if (message.remote_constraints != null && $Object.hasOwnProperty.call(message, "remote_constraints")) {
                let error = $root.lnrpc.ChannelConstraints.verify(message.remote_constraints, _depth + 1);
                if (error)
                    return "remote_constraints." + error;
            }
            if (message.alias_scids != null && $Object.hasOwnProperty.call(message, "alias_scids")) {
                if (!$Array.isArray(message.alias_scids))
                    return "alias_scids: array expected";
                for (let i = 0; i < message.alias_scids.length; ++i)
                    if (!$util.isInteger(message.alias_scids[i]) && !(message.alias_scids[i] && $util.isInteger(message.alias_scids[i].low) && $util.isInteger(message.alias_scids[i].high)))
                        return "alias_scids: integer|Long[] expected";
            }
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf"))
                if (typeof message.zero_conf !== "boolean")
                    return "zero_conf: boolean expected";
            if (message.zero_conf_confirmed_scid != null && $Object.hasOwnProperty.call(message, "zero_conf_confirmed_scid"))
                if (!$util.isInteger(message.zero_conf_confirmed_scid) && !(message.zero_conf_confirmed_scid && $util.isInteger(message.zero_conf_confirmed_scid.low) && $util.isInteger(message.zero_conf_confirmed_scid.high)))
                    return "zero_conf_confirmed_scid: integer|Long expected";
            if (message.peer_alias != null && $Object.hasOwnProperty.call(message, "peer_alias"))
                if (!$util.isString(message.peer_alias))
                    return "peer_alias: string expected";
            if (message.peer_scid_alias != null && $Object.hasOwnProperty.call(message, "peer_scid_alias"))
                if (!$util.isInteger(message.peer_scid_alias) && !(message.peer_scid_alias && $util.isInteger(message.peer_scid_alias.low) && $util.isInteger(message.peer_scid_alias.high)))
                    return "peer_scid_alias: integer|Long expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                if (!(message.custom_channel_data && typeof message.custom_channel_data.length === "number" || $util.isString(message.custom_channel_data)))
                    return "custom_channel_data: buffer expected";
            return null;
        };

        /**
         * Creates a Channel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Channel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Channel} Channel
         */
        Channel.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Channel)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Channel: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Channel();
            if (object.active != null)
                if (object.active)
                    message.active = $Boolean(object.active);
            if (object.remote_pubkey != null)
                if (typeof object.remote_pubkey !== "string" || object.remote_pubkey.length)
                    message.remote_pubkey = $String(object.remote_pubkey);
            if (object.channel_point != null)
                if (typeof object.channel_point !== "string" || object.channel_point.length)
                    message.channel_point = $String(object.channel_point);
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.capacity != null)
                if (typeof object.capacity === "object" ? object.capacity.low || object.capacity.high : $Number(object.capacity) !== 0)
                    if ($util.Long)
                        message.capacity = $util.Long.fromValue(object.capacity, false);
                    else if (typeof object.capacity === "string")
                        message.capacity = $parseInt(object.capacity, 10);
                    else if (typeof object.capacity === "number")
                        message.capacity = object.capacity;
                    else if (typeof object.capacity === "object")
                        message.capacity = new $util.LongBits(object.capacity.low >>> 0, object.capacity.high >>> 0).toNumber();
            if (object.local_balance != null)
                if (typeof object.local_balance === "object" ? object.local_balance.low || object.local_balance.high : $Number(object.local_balance) !== 0)
                    if ($util.Long)
                        message.local_balance = $util.Long.fromValue(object.local_balance, false);
                    else if (typeof object.local_balance === "string")
                        message.local_balance = $parseInt(object.local_balance, 10);
                    else if (typeof object.local_balance === "number")
                        message.local_balance = object.local_balance;
                    else if (typeof object.local_balance === "object")
                        message.local_balance = new $util.LongBits(object.local_balance.low >>> 0, object.local_balance.high >>> 0).toNumber();
            if (object.remote_balance != null)
                if (typeof object.remote_balance === "object" ? object.remote_balance.low || object.remote_balance.high : $Number(object.remote_balance) !== 0)
                    if ($util.Long)
                        message.remote_balance = $util.Long.fromValue(object.remote_balance, false);
                    else if (typeof object.remote_balance === "string")
                        message.remote_balance = $parseInt(object.remote_balance, 10);
                    else if (typeof object.remote_balance === "number")
                        message.remote_balance = object.remote_balance;
                    else if (typeof object.remote_balance === "object")
                        message.remote_balance = new $util.LongBits(object.remote_balance.low >>> 0, object.remote_balance.high >>> 0).toNumber();
            if (object.commit_fee != null)
                if (typeof object.commit_fee === "object" ? object.commit_fee.low || object.commit_fee.high : $Number(object.commit_fee) !== 0)
                    if ($util.Long)
                        message.commit_fee = $util.Long.fromValue(object.commit_fee, false);
                    else if (typeof object.commit_fee === "string")
                        message.commit_fee = $parseInt(object.commit_fee, 10);
                    else if (typeof object.commit_fee === "number")
                        message.commit_fee = object.commit_fee;
                    else if (typeof object.commit_fee === "object")
                        message.commit_fee = new $util.LongBits(object.commit_fee.low >>> 0, object.commit_fee.high >>> 0).toNumber();
            if (object.commit_weight != null)
                if (typeof object.commit_weight === "object" ? object.commit_weight.low || object.commit_weight.high : $Number(object.commit_weight) !== 0)
                    if ($util.Long)
                        message.commit_weight = $util.Long.fromValue(object.commit_weight, false);
                    else if (typeof object.commit_weight === "string")
                        message.commit_weight = $parseInt(object.commit_weight, 10);
                    else if (typeof object.commit_weight === "number")
                        message.commit_weight = object.commit_weight;
                    else if (typeof object.commit_weight === "object")
                        message.commit_weight = new $util.LongBits(object.commit_weight.low >>> 0, object.commit_weight.high >>> 0).toNumber();
            if (object.fee_per_kw != null)
                if (typeof object.fee_per_kw === "object" ? object.fee_per_kw.low || object.fee_per_kw.high : $Number(object.fee_per_kw) !== 0)
                    if ($util.Long)
                        message.fee_per_kw = $util.Long.fromValue(object.fee_per_kw, false);
                    else if (typeof object.fee_per_kw === "string")
                        message.fee_per_kw = $parseInt(object.fee_per_kw, 10);
                    else if (typeof object.fee_per_kw === "number")
                        message.fee_per_kw = object.fee_per_kw;
                    else if (typeof object.fee_per_kw === "object")
                        message.fee_per_kw = new $util.LongBits(object.fee_per_kw.low >>> 0, object.fee_per_kw.high >>> 0).toNumber();
            if (object.unsettled_balance != null)
                if (typeof object.unsettled_balance === "object" ? object.unsettled_balance.low || object.unsettled_balance.high : $Number(object.unsettled_balance) !== 0)
                    if ($util.Long)
                        message.unsettled_balance = $util.Long.fromValue(object.unsettled_balance, false);
                    else if (typeof object.unsettled_balance === "string")
                        message.unsettled_balance = $parseInt(object.unsettled_balance, 10);
                    else if (typeof object.unsettled_balance === "number")
                        message.unsettled_balance = object.unsettled_balance;
                    else if (typeof object.unsettled_balance === "object")
                        message.unsettled_balance = new $util.LongBits(object.unsettled_balance.low >>> 0, object.unsettled_balance.high >>> 0).toNumber();
            if (object.total_satoshis_sent != null)
                if (typeof object.total_satoshis_sent === "object" ? object.total_satoshis_sent.low || object.total_satoshis_sent.high : $Number(object.total_satoshis_sent) !== 0)
                    if ($util.Long)
                        message.total_satoshis_sent = $util.Long.fromValue(object.total_satoshis_sent, false);
                    else if (typeof object.total_satoshis_sent === "string")
                        message.total_satoshis_sent = $parseInt(object.total_satoshis_sent, 10);
                    else if (typeof object.total_satoshis_sent === "number")
                        message.total_satoshis_sent = object.total_satoshis_sent;
                    else if (typeof object.total_satoshis_sent === "object")
                        message.total_satoshis_sent = new $util.LongBits(object.total_satoshis_sent.low >>> 0, object.total_satoshis_sent.high >>> 0).toNumber();
            if (object.total_satoshis_received != null)
                if (typeof object.total_satoshis_received === "object" ? object.total_satoshis_received.low || object.total_satoshis_received.high : $Number(object.total_satoshis_received) !== 0)
                    if ($util.Long)
                        message.total_satoshis_received = $util.Long.fromValue(object.total_satoshis_received, false);
                    else if (typeof object.total_satoshis_received === "string")
                        message.total_satoshis_received = $parseInt(object.total_satoshis_received, 10);
                    else if (typeof object.total_satoshis_received === "number")
                        message.total_satoshis_received = object.total_satoshis_received;
                    else if (typeof object.total_satoshis_received === "object")
                        message.total_satoshis_received = new $util.LongBits(object.total_satoshis_received.low >>> 0, object.total_satoshis_received.high >>> 0).toNumber();
            if (object.num_updates != null)
                if (typeof object.num_updates === "object" ? object.num_updates.low || object.num_updates.high : $Number(object.num_updates) !== 0)
                    if ($util.Long)
                        message.num_updates = $util.Long.fromValue(object.num_updates, true);
                    else if (typeof object.num_updates === "string")
                        message.num_updates = $parseInt(object.num_updates, 10);
                    else if (typeof object.num_updates === "number")
                        message.num_updates = object.num_updates;
                    else if (typeof object.num_updates === "object")
                        message.num_updates = new $util.LongBits(object.num_updates.low >>> 0, object.num_updates.high >>> 0).toNumber(true);
            if (object.pending_htlcs) {
                if (!$Array.isArray(object.pending_htlcs))
                    throw $TypeError(".lnrpc.Channel.pending_htlcs: array expected");
                message.pending_htlcs = $Array(object.pending_htlcs.length);
                for (let i = 0; i < object.pending_htlcs.length; ++i) {
                    if (!$util.isObject(object.pending_htlcs[i]))
                        throw $TypeError(".lnrpc.Channel.pending_htlcs: object expected");
                    message.pending_htlcs[i] = $root.lnrpc.HTLC.fromObject(object.pending_htlcs[i], _depth + 1);
                }
            }
            if (object.csv_delay != null)
                if ($Number(object.csv_delay) !== 0)
                    message.csv_delay = object.csv_delay >>> 0;
            if (object["private"] != null)
                if (object["private"])
                    message["private"] = $Boolean(object["private"]);
            if (object.initiator != null)
                if (object.initiator)
                    message.initiator = $Boolean(object.initiator);
            if (object.chan_status_flags != null)
                if (typeof object.chan_status_flags !== "string" || object.chan_status_flags.length)
                    message.chan_status_flags = $String(object.chan_status_flags);
            if (object.local_chan_reserve_sat != null)
                if (typeof object.local_chan_reserve_sat === "object" ? object.local_chan_reserve_sat.low || object.local_chan_reserve_sat.high : $Number(object.local_chan_reserve_sat) !== 0)
                    if ($util.Long)
                        message.local_chan_reserve_sat = $util.Long.fromValue(object.local_chan_reserve_sat, false);
                    else if (typeof object.local_chan_reserve_sat === "string")
                        message.local_chan_reserve_sat = $parseInt(object.local_chan_reserve_sat, 10);
                    else if (typeof object.local_chan_reserve_sat === "number")
                        message.local_chan_reserve_sat = object.local_chan_reserve_sat;
                    else if (typeof object.local_chan_reserve_sat === "object")
                        message.local_chan_reserve_sat = new $util.LongBits(object.local_chan_reserve_sat.low >>> 0, object.local_chan_reserve_sat.high >>> 0).toNumber();
            if (object.remote_chan_reserve_sat != null)
                if (typeof object.remote_chan_reserve_sat === "object" ? object.remote_chan_reserve_sat.low || object.remote_chan_reserve_sat.high : $Number(object.remote_chan_reserve_sat) !== 0)
                    if ($util.Long)
                        message.remote_chan_reserve_sat = $util.Long.fromValue(object.remote_chan_reserve_sat, false);
                    else if (typeof object.remote_chan_reserve_sat === "string")
                        message.remote_chan_reserve_sat = $parseInt(object.remote_chan_reserve_sat, 10);
                    else if (typeof object.remote_chan_reserve_sat === "number")
                        message.remote_chan_reserve_sat = object.remote_chan_reserve_sat;
                    else if (typeof object.remote_chan_reserve_sat === "object")
                        message.remote_chan_reserve_sat = new $util.LongBits(object.remote_chan_reserve_sat.low >>> 0, object.remote_chan_reserve_sat.high >>> 0).toNumber();
            if (object.static_remote_key != null)
                if (object.static_remote_key)
                    message.static_remote_key = $Boolean(object.static_remote_key);
            if (object.commitment_type !== 0 && (typeof object.commitment_type !== "string" || $root.lnrpc.CommitmentType[object.commitment_type] !== 0))
                switch (object.commitment_type) {
                case "UNKNOWN_COMMITMENT_TYPE":
                case 0:
                    message.commitment_type = 0;
                    break;
                case "LEGACY":
                case 1:
                    message.commitment_type = 1;
                    break;
                case "STATIC_REMOTE_KEY":
                case 2:
                    message.commitment_type = 2;
                    break;
                case "ANCHORS":
                case 3:
                    message.commitment_type = 3;
                    break;
                case "SCRIPT_ENFORCED_LEASE":
                case 4:
                    message.commitment_type = 4;
                    break;
                case "SIMPLE_TAPROOT":
                case 5:
                    message.commitment_type = 5;
                    break;
                case "SIMPLE_TAPROOT_OVERLAY":
                case 6:
                    message.commitment_type = 6;
                    break;
                default:
                    if (typeof object.commitment_type === "number" && (object.commitment_type | 0) === object.commitment_type)
                        message.commitment_type = object.commitment_type;
                }
            if (object.lifetime != null)
                if (typeof object.lifetime === "object" ? object.lifetime.low || object.lifetime.high : $Number(object.lifetime) !== 0)
                    if ($util.Long)
                        message.lifetime = $util.Long.fromValue(object.lifetime, false);
                    else if (typeof object.lifetime === "string")
                        message.lifetime = $parseInt(object.lifetime, 10);
                    else if (typeof object.lifetime === "number")
                        message.lifetime = object.lifetime;
                    else if (typeof object.lifetime === "object")
                        message.lifetime = new $util.LongBits(object.lifetime.low >>> 0, object.lifetime.high >>> 0).toNumber();
            if (object.uptime != null)
                if (typeof object.uptime === "object" ? object.uptime.low || object.uptime.high : $Number(object.uptime) !== 0)
                    if ($util.Long)
                        message.uptime = $util.Long.fromValue(object.uptime, false);
                    else if (typeof object.uptime === "string")
                        message.uptime = $parseInt(object.uptime, 10);
                    else if (typeof object.uptime === "number")
                        message.uptime = object.uptime;
                    else if (typeof object.uptime === "object")
                        message.uptime = new $util.LongBits(object.uptime.low >>> 0, object.uptime.high >>> 0).toNumber();
            if (object.close_address != null)
                if (typeof object.close_address !== "string" || object.close_address.length)
                    message.close_address = $String(object.close_address);
            if (object.push_amount_sat != null)
                if (typeof object.push_amount_sat === "object" ? object.push_amount_sat.low || object.push_amount_sat.high : $Number(object.push_amount_sat) !== 0)
                    if ($util.Long)
                        message.push_amount_sat = $util.Long.fromValue(object.push_amount_sat, true);
                    else if (typeof object.push_amount_sat === "string")
                        message.push_amount_sat = $parseInt(object.push_amount_sat, 10);
                    else if (typeof object.push_amount_sat === "number")
                        message.push_amount_sat = object.push_amount_sat;
                    else if (typeof object.push_amount_sat === "object")
                        message.push_amount_sat = new $util.LongBits(object.push_amount_sat.low >>> 0, object.push_amount_sat.high >>> 0).toNumber(true);
            if (object.thaw_height != null)
                if ($Number(object.thaw_height) !== 0)
                    message.thaw_height = object.thaw_height >>> 0;
            if (object.local_constraints != null) {
                if (!$util.isObject(object.local_constraints))
                    throw $TypeError(".lnrpc.Channel.local_constraints: object expected");
                message.local_constraints = $root.lnrpc.ChannelConstraints.fromObject(object.local_constraints, _depth + 1);
            }
            if (object.remote_constraints != null) {
                if (!$util.isObject(object.remote_constraints))
                    throw $TypeError(".lnrpc.Channel.remote_constraints: object expected");
                message.remote_constraints = $root.lnrpc.ChannelConstraints.fromObject(object.remote_constraints, _depth + 1);
            }
            if (object.alias_scids) {
                if (!$Array.isArray(object.alias_scids))
                    throw $TypeError(".lnrpc.Channel.alias_scids: array expected");
                message.alias_scids = $Array(object.alias_scids.length);
                for (let i = 0; i < object.alias_scids.length; ++i)
                    if ($util.Long)
                        message.alias_scids[i] = $util.Long.fromValue(object.alias_scids[i], true);
                    else if (typeof object.alias_scids[i] === "string")
                        message.alias_scids[i] = $parseInt(object.alias_scids[i], 10);
                    else if (typeof object.alias_scids[i] === "number")
                        message.alias_scids[i] = object.alias_scids[i];
                    else if (typeof object.alias_scids[i] === "object")
                        message.alias_scids[i] = new $util.LongBits(object.alias_scids[i].low >>> 0, object.alias_scids[i].high >>> 0).toNumber(true);
            }
            if (object.zero_conf != null)
                if (object.zero_conf)
                    message.zero_conf = $Boolean(object.zero_conf);
            if (object.zero_conf_confirmed_scid != null)
                if (typeof object.zero_conf_confirmed_scid === "object" ? object.zero_conf_confirmed_scid.low || object.zero_conf_confirmed_scid.high : $Number(object.zero_conf_confirmed_scid) !== 0)
                    if ($util.Long)
                        message.zero_conf_confirmed_scid = $util.Long.fromValue(object.zero_conf_confirmed_scid, true);
                    else if (typeof object.zero_conf_confirmed_scid === "string")
                        message.zero_conf_confirmed_scid = $parseInt(object.zero_conf_confirmed_scid, 10);
                    else if (typeof object.zero_conf_confirmed_scid === "number")
                        message.zero_conf_confirmed_scid = object.zero_conf_confirmed_scid;
                    else if (typeof object.zero_conf_confirmed_scid === "object")
                        message.zero_conf_confirmed_scid = new $util.LongBits(object.zero_conf_confirmed_scid.low >>> 0, object.zero_conf_confirmed_scid.high >>> 0).toNumber(true);
            if (object.peer_alias != null)
                if (typeof object.peer_alias !== "string" || object.peer_alias.length)
                    message.peer_alias = $String(object.peer_alias);
            if (object.peer_scid_alias != null)
                if (typeof object.peer_scid_alias === "object" ? object.peer_scid_alias.low || object.peer_scid_alias.high : $Number(object.peer_scid_alias) !== 0)
                    if ($util.Long)
                        message.peer_scid_alias = $util.Long.fromValue(object.peer_scid_alias, true);
                    else if (typeof object.peer_scid_alias === "string")
                        message.peer_scid_alias = $parseInt(object.peer_scid_alias, 10);
                    else if (typeof object.peer_scid_alias === "number")
                        message.peer_scid_alias = object.peer_scid_alias;
                    else if (typeof object.peer_scid_alias === "object")
                        message.peer_scid_alias = new $util.LongBits(object.peer_scid_alias.low >>> 0, object.peer_scid_alias.high >>> 0).toNumber(true);
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.custom_channel_data != null)
                if (object.custom_channel_data.length)
                    if (typeof object.custom_channel_data === "string")
                        $util.base64.decode(object.custom_channel_data, message.custom_channel_data = $util.newBuffer($util.base64.length(object.custom_channel_data)), 0);
                    else if (object.custom_channel_data.length >= 0)
                        message.custom_channel_data = object.custom_channel_data;
            return message;
        };

        /**
         * Creates a plain object from a Channel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Channel
         * @static
         * @param {lnrpc.Channel} message Channel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Channel.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.pending_htlcs = [];
                object.alias_scids = [];
            }
            if (options.defaults) {
                object.active = false;
                object.remote_pubkey = "";
                object.channel_point = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.capacity = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.capacity = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.local_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.local_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.remote_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.remote_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.commit_fee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.commit_fee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.commit_weight = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.commit_weight = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_per_kw = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_per_kw = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.unsettled_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.unsettled_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_satoshis_sent = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_satoshis_sent = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_satoshis_received = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_satoshis_received = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.num_updates = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.num_updates = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.csv_delay = 0;
                object["private"] = false;
                object.initiator = false;
                object.chan_status_flags = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.local_chan_reserve_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.local_chan_reserve_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.remote_chan_reserve_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.remote_chan_reserve_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.static_remote_key = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lifetime = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.lifetime = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.uptime = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.uptime = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.close_address = "";
                object.commitment_type = options.enums === $String ? "UNKNOWN_COMMITMENT_TYPE" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.push_amount_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.push_amount_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.thaw_height = 0;
                object.local_constraints = null;
                object.remote_constraints = null;
                object.zero_conf = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.zero_conf_confirmed_scid = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.zero_conf_confirmed_scid = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.peer_alias = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.peer_scid_alias = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.peer_scid_alias = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.memo = "";
                if (options.bytes === $String)
                    object.custom_channel_data = "";
                else {
                    object.custom_channel_data = [];
                    if (options.bytes !== $Array)
                        object.custom_channel_data = $util.newBuffer(object.custom_channel_data);
                }
            }
            if (message.active != null && $Object.hasOwnProperty.call(message, "active"))
                object.active = message.active;
            if (message.remote_pubkey != null && $Object.hasOwnProperty.call(message, "remote_pubkey"))
                object.remote_pubkey = message.remote_pubkey;
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                object.channel_point = message.channel_point;
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.capacity = typeof message.capacity === "number" ? $BigInt(message.capacity) : $util.Long.fromBits(message.capacity.low >>> 0, message.capacity.high >>> 0, false).toBigInt();
                else if (typeof message.capacity === "number")
                    object.capacity = options.longs === $String ? $String(message.capacity) : message.capacity;
                else
                    object.capacity = options.longs === $String ? $util.Long.prototype.toString.call(message.capacity) : options.longs === $Number ? new $util.LongBits(message.capacity.low >>> 0, message.capacity.high >>> 0).toNumber() : message.capacity;
            if (message.local_balance != null && $Object.hasOwnProperty.call(message, "local_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.local_balance = typeof message.local_balance === "number" ? $BigInt(message.local_balance) : $util.Long.fromBits(message.local_balance.low >>> 0, message.local_balance.high >>> 0, false).toBigInt();
                else if (typeof message.local_balance === "number")
                    object.local_balance = options.longs === $String ? $String(message.local_balance) : message.local_balance;
                else
                    object.local_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.local_balance) : options.longs === $Number ? new $util.LongBits(message.local_balance.low >>> 0, message.local_balance.high >>> 0).toNumber() : message.local_balance;
            if (message.remote_balance != null && $Object.hasOwnProperty.call(message, "remote_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.remote_balance = typeof message.remote_balance === "number" ? $BigInt(message.remote_balance) : $util.Long.fromBits(message.remote_balance.low >>> 0, message.remote_balance.high >>> 0, false).toBigInt();
                else if (typeof message.remote_balance === "number")
                    object.remote_balance = options.longs === $String ? $String(message.remote_balance) : message.remote_balance;
                else
                    object.remote_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_balance) : options.longs === $Number ? new $util.LongBits(message.remote_balance.low >>> 0, message.remote_balance.high >>> 0).toNumber() : message.remote_balance;
            if (message.commit_fee != null && $Object.hasOwnProperty.call(message, "commit_fee"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.commit_fee = typeof message.commit_fee === "number" ? $BigInt(message.commit_fee) : $util.Long.fromBits(message.commit_fee.low >>> 0, message.commit_fee.high >>> 0, false).toBigInt();
                else if (typeof message.commit_fee === "number")
                    object.commit_fee = options.longs === $String ? $String(message.commit_fee) : message.commit_fee;
                else
                    object.commit_fee = options.longs === $String ? $util.Long.prototype.toString.call(message.commit_fee) : options.longs === $Number ? new $util.LongBits(message.commit_fee.low >>> 0, message.commit_fee.high >>> 0).toNumber() : message.commit_fee;
            if (message.commit_weight != null && $Object.hasOwnProperty.call(message, "commit_weight"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.commit_weight = typeof message.commit_weight === "number" ? $BigInt(message.commit_weight) : $util.Long.fromBits(message.commit_weight.low >>> 0, message.commit_weight.high >>> 0, false).toBigInt();
                else if (typeof message.commit_weight === "number")
                    object.commit_weight = options.longs === $String ? $String(message.commit_weight) : message.commit_weight;
                else
                    object.commit_weight = options.longs === $String ? $util.Long.prototype.toString.call(message.commit_weight) : options.longs === $Number ? new $util.LongBits(message.commit_weight.low >>> 0, message.commit_weight.high >>> 0).toNumber() : message.commit_weight;
            if (message.fee_per_kw != null && $Object.hasOwnProperty.call(message, "fee_per_kw"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_per_kw = typeof message.fee_per_kw === "number" ? $BigInt(message.fee_per_kw) : $util.Long.fromBits(message.fee_per_kw.low >>> 0, message.fee_per_kw.high >>> 0, false).toBigInt();
                else if (typeof message.fee_per_kw === "number")
                    object.fee_per_kw = options.longs === $String ? $String(message.fee_per_kw) : message.fee_per_kw;
                else
                    object.fee_per_kw = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_per_kw) : options.longs === $Number ? new $util.LongBits(message.fee_per_kw.low >>> 0, message.fee_per_kw.high >>> 0).toNumber() : message.fee_per_kw;
            if (message.unsettled_balance != null && $Object.hasOwnProperty.call(message, "unsettled_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.unsettled_balance = typeof message.unsettled_balance === "number" ? $BigInt(message.unsettled_balance) : $util.Long.fromBits(message.unsettled_balance.low >>> 0, message.unsettled_balance.high >>> 0, false).toBigInt();
                else if (typeof message.unsettled_balance === "number")
                    object.unsettled_balance = options.longs === $String ? $String(message.unsettled_balance) : message.unsettled_balance;
                else
                    object.unsettled_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.unsettled_balance) : options.longs === $Number ? new $util.LongBits(message.unsettled_balance.low >>> 0, message.unsettled_balance.high >>> 0).toNumber() : message.unsettled_balance;
            if (message.total_satoshis_sent != null && $Object.hasOwnProperty.call(message, "total_satoshis_sent"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_satoshis_sent = typeof message.total_satoshis_sent === "number" ? $BigInt(message.total_satoshis_sent) : $util.Long.fromBits(message.total_satoshis_sent.low >>> 0, message.total_satoshis_sent.high >>> 0, false).toBigInt();
                else if (typeof message.total_satoshis_sent === "number")
                    object.total_satoshis_sent = options.longs === $String ? $String(message.total_satoshis_sent) : message.total_satoshis_sent;
                else
                    object.total_satoshis_sent = options.longs === $String ? $util.Long.prototype.toString.call(message.total_satoshis_sent) : options.longs === $Number ? new $util.LongBits(message.total_satoshis_sent.low >>> 0, message.total_satoshis_sent.high >>> 0).toNumber() : message.total_satoshis_sent;
            if (message.total_satoshis_received != null && $Object.hasOwnProperty.call(message, "total_satoshis_received"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_satoshis_received = typeof message.total_satoshis_received === "number" ? $BigInt(message.total_satoshis_received) : $util.Long.fromBits(message.total_satoshis_received.low >>> 0, message.total_satoshis_received.high >>> 0, false).toBigInt();
                else if (typeof message.total_satoshis_received === "number")
                    object.total_satoshis_received = options.longs === $String ? $String(message.total_satoshis_received) : message.total_satoshis_received;
                else
                    object.total_satoshis_received = options.longs === $String ? $util.Long.prototype.toString.call(message.total_satoshis_received) : options.longs === $Number ? new $util.LongBits(message.total_satoshis_received.low >>> 0, message.total_satoshis_received.high >>> 0).toNumber() : message.total_satoshis_received;
            if (message.num_updates != null && $Object.hasOwnProperty.call(message, "num_updates"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.num_updates = typeof message.num_updates === "number" ? $BigInt(message.num_updates) : $util.Long.fromBits(message.num_updates.low >>> 0, message.num_updates.high >>> 0, true).toBigInt();
                else if (typeof message.num_updates === "number")
                    object.num_updates = options.longs === $String ? $String(message.num_updates) : message.num_updates;
                else
                    object.num_updates = options.longs === $String ? $util.Long.prototype.toString.call(message.num_updates) : options.longs === $Number ? new $util.LongBits(message.num_updates.low >>> 0, message.num_updates.high >>> 0).toNumber(true) : message.num_updates;
            if (message.pending_htlcs && message.pending_htlcs.length) {
                object.pending_htlcs = $Array(message.pending_htlcs.length);
                for (let j = 0; j < message.pending_htlcs.length; ++j)
                    object.pending_htlcs[j] = $root.lnrpc.HTLC.toObject(message.pending_htlcs[j], options, _depth + 1);
            }
            if (message.csv_delay != null && $Object.hasOwnProperty.call(message, "csv_delay"))
                object.csv_delay = message.csv_delay;
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                object["private"] = message["private"];
            if (message.initiator != null && $Object.hasOwnProperty.call(message, "initiator"))
                object.initiator = message.initiator;
            if (message.chan_status_flags != null && $Object.hasOwnProperty.call(message, "chan_status_flags"))
                object.chan_status_flags = message.chan_status_flags;
            if (message.local_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "local_chan_reserve_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.local_chan_reserve_sat = typeof message.local_chan_reserve_sat === "number" ? $BigInt(message.local_chan_reserve_sat) : $util.Long.fromBits(message.local_chan_reserve_sat.low >>> 0, message.local_chan_reserve_sat.high >>> 0, false).toBigInt();
                else if (typeof message.local_chan_reserve_sat === "number")
                    object.local_chan_reserve_sat = options.longs === $String ? $String(message.local_chan_reserve_sat) : message.local_chan_reserve_sat;
                else
                    object.local_chan_reserve_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.local_chan_reserve_sat) : options.longs === $Number ? new $util.LongBits(message.local_chan_reserve_sat.low >>> 0, message.local_chan_reserve_sat.high >>> 0).toNumber() : message.local_chan_reserve_sat;
            if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.remote_chan_reserve_sat = typeof message.remote_chan_reserve_sat === "number" ? $BigInt(message.remote_chan_reserve_sat) : $util.Long.fromBits(message.remote_chan_reserve_sat.low >>> 0, message.remote_chan_reserve_sat.high >>> 0, false).toBigInt();
                else if (typeof message.remote_chan_reserve_sat === "number")
                    object.remote_chan_reserve_sat = options.longs === $String ? $String(message.remote_chan_reserve_sat) : message.remote_chan_reserve_sat;
                else
                    object.remote_chan_reserve_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_chan_reserve_sat) : options.longs === $Number ? new $util.LongBits(message.remote_chan_reserve_sat.low >>> 0, message.remote_chan_reserve_sat.high >>> 0).toNumber() : message.remote_chan_reserve_sat;
            if (message.static_remote_key != null && $Object.hasOwnProperty.call(message, "static_remote_key"))
                object.static_remote_key = message.static_remote_key;
            if (message.lifetime != null && $Object.hasOwnProperty.call(message, "lifetime"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.lifetime = typeof message.lifetime === "number" ? $BigInt(message.lifetime) : $util.Long.fromBits(message.lifetime.low >>> 0, message.lifetime.high >>> 0, false).toBigInt();
                else if (typeof message.lifetime === "number")
                    object.lifetime = options.longs === $String ? $String(message.lifetime) : message.lifetime;
                else
                    object.lifetime = options.longs === $String ? $util.Long.prototype.toString.call(message.lifetime) : options.longs === $Number ? new $util.LongBits(message.lifetime.low >>> 0, message.lifetime.high >>> 0).toNumber() : message.lifetime;
            if (message.uptime != null && $Object.hasOwnProperty.call(message, "uptime"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.uptime = typeof message.uptime === "number" ? $BigInt(message.uptime) : $util.Long.fromBits(message.uptime.low >>> 0, message.uptime.high >>> 0, false).toBigInt();
                else if (typeof message.uptime === "number")
                    object.uptime = options.longs === $String ? $String(message.uptime) : message.uptime;
                else
                    object.uptime = options.longs === $String ? $util.Long.prototype.toString.call(message.uptime) : options.longs === $Number ? new $util.LongBits(message.uptime.low >>> 0, message.uptime.high >>> 0).toNumber() : message.uptime;
            if (message.close_address != null && $Object.hasOwnProperty.call(message, "close_address"))
                object.close_address = message.close_address;
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                object.commitment_type = options.enums === $String ? $root.lnrpc.CommitmentType[message.commitment_type] === $undefined ? message.commitment_type : $root.lnrpc.CommitmentType[message.commitment_type] : message.commitment_type;
            if (message.push_amount_sat != null && $Object.hasOwnProperty.call(message, "push_amount_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.push_amount_sat = typeof message.push_amount_sat === "number" ? $BigInt(message.push_amount_sat) : $util.Long.fromBits(message.push_amount_sat.low >>> 0, message.push_amount_sat.high >>> 0, true).toBigInt();
                else if (typeof message.push_amount_sat === "number")
                    object.push_amount_sat = options.longs === $String ? $String(message.push_amount_sat) : message.push_amount_sat;
                else
                    object.push_amount_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.push_amount_sat) : options.longs === $Number ? new $util.LongBits(message.push_amount_sat.low >>> 0, message.push_amount_sat.high >>> 0).toNumber(true) : message.push_amount_sat;
            if (message.thaw_height != null && $Object.hasOwnProperty.call(message, "thaw_height"))
                object.thaw_height = message.thaw_height;
            if (message.local_constraints != null && $Object.hasOwnProperty.call(message, "local_constraints"))
                object.local_constraints = $root.lnrpc.ChannelConstraints.toObject(message.local_constraints, options, _depth + 1);
            if (message.remote_constraints != null && $Object.hasOwnProperty.call(message, "remote_constraints"))
                object.remote_constraints = $root.lnrpc.ChannelConstraints.toObject(message.remote_constraints, options, _depth + 1);
            if (message.alias_scids && message.alias_scids.length) {
                object.alias_scids = $Array(message.alias_scids.length);
                for (let j = 0; j < message.alias_scids.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.alias_scids[j] = typeof message.alias_scids[j] === "number" ? $BigInt(message.alias_scids[j]) : $util.Long.fromBits(message.alias_scids[j].low >>> 0, message.alias_scids[j].high >>> 0, true).toBigInt();
                    else if (typeof message.alias_scids[j] === "number")
                        object.alias_scids[j] = options.longs === $String ? $String(message.alias_scids[j]) : message.alias_scids[j];
                    else
                        object.alias_scids[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.alias_scids[j]) : options.longs === $Number ? new $util.LongBits(message.alias_scids[j].low >>> 0, message.alias_scids[j].high >>> 0).toNumber(true) : message.alias_scids[j];
            }
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf"))
                object.zero_conf = message.zero_conf;
            if (message.zero_conf_confirmed_scid != null && $Object.hasOwnProperty.call(message, "zero_conf_confirmed_scid"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.zero_conf_confirmed_scid = typeof message.zero_conf_confirmed_scid === "number" ? $BigInt(message.zero_conf_confirmed_scid) : $util.Long.fromBits(message.zero_conf_confirmed_scid.low >>> 0, message.zero_conf_confirmed_scid.high >>> 0, true).toBigInt();
                else if (typeof message.zero_conf_confirmed_scid === "number")
                    object.zero_conf_confirmed_scid = options.longs === $String ? $String(message.zero_conf_confirmed_scid) : message.zero_conf_confirmed_scid;
                else
                    object.zero_conf_confirmed_scid = options.longs === $String ? $util.Long.prototype.toString.call(message.zero_conf_confirmed_scid) : options.longs === $Number ? new $util.LongBits(message.zero_conf_confirmed_scid.low >>> 0, message.zero_conf_confirmed_scid.high >>> 0).toNumber(true) : message.zero_conf_confirmed_scid;
            if (message.peer_alias != null && $Object.hasOwnProperty.call(message, "peer_alias"))
                object.peer_alias = message.peer_alias;
            if (message.peer_scid_alias != null && $Object.hasOwnProperty.call(message, "peer_scid_alias"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.peer_scid_alias = typeof message.peer_scid_alias === "number" ? $BigInt(message.peer_scid_alias) : $util.Long.fromBits(message.peer_scid_alias.low >>> 0, message.peer_scid_alias.high >>> 0, true).toBigInt();
                else if (typeof message.peer_scid_alias === "number")
                    object.peer_scid_alias = options.longs === $String ? $String(message.peer_scid_alias) : message.peer_scid_alias;
                else
                    object.peer_scid_alias = options.longs === $String ? $util.Long.prototype.toString.call(message.peer_scid_alias) : options.longs === $Number ? new $util.LongBits(message.peer_scid_alias.low >>> 0, message.peer_scid_alias.high >>> 0).toNumber(true) : message.peer_scid_alias;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                object.custom_channel_data = options.bytes === $String ? $util.base64.encode(message.custom_channel_data, 0, message.custom_channel_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_channel_data) : message.custom_channel_data;
            return object;
        };

        /**
         * Converts this Channel to JSON.
         * @function toJSON
         * @memberof lnrpc.Channel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Channel.prototype.toJSON = function() {
            return Channel.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Channel
         * @function getTypeUrl
         * @memberof lnrpc.Channel
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Channel.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Channel";
        };

        return Channel;
    })();

    lnrpc.ListChannelsRequest = (function() {

        /**
         * Properties of a ListChannelsRequest.
         * @typedef {Object} lnrpc.ListChannelsRequest.$Properties
         * @property {boolean|null} [active_only] ListChannelsRequest active_only
         * @property {boolean|null} [inactive_only] ListChannelsRequest inactive_only
         * @property {boolean|null} [public_only] ListChannelsRequest public_only
         * @property {boolean|null} [private_only] ListChannelsRequest private_only
         * @property {Uint8Array|null} [peer] ListChannelsRequest peer
         * @property {boolean|null} [peer_alias_lookup] ListChannelsRequest peer_alias_lookup
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListChannelsRequest.
         * @memberof lnrpc
         * @interface IListChannelsRequest
         * @augments lnrpc.ListChannelsRequest.$Properties
         * @deprecated Use lnrpc.ListChannelsRequest.$Properties instead.
         */

        /**
         * Shape of a ListChannelsRequest.
         * @typedef {lnrpc.ListChannelsRequest.$Properties} lnrpc.ListChannelsRequest.$Shape
         */

        /**
         * Constructs a new ListChannelsRequest.
         * @memberof lnrpc
         * @classdesc Represents a ListChannelsRequest.
         * @constructor
         * @param {lnrpc.ListChannelsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListChannelsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListChannelsRequest active_only.
         * @member {boolean} active_only
         * @memberof lnrpc.ListChannelsRequest
         * @instance
         */
        ListChannelsRequest.prototype.active_only = false;

        /**
         * ListChannelsRequest inactive_only.
         * @member {boolean} inactive_only
         * @memberof lnrpc.ListChannelsRequest
         * @instance
         */
        ListChannelsRequest.prototype.inactive_only = false;

        /**
         * ListChannelsRequest public_only.
         * @member {boolean} public_only
         * @memberof lnrpc.ListChannelsRequest
         * @instance
         */
        ListChannelsRequest.prototype.public_only = false;

        /**
         * ListChannelsRequest private_only.
         * @member {boolean} private_only
         * @memberof lnrpc.ListChannelsRequest
         * @instance
         */
        ListChannelsRequest.prototype.private_only = false;

        /**
         * ListChannelsRequest peer.
         * @member {Uint8Array} peer
         * @memberof lnrpc.ListChannelsRequest
         * @instance
         */
        ListChannelsRequest.prototype.peer = $util.newBuffer([]);

        /**
         * ListChannelsRequest peer_alias_lookup.
         * @member {boolean} peer_alias_lookup
         * @memberof lnrpc.ListChannelsRequest
         * @instance
         */
        ListChannelsRequest.prototype.peer_alias_lookup = false;

        /**
         * Creates a new ListChannelsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListChannelsRequest
         * @static
         * @param {lnrpc.ListChannelsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListChannelsRequest} ListChannelsRequest instance
         * @type {{
         *   (properties: lnrpc.ListChannelsRequest.$Shape): lnrpc.ListChannelsRequest & lnrpc.ListChannelsRequest.$Shape;
         *   (properties?: lnrpc.ListChannelsRequest.$Properties): lnrpc.ListChannelsRequest;
         * }}
         */
        ListChannelsRequest.create = function(properties) {
            return new ListChannelsRequest(properties);
        };

        /**
         * Encodes the specified ListChannelsRequest message. Does not implicitly {@link lnrpc.ListChannelsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListChannelsRequest
         * @static
         * @param {lnrpc.ListChannelsRequest.$Properties} message ListChannelsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListChannelsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.active_only != null && $Object.hasOwnProperty.call(message, "active_only") && message.active_only !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.active_only);
            if (message.inactive_only != null && $Object.hasOwnProperty.call(message, "inactive_only") && message.inactive_only !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.inactive_only);
            if (message.public_only != null && $Object.hasOwnProperty.call(message, "public_only") && message.public_only !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.public_only);
            if (message.private_only != null && $Object.hasOwnProperty.call(message, "private_only") && message.private_only !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.private_only);
            if (message.peer != null && $Object.hasOwnProperty.call(message, "peer") && message.peer.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.peer);
            if (message.peer_alias_lookup != null && $Object.hasOwnProperty.call(message, "peer_alias_lookup") && message.peer_alias_lookup !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.peer_alias_lookup);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListChannelsRequest message, length delimited. Does not implicitly {@link lnrpc.ListChannelsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListChannelsRequest
         * @static
         * @param {lnrpc.ListChannelsRequest.$Properties} message ListChannelsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListChannelsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListChannelsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListChannelsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListChannelsRequest & lnrpc.ListChannelsRequest.$Shape} ListChannelsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListChannelsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListChannelsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.active_only = value;
                        else
                            delete message.active_only;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.inactive_only = value;
                        else
                            delete message.inactive_only;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.public_only = value;
                        else
                            delete message.public_only;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.private_only = value;
                        else
                            delete message.private_only;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.peer = value;
                        else
                            delete message.peer;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.peer_alias_lookup = value;
                        else
                            delete message.peer_alias_lookup;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListChannelsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListChannelsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListChannelsRequest & lnrpc.ListChannelsRequest.$Shape} ListChannelsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListChannelsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListChannelsRequest message.
         * @function verify
         * @memberof lnrpc.ListChannelsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListChannelsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.active_only != null && $Object.hasOwnProperty.call(message, "active_only"))
                if (typeof message.active_only !== "boolean")
                    return "active_only: boolean expected";
            if (message.inactive_only != null && $Object.hasOwnProperty.call(message, "inactive_only"))
                if (typeof message.inactive_only !== "boolean")
                    return "inactive_only: boolean expected";
            if (message.public_only != null && $Object.hasOwnProperty.call(message, "public_only"))
                if (typeof message.public_only !== "boolean")
                    return "public_only: boolean expected";
            if (message.private_only != null && $Object.hasOwnProperty.call(message, "private_only"))
                if (typeof message.private_only !== "boolean")
                    return "private_only: boolean expected";
            if (message.peer != null && $Object.hasOwnProperty.call(message, "peer"))
                if (!(message.peer && typeof message.peer.length === "number" || $util.isString(message.peer)))
                    return "peer: buffer expected";
            if (message.peer_alias_lookup != null && $Object.hasOwnProperty.call(message, "peer_alias_lookup"))
                if (typeof message.peer_alias_lookup !== "boolean")
                    return "peer_alias_lookup: boolean expected";
            return null;
        };

        /**
         * Creates a ListChannelsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListChannelsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListChannelsRequest} ListChannelsRequest
         */
        ListChannelsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListChannelsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListChannelsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListChannelsRequest();
            if (object.active_only != null)
                if (object.active_only)
                    message.active_only = $Boolean(object.active_only);
            if (object.inactive_only != null)
                if (object.inactive_only)
                    message.inactive_only = $Boolean(object.inactive_only);
            if (object.public_only != null)
                if (object.public_only)
                    message.public_only = $Boolean(object.public_only);
            if (object.private_only != null)
                if (object.private_only)
                    message.private_only = $Boolean(object.private_only);
            if (object.peer != null)
                if (object.peer.length)
                    if (typeof object.peer === "string")
                        $util.base64.decode(object.peer, message.peer = $util.newBuffer($util.base64.length(object.peer)), 0);
                    else if (object.peer.length >= 0)
                        message.peer = object.peer;
            if (object.peer_alias_lookup != null)
                if (object.peer_alias_lookup)
                    message.peer_alias_lookup = $Boolean(object.peer_alias_lookup);
            return message;
        };

        /**
         * Creates a plain object from a ListChannelsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListChannelsRequest
         * @static
         * @param {lnrpc.ListChannelsRequest} message ListChannelsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListChannelsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.active_only = false;
                object.inactive_only = false;
                object.public_only = false;
                object.private_only = false;
                if (options.bytes === $String)
                    object.peer = "";
                else {
                    object.peer = [];
                    if (options.bytes !== $Array)
                        object.peer = $util.newBuffer(object.peer);
                }
                object.peer_alias_lookup = false;
            }
            if (message.active_only != null && $Object.hasOwnProperty.call(message, "active_only"))
                object.active_only = message.active_only;
            if (message.inactive_only != null && $Object.hasOwnProperty.call(message, "inactive_only"))
                object.inactive_only = message.inactive_only;
            if (message.public_only != null && $Object.hasOwnProperty.call(message, "public_only"))
                object.public_only = message.public_only;
            if (message.private_only != null && $Object.hasOwnProperty.call(message, "private_only"))
                object.private_only = message.private_only;
            if (message.peer != null && $Object.hasOwnProperty.call(message, "peer"))
                object.peer = options.bytes === $String ? $util.base64.encode(message.peer, 0, message.peer.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.peer) : message.peer;
            if (message.peer_alias_lookup != null && $Object.hasOwnProperty.call(message, "peer_alias_lookup"))
                object.peer_alias_lookup = message.peer_alias_lookup;
            return object;
        };

        /**
         * Converts this ListChannelsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ListChannelsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListChannelsRequest.prototype.toJSON = function() {
            return ListChannelsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListChannelsRequest
         * @function getTypeUrl
         * @memberof lnrpc.ListChannelsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListChannelsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListChannelsRequest";
        };

        return ListChannelsRequest;
    })();

    lnrpc.ListChannelsResponse = (function() {

        /**
         * Properties of a ListChannelsResponse.
         * @typedef {Object} lnrpc.ListChannelsResponse.$Properties
         * @property {Array.<lnrpc.Channel.$Properties>|null} [channels] ListChannelsResponse channels
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListChannelsResponse.
         * @memberof lnrpc
         * @interface IListChannelsResponse
         * @augments lnrpc.ListChannelsResponse.$Properties
         * @deprecated Use lnrpc.ListChannelsResponse.$Properties instead.
         */

        /**
         * Shape of a ListChannelsResponse.
         * @typedef {lnrpc.ListChannelsResponse.$Properties} lnrpc.ListChannelsResponse.$Shape
         */

        /**
         * Constructs a new ListChannelsResponse.
         * @memberof lnrpc
         * @classdesc Represents a ListChannelsResponse.
         * @constructor
         * @param {lnrpc.ListChannelsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListChannelsResponse = function (properties) {
            this.channels = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListChannelsResponse channels.
         * @member {Array.<lnrpc.Channel.$Properties>} channels
         * @memberof lnrpc.ListChannelsResponse
         * @instance
         */
        ListChannelsResponse.prototype.channels = $util.emptyArray;

        /**
         * Creates a new ListChannelsResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListChannelsResponse
         * @static
         * @param {lnrpc.ListChannelsResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListChannelsResponse} ListChannelsResponse instance
         * @type {{
         *   (properties: lnrpc.ListChannelsResponse.$Shape): lnrpc.ListChannelsResponse & lnrpc.ListChannelsResponse.$Shape;
         *   (properties?: lnrpc.ListChannelsResponse.$Properties): lnrpc.ListChannelsResponse;
         * }}
         */
        ListChannelsResponse.create = function(properties) {
            return new ListChannelsResponse(properties);
        };

        /**
         * Encodes the specified ListChannelsResponse message. Does not implicitly {@link lnrpc.ListChannelsResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListChannelsResponse
         * @static
         * @param {lnrpc.ListChannelsResponse.$Properties} message ListChannelsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListChannelsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channels != null && message.channels.length)
                for (let i = 0; i < message.channels.length; ++i)
                    $root.lnrpc.Channel.encode(message.channels[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListChannelsResponse message, length delimited. Does not implicitly {@link lnrpc.ListChannelsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListChannelsResponse
         * @static
         * @param {lnrpc.ListChannelsResponse.$Properties} message ListChannelsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListChannelsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListChannelsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListChannelsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListChannelsResponse & lnrpc.ListChannelsResponse.$Shape} ListChannelsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListChannelsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListChannelsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.channels && message.channels.length))
                            message.channels = [];
                        message.channels.push($root.lnrpc.Channel.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListChannelsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListChannelsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListChannelsResponse & lnrpc.ListChannelsResponse.$Shape} ListChannelsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListChannelsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListChannelsResponse message.
         * @function verify
         * @memberof lnrpc.ListChannelsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListChannelsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channels != null && $Object.hasOwnProperty.call(message, "channels")) {
                if (!$Array.isArray(message.channels))
                    return "channels: array expected";
                for (let i = 0; i < message.channels.length; ++i) {
                    let error = $root.lnrpc.Channel.verify(message.channels[i], _depth + 1);
                    if (error)
                        return "channels." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListChannelsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListChannelsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListChannelsResponse} ListChannelsResponse
         */
        ListChannelsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListChannelsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListChannelsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListChannelsResponse();
            if (object.channels) {
                if (!$Array.isArray(object.channels))
                    throw $TypeError(".lnrpc.ListChannelsResponse.channels: array expected");
                message.channels = $Array(object.channels.length);
                for (let i = 0; i < object.channels.length; ++i) {
                    if (!$util.isObject(object.channels[i]))
                        throw $TypeError(".lnrpc.ListChannelsResponse.channels: object expected");
                    message.channels[i] = $root.lnrpc.Channel.fromObject(object.channels[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListChannelsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListChannelsResponse
         * @static
         * @param {lnrpc.ListChannelsResponse} message ListChannelsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListChannelsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.channels = [];
            if (message.channels && message.channels.length) {
                object.channels = $Array(message.channels.length);
                for (let j = 0; j < message.channels.length; ++j)
                    object.channels[j] = $root.lnrpc.Channel.toObject(message.channels[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ListChannelsResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ListChannelsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListChannelsResponse.prototype.toJSON = function() {
            return ListChannelsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListChannelsResponse
         * @function getTypeUrl
         * @memberof lnrpc.ListChannelsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListChannelsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListChannelsResponse";
        };

        return ListChannelsResponse;
    })();

    lnrpc.AliasMap = (function() {

        /**
         * Properties of an AliasMap.
         * @typedef {Object} lnrpc.AliasMap.$Properties
         * @property {Long|null} [base_scid] AliasMap base_scid
         * @property {Array.<Long>|null} [aliases] AliasMap aliases
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AliasMap.
         * @memberof lnrpc
         * @interface IAliasMap
         * @augments lnrpc.AliasMap.$Properties
         * @deprecated Use lnrpc.AliasMap.$Properties instead.
         */

        /**
         * Shape of an AliasMap.
         * @typedef {lnrpc.AliasMap.$Properties} lnrpc.AliasMap.$Shape
         */

        /**
         * Constructs a new AliasMap.
         * @memberof lnrpc
         * @classdesc Represents an AliasMap.
         * @constructor
         * @param {lnrpc.AliasMap.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AliasMap = function (properties) {
            this.aliases = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AliasMap base_scid.
         * @member {Long} base_scid
         * @memberof lnrpc.AliasMap
         * @instance
         */
        AliasMap.prototype.base_scid = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * AliasMap aliases.
         * @member {Array.<Long>} aliases
         * @memberof lnrpc.AliasMap
         * @instance
         */
        AliasMap.prototype.aliases = $util.emptyArray;

        /**
         * Creates a new AliasMap instance using the specified properties.
         * @function create
         * @memberof lnrpc.AliasMap
         * @static
         * @param {lnrpc.AliasMap.$Properties=} [properties] Properties to set
         * @returns {lnrpc.AliasMap} AliasMap instance
         * @type {{
         *   (properties: lnrpc.AliasMap.$Shape): lnrpc.AliasMap & lnrpc.AliasMap.$Shape;
         *   (properties?: lnrpc.AliasMap.$Properties): lnrpc.AliasMap;
         * }}
         */
        AliasMap.create = function(properties) {
            return new AliasMap(properties);
        };

        /**
         * Encodes the specified AliasMap message. Does not implicitly {@link lnrpc.AliasMap.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.AliasMap
         * @static
         * @param {lnrpc.AliasMap.$Properties} message AliasMap message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AliasMap.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.base_scid != null && $Object.hasOwnProperty.call(message, "base_scid") && (typeof message.base_scid === "object" ? message.base_scid.low || message.base_scid.high : message.base_scid !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.base_scid);
            if (message.aliases != null && message.aliases.length)
                writer.uint32(/* id 2, wireType 2 =*/18).uint64s(message.aliases);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AliasMap message, length delimited. Does not implicitly {@link lnrpc.AliasMap.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.AliasMap
         * @static
         * @param {lnrpc.AliasMap.$Properties} message AliasMap message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AliasMap.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AliasMap message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.AliasMap
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.AliasMap & lnrpc.AliasMap.$Shape} AliasMap
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AliasMap.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.AliasMap(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.base_scid = value;
                        else
                            delete message.base_scid;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.aliases && message.aliases.length))
                                message.aliases = [];
                            reader.uint64s(message.aliases);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.aliases && message.aliases.length))
                            message.aliases = [];
                        message.aliases.push(reader.uint64());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AliasMap message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.AliasMap
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.AliasMap & lnrpc.AliasMap.$Shape} AliasMap
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AliasMap.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AliasMap message.
         * @function verify
         * @memberof lnrpc.AliasMap
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AliasMap.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.base_scid != null && $Object.hasOwnProperty.call(message, "base_scid"))
                if (!$util.isInteger(message.base_scid) && !(message.base_scid && $util.isInteger(message.base_scid.low) && $util.isInteger(message.base_scid.high)))
                    return "base_scid: integer|Long expected";
            if (message.aliases != null && $Object.hasOwnProperty.call(message, "aliases")) {
                if (!$Array.isArray(message.aliases))
                    return "aliases: array expected";
                for (let i = 0; i < message.aliases.length; ++i)
                    if (!$util.isInteger(message.aliases[i]) && !(message.aliases[i] && $util.isInteger(message.aliases[i].low) && $util.isInteger(message.aliases[i].high)))
                        return "aliases: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates an AliasMap message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.AliasMap
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.AliasMap} AliasMap
         */
        AliasMap.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.AliasMap)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.AliasMap: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.AliasMap();
            if (object.base_scid != null)
                if (typeof object.base_scid === "object" ? object.base_scid.low || object.base_scid.high : $Number(object.base_scid) !== 0)
                    if ($util.Long)
                        message.base_scid = $util.Long.fromValue(object.base_scid, true);
                    else if (typeof object.base_scid === "string")
                        message.base_scid = $parseInt(object.base_scid, 10);
                    else if (typeof object.base_scid === "number")
                        message.base_scid = object.base_scid;
                    else if (typeof object.base_scid === "object")
                        message.base_scid = new $util.LongBits(object.base_scid.low >>> 0, object.base_scid.high >>> 0).toNumber(true);
            if (object.aliases) {
                if (!$Array.isArray(object.aliases))
                    throw $TypeError(".lnrpc.AliasMap.aliases: array expected");
                message.aliases = $Array(object.aliases.length);
                for (let i = 0; i < object.aliases.length; ++i)
                    if ($util.Long)
                        message.aliases[i] = $util.Long.fromValue(object.aliases[i], true);
                    else if (typeof object.aliases[i] === "string")
                        message.aliases[i] = $parseInt(object.aliases[i], 10);
                    else if (typeof object.aliases[i] === "number")
                        message.aliases[i] = object.aliases[i];
                    else if (typeof object.aliases[i] === "object")
                        message.aliases[i] = new $util.LongBits(object.aliases[i].low >>> 0, object.aliases[i].high >>> 0).toNumber(true);
            }
            return message;
        };

        /**
         * Creates a plain object from an AliasMap message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.AliasMap
         * @static
         * @param {lnrpc.AliasMap} message AliasMap
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AliasMap.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.aliases = [];
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.base_scid = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.base_scid = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.base_scid != null && $Object.hasOwnProperty.call(message, "base_scid"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.base_scid = typeof message.base_scid === "number" ? $BigInt(message.base_scid) : $util.Long.fromBits(message.base_scid.low >>> 0, message.base_scid.high >>> 0, true).toBigInt();
                else if (typeof message.base_scid === "number")
                    object.base_scid = options.longs === $String ? $String(message.base_scid) : message.base_scid;
                else
                    object.base_scid = options.longs === $String ? $util.Long.prototype.toString.call(message.base_scid) : options.longs === $Number ? new $util.LongBits(message.base_scid.low >>> 0, message.base_scid.high >>> 0).toNumber(true) : message.base_scid;
            if (message.aliases && message.aliases.length) {
                object.aliases = $Array(message.aliases.length);
                for (let j = 0; j < message.aliases.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.aliases[j] = typeof message.aliases[j] === "number" ? $BigInt(message.aliases[j]) : $util.Long.fromBits(message.aliases[j].low >>> 0, message.aliases[j].high >>> 0, true).toBigInt();
                    else if (typeof message.aliases[j] === "number")
                        object.aliases[j] = options.longs === $String ? $String(message.aliases[j]) : message.aliases[j];
                    else
                        object.aliases[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.aliases[j]) : options.longs === $Number ? new $util.LongBits(message.aliases[j].low >>> 0, message.aliases[j].high >>> 0).toNumber(true) : message.aliases[j];
            }
            return object;
        };

        /**
         * Converts this AliasMap to JSON.
         * @function toJSON
         * @memberof lnrpc.AliasMap
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AliasMap.prototype.toJSON = function() {
            return AliasMap.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AliasMap
         * @function getTypeUrl
         * @memberof lnrpc.AliasMap
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AliasMap.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.AliasMap";
        };

        return AliasMap;
    })();

    lnrpc.ListAliasesRequest = (function() {

        /**
         * Properties of a ListAliasesRequest.
         * @typedef {Object} lnrpc.ListAliasesRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListAliasesRequest.
         * @memberof lnrpc
         * @interface IListAliasesRequest
         * @augments lnrpc.ListAliasesRequest.$Properties
         * @deprecated Use lnrpc.ListAliasesRequest.$Properties instead.
         */

        /**
         * Shape of a ListAliasesRequest.
         * @typedef {lnrpc.ListAliasesRequest.$Properties} lnrpc.ListAliasesRequest.$Shape
         */

        /**
         * Constructs a new ListAliasesRequest.
         * @memberof lnrpc
         * @classdesc Represents a ListAliasesRequest.
         * @constructor
         * @param {lnrpc.ListAliasesRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListAliasesRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ListAliasesRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListAliasesRequest
         * @static
         * @param {lnrpc.ListAliasesRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListAliasesRequest} ListAliasesRequest instance
         * @type {{
         *   (properties: lnrpc.ListAliasesRequest.$Shape): lnrpc.ListAliasesRequest & lnrpc.ListAliasesRequest.$Shape;
         *   (properties?: lnrpc.ListAliasesRequest.$Properties): lnrpc.ListAliasesRequest;
         * }}
         */
        ListAliasesRequest.create = function(properties) {
            return new ListAliasesRequest(properties);
        };

        /**
         * Encodes the specified ListAliasesRequest message. Does not implicitly {@link lnrpc.ListAliasesRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListAliasesRequest
         * @static
         * @param {lnrpc.ListAliasesRequest.$Properties} message ListAliasesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAliasesRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListAliasesRequest message, length delimited. Does not implicitly {@link lnrpc.ListAliasesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListAliasesRequest
         * @static
         * @param {lnrpc.ListAliasesRequest.$Properties} message ListAliasesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAliasesRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListAliasesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListAliasesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListAliasesRequest & lnrpc.ListAliasesRequest.$Shape} ListAliasesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAliasesRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListAliasesRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListAliasesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListAliasesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListAliasesRequest & lnrpc.ListAliasesRequest.$Shape} ListAliasesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAliasesRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListAliasesRequest message.
         * @function verify
         * @memberof lnrpc.ListAliasesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListAliasesRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ListAliasesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListAliasesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListAliasesRequest} ListAliasesRequest
         */
        ListAliasesRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListAliasesRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListAliasesRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.ListAliasesRequest();
        };

        /**
         * Creates a plain object from a ListAliasesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListAliasesRequest
         * @static
         * @param {lnrpc.ListAliasesRequest} message ListAliasesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListAliasesRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this ListAliasesRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ListAliasesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListAliasesRequest.prototype.toJSON = function() {
            return ListAliasesRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListAliasesRequest
         * @function getTypeUrl
         * @memberof lnrpc.ListAliasesRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListAliasesRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListAliasesRequest";
        };

        return ListAliasesRequest;
    })();

    lnrpc.ListAliasesResponse = (function() {

        /**
         * Properties of a ListAliasesResponse.
         * @typedef {Object} lnrpc.ListAliasesResponse.$Properties
         * @property {Array.<lnrpc.AliasMap.$Properties>|null} [alias_maps] ListAliasesResponse alias_maps
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListAliasesResponse.
         * @memberof lnrpc
         * @interface IListAliasesResponse
         * @augments lnrpc.ListAliasesResponse.$Properties
         * @deprecated Use lnrpc.ListAliasesResponse.$Properties instead.
         */

        /**
         * Shape of a ListAliasesResponse.
         * @typedef {lnrpc.ListAliasesResponse.$Properties} lnrpc.ListAliasesResponse.$Shape
         */

        /**
         * Constructs a new ListAliasesResponse.
         * @memberof lnrpc
         * @classdesc Represents a ListAliasesResponse.
         * @constructor
         * @param {lnrpc.ListAliasesResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListAliasesResponse = function (properties) {
            this.alias_maps = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListAliasesResponse alias_maps.
         * @member {Array.<lnrpc.AliasMap.$Properties>} alias_maps
         * @memberof lnrpc.ListAliasesResponse
         * @instance
         */
        ListAliasesResponse.prototype.alias_maps = $util.emptyArray;

        /**
         * Creates a new ListAliasesResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListAliasesResponse
         * @static
         * @param {lnrpc.ListAliasesResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListAliasesResponse} ListAliasesResponse instance
         * @type {{
         *   (properties: lnrpc.ListAliasesResponse.$Shape): lnrpc.ListAliasesResponse & lnrpc.ListAliasesResponse.$Shape;
         *   (properties?: lnrpc.ListAliasesResponse.$Properties): lnrpc.ListAliasesResponse;
         * }}
         */
        ListAliasesResponse.create = function(properties) {
            return new ListAliasesResponse(properties);
        };

        /**
         * Encodes the specified ListAliasesResponse message. Does not implicitly {@link lnrpc.ListAliasesResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListAliasesResponse
         * @static
         * @param {lnrpc.ListAliasesResponse.$Properties} message ListAliasesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAliasesResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.alias_maps != null && message.alias_maps.length)
                for (let i = 0; i < message.alias_maps.length; ++i)
                    $root.lnrpc.AliasMap.encode(message.alias_maps[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListAliasesResponse message, length delimited. Does not implicitly {@link lnrpc.ListAliasesResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListAliasesResponse
         * @static
         * @param {lnrpc.ListAliasesResponse.$Properties} message ListAliasesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAliasesResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListAliasesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListAliasesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListAliasesResponse & lnrpc.ListAliasesResponse.$Shape} ListAliasesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAliasesResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListAliasesResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.alias_maps && message.alias_maps.length))
                            message.alias_maps = [];
                        message.alias_maps.push($root.lnrpc.AliasMap.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListAliasesResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListAliasesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListAliasesResponse & lnrpc.ListAliasesResponse.$Shape} ListAliasesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAliasesResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListAliasesResponse message.
         * @function verify
         * @memberof lnrpc.ListAliasesResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListAliasesResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.alias_maps != null && $Object.hasOwnProperty.call(message, "alias_maps")) {
                if (!$Array.isArray(message.alias_maps))
                    return "alias_maps: array expected";
                for (let i = 0; i < message.alias_maps.length; ++i) {
                    let error = $root.lnrpc.AliasMap.verify(message.alias_maps[i], _depth + 1);
                    if (error)
                        return "alias_maps." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListAliasesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListAliasesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListAliasesResponse} ListAliasesResponse
         */
        ListAliasesResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListAliasesResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListAliasesResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListAliasesResponse();
            if (object.alias_maps) {
                if (!$Array.isArray(object.alias_maps))
                    throw $TypeError(".lnrpc.ListAliasesResponse.alias_maps: array expected");
                message.alias_maps = $Array(object.alias_maps.length);
                for (let i = 0; i < object.alias_maps.length; ++i) {
                    if (!$util.isObject(object.alias_maps[i]))
                        throw $TypeError(".lnrpc.ListAliasesResponse.alias_maps: object expected");
                    message.alias_maps[i] = $root.lnrpc.AliasMap.fromObject(object.alias_maps[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListAliasesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListAliasesResponse
         * @static
         * @param {lnrpc.ListAliasesResponse} message ListAliasesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListAliasesResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.alias_maps = [];
            if (message.alias_maps && message.alias_maps.length) {
                object.alias_maps = $Array(message.alias_maps.length);
                for (let j = 0; j < message.alias_maps.length; ++j)
                    object.alias_maps[j] = $root.lnrpc.AliasMap.toObject(message.alias_maps[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ListAliasesResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ListAliasesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListAliasesResponse.prototype.toJSON = function() {
            return ListAliasesResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListAliasesResponse
         * @function getTypeUrl
         * @memberof lnrpc.ListAliasesResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListAliasesResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListAliasesResponse";
        };

        return ListAliasesResponse;
    })();

    /**
     * Initiator enum.
     * @name lnrpc.Initiator
     * @enum {number}
     * @property {number} INITIATOR_UNKNOWN=0 INITIATOR_UNKNOWN value
     * @property {number} INITIATOR_LOCAL=1 INITIATOR_LOCAL value
     * @property {number} INITIATOR_REMOTE=2 INITIATOR_REMOTE value
     * @property {number} INITIATOR_BOTH=3 INITIATOR_BOTH value
     */
    lnrpc.Initiator = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "INITIATOR_UNKNOWN"] = 0;
        values[valuesById[1] = "INITIATOR_LOCAL"] = 1;
        values[valuesById[2] = "INITIATOR_REMOTE"] = 2;
        values[valuesById[3] = "INITIATOR_BOTH"] = 3;
        return values;
    })();

    lnrpc.ChannelCloseSummary = (function() {

        /**
         * Properties of a ChannelCloseSummary.
         * @typedef {Object} lnrpc.ChannelCloseSummary.$Properties
         * @property {string|null} [channel_point] ChannelCloseSummary channel_point
         * @property {Long|null} [chan_id] ChannelCloseSummary chan_id
         * @property {string|null} [chain_hash] ChannelCloseSummary chain_hash
         * @property {string|null} [closing_tx_hash] ChannelCloseSummary closing_tx_hash
         * @property {string|null} [remote_pubkey] ChannelCloseSummary remote_pubkey
         * @property {Long|null} [capacity] ChannelCloseSummary capacity
         * @property {number|null} [close_height] ChannelCloseSummary close_height
         * @property {Long|null} [settled_balance] ChannelCloseSummary settled_balance
         * @property {Long|null} [time_locked_balance] ChannelCloseSummary time_locked_balance
         * @property {lnrpc.ChannelCloseSummary.ClosureType|null} [close_type] ChannelCloseSummary close_type
         * @property {lnrpc.Initiator|null} [open_initiator] ChannelCloseSummary open_initiator
         * @property {lnrpc.Initiator|null} [close_initiator] ChannelCloseSummary close_initiator
         * @property {Array.<lnrpc.Resolution.$Properties>|null} [resolutions] ChannelCloseSummary resolutions
         * @property {Array.<Long>|null} [alias_scids] ChannelCloseSummary alias_scids
         * @property {Long|null} [zero_conf_confirmed_scid] ChannelCloseSummary zero_conf_confirmed_scid
         * @property {Uint8Array|null} [custom_channel_data] ChannelCloseSummary custom_channel_data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelCloseSummary.
         * @memberof lnrpc
         * @interface IChannelCloseSummary
         * @augments lnrpc.ChannelCloseSummary.$Properties
         * @deprecated Use lnrpc.ChannelCloseSummary.$Properties instead.
         */

        /**
         * Shape of a ChannelCloseSummary.
         * @typedef {lnrpc.ChannelCloseSummary.$Properties} lnrpc.ChannelCloseSummary.$Shape
         */

        /**
         * Constructs a new ChannelCloseSummary.
         * @memberof lnrpc
         * @classdesc Represents a ChannelCloseSummary.
         * @constructor
         * @param {lnrpc.ChannelCloseSummary.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelCloseSummary = function (properties) {
            this.resolutions = [];
            this.alias_scids = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelCloseSummary channel_point.
         * @member {string} channel_point
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.channel_point = "";

        /**
         * ChannelCloseSummary chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelCloseSummary chain_hash.
         * @member {string} chain_hash
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.chain_hash = "";

        /**
         * ChannelCloseSummary closing_tx_hash.
         * @member {string} closing_tx_hash
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.closing_tx_hash = "";

        /**
         * ChannelCloseSummary remote_pubkey.
         * @member {string} remote_pubkey
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.remote_pubkey = "";

        /**
         * ChannelCloseSummary capacity.
         * @member {Long} capacity
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.capacity = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChannelCloseSummary close_height.
         * @member {number} close_height
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.close_height = 0;

        /**
         * ChannelCloseSummary settled_balance.
         * @member {Long} settled_balance
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.settled_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChannelCloseSummary time_locked_balance.
         * @member {Long} time_locked_balance
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.time_locked_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChannelCloseSummary close_type.
         * @member {lnrpc.ChannelCloseSummary.ClosureType} close_type
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.close_type = 0;

        /**
         * ChannelCloseSummary open_initiator.
         * @member {lnrpc.Initiator} open_initiator
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.open_initiator = 0;

        /**
         * ChannelCloseSummary close_initiator.
         * @member {lnrpc.Initiator} close_initiator
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.close_initiator = 0;

        /**
         * ChannelCloseSummary resolutions.
         * @member {Array.<lnrpc.Resolution.$Properties>} resolutions
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.resolutions = $util.emptyArray;

        /**
         * ChannelCloseSummary alias_scids.
         * @member {Array.<Long>} alias_scids
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.alias_scids = $util.emptyArray;

        /**
         * ChannelCloseSummary zero_conf_confirmed_scid.
         * @member {Long} zero_conf_confirmed_scid
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.zero_conf_confirmed_scid = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelCloseSummary custom_channel_data.
         * @member {Uint8Array} custom_channel_data
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         */
        ChannelCloseSummary.prototype.custom_channel_data = $util.newBuffer([]);

        /**
         * Creates a new ChannelCloseSummary instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelCloseSummary
         * @static
         * @param {lnrpc.ChannelCloseSummary.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelCloseSummary} ChannelCloseSummary instance
         * @type {{
         *   (properties: lnrpc.ChannelCloseSummary.$Shape): lnrpc.ChannelCloseSummary & lnrpc.ChannelCloseSummary.$Shape;
         *   (properties?: lnrpc.ChannelCloseSummary.$Properties): lnrpc.ChannelCloseSummary;
         * }}
         */
        ChannelCloseSummary.create = function(properties) {
            return new ChannelCloseSummary(properties);
        };

        /**
         * Encodes the specified ChannelCloseSummary message. Does not implicitly {@link lnrpc.ChannelCloseSummary.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelCloseSummary
         * @static
         * @param {lnrpc.ChannelCloseSummary.$Properties} message ChannelCloseSummary message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelCloseSummary.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point") && message.channel_point !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.channel_point);
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.chan_id);
            if (message.chain_hash != null && $Object.hasOwnProperty.call(message, "chain_hash") && message.chain_hash !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.chain_hash);
            if (message.closing_tx_hash != null && $Object.hasOwnProperty.call(message, "closing_tx_hash") && message.closing_tx_hash !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.closing_tx_hash);
            if (message.remote_pubkey != null && $Object.hasOwnProperty.call(message, "remote_pubkey") && message.remote_pubkey !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.remote_pubkey);
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity") && (typeof message.capacity === "object" ? message.capacity.low || message.capacity.high : message.capacity !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.capacity);
            if (message.close_height != null && $Object.hasOwnProperty.call(message, "close_height") && message.close_height !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.close_height);
            if (message.settled_balance != null && $Object.hasOwnProperty.call(message, "settled_balance") && (typeof message.settled_balance === "object" ? message.settled_balance.low || message.settled_balance.high : message.settled_balance !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.settled_balance);
            if (message.time_locked_balance != null && $Object.hasOwnProperty.call(message, "time_locked_balance") && (typeof message.time_locked_balance === "object" ? message.time_locked_balance.low || message.time_locked_balance.high : message.time_locked_balance !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.time_locked_balance);
            if (message.close_type != null && $Object.hasOwnProperty.call(message, "close_type") && message.close_type !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.close_type);
            if (message.open_initiator != null && $Object.hasOwnProperty.call(message, "open_initiator") && message.open_initiator !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.open_initiator);
            if (message.close_initiator != null && $Object.hasOwnProperty.call(message, "close_initiator") && message.close_initiator !== 0)
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.close_initiator);
            if (message.resolutions != null && message.resolutions.length)
                for (let i = 0; i < message.resolutions.length; ++i)
                    $root.lnrpc.Resolution.encode(message.resolutions[i], writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.alias_scids != null && message.alias_scids.length)
                writer.uint32(/* id 14, wireType 2 =*/114).uint64s(message.alias_scids);
            if (message.zero_conf_confirmed_scid != null && $Object.hasOwnProperty.call(message, "zero_conf_confirmed_scid") && (typeof message.zero_conf_confirmed_scid === "object" ? message.zero_conf_confirmed_scid.low || message.zero_conf_confirmed_scid.high : message.zero_conf_confirmed_scid !== 0))
                writer.uint32(/* id 15, wireType 0 =*/120).uint64(message.zero_conf_confirmed_scid);
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data") && message.custom_channel_data.length)
                writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.custom_channel_data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelCloseSummary message, length delimited. Does not implicitly {@link lnrpc.ChannelCloseSummary.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelCloseSummary
         * @static
         * @param {lnrpc.ChannelCloseSummary.$Properties} message ChannelCloseSummary message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelCloseSummary.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelCloseSummary message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelCloseSummary
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelCloseSummary & lnrpc.ChannelCloseSummary.$Shape} ChannelCloseSummary
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelCloseSummary.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelCloseSummary(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.channel_point = value;
                        else
                            delete message.channel_point;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.chain_hash = value;
                        else
                            delete message.chain_hash;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.closing_tx_hash = value;
                        else
                            delete message.closing_tx_hash;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.remote_pubkey = value;
                        else
                            delete message.remote_pubkey;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.capacity = value;
                        else
                            delete message.capacity;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.close_height = value;
                        else
                            delete message.close_height;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.settled_balance = value;
                        else
                            delete message.settled_balance;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.time_locked_balance = value;
                        else
                            delete message.time_locked_balance;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.close_type = value;
                        else
                            delete message.close_type;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.open_initiator = value;
                        else
                            delete message.open_initiator;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.close_initiator = value;
                        else
                            delete message.close_initiator;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if (!(message.resolutions && message.resolutions.length))
                            message.resolutions = [];
                        message.resolutions.push($root.lnrpc.Resolution.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 14: {
                        if (wireType === 2) {
                            if (!(message.alias_scids && message.alias_scids.length))
                                message.alias_scids = [];
                            reader.uint64s(message.alias_scids);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.alias_scids && message.alias_scids.length))
                            message.alias_scids = [];
                        message.alias_scids.push(reader.uint64());
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.zero_conf_confirmed_scid = value;
                        else
                            delete message.zero_conf_confirmed_scid;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.custom_channel_data = value;
                        else
                            delete message.custom_channel_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelCloseSummary message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelCloseSummary
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelCloseSummary & lnrpc.ChannelCloseSummary.$Shape} ChannelCloseSummary
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelCloseSummary.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelCloseSummary message.
         * @function verify
         * @memberof lnrpc.ChannelCloseSummary
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelCloseSummary.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                if (!$util.isString(message.channel_point))
                    return "channel_point: string expected";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.chain_hash != null && $Object.hasOwnProperty.call(message, "chain_hash"))
                if (!$util.isString(message.chain_hash))
                    return "chain_hash: string expected";
            if (message.closing_tx_hash != null && $Object.hasOwnProperty.call(message, "closing_tx_hash"))
                if (!$util.isString(message.closing_tx_hash))
                    return "closing_tx_hash: string expected";
            if (message.remote_pubkey != null && $Object.hasOwnProperty.call(message, "remote_pubkey"))
                if (!$util.isString(message.remote_pubkey))
                    return "remote_pubkey: string expected";
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (!$util.isInteger(message.capacity) && !(message.capacity && $util.isInteger(message.capacity.low) && $util.isInteger(message.capacity.high)))
                    return "capacity: integer|Long expected";
            if (message.close_height != null && $Object.hasOwnProperty.call(message, "close_height"))
                if (!$util.isInteger(message.close_height))
                    return "close_height: integer expected";
            if (message.settled_balance != null && $Object.hasOwnProperty.call(message, "settled_balance"))
                if (!$util.isInteger(message.settled_balance) && !(message.settled_balance && $util.isInteger(message.settled_balance.low) && $util.isInteger(message.settled_balance.high)))
                    return "settled_balance: integer|Long expected";
            if (message.time_locked_balance != null && $Object.hasOwnProperty.call(message, "time_locked_balance"))
                if (!$util.isInteger(message.time_locked_balance) && !(message.time_locked_balance && $util.isInteger(message.time_locked_balance.low) && $util.isInteger(message.time_locked_balance.high)))
                    return "time_locked_balance: integer|Long expected";
            if (message.close_type != null && $Object.hasOwnProperty.call(message, "close_type"))
                if (typeof message.close_type !== "number" || (message.close_type | 0) !== message.close_type)
                    return "close_type: enum value expected";
            if (message.open_initiator != null && $Object.hasOwnProperty.call(message, "open_initiator"))
                if (typeof message.open_initiator !== "number" || (message.open_initiator | 0) !== message.open_initiator)
                    return "open_initiator: enum value expected";
            if (message.close_initiator != null && $Object.hasOwnProperty.call(message, "close_initiator"))
                if (typeof message.close_initiator !== "number" || (message.close_initiator | 0) !== message.close_initiator)
                    return "close_initiator: enum value expected";
            if (message.resolutions != null && $Object.hasOwnProperty.call(message, "resolutions")) {
                if (!$Array.isArray(message.resolutions))
                    return "resolutions: array expected";
                for (let i = 0; i < message.resolutions.length; ++i) {
                    let error = $root.lnrpc.Resolution.verify(message.resolutions[i], _depth + 1);
                    if (error)
                        return "resolutions." + error;
                }
            }
            if (message.alias_scids != null && $Object.hasOwnProperty.call(message, "alias_scids")) {
                if (!$Array.isArray(message.alias_scids))
                    return "alias_scids: array expected";
                for (let i = 0; i < message.alias_scids.length; ++i)
                    if (!$util.isInteger(message.alias_scids[i]) && !(message.alias_scids[i] && $util.isInteger(message.alias_scids[i].low) && $util.isInteger(message.alias_scids[i].high)))
                        return "alias_scids: integer|Long[] expected";
            }
            if (message.zero_conf_confirmed_scid != null && $Object.hasOwnProperty.call(message, "zero_conf_confirmed_scid"))
                if (!$util.isInteger(message.zero_conf_confirmed_scid) && !(message.zero_conf_confirmed_scid && $util.isInteger(message.zero_conf_confirmed_scid.low) && $util.isInteger(message.zero_conf_confirmed_scid.high)))
                    return "zero_conf_confirmed_scid: integer|Long expected";
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                if (!(message.custom_channel_data && typeof message.custom_channel_data.length === "number" || $util.isString(message.custom_channel_data)))
                    return "custom_channel_data: buffer expected";
            return null;
        };

        /**
         * Creates a ChannelCloseSummary message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelCloseSummary
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelCloseSummary} ChannelCloseSummary
         */
        ChannelCloseSummary.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelCloseSummary)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelCloseSummary: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelCloseSummary();
            if (object.channel_point != null)
                if (typeof object.channel_point !== "string" || object.channel_point.length)
                    message.channel_point = $String(object.channel_point);
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.chain_hash != null)
                if (typeof object.chain_hash !== "string" || object.chain_hash.length)
                    message.chain_hash = $String(object.chain_hash);
            if (object.closing_tx_hash != null)
                if (typeof object.closing_tx_hash !== "string" || object.closing_tx_hash.length)
                    message.closing_tx_hash = $String(object.closing_tx_hash);
            if (object.remote_pubkey != null)
                if (typeof object.remote_pubkey !== "string" || object.remote_pubkey.length)
                    message.remote_pubkey = $String(object.remote_pubkey);
            if (object.capacity != null)
                if (typeof object.capacity === "object" ? object.capacity.low || object.capacity.high : $Number(object.capacity) !== 0)
                    if ($util.Long)
                        message.capacity = $util.Long.fromValue(object.capacity, false);
                    else if (typeof object.capacity === "string")
                        message.capacity = $parseInt(object.capacity, 10);
                    else if (typeof object.capacity === "number")
                        message.capacity = object.capacity;
                    else if (typeof object.capacity === "object")
                        message.capacity = new $util.LongBits(object.capacity.low >>> 0, object.capacity.high >>> 0).toNumber();
            if (object.close_height != null)
                if ($Number(object.close_height) !== 0)
                    message.close_height = object.close_height >>> 0;
            if (object.settled_balance != null)
                if (typeof object.settled_balance === "object" ? object.settled_balance.low || object.settled_balance.high : $Number(object.settled_balance) !== 0)
                    if ($util.Long)
                        message.settled_balance = $util.Long.fromValue(object.settled_balance, false);
                    else if (typeof object.settled_balance === "string")
                        message.settled_balance = $parseInt(object.settled_balance, 10);
                    else if (typeof object.settled_balance === "number")
                        message.settled_balance = object.settled_balance;
                    else if (typeof object.settled_balance === "object")
                        message.settled_balance = new $util.LongBits(object.settled_balance.low >>> 0, object.settled_balance.high >>> 0).toNumber();
            if (object.time_locked_balance != null)
                if (typeof object.time_locked_balance === "object" ? object.time_locked_balance.low || object.time_locked_balance.high : $Number(object.time_locked_balance) !== 0)
                    if ($util.Long)
                        message.time_locked_balance = $util.Long.fromValue(object.time_locked_balance, false);
                    else if (typeof object.time_locked_balance === "string")
                        message.time_locked_balance = $parseInt(object.time_locked_balance, 10);
                    else if (typeof object.time_locked_balance === "number")
                        message.time_locked_balance = object.time_locked_balance;
                    else if (typeof object.time_locked_balance === "object")
                        message.time_locked_balance = new $util.LongBits(object.time_locked_balance.low >>> 0, object.time_locked_balance.high >>> 0).toNumber();
            if (object.close_type !== 0 && (typeof object.close_type !== "string" || $root.lnrpc.ChannelCloseSummary.ClosureType[object.close_type] !== 0))
                switch (object.close_type) {
                case "COOPERATIVE_CLOSE":
                case 0:
                    message.close_type = 0;
                    break;
                case "LOCAL_FORCE_CLOSE":
                case 1:
                    message.close_type = 1;
                    break;
                case "REMOTE_FORCE_CLOSE":
                case 2:
                    message.close_type = 2;
                    break;
                case "BREACH_CLOSE":
                case 3:
                    message.close_type = 3;
                    break;
                case "FUNDING_CANCELED":
                case 4:
                    message.close_type = 4;
                    break;
                case "ABANDONED":
                case 5:
                    message.close_type = 5;
                    break;
                default:
                    if (typeof object.close_type === "number" && (object.close_type | 0) === object.close_type)
                        message.close_type = object.close_type;
                }
            if (object.open_initiator !== 0 && (typeof object.open_initiator !== "string" || $root.lnrpc.Initiator[object.open_initiator] !== 0))
                switch (object.open_initiator) {
                case "INITIATOR_UNKNOWN":
                case 0:
                    message.open_initiator = 0;
                    break;
                case "INITIATOR_LOCAL":
                case 1:
                    message.open_initiator = 1;
                    break;
                case "INITIATOR_REMOTE":
                case 2:
                    message.open_initiator = 2;
                    break;
                case "INITIATOR_BOTH":
                case 3:
                    message.open_initiator = 3;
                    break;
                default:
                    if (typeof object.open_initiator === "number" && (object.open_initiator | 0) === object.open_initiator)
                        message.open_initiator = object.open_initiator;
                }
            if (object.close_initiator !== 0 && (typeof object.close_initiator !== "string" || $root.lnrpc.Initiator[object.close_initiator] !== 0))
                switch (object.close_initiator) {
                case "INITIATOR_UNKNOWN":
                case 0:
                    message.close_initiator = 0;
                    break;
                case "INITIATOR_LOCAL":
                case 1:
                    message.close_initiator = 1;
                    break;
                case "INITIATOR_REMOTE":
                case 2:
                    message.close_initiator = 2;
                    break;
                case "INITIATOR_BOTH":
                case 3:
                    message.close_initiator = 3;
                    break;
                default:
                    if (typeof object.close_initiator === "number" && (object.close_initiator | 0) === object.close_initiator)
                        message.close_initiator = object.close_initiator;
                }
            if (object.resolutions) {
                if (!$Array.isArray(object.resolutions))
                    throw $TypeError(".lnrpc.ChannelCloseSummary.resolutions: array expected");
                message.resolutions = $Array(object.resolutions.length);
                for (let i = 0; i < object.resolutions.length; ++i) {
                    if (!$util.isObject(object.resolutions[i]))
                        throw $TypeError(".lnrpc.ChannelCloseSummary.resolutions: object expected");
                    message.resolutions[i] = $root.lnrpc.Resolution.fromObject(object.resolutions[i], _depth + 1);
                }
            }
            if (object.alias_scids) {
                if (!$Array.isArray(object.alias_scids))
                    throw $TypeError(".lnrpc.ChannelCloseSummary.alias_scids: array expected");
                message.alias_scids = $Array(object.alias_scids.length);
                for (let i = 0; i < object.alias_scids.length; ++i)
                    if ($util.Long)
                        message.alias_scids[i] = $util.Long.fromValue(object.alias_scids[i], true);
                    else if (typeof object.alias_scids[i] === "string")
                        message.alias_scids[i] = $parseInt(object.alias_scids[i], 10);
                    else if (typeof object.alias_scids[i] === "number")
                        message.alias_scids[i] = object.alias_scids[i];
                    else if (typeof object.alias_scids[i] === "object")
                        message.alias_scids[i] = new $util.LongBits(object.alias_scids[i].low >>> 0, object.alias_scids[i].high >>> 0).toNumber(true);
            }
            if (object.zero_conf_confirmed_scid != null)
                if (typeof object.zero_conf_confirmed_scid === "object" ? object.zero_conf_confirmed_scid.low || object.zero_conf_confirmed_scid.high : $Number(object.zero_conf_confirmed_scid) !== 0)
                    if ($util.Long)
                        message.zero_conf_confirmed_scid = $util.Long.fromValue(object.zero_conf_confirmed_scid, true);
                    else if (typeof object.zero_conf_confirmed_scid === "string")
                        message.zero_conf_confirmed_scid = $parseInt(object.zero_conf_confirmed_scid, 10);
                    else if (typeof object.zero_conf_confirmed_scid === "number")
                        message.zero_conf_confirmed_scid = object.zero_conf_confirmed_scid;
                    else if (typeof object.zero_conf_confirmed_scid === "object")
                        message.zero_conf_confirmed_scid = new $util.LongBits(object.zero_conf_confirmed_scid.low >>> 0, object.zero_conf_confirmed_scid.high >>> 0).toNumber(true);
            if (object.custom_channel_data != null)
                if (object.custom_channel_data.length)
                    if (typeof object.custom_channel_data === "string")
                        $util.base64.decode(object.custom_channel_data, message.custom_channel_data = $util.newBuffer($util.base64.length(object.custom_channel_data)), 0);
                    else if (object.custom_channel_data.length >= 0)
                        message.custom_channel_data = object.custom_channel_data;
            return message;
        };

        /**
         * Creates a plain object from a ChannelCloseSummary message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelCloseSummary
         * @static
         * @param {lnrpc.ChannelCloseSummary} message ChannelCloseSummary
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelCloseSummary.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.resolutions = [];
                object.alias_scids = [];
            }
            if (options.defaults) {
                object.channel_point = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.chain_hash = "";
                object.closing_tx_hash = "";
                object.remote_pubkey = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.capacity = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.capacity = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.close_height = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.settled_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.settled_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.time_locked_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.time_locked_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.close_type = options.enums === $String ? "COOPERATIVE_CLOSE" : 0;
                object.open_initiator = options.enums === $String ? "INITIATOR_UNKNOWN" : 0;
                object.close_initiator = options.enums === $String ? "INITIATOR_UNKNOWN" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.zero_conf_confirmed_scid = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.zero_conf_confirmed_scid = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.custom_channel_data = "";
                else {
                    object.custom_channel_data = [];
                    if (options.bytes !== $Array)
                        object.custom_channel_data = $util.newBuffer(object.custom_channel_data);
                }
            }
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                object.channel_point = message.channel_point;
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.chain_hash != null && $Object.hasOwnProperty.call(message, "chain_hash"))
                object.chain_hash = message.chain_hash;
            if (message.closing_tx_hash != null && $Object.hasOwnProperty.call(message, "closing_tx_hash"))
                object.closing_tx_hash = message.closing_tx_hash;
            if (message.remote_pubkey != null && $Object.hasOwnProperty.call(message, "remote_pubkey"))
                object.remote_pubkey = message.remote_pubkey;
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.capacity = typeof message.capacity === "number" ? $BigInt(message.capacity) : $util.Long.fromBits(message.capacity.low >>> 0, message.capacity.high >>> 0, false).toBigInt();
                else if (typeof message.capacity === "number")
                    object.capacity = options.longs === $String ? $String(message.capacity) : message.capacity;
                else
                    object.capacity = options.longs === $String ? $util.Long.prototype.toString.call(message.capacity) : options.longs === $Number ? new $util.LongBits(message.capacity.low >>> 0, message.capacity.high >>> 0).toNumber() : message.capacity;
            if (message.close_height != null && $Object.hasOwnProperty.call(message, "close_height"))
                object.close_height = message.close_height;
            if (message.settled_balance != null && $Object.hasOwnProperty.call(message, "settled_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.settled_balance = typeof message.settled_balance === "number" ? $BigInt(message.settled_balance) : $util.Long.fromBits(message.settled_balance.low >>> 0, message.settled_balance.high >>> 0, false).toBigInt();
                else if (typeof message.settled_balance === "number")
                    object.settled_balance = options.longs === $String ? $String(message.settled_balance) : message.settled_balance;
                else
                    object.settled_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.settled_balance) : options.longs === $Number ? new $util.LongBits(message.settled_balance.low >>> 0, message.settled_balance.high >>> 0).toNumber() : message.settled_balance;
            if (message.time_locked_balance != null && $Object.hasOwnProperty.call(message, "time_locked_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.time_locked_balance = typeof message.time_locked_balance === "number" ? $BigInt(message.time_locked_balance) : $util.Long.fromBits(message.time_locked_balance.low >>> 0, message.time_locked_balance.high >>> 0, false).toBigInt();
                else if (typeof message.time_locked_balance === "number")
                    object.time_locked_balance = options.longs === $String ? $String(message.time_locked_balance) : message.time_locked_balance;
                else
                    object.time_locked_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.time_locked_balance) : options.longs === $Number ? new $util.LongBits(message.time_locked_balance.low >>> 0, message.time_locked_balance.high >>> 0).toNumber() : message.time_locked_balance;
            if (message.close_type != null && $Object.hasOwnProperty.call(message, "close_type"))
                object.close_type = options.enums === $String ? $root.lnrpc.ChannelCloseSummary.ClosureType[message.close_type] === $undefined ? message.close_type : $root.lnrpc.ChannelCloseSummary.ClosureType[message.close_type] : message.close_type;
            if (message.open_initiator != null && $Object.hasOwnProperty.call(message, "open_initiator"))
                object.open_initiator = options.enums === $String ? $root.lnrpc.Initiator[message.open_initiator] === $undefined ? message.open_initiator : $root.lnrpc.Initiator[message.open_initiator] : message.open_initiator;
            if (message.close_initiator != null && $Object.hasOwnProperty.call(message, "close_initiator"))
                object.close_initiator = options.enums === $String ? $root.lnrpc.Initiator[message.close_initiator] === $undefined ? message.close_initiator : $root.lnrpc.Initiator[message.close_initiator] : message.close_initiator;
            if (message.resolutions && message.resolutions.length) {
                object.resolutions = $Array(message.resolutions.length);
                for (let j = 0; j < message.resolutions.length; ++j)
                    object.resolutions[j] = $root.lnrpc.Resolution.toObject(message.resolutions[j], options, _depth + 1);
            }
            if (message.alias_scids && message.alias_scids.length) {
                object.alias_scids = $Array(message.alias_scids.length);
                for (let j = 0; j < message.alias_scids.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.alias_scids[j] = typeof message.alias_scids[j] === "number" ? $BigInt(message.alias_scids[j]) : $util.Long.fromBits(message.alias_scids[j].low >>> 0, message.alias_scids[j].high >>> 0, true).toBigInt();
                    else if (typeof message.alias_scids[j] === "number")
                        object.alias_scids[j] = options.longs === $String ? $String(message.alias_scids[j]) : message.alias_scids[j];
                    else
                        object.alias_scids[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.alias_scids[j]) : options.longs === $Number ? new $util.LongBits(message.alias_scids[j].low >>> 0, message.alias_scids[j].high >>> 0).toNumber(true) : message.alias_scids[j];
            }
            if (message.zero_conf_confirmed_scid != null && $Object.hasOwnProperty.call(message, "zero_conf_confirmed_scid"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.zero_conf_confirmed_scid = typeof message.zero_conf_confirmed_scid === "number" ? $BigInt(message.zero_conf_confirmed_scid) : $util.Long.fromBits(message.zero_conf_confirmed_scid.low >>> 0, message.zero_conf_confirmed_scid.high >>> 0, true).toBigInt();
                else if (typeof message.zero_conf_confirmed_scid === "number")
                    object.zero_conf_confirmed_scid = options.longs === $String ? $String(message.zero_conf_confirmed_scid) : message.zero_conf_confirmed_scid;
                else
                    object.zero_conf_confirmed_scid = options.longs === $String ? $util.Long.prototype.toString.call(message.zero_conf_confirmed_scid) : options.longs === $Number ? new $util.LongBits(message.zero_conf_confirmed_scid.low >>> 0, message.zero_conf_confirmed_scid.high >>> 0).toNumber(true) : message.zero_conf_confirmed_scid;
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                object.custom_channel_data = options.bytes === $String ? $util.base64.encode(message.custom_channel_data, 0, message.custom_channel_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_channel_data) : message.custom_channel_data;
            return object;
        };

        /**
         * Converts this ChannelCloseSummary to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelCloseSummary
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelCloseSummary.prototype.toJSON = function() {
            return ChannelCloseSummary.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelCloseSummary
         * @function getTypeUrl
         * @memberof lnrpc.ChannelCloseSummary
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelCloseSummary.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelCloseSummary";
        };

        /**
         * ClosureType enum.
         * @name lnrpc.ChannelCloseSummary.ClosureType
         * @enum {number}
         * @property {number} COOPERATIVE_CLOSE=0 COOPERATIVE_CLOSE value
         * @property {number} LOCAL_FORCE_CLOSE=1 LOCAL_FORCE_CLOSE value
         * @property {number} REMOTE_FORCE_CLOSE=2 REMOTE_FORCE_CLOSE value
         * @property {number} BREACH_CLOSE=3 BREACH_CLOSE value
         * @property {number} FUNDING_CANCELED=4 FUNDING_CANCELED value
         * @property {number} ABANDONED=5 ABANDONED value
         */
        ChannelCloseSummary.ClosureType = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "COOPERATIVE_CLOSE"] = 0;
            values[valuesById[1] = "LOCAL_FORCE_CLOSE"] = 1;
            values[valuesById[2] = "REMOTE_FORCE_CLOSE"] = 2;
            values[valuesById[3] = "BREACH_CLOSE"] = 3;
            values[valuesById[4] = "FUNDING_CANCELED"] = 4;
            values[valuesById[5] = "ABANDONED"] = 5;
            return values;
        })();

        return ChannelCloseSummary;
    })();

    /**
     * ResolutionType enum.
     * @name lnrpc.ResolutionType
     * @enum {number}
     * @property {number} TYPE_UNKNOWN=0 TYPE_UNKNOWN value
     * @property {number} ANCHOR=1 ANCHOR value
     * @property {number} INCOMING_HTLC=2 INCOMING_HTLC value
     * @property {number} OUTGOING_HTLC=3 OUTGOING_HTLC value
     * @property {number} COMMIT=4 COMMIT value
     */
    lnrpc.ResolutionType = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "TYPE_UNKNOWN"] = 0;
        values[valuesById[1] = "ANCHOR"] = 1;
        values[valuesById[2] = "INCOMING_HTLC"] = 2;
        values[valuesById[3] = "OUTGOING_HTLC"] = 3;
        values[valuesById[4] = "COMMIT"] = 4;
        return values;
    })();

    /**
     * ResolutionOutcome enum.
     * @name lnrpc.ResolutionOutcome
     * @enum {number}
     * @property {number} OUTCOME_UNKNOWN=0 OUTCOME_UNKNOWN value
     * @property {number} CLAIMED=1 CLAIMED value
     * @property {number} UNCLAIMED=2 UNCLAIMED value
     * @property {number} ABANDONED=3 ABANDONED value
     * @property {number} FIRST_STAGE=4 FIRST_STAGE value
     * @property {number} TIMEOUT=5 TIMEOUT value
     */
    lnrpc.ResolutionOutcome = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "OUTCOME_UNKNOWN"] = 0;
        values[valuesById[1] = "CLAIMED"] = 1;
        values[valuesById[2] = "UNCLAIMED"] = 2;
        values[valuesById[3] = "ABANDONED"] = 3;
        values[valuesById[4] = "FIRST_STAGE"] = 4;
        values[valuesById[5] = "TIMEOUT"] = 5;
        return values;
    })();

    lnrpc.Resolution = (function() {

        /**
         * Properties of a Resolution.
         * @typedef {Object} lnrpc.Resolution.$Properties
         * @property {lnrpc.ResolutionType|null} [resolution_type] Resolution resolution_type
         * @property {lnrpc.ResolutionOutcome|null} [outcome] Resolution outcome
         * @property {lnrpc.OutPoint.$Properties|null} [outpoint] Resolution outpoint
         * @property {Long|null} [amount_sat] Resolution amount_sat
         * @property {string|null} [sweep_txid] Resolution sweep_txid
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Resolution.
         * @memberof lnrpc
         * @interface IResolution
         * @augments lnrpc.Resolution.$Properties
         * @deprecated Use lnrpc.Resolution.$Properties instead.
         */

        /**
         * Shape of a Resolution.
         * @typedef {lnrpc.Resolution.$Properties} lnrpc.Resolution.$Shape
         */

        /**
         * Constructs a new Resolution.
         * @memberof lnrpc
         * @classdesc Represents a Resolution.
         * @constructor
         * @param {lnrpc.Resolution.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Resolution = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Resolution resolution_type.
         * @member {lnrpc.ResolutionType} resolution_type
         * @memberof lnrpc.Resolution
         * @instance
         */
        Resolution.prototype.resolution_type = 0;

        /**
         * Resolution outcome.
         * @member {lnrpc.ResolutionOutcome} outcome
         * @memberof lnrpc.Resolution
         * @instance
         */
        Resolution.prototype.outcome = 0;

        /**
         * Resolution outpoint.
         * @member {lnrpc.OutPoint.$Properties|null|undefined} outpoint
         * @memberof lnrpc.Resolution
         * @instance
         */
        Resolution.prototype.outpoint = null;

        /**
         * Resolution amount_sat.
         * @member {Long} amount_sat
         * @memberof lnrpc.Resolution
         * @instance
         */
        Resolution.prototype.amount_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Resolution sweep_txid.
         * @member {string} sweep_txid
         * @memberof lnrpc.Resolution
         * @instance
         */
        Resolution.prototype.sweep_txid = "";

        /**
         * Creates a new Resolution instance using the specified properties.
         * @function create
         * @memberof lnrpc.Resolution
         * @static
         * @param {lnrpc.Resolution.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Resolution} Resolution instance
         * @type {{
         *   (properties: lnrpc.Resolution.$Shape): lnrpc.Resolution & lnrpc.Resolution.$Shape;
         *   (properties?: lnrpc.Resolution.$Properties): lnrpc.Resolution;
         * }}
         */
        Resolution.create = function(properties) {
            return new Resolution(properties);
        };

        /**
         * Encodes the specified Resolution message. Does not implicitly {@link lnrpc.Resolution.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Resolution
         * @static
         * @param {lnrpc.Resolution.$Properties} message Resolution message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Resolution.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.resolution_type != null && $Object.hasOwnProperty.call(message, "resolution_type") && message.resolution_type !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.resolution_type);
            if (message.outcome != null && $Object.hasOwnProperty.call(message, "outcome") && message.outcome !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.outcome);
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                $root.lnrpc.OutPoint.encode(message.outpoint, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat") && (typeof message.amount_sat === "object" ? message.amount_sat.low || message.amount_sat.high : message.amount_sat !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.amount_sat);
            if (message.sweep_txid != null && $Object.hasOwnProperty.call(message, "sweep_txid") && message.sweep_txid !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.sweep_txid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Resolution message, length delimited. Does not implicitly {@link lnrpc.Resolution.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Resolution
         * @static
         * @param {lnrpc.Resolution.$Properties} message Resolution message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Resolution.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Resolution message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Resolution
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Resolution & lnrpc.Resolution.$Shape} Resolution
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Resolution.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Resolution(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.resolution_type = value;
                        else
                            delete message.resolution_type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.outcome = value;
                        else
                            delete message.outcome;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.outpoint = $root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.outpoint);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount_sat = value;
                        else
                            delete message.amount_sat;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.sweep_txid = value;
                        else
                            delete message.sweep_txid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Resolution message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Resolution
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Resolution & lnrpc.Resolution.$Shape} Resolution
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Resolution.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Resolution message.
         * @function verify
         * @memberof lnrpc.Resolution
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Resolution.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.resolution_type != null && $Object.hasOwnProperty.call(message, "resolution_type"))
                if (typeof message.resolution_type !== "number" || (message.resolution_type | 0) !== message.resolution_type)
                    return "resolution_type: enum value expected";
            if (message.outcome != null && $Object.hasOwnProperty.call(message, "outcome"))
                if (typeof message.outcome !== "number" || (message.outcome | 0) !== message.outcome)
                    return "outcome: enum value expected";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint")) {
                let error = $root.lnrpc.OutPoint.verify(message.outpoint, _depth + 1);
                if (error)
                    return "outpoint." + error;
            }
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat"))
                if (!$util.isInteger(message.amount_sat) && !(message.amount_sat && $util.isInteger(message.amount_sat.low) && $util.isInteger(message.amount_sat.high)))
                    return "amount_sat: integer|Long expected";
            if (message.sweep_txid != null && $Object.hasOwnProperty.call(message, "sweep_txid"))
                if (!$util.isString(message.sweep_txid))
                    return "sweep_txid: string expected";
            return null;
        };

        /**
         * Creates a Resolution message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Resolution
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Resolution} Resolution
         */
        Resolution.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Resolution)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Resolution: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Resolution();
            if (object.resolution_type !== 0 && (typeof object.resolution_type !== "string" || $root.lnrpc.ResolutionType[object.resolution_type] !== 0))
                switch (object.resolution_type) {
                case "TYPE_UNKNOWN":
                case 0:
                    message.resolution_type = 0;
                    break;
                case "ANCHOR":
                case 1:
                    message.resolution_type = 1;
                    break;
                case "INCOMING_HTLC":
                case 2:
                    message.resolution_type = 2;
                    break;
                case "OUTGOING_HTLC":
                case 3:
                    message.resolution_type = 3;
                    break;
                case "COMMIT":
                case 4:
                    message.resolution_type = 4;
                    break;
                default:
                    if (typeof object.resolution_type === "number" && (object.resolution_type | 0) === object.resolution_type)
                        message.resolution_type = object.resolution_type;
                }
            if (object.outcome !== 0 && (typeof object.outcome !== "string" || $root.lnrpc.ResolutionOutcome[object.outcome] !== 0))
                switch (object.outcome) {
                case "OUTCOME_UNKNOWN":
                case 0:
                    message.outcome = 0;
                    break;
                case "CLAIMED":
                case 1:
                    message.outcome = 1;
                    break;
                case "UNCLAIMED":
                case 2:
                    message.outcome = 2;
                    break;
                case "ABANDONED":
                case 3:
                    message.outcome = 3;
                    break;
                case "FIRST_STAGE":
                case 4:
                    message.outcome = 4;
                    break;
                case "TIMEOUT":
                case 5:
                    message.outcome = 5;
                    break;
                default:
                    if (typeof object.outcome === "number" && (object.outcome | 0) === object.outcome)
                        message.outcome = object.outcome;
                }
            if (object.outpoint != null) {
                if (!$util.isObject(object.outpoint))
                    throw $TypeError(".lnrpc.Resolution.outpoint: object expected");
                message.outpoint = $root.lnrpc.OutPoint.fromObject(object.outpoint, _depth + 1);
            }
            if (object.amount_sat != null)
                if (typeof object.amount_sat === "object" ? object.amount_sat.low || object.amount_sat.high : $Number(object.amount_sat) !== 0)
                    if ($util.Long)
                        message.amount_sat = $util.Long.fromValue(object.amount_sat, true);
                    else if (typeof object.amount_sat === "string")
                        message.amount_sat = $parseInt(object.amount_sat, 10);
                    else if (typeof object.amount_sat === "number")
                        message.amount_sat = object.amount_sat;
                    else if (typeof object.amount_sat === "object")
                        message.amount_sat = new $util.LongBits(object.amount_sat.low >>> 0, object.amount_sat.high >>> 0).toNumber(true);
            if (object.sweep_txid != null)
                if (typeof object.sweep_txid !== "string" || object.sweep_txid.length)
                    message.sweep_txid = $String(object.sweep_txid);
            return message;
        };

        /**
         * Creates a plain object from a Resolution message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Resolution
         * @static
         * @param {lnrpc.Resolution} message Resolution
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Resolution.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.resolution_type = options.enums === $String ? "TYPE_UNKNOWN" : 0;
                object.outcome = options.enums === $String ? "OUTCOME_UNKNOWN" : 0;
                object.outpoint = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amount_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.sweep_txid = "";
            }
            if (message.resolution_type != null && $Object.hasOwnProperty.call(message, "resolution_type"))
                object.resolution_type = options.enums === $String ? $root.lnrpc.ResolutionType[message.resolution_type] === $undefined ? message.resolution_type : $root.lnrpc.ResolutionType[message.resolution_type] : message.resolution_type;
            if (message.outcome != null && $Object.hasOwnProperty.call(message, "outcome"))
                object.outcome = options.enums === $String ? $root.lnrpc.ResolutionOutcome[message.outcome] === $undefined ? message.outcome : $root.lnrpc.ResolutionOutcome[message.outcome] : message.outcome;
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = $root.lnrpc.OutPoint.toObject(message.outpoint, options, _depth + 1);
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount_sat = typeof message.amount_sat === "number" ? $BigInt(message.amount_sat) : $util.Long.fromBits(message.amount_sat.low >>> 0, message.amount_sat.high >>> 0, true).toBigInt();
                else if (typeof message.amount_sat === "number")
                    object.amount_sat = options.longs === $String ? $String(message.amount_sat) : message.amount_sat;
                else
                    object.amount_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.amount_sat) : options.longs === $Number ? new $util.LongBits(message.amount_sat.low >>> 0, message.amount_sat.high >>> 0).toNumber(true) : message.amount_sat;
            if (message.sweep_txid != null && $Object.hasOwnProperty.call(message, "sweep_txid"))
                object.sweep_txid = message.sweep_txid;
            return object;
        };

        /**
         * Converts this Resolution to JSON.
         * @function toJSON
         * @memberof lnrpc.Resolution
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Resolution.prototype.toJSON = function() {
            return Resolution.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Resolution
         * @function getTypeUrl
         * @memberof lnrpc.Resolution
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Resolution.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Resolution";
        };

        return Resolution;
    })();

    lnrpc.ClosedChannelsRequest = (function() {

        /**
         * Properties of a ClosedChannelsRequest.
         * @typedef {Object} lnrpc.ClosedChannelsRequest.$Properties
         * @property {boolean|null} [cooperative] ClosedChannelsRequest cooperative
         * @property {boolean|null} [local_force] ClosedChannelsRequest local_force
         * @property {boolean|null} [remote_force] ClosedChannelsRequest remote_force
         * @property {boolean|null} [breach] ClosedChannelsRequest breach
         * @property {boolean|null} [funding_canceled] ClosedChannelsRequest funding_canceled
         * @property {boolean|null} [abandoned] ClosedChannelsRequest abandoned
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ClosedChannelsRequest.
         * @memberof lnrpc
         * @interface IClosedChannelsRequest
         * @augments lnrpc.ClosedChannelsRequest.$Properties
         * @deprecated Use lnrpc.ClosedChannelsRequest.$Properties instead.
         */

        /**
         * Shape of a ClosedChannelsRequest.
         * @typedef {lnrpc.ClosedChannelsRequest.$Properties} lnrpc.ClosedChannelsRequest.$Shape
         */

        /**
         * Constructs a new ClosedChannelsRequest.
         * @memberof lnrpc
         * @classdesc Represents a ClosedChannelsRequest.
         * @constructor
         * @param {lnrpc.ClosedChannelsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ClosedChannelsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ClosedChannelsRequest cooperative.
         * @member {boolean} cooperative
         * @memberof lnrpc.ClosedChannelsRequest
         * @instance
         */
        ClosedChannelsRequest.prototype.cooperative = false;

        /**
         * ClosedChannelsRequest local_force.
         * @member {boolean} local_force
         * @memberof lnrpc.ClosedChannelsRequest
         * @instance
         */
        ClosedChannelsRequest.prototype.local_force = false;

        /**
         * ClosedChannelsRequest remote_force.
         * @member {boolean} remote_force
         * @memberof lnrpc.ClosedChannelsRequest
         * @instance
         */
        ClosedChannelsRequest.prototype.remote_force = false;

        /**
         * ClosedChannelsRequest breach.
         * @member {boolean} breach
         * @memberof lnrpc.ClosedChannelsRequest
         * @instance
         */
        ClosedChannelsRequest.prototype.breach = false;

        /**
         * ClosedChannelsRequest funding_canceled.
         * @member {boolean} funding_canceled
         * @memberof lnrpc.ClosedChannelsRequest
         * @instance
         */
        ClosedChannelsRequest.prototype.funding_canceled = false;

        /**
         * ClosedChannelsRequest abandoned.
         * @member {boolean} abandoned
         * @memberof lnrpc.ClosedChannelsRequest
         * @instance
         */
        ClosedChannelsRequest.prototype.abandoned = false;

        /**
         * Creates a new ClosedChannelsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ClosedChannelsRequest
         * @static
         * @param {lnrpc.ClosedChannelsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ClosedChannelsRequest} ClosedChannelsRequest instance
         * @type {{
         *   (properties: lnrpc.ClosedChannelsRequest.$Shape): lnrpc.ClosedChannelsRequest & lnrpc.ClosedChannelsRequest.$Shape;
         *   (properties?: lnrpc.ClosedChannelsRequest.$Properties): lnrpc.ClosedChannelsRequest;
         * }}
         */
        ClosedChannelsRequest.create = function(properties) {
            return new ClosedChannelsRequest(properties);
        };

        /**
         * Encodes the specified ClosedChannelsRequest message. Does not implicitly {@link lnrpc.ClosedChannelsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ClosedChannelsRequest
         * @static
         * @param {lnrpc.ClosedChannelsRequest.$Properties} message ClosedChannelsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClosedChannelsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.cooperative != null && $Object.hasOwnProperty.call(message, "cooperative") && message.cooperative !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.cooperative);
            if (message.local_force != null && $Object.hasOwnProperty.call(message, "local_force") && message.local_force !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.local_force);
            if (message.remote_force != null && $Object.hasOwnProperty.call(message, "remote_force") && message.remote_force !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.remote_force);
            if (message.breach != null && $Object.hasOwnProperty.call(message, "breach") && message.breach !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.breach);
            if (message.funding_canceled != null && $Object.hasOwnProperty.call(message, "funding_canceled") && message.funding_canceled !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.funding_canceled);
            if (message.abandoned != null && $Object.hasOwnProperty.call(message, "abandoned") && message.abandoned !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.abandoned);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ClosedChannelsRequest message, length delimited. Does not implicitly {@link lnrpc.ClosedChannelsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ClosedChannelsRequest
         * @static
         * @param {lnrpc.ClosedChannelsRequest.$Properties} message ClosedChannelsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClosedChannelsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ClosedChannelsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ClosedChannelsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ClosedChannelsRequest & lnrpc.ClosedChannelsRequest.$Shape} ClosedChannelsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClosedChannelsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ClosedChannelsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cooperative = value;
                        else
                            delete message.cooperative;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.local_force = value;
                        else
                            delete message.local_force;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.remote_force = value;
                        else
                            delete message.remote_force;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.breach = value;
                        else
                            delete message.breach;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.funding_canceled = value;
                        else
                            delete message.funding_canceled;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.abandoned = value;
                        else
                            delete message.abandoned;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ClosedChannelsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ClosedChannelsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ClosedChannelsRequest & lnrpc.ClosedChannelsRequest.$Shape} ClosedChannelsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClosedChannelsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ClosedChannelsRequest message.
         * @function verify
         * @memberof lnrpc.ClosedChannelsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ClosedChannelsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.cooperative != null && $Object.hasOwnProperty.call(message, "cooperative"))
                if (typeof message.cooperative !== "boolean")
                    return "cooperative: boolean expected";
            if (message.local_force != null && $Object.hasOwnProperty.call(message, "local_force"))
                if (typeof message.local_force !== "boolean")
                    return "local_force: boolean expected";
            if (message.remote_force != null && $Object.hasOwnProperty.call(message, "remote_force"))
                if (typeof message.remote_force !== "boolean")
                    return "remote_force: boolean expected";
            if (message.breach != null && $Object.hasOwnProperty.call(message, "breach"))
                if (typeof message.breach !== "boolean")
                    return "breach: boolean expected";
            if (message.funding_canceled != null && $Object.hasOwnProperty.call(message, "funding_canceled"))
                if (typeof message.funding_canceled !== "boolean")
                    return "funding_canceled: boolean expected";
            if (message.abandoned != null && $Object.hasOwnProperty.call(message, "abandoned"))
                if (typeof message.abandoned !== "boolean")
                    return "abandoned: boolean expected";
            return null;
        };

        /**
         * Creates a ClosedChannelsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ClosedChannelsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ClosedChannelsRequest} ClosedChannelsRequest
         */
        ClosedChannelsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ClosedChannelsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ClosedChannelsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ClosedChannelsRequest();
            if (object.cooperative != null)
                if (object.cooperative)
                    message.cooperative = $Boolean(object.cooperative);
            if (object.local_force != null)
                if (object.local_force)
                    message.local_force = $Boolean(object.local_force);
            if (object.remote_force != null)
                if (object.remote_force)
                    message.remote_force = $Boolean(object.remote_force);
            if (object.breach != null)
                if (object.breach)
                    message.breach = $Boolean(object.breach);
            if (object.funding_canceled != null)
                if (object.funding_canceled)
                    message.funding_canceled = $Boolean(object.funding_canceled);
            if (object.abandoned != null)
                if (object.abandoned)
                    message.abandoned = $Boolean(object.abandoned);
            return message;
        };

        /**
         * Creates a plain object from a ClosedChannelsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ClosedChannelsRequest
         * @static
         * @param {lnrpc.ClosedChannelsRequest} message ClosedChannelsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ClosedChannelsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.cooperative = false;
                object.local_force = false;
                object.remote_force = false;
                object.breach = false;
                object.funding_canceled = false;
                object.abandoned = false;
            }
            if (message.cooperative != null && $Object.hasOwnProperty.call(message, "cooperative"))
                object.cooperative = message.cooperative;
            if (message.local_force != null && $Object.hasOwnProperty.call(message, "local_force"))
                object.local_force = message.local_force;
            if (message.remote_force != null && $Object.hasOwnProperty.call(message, "remote_force"))
                object.remote_force = message.remote_force;
            if (message.breach != null && $Object.hasOwnProperty.call(message, "breach"))
                object.breach = message.breach;
            if (message.funding_canceled != null && $Object.hasOwnProperty.call(message, "funding_canceled"))
                object.funding_canceled = message.funding_canceled;
            if (message.abandoned != null && $Object.hasOwnProperty.call(message, "abandoned"))
                object.abandoned = message.abandoned;
            return object;
        };

        /**
         * Converts this ClosedChannelsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ClosedChannelsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ClosedChannelsRequest.prototype.toJSON = function() {
            return ClosedChannelsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ClosedChannelsRequest
         * @function getTypeUrl
         * @memberof lnrpc.ClosedChannelsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ClosedChannelsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ClosedChannelsRequest";
        };

        return ClosedChannelsRequest;
    })();

    lnrpc.ClosedChannelsResponse = (function() {

        /**
         * Properties of a ClosedChannelsResponse.
         * @typedef {Object} lnrpc.ClosedChannelsResponse.$Properties
         * @property {Array.<lnrpc.ChannelCloseSummary.$Properties>|null} [channels] ClosedChannelsResponse channels
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ClosedChannelsResponse.
         * @memberof lnrpc
         * @interface IClosedChannelsResponse
         * @augments lnrpc.ClosedChannelsResponse.$Properties
         * @deprecated Use lnrpc.ClosedChannelsResponse.$Properties instead.
         */

        /**
         * Shape of a ClosedChannelsResponse.
         * @typedef {lnrpc.ClosedChannelsResponse.$Properties} lnrpc.ClosedChannelsResponse.$Shape
         */

        /**
         * Constructs a new ClosedChannelsResponse.
         * @memberof lnrpc
         * @classdesc Represents a ClosedChannelsResponse.
         * @constructor
         * @param {lnrpc.ClosedChannelsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ClosedChannelsResponse = function (properties) {
            this.channels = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ClosedChannelsResponse channels.
         * @member {Array.<lnrpc.ChannelCloseSummary.$Properties>} channels
         * @memberof lnrpc.ClosedChannelsResponse
         * @instance
         */
        ClosedChannelsResponse.prototype.channels = $util.emptyArray;

        /**
         * Creates a new ClosedChannelsResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ClosedChannelsResponse
         * @static
         * @param {lnrpc.ClosedChannelsResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ClosedChannelsResponse} ClosedChannelsResponse instance
         * @type {{
         *   (properties: lnrpc.ClosedChannelsResponse.$Shape): lnrpc.ClosedChannelsResponse & lnrpc.ClosedChannelsResponse.$Shape;
         *   (properties?: lnrpc.ClosedChannelsResponse.$Properties): lnrpc.ClosedChannelsResponse;
         * }}
         */
        ClosedChannelsResponse.create = function(properties) {
            return new ClosedChannelsResponse(properties);
        };

        /**
         * Encodes the specified ClosedChannelsResponse message. Does not implicitly {@link lnrpc.ClosedChannelsResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ClosedChannelsResponse
         * @static
         * @param {lnrpc.ClosedChannelsResponse.$Properties} message ClosedChannelsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClosedChannelsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channels != null && message.channels.length)
                for (let i = 0; i < message.channels.length; ++i)
                    $root.lnrpc.ChannelCloseSummary.encode(message.channels[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ClosedChannelsResponse message, length delimited. Does not implicitly {@link lnrpc.ClosedChannelsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ClosedChannelsResponse
         * @static
         * @param {lnrpc.ClosedChannelsResponse.$Properties} message ClosedChannelsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClosedChannelsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ClosedChannelsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ClosedChannelsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ClosedChannelsResponse & lnrpc.ClosedChannelsResponse.$Shape} ClosedChannelsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClosedChannelsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ClosedChannelsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.channels && message.channels.length))
                            message.channels = [];
                        message.channels.push($root.lnrpc.ChannelCloseSummary.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ClosedChannelsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ClosedChannelsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ClosedChannelsResponse & lnrpc.ClosedChannelsResponse.$Shape} ClosedChannelsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClosedChannelsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ClosedChannelsResponse message.
         * @function verify
         * @memberof lnrpc.ClosedChannelsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ClosedChannelsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channels != null && $Object.hasOwnProperty.call(message, "channels")) {
                if (!$Array.isArray(message.channels))
                    return "channels: array expected";
                for (let i = 0; i < message.channels.length; ++i) {
                    let error = $root.lnrpc.ChannelCloseSummary.verify(message.channels[i], _depth + 1);
                    if (error)
                        return "channels." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ClosedChannelsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ClosedChannelsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ClosedChannelsResponse} ClosedChannelsResponse
         */
        ClosedChannelsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ClosedChannelsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ClosedChannelsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ClosedChannelsResponse();
            if (object.channels) {
                if (!$Array.isArray(object.channels))
                    throw $TypeError(".lnrpc.ClosedChannelsResponse.channels: array expected");
                message.channels = $Array(object.channels.length);
                for (let i = 0; i < object.channels.length; ++i) {
                    if (!$util.isObject(object.channels[i]))
                        throw $TypeError(".lnrpc.ClosedChannelsResponse.channels: object expected");
                    message.channels[i] = $root.lnrpc.ChannelCloseSummary.fromObject(object.channels[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ClosedChannelsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ClosedChannelsResponse
         * @static
         * @param {lnrpc.ClosedChannelsResponse} message ClosedChannelsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ClosedChannelsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.channels = [];
            if (message.channels && message.channels.length) {
                object.channels = $Array(message.channels.length);
                for (let j = 0; j < message.channels.length; ++j)
                    object.channels[j] = $root.lnrpc.ChannelCloseSummary.toObject(message.channels[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ClosedChannelsResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ClosedChannelsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ClosedChannelsResponse.prototype.toJSON = function() {
            return ClosedChannelsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ClosedChannelsResponse
         * @function getTypeUrl
         * @memberof lnrpc.ClosedChannelsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ClosedChannelsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ClosedChannelsResponse";
        };

        return ClosedChannelsResponse;
    })();

    lnrpc.Peer = (function() {

        /**
         * Properties of a Peer.
         * @typedef {Object} lnrpc.Peer.$Properties
         * @property {string|null} [pub_key] Peer pub_key
         * @property {string|null} [address] Peer address
         * @property {Long|null} [bytes_sent] Peer bytes_sent
         * @property {Long|null} [bytes_recv] Peer bytes_recv
         * @property {Long|null} [sat_sent] Peer sat_sent
         * @property {Long|null} [sat_recv] Peer sat_recv
         * @property {boolean|null} [inbound] Peer inbound
         * @property {Long|null} [ping_time] Peer ping_time
         * @property {lnrpc.Peer.SyncType|null} [sync_type] Peer sync_type
         * @property {Object.<string,lnrpc.Feature.$Properties>|null} [features] Peer features
         * @property {Array.<lnrpc.TimestampedError.$Properties>|null} [errors] Peer errors
         * @property {number|null} [flap_count] Peer flap_count
         * @property {Long|null} [last_flap_ns] Peer last_flap_ns
         * @property {Uint8Array|null} [last_ping_payload] Peer last_ping_payload
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Peer.
         * @memberof lnrpc
         * @interface IPeer
         * @augments lnrpc.Peer.$Properties
         * @deprecated Use lnrpc.Peer.$Properties instead.
         */

        /**
         * Shape of a Peer.
         * @typedef {lnrpc.Peer.$Properties} lnrpc.Peer.$Shape
         */

        /**
         * Constructs a new Peer.
         * @memberof lnrpc
         * @classdesc Represents a Peer.
         * @constructor
         * @param {lnrpc.Peer.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Peer = function (properties) {
            this.features = {};
            this.errors = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Peer pub_key.
         * @member {string} pub_key
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.pub_key = "";

        /**
         * Peer address.
         * @member {string} address
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.address = "";

        /**
         * Peer bytes_sent.
         * @member {Long} bytes_sent
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.bytes_sent = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Peer bytes_recv.
         * @member {Long} bytes_recv
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.bytes_recv = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Peer sat_sent.
         * @member {Long} sat_sent
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.sat_sent = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Peer sat_recv.
         * @member {Long} sat_recv
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.sat_recv = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Peer inbound.
         * @member {boolean} inbound
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.inbound = false;

        /**
         * Peer ping_time.
         * @member {Long} ping_time
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.ping_time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Peer sync_type.
         * @member {lnrpc.Peer.SyncType} sync_type
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.sync_type = 0;

        /**
         * Peer features.
         * @member {Object.<string,lnrpc.Feature.$Properties>} features
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.features = $util.emptyObject;

        /**
         * Peer errors.
         * @member {Array.<lnrpc.TimestampedError.$Properties>} errors
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.errors = $util.emptyArray;

        /**
         * Peer flap_count.
         * @member {number} flap_count
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.flap_count = 0;

        /**
         * Peer last_flap_ns.
         * @member {Long} last_flap_ns
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.last_flap_ns = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Peer last_ping_payload.
         * @member {Uint8Array} last_ping_payload
         * @memberof lnrpc.Peer
         * @instance
         */
        Peer.prototype.last_ping_payload = $util.newBuffer([]);

        /**
         * Creates a new Peer instance using the specified properties.
         * @function create
         * @memberof lnrpc.Peer
         * @static
         * @param {lnrpc.Peer.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Peer} Peer instance
         * @type {{
         *   (properties: lnrpc.Peer.$Shape): lnrpc.Peer & lnrpc.Peer.$Shape;
         *   (properties?: lnrpc.Peer.$Properties): lnrpc.Peer;
         * }}
         */
        Peer.create = function(properties) {
            return new Peer(properties);
        };

        /**
         * Encodes the specified Peer message. Does not implicitly {@link lnrpc.Peer.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Peer
         * @static
         * @param {lnrpc.Peer.$Properties} message Peer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Peer.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key") && message.pub_key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pub_key);
            if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.address);
            if (message.bytes_sent != null && $Object.hasOwnProperty.call(message, "bytes_sent") && (typeof message.bytes_sent === "object" ? message.bytes_sent.low || message.bytes_sent.high : message.bytes_sent !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.bytes_sent);
            if (message.bytes_recv != null && $Object.hasOwnProperty.call(message, "bytes_recv") && (typeof message.bytes_recv === "object" ? message.bytes_recv.low || message.bytes_recv.high : message.bytes_recv !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.bytes_recv);
            if (message.sat_sent != null && $Object.hasOwnProperty.call(message, "sat_sent") && (typeof message.sat_sent === "object" ? message.sat_sent.low || message.sat_sent.high : message.sat_sent !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.sat_sent);
            if (message.sat_recv != null && $Object.hasOwnProperty.call(message, "sat_recv") && (typeof message.sat_recv === "object" ? message.sat_recv.low || message.sat_recv.high : message.sat_recv !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.sat_recv);
            if (message.inbound != null && $Object.hasOwnProperty.call(message, "inbound") && message.inbound !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.inbound);
            if (message.ping_time != null && $Object.hasOwnProperty.call(message, "ping_time") && (typeof message.ping_time === "object" ? message.ping_time.low || message.ping_time.high : message.ping_time !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.ping_time);
            if (message.sync_type != null && $Object.hasOwnProperty.call(message, "sync_type") && message.sync_type !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.sync_type);
            if (message.features != null && $Object.hasOwnProperty.call(message, "features"))
                for (let keys = $Object.keys(message.features), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 11, wireType 2 =*/90).fork().uint32(/* id 1, wireType 0 =*/8).uint32(keys[i]);
                    $root.lnrpc.Feature.encode(message.features[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.errors != null && message.errors.length)
                for (let i = 0; i < message.errors.length; ++i)
                    $root.lnrpc.TimestampedError.encode(message.errors[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.flap_count != null && $Object.hasOwnProperty.call(message, "flap_count") && message.flap_count !== 0)
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.flap_count);
            if (message.last_flap_ns != null && $Object.hasOwnProperty.call(message, "last_flap_ns") && (typeof message.last_flap_ns === "object" ? message.last_flap_ns.low || message.last_flap_ns.high : message.last_flap_ns !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.last_flap_ns);
            if (message.last_ping_payload != null && $Object.hasOwnProperty.call(message, "last_ping_payload") && message.last_ping_payload.length)
                writer.uint32(/* id 15, wireType 2 =*/122).bytes(message.last_ping_payload);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Peer message, length delimited. Does not implicitly {@link lnrpc.Peer.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Peer
         * @static
         * @param {lnrpc.Peer.$Properties} message Peer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Peer.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Peer message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Peer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Peer & lnrpc.Peer.$Shape} Peer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Peer.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Peer(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pub_key = value;
                        else
                            delete message.pub_key;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.address = value;
                        else
                            delete message.address;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.bytes_sent = value;
                        else
                            delete message.bytes_sent;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.bytes_recv = value;
                        else
                            delete message.bytes_recv;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_sent = value;
                        else
                            delete message.sat_sent;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_recv = value;
                        else
                            delete message.sat_recv;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.inbound = value;
                        else
                            delete message.inbound;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.ping_time = value;
                        else
                            delete message.ping_time;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.sync_type = value;
                        else
                            delete message.sync_type;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (message.features === $util.emptyObject)
                            message.features = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint32();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.Feature.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.features[key] = value || new $root.lnrpc.Feature();
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.errors && message.errors.length))
                            message.errors = [];
                        message.errors.push($root.lnrpc.TimestampedError.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.flap_count = value;
                        else
                            delete message.flap_count;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.last_flap_ns = value;
                        else
                            delete message.last_flap_ns;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.last_ping_payload = value;
                        else
                            delete message.last_ping_payload;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Peer message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Peer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Peer & lnrpc.Peer.$Shape} Peer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Peer.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Peer message.
         * @function verify
         * @memberof lnrpc.Peer
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Peer.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                if (!$util.isString(message.pub_key))
                    return "pub_key: string expected";
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.bytes_sent != null && $Object.hasOwnProperty.call(message, "bytes_sent"))
                if (!$util.isInteger(message.bytes_sent) && !(message.bytes_sent && $util.isInteger(message.bytes_sent.low) && $util.isInteger(message.bytes_sent.high)))
                    return "bytes_sent: integer|Long expected";
            if (message.bytes_recv != null && $Object.hasOwnProperty.call(message, "bytes_recv"))
                if (!$util.isInteger(message.bytes_recv) && !(message.bytes_recv && $util.isInteger(message.bytes_recv.low) && $util.isInteger(message.bytes_recv.high)))
                    return "bytes_recv: integer|Long expected";
            if (message.sat_sent != null && $Object.hasOwnProperty.call(message, "sat_sent"))
                if (!$util.isInteger(message.sat_sent) && !(message.sat_sent && $util.isInteger(message.sat_sent.low) && $util.isInteger(message.sat_sent.high)))
                    return "sat_sent: integer|Long expected";
            if (message.sat_recv != null && $Object.hasOwnProperty.call(message, "sat_recv"))
                if (!$util.isInteger(message.sat_recv) && !(message.sat_recv && $util.isInteger(message.sat_recv.low) && $util.isInteger(message.sat_recv.high)))
                    return "sat_recv: integer|Long expected";
            if (message.inbound != null && $Object.hasOwnProperty.call(message, "inbound"))
                if (typeof message.inbound !== "boolean")
                    return "inbound: boolean expected";
            if (message.ping_time != null && $Object.hasOwnProperty.call(message, "ping_time"))
                if (!$util.isInteger(message.ping_time) && !(message.ping_time && $util.isInteger(message.ping_time.low) && $util.isInteger(message.ping_time.high)))
                    return "ping_time: integer|Long expected";
            if (message.sync_type != null && $Object.hasOwnProperty.call(message, "sync_type"))
                if (typeof message.sync_type !== "number" || (message.sync_type | 0) !== message.sync_type)
                    return "sync_type: enum value expected";
            if (message.features != null && $Object.hasOwnProperty.call(message, "features")) {
                if (!$util.isObject(message.features))
                    return "features: object expected";
                let key = $Object.keys(message.features);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "features: integer key{k:uint32} expected";
                    {
                        let error = $root.lnrpc.Feature.verify(message.features[key[i]], _depth + 1);
                        if (error)
                            return "features." + error;
                    }
                }
            }
            if (message.errors != null && $Object.hasOwnProperty.call(message, "errors")) {
                if (!$Array.isArray(message.errors))
                    return "errors: array expected";
                for (let i = 0; i < message.errors.length; ++i) {
                    let error = $root.lnrpc.TimestampedError.verify(message.errors[i], _depth + 1);
                    if (error)
                        return "errors." + error;
                }
            }
            if (message.flap_count != null && $Object.hasOwnProperty.call(message, "flap_count"))
                if (!$util.isInteger(message.flap_count))
                    return "flap_count: integer expected";
            if (message.last_flap_ns != null && $Object.hasOwnProperty.call(message, "last_flap_ns"))
                if (!$util.isInteger(message.last_flap_ns) && !(message.last_flap_ns && $util.isInteger(message.last_flap_ns.low) && $util.isInteger(message.last_flap_ns.high)))
                    return "last_flap_ns: integer|Long expected";
            if (message.last_ping_payload != null && $Object.hasOwnProperty.call(message, "last_ping_payload"))
                if (!(message.last_ping_payload && typeof message.last_ping_payload.length === "number" || $util.isString(message.last_ping_payload)))
                    return "last_ping_payload: buffer expected";
            return null;
        };

        /**
         * Creates a Peer message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Peer
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Peer} Peer
         */
        Peer.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Peer)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Peer: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Peer();
            if (object.pub_key != null)
                if (typeof object.pub_key !== "string" || object.pub_key.length)
                    message.pub_key = $String(object.pub_key);
            if (object.address != null)
                if (typeof object.address !== "string" || object.address.length)
                    message.address = $String(object.address);
            if (object.bytes_sent != null)
                if (typeof object.bytes_sent === "object" ? object.bytes_sent.low || object.bytes_sent.high : $Number(object.bytes_sent) !== 0)
                    if ($util.Long)
                        message.bytes_sent = $util.Long.fromValue(object.bytes_sent, true);
                    else if (typeof object.bytes_sent === "string")
                        message.bytes_sent = $parseInt(object.bytes_sent, 10);
                    else if (typeof object.bytes_sent === "number")
                        message.bytes_sent = object.bytes_sent;
                    else if (typeof object.bytes_sent === "object")
                        message.bytes_sent = new $util.LongBits(object.bytes_sent.low >>> 0, object.bytes_sent.high >>> 0).toNumber(true);
            if (object.bytes_recv != null)
                if (typeof object.bytes_recv === "object" ? object.bytes_recv.low || object.bytes_recv.high : $Number(object.bytes_recv) !== 0)
                    if ($util.Long)
                        message.bytes_recv = $util.Long.fromValue(object.bytes_recv, true);
                    else if (typeof object.bytes_recv === "string")
                        message.bytes_recv = $parseInt(object.bytes_recv, 10);
                    else if (typeof object.bytes_recv === "number")
                        message.bytes_recv = object.bytes_recv;
                    else if (typeof object.bytes_recv === "object")
                        message.bytes_recv = new $util.LongBits(object.bytes_recv.low >>> 0, object.bytes_recv.high >>> 0).toNumber(true);
            if (object.sat_sent != null)
                if (typeof object.sat_sent === "object" ? object.sat_sent.low || object.sat_sent.high : $Number(object.sat_sent) !== 0)
                    if ($util.Long)
                        message.sat_sent = $util.Long.fromValue(object.sat_sent, false);
                    else if (typeof object.sat_sent === "string")
                        message.sat_sent = $parseInt(object.sat_sent, 10);
                    else if (typeof object.sat_sent === "number")
                        message.sat_sent = object.sat_sent;
                    else if (typeof object.sat_sent === "object")
                        message.sat_sent = new $util.LongBits(object.sat_sent.low >>> 0, object.sat_sent.high >>> 0).toNumber();
            if (object.sat_recv != null)
                if (typeof object.sat_recv === "object" ? object.sat_recv.low || object.sat_recv.high : $Number(object.sat_recv) !== 0)
                    if ($util.Long)
                        message.sat_recv = $util.Long.fromValue(object.sat_recv, false);
                    else if (typeof object.sat_recv === "string")
                        message.sat_recv = $parseInt(object.sat_recv, 10);
                    else if (typeof object.sat_recv === "number")
                        message.sat_recv = object.sat_recv;
                    else if (typeof object.sat_recv === "object")
                        message.sat_recv = new $util.LongBits(object.sat_recv.low >>> 0, object.sat_recv.high >>> 0).toNumber();
            if (object.inbound != null)
                if (object.inbound)
                    message.inbound = $Boolean(object.inbound);
            if (object.ping_time != null)
                if (typeof object.ping_time === "object" ? object.ping_time.low || object.ping_time.high : $Number(object.ping_time) !== 0)
                    if ($util.Long)
                        message.ping_time = $util.Long.fromValue(object.ping_time, false);
                    else if (typeof object.ping_time === "string")
                        message.ping_time = $parseInt(object.ping_time, 10);
                    else if (typeof object.ping_time === "number")
                        message.ping_time = object.ping_time;
                    else if (typeof object.ping_time === "object")
                        message.ping_time = new $util.LongBits(object.ping_time.low >>> 0, object.ping_time.high >>> 0).toNumber();
            if (object.sync_type !== 0 && (typeof object.sync_type !== "string" || $root.lnrpc.Peer.SyncType[object.sync_type] !== 0))
                switch (object.sync_type) {
                case "UNKNOWN_SYNC":
                case 0:
                    message.sync_type = 0;
                    break;
                case "ACTIVE_SYNC":
                case 1:
                    message.sync_type = 1;
                    break;
                case "PASSIVE_SYNC":
                case 2:
                    message.sync_type = 2;
                    break;
                case "PINNED_SYNC":
                case 3:
                    message.sync_type = 3;
                    break;
                default:
                    if (typeof object.sync_type === "number" && (object.sync_type | 0) === object.sync_type)
                        message.sync_type = object.sync_type;
                }
            if (object.features) {
                if (!$util.isObject(object.features))
                    throw $TypeError(".lnrpc.Peer.features: object expected");
                message.features = {};
                for (let keys = $Object.keys(object.features), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.features, keys[i]);
                    if (!$util.isObject(object.features[keys[i]]))
                        throw $TypeError(".lnrpc.Peer.features: object expected");
                    message.features[keys[i]] = $root.lnrpc.Feature.fromObject(object.features[keys[i]], _depth + 1);
                }
            }
            if (object.errors) {
                if (!$Array.isArray(object.errors))
                    throw $TypeError(".lnrpc.Peer.errors: array expected");
                message.errors = $Array(object.errors.length);
                for (let i = 0; i < object.errors.length; ++i) {
                    if (!$util.isObject(object.errors[i]))
                        throw $TypeError(".lnrpc.Peer.errors: object expected");
                    message.errors[i] = $root.lnrpc.TimestampedError.fromObject(object.errors[i], _depth + 1);
                }
            }
            if (object.flap_count != null)
                if ($Number(object.flap_count) !== 0)
                    message.flap_count = object.flap_count | 0;
            if (object.last_flap_ns != null)
                if (typeof object.last_flap_ns === "object" ? object.last_flap_ns.low || object.last_flap_ns.high : $Number(object.last_flap_ns) !== 0)
                    if ($util.Long)
                        message.last_flap_ns = $util.Long.fromValue(object.last_flap_ns, false);
                    else if (typeof object.last_flap_ns === "string")
                        message.last_flap_ns = $parseInt(object.last_flap_ns, 10);
                    else if (typeof object.last_flap_ns === "number")
                        message.last_flap_ns = object.last_flap_ns;
                    else if (typeof object.last_flap_ns === "object")
                        message.last_flap_ns = new $util.LongBits(object.last_flap_ns.low >>> 0, object.last_flap_ns.high >>> 0).toNumber();
            if (object.last_ping_payload != null)
                if (object.last_ping_payload.length)
                    if (typeof object.last_ping_payload === "string")
                        $util.base64.decode(object.last_ping_payload, message.last_ping_payload = $util.newBuffer($util.base64.length(object.last_ping_payload)), 0);
                    else if (object.last_ping_payload.length >= 0)
                        message.last_ping_payload = object.last_ping_payload;
            return message;
        };

        /**
         * Creates a plain object from a Peer message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Peer
         * @static
         * @param {lnrpc.Peer} message Peer
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Peer.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.errors = [];
            if (options.objects || options.defaults)
                object.features = {};
            if (options.defaults) {
                object.pub_key = "";
                object.address = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.bytes_sent = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.bytes_sent = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.bytes_recv = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.bytes_recv = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sat_sent = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_sent = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sat_recv = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_recv = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.inbound = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.ping_time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.ping_time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.sync_type = options.enums === $String ? "UNKNOWN_SYNC" : 0;
                object.flap_count = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.last_flap_ns = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.last_flap_ns = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.last_ping_payload = "";
                else {
                    object.last_ping_payload = [];
                    if (options.bytes !== $Array)
                        object.last_ping_payload = $util.newBuffer(object.last_ping_payload);
                }
            }
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                object.pub_key = message.pub_key;
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                object.address = message.address;
            if (message.bytes_sent != null && $Object.hasOwnProperty.call(message, "bytes_sent"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.bytes_sent = typeof message.bytes_sent === "number" ? $BigInt(message.bytes_sent) : $util.Long.fromBits(message.bytes_sent.low >>> 0, message.bytes_sent.high >>> 0, true).toBigInt();
                else if (typeof message.bytes_sent === "number")
                    object.bytes_sent = options.longs === $String ? $String(message.bytes_sent) : message.bytes_sent;
                else
                    object.bytes_sent = options.longs === $String ? $util.Long.prototype.toString.call(message.bytes_sent) : options.longs === $Number ? new $util.LongBits(message.bytes_sent.low >>> 0, message.bytes_sent.high >>> 0).toNumber(true) : message.bytes_sent;
            if (message.bytes_recv != null && $Object.hasOwnProperty.call(message, "bytes_recv"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.bytes_recv = typeof message.bytes_recv === "number" ? $BigInt(message.bytes_recv) : $util.Long.fromBits(message.bytes_recv.low >>> 0, message.bytes_recv.high >>> 0, true).toBigInt();
                else if (typeof message.bytes_recv === "number")
                    object.bytes_recv = options.longs === $String ? $String(message.bytes_recv) : message.bytes_recv;
                else
                    object.bytes_recv = options.longs === $String ? $util.Long.prototype.toString.call(message.bytes_recv) : options.longs === $Number ? new $util.LongBits(message.bytes_recv.low >>> 0, message.bytes_recv.high >>> 0).toNumber(true) : message.bytes_recv;
            if (message.sat_sent != null && $Object.hasOwnProperty.call(message, "sat_sent"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_sent = typeof message.sat_sent === "number" ? $BigInt(message.sat_sent) : $util.Long.fromBits(message.sat_sent.low >>> 0, message.sat_sent.high >>> 0, false).toBigInt();
                else if (typeof message.sat_sent === "number")
                    object.sat_sent = options.longs === $String ? $String(message.sat_sent) : message.sat_sent;
                else
                    object.sat_sent = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_sent) : options.longs === $Number ? new $util.LongBits(message.sat_sent.low >>> 0, message.sat_sent.high >>> 0).toNumber() : message.sat_sent;
            if (message.sat_recv != null && $Object.hasOwnProperty.call(message, "sat_recv"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_recv = typeof message.sat_recv === "number" ? $BigInt(message.sat_recv) : $util.Long.fromBits(message.sat_recv.low >>> 0, message.sat_recv.high >>> 0, false).toBigInt();
                else if (typeof message.sat_recv === "number")
                    object.sat_recv = options.longs === $String ? $String(message.sat_recv) : message.sat_recv;
                else
                    object.sat_recv = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_recv) : options.longs === $Number ? new $util.LongBits(message.sat_recv.low >>> 0, message.sat_recv.high >>> 0).toNumber() : message.sat_recv;
            if (message.inbound != null && $Object.hasOwnProperty.call(message, "inbound"))
                object.inbound = message.inbound;
            if (message.ping_time != null && $Object.hasOwnProperty.call(message, "ping_time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.ping_time = typeof message.ping_time === "number" ? $BigInt(message.ping_time) : $util.Long.fromBits(message.ping_time.low >>> 0, message.ping_time.high >>> 0, false).toBigInt();
                else if (typeof message.ping_time === "number")
                    object.ping_time = options.longs === $String ? $String(message.ping_time) : message.ping_time;
                else
                    object.ping_time = options.longs === $String ? $util.Long.prototype.toString.call(message.ping_time) : options.longs === $Number ? new $util.LongBits(message.ping_time.low >>> 0, message.ping_time.high >>> 0).toNumber() : message.ping_time;
            if (message.sync_type != null && $Object.hasOwnProperty.call(message, "sync_type"))
                object.sync_type = options.enums === $String ? $root.lnrpc.Peer.SyncType[message.sync_type] === $undefined ? message.sync_type : $root.lnrpc.Peer.SyncType[message.sync_type] : message.sync_type;
            let keys2;
            if (message.features && (keys2 = $Object.keys(message.features)).length) {
                object.features = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.features, keys2[j]);
                    object.features[keys2[j]] = $root.lnrpc.Feature.toObject(message.features[keys2[j]], options, _depth + 1);
                }
            }
            if (message.errors && message.errors.length) {
                object.errors = $Array(message.errors.length);
                for (let j = 0; j < message.errors.length; ++j)
                    object.errors[j] = $root.lnrpc.TimestampedError.toObject(message.errors[j], options, _depth + 1);
            }
            if (message.flap_count != null && $Object.hasOwnProperty.call(message, "flap_count"))
                object.flap_count = message.flap_count;
            if (message.last_flap_ns != null && $Object.hasOwnProperty.call(message, "last_flap_ns"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.last_flap_ns = typeof message.last_flap_ns === "number" ? $BigInt(message.last_flap_ns) : $util.Long.fromBits(message.last_flap_ns.low >>> 0, message.last_flap_ns.high >>> 0, false).toBigInt();
                else if (typeof message.last_flap_ns === "number")
                    object.last_flap_ns = options.longs === $String ? $String(message.last_flap_ns) : message.last_flap_ns;
                else
                    object.last_flap_ns = options.longs === $String ? $util.Long.prototype.toString.call(message.last_flap_ns) : options.longs === $Number ? new $util.LongBits(message.last_flap_ns.low >>> 0, message.last_flap_ns.high >>> 0).toNumber() : message.last_flap_ns;
            if (message.last_ping_payload != null && $Object.hasOwnProperty.call(message, "last_ping_payload"))
                object.last_ping_payload = options.bytes === $String ? $util.base64.encode(message.last_ping_payload, 0, message.last_ping_payload.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.last_ping_payload) : message.last_ping_payload;
            return object;
        };

        /**
         * Converts this Peer to JSON.
         * @function toJSON
         * @memberof lnrpc.Peer
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Peer.prototype.toJSON = function() {
            return Peer.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Peer
         * @function getTypeUrl
         * @memberof lnrpc.Peer
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Peer.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Peer";
        };

        /**
         * SyncType enum.
         * @name lnrpc.Peer.SyncType
         * @enum {number}
         * @property {number} UNKNOWN_SYNC=0 UNKNOWN_SYNC value
         * @property {number} ACTIVE_SYNC=1 ACTIVE_SYNC value
         * @property {number} PASSIVE_SYNC=2 PASSIVE_SYNC value
         * @property {number} PINNED_SYNC=3 PINNED_SYNC value
         */
        Peer.SyncType = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_SYNC"] = 0;
            values[valuesById[1] = "ACTIVE_SYNC"] = 1;
            values[valuesById[2] = "PASSIVE_SYNC"] = 2;
            values[valuesById[3] = "PINNED_SYNC"] = 3;
            return values;
        })();

        return Peer;
    })();

    lnrpc.TimestampedError = (function() {

        /**
         * Properties of a TimestampedError.
         * @typedef {Object} lnrpc.TimestampedError.$Properties
         * @property {Long|null} [timestamp] TimestampedError timestamp
         * @property {string|null} [error] TimestampedError error
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TimestampedError.
         * @memberof lnrpc
         * @interface ITimestampedError
         * @augments lnrpc.TimestampedError.$Properties
         * @deprecated Use lnrpc.TimestampedError.$Properties instead.
         */

        /**
         * Shape of a TimestampedError.
         * @typedef {lnrpc.TimestampedError.$Properties} lnrpc.TimestampedError.$Shape
         */

        /**
         * Constructs a new TimestampedError.
         * @memberof lnrpc
         * @classdesc Represents a TimestampedError.
         * @constructor
         * @param {lnrpc.TimestampedError.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TimestampedError = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TimestampedError timestamp.
         * @member {Long} timestamp
         * @memberof lnrpc.TimestampedError
         * @instance
         */
        TimestampedError.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TimestampedError error.
         * @member {string} error
         * @memberof lnrpc.TimestampedError
         * @instance
         */
        TimestampedError.prototype.error = "";

        /**
         * Creates a new TimestampedError instance using the specified properties.
         * @function create
         * @memberof lnrpc.TimestampedError
         * @static
         * @param {lnrpc.TimestampedError.$Properties=} [properties] Properties to set
         * @returns {lnrpc.TimestampedError} TimestampedError instance
         * @type {{
         *   (properties: lnrpc.TimestampedError.$Shape): lnrpc.TimestampedError & lnrpc.TimestampedError.$Shape;
         *   (properties?: lnrpc.TimestampedError.$Properties): lnrpc.TimestampedError;
         * }}
         */
        TimestampedError.create = function(properties) {
            return new TimestampedError(properties);
        };

        /**
         * Encodes the specified TimestampedError message. Does not implicitly {@link lnrpc.TimestampedError.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.TimestampedError
         * @static
         * @param {lnrpc.TimestampedError.$Properties} message TimestampedError message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TimestampedError.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp") && (typeof message.timestamp === "object" ? message.timestamp.low || message.timestamp.high : message.timestamp !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.timestamp);
            if (message.error != null && $Object.hasOwnProperty.call(message, "error") && message.error !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.error);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TimestampedError message, length delimited. Does not implicitly {@link lnrpc.TimestampedError.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.TimestampedError
         * @static
         * @param {lnrpc.TimestampedError.$Properties} message TimestampedError message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TimestampedError.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TimestampedError message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.TimestampedError
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.TimestampedError & lnrpc.TimestampedError.$Shape} TimestampedError
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TimestampedError.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.TimestampedError(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.error = value;
                        else
                            delete message.error;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TimestampedError message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.TimestampedError
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.TimestampedError & lnrpc.TimestampedError.$Shape} TimestampedError
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TimestampedError.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TimestampedError message.
         * @function verify
         * @memberof lnrpc.TimestampedError
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TimestampedError.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a TimestampedError message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.TimestampedError
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.TimestampedError} TimestampedError
         */
        TimestampedError.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.TimestampedError)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.TimestampedError: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.TimestampedError();
            if (object.timestamp != null)
                if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, true);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = $parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            if (object.error != null)
                if (typeof object.error !== "string" || object.error.length)
                    message.error = $String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a TimestampedError message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.TimestampedError
         * @static
         * @param {lnrpc.TimestampedError} message TimestampedError
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TimestampedError.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.error = "";
            }
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this TimestampedError to JSON.
         * @function toJSON
         * @memberof lnrpc.TimestampedError
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TimestampedError.prototype.toJSON = function() {
            return TimestampedError.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TimestampedError
         * @function getTypeUrl
         * @memberof lnrpc.TimestampedError
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TimestampedError.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.TimestampedError";
        };

        return TimestampedError;
    })();

    lnrpc.ListPeersRequest = (function() {

        /**
         * Properties of a ListPeersRequest.
         * @typedef {Object} lnrpc.ListPeersRequest.$Properties
         * @property {boolean|null} [latest_error] ListPeersRequest latest_error
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListPeersRequest.
         * @memberof lnrpc
         * @interface IListPeersRequest
         * @augments lnrpc.ListPeersRequest.$Properties
         * @deprecated Use lnrpc.ListPeersRequest.$Properties instead.
         */

        /**
         * Shape of a ListPeersRequest.
         * @typedef {lnrpc.ListPeersRequest.$Properties} lnrpc.ListPeersRequest.$Shape
         */

        /**
         * Constructs a new ListPeersRequest.
         * @memberof lnrpc
         * @classdesc Represents a ListPeersRequest.
         * @constructor
         * @param {lnrpc.ListPeersRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListPeersRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListPeersRequest latest_error.
         * @member {boolean} latest_error
         * @memberof lnrpc.ListPeersRequest
         * @instance
         */
        ListPeersRequest.prototype.latest_error = false;

        /**
         * Creates a new ListPeersRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListPeersRequest
         * @static
         * @param {lnrpc.ListPeersRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListPeersRequest} ListPeersRequest instance
         * @type {{
         *   (properties: lnrpc.ListPeersRequest.$Shape): lnrpc.ListPeersRequest & lnrpc.ListPeersRequest.$Shape;
         *   (properties?: lnrpc.ListPeersRequest.$Properties): lnrpc.ListPeersRequest;
         * }}
         */
        ListPeersRequest.create = function(properties) {
            return new ListPeersRequest(properties);
        };

        /**
         * Encodes the specified ListPeersRequest message. Does not implicitly {@link lnrpc.ListPeersRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListPeersRequest
         * @static
         * @param {lnrpc.ListPeersRequest.$Properties} message ListPeersRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPeersRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.latest_error != null && $Object.hasOwnProperty.call(message, "latest_error") && message.latest_error !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.latest_error);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListPeersRequest message, length delimited. Does not implicitly {@link lnrpc.ListPeersRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListPeersRequest
         * @static
         * @param {lnrpc.ListPeersRequest.$Properties} message ListPeersRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPeersRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListPeersRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListPeersRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListPeersRequest & lnrpc.ListPeersRequest.$Shape} ListPeersRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPeersRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListPeersRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.latest_error = value;
                        else
                            delete message.latest_error;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListPeersRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListPeersRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListPeersRequest & lnrpc.ListPeersRequest.$Shape} ListPeersRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPeersRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListPeersRequest message.
         * @function verify
         * @memberof lnrpc.ListPeersRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListPeersRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.latest_error != null && $Object.hasOwnProperty.call(message, "latest_error"))
                if (typeof message.latest_error !== "boolean")
                    return "latest_error: boolean expected";
            return null;
        };

        /**
         * Creates a ListPeersRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListPeersRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListPeersRequest} ListPeersRequest
         */
        ListPeersRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListPeersRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListPeersRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListPeersRequest();
            if (object.latest_error != null)
                if (object.latest_error)
                    message.latest_error = $Boolean(object.latest_error);
            return message;
        };

        /**
         * Creates a plain object from a ListPeersRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListPeersRequest
         * @static
         * @param {lnrpc.ListPeersRequest} message ListPeersRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListPeersRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.latest_error = false;
            if (message.latest_error != null && $Object.hasOwnProperty.call(message, "latest_error"))
                object.latest_error = message.latest_error;
            return object;
        };

        /**
         * Converts this ListPeersRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ListPeersRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListPeersRequest.prototype.toJSON = function() {
            return ListPeersRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListPeersRequest
         * @function getTypeUrl
         * @memberof lnrpc.ListPeersRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListPeersRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListPeersRequest";
        };

        return ListPeersRequest;
    })();

    lnrpc.ListPeersResponse = (function() {

        /**
         * Properties of a ListPeersResponse.
         * @typedef {Object} lnrpc.ListPeersResponse.$Properties
         * @property {Array.<lnrpc.Peer.$Properties>|null} [peers] ListPeersResponse peers
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListPeersResponse.
         * @memberof lnrpc
         * @interface IListPeersResponse
         * @augments lnrpc.ListPeersResponse.$Properties
         * @deprecated Use lnrpc.ListPeersResponse.$Properties instead.
         */

        /**
         * Shape of a ListPeersResponse.
         * @typedef {lnrpc.ListPeersResponse.$Properties} lnrpc.ListPeersResponse.$Shape
         */

        /**
         * Constructs a new ListPeersResponse.
         * @memberof lnrpc
         * @classdesc Represents a ListPeersResponse.
         * @constructor
         * @param {lnrpc.ListPeersResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListPeersResponse = function (properties) {
            this.peers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListPeersResponse peers.
         * @member {Array.<lnrpc.Peer.$Properties>} peers
         * @memberof lnrpc.ListPeersResponse
         * @instance
         */
        ListPeersResponse.prototype.peers = $util.emptyArray;

        /**
         * Creates a new ListPeersResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListPeersResponse
         * @static
         * @param {lnrpc.ListPeersResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListPeersResponse} ListPeersResponse instance
         * @type {{
         *   (properties: lnrpc.ListPeersResponse.$Shape): lnrpc.ListPeersResponse & lnrpc.ListPeersResponse.$Shape;
         *   (properties?: lnrpc.ListPeersResponse.$Properties): lnrpc.ListPeersResponse;
         * }}
         */
        ListPeersResponse.create = function(properties) {
            return new ListPeersResponse(properties);
        };

        /**
         * Encodes the specified ListPeersResponse message. Does not implicitly {@link lnrpc.ListPeersResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListPeersResponse
         * @static
         * @param {lnrpc.ListPeersResponse.$Properties} message ListPeersResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPeersResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.peers != null && message.peers.length)
                for (let i = 0; i < message.peers.length; ++i)
                    $root.lnrpc.Peer.encode(message.peers[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListPeersResponse message, length delimited. Does not implicitly {@link lnrpc.ListPeersResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListPeersResponse
         * @static
         * @param {lnrpc.ListPeersResponse.$Properties} message ListPeersResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPeersResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListPeersResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListPeersResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListPeersResponse & lnrpc.ListPeersResponse.$Shape} ListPeersResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPeersResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListPeersResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.peers && message.peers.length))
                            message.peers = [];
                        message.peers.push($root.lnrpc.Peer.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListPeersResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListPeersResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListPeersResponse & lnrpc.ListPeersResponse.$Shape} ListPeersResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPeersResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListPeersResponse message.
         * @function verify
         * @memberof lnrpc.ListPeersResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListPeersResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.peers != null && $Object.hasOwnProperty.call(message, "peers")) {
                if (!$Array.isArray(message.peers))
                    return "peers: array expected";
                for (let i = 0; i < message.peers.length; ++i) {
                    let error = $root.lnrpc.Peer.verify(message.peers[i], _depth + 1);
                    if (error)
                        return "peers." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListPeersResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListPeersResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListPeersResponse} ListPeersResponse
         */
        ListPeersResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListPeersResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListPeersResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListPeersResponse();
            if (object.peers) {
                if (!$Array.isArray(object.peers))
                    throw $TypeError(".lnrpc.ListPeersResponse.peers: array expected");
                message.peers = $Array(object.peers.length);
                for (let i = 0; i < object.peers.length; ++i) {
                    if (!$util.isObject(object.peers[i]))
                        throw $TypeError(".lnrpc.ListPeersResponse.peers: object expected");
                    message.peers[i] = $root.lnrpc.Peer.fromObject(object.peers[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListPeersResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListPeersResponse
         * @static
         * @param {lnrpc.ListPeersResponse} message ListPeersResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListPeersResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.peers = [];
            if (message.peers && message.peers.length) {
                object.peers = $Array(message.peers.length);
                for (let j = 0; j < message.peers.length; ++j)
                    object.peers[j] = $root.lnrpc.Peer.toObject(message.peers[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ListPeersResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ListPeersResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListPeersResponse.prototype.toJSON = function() {
            return ListPeersResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListPeersResponse
         * @function getTypeUrl
         * @memberof lnrpc.ListPeersResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListPeersResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListPeersResponse";
        };

        return ListPeersResponse;
    })();

    lnrpc.PeerEventSubscription = (function() {

        /**
         * Properties of a PeerEventSubscription.
         * @typedef {Object} lnrpc.PeerEventSubscription.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PeerEventSubscription.
         * @memberof lnrpc
         * @interface IPeerEventSubscription
         * @augments lnrpc.PeerEventSubscription.$Properties
         * @deprecated Use lnrpc.PeerEventSubscription.$Properties instead.
         */

        /**
         * Shape of a PeerEventSubscription.
         * @typedef {lnrpc.PeerEventSubscription.$Properties} lnrpc.PeerEventSubscription.$Shape
         */

        /**
         * Constructs a new PeerEventSubscription.
         * @memberof lnrpc
         * @classdesc Represents a PeerEventSubscription.
         * @constructor
         * @param {lnrpc.PeerEventSubscription.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PeerEventSubscription = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new PeerEventSubscription instance using the specified properties.
         * @function create
         * @memberof lnrpc.PeerEventSubscription
         * @static
         * @param {lnrpc.PeerEventSubscription.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PeerEventSubscription} PeerEventSubscription instance
         * @type {{
         *   (properties: lnrpc.PeerEventSubscription.$Shape): lnrpc.PeerEventSubscription & lnrpc.PeerEventSubscription.$Shape;
         *   (properties?: lnrpc.PeerEventSubscription.$Properties): lnrpc.PeerEventSubscription;
         * }}
         */
        PeerEventSubscription.create = function(properties) {
            return new PeerEventSubscription(properties);
        };

        /**
         * Encodes the specified PeerEventSubscription message. Does not implicitly {@link lnrpc.PeerEventSubscription.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PeerEventSubscription
         * @static
         * @param {lnrpc.PeerEventSubscription.$Properties} message PeerEventSubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PeerEventSubscription.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PeerEventSubscription message, length delimited. Does not implicitly {@link lnrpc.PeerEventSubscription.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PeerEventSubscription
         * @static
         * @param {lnrpc.PeerEventSubscription.$Properties} message PeerEventSubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PeerEventSubscription.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PeerEventSubscription message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PeerEventSubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PeerEventSubscription & lnrpc.PeerEventSubscription.$Shape} PeerEventSubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PeerEventSubscription.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PeerEventSubscription();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PeerEventSubscription message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PeerEventSubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PeerEventSubscription & lnrpc.PeerEventSubscription.$Shape} PeerEventSubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PeerEventSubscription.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PeerEventSubscription message.
         * @function verify
         * @memberof lnrpc.PeerEventSubscription
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PeerEventSubscription.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a PeerEventSubscription message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PeerEventSubscription
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PeerEventSubscription} PeerEventSubscription
         */
        PeerEventSubscription.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PeerEventSubscription)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PeerEventSubscription: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.PeerEventSubscription();
        };

        /**
         * Creates a plain object from a PeerEventSubscription message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PeerEventSubscription
         * @static
         * @param {lnrpc.PeerEventSubscription} message PeerEventSubscription
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PeerEventSubscription.toObject = function () {
            return {};
        };

        /**
         * Converts this PeerEventSubscription to JSON.
         * @function toJSON
         * @memberof lnrpc.PeerEventSubscription
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PeerEventSubscription.prototype.toJSON = function() {
            return PeerEventSubscription.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PeerEventSubscription
         * @function getTypeUrl
         * @memberof lnrpc.PeerEventSubscription
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PeerEventSubscription.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PeerEventSubscription";
        };

        return PeerEventSubscription;
    })();

    lnrpc.PeerEvent = (function() {

        /**
         * Properties of a PeerEvent.
         * @typedef {Object} lnrpc.PeerEvent.$Properties
         * @property {string|null} [pub_key] PeerEvent pub_key
         * @property {lnrpc.PeerEvent.EventType|null} [type] PeerEvent type
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PeerEvent.
         * @memberof lnrpc
         * @interface IPeerEvent
         * @augments lnrpc.PeerEvent.$Properties
         * @deprecated Use lnrpc.PeerEvent.$Properties instead.
         */

        /**
         * Shape of a PeerEvent.
         * @typedef {lnrpc.PeerEvent.$Properties} lnrpc.PeerEvent.$Shape
         */

        /**
         * Constructs a new PeerEvent.
         * @memberof lnrpc
         * @classdesc Represents a PeerEvent.
         * @constructor
         * @param {lnrpc.PeerEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PeerEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PeerEvent pub_key.
         * @member {string} pub_key
         * @memberof lnrpc.PeerEvent
         * @instance
         */
        PeerEvent.prototype.pub_key = "";

        /**
         * PeerEvent type.
         * @member {lnrpc.PeerEvent.EventType} type
         * @memberof lnrpc.PeerEvent
         * @instance
         */
        PeerEvent.prototype.type = 0;

        /**
         * Creates a new PeerEvent instance using the specified properties.
         * @function create
         * @memberof lnrpc.PeerEvent
         * @static
         * @param {lnrpc.PeerEvent.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PeerEvent} PeerEvent instance
         * @type {{
         *   (properties: lnrpc.PeerEvent.$Shape): lnrpc.PeerEvent & lnrpc.PeerEvent.$Shape;
         *   (properties?: lnrpc.PeerEvent.$Properties): lnrpc.PeerEvent;
         * }}
         */
        PeerEvent.create = function(properties) {
            return new PeerEvent(properties);
        };

        /**
         * Encodes the specified PeerEvent message. Does not implicitly {@link lnrpc.PeerEvent.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PeerEvent
         * @static
         * @param {lnrpc.PeerEvent.$Properties} message PeerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PeerEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key") && message.pub_key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pub_key);
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PeerEvent message, length delimited. Does not implicitly {@link lnrpc.PeerEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PeerEvent
         * @static
         * @param {lnrpc.PeerEvent.$Properties} message PeerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PeerEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PeerEvent message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PeerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PeerEvent & lnrpc.PeerEvent.$Shape} PeerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PeerEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PeerEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pub_key = value;
                        else
                            delete message.pub_key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PeerEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PeerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PeerEvent & lnrpc.PeerEvent.$Shape} PeerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PeerEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PeerEvent message.
         * @function verify
         * @memberof lnrpc.PeerEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PeerEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                if (!$util.isString(message.pub_key))
                    return "pub_key: string expected";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            return null;
        };

        /**
         * Creates a PeerEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PeerEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PeerEvent} PeerEvent
         */
        PeerEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PeerEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PeerEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PeerEvent();
            if (object.pub_key != null)
                if (typeof object.pub_key !== "string" || object.pub_key.length)
                    message.pub_key = $String(object.pub_key);
            if (object.type !== 0 && (typeof object.type !== "string" || $root.lnrpc.PeerEvent.EventType[object.type] !== 0))
                switch (object.type) {
                case "PEER_ONLINE":
                case 0:
                    message.type = 0;
                    break;
                case "PEER_OFFLINE":
                case 1:
                    message.type = 1;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
            return message;
        };

        /**
         * Creates a plain object from a PeerEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PeerEvent
         * @static
         * @param {lnrpc.PeerEvent} message PeerEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PeerEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.pub_key = "";
                object.type = options.enums === $String ? "PEER_ONLINE" : 0;
            }
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                object.pub_key = message.pub_key;
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === $String ? $root.lnrpc.PeerEvent.EventType[message.type] === $undefined ? message.type : $root.lnrpc.PeerEvent.EventType[message.type] : message.type;
            return object;
        };

        /**
         * Converts this PeerEvent to JSON.
         * @function toJSON
         * @memberof lnrpc.PeerEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PeerEvent.prototype.toJSON = function() {
            return PeerEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PeerEvent
         * @function getTypeUrl
         * @memberof lnrpc.PeerEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PeerEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PeerEvent";
        };

        /**
         * EventType enum.
         * @name lnrpc.PeerEvent.EventType
         * @enum {number}
         * @property {number} PEER_ONLINE=0 PEER_ONLINE value
         * @property {number} PEER_OFFLINE=1 PEER_OFFLINE value
         */
        PeerEvent.EventType = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "PEER_ONLINE"] = 0;
            values[valuesById[1] = "PEER_OFFLINE"] = 1;
            return values;
        })();

        return PeerEvent;
    })();

    lnrpc.GetInfoRequest = (function() {

        /**
         * Properties of a GetInfoRequest.
         * @typedef {Object} lnrpc.GetInfoRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetInfoRequest.
         * @memberof lnrpc
         * @interface IGetInfoRequest
         * @augments lnrpc.GetInfoRequest.$Properties
         * @deprecated Use lnrpc.GetInfoRequest.$Properties instead.
         */

        /**
         * Shape of a GetInfoRequest.
         * @typedef {lnrpc.GetInfoRequest.$Properties} lnrpc.GetInfoRequest.$Shape
         */

        /**
         * Constructs a new GetInfoRequest.
         * @memberof lnrpc
         * @classdesc Represents a GetInfoRequest.
         * @constructor
         * @param {lnrpc.GetInfoRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetInfoRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new GetInfoRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.GetInfoRequest
         * @static
         * @param {lnrpc.GetInfoRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GetInfoRequest} GetInfoRequest instance
         * @type {{
         *   (properties: lnrpc.GetInfoRequest.$Shape): lnrpc.GetInfoRequest & lnrpc.GetInfoRequest.$Shape;
         *   (properties?: lnrpc.GetInfoRequest.$Properties): lnrpc.GetInfoRequest;
         * }}
         */
        GetInfoRequest.create = function(properties) {
            return new GetInfoRequest(properties);
        };

        /**
         * Encodes the specified GetInfoRequest message. Does not implicitly {@link lnrpc.GetInfoRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GetInfoRequest
         * @static
         * @param {lnrpc.GetInfoRequest.$Properties} message GetInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetInfoRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetInfoRequest message, length delimited. Does not implicitly {@link lnrpc.GetInfoRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GetInfoRequest
         * @static
         * @param {lnrpc.GetInfoRequest.$Properties} message GetInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetInfoRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetInfoRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GetInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GetInfoRequest & lnrpc.GetInfoRequest.$Shape} GetInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetInfoRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GetInfoRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetInfoRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GetInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GetInfoRequest & lnrpc.GetInfoRequest.$Shape} GetInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetInfoRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetInfoRequest message.
         * @function verify
         * @memberof lnrpc.GetInfoRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetInfoRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a GetInfoRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GetInfoRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GetInfoRequest} GetInfoRequest
         */
        GetInfoRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GetInfoRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GetInfoRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.GetInfoRequest();
        };

        /**
         * Creates a plain object from a GetInfoRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GetInfoRequest
         * @static
         * @param {lnrpc.GetInfoRequest} message GetInfoRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetInfoRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this GetInfoRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.GetInfoRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetInfoRequest.prototype.toJSON = function() {
            return GetInfoRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetInfoRequest
         * @function getTypeUrl
         * @memberof lnrpc.GetInfoRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetInfoRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GetInfoRequest";
        };

        return GetInfoRequest;
    })();

    lnrpc.GetInfoResponse = (function() {

        /**
         * Properties of a GetInfoResponse.
         * @typedef {Object} lnrpc.GetInfoResponse.$Properties
         * @property {string|null} [version] GetInfoResponse version
         * @property {string|null} [commit_hash] GetInfoResponse commit_hash
         * @property {string|null} [identity_pubkey] GetInfoResponse identity_pubkey
         * @property {string|null} [alias] GetInfoResponse alias
         * @property {string|null} [color] GetInfoResponse color
         * @property {number|null} [num_pending_channels] GetInfoResponse num_pending_channels
         * @property {number|null} [num_active_channels] GetInfoResponse num_active_channels
         * @property {number|null} [num_inactive_channels] GetInfoResponse num_inactive_channels
         * @property {number|null} [num_peers] GetInfoResponse num_peers
         * @property {number|null} [block_height] GetInfoResponse block_height
         * @property {string|null} [block_hash] GetInfoResponse block_hash
         * @property {Long|null} [best_header_timestamp] GetInfoResponse best_header_timestamp
         * @property {boolean|null} [synced_to_chain] GetInfoResponse synced_to_chain
         * @property {boolean|null} [synced_to_graph] GetInfoResponse synced_to_graph
         * @property {boolean|null} [testnet] GetInfoResponse testnet
         * @property {Array.<lnrpc.Chain.$Properties>|null} [chains] GetInfoResponse chains
         * @property {Array.<string>|null} [uris] GetInfoResponse uris
         * @property {Object.<string,lnrpc.Feature.$Properties>|null} [features] GetInfoResponse features
         * @property {boolean|null} [require_htlc_interceptor] GetInfoResponse require_htlc_interceptor
         * @property {boolean|null} [store_final_htlc_resolutions] GetInfoResponse store_final_htlc_resolutions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetInfoResponse.
         * @memberof lnrpc
         * @interface IGetInfoResponse
         * @augments lnrpc.GetInfoResponse.$Properties
         * @deprecated Use lnrpc.GetInfoResponse.$Properties instead.
         */

        /**
         * Shape of a GetInfoResponse.
         * @typedef {lnrpc.GetInfoResponse.$Properties} lnrpc.GetInfoResponse.$Shape
         */

        /**
         * Constructs a new GetInfoResponse.
         * @memberof lnrpc
         * @classdesc Represents a GetInfoResponse.
         * @constructor
         * @param {lnrpc.GetInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetInfoResponse = function (properties) {
            this.chains = [];
            this.uris = [];
            this.features = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetInfoResponse version.
         * @member {string} version
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.version = "";

        /**
         * GetInfoResponse commit_hash.
         * @member {string} commit_hash
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.commit_hash = "";

        /**
         * GetInfoResponse identity_pubkey.
         * @member {string} identity_pubkey
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.identity_pubkey = "";

        /**
         * GetInfoResponse alias.
         * @member {string} alias
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.alias = "";

        /**
         * GetInfoResponse color.
         * @member {string} color
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.color = "";

        /**
         * GetInfoResponse num_pending_channels.
         * @member {number} num_pending_channels
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.num_pending_channels = 0;

        /**
         * GetInfoResponse num_active_channels.
         * @member {number} num_active_channels
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.num_active_channels = 0;

        /**
         * GetInfoResponse num_inactive_channels.
         * @member {number} num_inactive_channels
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.num_inactive_channels = 0;

        /**
         * GetInfoResponse num_peers.
         * @member {number} num_peers
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.num_peers = 0;

        /**
         * GetInfoResponse block_height.
         * @member {number} block_height
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.block_height = 0;

        /**
         * GetInfoResponse block_hash.
         * @member {string} block_hash
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.block_hash = "";

        /**
         * GetInfoResponse best_header_timestamp.
         * @member {Long} best_header_timestamp
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.best_header_timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetInfoResponse synced_to_chain.
         * @member {boolean} synced_to_chain
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.synced_to_chain = false;

        /**
         * GetInfoResponse synced_to_graph.
         * @member {boolean} synced_to_graph
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.synced_to_graph = false;

        /**
         * GetInfoResponse testnet.
         * @member {boolean} testnet
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.testnet = false;

        /**
         * GetInfoResponse chains.
         * @member {Array.<lnrpc.Chain.$Properties>} chains
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.chains = $util.emptyArray;

        /**
         * GetInfoResponse uris.
         * @member {Array.<string>} uris
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.uris = $util.emptyArray;

        /**
         * GetInfoResponse features.
         * @member {Object.<string,lnrpc.Feature.$Properties>} features
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.features = $util.emptyObject;

        /**
         * GetInfoResponse require_htlc_interceptor.
         * @member {boolean} require_htlc_interceptor
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.require_htlc_interceptor = false;

        /**
         * GetInfoResponse store_final_htlc_resolutions.
         * @member {boolean} store_final_htlc_resolutions
         * @memberof lnrpc.GetInfoResponse
         * @instance
         */
        GetInfoResponse.prototype.store_final_htlc_resolutions = false;

        /**
         * Creates a new GetInfoResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.GetInfoResponse
         * @static
         * @param {lnrpc.GetInfoResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GetInfoResponse} GetInfoResponse instance
         * @type {{
         *   (properties: lnrpc.GetInfoResponse.$Shape): lnrpc.GetInfoResponse & lnrpc.GetInfoResponse.$Shape;
         *   (properties?: lnrpc.GetInfoResponse.$Properties): lnrpc.GetInfoResponse;
         * }}
         */
        GetInfoResponse.create = function(properties) {
            return new GetInfoResponse(properties);
        };

        /**
         * Encodes the specified GetInfoResponse message. Does not implicitly {@link lnrpc.GetInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GetInfoResponse
         * @static
         * @param {lnrpc.GetInfoResponse.$Properties} message GetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.identity_pubkey != null && $Object.hasOwnProperty.call(message, "identity_pubkey") && message.identity_pubkey !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.identity_pubkey);
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias") && message.alias !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.alias);
            if (message.num_pending_channels != null && $Object.hasOwnProperty.call(message, "num_pending_channels") && message.num_pending_channels !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.num_pending_channels);
            if (message.num_active_channels != null && $Object.hasOwnProperty.call(message, "num_active_channels") && message.num_active_channels !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.num_active_channels);
            if (message.num_peers != null && $Object.hasOwnProperty.call(message, "num_peers") && message.num_peers !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.num_peers);
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height") && message.block_height !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.block_height);
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash") && message.block_hash !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.block_hash);
            if (message.synced_to_chain != null && $Object.hasOwnProperty.call(message, "synced_to_chain") && message.synced_to_chain !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.synced_to_chain);
            if (message.testnet != null && $Object.hasOwnProperty.call(message, "testnet") && message.testnet !== false)
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.testnet);
            if (message.uris != null && message.uris.length)
                for (let i = 0; i < message.uris.length; ++i)
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.uris[i]);
            if (message.best_header_timestamp != null && $Object.hasOwnProperty.call(message, "best_header_timestamp") && (typeof message.best_header_timestamp === "object" ? message.best_header_timestamp.low || message.best_header_timestamp.high : message.best_header_timestamp !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.best_header_timestamp);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.version);
            if (message.num_inactive_channels != null && $Object.hasOwnProperty.call(message, "num_inactive_channels") && message.num_inactive_channels !== 0)
                writer.uint32(/* id 15, wireType 0 =*/120).uint32(message.num_inactive_channels);
            if (message.chains != null && message.chains.length)
                for (let i = 0; i < message.chains.length; ++i)
                    $root.lnrpc.Chain.encode(message.chains[i], writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.color != null && $Object.hasOwnProperty.call(message, "color") && message.color !== "")
                writer.uint32(/* id 17, wireType 2 =*/138).string(message.color);
            if (message.synced_to_graph != null && $Object.hasOwnProperty.call(message, "synced_to_graph") && message.synced_to_graph !== false)
                writer.uint32(/* id 18, wireType 0 =*/144).bool(message.synced_to_graph);
            if (message.features != null && $Object.hasOwnProperty.call(message, "features"))
                for (let keys = $Object.keys(message.features), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 19, wireType 2 =*/154).fork().uint32(/* id 1, wireType 0 =*/8).uint32(keys[i]);
                    $root.lnrpc.Feature.encode(message.features[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.commit_hash != null && $Object.hasOwnProperty.call(message, "commit_hash") && message.commit_hash !== "")
                writer.uint32(/* id 20, wireType 2 =*/162).string(message.commit_hash);
            if (message.require_htlc_interceptor != null && $Object.hasOwnProperty.call(message, "require_htlc_interceptor") && message.require_htlc_interceptor !== false)
                writer.uint32(/* id 21, wireType 0 =*/168).bool(message.require_htlc_interceptor);
            if (message.store_final_htlc_resolutions != null && $Object.hasOwnProperty.call(message, "store_final_htlc_resolutions") && message.store_final_htlc_resolutions !== false)
                writer.uint32(/* id 22, wireType 0 =*/176).bool(message.store_final_htlc_resolutions);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetInfoResponse message, length delimited. Does not implicitly {@link lnrpc.GetInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GetInfoResponse
         * @static
         * @param {lnrpc.GetInfoResponse.$Properties} message GetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GetInfoResponse & lnrpc.GetInfoResponse.$Shape} GetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GetInfoResponse(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 14: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.commit_hash = value;
                        else
                            delete message.commit_hash;
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.identity_pubkey = value;
                        else
                            delete message.identity_pubkey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.alias = value;
                        else
                            delete message.alias;
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.color = value;
                        else
                            delete message.color;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_pending_channels = value;
                        else
                            delete message.num_pending_channels;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_active_channels = value;
                        else
                            delete message.num_active_channels;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_inactive_channels = value;
                        else
                            delete message.num_inactive_channels;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_peers = value;
                        else
                            delete message.num_peers;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.block_height = value;
                        else
                            delete message.block_height;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.block_hash = value;
                        else
                            delete message.block_hash;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.best_header_timestamp = value;
                        else
                            delete message.best_header_timestamp;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.synced_to_chain = value;
                        else
                            delete message.synced_to_chain;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.synced_to_graph = value;
                        else
                            delete message.synced_to_graph;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.testnet = value;
                        else
                            delete message.testnet;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.chains && message.chains.length))
                            message.chains = [];
                        message.chains.push($root.lnrpc.Chain.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.uris && message.uris.length))
                            message.uris = [];
                        message.uris.push(reader.stringVerify());
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if (message.features === $util.emptyObject)
                            message.features = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint32();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.Feature.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.features[key] = value || new $root.lnrpc.Feature();
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.require_htlc_interceptor = value;
                        else
                            delete message.require_htlc_interceptor;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.store_final_htlc_resolutions = value;
                        else
                            delete message.store_final_htlc_resolutions;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GetInfoResponse & lnrpc.GetInfoResponse.$Shape} GetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetInfoResponse message.
         * @function verify
         * @memberof lnrpc.GetInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                if (!$util.isString(message.version))
                    return "version: string expected";
            if (message.commit_hash != null && $Object.hasOwnProperty.call(message, "commit_hash"))
                if (!$util.isString(message.commit_hash))
                    return "commit_hash: string expected";
            if (message.identity_pubkey != null && $Object.hasOwnProperty.call(message, "identity_pubkey"))
                if (!$util.isString(message.identity_pubkey))
                    return "identity_pubkey: string expected";
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                if (!$util.isString(message.alias))
                    return "alias: string expected";
            if (message.color != null && $Object.hasOwnProperty.call(message, "color"))
                if (!$util.isString(message.color))
                    return "color: string expected";
            if (message.num_pending_channels != null && $Object.hasOwnProperty.call(message, "num_pending_channels"))
                if (!$util.isInteger(message.num_pending_channels))
                    return "num_pending_channels: integer expected";
            if (message.num_active_channels != null && $Object.hasOwnProperty.call(message, "num_active_channels"))
                if (!$util.isInteger(message.num_active_channels))
                    return "num_active_channels: integer expected";
            if (message.num_inactive_channels != null && $Object.hasOwnProperty.call(message, "num_inactive_channels"))
                if (!$util.isInteger(message.num_inactive_channels))
                    return "num_inactive_channels: integer expected";
            if (message.num_peers != null && $Object.hasOwnProperty.call(message, "num_peers"))
                if (!$util.isInteger(message.num_peers))
                    return "num_peers: integer expected";
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height"))
                if (!$util.isInteger(message.block_height))
                    return "block_height: integer expected";
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash"))
                if (!$util.isString(message.block_hash))
                    return "block_hash: string expected";
            if (message.best_header_timestamp != null && $Object.hasOwnProperty.call(message, "best_header_timestamp"))
                if (!$util.isInteger(message.best_header_timestamp) && !(message.best_header_timestamp && $util.isInteger(message.best_header_timestamp.low) && $util.isInteger(message.best_header_timestamp.high)))
                    return "best_header_timestamp: integer|Long expected";
            if (message.synced_to_chain != null && $Object.hasOwnProperty.call(message, "synced_to_chain"))
                if (typeof message.synced_to_chain !== "boolean")
                    return "synced_to_chain: boolean expected";
            if (message.synced_to_graph != null && $Object.hasOwnProperty.call(message, "synced_to_graph"))
                if (typeof message.synced_to_graph !== "boolean")
                    return "synced_to_graph: boolean expected";
            if (message.testnet != null && $Object.hasOwnProperty.call(message, "testnet"))
                if (typeof message.testnet !== "boolean")
                    return "testnet: boolean expected";
            if (message.chains != null && $Object.hasOwnProperty.call(message, "chains")) {
                if (!$Array.isArray(message.chains))
                    return "chains: array expected";
                for (let i = 0; i < message.chains.length; ++i) {
                    let error = $root.lnrpc.Chain.verify(message.chains[i], _depth + 1);
                    if (error)
                        return "chains." + error;
                }
            }
            if (message.uris != null && $Object.hasOwnProperty.call(message, "uris")) {
                if (!$Array.isArray(message.uris))
                    return "uris: array expected";
                for (let i = 0; i < message.uris.length; ++i)
                    if (!$util.isString(message.uris[i]))
                        return "uris: string[] expected";
            }
            if (message.features != null && $Object.hasOwnProperty.call(message, "features")) {
                if (!$util.isObject(message.features))
                    return "features: object expected";
                let key = $Object.keys(message.features);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "features: integer key{k:uint32} expected";
                    {
                        let error = $root.lnrpc.Feature.verify(message.features[key[i]], _depth + 1);
                        if (error)
                            return "features." + error;
                    }
                }
            }
            if (message.require_htlc_interceptor != null && $Object.hasOwnProperty.call(message, "require_htlc_interceptor"))
                if (typeof message.require_htlc_interceptor !== "boolean")
                    return "require_htlc_interceptor: boolean expected";
            if (message.store_final_htlc_resolutions != null && $Object.hasOwnProperty.call(message, "store_final_htlc_resolutions"))
                if (typeof message.store_final_htlc_resolutions !== "boolean")
                    return "store_final_htlc_resolutions: boolean expected";
            return null;
        };

        /**
         * Creates a GetInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GetInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GetInfoResponse} GetInfoResponse
         */
        GetInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GetInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GetInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.GetInfoResponse();
            if (object.version != null)
                if (typeof object.version !== "string" || object.version.length)
                    message.version = $String(object.version);
            if (object.commit_hash != null)
                if (typeof object.commit_hash !== "string" || object.commit_hash.length)
                    message.commit_hash = $String(object.commit_hash);
            if (object.identity_pubkey != null)
                if (typeof object.identity_pubkey !== "string" || object.identity_pubkey.length)
                    message.identity_pubkey = $String(object.identity_pubkey);
            if (object.alias != null)
                if (typeof object.alias !== "string" || object.alias.length)
                    message.alias = $String(object.alias);
            if (object.color != null)
                if (typeof object.color !== "string" || object.color.length)
                    message.color = $String(object.color);
            if (object.num_pending_channels != null)
                if ($Number(object.num_pending_channels) !== 0)
                    message.num_pending_channels = object.num_pending_channels >>> 0;
            if (object.num_active_channels != null)
                if ($Number(object.num_active_channels) !== 0)
                    message.num_active_channels = object.num_active_channels >>> 0;
            if (object.num_inactive_channels != null)
                if ($Number(object.num_inactive_channels) !== 0)
                    message.num_inactive_channels = object.num_inactive_channels >>> 0;
            if (object.num_peers != null)
                if ($Number(object.num_peers) !== 0)
                    message.num_peers = object.num_peers >>> 0;
            if (object.block_height != null)
                if ($Number(object.block_height) !== 0)
                    message.block_height = object.block_height >>> 0;
            if (object.block_hash != null)
                if (typeof object.block_hash !== "string" || object.block_hash.length)
                    message.block_hash = $String(object.block_hash);
            if (object.best_header_timestamp != null)
                if (typeof object.best_header_timestamp === "object" ? object.best_header_timestamp.low || object.best_header_timestamp.high : $Number(object.best_header_timestamp) !== 0)
                    if ($util.Long)
                        message.best_header_timestamp = $util.Long.fromValue(object.best_header_timestamp, false);
                    else if (typeof object.best_header_timestamp === "string")
                        message.best_header_timestamp = $parseInt(object.best_header_timestamp, 10);
                    else if (typeof object.best_header_timestamp === "number")
                        message.best_header_timestamp = object.best_header_timestamp;
                    else if (typeof object.best_header_timestamp === "object")
                        message.best_header_timestamp = new $util.LongBits(object.best_header_timestamp.low >>> 0, object.best_header_timestamp.high >>> 0).toNumber();
            if (object.synced_to_chain != null)
                if (object.synced_to_chain)
                    message.synced_to_chain = $Boolean(object.synced_to_chain);
            if (object.synced_to_graph != null)
                if (object.synced_to_graph)
                    message.synced_to_graph = $Boolean(object.synced_to_graph);
            if (object.testnet != null)
                if (object.testnet)
                    message.testnet = $Boolean(object.testnet);
            if (object.chains) {
                if (!$Array.isArray(object.chains))
                    throw $TypeError(".lnrpc.GetInfoResponse.chains: array expected");
                message.chains = $Array(object.chains.length);
                for (let i = 0; i < object.chains.length; ++i) {
                    if (!$util.isObject(object.chains[i]))
                        throw $TypeError(".lnrpc.GetInfoResponse.chains: object expected");
                    message.chains[i] = $root.lnrpc.Chain.fromObject(object.chains[i], _depth + 1);
                }
            }
            if (object.uris) {
                if (!$Array.isArray(object.uris))
                    throw $TypeError(".lnrpc.GetInfoResponse.uris: array expected");
                message.uris = $Array(object.uris.length);
                for (let i = 0; i < object.uris.length; ++i)
                    message.uris[i] = $String(object.uris[i]);
            }
            if (object.features) {
                if (!$util.isObject(object.features))
                    throw $TypeError(".lnrpc.GetInfoResponse.features: object expected");
                message.features = {};
                for (let keys = $Object.keys(object.features), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.features, keys[i]);
                    if (!$util.isObject(object.features[keys[i]]))
                        throw $TypeError(".lnrpc.GetInfoResponse.features: object expected");
                    message.features[keys[i]] = $root.lnrpc.Feature.fromObject(object.features[keys[i]], _depth + 1);
                }
            }
            if (object.require_htlc_interceptor != null)
                if (object.require_htlc_interceptor)
                    message.require_htlc_interceptor = $Boolean(object.require_htlc_interceptor);
            if (object.store_final_htlc_resolutions != null)
                if (object.store_final_htlc_resolutions)
                    message.store_final_htlc_resolutions = $Boolean(object.store_final_htlc_resolutions);
            return message;
        };

        /**
         * Creates a plain object from a GetInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GetInfoResponse
         * @static
         * @param {lnrpc.GetInfoResponse} message GetInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.uris = [];
                object.chains = [];
            }
            if (options.objects || options.defaults)
                object.features = {};
            if (options.defaults) {
                object.identity_pubkey = "";
                object.alias = "";
                object.num_pending_channels = 0;
                object.num_active_channels = 0;
                object.num_peers = 0;
                object.block_height = 0;
                object.block_hash = "";
                object.synced_to_chain = false;
                object.testnet = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.best_header_timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.best_header_timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.version = "";
                object.num_inactive_channels = 0;
                object.color = "";
                object.synced_to_graph = false;
                object.commit_hash = "";
                object.require_htlc_interceptor = false;
                object.store_final_htlc_resolutions = false;
            }
            if (message.identity_pubkey != null && $Object.hasOwnProperty.call(message, "identity_pubkey"))
                object.identity_pubkey = message.identity_pubkey;
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                object.alias = message.alias;
            if (message.num_pending_channels != null && $Object.hasOwnProperty.call(message, "num_pending_channels"))
                object.num_pending_channels = message.num_pending_channels;
            if (message.num_active_channels != null && $Object.hasOwnProperty.call(message, "num_active_channels"))
                object.num_active_channels = message.num_active_channels;
            if (message.num_peers != null && $Object.hasOwnProperty.call(message, "num_peers"))
                object.num_peers = message.num_peers;
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height"))
                object.block_height = message.block_height;
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash"))
                object.block_hash = message.block_hash;
            if (message.synced_to_chain != null && $Object.hasOwnProperty.call(message, "synced_to_chain"))
                object.synced_to_chain = message.synced_to_chain;
            if (message.testnet != null && $Object.hasOwnProperty.call(message, "testnet"))
                object.testnet = message.testnet;
            if (message.uris && message.uris.length) {
                object.uris = $Array(message.uris.length);
                for (let j = 0; j < message.uris.length; ++j)
                    object.uris[j] = message.uris[j];
            }
            if (message.best_header_timestamp != null && $Object.hasOwnProperty.call(message, "best_header_timestamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.best_header_timestamp = typeof message.best_header_timestamp === "number" ? $BigInt(message.best_header_timestamp) : $util.Long.fromBits(message.best_header_timestamp.low >>> 0, message.best_header_timestamp.high >>> 0, false).toBigInt();
                else if (typeof message.best_header_timestamp === "number")
                    object.best_header_timestamp = options.longs === $String ? $String(message.best_header_timestamp) : message.best_header_timestamp;
                else
                    object.best_header_timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.best_header_timestamp) : options.longs === $Number ? new $util.LongBits(message.best_header_timestamp.low >>> 0, message.best_header_timestamp.high >>> 0).toNumber() : message.best_header_timestamp;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.num_inactive_channels != null && $Object.hasOwnProperty.call(message, "num_inactive_channels"))
                object.num_inactive_channels = message.num_inactive_channels;
            if (message.chains && message.chains.length) {
                object.chains = $Array(message.chains.length);
                for (let j = 0; j < message.chains.length; ++j)
                    object.chains[j] = $root.lnrpc.Chain.toObject(message.chains[j], options, _depth + 1);
            }
            if (message.color != null && $Object.hasOwnProperty.call(message, "color"))
                object.color = message.color;
            if (message.synced_to_graph != null && $Object.hasOwnProperty.call(message, "synced_to_graph"))
                object.synced_to_graph = message.synced_to_graph;
            let keys2;
            if (message.features && (keys2 = $Object.keys(message.features)).length) {
                object.features = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.features, keys2[j]);
                    object.features[keys2[j]] = $root.lnrpc.Feature.toObject(message.features[keys2[j]], options, _depth + 1);
                }
            }
            if (message.commit_hash != null && $Object.hasOwnProperty.call(message, "commit_hash"))
                object.commit_hash = message.commit_hash;
            if (message.require_htlc_interceptor != null && $Object.hasOwnProperty.call(message, "require_htlc_interceptor"))
                object.require_htlc_interceptor = message.require_htlc_interceptor;
            if (message.store_final_htlc_resolutions != null && $Object.hasOwnProperty.call(message, "store_final_htlc_resolutions"))
                object.store_final_htlc_resolutions = message.store_final_htlc_resolutions;
            return object;
        };

        /**
         * Converts this GetInfoResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.GetInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetInfoResponse.prototype.toJSON = function() {
            return GetInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetInfoResponse
         * @function getTypeUrl
         * @memberof lnrpc.GetInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GetInfoResponse";
        };

        return GetInfoResponse;
    })();

    lnrpc.GetDebugInfoRequest = (function() {

        /**
         * Properties of a GetDebugInfoRequest.
         * @typedef {Object} lnrpc.GetDebugInfoRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetDebugInfoRequest.
         * @memberof lnrpc
         * @interface IGetDebugInfoRequest
         * @augments lnrpc.GetDebugInfoRequest.$Properties
         * @deprecated Use lnrpc.GetDebugInfoRequest.$Properties instead.
         */

        /**
         * Shape of a GetDebugInfoRequest.
         * @typedef {lnrpc.GetDebugInfoRequest.$Properties} lnrpc.GetDebugInfoRequest.$Shape
         */

        /**
         * Constructs a new GetDebugInfoRequest.
         * @memberof lnrpc
         * @classdesc Represents a GetDebugInfoRequest.
         * @constructor
         * @param {lnrpc.GetDebugInfoRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetDebugInfoRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new GetDebugInfoRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.GetDebugInfoRequest
         * @static
         * @param {lnrpc.GetDebugInfoRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GetDebugInfoRequest} GetDebugInfoRequest instance
         * @type {{
         *   (properties: lnrpc.GetDebugInfoRequest.$Shape): lnrpc.GetDebugInfoRequest & lnrpc.GetDebugInfoRequest.$Shape;
         *   (properties?: lnrpc.GetDebugInfoRequest.$Properties): lnrpc.GetDebugInfoRequest;
         * }}
         */
        GetDebugInfoRequest.create = function(properties) {
            return new GetDebugInfoRequest(properties);
        };

        /**
         * Encodes the specified GetDebugInfoRequest message. Does not implicitly {@link lnrpc.GetDebugInfoRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GetDebugInfoRequest
         * @static
         * @param {lnrpc.GetDebugInfoRequest.$Properties} message GetDebugInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetDebugInfoRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetDebugInfoRequest message, length delimited. Does not implicitly {@link lnrpc.GetDebugInfoRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GetDebugInfoRequest
         * @static
         * @param {lnrpc.GetDebugInfoRequest.$Properties} message GetDebugInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetDebugInfoRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetDebugInfoRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GetDebugInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GetDebugInfoRequest & lnrpc.GetDebugInfoRequest.$Shape} GetDebugInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetDebugInfoRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GetDebugInfoRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetDebugInfoRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GetDebugInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GetDebugInfoRequest & lnrpc.GetDebugInfoRequest.$Shape} GetDebugInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetDebugInfoRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetDebugInfoRequest message.
         * @function verify
         * @memberof lnrpc.GetDebugInfoRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetDebugInfoRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a GetDebugInfoRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GetDebugInfoRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GetDebugInfoRequest} GetDebugInfoRequest
         */
        GetDebugInfoRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GetDebugInfoRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GetDebugInfoRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.GetDebugInfoRequest();
        };

        /**
         * Creates a plain object from a GetDebugInfoRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GetDebugInfoRequest
         * @static
         * @param {lnrpc.GetDebugInfoRequest} message GetDebugInfoRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetDebugInfoRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this GetDebugInfoRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.GetDebugInfoRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetDebugInfoRequest.prototype.toJSON = function() {
            return GetDebugInfoRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetDebugInfoRequest
         * @function getTypeUrl
         * @memberof lnrpc.GetDebugInfoRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetDebugInfoRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GetDebugInfoRequest";
        };

        return GetDebugInfoRequest;
    })();

    lnrpc.GetDebugInfoResponse = (function() {

        /**
         * Properties of a GetDebugInfoResponse.
         * @typedef {Object} lnrpc.GetDebugInfoResponse.$Properties
         * @property {Object.<string,string>|null} [config] GetDebugInfoResponse config
         * @property {Array.<string>|null} [log] GetDebugInfoResponse log
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetDebugInfoResponse.
         * @memberof lnrpc
         * @interface IGetDebugInfoResponse
         * @augments lnrpc.GetDebugInfoResponse.$Properties
         * @deprecated Use lnrpc.GetDebugInfoResponse.$Properties instead.
         */

        /**
         * Shape of a GetDebugInfoResponse.
         * @typedef {lnrpc.GetDebugInfoResponse.$Properties} lnrpc.GetDebugInfoResponse.$Shape
         */

        /**
         * Constructs a new GetDebugInfoResponse.
         * @memberof lnrpc
         * @classdesc Represents a GetDebugInfoResponse.
         * @constructor
         * @param {lnrpc.GetDebugInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetDebugInfoResponse = function (properties) {
            this.config = {};
            this.log = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetDebugInfoResponse config.
         * @member {Object.<string,string>} config
         * @memberof lnrpc.GetDebugInfoResponse
         * @instance
         */
        GetDebugInfoResponse.prototype.config = $util.emptyObject;

        /**
         * GetDebugInfoResponse log.
         * @member {Array.<string>} log
         * @memberof lnrpc.GetDebugInfoResponse
         * @instance
         */
        GetDebugInfoResponse.prototype.log = $util.emptyArray;

        /**
         * Creates a new GetDebugInfoResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.GetDebugInfoResponse
         * @static
         * @param {lnrpc.GetDebugInfoResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GetDebugInfoResponse} GetDebugInfoResponse instance
         * @type {{
         *   (properties: lnrpc.GetDebugInfoResponse.$Shape): lnrpc.GetDebugInfoResponse & lnrpc.GetDebugInfoResponse.$Shape;
         *   (properties?: lnrpc.GetDebugInfoResponse.$Properties): lnrpc.GetDebugInfoResponse;
         * }}
         */
        GetDebugInfoResponse.create = function(properties) {
            return new GetDebugInfoResponse(properties);
        };

        /**
         * Encodes the specified GetDebugInfoResponse message. Does not implicitly {@link lnrpc.GetDebugInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GetDebugInfoResponse
         * @static
         * @param {lnrpc.GetDebugInfoResponse.$Properties} message GetDebugInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetDebugInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                for (let keys = $Object.keys(message.config), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.config[keys[i]]).ldelim();
            if (message.log != null && message.log.length)
                for (let i = 0; i < message.log.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.log[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetDebugInfoResponse message, length delimited. Does not implicitly {@link lnrpc.GetDebugInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GetDebugInfoResponse
         * @static
         * @param {lnrpc.GetDebugInfoResponse.$Properties} message GetDebugInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetDebugInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetDebugInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GetDebugInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GetDebugInfoResponse & lnrpc.GetDebugInfoResponse.$Shape} GetDebugInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetDebugInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GetDebugInfoResponse(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (message.config === $util.emptyObject)
                            message.config = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.stringVerify();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.config, key);
                        message.config[key] = value;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.log && message.log.length))
                            message.log = [];
                        message.log.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetDebugInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GetDebugInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GetDebugInfoResponse & lnrpc.GetDebugInfoResponse.$Shape} GetDebugInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetDebugInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetDebugInfoResponse message.
         * @function verify
         * @memberof lnrpc.GetDebugInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetDebugInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                if (!$util.isObject(message.config))
                    return "config: object expected";
                let key = $Object.keys(message.config);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.config[key[i]]))
                        return "config: string{k:string} expected";
            }
            if (message.log != null && $Object.hasOwnProperty.call(message, "log")) {
                if (!$Array.isArray(message.log))
                    return "log: array expected";
                for (let i = 0; i < message.log.length; ++i)
                    if (!$util.isString(message.log[i]))
                        return "log: string[] expected";
            }
            return null;
        };

        /**
         * Creates a GetDebugInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GetDebugInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GetDebugInfoResponse} GetDebugInfoResponse
         */
        GetDebugInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GetDebugInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GetDebugInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.GetDebugInfoResponse();
            if (object.config) {
                if (!$util.isObject(object.config))
                    throw $TypeError(".lnrpc.GetDebugInfoResponse.config: object expected");
                message.config = {};
                for (let keys = $Object.keys(object.config), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.config, keys[i]);
                    message.config[keys[i]] = $String(object.config[keys[i]]);
                }
            }
            if (object.log) {
                if (!$Array.isArray(object.log))
                    throw $TypeError(".lnrpc.GetDebugInfoResponse.log: array expected");
                message.log = $Array(object.log.length);
                for (let i = 0; i < object.log.length; ++i)
                    message.log[i] = $String(object.log[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetDebugInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GetDebugInfoResponse
         * @static
         * @param {lnrpc.GetDebugInfoResponse} message GetDebugInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetDebugInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.log = [];
            if (options.objects || options.defaults)
                object.config = {};
            let keys2;
            if (message.config && (keys2 = $Object.keys(message.config)).length) {
                object.config = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.config, keys2[j]);
                    object.config[keys2[j]] = message.config[keys2[j]];
                }
            }
            if (message.log && message.log.length) {
                object.log = $Array(message.log.length);
                for (let j = 0; j < message.log.length; ++j)
                    object.log[j] = message.log[j];
            }
            return object;
        };

        /**
         * Converts this GetDebugInfoResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.GetDebugInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetDebugInfoResponse.prototype.toJSON = function() {
            return GetDebugInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetDebugInfoResponse
         * @function getTypeUrl
         * @memberof lnrpc.GetDebugInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetDebugInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GetDebugInfoResponse";
        };

        return GetDebugInfoResponse;
    })();

    lnrpc.GetRecoveryInfoRequest = (function() {

        /**
         * Properties of a GetRecoveryInfoRequest.
         * @typedef {Object} lnrpc.GetRecoveryInfoRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetRecoveryInfoRequest.
         * @memberof lnrpc
         * @interface IGetRecoveryInfoRequest
         * @augments lnrpc.GetRecoveryInfoRequest.$Properties
         * @deprecated Use lnrpc.GetRecoveryInfoRequest.$Properties instead.
         */

        /**
         * Shape of a GetRecoveryInfoRequest.
         * @typedef {lnrpc.GetRecoveryInfoRequest.$Properties} lnrpc.GetRecoveryInfoRequest.$Shape
         */

        /**
         * Constructs a new GetRecoveryInfoRequest.
         * @memberof lnrpc
         * @classdesc Represents a GetRecoveryInfoRequest.
         * @constructor
         * @param {lnrpc.GetRecoveryInfoRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetRecoveryInfoRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new GetRecoveryInfoRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @static
         * @param {lnrpc.GetRecoveryInfoRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GetRecoveryInfoRequest} GetRecoveryInfoRequest instance
         * @type {{
         *   (properties: lnrpc.GetRecoveryInfoRequest.$Shape): lnrpc.GetRecoveryInfoRequest & lnrpc.GetRecoveryInfoRequest.$Shape;
         *   (properties?: lnrpc.GetRecoveryInfoRequest.$Properties): lnrpc.GetRecoveryInfoRequest;
         * }}
         */
        GetRecoveryInfoRequest.create = function(properties) {
            return new GetRecoveryInfoRequest(properties);
        };

        /**
         * Encodes the specified GetRecoveryInfoRequest message. Does not implicitly {@link lnrpc.GetRecoveryInfoRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @static
         * @param {lnrpc.GetRecoveryInfoRequest.$Properties} message GetRecoveryInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetRecoveryInfoRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetRecoveryInfoRequest message, length delimited. Does not implicitly {@link lnrpc.GetRecoveryInfoRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @static
         * @param {lnrpc.GetRecoveryInfoRequest.$Properties} message GetRecoveryInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetRecoveryInfoRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetRecoveryInfoRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GetRecoveryInfoRequest & lnrpc.GetRecoveryInfoRequest.$Shape} GetRecoveryInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetRecoveryInfoRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GetRecoveryInfoRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetRecoveryInfoRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GetRecoveryInfoRequest & lnrpc.GetRecoveryInfoRequest.$Shape} GetRecoveryInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetRecoveryInfoRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetRecoveryInfoRequest message.
         * @function verify
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetRecoveryInfoRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a GetRecoveryInfoRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GetRecoveryInfoRequest} GetRecoveryInfoRequest
         */
        GetRecoveryInfoRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GetRecoveryInfoRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GetRecoveryInfoRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.GetRecoveryInfoRequest();
        };

        /**
         * Creates a plain object from a GetRecoveryInfoRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @static
         * @param {lnrpc.GetRecoveryInfoRequest} message GetRecoveryInfoRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetRecoveryInfoRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this GetRecoveryInfoRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetRecoveryInfoRequest.prototype.toJSON = function() {
            return GetRecoveryInfoRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetRecoveryInfoRequest
         * @function getTypeUrl
         * @memberof lnrpc.GetRecoveryInfoRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetRecoveryInfoRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GetRecoveryInfoRequest";
        };

        return GetRecoveryInfoRequest;
    })();

    lnrpc.GetRecoveryInfoResponse = (function() {

        /**
         * Properties of a GetRecoveryInfoResponse.
         * @typedef {Object} lnrpc.GetRecoveryInfoResponse.$Properties
         * @property {boolean|null} [recovery_mode] GetRecoveryInfoResponse recovery_mode
         * @property {boolean|null} [recovery_finished] GetRecoveryInfoResponse recovery_finished
         * @property {number|null} [progress] GetRecoveryInfoResponse progress
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetRecoveryInfoResponse.
         * @memberof lnrpc
         * @interface IGetRecoveryInfoResponse
         * @augments lnrpc.GetRecoveryInfoResponse.$Properties
         * @deprecated Use lnrpc.GetRecoveryInfoResponse.$Properties instead.
         */

        /**
         * Shape of a GetRecoveryInfoResponse.
         * @typedef {lnrpc.GetRecoveryInfoResponse.$Properties} lnrpc.GetRecoveryInfoResponse.$Shape
         */

        /**
         * Constructs a new GetRecoveryInfoResponse.
         * @memberof lnrpc
         * @classdesc Represents a GetRecoveryInfoResponse.
         * @constructor
         * @param {lnrpc.GetRecoveryInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetRecoveryInfoResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetRecoveryInfoResponse recovery_mode.
         * @member {boolean} recovery_mode
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @instance
         */
        GetRecoveryInfoResponse.prototype.recovery_mode = false;

        /**
         * GetRecoveryInfoResponse recovery_finished.
         * @member {boolean} recovery_finished
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @instance
         */
        GetRecoveryInfoResponse.prototype.recovery_finished = false;

        /**
         * GetRecoveryInfoResponse progress.
         * @member {number} progress
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @instance
         */
        GetRecoveryInfoResponse.prototype.progress = 0;

        /**
         * Creates a new GetRecoveryInfoResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @static
         * @param {lnrpc.GetRecoveryInfoResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GetRecoveryInfoResponse} GetRecoveryInfoResponse instance
         * @type {{
         *   (properties: lnrpc.GetRecoveryInfoResponse.$Shape): lnrpc.GetRecoveryInfoResponse & lnrpc.GetRecoveryInfoResponse.$Shape;
         *   (properties?: lnrpc.GetRecoveryInfoResponse.$Properties): lnrpc.GetRecoveryInfoResponse;
         * }}
         */
        GetRecoveryInfoResponse.create = function(properties) {
            return new GetRecoveryInfoResponse(properties);
        };

        /**
         * Encodes the specified GetRecoveryInfoResponse message. Does not implicitly {@link lnrpc.GetRecoveryInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @static
         * @param {lnrpc.GetRecoveryInfoResponse.$Properties} message GetRecoveryInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetRecoveryInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.recovery_mode != null && $Object.hasOwnProperty.call(message, "recovery_mode") && message.recovery_mode !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.recovery_mode);
            if (message.recovery_finished != null && $Object.hasOwnProperty.call(message, "recovery_finished") && message.recovery_finished !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.recovery_finished);
            if (message.progress != null && $Object.hasOwnProperty.call(message, "progress") && !$Object.is(message.progress, 0))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.progress);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetRecoveryInfoResponse message, length delimited. Does not implicitly {@link lnrpc.GetRecoveryInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @static
         * @param {lnrpc.GetRecoveryInfoResponse.$Properties} message GetRecoveryInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetRecoveryInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetRecoveryInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GetRecoveryInfoResponse & lnrpc.GetRecoveryInfoResponse.$Shape} GetRecoveryInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetRecoveryInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GetRecoveryInfoResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.recovery_mode = value;
                        else
                            delete message.recovery_mode;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.recovery_finished = value;
                        else
                            delete message.recovery_finished;
                        continue;
                    }
                case 3: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.progress = value;
                        else
                            delete message.progress;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetRecoveryInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GetRecoveryInfoResponse & lnrpc.GetRecoveryInfoResponse.$Shape} GetRecoveryInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetRecoveryInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetRecoveryInfoResponse message.
         * @function verify
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetRecoveryInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.recovery_mode != null && $Object.hasOwnProperty.call(message, "recovery_mode"))
                if (typeof message.recovery_mode !== "boolean")
                    return "recovery_mode: boolean expected";
            if (message.recovery_finished != null && $Object.hasOwnProperty.call(message, "recovery_finished"))
                if (typeof message.recovery_finished !== "boolean")
                    return "recovery_finished: boolean expected";
            if (message.progress != null && $Object.hasOwnProperty.call(message, "progress"))
                if (typeof message.progress !== "number")
                    return "progress: number expected";
            return null;
        };

        /**
         * Creates a GetRecoveryInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GetRecoveryInfoResponse} GetRecoveryInfoResponse
         */
        GetRecoveryInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GetRecoveryInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GetRecoveryInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.GetRecoveryInfoResponse();
            if (object.recovery_mode != null)
                if (object.recovery_mode)
                    message.recovery_mode = $Boolean(object.recovery_mode);
            if (object.recovery_finished != null)
                if (object.recovery_finished)
                    message.recovery_finished = $Boolean(object.recovery_finished);
            if (object.progress != null)
                if (!$Object.is($Number(object.progress), 0))
                    message.progress = $Number(object.progress);
            return message;
        };

        /**
         * Creates a plain object from a GetRecoveryInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @static
         * @param {lnrpc.GetRecoveryInfoResponse} message GetRecoveryInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetRecoveryInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.recovery_mode = false;
                object.recovery_finished = false;
                object.progress = 0;
            }
            if (message.recovery_mode != null && $Object.hasOwnProperty.call(message, "recovery_mode"))
                object.recovery_mode = message.recovery_mode;
            if (message.recovery_finished != null && $Object.hasOwnProperty.call(message, "recovery_finished"))
                object.recovery_finished = message.recovery_finished;
            if (message.progress != null && $Object.hasOwnProperty.call(message, "progress"))
                object.progress = options.json && !$isFinite(message.progress) ? $String(message.progress) : message.progress;
            return object;
        };

        /**
         * Converts this GetRecoveryInfoResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetRecoveryInfoResponse.prototype.toJSON = function() {
            return GetRecoveryInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetRecoveryInfoResponse
         * @function getTypeUrl
         * @memberof lnrpc.GetRecoveryInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetRecoveryInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GetRecoveryInfoResponse";
        };

        return GetRecoveryInfoResponse;
    })();

    lnrpc.Chain = (function() {

        /**
         * Properties of a Chain.
         * @typedef {Object} lnrpc.Chain.$Properties
         * @property {string|null} [chain] Chain chain
         * @property {string|null} [network] Chain network
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Chain.
         * @memberof lnrpc
         * @interface IChain
         * @augments lnrpc.Chain.$Properties
         * @deprecated Use lnrpc.Chain.$Properties instead.
         */

        /**
         * Shape of a Chain.
         * @typedef {lnrpc.Chain.$Properties} lnrpc.Chain.$Shape
         */

        /**
         * Constructs a new Chain.
         * @memberof lnrpc
         * @classdesc Represents a Chain.
         * @constructor
         * @param {lnrpc.Chain.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Chain = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Chain chain.
         * @member {string} chain
         * @memberof lnrpc.Chain
         * @instance
         */
        Chain.prototype.chain = "";

        /**
         * Chain network.
         * @member {string} network
         * @memberof lnrpc.Chain
         * @instance
         */
        Chain.prototype.network = "";

        /**
         * Creates a new Chain instance using the specified properties.
         * @function create
         * @memberof lnrpc.Chain
         * @static
         * @param {lnrpc.Chain.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Chain} Chain instance
         * @type {{
         *   (properties: lnrpc.Chain.$Shape): lnrpc.Chain & lnrpc.Chain.$Shape;
         *   (properties?: lnrpc.Chain.$Properties): lnrpc.Chain;
         * }}
         */
        Chain.create = function(properties) {
            return new Chain(properties);
        };

        /**
         * Encodes the specified Chain message. Does not implicitly {@link lnrpc.Chain.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Chain
         * @static
         * @param {lnrpc.Chain.$Properties} message Chain message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Chain.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chain != null && $Object.hasOwnProperty.call(message, "chain") && message.chain !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.network != null && $Object.hasOwnProperty.call(message, "network") && message.network !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.network);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Chain message, length delimited. Does not implicitly {@link lnrpc.Chain.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Chain
         * @static
         * @param {lnrpc.Chain.$Properties} message Chain message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Chain.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Chain message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Chain
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Chain & lnrpc.Chain.$Shape} Chain
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Chain.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Chain(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.chain = value;
                        else
                            delete message.chain;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.network = value;
                        else
                            delete message.network;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Chain message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Chain
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Chain & lnrpc.Chain.$Shape} Chain
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Chain.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Chain message.
         * @function verify
         * @memberof lnrpc.Chain
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Chain.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chain != null && $Object.hasOwnProperty.call(message, "chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.network != null && $Object.hasOwnProperty.call(message, "network"))
                if (!$util.isString(message.network))
                    return "network: string expected";
            return null;
        };

        /**
         * Creates a Chain message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Chain
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Chain} Chain
         */
        Chain.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Chain)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Chain: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Chain();
            if (object.chain != null)
                if (typeof object.chain !== "string" || object.chain.length)
                    message.chain = $String(object.chain);
            if (object.network != null)
                if (typeof object.network !== "string" || object.network.length)
                    message.network = $String(object.network);
            return message;
        };

        /**
         * Creates a plain object from a Chain message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Chain
         * @static
         * @param {lnrpc.Chain} message Chain
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Chain.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.chain = "";
                object.network = "";
            }
            if (message.chain != null && $Object.hasOwnProperty.call(message, "chain"))
                object.chain = message.chain;
            if (message.network != null && $Object.hasOwnProperty.call(message, "network"))
                object.network = message.network;
            return object;
        };

        /**
         * Converts this Chain to JSON.
         * @function toJSON
         * @memberof lnrpc.Chain
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Chain.prototype.toJSON = function() {
            return Chain.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Chain
         * @function getTypeUrl
         * @memberof lnrpc.Chain
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Chain.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Chain";
        };

        return Chain;
    })();

    lnrpc.ChannelOpenUpdate = (function() {

        /**
         * Properties of a ChannelOpenUpdate.
         * @typedef {Object} lnrpc.ChannelOpenUpdate.$Properties
         * @property {lnrpc.ChannelPoint.$Properties|null} [channel_point] ChannelOpenUpdate channel_point
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelOpenUpdate.
         * @memberof lnrpc
         * @interface IChannelOpenUpdate
         * @augments lnrpc.ChannelOpenUpdate.$Properties
         * @deprecated Use lnrpc.ChannelOpenUpdate.$Properties instead.
         */

        /**
         * Shape of a ChannelOpenUpdate.
         * @typedef {{
         *   channel_point?: lnrpc.ChannelPoint.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.ChannelOpenUpdate.$Shape
         */

        /**
         * Constructs a new ChannelOpenUpdate.
         * @memberof lnrpc
         * @classdesc Represents a ChannelOpenUpdate.
         * @constructor
         * @param {lnrpc.ChannelOpenUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelOpenUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelOpenUpdate channel_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} channel_point
         * @memberof lnrpc.ChannelOpenUpdate
         * @instance
         */
        ChannelOpenUpdate.prototype.channel_point = null;

        /**
         * Creates a new ChannelOpenUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelOpenUpdate
         * @static
         * @param {lnrpc.ChannelOpenUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelOpenUpdate} ChannelOpenUpdate instance
         * @type {{
         *   (properties: lnrpc.ChannelOpenUpdate.$Shape): lnrpc.ChannelOpenUpdate & lnrpc.ChannelOpenUpdate.$Shape;
         *   (properties?: lnrpc.ChannelOpenUpdate.$Properties): lnrpc.ChannelOpenUpdate;
         * }}
         */
        ChannelOpenUpdate.create = function(properties) {
            return new ChannelOpenUpdate(properties);
        };

        /**
         * Encodes the specified ChannelOpenUpdate message. Does not implicitly {@link lnrpc.ChannelOpenUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelOpenUpdate
         * @static
         * @param {lnrpc.ChannelOpenUpdate.$Properties} message ChannelOpenUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelOpenUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                $root.lnrpc.ChannelPoint.encode(message.channel_point, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelOpenUpdate message, length delimited. Does not implicitly {@link lnrpc.ChannelOpenUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelOpenUpdate
         * @static
         * @param {lnrpc.ChannelOpenUpdate.$Properties} message ChannelOpenUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelOpenUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelOpenUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelOpenUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelOpenUpdate & lnrpc.ChannelOpenUpdate.$Shape} ChannelOpenUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelOpenUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelOpenUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.channel_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel_point);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelOpenUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelOpenUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelOpenUpdate & lnrpc.ChannelOpenUpdate.$Shape} ChannelOpenUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelOpenUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelOpenUpdate message.
         * @function verify
         * @memberof lnrpc.ChannelOpenUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelOpenUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.channel_point, _depth + 1);
                if (error)
                    return "channel_point." + error;
            }
            return null;
        };

        /**
         * Creates a ChannelOpenUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelOpenUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelOpenUpdate} ChannelOpenUpdate
         */
        ChannelOpenUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelOpenUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelOpenUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelOpenUpdate();
            if (object.channel_point != null) {
                if (!$util.isObject(object.channel_point))
                    throw $TypeError(".lnrpc.ChannelOpenUpdate.channel_point: object expected");
                message.channel_point = $root.lnrpc.ChannelPoint.fromObject(object.channel_point, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ChannelOpenUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelOpenUpdate
         * @static
         * @param {lnrpc.ChannelOpenUpdate} message ChannelOpenUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelOpenUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.channel_point = null;
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                object.channel_point = $root.lnrpc.ChannelPoint.toObject(message.channel_point, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ChannelOpenUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelOpenUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelOpenUpdate.prototype.toJSON = function() {
            return ChannelOpenUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelOpenUpdate
         * @function getTypeUrl
         * @memberof lnrpc.ChannelOpenUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelOpenUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelOpenUpdate";
        };

        return ChannelOpenUpdate;
    })();

    lnrpc.CloseOutput = (function() {

        /**
         * Properties of a CloseOutput.
         * @typedef {Object} lnrpc.CloseOutput.$Properties
         * @property {Long|null} [amount_sat] CloseOutput amount_sat
         * @property {Uint8Array|null} [pk_script] CloseOutput pk_script
         * @property {boolean|null} [is_local] CloseOutput is_local
         * @property {Uint8Array|null} [custom_channel_data] CloseOutput custom_channel_data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CloseOutput.
         * @memberof lnrpc
         * @interface ICloseOutput
         * @augments lnrpc.CloseOutput.$Properties
         * @deprecated Use lnrpc.CloseOutput.$Properties instead.
         */

        /**
         * Shape of a CloseOutput.
         * @typedef {lnrpc.CloseOutput.$Properties} lnrpc.CloseOutput.$Shape
         */

        /**
         * Constructs a new CloseOutput.
         * @memberof lnrpc
         * @classdesc Represents a CloseOutput.
         * @constructor
         * @param {lnrpc.CloseOutput.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CloseOutput = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CloseOutput amount_sat.
         * @member {Long} amount_sat
         * @memberof lnrpc.CloseOutput
         * @instance
         */
        CloseOutput.prototype.amount_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CloseOutput pk_script.
         * @member {Uint8Array} pk_script
         * @memberof lnrpc.CloseOutput
         * @instance
         */
        CloseOutput.prototype.pk_script = $util.newBuffer([]);

        /**
         * CloseOutput is_local.
         * @member {boolean} is_local
         * @memberof lnrpc.CloseOutput
         * @instance
         */
        CloseOutput.prototype.is_local = false;

        /**
         * CloseOutput custom_channel_data.
         * @member {Uint8Array} custom_channel_data
         * @memberof lnrpc.CloseOutput
         * @instance
         */
        CloseOutput.prototype.custom_channel_data = $util.newBuffer([]);

        /**
         * Creates a new CloseOutput instance using the specified properties.
         * @function create
         * @memberof lnrpc.CloseOutput
         * @static
         * @param {lnrpc.CloseOutput.$Properties=} [properties] Properties to set
         * @returns {lnrpc.CloseOutput} CloseOutput instance
         * @type {{
         *   (properties: lnrpc.CloseOutput.$Shape): lnrpc.CloseOutput & lnrpc.CloseOutput.$Shape;
         *   (properties?: lnrpc.CloseOutput.$Properties): lnrpc.CloseOutput;
         * }}
         */
        CloseOutput.create = function(properties) {
            return new CloseOutput(properties);
        };

        /**
         * Encodes the specified CloseOutput message. Does not implicitly {@link lnrpc.CloseOutput.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.CloseOutput
         * @static
         * @param {lnrpc.CloseOutput.$Properties} message CloseOutput message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CloseOutput.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat") && (typeof message.amount_sat === "object" ? message.amount_sat.low || message.amount_sat.high : message.amount_sat !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.amount_sat);
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script") && message.pk_script.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.pk_script);
            if (message.is_local != null && $Object.hasOwnProperty.call(message, "is_local") && message.is_local !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.is_local);
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data") && message.custom_channel_data.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.custom_channel_data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CloseOutput message, length delimited. Does not implicitly {@link lnrpc.CloseOutput.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.CloseOutput
         * @static
         * @param {lnrpc.CloseOutput.$Properties} message CloseOutput message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CloseOutput.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CloseOutput message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.CloseOutput
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.CloseOutput & lnrpc.CloseOutput.$Shape} CloseOutput
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CloseOutput.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.CloseOutput(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount_sat = value;
                        else
                            delete message.amount_sat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pk_script = value;
                        else
                            delete message.pk_script;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_local = value;
                        else
                            delete message.is_local;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.custom_channel_data = value;
                        else
                            delete message.custom_channel_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CloseOutput message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.CloseOutput
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.CloseOutput & lnrpc.CloseOutput.$Shape} CloseOutput
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CloseOutput.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CloseOutput message.
         * @function verify
         * @memberof lnrpc.CloseOutput
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CloseOutput.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat"))
                if (!$util.isInteger(message.amount_sat) && !(message.amount_sat && $util.isInteger(message.amount_sat.low) && $util.isInteger(message.amount_sat.high)))
                    return "amount_sat: integer|Long expected";
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                if (!(message.pk_script && typeof message.pk_script.length === "number" || $util.isString(message.pk_script)))
                    return "pk_script: buffer expected";
            if (message.is_local != null && $Object.hasOwnProperty.call(message, "is_local"))
                if (typeof message.is_local !== "boolean")
                    return "is_local: boolean expected";
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                if (!(message.custom_channel_data && typeof message.custom_channel_data.length === "number" || $util.isString(message.custom_channel_data)))
                    return "custom_channel_data: buffer expected";
            return null;
        };

        /**
         * Creates a CloseOutput message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.CloseOutput
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.CloseOutput} CloseOutput
         */
        CloseOutput.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.CloseOutput)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.CloseOutput: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.CloseOutput();
            if (object.amount_sat != null)
                if (typeof object.amount_sat === "object" ? object.amount_sat.low || object.amount_sat.high : $Number(object.amount_sat) !== 0)
                    if ($util.Long)
                        message.amount_sat = $util.Long.fromValue(object.amount_sat, false);
                    else if (typeof object.amount_sat === "string")
                        message.amount_sat = $parseInt(object.amount_sat, 10);
                    else if (typeof object.amount_sat === "number")
                        message.amount_sat = object.amount_sat;
                    else if (typeof object.amount_sat === "object")
                        message.amount_sat = new $util.LongBits(object.amount_sat.low >>> 0, object.amount_sat.high >>> 0).toNumber();
            if (object.pk_script != null)
                if (object.pk_script.length)
                    if (typeof object.pk_script === "string")
                        $util.base64.decode(object.pk_script, message.pk_script = $util.newBuffer($util.base64.length(object.pk_script)), 0);
                    else if (object.pk_script.length >= 0)
                        message.pk_script = object.pk_script;
            if (object.is_local != null)
                if (object.is_local)
                    message.is_local = $Boolean(object.is_local);
            if (object.custom_channel_data != null)
                if (object.custom_channel_data.length)
                    if (typeof object.custom_channel_data === "string")
                        $util.base64.decode(object.custom_channel_data, message.custom_channel_data = $util.newBuffer($util.base64.length(object.custom_channel_data)), 0);
                    else if (object.custom_channel_data.length >= 0)
                        message.custom_channel_data = object.custom_channel_data;
            return message;
        };

        /**
         * Creates a plain object from a CloseOutput message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.CloseOutput
         * @static
         * @param {lnrpc.CloseOutput} message CloseOutput
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CloseOutput.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.pk_script = "";
                else {
                    object.pk_script = [];
                    if (options.bytes !== $Array)
                        object.pk_script = $util.newBuffer(object.pk_script);
                }
                object.is_local = false;
                if (options.bytes === $String)
                    object.custom_channel_data = "";
                else {
                    object.custom_channel_data = [];
                    if (options.bytes !== $Array)
                        object.custom_channel_data = $util.newBuffer(object.custom_channel_data);
                }
            }
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount_sat = typeof message.amount_sat === "number" ? $BigInt(message.amount_sat) : $util.Long.fromBits(message.amount_sat.low >>> 0, message.amount_sat.high >>> 0, false).toBigInt();
                else if (typeof message.amount_sat === "number")
                    object.amount_sat = options.longs === $String ? $String(message.amount_sat) : message.amount_sat;
                else
                    object.amount_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.amount_sat) : options.longs === $Number ? new $util.LongBits(message.amount_sat.low >>> 0, message.amount_sat.high >>> 0).toNumber() : message.amount_sat;
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                object.pk_script = options.bytes === $String ? $util.base64.encode(message.pk_script, 0, message.pk_script.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pk_script) : message.pk_script;
            if (message.is_local != null && $Object.hasOwnProperty.call(message, "is_local"))
                object.is_local = message.is_local;
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                object.custom_channel_data = options.bytes === $String ? $util.base64.encode(message.custom_channel_data, 0, message.custom_channel_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_channel_data) : message.custom_channel_data;
            return object;
        };

        /**
         * Converts this CloseOutput to JSON.
         * @function toJSON
         * @memberof lnrpc.CloseOutput
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CloseOutput.prototype.toJSON = function() {
            return CloseOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CloseOutput
         * @function getTypeUrl
         * @memberof lnrpc.CloseOutput
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CloseOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.CloseOutput";
        };

        return CloseOutput;
    })();

    lnrpc.ChannelCloseUpdate = (function() {

        /**
         * Properties of a ChannelCloseUpdate.
         * @typedef {Object} lnrpc.ChannelCloseUpdate.$Properties
         * @property {Uint8Array|null} [closing_txid] ChannelCloseUpdate closing_txid
         * @property {boolean|null} [success] ChannelCloseUpdate success
         * @property {lnrpc.CloseOutput.$Properties|null} [local_close_output] ChannelCloseUpdate local_close_output
         * @property {lnrpc.CloseOutput.$Properties|null} [remote_close_output] ChannelCloseUpdate remote_close_output
         * @property {Array.<lnrpc.CloseOutput.$Properties>|null} [additional_outputs] ChannelCloseUpdate additional_outputs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelCloseUpdate.
         * @memberof lnrpc
         * @interface IChannelCloseUpdate
         * @augments lnrpc.ChannelCloseUpdate.$Properties
         * @deprecated Use lnrpc.ChannelCloseUpdate.$Properties instead.
         */

        /**
         * Shape of a ChannelCloseUpdate.
         * @typedef {lnrpc.ChannelCloseUpdate.$Properties} lnrpc.ChannelCloseUpdate.$Shape
         */

        /**
         * Constructs a new ChannelCloseUpdate.
         * @memberof lnrpc
         * @classdesc Represents a ChannelCloseUpdate.
         * @constructor
         * @param {lnrpc.ChannelCloseUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelCloseUpdate = function (properties) {
            this.additional_outputs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelCloseUpdate closing_txid.
         * @member {Uint8Array} closing_txid
         * @memberof lnrpc.ChannelCloseUpdate
         * @instance
         */
        ChannelCloseUpdate.prototype.closing_txid = $util.newBuffer([]);

        /**
         * ChannelCloseUpdate success.
         * @member {boolean} success
         * @memberof lnrpc.ChannelCloseUpdate
         * @instance
         */
        ChannelCloseUpdate.prototype.success = false;

        /**
         * ChannelCloseUpdate local_close_output.
         * @member {lnrpc.CloseOutput.$Properties|null|undefined} local_close_output
         * @memberof lnrpc.ChannelCloseUpdate
         * @instance
         */
        ChannelCloseUpdate.prototype.local_close_output = null;

        /**
         * ChannelCloseUpdate remote_close_output.
         * @member {lnrpc.CloseOutput.$Properties|null|undefined} remote_close_output
         * @memberof lnrpc.ChannelCloseUpdate
         * @instance
         */
        ChannelCloseUpdate.prototype.remote_close_output = null;

        /**
         * ChannelCloseUpdate additional_outputs.
         * @member {Array.<lnrpc.CloseOutput.$Properties>} additional_outputs
         * @memberof lnrpc.ChannelCloseUpdate
         * @instance
         */
        ChannelCloseUpdate.prototype.additional_outputs = $util.emptyArray;

        /**
         * Creates a new ChannelCloseUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelCloseUpdate
         * @static
         * @param {lnrpc.ChannelCloseUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelCloseUpdate} ChannelCloseUpdate instance
         * @type {{
         *   (properties: lnrpc.ChannelCloseUpdate.$Shape): lnrpc.ChannelCloseUpdate & lnrpc.ChannelCloseUpdate.$Shape;
         *   (properties?: lnrpc.ChannelCloseUpdate.$Properties): lnrpc.ChannelCloseUpdate;
         * }}
         */
        ChannelCloseUpdate.create = function(properties) {
            return new ChannelCloseUpdate(properties);
        };

        /**
         * Encodes the specified ChannelCloseUpdate message. Does not implicitly {@link lnrpc.ChannelCloseUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelCloseUpdate
         * @static
         * @param {lnrpc.ChannelCloseUpdate.$Properties} message ChannelCloseUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelCloseUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid") && message.closing_txid.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.closing_txid);
            if (message.success != null && $Object.hasOwnProperty.call(message, "success") && message.success !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.success);
            if (message.local_close_output != null && $Object.hasOwnProperty.call(message, "local_close_output"))
                $root.lnrpc.CloseOutput.encode(message.local_close_output, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.remote_close_output != null && $Object.hasOwnProperty.call(message, "remote_close_output"))
                $root.lnrpc.CloseOutput.encode(message.remote_close_output, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.additional_outputs != null && message.additional_outputs.length)
                for (let i = 0; i < message.additional_outputs.length; ++i)
                    $root.lnrpc.CloseOutput.encode(message.additional_outputs[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelCloseUpdate message, length delimited. Does not implicitly {@link lnrpc.ChannelCloseUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelCloseUpdate
         * @static
         * @param {lnrpc.ChannelCloseUpdate.$Properties} message ChannelCloseUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelCloseUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelCloseUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelCloseUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelCloseUpdate & lnrpc.ChannelCloseUpdate.$Shape} ChannelCloseUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelCloseUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelCloseUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.closing_txid = value;
                        else
                            delete message.closing_txid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.success = value;
                        else
                            delete message.success;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.local_close_output = $root.lnrpc.CloseOutput.decode(reader, reader.uint32(), $undefined, _depth + 1, message.local_close_output);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.remote_close_output = $root.lnrpc.CloseOutput.decode(reader, reader.uint32(), $undefined, _depth + 1, message.remote_close_output);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.additional_outputs && message.additional_outputs.length))
                            message.additional_outputs = [];
                        message.additional_outputs.push($root.lnrpc.CloseOutput.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelCloseUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelCloseUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelCloseUpdate & lnrpc.ChannelCloseUpdate.$Shape} ChannelCloseUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelCloseUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelCloseUpdate message.
         * @function verify
         * @memberof lnrpc.ChannelCloseUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelCloseUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid"))
                if (!(message.closing_txid && typeof message.closing_txid.length === "number" || $util.isString(message.closing_txid)))
                    return "closing_txid: buffer expected";
            if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                if (typeof message.success !== "boolean")
                    return "success: boolean expected";
            if (message.local_close_output != null && $Object.hasOwnProperty.call(message, "local_close_output")) {
                let error = $root.lnrpc.CloseOutput.verify(message.local_close_output, _depth + 1);
                if (error)
                    return "local_close_output." + error;
            }
            if (message.remote_close_output != null && $Object.hasOwnProperty.call(message, "remote_close_output")) {
                let error = $root.lnrpc.CloseOutput.verify(message.remote_close_output, _depth + 1);
                if (error)
                    return "remote_close_output." + error;
            }
            if (message.additional_outputs != null && $Object.hasOwnProperty.call(message, "additional_outputs")) {
                if (!$Array.isArray(message.additional_outputs))
                    return "additional_outputs: array expected";
                for (let i = 0; i < message.additional_outputs.length; ++i) {
                    let error = $root.lnrpc.CloseOutput.verify(message.additional_outputs[i], _depth + 1);
                    if (error)
                        return "additional_outputs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ChannelCloseUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelCloseUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelCloseUpdate} ChannelCloseUpdate
         */
        ChannelCloseUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelCloseUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelCloseUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelCloseUpdate();
            if (object.closing_txid != null)
                if (object.closing_txid.length)
                    if (typeof object.closing_txid === "string")
                        $util.base64.decode(object.closing_txid, message.closing_txid = $util.newBuffer($util.base64.length(object.closing_txid)), 0);
                    else if (object.closing_txid.length >= 0)
                        message.closing_txid = object.closing_txid;
            if (object.success != null)
                if (object.success)
                    message.success = $Boolean(object.success);
            if (object.local_close_output != null) {
                if (!$util.isObject(object.local_close_output))
                    throw $TypeError(".lnrpc.ChannelCloseUpdate.local_close_output: object expected");
                message.local_close_output = $root.lnrpc.CloseOutput.fromObject(object.local_close_output, _depth + 1);
            }
            if (object.remote_close_output != null) {
                if (!$util.isObject(object.remote_close_output))
                    throw $TypeError(".lnrpc.ChannelCloseUpdate.remote_close_output: object expected");
                message.remote_close_output = $root.lnrpc.CloseOutput.fromObject(object.remote_close_output, _depth + 1);
            }
            if (object.additional_outputs) {
                if (!$Array.isArray(object.additional_outputs))
                    throw $TypeError(".lnrpc.ChannelCloseUpdate.additional_outputs: array expected");
                message.additional_outputs = $Array(object.additional_outputs.length);
                for (let i = 0; i < object.additional_outputs.length; ++i) {
                    if (!$util.isObject(object.additional_outputs[i]))
                        throw $TypeError(".lnrpc.ChannelCloseUpdate.additional_outputs: object expected");
                    message.additional_outputs[i] = $root.lnrpc.CloseOutput.fromObject(object.additional_outputs[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ChannelCloseUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelCloseUpdate
         * @static
         * @param {lnrpc.ChannelCloseUpdate} message ChannelCloseUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelCloseUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.additional_outputs = [];
            if (options.defaults) {
                if (options.bytes === $String)
                    object.closing_txid = "";
                else {
                    object.closing_txid = [];
                    if (options.bytes !== $Array)
                        object.closing_txid = $util.newBuffer(object.closing_txid);
                }
                object.success = false;
                object.local_close_output = null;
                object.remote_close_output = null;
            }
            if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid"))
                object.closing_txid = options.bytes === $String ? $util.base64.encode(message.closing_txid, 0, message.closing_txid.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.closing_txid) : message.closing_txid;
            if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                object.success = message.success;
            if (message.local_close_output != null && $Object.hasOwnProperty.call(message, "local_close_output"))
                object.local_close_output = $root.lnrpc.CloseOutput.toObject(message.local_close_output, options, _depth + 1);
            if (message.remote_close_output != null && $Object.hasOwnProperty.call(message, "remote_close_output"))
                object.remote_close_output = $root.lnrpc.CloseOutput.toObject(message.remote_close_output, options, _depth + 1);
            if (message.additional_outputs && message.additional_outputs.length) {
                object.additional_outputs = $Array(message.additional_outputs.length);
                for (let j = 0; j < message.additional_outputs.length; ++j)
                    object.additional_outputs[j] = $root.lnrpc.CloseOutput.toObject(message.additional_outputs[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ChannelCloseUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelCloseUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelCloseUpdate.prototype.toJSON = function() {
            return ChannelCloseUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelCloseUpdate
         * @function getTypeUrl
         * @memberof lnrpc.ChannelCloseUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelCloseUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelCloseUpdate";
        };

        return ChannelCloseUpdate;
    })();

    lnrpc.CloseChannelRequest = (function() {

        /**
         * Properties of a CloseChannelRequest.
         * @typedef {Object} lnrpc.CloseChannelRequest.$Properties
         * @property {lnrpc.ChannelPoint.$Properties|null} [channel_point] CloseChannelRequest channel_point
         * @property {boolean|null} [force] CloseChannelRequest force
         * @property {number|null} [target_conf] CloseChannelRequest target_conf
         * @property {Long|null} [sat_per_byte] CloseChannelRequest sat_per_byte
         * @property {string|null} [delivery_address] CloseChannelRequest delivery_address
         * @property {Long|null} [sat_per_vbyte] CloseChannelRequest sat_per_vbyte
         * @property {Long|null} [max_fee_per_vbyte] CloseChannelRequest max_fee_per_vbyte
         * @property {boolean|null} [no_wait] CloseChannelRequest no_wait
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CloseChannelRequest.
         * @memberof lnrpc
         * @interface ICloseChannelRequest
         * @augments lnrpc.CloseChannelRequest.$Properties
         * @deprecated Use lnrpc.CloseChannelRequest.$Properties instead.
         */

        /**
         * Shape of a CloseChannelRequest.
         * @typedef {{
         *   channel_point?: lnrpc.ChannelPoint.$Shape|null;
         *   force?: boolean|null;
         *   target_conf?: number|null;
         *   sat_per_byte?: Long|null;
         *   delivery_address?: string|null;
         *   sat_per_vbyte?: Long|null;
         *   max_fee_per_vbyte?: Long|null;
         *   no_wait?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.CloseChannelRequest.$Shape
         */

        /**
         * Constructs a new CloseChannelRequest.
         * @memberof lnrpc
         * @classdesc Represents a CloseChannelRequest.
         * @constructor
         * @param {lnrpc.CloseChannelRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CloseChannelRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CloseChannelRequest channel_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} channel_point
         * @memberof lnrpc.CloseChannelRequest
         * @instance
         */
        CloseChannelRequest.prototype.channel_point = null;

        /**
         * CloseChannelRequest force.
         * @member {boolean} force
         * @memberof lnrpc.CloseChannelRequest
         * @instance
         */
        CloseChannelRequest.prototype.force = false;

        /**
         * CloseChannelRequest target_conf.
         * @member {number} target_conf
         * @memberof lnrpc.CloseChannelRequest
         * @instance
         */
        CloseChannelRequest.prototype.target_conf = 0;

        /**
         * CloseChannelRequest sat_per_byte.
         * @member {Long} sat_per_byte
         * @memberof lnrpc.CloseChannelRequest
         * @instance
         */
        CloseChannelRequest.prototype.sat_per_byte = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CloseChannelRequest delivery_address.
         * @member {string} delivery_address
         * @memberof lnrpc.CloseChannelRequest
         * @instance
         */
        CloseChannelRequest.prototype.delivery_address = "";

        /**
         * CloseChannelRequest sat_per_vbyte.
         * @member {Long} sat_per_vbyte
         * @memberof lnrpc.CloseChannelRequest
         * @instance
         */
        CloseChannelRequest.prototype.sat_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CloseChannelRequest max_fee_per_vbyte.
         * @member {Long} max_fee_per_vbyte
         * @memberof lnrpc.CloseChannelRequest
         * @instance
         */
        CloseChannelRequest.prototype.max_fee_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CloseChannelRequest no_wait.
         * @member {boolean} no_wait
         * @memberof lnrpc.CloseChannelRequest
         * @instance
         */
        CloseChannelRequest.prototype.no_wait = false;

        /**
         * Creates a new CloseChannelRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.CloseChannelRequest
         * @static
         * @param {lnrpc.CloseChannelRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.CloseChannelRequest} CloseChannelRequest instance
         * @type {{
         *   (properties: lnrpc.CloseChannelRequest.$Shape): lnrpc.CloseChannelRequest & lnrpc.CloseChannelRequest.$Shape;
         *   (properties?: lnrpc.CloseChannelRequest.$Properties): lnrpc.CloseChannelRequest;
         * }}
         */
        CloseChannelRequest.create = function(properties) {
            return new CloseChannelRequest(properties);
        };

        /**
         * Encodes the specified CloseChannelRequest message. Does not implicitly {@link lnrpc.CloseChannelRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.CloseChannelRequest
         * @static
         * @param {lnrpc.CloseChannelRequest.$Properties} message CloseChannelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CloseChannelRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                $root.lnrpc.ChannelPoint.encode(message.channel_point, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.force != null && $Object.hasOwnProperty.call(message, "force") && message.force !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.force);
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf") && message.target_conf !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.target_conf);
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte") && (typeof message.sat_per_byte === "object" ? message.sat_per_byte.low || message.sat_per_byte.high : message.sat_per_byte !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.sat_per_byte);
            if (message.delivery_address != null && $Object.hasOwnProperty.call(message, "delivery_address") && message.delivery_address !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.delivery_address);
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte") && (typeof message.sat_per_vbyte === "object" ? message.sat_per_vbyte.low || message.sat_per_vbyte.high : message.sat_per_vbyte !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.sat_per_vbyte);
            if (message.max_fee_per_vbyte != null && $Object.hasOwnProperty.call(message, "max_fee_per_vbyte") && (typeof message.max_fee_per_vbyte === "object" ? message.max_fee_per_vbyte.low || message.max_fee_per_vbyte.high : message.max_fee_per_vbyte !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.max_fee_per_vbyte);
            if (message.no_wait != null && $Object.hasOwnProperty.call(message, "no_wait") && message.no_wait !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.no_wait);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CloseChannelRequest message, length delimited. Does not implicitly {@link lnrpc.CloseChannelRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.CloseChannelRequest
         * @static
         * @param {lnrpc.CloseChannelRequest.$Properties} message CloseChannelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CloseChannelRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CloseChannelRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.CloseChannelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.CloseChannelRequest & lnrpc.CloseChannelRequest.$Shape} CloseChannelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CloseChannelRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.CloseChannelRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.channel_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel_point);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.force = value;
                        else
                            delete message.force;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.target_conf = value;
                        else
                            delete message.target_conf;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_byte = value;
                        else
                            delete message.sat_per_byte;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.delivery_address = value;
                        else
                            delete message.delivery_address;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_vbyte = value;
                        else
                            delete message.sat_per_vbyte;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_fee_per_vbyte = value;
                        else
                            delete message.max_fee_per_vbyte;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.no_wait = value;
                        else
                            delete message.no_wait;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CloseChannelRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.CloseChannelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.CloseChannelRequest & lnrpc.CloseChannelRequest.$Shape} CloseChannelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CloseChannelRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CloseChannelRequest message.
         * @function verify
         * @memberof lnrpc.CloseChannelRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CloseChannelRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.channel_point, _depth + 1);
                if (error)
                    return "channel_point." + error;
            }
            if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                if (typeof message.force !== "boolean")
                    return "force: boolean expected";
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                if (!$util.isInteger(message.target_conf))
                    return "target_conf: integer expected";
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (!$util.isInteger(message.sat_per_byte) && !(message.sat_per_byte && $util.isInteger(message.sat_per_byte.low) && $util.isInteger(message.sat_per_byte.high)))
                    return "sat_per_byte: integer|Long expected";
            if (message.delivery_address != null && $Object.hasOwnProperty.call(message, "delivery_address"))
                if (!$util.isString(message.delivery_address))
                    return "delivery_address: string expected";
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (!$util.isInteger(message.sat_per_vbyte) && !(message.sat_per_vbyte && $util.isInteger(message.sat_per_vbyte.low) && $util.isInteger(message.sat_per_vbyte.high)))
                    return "sat_per_vbyte: integer|Long expected";
            if (message.max_fee_per_vbyte != null && $Object.hasOwnProperty.call(message, "max_fee_per_vbyte"))
                if (!$util.isInteger(message.max_fee_per_vbyte) && !(message.max_fee_per_vbyte && $util.isInteger(message.max_fee_per_vbyte.low) && $util.isInteger(message.max_fee_per_vbyte.high)))
                    return "max_fee_per_vbyte: integer|Long expected";
            if (message.no_wait != null && $Object.hasOwnProperty.call(message, "no_wait"))
                if (typeof message.no_wait !== "boolean")
                    return "no_wait: boolean expected";
            return null;
        };

        /**
         * Creates a CloseChannelRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.CloseChannelRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.CloseChannelRequest} CloseChannelRequest
         */
        CloseChannelRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.CloseChannelRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.CloseChannelRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.CloseChannelRequest();
            if (object.channel_point != null) {
                if (!$util.isObject(object.channel_point))
                    throw $TypeError(".lnrpc.CloseChannelRequest.channel_point: object expected");
                message.channel_point = $root.lnrpc.ChannelPoint.fromObject(object.channel_point, _depth + 1);
            }
            if (object.force != null)
                if (object.force)
                    message.force = $Boolean(object.force);
            if (object.target_conf != null)
                if ($Number(object.target_conf) !== 0)
                    message.target_conf = object.target_conf | 0;
            if (object.sat_per_byte != null)
                if (typeof object.sat_per_byte === "object" ? object.sat_per_byte.low || object.sat_per_byte.high : $Number(object.sat_per_byte) !== 0)
                    if ($util.Long)
                        message.sat_per_byte = $util.Long.fromValue(object.sat_per_byte, false);
                    else if (typeof object.sat_per_byte === "string")
                        message.sat_per_byte = $parseInt(object.sat_per_byte, 10);
                    else if (typeof object.sat_per_byte === "number")
                        message.sat_per_byte = object.sat_per_byte;
                    else if (typeof object.sat_per_byte === "object")
                        message.sat_per_byte = new $util.LongBits(object.sat_per_byte.low >>> 0, object.sat_per_byte.high >>> 0).toNumber();
            if (object.delivery_address != null)
                if (typeof object.delivery_address !== "string" || object.delivery_address.length)
                    message.delivery_address = $String(object.delivery_address);
            if (object.sat_per_vbyte != null)
                if (typeof object.sat_per_vbyte === "object" ? object.sat_per_vbyte.low || object.sat_per_vbyte.high : $Number(object.sat_per_vbyte) !== 0)
                    if ($util.Long)
                        message.sat_per_vbyte = $util.Long.fromValue(object.sat_per_vbyte, true);
                    else if (typeof object.sat_per_vbyte === "string")
                        message.sat_per_vbyte = $parseInt(object.sat_per_vbyte, 10);
                    else if (typeof object.sat_per_vbyte === "number")
                        message.sat_per_vbyte = object.sat_per_vbyte;
                    else if (typeof object.sat_per_vbyte === "object")
                        message.sat_per_vbyte = new $util.LongBits(object.sat_per_vbyte.low >>> 0, object.sat_per_vbyte.high >>> 0).toNumber(true);
            if (object.max_fee_per_vbyte != null)
                if (typeof object.max_fee_per_vbyte === "object" ? object.max_fee_per_vbyte.low || object.max_fee_per_vbyte.high : $Number(object.max_fee_per_vbyte) !== 0)
                    if ($util.Long)
                        message.max_fee_per_vbyte = $util.Long.fromValue(object.max_fee_per_vbyte, true);
                    else if (typeof object.max_fee_per_vbyte === "string")
                        message.max_fee_per_vbyte = $parseInt(object.max_fee_per_vbyte, 10);
                    else if (typeof object.max_fee_per_vbyte === "number")
                        message.max_fee_per_vbyte = object.max_fee_per_vbyte;
                    else if (typeof object.max_fee_per_vbyte === "object")
                        message.max_fee_per_vbyte = new $util.LongBits(object.max_fee_per_vbyte.low >>> 0, object.max_fee_per_vbyte.high >>> 0).toNumber(true);
            if (object.no_wait != null)
                if (object.no_wait)
                    message.no_wait = $Boolean(object.no_wait);
            return message;
        };

        /**
         * Creates a plain object from a CloseChannelRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.CloseChannelRequest
         * @static
         * @param {lnrpc.CloseChannelRequest} message CloseChannelRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CloseChannelRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.channel_point = null;
                object.force = false;
                object.target_conf = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sat_per_byte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_byte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.delivery_address = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sat_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.max_fee_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.max_fee_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.no_wait = false;
            }
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                object.channel_point = $root.lnrpc.ChannelPoint.toObject(message.channel_point, options, _depth + 1);
            if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                object.force = message.force;
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                object.target_conf = message.target_conf;
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_byte = typeof message.sat_per_byte === "number" ? $BigInt(message.sat_per_byte) : $util.Long.fromBits(message.sat_per_byte.low >>> 0, message.sat_per_byte.high >>> 0, false).toBigInt();
                else if (typeof message.sat_per_byte === "number")
                    object.sat_per_byte = options.longs === $String ? $String(message.sat_per_byte) : message.sat_per_byte;
                else
                    object.sat_per_byte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_byte) : options.longs === $Number ? new $util.LongBits(message.sat_per_byte.low >>> 0, message.sat_per_byte.high >>> 0).toNumber() : message.sat_per_byte;
            if (message.delivery_address != null && $Object.hasOwnProperty.call(message, "delivery_address"))
                object.delivery_address = message.delivery_address;
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_vbyte = typeof message.sat_per_vbyte === "number" ? $BigInt(message.sat_per_vbyte) : $util.Long.fromBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.sat_per_vbyte === "number")
                    object.sat_per_vbyte = options.longs === $String ? $String(message.sat_per_vbyte) : message.sat_per_vbyte;
                else
                    object.sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0).toNumber(true) : message.sat_per_vbyte;
            if (message.max_fee_per_vbyte != null && $Object.hasOwnProperty.call(message, "max_fee_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.max_fee_per_vbyte = typeof message.max_fee_per_vbyte === "number" ? $BigInt(message.max_fee_per_vbyte) : $util.Long.fromBits(message.max_fee_per_vbyte.low >>> 0, message.max_fee_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.max_fee_per_vbyte === "number")
                    object.max_fee_per_vbyte = options.longs === $String ? $String(message.max_fee_per_vbyte) : message.max_fee_per_vbyte;
                else
                    object.max_fee_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.max_fee_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.max_fee_per_vbyte.low >>> 0, message.max_fee_per_vbyte.high >>> 0).toNumber(true) : message.max_fee_per_vbyte;
            if (message.no_wait != null && $Object.hasOwnProperty.call(message, "no_wait"))
                object.no_wait = message.no_wait;
            return object;
        };

        /**
         * Converts this CloseChannelRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.CloseChannelRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CloseChannelRequest.prototype.toJSON = function() {
            return CloseChannelRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CloseChannelRequest
         * @function getTypeUrl
         * @memberof lnrpc.CloseChannelRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CloseChannelRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.CloseChannelRequest";
        };

        return CloseChannelRequest;
    })();

    lnrpc.CloseStatusUpdate = (function() {

        /**
         * Properties of a CloseStatusUpdate.
         * @typedef {Object} lnrpc.CloseStatusUpdate.$Properties
         * @property {lnrpc.PendingUpdate.$Properties|null} [close_pending] CloseStatusUpdate close_pending
         * @property {lnrpc.ChannelCloseUpdate.$Properties|null} [chan_close] CloseStatusUpdate chan_close
         * @property {lnrpc.InstantUpdate.$Properties|null} [close_instant] CloseStatusUpdate close_instant
         * @property {"close_pending"|"chan_close"|"close_instant"} [update] CloseStatusUpdate update
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CloseStatusUpdate.
         * @memberof lnrpc
         * @interface ICloseStatusUpdate
         * @augments lnrpc.CloseStatusUpdate.$Properties
         * @deprecated Use lnrpc.CloseStatusUpdate.$Properties instead.
         */

        /**
         * Narrowed shape of a CloseStatusUpdate.
         * @typedef {{
         *   close_pending?: lnrpc.PendingUpdate.$Shape|null;
         *   chan_close?: lnrpc.ChannelCloseUpdate.$Shape|null;
         *   close_instant?: lnrpc.InstantUpdate.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ update?: undefined; close_pending?: null; chan_close?: null; close_instant?: null }|{ update?: "close_pending"; close_pending: lnrpc.PendingUpdate.$Shape; chan_close?: null; close_instant?: null }|{ update?: "chan_close"; close_pending?: null; chan_close: lnrpc.ChannelCloseUpdate.$Shape; close_instant?: null }|{ update?: "close_instant"; close_pending?: null; chan_close?: null; close_instant: lnrpc.InstantUpdate.$Shape })
         * )} lnrpc.CloseStatusUpdate.$Shape
         */

        /**
         * Constructs a new CloseStatusUpdate.
         * @memberof lnrpc
         * @classdesc Represents a CloseStatusUpdate.
         * @constructor
         * @param {lnrpc.CloseStatusUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CloseStatusUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CloseStatusUpdate close_pending.
         * @member {lnrpc.PendingUpdate.$Properties|null|undefined} close_pending
         * @memberof lnrpc.CloseStatusUpdate
         * @instance
         */
        CloseStatusUpdate.prototype.close_pending = null;

        /**
         * CloseStatusUpdate chan_close.
         * @member {lnrpc.ChannelCloseUpdate.$Properties|null|undefined} chan_close
         * @memberof lnrpc.CloseStatusUpdate
         * @instance
         */
        CloseStatusUpdate.prototype.chan_close = null;

        /**
         * CloseStatusUpdate close_instant.
         * @member {lnrpc.InstantUpdate.$Properties|null|undefined} close_instant
         * @memberof lnrpc.CloseStatusUpdate
         * @instance
         */
        CloseStatusUpdate.prototype.close_instant = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * CloseStatusUpdate update.
         * @member {"close_pending"|"chan_close"|"close_instant"|undefined} update
         * @memberof lnrpc.CloseStatusUpdate
         * @instance
         */
        $Object.defineProperty(CloseStatusUpdate.prototype, "update", {
            get: $util.oneOfGetter($oneOfFields = ["close_pending", "chan_close", "close_instant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CloseStatusUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.CloseStatusUpdate
         * @static
         * @param {lnrpc.CloseStatusUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.CloseStatusUpdate} CloseStatusUpdate instance
         * @type {{
         *   (properties: lnrpc.CloseStatusUpdate.$Shape): lnrpc.CloseStatusUpdate & lnrpc.CloseStatusUpdate.$Shape;
         *   (properties?: lnrpc.CloseStatusUpdate.$Properties): lnrpc.CloseStatusUpdate;
         * }}
         */
        CloseStatusUpdate.create = function(properties) {
            return new CloseStatusUpdate(properties);
        };

        /**
         * Encodes the specified CloseStatusUpdate message. Does not implicitly {@link lnrpc.CloseStatusUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.CloseStatusUpdate
         * @static
         * @param {lnrpc.CloseStatusUpdate.$Properties} message CloseStatusUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CloseStatusUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.close_pending != null && $Object.hasOwnProperty.call(message, "close_pending"))
                $root.lnrpc.PendingUpdate.encode(message.close_pending, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.chan_close != null && $Object.hasOwnProperty.call(message, "chan_close"))
                $root.lnrpc.ChannelCloseUpdate.encode(message.chan_close, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.close_instant != null && $Object.hasOwnProperty.call(message, "close_instant"))
                $root.lnrpc.InstantUpdate.encode(message.close_instant, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CloseStatusUpdate message, length delimited. Does not implicitly {@link lnrpc.CloseStatusUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.CloseStatusUpdate
         * @static
         * @param {lnrpc.CloseStatusUpdate.$Properties} message CloseStatusUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CloseStatusUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CloseStatusUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.CloseStatusUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.CloseStatusUpdate & lnrpc.CloseStatusUpdate.$Shape} CloseStatusUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CloseStatusUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.CloseStatusUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.close_pending = $root.lnrpc.PendingUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.close_pending);
                        message.update = "close_pending";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.chan_close = $root.lnrpc.ChannelCloseUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_close);
                        message.update = "chan_close";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.close_instant = $root.lnrpc.InstantUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.close_instant);
                        message.update = "close_instant";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CloseStatusUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.CloseStatusUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.CloseStatusUpdate & lnrpc.CloseStatusUpdate.$Shape} CloseStatusUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CloseStatusUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CloseStatusUpdate message.
         * @function verify
         * @memberof lnrpc.CloseStatusUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CloseStatusUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.close_pending != null && $Object.hasOwnProperty.call(message, "close_pending")) {
                properties.update = 1;
                {
                    let error = $root.lnrpc.PendingUpdate.verify(message.close_pending, _depth + 1);
                    if (error)
                        return "close_pending." + error;
                }
            }
            if (message.chan_close != null && $Object.hasOwnProperty.call(message, "chan_close")) {
                if (properties.update === 1)
                    return "update: multiple values";
                properties.update = 1;
                {
                    let error = $root.lnrpc.ChannelCloseUpdate.verify(message.chan_close, _depth + 1);
                    if (error)
                        return "chan_close." + error;
                }
            }
            if (message.close_instant != null && $Object.hasOwnProperty.call(message, "close_instant")) {
                if (properties.update === 1)
                    return "update: multiple values";
                properties.update = 1;
                {
                    let error = $root.lnrpc.InstantUpdate.verify(message.close_instant, _depth + 1);
                    if (error)
                        return "close_instant." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CloseStatusUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.CloseStatusUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.CloseStatusUpdate} CloseStatusUpdate
         */
        CloseStatusUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.CloseStatusUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.CloseStatusUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.CloseStatusUpdate();
            if (object.close_pending != null) {
                if (!$util.isObject(object.close_pending))
                    throw $TypeError(".lnrpc.CloseStatusUpdate.close_pending: object expected");
                message.close_pending = $root.lnrpc.PendingUpdate.fromObject(object.close_pending, _depth + 1);
            }
            if (object.chan_close != null) {
                if (!$util.isObject(object.chan_close))
                    throw $TypeError(".lnrpc.CloseStatusUpdate.chan_close: object expected");
                message.chan_close = $root.lnrpc.ChannelCloseUpdate.fromObject(object.chan_close, _depth + 1);
            }
            if (object.close_instant != null) {
                if (!$util.isObject(object.close_instant))
                    throw $TypeError(".lnrpc.CloseStatusUpdate.close_instant: object expected");
                message.close_instant = $root.lnrpc.InstantUpdate.fromObject(object.close_instant, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CloseStatusUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.CloseStatusUpdate
         * @static
         * @param {lnrpc.CloseStatusUpdate} message CloseStatusUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CloseStatusUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.close_pending != null && $Object.hasOwnProperty.call(message, "close_pending")) {
                object.close_pending = $root.lnrpc.PendingUpdate.toObject(message.close_pending, options, _depth + 1);
                if (options.oneofs)
                    object.update = "close_pending";
            }
            if (message.chan_close != null && $Object.hasOwnProperty.call(message, "chan_close")) {
                object.chan_close = $root.lnrpc.ChannelCloseUpdate.toObject(message.chan_close, options, _depth + 1);
                if (options.oneofs)
                    object.update = "chan_close";
            }
            if (message.close_instant != null && $Object.hasOwnProperty.call(message, "close_instant")) {
                object.close_instant = $root.lnrpc.InstantUpdate.toObject(message.close_instant, options, _depth + 1);
                if (options.oneofs)
                    object.update = "close_instant";
            }
            return object;
        };

        /**
         * Converts this CloseStatusUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.CloseStatusUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CloseStatusUpdate.prototype.toJSON = function() {
            return CloseStatusUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CloseStatusUpdate
         * @function getTypeUrl
         * @memberof lnrpc.CloseStatusUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CloseStatusUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.CloseStatusUpdate";
        };

        return CloseStatusUpdate;
    })();

    lnrpc.PendingUpdate = (function() {

        /**
         * Properties of a PendingUpdate.
         * @typedef {Object} lnrpc.PendingUpdate.$Properties
         * @property {Uint8Array|null} [txid] PendingUpdate txid
         * @property {number|null} [output_index] PendingUpdate output_index
         * @property {Long|null} [fee_per_vbyte] PendingUpdate fee_per_vbyte
         * @property {boolean|null} [local_close_tx] PendingUpdate local_close_tx
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PendingUpdate.
         * @memberof lnrpc
         * @interface IPendingUpdate
         * @augments lnrpc.PendingUpdate.$Properties
         * @deprecated Use lnrpc.PendingUpdate.$Properties instead.
         */

        /**
         * Shape of a PendingUpdate.
         * @typedef {lnrpc.PendingUpdate.$Properties} lnrpc.PendingUpdate.$Shape
         */

        /**
         * Constructs a new PendingUpdate.
         * @memberof lnrpc
         * @classdesc Represents a PendingUpdate.
         * @constructor
         * @param {lnrpc.PendingUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PendingUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PendingUpdate txid.
         * @member {Uint8Array} txid
         * @memberof lnrpc.PendingUpdate
         * @instance
         */
        PendingUpdate.prototype.txid = $util.newBuffer([]);

        /**
         * PendingUpdate output_index.
         * @member {number} output_index
         * @memberof lnrpc.PendingUpdate
         * @instance
         */
        PendingUpdate.prototype.output_index = 0;

        /**
         * PendingUpdate fee_per_vbyte.
         * @member {Long} fee_per_vbyte
         * @memberof lnrpc.PendingUpdate
         * @instance
         */
        PendingUpdate.prototype.fee_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PendingUpdate local_close_tx.
         * @member {boolean} local_close_tx
         * @memberof lnrpc.PendingUpdate
         * @instance
         */
        PendingUpdate.prototype.local_close_tx = false;

        /**
         * Creates a new PendingUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.PendingUpdate
         * @static
         * @param {lnrpc.PendingUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PendingUpdate} PendingUpdate instance
         * @type {{
         *   (properties: lnrpc.PendingUpdate.$Shape): lnrpc.PendingUpdate & lnrpc.PendingUpdate.$Shape;
         *   (properties?: lnrpc.PendingUpdate.$Properties): lnrpc.PendingUpdate;
         * }}
         */
        PendingUpdate.create = function(properties) {
            return new PendingUpdate(properties);
        };

        /**
         * Encodes the specified PendingUpdate message. Does not implicitly {@link lnrpc.PendingUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PendingUpdate
         * @static
         * @param {lnrpc.PendingUpdate.$Properties} message PendingUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid") && message.txid.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.txid);
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index") && message.output_index !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.output_index);
            if (message.fee_per_vbyte != null && $Object.hasOwnProperty.call(message, "fee_per_vbyte") && (typeof message.fee_per_vbyte === "object" ? message.fee_per_vbyte.low || message.fee_per_vbyte.high : message.fee_per_vbyte !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.fee_per_vbyte);
            if (message.local_close_tx != null && $Object.hasOwnProperty.call(message, "local_close_tx") && message.local_close_tx !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.local_close_tx);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingUpdate message, length delimited. Does not implicitly {@link lnrpc.PendingUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PendingUpdate
         * @static
         * @param {lnrpc.PendingUpdate.$Properties} message PendingUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PendingUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PendingUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PendingUpdate & lnrpc.PendingUpdate.$Shape} PendingUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.txid = value;
                        else
                            delete message.txid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.output_index = value;
                        else
                            delete message.output_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_per_vbyte = value;
                        else
                            delete message.fee_per_vbyte;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.local_close_tx = value;
                        else
                            delete message.local_close_tx;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PendingUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PendingUpdate & lnrpc.PendingUpdate.$Shape} PendingUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingUpdate message.
         * @function verify
         * @memberof lnrpc.PendingUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                if (!(message.txid && typeof message.txid.length === "number" || $util.isString(message.txid)))
                    return "txid: buffer expected";
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index"))
                if (!$util.isInteger(message.output_index))
                    return "output_index: integer expected";
            if (message.fee_per_vbyte != null && $Object.hasOwnProperty.call(message, "fee_per_vbyte"))
                if (!$util.isInteger(message.fee_per_vbyte) && !(message.fee_per_vbyte && $util.isInteger(message.fee_per_vbyte.low) && $util.isInteger(message.fee_per_vbyte.high)))
                    return "fee_per_vbyte: integer|Long expected";
            if (message.local_close_tx != null && $Object.hasOwnProperty.call(message, "local_close_tx"))
                if (typeof message.local_close_tx !== "boolean")
                    return "local_close_tx: boolean expected";
            return null;
        };

        /**
         * Creates a PendingUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PendingUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PendingUpdate} PendingUpdate
         */
        PendingUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PendingUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PendingUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PendingUpdate();
            if (object.txid != null)
                if (object.txid.length)
                    if (typeof object.txid === "string")
                        $util.base64.decode(object.txid, message.txid = $util.newBuffer($util.base64.length(object.txid)), 0);
                    else if (object.txid.length >= 0)
                        message.txid = object.txid;
            if (object.output_index != null)
                if ($Number(object.output_index) !== 0)
                    message.output_index = object.output_index >>> 0;
            if (object.fee_per_vbyte != null)
                if (typeof object.fee_per_vbyte === "object" ? object.fee_per_vbyte.low || object.fee_per_vbyte.high : $Number(object.fee_per_vbyte) !== 0)
                    if ($util.Long)
                        message.fee_per_vbyte = $util.Long.fromValue(object.fee_per_vbyte, false);
                    else if (typeof object.fee_per_vbyte === "string")
                        message.fee_per_vbyte = $parseInt(object.fee_per_vbyte, 10);
                    else if (typeof object.fee_per_vbyte === "number")
                        message.fee_per_vbyte = object.fee_per_vbyte;
                    else if (typeof object.fee_per_vbyte === "object")
                        message.fee_per_vbyte = new $util.LongBits(object.fee_per_vbyte.low >>> 0, object.fee_per_vbyte.high >>> 0).toNumber();
            if (object.local_close_tx != null)
                if (object.local_close_tx)
                    message.local_close_tx = $Boolean(object.local_close_tx);
            return message;
        };

        /**
         * Creates a plain object from a PendingUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PendingUpdate
         * @static
         * @param {lnrpc.PendingUpdate} message PendingUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.txid = "";
                else {
                    object.txid = [];
                    if (options.bytes !== $Array)
                        object.txid = $util.newBuffer(object.txid);
                }
                object.output_index = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.local_close_tx = false;
            }
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                object.txid = options.bytes === $String ? $util.base64.encode(message.txid, 0, message.txid.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.txid) : message.txid;
            if (message.output_index != null && $Object.hasOwnProperty.call(message, "output_index"))
                object.output_index = message.output_index;
            if (message.fee_per_vbyte != null && $Object.hasOwnProperty.call(message, "fee_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_per_vbyte = typeof message.fee_per_vbyte === "number" ? $BigInt(message.fee_per_vbyte) : $util.Long.fromBits(message.fee_per_vbyte.low >>> 0, message.fee_per_vbyte.high >>> 0, false).toBigInt();
                else if (typeof message.fee_per_vbyte === "number")
                    object.fee_per_vbyte = options.longs === $String ? $String(message.fee_per_vbyte) : message.fee_per_vbyte;
                else
                    object.fee_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.fee_per_vbyte.low >>> 0, message.fee_per_vbyte.high >>> 0).toNumber() : message.fee_per_vbyte;
            if (message.local_close_tx != null && $Object.hasOwnProperty.call(message, "local_close_tx"))
                object.local_close_tx = message.local_close_tx;
            return object;
        };

        /**
         * Converts this PendingUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.PendingUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingUpdate.prototype.toJSON = function() {
            return PendingUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingUpdate
         * @function getTypeUrl
         * @memberof lnrpc.PendingUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PendingUpdate";
        };

        return PendingUpdate;
    })();

    lnrpc.InstantUpdate = (function() {

        /**
         * Properties of an InstantUpdate.
         * @typedef {Object} lnrpc.InstantUpdate.$Properties
         * @property {number|null} [num_pending_htlcs] InstantUpdate num_pending_htlcs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an InstantUpdate.
         * @memberof lnrpc
         * @interface IInstantUpdate
         * @augments lnrpc.InstantUpdate.$Properties
         * @deprecated Use lnrpc.InstantUpdate.$Properties instead.
         */

        /**
         * Shape of an InstantUpdate.
         * @typedef {lnrpc.InstantUpdate.$Properties} lnrpc.InstantUpdate.$Shape
         */

        /**
         * Constructs a new InstantUpdate.
         * @memberof lnrpc
         * @classdesc Represents an InstantUpdate.
         * @constructor
         * @param {lnrpc.InstantUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const InstantUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InstantUpdate num_pending_htlcs.
         * @member {number} num_pending_htlcs
         * @memberof lnrpc.InstantUpdate
         * @instance
         */
        InstantUpdate.prototype.num_pending_htlcs = 0;

        /**
         * Creates a new InstantUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.InstantUpdate
         * @static
         * @param {lnrpc.InstantUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.InstantUpdate} InstantUpdate instance
         * @type {{
         *   (properties: lnrpc.InstantUpdate.$Shape): lnrpc.InstantUpdate & lnrpc.InstantUpdate.$Shape;
         *   (properties?: lnrpc.InstantUpdate.$Properties): lnrpc.InstantUpdate;
         * }}
         */
        InstantUpdate.create = function(properties) {
            return new InstantUpdate(properties);
        };

        /**
         * Encodes the specified InstantUpdate message. Does not implicitly {@link lnrpc.InstantUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.InstantUpdate
         * @static
         * @param {lnrpc.InstantUpdate.$Properties} message InstantUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InstantUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.num_pending_htlcs != null && $Object.hasOwnProperty.call(message, "num_pending_htlcs") && message.num_pending_htlcs !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.num_pending_htlcs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InstantUpdate message, length delimited. Does not implicitly {@link lnrpc.InstantUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.InstantUpdate
         * @static
         * @param {lnrpc.InstantUpdate.$Properties} message InstantUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InstantUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InstantUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.InstantUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.InstantUpdate & lnrpc.InstantUpdate.$Shape} InstantUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InstantUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.InstantUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.num_pending_htlcs = value;
                        else
                            delete message.num_pending_htlcs;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InstantUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.InstantUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.InstantUpdate & lnrpc.InstantUpdate.$Shape} InstantUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InstantUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InstantUpdate message.
         * @function verify
         * @memberof lnrpc.InstantUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InstantUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.num_pending_htlcs != null && $Object.hasOwnProperty.call(message, "num_pending_htlcs"))
                if (!$util.isInteger(message.num_pending_htlcs))
                    return "num_pending_htlcs: integer expected";
            return null;
        };

        /**
         * Creates an InstantUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.InstantUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.InstantUpdate} InstantUpdate
         */
        InstantUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.InstantUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.InstantUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.InstantUpdate();
            if (object.num_pending_htlcs != null)
                if ($Number(object.num_pending_htlcs) !== 0)
                    message.num_pending_htlcs = object.num_pending_htlcs | 0;
            return message;
        };

        /**
         * Creates a plain object from an InstantUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.InstantUpdate
         * @static
         * @param {lnrpc.InstantUpdate} message InstantUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InstantUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.num_pending_htlcs = 0;
            if (message.num_pending_htlcs != null && $Object.hasOwnProperty.call(message, "num_pending_htlcs"))
                object.num_pending_htlcs = message.num_pending_htlcs;
            return object;
        };

        /**
         * Converts this InstantUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.InstantUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InstantUpdate.prototype.toJSON = function() {
            return InstantUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InstantUpdate
         * @function getTypeUrl
         * @memberof lnrpc.InstantUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InstantUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.InstantUpdate";
        };

        return InstantUpdate;
    })();

    lnrpc.ReadyForPsbtFunding = (function() {

        /**
         * Properties of a ReadyForPsbtFunding.
         * @typedef {Object} lnrpc.ReadyForPsbtFunding.$Properties
         * @property {string|null} [funding_address] ReadyForPsbtFunding funding_address
         * @property {Long|null} [funding_amount] ReadyForPsbtFunding funding_amount
         * @property {Uint8Array|null} [psbt] ReadyForPsbtFunding psbt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ReadyForPsbtFunding.
         * @memberof lnrpc
         * @interface IReadyForPsbtFunding
         * @augments lnrpc.ReadyForPsbtFunding.$Properties
         * @deprecated Use lnrpc.ReadyForPsbtFunding.$Properties instead.
         */

        /**
         * Shape of a ReadyForPsbtFunding.
         * @typedef {lnrpc.ReadyForPsbtFunding.$Properties} lnrpc.ReadyForPsbtFunding.$Shape
         */

        /**
         * Constructs a new ReadyForPsbtFunding.
         * @memberof lnrpc
         * @classdesc Represents a ReadyForPsbtFunding.
         * @constructor
         * @param {lnrpc.ReadyForPsbtFunding.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ReadyForPsbtFunding = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ReadyForPsbtFunding funding_address.
         * @member {string} funding_address
         * @memberof lnrpc.ReadyForPsbtFunding
         * @instance
         */
        ReadyForPsbtFunding.prototype.funding_address = "";

        /**
         * ReadyForPsbtFunding funding_amount.
         * @member {Long} funding_amount
         * @memberof lnrpc.ReadyForPsbtFunding
         * @instance
         */
        ReadyForPsbtFunding.prototype.funding_amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ReadyForPsbtFunding psbt.
         * @member {Uint8Array} psbt
         * @memberof lnrpc.ReadyForPsbtFunding
         * @instance
         */
        ReadyForPsbtFunding.prototype.psbt = $util.newBuffer([]);

        /**
         * Creates a new ReadyForPsbtFunding instance using the specified properties.
         * @function create
         * @memberof lnrpc.ReadyForPsbtFunding
         * @static
         * @param {lnrpc.ReadyForPsbtFunding.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ReadyForPsbtFunding} ReadyForPsbtFunding instance
         * @type {{
         *   (properties: lnrpc.ReadyForPsbtFunding.$Shape): lnrpc.ReadyForPsbtFunding & lnrpc.ReadyForPsbtFunding.$Shape;
         *   (properties?: lnrpc.ReadyForPsbtFunding.$Properties): lnrpc.ReadyForPsbtFunding;
         * }}
         */
        ReadyForPsbtFunding.create = function(properties) {
            return new ReadyForPsbtFunding(properties);
        };

        /**
         * Encodes the specified ReadyForPsbtFunding message. Does not implicitly {@link lnrpc.ReadyForPsbtFunding.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ReadyForPsbtFunding
         * @static
         * @param {lnrpc.ReadyForPsbtFunding.$Properties} message ReadyForPsbtFunding message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReadyForPsbtFunding.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.funding_address != null && $Object.hasOwnProperty.call(message, "funding_address") && message.funding_address !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.funding_address);
            if (message.funding_amount != null && $Object.hasOwnProperty.call(message, "funding_amount") && (typeof message.funding_amount === "object" ? message.funding_amount.low || message.funding_amount.high : message.funding_amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.funding_amount);
            if (message.psbt != null && $Object.hasOwnProperty.call(message, "psbt") && message.psbt.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.psbt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ReadyForPsbtFunding message, length delimited. Does not implicitly {@link lnrpc.ReadyForPsbtFunding.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ReadyForPsbtFunding
         * @static
         * @param {lnrpc.ReadyForPsbtFunding.$Properties} message ReadyForPsbtFunding message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReadyForPsbtFunding.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReadyForPsbtFunding message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ReadyForPsbtFunding
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ReadyForPsbtFunding & lnrpc.ReadyForPsbtFunding.$Shape} ReadyForPsbtFunding
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReadyForPsbtFunding.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ReadyForPsbtFunding(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.funding_address = value;
                        else
                            delete message.funding_address;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.funding_amount = value;
                        else
                            delete message.funding_amount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.psbt = value;
                        else
                            delete message.psbt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ReadyForPsbtFunding message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ReadyForPsbtFunding
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ReadyForPsbtFunding & lnrpc.ReadyForPsbtFunding.$Shape} ReadyForPsbtFunding
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReadyForPsbtFunding.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReadyForPsbtFunding message.
         * @function verify
         * @memberof lnrpc.ReadyForPsbtFunding
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReadyForPsbtFunding.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.funding_address != null && $Object.hasOwnProperty.call(message, "funding_address"))
                if (!$util.isString(message.funding_address))
                    return "funding_address: string expected";
            if (message.funding_amount != null && $Object.hasOwnProperty.call(message, "funding_amount"))
                if (!$util.isInteger(message.funding_amount) && !(message.funding_amount && $util.isInteger(message.funding_amount.low) && $util.isInteger(message.funding_amount.high)))
                    return "funding_amount: integer|Long expected";
            if (message.psbt != null && $Object.hasOwnProperty.call(message, "psbt"))
                if (!(message.psbt && typeof message.psbt.length === "number" || $util.isString(message.psbt)))
                    return "psbt: buffer expected";
            return null;
        };

        /**
         * Creates a ReadyForPsbtFunding message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ReadyForPsbtFunding
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ReadyForPsbtFunding} ReadyForPsbtFunding
         */
        ReadyForPsbtFunding.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ReadyForPsbtFunding)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ReadyForPsbtFunding: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ReadyForPsbtFunding();
            if (object.funding_address != null)
                if (typeof object.funding_address !== "string" || object.funding_address.length)
                    message.funding_address = $String(object.funding_address);
            if (object.funding_amount != null)
                if (typeof object.funding_amount === "object" ? object.funding_amount.low || object.funding_amount.high : $Number(object.funding_amount) !== 0)
                    if ($util.Long)
                        message.funding_amount = $util.Long.fromValue(object.funding_amount, false);
                    else if (typeof object.funding_amount === "string")
                        message.funding_amount = $parseInt(object.funding_amount, 10);
                    else if (typeof object.funding_amount === "number")
                        message.funding_amount = object.funding_amount;
                    else if (typeof object.funding_amount === "object")
                        message.funding_amount = new $util.LongBits(object.funding_amount.low >>> 0, object.funding_amount.high >>> 0).toNumber();
            if (object.psbt != null)
                if (object.psbt.length)
                    if (typeof object.psbt === "string")
                        $util.base64.decode(object.psbt, message.psbt = $util.newBuffer($util.base64.length(object.psbt)), 0);
                    else if (object.psbt.length >= 0)
                        message.psbt = object.psbt;
            return message;
        };

        /**
         * Creates a plain object from a ReadyForPsbtFunding message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ReadyForPsbtFunding
         * @static
         * @param {lnrpc.ReadyForPsbtFunding} message ReadyForPsbtFunding
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReadyForPsbtFunding.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.funding_address = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.funding_amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.funding_amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.psbt = "";
                else {
                    object.psbt = [];
                    if (options.bytes !== $Array)
                        object.psbt = $util.newBuffer(object.psbt);
                }
            }
            if (message.funding_address != null && $Object.hasOwnProperty.call(message, "funding_address"))
                object.funding_address = message.funding_address;
            if (message.funding_amount != null && $Object.hasOwnProperty.call(message, "funding_amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.funding_amount = typeof message.funding_amount === "number" ? $BigInt(message.funding_amount) : $util.Long.fromBits(message.funding_amount.low >>> 0, message.funding_amount.high >>> 0, false).toBigInt();
                else if (typeof message.funding_amount === "number")
                    object.funding_amount = options.longs === $String ? $String(message.funding_amount) : message.funding_amount;
                else
                    object.funding_amount = options.longs === $String ? $util.Long.prototype.toString.call(message.funding_amount) : options.longs === $Number ? new $util.LongBits(message.funding_amount.low >>> 0, message.funding_amount.high >>> 0).toNumber() : message.funding_amount;
            if (message.psbt != null && $Object.hasOwnProperty.call(message, "psbt"))
                object.psbt = options.bytes === $String ? $util.base64.encode(message.psbt, 0, message.psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.psbt) : message.psbt;
            return object;
        };

        /**
         * Converts this ReadyForPsbtFunding to JSON.
         * @function toJSON
         * @memberof lnrpc.ReadyForPsbtFunding
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReadyForPsbtFunding.prototype.toJSON = function() {
            return ReadyForPsbtFunding.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ReadyForPsbtFunding
         * @function getTypeUrl
         * @memberof lnrpc.ReadyForPsbtFunding
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ReadyForPsbtFunding.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ReadyForPsbtFunding";
        };

        return ReadyForPsbtFunding;
    })();

    lnrpc.BatchOpenChannelRequest = (function() {

        /**
         * Properties of a BatchOpenChannelRequest.
         * @typedef {Object} lnrpc.BatchOpenChannelRequest.$Properties
         * @property {Array.<lnrpc.BatchOpenChannel.$Properties>|null} [channels] BatchOpenChannelRequest channels
         * @property {number|null} [target_conf] BatchOpenChannelRequest target_conf
         * @property {Long|null} [sat_per_vbyte] BatchOpenChannelRequest sat_per_vbyte
         * @property {number|null} [min_confs] BatchOpenChannelRequest min_confs
         * @property {boolean|null} [spend_unconfirmed] BatchOpenChannelRequest spend_unconfirmed
         * @property {string|null} [label] BatchOpenChannelRequest label
         * @property {lnrpc.CoinSelectionStrategy|null} [coin_selection_strategy] BatchOpenChannelRequest coin_selection_strategy
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BatchOpenChannelRequest.
         * @memberof lnrpc
         * @interface IBatchOpenChannelRequest
         * @augments lnrpc.BatchOpenChannelRequest.$Properties
         * @deprecated Use lnrpc.BatchOpenChannelRequest.$Properties instead.
         */

        /**
         * Shape of a BatchOpenChannelRequest.
         * @typedef {lnrpc.BatchOpenChannelRequest.$Properties} lnrpc.BatchOpenChannelRequest.$Shape
         */

        /**
         * Constructs a new BatchOpenChannelRequest.
         * @memberof lnrpc
         * @classdesc Represents a BatchOpenChannelRequest.
         * @constructor
         * @param {lnrpc.BatchOpenChannelRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BatchOpenChannelRequest = function (properties) {
            this.channels = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BatchOpenChannelRequest channels.
         * @member {Array.<lnrpc.BatchOpenChannel.$Properties>} channels
         * @memberof lnrpc.BatchOpenChannelRequest
         * @instance
         */
        BatchOpenChannelRequest.prototype.channels = $util.emptyArray;

        /**
         * BatchOpenChannelRequest target_conf.
         * @member {number} target_conf
         * @memberof lnrpc.BatchOpenChannelRequest
         * @instance
         */
        BatchOpenChannelRequest.prototype.target_conf = 0;

        /**
         * BatchOpenChannelRequest sat_per_vbyte.
         * @member {Long} sat_per_vbyte
         * @memberof lnrpc.BatchOpenChannelRequest
         * @instance
         */
        BatchOpenChannelRequest.prototype.sat_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BatchOpenChannelRequest min_confs.
         * @member {number} min_confs
         * @memberof lnrpc.BatchOpenChannelRequest
         * @instance
         */
        BatchOpenChannelRequest.prototype.min_confs = 0;

        /**
         * BatchOpenChannelRequest spend_unconfirmed.
         * @member {boolean} spend_unconfirmed
         * @memberof lnrpc.BatchOpenChannelRequest
         * @instance
         */
        BatchOpenChannelRequest.prototype.spend_unconfirmed = false;

        /**
         * BatchOpenChannelRequest label.
         * @member {string} label
         * @memberof lnrpc.BatchOpenChannelRequest
         * @instance
         */
        BatchOpenChannelRequest.prototype.label = "";

        /**
         * BatchOpenChannelRequest coin_selection_strategy.
         * @member {lnrpc.CoinSelectionStrategy} coin_selection_strategy
         * @memberof lnrpc.BatchOpenChannelRequest
         * @instance
         */
        BatchOpenChannelRequest.prototype.coin_selection_strategy = 0;

        /**
         * Creates a new BatchOpenChannelRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.BatchOpenChannelRequest
         * @static
         * @param {lnrpc.BatchOpenChannelRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.BatchOpenChannelRequest} BatchOpenChannelRequest instance
         * @type {{
         *   (properties: lnrpc.BatchOpenChannelRequest.$Shape): lnrpc.BatchOpenChannelRequest & lnrpc.BatchOpenChannelRequest.$Shape;
         *   (properties?: lnrpc.BatchOpenChannelRequest.$Properties): lnrpc.BatchOpenChannelRequest;
         * }}
         */
        BatchOpenChannelRequest.create = function(properties) {
            return new BatchOpenChannelRequest(properties);
        };

        /**
         * Encodes the specified BatchOpenChannelRequest message. Does not implicitly {@link lnrpc.BatchOpenChannelRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.BatchOpenChannelRequest
         * @static
         * @param {lnrpc.BatchOpenChannelRequest.$Properties} message BatchOpenChannelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchOpenChannelRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channels != null && message.channels.length)
                for (let i = 0; i < message.channels.length; ++i)
                    $root.lnrpc.BatchOpenChannel.encode(message.channels[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf") && message.target_conf !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.target_conf);
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte") && (typeof message.sat_per_vbyte === "object" ? message.sat_per_vbyte.low || message.sat_per_vbyte.high : message.sat_per_vbyte !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.sat_per_vbyte);
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.min_confs);
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed") && message.spend_unconfirmed !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.spend_unconfirmed);
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.label);
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy") && message.coin_selection_strategy !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.coin_selection_strategy);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BatchOpenChannelRequest message, length delimited. Does not implicitly {@link lnrpc.BatchOpenChannelRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.BatchOpenChannelRequest
         * @static
         * @param {lnrpc.BatchOpenChannelRequest.$Properties} message BatchOpenChannelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchOpenChannelRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BatchOpenChannelRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.BatchOpenChannelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.BatchOpenChannelRequest & lnrpc.BatchOpenChannelRequest.$Shape} BatchOpenChannelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchOpenChannelRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.BatchOpenChannelRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.channels && message.channels.length))
                            message.channels = [];
                        message.channels.push($root.lnrpc.BatchOpenChannel.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.target_conf = value;
                        else
                            delete message.target_conf;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_vbyte = value;
                        else
                            delete message.sat_per_vbyte;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.spend_unconfirmed = value;
                        else
                            delete message.spend_unconfirmed;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.coin_selection_strategy = value;
                        else
                            delete message.coin_selection_strategy;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BatchOpenChannelRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.BatchOpenChannelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.BatchOpenChannelRequest & lnrpc.BatchOpenChannelRequest.$Shape} BatchOpenChannelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchOpenChannelRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BatchOpenChannelRequest message.
         * @function verify
         * @memberof lnrpc.BatchOpenChannelRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BatchOpenChannelRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channels != null && $Object.hasOwnProperty.call(message, "channels")) {
                if (!$Array.isArray(message.channels))
                    return "channels: array expected";
                for (let i = 0; i < message.channels.length; ++i) {
                    let error = $root.lnrpc.BatchOpenChannel.verify(message.channels[i], _depth + 1);
                    if (error)
                        return "channels." + error;
                }
            }
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                if (!$util.isInteger(message.target_conf))
                    return "target_conf: integer expected";
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (!$util.isInteger(message.sat_per_vbyte) && !(message.sat_per_vbyte && $util.isInteger(message.sat_per_vbyte.low) && $util.isInteger(message.sat_per_vbyte.high)))
                    return "sat_per_vbyte: integer|Long expected";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                if (typeof message.spend_unconfirmed !== "boolean")
                    return "spend_unconfirmed: boolean expected";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                if (typeof message.coin_selection_strategy !== "number" || (message.coin_selection_strategy | 0) !== message.coin_selection_strategy)
                    return "coin_selection_strategy: enum value expected";
            return null;
        };

        /**
         * Creates a BatchOpenChannelRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.BatchOpenChannelRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.BatchOpenChannelRequest} BatchOpenChannelRequest
         */
        BatchOpenChannelRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.BatchOpenChannelRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.BatchOpenChannelRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.BatchOpenChannelRequest();
            if (object.channels) {
                if (!$Array.isArray(object.channels))
                    throw $TypeError(".lnrpc.BatchOpenChannelRequest.channels: array expected");
                message.channels = $Array(object.channels.length);
                for (let i = 0; i < object.channels.length; ++i) {
                    if (!$util.isObject(object.channels[i]))
                        throw $TypeError(".lnrpc.BatchOpenChannelRequest.channels: object expected");
                    message.channels[i] = $root.lnrpc.BatchOpenChannel.fromObject(object.channels[i], _depth + 1);
                }
            }
            if (object.target_conf != null)
                if ($Number(object.target_conf) !== 0)
                    message.target_conf = object.target_conf | 0;
            if (object.sat_per_vbyte != null)
                if (typeof object.sat_per_vbyte === "object" ? object.sat_per_vbyte.low || object.sat_per_vbyte.high : $Number(object.sat_per_vbyte) !== 0)
                    if ($util.Long)
                        message.sat_per_vbyte = $util.Long.fromValue(object.sat_per_vbyte, false);
                    else if (typeof object.sat_per_vbyte === "string")
                        message.sat_per_vbyte = $parseInt(object.sat_per_vbyte, 10);
                    else if (typeof object.sat_per_vbyte === "number")
                        message.sat_per_vbyte = object.sat_per_vbyte;
                    else if (typeof object.sat_per_vbyte === "object")
                        message.sat_per_vbyte = new $util.LongBits(object.sat_per_vbyte.low >>> 0, object.sat_per_vbyte.high >>> 0).toNumber();
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            if (object.spend_unconfirmed != null)
                if (object.spend_unconfirmed)
                    message.spend_unconfirmed = $Boolean(object.spend_unconfirmed);
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.coin_selection_strategy !== 0 && (typeof object.coin_selection_strategy !== "string" || $root.lnrpc.CoinSelectionStrategy[object.coin_selection_strategy] !== 0))
                switch (object.coin_selection_strategy) {
                case "STRATEGY_USE_GLOBAL_CONFIG":
                case 0:
                    message.coin_selection_strategy = 0;
                    break;
                case "STRATEGY_LARGEST":
                case 1:
                    message.coin_selection_strategy = 1;
                    break;
                case "STRATEGY_RANDOM":
                case 2:
                    message.coin_selection_strategy = 2;
                    break;
                default:
                    if (typeof object.coin_selection_strategy === "number" && (object.coin_selection_strategy | 0) === object.coin_selection_strategy)
                        message.coin_selection_strategy = object.coin_selection_strategy;
                }
            return message;
        };

        /**
         * Creates a plain object from a BatchOpenChannelRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.BatchOpenChannelRequest
         * @static
         * @param {lnrpc.BatchOpenChannelRequest} message BatchOpenChannelRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BatchOpenChannelRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.channels = [];
            if (options.defaults) {
                object.target_conf = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sat_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.min_confs = 0;
                object.spend_unconfirmed = false;
                object.label = "";
                object.coin_selection_strategy = options.enums === $String ? "STRATEGY_USE_GLOBAL_CONFIG" : 0;
            }
            if (message.channels && message.channels.length) {
                object.channels = $Array(message.channels.length);
                for (let j = 0; j < message.channels.length; ++j)
                    object.channels[j] = $root.lnrpc.BatchOpenChannel.toObject(message.channels[j], options, _depth + 1);
            }
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                object.target_conf = message.target_conf;
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_vbyte = typeof message.sat_per_vbyte === "number" ? $BigInt(message.sat_per_vbyte) : $util.Long.fromBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0, false).toBigInt();
                else if (typeof message.sat_per_vbyte === "number")
                    object.sat_per_vbyte = options.longs === $String ? $String(message.sat_per_vbyte) : message.sat_per_vbyte;
                else
                    object.sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0).toNumber() : message.sat_per_vbyte;
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                object.spend_unconfirmed = message.spend_unconfirmed;
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                object.coin_selection_strategy = options.enums === $String ? $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] === $undefined ? message.coin_selection_strategy : $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] : message.coin_selection_strategy;
            return object;
        };

        /**
         * Converts this BatchOpenChannelRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.BatchOpenChannelRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BatchOpenChannelRequest.prototype.toJSON = function() {
            return BatchOpenChannelRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BatchOpenChannelRequest
         * @function getTypeUrl
         * @memberof lnrpc.BatchOpenChannelRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BatchOpenChannelRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.BatchOpenChannelRequest";
        };

        return BatchOpenChannelRequest;
    })();

    lnrpc.BatchOpenChannel = (function() {

        /**
         * Properties of a BatchOpenChannel.
         * @typedef {Object} lnrpc.BatchOpenChannel.$Properties
         * @property {Uint8Array|null} [node_pubkey] BatchOpenChannel node_pubkey
         * @property {Long|null} [local_funding_amount] BatchOpenChannel local_funding_amount
         * @property {Long|null} [push_sat] BatchOpenChannel push_sat
         * @property {boolean|null} ["private"] BatchOpenChannel private
         * @property {Long|null} [min_htlc_msat] BatchOpenChannel min_htlc_msat
         * @property {number|null} [remote_csv_delay] BatchOpenChannel remote_csv_delay
         * @property {string|null} [close_address] BatchOpenChannel close_address
         * @property {Uint8Array|null} [pending_chan_id] BatchOpenChannel pending_chan_id
         * @property {lnrpc.CommitmentType|null} [commitment_type] BatchOpenChannel commitment_type
         * @property {Long|null} [remote_max_value_in_flight_msat] BatchOpenChannel remote_max_value_in_flight_msat
         * @property {number|null} [remote_max_htlcs] BatchOpenChannel remote_max_htlcs
         * @property {number|null} [max_local_csv] BatchOpenChannel max_local_csv
         * @property {boolean|null} [zero_conf] BatchOpenChannel zero_conf
         * @property {boolean|null} [scid_alias] BatchOpenChannel scid_alias
         * @property {Long|null} [base_fee] BatchOpenChannel base_fee
         * @property {Long|null} [fee_rate] BatchOpenChannel fee_rate
         * @property {boolean|null} [use_base_fee] BatchOpenChannel use_base_fee
         * @property {boolean|null} [use_fee_rate] BatchOpenChannel use_fee_rate
         * @property {Long|null} [remote_chan_reserve_sat] BatchOpenChannel remote_chan_reserve_sat
         * @property {string|null} [memo] BatchOpenChannel memo
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BatchOpenChannel.
         * @memberof lnrpc
         * @interface IBatchOpenChannel
         * @augments lnrpc.BatchOpenChannel.$Properties
         * @deprecated Use lnrpc.BatchOpenChannel.$Properties instead.
         */

        /**
         * Shape of a BatchOpenChannel.
         * @typedef {lnrpc.BatchOpenChannel.$Properties} lnrpc.BatchOpenChannel.$Shape
         */

        /**
         * Constructs a new BatchOpenChannel.
         * @memberof lnrpc
         * @classdesc Represents a BatchOpenChannel.
         * @constructor
         * @param {lnrpc.BatchOpenChannel.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BatchOpenChannel = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BatchOpenChannel node_pubkey.
         * @member {Uint8Array} node_pubkey
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.node_pubkey = $util.newBuffer([]);

        /**
         * BatchOpenChannel local_funding_amount.
         * @member {Long} local_funding_amount
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.local_funding_amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BatchOpenChannel push_sat.
         * @member {Long} push_sat
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.push_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BatchOpenChannel private.
         * @member {boolean} private
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype["private"] = false;

        /**
         * BatchOpenChannel min_htlc_msat.
         * @member {Long} min_htlc_msat
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.min_htlc_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BatchOpenChannel remote_csv_delay.
         * @member {number} remote_csv_delay
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.remote_csv_delay = 0;

        /**
         * BatchOpenChannel close_address.
         * @member {string} close_address
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.close_address = "";

        /**
         * BatchOpenChannel pending_chan_id.
         * @member {Uint8Array} pending_chan_id
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.pending_chan_id = $util.newBuffer([]);

        /**
         * BatchOpenChannel commitment_type.
         * @member {lnrpc.CommitmentType} commitment_type
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.commitment_type = 0;

        /**
         * BatchOpenChannel remote_max_value_in_flight_msat.
         * @member {Long} remote_max_value_in_flight_msat
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.remote_max_value_in_flight_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BatchOpenChannel remote_max_htlcs.
         * @member {number} remote_max_htlcs
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.remote_max_htlcs = 0;

        /**
         * BatchOpenChannel max_local_csv.
         * @member {number} max_local_csv
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.max_local_csv = 0;

        /**
         * BatchOpenChannel zero_conf.
         * @member {boolean} zero_conf
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.zero_conf = false;

        /**
         * BatchOpenChannel scid_alias.
         * @member {boolean} scid_alias
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.scid_alias = false;

        /**
         * BatchOpenChannel base_fee.
         * @member {Long} base_fee
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.base_fee = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BatchOpenChannel fee_rate.
         * @member {Long} fee_rate
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.fee_rate = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BatchOpenChannel use_base_fee.
         * @member {boolean} use_base_fee
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.use_base_fee = false;

        /**
         * BatchOpenChannel use_fee_rate.
         * @member {boolean} use_fee_rate
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.use_fee_rate = false;

        /**
         * BatchOpenChannel remote_chan_reserve_sat.
         * @member {Long} remote_chan_reserve_sat
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.remote_chan_reserve_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BatchOpenChannel memo.
         * @member {string} memo
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         */
        BatchOpenChannel.prototype.memo = "";

        /**
         * Creates a new BatchOpenChannel instance using the specified properties.
         * @function create
         * @memberof lnrpc.BatchOpenChannel
         * @static
         * @param {lnrpc.BatchOpenChannel.$Properties=} [properties] Properties to set
         * @returns {lnrpc.BatchOpenChannel} BatchOpenChannel instance
         * @type {{
         *   (properties: lnrpc.BatchOpenChannel.$Shape): lnrpc.BatchOpenChannel & lnrpc.BatchOpenChannel.$Shape;
         *   (properties?: lnrpc.BatchOpenChannel.$Properties): lnrpc.BatchOpenChannel;
         * }}
         */
        BatchOpenChannel.create = function(properties) {
            return new BatchOpenChannel(properties);
        };

        /**
         * Encodes the specified BatchOpenChannel message. Does not implicitly {@link lnrpc.BatchOpenChannel.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.BatchOpenChannel
         * @static
         * @param {lnrpc.BatchOpenChannel.$Properties} message BatchOpenChannel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchOpenChannel.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.node_pubkey != null && $Object.hasOwnProperty.call(message, "node_pubkey") && message.node_pubkey.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.node_pubkey);
            if (message.local_funding_amount != null && $Object.hasOwnProperty.call(message, "local_funding_amount") && (typeof message.local_funding_amount === "object" ? message.local_funding_amount.low || message.local_funding_amount.high : message.local_funding_amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.local_funding_amount);
            if (message.push_sat != null && $Object.hasOwnProperty.call(message, "push_sat") && (typeof message.push_sat === "object" ? message.push_sat.low || message.push_sat.high : message.push_sat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.push_sat);
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private") && message["private"] !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message["private"]);
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat") && (typeof message.min_htlc_msat === "object" ? message.min_htlc_msat.low || message.min_htlc_msat.high : message.min_htlc_msat !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.min_htlc_msat);
            if (message.remote_csv_delay != null && $Object.hasOwnProperty.call(message, "remote_csv_delay") && message.remote_csv_delay !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.remote_csv_delay);
            if (message.close_address != null && $Object.hasOwnProperty.call(message, "close_address") && message.close_address !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.close_address);
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id") && message.pending_chan_id.length)
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.pending_chan_id);
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type") && message.commitment_type !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.commitment_type);
            if (message.remote_max_value_in_flight_msat != null && $Object.hasOwnProperty.call(message, "remote_max_value_in_flight_msat") && (typeof message.remote_max_value_in_flight_msat === "object" ? message.remote_max_value_in_flight_msat.low || message.remote_max_value_in_flight_msat.high : message.remote_max_value_in_flight_msat !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.remote_max_value_in_flight_msat);
            if (message.remote_max_htlcs != null && $Object.hasOwnProperty.call(message, "remote_max_htlcs") && message.remote_max_htlcs !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.remote_max_htlcs);
            if (message.max_local_csv != null && $Object.hasOwnProperty.call(message, "max_local_csv") && message.max_local_csv !== 0)
                writer.uint32(/* id 12, wireType 0 =*/96).uint32(message.max_local_csv);
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf") && message.zero_conf !== false)
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.zero_conf);
            if (message.scid_alias != null && $Object.hasOwnProperty.call(message, "scid_alias") && message.scid_alias !== false)
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.scid_alias);
            if (message.base_fee != null && $Object.hasOwnProperty.call(message, "base_fee") && (typeof message.base_fee === "object" ? message.base_fee.low || message.base_fee.high : message.base_fee !== 0))
                writer.uint32(/* id 15, wireType 0 =*/120).uint64(message.base_fee);
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate") && (typeof message.fee_rate === "object" ? message.fee_rate.low || message.fee_rate.high : message.fee_rate !== 0))
                writer.uint32(/* id 16, wireType 0 =*/128).uint64(message.fee_rate);
            if (message.use_base_fee != null && $Object.hasOwnProperty.call(message, "use_base_fee") && message.use_base_fee !== false)
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.use_base_fee);
            if (message.use_fee_rate != null && $Object.hasOwnProperty.call(message, "use_fee_rate") && message.use_fee_rate !== false)
                writer.uint32(/* id 18, wireType 0 =*/144).bool(message.use_fee_rate);
            if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat") && (typeof message.remote_chan_reserve_sat === "object" ? message.remote_chan_reserve_sat.low || message.remote_chan_reserve_sat.high : message.remote_chan_reserve_sat !== 0))
                writer.uint32(/* id 19, wireType 0 =*/152).uint64(message.remote_chan_reserve_sat);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 20, wireType 2 =*/162).string(message.memo);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BatchOpenChannel message, length delimited. Does not implicitly {@link lnrpc.BatchOpenChannel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.BatchOpenChannel
         * @static
         * @param {lnrpc.BatchOpenChannel.$Properties} message BatchOpenChannel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchOpenChannel.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BatchOpenChannel message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.BatchOpenChannel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.BatchOpenChannel & lnrpc.BatchOpenChannel.$Shape} BatchOpenChannel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchOpenChannel.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.BatchOpenChannel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.node_pubkey = value;
                        else
                            delete message.node_pubkey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.local_funding_amount = value;
                        else
                            delete message.local_funding_amount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.push_sat = value;
                        else
                            delete message.push_sat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message["private"] = value;
                        else
                            delete message["private"];
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.min_htlc_msat = value;
                        else
                            delete message.min_htlc_msat;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remote_csv_delay = value;
                        else
                            delete message.remote_csv_delay;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.close_address = value;
                        else
                            delete message.close_address;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pending_chan_id = value;
                        else
                            delete message.pending_chan_id;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.commitment_type = value;
                        else
                            delete message.commitment_type;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.remote_max_value_in_flight_msat = value;
                        else
                            delete message.remote_max_value_in_flight_msat;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remote_max_htlcs = value;
                        else
                            delete message.remote_max_htlcs;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_local_csv = value;
                        else
                            delete message.max_local_csv;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zero_conf = value;
                        else
                            delete message.zero_conf;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.scid_alias = value;
                        else
                            delete message.scid_alias;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.base_fee = value;
                        else
                            delete message.base_fee;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_rate = value;
                        else
                            delete message.fee_rate;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.use_base_fee = value;
                        else
                            delete message.use_base_fee;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.use_fee_rate = value;
                        else
                            delete message.use_fee_rate;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.remote_chan_reserve_sat = value;
                        else
                            delete message.remote_chan_reserve_sat;
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BatchOpenChannel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.BatchOpenChannel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.BatchOpenChannel & lnrpc.BatchOpenChannel.$Shape} BatchOpenChannel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchOpenChannel.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BatchOpenChannel message.
         * @function verify
         * @memberof lnrpc.BatchOpenChannel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BatchOpenChannel.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.node_pubkey != null && $Object.hasOwnProperty.call(message, "node_pubkey"))
                if (!(message.node_pubkey && typeof message.node_pubkey.length === "number" || $util.isString(message.node_pubkey)))
                    return "node_pubkey: buffer expected";
            if (message.local_funding_amount != null && $Object.hasOwnProperty.call(message, "local_funding_amount"))
                if (!$util.isInteger(message.local_funding_amount) && !(message.local_funding_amount && $util.isInteger(message.local_funding_amount.low) && $util.isInteger(message.local_funding_amount.high)))
                    return "local_funding_amount: integer|Long expected";
            if (message.push_sat != null && $Object.hasOwnProperty.call(message, "push_sat"))
                if (!$util.isInteger(message.push_sat) && !(message.push_sat && $util.isInteger(message.push_sat.low) && $util.isInteger(message.push_sat.high)))
                    return "push_sat: integer|Long expected";
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                if (typeof message["private"] !== "boolean")
                    return "private: boolean expected";
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat"))
                if (!$util.isInteger(message.min_htlc_msat) && !(message.min_htlc_msat && $util.isInteger(message.min_htlc_msat.low) && $util.isInteger(message.min_htlc_msat.high)))
                    return "min_htlc_msat: integer|Long expected";
            if (message.remote_csv_delay != null && $Object.hasOwnProperty.call(message, "remote_csv_delay"))
                if (!$util.isInteger(message.remote_csv_delay))
                    return "remote_csv_delay: integer expected";
            if (message.close_address != null && $Object.hasOwnProperty.call(message, "close_address"))
                if (!$util.isString(message.close_address))
                    return "close_address: string expected";
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                if (!(message.pending_chan_id && typeof message.pending_chan_id.length === "number" || $util.isString(message.pending_chan_id)))
                    return "pending_chan_id: buffer expected";
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                if (typeof message.commitment_type !== "number" || (message.commitment_type | 0) !== message.commitment_type)
                    return "commitment_type: enum value expected";
            if (message.remote_max_value_in_flight_msat != null && $Object.hasOwnProperty.call(message, "remote_max_value_in_flight_msat"))
                if (!$util.isInteger(message.remote_max_value_in_flight_msat) && !(message.remote_max_value_in_flight_msat && $util.isInteger(message.remote_max_value_in_flight_msat.low) && $util.isInteger(message.remote_max_value_in_flight_msat.high)))
                    return "remote_max_value_in_flight_msat: integer|Long expected";
            if (message.remote_max_htlcs != null && $Object.hasOwnProperty.call(message, "remote_max_htlcs"))
                if (!$util.isInteger(message.remote_max_htlcs))
                    return "remote_max_htlcs: integer expected";
            if (message.max_local_csv != null && $Object.hasOwnProperty.call(message, "max_local_csv"))
                if (!$util.isInteger(message.max_local_csv))
                    return "max_local_csv: integer expected";
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf"))
                if (typeof message.zero_conf !== "boolean")
                    return "zero_conf: boolean expected";
            if (message.scid_alias != null && $Object.hasOwnProperty.call(message, "scid_alias"))
                if (typeof message.scid_alias !== "boolean")
                    return "scid_alias: boolean expected";
            if (message.base_fee != null && $Object.hasOwnProperty.call(message, "base_fee"))
                if (!$util.isInteger(message.base_fee) && !(message.base_fee && $util.isInteger(message.base_fee.low) && $util.isInteger(message.base_fee.high)))
                    return "base_fee: integer|Long expected";
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                if (!$util.isInteger(message.fee_rate) && !(message.fee_rate && $util.isInteger(message.fee_rate.low) && $util.isInteger(message.fee_rate.high)))
                    return "fee_rate: integer|Long expected";
            if (message.use_base_fee != null && $Object.hasOwnProperty.call(message, "use_base_fee"))
                if (typeof message.use_base_fee !== "boolean")
                    return "use_base_fee: boolean expected";
            if (message.use_fee_rate != null && $Object.hasOwnProperty.call(message, "use_fee_rate"))
                if (typeof message.use_fee_rate !== "boolean")
                    return "use_fee_rate: boolean expected";
            if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat"))
                if (!$util.isInteger(message.remote_chan_reserve_sat) && !(message.remote_chan_reserve_sat && $util.isInteger(message.remote_chan_reserve_sat.low) && $util.isInteger(message.remote_chan_reserve_sat.high)))
                    return "remote_chan_reserve_sat: integer|Long expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            return null;
        };

        /**
         * Creates a BatchOpenChannel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.BatchOpenChannel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.BatchOpenChannel} BatchOpenChannel
         */
        BatchOpenChannel.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.BatchOpenChannel)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.BatchOpenChannel: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.BatchOpenChannel();
            if (object.node_pubkey != null)
                if (object.node_pubkey.length)
                    if (typeof object.node_pubkey === "string")
                        $util.base64.decode(object.node_pubkey, message.node_pubkey = $util.newBuffer($util.base64.length(object.node_pubkey)), 0);
                    else if (object.node_pubkey.length >= 0)
                        message.node_pubkey = object.node_pubkey;
            if (object.local_funding_amount != null)
                if (typeof object.local_funding_amount === "object" ? object.local_funding_amount.low || object.local_funding_amount.high : $Number(object.local_funding_amount) !== 0)
                    if ($util.Long)
                        message.local_funding_amount = $util.Long.fromValue(object.local_funding_amount, false);
                    else if (typeof object.local_funding_amount === "string")
                        message.local_funding_amount = $parseInt(object.local_funding_amount, 10);
                    else if (typeof object.local_funding_amount === "number")
                        message.local_funding_amount = object.local_funding_amount;
                    else if (typeof object.local_funding_amount === "object")
                        message.local_funding_amount = new $util.LongBits(object.local_funding_amount.low >>> 0, object.local_funding_amount.high >>> 0).toNumber();
            if (object.push_sat != null)
                if (typeof object.push_sat === "object" ? object.push_sat.low || object.push_sat.high : $Number(object.push_sat) !== 0)
                    if ($util.Long)
                        message.push_sat = $util.Long.fromValue(object.push_sat, false);
                    else if (typeof object.push_sat === "string")
                        message.push_sat = $parseInt(object.push_sat, 10);
                    else if (typeof object.push_sat === "number")
                        message.push_sat = object.push_sat;
                    else if (typeof object.push_sat === "object")
                        message.push_sat = new $util.LongBits(object.push_sat.low >>> 0, object.push_sat.high >>> 0).toNumber();
            if (object["private"] != null)
                if (object["private"])
                    message["private"] = $Boolean(object["private"]);
            if (object.min_htlc_msat != null)
                if (typeof object.min_htlc_msat === "object" ? object.min_htlc_msat.low || object.min_htlc_msat.high : $Number(object.min_htlc_msat) !== 0)
                    if ($util.Long)
                        message.min_htlc_msat = $util.Long.fromValue(object.min_htlc_msat, false);
                    else if (typeof object.min_htlc_msat === "string")
                        message.min_htlc_msat = $parseInt(object.min_htlc_msat, 10);
                    else if (typeof object.min_htlc_msat === "number")
                        message.min_htlc_msat = object.min_htlc_msat;
                    else if (typeof object.min_htlc_msat === "object")
                        message.min_htlc_msat = new $util.LongBits(object.min_htlc_msat.low >>> 0, object.min_htlc_msat.high >>> 0).toNumber();
            if (object.remote_csv_delay != null)
                if ($Number(object.remote_csv_delay) !== 0)
                    message.remote_csv_delay = object.remote_csv_delay >>> 0;
            if (object.close_address != null)
                if (typeof object.close_address !== "string" || object.close_address.length)
                    message.close_address = $String(object.close_address);
            if (object.pending_chan_id != null)
                if (object.pending_chan_id.length)
                    if (typeof object.pending_chan_id === "string")
                        $util.base64.decode(object.pending_chan_id, message.pending_chan_id = $util.newBuffer($util.base64.length(object.pending_chan_id)), 0);
                    else if (object.pending_chan_id.length >= 0)
                        message.pending_chan_id = object.pending_chan_id;
            if (object.commitment_type !== 0 && (typeof object.commitment_type !== "string" || $root.lnrpc.CommitmentType[object.commitment_type] !== 0))
                switch (object.commitment_type) {
                case "UNKNOWN_COMMITMENT_TYPE":
                case 0:
                    message.commitment_type = 0;
                    break;
                case "LEGACY":
                case 1:
                    message.commitment_type = 1;
                    break;
                case "STATIC_REMOTE_KEY":
                case 2:
                    message.commitment_type = 2;
                    break;
                case "ANCHORS":
                case 3:
                    message.commitment_type = 3;
                    break;
                case "SCRIPT_ENFORCED_LEASE":
                case 4:
                    message.commitment_type = 4;
                    break;
                case "SIMPLE_TAPROOT":
                case 5:
                    message.commitment_type = 5;
                    break;
                case "SIMPLE_TAPROOT_OVERLAY":
                case 6:
                    message.commitment_type = 6;
                    break;
                default:
                    if (typeof object.commitment_type === "number" && (object.commitment_type | 0) === object.commitment_type)
                        message.commitment_type = object.commitment_type;
                }
            if (object.remote_max_value_in_flight_msat != null)
                if (typeof object.remote_max_value_in_flight_msat === "object" ? object.remote_max_value_in_flight_msat.low || object.remote_max_value_in_flight_msat.high : $Number(object.remote_max_value_in_flight_msat) !== 0)
                    if ($util.Long)
                        message.remote_max_value_in_flight_msat = $util.Long.fromValue(object.remote_max_value_in_flight_msat, true);
                    else if (typeof object.remote_max_value_in_flight_msat === "string")
                        message.remote_max_value_in_flight_msat = $parseInt(object.remote_max_value_in_flight_msat, 10);
                    else if (typeof object.remote_max_value_in_flight_msat === "number")
                        message.remote_max_value_in_flight_msat = object.remote_max_value_in_flight_msat;
                    else if (typeof object.remote_max_value_in_flight_msat === "object")
                        message.remote_max_value_in_flight_msat = new $util.LongBits(object.remote_max_value_in_flight_msat.low >>> 0, object.remote_max_value_in_flight_msat.high >>> 0).toNumber(true);
            if (object.remote_max_htlcs != null)
                if ($Number(object.remote_max_htlcs) !== 0)
                    message.remote_max_htlcs = object.remote_max_htlcs >>> 0;
            if (object.max_local_csv != null)
                if ($Number(object.max_local_csv) !== 0)
                    message.max_local_csv = object.max_local_csv >>> 0;
            if (object.zero_conf != null)
                if (object.zero_conf)
                    message.zero_conf = $Boolean(object.zero_conf);
            if (object.scid_alias != null)
                if (object.scid_alias)
                    message.scid_alias = $Boolean(object.scid_alias);
            if (object.base_fee != null)
                if (typeof object.base_fee === "object" ? object.base_fee.low || object.base_fee.high : $Number(object.base_fee) !== 0)
                    if ($util.Long)
                        message.base_fee = $util.Long.fromValue(object.base_fee, true);
                    else if (typeof object.base_fee === "string")
                        message.base_fee = $parseInt(object.base_fee, 10);
                    else if (typeof object.base_fee === "number")
                        message.base_fee = object.base_fee;
                    else if (typeof object.base_fee === "object")
                        message.base_fee = new $util.LongBits(object.base_fee.low >>> 0, object.base_fee.high >>> 0).toNumber(true);
            if (object.fee_rate != null)
                if (typeof object.fee_rate === "object" ? object.fee_rate.low || object.fee_rate.high : $Number(object.fee_rate) !== 0)
                    if ($util.Long)
                        message.fee_rate = $util.Long.fromValue(object.fee_rate, true);
                    else if (typeof object.fee_rate === "string")
                        message.fee_rate = $parseInt(object.fee_rate, 10);
                    else if (typeof object.fee_rate === "number")
                        message.fee_rate = object.fee_rate;
                    else if (typeof object.fee_rate === "object")
                        message.fee_rate = new $util.LongBits(object.fee_rate.low >>> 0, object.fee_rate.high >>> 0).toNumber(true);
            if (object.use_base_fee != null)
                if (object.use_base_fee)
                    message.use_base_fee = $Boolean(object.use_base_fee);
            if (object.use_fee_rate != null)
                if (object.use_fee_rate)
                    message.use_fee_rate = $Boolean(object.use_fee_rate);
            if (object.remote_chan_reserve_sat != null)
                if (typeof object.remote_chan_reserve_sat === "object" ? object.remote_chan_reserve_sat.low || object.remote_chan_reserve_sat.high : $Number(object.remote_chan_reserve_sat) !== 0)
                    if ($util.Long)
                        message.remote_chan_reserve_sat = $util.Long.fromValue(object.remote_chan_reserve_sat, true);
                    else if (typeof object.remote_chan_reserve_sat === "string")
                        message.remote_chan_reserve_sat = $parseInt(object.remote_chan_reserve_sat, 10);
                    else if (typeof object.remote_chan_reserve_sat === "number")
                        message.remote_chan_reserve_sat = object.remote_chan_reserve_sat;
                    else if (typeof object.remote_chan_reserve_sat === "object")
                        message.remote_chan_reserve_sat = new $util.LongBits(object.remote_chan_reserve_sat.low >>> 0, object.remote_chan_reserve_sat.high >>> 0).toNumber(true);
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            return message;
        };

        /**
         * Creates a plain object from a BatchOpenChannel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.BatchOpenChannel
         * @static
         * @param {lnrpc.BatchOpenChannel} message BatchOpenChannel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BatchOpenChannel.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.node_pubkey = "";
                else {
                    object.node_pubkey = [];
                    if (options.bytes !== $Array)
                        object.node_pubkey = $util.newBuffer(object.node_pubkey);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.local_funding_amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.local_funding_amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.push_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.push_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object["private"] = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.min_htlc_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min_htlc_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.remote_csv_delay = 0;
                object.close_address = "";
                if (options.bytes === $String)
                    object.pending_chan_id = "";
                else {
                    object.pending_chan_id = [];
                    if (options.bytes !== $Array)
                        object.pending_chan_id = $util.newBuffer(object.pending_chan_id);
                }
                object.commitment_type = options.enums === $String ? "UNKNOWN_COMMITMENT_TYPE" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.remote_max_value_in_flight_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.remote_max_value_in_flight_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.remote_max_htlcs = 0;
                object.max_local_csv = 0;
                object.zero_conf = false;
                object.scid_alias = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.base_fee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.base_fee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.fee_rate = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_rate = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.use_base_fee = false;
                object.use_fee_rate = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.remote_chan_reserve_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.remote_chan_reserve_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.memo = "";
            }
            if (message.node_pubkey != null && $Object.hasOwnProperty.call(message, "node_pubkey"))
                object.node_pubkey = options.bytes === $String ? $util.base64.encode(message.node_pubkey, 0, message.node_pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.node_pubkey) : message.node_pubkey;
            if (message.local_funding_amount != null && $Object.hasOwnProperty.call(message, "local_funding_amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.local_funding_amount = typeof message.local_funding_amount === "number" ? $BigInt(message.local_funding_amount) : $util.Long.fromBits(message.local_funding_amount.low >>> 0, message.local_funding_amount.high >>> 0, false).toBigInt();
                else if (typeof message.local_funding_amount === "number")
                    object.local_funding_amount = options.longs === $String ? $String(message.local_funding_amount) : message.local_funding_amount;
                else
                    object.local_funding_amount = options.longs === $String ? $util.Long.prototype.toString.call(message.local_funding_amount) : options.longs === $Number ? new $util.LongBits(message.local_funding_amount.low >>> 0, message.local_funding_amount.high >>> 0).toNumber() : message.local_funding_amount;
            if (message.push_sat != null && $Object.hasOwnProperty.call(message, "push_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.push_sat = typeof message.push_sat === "number" ? $BigInt(message.push_sat) : $util.Long.fromBits(message.push_sat.low >>> 0, message.push_sat.high >>> 0, false).toBigInt();
                else if (typeof message.push_sat === "number")
                    object.push_sat = options.longs === $String ? $String(message.push_sat) : message.push_sat;
                else
                    object.push_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.push_sat) : options.longs === $Number ? new $util.LongBits(message.push_sat.low >>> 0, message.push_sat.high >>> 0).toNumber() : message.push_sat;
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                object["private"] = message["private"];
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min_htlc_msat = typeof message.min_htlc_msat === "number" ? $BigInt(message.min_htlc_msat) : $util.Long.fromBits(message.min_htlc_msat.low >>> 0, message.min_htlc_msat.high >>> 0, false).toBigInt();
                else if (typeof message.min_htlc_msat === "number")
                    object.min_htlc_msat = options.longs === $String ? $String(message.min_htlc_msat) : message.min_htlc_msat;
                else
                    object.min_htlc_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.min_htlc_msat) : options.longs === $Number ? new $util.LongBits(message.min_htlc_msat.low >>> 0, message.min_htlc_msat.high >>> 0).toNumber() : message.min_htlc_msat;
            if (message.remote_csv_delay != null && $Object.hasOwnProperty.call(message, "remote_csv_delay"))
                object.remote_csv_delay = message.remote_csv_delay;
            if (message.close_address != null && $Object.hasOwnProperty.call(message, "close_address"))
                object.close_address = message.close_address;
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                object.pending_chan_id = options.bytes === $String ? $util.base64.encode(message.pending_chan_id, 0, message.pending_chan_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pending_chan_id) : message.pending_chan_id;
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                object.commitment_type = options.enums === $String ? $root.lnrpc.CommitmentType[message.commitment_type] === $undefined ? message.commitment_type : $root.lnrpc.CommitmentType[message.commitment_type] : message.commitment_type;
            if (message.remote_max_value_in_flight_msat != null && $Object.hasOwnProperty.call(message, "remote_max_value_in_flight_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.remote_max_value_in_flight_msat = typeof message.remote_max_value_in_flight_msat === "number" ? $BigInt(message.remote_max_value_in_flight_msat) : $util.Long.fromBits(message.remote_max_value_in_flight_msat.low >>> 0, message.remote_max_value_in_flight_msat.high >>> 0, true).toBigInt();
                else if (typeof message.remote_max_value_in_flight_msat === "number")
                    object.remote_max_value_in_flight_msat = options.longs === $String ? $String(message.remote_max_value_in_flight_msat) : message.remote_max_value_in_flight_msat;
                else
                    object.remote_max_value_in_flight_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_max_value_in_flight_msat) : options.longs === $Number ? new $util.LongBits(message.remote_max_value_in_flight_msat.low >>> 0, message.remote_max_value_in_flight_msat.high >>> 0).toNumber(true) : message.remote_max_value_in_flight_msat;
            if (message.remote_max_htlcs != null && $Object.hasOwnProperty.call(message, "remote_max_htlcs"))
                object.remote_max_htlcs = message.remote_max_htlcs;
            if (message.max_local_csv != null && $Object.hasOwnProperty.call(message, "max_local_csv"))
                object.max_local_csv = message.max_local_csv;
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf"))
                object.zero_conf = message.zero_conf;
            if (message.scid_alias != null && $Object.hasOwnProperty.call(message, "scid_alias"))
                object.scid_alias = message.scid_alias;
            if (message.base_fee != null && $Object.hasOwnProperty.call(message, "base_fee"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.base_fee = typeof message.base_fee === "number" ? $BigInt(message.base_fee) : $util.Long.fromBits(message.base_fee.low >>> 0, message.base_fee.high >>> 0, true).toBigInt();
                else if (typeof message.base_fee === "number")
                    object.base_fee = options.longs === $String ? $String(message.base_fee) : message.base_fee;
                else
                    object.base_fee = options.longs === $String ? $util.Long.prototype.toString.call(message.base_fee) : options.longs === $Number ? new $util.LongBits(message.base_fee.low >>> 0, message.base_fee.high >>> 0).toNumber(true) : message.base_fee;
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_rate = typeof message.fee_rate === "number" ? $BigInt(message.fee_rate) : $util.Long.fromBits(message.fee_rate.low >>> 0, message.fee_rate.high >>> 0, true).toBigInt();
                else if (typeof message.fee_rate === "number")
                    object.fee_rate = options.longs === $String ? $String(message.fee_rate) : message.fee_rate;
                else
                    object.fee_rate = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_rate) : options.longs === $Number ? new $util.LongBits(message.fee_rate.low >>> 0, message.fee_rate.high >>> 0).toNumber(true) : message.fee_rate;
            if (message.use_base_fee != null && $Object.hasOwnProperty.call(message, "use_base_fee"))
                object.use_base_fee = message.use_base_fee;
            if (message.use_fee_rate != null && $Object.hasOwnProperty.call(message, "use_fee_rate"))
                object.use_fee_rate = message.use_fee_rate;
            if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.remote_chan_reserve_sat = typeof message.remote_chan_reserve_sat === "number" ? $BigInt(message.remote_chan_reserve_sat) : $util.Long.fromBits(message.remote_chan_reserve_sat.low >>> 0, message.remote_chan_reserve_sat.high >>> 0, true).toBigInt();
                else if (typeof message.remote_chan_reserve_sat === "number")
                    object.remote_chan_reserve_sat = options.longs === $String ? $String(message.remote_chan_reserve_sat) : message.remote_chan_reserve_sat;
                else
                    object.remote_chan_reserve_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_chan_reserve_sat) : options.longs === $Number ? new $util.LongBits(message.remote_chan_reserve_sat.low >>> 0, message.remote_chan_reserve_sat.high >>> 0).toNumber(true) : message.remote_chan_reserve_sat;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            return object;
        };

        /**
         * Converts this BatchOpenChannel to JSON.
         * @function toJSON
         * @memberof lnrpc.BatchOpenChannel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BatchOpenChannel.prototype.toJSON = function() {
            return BatchOpenChannel.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BatchOpenChannel
         * @function getTypeUrl
         * @memberof lnrpc.BatchOpenChannel
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BatchOpenChannel.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.BatchOpenChannel";
        };

        return BatchOpenChannel;
    })();

    lnrpc.BatchOpenChannelResponse = (function() {

        /**
         * Properties of a BatchOpenChannelResponse.
         * @typedef {Object} lnrpc.BatchOpenChannelResponse.$Properties
         * @property {Array.<lnrpc.PendingUpdate.$Properties>|null} [pending_channels] BatchOpenChannelResponse pending_channels
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BatchOpenChannelResponse.
         * @memberof lnrpc
         * @interface IBatchOpenChannelResponse
         * @augments lnrpc.BatchOpenChannelResponse.$Properties
         * @deprecated Use lnrpc.BatchOpenChannelResponse.$Properties instead.
         */

        /**
         * Shape of a BatchOpenChannelResponse.
         * @typedef {lnrpc.BatchOpenChannelResponse.$Properties} lnrpc.BatchOpenChannelResponse.$Shape
         */

        /**
         * Constructs a new BatchOpenChannelResponse.
         * @memberof lnrpc
         * @classdesc Represents a BatchOpenChannelResponse.
         * @constructor
         * @param {lnrpc.BatchOpenChannelResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BatchOpenChannelResponse = function (properties) {
            this.pending_channels = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BatchOpenChannelResponse pending_channels.
         * @member {Array.<lnrpc.PendingUpdate.$Properties>} pending_channels
         * @memberof lnrpc.BatchOpenChannelResponse
         * @instance
         */
        BatchOpenChannelResponse.prototype.pending_channels = $util.emptyArray;

        /**
         * Creates a new BatchOpenChannelResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.BatchOpenChannelResponse
         * @static
         * @param {lnrpc.BatchOpenChannelResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.BatchOpenChannelResponse} BatchOpenChannelResponse instance
         * @type {{
         *   (properties: lnrpc.BatchOpenChannelResponse.$Shape): lnrpc.BatchOpenChannelResponse & lnrpc.BatchOpenChannelResponse.$Shape;
         *   (properties?: lnrpc.BatchOpenChannelResponse.$Properties): lnrpc.BatchOpenChannelResponse;
         * }}
         */
        BatchOpenChannelResponse.create = function(properties) {
            return new BatchOpenChannelResponse(properties);
        };

        /**
         * Encodes the specified BatchOpenChannelResponse message. Does not implicitly {@link lnrpc.BatchOpenChannelResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.BatchOpenChannelResponse
         * @static
         * @param {lnrpc.BatchOpenChannelResponse.$Properties} message BatchOpenChannelResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchOpenChannelResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pending_channels != null && message.pending_channels.length)
                for (let i = 0; i < message.pending_channels.length; ++i)
                    $root.lnrpc.PendingUpdate.encode(message.pending_channels[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BatchOpenChannelResponse message, length delimited. Does not implicitly {@link lnrpc.BatchOpenChannelResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.BatchOpenChannelResponse
         * @static
         * @param {lnrpc.BatchOpenChannelResponse.$Properties} message BatchOpenChannelResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchOpenChannelResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BatchOpenChannelResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.BatchOpenChannelResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.BatchOpenChannelResponse & lnrpc.BatchOpenChannelResponse.$Shape} BatchOpenChannelResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchOpenChannelResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.BatchOpenChannelResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pending_channels && message.pending_channels.length))
                            message.pending_channels = [];
                        message.pending_channels.push($root.lnrpc.PendingUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BatchOpenChannelResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.BatchOpenChannelResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.BatchOpenChannelResponse & lnrpc.BatchOpenChannelResponse.$Shape} BatchOpenChannelResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchOpenChannelResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BatchOpenChannelResponse message.
         * @function verify
         * @memberof lnrpc.BatchOpenChannelResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BatchOpenChannelResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pending_channels != null && $Object.hasOwnProperty.call(message, "pending_channels")) {
                if (!$Array.isArray(message.pending_channels))
                    return "pending_channels: array expected";
                for (let i = 0; i < message.pending_channels.length; ++i) {
                    let error = $root.lnrpc.PendingUpdate.verify(message.pending_channels[i], _depth + 1);
                    if (error)
                        return "pending_channels." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BatchOpenChannelResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.BatchOpenChannelResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.BatchOpenChannelResponse} BatchOpenChannelResponse
         */
        BatchOpenChannelResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.BatchOpenChannelResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.BatchOpenChannelResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.BatchOpenChannelResponse();
            if (object.pending_channels) {
                if (!$Array.isArray(object.pending_channels))
                    throw $TypeError(".lnrpc.BatchOpenChannelResponse.pending_channels: array expected");
                message.pending_channels = $Array(object.pending_channels.length);
                for (let i = 0; i < object.pending_channels.length; ++i) {
                    if (!$util.isObject(object.pending_channels[i]))
                        throw $TypeError(".lnrpc.BatchOpenChannelResponse.pending_channels: object expected");
                    message.pending_channels[i] = $root.lnrpc.PendingUpdate.fromObject(object.pending_channels[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BatchOpenChannelResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.BatchOpenChannelResponse
         * @static
         * @param {lnrpc.BatchOpenChannelResponse} message BatchOpenChannelResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BatchOpenChannelResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.pending_channels = [];
            if (message.pending_channels && message.pending_channels.length) {
                object.pending_channels = $Array(message.pending_channels.length);
                for (let j = 0; j < message.pending_channels.length; ++j)
                    object.pending_channels[j] = $root.lnrpc.PendingUpdate.toObject(message.pending_channels[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this BatchOpenChannelResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.BatchOpenChannelResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BatchOpenChannelResponse.prototype.toJSON = function() {
            return BatchOpenChannelResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BatchOpenChannelResponse
         * @function getTypeUrl
         * @memberof lnrpc.BatchOpenChannelResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BatchOpenChannelResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.BatchOpenChannelResponse";
        };

        return BatchOpenChannelResponse;
    })();

    lnrpc.OpenChannelRequest = (function() {

        /**
         * Properties of an OpenChannelRequest.
         * @typedef {Object} lnrpc.OpenChannelRequest.$Properties
         * @property {Long|null} [sat_per_vbyte] OpenChannelRequest sat_per_vbyte
         * @property {Uint8Array|null} [node_pubkey] OpenChannelRequest node_pubkey
         * @property {string|null} [node_pubkey_string] OpenChannelRequest node_pubkey_string
         * @property {Long|null} [local_funding_amount] OpenChannelRequest local_funding_amount
         * @property {Long|null} [push_sat] OpenChannelRequest push_sat
         * @property {number|null} [target_conf] OpenChannelRequest target_conf
         * @property {Long|null} [sat_per_byte] OpenChannelRequest sat_per_byte
         * @property {boolean|null} ["private"] OpenChannelRequest private
         * @property {Long|null} [min_htlc_msat] OpenChannelRequest min_htlc_msat
         * @property {number|null} [remote_csv_delay] OpenChannelRequest remote_csv_delay
         * @property {number|null} [min_confs] OpenChannelRequest min_confs
         * @property {boolean|null} [spend_unconfirmed] OpenChannelRequest spend_unconfirmed
         * @property {string|null} [close_address] OpenChannelRequest close_address
         * @property {lnrpc.FundingShim.$Properties|null} [funding_shim] OpenChannelRequest funding_shim
         * @property {Long|null} [remote_max_value_in_flight_msat] OpenChannelRequest remote_max_value_in_flight_msat
         * @property {number|null} [remote_max_htlcs] OpenChannelRequest remote_max_htlcs
         * @property {number|null} [max_local_csv] OpenChannelRequest max_local_csv
         * @property {lnrpc.CommitmentType|null} [commitment_type] OpenChannelRequest commitment_type
         * @property {boolean|null} [zero_conf] OpenChannelRequest zero_conf
         * @property {boolean|null} [scid_alias] OpenChannelRequest scid_alias
         * @property {Long|null} [base_fee] OpenChannelRequest base_fee
         * @property {Long|null} [fee_rate] OpenChannelRequest fee_rate
         * @property {boolean|null} [use_base_fee] OpenChannelRequest use_base_fee
         * @property {boolean|null} [use_fee_rate] OpenChannelRequest use_fee_rate
         * @property {Long|null} [remote_chan_reserve_sat] OpenChannelRequest remote_chan_reserve_sat
         * @property {boolean|null} [fund_max] OpenChannelRequest fund_max
         * @property {string|null} [memo] OpenChannelRequest memo
         * @property {Array.<lnrpc.OutPoint.$Properties>|null} [outpoints] OpenChannelRequest outpoints
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an OpenChannelRequest.
         * @memberof lnrpc
         * @interface IOpenChannelRequest
         * @augments lnrpc.OpenChannelRequest.$Properties
         * @deprecated Use lnrpc.OpenChannelRequest.$Properties instead.
         */

        /**
         * Shape of an OpenChannelRequest.
         * @typedef {{
         *   sat_per_vbyte?: Long|null;
         *   node_pubkey?: Uint8Array|null;
         *   node_pubkey_string?: string|null;
         *   local_funding_amount?: Long|null;
         *   push_sat?: Long|null;
         *   target_conf?: number|null;
         *   sat_per_byte?: Long|null;
         *   "private"?: boolean|null;
         *   min_htlc_msat?: Long|null;
         *   remote_csv_delay?: number|null;
         *   min_confs?: number|null;
         *   spend_unconfirmed?: boolean|null;
         *   close_address?: string|null;
         *   funding_shim?: lnrpc.FundingShim.$Shape|null;
         *   remote_max_value_in_flight_msat?: Long|null;
         *   remote_max_htlcs?: number|null;
         *   max_local_csv?: number|null;
         *   commitment_type?: lnrpc.CommitmentType|null;
         *   zero_conf?: boolean|null;
         *   scid_alias?: boolean|null;
         *   base_fee?: Long|null;
         *   fee_rate?: Long|null;
         *   use_base_fee?: boolean|null;
         *   use_fee_rate?: boolean|null;
         *   remote_chan_reserve_sat?: Long|null;
         *   fund_max?: boolean|null;
         *   memo?: string|null;
         *   outpoints?: Array.<lnrpc.OutPoint.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.OpenChannelRequest.$Shape
         */

        /**
         * Constructs a new OpenChannelRequest.
         * @memberof lnrpc
         * @classdesc Represents an OpenChannelRequest.
         * @constructor
         * @param {lnrpc.OpenChannelRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const OpenChannelRequest = function (properties) {
            this.outpoints = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OpenChannelRequest sat_per_vbyte.
         * @member {Long} sat_per_vbyte
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.sat_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * OpenChannelRequest node_pubkey.
         * @member {Uint8Array} node_pubkey
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.node_pubkey = $util.newBuffer([]);

        /**
         * OpenChannelRequest node_pubkey_string.
         * @member {string} node_pubkey_string
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.node_pubkey_string = "";

        /**
         * OpenChannelRequest local_funding_amount.
         * @member {Long} local_funding_amount
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.local_funding_amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * OpenChannelRequest push_sat.
         * @member {Long} push_sat
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.push_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * OpenChannelRequest target_conf.
         * @member {number} target_conf
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.target_conf = 0;

        /**
         * OpenChannelRequest sat_per_byte.
         * @member {Long} sat_per_byte
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.sat_per_byte = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * OpenChannelRequest private.
         * @member {boolean} private
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype["private"] = false;

        /**
         * OpenChannelRequest min_htlc_msat.
         * @member {Long} min_htlc_msat
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.min_htlc_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * OpenChannelRequest remote_csv_delay.
         * @member {number} remote_csv_delay
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.remote_csv_delay = 0;

        /**
         * OpenChannelRequest min_confs.
         * @member {number} min_confs
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.min_confs = 0;

        /**
         * OpenChannelRequest spend_unconfirmed.
         * @member {boolean} spend_unconfirmed
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.spend_unconfirmed = false;

        /**
         * OpenChannelRequest close_address.
         * @member {string} close_address
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.close_address = "";

        /**
         * OpenChannelRequest funding_shim.
         * @member {lnrpc.FundingShim.$Properties|null|undefined} funding_shim
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.funding_shim = null;

        /**
         * OpenChannelRequest remote_max_value_in_flight_msat.
         * @member {Long} remote_max_value_in_flight_msat
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.remote_max_value_in_flight_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * OpenChannelRequest remote_max_htlcs.
         * @member {number} remote_max_htlcs
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.remote_max_htlcs = 0;

        /**
         * OpenChannelRequest max_local_csv.
         * @member {number} max_local_csv
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.max_local_csv = 0;

        /**
         * OpenChannelRequest commitment_type.
         * @member {lnrpc.CommitmentType} commitment_type
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.commitment_type = 0;

        /**
         * OpenChannelRequest zero_conf.
         * @member {boolean} zero_conf
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.zero_conf = false;

        /**
         * OpenChannelRequest scid_alias.
         * @member {boolean} scid_alias
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.scid_alias = false;

        /**
         * OpenChannelRequest base_fee.
         * @member {Long} base_fee
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.base_fee = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * OpenChannelRequest fee_rate.
         * @member {Long} fee_rate
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.fee_rate = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * OpenChannelRequest use_base_fee.
         * @member {boolean} use_base_fee
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.use_base_fee = false;

        /**
         * OpenChannelRequest use_fee_rate.
         * @member {boolean} use_fee_rate
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.use_fee_rate = false;

        /**
         * OpenChannelRequest remote_chan_reserve_sat.
         * @member {Long} remote_chan_reserve_sat
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.remote_chan_reserve_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * OpenChannelRequest fund_max.
         * @member {boolean} fund_max
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.fund_max = false;

        /**
         * OpenChannelRequest memo.
         * @member {string} memo
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.memo = "";

        /**
         * OpenChannelRequest outpoints.
         * @member {Array.<lnrpc.OutPoint.$Properties>} outpoints
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         */
        OpenChannelRequest.prototype.outpoints = $util.emptyArray;

        /**
         * Creates a new OpenChannelRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.OpenChannelRequest
         * @static
         * @param {lnrpc.OpenChannelRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.OpenChannelRequest} OpenChannelRequest instance
         * @type {{
         *   (properties: lnrpc.OpenChannelRequest.$Shape): lnrpc.OpenChannelRequest & lnrpc.OpenChannelRequest.$Shape;
         *   (properties?: lnrpc.OpenChannelRequest.$Properties): lnrpc.OpenChannelRequest;
         * }}
         */
        OpenChannelRequest.create = function(properties) {
            return new OpenChannelRequest(properties);
        };

        /**
         * Encodes the specified OpenChannelRequest message. Does not implicitly {@link lnrpc.OpenChannelRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.OpenChannelRequest
         * @static
         * @param {lnrpc.OpenChannelRequest.$Properties} message OpenChannelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OpenChannelRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte") && (typeof message.sat_per_vbyte === "object" ? message.sat_per_vbyte.low || message.sat_per_vbyte.high : message.sat_per_vbyte !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.sat_per_vbyte);
            if (message.node_pubkey != null && $Object.hasOwnProperty.call(message, "node_pubkey") && message.node_pubkey.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.node_pubkey);
            if (message.node_pubkey_string != null && $Object.hasOwnProperty.call(message, "node_pubkey_string") && message.node_pubkey_string !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.node_pubkey_string);
            if (message.local_funding_amount != null && $Object.hasOwnProperty.call(message, "local_funding_amount") && (typeof message.local_funding_amount === "object" ? message.local_funding_amount.low || message.local_funding_amount.high : message.local_funding_amount !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.local_funding_amount);
            if (message.push_sat != null && $Object.hasOwnProperty.call(message, "push_sat") && (typeof message.push_sat === "object" ? message.push_sat.low || message.push_sat.high : message.push_sat !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.push_sat);
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf") && message.target_conf !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.target_conf);
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte") && (typeof message.sat_per_byte === "object" ? message.sat_per_byte.low || message.sat_per_byte.high : message.sat_per_byte !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.sat_per_byte);
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private") && message["private"] !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message["private"]);
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat") && (typeof message.min_htlc_msat === "object" ? message.min_htlc_msat.low || message.min_htlc_msat.high : message.min_htlc_msat !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.min_htlc_msat);
            if (message.remote_csv_delay != null && $Object.hasOwnProperty.call(message, "remote_csv_delay") && message.remote_csv_delay !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.remote_csv_delay);
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.min_confs);
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed") && message.spend_unconfirmed !== false)
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.spend_unconfirmed);
            if (message.close_address != null && $Object.hasOwnProperty.call(message, "close_address") && message.close_address !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.close_address);
            if (message.funding_shim != null && $Object.hasOwnProperty.call(message, "funding_shim"))
                $root.lnrpc.FundingShim.encode(message.funding_shim, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.remote_max_value_in_flight_msat != null && $Object.hasOwnProperty.call(message, "remote_max_value_in_flight_msat") && (typeof message.remote_max_value_in_flight_msat === "object" ? message.remote_max_value_in_flight_msat.low || message.remote_max_value_in_flight_msat.high : message.remote_max_value_in_flight_msat !== 0))
                writer.uint32(/* id 15, wireType 0 =*/120).uint64(message.remote_max_value_in_flight_msat);
            if (message.remote_max_htlcs != null && $Object.hasOwnProperty.call(message, "remote_max_htlcs") && message.remote_max_htlcs !== 0)
                writer.uint32(/* id 16, wireType 0 =*/128).uint32(message.remote_max_htlcs);
            if (message.max_local_csv != null && $Object.hasOwnProperty.call(message, "max_local_csv") && message.max_local_csv !== 0)
                writer.uint32(/* id 17, wireType 0 =*/136).uint32(message.max_local_csv);
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type") && message.commitment_type !== 0)
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.commitment_type);
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf") && message.zero_conf !== false)
                writer.uint32(/* id 19, wireType 0 =*/152).bool(message.zero_conf);
            if (message.scid_alias != null && $Object.hasOwnProperty.call(message, "scid_alias") && message.scid_alias !== false)
                writer.uint32(/* id 20, wireType 0 =*/160).bool(message.scid_alias);
            if (message.base_fee != null && $Object.hasOwnProperty.call(message, "base_fee") && (typeof message.base_fee === "object" ? message.base_fee.low || message.base_fee.high : message.base_fee !== 0))
                writer.uint32(/* id 21, wireType 0 =*/168).uint64(message.base_fee);
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate") && (typeof message.fee_rate === "object" ? message.fee_rate.low || message.fee_rate.high : message.fee_rate !== 0))
                writer.uint32(/* id 22, wireType 0 =*/176).uint64(message.fee_rate);
            if (message.use_base_fee != null && $Object.hasOwnProperty.call(message, "use_base_fee") && message.use_base_fee !== false)
                writer.uint32(/* id 23, wireType 0 =*/184).bool(message.use_base_fee);
            if (message.use_fee_rate != null && $Object.hasOwnProperty.call(message, "use_fee_rate") && message.use_fee_rate !== false)
                writer.uint32(/* id 24, wireType 0 =*/192).bool(message.use_fee_rate);
            if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat") && (typeof message.remote_chan_reserve_sat === "object" ? message.remote_chan_reserve_sat.low || message.remote_chan_reserve_sat.high : message.remote_chan_reserve_sat !== 0))
                writer.uint32(/* id 25, wireType 0 =*/200).uint64(message.remote_chan_reserve_sat);
            if (message.fund_max != null && $Object.hasOwnProperty.call(message, "fund_max") && message.fund_max !== false)
                writer.uint32(/* id 26, wireType 0 =*/208).bool(message.fund_max);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 27, wireType 2 =*/218).string(message.memo);
            if (message.outpoints != null && message.outpoints.length)
                for (let i = 0; i < message.outpoints.length; ++i)
                    $root.lnrpc.OutPoint.encode(message.outpoints[i], writer.uint32(/* id 28, wireType 2 =*/226).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified OpenChannelRequest message, length delimited. Does not implicitly {@link lnrpc.OpenChannelRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.OpenChannelRequest
         * @static
         * @param {lnrpc.OpenChannelRequest.$Properties} message OpenChannelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OpenChannelRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an OpenChannelRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.OpenChannelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.OpenChannelRequest & lnrpc.OpenChannelRequest.$Shape} OpenChannelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OpenChannelRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.OpenChannelRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_vbyte = value;
                        else
                            delete message.sat_per_vbyte;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.node_pubkey = value;
                        else
                            delete message.node_pubkey;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.node_pubkey_string = value;
                        else
                            delete message.node_pubkey_string;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.local_funding_amount = value;
                        else
                            delete message.local_funding_amount;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.push_sat = value;
                        else
                            delete message.push_sat;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.target_conf = value;
                        else
                            delete message.target_conf;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_byte = value;
                        else
                            delete message.sat_per_byte;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message["private"] = value;
                        else
                            delete message["private"];
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.min_htlc_msat = value;
                        else
                            delete message.min_htlc_msat;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remote_csv_delay = value;
                        else
                            delete message.remote_csv_delay;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.spend_unconfirmed = value;
                        else
                            delete message.spend_unconfirmed;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.close_address = value;
                        else
                            delete message.close_address;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.funding_shim = $root.lnrpc.FundingShim.decode(reader, reader.uint32(), $undefined, _depth + 1, message.funding_shim);
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.remote_max_value_in_flight_msat = value;
                        else
                            delete message.remote_max_value_in_flight_msat;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remote_max_htlcs = value;
                        else
                            delete message.remote_max_htlcs;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_local_csv = value;
                        else
                            delete message.max_local_csv;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.commitment_type = value;
                        else
                            delete message.commitment_type;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zero_conf = value;
                        else
                            delete message.zero_conf;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.scid_alias = value;
                        else
                            delete message.scid_alias;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.base_fee = value;
                        else
                            delete message.base_fee;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_rate = value;
                        else
                            delete message.fee_rate;
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.use_base_fee = value;
                        else
                            delete message.use_base_fee;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.use_fee_rate = value;
                        else
                            delete message.use_fee_rate;
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.remote_chan_reserve_sat = value;
                        else
                            delete message.remote_chan_reserve_sat;
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.fund_max = value;
                        else
                            delete message.fund_max;
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        if (!(message.outpoints && message.outpoints.length))
                            message.outpoints = [];
                        message.outpoints.push($root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an OpenChannelRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.OpenChannelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.OpenChannelRequest & lnrpc.OpenChannelRequest.$Shape} OpenChannelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OpenChannelRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OpenChannelRequest message.
         * @function verify
         * @memberof lnrpc.OpenChannelRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OpenChannelRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (!$util.isInteger(message.sat_per_vbyte) && !(message.sat_per_vbyte && $util.isInteger(message.sat_per_vbyte.low) && $util.isInteger(message.sat_per_vbyte.high)))
                    return "sat_per_vbyte: integer|Long expected";
            if (message.node_pubkey != null && $Object.hasOwnProperty.call(message, "node_pubkey"))
                if (!(message.node_pubkey && typeof message.node_pubkey.length === "number" || $util.isString(message.node_pubkey)))
                    return "node_pubkey: buffer expected";
            if (message.node_pubkey_string != null && $Object.hasOwnProperty.call(message, "node_pubkey_string"))
                if (!$util.isString(message.node_pubkey_string))
                    return "node_pubkey_string: string expected";
            if (message.local_funding_amount != null && $Object.hasOwnProperty.call(message, "local_funding_amount"))
                if (!$util.isInteger(message.local_funding_amount) && !(message.local_funding_amount && $util.isInteger(message.local_funding_amount.low) && $util.isInteger(message.local_funding_amount.high)))
                    return "local_funding_amount: integer|Long expected";
            if (message.push_sat != null && $Object.hasOwnProperty.call(message, "push_sat"))
                if (!$util.isInteger(message.push_sat) && !(message.push_sat && $util.isInteger(message.push_sat.low) && $util.isInteger(message.push_sat.high)))
                    return "push_sat: integer|Long expected";
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                if (!$util.isInteger(message.target_conf))
                    return "target_conf: integer expected";
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (!$util.isInteger(message.sat_per_byte) && !(message.sat_per_byte && $util.isInteger(message.sat_per_byte.low) && $util.isInteger(message.sat_per_byte.high)))
                    return "sat_per_byte: integer|Long expected";
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                if (typeof message["private"] !== "boolean")
                    return "private: boolean expected";
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat"))
                if (!$util.isInteger(message.min_htlc_msat) && !(message.min_htlc_msat && $util.isInteger(message.min_htlc_msat.low) && $util.isInteger(message.min_htlc_msat.high)))
                    return "min_htlc_msat: integer|Long expected";
            if (message.remote_csv_delay != null && $Object.hasOwnProperty.call(message, "remote_csv_delay"))
                if (!$util.isInteger(message.remote_csv_delay))
                    return "remote_csv_delay: integer expected";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                if (typeof message.spend_unconfirmed !== "boolean")
                    return "spend_unconfirmed: boolean expected";
            if (message.close_address != null && $Object.hasOwnProperty.call(message, "close_address"))
                if (!$util.isString(message.close_address))
                    return "close_address: string expected";
            if (message.funding_shim != null && $Object.hasOwnProperty.call(message, "funding_shim")) {
                let error = $root.lnrpc.FundingShim.verify(message.funding_shim, _depth + 1);
                if (error)
                    return "funding_shim." + error;
            }
            if (message.remote_max_value_in_flight_msat != null && $Object.hasOwnProperty.call(message, "remote_max_value_in_flight_msat"))
                if (!$util.isInteger(message.remote_max_value_in_flight_msat) && !(message.remote_max_value_in_flight_msat && $util.isInteger(message.remote_max_value_in_flight_msat.low) && $util.isInteger(message.remote_max_value_in_flight_msat.high)))
                    return "remote_max_value_in_flight_msat: integer|Long expected";
            if (message.remote_max_htlcs != null && $Object.hasOwnProperty.call(message, "remote_max_htlcs"))
                if (!$util.isInteger(message.remote_max_htlcs))
                    return "remote_max_htlcs: integer expected";
            if (message.max_local_csv != null && $Object.hasOwnProperty.call(message, "max_local_csv"))
                if (!$util.isInteger(message.max_local_csv))
                    return "max_local_csv: integer expected";
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                if (typeof message.commitment_type !== "number" || (message.commitment_type | 0) !== message.commitment_type)
                    return "commitment_type: enum value expected";
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf"))
                if (typeof message.zero_conf !== "boolean")
                    return "zero_conf: boolean expected";
            if (message.scid_alias != null && $Object.hasOwnProperty.call(message, "scid_alias"))
                if (typeof message.scid_alias !== "boolean")
                    return "scid_alias: boolean expected";
            if (message.base_fee != null && $Object.hasOwnProperty.call(message, "base_fee"))
                if (!$util.isInteger(message.base_fee) && !(message.base_fee && $util.isInteger(message.base_fee.low) && $util.isInteger(message.base_fee.high)))
                    return "base_fee: integer|Long expected";
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                if (!$util.isInteger(message.fee_rate) && !(message.fee_rate && $util.isInteger(message.fee_rate.low) && $util.isInteger(message.fee_rate.high)))
                    return "fee_rate: integer|Long expected";
            if (message.use_base_fee != null && $Object.hasOwnProperty.call(message, "use_base_fee"))
                if (typeof message.use_base_fee !== "boolean")
                    return "use_base_fee: boolean expected";
            if (message.use_fee_rate != null && $Object.hasOwnProperty.call(message, "use_fee_rate"))
                if (typeof message.use_fee_rate !== "boolean")
                    return "use_fee_rate: boolean expected";
            if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat"))
                if (!$util.isInteger(message.remote_chan_reserve_sat) && !(message.remote_chan_reserve_sat && $util.isInteger(message.remote_chan_reserve_sat.low) && $util.isInteger(message.remote_chan_reserve_sat.high)))
                    return "remote_chan_reserve_sat: integer|Long expected";
            if (message.fund_max != null && $Object.hasOwnProperty.call(message, "fund_max"))
                if (typeof message.fund_max !== "boolean")
                    return "fund_max: boolean expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.outpoints != null && $Object.hasOwnProperty.call(message, "outpoints")) {
                if (!$Array.isArray(message.outpoints))
                    return "outpoints: array expected";
                for (let i = 0; i < message.outpoints.length; ++i) {
                    let error = $root.lnrpc.OutPoint.verify(message.outpoints[i], _depth + 1);
                    if (error)
                        return "outpoints." + error;
                }
            }
            return null;
        };

        /**
         * Creates an OpenChannelRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.OpenChannelRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.OpenChannelRequest} OpenChannelRequest
         */
        OpenChannelRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.OpenChannelRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.OpenChannelRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.OpenChannelRequest();
            if (object.sat_per_vbyte != null)
                if (typeof object.sat_per_vbyte === "object" ? object.sat_per_vbyte.low || object.sat_per_vbyte.high : $Number(object.sat_per_vbyte) !== 0)
                    if ($util.Long)
                        message.sat_per_vbyte = $util.Long.fromValue(object.sat_per_vbyte, true);
                    else if (typeof object.sat_per_vbyte === "string")
                        message.sat_per_vbyte = $parseInt(object.sat_per_vbyte, 10);
                    else if (typeof object.sat_per_vbyte === "number")
                        message.sat_per_vbyte = object.sat_per_vbyte;
                    else if (typeof object.sat_per_vbyte === "object")
                        message.sat_per_vbyte = new $util.LongBits(object.sat_per_vbyte.low >>> 0, object.sat_per_vbyte.high >>> 0).toNumber(true);
            if (object.node_pubkey != null)
                if (object.node_pubkey.length)
                    if (typeof object.node_pubkey === "string")
                        $util.base64.decode(object.node_pubkey, message.node_pubkey = $util.newBuffer($util.base64.length(object.node_pubkey)), 0);
                    else if (object.node_pubkey.length >= 0)
                        message.node_pubkey = object.node_pubkey;
            if (object.node_pubkey_string != null)
                if (typeof object.node_pubkey_string !== "string" || object.node_pubkey_string.length)
                    message.node_pubkey_string = $String(object.node_pubkey_string);
            if (object.local_funding_amount != null)
                if (typeof object.local_funding_amount === "object" ? object.local_funding_amount.low || object.local_funding_amount.high : $Number(object.local_funding_amount) !== 0)
                    if ($util.Long)
                        message.local_funding_amount = $util.Long.fromValue(object.local_funding_amount, false);
                    else if (typeof object.local_funding_amount === "string")
                        message.local_funding_amount = $parseInt(object.local_funding_amount, 10);
                    else if (typeof object.local_funding_amount === "number")
                        message.local_funding_amount = object.local_funding_amount;
                    else if (typeof object.local_funding_amount === "object")
                        message.local_funding_amount = new $util.LongBits(object.local_funding_amount.low >>> 0, object.local_funding_amount.high >>> 0).toNumber();
            if (object.push_sat != null)
                if (typeof object.push_sat === "object" ? object.push_sat.low || object.push_sat.high : $Number(object.push_sat) !== 0)
                    if ($util.Long)
                        message.push_sat = $util.Long.fromValue(object.push_sat, false);
                    else if (typeof object.push_sat === "string")
                        message.push_sat = $parseInt(object.push_sat, 10);
                    else if (typeof object.push_sat === "number")
                        message.push_sat = object.push_sat;
                    else if (typeof object.push_sat === "object")
                        message.push_sat = new $util.LongBits(object.push_sat.low >>> 0, object.push_sat.high >>> 0).toNumber();
            if (object.target_conf != null)
                if ($Number(object.target_conf) !== 0)
                    message.target_conf = object.target_conf | 0;
            if (object.sat_per_byte != null)
                if (typeof object.sat_per_byte === "object" ? object.sat_per_byte.low || object.sat_per_byte.high : $Number(object.sat_per_byte) !== 0)
                    if ($util.Long)
                        message.sat_per_byte = $util.Long.fromValue(object.sat_per_byte, false);
                    else if (typeof object.sat_per_byte === "string")
                        message.sat_per_byte = $parseInt(object.sat_per_byte, 10);
                    else if (typeof object.sat_per_byte === "number")
                        message.sat_per_byte = object.sat_per_byte;
                    else if (typeof object.sat_per_byte === "object")
                        message.sat_per_byte = new $util.LongBits(object.sat_per_byte.low >>> 0, object.sat_per_byte.high >>> 0).toNumber();
            if (object["private"] != null)
                if (object["private"])
                    message["private"] = $Boolean(object["private"]);
            if (object.min_htlc_msat != null)
                if (typeof object.min_htlc_msat === "object" ? object.min_htlc_msat.low || object.min_htlc_msat.high : $Number(object.min_htlc_msat) !== 0)
                    if ($util.Long)
                        message.min_htlc_msat = $util.Long.fromValue(object.min_htlc_msat, false);
                    else if (typeof object.min_htlc_msat === "string")
                        message.min_htlc_msat = $parseInt(object.min_htlc_msat, 10);
                    else if (typeof object.min_htlc_msat === "number")
                        message.min_htlc_msat = object.min_htlc_msat;
                    else if (typeof object.min_htlc_msat === "object")
                        message.min_htlc_msat = new $util.LongBits(object.min_htlc_msat.low >>> 0, object.min_htlc_msat.high >>> 0).toNumber();
            if (object.remote_csv_delay != null)
                if ($Number(object.remote_csv_delay) !== 0)
                    message.remote_csv_delay = object.remote_csv_delay >>> 0;
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            if (object.spend_unconfirmed != null)
                if (object.spend_unconfirmed)
                    message.spend_unconfirmed = $Boolean(object.spend_unconfirmed);
            if (object.close_address != null)
                if (typeof object.close_address !== "string" || object.close_address.length)
                    message.close_address = $String(object.close_address);
            if (object.funding_shim != null) {
                if (!$util.isObject(object.funding_shim))
                    throw $TypeError(".lnrpc.OpenChannelRequest.funding_shim: object expected");
                message.funding_shim = $root.lnrpc.FundingShim.fromObject(object.funding_shim, _depth + 1);
            }
            if (object.remote_max_value_in_flight_msat != null)
                if (typeof object.remote_max_value_in_flight_msat === "object" ? object.remote_max_value_in_flight_msat.low || object.remote_max_value_in_flight_msat.high : $Number(object.remote_max_value_in_flight_msat) !== 0)
                    if ($util.Long)
                        message.remote_max_value_in_flight_msat = $util.Long.fromValue(object.remote_max_value_in_flight_msat, true);
                    else if (typeof object.remote_max_value_in_flight_msat === "string")
                        message.remote_max_value_in_flight_msat = $parseInt(object.remote_max_value_in_flight_msat, 10);
                    else if (typeof object.remote_max_value_in_flight_msat === "number")
                        message.remote_max_value_in_flight_msat = object.remote_max_value_in_flight_msat;
                    else if (typeof object.remote_max_value_in_flight_msat === "object")
                        message.remote_max_value_in_flight_msat = new $util.LongBits(object.remote_max_value_in_flight_msat.low >>> 0, object.remote_max_value_in_flight_msat.high >>> 0).toNumber(true);
            if (object.remote_max_htlcs != null)
                if ($Number(object.remote_max_htlcs) !== 0)
                    message.remote_max_htlcs = object.remote_max_htlcs >>> 0;
            if (object.max_local_csv != null)
                if ($Number(object.max_local_csv) !== 0)
                    message.max_local_csv = object.max_local_csv >>> 0;
            if (object.commitment_type !== 0 && (typeof object.commitment_type !== "string" || $root.lnrpc.CommitmentType[object.commitment_type] !== 0))
                switch (object.commitment_type) {
                case "UNKNOWN_COMMITMENT_TYPE":
                case 0:
                    message.commitment_type = 0;
                    break;
                case "LEGACY":
                case 1:
                    message.commitment_type = 1;
                    break;
                case "STATIC_REMOTE_KEY":
                case 2:
                    message.commitment_type = 2;
                    break;
                case "ANCHORS":
                case 3:
                    message.commitment_type = 3;
                    break;
                case "SCRIPT_ENFORCED_LEASE":
                case 4:
                    message.commitment_type = 4;
                    break;
                case "SIMPLE_TAPROOT":
                case 5:
                    message.commitment_type = 5;
                    break;
                case "SIMPLE_TAPROOT_OVERLAY":
                case 6:
                    message.commitment_type = 6;
                    break;
                default:
                    if (typeof object.commitment_type === "number" && (object.commitment_type | 0) === object.commitment_type)
                        message.commitment_type = object.commitment_type;
                }
            if (object.zero_conf != null)
                if (object.zero_conf)
                    message.zero_conf = $Boolean(object.zero_conf);
            if (object.scid_alias != null)
                if (object.scid_alias)
                    message.scid_alias = $Boolean(object.scid_alias);
            if (object.base_fee != null)
                if (typeof object.base_fee === "object" ? object.base_fee.low || object.base_fee.high : $Number(object.base_fee) !== 0)
                    if ($util.Long)
                        message.base_fee = $util.Long.fromValue(object.base_fee, true);
                    else if (typeof object.base_fee === "string")
                        message.base_fee = $parseInt(object.base_fee, 10);
                    else if (typeof object.base_fee === "number")
                        message.base_fee = object.base_fee;
                    else if (typeof object.base_fee === "object")
                        message.base_fee = new $util.LongBits(object.base_fee.low >>> 0, object.base_fee.high >>> 0).toNumber(true);
            if (object.fee_rate != null)
                if (typeof object.fee_rate === "object" ? object.fee_rate.low || object.fee_rate.high : $Number(object.fee_rate) !== 0)
                    if ($util.Long)
                        message.fee_rate = $util.Long.fromValue(object.fee_rate, true);
                    else if (typeof object.fee_rate === "string")
                        message.fee_rate = $parseInt(object.fee_rate, 10);
                    else if (typeof object.fee_rate === "number")
                        message.fee_rate = object.fee_rate;
                    else if (typeof object.fee_rate === "object")
                        message.fee_rate = new $util.LongBits(object.fee_rate.low >>> 0, object.fee_rate.high >>> 0).toNumber(true);
            if (object.use_base_fee != null)
                if (object.use_base_fee)
                    message.use_base_fee = $Boolean(object.use_base_fee);
            if (object.use_fee_rate != null)
                if (object.use_fee_rate)
                    message.use_fee_rate = $Boolean(object.use_fee_rate);
            if (object.remote_chan_reserve_sat != null)
                if (typeof object.remote_chan_reserve_sat === "object" ? object.remote_chan_reserve_sat.low || object.remote_chan_reserve_sat.high : $Number(object.remote_chan_reserve_sat) !== 0)
                    if ($util.Long)
                        message.remote_chan_reserve_sat = $util.Long.fromValue(object.remote_chan_reserve_sat, true);
                    else if (typeof object.remote_chan_reserve_sat === "string")
                        message.remote_chan_reserve_sat = $parseInt(object.remote_chan_reserve_sat, 10);
                    else if (typeof object.remote_chan_reserve_sat === "number")
                        message.remote_chan_reserve_sat = object.remote_chan_reserve_sat;
                    else if (typeof object.remote_chan_reserve_sat === "object")
                        message.remote_chan_reserve_sat = new $util.LongBits(object.remote_chan_reserve_sat.low >>> 0, object.remote_chan_reserve_sat.high >>> 0).toNumber(true);
            if (object.fund_max != null)
                if (object.fund_max)
                    message.fund_max = $Boolean(object.fund_max);
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.outpoints) {
                if (!$Array.isArray(object.outpoints))
                    throw $TypeError(".lnrpc.OpenChannelRequest.outpoints: array expected");
                message.outpoints = $Array(object.outpoints.length);
                for (let i = 0; i < object.outpoints.length; ++i) {
                    if (!$util.isObject(object.outpoints[i]))
                        throw $TypeError(".lnrpc.OpenChannelRequest.outpoints: object expected");
                    message.outpoints[i] = $root.lnrpc.OutPoint.fromObject(object.outpoints[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an OpenChannelRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.OpenChannelRequest
         * @static
         * @param {lnrpc.OpenChannelRequest} message OpenChannelRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OpenChannelRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.outpoints = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sat_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.node_pubkey = "";
                else {
                    object.node_pubkey = [];
                    if (options.bytes !== $Array)
                        object.node_pubkey = $util.newBuffer(object.node_pubkey);
                }
                object.node_pubkey_string = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.local_funding_amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.local_funding_amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.push_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.push_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.target_conf = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sat_per_byte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_byte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object["private"] = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.min_htlc_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min_htlc_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.remote_csv_delay = 0;
                object.min_confs = 0;
                object.spend_unconfirmed = false;
                object.close_address = "";
                object.funding_shim = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.remote_max_value_in_flight_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.remote_max_value_in_flight_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.remote_max_htlcs = 0;
                object.max_local_csv = 0;
                object.commitment_type = options.enums === $String ? "UNKNOWN_COMMITMENT_TYPE" : 0;
                object.zero_conf = false;
                object.scid_alias = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.base_fee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.base_fee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.fee_rate = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_rate = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.use_base_fee = false;
                object.use_fee_rate = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.remote_chan_reserve_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.remote_chan_reserve_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.fund_max = false;
                object.memo = "";
            }
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_vbyte = typeof message.sat_per_vbyte === "number" ? $BigInt(message.sat_per_vbyte) : $util.Long.fromBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.sat_per_vbyte === "number")
                    object.sat_per_vbyte = options.longs === $String ? $String(message.sat_per_vbyte) : message.sat_per_vbyte;
                else
                    object.sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0).toNumber(true) : message.sat_per_vbyte;
            if (message.node_pubkey != null && $Object.hasOwnProperty.call(message, "node_pubkey"))
                object.node_pubkey = options.bytes === $String ? $util.base64.encode(message.node_pubkey, 0, message.node_pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.node_pubkey) : message.node_pubkey;
            if (message.node_pubkey_string != null && $Object.hasOwnProperty.call(message, "node_pubkey_string"))
                object.node_pubkey_string = message.node_pubkey_string;
            if (message.local_funding_amount != null && $Object.hasOwnProperty.call(message, "local_funding_amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.local_funding_amount = typeof message.local_funding_amount === "number" ? $BigInt(message.local_funding_amount) : $util.Long.fromBits(message.local_funding_amount.low >>> 0, message.local_funding_amount.high >>> 0, false).toBigInt();
                else if (typeof message.local_funding_amount === "number")
                    object.local_funding_amount = options.longs === $String ? $String(message.local_funding_amount) : message.local_funding_amount;
                else
                    object.local_funding_amount = options.longs === $String ? $util.Long.prototype.toString.call(message.local_funding_amount) : options.longs === $Number ? new $util.LongBits(message.local_funding_amount.low >>> 0, message.local_funding_amount.high >>> 0).toNumber() : message.local_funding_amount;
            if (message.push_sat != null && $Object.hasOwnProperty.call(message, "push_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.push_sat = typeof message.push_sat === "number" ? $BigInt(message.push_sat) : $util.Long.fromBits(message.push_sat.low >>> 0, message.push_sat.high >>> 0, false).toBigInt();
                else if (typeof message.push_sat === "number")
                    object.push_sat = options.longs === $String ? $String(message.push_sat) : message.push_sat;
                else
                    object.push_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.push_sat) : options.longs === $Number ? new $util.LongBits(message.push_sat.low >>> 0, message.push_sat.high >>> 0).toNumber() : message.push_sat;
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                object.target_conf = message.target_conf;
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_byte = typeof message.sat_per_byte === "number" ? $BigInt(message.sat_per_byte) : $util.Long.fromBits(message.sat_per_byte.low >>> 0, message.sat_per_byte.high >>> 0, false).toBigInt();
                else if (typeof message.sat_per_byte === "number")
                    object.sat_per_byte = options.longs === $String ? $String(message.sat_per_byte) : message.sat_per_byte;
                else
                    object.sat_per_byte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_byte) : options.longs === $Number ? new $util.LongBits(message.sat_per_byte.low >>> 0, message.sat_per_byte.high >>> 0).toNumber() : message.sat_per_byte;
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                object["private"] = message["private"];
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min_htlc_msat = typeof message.min_htlc_msat === "number" ? $BigInt(message.min_htlc_msat) : $util.Long.fromBits(message.min_htlc_msat.low >>> 0, message.min_htlc_msat.high >>> 0, false).toBigInt();
                else if (typeof message.min_htlc_msat === "number")
                    object.min_htlc_msat = options.longs === $String ? $String(message.min_htlc_msat) : message.min_htlc_msat;
                else
                    object.min_htlc_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.min_htlc_msat) : options.longs === $Number ? new $util.LongBits(message.min_htlc_msat.low >>> 0, message.min_htlc_msat.high >>> 0).toNumber() : message.min_htlc_msat;
            if (message.remote_csv_delay != null && $Object.hasOwnProperty.call(message, "remote_csv_delay"))
                object.remote_csv_delay = message.remote_csv_delay;
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                object.spend_unconfirmed = message.spend_unconfirmed;
            if (message.close_address != null && $Object.hasOwnProperty.call(message, "close_address"))
                object.close_address = message.close_address;
            if (message.funding_shim != null && $Object.hasOwnProperty.call(message, "funding_shim"))
                object.funding_shim = $root.lnrpc.FundingShim.toObject(message.funding_shim, options, _depth + 1);
            if (message.remote_max_value_in_flight_msat != null && $Object.hasOwnProperty.call(message, "remote_max_value_in_flight_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.remote_max_value_in_flight_msat = typeof message.remote_max_value_in_flight_msat === "number" ? $BigInt(message.remote_max_value_in_flight_msat) : $util.Long.fromBits(message.remote_max_value_in_flight_msat.low >>> 0, message.remote_max_value_in_flight_msat.high >>> 0, true).toBigInt();
                else if (typeof message.remote_max_value_in_flight_msat === "number")
                    object.remote_max_value_in_flight_msat = options.longs === $String ? $String(message.remote_max_value_in_flight_msat) : message.remote_max_value_in_flight_msat;
                else
                    object.remote_max_value_in_flight_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_max_value_in_flight_msat) : options.longs === $Number ? new $util.LongBits(message.remote_max_value_in_flight_msat.low >>> 0, message.remote_max_value_in_flight_msat.high >>> 0).toNumber(true) : message.remote_max_value_in_flight_msat;
            if (message.remote_max_htlcs != null && $Object.hasOwnProperty.call(message, "remote_max_htlcs"))
                object.remote_max_htlcs = message.remote_max_htlcs;
            if (message.max_local_csv != null && $Object.hasOwnProperty.call(message, "max_local_csv"))
                object.max_local_csv = message.max_local_csv;
            if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                object.commitment_type = options.enums === $String ? $root.lnrpc.CommitmentType[message.commitment_type] === $undefined ? message.commitment_type : $root.lnrpc.CommitmentType[message.commitment_type] : message.commitment_type;
            if (message.zero_conf != null && $Object.hasOwnProperty.call(message, "zero_conf"))
                object.zero_conf = message.zero_conf;
            if (message.scid_alias != null && $Object.hasOwnProperty.call(message, "scid_alias"))
                object.scid_alias = message.scid_alias;
            if (message.base_fee != null && $Object.hasOwnProperty.call(message, "base_fee"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.base_fee = typeof message.base_fee === "number" ? $BigInt(message.base_fee) : $util.Long.fromBits(message.base_fee.low >>> 0, message.base_fee.high >>> 0, true).toBigInt();
                else if (typeof message.base_fee === "number")
                    object.base_fee = options.longs === $String ? $String(message.base_fee) : message.base_fee;
                else
                    object.base_fee = options.longs === $String ? $util.Long.prototype.toString.call(message.base_fee) : options.longs === $Number ? new $util.LongBits(message.base_fee.low >>> 0, message.base_fee.high >>> 0).toNumber(true) : message.base_fee;
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_rate = typeof message.fee_rate === "number" ? $BigInt(message.fee_rate) : $util.Long.fromBits(message.fee_rate.low >>> 0, message.fee_rate.high >>> 0, true).toBigInt();
                else if (typeof message.fee_rate === "number")
                    object.fee_rate = options.longs === $String ? $String(message.fee_rate) : message.fee_rate;
                else
                    object.fee_rate = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_rate) : options.longs === $Number ? new $util.LongBits(message.fee_rate.low >>> 0, message.fee_rate.high >>> 0).toNumber(true) : message.fee_rate;
            if (message.use_base_fee != null && $Object.hasOwnProperty.call(message, "use_base_fee"))
                object.use_base_fee = message.use_base_fee;
            if (message.use_fee_rate != null && $Object.hasOwnProperty.call(message, "use_fee_rate"))
                object.use_fee_rate = message.use_fee_rate;
            if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.remote_chan_reserve_sat = typeof message.remote_chan_reserve_sat === "number" ? $BigInt(message.remote_chan_reserve_sat) : $util.Long.fromBits(message.remote_chan_reserve_sat.low >>> 0, message.remote_chan_reserve_sat.high >>> 0, true).toBigInt();
                else if (typeof message.remote_chan_reserve_sat === "number")
                    object.remote_chan_reserve_sat = options.longs === $String ? $String(message.remote_chan_reserve_sat) : message.remote_chan_reserve_sat;
                else
                    object.remote_chan_reserve_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_chan_reserve_sat) : options.longs === $Number ? new $util.LongBits(message.remote_chan_reserve_sat.low >>> 0, message.remote_chan_reserve_sat.high >>> 0).toNumber(true) : message.remote_chan_reserve_sat;
            if (message.fund_max != null && $Object.hasOwnProperty.call(message, "fund_max"))
                object.fund_max = message.fund_max;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.outpoints && message.outpoints.length) {
                object.outpoints = $Array(message.outpoints.length);
                for (let j = 0; j < message.outpoints.length; ++j)
                    object.outpoints[j] = $root.lnrpc.OutPoint.toObject(message.outpoints[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this OpenChannelRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.OpenChannelRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OpenChannelRequest.prototype.toJSON = function() {
            return OpenChannelRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for OpenChannelRequest
         * @function getTypeUrl
         * @memberof lnrpc.OpenChannelRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        OpenChannelRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.OpenChannelRequest";
        };

        return OpenChannelRequest;
    })();

    lnrpc.OpenStatusUpdate = (function() {

        /**
         * Properties of an OpenStatusUpdate.
         * @typedef {Object} lnrpc.OpenStatusUpdate.$Properties
         * @property {lnrpc.PendingUpdate.$Properties|null} [chan_pending] OpenStatusUpdate chan_pending
         * @property {lnrpc.ChannelOpenUpdate.$Properties|null} [chan_open] OpenStatusUpdate chan_open
         * @property {lnrpc.ReadyForPsbtFunding.$Properties|null} [psbt_fund] OpenStatusUpdate psbt_fund
         * @property {Uint8Array|null} [pending_chan_id] OpenStatusUpdate pending_chan_id
         * @property {"chan_pending"|"chan_open"|"psbt_fund"} [update] OpenStatusUpdate update
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an OpenStatusUpdate.
         * @memberof lnrpc
         * @interface IOpenStatusUpdate
         * @augments lnrpc.OpenStatusUpdate.$Properties
         * @deprecated Use lnrpc.OpenStatusUpdate.$Properties instead.
         */

        /**
         * Narrowed shape of an OpenStatusUpdate.
         * @typedef {{
         *   chan_pending?: lnrpc.PendingUpdate.$Shape|null;
         *   chan_open?: lnrpc.ChannelOpenUpdate.$Shape|null;
         *   psbt_fund?: lnrpc.ReadyForPsbtFunding.$Shape|null;
         *   pending_chan_id?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ update?: undefined; chan_pending?: null; chan_open?: null; psbt_fund?: null }|{ update?: "chan_pending"; chan_pending: lnrpc.PendingUpdate.$Shape; chan_open?: null; psbt_fund?: null }|{ update?: "chan_open"; chan_pending?: null; chan_open: lnrpc.ChannelOpenUpdate.$Shape; psbt_fund?: null }|{ update?: "psbt_fund"; chan_pending?: null; chan_open?: null; psbt_fund: lnrpc.ReadyForPsbtFunding.$Shape })
         * )} lnrpc.OpenStatusUpdate.$Shape
         */

        /**
         * Constructs a new OpenStatusUpdate.
         * @memberof lnrpc
         * @classdesc Represents an OpenStatusUpdate.
         * @constructor
         * @param {lnrpc.OpenStatusUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const OpenStatusUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OpenStatusUpdate chan_pending.
         * @member {lnrpc.PendingUpdate.$Properties|null|undefined} chan_pending
         * @memberof lnrpc.OpenStatusUpdate
         * @instance
         */
        OpenStatusUpdate.prototype.chan_pending = null;

        /**
         * OpenStatusUpdate chan_open.
         * @member {lnrpc.ChannelOpenUpdate.$Properties|null|undefined} chan_open
         * @memberof lnrpc.OpenStatusUpdate
         * @instance
         */
        OpenStatusUpdate.prototype.chan_open = null;

        /**
         * OpenStatusUpdate psbt_fund.
         * @member {lnrpc.ReadyForPsbtFunding.$Properties|null|undefined} psbt_fund
         * @memberof lnrpc.OpenStatusUpdate
         * @instance
         */
        OpenStatusUpdate.prototype.psbt_fund = null;

        /**
         * OpenStatusUpdate pending_chan_id.
         * @member {Uint8Array} pending_chan_id
         * @memberof lnrpc.OpenStatusUpdate
         * @instance
         */
        OpenStatusUpdate.prototype.pending_chan_id = $util.newBuffer([]);

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * OpenStatusUpdate update.
         * @member {"chan_pending"|"chan_open"|"psbt_fund"|undefined} update
         * @memberof lnrpc.OpenStatusUpdate
         * @instance
         */
        $Object.defineProperty(OpenStatusUpdate.prototype, "update", {
            get: $util.oneOfGetter($oneOfFields = ["chan_pending", "chan_open", "psbt_fund"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new OpenStatusUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.OpenStatusUpdate
         * @static
         * @param {lnrpc.OpenStatusUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.OpenStatusUpdate} OpenStatusUpdate instance
         * @type {{
         *   (properties: lnrpc.OpenStatusUpdate.$Shape): lnrpc.OpenStatusUpdate & lnrpc.OpenStatusUpdate.$Shape;
         *   (properties?: lnrpc.OpenStatusUpdate.$Properties): lnrpc.OpenStatusUpdate;
         * }}
         */
        OpenStatusUpdate.create = function(properties) {
            return new OpenStatusUpdate(properties);
        };

        /**
         * Encodes the specified OpenStatusUpdate message. Does not implicitly {@link lnrpc.OpenStatusUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.OpenStatusUpdate
         * @static
         * @param {lnrpc.OpenStatusUpdate.$Properties} message OpenStatusUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OpenStatusUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_pending != null && $Object.hasOwnProperty.call(message, "chan_pending"))
                $root.lnrpc.PendingUpdate.encode(message.chan_pending, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.chan_open != null && $Object.hasOwnProperty.call(message, "chan_open"))
                $root.lnrpc.ChannelOpenUpdate.encode(message.chan_open, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id") && message.pending_chan_id.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.pending_chan_id);
            if (message.psbt_fund != null && $Object.hasOwnProperty.call(message, "psbt_fund"))
                $root.lnrpc.ReadyForPsbtFunding.encode(message.psbt_fund, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified OpenStatusUpdate message, length delimited. Does not implicitly {@link lnrpc.OpenStatusUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.OpenStatusUpdate
         * @static
         * @param {lnrpc.OpenStatusUpdate.$Properties} message OpenStatusUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OpenStatusUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an OpenStatusUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.OpenStatusUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.OpenStatusUpdate & lnrpc.OpenStatusUpdate.$Shape} OpenStatusUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OpenStatusUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.OpenStatusUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.chan_pending = $root.lnrpc.PendingUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_pending);
                        message.update = "chan_pending";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.chan_open = $root.lnrpc.ChannelOpenUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_open);
                        message.update = "chan_open";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.psbt_fund = $root.lnrpc.ReadyForPsbtFunding.decode(reader, reader.uint32(), $undefined, _depth + 1, message.psbt_fund);
                        message.update = "psbt_fund";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pending_chan_id = value;
                        else
                            delete message.pending_chan_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an OpenStatusUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.OpenStatusUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.OpenStatusUpdate & lnrpc.OpenStatusUpdate.$Shape} OpenStatusUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OpenStatusUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OpenStatusUpdate message.
         * @function verify
         * @memberof lnrpc.OpenStatusUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OpenStatusUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.chan_pending != null && $Object.hasOwnProperty.call(message, "chan_pending")) {
                properties.update = 1;
                {
                    let error = $root.lnrpc.PendingUpdate.verify(message.chan_pending, _depth + 1);
                    if (error)
                        return "chan_pending." + error;
                }
            }
            if (message.chan_open != null && $Object.hasOwnProperty.call(message, "chan_open")) {
                if (properties.update === 1)
                    return "update: multiple values";
                properties.update = 1;
                {
                    let error = $root.lnrpc.ChannelOpenUpdate.verify(message.chan_open, _depth + 1);
                    if (error)
                        return "chan_open." + error;
                }
            }
            if (message.psbt_fund != null && $Object.hasOwnProperty.call(message, "psbt_fund")) {
                if (properties.update === 1)
                    return "update: multiple values";
                properties.update = 1;
                {
                    let error = $root.lnrpc.ReadyForPsbtFunding.verify(message.psbt_fund, _depth + 1);
                    if (error)
                        return "psbt_fund." + error;
                }
            }
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                if (!(message.pending_chan_id && typeof message.pending_chan_id.length === "number" || $util.isString(message.pending_chan_id)))
                    return "pending_chan_id: buffer expected";
            return null;
        };

        /**
         * Creates an OpenStatusUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.OpenStatusUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.OpenStatusUpdate} OpenStatusUpdate
         */
        OpenStatusUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.OpenStatusUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.OpenStatusUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.OpenStatusUpdate();
            if (object.chan_pending != null) {
                if (!$util.isObject(object.chan_pending))
                    throw $TypeError(".lnrpc.OpenStatusUpdate.chan_pending: object expected");
                message.chan_pending = $root.lnrpc.PendingUpdate.fromObject(object.chan_pending, _depth + 1);
            }
            if (object.chan_open != null) {
                if (!$util.isObject(object.chan_open))
                    throw $TypeError(".lnrpc.OpenStatusUpdate.chan_open: object expected");
                message.chan_open = $root.lnrpc.ChannelOpenUpdate.fromObject(object.chan_open, _depth + 1);
            }
            if (object.psbt_fund != null) {
                if (!$util.isObject(object.psbt_fund))
                    throw $TypeError(".lnrpc.OpenStatusUpdate.psbt_fund: object expected");
                message.psbt_fund = $root.lnrpc.ReadyForPsbtFunding.fromObject(object.psbt_fund, _depth + 1);
            }
            if (object.pending_chan_id != null)
                if (object.pending_chan_id.length)
                    if (typeof object.pending_chan_id === "string")
                        $util.base64.decode(object.pending_chan_id, message.pending_chan_id = $util.newBuffer($util.base64.length(object.pending_chan_id)), 0);
                    else if (object.pending_chan_id.length >= 0)
                        message.pending_chan_id = object.pending_chan_id;
            return message;
        };

        /**
         * Creates a plain object from an OpenStatusUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.OpenStatusUpdate
         * @static
         * @param {lnrpc.OpenStatusUpdate} message OpenStatusUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OpenStatusUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.pending_chan_id = "";
                else {
                    object.pending_chan_id = [];
                    if (options.bytes !== $Array)
                        object.pending_chan_id = $util.newBuffer(object.pending_chan_id);
                }
            if (message.chan_pending != null && $Object.hasOwnProperty.call(message, "chan_pending")) {
                object.chan_pending = $root.lnrpc.PendingUpdate.toObject(message.chan_pending, options, _depth + 1);
                if (options.oneofs)
                    object.update = "chan_pending";
            }
            if (message.chan_open != null && $Object.hasOwnProperty.call(message, "chan_open")) {
                object.chan_open = $root.lnrpc.ChannelOpenUpdate.toObject(message.chan_open, options, _depth + 1);
                if (options.oneofs)
                    object.update = "chan_open";
            }
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                object.pending_chan_id = options.bytes === $String ? $util.base64.encode(message.pending_chan_id, 0, message.pending_chan_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pending_chan_id) : message.pending_chan_id;
            if (message.psbt_fund != null && $Object.hasOwnProperty.call(message, "psbt_fund")) {
                object.psbt_fund = $root.lnrpc.ReadyForPsbtFunding.toObject(message.psbt_fund, options, _depth + 1);
                if (options.oneofs)
                    object.update = "psbt_fund";
            }
            return object;
        };

        /**
         * Converts this OpenStatusUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.OpenStatusUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OpenStatusUpdate.prototype.toJSON = function() {
            return OpenStatusUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for OpenStatusUpdate
         * @function getTypeUrl
         * @memberof lnrpc.OpenStatusUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        OpenStatusUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.OpenStatusUpdate";
        };

        return OpenStatusUpdate;
    })();

    lnrpc.KeyLocator = (function() {

        /**
         * Properties of a KeyLocator.
         * @typedef {Object} lnrpc.KeyLocator.$Properties
         * @property {number|null} [key_family] KeyLocator key_family
         * @property {number|null} [key_index] KeyLocator key_index
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a KeyLocator.
         * @memberof lnrpc
         * @interface IKeyLocator
         * @augments lnrpc.KeyLocator.$Properties
         * @deprecated Use lnrpc.KeyLocator.$Properties instead.
         */

        /**
         * Shape of a KeyLocator.
         * @typedef {lnrpc.KeyLocator.$Properties} lnrpc.KeyLocator.$Shape
         */

        /**
         * Constructs a new KeyLocator.
         * @memberof lnrpc
         * @classdesc Represents a KeyLocator.
         * @constructor
         * @param {lnrpc.KeyLocator.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const KeyLocator = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * KeyLocator key_family.
         * @member {number} key_family
         * @memberof lnrpc.KeyLocator
         * @instance
         */
        KeyLocator.prototype.key_family = 0;

        /**
         * KeyLocator key_index.
         * @member {number} key_index
         * @memberof lnrpc.KeyLocator
         * @instance
         */
        KeyLocator.prototype.key_index = 0;

        /**
         * Creates a new KeyLocator instance using the specified properties.
         * @function create
         * @memberof lnrpc.KeyLocator
         * @static
         * @param {lnrpc.KeyLocator.$Properties=} [properties] Properties to set
         * @returns {lnrpc.KeyLocator} KeyLocator instance
         * @type {{
         *   (properties: lnrpc.KeyLocator.$Shape): lnrpc.KeyLocator & lnrpc.KeyLocator.$Shape;
         *   (properties?: lnrpc.KeyLocator.$Properties): lnrpc.KeyLocator;
         * }}
         */
        KeyLocator.create = function(properties) {
            return new KeyLocator(properties);
        };

        /**
         * Encodes the specified KeyLocator message. Does not implicitly {@link lnrpc.KeyLocator.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.KeyLocator
         * @static
         * @param {lnrpc.KeyLocator.$Properties} message KeyLocator message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyLocator.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key_family != null && $Object.hasOwnProperty.call(message, "key_family") && message.key_family !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.key_family);
            if (message.key_index != null && $Object.hasOwnProperty.call(message, "key_index") && message.key_index !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.key_index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified KeyLocator message, length delimited. Does not implicitly {@link lnrpc.KeyLocator.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.KeyLocator
         * @static
         * @param {lnrpc.KeyLocator.$Properties} message KeyLocator message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyLocator.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KeyLocator message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.KeyLocator
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.KeyLocator & lnrpc.KeyLocator.$Shape} KeyLocator
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyLocator.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.KeyLocator(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.key_family = value;
                        else
                            delete message.key_family;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.key_index = value;
                        else
                            delete message.key_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a KeyLocator message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.KeyLocator
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.KeyLocator & lnrpc.KeyLocator.$Shape} KeyLocator
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyLocator.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeyLocator message.
         * @function verify
         * @memberof lnrpc.KeyLocator
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeyLocator.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key_family != null && $Object.hasOwnProperty.call(message, "key_family"))
                if (!$util.isInteger(message.key_family))
                    return "key_family: integer expected";
            if (message.key_index != null && $Object.hasOwnProperty.call(message, "key_index"))
                if (!$util.isInteger(message.key_index))
                    return "key_index: integer expected";
            return null;
        };

        /**
         * Creates a KeyLocator message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.KeyLocator
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.KeyLocator} KeyLocator
         */
        KeyLocator.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.KeyLocator)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.KeyLocator: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.KeyLocator();
            if (object.key_family != null)
                if ($Number(object.key_family) !== 0)
                    message.key_family = object.key_family | 0;
            if (object.key_index != null)
                if ($Number(object.key_index) !== 0)
                    message.key_index = object.key_index | 0;
            return message;
        };

        /**
         * Creates a plain object from a KeyLocator message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.KeyLocator
         * @static
         * @param {lnrpc.KeyLocator} message KeyLocator
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeyLocator.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key_family = 0;
                object.key_index = 0;
            }
            if (message.key_family != null && $Object.hasOwnProperty.call(message, "key_family"))
                object.key_family = message.key_family;
            if (message.key_index != null && $Object.hasOwnProperty.call(message, "key_index"))
                object.key_index = message.key_index;
            return object;
        };

        /**
         * Converts this KeyLocator to JSON.
         * @function toJSON
         * @memberof lnrpc.KeyLocator
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeyLocator.prototype.toJSON = function() {
            return KeyLocator.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for KeyLocator
         * @function getTypeUrl
         * @memberof lnrpc.KeyLocator
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        KeyLocator.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.KeyLocator";
        };

        return KeyLocator;
    })();

    lnrpc.KeyDescriptor = (function() {

        /**
         * Properties of a KeyDescriptor.
         * @typedef {Object} lnrpc.KeyDescriptor.$Properties
         * @property {Uint8Array|null} [raw_key_bytes] KeyDescriptor raw_key_bytes
         * @property {lnrpc.KeyLocator.$Properties|null} [key_loc] KeyDescriptor key_loc
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a KeyDescriptor.
         * @memberof lnrpc
         * @interface IKeyDescriptor
         * @augments lnrpc.KeyDescriptor.$Properties
         * @deprecated Use lnrpc.KeyDescriptor.$Properties instead.
         */

        /**
         * Shape of a KeyDescriptor.
         * @typedef {lnrpc.KeyDescriptor.$Properties} lnrpc.KeyDescriptor.$Shape
         */

        /**
         * Constructs a new KeyDescriptor.
         * @memberof lnrpc
         * @classdesc Represents a KeyDescriptor.
         * @constructor
         * @param {lnrpc.KeyDescriptor.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const KeyDescriptor = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * KeyDescriptor raw_key_bytes.
         * @member {Uint8Array} raw_key_bytes
         * @memberof lnrpc.KeyDescriptor
         * @instance
         */
        KeyDescriptor.prototype.raw_key_bytes = $util.newBuffer([]);

        /**
         * KeyDescriptor key_loc.
         * @member {lnrpc.KeyLocator.$Properties|null|undefined} key_loc
         * @memberof lnrpc.KeyDescriptor
         * @instance
         */
        KeyDescriptor.prototype.key_loc = null;

        /**
         * Creates a new KeyDescriptor instance using the specified properties.
         * @function create
         * @memberof lnrpc.KeyDescriptor
         * @static
         * @param {lnrpc.KeyDescriptor.$Properties=} [properties] Properties to set
         * @returns {lnrpc.KeyDescriptor} KeyDescriptor instance
         * @type {{
         *   (properties: lnrpc.KeyDescriptor.$Shape): lnrpc.KeyDescriptor & lnrpc.KeyDescriptor.$Shape;
         *   (properties?: lnrpc.KeyDescriptor.$Properties): lnrpc.KeyDescriptor;
         * }}
         */
        KeyDescriptor.create = function(properties) {
            return new KeyDescriptor(properties);
        };

        /**
         * Encodes the specified KeyDescriptor message. Does not implicitly {@link lnrpc.KeyDescriptor.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.KeyDescriptor
         * @static
         * @param {lnrpc.KeyDescriptor.$Properties} message KeyDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyDescriptor.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.raw_key_bytes != null && $Object.hasOwnProperty.call(message, "raw_key_bytes") && message.raw_key_bytes.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.raw_key_bytes);
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                $root.lnrpc.KeyLocator.encode(message.key_loc, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified KeyDescriptor message, length delimited. Does not implicitly {@link lnrpc.KeyDescriptor.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.KeyDescriptor
         * @static
         * @param {lnrpc.KeyDescriptor.$Properties} message KeyDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyDescriptor.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KeyDescriptor message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.KeyDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.KeyDescriptor & lnrpc.KeyDescriptor.$Shape} KeyDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyDescriptor.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.KeyDescriptor(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_key_bytes = value;
                        else
                            delete message.raw_key_bytes;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.key_loc = $root.lnrpc.KeyLocator.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key_loc);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a KeyDescriptor message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.KeyDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.KeyDescriptor & lnrpc.KeyDescriptor.$Shape} KeyDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyDescriptor.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeyDescriptor message.
         * @function verify
         * @memberof lnrpc.KeyDescriptor
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeyDescriptor.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.raw_key_bytes != null && $Object.hasOwnProperty.call(message, "raw_key_bytes"))
                if (!(message.raw_key_bytes && typeof message.raw_key_bytes.length === "number" || $util.isString(message.raw_key_bytes)))
                    return "raw_key_bytes: buffer expected";
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc")) {
                let error = $root.lnrpc.KeyLocator.verify(message.key_loc, _depth + 1);
                if (error)
                    return "key_loc." + error;
            }
            return null;
        };

        /**
         * Creates a KeyDescriptor message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.KeyDescriptor
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.KeyDescriptor} KeyDescriptor
         */
        KeyDescriptor.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.KeyDescriptor)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.KeyDescriptor: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.KeyDescriptor();
            if (object.raw_key_bytes != null)
                if (object.raw_key_bytes.length)
                    if (typeof object.raw_key_bytes === "string")
                        $util.base64.decode(object.raw_key_bytes, message.raw_key_bytes = $util.newBuffer($util.base64.length(object.raw_key_bytes)), 0);
                    else if (object.raw_key_bytes.length >= 0)
                        message.raw_key_bytes = object.raw_key_bytes;
            if (object.key_loc != null) {
                if (!$util.isObject(object.key_loc))
                    throw $TypeError(".lnrpc.KeyDescriptor.key_loc: object expected");
                message.key_loc = $root.lnrpc.KeyLocator.fromObject(object.key_loc, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a KeyDescriptor message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.KeyDescriptor
         * @static
         * @param {lnrpc.KeyDescriptor} message KeyDescriptor
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeyDescriptor.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.raw_key_bytes = "";
                else {
                    object.raw_key_bytes = [];
                    if (options.bytes !== $Array)
                        object.raw_key_bytes = $util.newBuffer(object.raw_key_bytes);
                }
                object.key_loc = null;
            }
            if (message.raw_key_bytes != null && $Object.hasOwnProperty.call(message, "raw_key_bytes"))
                object.raw_key_bytes = options.bytes === $String ? $util.base64.encode(message.raw_key_bytes, 0, message.raw_key_bytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_key_bytes) : message.raw_key_bytes;
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                object.key_loc = $root.lnrpc.KeyLocator.toObject(message.key_loc, options, _depth + 1);
            return object;
        };

        /**
         * Converts this KeyDescriptor to JSON.
         * @function toJSON
         * @memberof lnrpc.KeyDescriptor
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeyDescriptor.prototype.toJSON = function() {
            return KeyDescriptor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for KeyDescriptor
         * @function getTypeUrl
         * @memberof lnrpc.KeyDescriptor
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        KeyDescriptor.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.KeyDescriptor";
        };

        return KeyDescriptor;
    })();

    lnrpc.ChanPointShim = (function() {

        /**
         * Properties of a ChanPointShim.
         * @typedef {Object} lnrpc.ChanPointShim.$Properties
         * @property {Long|null} [amt] ChanPointShim amt
         * @property {lnrpc.ChannelPoint.$Properties|null} [chan_point] ChanPointShim chan_point
         * @property {lnrpc.KeyDescriptor.$Properties|null} [local_key] ChanPointShim local_key
         * @property {Uint8Array|null} [remote_key] ChanPointShim remote_key
         * @property {Uint8Array|null} [pending_chan_id] ChanPointShim pending_chan_id
         * @property {number|null} [thaw_height] ChanPointShim thaw_height
         * @property {boolean|null} [musig2] ChanPointShim musig2
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChanPointShim.
         * @memberof lnrpc
         * @interface IChanPointShim
         * @augments lnrpc.ChanPointShim.$Properties
         * @deprecated Use lnrpc.ChanPointShim.$Properties instead.
         */

        /**
         * Shape of a ChanPointShim.
         * @typedef {{
         *   amt?: Long|null;
         *   chan_point?: lnrpc.ChannelPoint.$Shape|null;
         *   local_key?: lnrpc.KeyDescriptor.$Shape|null;
         *   remote_key?: Uint8Array|null;
         *   pending_chan_id?: Uint8Array|null;
         *   thaw_height?: number|null;
         *   musig2?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.ChanPointShim.$Shape
         */

        /**
         * Constructs a new ChanPointShim.
         * @memberof lnrpc
         * @classdesc Represents a ChanPointShim.
         * @constructor
         * @param {lnrpc.ChanPointShim.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChanPointShim = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChanPointShim amt.
         * @member {Long} amt
         * @memberof lnrpc.ChanPointShim
         * @instance
         */
        ChanPointShim.prototype.amt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChanPointShim chan_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} chan_point
         * @memberof lnrpc.ChanPointShim
         * @instance
         */
        ChanPointShim.prototype.chan_point = null;

        /**
         * ChanPointShim local_key.
         * @member {lnrpc.KeyDescriptor.$Properties|null|undefined} local_key
         * @memberof lnrpc.ChanPointShim
         * @instance
         */
        ChanPointShim.prototype.local_key = null;

        /**
         * ChanPointShim remote_key.
         * @member {Uint8Array} remote_key
         * @memberof lnrpc.ChanPointShim
         * @instance
         */
        ChanPointShim.prototype.remote_key = $util.newBuffer([]);

        /**
         * ChanPointShim pending_chan_id.
         * @member {Uint8Array} pending_chan_id
         * @memberof lnrpc.ChanPointShim
         * @instance
         */
        ChanPointShim.prototype.pending_chan_id = $util.newBuffer([]);

        /**
         * ChanPointShim thaw_height.
         * @member {number} thaw_height
         * @memberof lnrpc.ChanPointShim
         * @instance
         */
        ChanPointShim.prototype.thaw_height = 0;

        /**
         * ChanPointShim musig2.
         * @member {boolean} musig2
         * @memberof lnrpc.ChanPointShim
         * @instance
         */
        ChanPointShim.prototype.musig2 = false;

        /**
         * Creates a new ChanPointShim instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChanPointShim
         * @static
         * @param {lnrpc.ChanPointShim.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChanPointShim} ChanPointShim instance
         * @type {{
         *   (properties: lnrpc.ChanPointShim.$Shape): lnrpc.ChanPointShim & lnrpc.ChanPointShim.$Shape;
         *   (properties?: lnrpc.ChanPointShim.$Properties): lnrpc.ChanPointShim;
         * }}
         */
        ChanPointShim.create = function(properties) {
            return new ChanPointShim(properties);
        };

        /**
         * Encodes the specified ChanPointShim message. Does not implicitly {@link lnrpc.ChanPointShim.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChanPointShim
         * @static
         * @param {lnrpc.ChanPointShim.$Properties} message ChanPointShim message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChanPointShim.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt") && (typeof message.amt === "object" ? message.amt.low || message.amt.high : message.amt !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.amt);
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                $root.lnrpc.ChannelPoint.encode(message.chan_point, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.local_key != null && $Object.hasOwnProperty.call(message, "local_key"))
                $root.lnrpc.KeyDescriptor.encode(message.local_key, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.remote_key != null && $Object.hasOwnProperty.call(message, "remote_key") && message.remote_key.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.remote_key);
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id") && message.pending_chan_id.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.pending_chan_id);
            if (message.thaw_height != null && $Object.hasOwnProperty.call(message, "thaw_height") && message.thaw_height !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.thaw_height);
            if (message.musig2 != null && $Object.hasOwnProperty.call(message, "musig2") && message.musig2 !== false)
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.musig2);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChanPointShim message, length delimited. Does not implicitly {@link lnrpc.ChanPointShim.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChanPointShim
         * @static
         * @param {lnrpc.ChanPointShim.$Properties} message ChanPointShim message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChanPointShim.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChanPointShim message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChanPointShim
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChanPointShim & lnrpc.ChanPointShim.$Shape} ChanPointShim
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChanPointShim.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChanPointShim(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt = value;
                        else
                            delete message.amt;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.chan_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_point);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.local_key = $root.lnrpc.KeyDescriptor.decode(reader, reader.uint32(), $undefined, _depth + 1, message.local_key);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.remote_key = value;
                        else
                            delete message.remote_key;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pending_chan_id = value;
                        else
                            delete message.pending_chan_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.thaw_height = value;
                        else
                            delete message.thaw_height;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.musig2 = value;
                        else
                            delete message.musig2;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChanPointShim message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChanPointShim
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChanPointShim & lnrpc.ChanPointShim.$Shape} ChanPointShim
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChanPointShim.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChanPointShim message.
         * @function verify
         * @memberof lnrpc.ChanPointShim
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChanPointShim.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt"))
                if (!$util.isInteger(message.amt) && !(message.amt && $util.isInteger(message.amt.low) && $util.isInteger(message.amt.high)))
                    return "amt: integer|Long expected";
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.chan_point, _depth + 1);
                if (error)
                    return "chan_point." + error;
            }
            if (message.local_key != null && $Object.hasOwnProperty.call(message, "local_key")) {
                let error = $root.lnrpc.KeyDescriptor.verify(message.local_key, _depth + 1);
                if (error)
                    return "local_key." + error;
            }
            if (message.remote_key != null && $Object.hasOwnProperty.call(message, "remote_key"))
                if (!(message.remote_key && typeof message.remote_key.length === "number" || $util.isString(message.remote_key)))
                    return "remote_key: buffer expected";
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                if (!(message.pending_chan_id && typeof message.pending_chan_id.length === "number" || $util.isString(message.pending_chan_id)))
                    return "pending_chan_id: buffer expected";
            if (message.thaw_height != null && $Object.hasOwnProperty.call(message, "thaw_height"))
                if (!$util.isInteger(message.thaw_height))
                    return "thaw_height: integer expected";
            if (message.musig2 != null && $Object.hasOwnProperty.call(message, "musig2"))
                if (typeof message.musig2 !== "boolean")
                    return "musig2: boolean expected";
            return null;
        };

        /**
         * Creates a ChanPointShim message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChanPointShim
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChanPointShim} ChanPointShim
         */
        ChanPointShim.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChanPointShim)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChanPointShim: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChanPointShim();
            if (object.amt != null)
                if (typeof object.amt === "object" ? object.amt.low || object.amt.high : $Number(object.amt) !== 0)
                    if ($util.Long)
                        message.amt = $util.Long.fromValue(object.amt, false);
                    else if (typeof object.amt === "string")
                        message.amt = $parseInt(object.amt, 10);
                    else if (typeof object.amt === "number")
                        message.amt = object.amt;
                    else if (typeof object.amt === "object")
                        message.amt = new $util.LongBits(object.amt.low >>> 0, object.amt.high >>> 0).toNumber();
            if (object.chan_point != null) {
                if (!$util.isObject(object.chan_point))
                    throw $TypeError(".lnrpc.ChanPointShim.chan_point: object expected");
                message.chan_point = $root.lnrpc.ChannelPoint.fromObject(object.chan_point, _depth + 1);
            }
            if (object.local_key != null) {
                if (!$util.isObject(object.local_key))
                    throw $TypeError(".lnrpc.ChanPointShim.local_key: object expected");
                message.local_key = $root.lnrpc.KeyDescriptor.fromObject(object.local_key, _depth + 1);
            }
            if (object.remote_key != null)
                if (object.remote_key.length)
                    if (typeof object.remote_key === "string")
                        $util.base64.decode(object.remote_key, message.remote_key = $util.newBuffer($util.base64.length(object.remote_key)), 0);
                    else if (object.remote_key.length >= 0)
                        message.remote_key = object.remote_key;
            if (object.pending_chan_id != null)
                if (object.pending_chan_id.length)
                    if (typeof object.pending_chan_id === "string")
                        $util.base64.decode(object.pending_chan_id, message.pending_chan_id = $util.newBuffer($util.base64.length(object.pending_chan_id)), 0);
                    else if (object.pending_chan_id.length >= 0)
                        message.pending_chan_id = object.pending_chan_id;
            if (object.thaw_height != null)
                if ($Number(object.thaw_height) !== 0)
                    message.thaw_height = object.thaw_height >>> 0;
            if (object.musig2 != null)
                if (object.musig2)
                    message.musig2 = $Boolean(object.musig2);
            return message;
        };

        /**
         * Creates a plain object from a ChanPointShim message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChanPointShim
         * @static
         * @param {lnrpc.ChanPointShim} message ChanPointShim
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChanPointShim.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.chan_point = null;
                object.local_key = null;
                if (options.bytes === $String)
                    object.remote_key = "";
                else {
                    object.remote_key = [];
                    if (options.bytes !== $Array)
                        object.remote_key = $util.newBuffer(object.remote_key);
                }
                if (options.bytes === $String)
                    object.pending_chan_id = "";
                else {
                    object.pending_chan_id = [];
                    if (options.bytes !== $Array)
                        object.pending_chan_id = $util.newBuffer(object.pending_chan_id);
                }
                object.thaw_height = 0;
                object.musig2 = false;
            }
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt = typeof message.amt === "number" ? $BigInt(message.amt) : $util.Long.fromBits(message.amt.low >>> 0, message.amt.high >>> 0, false).toBigInt();
                else if (typeof message.amt === "number")
                    object.amt = options.longs === $String ? $String(message.amt) : message.amt;
                else
                    object.amt = options.longs === $String ? $util.Long.prototype.toString.call(message.amt) : options.longs === $Number ? new $util.LongBits(message.amt.low >>> 0, message.amt.high >>> 0).toNumber() : message.amt;
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                object.chan_point = $root.lnrpc.ChannelPoint.toObject(message.chan_point, options, _depth + 1);
            if (message.local_key != null && $Object.hasOwnProperty.call(message, "local_key"))
                object.local_key = $root.lnrpc.KeyDescriptor.toObject(message.local_key, options, _depth + 1);
            if (message.remote_key != null && $Object.hasOwnProperty.call(message, "remote_key"))
                object.remote_key = options.bytes === $String ? $util.base64.encode(message.remote_key, 0, message.remote_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.remote_key) : message.remote_key;
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                object.pending_chan_id = options.bytes === $String ? $util.base64.encode(message.pending_chan_id, 0, message.pending_chan_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pending_chan_id) : message.pending_chan_id;
            if (message.thaw_height != null && $Object.hasOwnProperty.call(message, "thaw_height"))
                object.thaw_height = message.thaw_height;
            if (message.musig2 != null && $Object.hasOwnProperty.call(message, "musig2"))
                object.musig2 = message.musig2;
            return object;
        };

        /**
         * Converts this ChanPointShim to JSON.
         * @function toJSON
         * @memberof lnrpc.ChanPointShim
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChanPointShim.prototype.toJSON = function() {
            return ChanPointShim.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChanPointShim
         * @function getTypeUrl
         * @memberof lnrpc.ChanPointShim
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChanPointShim.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChanPointShim";
        };

        return ChanPointShim;
    })();

    lnrpc.PsbtShim = (function() {

        /**
         * Properties of a PsbtShim.
         * @typedef {Object} lnrpc.PsbtShim.$Properties
         * @property {Uint8Array|null} [pending_chan_id] PsbtShim pending_chan_id
         * @property {Uint8Array|null} [base_psbt] PsbtShim base_psbt
         * @property {boolean|null} [no_publish] PsbtShim no_publish
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PsbtShim.
         * @memberof lnrpc
         * @interface IPsbtShim
         * @augments lnrpc.PsbtShim.$Properties
         * @deprecated Use lnrpc.PsbtShim.$Properties instead.
         */

        /**
         * Shape of a PsbtShim.
         * @typedef {lnrpc.PsbtShim.$Properties} lnrpc.PsbtShim.$Shape
         */

        /**
         * Constructs a new PsbtShim.
         * @memberof lnrpc
         * @classdesc Represents a PsbtShim.
         * @constructor
         * @param {lnrpc.PsbtShim.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PsbtShim = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PsbtShim pending_chan_id.
         * @member {Uint8Array} pending_chan_id
         * @memberof lnrpc.PsbtShim
         * @instance
         */
        PsbtShim.prototype.pending_chan_id = $util.newBuffer([]);

        /**
         * PsbtShim base_psbt.
         * @member {Uint8Array} base_psbt
         * @memberof lnrpc.PsbtShim
         * @instance
         */
        PsbtShim.prototype.base_psbt = $util.newBuffer([]);

        /**
         * PsbtShim no_publish.
         * @member {boolean} no_publish
         * @memberof lnrpc.PsbtShim
         * @instance
         */
        PsbtShim.prototype.no_publish = false;

        /**
         * Creates a new PsbtShim instance using the specified properties.
         * @function create
         * @memberof lnrpc.PsbtShim
         * @static
         * @param {lnrpc.PsbtShim.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PsbtShim} PsbtShim instance
         * @type {{
         *   (properties: lnrpc.PsbtShim.$Shape): lnrpc.PsbtShim & lnrpc.PsbtShim.$Shape;
         *   (properties?: lnrpc.PsbtShim.$Properties): lnrpc.PsbtShim;
         * }}
         */
        PsbtShim.create = function(properties) {
            return new PsbtShim(properties);
        };

        /**
         * Encodes the specified PsbtShim message. Does not implicitly {@link lnrpc.PsbtShim.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PsbtShim
         * @static
         * @param {lnrpc.PsbtShim.$Properties} message PsbtShim message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PsbtShim.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id") && message.pending_chan_id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pending_chan_id);
            if (message.base_psbt != null && $Object.hasOwnProperty.call(message, "base_psbt") && message.base_psbt.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.base_psbt);
            if (message.no_publish != null && $Object.hasOwnProperty.call(message, "no_publish") && message.no_publish !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.no_publish);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PsbtShim message, length delimited. Does not implicitly {@link lnrpc.PsbtShim.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PsbtShim
         * @static
         * @param {lnrpc.PsbtShim.$Properties} message PsbtShim message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PsbtShim.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PsbtShim message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PsbtShim
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PsbtShim & lnrpc.PsbtShim.$Shape} PsbtShim
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PsbtShim.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PsbtShim(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pending_chan_id = value;
                        else
                            delete message.pending_chan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.base_psbt = value;
                        else
                            delete message.base_psbt;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.no_publish = value;
                        else
                            delete message.no_publish;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PsbtShim message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PsbtShim
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PsbtShim & lnrpc.PsbtShim.$Shape} PsbtShim
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PsbtShim.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PsbtShim message.
         * @function verify
         * @memberof lnrpc.PsbtShim
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PsbtShim.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                if (!(message.pending_chan_id && typeof message.pending_chan_id.length === "number" || $util.isString(message.pending_chan_id)))
                    return "pending_chan_id: buffer expected";
            if (message.base_psbt != null && $Object.hasOwnProperty.call(message, "base_psbt"))
                if (!(message.base_psbt && typeof message.base_psbt.length === "number" || $util.isString(message.base_psbt)))
                    return "base_psbt: buffer expected";
            if (message.no_publish != null && $Object.hasOwnProperty.call(message, "no_publish"))
                if (typeof message.no_publish !== "boolean")
                    return "no_publish: boolean expected";
            return null;
        };

        /**
         * Creates a PsbtShim message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PsbtShim
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PsbtShim} PsbtShim
         */
        PsbtShim.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PsbtShim)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PsbtShim: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PsbtShim();
            if (object.pending_chan_id != null)
                if (object.pending_chan_id.length)
                    if (typeof object.pending_chan_id === "string")
                        $util.base64.decode(object.pending_chan_id, message.pending_chan_id = $util.newBuffer($util.base64.length(object.pending_chan_id)), 0);
                    else if (object.pending_chan_id.length >= 0)
                        message.pending_chan_id = object.pending_chan_id;
            if (object.base_psbt != null)
                if (object.base_psbt.length)
                    if (typeof object.base_psbt === "string")
                        $util.base64.decode(object.base_psbt, message.base_psbt = $util.newBuffer($util.base64.length(object.base_psbt)), 0);
                    else if (object.base_psbt.length >= 0)
                        message.base_psbt = object.base_psbt;
            if (object.no_publish != null)
                if (object.no_publish)
                    message.no_publish = $Boolean(object.no_publish);
            return message;
        };

        /**
         * Creates a plain object from a PsbtShim message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PsbtShim
         * @static
         * @param {lnrpc.PsbtShim} message PsbtShim
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PsbtShim.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.pending_chan_id = "";
                else {
                    object.pending_chan_id = [];
                    if (options.bytes !== $Array)
                        object.pending_chan_id = $util.newBuffer(object.pending_chan_id);
                }
                if (options.bytes === $String)
                    object.base_psbt = "";
                else {
                    object.base_psbt = [];
                    if (options.bytes !== $Array)
                        object.base_psbt = $util.newBuffer(object.base_psbt);
                }
                object.no_publish = false;
            }
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                object.pending_chan_id = options.bytes === $String ? $util.base64.encode(message.pending_chan_id, 0, message.pending_chan_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pending_chan_id) : message.pending_chan_id;
            if (message.base_psbt != null && $Object.hasOwnProperty.call(message, "base_psbt"))
                object.base_psbt = options.bytes === $String ? $util.base64.encode(message.base_psbt, 0, message.base_psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.base_psbt) : message.base_psbt;
            if (message.no_publish != null && $Object.hasOwnProperty.call(message, "no_publish"))
                object.no_publish = message.no_publish;
            return object;
        };

        /**
         * Converts this PsbtShim to JSON.
         * @function toJSON
         * @memberof lnrpc.PsbtShim
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PsbtShim.prototype.toJSON = function() {
            return PsbtShim.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PsbtShim
         * @function getTypeUrl
         * @memberof lnrpc.PsbtShim
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PsbtShim.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PsbtShim";
        };

        return PsbtShim;
    })();

    lnrpc.FundingShim = (function() {

        /**
         * Properties of a FundingShim.
         * @typedef {Object} lnrpc.FundingShim.$Properties
         * @property {lnrpc.ChanPointShim.$Properties|null} [chan_point_shim] FundingShim chan_point_shim
         * @property {lnrpc.PsbtShim.$Properties|null} [psbt_shim] FundingShim psbt_shim
         * @property {"chan_point_shim"|"psbt_shim"} [shim] FundingShim shim
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FundingShim.
         * @memberof lnrpc
         * @interface IFundingShim
         * @augments lnrpc.FundingShim.$Properties
         * @deprecated Use lnrpc.FundingShim.$Properties instead.
         */

        /**
         * Narrowed shape of a FundingShim.
         * @typedef {{
         *   chan_point_shim?: lnrpc.ChanPointShim.$Shape|null;
         *   psbt_shim?: lnrpc.PsbtShim.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ shim?: undefined; chan_point_shim?: null; psbt_shim?: null }|{ shim?: "chan_point_shim"; chan_point_shim: lnrpc.ChanPointShim.$Shape; psbt_shim?: null }|{ shim?: "psbt_shim"; chan_point_shim?: null; psbt_shim: lnrpc.PsbtShim.$Shape })
         * )} lnrpc.FundingShim.$Shape
         */

        /**
         * Constructs a new FundingShim.
         * @memberof lnrpc
         * @classdesc Represents a FundingShim.
         * @constructor
         * @param {lnrpc.FundingShim.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FundingShim = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FundingShim chan_point_shim.
         * @member {lnrpc.ChanPointShim.$Properties|null|undefined} chan_point_shim
         * @memberof lnrpc.FundingShim
         * @instance
         */
        FundingShim.prototype.chan_point_shim = null;

        /**
         * FundingShim psbt_shim.
         * @member {lnrpc.PsbtShim.$Properties|null|undefined} psbt_shim
         * @memberof lnrpc.FundingShim
         * @instance
         */
        FundingShim.prototype.psbt_shim = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * FundingShim shim.
         * @member {"chan_point_shim"|"psbt_shim"|undefined} shim
         * @memberof lnrpc.FundingShim
         * @instance
         */
        $Object.defineProperty(FundingShim.prototype, "shim", {
            get: $util.oneOfGetter($oneOfFields = ["chan_point_shim", "psbt_shim"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new FundingShim instance using the specified properties.
         * @function create
         * @memberof lnrpc.FundingShim
         * @static
         * @param {lnrpc.FundingShim.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FundingShim} FundingShim instance
         * @type {{
         *   (properties: lnrpc.FundingShim.$Shape): lnrpc.FundingShim & lnrpc.FundingShim.$Shape;
         *   (properties?: lnrpc.FundingShim.$Properties): lnrpc.FundingShim;
         * }}
         */
        FundingShim.create = function(properties) {
            return new FundingShim(properties);
        };

        /**
         * Encodes the specified FundingShim message. Does not implicitly {@link lnrpc.FundingShim.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FundingShim
         * @static
         * @param {lnrpc.FundingShim.$Properties} message FundingShim message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingShim.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_point_shim != null && $Object.hasOwnProperty.call(message, "chan_point_shim"))
                $root.lnrpc.ChanPointShim.encode(message.chan_point_shim, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.psbt_shim != null && $Object.hasOwnProperty.call(message, "psbt_shim"))
                $root.lnrpc.PsbtShim.encode(message.psbt_shim, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FundingShim message, length delimited. Does not implicitly {@link lnrpc.FundingShim.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FundingShim
         * @static
         * @param {lnrpc.FundingShim.$Properties} message FundingShim message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingShim.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FundingShim message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FundingShim
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FundingShim & lnrpc.FundingShim.$Shape} FundingShim
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingShim.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FundingShim();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.chan_point_shim = $root.lnrpc.ChanPointShim.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_point_shim);
                        message.shim = "chan_point_shim";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.psbt_shim = $root.lnrpc.PsbtShim.decode(reader, reader.uint32(), $undefined, _depth + 1, message.psbt_shim);
                        message.shim = "psbt_shim";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FundingShim message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FundingShim
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FundingShim & lnrpc.FundingShim.$Shape} FundingShim
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingShim.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FundingShim message.
         * @function verify
         * @memberof lnrpc.FundingShim
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FundingShim.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.chan_point_shim != null && $Object.hasOwnProperty.call(message, "chan_point_shim")) {
                properties.shim = 1;
                {
                    let error = $root.lnrpc.ChanPointShim.verify(message.chan_point_shim, _depth + 1);
                    if (error)
                        return "chan_point_shim." + error;
                }
            }
            if (message.psbt_shim != null && $Object.hasOwnProperty.call(message, "psbt_shim")) {
                if (properties.shim === 1)
                    return "shim: multiple values";
                properties.shim = 1;
                {
                    let error = $root.lnrpc.PsbtShim.verify(message.psbt_shim, _depth + 1);
                    if (error)
                        return "psbt_shim." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FundingShim message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FundingShim
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FundingShim} FundingShim
         */
        FundingShim.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FundingShim)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FundingShim: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.FundingShim();
            if (object.chan_point_shim != null) {
                if (!$util.isObject(object.chan_point_shim))
                    throw $TypeError(".lnrpc.FundingShim.chan_point_shim: object expected");
                message.chan_point_shim = $root.lnrpc.ChanPointShim.fromObject(object.chan_point_shim, _depth + 1);
            }
            if (object.psbt_shim != null) {
                if (!$util.isObject(object.psbt_shim))
                    throw $TypeError(".lnrpc.FundingShim.psbt_shim: object expected");
                message.psbt_shim = $root.lnrpc.PsbtShim.fromObject(object.psbt_shim, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FundingShim message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FundingShim
         * @static
         * @param {lnrpc.FundingShim} message FundingShim
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FundingShim.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.chan_point_shim != null && $Object.hasOwnProperty.call(message, "chan_point_shim")) {
                object.chan_point_shim = $root.lnrpc.ChanPointShim.toObject(message.chan_point_shim, options, _depth + 1);
                if (options.oneofs)
                    object.shim = "chan_point_shim";
            }
            if (message.psbt_shim != null && $Object.hasOwnProperty.call(message, "psbt_shim")) {
                object.psbt_shim = $root.lnrpc.PsbtShim.toObject(message.psbt_shim, options, _depth + 1);
                if (options.oneofs)
                    object.shim = "psbt_shim";
            }
            return object;
        };

        /**
         * Converts this FundingShim to JSON.
         * @function toJSON
         * @memberof lnrpc.FundingShim
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FundingShim.prototype.toJSON = function() {
            return FundingShim.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FundingShim
         * @function getTypeUrl
         * @memberof lnrpc.FundingShim
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FundingShim.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FundingShim";
        };

        return FundingShim;
    })();

    lnrpc.FundingShimCancel = (function() {

        /**
         * Properties of a FundingShimCancel.
         * @typedef {Object} lnrpc.FundingShimCancel.$Properties
         * @property {Uint8Array|null} [pending_chan_id] FundingShimCancel pending_chan_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FundingShimCancel.
         * @memberof lnrpc
         * @interface IFundingShimCancel
         * @augments lnrpc.FundingShimCancel.$Properties
         * @deprecated Use lnrpc.FundingShimCancel.$Properties instead.
         */

        /**
         * Shape of a FundingShimCancel.
         * @typedef {lnrpc.FundingShimCancel.$Properties} lnrpc.FundingShimCancel.$Shape
         */

        /**
         * Constructs a new FundingShimCancel.
         * @memberof lnrpc
         * @classdesc Represents a FundingShimCancel.
         * @constructor
         * @param {lnrpc.FundingShimCancel.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FundingShimCancel = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FundingShimCancel pending_chan_id.
         * @member {Uint8Array} pending_chan_id
         * @memberof lnrpc.FundingShimCancel
         * @instance
         */
        FundingShimCancel.prototype.pending_chan_id = $util.newBuffer([]);

        /**
         * Creates a new FundingShimCancel instance using the specified properties.
         * @function create
         * @memberof lnrpc.FundingShimCancel
         * @static
         * @param {lnrpc.FundingShimCancel.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FundingShimCancel} FundingShimCancel instance
         * @type {{
         *   (properties: lnrpc.FundingShimCancel.$Shape): lnrpc.FundingShimCancel & lnrpc.FundingShimCancel.$Shape;
         *   (properties?: lnrpc.FundingShimCancel.$Properties): lnrpc.FundingShimCancel;
         * }}
         */
        FundingShimCancel.create = function(properties) {
            return new FundingShimCancel(properties);
        };

        /**
         * Encodes the specified FundingShimCancel message. Does not implicitly {@link lnrpc.FundingShimCancel.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FundingShimCancel
         * @static
         * @param {lnrpc.FundingShimCancel.$Properties} message FundingShimCancel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingShimCancel.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id") && message.pending_chan_id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pending_chan_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FundingShimCancel message, length delimited. Does not implicitly {@link lnrpc.FundingShimCancel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FundingShimCancel
         * @static
         * @param {lnrpc.FundingShimCancel.$Properties} message FundingShimCancel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingShimCancel.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FundingShimCancel message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FundingShimCancel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FundingShimCancel & lnrpc.FundingShimCancel.$Shape} FundingShimCancel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingShimCancel.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FundingShimCancel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pending_chan_id = value;
                        else
                            delete message.pending_chan_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FundingShimCancel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FundingShimCancel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FundingShimCancel & lnrpc.FundingShimCancel.$Shape} FundingShimCancel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingShimCancel.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FundingShimCancel message.
         * @function verify
         * @memberof lnrpc.FundingShimCancel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FundingShimCancel.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                if (!(message.pending_chan_id && typeof message.pending_chan_id.length === "number" || $util.isString(message.pending_chan_id)))
                    return "pending_chan_id: buffer expected";
            return null;
        };

        /**
         * Creates a FundingShimCancel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FundingShimCancel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FundingShimCancel} FundingShimCancel
         */
        FundingShimCancel.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FundingShimCancel)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FundingShimCancel: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.FundingShimCancel();
            if (object.pending_chan_id != null)
                if (object.pending_chan_id.length)
                    if (typeof object.pending_chan_id === "string")
                        $util.base64.decode(object.pending_chan_id, message.pending_chan_id = $util.newBuffer($util.base64.length(object.pending_chan_id)), 0);
                    else if (object.pending_chan_id.length >= 0)
                        message.pending_chan_id = object.pending_chan_id;
            return message;
        };

        /**
         * Creates a plain object from a FundingShimCancel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FundingShimCancel
         * @static
         * @param {lnrpc.FundingShimCancel} message FundingShimCancel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FundingShimCancel.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.pending_chan_id = "";
                else {
                    object.pending_chan_id = [];
                    if (options.bytes !== $Array)
                        object.pending_chan_id = $util.newBuffer(object.pending_chan_id);
                }
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                object.pending_chan_id = options.bytes === $String ? $util.base64.encode(message.pending_chan_id, 0, message.pending_chan_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pending_chan_id) : message.pending_chan_id;
            return object;
        };

        /**
         * Converts this FundingShimCancel to JSON.
         * @function toJSON
         * @memberof lnrpc.FundingShimCancel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FundingShimCancel.prototype.toJSON = function() {
            return FundingShimCancel.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FundingShimCancel
         * @function getTypeUrl
         * @memberof lnrpc.FundingShimCancel
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FundingShimCancel.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FundingShimCancel";
        };

        return FundingShimCancel;
    })();

    lnrpc.FundingPsbtVerify = (function() {

        /**
         * Properties of a FundingPsbtVerify.
         * @typedef {Object} lnrpc.FundingPsbtVerify.$Properties
         * @property {Uint8Array|null} [funded_psbt] FundingPsbtVerify funded_psbt
         * @property {Uint8Array|null} [pending_chan_id] FundingPsbtVerify pending_chan_id
         * @property {boolean|null} [skip_finalize] FundingPsbtVerify skip_finalize
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FundingPsbtVerify.
         * @memberof lnrpc
         * @interface IFundingPsbtVerify
         * @augments lnrpc.FundingPsbtVerify.$Properties
         * @deprecated Use lnrpc.FundingPsbtVerify.$Properties instead.
         */

        /**
         * Shape of a FundingPsbtVerify.
         * @typedef {lnrpc.FundingPsbtVerify.$Properties} lnrpc.FundingPsbtVerify.$Shape
         */

        /**
         * Constructs a new FundingPsbtVerify.
         * @memberof lnrpc
         * @classdesc Represents a FundingPsbtVerify.
         * @constructor
         * @param {lnrpc.FundingPsbtVerify.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FundingPsbtVerify = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FundingPsbtVerify funded_psbt.
         * @member {Uint8Array} funded_psbt
         * @memberof lnrpc.FundingPsbtVerify
         * @instance
         */
        FundingPsbtVerify.prototype.funded_psbt = $util.newBuffer([]);

        /**
         * FundingPsbtVerify pending_chan_id.
         * @member {Uint8Array} pending_chan_id
         * @memberof lnrpc.FundingPsbtVerify
         * @instance
         */
        FundingPsbtVerify.prototype.pending_chan_id = $util.newBuffer([]);

        /**
         * FundingPsbtVerify skip_finalize.
         * @member {boolean} skip_finalize
         * @memberof lnrpc.FundingPsbtVerify
         * @instance
         */
        FundingPsbtVerify.prototype.skip_finalize = false;

        /**
         * Creates a new FundingPsbtVerify instance using the specified properties.
         * @function create
         * @memberof lnrpc.FundingPsbtVerify
         * @static
         * @param {lnrpc.FundingPsbtVerify.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FundingPsbtVerify} FundingPsbtVerify instance
         * @type {{
         *   (properties: lnrpc.FundingPsbtVerify.$Shape): lnrpc.FundingPsbtVerify & lnrpc.FundingPsbtVerify.$Shape;
         *   (properties?: lnrpc.FundingPsbtVerify.$Properties): lnrpc.FundingPsbtVerify;
         * }}
         */
        FundingPsbtVerify.create = function(properties) {
            return new FundingPsbtVerify(properties);
        };

        /**
         * Encodes the specified FundingPsbtVerify message. Does not implicitly {@link lnrpc.FundingPsbtVerify.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FundingPsbtVerify
         * @static
         * @param {lnrpc.FundingPsbtVerify.$Properties} message FundingPsbtVerify message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingPsbtVerify.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt") && message.funded_psbt.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.funded_psbt);
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id") && message.pending_chan_id.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.pending_chan_id);
            if (message.skip_finalize != null && $Object.hasOwnProperty.call(message, "skip_finalize") && message.skip_finalize !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.skip_finalize);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FundingPsbtVerify message, length delimited. Does not implicitly {@link lnrpc.FundingPsbtVerify.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FundingPsbtVerify
         * @static
         * @param {lnrpc.FundingPsbtVerify.$Properties} message FundingPsbtVerify message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingPsbtVerify.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FundingPsbtVerify message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FundingPsbtVerify
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FundingPsbtVerify & lnrpc.FundingPsbtVerify.$Shape} FundingPsbtVerify
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingPsbtVerify.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FundingPsbtVerify(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.funded_psbt = value;
                        else
                            delete message.funded_psbt;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pending_chan_id = value;
                        else
                            delete message.pending_chan_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.skip_finalize = value;
                        else
                            delete message.skip_finalize;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FundingPsbtVerify message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FundingPsbtVerify
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FundingPsbtVerify & lnrpc.FundingPsbtVerify.$Shape} FundingPsbtVerify
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingPsbtVerify.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FundingPsbtVerify message.
         * @function verify
         * @memberof lnrpc.FundingPsbtVerify
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FundingPsbtVerify.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt"))
                if (!(message.funded_psbt && typeof message.funded_psbt.length === "number" || $util.isString(message.funded_psbt)))
                    return "funded_psbt: buffer expected";
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                if (!(message.pending_chan_id && typeof message.pending_chan_id.length === "number" || $util.isString(message.pending_chan_id)))
                    return "pending_chan_id: buffer expected";
            if (message.skip_finalize != null && $Object.hasOwnProperty.call(message, "skip_finalize"))
                if (typeof message.skip_finalize !== "boolean")
                    return "skip_finalize: boolean expected";
            return null;
        };

        /**
         * Creates a FundingPsbtVerify message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FundingPsbtVerify
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FundingPsbtVerify} FundingPsbtVerify
         */
        FundingPsbtVerify.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FundingPsbtVerify)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FundingPsbtVerify: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.FundingPsbtVerify();
            if (object.funded_psbt != null)
                if (object.funded_psbt.length)
                    if (typeof object.funded_psbt === "string")
                        $util.base64.decode(object.funded_psbt, message.funded_psbt = $util.newBuffer($util.base64.length(object.funded_psbt)), 0);
                    else if (object.funded_psbt.length >= 0)
                        message.funded_psbt = object.funded_psbt;
            if (object.pending_chan_id != null)
                if (object.pending_chan_id.length)
                    if (typeof object.pending_chan_id === "string")
                        $util.base64.decode(object.pending_chan_id, message.pending_chan_id = $util.newBuffer($util.base64.length(object.pending_chan_id)), 0);
                    else if (object.pending_chan_id.length >= 0)
                        message.pending_chan_id = object.pending_chan_id;
            if (object.skip_finalize != null)
                if (object.skip_finalize)
                    message.skip_finalize = $Boolean(object.skip_finalize);
            return message;
        };

        /**
         * Creates a plain object from a FundingPsbtVerify message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FundingPsbtVerify
         * @static
         * @param {lnrpc.FundingPsbtVerify} message FundingPsbtVerify
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FundingPsbtVerify.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.funded_psbt = "";
                else {
                    object.funded_psbt = [];
                    if (options.bytes !== $Array)
                        object.funded_psbt = $util.newBuffer(object.funded_psbt);
                }
                if (options.bytes === $String)
                    object.pending_chan_id = "";
                else {
                    object.pending_chan_id = [];
                    if (options.bytes !== $Array)
                        object.pending_chan_id = $util.newBuffer(object.pending_chan_id);
                }
                object.skip_finalize = false;
            }
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt"))
                object.funded_psbt = options.bytes === $String ? $util.base64.encode(message.funded_psbt, 0, message.funded_psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.funded_psbt) : message.funded_psbt;
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                object.pending_chan_id = options.bytes === $String ? $util.base64.encode(message.pending_chan_id, 0, message.pending_chan_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pending_chan_id) : message.pending_chan_id;
            if (message.skip_finalize != null && $Object.hasOwnProperty.call(message, "skip_finalize"))
                object.skip_finalize = message.skip_finalize;
            return object;
        };

        /**
         * Converts this FundingPsbtVerify to JSON.
         * @function toJSON
         * @memberof lnrpc.FundingPsbtVerify
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FundingPsbtVerify.prototype.toJSON = function() {
            return FundingPsbtVerify.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FundingPsbtVerify
         * @function getTypeUrl
         * @memberof lnrpc.FundingPsbtVerify
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FundingPsbtVerify.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FundingPsbtVerify";
        };

        return FundingPsbtVerify;
    })();

    lnrpc.FundingPsbtFinalize = (function() {

        /**
         * Properties of a FundingPsbtFinalize.
         * @typedef {Object} lnrpc.FundingPsbtFinalize.$Properties
         * @property {Uint8Array|null} [signed_psbt] FundingPsbtFinalize signed_psbt
         * @property {Uint8Array|null} [pending_chan_id] FundingPsbtFinalize pending_chan_id
         * @property {Uint8Array|null} [final_raw_tx] FundingPsbtFinalize final_raw_tx
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FundingPsbtFinalize.
         * @memberof lnrpc
         * @interface IFundingPsbtFinalize
         * @augments lnrpc.FundingPsbtFinalize.$Properties
         * @deprecated Use lnrpc.FundingPsbtFinalize.$Properties instead.
         */

        /**
         * Shape of a FundingPsbtFinalize.
         * @typedef {lnrpc.FundingPsbtFinalize.$Properties} lnrpc.FundingPsbtFinalize.$Shape
         */

        /**
         * Constructs a new FundingPsbtFinalize.
         * @memberof lnrpc
         * @classdesc Represents a FundingPsbtFinalize.
         * @constructor
         * @param {lnrpc.FundingPsbtFinalize.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FundingPsbtFinalize = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FundingPsbtFinalize signed_psbt.
         * @member {Uint8Array} signed_psbt
         * @memberof lnrpc.FundingPsbtFinalize
         * @instance
         */
        FundingPsbtFinalize.prototype.signed_psbt = $util.newBuffer([]);

        /**
         * FundingPsbtFinalize pending_chan_id.
         * @member {Uint8Array} pending_chan_id
         * @memberof lnrpc.FundingPsbtFinalize
         * @instance
         */
        FundingPsbtFinalize.prototype.pending_chan_id = $util.newBuffer([]);

        /**
         * FundingPsbtFinalize final_raw_tx.
         * @member {Uint8Array} final_raw_tx
         * @memberof lnrpc.FundingPsbtFinalize
         * @instance
         */
        FundingPsbtFinalize.prototype.final_raw_tx = $util.newBuffer([]);

        /**
         * Creates a new FundingPsbtFinalize instance using the specified properties.
         * @function create
         * @memberof lnrpc.FundingPsbtFinalize
         * @static
         * @param {lnrpc.FundingPsbtFinalize.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FundingPsbtFinalize} FundingPsbtFinalize instance
         * @type {{
         *   (properties: lnrpc.FundingPsbtFinalize.$Shape): lnrpc.FundingPsbtFinalize & lnrpc.FundingPsbtFinalize.$Shape;
         *   (properties?: lnrpc.FundingPsbtFinalize.$Properties): lnrpc.FundingPsbtFinalize;
         * }}
         */
        FundingPsbtFinalize.create = function(properties) {
            return new FundingPsbtFinalize(properties);
        };

        /**
         * Encodes the specified FundingPsbtFinalize message. Does not implicitly {@link lnrpc.FundingPsbtFinalize.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FundingPsbtFinalize
         * @static
         * @param {lnrpc.FundingPsbtFinalize.$Properties} message FundingPsbtFinalize message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingPsbtFinalize.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.signed_psbt != null && $Object.hasOwnProperty.call(message, "signed_psbt") && message.signed_psbt.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.signed_psbt);
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id") && message.pending_chan_id.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.pending_chan_id);
            if (message.final_raw_tx != null && $Object.hasOwnProperty.call(message, "final_raw_tx") && message.final_raw_tx.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.final_raw_tx);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FundingPsbtFinalize message, length delimited. Does not implicitly {@link lnrpc.FundingPsbtFinalize.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FundingPsbtFinalize
         * @static
         * @param {lnrpc.FundingPsbtFinalize.$Properties} message FundingPsbtFinalize message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingPsbtFinalize.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FundingPsbtFinalize message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FundingPsbtFinalize
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FundingPsbtFinalize & lnrpc.FundingPsbtFinalize.$Shape} FundingPsbtFinalize
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingPsbtFinalize.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FundingPsbtFinalize(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.signed_psbt = value;
                        else
                            delete message.signed_psbt;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pending_chan_id = value;
                        else
                            delete message.pending_chan_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.final_raw_tx = value;
                        else
                            delete message.final_raw_tx;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FundingPsbtFinalize message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FundingPsbtFinalize
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FundingPsbtFinalize & lnrpc.FundingPsbtFinalize.$Shape} FundingPsbtFinalize
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingPsbtFinalize.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FundingPsbtFinalize message.
         * @function verify
         * @memberof lnrpc.FundingPsbtFinalize
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FundingPsbtFinalize.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.signed_psbt != null && $Object.hasOwnProperty.call(message, "signed_psbt"))
                if (!(message.signed_psbt && typeof message.signed_psbt.length === "number" || $util.isString(message.signed_psbt)))
                    return "signed_psbt: buffer expected";
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                if (!(message.pending_chan_id && typeof message.pending_chan_id.length === "number" || $util.isString(message.pending_chan_id)))
                    return "pending_chan_id: buffer expected";
            if (message.final_raw_tx != null && $Object.hasOwnProperty.call(message, "final_raw_tx"))
                if (!(message.final_raw_tx && typeof message.final_raw_tx.length === "number" || $util.isString(message.final_raw_tx)))
                    return "final_raw_tx: buffer expected";
            return null;
        };

        /**
         * Creates a FundingPsbtFinalize message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FundingPsbtFinalize
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FundingPsbtFinalize} FundingPsbtFinalize
         */
        FundingPsbtFinalize.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FundingPsbtFinalize)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FundingPsbtFinalize: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.FundingPsbtFinalize();
            if (object.signed_psbt != null)
                if (object.signed_psbt.length)
                    if (typeof object.signed_psbt === "string")
                        $util.base64.decode(object.signed_psbt, message.signed_psbt = $util.newBuffer($util.base64.length(object.signed_psbt)), 0);
                    else if (object.signed_psbt.length >= 0)
                        message.signed_psbt = object.signed_psbt;
            if (object.pending_chan_id != null)
                if (object.pending_chan_id.length)
                    if (typeof object.pending_chan_id === "string")
                        $util.base64.decode(object.pending_chan_id, message.pending_chan_id = $util.newBuffer($util.base64.length(object.pending_chan_id)), 0);
                    else if (object.pending_chan_id.length >= 0)
                        message.pending_chan_id = object.pending_chan_id;
            if (object.final_raw_tx != null)
                if (object.final_raw_tx.length)
                    if (typeof object.final_raw_tx === "string")
                        $util.base64.decode(object.final_raw_tx, message.final_raw_tx = $util.newBuffer($util.base64.length(object.final_raw_tx)), 0);
                    else if (object.final_raw_tx.length >= 0)
                        message.final_raw_tx = object.final_raw_tx;
            return message;
        };

        /**
         * Creates a plain object from a FundingPsbtFinalize message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FundingPsbtFinalize
         * @static
         * @param {lnrpc.FundingPsbtFinalize} message FundingPsbtFinalize
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FundingPsbtFinalize.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.signed_psbt = "";
                else {
                    object.signed_psbt = [];
                    if (options.bytes !== $Array)
                        object.signed_psbt = $util.newBuffer(object.signed_psbt);
                }
                if (options.bytes === $String)
                    object.pending_chan_id = "";
                else {
                    object.pending_chan_id = [];
                    if (options.bytes !== $Array)
                        object.pending_chan_id = $util.newBuffer(object.pending_chan_id);
                }
                if (options.bytes === $String)
                    object.final_raw_tx = "";
                else {
                    object.final_raw_tx = [];
                    if (options.bytes !== $Array)
                        object.final_raw_tx = $util.newBuffer(object.final_raw_tx);
                }
            }
            if (message.signed_psbt != null && $Object.hasOwnProperty.call(message, "signed_psbt"))
                object.signed_psbt = options.bytes === $String ? $util.base64.encode(message.signed_psbt, 0, message.signed_psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signed_psbt) : message.signed_psbt;
            if (message.pending_chan_id != null && $Object.hasOwnProperty.call(message, "pending_chan_id"))
                object.pending_chan_id = options.bytes === $String ? $util.base64.encode(message.pending_chan_id, 0, message.pending_chan_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pending_chan_id) : message.pending_chan_id;
            if (message.final_raw_tx != null && $Object.hasOwnProperty.call(message, "final_raw_tx"))
                object.final_raw_tx = options.bytes === $String ? $util.base64.encode(message.final_raw_tx, 0, message.final_raw_tx.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.final_raw_tx) : message.final_raw_tx;
            return object;
        };

        /**
         * Converts this FundingPsbtFinalize to JSON.
         * @function toJSON
         * @memberof lnrpc.FundingPsbtFinalize
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FundingPsbtFinalize.prototype.toJSON = function() {
            return FundingPsbtFinalize.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FundingPsbtFinalize
         * @function getTypeUrl
         * @memberof lnrpc.FundingPsbtFinalize
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FundingPsbtFinalize.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FundingPsbtFinalize";
        };

        return FundingPsbtFinalize;
    })();

    lnrpc.FundingTransitionMsg = (function() {

        /**
         * Properties of a FundingTransitionMsg.
         * @typedef {Object} lnrpc.FundingTransitionMsg.$Properties
         * @property {lnrpc.FundingShim.$Properties|null} [shim_register] FundingTransitionMsg shim_register
         * @property {lnrpc.FundingShimCancel.$Properties|null} [shim_cancel] FundingTransitionMsg shim_cancel
         * @property {lnrpc.FundingPsbtVerify.$Properties|null} [psbt_verify] FundingTransitionMsg psbt_verify
         * @property {lnrpc.FundingPsbtFinalize.$Properties|null} [psbt_finalize] FundingTransitionMsg psbt_finalize
         * @property {"shim_register"|"shim_cancel"|"psbt_verify"|"psbt_finalize"} [trigger] FundingTransitionMsg trigger
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FundingTransitionMsg.
         * @memberof lnrpc
         * @interface IFundingTransitionMsg
         * @augments lnrpc.FundingTransitionMsg.$Properties
         * @deprecated Use lnrpc.FundingTransitionMsg.$Properties instead.
         */

        /**
         * Narrowed shape of a FundingTransitionMsg.
         * @typedef {{
         *   shim_register?: lnrpc.FundingShim.$Shape|null;
         *   shim_cancel?: lnrpc.FundingShimCancel.$Shape|null;
         *   psbt_verify?: lnrpc.FundingPsbtVerify.$Shape|null;
         *   psbt_finalize?: lnrpc.FundingPsbtFinalize.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ trigger?: undefined; shim_register?: null; shim_cancel?: null; psbt_verify?: null; psbt_finalize?: null }|{ trigger?: "shim_register"; shim_register: lnrpc.FundingShim.$Shape; shim_cancel?: null; psbt_verify?: null; psbt_finalize?: null }|{ trigger?: "shim_cancel"; shim_register?: null; shim_cancel: lnrpc.FundingShimCancel.$Shape; psbt_verify?: null; psbt_finalize?: null }|{ trigger?: "psbt_verify"; shim_register?: null; shim_cancel?: null; psbt_verify: lnrpc.FundingPsbtVerify.$Shape; psbt_finalize?: null }|{ trigger?: "psbt_finalize"; shim_register?: null; shim_cancel?: null; psbt_verify?: null; psbt_finalize: lnrpc.FundingPsbtFinalize.$Shape })
         * )} lnrpc.FundingTransitionMsg.$Shape
         */

        /**
         * Constructs a new FundingTransitionMsg.
         * @memberof lnrpc
         * @classdesc Represents a FundingTransitionMsg.
         * @constructor
         * @param {lnrpc.FundingTransitionMsg.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FundingTransitionMsg = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FundingTransitionMsg shim_register.
         * @member {lnrpc.FundingShim.$Properties|null|undefined} shim_register
         * @memberof lnrpc.FundingTransitionMsg
         * @instance
         */
        FundingTransitionMsg.prototype.shim_register = null;

        /**
         * FundingTransitionMsg shim_cancel.
         * @member {lnrpc.FundingShimCancel.$Properties|null|undefined} shim_cancel
         * @memberof lnrpc.FundingTransitionMsg
         * @instance
         */
        FundingTransitionMsg.prototype.shim_cancel = null;

        /**
         * FundingTransitionMsg psbt_verify.
         * @member {lnrpc.FundingPsbtVerify.$Properties|null|undefined} psbt_verify
         * @memberof lnrpc.FundingTransitionMsg
         * @instance
         */
        FundingTransitionMsg.prototype.psbt_verify = null;

        /**
         * FundingTransitionMsg psbt_finalize.
         * @member {lnrpc.FundingPsbtFinalize.$Properties|null|undefined} psbt_finalize
         * @memberof lnrpc.FundingTransitionMsg
         * @instance
         */
        FundingTransitionMsg.prototype.psbt_finalize = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * FundingTransitionMsg trigger.
         * @member {"shim_register"|"shim_cancel"|"psbt_verify"|"psbt_finalize"|undefined} trigger
         * @memberof lnrpc.FundingTransitionMsg
         * @instance
         */
        $Object.defineProperty(FundingTransitionMsg.prototype, "trigger", {
            get: $util.oneOfGetter($oneOfFields = ["shim_register", "shim_cancel", "psbt_verify", "psbt_finalize"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new FundingTransitionMsg instance using the specified properties.
         * @function create
         * @memberof lnrpc.FundingTransitionMsg
         * @static
         * @param {lnrpc.FundingTransitionMsg.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FundingTransitionMsg} FundingTransitionMsg instance
         * @type {{
         *   (properties: lnrpc.FundingTransitionMsg.$Shape): lnrpc.FundingTransitionMsg & lnrpc.FundingTransitionMsg.$Shape;
         *   (properties?: lnrpc.FundingTransitionMsg.$Properties): lnrpc.FundingTransitionMsg;
         * }}
         */
        FundingTransitionMsg.create = function(properties) {
            return new FundingTransitionMsg(properties);
        };

        /**
         * Encodes the specified FundingTransitionMsg message. Does not implicitly {@link lnrpc.FundingTransitionMsg.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FundingTransitionMsg
         * @static
         * @param {lnrpc.FundingTransitionMsg.$Properties} message FundingTransitionMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingTransitionMsg.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shim_register != null && $Object.hasOwnProperty.call(message, "shim_register"))
                $root.lnrpc.FundingShim.encode(message.shim_register, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.shim_cancel != null && $Object.hasOwnProperty.call(message, "shim_cancel"))
                $root.lnrpc.FundingShimCancel.encode(message.shim_cancel, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.psbt_verify != null && $Object.hasOwnProperty.call(message, "psbt_verify"))
                $root.lnrpc.FundingPsbtVerify.encode(message.psbt_verify, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.psbt_finalize != null && $Object.hasOwnProperty.call(message, "psbt_finalize"))
                $root.lnrpc.FundingPsbtFinalize.encode(message.psbt_finalize, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FundingTransitionMsg message, length delimited. Does not implicitly {@link lnrpc.FundingTransitionMsg.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FundingTransitionMsg
         * @static
         * @param {lnrpc.FundingTransitionMsg.$Properties} message FundingTransitionMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingTransitionMsg.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FundingTransitionMsg message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FundingTransitionMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FundingTransitionMsg & lnrpc.FundingTransitionMsg.$Shape} FundingTransitionMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingTransitionMsg.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FundingTransitionMsg();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.shim_register = $root.lnrpc.FundingShim.decode(reader, reader.uint32(), $undefined, _depth + 1, message.shim_register);
                        message.trigger = "shim_register";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.shim_cancel = $root.lnrpc.FundingShimCancel.decode(reader, reader.uint32(), $undefined, _depth + 1, message.shim_cancel);
                        message.trigger = "shim_cancel";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.psbt_verify = $root.lnrpc.FundingPsbtVerify.decode(reader, reader.uint32(), $undefined, _depth + 1, message.psbt_verify);
                        message.trigger = "psbt_verify";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.psbt_finalize = $root.lnrpc.FundingPsbtFinalize.decode(reader, reader.uint32(), $undefined, _depth + 1, message.psbt_finalize);
                        message.trigger = "psbt_finalize";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FundingTransitionMsg message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FundingTransitionMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FundingTransitionMsg & lnrpc.FundingTransitionMsg.$Shape} FundingTransitionMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingTransitionMsg.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FundingTransitionMsg message.
         * @function verify
         * @memberof lnrpc.FundingTransitionMsg
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FundingTransitionMsg.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.shim_register != null && $Object.hasOwnProperty.call(message, "shim_register")) {
                properties.trigger = 1;
                {
                    let error = $root.lnrpc.FundingShim.verify(message.shim_register, _depth + 1);
                    if (error)
                        return "shim_register." + error;
                }
            }
            if (message.shim_cancel != null && $Object.hasOwnProperty.call(message, "shim_cancel")) {
                if (properties.trigger === 1)
                    return "trigger: multiple values";
                properties.trigger = 1;
                {
                    let error = $root.lnrpc.FundingShimCancel.verify(message.shim_cancel, _depth + 1);
                    if (error)
                        return "shim_cancel." + error;
                }
            }
            if (message.psbt_verify != null && $Object.hasOwnProperty.call(message, "psbt_verify")) {
                if (properties.trigger === 1)
                    return "trigger: multiple values";
                properties.trigger = 1;
                {
                    let error = $root.lnrpc.FundingPsbtVerify.verify(message.psbt_verify, _depth + 1);
                    if (error)
                        return "psbt_verify." + error;
                }
            }
            if (message.psbt_finalize != null && $Object.hasOwnProperty.call(message, "psbt_finalize")) {
                if (properties.trigger === 1)
                    return "trigger: multiple values";
                properties.trigger = 1;
                {
                    let error = $root.lnrpc.FundingPsbtFinalize.verify(message.psbt_finalize, _depth + 1);
                    if (error)
                        return "psbt_finalize." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FundingTransitionMsg message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FundingTransitionMsg
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FundingTransitionMsg} FundingTransitionMsg
         */
        FundingTransitionMsg.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FundingTransitionMsg)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FundingTransitionMsg: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.FundingTransitionMsg();
            if (object.shim_register != null) {
                if (!$util.isObject(object.shim_register))
                    throw $TypeError(".lnrpc.FundingTransitionMsg.shim_register: object expected");
                message.shim_register = $root.lnrpc.FundingShim.fromObject(object.shim_register, _depth + 1);
            }
            if (object.shim_cancel != null) {
                if (!$util.isObject(object.shim_cancel))
                    throw $TypeError(".lnrpc.FundingTransitionMsg.shim_cancel: object expected");
                message.shim_cancel = $root.lnrpc.FundingShimCancel.fromObject(object.shim_cancel, _depth + 1);
            }
            if (object.psbt_verify != null) {
                if (!$util.isObject(object.psbt_verify))
                    throw $TypeError(".lnrpc.FundingTransitionMsg.psbt_verify: object expected");
                message.psbt_verify = $root.lnrpc.FundingPsbtVerify.fromObject(object.psbt_verify, _depth + 1);
            }
            if (object.psbt_finalize != null) {
                if (!$util.isObject(object.psbt_finalize))
                    throw $TypeError(".lnrpc.FundingTransitionMsg.psbt_finalize: object expected");
                message.psbt_finalize = $root.lnrpc.FundingPsbtFinalize.fromObject(object.psbt_finalize, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FundingTransitionMsg message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FundingTransitionMsg
         * @static
         * @param {lnrpc.FundingTransitionMsg} message FundingTransitionMsg
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FundingTransitionMsg.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.shim_register != null && $Object.hasOwnProperty.call(message, "shim_register")) {
                object.shim_register = $root.lnrpc.FundingShim.toObject(message.shim_register, options, _depth + 1);
                if (options.oneofs)
                    object.trigger = "shim_register";
            }
            if (message.shim_cancel != null && $Object.hasOwnProperty.call(message, "shim_cancel")) {
                object.shim_cancel = $root.lnrpc.FundingShimCancel.toObject(message.shim_cancel, options, _depth + 1);
                if (options.oneofs)
                    object.trigger = "shim_cancel";
            }
            if (message.psbt_verify != null && $Object.hasOwnProperty.call(message, "psbt_verify")) {
                object.psbt_verify = $root.lnrpc.FundingPsbtVerify.toObject(message.psbt_verify, options, _depth + 1);
                if (options.oneofs)
                    object.trigger = "psbt_verify";
            }
            if (message.psbt_finalize != null && $Object.hasOwnProperty.call(message, "psbt_finalize")) {
                object.psbt_finalize = $root.lnrpc.FundingPsbtFinalize.toObject(message.psbt_finalize, options, _depth + 1);
                if (options.oneofs)
                    object.trigger = "psbt_finalize";
            }
            return object;
        };

        /**
         * Converts this FundingTransitionMsg to JSON.
         * @function toJSON
         * @memberof lnrpc.FundingTransitionMsg
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FundingTransitionMsg.prototype.toJSON = function() {
            return FundingTransitionMsg.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FundingTransitionMsg
         * @function getTypeUrl
         * @memberof lnrpc.FundingTransitionMsg
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FundingTransitionMsg.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FundingTransitionMsg";
        };

        return FundingTransitionMsg;
    })();

    lnrpc.FundingStateStepResp = (function() {

        /**
         * Properties of a FundingStateStepResp.
         * @typedef {Object} lnrpc.FundingStateStepResp.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FundingStateStepResp.
         * @memberof lnrpc
         * @interface IFundingStateStepResp
         * @augments lnrpc.FundingStateStepResp.$Properties
         * @deprecated Use lnrpc.FundingStateStepResp.$Properties instead.
         */

        /**
         * Shape of a FundingStateStepResp.
         * @typedef {lnrpc.FundingStateStepResp.$Properties} lnrpc.FundingStateStepResp.$Shape
         */

        /**
         * Constructs a new FundingStateStepResp.
         * @memberof lnrpc
         * @classdesc Represents a FundingStateStepResp.
         * @constructor
         * @param {lnrpc.FundingStateStepResp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FundingStateStepResp = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new FundingStateStepResp instance using the specified properties.
         * @function create
         * @memberof lnrpc.FundingStateStepResp
         * @static
         * @param {lnrpc.FundingStateStepResp.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FundingStateStepResp} FundingStateStepResp instance
         * @type {{
         *   (properties: lnrpc.FundingStateStepResp.$Shape): lnrpc.FundingStateStepResp & lnrpc.FundingStateStepResp.$Shape;
         *   (properties?: lnrpc.FundingStateStepResp.$Properties): lnrpc.FundingStateStepResp;
         * }}
         */
        FundingStateStepResp.create = function(properties) {
            return new FundingStateStepResp(properties);
        };

        /**
         * Encodes the specified FundingStateStepResp message. Does not implicitly {@link lnrpc.FundingStateStepResp.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FundingStateStepResp
         * @static
         * @param {lnrpc.FundingStateStepResp.$Properties} message FundingStateStepResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingStateStepResp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FundingStateStepResp message, length delimited. Does not implicitly {@link lnrpc.FundingStateStepResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FundingStateStepResp
         * @static
         * @param {lnrpc.FundingStateStepResp.$Properties} message FundingStateStepResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundingStateStepResp.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FundingStateStepResp message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FundingStateStepResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FundingStateStepResp & lnrpc.FundingStateStepResp.$Shape} FundingStateStepResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingStateStepResp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FundingStateStepResp();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FundingStateStepResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FundingStateStepResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FundingStateStepResp & lnrpc.FundingStateStepResp.$Shape} FundingStateStepResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundingStateStepResp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FundingStateStepResp message.
         * @function verify
         * @memberof lnrpc.FundingStateStepResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FundingStateStepResp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a FundingStateStepResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FundingStateStepResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FundingStateStepResp} FundingStateStepResp
         */
        FundingStateStepResp.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FundingStateStepResp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FundingStateStepResp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.FundingStateStepResp();
        };

        /**
         * Creates a plain object from a FundingStateStepResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FundingStateStepResp
         * @static
         * @param {lnrpc.FundingStateStepResp} message FundingStateStepResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FundingStateStepResp.toObject = function () {
            return {};
        };

        /**
         * Converts this FundingStateStepResp to JSON.
         * @function toJSON
         * @memberof lnrpc.FundingStateStepResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FundingStateStepResp.prototype.toJSON = function() {
            return FundingStateStepResp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FundingStateStepResp
         * @function getTypeUrl
         * @memberof lnrpc.FundingStateStepResp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FundingStateStepResp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FundingStateStepResp";
        };

        return FundingStateStepResp;
    })();

    lnrpc.PendingHTLC = (function() {

        /**
         * Properties of a PendingHTLC.
         * @typedef {Object} lnrpc.PendingHTLC.$Properties
         * @property {boolean|null} [incoming] PendingHTLC incoming
         * @property {Long|null} [amount] PendingHTLC amount
         * @property {string|null} [outpoint] PendingHTLC outpoint
         * @property {number|null} [maturity_height] PendingHTLC maturity_height
         * @property {number|null} [blocks_til_maturity] PendingHTLC blocks_til_maturity
         * @property {number|null} [stage] PendingHTLC stage
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PendingHTLC.
         * @memberof lnrpc
         * @interface IPendingHTLC
         * @augments lnrpc.PendingHTLC.$Properties
         * @deprecated Use lnrpc.PendingHTLC.$Properties instead.
         */

        /**
         * Shape of a PendingHTLC.
         * @typedef {lnrpc.PendingHTLC.$Properties} lnrpc.PendingHTLC.$Shape
         */

        /**
         * Constructs a new PendingHTLC.
         * @memberof lnrpc
         * @classdesc Represents a PendingHTLC.
         * @constructor
         * @param {lnrpc.PendingHTLC.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PendingHTLC = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PendingHTLC incoming.
         * @member {boolean} incoming
         * @memberof lnrpc.PendingHTLC
         * @instance
         */
        PendingHTLC.prototype.incoming = false;

        /**
         * PendingHTLC amount.
         * @member {Long} amount
         * @memberof lnrpc.PendingHTLC
         * @instance
         */
        PendingHTLC.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PendingHTLC outpoint.
         * @member {string} outpoint
         * @memberof lnrpc.PendingHTLC
         * @instance
         */
        PendingHTLC.prototype.outpoint = "";

        /**
         * PendingHTLC maturity_height.
         * @member {number} maturity_height
         * @memberof lnrpc.PendingHTLC
         * @instance
         */
        PendingHTLC.prototype.maturity_height = 0;

        /**
         * PendingHTLC blocks_til_maturity.
         * @member {number} blocks_til_maturity
         * @memberof lnrpc.PendingHTLC
         * @instance
         */
        PendingHTLC.prototype.blocks_til_maturity = 0;

        /**
         * PendingHTLC stage.
         * @member {number} stage
         * @memberof lnrpc.PendingHTLC
         * @instance
         */
        PendingHTLC.prototype.stage = 0;

        /**
         * Creates a new PendingHTLC instance using the specified properties.
         * @function create
         * @memberof lnrpc.PendingHTLC
         * @static
         * @param {lnrpc.PendingHTLC.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PendingHTLC} PendingHTLC instance
         * @type {{
         *   (properties: lnrpc.PendingHTLC.$Shape): lnrpc.PendingHTLC & lnrpc.PendingHTLC.$Shape;
         *   (properties?: lnrpc.PendingHTLC.$Properties): lnrpc.PendingHTLC;
         * }}
         */
        PendingHTLC.create = function(properties) {
            return new PendingHTLC(properties);
        };

        /**
         * Encodes the specified PendingHTLC message. Does not implicitly {@link lnrpc.PendingHTLC.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PendingHTLC
         * @static
         * @param {lnrpc.PendingHTLC.$Properties} message PendingHTLC message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingHTLC.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.incoming != null && $Object.hasOwnProperty.call(message, "incoming") && message.incoming !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.incoming);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amount);
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint") && message.outpoint !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.outpoint);
            if (message.maturity_height != null && $Object.hasOwnProperty.call(message, "maturity_height") && message.maturity_height !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.maturity_height);
            if (message.blocks_til_maturity != null && $Object.hasOwnProperty.call(message, "blocks_til_maturity") && message.blocks_til_maturity !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.blocks_til_maturity);
            if (message.stage != null && $Object.hasOwnProperty.call(message, "stage") && message.stage !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.stage);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingHTLC message, length delimited. Does not implicitly {@link lnrpc.PendingHTLC.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PendingHTLC
         * @static
         * @param {lnrpc.PendingHTLC.$Properties} message PendingHTLC message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingHTLC.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PendingHTLC message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PendingHTLC
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PendingHTLC & lnrpc.PendingHTLC.$Shape} PendingHTLC
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingHTLC.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingHTLC(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.incoming = value;
                        else
                            delete message.incoming;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.outpoint = value;
                        else
                            delete message.outpoint;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.maturity_height = value;
                        else
                            delete message.maturity_height;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.blocks_til_maturity = value;
                        else
                            delete message.blocks_til_maturity;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.stage = value;
                        else
                            delete message.stage;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingHTLC message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PendingHTLC
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PendingHTLC & lnrpc.PendingHTLC.$Shape} PendingHTLC
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingHTLC.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingHTLC message.
         * @function verify
         * @memberof lnrpc.PendingHTLC
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingHTLC.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.incoming != null && $Object.hasOwnProperty.call(message, "incoming"))
                if (typeof message.incoming !== "boolean")
                    return "incoming: boolean expected";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                if (!$util.isString(message.outpoint))
                    return "outpoint: string expected";
            if (message.maturity_height != null && $Object.hasOwnProperty.call(message, "maturity_height"))
                if (!$util.isInteger(message.maturity_height))
                    return "maturity_height: integer expected";
            if (message.blocks_til_maturity != null && $Object.hasOwnProperty.call(message, "blocks_til_maturity"))
                if (!$util.isInteger(message.blocks_til_maturity))
                    return "blocks_til_maturity: integer expected";
            if (message.stage != null && $Object.hasOwnProperty.call(message, "stage"))
                if (!$util.isInteger(message.stage))
                    return "stage: integer expected";
            return null;
        };

        /**
         * Creates a PendingHTLC message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PendingHTLC
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PendingHTLC} PendingHTLC
         */
        PendingHTLC.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PendingHTLC)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PendingHTLC: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PendingHTLC();
            if (object.incoming != null)
                if (object.incoming)
                    message.incoming = $Boolean(object.incoming);
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.outpoint != null)
                if (typeof object.outpoint !== "string" || object.outpoint.length)
                    message.outpoint = $String(object.outpoint);
            if (object.maturity_height != null)
                if ($Number(object.maturity_height) !== 0)
                    message.maturity_height = object.maturity_height >>> 0;
            if (object.blocks_til_maturity != null)
                if ($Number(object.blocks_til_maturity) !== 0)
                    message.blocks_til_maturity = object.blocks_til_maturity | 0;
            if (object.stage != null)
                if ($Number(object.stage) !== 0)
                    message.stage = object.stage >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a PendingHTLC message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PendingHTLC
         * @static
         * @param {lnrpc.PendingHTLC} message PendingHTLC
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingHTLC.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.incoming = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.outpoint = "";
                object.maturity_height = 0;
                object.blocks_til_maturity = 0;
                object.stage = 0;
            }
            if (message.incoming != null && $Object.hasOwnProperty.call(message, "incoming"))
                object.incoming = message.incoming;
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = message.outpoint;
            if (message.maturity_height != null && $Object.hasOwnProperty.call(message, "maturity_height"))
                object.maturity_height = message.maturity_height;
            if (message.blocks_til_maturity != null && $Object.hasOwnProperty.call(message, "blocks_til_maturity"))
                object.blocks_til_maturity = message.blocks_til_maturity;
            if (message.stage != null && $Object.hasOwnProperty.call(message, "stage"))
                object.stage = message.stage;
            return object;
        };

        /**
         * Converts this PendingHTLC to JSON.
         * @function toJSON
         * @memberof lnrpc.PendingHTLC
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingHTLC.prototype.toJSON = function() {
            return PendingHTLC.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingHTLC
         * @function getTypeUrl
         * @memberof lnrpc.PendingHTLC
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingHTLC.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PendingHTLC";
        };

        return PendingHTLC;
    })();

    lnrpc.PendingChannelsRequest = (function() {

        /**
         * Properties of a PendingChannelsRequest.
         * @typedef {Object} lnrpc.PendingChannelsRequest.$Properties
         * @property {boolean|null} [include_raw_tx] PendingChannelsRequest include_raw_tx
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PendingChannelsRequest.
         * @memberof lnrpc
         * @interface IPendingChannelsRequest
         * @augments lnrpc.PendingChannelsRequest.$Properties
         * @deprecated Use lnrpc.PendingChannelsRequest.$Properties instead.
         */

        /**
         * Shape of a PendingChannelsRequest.
         * @typedef {lnrpc.PendingChannelsRequest.$Properties} lnrpc.PendingChannelsRequest.$Shape
         */

        /**
         * Constructs a new PendingChannelsRequest.
         * @memberof lnrpc
         * @classdesc Represents a PendingChannelsRequest.
         * @constructor
         * @param {lnrpc.PendingChannelsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PendingChannelsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PendingChannelsRequest include_raw_tx.
         * @member {boolean} include_raw_tx
         * @memberof lnrpc.PendingChannelsRequest
         * @instance
         */
        PendingChannelsRequest.prototype.include_raw_tx = false;

        /**
         * Creates a new PendingChannelsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.PendingChannelsRequest
         * @static
         * @param {lnrpc.PendingChannelsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PendingChannelsRequest} PendingChannelsRequest instance
         * @type {{
         *   (properties: lnrpc.PendingChannelsRequest.$Shape): lnrpc.PendingChannelsRequest & lnrpc.PendingChannelsRequest.$Shape;
         *   (properties?: lnrpc.PendingChannelsRequest.$Properties): lnrpc.PendingChannelsRequest;
         * }}
         */
        PendingChannelsRequest.create = function(properties) {
            return new PendingChannelsRequest(properties);
        };

        /**
         * Encodes the specified PendingChannelsRequest message. Does not implicitly {@link lnrpc.PendingChannelsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PendingChannelsRequest
         * @static
         * @param {lnrpc.PendingChannelsRequest.$Properties} message PendingChannelsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingChannelsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.include_raw_tx != null && $Object.hasOwnProperty.call(message, "include_raw_tx") && message.include_raw_tx !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.include_raw_tx);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingChannelsRequest message, length delimited. Does not implicitly {@link lnrpc.PendingChannelsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PendingChannelsRequest
         * @static
         * @param {lnrpc.PendingChannelsRequest.$Properties} message PendingChannelsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingChannelsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PendingChannelsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PendingChannelsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PendingChannelsRequest & lnrpc.PendingChannelsRequest.$Shape} PendingChannelsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingChannelsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingChannelsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_raw_tx = value;
                        else
                            delete message.include_raw_tx;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingChannelsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PendingChannelsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PendingChannelsRequest & lnrpc.PendingChannelsRequest.$Shape} PendingChannelsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingChannelsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingChannelsRequest message.
         * @function verify
         * @memberof lnrpc.PendingChannelsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingChannelsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.include_raw_tx != null && $Object.hasOwnProperty.call(message, "include_raw_tx"))
                if (typeof message.include_raw_tx !== "boolean")
                    return "include_raw_tx: boolean expected";
            return null;
        };

        /**
         * Creates a PendingChannelsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PendingChannelsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PendingChannelsRequest} PendingChannelsRequest
         */
        PendingChannelsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PendingChannelsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PendingChannelsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PendingChannelsRequest();
            if (object.include_raw_tx != null)
                if (object.include_raw_tx)
                    message.include_raw_tx = $Boolean(object.include_raw_tx);
            return message;
        };

        /**
         * Creates a plain object from a PendingChannelsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PendingChannelsRequest
         * @static
         * @param {lnrpc.PendingChannelsRequest} message PendingChannelsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingChannelsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.include_raw_tx = false;
            if (message.include_raw_tx != null && $Object.hasOwnProperty.call(message, "include_raw_tx"))
                object.include_raw_tx = message.include_raw_tx;
            return object;
        };

        /**
         * Converts this PendingChannelsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.PendingChannelsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingChannelsRequest.prototype.toJSON = function() {
            return PendingChannelsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingChannelsRequest
         * @function getTypeUrl
         * @memberof lnrpc.PendingChannelsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingChannelsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PendingChannelsRequest";
        };

        return PendingChannelsRequest;
    })();

    lnrpc.PendingChannelsResponse = (function() {

        /**
         * Properties of a PendingChannelsResponse.
         * @typedef {Object} lnrpc.PendingChannelsResponse.$Properties
         * @property {Long|null} [total_limbo_balance] PendingChannelsResponse total_limbo_balance
         * @property {Array.<lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties>|null} [pending_open_channels] PendingChannelsResponse pending_open_channels
         * @property {Array.<lnrpc.PendingChannelsResponse.ClosedChannel.$Properties>|null} [pending_closing_channels] PendingChannelsResponse pending_closing_channels
         * @property {Array.<lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties>|null} [pending_force_closing_channels] PendingChannelsResponse pending_force_closing_channels
         * @property {Array.<lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties>|null} [waiting_close_channels] PendingChannelsResponse waiting_close_channels
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PendingChannelsResponse.
         * @memberof lnrpc
         * @interface IPendingChannelsResponse
         * @augments lnrpc.PendingChannelsResponse.$Properties
         * @deprecated Use lnrpc.PendingChannelsResponse.$Properties instead.
         */

        /**
         * Shape of a PendingChannelsResponse.
         * @typedef {lnrpc.PendingChannelsResponse.$Properties} lnrpc.PendingChannelsResponse.$Shape
         */

        /**
         * Constructs a new PendingChannelsResponse.
         * @memberof lnrpc
         * @classdesc Represents a PendingChannelsResponse.
         * @constructor
         * @param {lnrpc.PendingChannelsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PendingChannelsResponse = function (properties) {
            this.pending_open_channels = [];
            this.pending_closing_channels = [];
            this.pending_force_closing_channels = [];
            this.waiting_close_channels = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PendingChannelsResponse total_limbo_balance.
         * @member {Long} total_limbo_balance
         * @memberof lnrpc.PendingChannelsResponse
         * @instance
         */
        PendingChannelsResponse.prototype.total_limbo_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PendingChannelsResponse pending_open_channels.
         * @member {Array.<lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties>} pending_open_channels
         * @memberof lnrpc.PendingChannelsResponse
         * @instance
         */
        PendingChannelsResponse.prototype.pending_open_channels = $util.emptyArray;

        /**
         * PendingChannelsResponse pending_closing_channels.
         * @member {Array.<lnrpc.PendingChannelsResponse.ClosedChannel.$Properties>} pending_closing_channels
         * @memberof lnrpc.PendingChannelsResponse
         * @instance
         */
        PendingChannelsResponse.prototype.pending_closing_channels = $util.emptyArray;

        /**
         * PendingChannelsResponse pending_force_closing_channels.
         * @member {Array.<lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties>} pending_force_closing_channels
         * @memberof lnrpc.PendingChannelsResponse
         * @instance
         */
        PendingChannelsResponse.prototype.pending_force_closing_channels = $util.emptyArray;

        /**
         * PendingChannelsResponse waiting_close_channels.
         * @member {Array.<lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties>} waiting_close_channels
         * @memberof lnrpc.PendingChannelsResponse
         * @instance
         */
        PendingChannelsResponse.prototype.waiting_close_channels = $util.emptyArray;

        /**
         * Creates a new PendingChannelsResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.PendingChannelsResponse
         * @static
         * @param {lnrpc.PendingChannelsResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PendingChannelsResponse} PendingChannelsResponse instance
         * @type {{
         *   (properties: lnrpc.PendingChannelsResponse.$Shape): lnrpc.PendingChannelsResponse & lnrpc.PendingChannelsResponse.$Shape;
         *   (properties?: lnrpc.PendingChannelsResponse.$Properties): lnrpc.PendingChannelsResponse;
         * }}
         */
        PendingChannelsResponse.create = function(properties) {
            return new PendingChannelsResponse(properties);
        };

        /**
         * Encodes the specified PendingChannelsResponse message. Does not implicitly {@link lnrpc.PendingChannelsResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PendingChannelsResponse
         * @static
         * @param {lnrpc.PendingChannelsResponse.$Properties} message PendingChannelsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingChannelsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.total_limbo_balance != null && $Object.hasOwnProperty.call(message, "total_limbo_balance") && (typeof message.total_limbo_balance === "object" ? message.total_limbo_balance.low || message.total_limbo_balance.high : message.total_limbo_balance !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.total_limbo_balance);
            if (message.pending_open_channels != null && message.pending_open_channels.length)
                for (let i = 0; i < message.pending_open_channels.length; ++i)
                    $root.lnrpc.PendingChannelsResponse.PendingOpenChannel.encode(message.pending_open_channels[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.pending_closing_channels != null && message.pending_closing_channels.length)
                for (let i = 0; i < message.pending_closing_channels.length; ++i)
                    $root.lnrpc.PendingChannelsResponse.ClosedChannel.encode(message.pending_closing_channels[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.pending_force_closing_channels != null && message.pending_force_closing_channels.length)
                for (let i = 0; i < message.pending_force_closing_channels.length; ++i)
                    $root.lnrpc.PendingChannelsResponse.ForceClosedChannel.encode(message.pending_force_closing_channels[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.waiting_close_channels != null && message.waiting_close_channels.length)
                for (let i = 0; i < message.waiting_close_channels.length; ++i)
                    $root.lnrpc.PendingChannelsResponse.WaitingCloseChannel.encode(message.waiting_close_channels[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingChannelsResponse message, length delimited. Does not implicitly {@link lnrpc.PendingChannelsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PendingChannelsResponse
         * @static
         * @param {lnrpc.PendingChannelsResponse.$Properties} message PendingChannelsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingChannelsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PendingChannelsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PendingChannelsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PendingChannelsResponse & lnrpc.PendingChannelsResponse.$Shape} PendingChannelsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingChannelsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingChannelsResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_limbo_balance = value;
                        else
                            delete message.total_limbo_balance;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pending_open_channels && message.pending_open_channels.length))
                            message.pending_open_channels = [];
                        message.pending_open_channels.push($root.lnrpc.PendingChannelsResponse.PendingOpenChannel.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pending_closing_channels && message.pending_closing_channels.length))
                            message.pending_closing_channels = [];
                        message.pending_closing_channels.push($root.lnrpc.PendingChannelsResponse.ClosedChannel.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pending_force_closing_channels && message.pending_force_closing_channels.length))
                            message.pending_force_closing_channels = [];
                        message.pending_force_closing_channels.push($root.lnrpc.PendingChannelsResponse.ForceClosedChannel.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.waiting_close_channels && message.waiting_close_channels.length))
                            message.waiting_close_channels = [];
                        message.waiting_close_channels.push($root.lnrpc.PendingChannelsResponse.WaitingCloseChannel.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingChannelsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PendingChannelsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PendingChannelsResponse & lnrpc.PendingChannelsResponse.$Shape} PendingChannelsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingChannelsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingChannelsResponse message.
         * @function verify
         * @memberof lnrpc.PendingChannelsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingChannelsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.total_limbo_balance != null && $Object.hasOwnProperty.call(message, "total_limbo_balance"))
                if (!$util.isInteger(message.total_limbo_balance) && !(message.total_limbo_balance && $util.isInteger(message.total_limbo_balance.low) && $util.isInteger(message.total_limbo_balance.high)))
                    return "total_limbo_balance: integer|Long expected";
            if (message.pending_open_channels != null && $Object.hasOwnProperty.call(message, "pending_open_channels")) {
                if (!$Array.isArray(message.pending_open_channels))
                    return "pending_open_channels: array expected";
                for (let i = 0; i < message.pending_open_channels.length; ++i) {
                    let error = $root.lnrpc.PendingChannelsResponse.PendingOpenChannel.verify(message.pending_open_channels[i], _depth + 1);
                    if (error)
                        return "pending_open_channels." + error;
                }
            }
            if (message.pending_closing_channels != null && $Object.hasOwnProperty.call(message, "pending_closing_channels")) {
                if (!$Array.isArray(message.pending_closing_channels))
                    return "pending_closing_channels: array expected";
                for (let i = 0; i < message.pending_closing_channels.length; ++i) {
                    let error = $root.lnrpc.PendingChannelsResponse.ClosedChannel.verify(message.pending_closing_channels[i], _depth + 1);
                    if (error)
                        return "pending_closing_channels." + error;
                }
            }
            if (message.pending_force_closing_channels != null && $Object.hasOwnProperty.call(message, "pending_force_closing_channels")) {
                if (!$Array.isArray(message.pending_force_closing_channels))
                    return "pending_force_closing_channels: array expected";
                for (let i = 0; i < message.pending_force_closing_channels.length; ++i) {
                    let error = $root.lnrpc.PendingChannelsResponse.ForceClosedChannel.verify(message.pending_force_closing_channels[i], _depth + 1);
                    if (error)
                        return "pending_force_closing_channels." + error;
                }
            }
            if (message.waiting_close_channels != null && $Object.hasOwnProperty.call(message, "waiting_close_channels")) {
                if (!$Array.isArray(message.waiting_close_channels))
                    return "waiting_close_channels: array expected";
                for (let i = 0; i < message.waiting_close_channels.length; ++i) {
                    let error = $root.lnrpc.PendingChannelsResponse.WaitingCloseChannel.verify(message.waiting_close_channels[i], _depth + 1);
                    if (error)
                        return "waiting_close_channels." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PendingChannelsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PendingChannelsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PendingChannelsResponse} PendingChannelsResponse
         */
        PendingChannelsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PendingChannelsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PendingChannelsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PendingChannelsResponse();
            if (object.total_limbo_balance != null)
                if (typeof object.total_limbo_balance === "object" ? object.total_limbo_balance.low || object.total_limbo_balance.high : $Number(object.total_limbo_balance) !== 0)
                    if ($util.Long)
                        message.total_limbo_balance = $util.Long.fromValue(object.total_limbo_balance, false);
                    else if (typeof object.total_limbo_balance === "string")
                        message.total_limbo_balance = $parseInt(object.total_limbo_balance, 10);
                    else if (typeof object.total_limbo_balance === "number")
                        message.total_limbo_balance = object.total_limbo_balance;
                    else if (typeof object.total_limbo_balance === "object")
                        message.total_limbo_balance = new $util.LongBits(object.total_limbo_balance.low >>> 0, object.total_limbo_balance.high >>> 0).toNumber();
            if (object.pending_open_channels) {
                if (!$Array.isArray(object.pending_open_channels))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.pending_open_channels: array expected");
                message.pending_open_channels = $Array(object.pending_open_channels.length);
                for (let i = 0; i < object.pending_open_channels.length; ++i) {
                    if (!$util.isObject(object.pending_open_channels[i]))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.pending_open_channels: object expected");
                    message.pending_open_channels[i] = $root.lnrpc.PendingChannelsResponse.PendingOpenChannel.fromObject(object.pending_open_channels[i], _depth + 1);
                }
            }
            if (object.pending_closing_channels) {
                if (!$Array.isArray(object.pending_closing_channels))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.pending_closing_channels: array expected");
                message.pending_closing_channels = $Array(object.pending_closing_channels.length);
                for (let i = 0; i < object.pending_closing_channels.length; ++i) {
                    if (!$util.isObject(object.pending_closing_channels[i]))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.pending_closing_channels: object expected");
                    message.pending_closing_channels[i] = $root.lnrpc.PendingChannelsResponse.ClosedChannel.fromObject(object.pending_closing_channels[i], _depth + 1);
                }
            }
            if (object.pending_force_closing_channels) {
                if (!$Array.isArray(object.pending_force_closing_channels))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.pending_force_closing_channels: array expected");
                message.pending_force_closing_channels = $Array(object.pending_force_closing_channels.length);
                for (let i = 0; i < object.pending_force_closing_channels.length; ++i) {
                    if (!$util.isObject(object.pending_force_closing_channels[i]))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.pending_force_closing_channels: object expected");
                    message.pending_force_closing_channels[i] = $root.lnrpc.PendingChannelsResponse.ForceClosedChannel.fromObject(object.pending_force_closing_channels[i], _depth + 1);
                }
            }
            if (object.waiting_close_channels) {
                if (!$Array.isArray(object.waiting_close_channels))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.waiting_close_channels: array expected");
                message.waiting_close_channels = $Array(object.waiting_close_channels.length);
                for (let i = 0; i < object.waiting_close_channels.length; ++i) {
                    if (!$util.isObject(object.waiting_close_channels[i]))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.waiting_close_channels: object expected");
                    message.waiting_close_channels[i] = $root.lnrpc.PendingChannelsResponse.WaitingCloseChannel.fromObject(object.waiting_close_channels[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PendingChannelsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PendingChannelsResponse
         * @static
         * @param {lnrpc.PendingChannelsResponse} message PendingChannelsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingChannelsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.pending_open_channels = [];
                object.pending_closing_channels = [];
                object.pending_force_closing_channels = [];
                object.waiting_close_channels = [];
            }
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_limbo_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_limbo_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.total_limbo_balance != null && $Object.hasOwnProperty.call(message, "total_limbo_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_limbo_balance = typeof message.total_limbo_balance === "number" ? $BigInt(message.total_limbo_balance) : $util.Long.fromBits(message.total_limbo_balance.low >>> 0, message.total_limbo_balance.high >>> 0, false).toBigInt();
                else if (typeof message.total_limbo_balance === "number")
                    object.total_limbo_balance = options.longs === $String ? $String(message.total_limbo_balance) : message.total_limbo_balance;
                else
                    object.total_limbo_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.total_limbo_balance) : options.longs === $Number ? new $util.LongBits(message.total_limbo_balance.low >>> 0, message.total_limbo_balance.high >>> 0).toNumber() : message.total_limbo_balance;
            if (message.pending_open_channels && message.pending_open_channels.length) {
                object.pending_open_channels = $Array(message.pending_open_channels.length);
                for (let j = 0; j < message.pending_open_channels.length; ++j)
                    object.pending_open_channels[j] = $root.lnrpc.PendingChannelsResponse.PendingOpenChannel.toObject(message.pending_open_channels[j], options, _depth + 1);
            }
            if (message.pending_closing_channels && message.pending_closing_channels.length) {
                object.pending_closing_channels = $Array(message.pending_closing_channels.length);
                for (let j = 0; j < message.pending_closing_channels.length; ++j)
                    object.pending_closing_channels[j] = $root.lnrpc.PendingChannelsResponse.ClosedChannel.toObject(message.pending_closing_channels[j], options, _depth + 1);
            }
            if (message.pending_force_closing_channels && message.pending_force_closing_channels.length) {
                object.pending_force_closing_channels = $Array(message.pending_force_closing_channels.length);
                for (let j = 0; j < message.pending_force_closing_channels.length; ++j)
                    object.pending_force_closing_channels[j] = $root.lnrpc.PendingChannelsResponse.ForceClosedChannel.toObject(message.pending_force_closing_channels[j], options, _depth + 1);
            }
            if (message.waiting_close_channels && message.waiting_close_channels.length) {
                object.waiting_close_channels = $Array(message.waiting_close_channels.length);
                for (let j = 0; j < message.waiting_close_channels.length; ++j)
                    object.waiting_close_channels[j] = $root.lnrpc.PendingChannelsResponse.WaitingCloseChannel.toObject(message.waiting_close_channels[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this PendingChannelsResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.PendingChannelsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingChannelsResponse.prototype.toJSON = function() {
            return PendingChannelsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingChannelsResponse
         * @function getTypeUrl
         * @memberof lnrpc.PendingChannelsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingChannelsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PendingChannelsResponse";
        };

        PendingChannelsResponse.PendingChannel = (function() {

            /**
             * Properties of a PendingChannel.
             * @typedef {Object} lnrpc.PendingChannelsResponse.PendingChannel.$Properties
             * @property {string|null} [remote_node_pub] PendingChannel remote_node_pub
             * @property {string|null} [channel_point] PendingChannel channel_point
             * @property {Long|null} [capacity] PendingChannel capacity
             * @property {Long|null} [local_balance] PendingChannel local_balance
             * @property {Long|null} [remote_balance] PendingChannel remote_balance
             * @property {Long|null} [local_chan_reserve_sat] PendingChannel local_chan_reserve_sat
             * @property {Long|null} [remote_chan_reserve_sat] PendingChannel remote_chan_reserve_sat
             * @property {lnrpc.Initiator|null} [initiator] PendingChannel initiator
             * @property {lnrpc.CommitmentType|null} [commitment_type] PendingChannel commitment_type
             * @property {Long|null} [num_forwarding_packages] PendingChannel num_forwarding_packages
             * @property {string|null} [chan_status_flags] PendingChannel chan_status_flags
             * @property {boolean|null} ["private"] PendingChannel private
             * @property {string|null} [memo] PendingChannel memo
             * @property {Uint8Array|null} [custom_channel_data] PendingChannel custom_channel_data
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */

            /**
             * Properties of a PendingChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @interface IPendingChannel
             * @augments lnrpc.PendingChannelsResponse.PendingChannel.$Properties
             * @deprecated Use lnrpc.PendingChannelsResponse.PendingChannel.$Properties instead.
             */

            /**
             * Shape of a PendingChannel.
             * @typedef {lnrpc.PendingChannelsResponse.PendingChannel.$Properties} lnrpc.PendingChannelsResponse.PendingChannel.$Shape
             */

            /**
             * Constructs a new PendingChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @classdesc Represents a PendingChannel.
             * @constructor
             * @param {lnrpc.PendingChannelsResponse.PendingChannel.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */
            const PendingChannel = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PendingChannel remote_node_pub.
             * @member {string} remote_node_pub
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.remote_node_pub = "";

            /**
             * PendingChannel channel_point.
             * @member {string} channel_point
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.channel_point = "";

            /**
             * PendingChannel capacity.
             * @member {Long} capacity
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.capacity = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PendingChannel local_balance.
             * @member {Long} local_balance
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.local_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PendingChannel remote_balance.
             * @member {Long} remote_balance
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.remote_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PendingChannel local_chan_reserve_sat.
             * @member {Long} local_chan_reserve_sat
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.local_chan_reserve_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PendingChannel remote_chan_reserve_sat.
             * @member {Long} remote_chan_reserve_sat
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.remote_chan_reserve_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PendingChannel initiator.
             * @member {lnrpc.Initiator} initiator
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.initiator = 0;

            /**
             * PendingChannel commitment_type.
             * @member {lnrpc.CommitmentType} commitment_type
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.commitment_type = 0;

            /**
             * PendingChannel num_forwarding_packages.
             * @member {Long} num_forwarding_packages
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.num_forwarding_packages = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PendingChannel chan_status_flags.
             * @member {string} chan_status_flags
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.chan_status_flags = "";

            /**
             * PendingChannel private.
             * @member {boolean} private
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype["private"] = false;

            /**
             * PendingChannel memo.
             * @member {string} memo
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.memo = "";

            /**
             * PendingChannel custom_channel_data.
             * @member {Uint8Array} custom_channel_data
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             */
            PendingChannel.prototype.custom_channel_data = $util.newBuffer([]);

            /**
             * Creates a new PendingChannel instance using the specified properties.
             * @function create
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.PendingChannel.$Properties=} [properties] Properties to set
             * @returns {lnrpc.PendingChannelsResponse.PendingChannel} PendingChannel instance
             * @type {{
             *   (properties: lnrpc.PendingChannelsResponse.PendingChannel.$Shape): lnrpc.PendingChannelsResponse.PendingChannel & lnrpc.PendingChannelsResponse.PendingChannel.$Shape;
             *   (properties?: lnrpc.PendingChannelsResponse.PendingChannel.$Properties): lnrpc.PendingChannelsResponse.PendingChannel;
             * }}
             */
            PendingChannel.create = function(properties) {
                return new PendingChannel(properties);
            };

            /**
             * Encodes the specified PendingChannel message. Does not implicitly {@link lnrpc.PendingChannelsResponse.PendingChannel.verify|verify} messages.
             * @function encode
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.PendingChannel.$Properties} message PendingChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PendingChannel.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.remote_node_pub != null && $Object.hasOwnProperty.call(message, "remote_node_pub") && message.remote_node_pub !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.remote_node_pub);
                if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point") && message.channel_point !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.channel_point);
                if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity") && (typeof message.capacity === "object" ? message.capacity.low || message.capacity.high : message.capacity !== 0))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.capacity);
                if (message.local_balance != null && $Object.hasOwnProperty.call(message, "local_balance") && (typeof message.local_balance === "object" ? message.local_balance.low || message.local_balance.high : message.local_balance !== 0))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.local_balance);
                if (message.remote_balance != null && $Object.hasOwnProperty.call(message, "remote_balance") && (typeof message.remote_balance === "object" ? message.remote_balance.low || message.remote_balance.high : message.remote_balance !== 0))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.remote_balance);
                if (message.local_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "local_chan_reserve_sat") && (typeof message.local_chan_reserve_sat === "object" ? message.local_chan_reserve_sat.low || message.local_chan_reserve_sat.high : message.local_chan_reserve_sat !== 0))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.local_chan_reserve_sat);
                if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat") && (typeof message.remote_chan_reserve_sat === "object" ? message.remote_chan_reserve_sat.low || message.remote_chan_reserve_sat.high : message.remote_chan_reserve_sat !== 0))
                    writer.uint32(/* id 7, wireType 0 =*/56).int64(message.remote_chan_reserve_sat);
                if (message.initiator != null && $Object.hasOwnProperty.call(message, "initiator") && message.initiator !== 0)
                    writer.uint32(/* id 8, wireType 0 =*/64).int32(message.initiator);
                if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type") && message.commitment_type !== 0)
                    writer.uint32(/* id 9, wireType 0 =*/72).int32(message.commitment_type);
                if (message.num_forwarding_packages != null && $Object.hasOwnProperty.call(message, "num_forwarding_packages") && (typeof message.num_forwarding_packages === "object" ? message.num_forwarding_packages.low || message.num_forwarding_packages.high : message.num_forwarding_packages !== 0))
                    writer.uint32(/* id 10, wireType 0 =*/80).int64(message.num_forwarding_packages);
                if (message.chan_status_flags != null && $Object.hasOwnProperty.call(message, "chan_status_flags") && message.chan_status_flags !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.chan_status_flags);
                if (message["private"] != null && $Object.hasOwnProperty.call(message, "private") && message["private"] !== false)
                    writer.uint32(/* id 12, wireType 0 =*/96).bool(message["private"]);
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.memo);
                if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data") && message.custom_channel_data.length)
                    writer.uint32(/* id 34, wireType 2 =*/274).bytes(message.custom_channel_data);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PendingChannel message, length delimited. Does not implicitly {@link lnrpc.PendingChannelsResponse.PendingChannel.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.PendingChannel.$Properties} message PendingChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PendingChannel.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PendingChannel message from the specified reader or buffer.
             * @function decode
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lnrpc.PendingChannelsResponse.PendingChannel & lnrpc.PendingChannelsResponse.PendingChannel.$Shape} PendingChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PendingChannel.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingChannelsResponse.PendingChannel(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.remote_node_pub = value;
                            else
                                delete message.remote_node_pub;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.channel_point = value;
                            else
                                delete message.channel_point;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.capacity = value;
                            else
                                delete message.capacity;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.local_balance = value;
                            else
                                delete message.local_balance;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.remote_balance = value;
                            else
                                delete message.remote_balance;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.local_chan_reserve_sat = value;
                            else
                                delete message.local_chan_reserve_sat;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.remote_chan_reserve_sat = value;
                            else
                                delete message.remote_chan_reserve_sat;
                            continue;
                        }
                    case 8: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.initiator = value;
                            else
                                delete message.initiator;
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.commitment_type = value;
                            else
                                delete message.commitment_type;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.num_forwarding_packages = value;
                            else
                                delete message.num_forwarding_packages;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.chan_status_flags = value;
                            else
                                delete message.chan_status_flags;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message["private"] = value;
                            else
                                delete message["private"];
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.memo = value;
                            else
                                delete message.memo;
                            continue;
                        }
                    case 34: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.custom_channel_data = value;
                            else
                                delete message.custom_channel_data;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a PendingChannel message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lnrpc.PendingChannelsResponse.PendingChannel & lnrpc.PendingChannelsResponse.PendingChannel.$Shape} PendingChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PendingChannel.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PendingChannel message.
             * @function verify
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PendingChannel.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.remote_node_pub != null && $Object.hasOwnProperty.call(message, "remote_node_pub"))
                    if (!$util.isString(message.remote_node_pub))
                        return "remote_node_pub: string expected";
                if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                    if (!$util.isString(message.channel_point))
                        return "channel_point: string expected";
                if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                    if (!$util.isInteger(message.capacity) && !(message.capacity && $util.isInteger(message.capacity.low) && $util.isInteger(message.capacity.high)))
                        return "capacity: integer|Long expected";
                if (message.local_balance != null && $Object.hasOwnProperty.call(message, "local_balance"))
                    if (!$util.isInteger(message.local_balance) && !(message.local_balance && $util.isInteger(message.local_balance.low) && $util.isInteger(message.local_balance.high)))
                        return "local_balance: integer|Long expected";
                if (message.remote_balance != null && $Object.hasOwnProperty.call(message, "remote_balance"))
                    if (!$util.isInteger(message.remote_balance) && !(message.remote_balance && $util.isInteger(message.remote_balance.low) && $util.isInteger(message.remote_balance.high)))
                        return "remote_balance: integer|Long expected";
                if (message.local_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "local_chan_reserve_sat"))
                    if (!$util.isInteger(message.local_chan_reserve_sat) && !(message.local_chan_reserve_sat && $util.isInteger(message.local_chan_reserve_sat.low) && $util.isInteger(message.local_chan_reserve_sat.high)))
                        return "local_chan_reserve_sat: integer|Long expected";
                if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat"))
                    if (!$util.isInteger(message.remote_chan_reserve_sat) && !(message.remote_chan_reserve_sat && $util.isInteger(message.remote_chan_reserve_sat.low) && $util.isInteger(message.remote_chan_reserve_sat.high)))
                        return "remote_chan_reserve_sat: integer|Long expected";
                if (message.initiator != null && $Object.hasOwnProperty.call(message, "initiator"))
                    if (typeof message.initiator !== "number" || (message.initiator | 0) !== message.initiator)
                        return "initiator: enum value expected";
                if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                    if (typeof message.commitment_type !== "number" || (message.commitment_type | 0) !== message.commitment_type)
                        return "commitment_type: enum value expected";
                if (message.num_forwarding_packages != null && $Object.hasOwnProperty.call(message, "num_forwarding_packages"))
                    if (!$util.isInteger(message.num_forwarding_packages) && !(message.num_forwarding_packages && $util.isInteger(message.num_forwarding_packages.low) && $util.isInteger(message.num_forwarding_packages.high)))
                        return "num_forwarding_packages: integer|Long expected";
                if (message.chan_status_flags != null && $Object.hasOwnProperty.call(message, "chan_status_flags"))
                    if (!$util.isString(message.chan_status_flags))
                        return "chan_status_flags: string expected";
                if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                    if (typeof message["private"] !== "boolean")
                        return "private: boolean expected";
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    if (!$util.isString(message.memo))
                        return "memo: string expected";
                if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                    if (!(message.custom_channel_data && typeof message.custom_channel_data.length === "number" || $util.isString(message.custom_channel_data)))
                        return "custom_channel_data: buffer expected";
                return null;
            };

            /**
             * Creates a PendingChannel message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lnrpc.PendingChannelsResponse.PendingChannel} PendingChannel
             */
            PendingChannel.fromObject = function (object, _depth) {
                if (object instanceof $root.lnrpc.PendingChannelsResponse.PendingChannel)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.PendingChannel: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.lnrpc.PendingChannelsResponse.PendingChannel();
                if (object.remote_node_pub != null)
                    if (typeof object.remote_node_pub !== "string" || object.remote_node_pub.length)
                        message.remote_node_pub = $String(object.remote_node_pub);
                if (object.channel_point != null)
                    if (typeof object.channel_point !== "string" || object.channel_point.length)
                        message.channel_point = $String(object.channel_point);
                if (object.capacity != null)
                    if (typeof object.capacity === "object" ? object.capacity.low || object.capacity.high : $Number(object.capacity) !== 0)
                        if ($util.Long)
                            message.capacity = $util.Long.fromValue(object.capacity, false);
                        else if (typeof object.capacity === "string")
                            message.capacity = $parseInt(object.capacity, 10);
                        else if (typeof object.capacity === "number")
                            message.capacity = object.capacity;
                        else if (typeof object.capacity === "object")
                            message.capacity = new $util.LongBits(object.capacity.low >>> 0, object.capacity.high >>> 0).toNumber();
                if (object.local_balance != null)
                    if (typeof object.local_balance === "object" ? object.local_balance.low || object.local_balance.high : $Number(object.local_balance) !== 0)
                        if ($util.Long)
                            message.local_balance = $util.Long.fromValue(object.local_balance, false);
                        else if (typeof object.local_balance === "string")
                            message.local_balance = $parseInt(object.local_balance, 10);
                        else if (typeof object.local_balance === "number")
                            message.local_balance = object.local_balance;
                        else if (typeof object.local_balance === "object")
                            message.local_balance = new $util.LongBits(object.local_balance.low >>> 0, object.local_balance.high >>> 0).toNumber();
                if (object.remote_balance != null)
                    if (typeof object.remote_balance === "object" ? object.remote_balance.low || object.remote_balance.high : $Number(object.remote_balance) !== 0)
                        if ($util.Long)
                            message.remote_balance = $util.Long.fromValue(object.remote_balance, false);
                        else if (typeof object.remote_balance === "string")
                            message.remote_balance = $parseInt(object.remote_balance, 10);
                        else if (typeof object.remote_balance === "number")
                            message.remote_balance = object.remote_balance;
                        else if (typeof object.remote_balance === "object")
                            message.remote_balance = new $util.LongBits(object.remote_balance.low >>> 0, object.remote_balance.high >>> 0).toNumber();
                if (object.local_chan_reserve_sat != null)
                    if (typeof object.local_chan_reserve_sat === "object" ? object.local_chan_reserve_sat.low || object.local_chan_reserve_sat.high : $Number(object.local_chan_reserve_sat) !== 0)
                        if ($util.Long)
                            message.local_chan_reserve_sat = $util.Long.fromValue(object.local_chan_reserve_sat, false);
                        else if (typeof object.local_chan_reserve_sat === "string")
                            message.local_chan_reserve_sat = $parseInt(object.local_chan_reserve_sat, 10);
                        else if (typeof object.local_chan_reserve_sat === "number")
                            message.local_chan_reserve_sat = object.local_chan_reserve_sat;
                        else if (typeof object.local_chan_reserve_sat === "object")
                            message.local_chan_reserve_sat = new $util.LongBits(object.local_chan_reserve_sat.low >>> 0, object.local_chan_reserve_sat.high >>> 0).toNumber();
                if (object.remote_chan_reserve_sat != null)
                    if (typeof object.remote_chan_reserve_sat === "object" ? object.remote_chan_reserve_sat.low || object.remote_chan_reserve_sat.high : $Number(object.remote_chan_reserve_sat) !== 0)
                        if ($util.Long)
                            message.remote_chan_reserve_sat = $util.Long.fromValue(object.remote_chan_reserve_sat, false);
                        else if (typeof object.remote_chan_reserve_sat === "string")
                            message.remote_chan_reserve_sat = $parseInt(object.remote_chan_reserve_sat, 10);
                        else if (typeof object.remote_chan_reserve_sat === "number")
                            message.remote_chan_reserve_sat = object.remote_chan_reserve_sat;
                        else if (typeof object.remote_chan_reserve_sat === "object")
                            message.remote_chan_reserve_sat = new $util.LongBits(object.remote_chan_reserve_sat.low >>> 0, object.remote_chan_reserve_sat.high >>> 0).toNumber();
                if (object.initiator !== 0 && (typeof object.initiator !== "string" || $root.lnrpc.Initiator[object.initiator] !== 0))
                    switch (object.initiator) {
                    case "INITIATOR_UNKNOWN":
                    case 0:
                        message.initiator = 0;
                        break;
                    case "INITIATOR_LOCAL":
                    case 1:
                        message.initiator = 1;
                        break;
                    case "INITIATOR_REMOTE":
                    case 2:
                        message.initiator = 2;
                        break;
                    case "INITIATOR_BOTH":
                    case 3:
                        message.initiator = 3;
                        break;
                    default:
                        if (typeof object.initiator === "number" && (object.initiator | 0) === object.initiator)
                            message.initiator = object.initiator;
                    }
                if (object.commitment_type !== 0 && (typeof object.commitment_type !== "string" || $root.lnrpc.CommitmentType[object.commitment_type] !== 0))
                    switch (object.commitment_type) {
                    case "UNKNOWN_COMMITMENT_TYPE":
                    case 0:
                        message.commitment_type = 0;
                        break;
                    case "LEGACY":
                    case 1:
                        message.commitment_type = 1;
                        break;
                    case "STATIC_REMOTE_KEY":
                    case 2:
                        message.commitment_type = 2;
                        break;
                    case "ANCHORS":
                    case 3:
                        message.commitment_type = 3;
                        break;
                    case "SCRIPT_ENFORCED_LEASE":
                    case 4:
                        message.commitment_type = 4;
                        break;
                    case "SIMPLE_TAPROOT":
                    case 5:
                        message.commitment_type = 5;
                        break;
                    case "SIMPLE_TAPROOT_OVERLAY":
                    case 6:
                        message.commitment_type = 6;
                        break;
                    default:
                        if (typeof object.commitment_type === "number" && (object.commitment_type | 0) === object.commitment_type)
                            message.commitment_type = object.commitment_type;
                    }
                if (object.num_forwarding_packages != null)
                    if (typeof object.num_forwarding_packages === "object" ? object.num_forwarding_packages.low || object.num_forwarding_packages.high : $Number(object.num_forwarding_packages) !== 0)
                        if ($util.Long)
                            message.num_forwarding_packages = $util.Long.fromValue(object.num_forwarding_packages, false);
                        else if (typeof object.num_forwarding_packages === "string")
                            message.num_forwarding_packages = $parseInt(object.num_forwarding_packages, 10);
                        else if (typeof object.num_forwarding_packages === "number")
                            message.num_forwarding_packages = object.num_forwarding_packages;
                        else if (typeof object.num_forwarding_packages === "object")
                            message.num_forwarding_packages = new $util.LongBits(object.num_forwarding_packages.low >>> 0, object.num_forwarding_packages.high >>> 0).toNumber();
                if (object.chan_status_flags != null)
                    if (typeof object.chan_status_flags !== "string" || object.chan_status_flags.length)
                        message.chan_status_flags = $String(object.chan_status_flags);
                if (object["private"] != null)
                    if (object["private"])
                        message["private"] = $Boolean(object["private"]);
                if (object.memo != null)
                    if (typeof object.memo !== "string" || object.memo.length)
                        message.memo = $String(object.memo);
                if (object.custom_channel_data != null)
                    if (object.custom_channel_data.length)
                        if (typeof object.custom_channel_data === "string")
                            $util.base64.decode(object.custom_channel_data, message.custom_channel_data = $util.newBuffer($util.base64.length(object.custom_channel_data)), 0);
                        else if (object.custom_channel_data.length >= 0)
                            message.custom_channel_data = object.custom_channel_data;
                return message;
            };

            /**
             * Creates a plain object from a PendingChannel message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.PendingChannel} message PendingChannel
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PendingChannel.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.remote_node_pub = "";
                    object.channel_point = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.capacity = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.capacity = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.local_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.local_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.remote_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.remote_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.local_chan_reserve_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.local_chan_reserve_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.remote_chan_reserve_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.remote_chan_reserve_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.initiator = options.enums === $String ? "INITIATOR_UNKNOWN" : 0;
                    object.commitment_type = options.enums === $String ? "UNKNOWN_COMMITMENT_TYPE" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.num_forwarding_packages = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.num_forwarding_packages = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.chan_status_flags = "";
                    object["private"] = false;
                    object.memo = "";
                    if (options.bytes === $String)
                        object.custom_channel_data = "";
                    else {
                        object.custom_channel_data = [];
                        if (options.bytes !== $Array)
                            object.custom_channel_data = $util.newBuffer(object.custom_channel_data);
                    }
                }
                if (message.remote_node_pub != null && $Object.hasOwnProperty.call(message, "remote_node_pub"))
                    object.remote_node_pub = message.remote_node_pub;
                if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                    object.channel_point = message.channel_point;
                if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.capacity = typeof message.capacity === "number" ? $BigInt(message.capacity) : $util.Long.fromBits(message.capacity.low >>> 0, message.capacity.high >>> 0, false).toBigInt();
                    else if (typeof message.capacity === "number")
                        object.capacity = options.longs === $String ? $String(message.capacity) : message.capacity;
                    else
                        object.capacity = options.longs === $String ? $util.Long.prototype.toString.call(message.capacity) : options.longs === $Number ? new $util.LongBits(message.capacity.low >>> 0, message.capacity.high >>> 0).toNumber() : message.capacity;
                if (message.local_balance != null && $Object.hasOwnProperty.call(message, "local_balance"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.local_balance = typeof message.local_balance === "number" ? $BigInt(message.local_balance) : $util.Long.fromBits(message.local_balance.low >>> 0, message.local_balance.high >>> 0, false).toBigInt();
                    else if (typeof message.local_balance === "number")
                        object.local_balance = options.longs === $String ? $String(message.local_balance) : message.local_balance;
                    else
                        object.local_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.local_balance) : options.longs === $Number ? new $util.LongBits(message.local_balance.low >>> 0, message.local_balance.high >>> 0).toNumber() : message.local_balance;
                if (message.remote_balance != null && $Object.hasOwnProperty.call(message, "remote_balance"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.remote_balance = typeof message.remote_balance === "number" ? $BigInt(message.remote_balance) : $util.Long.fromBits(message.remote_balance.low >>> 0, message.remote_balance.high >>> 0, false).toBigInt();
                    else if (typeof message.remote_balance === "number")
                        object.remote_balance = options.longs === $String ? $String(message.remote_balance) : message.remote_balance;
                    else
                        object.remote_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_balance) : options.longs === $Number ? new $util.LongBits(message.remote_balance.low >>> 0, message.remote_balance.high >>> 0).toNumber() : message.remote_balance;
                if (message.local_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "local_chan_reserve_sat"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.local_chan_reserve_sat = typeof message.local_chan_reserve_sat === "number" ? $BigInt(message.local_chan_reserve_sat) : $util.Long.fromBits(message.local_chan_reserve_sat.low >>> 0, message.local_chan_reserve_sat.high >>> 0, false).toBigInt();
                    else if (typeof message.local_chan_reserve_sat === "number")
                        object.local_chan_reserve_sat = options.longs === $String ? $String(message.local_chan_reserve_sat) : message.local_chan_reserve_sat;
                    else
                        object.local_chan_reserve_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.local_chan_reserve_sat) : options.longs === $Number ? new $util.LongBits(message.local_chan_reserve_sat.low >>> 0, message.local_chan_reserve_sat.high >>> 0).toNumber() : message.local_chan_reserve_sat;
                if (message.remote_chan_reserve_sat != null && $Object.hasOwnProperty.call(message, "remote_chan_reserve_sat"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.remote_chan_reserve_sat = typeof message.remote_chan_reserve_sat === "number" ? $BigInt(message.remote_chan_reserve_sat) : $util.Long.fromBits(message.remote_chan_reserve_sat.low >>> 0, message.remote_chan_reserve_sat.high >>> 0, false).toBigInt();
                    else if (typeof message.remote_chan_reserve_sat === "number")
                        object.remote_chan_reserve_sat = options.longs === $String ? $String(message.remote_chan_reserve_sat) : message.remote_chan_reserve_sat;
                    else
                        object.remote_chan_reserve_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_chan_reserve_sat) : options.longs === $Number ? new $util.LongBits(message.remote_chan_reserve_sat.low >>> 0, message.remote_chan_reserve_sat.high >>> 0).toNumber() : message.remote_chan_reserve_sat;
                if (message.initiator != null && $Object.hasOwnProperty.call(message, "initiator"))
                    object.initiator = options.enums === $String ? $root.lnrpc.Initiator[message.initiator] === $undefined ? message.initiator : $root.lnrpc.Initiator[message.initiator] : message.initiator;
                if (message.commitment_type != null && $Object.hasOwnProperty.call(message, "commitment_type"))
                    object.commitment_type = options.enums === $String ? $root.lnrpc.CommitmentType[message.commitment_type] === $undefined ? message.commitment_type : $root.lnrpc.CommitmentType[message.commitment_type] : message.commitment_type;
                if (message.num_forwarding_packages != null && $Object.hasOwnProperty.call(message, "num_forwarding_packages"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.num_forwarding_packages = typeof message.num_forwarding_packages === "number" ? $BigInt(message.num_forwarding_packages) : $util.Long.fromBits(message.num_forwarding_packages.low >>> 0, message.num_forwarding_packages.high >>> 0, false).toBigInt();
                    else if (typeof message.num_forwarding_packages === "number")
                        object.num_forwarding_packages = options.longs === $String ? $String(message.num_forwarding_packages) : message.num_forwarding_packages;
                    else
                        object.num_forwarding_packages = options.longs === $String ? $util.Long.prototype.toString.call(message.num_forwarding_packages) : options.longs === $Number ? new $util.LongBits(message.num_forwarding_packages.low >>> 0, message.num_forwarding_packages.high >>> 0).toNumber() : message.num_forwarding_packages;
                if (message.chan_status_flags != null && $Object.hasOwnProperty.call(message, "chan_status_flags"))
                    object.chan_status_flags = message.chan_status_flags;
                if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                    object["private"] = message["private"];
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    object.memo = message.memo;
                if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                    object.custom_channel_data = options.bytes === $String ? $util.base64.encode(message.custom_channel_data, 0, message.custom_channel_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_channel_data) : message.custom_channel_data;
                return object;
            };

            /**
             * Converts this PendingChannel to JSON.
             * @function toJSON
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PendingChannel.prototype.toJSON = function() {
                return PendingChannel.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PendingChannel
             * @function getTypeUrl
             * @memberof lnrpc.PendingChannelsResponse.PendingChannel
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PendingChannel.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lnrpc.PendingChannelsResponse.PendingChannel";
            };

            return PendingChannel;
        })();

        PendingChannelsResponse.PendingOpenChannel = (function() {

            /**
             * Properties of a PendingOpenChannel.
             * @typedef {Object} lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties
             * @property {lnrpc.PendingChannelsResponse.PendingChannel.$Properties|null} [channel] PendingOpenChannel channel
             * @property {Long|null} [commit_fee] PendingOpenChannel commit_fee
             * @property {Long|null} [commit_weight] PendingOpenChannel commit_weight
             * @property {Long|null} [fee_per_kw] PendingOpenChannel fee_per_kw
             * @property {number|null} [funding_expiry_blocks] PendingOpenChannel funding_expiry_blocks
             * @property {number|null} [confirmations_until_active] PendingOpenChannel confirmations_until_active
             * @property {number|null} [confirmation_height] PendingOpenChannel confirmation_height
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */

            /**
             * Properties of a PendingOpenChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @interface IPendingOpenChannel
             * @augments lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties
             * @deprecated Use lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties instead.
             */

            /**
             * Shape of a PendingOpenChannel.
             * @typedef {lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties} lnrpc.PendingChannelsResponse.PendingOpenChannel.$Shape
             */

            /**
             * Constructs a new PendingOpenChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @classdesc Represents a PendingOpenChannel.
             * @constructor
             * @param {lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */
            const PendingOpenChannel = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PendingOpenChannel channel.
             * @member {lnrpc.PendingChannelsResponse.PendingChannel.$Properties|null|undefined} channel
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @instance
             */
            PendingOpenChannel.prototype.channel = null;

            /**
             * PendingOpenChannel commit_fee.
             * @member {Long} commit_fee
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @instance
             */
            PendingOpenChannel.prototype.commit_fee = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PendingOpenChannel commit_weight.
             * @member {Long} commit_weight
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @instance
             */
            PendingOpenChannel.prototype.commit_weight = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PendingOpenChannel fee_per_kw.
             * @member {Long} fee_per_kw
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @instance
             */
            PendingOpenChannel.prototype.fee_per_kw = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PendingOpenChannel funding_expiry_blocks.
             * @member {number} funding_expiry_blocks
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @instance
             */
            PendingOpenChannel.prototype.funding_expiry_blocks = 0;

            /**
             * PendingOpenChannel confirmations_until_active.
             * @member {number} confirmations_until_active
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @instance
             */
            PendingOpenChannel.prototype.confirmations_until_active = 0;

            /**
             * PendingOpenChannel confirmation_height.
             * @member {number} confirmation_height
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @instance
             */
            PendingOpenChannel.prototype.confirmation_height = 0;

            /**
             * Creates a new PendingOpenChannel instance using the specified properties.
             * @function create
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties=} [properties] Properties to set
             * @returns {lnrpc.PendingChannelsResponse.PendingOpenChannel} PendingOpenChannel instance
             * @type {{
             *   (properties: lnrpc.PendingChannelsResponse.PendingOpenChannel.$Shape): lnrpc.PendingChannelsResponse.PendingOpenChannel & lnrpc.PendingChannelsResponse.PendingOpenChannel.$Shape;
             *   (properties?: lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties): lnrpc.PendingChannelsResponse.PendingOpenChannel;
             * }}
             */
            PendingOpenChannel.create = function(properties) {
                return new PendingOpenChannel(properties);
            };

            /**
             * Encodes the specified PendingOpenChannel message. Does not implicitly {@link lnrpc.PendingChannelsResponse.PendingOpenChannel.verify|verify} messages.
             * @function encode
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties} message PendingOpenChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PendingOpenChannel.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                    $root.lnrpc.PendingChannelsResponse.PendingChannel.encode(message.channel, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.funding_expiry_blocks != null && $Object.hasOwnProperty.call(message, "funding_expiry_blocks") && message.funding_expiry_blocks !== 0)
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.funding_expiry_blocks);
                if (message.commit_fee != null && $Object.hasOwnProperty.call(message, "commit_fee") && (typeof message.commit_fee === "object" ? message.commit_fee.low || message.commit_fee.high : message.commit_fee !== 0))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.commit_fee);
                if (message.commit_weight != null && $Object.hasOwnProperty.call(message, "commit_weight") && (typeof message.commit_weight === "object" ? message.commit_weight.low || message.commit_weight.high : message.commit_weight !== 0))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.commit_weight);
                if (message.fee_per_kw != null && $Object.hasOwnProperty.call(message, "fee_per_kw") && (typeof message.fee_per_kw === "object" ? message.fee_per_kw.low || message.fee_per_kw.high : message.fee_per_kw !== 0))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.fee_per_kw);
                if (message.confirmations_until_active != null && $Object.hasOwnProperty.call(message, "confirmations_until_active") && message.confirmations_until_active !== 0)
                    writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.confirmations_until_active);
                if (message.confirmation_height != null && $Object.hasOwnProperty.call(message, "confirmation_height") && message.confirmation_height !== 0)
                    writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.confirmation_height);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PendingOpenChannel message, length delimited. Does not implicitly {@link lnrpc.PendingChannelsResponse.PendingOpenChannel.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.PendingOpenChannel.$Properties} message PendingOpenChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PendingOpenChannel.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PendingOpenChannel message from the specified reader or buffer.
             * @function decode
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lnrpc.PendingChannelsResponse.PendingOpenChannel & lnrpc.PendingChannelsResponse.PendingOpenChannel.$Shape} PendingOpenChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PendingOpenChannel.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingChannelsResponse.PendingOpenChannel(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.commit_fee = value;
                            else
                                delete message.commit_fee;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.commit_weight = value;
                            else
                                delete message.commit_weight;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.fee_per_kw = value;
                            else
                                delete message.fee_per_kw;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.funding_expiry_blocks = value;
                            else
                                delete message.funding_expiry_blocks;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.confirmations_until_active = value;
                            else
                                delete message.confirmations_until_active;
                            continue;
                        }
                    case 8: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.confirmation_height = value;
                            else
                                delete message.confirmation_height;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a PendingOpenChannel message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lnrpc.PendingChannelsResponse.PendingOpenChannel & lnrpc.PendingChannelsResponse.PendingOpenChannel.$Shape} PendingOpenChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PendingOpenChannel.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PendingOpenChannel message.
             * @function verify
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PendingOpenChannel.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel")) {
                    let error = $root.lnrpc.PendingChannelsResponse.PendingChannel.verify(message.channel, _depth + 1);
                    if (error)
                        return "channel." + error;
                }
                if (message.commit_fee != null && $Object.hasOwnProperty.call(message, "commit_fee"))
                    if (!$util.isInteger(message.commit_fee) && !(message.commit_fee && $util.isInteger(message.commit_fee.low) && $util.isInteger(message.commit_fee.high)))
                        return "commit_fee: integer|Long expected";
                if (message.commit_weight != null && $Object.hasOwnProperty.call(message, "commit_weight"))
                    if (!$util.isInteger(message.commit_weight) && !(message.commit_weight && $util.isInteger(message.commit_weight.low) && $util.isInteger(message.commit_weight.high)))
                        return "commit_weight: integer|Long expected";
                if (message.fee_per_kw != null && $Object.hasOwnProperty.call(message, "fee_per_kw"))
                    if (!$util.isInteger(message.fee_per_kw) && !(message.fee_per_kw && $util.isInteger(message.fee_per_kw.low) && $util.isInteger(message.fee_per_kw.high)))
                        return "fee_per_kw: integer|Long expected";
                if (message.funding_expiry_blocks != null && $Object.hasOwnProperty.call(message, "funding_expiry_blocks"))
                    if (!$util.isInteger(message.funding_expiry_blocks))
                        return "funding_expiry_blocks: integer expected";
                if (message.confirmations_until_active != null && $Object.hasOwnProperty.call(message, "confirmations_until_active"))
                    if (!$util.isInteger(message.confirmations_until_active))
                        return "confirmations_until_active: integer expected";
                if (message.confirmation_height != null && $Object.hasOwnProperty.call(message, "confirmation_height"))
                    if (!$util.isInteger(message.confirmation_height))
                        return "confirmation_height: integer expected";
                return null;
            };

            /**
             * Creates a PendingOpenChannel message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lnrpc.PendingChannelsResponse.PendingOpenChannel} PendingOpenChannel
             */
            PendingOpenChannel.fromObject = function (object, _depth) {
                if (object instanceof $root.lnrpc.PendingChannelsResponse.PendingOpenChannel)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.PendingOpenChannel: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.lnrpc.PendingChannelsResponse.PendingOpenChannel();
                if (object.channel != null) {
                    if (!$util.isObject(object.channel))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.PendingOpenChannel.channel: object expected");
                    message.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.fromObject(object.channel, _depth + 1);
                }
                if (object.commit_fee != null)
                    if (typeof object.commit_fee === "object" ? object.commit_fee.low || object.commit_fee.high : $Number(object.commit_fee) !== 0)
                        if ($util.Long)
                            message.commit_fee = $util.Long.fromValue(object.commit_fee, false);
                        else if (typeof object.commit_fee === "string")
                            message.commit_fee = $parseInt(object.commit_fee, 10);
                        else if (typeof object.commit_fee === "number")
                            message.commit_fee = object.commit_fee;
                        else if (typeof object.commit_fee === "object")
                            message.commit_fee = new $util.LongBits(object.commit_fee.low >>> 0, object.commit_fee.high >>> 0).toNumber();
                if (object.commit_weight != null)
                    if (typeof object.commit_weight === "object" ? object.commit_weight.low || object.commit_weight.high : $Number(object.commit_weight) !== 0)
                        if ($util.Long)
                            message.commit_weight = $util.Long.fromValue(object.commit_weight, false);
                        else if (typeof object.commit_weight === "string")
                            message.commit_weight = $parseInt(object.commit_weight, 10);
                        else if (typeof object.commit_weight === "number")
                            message.commit_weight = object.commit_weight;
                        else if (typeof object.commit_weight === "object")
                            message.commit_weight = new $util.LongBits(object.commit_weight.low >>> 0, object.commit_weight.high >>> 0).toNumber();
                if (object.fee_per_kw != null)
                    if (typeof object.fee_per_kw === "object" ? object.fee_per_kw.low || object.fee_per_kw.high : $Number(object.fee_per_kw) !== 0)
                        if ($util.Long)
                            message.fee_per_kw = $util.Long.fromValue(object.fee_per_kw, false);
                        else if (typeof object.fee_per_kw === "string")
                            message.fee_per_kw = $parseInt(object.fee_per_kw, 10);
                        else if (typeof object.fee_per_kw === "number")
                            message.fee_per_kw = object.fee_per_kw;
                        else if (typeof object.fee_per_kw === "object")
                            message.fee_per_kw = new $util.LongBits(object.fee_per_kw.low >>> 0, object.fee_per_kw.high >>> 0).toNumber();
                if (object.funding_expiry_blocks != null)
                    if ($Number(object.funding_expiry_blocks) !== 0)
                        message.funding_expiry_blocks = object.funding_expiry_blocks | 0;
                if (object.confirmations_until_active != null)
                    if ($Number(object.confirmations_until_active) !== 0)
                        message.confirmations_until_active = object.confirmations_until_active >>> 0;
                if (object.confirmation_height != null)
                    if ($Number(object.confirmation_height) !== 0)
                        message.confirmation_height = object.confirmation_height >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a PendingOpenChannel message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.PendingOpenChannel} message PendingOpenChannel
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PendingOpenChannel.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.channel = null;
                    object.funding_expiry_blocks = 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.commit_fee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.commit_fee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.commit_weight = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.commit_weight = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.fee_per_kw = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.fee_per_kw = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.confirmations_until_active = 0;
                    object.confirmation_height = 0;
                }
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                    object.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.toObject(message.channel, options, _depth + 1);
                if (message.funding_expiry_blocks != null && $Object.hasOwnProperty.call(message, "funding_expiry_blocks"))
                    object.funding_expiry_blocks = message.funding_expiry_blocks;
                if (message.commit_fee != null && $Object.hasOwnProperty.call(message, "commit_fee"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.commit_fee = typeof message.commit_fee === "number" ? $BigInt(message.commit_fee) : $util.Long.fromBits(message.commit_fee.low >>> 0, message.commit_fee.high >>> 0, false).toBigInt();
                    else if (typeof message.commit_fee === "number")
                        object.commit_fee = options.longs === $String ? $String(message.commit_fee) : message.commit_fee;
                    else
                        object.commit_fee = options.longs === $String ? $util.Long.prototype.toString.call(message.commit_fee) : options.longs === $Number ? new $util.LongBits(message.commit_fee.low >>> 0, message.commit_fee.high >>> 0).toNumber() : message.commit_fee;
                if (message.commit_weight != null && $Object.hasOwnProperty.call(message, "commit_weight"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.commit_weight = typeof message.commit_weight === "number" ? $BigInt(message.commit_weight) : $util.Long.fromBits(message.commit_weight.low >>> 0, message.commit_weight.high >>> 0, false).toBigInt();
                    else if (typeof message.commit_weight === "number")
                        object.commit_weight = options.longs === $String ? $String(message.commit_weight) : message.commit_weight;
                    else
                        object.commit_weight = options.longs === $String ? $util.Long.prototype.toString.call(message.commit_weight) : options.longs === $Number ? new $util.LongBits(message.commit_weight.low >>> 0, message.commit_weight.high >>> 0).toNumber() : message.commit_weight;
                if (message.fee_per_kw != null && $Object.hasOwnProperty.call(message, "fee_per_kw"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.fee_per_kw = typeof message.fee_per_kw === "number" ? $BigInt(message.fee_per_kw) : $util.Long.fromBits(message.fee_per_kw.low >>> 0, message.fee_per_kw.high >>> 0, false).toBigInt();
                    else if (typeof message.fee_per_kw === "number")
                        object.fee_per_kw = options.longs === $String ? $String(message.fee_per_kw) : message.fee_per_kw;
                    else
                        object.fee_per_kw = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_per_kw) : options.longs === $Number ? new $util.LongBits(message.fee_per_kw.low >>> 0, message.fee_per_kw.high >>> 0).toNumber() : message.fee_per_kw;
                if (message.confirmations_until_active != null && $Object.hasOwnProperty.call(message, "confirmations_until_active"))
                    object.confirmations_until_active = message.confirmations_until_active;
                if (message.confirmation_height != null && $Object.hasOwnProperty.call(message, "confirmation_height"))
                    object.confirmation_height = message.confirmation_height;
                return object;
            };

            /**
             * Converts this PendingOpenChannel to JSON.
             * @function toJSON
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PendingOpenChannel.prototype.toJSON = function() {
                return PendingOpenChannel.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PendingOpenChannel
             * @function getTypeUrl
             * @memberof lnrpc.PendingChannelsResponse.PendingOpenChannel
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PendingOpenChannel.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lnrpc.PendingChannelsResponse.PendingOpenChannel";
            };

            return PendingOpenChannel;
        })();

        PendingChannelsResponse.WaitingCloseChannel = (function() {

            /**
             * Properties of a WaitingCloseChannel.
             * @typedef {Object} lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties
             * @property {lnrpc.PendingChannelsResponse.PendingChannel.$Properties|null} [channel] WaitingCloseChannel channel
             * @property {Long|null} [limbo_balance] WaitingCloseChannel limbo_balance
             * @property {lnrpc.PendingChannelsResponse.Commitments.$Properties|null} [commitments] WaitingCloseChannel commitments
             * @property {string|null} [closing_txid] WaitingCloseChannel closing_txid
             * @property {string|null} [closing_tx_hex] WaitingCloseChannel closing_tx_hex
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */

            /**
             * Properties of a WaitingCloseChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @interface IWaitingCloseChannel
             * @augments lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties
             * @deprecated Use lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties instead.
             */

            /**
             * Shape of a WaitingCloseChannel.
             * @typedef {lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties} lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Shape
             */

            /**
             * Constructs a new WaitingCloseChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @classdesc Represents a WaitingCloseChannel.
             * @constructor
             * @param {lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */
            const WaitingCloseChannel = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * WaitingCloseChannel channel.
             * @member {lnrpc.PendingChannelsResponse.PendingChannel.$Properties|null|undefined} channel
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @instance
             */
            WaitingCloseChannel.prototype.channel = null;

            /**
             * WaitingCloseChannel limbo_balance.
             * @member {Long} limbo_balance
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @instance
             */
            WaitingCloseChannel.prototype.limbo_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * WaitingCloseChannel commitments.
             * @member {lnrpc.PendingChannelsResponse.Commitments.$Properties|null|undefined} commitments
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @instance
             */
            WaitingCloseChannel.prototype.commitments = null;

            /**
             * WaitingCloseChannel closing_txid.
             * @member {string} closing_txid
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @instance
             */
            WaitingCloseChannel.prototype.closing_txid = "";

            /**
             * WaitingCloseChannel closing_tx_hex.
             * @member {string} closing_tx_hex
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @instance
             */
            WaitingCloseChannel.prototype.closing_tx_hex = "";

            /**
             * Creates a new WaitingCloseChannel instance using the specified properties.
             * @function create
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties=} [properties] Properties to set
             * @returns {lnrpc.PendingChannelsResponse.WaitingCloseChannel} WaitingCloseChannel instance
             * @type {{
             *   (properties: lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Shape): lnrpc.PendingChannelsResponse.WaitingCloseChannel & lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Shape;
             *   (properties?: lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties): lnrpc.PendingChannelsResponse.WaitingCloseChannel;
             * }}
             */
            WaitingCloseChannel.create = function(properties) {
                return new WaitingCloseChannel(properties);
            };

            /**
             * Encodes the specified WaitingCloseChannel message. Does not implicitly {@link lnrpc.PendingChannelsResponse.WaitingCloseChannel.verify|verify} messages.
             * @function encode
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties} message WaitingCloseChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WaitingCloseChannel.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                    $root.lnrpc.PendingChannelsResponse.PendingChannel.encode(message.channel, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.limbo_balance != null && $Object.hasOwnProperty.call(message, "limbo_balance") && (typeof message.limbo_balance === "object" ? message.limbo_balance.low || message.limbo_balance.high : message.limbo_balance !== 0))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.limbo_balance);
                if (message.commitments != null && $Object.hasOwnProperty.call(message, "commitments"))
                    $root.lnrpc.PendingChannelsResponse.Commitments.encode(message.commitments, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid") && message.closing_txid !== "")
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.closing_txid);
                if (message.closing_tx_hex != null && $Object.hasOwnProperty.call(message, "closing_tx_hex") && message.closing_tx_hex !== "")
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.closing_tx_hex);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified WaitingCloseChannel message, length delimited. Does not implicitly {@link lnrpc.PendingChannelsResponse.WaitingCloseChannel.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Properties} message WaitingCloseChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WaitingCloseChannel.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a WaitingCloseChannel message from the specified reader or buffer.
             * @function decode
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lnrpc.PendingChannelsResponse.WaitingCloseChannel & lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Shape} WaitingCloseChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WaitingCloseChannel.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingChannelsResponse.WaitingCloseChannel(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.limbo_balance = value;
                            else
                                delete message.limbo_balance;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.commitments = $root.lnrpc.PendingChannelsResponse.Commitments.decode(reader, reader.uint32(), $undefined, _depth + 1, message.commitments);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.closing_txid = value;
                            else
                                delete message.closing_txid;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.closing_tx_hex = value;
                            else
                                delete message.closing_tx_hex;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a WaitingCloseChannel message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lnrpc.PendingChannelsResponse.WaitingCloseChannel & lnrpc.PendingChannelsResponse.WaitingCloseChannel.$Shape} WaitingCloseChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WaitingCloseChannel.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a WaitingCloseChannel message.
             * @function verify
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            WaitingCloseChannel.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel")) {
                    let error = $root.lnrpc.PendingChannelsResponse.PendingChannel.verify(message.channel, _depth + 1);
                    if (error)
                        return "channel." + error;
                }
                if (message.limbo_balance != null && $Object.hasOwnProperty.call(message, "limbo_balance"))
                    if (!$util.isInteger(message.limbo_balance) && !(message.limbo_balance && $util.isInteger(message.limbo_balance.low) && $util.isInteger(message.limbo_balance.high)))
                        return "limbo_balance: integer|Long expected";
                if (message.commitments != null && $Object.hasOwnProperty.call(message, "commitments")) {
                    let error = $root.lnrpc.PendingChannelsResponse.Commitments.verify(message.commitments, _depth + 1);
                    if (error)
                        return "commitments." + error;
                }
                if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid"))
                    if (!$util.isString(message.closing_txid))
                        return "closing_txid: string expected";
                if (message.closing_tx_hex != null && $Object.hasOwnProperty.call(message, "closing_tx_hex"))
                    if (!$util.isString(message.closing_tx_hex))
                        return "closing_tx_hex: string expected";
                return null;
            };

            /**
             * Creates a WaitingCloseChannel message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lnrpc.PendingChannelsResponse.WaitingCloseChannel} WaitingCloseChannel
             */
            WaitingCloseChannel.fromObject = function (object, _depth) {
                if (object instanceof $root.lnrpc.PendingChannelsResponse.WaitingCloseChannel)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.WaitingCloseChannel: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.lnrpc.PendingChannelsResponse.WaitingCloseChannel();
                if (object.channel != null) {
                    if (!$util.isObject(object.channel))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.WaitingCloseChannel.channel: object expected");
                    message.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.fromObject(object.channel, _depth + 1);
                }
                if (object.limbo_balance != null)
                    if (typeof object.limbo_balance === "object" ? object.limbo_balance.low || object.limbo_balance.high : $Number(object.limbo_balance) !== 0)
                        if ($util.Long)
                            message.limbo_balance = $util.Long.fromValue(object.limbo_balance, false);
                        else if (typeof object.limbo_balance === "string")
                            message.limbo_balance = $parseInt(object.limbo_balance, 10);
                        else if (typeof object.limbo_balance === "number")
                            message.limbo_balance = object.limbo_balance;
                        else if (typeof object.limbo_balance === "object")
                            message.limbo_balance = new $util.LongBits(object.limbo_balance.low >>> 0, object.limbo_balance.high >>> 0).toNumber();
                if (object.commitments != null) {
                    if (!$util.isObject(object.commitments))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.WaitingCloseChannel.commitments: object expected");
                    message.commitments = $root.lnrpc.PendingChannelsResponse.Commitments.fromObject(object.commitments, _depth + 1);
                }
                if (object.closing_txid != null)
                    if (typeof object.closing_txid !== "string" || object.closing_txid.length)
                        message.closing_txid = $String(object.closing_txid);
                if (object.closing_tx_hex != null)
                    if (typeof object.closing_tx_hex !== "string" || object.closing_tx_hex.length)
                        message.closing_tx_hex = $String(object.closing_tx_hex);
                return message;
            };

            /**
             * Creates a plain object from a WaitingCloseChannel message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.WaitingCloseChannel} message WaitingCloseChannel
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            WaitingCloseChannel.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.channel = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.limbo_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.limbo_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.commitments = null;
                    object.closing_txid = "";
                    object.closing_tx_hex = "";
                }
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                    object.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.toObject(message.channel, options, _depth + 1);
                if (message.limbo_balance != null && $Object.hasOwnProperty.call(message, "limbo_balance"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.limbo_balance = typeof message.limbo_balance === "number" ? $BigInt(message.limbo_balance) : $util.Long.fromBits(message.limbo_balance.low >>> 0, message.limbo_balance.high >>> 0, false).toBigInt();
                    else if (typeof message.limbo_balance === "number")
                        object.limbo_balance = options.longs === $String ? $String(message.limbo_balance) : message.limbo_balance;
                    else
                        object.limbo_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.limbo_balance) : options.longs === $Number ? new $util.LongBits(message.limbo_balance.low >>> 0, message.limbo_balance.high >>> 0).toNumber() : message.limbo_balance;
                if (message.commitments != null && $Object.hasOwnProperty.call(message, "commitments"))
                    object.commitments = $root.lnrpc.PendingChannelsResponse.Commitments.toObject(message.commitments, options, _depth + 1);
                if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid"))
                    object.closing_txid = message.closing_txid;
                if (message.closing_tx_hex != null && $Object.hasOwnProperty.call(message, "closing_tx_hex"))
                    object.closing_tx_hex = message.closing_tx_hex;
                return object;
            };

            /**
             * Converts this WaitingCloseChannel to JSON.
             * @function toJSON
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            WaitingCloseChannel.prototype.toJSON = function() {
                return WaitingCloseChannel.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for WaitingCloseChannel
             * @function getTypeUrl
             * @memberof lnrpc.PendingChannelsResponse.WaitingCloseChannel
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            WaitingCloseChannel.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lnrpc.PendingChannelsResponse.WaitingCloseChannel";
            };

            return WaitingCloseChannel;
        })();

        PendingChannelsResponse.Commitments = (function() {

            /**
             * Properties of a Commitments.
             * @typedef {Object} lnrpc.PendingChannelsResponse.Commitments.$Properties
             * @property {string|null} [local_txid] Commitments local_txid
             * @property {string|null} [remote_txid] Commitments remote_txid
             * @property {string|null} [remote_pending_txid] Commitments remote_pending_txid
             * @property {Long|null} [local_commit_fee_sat] Commitments local_commit_fee_sat
             * @property {Long|null} [remote_commit_fee_sat] Commitments remote_commit_fee_sat
             * @property {Long|null} [remote_pending_commit_fee_sat] Commitments remote_pending_commit_fee_sat
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */

            /**
             * Properties of a Commitments.
             * @memberof lnrpc.PendingChannelsResponse
             * @interface ICommitments
             * @augments lnrpc.PendingChannelsResponse.Commitments.$Properties
             * @deprecated Use lnrpc.PendingChannelsResponse.Commitments.$Properties instead.
             */

            /**
             * Shape of a Commitments.
             * @typedef {lnrpc.PendingChannelsResponse.Commitments.$Properties} lnrpc.PendingChannelsResponse.Commitments.$Shape
             */

            /**
             * Constructs a new Commitments.
             * @memberof lnrpc.PendingChannelsResponse
             * @classdesc Represents a Commitments.
             * @constructor
             * @param {lnrpc.PendingChannelsResponse.Commitments.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */
            const Commitments = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Commitments local_txid.
             * @member {string} local_txid
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @instance
             */
            Commitments.prototype.local_txid = "";

            /**
             * Commitments remote_txid.
             * @member {string} remote_txid
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @instance
             */
            Commitments.prototype.remote_txid = "";

            /**
             * Commitments remote_pending_txid.
             * @member {string} remote_pending_txid
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @instance
             */
            Commitments.prototype.remote_pending_txid = "";

            /**
             * Commitments local_commit_fee_sat.
             * @member {Long} local_commit_fee_sat
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @instance
             */
            Commitments.prototype.local_commit_fee_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Commitments remote_commit_fee_sat.
             * @member {Long} remote_commit_fee_sat
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @instance
             */
            Commitments.prototype.remote_commit_fee_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Commitments remote_pending_commit_fee_sat.
             * @member {Long} remote_pending_commit_fee_sat
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @instance
             */
            Commitments.prototype.remote_pending_commit_fee_sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Creates a new Commitments instance using the specified properties.
             * @function create
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @static
             * @param {lnrpc.PendingChannelsResponse.Commitments.$Properties=} [properties] Properties to set
             * @returns {lnrpc.PendingChannelsResponse.Commitments} Commitments instance
             * @type {{
             *   (properties: lnrpc.PendingChannelsResponse.Commitments.$Shape): lnrpc.PendingChannelsResponse.Commitments & lnrpc.PendingChannelsResponse.Commitments.$Shape;
             *   (properties?: lnrpc.PendingChannelsResponse.Commitments.$Properties): lnrpc.PendingChannelsResponse.Commitments;
             * }}
             */
            Commitments.create = function(properties) {
                return new Commitments(properties);
            };

            /**
             * Encodes the specified Commitments message. Does not implicitly {@link lnrpc.PendingChannelsResponse.Commitments.verify|verify} messages.
             * @function encode
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @static
             * @param {lnrpc.PendingChannelsResponse.Commitments.$Properties} message Commitments message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Commitments.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.local_txid != null && $Object.hasOwnProperty.call(message, "local_txid") && message.local_txid !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.local_txid);
                if (message.remote_txid != null && $Object.hasOwnProperty.call(message, "remote_txid") && message.remote_txid !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.remote_txid);
                if (message.remote_pending_txid != null && $Object.hasOwnProperty.call(message, "remote_pending_txid") && message.remote_pending_txid !== "")
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.remote_pending_txid);
                if (message.local_commit_fee_sat != null && $Object.hasOwnProperty.call(message, "local_commit_fee_sat") && (typeof message.local_commit_fee_sat === "object" ? message.local_commit_fee_sat.low || message.local_commit_fee_sat.high : message.local_commit_fee_sat !== 0))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.local_commit_fee_sat);
                if (message.remote_commit_fee_sat != null && $Object.hasOwnProperty.call(message, "remote_commit_fee_sat") && (typeof message.remote_commit_fee_sat === "object" ? message.remote_commit_fee_sat.low || message.remote_commit_fee_sat.high : message.remote_commit_fee_sat !== 0))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.remote_commit_fee_sat);
                if (message.remote_pending_commit_fee_sat != null && $Object.hasOwnProperty.call(message, "remote_pending_commit_fee_sat") && (typeof message.remote_pending_commit_fee_sat === "object" ? message.remote_pending_commit_fee_sat.low || message.remote_pending_commit_fee_sat.high : message.remote_pending_commit_fee_sat !== 0))
                    writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.remote_pending_commit_fee_sat);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Commitments message, length delimited. Does not implicitly {@link lnrpc.PendingChannelsResponse.Commitments.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @static
             * @param {lnrpc.PendingChannelsResponse.Commitments.$Properties} message Commitments message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Commitments.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Commitments message from the specified reader or buffer.
             * @function decode
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lnrpc.PendingChannelsResponse.Commitments & lnrpc.PendingChannelsResponse.Commitments.$Shape} Commitments
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Commitments.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingChannelsResponse.Commitments(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.local_txid = value;
                            else
                                delete message.local_txid;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.remote_txid = value;
                            else
                                delete message.remote_txid;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.remote_pending_txid = value;
                            else
                                delete message.remote_pending_txid;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.local_commit_fee_sat = value;
                            else
                                delete message.local_commit_fee_sat;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.remote_commit_fee_sat = value;
                            else
                                delete message.remote_commit_fee_sat;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.remote_pending_commit_fee_sat = value;
                            else
                                delete message.remote_pending_commit_fee_sat;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Commitments message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lnrpc.PendingChannelsResponse.Commitments & lnrpc.PendingChannelsResponse.Commitments.$Shape} Commitments
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Commitments.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Commitments message.
             * @function verify
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Commitments.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.local_txid != null && $Object.hasOwnProperty.call(message, "local_txid"))
                    if (!$util.isString(message.local_txid))
                        return "local_txid: string expected";
                if (message.remote_txid != null && $Object.hasOwnProperty.call(message, "remote_txid"))
                    if (!$util.isString(message.remote_txid))
                        return "remote_txid: string expected";
                if (message.remote_pending_txid != null && $Object.hasOwnProperty.call(message, "remote_pending_txid"))
                    if (!$util.isString(message.remote_pending_txid))
                        return "remote_pending_txid: string expected";
                if (message.local_commit_fee_sat != null && $Object.hasOwnProperty.call(message, "local_commit_fee_sat"))
                    if (!$util.isInteger(message.local_commit_fee_sat) && !(message.local_commit_fee_sat && $util.isInteger(message.local_commit_fee_sat.low) && $util.isInteger(message.local_commit_fee_sat.high)))
                        return "local_commit_fee_sat: integer|Long expected";
                if (message.remote_commit_fee_sat != null && $Object.hasOwnProperty.call(message, "remote_commit_fee_sat"))
                    if (!$util.isInteger(message.remote_commit_fee_sat) && !(message.remote_commit_fee_sat && $util.isInteger(message.remote_commit_fee_sat.low) && $util.isInteger(message.remote_commit_fee_sat.high)))
                        return "remote_commit_fee_sat: integer|Long expected";
                if (message.remote_pending_commit_fee_sat != null && $Object.hasOwnProperty.call(message, "remote_pending_commit_fee_sat"))
                    if (!$util.isInteger(message.remote_pending_commit_fee_sat) && !(message.remote_pending_commit_fee_sat && $util.isInteger(message.remote_pending_commit_fee_sat.low) && $util.isInteger(message.remote_pending_commit_fee_sat.high)))
                        return "remote_pending_commit_fee_sat: integer|Long expected";
                return null;
            };

            /**
             * Creates a Commitments message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lnrpc.PendingChannelsResponse.Commitments} Commitments
             */
            Commitments.fromObject = function (object, _depth) {
                if (object instanceof $root.lnrpc.PendingChannelsResponse.Commitments)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.Commitments: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.lnrpc.PendingChannelsResponse.Commitments();
                if (object.local_txid != null)
                    if (typeof object.local_txid !== "string" || object.local_txid.length)
                        message.local_txid = $String(object.local_txid);
                if (object.remote_txid != null)
                    if (typeof object.remote_txid !== "string" || object.remote_txid.length)
                        message.remote_txid = $String(object.remote_txid);
                if (object.remote_pending_txid != null)
                    if (typeof object.remote_pending_txid !== "string" || object.remote_pending_txid.length)
                        message.remote_pending_txid = $String(object.remote_pending_txid);
                if (object.local_commit_fee_sat != null)
                    if (typeof object.local_commit_fee_sat === "object" ? object.local_commit_fee_sat.low || object.local_commit_fee_sat.high : $Number(object.local_commit_fee_sat) !== 0)
                        if ($util.Long)
                            message.local_commit_fee_sat = $util.Long.fromValue(object.local_commit_fee_sat, true);
                        else if (typeof object.local_commit_fee_sat === "string")
                            message.local_commit_fee_sat = $parseInt(object.local_commit_fee_sat, 10);
                        else if (typeof object.local_commit_fee_sat === "number")
                            message.local_commit_fee_sat = object.local_commit_fee_sat;
                        else if (typeof object.local_commit_fee_sat === "object")
                            message.local_commit_fee_sat = new $util.LongBits(object.local_commit_fee_sat.low >>> 0, object.local_commit_fee_sat.high >>> 0).toNumber(true);
                if (object.remote_commit_fee_sat != null)
                    if (typeof object.remote_commit_fee_sat === "object" ? object.remote_commit_fee_sat.low || object.remote_commit_fee_sat.high : $Number(object.remote_commit_fee_sat) !== 0)
                        if ($util.Long)
                            message.remote_commit_fee_sat = $util.Long.fromValue(object.remote_commit_fee_sat, true);
                        else if (typeof object.remote_commit_fee_sat === "string")
                            message.remote_commit_fee_sat = $parseInt(object.remote_commit_fee_sat, 10);
                        else if (typeof object.remote_commit_fee_sat === "number")
                            message.remote_commit_fee_sat = object.remote_commit_fee_sat;
                        else if (typeof object.remote_commit_fee_sat === "object")
                            message.remote_commit_fee_sat = new $util.LongBits(object.remote_commit_fee_sat.low >>> 0, object.remote_commit_fee_sat.high >>> 0).toNumber(true);
                if (object.remote_pending_commit_fee_sat != null)
                    if (typeof object.remote_pending_commit_fee_sat === "object" ? object.remote_pending_commit_fee_sat.low || object.remote_pending_commit_fee_sat.high : $Number(object.remote_pending_commit_fee_sat) !== 0)
                        if ($util.Long)
                            message.remote_pending_commit_fee_sat = $util.Long.fromValue(object.remote_pending_commit_fee_sat, true);
                        else if (typeof object.remote_pending_commit_fee_sat === "string")
                            message.remote_pending_commit_fee_sat = $parseInt(object.remote_pending_commit_fee_sat, 10);
                        else if (typeof object.remote_pending_commit_fee_sat === "number")
                            message.remote_pending_commit_fee_sat = object.remote_pending_commit_fee_sat;
                        else if (typeof object.remote_pending_commit_fee_sat === "object")
                            message.remote_pending_commit_fee_sat = new $util.LongBits(object.remote_pending_commit_fee_sat.low >>> 0, object.remote_pending_commit_fee_sat.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a Commitments message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @static
             * @param {lnrpc.PendingChannelsResponse.Commitments} message Commitments
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Commitments.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.local_txid = "";
                    object.remote_txid = "";
                    object.remote_pending_txid = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.local_commit_fee_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.local_commit_fee_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.remote_commit_fee_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.remote_commit_fee_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.remote_pending_commit_fee_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.remote_pending_commit_fee_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                }
                if (message.local_txid != null && $Object.hasOwnProperty.call(message, "local_txid"))
                    object.local_txid = message.local_txid;
                if (message.remote_txid != null && $Object.hasOwnProperty.call(message, "remote_txid"))
                    object.remote_txid = message.remote_txid;
                if (message.remote_pending_txid != null && $Object.hasOwnProperty.call(message, "remote_pending_txid"))
                    object.remote_pending_txid = message.remote_pending_txid;
                if (message.local_commit_fee_sat != null && $Object.hasOwnProperty.call(message, "local_commit_fee_sat"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.local_commit_fee_sat = typeof message.local_commit_fee_sat === "number" ? $BigInt(message.local_commit_fee_sat) : $util.Long.fromBits(message.local_commit_fee_sat.low >>> 0, message.local_commit_fee_sat.high >>> 0, true).toBigInt();
                    else if (typeof message.local_commit_fee_sat === "number")
                        object.local_commit_fee_sat = options.longs === $String ? $String(message.local_commit_fee_sat) : message.local_commit_fee_sat;
                    else
                        object.local_commit_fee_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.local_commit_fee_sat) : options.longs === $Number ? new $util.LongBits(message.local_commit_fee_sat.low >>> 0, message.local_commit_fee_sat.high >>> 0).toNumber(true) : message.local_commit_fee_sat;
                if (message.remote_commit_fee_sat != null && $Object.hasOwnProperty.call(message, "remote_commit_fee_sat"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.remote_commit_fee_sat = typeof message.remote_commit_fee_sat === "number" ? $BigInt(message.remote_commit_fee_sat) : $util.Long.fromBits(message.remote_commit_fee_sat.low >>> 0, message.remote_commit_fee_sat.high >>> 0, true).toBigInt();
                    else if (typeof message.remote_commit_fee_sat === "number")
                        object.remote_commit_fee_sat = options.longs === $String ? $String(message.remote_commit_fee_sat) : message.remote_commit_fee_sat;
                    else
                        object.remote_commit_fee_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_commit_fee_sat) : options.longs === $Number ? new $util.LongBits(message.remote_commit_fee_sat.low >>> 0, message.remote_commit_fee_sat.high >>> 0).toNumber(true) : message.remote_commit_fee_sat;
                if (message.remote_pending_commit_fee_sat != null && $Object.hasOwnProperty.call(message, "remote_pending_commit_fee_sat"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.remote_pending_commit_fee_sat = typeof message.remote_pending_commit_fee_sat === "number" ? $BigInt(message.remote_pending_commit_fee_sat) : $util.Long.fromBits(message.remote_pending_commit_fee_sat.low >>> 0, message.remote_pending_commit_fee_sat.high >>> 0, true).toBigInt();
                    else if (typeof message.remote_pending_commit_fee_sat === "number")
                        object.remote_pending_commit_fee_sat = options.longs === $String ? $String(message.remote_pending_commit_fee_sat) : message.remote_pending_commit_fee_sat;
                    else
                        object.remote_pending_commit_fee_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.remote_pending_commit_fee_sat) : options.longs === $Number ? new $util.LongBits(message.remote_pending_commit_fee_sat.low >>> 0, message.remote_pending_commit_fee_sat.high >>> 0).toNumber(true) : message.remote_pending_commit_fee_sat;
                return object;
            };

            /**
             * Converts this Commitments to JSON.
             * @function toJSON
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Commitments.prototype.toJSON = function() {
                return Commitments.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Commitments
             * @function getTypeUrl
             * @memberof lnrpc.PendingChannelsResponse.Commitments
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Commitments.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lnrpc.PendingChannelsResponse.Commitments";
            };

            return Commitments;
        })();

        PendingChannelsResponse.ClosedChannel = (function() {

            /**
             * Properties of a ClosedChannel.
             * @typedef {Object} lnrpc.PendingChannelsResponse.ClosedChannel.$Properties
             * @property {lnrpc.PendingChannelsResponse.PendingChannel.$Properties|null} [channel] ClosedChannel channel
             * @property {string|null} [closing_txid] ClosedChannel closing_txid
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */

            /**
             * Properties of a ClosedChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @interface IClosedChannel
             * @augments lnrpc.PendingChannelsResponse.ClosedChannel.$Properties
             * @deprecated Use lnrpc.PendingChannelsResponse.ClosedChannel.$Properties instead.
             */

            /**
             * Shape of a ClosedChannel.
             * @typedef {lnrpc.PendingChannelsResponse.ClosedChannel.$Properties} lnrpc.PendingChannelsResponse.ClosedChannel.$Shape
             */

            /**
             * Constructs a new ClosedChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @classdesc Represents a ClosedChannel.
             * @constructor
             * @param {lnrpc.PendingChannelsResponse.ClosedChannel.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */
            const ClosedChannel = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ClosedChannel channel.
             * @member {lnrpc.PendingChannelsResponse.PendingChannel.$Properties|null|undefined} channel
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @instance
             */
            ClosedChannel.prototype.channel = null;

            /**
             * ClosedChannel closing_txid.
             * @member {string} closing_txid
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @instance
             */
            ClosedChannel.prototype.closing_txid = "";

            /**
             * Creates a new ClosedChannel instance using the specified properties.
             * @function create
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.ClosedChannel.$Properties=} [properties] Properties to set
             * @returns {lnrpc.PendingChannelsResponse.ClosedChannel} ClosedChannel instance
             * @type {{
             *   (properties: lnrpc.PendingChannelsResponse.ClosedChannel.$Shape): lnrpc.PendingChannelsResponse.ClosedChannel & lnrpc.PendingChannelsResponse.ClosedChannel.$Shape;
             *   (properties?: lnrpc.PendingChannelsResponse.ClosedChannel.$Properties): lnrpc.PendingChannelsResponse.ClosedChannel;
             * }}
             */
            ClosedChannel.create = function(properties) {
                return new ClosedChannel(properties);
            };

            /**
             * Encodes the specified ClosedChannel message. Does not implicitly {@link lnrpc.PendingChannelsResponse.ClosedChannel.verify|verify} messages.
             * @function encode
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.ClosedChannel.$Properties} message ClosedChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClosedChannel.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                    $root.lnrpc.PendingChannelsResponse.PendingChannel.encode(message.channel, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid") && message.closing_txid !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.closing_txid);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ClosedChannel message, length delimited. Does not implicitly {@link lnrpc.PendingChannelsResponse.ClosedChannel.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.ClosedChannel.$Properties} message ClosedChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClosedChannel.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ClosedChannel message from the specified reader or buffer.
             * @function decode
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lnrpc.PendingChannelsResponse.ClosedChannel & lnrpc.PendingChannelsResponse.ClosedChannel.$Shape} ClosedChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClosedChannel.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingChannelsResponse.ClosedChannel(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.closing_txid = value;
                            else
                                delete message.closing_txid;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ClosedChannel message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lnrpc.PendingChannelsResponse.ClosedChannel & lnrpc.PendingChannelsResponse.ClosedChannel.$Shape} ClosedChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClosedChannel.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ClosedChannel message.
             * @function verify
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ClosedChannel.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel")) {
                    let error = $root.lnrpc.PendingChannelsResponse.PendingChannel.verify(message.channel, _depth + 1);
                    if (error)
                        return "channel." + error;
                }
                if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid"))
                    if (!$util.isString(message.closing_txid))
                        return "closing_txid: string expected";
                return null;
            };

            /**
             * Creates a ClosedChannel message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lnrpc.PendingChannelsResponse.ClosedChannel} ClosedChannel
             */
            ClosedChannel.fromObject = function (object, _depth) {
                if (object instanceof $root.lnrpc.PendingChannelsResponse.ClosedChannel)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.ClosedChannel: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.lnrpc.PendingChannelsResponse.ClosedChannel();
                if (object.channel != null) {
                    if (!$util.isObject(object.channel))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.ClosedChannel.channel: object expected");
                    message.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.fromObject(object.channel, _depth + 1);
                }
                if (object.closing_txid != null)
                    if (typeof object.closing_txid !== "string" || object.closing_txid.length)
                        message.closing_txid = $String(object.closing_txid);
                return message;
            };

            /**
             * Creates a plain object from a ClosedChannel message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.ClosedChannel} message ClosedChannel
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ClosedChannel.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.channel = null;
                    object.closing_txid = "";
                }
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                    object.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.toObject(message.channel, options, _depth + 1);
                if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid"))
                    object.closing_txid = message.closing_txid;
                return object;
            };

            /**
             * Converts this ClosedChannel to JSON.
             * @function toJSON
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ClosedChannel.prototype.toJSON = function() {
                return ClosedChannel.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ClosedChannel
             * @function getTypeUrl
             * @memberof lnrpc.PendingChannelsResponse.ClosedChannel
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ClosedChannel.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lnrpc.PendingChannelsResponse.ClosedChannel";
            };

            return ClosedChannel;
        })();

        PendingChannelsResponse.ForceClosedChannel = (function() {

            /**
             * Properties of a ForceClosedChannel.
             * @typedef {Object} lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties
             * @property {lnrpc.PendingChannelsResponse.PendingChannel.$Properties|null} [channel] ForceClosedChannel channel
             * @property {string|null} [closing_txid] ForceClosedChannel closing_txid
             * @property {Long|null} [limbo_balance] ForceClosedChannel limbo_balance
             * @property {number|null} [maturity_height] ForceClosedChannel maturity_height
             * @property {number|null} [blocks_til_maturity] ForceClosedChannel blocks_til_maturity
             * @property {Long|null} [recovered_balance] ForceClosedChannel recovered_balance
             * @property {Array.<lnrpc.PendingHTLC.$Properties>|null} [pending_htlcs] ForceClosedChannel pending_htlcs
             * @property {lnrpc.PendingChannelsResponse.ForceClosedChannel.AnchorState|null} [anchor] ForceClosedChannel anchor
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */

            /**
             * Properties of a ForceClosedChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @interface IForceClosedChannel
             * @augments lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties
             * @deprecated Use lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties instead.
             */

            /**
             * Shape of a ForceClosedChannel.
             * @typedef {lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties} lnrpc.PendingChannelsResponse.ForceClosedChannel.$Shape
             */

            /**
             * Constructs a new ForceClosedChannel.
             * @memberof lnrpc.PendingChannelsResponse
             * @classdesc Represents a ForceClosedChannel.
             * @constructor
             * @param {lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */
            const ForceClosedChannel = function (properties) {
                this.pending_htlcs = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ForceClosedChannel channel.
             * @member {lnrpc.PendingChannelsResponse.PendingChannel.$Properties|null|undefined} channel
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @instance
             */
            ForceClosedChannel.prototype.channel = null;

            /**
             * ForceClosedChannel closing_txid.
             * @member {string} closing_txid
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @instance
             */
            ForceClosedChannel.prototype.closing_txid = "";

            /**
             * ForceClosedChannel limbo_balance.
             * @member {Long} limbo_balance
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @instance
             */
            ForceClosedChannel.prototype.limbo_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * ForceClosedChannel maturity_height.
             * @member {number} maturity_height
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @instance
             */
            ForceClosedChannel.prototype.maturity_height = 0;

            /**
             * ForceClosedChannel blocks_til_maturity.
             * @member {number} blocks_til_maturity
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @instance
             */
            ForceClosedChannel.prototype.blocks_til_maturity = 0;

            /**
             * ForceClosedChannel recovered_balance.
             * @member {Long} recovered_balance
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @instance
             */
            ForceClosedChannel.prototype.recovered_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * ForceClosedChannel pending_htlcs.
             * @member {Array.<lnrpc.PendingHTLC.$Properties>} pending_htlcs
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @instance
             */
            ForceClosedChannel.prototype.pending_htlcs = $util.emptyArray;

            /**
             * ForceClosedChannel anchor.
             * @member {lnrpc.PendingChannelsResponse.ForceClosedChannel.AnchorState} anchor
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @instance
             */
            ForceClosedChannel.prototype.anchor = 0;

            /**
             * Creates a new ForceClosedChannel instance using the specified properties.
             * @function create
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties=} [properties] Properties to set
             * @returns {lnrpc.PendingChannelsResponse.ForceClosedChannel} ForceClosedChannel instance
             * @type {{
             *   (properties: lnrpc.PendingChannelsResponse.ForceClosedChannel.$Shape): lnrpc.PendingChannelsResponse.ForceClosedChannel & lnrpc.PendingChannelsResponse.ForceClosedChannel.$Shape;
             *   (properties?: lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties): lnrpc.PendingChannelsResponse.ForceClosedChannel;
             * }}
             */
            ForceClosedChannel.create = function(properties) {
                return new ForceClosedChannel(properties);
            };

            /**
             * Encodes the specified ForceClosedChannel message. Does not implicitly {@link lnrpc.PendingChannelsResponse.ForceClosedChannel.verify|verify} messages.
             * @function encode
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties} message ForceClosedChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ForceClosedChannel.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                    $root.lnrpc.PendingChannelsResponse.PendingChannel.encode(message.channel, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid") && message.closing_txid !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.closing_txid);
                if (message.limbo_balance != null && $Object.hasOwnProperty.call(message, "limbo_balance") && (typeof message.limbo_balance === "object" ? message.limbo_balance.low || message.limbo_balance.high : message.limbo_balance !== 0))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.limbo_balance);
                if (message.maturity_height != null && $Object.hasOwnProperty.call(message, "maturity_height") && message.maturity_height !== 0)
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.maturity_height);
                if (message.blocks_til_maturity != null && $Object.hasOwnProperty.call(message, "blocks_til_maturity") && message.blocks_til_maturity !== 0)
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.blocks_til_maturity);
                if (message.recovered_balance != null && $Object.hasOwnProperty.call(message, "recovered_balance") && (typeof message.recovered_balance === "object" ? message.recovered_balance.low || message.recovered_balance.high : message.recovered_balance !== 0))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.recovered_balance);
                if (message.pending_htlcs != null && message.pending_htlcs.length)
                    for (let i = 0; i < message.pending_htlcs.length; ++i)
                        $root.lnrpc.PendingHTLC.encode(message.pending_htlcs[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                if (message.anchor != null && $Object.hasOwnProperty.call(message, "anchor") && message.anchor !== 0)
                    writer.uint32(/* id 9, wireType 0 =*/72).int32(message.anchor);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ForceClosedChannel message, length delimited. Does not implicitly {@link lnrpc.PendingChannelsResponse.ForceClosedChannel.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.ForceClosedChannel.$Properties} message ForceClosedChannel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ForceClosedChannel.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ForceClosedChannel message from the specified reader or buffer.
             * @function decode
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lnrpc.PendingChannelsResponse.ForceClosedChannel & lnrpc.PendingChannelsResponse.ForceClosedChannel.$Shape} ForceClosedChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ForceClosedChannel.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PendingChannelsResponse.ForceClosedChannel(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.closing_txid = value;
                            else
                                delete message.closing_txid;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.limbo_balance = value;
                            else
                                delete message.limbo_balance;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.maturity_height = value;
                            else
                                delete message.maturity_height;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.blocks_til_maturity = value;
                            else
                                delete message.blocks_til_maturity;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.recovered_balance = value;
                            else
                                delete message.recovered_balance;
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            if (!(message.pending_htlcs && message.pending_htlcs.length))
                                message.pending_htlcs = [];
                            message.pending_htlcs.push($root.lnrpc.PendingHTLC.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.anchor = value;
                            else
                                delete message.anchor;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ForceClosedChannel message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lnrpc.PendingChannelsResponse.ForceClosedChannel & lnrpc.PendingChannelsResponse.ForceClosedChannel.$Shape} ForceClosedChannel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ForceClosedChannel.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ForceClosedChannel message.
             * @function verify
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ForceClosedChannel.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel")) {
                    let error = $root.lnrpc.PendingChannelsResponse.PendingChannel.verify(message.channel, _depth + 1);
                    if (error)
                        return "channel." + error;
                }
                if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid"))
                    if (!$util.isString(message.closing_txid))
                        return "closing_txid: string expected";
                if (message.limbo_balance != null && $Object.hasOwnProperty.call(message, "limbo_balance"))
                    if (!$util.isInteger(message.limbo_balance) && !(message.limbo_balance && $util.isInteger(message.limbo_balance.low) && $util.isInteger(message.limbo_balance.high)))
                        return "limbo_balance: integer|Long expected";
                if (message.maturity_height != null && $Object.hasOwnProperty.call(message, "maturity_height"))
                    if (!$util.isInteger(message.maturity_height))
                        return "maturity_height: integer expected";
                if (message.blocks_til_maturity != null && $Object.hasOwnProperty.call(message, "blocks_til_maturity"))
                    if (!$util.isInteger(message.blocks_til_maturity))
                        return "blocks_til_maturity: integer expected";
                if (message.recovered_balance != null && $Object.hasOwnProperty.call(message, "recovered_balance"))
                    if (!$util.isInteger(message.recovered_balance) && !(message.recovered_balance && $util.isInteger(message.recovered_balance.low) && $util.isInteger(message.recovered_balance.high)))
                        return "recovered_balance: integer|Long expected";
                if (message.pending_htlcs != null && $Object.hasOwnProperty.call(message, "pending_htlcs")) {
                    if (!$Array.isArray(message.pending_htlcs))
                        return "pending_htlcs: array expected";
                    for (let i = 0; i < message.pending_htlcs.length; ++i) {
                        let error = $root.lnrpc.PendingHTLC.verify(message.pending_htlcs[i], _depth + 1);
                        if (error)
                            return "pending_htlcs." + error;
                    }
                }
                if (message.anchor != null && $Object.hasOwnProperty.call(message, "anchor"))
                    if (typeof message.anchor !== "number" || (message.anchor | 0) !== message.anchor)
                        return "anchor: enum value expected";
                return null;
            };

            /**
             * Creates a ForceClosedChannel message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lnrpc.PendingChannelsResponse.ForceClosedChannel} ForceClosedChannel
             */
            ForceClosedChannel.fromObject = function (object, _depth) {
                if (object instanceof $root.lnrpc.PendingChannelsResponse.ForceClosedChannel)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lnrpc.PendingChannelsResponse.ForceClosedChannel: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.lnrpc.PendingChannelsResponse.ForceClosedChannel();
                if (object.channel != null) {
                    if (!$util.isObject(object.channel))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.ForceClosedChannel.channel: object expected");
                    message.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.fromObject(object.channel, _depth + 1);
                }
                if (object.closing_txid != null)
                    if (typeof object.closing_txid !== "string" || object.closing_txid.length)
                        message.closing_txid = $String(object.closing_txid);
                if (object.limbo_balance != null)
                    if (typeof object.limbo_balance === "object" ? object.limbo_balance.low || object.limbo_balance.high : $Number(object.limbo_balance) !== 0)
                        if ($util.Long)
                            message.limbo_balance = $util.Long.fromValue(object.limbo_balance, false);
                        else if (typeof object.limbo_balance === "string")
                            message.limbo_balance = $parseInt(object.limbo_balance, 10);
                        else if (typeof object.limbo_balance === "number")
                            message.limbo_balance = object.limbo_balance;
                        else if (typeof object.limbo_balance === "object")
                            message.limbo_balance = new $util.LongBits(object.limbo_balance.low >>> 0, object.limbo_balance.high >>> 0).toNumber();
                if (object.maturity_height != null)
                    if ($Number(object.maturity_height) !== 0)
                        message.maturity_height = object.maturity_height >>> 0;
                if (object.blocks_til_maturity != null)
                    if ($Number(object.blocks_til_maturity) !== 0)
                        message.blocks_til_maturity = object.blocks_til_maturity | 0;
                if (object.recovered_balance != null)
                    if (typeof object.recovered_balance === "object" ? object.recovered_balance.low || object.recovered_balance.high : $Number(object.recovered_balance) !== 0)
                        if ($util.Long)
                            message.recovered_balance = $util.Long.fromValue(object.recovered_balance, false);
                        else if (typeof object.recovered_balance === "string")
                            message.recovered_balance = $parseInt(object.recovered_balance, 10);
                        else if (typeof object.recovered_balance === "number")
                            message.recovered_balance = object.recovered_balance;
                        else if (typeof object.recovered_balance === "object")
                            message.recovered_balance = new $util.LongBits(object.recovered_balance.low >>> 0, object.recovered_balance.high >>> 0).toNumber();
                if (object.pending_htlcs) {
                    if (!$Array.isArray(object.pending_htlcs))
                        throw $TypeError(".lnrpc.PendingChannelsResponse.ForceClosedChannel.pending_htlcs: array expected");
                    message.pending_htlcs = $Array(object.pending_htlcs.length);
                    for (let i = 0; i < object.pending_htlcs.length; ++i) {
                        if (!$util.isObject(object.pending_htlcs[i]))
                            throw $TypeError(".lnrpc.PendingChannelsResponse.ForceClosedChannel.pending_htlcs: object expected");
                        message.pending_htlcs[i] = $root.lnrpc.PendingHTLC.fromObject(object.pending_htlcs[i], _depth + 1);
                    }
                }
                if (object.anchor !== 0 && (typeof object.anchor !== "string" || $root.lnrpc.PendingChannelsResponse.ForceClosedChannel.AnchorState[object.anchor] !== 0))
                    switch (object.anchor) {
                    case "LIMBO":
                    case 0:
                        message.anchor = 0;
                        break;
                    case "RECOVERED":
                    case 1:
                        message.anchor = 1;
                        break;
                    case "LOST":
                    case 2:
                        message.anchor = 2;
                        break;
                    default:
                        if (typeof object.anchor === "number" && (object.anchor | 0) === object.anchor)
                            message.anchor = object.anchor;
                    }
                return message;
            };

            /**
             * Creates a plain object from a ForceClosedChannel message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @static
             * @param {lnrpc.PendingChannelsResponse.ForceClosedChannel} message ForceClosedChannel
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ForceClosedChannel.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.pending_htlcs = [];
                if (options.defaults) {
                    object.channel = null;
                    object.closing_txid = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.limbo_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.limbo_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.maturity_height = 0;
                    object.blocks_til_maturity = 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.recovered_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.recovered_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.anchor = options.enums === $String ? "LIMBO" : 0;
                }
                if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                    object.channel = $root.lnrpc.PendingChannelsResponse.PendingChannel.toObject(message.channel, options, _depth + 1);
                if (message.closing_txid != null && $Object.hasOwnProperty.call(message, "closing_txid"))
                    object.closing_txid = message.closing_txid;
                if (message.limbo_balance != null && $Object.hasOwnProperty.call(message, "limbo_balance"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.limbo_balance = typeof message.limbo_balance === "number" ? $BigInt(message.limbo_balance) : $util.Long.fromBits(message.limbo_balance.low >>> 0, message.limbo_balance.high >>> 0, false).toBigInt();
                    else if (typeof message.limbo_balance === "number")
                        object.limbo_balance = options.longs === $String ? $String(message.limbo_balance) : message.limbo_balance;
                    else
                        object.limbo_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.limbo_balance) : options.longs === $Number ? new $util.LongBits(message.limbo_balance.low >>> 0, message.limbo_balance.high >>> 0).toNumber() : message.limbo_balance;
                if (message.maturity_height != null && $Object.hasOwnProperty.call(message, "maturity_height"))
                    object.maturity_height = message.maturity_height;
                if (message.blocks_til_maturity != null && $Object.hasOwnProperty.call(message, "blocks_til_maturity"))
                    object.blocks_til_maturity = message.blocks_til_maturity;
                if (message.recovered_balance != null && $Object.hasOwnProperty.call(message, "recovered_balance"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.recovered_balance = typeof message.recovered_balance === "number" ? $BigInt(message.recovered_balance) : $util.Long.fromBits(message.recovered_balance.low >>> 0, message.recovered_balance.high >>> 0, false).toBigInt();
                    else if (typeof message.recovered_balance === "number")
                        object.recovered_balance = options.longs === $String ? $String(message.recovered_balance) : message.recovered_balance;
                    else
                        object.recovered_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.recovered_balance) : options.longs === $Number ? new $util.LongBits(message.recovered_balance.low >>> 0, message.recovered_balance.high >>> 0).toNumber() : message.recovered_balance;
                if (message.pending_htlcs && message.pending_htlcs.length) {
                    object.pending_htlcs = $Array(message.pending_htlcs.length);
                    for (let j = 0; j < message.pending_htlcs.length; ++j)
                        object.pending_htlcs[j] = $root.lnrpc.PendingHTLC.toObject(message.pending_htlcs[j], options, _depth + 1);
                }
                if (message.anchor != null && $Object.hasOwnProperty.call(message, "anchor"))
                    object.anchor = options.enums === $String ? $root.lnrpc.PendingChannelsResponse.ForceClosedChannel.AnchorState[message.anchor] === $undefined ? message.anchor : $root.lnrpc.PendingChannelsResponse.ForceClosedChannel.AnchorState[message.anchor] : message.anchor;
                return object;
            };

            /**
             * Converts this ForceClosedChannel to JSON.
             * @function toJSON
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ForceClosedChannel.prototype.toJSON = function() {
                return ForceClosedChannel.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ForceClosedChannel
             * @function getTypeUrl
             * @memberof lnrpc.PendingChannelsResponse.ForceClosedChannel
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ForceClosedChannel.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lnrpc.PendingChannelsResponse.ForceClosedChannel";
            };

            /**
             * AnchorState enum.
             * @name lnrpc.PendingChannelsResponse.ForceClosedChannel.AnchorState
             * @enum {number}
             * @property {number} LIMBO=0 LIMBO value
             * @property {number} RECOVERED=1 RECOVERED value
             * @property {number} LOST=2 LOST value
             */
            ForceClosedChannel.AnchorState = (function() {
                const valuesById = {}, values = $Object.create(valuesById);
                values[valuesById[0] = "LIMBO"] = 0;
                values[valuesById[1] = "RECOVERED"] = 1;
                values[valuesById[2] = "LOST"] = 2;
                return values;
            })();

            return ForceClosedChannel;
        })();

        return PendingChannelsResponse;
    })();

    lnrpc.ChannelEventSubscription = (function() {

        /**
         * Properties of a ChannelEventSubscription.
         * @typedef {Object} lnrpc.ChannelEventSubscription.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelEventSubscription.
         * @memberof lnrpc
         * @interface IChannelEventSubscription
         * @augments lnrpc.ChannelEventSubscription.$Properties
         * @deprecated Use lnrpc.ChannelEventSubscription.$Properties instead.
         */

        /**
         * Shape of a ChannelEventSubscription.
         * @typedef {lnrpc.ChannelEventSubscription.$Properties} lnrpc.ChannelEventSubscription.$Shape
         */

        /**
         * Constructs a new ChannelEventSubscription.
         * @memberof lnrpc
         * @classdesc Represents a ChannelEventSubscription.
         * @constructor
         * @param {lnrpc.ChannelEventSubscription.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelEventSubscription = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ChannelEventSubscription instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelEventSubscription
         * @static
         * @param {lnrpc.ChannelEventSubscription.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelEventSubscription} ChannelEventSubscription instance
         * @type {{
         *   (properties: lnrpc.ChannelEventSubscription.$Shape): lnrpc.ChannelEventSubscription & lnrpc.ChannelEventSubscription.$Shape;
         *   (properties?: lnrpc.ChannelEventSubscription.$Properties): lnrpc.ChannelEventSubscription;
         * }}
         */
        ChannelEventSubscription.create = function(properties) {
            return new ChannelEventSubscription(properties);
        };

        /**
         * Encodes the specified ChannelEventSubscription message. Does not implicitly {@link lnrpc.ChannelEventSubscription.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelEventSubscription
         * @static
         * @param {lnrpc.ChannelEventSubscription.$Properties} message ChannelEventSubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelEventSubscription.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelEventSubscription message, length delimited. Does not implicitly {@link lnrpc.ChannelEventSubscription.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelEventSubscription
         * @static
         * @param {lnrpc.ChannelEventSubscription.$Properties} message ChannelEventSubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelEventSubscription.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelEventSubscription message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelEventSubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelEventSubscription & lnrpc.ChannelEventSubscription.$Shape} ChannelEventSubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelEventSubscription.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelEventSubscription();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelEventSubscription message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelEventSubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelEventSubscription & lnrpc.ChannelEventSubscription.$Shape} ChannelEventSubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelEventSubscription.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelEventSubscription message.
         * @function verify
         * @memberof lnrpc.ChannelEventSubscription
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelEventSubscription.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ChannelEventSubscription message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelEventSubscription
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelEventSubscription} ChannelEventSubscription
         */
        ChannelEventSubscription.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelEventSubscription)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelEventSubscription: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.ChannelEventSubscription();
        };

        /**
         * Creates a plain object from a ChannelEventSubscription message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelEventSubscription
         * @static
         * @param {lnrpc.ChannelEventSubscription} message ChannelEventSubscription
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelEventSubscription.toObject = function () {
            return {};
        };

        /**
         * Converts this ChannelEventSubscription to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelEventSubscription
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelEventSubscription.prototype.toJSON = function() {
            return ChannelEventSubscription.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelEventSubscription
         * @function getTypeUrl
         * @memberof lnrpc.ChannelEventSubscription
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelEventSubscription.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelEventSubscription";
        };

        return ChannelEventSubscription;
    })();

    lnrpc.ChannelEventUpdate = (function() {

        /**
         * Properties of a ChannelEventUpdate.
         * @typedef {Object} lnrpc.ChannelEventUpdate.$Properties
         * @property {lnrpc.Channel.$Properties|null} [open_channel] ChannelEventUpdate open_channel
         * @property {lnrpc.ChannelCloseSummary.$Properties|null} [closed_channel] ChannelEventUpdate closed_channel
         * @property {lnrpc.ChannelPoint.$Properties|null} [active_channel] ChannelEventUpdate active_channel
         * @property {lnrpc.ChannelPoint.$Properties|null} [inactive_channel] ChannelEventUpdate inactive_channel
         * @property {lnrpc.PendingUpdate.$Properties|null} [pending_open_channel] ChannelEventUpdate pending_open_channel
         * @property {lnrpc.ChannelPoint.$Properties|null} [fully_resolved_channel] ChannelEventUpdate fully_resolved_channel
         * @property {lnrpc.ChannelPoint.$Properties|null} [channel_funding_timeout] ChannelEventUpdate channel_funding_timeout
         * @property {lnrpc.ChannelEventUpdate.UpdateType|null} [type] ChannelEventUpdate type
         * @property {"open_channel"|"closed_channel"|"active_channel"|"inactive_channel"|"pending_open_channel"|"fully_resolved_channel"|"channel_funding_timeout"} [channel] ChannelEventUpdate channel
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelEventUpdate.
         * @memberof lnrpc
         * @interface IChannelEventUpdate
         * @augments lnrpc.ChannelEventUpdate.$Properties
         * @deprecated Use lnrpc.ChannelEventUpdate.$Properties instead.
         */

        /**
         * Narrowed shape of a ChannelEventUpdate.
         * @typedef {{
         *   open_channel?: lnrpc.Channel.$Shape|null;
         *   closed_channel?: lnrpc.ChannelCloseSummary.$Shape|null;
         *   active_channel?: lnrpc.ChannelPoint.$Shape|null;
         *   inactive_channel?: lnrpc.ChannelPoint.$Shape|null;
         *   pending_open_channel?: lnrpc.PendingUpdate.$Shape|null;
         *   fully_resolved_channel?: lnrpc.ChannelPoint.$Shape|null;
         *   channel_funding_timeout?: lnrpc.ChannelPoint.$Shape|null;
         *   type?: lnrpc.ChannelEventUpdate.UpdateType|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ channel?: undefined; open_channel?: null; closed_channel?: null; active_channel?: null; inactive_channel?: null; pending_open_channel?: null; fully_resolved_channel?: null; channel_funding_timeout?: null }|{ channel?: "open_channel"; open_channel: lnrpc.Channel.$Shape; closed_channel?: null; active_channel?: null; inactive_channel?: null; pending_open_channel?: null; fully_resolved_channel?: null; channel_funding_timeout?: null }|{ channel?: "closed_channel"; open_channel?: null; closed_channel: lnrpc.ChannelCloseSummary.$Shape; active_channel?: null; inactive_channel?: null; pending_open_channel?: null; fully_resolved_channel?: null; channel_funding_timeout?: null }|{ channel?: "active_channel"; open_channel?: null; closed_channel?: null; active_channel: lnrpc.ChannelPoint.$Shape; inactive_channel?: null; pending_open_channel?: null; fully_resolved_channel?: null; channel_funding_timeout?: null }|{ channel?: "inactive_channel"; open_channel?: null; closed_channel?: null; active_channel?: null; inactive_channel: lnrpc.ChannelPoint.$Shape; pending_open_channel?: null; fully_resolved_channel?: null; channel_funding_timeout?: null }|{ channel?: "pending_open_channel"; open_channel?: null; closed_channel?: null; active_channel?: null; inactive_channel?: null; pending_open_channel: lnrpc.PendingUpdate.$Shape; fully_resolved_channel?: null; channel_funding_timeout?: null }|{ channel?: "fully_resolved_channel"; open_channel?: null; closed_channel?: null; active_channel?: null; inactive_channel?: null; pending_open_channel?: null; fully_resolved_channel: lnrpc.ChannelPoint.$Shape; channel_funding_timeout?: null }|{ channel?: "channel_funding_timeout"; open_channel?: null; closed_channel?: null; active_channel?: null; inactive_channel?: null; pending_open_channel?: null; fully_resolved_channel?: null; channel_funding_timeout: lnrpc.ChannelPoint.$Shape })
         * )} lnrpc.ChannelEventUpdate.$Shape
         */

        /**
         * Constructs a new ChannelEventUpdate.
         * @memberof lnrpc
         * @classdesc Represents a ChannelEventUpdate.
         * @constructor
         * @param {lnrpc.ChannelEventUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelEventUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelEventUpdate open_channel.
         * @member {lnrpc.Channel.$Properties|null|undefined} open_channel
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         */
        ChannelEventUpdate.prototype.open_channel = null;

        /**
         * ChannelEventUpdate closed_channel.
         * @member {lnrpc.ChannelCloseSummary.$Properties|null|undefined} closed_channel
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         */
        ChannelEventUpdate.prototype.closed_channel = null;

        /**
         * ChannelEventUpdate active_channel.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} active_channel
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         */
        ChannelEventUpdate.prototype.active_channel = null;

        /**
         * ChannelEventUpdate inactive_channel.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} inactive_channel
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         */
        ChannelEventUpdate.prototype.inactive_channel = null;

        /**
         * ChannelEventUpdate pending_open_channel.
         * @member {lnrpc.PendingUpdate.$Properties|null|undefined} pending_open_channel
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         */
        ChannelEventUpdate.prototype.pending_open_channel = null;

        /**
         * ChannelEventUpdate fully_resolved_channel.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} fully_resolved_channel
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         */
        ChannelEventUpdate.prototype.fully_resolved_channel = null;

        /**
         * ChannelEventUpdate channel_funding_timeout.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} channel_funding_timeout
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         */
        ChannelEventUpdate.prototype.channel_funding_timeout = null;

        /**
         * ChannelEventUpdate type.
         * @member {lnrpc.ChannelEventUpdate.UpdateType} type
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         */
        ChannelEventUpdate.prototype.type = 0;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ChannelEventUpdate channel.
         * @member {"open_channel"|"closed_channel"|"active_channel"|"inactive_channel"|"pending_open_channel"|"fully_resolved_channel"|"channel_funding_timeout"|undefined} channel
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         */
        $Object.defineProperty(ChannelEventUpdate.prototype, "channel", {
            get: $util.oneOfGetter($oneOfFields = ["open_channel", "closed_channel", "active_channel", "inactive_channel", "pending_open_channel", "fully_resolved_channel", "channel_funding_timeout"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ChannelEventUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelEventUpdate
         * @static
         * @param {lnrpc.ChannelEventUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelEventUpdate} ChannelEventUpdate instance
         * @type {{
         *   (properties: lnrpc.ChannelEventUpdate.$Shape): lnrpc.ChannelEventUpdate & lnrpc.ChannelEventUpdate.$Shape;
         *   (properties?: lnrpc.ChannelEventUpdate.$Properties): lnrpc.ChannelEventUpdate;
         * }}
         */
        ChannelEventUpdate.create = function(properties) {
            return new ChannelEventUpdate(properties);
        };

        /**
         * Encodes the specified ChannelEventUpdate message. Does not implicitly {@link lnrpc.ChannelEventUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelEventUpdate
         * @static
         * @param {lnrpc.ChannelEventUpdate.$Properties} message ChannelEventUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelEventUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.open_channel != null && $Object.hasOwnProperty.call(message, "open_channel"))
                $root.lnrpc.Channel.encode(message.open_channel, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.closed_channel != null && $Object.hasOwnProperty.call(message, "closed_channel"))
                $root.lnrpc.ChannelCloseSummary.encode(message.closed_channel, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.active_channel != null && $Object.hasOwnProperty.call(message, "active_channel"))
                $root.lnrpc.ChannelPoint.encode(message.active_channel, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.inactive_channel != null && $Object.hasOwnProperty.call(message, "inactive_channel"))
                $root.lnrpc.ChannelPoint.encode(message.inactive_channel, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.type);
            if (message.pending_open_channel != null && $Object.hasOwnProperty.call(message, "pending_open_channel"))
                $root.lnrpc.PendingUpdate.encode(message.pending_open_channel, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.fully_resolved_channel != null && $Object.hasOwnProperty.call(message, "fully_resolved_channel"))
                $root.lnrpc.ChannelPoint.encode(message.fully_resolved_channel, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.channel_funding_timeout != null && $Object.hasOwnProperty.call(message, "channel_funding_timeout"))
                $root.lnrpc.ChannelPoint.encode(message.channel_funding_timeout, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelEventUpdate message, length delimited. Does not implicitly {@link lnrpc.ChannelEventUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelEventUpdate
         * @static
         * @param {lnrpc.ChannelEventUpdate.$Properties} message ChannelEventUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelEventUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelEventUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelEventUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelEventUpdate & lnrpc.ChannelEventUpdate.$Shape} ChannelEventUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelEventUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelEventUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.open_channel = $root.lnrpc.Channel.decode(reader, reader.uint32(), $undefined, _depth + 1, message.open_channel);
                        message.channel = "open_channel";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.closed_channel = $root.lnrpc.ChannelCloseSummary.decode(reader, reader.uint32(), $undefined, _depth + 1, message.closed_channel);
                        message.channel = "closed_channel";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.active_channel = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.active_channel);
                        message.channel = "active_channel";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.inactive_channel = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.inactive_channel);
                        message.channel = "inactive_channel";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.pending_open_channel = $root.lnrpc.PendingUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pending_open_channel);
                        message.channel = "pending_open_channel";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.fully_resolved_channel = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fully_resolved_channel);
                        message.channel = "fully_resolved_channel";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.channel_funding_timeout = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel_funding_timeout);
                        message.channel = "channel_funding_timeout";
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelEventUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelEventUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelEventUpdate & lnrpc.ChannelEventUpdate.$Shape} ChannelEventUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelEventUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelEventUpdate message.
         * @function verify
         * @memberof lnrpc.ChannelEventUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelEventUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.open_channel != null && $Object.hasOwnProperty.call(message, "open_channel")) {
                properties.channel = 1;
                {
                    let error = $root.lnrpc.Channel.verify(message.open_channel, _depth + 1);
                    if (error)
                        return "open_channel." + error;
                }
            }
            if (message.closed_channel != null && $Object.hasOwnProperty.call(message, "closed_channel")) {
                if (properties.channel === 1)
                    return "channel: multiple values";
                properties.channel = 1;
                {
                    let error = $root.lnrpc.ChannelCloseSummary.verify(message.closed_channel, _depth + 1);
                    if (error)
                        return "closed_channel." + error;
                }
            }
            if (message.active_channel != null && $Object.hasOwnProperty.call(message, "active_channel")) {
                if (properties.channel === 1)
                    return "channel: multiple values";
                properties.channel = 1;
                {
                    let error = $root.lnrpc.ChannelPoint.verify(message.active_channel, _depth + 1);
                    if (error)
                        return "active_channel." + error;
                }
            }
            if (message.inactive_channel != null && $Object.hasOwnProperty.call(message, "inactive_channel")) {
                if (properties.channel === 1)
                    return "channel: multiple values";
                properties.channel = 1;
                {
                    let error = $root.lnrpc.ChannelPoint.verify(message.inactive_channel, _depth + 1);
                    if (error)
                        return "inactive_channel." + error;
                }
            }
            if (message.pending_open_channel != null && $Object.hasOwnProperty.call(message, "pending_open_channel")) {
                if (properties.channel === 1)
                    return "channel: multiple values";
                properties.channel = 1;
                {
                    let error = $root.lnrpc.PendingUpdate.verify(message.pending_open_channel, _depth + 1);
                    if (error)
                        return "pending_open_channel." + error;
                }
            }
            if (message.fully_resolved_channel != null && $Object.hasOwnProperty.call(message, "fully_resolved_channel")) {
                if (properties.channel === 1)
                    return "channel: multiple values";
                properties.channel = 1;
                {
                    let error = $root.lnrpc.ChannelPoint.verify(message.fully_resolved_channel, _depth + 1);
                    if (error)
                        return "fully_resolved_channel." + error;
                }
            }
            if (message.channel_funding_timeout != null && $Object.hasOwnProperty.call(message, "channel_funding_timeout")) {
                if (properties.channel === 1)
                    return "channel: multiple values";
                properties.channel = 1;
                {
                    let error = $root.lnrpc.ChannelPoint.verify(message.channel_funding_timeout, _depth + 1);
                    if (error)
                        return "channel_funding_timeout." + error;
                }
            }
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            return null;
        };

        /**
         * Creates a ChannelEventUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelEventUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelEventUpdate} ChannelEventUpdate
         */
        ChannelEventUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelEventUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelEventUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelEventUpdate();
            if (object.open_channel != null) {
                if (!$util.isObject(object.open_channel))
                    throw $TypeError(".lnrpc.ChannelEventUpdate.open_channel: object expected");
                message.open_channel = $root.lnrpc.Channel.fromObject(object.open_channel, _depth + 1);
            }
            if (object.closed_channel != null) {
                if (!$util.isObject(object.closed_channel))
                    throw $TypeError(".lnrpc.ChannelEventUpdate.closed_channel: object expected");
                message.closed_channel = $root.lnrpc.ChannelCloseSummary.fromObject(object.closed_channel, _depth + 1);
            }
            if (object.active_channel != null) {
                if (!$util.isObject(object.active_channel))
                    throw $TypeError(".lnrpc.ChannelEventUpdate.active_channel: object expected");
                message.active_channel = $root.lnrpc.ChannelPoint.fromObject(object.active_channel, _depth + 1);
            }
            if (object.inactive_channel != null) {
                if (!$util.isObject(object.inactive_channel))
                    throw $TypeError(".lnrpc.ChannelEventUpdate.inactive_channel: object expected");
                message.inactive_channel = $root.lnrpc.ChannelPoint.fromObject(object.inactive_channel, _depth + 1);
            }
            if (object.pending_open_channel != null) {
                if (!$util.isObject(object.pending_open_channel))
                    throw $TypeError(".lnrpc.ChannelEventUpdate.pending_open_channel: object expected");
                message.pending_open_channel = $root.lnrpc.PendingUpdate.fromObject(object.pending_open_channel, _depth + 1);
            }
            if (object.fully_resolved_channel != null) {
                if (!$util.isObject(object.fully_resolved_channel))
                    throw $TypeError(".lnrpc.ChannelEventUpdate.fully_resolved_channel: object expected");
                message.fully_resolved_channel = $root.lnrpc.ChannelPoint.fromObject(object.fully_resolved_channel, _depth + 1);
            }
            if (object.channel_funding_timeout != null) {
                if (!$util.isObject(object.channel_funding_timeout))
                    throw $TypeError(".lnrpc.ChannelEventUpdate.channel_funding_timeout: object expected");
                message.channel_funding_timeout = $root.lnrpc.ChannelPoint.fromObject(object.channel_funding_timeout, _depth + 1);
            }
            if (object.type !== 0 && (typeof object.type !== "string" || $root.lnrpc.ChannelEventUpdate.UpdateType[object.type] !== 0))
                switch (object.type) {
                case "OPEN_CHANNEL":
                case 0:
                    message.type = 0;
                    break;
                case "CLOSED_CHANNEL":
                case 1:
                    message.type = 1;
                    break;
                case "ACTIVE_CHANNEL":
                case 2:
                    message.type = 2;
                    break;
                case "INACTIVE_CHANNEL":
                case 3:
                    message.type = 3;
                    break;
                case "PENDING_OPEN_CHANNEL":
                case 4:
                    message.type = 4;
                    break;
                case "FULLY_RESOLVED_CHANNEL":
                case 5:
                    message.type = 5;
                    break;
                case "CHANNEL_FUNDING_TIMEOUT":
                case 6:
                    message.type = 6;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
            return message;
        };

        /**
         * Creates a plain object from a ChannelEventUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelEventUpdate
         * @static
         * @param {lnrpc.ChannelEventUpdate} message ChannelEventUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelEventUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.type = options.enums === $String ? "OPEN_CHANNEL" : 0;
            if (message.open_channel != null && $Object.hasOwnProperty.call(message, "open_channel")) {
                object.open_channel = $root.lnrpc.Channel.toObject(message.open_channel, options, _depth + 1);
                if (options.oneofs)
                    object.channel = "open_channel";
            }
            if (message.closed_channel != null && $Object.hasOwnProperty.call(message, "closed_channel")) {
                object.closed_channel = $root.lnrpc.ChannelCloseSummary.toObject(message.closed_channel, options, _depth + 1);
                if (options.oneofs)
                    object.channel = "closed_channel";
            }
            if (message.active_channel != null && $Object.hasOwnProperty.call(message, "active_channel")) {
                object.active_channel = $root.lnrpc.ChannelPoint.toObject(message.active_channel, options, _depth + 1);
                if (options.oneofs)
                    object.channel = "active_channel";
            }
            if (message.inactive_channel != null && $Object.hasOwnProperty.call(message, "inactive_channel")) {
                object.inactive_channel = $root.lnrpc.ChannelPoint.toObject(message.inactive_channel, options, _depth + 1);
                if (options.oneofs)
                    object.channel = "inactive_channel";
            }
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === $String ? $root.lnrpc.ChannelEventUpdate.UpdateType[message.type] === $undefined ? message.type : $root.lnrpc.ChannelEventUpdate.UpdateType[message.type] : message.type;
            if (message.pending_open_channel != null && $Object.hasOwnProperty.call(message, "pending_open_channel")) {
                object.pending_open_channel = $root.lnrpc.PendingUpdate.toObject(message.pending_open_channel, options, _depth + 1);
                if (options.oneofs)
                    object.channel = "pending_open_channel";
            }
            if (message.fully_resolved_channel != null && $Object.hasOwnProperty.call(message, "fully_resolved_channel")) {
                object.fully_resolved_channel = $root.lnrpc.ChannelPoint.toObject(message.fully_resolved_channel, options, _depth + 1);
                if (options.oneofs)
                    object.channel = "fully_resolved_channel";
            }
            if (message.channel_funding_timeout != null && $Object.hasOwnProperty.call(message, "channel_funding_timeout")) {
                object.channel_funding_timeout = $root.lnrpc.ChannelPoint.toObject(message.channel_funding_timeout, options, _depth + 1);
                if (options.oneofs)
                    object.channel = "channel_funding_timeout";
            }
            return object;
        };

        /**
         * Converts this ChannelEventUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelEventUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelEventUpdate.prototype.toJSON = function() {
            return ChannelEventUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelEventUpdate
         * @function getTypeUrl
         * @memberof lnrpc.ChannelEventUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelEventUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelEventUpdate";
        };

        /**
         * UpdateType enum.
         * @name lnrpc.ChannelEventUpdate.UpdateType
         * @enum {number}
         * @property {number} OPEN_CHANNEL=0 OPEN_CHANNEL value
         * @property {number} CLOSED_CHANNEL=1 CLOSED_CHANNEL value
         * @property {number} ACTIVE_CHANNEL=2 ACTIVE_CHANNEL value
         * @property {number} INACTIVE_CHANNEL=3 INACTIVE_CHANNEL value
         * @property {number} PENDING_OPEN_CHANNEL=4 PENDING_OPEN_CHANNEL value
         * @property {number} FULLY_RESOLVED_CHANNEL=5 FULLY_RESOLVED_CHANNEL value
         * @property {number} CHANNEL_FUNDING_TIMEOUT=6 CHANNEL_FUNDING_TIMEOUT value
         */
        ChannelEventUpdate.UpdateType = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "OPEN_CHANNEL"] = 0;
            values[valuesById[1] = "CLOSED_CHANNEL"] = 1;
            values[valuesById[2] = "ACTIVE_CHANNEL"] = 2;
            values[valuesById[3] = "INACTIVE_CHANNEL"] = 3;
            values[valuesById[4] = "PENDING_OPEN_CHANNEL"] = 4;
            values[valuesById[5] = "FULLY_RESOLVED_CHANNEL"] = 5;
            values[valuesById[6] = "CHANNEL_FUNDING_TIMEOUT"] = 6;
            return values;
        })();

        return ChannelEventUpdate;
    })();

    lnrpc.WalletAccountBalance = (function() {

        /**
         * Properties of a WalletAccountBalance.
         * @typedef {Object} lnrpc.WalletAccountBalance.$Properties
         * @property {Long|null} [confirmed_balance] WalletAccountBalance confirmed_balance
         * @property {Long|null} [unconfirmed_balance] WalletAccountBalance unconfirmed_balance
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a WalletAccountBalance.
         * @memberof lnrpc
         * @interface IWalletAccountBalance
         * @augments lnrpc.WalletAccountBalance.$Properties
         * @deprecated Use lnrpc.WalletAccountBalance.$Properties instead.
         */

        /**
         * Shape of a WalletAccountBalance.
         * @typedef {lnrpc.WalletAccountBalance.$Properties} lnrpc.WalletAccountBalance.$Shape
         */

        /**
         * Constructs a new WalletAccountBalance.
         * @memberof lnrpc
         * @classdesc Represents a WalletAccountBalance.
         * @constructor
         * @param {lnrpc.WalletAccountBalance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const WalletAccountBalance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * WalletAccountBalance confirmed_balance.
         * @member {Long} confirmed_balance
         * @memberof lnrpc.WalletAccountBalance
         * @instance
         */
        WalletAccountBalance.prototype.confirmed_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * WalletAccountBalance unconfirmed_balance.
         * @member {Long} unconfirmed_balance
         * @memberof lnrpc.WalletAccountBalance
         * @instance
         */
        WalletAccountBalance.prototype.unconfirmed_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new WalletAccountBalance instance using the specified properties.
         * @function create
         * @memberof lnrpc.WalletAccountBalance
         * @static
         * @param {lnrpc.WalletAccountBalance.$Properties=} [properties] Properties to set
         * @returns {lnrpc.WalletAccountBalance} WalletAccountBalance instance
         * @type {{
         *   (properties: lnrpc.WalletAccountBalance.$Shape): lnrpc.WalletAccountBalance & lnrpc.WalletAccountBalance.$Shape;
         *   (properties?: lnrpc.WalletAccountBalance.$Properties): lnrpc.WalletAccountBalance;
         * }}
         */
        WalletAccountBalance.create = function(properties) {
            return new WalletAccountBalance(properties);
        };

        /**
         * Encodes the specified WalletAccountBalance message. Does not implicitly {@link lnrpc.WalletAccountBalance.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.WalletAccountBalance
         * @static
         * @param {lnrpc.WalletAccountBalance.$Properties} message WalletAccountBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletAccountBalance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.confirmed_balance != null && $Object.hasOwnProperty.call(message, "confirmed_balance") && (typeof message.confirmed_balance === "object" ? message.confirmed_balance.low || message.confirmed_balance.high : message.confirmed_balance !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.confirmed_balance);
            if (message.unconfirmed_balance != null && $Object.hasOwnProperty.call(message, "unconfirmed_balance") && (typeof message.unconfirmed_balance === "object" ? message.unconfirmed_balance.low || message.unconfirmed_balance.high : message.unconfirmed_balance !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.unconfirmed_balance);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified WalletAccountBalance message, length delimited. Does not implicitly {@link lnrpc.WalletAccountBalance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.WalletAccountBalance
         * @static
         * @param {lnrpc.WalletAccountBalance.$Properties} message WalletAccountBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletAccountBalance.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WalletAccountBalance message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.WalletAccountBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.WalletAccountBalance & lnrpc.WalletAccountBalance.$Shape} WalletAccountBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletAccountBalance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.WalletAccountBalance(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.confirmed_balance = value;
                        else
                            delete message.confirmed_balance;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.unconfirmed_balance = value;
                        else
                            delete message.unconfirmed_balance;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a WalletAccountBalance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.WalletAccountBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.WalletAccountBalance & lnrpc.WalletAccountBalance.$Shape} WalletAccountBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletAccountBalance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WalletAccountBalance message.
         * @function verify
         * @memberof lnrpc.WalletAccountBalance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WalletAccountBalance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.confirmed_balance != null && $Object.hasOwnProperty.call(message, "confirmed_balance"))
                if (!$util.isInteger(message.confirmed_balance) && !(message.confirmed_balance && $util.isInteger(message.confirmed_balance.low) && $util.isInteger(message.confirmed_balance.high)))
                    return "confirmed_balance: integer|Long expected";
            if (message.unconfirmed_balance != null && $Object.hasOwnProperty.call(message, "unconfirmed_balance"))
                if (!$util.isInteger(message.unconfirmed_balance) && !(message.unconfirmed_balance && $util.isInteger(message.unconfirmed_balance.low) && $util.isInteger(message.unconfirmed_balance.high)))
                    return "unconfirmed_balance: integer|Long expected";
            return null;
        };

        /**
         * Creates a WalletAccountBalance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.WalletAccountBalance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.WalletAccountBalance} WalletAccountBalance
         */
        WalletAccountBalance.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.WalletAccountBalance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.WalletAccountBalance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.WalletAccountBalance();
            if (object.confirmed_balance != null)
                if (typeof object.confirmed_balance === "object" ? object.confirmed_balance.low || object.confirmed_balance.high : $Number(object.confirmed_balance) !== 0)
                    if ($util.Long)
                        message.confirmed_balance = $util.Long.fromValue(object.confirmed_balance, false);
                    else if (typeof object.confirmed_balance === "string")
                        message.confirmed_balance = $parseInt(object.confirmed_balance, 10);
                    else if (typeof object.confirmed_balance === "number")
                        message.confirmed_balance = object.confirmed_balance;
                    else if (typeof object.confirmed_balance === "object")
                        message.confirmed_balance = new $util.LongBits(object.confirmed_balance.low >>> 0, object.confirmed_balance.high >>> 0).toNumber();
            if (object.unconfirmed_balance != null)
                if (typeof object.unconfirmed_balance === "object" ? object.unconfirmed_balance.low || object.unconfirmed_balance.high : $Number(object.unconfirmed_balance) !== 0)
                    if ($util.Long)
                        message.unconfirmed_balance = $util.Long.fromValue(object.unconfirmed_balance, false);
                    else if (typeof object.unconfirmed_balance === "string")
                        message.unconfirmed_balance = $parseInt(object.unconfirmed_balance, 10);
                    else if (typeof object.unconfirmed_balance === "number")
                        message.unconfirmed_balance = object.unconfirmed_balance;
                    else if (typeof object.unconfirmed_balance === "object")
                        message.unconfirmed_balance = new $util.LongBits(object.unconfirmed_balance.low >>> 0, object.unconfirmed_balance.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a WalletAccountBalance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.WalletAccountBalance
         * @static
         * @param {lnrpc.WalletAccountBalance} message WalletAccountBalance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WalletAccountBalance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.confirmed_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.confirmed_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.unconfirmed_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.unconfirmed_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.confirmed_balance != null && $Object.hasOwnProperty.call(message, "confirmed_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.confirmed_balance = typeof message.confirmed_balance === "number" ? $BigInt(message.confirmed_balance) : $util.Long.fromBits(message.confirmed_balance.low >>> 0, message.confirmed_balance.high >>> 0, false).toBigInt();
                else if (typeof message.confirmed_balance === "number")
                    object.confirmed_balance = options.longs === $String ? $String(message.confirmed_balance) : message.confirmed_balance;
                else
                    object.confirmed_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.confirmed_balance) : options.longs === $Number ? new $util.LongBits(message.confirmed_balance.low >>> 0, message.confirmed_balance.high >>> 0).toNumber() : message.confirmed_balance;
            if (message.unconfirmed_balance != null && $Object.hasOwnProperty.call(message, "unconfirmed_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.unconfirmed_balance = typeof message.unconfirmed_balance === "number" ? $BigInt(message.unconfirmed_balance) : $util.Long.fromBits(message.unconfirmed_balance.low >>> 0, message.unconfirmed_balance.high >>> 0, false).toBigInt();
                else if (typeof message.unconfirmed_balance === "number")
                    object.unconfirmed_balance = options.longs === $String ? $String(message.unconfirmed_balance) : message.unconfirmed_balance;
                else
                    object.unconfirmed_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.unconfirmed_balance) : options.longs === $Number ? new $util.LongBits(message.unconfirmed_balance.low >>> 0, message.unconfirmed_balance.high >>> 0).toNumber() : message.unconfirmed_balance;
            return object;
        };

        /**
         * Converts this WalletAccountBalance to JSON.
         * @function toJSON
         * @memberof lnrpc.WalletAccountBalance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WalletAccountBalance.prototype.toJSON = function() {
            return WalletAccountBalance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for WalletAccountBalance
         * @function getTypeUrl
         * @memberof lnrpc.WalletAccountBalance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        WalletAccountBalance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.WalletAccountBalance";
        };

        return WalletAccountBalance;
    })();

    lnrpc.WalletBalanceRequest = (function() {

        /**
         * Properties of a WalletBalanceRequest.
         * @typedef {Object} lnrpc.WalletBalanceRequest.$Properties
         * @property {string|null} [account] WalletBalanceRequest account
         * @property {number|null} [min_confs] WalletBalanceRequest min_confs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a WalletBalanceRequest.
         * @memberof lnrpc
         * @interface IWalletBalanceRequest
         * @augments lnrpc.WalletBalanceRequest.$Properties
         * @deprecated Use lnrpc.WalletBalanceRequest.$Properties instead.
         */

        /**
         * Shape of a WalletBalanceRequest.
         * @typedef {lnrpc.WalletBalanceRequest.$Properties} lnrpc.WalletBalanceRequest.$Shape
         */

        /**
         * Constructs a new WalletBalanceRequest.
         * @memberof lnrpc
         * @classdesc Represents a WalletBalanceRequest.
         * @constructor
         * @param {lnrpc.WalletBalanceRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const WalletBalanceRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * WalletBalanceRequest account.
         * @member {string} account
         * @memberof lnrpc.WalletBalanceRequest
         * @instance
         */
        WalletBalanceRequest.prototype.account = "";

        /**
         * WalletBalanceRequest min_confs.
         * @member {number} min_confs
         * @memberof lnrpc.WalletBalanceRequest
         * @instance
         */
        WalletBalanceRequest.prototype.min_confs = 0;

        /**
         * Creates a new WalletBalanceRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.WalletBalanceRequest
         * @static
         * @param {lnrpc.WalletBalanceRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.WalletBalanceRequest} WalletBalanceRequest instance
         * @type {{
         *   (properties: lnrpc.WalletBalanceRequest.$Shape): lnrpc.WalletBalanceRequest & lnrpc.WalletBalanceRequest.$Shape;
         *   (properties?: lnrpc.WalletBalanceRequest.$Properties): lnrpc.WalletBalanceRequest;
         * }}
         */
        WalletBalanceRequest.create = function(properties) {
            return new WalletBalanceRequest(properties);
        };

        /**
         * Encodes the specified WalletBalanceRequest message. Does not implicitly {@link lnrpc.WalletBalanceRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.WalletBalanceRequest
         * @static
         * @param {lnrpc.WalletBalanceRequest.$Properties} message WalletBalanceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletBalanceRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.account != null && $Object.hasOwnProperty.call(message, "account") && message.account !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.account);
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.min_confs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified WalletBalanceRequest message, length delimited. Does not implicitly {@link lnrpc.WalletBalanceRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.WalletBalanceRequest
         * @static
         * @param {lnrpc.WalletBalanceRequest.$Properties} message WalletBalanceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletBalanceRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WalletBalanceRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.WalletBalanceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.WalletBalanceRequest & lnrpc.WalletBalanceRequest.$Shape} WalletBalanceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletBalanceRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.WalletBalanceRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a WalletBalanceRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.WalletBalanceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.WalletBalanceRequest & lnrpc.WalletBalanceRequest.$Shape} WalletBalanceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletBalanceRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WalletBalanceRequest message.
         * @function verify
         * @memberof lnrpc.WalletBalanceRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WalletBalanceRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            return null;
        };

        /**
         * Creates a WalletBalanceRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.WalletBalanceRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.WalletBalanceRequest} WalletBalanceRequest
         */
        WalletBalanceRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.WalletBalanceRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.WalletBalanceRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.WalletBalanceRequest();
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = $String(object.account);
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            return message;
        };

        /**
         * Creates a plain object from a WalletBalanceRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.WalletBalanceRequest
         * @static
         * @param {lnrpc.WalletBalanceRequest} message WalletBalanceRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WalletBalanceRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.account = "";
                object.min_confs = 0;
            }
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = message.account;
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            return object;
        };

        /**
         * Converts this WalletBalanceRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.WalletBalanceRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WalletBalanceRequest.prototype.toJSON = function() {
            return WalletBalanceRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for WalletBalanceRequest
         * @function getTypeUrl
         * @memberof lnrpc.WalletBalanceRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        WalletBalanceRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.WalletBalanceRequest";
        };

        return WalletBalanceRequest;
    })();

    lnrpc.WalletBalanceResponse = (function() {

        /**
         * Properties of a WalletBalanceResponse.
         * @typedef {Object} lnrpc.WalletBalanceResponse.$Properties
         * @property {Long|null} [total_balance] WalletBalanceResponse total_balance
         * @property {Long|null} [confirmed_balance] WalletBalanceResponse confirmed_balance
         * @property {Long|null} [unconfirmed_balance] WalletBalanceResponse unconfirmed_balance
         * @property {Long|null} [locked_balance] WalletBalanceResponse locked_balance
         * @property {Long|null} [reserved_balance_anchor_chan] WalletBalanceResponse reserved_balance_anchor_chan
         * @property {Object.<string,lnrpc.WalletAccountBalance.$Properties>|null} [account_balance] WalletBalanceResponse account_balance
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a WalletBalanceResponse.
         * @memberof lnrpc
         * @interface IWalletBalanceResponse
         * @augments lnrpc.WalletBalanceResponse.$Properties
         * @deprecated Use lnrpc.WalletBalanceResponse.$Properties instead.
         */

        /**
         * Shape of a WalletBalanceResponse.
         * @typedef {lnrpc.WalletBalanceResponse.$Properties} lnrpc.WalletBalanceResponse.$Shape
         */

        /**
         * Constructs a new WalletBalanceResponse.
         * @memberof lnrpc
         * @classdesc Represents a WalletBalanceResponse.
         * @constructor
         * @param {lnrpc.WalletBalanceResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const WalletBalanceResponse = function (properties) {
            this.account_balance = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * WalletBalanceResponse total_balance.
         * @member {Long} total_balance
         * @memberof lnrpc.WalletBalanceResponse
         * @instance
         */
        WalletBalanceResponse.prototype.total_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * WalletBalanceResponse confirmed_balance.
         * @member {Long} confirmed_balance
         * @memberof lnrpc.WalletBalanceResponse
         * @instance
         */
        WalletBalanceResponse.prototype.confirmed_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * WalletBalanceResponse unconfirmed_balance.
         * @member {Long} unconfirmed_balance
         * @memberof lnrpc.WalletBalanceResponse
         * @instance
         */
        WalletBalanceResponse.prototype.unconfirmed_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * WalletBalanceResponse locked_balance.
         * @member {Long} locked_balance
         * @memberof lnrpc.WalletBalanceResponse
         * @instance
         */
        WalletBalanceResponse.prototype.locked_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * WalletBalanceResponse reserved_balance_anchor_chan.
         * @member {Long} reserved_balance_anchor_chan
         * @memberof lnrpc.WalletBalanceResponse
         * @instance
         */
        WalletBalanceResponse.prototype.reserved_balance_anchor_chan = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * WalletBalanceResponse account_balance.
         * @member {Object.<string,lnrpc.WalletAccountBalance.$Properties>} account_balance
         * @memberof lnrpc.WalletBalanceResponse
         * @instance
         */
        WalletBalanceResponse.prototype.account_balance = $util.emptyObject;

        /**
         * Creates a new WalletBalanceResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.WalletBalanceResponse
         * @static
         * @param {lnrpc.WalletBalanceResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.WalletBalanceResponse} WalletBalanceResponse instance
         * @type {{
         *   (properties: lnrpc.WalletBalanceResponse.$Shape): lnrpc.WalletBalanceResponse & lnrpc.WalletBalanceResponse.$Shape;
         *   (properties?: lnrpc.WalletBalanceResponse.$Properties): lnrpc.WalletBalanceResponse;
         * }}
         */
        WalletBalanceResponse.create = function(properties) {
            return new WalletBalanceResponse(properties);
        };

        /**
         * Encodes the specified WalletBalanceResponse message. Does not implicitly {@link lnrpc.WalletBalanceResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.WalletBalanceResponse
         * @static
         * @param {lnrpc.WalletBalanceResponse.$Properties} message WalletBalanceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletBalanceResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.total_balance != null && $Object.hasOwnProperty.call(message, "total_balance") && (typeof message.total_balance === "object" ? message.total_balance.low || message.total_balance.high : message.total_balance !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.total_balance);
            if (message.confirmed_balance != null && $Object.hasOwnProperty.call(message, "confirmed_balance") && (typeof message.confirmed_balance === "object" ? message.confirmed_balance.low || message.confirmed_balance.high : message.confirmed_balance !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.confirmed_balance);
            if (message.unconfirmed_balance != null && $Object.hasOwnProperty.call(message, "unconfirmed_balance") && (typeof message.unconfirmed_balance === "object" ? message.unconfirmed_balance.low || message.unconfirmed_balance.high : message.unconfirmed_balance !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.unconfirmed_balance);
            if (message.account_balance != null && $Object.hasOwnProperty.call(message, "account_balance"))
                for (let keys = $Object.keys(message.account_balance), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 4, wireType 2 =*/34).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.lnrpc.WalletAccountBalance.encode(message.account_balance[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.locked_balance != null && $Object.hasOwnProperty.call(message, "locked_balance") && (typeof message.locked_balance === "object" ? message.locked_balance.low || message.locked_balance.high : message.locked_balance !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.locked_balance);
            if (message.reserved_balance_anchor_chan != null && $Object.hasOwnProperty.call(message, "reserved_balance_anchor_chan") && (typeof message.reserved_balance_anchor_chan === "object" ? message.reserved_balance_anchor_chan.low || message.reserved_balance_anchor_chan.high : message.reserved_balance_anchor_chan !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.reserved_balance_anchor_chan);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified WalletBalanceResponse message, length delimited. Does not implicitly {@link lnrpc.WalletBalanceResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.WalletBalanceResponse
         * @static
         * @param {lnrpc.WalletBalanceResponse.$Properties} message WalletBalanceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletBalanceResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WalletBalanceResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.WalletBalanceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.WalletBalanceResponse & lnrpc.WalletBalanceResponse.$Shape} WalletBalanceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletBalanceResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.WalletBalanceResponse(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_balance = value;
                        else
                            delete message.total_balance;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.confirmed_balance = value;
                        else
                            delete message.confirmed_balance;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.unconfirmed_balance = value;
                        else
                            delete message.unconfirmed_balance;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.locked_balance = value;
                        else
                            delete message.locked_balance;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.reserved_balance_anchor_chan = value;
                        else
                            delete message.reserved_balance_anchor_chan;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (message.account_balance === $util.emptyObject)
                            message.account_balance = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.WalletAccountBalance.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.account_balance, key);
                        message.account_balance[key] = value || new $root.lnrpc.WalletAccountBalance();
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a WalletBalanceResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.WalletBalanceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.WalletBalanceResponse & lnrpc.WalletBalanceResponse.$Shape} WalletBalanceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletBalanceResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WalletBalanceResponse message.
         * @function verify
         * @memberof lnrpc.WalletBalanceResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WalletBalanceResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.total_balance != null && $Object.hasOwnProperty.call(message, "total_balance"))
                if (!$util.isInteger(message.total_balance) && !(message.total_balance && $util.isInteger(message.total_balance.low) && $util.isInteger(message.total_balance.high)))
                    return "total_balance: integer|Long expected";
            if (message.confirmed_balance != null && $Object.hasOwnProperty.call(message, "confirmed_balance"))
                if (!$util.isInteger(message.confirmed_balance) && !(message.confirmed_balance && $util.isInteger(message.confirmed_balance.low) && $util.isInteger(message.confirmed_balance.high)))
                    return "confirmed_balance: integer|Long expected";
            if (message.unconfirmed_balance != null && $Object.hasOwnProperty.call(message, "unconfirmed_balance"))
                if (!$util.isInteger(message.unconfirmed_balance) && !(message.unconfirmed_balance && $util.isInteger(message.unconfirmed_balance.low) && $util.isInteger(message.unconfirmed_balance.high)))
                    return "unconfirmed_balance: integer|Long expected";
            if (message.locked_balance != null && $Object.hasOwnProperty.call(message, "locked_balance"))
                if (!$util.isInteger(message.locked_balance) && !(message.locked_balance && $util.isInteger(message.locked_balance.low) && $util.isInteger(message.locked_balance.high)))
                    return "locked_balance: integer|Long expected";
            if (message.reserved_balance_anchor_chan != null && $Object.hasOwnProperty.call(message, "reserved_balance_anchor_chan"))
                if (!$util.isInteger(message.reserved_balance_anchor_chan) && !(message.reserved_balance_anchor_chan && $util.isInteger(message.reserved_balance_anchor_chan.low) && $util.isInteger(message.reserved_balance_anchor_chan.high)))
                    return "reserved_balance_anchor_chan: integer|Long expected";
            if (message.account_balance != null && $Object.hasOwnProperty.call(message, "account_balance")) {
                if (!$util.isObject(message.account_balance))
                    return "account_balance: object expected";
                let key = $Object.keys(message.account_balance);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.lnrpc.WalletAccountBalance.verify(message.account_balance[key[i]], _depth + 1);
                    if (error)
                        return "account_balance." + error;
                }
            }
            return null;
        };

        /**
         * Creates a WalletBalanceResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.WalletBalanceResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.WalletBalanceResponse} WalletBalanceResponse
         */
        WalletBalanceResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.WalletBalanceResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.WalletBalanceResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.WalletBalanceResponse();
            if (object.total_balance != null)
                if (typeof object.total_balance === "object" ? object.total_balance.low || object.total_balance.high : $Number(object.total_balance) !== 0)
                    if ($util.Long)
                        message.total_balance = $util.Long.fromValue(object.total_balance, false);
                    else if (typeof object.total_balance === "string")
                        message.total_balance = $parseInt(object.total_balance, 10);
                    else if (typeof object.total_balance === "number")
                        message.total_balance = object.total_balance;
                    else if (typeof object.total_balance === "object")
                        message.total_balance = new $util.LongBits(object.total_balance.low >>> 0, object.total_balance.high >>> 0).toNumber();
            if (object.confirmed_balance != null)
                if (typeof object.confirmed_balance === "object" ? object.confirmed_balance.low || object.confirmed_balance.high : $Number(object.confirmed_balance) !== 0)
                    if ($util.Long)
                        message.confirmed_balance = $util.Long.fromValue(object.confirmed_balance, false);
                    else if (typeof object.confirmed_balance === "string")
                        message.confirmed_balance = $parseInt(object.confirmed_balance, 10);
                    else if (typeof object.confirmed_balance === "number")
                        message.confirmed_balance = object.confirmed_balance;
                    else if (typeof object.confirmed_balance === "object")
                        message.confirmed_balance = new $util.LongBits(object.confirmed_balance.low >>> 0, object.confirmed_balance.high >>> 0).toNumber();
            if (object.unconfirmed_balance != null)
                if (typeof object.unconfirmed_balance === "object" ? object.unconfirmed_balance.low || object.unconfirmed_balance.high : $Number(object.unconfirmed_balance) !== 0)
                    if ($util.Long)
                        message.unconfirmed_balance = $util.Long.fromValue(object.unconfirmed_balance, false);
                    else if (typeof object.unconfirmed_balance === "string")
                        message.unconfirmed_balance = $parseInt(object.unconfirmed_balance, 10);
                    else if (typeof object.unconfirmed_balance === "number")
                        message.unconfirmed_balance = object.unconfirmed_balance;
                    else if (typeof object.unconfirmed_balance === "object")
                        message.unconfirmed_balance = new $util.LongBits(object.unconfirmed_balance.low >>> 0, object.unconfirmed_balance.high >>> 0).toNumber();
            if (object.locked_balance != null)
                if (typeof object.locked_balance === "object" ? object.locked_balance.low || object.locked_balance.high : $Number(object.locked_balance) !== 0)
                    if ($util.Long)
                        message.locked_balance = $util.Long.fromValue(object.locked_balance, false);
                    else if (typeof object.locked_balance === "string")
                        message.locked_balance = $parseInt(object.locked_balance, 10);
                    else if (typeof object.locked_balance === "number")
                        message.locked_balance = object.locked_balance;
                    else if (typeof object.locked_balance === "object")
                        message.locked_balance = new $util.LongBits(object.locked_balance.low >>> 0, object.locked_balance.high >>> 0).toNumber();
            if (object.reserved_balance_anchor_chan != null)
                if (typeof object.reserved_balance_anchor_chan === "object" ? object.reserved_balance_anchor_chan.low || object.reserved_balance_anchor_chan.high : $Number(object.reserved_balance_anchor_chan) !== 0)
                    if ($util.Long)
                        message.reserved_balance_anchor_chan = $util.Long.fromValue(object.reserved_balance_anchor_chan, false);
                    else if (typeof object.reserved_balance_anchor_chan === "string")
                        message.reserved_balance_anchor_chan = $parseInt(object.reserved_balance_anchor_chan, 10);
                    else if (typeof object.reserved_balance_anchor_chan === "number")
                        message.reserved_balance_anchor_chan = object.reserved_balance_anchor_chan;
                    else if (typeof object.reserved_balance_anchor_chan === "object")
                        message.reserved_balance_anchor_chan = new $util.LongBits(object.reserved_balance_anchor_chan.low >>> 0, object.reserved_balance_anchor_chan.high >>> 0).toNumber();
            if (object.account_balance) {
                if (!$util.isObject(object.account_balance))
                    throw $TypeError(".lnrpc.WalletBalanceResponse.account_balance: object expected");
                message.account_balance = {};
                for (let keys = $Object.keys(object.account_balance), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.account_balance, keys[i]);
                    if (!$util.isObject(object.account_balance[keys[i]]))
                        throw $TypeError(".lnrpc.WalletBalanceResponse.account_balance: object expected");
                    message.account_balance[keys[i]] = $root.lnrpc.WalletAccountBalance.fromObject(object.account_balance[keys[i]], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a WalletBalanceResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.WalletBalanceResponse
         * @static
         * @param {lnrpc.WalletBalanceResponse} message WalletBalanceResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WalletBalanceResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.account_balance = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.confirmed_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.confirmed_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.unconfirmed_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.unconfirmed_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.locked_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.locked_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.reserved_balance_anchor_chan = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.reserved_balance_anchor_chan = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.total_balance != null && $Object.hasOwnProperty.call(message, "total_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_balance = typeof message.total_balance === "number" ? $BigInt(message.total_balance) : $util.Long.fromBits(message.total_balance.low >>> 0, message.total_balance.high >>> 0, false).toBigInt();
                else if (typeof message.total_balance === "number")
                    object.total_balance = options.longs === $String ? $String(message.total_balance) : message.total_balance;
                else
                    object.total_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.total_balance) : options.longs === $Number ? new $util.LongBits(message.total_balance.low >>> 0, message.total_balance.high >>> 0).toNumber() : message.total_balance;
            if (message.confirmed_balance != null && $Object.hasOwnProperty.call(message, "confirmed_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.confirmed_balance = typeof message.confirmed_balance === "number" ? $BigInt(message.confirmed_balance) : $util.Long.fromBits(message.confirmed_balance.low >>> 0, message.confirmed_balance.high >>> 0, false).toBigInt();
                else if (typeof message.confirmed_balance === "number")
                    object.confirmed_balance = options.longs === $String ? $String(message.confirmed_balance) : message.confirmed_balance;
                else
                    object.confirmed_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.confirmed_balance) : options.longs === $Number ? new $util.LongBits(message.confirmed_balance.low >>> 0, message.confirmed_balance.high >>> 0).toNumber() : message.confirmed_balance;
            if (message.unconfirmed_balance != null && $Object.hasOwnProperty.call(message, "unconfirmed_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.unconfirmed_balance = typeof message.unconfirmed_balance === "number" ? $BigInt(message.unconfirmed_balance) : $util.Long.fromBits(message.unconfirmed_balance.low >>> 0, message.unconfirmed_balance.high >>> 0, false).toBigInt();
                else if (typeof message.unconfirmed_balance === "number")
                    object.unconfirmed_balance = options.longs === $String ? $String(message.unconfirmed_balance) : message.unconfirmed_balance;
                else
                    object.unconfirmed_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.unconfirmed_balance) : options.longs === $Number ? new $util.LongBits(message.unconfirmed_balance.low >>> 0, message.unconfirmed_balance.high >>> 0).toNumber() : message.unconfirmed_balance;
            let keys2;
            if (message.account_balance && (keys2 = $Object.keys(message.account_balance)).length) {
                object.account_balance = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.account_balance, keys2[j]);
                    object.account_balance[keys2[j]] = $root.lnrpc.WalletAccountBalance.toObject(message.account_balance[keys2[j]], options, _depth + 1);
                }
            }
            if (message.locked_balance != null && $Object.hasOwnProperty.call(message, "locked_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.locked_balance = typeof message.locked_balance === "number" ? $BigInt(message.locked_balance) : $util.Long.fromBits(message.locked_balance.low >>> 0, message.locked_balance.high >>> 0, false).toBigInt();
                else if (typeof message.locked_balance === "number")
                    object.locked_balance = options.longs === $String ? $String(message.locked_balance) : message.locked_balance;
                else
                    object.locked_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.locked_balance) : options.longs === $Number ? new $util.LongBits(message.locked_balance.low >>> 0, message.locked_balance.high >>> 0).toNumber() : message.locked_balance;
            if (message.reserved_balance_anchor_chan != null && $Object.hasOwnProperty.call(message, "reserved_balance_anchor_chan"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.reserved_balance_anchor_chan = typeof message.reserved_balance_anchor_chan === "number" ? $BigInt(message.reserved_balance_anchor_chan) : $util.Long.fromBits(message.reserved_balance_anchor_chan.low >>> 0, message.reserved_balance_anchor_chan.high >>> 0, false).toBigInt();
                else if (typeof message.reserved_balance_anchor_chan === "number")
                    object.reserved_balance_anchor_chan = options.longs === $String ? $String(message.reserved_balance_anchor_chan) : message.reserved_balance_anchor_chan;
                else
                    object.reserved_balance_anchor_chan = options.longs === $String ? $util.Long.prototype.toString.call(message.reserved_balance_anchor_chan) : options.longs === $Number ? new $util.LongBits(message.reserved_balance_anchor_chan.low >>> 0, message.reserved_balance_anchor_chan.high >>> 0).toNumber() : message.reserved_balance_anchor_chan;
            return object;
        };

        /**
         * Converts this WalletBalanceResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.WalletBalanceResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WalletBalanceResponse.prototype.toJSON = function() {
            return WalletBalanceResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for WalletBalanceResponse
         * @function getTypeUrl
         * @memberof lnrpc.WalletBalanceResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        WalletBalanceResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.WalletBalanceResponse";
        };

        return WalletBalanceResponse;
    })();

    lnrpc.Amount = (function() {

        /**
         * Properties of an Amount.
         * @typedef {Object} lnrpc.Amount.$Properties
         * @property {Long|null} [sat] Amount sat
         * @property {Long|null} [msat] Amount msat
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an Amount.
         * @memberof lnrpc
         * @interface IAmount
         * @augments lnrpc.Amount.$Properties
         * @deprecated Use lnrpc.Amount.$Properties instead.
         */

        /**
         * Shape of an Amount.
         * @typedef {lnrpc.Amount.$Properties} lnrpc.Amount.$Shape
         */

        /**
         * Constructs a new Amount.
         * @memberof lnrpc
         * @classdesc Represents an Amount.
         * @constructor
         * @param {lnrpc.Amount.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Amount = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Amount sat.
         * @member {Long} sat
         * @memberof lnrpc.Amount
         * @instance
         */
        Amount.prototype.sat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Amount msat.
         * @member {Long} msat
         * @memberof lnrpc.Amount
         * @instance
         */
        Amount.prototype.msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new Amount instance using the specified properties.
         * @function create
         * @memberof lnrpc.Amount
         * @static
         * @param {lnrpc.Amount.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Amount} Amount instance
         * @type {{
         *   (properties: lnrpc.Amount.$Shape): lnrpc.Amount & lnrpc.Amount.$Shape;
         *   (properties?: lnrpc.Amount.$Properties): lnrpc.Amount;
         * }}
         */
        Amount.create = function(properties) {
            return new Amount(properties);
        };

        /**
         * Encodes the specified Amount message. Does not implicitly {@link lnrpc.Amount.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Amount
         * @static
         * @param {lnrpc.Amount.$Properties} message Amount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Amount.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sat != null && $Object.hasOwnProperty.call(message, "sat") && (typeof message.sat === "object" ? message.sat.low || message.sat.high : message.sat !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.sat);
            if (message.msat != null && $Object.hasOwnProperty.call(message, "msat") && (typeof message.msat === "object" ? message.msat.low || message.msat.high : message.msat !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.msat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Amount message, length delimited. Does not implicitly {@link lnrpc.Amount.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Amount
         * @static
         * @param {lnrpc.Amount.$Properties} message Amount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Amount.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Amount message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Amount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Amount & lnrpc.Amount.$Shape} Amount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Amount.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Amount(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat = value;
                        else
                            delete message.sat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.msat = value;
                        else
                            delete message.msat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Amount message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Amount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Amount & lnrpc.Amount.$Shape} Amount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Amount.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Amount message.
         * @function verify
         * @memberof lnrpc.Amount
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Amount.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sat != null && $Object.hasOwnProperty.call(message, "sat"))
                if (!$util.isInteger(message.sat) && !(message.sat && $util.isInteger(message.sat.low) && $util.isInteger(message.sat.high)))
                    return "sat: integer|Long expected";
            if (message.msat != null && $Object.hasOwnProperty.call(message, "msat"))
                if (!$util.isInteger(message.msat) && !(message.msat && $util.isInteger(message.msat.low) && $util.isInteger(message.msat.high)))
                    return "msat: integer|Long expected";
            return null;
        };

        /**
         * Creates an Amount message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Amount
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Amount} Amount
         */
        Amount.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Amount)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Amount: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Amount();
            if (object.sat != null)
                if (typeof object.sat === "object" ? object.sat.low || object.sat.high : $Number(object.sat) !== 0)
                    if ($util.Long)
                        message.sat = $util.Long.fromValue(object.sat, true);
                    else if (typeof object.sat === "string")
                        message.sat = $parseInt(object.sat, 10);
                    else if (typeof object.sat === "number")
                        message.sat = object.sat;
                    else if (typeof object.sat === "object")
                        message.sat = new $util.LongBits(object.sat.low >>> 0, object.sat.high >>> 0).toNumber(true);
            if (object.msat != null)
                if (typeof object.msat === "object" ? object.msat.low || object.msat.high : $Number(object.msat) !== 0)
                    if ($util.Long)
                        message.msat = $util.Long.fromValue(object.msat, true);
                    else if (typeof object.msat === "string")
                        message.msat = $parseInt(object.msat, 10);
                    else if (typeof object.msat === "number")
                        message.msat = object.msat;
                    else if (typeof object.msat === "object")
                        message.msat = new $util.LongBits(object.msat.low >>> 0, object.msat.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from an Amount message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Amount
         * @static
         * @param {lnrpc.Amount} message Amount
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Amount.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.sat != null && $Object.hasOwnProperty.call(message, "sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat = typeof message.sat === "number" ? $BigInt(message.sat) : $util.Long.fromBits(message.sat.low >>> 0, message.sat.high >>> 0, true).toBigInt();
                else if (typeof message.sat === "number")
                    object.sat = options.longs === $String ? $String(message.sat) : message.sat;
                else
                    object.sat = options.longs === $String ? $util.Long.prototype.toString.call(message.sat) : options.longs === $Number ? new $util.LongBits(message.sat.low >>> 0, message.sat.high >>> 0).toNumber(true) : message.sat;
            if (message.msat != null && $Object.hasOwnProperty.call(message, "msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.msat = typeof message.msat === "number" ? $BigInt(message.msat) : $util.Long.fromBits(message.msat.low >>> 0, message.msat.high >>> 0, true).toBigInt();
                else if (typeof message.msat === "number")
                    object.msat = options.longs === $String ? $String(message.msat) : message.msat;
                else
                    object.msat = options.longs === $String ? $util.Long.prototype.toString.call(message.msat) : options.longs === $Number ? new $util.LongBits(message.msat.low >>> 0, message.msat.high >>> 0).toNumber(true) : message.msat;
            return object;
        };

        /**
         * Converts this Amount to JSON.
         * @function toJSON
         * @memberof lnrpc.Amount
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Amount.prototype.toJSON = function() {
            return Amount.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Amount
         * @function getTypeUrl
         * @memberof lnrpc.Amount
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Amount.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Amount";
        };

        return Amount;
    })();

    lnrpc.ChannelBalanceRequest = (function() {

        /**
         * Properties of a ChannelBalanceRequest.
         * @typedef {Object} lnrpc.ChannelBalanceRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelBalanceRequest.
         * @memberof lnrpc
         * @interface IChannelBalanceRequest
         * @augments lnrpc.ChannelBalanceRequest.$Properties
         * @deprecated Use lnrpc.ChannelBalanceRequest.$Properties instead.
         */

        /**
         * Shape of a ChannelBalanceRequest.
         * @typedef {lnrpc.ChannelBalanceRequest.$Properties} lnrpc.ChannelBalanceRequest.$Shape
         */

        /**
         * Constructs a new ChannelBalanceRequest.
         * @memberof lnrpc
         * @classdesc Represents a ChannelBalanceRequest.
         * @constructor
         * @param {lnrpc.ChannelBalanceRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelBalanceRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ChannelBalanceRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelBalanceRequest
         * @static
         * @param {lnrpc.ChannelBalanceRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelBalanceRequest} ChannelBalanceRequest instance
         * @type {{
         *   (properties: lnrpc.ChannelBalanceRequest.$Shape): lnrpc.ChannelBalanceRequest & lnrpc.ChannelBalanceRequest.$Shape;
         *   (properties?: lnrpc.ChannelBalanceRequest.$Properties): lnrpc.ChannelBalanceRequest;
         * }}
         */
        ChannelBalanceRequest.create = function(properties) {
            return new ChannelBalanceRequest(properties);
        };

        /**
         * Encodes the specified ChannelBalanceRequest message. Does not implicitly {@link lnrpc.ChannelBalanceRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelBalanceRequest
         * @static
         * @param {lnrpc.ChannelBalanceRequest.$Properties} message ChannelBalanceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBalanceRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelBalanceRequest message, length delimited. Does not implicitly {@link lnrpc.ChannelBalanceRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelBalanceRequest
         * @static
         * @param {lnrpc.ChannelBalanceRequest.$Properties} message ChannelBalanceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBalanceRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelBalanceRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelBalanceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelBalanceRequest & lnrpc.ChannelBalanceRequest.$Shape} ChannelBalanceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBalanceRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelBalanceRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelBalanceRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelBalanceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelBalanceRequest & lnrpc.ChannelBalanceRequest.$Shape} ChannelBalanceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBalanceRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelBalanceRequest message.
         * @function verify
         * @memberof lnrpc.ChannelBalanceRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelBalanceRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ChannelBalanceRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelBalanceRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelBalanceRequest} ChannelBalanceRequest
         */
        ChannelBalanceRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelBalanceRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelBalanceRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.ChannelBalanceRequest();
        };

        /**
         * Creates a plain object from a ChannelBalanceRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelBalanceRequest
         * @static
         * @param {lnrpc.ChannelBalanceRequest} message ChannelBalanceRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelBalanceRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this ChannelBalanceRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelBalanceRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelBalanceRequest.prototype.toJSON = function() {
            return ChannelBalanceRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelBalanceRequest
         * @function getTypeUrl
         * @memberof lnrpc.ChannelBalanceRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelBalanceRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelBalanceRequest";
        };

        return ChannelBalanceRequest;
    })();

    lnrpc.ChannelBalanceResponse = (function() {

        /**
         * Properties of a ChannelBalanceResponse.
         * @typedef {Object} lnrpc.ChannelBalanceResponse.$Properties
         * @property {Long|null} [balance] ChannelBalanceResponse balance
         * @property {Long|null} [pending_open_balance] ChannelBalanceResponse pending_open_balance
         * @property {lnrpc.Amount.$Properties|null} [local_balance] ChannelBalanceResponse local_balance
         * @property {lnrpc.Amount.$Properties|null} [remote_balance] ChannelBalanceResponse remote_balance
         * @property {lnrpc.Amount.$Properties|null} [unsettled_local_balance] ChannelBalanceResponse unsettled_local_balance
         * @property {lnrpc.Amount.$Properties|null} [unsettled_remote_balance] ChannelBalanceResponse unsettled_remote_balance
         * @property {lnrpc.Amount.$Properties|null} [pending_open_local_balance] ChannelBalanceResponse pending_open_local_balance
         * @property {lnrpc.Amount.$Properties|null} [pending_open_remote_balance] ChannelBalanceResponse pending_open_remote_balance
         * @property {Uint8Array|null} [custom_channel_data] ChannelBalanceResponse custom_channel_data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelBalanceResponse.
         * @memberof lnrpc
         * @interface IChannelBalanceResponse
         * @augments lnrpc.ChannelBalanceResponse.$Properties
         * @deprecated Use lnrpc.ChannelBalanceResponse.$Properties instead.
         */

        /**
         * Shape of a ChannelBalanceResponse.
         * @typedef {lnrpc.ChannelBalanceResponse.$Properties} lnrpc.ChannelBalanceResponse.$Shape
         */

        /**
         * Constructs a new ChannelBalanceResponse.
         * @memberof lnrpc
         * @classdesc Represents a ChannelBalanceResponse.
         * @constructor
         * @param {lnrpc.ChannelBalanceResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelBalanceResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelBalanceResponse balance.
         * @member {Long} balance
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         */
        ChannelBalanceResponse.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChannelBalanceResponse pending_open_balance.
         * @member {Long} pending_open_balance
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         */
        ChannelBalanceResponse.prototype.pending_open_balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChannelBalanceResponse local_balance.
         * @member {lnrpc.Amount.$Properties|null|undefined} local_balance
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         */
        ChannelBalanceResponse.prototype.local_balance = null;

        /**
         * ChannelBalanceResponse remote_balance.
         * @member {lnrpc.Amount.$Properties|null|undefined} remote_balance
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         */
        ChannelBalanceResponse.prototype.remote_balance = null;

        /**
         * ChannelBalanceResponse unsettled_local_balance.
         * @member {lnrpc.Amount.$Properties|null|undefined} unsettled_local_balance
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         */
        ChannelBalanceResponse.prototype.unsettled_local_balance = null;

        /**
         * ChannelBalanceResponse unsettled_remote_balance.
         * @member {lnrpc.Amount.$Properties|null|undefined} unsettled_remote_balance
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         */
        ChannelBalanceResponse.prototype.unsettled_remote_balance = null;

        /**
         * ChannelBalanceResponse pending_open_local_balance.
         * @member {lnrpc.Amount.$Properties|null|undefined} pending_open_local_balance
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         */
        ChannelBalanceResponse.prototype.pending_open_local_balance = null;

        /**
         * ChannelBalanceResponse pending_open_remote_balance.
         * @member {lnrpc.Amount.$Properties|null|undefined} pending_open_remote_balance
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         */
        ChannelBalanceResponse.prototype.pending_open_remote_balance = null;

        /**
         * ChannelBalanceResponse custom_channel_data.
         * @member {Uint8Array} custom_channel_data
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         */
        ChannelBalanceResponse.prototype.custom_channel_data = $util.newBuffer([]);

        /**
         * Creates a new ChannelBalanceResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelBalanceResponse
         * @static
         * @param {lnrpc.ChannelBalanceResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelBalanceResponse} ChannelBalanceResponse instance
         * @type {{
         *   (properties: lnrpc.ChannelBalanceResponse.$Shape): lnrpc.ChannelBalanceResponse & lnrpc.ChannelBalanceResponse.$Shape;
         *   (properties?: lnrpc.ChannelBalanceResponse.$Properties): lnrpc.ChannelBalanceResponse;
         * }}
         */
        ChannelBalanceResponse.create = function(properties) {
            return new ChannelBalanceResponse(properties);
        };

        /**
         * Encodes the specified ChannelBalanceResponse message. Does not implicitly {@link lnrpc.ChannelBalanceResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelBalanceResponse
         * @static
         * @param {lnrpc.ChannelBalanceResponse.$Properties} message ChannelBalanceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBalanceResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.balance);
            if (message.pending_open_balance != null && $Object.hasOwnProperty.call(message, "pending_open_balance") && (typeof message.pending_open_balance === "object" ? message.pending_open_balance.low || message.pending_open_balance.high : message.pending_open_balance !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.pending_open_balance);
            if (message.local_balance != null && $Object.hasOwnProperty.call(message, "local_balance"))
                $root.lnrpc.Amount.encode(message.local_balance, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.remote_balance != null && $Object.hasOwnProperty.call(message, "remote_balance"))
                $root.lnrpc.Amount.encode(message.remote_balance, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.unsettled_local_balance != null && $Object.hasOwnProperty.call(message, "unsettled_local_balance"))
                $root.lnrpc.Amount.encode(message.unsettled_local_balance, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.unsettled_remote_balance != null && $Object.hasOwnProperty.call(message, "unsettled_remote_balance"))
                $root.lnrpc.Amount.encode(message.unsettled_remote_balance, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.pending_open_local_balance != null && $Object.hasOwnProperty.call(message, "pending_open_local_balance"))
                $root.lnrpc.Amount.encode(message.pending_open_local_balance, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.pending_open_remote_balance != null && $Object.hasOwnProperty.call(message, "pending_open_remote_balance"))
                $root.lnrpc.Amount.encode(message.pending_open_remote_balance, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data") && message.custom_channel_data.length)
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.custom_channel_data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelBalanceResponse message, length delimited. Does not implicitly {@link lnrpc.ChannelBalanceResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelBalanceResponse
         * @static
         * @param {lnrpc.ChannelBalanceResponse.$Properties} message ChannelBalanceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBalanceResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelBalanceResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelBalanceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelBalanceResponse & lnrpc.ChannelBalanceResponse.$Shape} ChannelBalanceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBalanceResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelBalanceResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.balance = value;
                        else
                            delete message.balance;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.pending_open_balance = value;
                        else
                            delete message.pending_open_balance;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.local_balance = $root.lnrpc.Amount.decode(reader, reader.uint32(), $undefined, _depth + 1, message.local_balance);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.remote_balance = $root.lnrpc.Amount.decode(reader, reader.uint32(), $undefined, _depth + 1, message.remote_balance);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.unsettled_local_balance = $root.lnrpc.Amount.decode(reader, reader.uint32(), $undefined, _depth + 1, message.unsettled_local_balance);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.unsettled_remote_balance = $root.lnrpc.Amount.decode(reader, reader.uint32(), $undefined, _depth + 1, message.unsettled_remote_balance);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.pending_open_local_balance = $root.lnrpc.Amount.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pending_open_local_balance);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.pending_open_remote_balance = $root.lnrpc.Amount.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pending_open_remote_balance);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.custom_channel_data = value;
                        else
                            delete message.custom_channel_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelBalanceResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelBalanceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelBalanceResponse & lnrpc.ChannelBalanceResponse.$Shape} ChannelBalanceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBalanceResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelBalanceResponse message.
         * @function verify
         * @memberof lnrpc.ChannelBalanceResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelBalanceResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                    return "balance: integer|Long expected";
            if (message.pending_open_balance != null && $Object.hasOwnProperty.call(message, "pending_open_balance"))
                if (!$util.isInteger(message.pending_open_balance) && !(message.pending_open_balance && $util.isInteger(message.pending_open_balance.low) && $util.isInteger(message.pending_open_balance.high)))
                    return "pending_open_balance: integer|Long expected";
            if (message.local_balance != null && $Object.hasOwnProperty.call(message, "local_balance")) {
                let error = $root.lnrpc.Amount.verify(message.local_balance, _depth + 1);
                if (error)
                    return "local_balance." + error;
            }
            if (message.remote_balance != null && $Object.hasOwnProperty.call(message, "remote_balance")) {
                let error = $root.lnrpc.Amount.verify(message.remote_balance, _depth + 1);
                if (error)
                    return "remote_balance." + error;
            }
            if (message.unsettled_local_balance != null && $Object.hasOwnProperty.call(message, "unsettled_local_balance")) {
                let error = $root.lnrpc.Amount.verify(message.unsettled_local_balance, _depth + 1);
                if (error)
                    return "unsettled_local_balance." + error;
            }
            if (message.unsettled_remote_balance != null && $Object.hasOwnProperty.call(message, "unsettled_remote_balance")) {
                let error = $root.lnrpc.Amount.verify(message.unsettled_remote_balance, _depth + 1);
                if (error)
                    return "unsettled_remote_balance." + error;
            }
            if (message.pending_open_local_balance != null && $Object.hasOwnProperty.call(message, "pending_open_local_balance")) {
                let error = $root.lnrpc.Amount.verify(message.pending_open_local_balance, _depth + 1);
                if (error)
                    return "pending_open_local_balance." + error;
            }
            if (message.pending_open_remote_balance != null && $Object.hasOwnProperty.call(message, "pending_open_remote_balance")) {
                let error = $root.lnrpc.Amount.verify(message.pending_open_remote_balance, _depth + 1);
                if (error)
                    return "pending_open_remote_balance." + error;
            }
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                if (!(message.custom_channel_data && typeof message.custom_channel_data.length === "number" || $util.isString(message.custom_channel_data)))
                    return "custom_channel_data: buffer expected";
            return null;
        };

        /**
         * Creates a ChannelBalanceResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelBalanceResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelBalanceResponse} ChannelBalanceResponse
         */
        ChannelBalanceResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelBalanceResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelBalanceResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelBalanceResponse();
            if (object.balance != null)
                if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                    if ($util.Long)
                        message.balance = $util.Long.fromValue(object.balance, false);
                    else if (typeof object.balance === "string")
                        message.balance = $parseInt(object.balance, 10);
                    else if (typeof object.balance === "number")
                        message.balance = object.balance;
                    else if (typeof object.balance === "object")
                        message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber();
            if (object.pending_open_balance != null)
                if (typeof object.pending_open_balance === "object" ? object.pending_open_balance.low || object.pending_open_balance.high : $Number(object.pending_open_balance) !== 0)
                    if ($util.Long)
                        message.pending_open_balance = $util.Long.fromValue(object.pending_open_balance, false);
                    else if (typeof object.pending_open_balance === "string")
                        message.pending_open_balance = $parseInt(object.pending_open_balance, 10);
                    else if (typeof object.pending_open_balance === "number")
                        message.pending_open_balance = object.pending_open_balance;
                    else if (typeof object.pending_open_balance === "object")
                        message.pending_open_balance = new $util.LongBits(object.pending_open_balance.low >>> 0, object.pending_open_balance.high >>> 0).toNumber();
            if (object.local_balance != null) {
                if (!$util.isObject(object.local_balance))
                    throw $TypeError(".lnrpc.ChannelBalanceResponse.local_balance: object expected");
                message.local_balance = $root.lnrpc.Amount.fromObject(object.local_balance, _depth + 1);
            }
            if (object.remote_balance != null) {
                if (!$util.isObject(object.remote_balance))
                    throw $TypeError(".lnrpc.ChannelBalanceResponse.remote_balance: object expected");
                message.remote_balance = $root.lnrpc.Amount.fromObject(object.remote_balance, _depth + 1);
            }
            if (object.unsettled_local_balance != null) {
                if (!$util.isObject(object.unsettled_local_balance))
                    throw $TypeError(".lnrpc.ChannelBalanceResponse.unsettled_local_balance: object expected");
                message.unsettled_local_balance = $root.lnrpc.Amount.fromObject(object.unsettled_local_balance, _depth + 1);
            }
            if (object.unsettled_remote_balance != null) {
                if (!$util.isObject(object.unsettled_remote_balance))
                    throw $TypeError(".lnrpc.ChannelBalanceResponse.unsettled_remote_balance: object expected");
                message.unsettled_remote_balance = $root.lnrpc.Amount.fromObject(object.unsettled_remote_balance, _depth + 1);
            }
            if (object.pending_open_local_balance != null) {
                if (!$util.isObject(object.pending_open_local_balance))
                    throw $TypeError(".lnrpc.ChannelBalanceResponse.pending_open_local_balance: object expected");
                message.pending_open_local_balance = $root.lnrpc.Amount.fromObject(object.pending_open_local_balance, _depth + 1);
            }
            if (object.pending_open_remote_balance != null) {
                if (!$util.isObject(object.pending_open_remote_balance))
                    throw $TypeError(".lnrpc.ChannelBalanceResponse.pending_open_remote_balance: object expected");
                message.pending_open_remote_balance = $root.lnrpc.Amount.fromObject(object.pending_open_remote_balance, _depth + 1);
            }
            if (object.custom_channel_data != null)
                if (object.custom_channel_data.length)
                    if (typeof object.custom_channel_data === "string")
                        $util.base64.decode(object.custom_channel_data, message.custom_channel_data = $util.newBuffer($util.base64.length(object.custom_channel_data)), 0);
                    else if (object.custom_channel_data.length >= 0)
                        message.custom_channel_data = object.custom_channel_data;
            return message;
        };

        /**
         * Creates a plain object from a ChannelBalanceResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelBalanceResponse
         * @static
         * @param {lnrpc.ChannelBalanceResponse} message ChannelBalanceResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelBalanceResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.pending_open_balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.pending_open_balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.local_balance = null;
                object.remote_balance = null;
                object.unsettled_local_balance = null;
                object.unsettled_remote_balance = null;
                object.pending_open_local_balance = null;
                object.pending_open_remote_balance = null;
                if (options.bytes === $String)
                    object.custom_channel_data = "";
                else {
                    object.custom_channel_data = [];
                    if (options.bytes !== $Array)
                        object.custom_channel_data = $util.newBuffer(object.custom_channel_data);
                }
            }
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, false).toBigInt();
                else if (typeof message.balance === "number")
                    object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                else
                    object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber() : message.balance;
            if (message.pending_open_balance != null && $Object.hasOwnProperty.call(message, "pending_open_balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.pending_open_balance = typeof message.pending_open_balance === "number" ? $BigInt(message.pending_open_balance) : $util.Long.fromBits(message.pending_open_balance.low >>> 0, message.pending_open_balance.high >>> 0, false).toBigInt();
                else if (typeof message.pending_open_balance === "number")
                    object.pending_open_balance = options.longs === $String ? $String(message.pending_open_balance) : message.pending_open_balance;
                else
                    object.pending_open_balance = options.longs === $String ? $util.Long.prototype.toString.call(message.pending_open_balance) : options.longs === $Number ? new $util.LongBits(message.pending_open_balance.low >>> 0, message.pending_open_balance.high >>> 0).toNumber() : message.pending_open_balance;
            if (message.local_balance != null && $Object.hasOwnProperty.call(message, "local_balance"))
                object.local_balance = $root.lnrpc.Amount.toObject(message.local_balance, options, _depth + 1);
            if (message.remote_balance != null && $Object.hasOwnProperty.call(message, "remote_balance"))
                object.remote_balance = $root.lnrpc.Amount.toObject(message.remote_balance, options, _depth + 1);
            if (message.unsettled_local_balance != null && $Object.hasOwnProperty.call(message, "unsettled_local_balance"))
                object.unsettled_local_balance = $root.lnrpc.Amount.toObject(message.unsettled_local_balance, options, _depth + 1);
            if (message.unsettled_remote_balance != null && $Object.hasOwnProperty.call(message, "unsettled_remote_balance"))
                object.unsettled_remote_balance = $root.lnrpc.Amount.toObject(message.unsettled_remote_balance, options, _depth + 1);
            if (message.pending_open_local_balance != null && $Object.hasOwnProperty.call(message, "pending_open_local_balance"))
                object.pending_open_local_balance = $root.lnrpc.Amount.toObject(message.pending_open_local_balance, options, _depth + 1);
            if (message.pending_open_remote_balance != null && $Object.hasOwnProperty.call(message, "pending_open_remote_balance"))
                object.pending_open_remote_balance = $root.lnrpc.Amount.toObject(message.pending_open_remote_balance, options, _depth + 1);
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                object.custom_channel_data = options.bytes === $String ? $util.base64.encode(message.custom_channel_data, 0, message.custom_channel_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_channel_data) : message.custom_channel_data;
            return object;
        };

        /**
         * Converts this ChannelBalanceResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelBalanceResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelBalanceResponse.prototype.toJSON = function() {
            return ChannelBalanceResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelBalanceResponse
         * @function getTypeUrl
         * @memberof lnrpc.ChannelBalanceResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelBalanceResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelBalanceResponse";
        };

        return ChannelBalanceResponse;
    })();

    lnrpc.QueryRoutesRequest = (function() {

        /**
         * Properties of a QueryRoutesRequest.
         * @typedef {Object} lnrpc.QueryRoutesRequest.$Properties
         * @property {string|null} [pub_key] QueryRoutesRequest pub_key
         * @property {Long|null} [amt] QueryRoutesRequest amt
         * @property {Long|null} [amt_msat] QueryRoutesRequest amt_msat
         * @property {number|null} [final_cltv_delta] QueryRoutesRequest final_cltv_delta
         * @property {lnrpc.FeeLimit.$Properties|null} [fee_limit] QueryRoutesRequest fee_limit
         * @property {Array.<Uint8Array>|null} [ignored_nodes] QueryRoutesRequest ignored_nodes
         * @property {Array.<lnrpc.EdgeLocator.$Properties>|null} [ignored_edges] QueryRoutesRequest ignored_edges
         * @property {string|null} [source_pub_key] QueryRoutesRequest source_pub_key
         * @property {boolean|null} [use_mission_control] QueryRoutesRequest use_mission_control
         * @property {Array.<lnrpc.NodePair.$Properties>|null} [ignored_pairs] QueryRoutesRequest ignored_pairs
         * @property {number|null} [cltv_limit] QueryRoutesRequest cltv_limit
         * @property {Object.<string,Uint8Array>|null} [dest_custom_records] QueryRoutesRequest dest_custom_records
         * @property {Long|null} [outgoing_chan_id] QueryRoutesRequest outgoing_chan_id
         * @property {Uint8Array|null} [last_hop_pubkey] QueryRoutesRequest last_hop_pubkey
         * @property {Array.<lnrpc.RouteHint.$Properties>|null} [route_hints] QueryRoutesRequest route_hints
         * @property {Array.<lnrpc.BlindedPaymentPath.$Properties>|null} [blinded_payment_paths] QueryRoutesRequest blinded_payment_paths
         * @property {Array.<lnrpc.FeatureBit>|null} [dest_features] QueryRoutesRequest dest_features
         * @property {number|null} [time_pref] QueryRoutesRequest time_pref
         * @property {Array.<Long>|null} [outgoing_chan_ids] QueryRoutesRequest outgoing_chan_ids
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a QueryRoutesRequest.
         * @memberof lnrpc
         * @interface IQueryRoutesRequest
         * @augments lnrpc.QueryRoutesRequest.$Properties
         * @deprecated Use lnrpc.QueryRoutesRequest.$Properties instead.
         */

        /**
         * Shape of a QueryRoutesRequest.
         * @typedef {{
         *   pub_key?: string|null;
         *   amt?: Long|null;
         *   amt_msat?: Long|null;
         *   final_cltv_delta?: number|null;
         *   fee_limit?: lnrpc.FeeLimit.$Shape|null;
         *   ignored_nodes?: Array.<Uint8Array>|null;
         *   ignored_edges?: Array.<lnrpc.EdgeLocator.$Shape>|null;
         *   source_pub_key?: string|null;
         *   use_mission_control?: boolean|null;
         *   ignored_pairs?: Array.<lnrpc.NodePair.$Shape>|null;
         *   cltv_limit?: number|null;
         *   dest_custom_records?: Object.<string,Uint8Array>|null;
         *   outgoing_chan_id?: Long|null;
         *   last_hop_pubkey?: Uint8Array|null;
         *   route_hints?: Array.<lnrpc.RouteHint.$Shape>|null;
         *   blinded_payment_paths?: Array.<lnrpc.BlindedPaymentPath.$Shape>|null;
         *   dest_features?: Array.<lnrpc.FeatureBit>|null;
         *   time_pref?: number|null;
         *   outgoing_chan_ids?: Array.<Long>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.QueryRoutesRequest.$Shape
         */

        /**
         * Constructs a new QueryRoutesRequest.
         * @memberof lnrpc
         * @classdesc Represents a QueryRoutesRequest.
         * @constructor
         * @param {lnrpc.QueryRoutesRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const QueryRoutesRequest = function (properties) {
            this.ignored_nodes = [];
            this.ignored_edges = [];
            this.ignored_pairs = [];
            this.dest_custom_records = {};
            this.route_hints = [];
            this.blinded_payment_paths = [];
            this.dest_features = [];
            this.outgoing_chan_ids = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * QueryRoutesRequest pub_key.
         * @member {string} pub_key
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.pub_key = "";

        /**
         * QueryRoutesRequest amt.
         * @member {Long} amt
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.amt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * QueryRoutesRequest amt_msat.
         * @member {Long} amt_msat
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.amt_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * QueryRoutesRequest final_cltv_delta.
         * @member {number} final_cltv_delta
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.final_cltv_delta = 0;

        /**
         * QueryRoutesRequest fee_limit.
         * @member {lnrpc.FeeLimit.$Properties|null|undefined} fee_limit
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.fee_limit = null;

        /**
         * QueryRoutesRequest ignored_nodes.
         * @member {Array.<Uint8Array>} ignored_nodes
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.ignored_nodes = $util.emptyArray;

        /**
         * QueryRoutesRequest ignored_edges.
         * @member {Array.<lnrpc.EdgeLocator.$Properties>} ignored_edges
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.ignored_edges = $util.emptyArray;

        /**
         * QueryRoutesRequest source_pub_key.
         * @member {string} source_pub_key
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.source_pub_key = "";

        /**
         * QueryRoutesRequest use_mission_control.
         * @member {boolean} use_mission_control
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.use_mission_control = false;

        /**
         * QueryRoutesRequest ignored_pairs.
         * @member {Array.<lnrpc.NodePair.$Properties>} ignored_pairs
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.ignored_pairs = $util.emptyArray;

        /**
         * QueryRoutesRequest cltv_limit.
         * @member {number} cltv_limit
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.cltv_limit = 0;

        /**
         * QueryRoutesRequest dest_custom_records.
         * @member {Object.<string,Uint8Array>} dest_custom_records
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.dest_custom_records = $util.emptyObject;

        /**
         * QueryRoutesRequest outgoing_chan_id.
         * @member {Long} outgoing_chan_id
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.outgoing_chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * QueryRoutesRequest last_hop_pubkey.
         * @member {Uint8Array} last_hop_pubkey
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.last_hop_pubkey = $util.newBuffer([]);

        /**
         * QueryRoutesRequest route_hints.
         * @member {Array.<lnrpc.RouteHint.$Properties>} route_hints
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.route_hints = $util.emptyArray;

        /**
         * QueryRoutesRequest blinded_payment_paths.
         * @member {Array.<lnrpc.BlindedPaymentPath.$Properties>} blinded_payment_paths
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.blinded_payment_paths = $util.emptyArray;

        /**
         * QueryRoutesRequest dest_features.
         * @member {Array.<lnrpc.FeatureBit>} dest_features
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.dest_features = $util.emptyArray;

        /**
         * QueryRoutesRequest time_pref.
         * @member {number} time_pref
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.time_pref = 0;

        /**
         * QueryRoutesRequest outgoing_chan_ids.
         * @member {Array.<Long>} outgoing_chan_ids
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         */
        QueryRoutesRequest.prototype.outgoing_chan_ids = $util.emptyArray;

        /**
         * Creates a new QueryRoutesRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.QueryRoutesRequest
         * @static
         * @param {lnrpc.QueryRoutesRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.QueryRoutesRequest} QueryRoutesRequest instance
         * @type {{
         *   (properties: lnrpc.QueryRoutesRequest.$Shape): lnrpc.QueryRoutesRequest & lnrpc.QueryRoutesRequest.$Shape;
         *   (properties?: lnrpc.QueryRoutesRequest.$Properties): lnrpc.QueryRoutesRequest;
         * }}
         */
        QueryRoutesRequest.create = function(properties) {
            return new QueryRoutesRequest(properties);
        };

        /**
         * Encodes the specified QueryRoutesRequest message. Does not implicitly {@link lnrpc.QueryRoutesRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.QueryRoutesRequest
         * @static
         * @param {lnrpc.QueryRoutesRequest.$Properties} message QueryRoutesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryRoutesRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key") && message.pub_key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pub_key);
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt") && (typeof message.amt === "object" ? message.amt.low || message.amt.high : message.amt !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amt);
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta") && message.final_cltv_delta !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.final_cltv_delta);
            if (message.fee_limit != null && $Object.hasOwnProperty.call(message, "fee_limit"))
                $root.lnrpc.FeeLimit.encode(message.fee_limit, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.ignored_nodes != null && message.ignored_nodes.length)
                for (let i = 0; i < message.ignored_nodes.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.ignored_nodes[i]);
            if (message.ignored_edges != null && message.ignored_edges.length)
                for (let i = 0; i < message.ignored_edges.length; ++i)
                    $root.lnrpc.EdgeLocator.encode(message.ignored_edges[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.source_pub_key != null && $Object.hasOwnProperty.call(message, "source_pub_key") && message.source_pub_key !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.source_pub_key);
            if (message.use_mission_control != null && $Object.hasOwnProperty.call(message, "use_mission_control") && message.use_mission_control !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.use_mission_control);
            if (message.ignored_pairs != null && message.ignored_pairs.length)
                for (let i = 0; i < message.ignored_pairs.length; ++i)
                    $root.lnrpc.NodePair.encode(message.ignored_pairs[i], writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.cltv_limit != null && $Object.hasOwnProperty.call(message, "cltv_limit") && message.cltv_limit !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.cltv_limit);
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat") && (typeof message.amt_msat === "object" ? message.amt_msat.low || message.amt_msat.high : message.amt_msat !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.amt_msat);
            if (message.dest_custom_records != null && $Object.hasOwnProperty.call(message, "dest_custom_records"))
                for (let keys = $Object.keys(message.dest_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 13, wireType 2 =*/106).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.dest_custom_records[keys[i]]).ldelim();
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id") && (typeof message.outgoing_chan_id === "object" ? message.outgoing_chan_id.low || message.outgoing_chan_id.high : message.outgoing_chan_id !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.outgoing_chan_id);
            if (message.last_hop_pubkey != null && $Object.hasOwnProperty.call(message, "last_hop_pubkey") && message.last_hop_pubkey.length)
                writer.uint32(/* id 15, wireType 2 =*/122).bytes(message.last_hop_pubkey);
            if (message.route_hints != null && message.route_hints.length)
                for (let i = 0; i < message.route_hints.length; ++i)
                    $root.lnrpc.RouteHint.encode(message.route_hints[i], writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.dest_features != null && message.dest_features.length)
                writer.uint32(/* id 17, wireType 2 =*/138).int32s(message.dest_features);
            if (message.time_pref != null && $Object.hasOwnProperty.call(message, "time_pref") && !$Object.is(message.time_pref, 0))
                writer.uint32(/* id 18, wireType 1 =*/145).double(message.time_pref);
            if (message.blinded_payment_paths != null && message.blinded_payment_paths.length)
                for (let i = 0; i < message.blinded_payment_paths.length; ++i)
                    $root.lnrpc.BlindedPaymentPath.encode(message.blinded_payment_paths[i], writer.uint32(/* id 19, wireType 2 =*/154).fork(), _depth + 1).ldelim();
            if (message.outgoing_chan_ids != null && message.outgoing_chan_ids.length)
                writer.uint32(/* id 20, wireType 2 =*/162).uint64s(message.outgoing_chan_ids);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QueryRoutesRequest message, length delimited. Does not implicitly {@link lnrpc.QueryRoutesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.QueryRoutesRequest
         * @static
         * @param {lnrpc.QueryRoutesRequest.$Properties} message QueryRoutesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryRoutesRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryRoutesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.QueryRoutesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.QueryRoutesRequest & lnrpc.QueryRoutesRequest.$Shape} QueryRoutesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryRoutesRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.QueryRoutesRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pub_key = value;
                        else
                            delete message.pub_key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt = value;
                        else
                            delete message.amt;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_msat = value;
                        else
                            delete message.amt_msat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.final_cltv_delta = value;
                        else
                            delete message.final_cltv_delta;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.fee_limit = $root.lnrpc.FeeLimit.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fee_limit);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ignored_nodes && message.ignored_nodes.length))
                            message.ignored_nodes = [];
                        message.ignored_nodes.push(reader.bytes());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ignored_edges && message.ignored_edges.length))
                            message.ignored_edges = [];
                        message.ignored_edges.push($root.lnrpc.EdgeLocator.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.source_pub_key = value;
                        else
                            delete message.source_pub_key;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.use_mission_control = value;
                        else
                            delete message.use_mission_control;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ignored_pairs && message.ignored_pairs.length))
                            message.ignored_pairs = [];
                        message.ignored_pairs.push($root.lnrpc.NodePair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.cltv_limit = value;
                        else
                            delete message.cltv_limit;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if (message.dest_custom_records === $util.emptyObject)
                            message.dest_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.dest_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.outgoing_chan_id = value;
                        else
                            delete message.outgoing_chan_id;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.last_hop_pubkey = value;
                        else
                            delete message.last_hop_pubkey;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.route_hints && message.route_hints.length))
                            message.route_hints = [];
                        message.route_hints.push($root.lnrpc.RouteHint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if (!(message.blinded_payment_paths && message.blinded_payment_paths.length))
                            message.blinded_payment_paths = [];
                        message.blinded_payment_paths.push($root.lnrpc.BlindedPaymentPath.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 17: {
                        if (wireType === 2) {
                            if (!(message.dest_features && message.dest_features.length))
                                message.dest_features = [];
                            reader.int32s(message.dest_features);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.dest_features && message.dest_features.length))
                            message.dest_features = [];
                        message.dest_features.push(reader.int32());
                        continue;
                    }
                case 18: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.time_pref = value;
                        else
                            delete message.time_pref;
                        continue;
                    }
                case 20: {
                        if (wireType === 2) {
                            if (!(message.outgoing_chan_ids && message.outgoing_chan_ids.length))
                                message.outgoing_chan_ids = [];
                            reader.uint64s(message.outgoing_chan_ids);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.outgoing_chan_ids && message.outgoing_chan_ids.length))
                            message.outgoing_chan_ids = [];
                        message.outgoing_chan_ids.push(reader.uint64());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QueryRoutesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.QueryRoutesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.QueryRoutesRequest & lnrpc.QueryRoutesRequest.$Shape} QueryRoutesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryRoutesRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryRoutesRequest message.
         * @function verify
         * @memberof lnrpc.QueryRoutesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryRoutesRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                if (!$util.isString(message.pub_key))
                    return "pub_key: string expected";
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt"))
                if (!$util.isInteger(message.amt) && !(message.amt && $util.isInteger(message.amt.low) && $util.isInteger(message.amt.high)))
                    return "amt: integer|Long expected";
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (!$util.isInteger(message.amt_msat) && !(message.amt_msat && $util.isInteger(message.amt_msat.low) && $util.isInteger(message.amt_msat.high)))
                    return "amt_msat: integer|Long expected";
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta"))
                if (!$util.isInteger(message.final_cltv_delta))
                    return "final_cltv_delta: integer expected";
            if (message.fee_limit != null && $Object.hasOwnProperty.call(message, "fee_limit")) {
                let error = $root.lnrpc.FeeLimit.verify(message.fee_limit, _depth + 1);
                if (error)
                    return "fee_limit." + error;
            }
            if (message.ignored_nodes != null && $Object.hasOwnProperty.call(message, "ignored_nodes")) {
                if (!$Array.isArray(message.ignored_nodes))
                    return "ignored_nodes: array expected";
                for (let i = 0; i < message.ignored_nodes.length; ++i)
                    if (!(message.ignored_nodes[i] && typeof message.ignored_nodes[i].length === "number" || $util.isString(message.ignored_nodes[i])))
                        return "ignored_nodes: buffer[] expected";
            }
            if (message.ignored_edges != null && $Object.hasOwnProperty.call(message, "ignored_edges")) {
                if (!$Array.isArray(message.ignored_edges))
                    return "ignored_edges: array expected";
                for (let i = 0; i < message.ignored_edges.length; ++i) {
                    let error = $root.lnrpc.EdgeLocator.verify(message.ignored_edges[i], _depth + 1);
                    if (error)
                        return "ignored_edges." + error;
                }
            }
            if (message.source_pub_key != null && $Object.hasOwnProperty.call(message, "source_pub_key"))
                if (!$util.isString(message.source_pub_key))
                    return "source_pub_key: string expected";
            if (message.use_mission_control != null && $Object.hasOwnProperty.call(message, "use_mission_control"))
                if (typeof message.use_mission_control !== "boolean")
                    return "use_mission_control: boolean expected";
            if (message.ignored_pairs != null && $Object.hasOwnProperty.call(message, "ignored_pairs")) {
                if (!$Array.isArray(message.ignored_pairs))
                    return "ignored_pairs: array expected";
                for (let i = 0; i < message.ignored_pairs.length; ++i) {
                    let error = $root.lnrpc.NodePair.verify(message.ignored_pairs[i], _depth + 1);
                    if (error)
                        return "ignored_pairs." + error;
                }
            }
            if (message.cltv_limit != null && $Object.hasOwnProperty.call(message, "cltv_limit"))
                if (!$util.isInteger(message.cltv_limit))
                    return "cltv_limit: integer expected";
            if (message.dest_custom_records != null && $Object.hasOwnProperty.call(message, "dest_custom_records")) {
                if (!$util.isObject(message.dest_custom_records))
                    return "dest_custom_records: object expected";
                let key = $Object.keys(message.dest_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "dest_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.dest_custom_records[key[i]] && typeof message.dest_custom_records[key[i]].length === "number" || $util.isString(message.dest_custom_records[key[i]])))
                        return "dest_custom_records: buffer{k:uint64} expected";
                }
            }
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id"))
                if (!$util.isInteger(message.outgoing_chan_id) && !(message.outgoing_chan_id && $util.isInteger(message.outgoing_chan_id.low) && $util.isInteger(message.outgoing_chan_id.high)))
                    return "outgoing_chan_id: integer|Long expected";
            if (message.last_hop_pubkey != null && $Object.hasOwnProperty.call(message, "last_hop_pubkey"))
                if (!(message.last_hop_pubkey && typeof message.last_hop_pubkey.length === "number" || $util.isString(message.last_hop_pubkey)))
                    return "last_hop_pubkey: buffer expected";
            if (message.route_hints != null && $Object.hasOwnProperty.call(message, "route_hints")) {
                if (!$Array.isArray(message.route_hints))
                    return "route_hints: array expected";
                for (let i = 0; i < message.route_hints.length; ++i) {
                    let error = $root.lnrpc.RouteHint.verify(message.route_hints[i], _depth + 1);
                    if (error)
                        return "route_hints." + error;
                }
            }
            if (message.blinded_payment_paths != null && $Object.hasOwnProperty.call(message, "blinded_payment_paths")) {
                if (!$Array.isArray(message.blinded_payment_paths))
                    return "blinded_payment_paths: array expected";
                for (let i = 0; i < message.blinded_payment_paths.length; ++i) {
                    let error = $root.lnrpc.BlindedPaymentPath.verify(message.blinded_payment_paths[i], _depth + 1);
                    if (error)
                        return "blinded_payment_paths." + error;
                }
            }
            if (message.dest_features != null && $Object.hasOwnProperty.call(message, "dest_features")) {
                if (!$Array.isArray(message.dest_features))
                    return "dest_features: array expected";
                for (let i = 0; i < message.dest_features.length; ++i)
                    if (typeof message.dest_features[i] !== "number" || (message.dest_features[i] | 0) !== message.dest_features[i])
                        return "dest_features: enum value[] expected";
            }
            if (message.time_pref != null && $Object.hasOwnProperty.call(message, "time_pref"))
                if (typeof message.time_pref !== "number")
                    return "time_pref: number expected";
            if (message.outgoing_chan_ids != null && $Object.hasOwnProperty.call(message, "outgoing_chan_ids")) {
                if (!$Array.isArray(message.outgoing_chan_ids))
                    return "outgoing_chan_ids: array expected";
                for (let i = 0; i < message.outgoing_chan_ids.length; ++i)
                    if (!$util.isInteger(message.outgoing_chan_ids[i]) && !(message.outgoing_chan_ids[i] && $util.isInteger(message.outgoing_chan_ids[i].low) && $util.isInteger(message.outgoing_chan_ids[i].high)))
                        return "outgoing_chan_ids: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates a QueryRoutesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.QueryRoutesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.QueryRoutesRequest} QueryRoutesRequest
         */
        QueryRoutesRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.QueryRoutesRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.QueryRoutesRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.QueryRoutesRequest();
            if (object.pub_key != null)
                if (typeof object.pub_key !== "string" || object.pub_key.length)
                    message.pub_key = $String(object.pub_key);
            if (object.amt != null)
                if (typeof object.amt === "object" ? object.amt.low || object.amt.high : $Number(object.amt) !== 0)
                    if ($util.Long)
                        message.amt = $util.Long.fromValue(object.amt, false);
                    else if (typeof object.amt === "string")
                        message.amt = $parseInt(object.amt, 10);
                    else if (typeof object.amt === "number")
                        message.amt = object.amt;
                    else if (typeof object.amt === "object")
                        message.amt = new $util.LongBits(object.amt.low >>> 0, object.amt.high >>> 0).toNumber();
            if (object.amt_msat != null)
                if (typeof object.amt_msat === "object" ? object.amt_msat.low || object.amt_msat.high : $Number(object.amt_msat) !== 0)
                    if ($util.Long)
                        message.amt_msat = $util.Long.fromValue(object.amt_msat, false);
                    else if (typeof object.amt_msat === "string")
                        message.amt_msat = $parseInt(object.amt_msat, 10);
                    else if (typeof object.amt_msat === "number")
                        message.amt_msat = object.amt_msat;
                    else if (typeof object.amt_msat === "object")
                        message.amt_msat = new $util.LongBits(object.amt_msat.low >>> 0, object.amt_msat.high >>> 0).toNumber();
            if (object.final_cltv_delta != null)
                if ($Number(object.final_cltv_delta) !== 0)
                    message.final_cltv_delta = object.final_cltv_delta | 0;
            if (object.fee_limit != null) {
                if (!$util.isObject(object.fee_limit))
                    throw $TypeError(".lnrpc.QueryRoutesRequest.fee_limit: object expected");
                message.fee_limit = $root.lnrpc.FeeLimit.fromObject(object.fee_limit, _depth + 1);
            }
            if (object.ignored_nodes) {
                if (!$Array.isArray(object.ignored_nodes))
                    throw $TypeError(".lnrpc.QueryRoutesRequest.ignored_nodes: array expected");
                message.ignored_nodes = $Array(object.ignored_nodes.length);
                for (let i = 0; i < object.ignored_nodes.length; ++i)
                    if (typeof object.ignored_nodes[i] === "string")
                        $util.base64.decode(object.ignored_nodes[i], message.ignored_nodes[i] = $util.newBuffer($util.base64.length(object.ignored_nodes[i])), 0);
                    else if (object.ignored_nodes[i].length >= 0)
                        message.ignored_nodes[i] = object.ignored_nodes[i];
            }
            if (object.ignored_edges) {
                if (!$Array.isArray(object.ignored_edges))
                    throw $TypeError(".lnrpc.QueryRoutesRequest.ignored_edges: array expected");
                message.ignored_edges = $Array(object.ignored_edges.length);
                for (let i = 0; i < object.ignored_edges.length; ++i) {
                    if (!$util.isObject(object.ignored_edges[i]))
                        throw $TypeError(".lnrpc.QueryRoutesRequest.ignored_edges: object expected");
                    message.ignored_edges[i] = $root.lnrpc.EdgeLocator.fromObject(object.ignored_edges[i], _depth + 1);
                }
            }
            if (object.source_pub_key != null)
                if (typeof object.source_pub_key !== "string" || object.source_pub_key.length)
                    message.source_pub_key = $String(object.source_pub_key);
            if (object.use_mission_control != null)
                if (object.use_mission_control)
                    message.use_mission_control = $Boolean(object.use_mission_control);
            if (object.ignored_pairs) {
                if (!$Array.isArray(object.ignored_pairs))
                    throw $TypeError(".lnrpc.QueryRoutesRequest.ignored_pairs: array expected");
                message.ignored_pairs = $Array(object.ignored_pairs.length);
                for (let i = 0; i < object.ignored_pairs.length; ++i) {
                    if (!$util.isObject(object.ignored_pairs[i]))
                        throw $TypeError(".lnrpc.QueryRoutesRequest.ignored_pairs: object expected");
                    message.ignored_pairs[i] = $root.lnrpc.NodePair.fromObject(object.ignored_pairs[i], _depth + 1);
                }
            }
            if (object.cltv_limit != null)
                if ($Number(object.cltv_limit) !== 0)
                    message.cltv_limit = object.cltv_limit >>> 0;
            if (object.dest_custom_records) {
                if (!$util.isObject(object.dest_custom_records))
                    throw $TypeError(".lnrpc.QueryRoutesRequest.dest_custom_records: object expected");
                message.dest_custom_records = {};
                for (let keys = $Object.keys(object.dest_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.dest_custom_records, keys[i]);
                    if (typeof object.dest_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.dest_custom_records[keys[i]], message.dest_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.dest_custom_records[keys[i]])), 0);
                    else if (object.dest_custom_records[keys[i]].length >= 0)
                        message.dest_custom_records[keys[i]] = object.dest_custom_records[keys[i]];
                }
            }
            if (object.outgoing_chan_id != null)
                if (typeof object.outgoing_chan_id === "object" ? object.outgoing_chan_id.low || object.outgoing_chan_id.high : $Number(object.outgoing_chan_id) !== 0)
                    if ($util.Long)
                        message.outgoing_chan_id = $util.Long.fromValue(object.outgoing_chan_id, true);
                    else if (typeof object.outgoing_chan_id === "string")
                        message.outgoing_chan_id = $parseInt(object.outgoing_chan_id, 10);
                    else if (typeof object.outgoing_chan_id === "number")
                        message.outgoing_chan_id = object.outgoing_chan_id;
                    else if (typeof object.outgoing_chan_id === "object")
                        message.outgoing_chan_id = new $util.LongBits(object.outgoing_chan_id.low >>> 0, object.outgoing_chan_id.high >>> 0).toNumber(true);
            if (object.last_hop_pubkey != null)
                if (object.last_hop_pubkey.length)
                    if (typeof object.last_hop_pubkey === "string")
                        $util.base64.decode(object.last_hop_pubkey, message.last_hop_pubkey = $util.newBuffer($util.base64.length(object.last_hop_pubkey)), 0);
                    else if (object.last_hop_pubkey.length >= 0)
                        message.last_hop_pubkey = object.last_hop_pubkey;
            if (object.route_hints) {
                if (!$Array.isArray(object.route_hints))
                    throw $TypeError(".lnrpc.QueryRoutesRequest.route_hints: array expected");
                message.route_hints = $Array(object.route_hints.length);
                for (let i = 0; i < object.route_hints.length; ++i) {
                    if (!$util.isObject(object.route_hints[i]))
                        throw $TypeError(".lnrpc.QueryRoutesRequest.route_hints: object expected");
                    message.route_hints[i] = $root.lnrpc.RouteHint.fromObject(object.route_hints[i], _depth + 1);
                }
            }
            if (object.blinded_payment_paths) {
                if (!$Array.isArray(object.blinded_payment_paths))
                    throw $TypeError(".lnrpc.QueryRoutesRequest.blinded_payment_paths: array expected");
                message.blinded_payment_paths = $Array(object.blinded_payment_paths.length);
                for (let i = 0; i < object.blinded_payment_paths.length; ++i) {
                    if (!$util.isObject(object.blinded_payment_paths[i]))
                        throw $TypeError(".lnrpc.QueryRoutesRequest.blinded_payment_paths: object expected");
                    message.blinded_payment_paths[i] = $root.lnrpc.BlindedPaymentPath.fromObject(object.blinded_payment_paths[i], _depth + 1);
                }
            }
            if (object.dest_features) {
                if (!$Array.isArray(object.dest_features))
                    throw $TypeError(".lnrpc.QueryRoutesRequest.dest_features: array expected");
                message.dest_features = [];
                for (let i = 0; i < object.dest_features.length; ++i)
                    switch (object.dest_features[i]) {
                    case "DATALOSS_PROTECT_REQ":
                    case 0:
                        message.dest_features[message.dest_features.length] = 0;
                        break;
                    case "DATALOSS_PROTECT_OPT":
                    case 1:
                        message.dest_features[message.dest_features.length] = 1;
                        break;
                    case "INITIAL_ROUING_SYNC":
                    case 3:
                        message.dest_features[message.dest_features.length] = 3;
                        break;
                    case "UPFRONT_SHUTDOWN_SCRIPT_REQ":
                    case 4:
                        message.dest_features[message.dest_features.length] = 4;
                        break;
                    case "UPFRONT_SHUTDOWN_SCRIPT_OPT":
                    case 5:
                        message.dest_features[message.dest_features.length] = 5;
                        break;
                    case "GOSSIP_QUERIES_REQ":
                    case 6:
                        message.dest_features[message.dest_features.length] = 6;
                        break;
                    case "GOSSIP_QUERIES_OPT":
                    case 7:
                        message.dest_features[message.dest_features.length] = 7;
                        break;
                    case "TLV_ONION_REQ":
                    case 8:
                        message.dest_features[message.dest_features.length] = 8;
                        break;
                    case "TLV_ONION_OPT":
                    case 9:
                        message.dest_features[message.dest_features.length] = 9;
                        break;
                    case "EXT_GOSSIP_QUERIES_REQ":
                    case 10:
                        message.dest_features[message.dest_features.length] = 10;
                        break;
                    case "EXT_GOSSIP_QUERIES_OPT":
                    case 11:
                        message.dest_features[message.dest_features.length] = 11;
                        break;
                    case "STATIC_REMOTE_KEY_REQ":
                    case 12:
                        message.dest_features[message.dest_features.length] = 12;
                        break;
                    case "STATIC_REMOTE_KEY_OPT":
                    case 13:
                        message.dest_features[message.dest_features.length] = 13;
                        break;
                    case "PAYMENT_ADDR_REQ":
                    case 14:
                        message.dest_features[message.dest_features.length] = 14;
                        break;
                    case "PAYMENT_ADDR_OPT":
                    case 15:
                        message.dest_features[message.dest_features.length] = 15;
                        break;
                    case "MPP_REQ":
                    case 16:
                        message.dest_features[message.dest_features.length] = 16;
                        break;
                    case "MPP_OPT":
                    case 17:
                        message.dest_features[message.dest_features.length] = 17;
                        break;
                    case "WUMBO_CHANNELS_REQ":
                    case 18:
                        message.dest_features[message.dest_features.length] = 18;
                        break;
                    case "WUMBO_CHANNELS_OPT":
                    case 19:
                        message.dest_features[message.dest_features.length] = 19;
                        break;
                    case "ANCHORS_REQ":
                    case 20:
                        message.dest_features[message.dest_features.length] = 20;
                        break;
                    case "ANCHORS_OPT":
                    case 21:
                        message.dest_features[message.dest_features.length] = 21;
                        break;
                    case "ANCHORS_ZERO_FEE_HTLC_REQ":
                    case 22:
                        message.dest_features[message.dest_features.length] = 22;
                        break;
                    case "ANCHORS_ZERO_FEE_HTLC_OPT":
                    case 23:
                        message.dest_features[message.dest_features.length] = 23;
                        break;
                    case "ROUTE_BLINDING_REQUIRED":
                    case 24:
                        message.dest_features[message.dest_features.length] = 24;
                        break;
                    case "ROUTE_BLINDING_OPTIONAL":
                    case 25:
                        message.dest_features[message.dest_features.length] = 25;
                        break;
                    case "AMP_REQ":
                    case 30:
                        message.dest_features[message.dest_features.length] = 30;
                        break;
                    case "AMP_OPT":
                    case 31:
                        message.dest_features[message.dest_features.length] = 31;
                        break;
                    default:
                        if (typeof object.dest_features[i] === "number" && (object.dest_features[i] | 0) === object.dest_features[i])
                            message.dest_features[message.dest_features.length] = object.dest_features[i];
                    }
            }
            if (object.time_pref != null)
                if (!$Object.is($Number(object.time_pref), 0))
                    message.time_pref = $Number(object.time_pref);
            if (object.outgoing_chan_ids) {
                if (!$Array.isArray(object.outgoing_chan_ids))
                    throw $TypeError(".lnrpc.QueryRoutesRequest.outgoing_chan_ids: array expected");
                message.outgoing_chan_ids = $Array(object.outgoing_chan_ids.length);
                for (let i = 0; i < object.outgoing_chan_ids.length; ++i)
                    if ($util.Long)
                        message.outgoing_chan_ids[i] = $util.Long.fromValue(object.outgoing_chan_ids[i], true);
                    else if (typeof object.outgoing_chan_ids[i] === "string")
                        message.outgoing_chan_ids[i] = $parseInt(object.outgoing_chan_ids[i], 10);
                    else if (typeof object.outgoing_chan_ids[i] === "number")
                        message.outgoing_chan_ids[i] = object.outgoing_chan_ids[i];
                    else if (typeof object.outgoing_chan_ids[i] === "object")
                        message.outgoing_chan_ids[i] = new $util.LongBits(object.outgoing_chan_ids[i].low >>> 0, object.outgoing_chan_ids[i].high >>> 0).toNumber(true);
            }
            return message;
        };

        /**
         * Creates a plain object from a QueryRoutesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.QueryRoutesRequest
         * @static
         * @param {lnrpc.QueryRoutesRequest} message QueryRoutesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryRoutesRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.ignored_nodes = [];
                object.ignored_edges = [];
                object.ignored_pairs = [];
                object.route_hints = [];
                object.dest_features = [];
                object.blinded_payment_paths = [];
                object.outgoing_chan_ids = [];
            }
            if (options.objects || options.defaults)
                object.dest_custom_records = {};
            if (options.defaults) {
                object.pub_key = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.final_cltv_delta = 0;
                object.fee_limit = null;
                object.source_pub_key = "";
                object.use_mission_control = false;
                object.cltv_limit = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.outgoing_chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.outgoing_chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.last_hop_pubkey = "";
                else {
                    object.last_hop_pubkey = [];
                    if (options.bytes !== $Array)
                        object.last_hop_pubkey = $util.newBuffer(object.last_hop_pubkey);
                }
                object.time_pref = 0;
            }
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                object.pub_key = message.pub_key;
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt = typeof message.amt === "number" ? $BigInt(message.amt) : $util.Long.fromBits(message.amt.low >>> 0, message.amt.high >>> 0, false).toBigInt();
                else if (typeof message.amt === "number")
                    object.amt = options.longs === $String ? $String(message.amt) : message.amt;
                else
                    object.amt = options.longs === $String ? $util.Long.prototype.toString.call(message.amt) : options.longs === $Number ? new $util.LongBits(message.amt.low >>> 0, message.amt.high >>> 0).toNumber() : message.amt;
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta"))
                object.final_cltv_delta = message.final_cltv_delta;
            if (message.fee_limit != null && $Object.hasOwnProperty.call(message, "fee_limit"))
                object.fee_limit = $root.lnrpc.FeeLimit.toObject(message.fee_limit, options, _depth + 1);
            if (message.ignored_nodes && message.ignored_nodes.length) {
                object.ignored_nodes = $Array(message.ignored_nodes.length);
                for (let j = 0; j < message.ignored_nodes.length; ++j)
                    object.ignored_nodes[j] = options.bytes === $String ? $util.base64.encode(message.ignored_nodes[j], 0, message.ignored_nodes[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ignored_nodes[j]) : message.ignored_nodes[j];
            }
            if (message.ignored_edges && message.ignored_edges.length) {
                object.ignored_edges = $Array(message.ignored_edges.length);
                for (let j = 0; j < message.ignored_edges.length; ++j)
                    object.ignored_edges[j] = $root.lnrpc.EdgeLocator.toObject(message.ignored_edges[j], options, _depth + 1);
            }
            if (message.source_pub_key != null && $Object.hasOwnProperty.call(message, "source_pub_key"))
                object.source_pub_key = message.source_pub_key;
            if (message.use_mission_control != null && $Object.hasOwnProperty.call(message, "use_mission_control"))
                object.use_mission_control = message.use_mission_control;
            if (message.ignored_pairs && message.ignored_pairs.length) {
                object.ignored_pairs = $Array(message.ignored_pairs.length);
                for (let j = 0; j < message.ignored_pairs.length; ++j)
                    object.ignored_pairs[j] = $root.lnrpc.NodePair.toObject(message.ignored_pairs[j], options, _depth + 1);
            }
            if (message.cltv_limit != null && $Object.hasOwnProperty.call(message, "cltv_limit"))
                object.cltv_limit = message.cltv_limit;
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_msat = typeof message.amt_msat === "number" ? $BigInt(message.amt_msat) : $util.Long.fromBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_msat === "number")
                    object.amt_msat = options.longs === $String ? $String(message.amt_msat) : message.amt_msat;
                else
                    object.amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_msat) : options.longs === $Number ? new $util.LongBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0).toNumber() : message.amt_msat;
            let keys2;
            if (message.dest_custom_records && (keys2 = $Object.keys(message.dest_custom_records)).length) {
                object.dest_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.dest_custom_records, keys2[j]);
                    object.dest_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.dest_custom_records[keys2[j]], 0, message.dest_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.dest_custom_records[keys2[j]]) : message.dest_custom_records[keys2[j]];
                }
            }
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_chan_id = typeof message.outgoing_chan_id === "number" ? $BigInt(message.outgoing_chan_id) : $util.Long.fromBits(message.outgoing_chan_id.low >>> 0, message.outgoing_chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_chan_id === "number")
                    object.outgoing_chan_id = options.longs === $String ? $String(message.outgoing_chan_id) : message.outgoing_chan_id;
                else
                    object.outgoing_chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_chan_id) : options.longs === $Number ? new $util.LongBits(message.outgoing_chan_id.low >>> 0, message.outgoing_chan_id.high >>> 0).toNumber(true) : message.outgoing_chan_id;
            if (message.last_hop_pubkey != null && $Object.hasOwnProperty.call(message, "last_hop_pubkey"))
                object.last_hop_pubkey = options.bytes === $String ? $util.base64.encode(message.last_hop_pubkey, 0, message.last_hop_pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.last_hop_pubkey) : message.last_hop_pubkey;
            if (message.route_hints && message.route_hints.length) {
                object.route_hints = $Array(message.route_hints.length);
                for (let j = 0; j < message.route_hints.length; ++j)
                    object.route_hints[j] = $root.lnrpc.RouteHint.toObject(message.route_hints[j], options, _depth + 1);
            }
            if (message.dest_features && message.dest_features.length) {
                object.dest_features = $Array(message.dest_features.length);
                for (let j = 0; j < message.dest_features.length; ++j)
                    object.dest_features[j] = options.enums === $String ? $root.lnrpc.FeatureBit[message.dest_features[j]] === $undefined ? message.dest_features[j] : $root.lnrpc.FeatureBit[message.dest_features[j]] : message.dest_features[j];
            }
            if (message.time_pref != null && $Object.hasOwnProperty.call(message, "time_pref"))
                object.time_pref = options.json && !$isFinite(message.time_pref) ? $String(message.time_pref) : message.time_pref;
            if (message.blinded_payment_paths && message.blinded_payment_paths.length) {
                object.blinded_payment_paths = $Array(message.blinded_payment_paths.length);
                for (let j = 0; j < message.blinded_payment_paths.length; ++j)
                    object.blinded_payment_paths[j] = $root.lnrpc.BlindedPaymentPath.toObject(message.blinded_payment_paths[j], options, _depth + 1);
            }
            if (message.outgoing_chan_ids && message.outgoing_chan_ids.length) {
                object.outgoing_chan_ids = $Array(message.outgoing_chan_ids.length);
                for (let j = 0; j < message.outgoing_chan_ids.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.outgoing_chan_ids[j] = typeof message.outgoing_chan_ids[j] === "number" ? $BigInt(message.outgoing_chan_ids[j]) : $util.Long.fromBits(message.outgoing_chan_ids[j].low >>> 0, message.outgoing_chan_ids[j].high >>> 0, true).toBigInt();
                    else if (typeof message.outgoing_chan_ids[j] === "number")
                        object.outgoing_chan_ids[j] = options.longs === $String ? $String(message.outgoing_chan_ids[j]) : message.outgoing_chan_ids[j];
                    else
                        object.outgoing_chan_ids[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_chan_ids[j]) : options.longs === $Number ? new $util.LongBits(message.outgoing_chan_ids[j].low >>> 0, message.outgoing_chan_ids[j].high >>> 0).toNumber(true) : message.outgoing_chan_ids[j];
            }
            return object;
        };

        /**
         * Converts this QueryRoutesRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.QueryRoutesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryRoutesRequest.prototype.toJSON = function() {
            return QueryRoutesRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QueryRoutesRequest
         * @function getTypeUrl
         * @memberof lnrpc.QueryRoutesRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QueryRoutesRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.QueryRoutesRequest";
        };

        return QueryRoutesRequest;
    })();

    lnrpc.NodePair = (function() {

        /**
         * Properties of a NodePair.
         * @typedef {Object} lnrpc.NodePair.$Properties
         * @property {Uint8Array|null} [from] NodePair from
         * @property {Uint8Array|null} [to] NodePair to
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NodePair.
         * @memberof lnrpc
         * @interface INodePair
         * @augments lnrpc.NodePair.$Properties
         * @deprecated Use lnrpc.NodePair.$Properties instead.
         */

        /**
         * Shape of a NodePair.
         * @typedef {lnrpc.NodePair.$Properties} lnrpc.NodePair.$Shape
         */

        /**
         * Constructs a new NodePair.
         * @memberof lnrpc
         * @classdesc Represents a NodePair.
         * @constructor
         * @param {lnrpc.NodePair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NodePair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NodePair from.
         * @member {Uint8Array} from
         * @memberof lnrpc.NodePair
         * @instance
         */
        NodePair.prototype.from = $util.newBuffer([]);

        /**
         * NodePair to.
         * @member {Uint8Array} to
         * @memberof lnrpc.NodePair
         * @instance
         */
        NodePair.prototype.to = $util.newBuffer([]);

        /**
         * Creates a new NodePair instance using the specified properties.
         * @function create
         * @memberof lnrpc.NodePair
         * @static
         * @param {lnrpc.NodePair.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NodePair} NodePair instance
         * @type {{
         *   (properties: lnrpc.NodePair.$Shape): lnrpc.NodePair & lnrpc.NodePair.$Shape;
         *   (properties?: lnrpc.NodePair.$Properties): lnrpc.NodePair;
         * }}
         */
        NodePair.create = function(properties) {
            return new NodePair(properties);
        };

        /**
         * Encodes the specified NodePair message. Does not implicitly {@link lnrpc.NodePair.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NodePair
         * @static
         * @param {lnrpc.NodePair.$Properties} message NodePair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodePair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.from != null && $Object.hasOwnProperty.call(message, "from") && message.from.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.from);
            if (message.to != null && $Object.hasOwnProperty.call(message, "to") && message.to.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.to);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodePair message, length delimited. Does not implicitly {@link lnrpc.NodePair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NodePair
         * @static
         * @param {lnrpc.NodePair.$Properties} message NodePair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodePair.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NodePair message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NodePair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NodePair & lnrpc.NodePair.$Shape} NodePair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodePair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NodePair(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.from = value;
                        else
                            delete message.from;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.to = value;
                        else
                            delete message.to;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodePair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NodePair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NodePair & lnrpc.NodePair.$Shape} NodePair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodePair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodePair message.
         * @function verify
         * @memberof lnrpc.NodePair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodePair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.from != null && $Object.hasOwnProperty.call(message, "from"))
                if (!(message.from && typeof message.from.length === "number" || $util.isString(message.from)))
                    return "from: buffer expected";
            if (message.to != null && $Object.hasOwnProperty.call(message, "to"))
                if (!(message.to && typeof message.to.length === "number" || $util.isString(message.to)))
                    return "to: buffer expected";
            return null;
        };

        /**
         * Creates a NodePair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NodePair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NodePair} NodePair
         */
        NodePair.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NodePair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NodePair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NodePair();
            if (object.from != null)
                if (object.from.length)
                    if (typeof object.from === "string")
                        $util.base64.decode(object.from, message.from = $util.newBuffer($util.base64.length(object.from)), 0);
                    else if (object.from.length >= 0)
                        message.from = object.from;
            if (object.to != null)
                if (object.to.length)
                    if (typeof object.to === "string")
                        $util.base64.decode(object.to, message.to = $util.newBuffer($util.base64.length(object.to)), 0);
                    else if (object.to.length >= 0)
                        message.to = object.to;
            return message;
        };

        /**
         * Creates a plain object from a NodePair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NodePair
         * @static
         * @param {lnrpc.NodePair} message NodePair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodePair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.from = "";
                else {
                    object.from = [];
                    if (options.bytes !== $Array)
                        object.from = $util.newBuffer(object.from);
                }
                if (options.bytes === $String)
                    object.to = "";
                else {
                    object.to = [];
                    if (options.bytes !== $Array)
                        object.to = $util.newBuffer(object.to);
                }
            }
            if (message.from != null && $Object.hasOwnProperty.call(message, "from"))
                object.from = options.bytes === $String ? $util.base64.encode(message.from, 0, message.from.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.from) : message.from;
            if (message.to != null && $Object.hasOwnProperty.call(message, "to"))
                object.to = options.bytes === $String ? $util.base64.encode(message.to, 0, message.to.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.to) : message.to;
            return object;
        };

        /**
         * Converts this NodePair to JSON.
         * @function toJSON
         * @memberof lnrpc.NodePair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodePair.prototype.toJSON = function() {
            return NodePair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodePair
         * @function getTypeUrl
         * @memberof lnrpc.NodePair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodePair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NodePair";
        };

        return NodePair;
    })();

    lnrpc.EdgeLocator = (function() {

        /**
         * Properties of an EdgeLocator.
         * @typedef {Object} lnrpc.EdgeLocator.$Properties
         * @property {Long|null} [channel_id] EdgeLocator channel_id
         * @property {boolean|null} [direction_reverse] EdgeLocator direction_reverse
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an EdgeLocator.
         * @memberof lnrpc
         * @interface IEdgeLocator
         * @augments lnrpc.EdgeLocator.$Properties
         * @deprecated Use lnrpc.EdgeLocator.$Properties instead.
         */

        /**
         * Shape of an EdgeLocator.
         * @typedef {lnrpc.EdgeLocator.$Properties} lnrpc.EdgeLocator.$Shape
         */

        /**
         * Constructs a new EdgeLocator.
         * @memberof lnrpc
         * @classdesc Represents an EdgeLocator.
         * @constructor
         * @param {lnrpc.EdgeLocator.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const EdgeLocator = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * EdgeLocator channel_id.
         * @member {Long} channel_id
         * @memberof lnrpc.EdgeLocator
         * @instance
         */
        EdgeLocator.prototype.channel_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * EdgeLocator direction_reverse.
         * @member {boolean} direction_reverse
         * @memberof lnrpc.EdgeLocator
         * @instance
         */
        EdgeLocator.prototype.direction_reverse = false;

        /**
         * Creates a new EdgeLocator instance using the specified properties.
         * @function create
         * @memberof lnrpc.EdgeLocator
         * @static
         * @param {lnrpc.EdgeLocator.$Properties=} [properties] Properties to set
         * @returns {lnrpc.EdgeLocator} EdgeLocator instance
         * @type {{
         *   (properties: lnrpc.EdgeLocator.$Shape): lnrpc.EdgeLocator & lnrpc.EdgeLocator.$Shape;
         *   (properties?: lnrpc.EdgeLocator.$Properties): lnrpc.EdgeLocator;
         * }}
         */
        EdgeLocator.create = function(properties) {
            return new EdgeLocator(properties);
        };

        /**
         * Encodes the specified EdgeLocator message. Does not implicitly {@link lnrpc.EdgeLocator.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.EdgeLocator
         * @static
         * @param {lnrpc.EdgeLocator.$Properties} message EdgeLocator message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EdgeLocator.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channel_id != null && $Object.hasOwnProperty.call(message, "channel_id") && (typeof message.channel_id === "object" ? message.channel_id.low || message.channel_id.high : message.channel_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.channel_id);
            if (message.direction_reverse != null && $Object.hasOwnProperty.call(message, "direction_reverse") && message.direction_reverse !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.direction_reverse);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EdgeLocator message, length delimited. Does not implicitly {@link lnrpc.EdgeLocator.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.EdgeLocator
         * @static
         * @param {lnrpc.EdgeLocator.$Properties} message EdgeLocator message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EdgeLocator.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EdgeLocator message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.EdgeLocator
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.EdgeLocator & lnrpc.EdgeLocator.$Shape} EdgeLocator
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EdgeLocator.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.EdgeLocator(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.channel_id = value;
                        else
                            delete message.channel_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.direction_reverse = value;
                        else
                            delete message.direction_reverse;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EdgeLocator message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.EdgeLocator
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.EdgeLocator & lnrpc.EdgeLocator.$Shape} EdgeLocator
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EdgeLocator.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EdgeLocator message.
         * @function verify
         * @memberof lnrpc.EdgeLocator
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EdgeLocator.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channel_id != null && $Object.hasOwnProperty.call(message, "channel_id"))
                if (!$util.isInteger(message.channel_id) && !(message.channel_id && $util.isInteger(message.channel_id.low) && $util.isInteger(message.channel_id.high)))
                    return "channel_id: integer|Long expected";
            if (message.direction_reverse != null && $Object.hasOwnProperty.call(message, "direction_reverse"))
                if (typeof message.direction_reverse !== "boolean")
                    return "direction_reverse: boolean expected";
            return null;
        };

        /**
         * Creates an EdgeLocator message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.EdgeLocator
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.EdgeLocator} EdgeLocator
         */
        EdgeLocator.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.EdgeLocator)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.EdgeLocator: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.EdgeLocator();
            if (object.channel_id != null)
                if (typeof object.channel_id === "object" ? object.channel_id.low || object.channel_id.high : $Number(object.channel_id) !== 0)
                    if ($util.Long)
                        message.channel_id = $util.Long.fromValue(object.channel_id, true);
                    else if (typeof object.channel_id === "string")
                        message.channel_id = $parseInt(object.channel_id, 10);
                    else if (typeof object.channel_id === "number")
                        message.channel_id = object.channel_id;
                    else if (typeof object.channel_id === "object")
                        message.channel_id = new $util.LongBits(object.channel_id.low >>> 0, object.channel_id.high >>> 0).toNumber(true);
            if (object.direction_reverse != null)
                if (object.direction_reverse)
                    message.direction_reverse = $Boolean(object.direction_reverse);
            return message;
        };

        /**
         * Creates a plain object from an EdgeLocator message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.EdgeLocator
         * @static
         * @param {lnrpc.EdgeLocator} message EdgeLocator
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EdgeLocator.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.channel_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.channel_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.direction_reverse = false;
            }
            if (message.channel_id != null && $Object.hasOwnProperty.call(message, "channel_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.channel_id = typeof message.channel_id === "number" ? $BigInt(message.channel_id) : $util.Long.fromBits(message.channel_id.low >>> 0, message.channel_id.high >>> 0, true).toBigInt();
                else if (typeof message.channel_id === "number")
                    object.channel_id = options.longs === $String ? $String(message.channel_id) : message.channel_id;
                else
                    object.channel_id = options.longs === $String ? $util.Long.prototype.toString.call(message.channel_id) : options.longs === $Number ? new $util.LongBits(message.channel_id.low >>> 0, message.channel_id.high >>> 0).toNumber(true) : message.channel_id;
            if (message.direction_reverse != null && $Object.hasOwnProperty.call(message, "direction_reverse"))
                object.direction_reverse = message.direction_reverse;
            return object;
        };

        /**
         * Converts this EdgeLocator to JSON.
         * @function toJSON
         * @memberof lnrpc.EdgeLocator
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EdgeLocator.prototype.toJSON = function() {
            return EdgeLocator.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EdgeLocator
         * @function getTypeUrl
         * @memberof lnrpc.EdgeLocator
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EdgeLocator.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.EdgeLocator";
        };

        return EdgeLocator;
    })();

    lnrpc.QueryRoutesResponse = (function() {

        /**
         * Properties of a QueryRoutesResponse.
         * @typedef {Object} lnrpc.QueryRoutesResponse.$Properties
         * @property {Array.<lnrpc.Route.$Properties>|null} [routes] QueryRoutesResponse routes
         * @property {number|null} [success_prob] QueryRoutesResponse success_prob
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a QueryRoutesResponse.
         * @memberof lnrpc
         * @interface IQueryRoutesResponse
         * @augments lnrpc.QueryRoutesResponse.$Properties
         * @deprecated Use lnrpc.QueryRoutesResponse.$Properties instead.
         */

        /**
         * Shape of a QueryRoutesResponse.
         * @typedef {lnrpc.QueryRoutesResponse.$Properties} lnrpc.QueryRoutesResponse.$Shape
         */

        /**
         * Constructs a new QueryRoutesResponse.
         * @memberof lnrpc
         * @classdesc Represents a QueryRoutesResponse.
         * @constructor
         * @param {lnrpc.QueryRoutesResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const QueryRoutesResponse = function (properties) {
            this.routes = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * QueryRoutesResponse routes.
         * @member {Array.<lnrpc.Route.$Properties>} routes
         * @memberof lnrpc.QueryRoutesResponse
         * @instance
         */
        QueryRoutesResponse.prototype.routes = $util.emptyArray;

        /**
         * QueryRoutesResponse success_prob.
         * @member {number} success_prob
         * @memberof lnrpc.QueryRoutesResponse
         * @instance
         */
        QueryRoutesResponse.prototype.success_prob = 0;

        /**
         * Creates a new QueryRoutesResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.QueryRoutesResponse
         * @static
         * @param {lnrpc.QueryRoutesResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.QueryRoutesResponse} QueryRoutesResponse instance
         * @type {{
         *   (properties: lnrpc.QueryRoutesResponse.$Shape): lnrpc.QueryRoutesResponse & lnrpc.QueryRoutesResponse.$Shape;
         *   (properties?: lnrpc.QueryRoutesResponse.$Properties): lnrpc.QueryRoutesResponse;
         * }}
         */
        QueryRoutesResponse.create = function(properties) {
            return new QueryRoutesResponse(properties);
        };

        /**
         * Encodes the specified QueryRoutesResponse message. Does not implicitly {@link lnrpc.QueryRoutesResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.QueryRoutesResponse
         * @static
         * @param {lnrpc.QueryRoutesResponse.$Properties} message QueryRoutesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryRoutesResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.routes != null && message.routes.length)
                for (let i = 0; i < message.routes.length; ++i)
                    $root.lnrpc.Route.encode(message.routes[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.success_prob != null && $Object.hasOwnProperty.call(message, "success_prob") && !$Object.is(message.success_prob, 0))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.success_prob);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QueryRoutesResponse message, length delimited. Does not implicitly {@link lnrpc.QueryRoutesResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.QueryRoutesResponse
         * @static
         * @param {lnrpc.QueryRoutesResponse.$Properties} message QueryRoutesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryRoutesResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryRoutesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.QueryRoutesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.QueryRoutesResponse & lnrpc.QueryRoutesResponse.$Shape} QueryRoutesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryRoutesResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.QueryRoutesResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.routes && message.routes.length))
                            message.routes = [];
                        message.routes.push($root.lnrpc.Route.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.success_prob = value;
                        else
                            delete message.success_prob;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QueryRoutesResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.QueryRoutesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.QueryRoutesResponse & lnrpc.QueryRoutesResponse.$Shape} QueryRoutesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryRoutesResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryRoutesResponse message.
         * @function verify
         * @memberof lnrpc.QueryRoutesResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryRoutesResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.routes != null && $Object.hasOwnProperty.call(message, "routes")) {
                if (!$Array.isArray(message.routes))
                    return "routes: array expected";
                for (let i = 0; i < message.routes.length; ++i) {
                    let error = $root.lnrpc.Route.verify(message.routes[i], _depth + 1);
                    if (error)
                        return "routes." + error;
                }
            }
            if (message.success_prob != null && $Object.hasOwnProperty.call(message, "success_prob"))
                if (typeof message.success_prob !== "number")
                    return "success_prob: number expected";
            return null;
        };

        /**
         * Creates a QueryRoutesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.QueryRoutesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.QueryRoutesResponse} QueryRoutesResponse
         */
        QueryRoutesResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.QueryRoutesResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.QueryRoutesResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.QueryRoutesResponse();
            if (object.routes) {
                if (!$Array.isArray(object.routes))
                    throw $TypeError(".lnrpc.QueryRoutesResponse.routes: array expected");
                message.routes = $Array(object.routes.length);
                for (let i = 0; i < object.routes.length; ++i) {
                    if (!$util.isObject(object.routes[i]))
                        throw $TypeError(".lnrpc.QueryRoutesResponse.routes: object expected");
                    message.routes[i] = $root.lnrpc.Route.fromObject(object.routes[i], _depth + 1);
                }
            }
            if (object.success_prob != null)
                if (!$Object.is($Number(object.success_prob), 0))
                    message.success_prob = $Number(object.success_prob);
            return message;
        };

        /**
         * Creates a plain object from a QueryRoutesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.QueryRoutesResponse
         * @static
         * @param {lnrpc.QueryRoutesResponse} message QueryRoutesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryRoutesResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.routes = [];
            if (options.defaults)
                object.success_prob = 0;
            if (message.routes && message.routes.length) {
                object.routes = $Array(message.routes.length);
                for (let j = 0; j < message.routes.length; ++j)
                    object.routes[j] = $root.lnrpc.Route.toObject(message.routes[j], options, _depth + 1);
            }
            if (message.success_prob != null && $Object.hasOwnProperty.call(message, "success_prob"))
                object.success_prob = options.json && !$isFinite(message.success_prob) ? $String(message.success_prob) : message.success_prob;
            return object;
        };

        /**
         * Converts this QueryRoutesResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.QueryRoutesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryRoutesResponse.prototype.toJSON = function() {
            return QueryRoutesResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QueryRoutesResponse
         * @function getTypeUrl
         * @memberof lnrpc.QueryRoutesResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QueryRoutesResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.QueryRoutesResponse";
        };

        return QueryRoutesResponse;
    })();

    lnrpc.Hop = (function() {

        /**
         * Properties of a Hop.
         * @typedef {Object} lnrpc.Hop.$Properties
         * @property {Long|null} [chan_id] Hop chan_id
         * @property {Long|null} [chan_capacity] Hop chan_capacity
         * @property {Long|null} [amt_to_forward] Hop amt_to_forward
         * @property {Long|null} [fee] Hop fee
         * @property {number|null} [expiry] Hop expiry
         * @property {Long|null} [amt_to_forward_msat] Hop amt_to_forward_msat
         * @property {Long|null} [fee_msat] Hop fee_msat
         * @property {string|null} [pub_key] Hop pub_key
         * @property {boolean|null} [tlv_payload] Hop tlv_payload
         * @property {lnrpc.MPPRecord.$Properties|null} [mpp_record] Hop mpp_record
         * @property {lnrpc.AMPRecord.$Properties|null} [amp_record] Hop amp_record
         * @property {Object.<string,Uint8Array>|null} [custom_records] Hop custom_records
         * @property {Uint8Array|null} [metadata] Hop metadata
         * @property {Uint8Array|null} [blinding_point] Hop blinding_point
         * @property {Uint8Array|null} [encrypted_data] Hop encrypted_data
         * @property {Long|null} [total_amt_msat] Hop total_amt_msat
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Hop.
         * @memberof lnrpc
         * @interface IHop
         * @augments lnrpc.Hop.$Properties
         * @deprecated Use lnrpc.Hop.$Properties instead.
         */

        /**
         * Shape of a Hop.
         * @typedef {lnrpc.Hop.$Properties} lnrpc.Hop.$Shape
         */

        /**
         * Constructs a new Hop.
         * @memberof lnrpc
         * @classdesc Represents a Hop.
         * @constructor
         * @param {lnrpc.Hop.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Hop = function (properties) {
            this.custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Hop chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Hop chan_capacity.
         * @member {Long} chan_capacity
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.chan_capacity = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Hop amt_to_forward.
         * @member {Long} amt_to_forward
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.amt_to_forward = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Hop fee.
         * @member {Long} fee
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.fee = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Hop expiry.
         * @member {number} expiry
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.expiry = 0;

        /**
         * Hop amt_to_forward_msat.
         * @member {Long} amt_to_forward_msat
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.amt_to_forward_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Hop fee_msat.
         * @member {Long} fee_msat
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.fee_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Hop pub_key.
         * @member {string} pub_key
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.pub_key = "";

        /**
         * Hop tlv_payload.
         * @member {boolean} tlv_payload
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.tlv_payload = false;

        /**
         * Hop mpp_record.
         * @member {lnrpc.MPPRecord.$Properties|null|undefined} mpp_record
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.mpp_record = null;

        /**
         * Hop amp_record.
         * @member {lnrpc.AMPRecord.$Properties|null|undefined} amp_record
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.amp_record = null;

        /**
         * Hop custom_records.
         * @member {Object.<string,Uint8Array>} custom_records
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.custom_records = $util.emptyObject;

        /**
         * Hop metadata.
         * @member {Uint8Array} metadata
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.metadata = $util.newBuffer([]);

        /**
         * Hop blinding_point.
         * @member {Uint8Array} blinding_point
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.blinding_point = $util.newBuffer([]);

        /**
         * Hop encrypted_data.
         * @member {Uint8Array} encrypted_data
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.encrypted_data = $util.newBuffer([]);

        /**
         * Hop total_amt_msat.
         * @member {Long} total_amt_msat
         * @memberof lnrpc.Hop
         * @instance
         */
        Hop.prototype.total_amt_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new Hop instance using the specified properties.
         * @function create
         * @memberof lnrpc.Hop
         * @static
         * @param {lnrpc.Hop.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Hop} Hop instance
         * @type {{
         *   (properties: lnrpc.Hop.$Shape): lnrpc.Hop & lnrpc.Hop.$Shape;
         *   (properties?: lnrpc.Hop.$Properties): lnrpc.Hop;
         * }}
         */
        Hop.create = function(properties) {
            return new Hop(properties);
        };

        /**
         * Encodes the specified Hop message. Does not implicitly {@link lnrpc.Hop.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Hop
         * @static
         * @param {lnrpc.Hop.$Properties} message Hop message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Hop.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.chan_id);
            if (message.chan_capacity != null && $Object.hasOwnProperty.call(message, "chan_capacity") && (typeof message.chan_capacity === "object" ? message.chan_capacity.low || message.chan_capacity.high : message.chan_capacity !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.chan_capacity);
            if (message.amt_to_forward != null && $Object.hasOwnProperty.call(message, "amt_to_forward") && (typeof message.amt_to_forward === "object" ? message.amt_to_forward.low || message.amt_to_forward.high : message.amt_to_forward !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.amt_to_forward);
            if (message.fee != null && $Object.hasOwnProperty.call(message, "fee") && (typeof message.fee === "object" ? message.fee.low || message.fee.high : message.fee !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.fee);
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry") && message.expiry !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.expiry);
            if (message.amt_to_forward_msat != null && $Object.hasOwnProperty.call(message, "amt_to_forward_msat") && (typeof message.amt_to_forward_msat === "object" ? message.amt_to_forward_msat.low || message.amt_to_forward_msat.high : message.amt_to_forward_msat !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.amt_to_forward_msat);
            if (message.fee_msat != null && $Object.hasOwnProperty.call(message, "fee_msat") && (typeof message.fee_msat === "object" ? message.fee_msat.low || message.fee_msat.high : message.fee_msat !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.fee_msat);
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key") && message.pub_key !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.pub_key);
            if (message.tlv_payload != null && $Object.hasOwnProperty.call(message, "tlv_payload") && message.tlv_payload !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.tlv_payload);
            if (message.mpp_record != null && $Object.hasOwnProperty.call(message, "mpp_record"))
                $root.lnrpc.MPPRecord.encode(message.mpp_record, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records"))
                for (let keys = $Object.keys(message.custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 11, wireType 2 =*/90).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.custom_records[keys[i]]).ldelim();
            if (message.amp_record != null && $Object.hasOwnProperty.call(message, "amp_record"))
                $root.lnrpc.AMPRecord.encode(message.amp_record, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata") && message.metadata.length)
                writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.metadata);
            if (message.blinding_point != null && $Object.hasOwnProperty.call(message, "blinding_point") && message.blinding_point.length)
                writer.uint32(/* id 14, wireType 2 =*/114).bytes(message.blinding_point);
            if (message.encrypted_data != null && $Object.hasOwnProperty.call(message, "encrypted_data") && message.encrypted_data.length)
                writer.uint32(/* id 15, wireType 2 =*/122).bytes(message.encrypted_data);
            if (message.total_amt_msat != null && $Object.hasOwnProperty.call(message, "total_amt_msat") && (typeof message.total_amt_msat === "object" ? message.total_amt_msat.low || message.total_amt_msat.high : message.total_amt_msat !== 0))
                writer.uint32(/* id 16, wireType 0 =*/128).uint64(message.total_amt_msat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Hop message, length delimited. Does not implicitly {@link lnrpc.Hop.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Hop
         * @static
         * @param {lnrpc.Hop.$Properties} message Hop message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Hop.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Hop message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Hop
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Hop & lnrpc.Hop.$Shape} Hop
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Hop.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Hop(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_capacity = value;
                        else
                            delete message.chan_capacity;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_to_forward = value;
                        else
                            delete message.amt_to_forward;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee = value;
                        else
                            delete message.fee;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.expiry = value;
                        else
                            delete message.expiry;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_to_forward_msat = value;
                        else
                            delete message.amt_to_forward_msat;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_msat = value;
                        else
                            delete message.fee_msat;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pub_key = value;
                        else
                            delete message.pub_key;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.tlv_payload = value;
                        else
                            delete message.tlv_payload;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.mpp_record = $root.lnrpc.MPPRecord.decode(reader, reader.uint32(), $undefined, _depth + 1, message.mpp_record);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.amp_record = $root.lnrpc.AMPRecord.decode(reader, reader.uint32(), $undefined, _depth + 1, message.amp_record);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (message.custom_records === $util.emptyObject)
                            message.custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.metadata = value;
                        else
                            delete message.metadata;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.blinding_point = value;
                        else
                            delete message.blinding_point;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.encrypted_data = value;
                        else
                            delete message.encrypted_data;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_amt_msat = value;
                        else
                            delete message.total_amt_msat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Hop message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Hop
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Hop & lnrpc.Hop.$Shape} Hop
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Hop.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Hop message.
         * @function verify
         * @memberof lnrpc.Hop
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Hop.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.chan_capacity != null && $Object.hasOwnProperty.call(message, "chan_capacity"))
                if (!$util.isInteger(message.chan_capacity) && !(message.chan_capacity && $util.isInteger(message.chan_capacity.low) && $util.isInteger(message.chan_capacity.high)))
                    return "chan_capacity: integer|Long expected";
            if (message.amt_to_forward != null && $Object.hasOwnProperty.call(message, "amt_to_forward"))
                if (!$util.isInteger(message.amt_to_forward) && !(message.amt_to_forward && $util.isInteger(message.amt_to_forward.low) && $util.isInteger(message.amt_to_forward.high)))
                    return "amt_to_forward: integer|Long expected";
            if (message.fee != null && $Object.hasOwnProperty.call(message, "fee"))
                if (!$util.isInteger(message.fee) && !(message.fee && $util.isInteger(message.fee.low) && $util.isInteger(message.fee.high)))
                    return "fee: integer|Long expected";
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                if (!$util.isInteger(message.expiry))
                    return "expiry: integer expected";
            if (message.amt_to_forward_msat != null && $Object.hasOwnProperty.call(message, "amt_to_forward_msat"))
                if (!$util.isInteger(message.amt_to_forward_msat) && !(message.amt_to_forward_msat && $util.isInteger(message.amt_to_forward_msat.low) && $util.isInteger(message.amt_to_forward_msat.high)))
                    return "amt_to_forward_msat: integer|Long expected";
            if (message.fee_msat != null && $Object.hasOwnProperty.call(message, "fee_msat"))
                if (!$util.isInteger(message.fee_msat) && !(message.fee_msat && $util.isInteger(message.fee_msat.low) && $util.isInteger(message.fee_msat.high)))
                    return "fee_msat: integer|Long expected";
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                if (!$util.isString(message.pub_key))
                    return "pub_key: string expected";
            if (message.tlv_payload != null && $Object.hasOwnProperty.call(message, "tlv_payload"))
                if (typeof message.tlv_payload !== "boolean")
                    return "tlv_payload: boolean expected";
            if (message.mpp_record != null && $Object.hasOwnProperty.call(message, "mpp_record")) {
                let error = $root.lnrpc.MPPRecord.verify(message.mpp_record, _depth + 1);
                if (error)
                    return "mpp_record." + error;
            }
            if (message.amp_record != null && $Object.hasOwnProperty.call(message, "amp_record")) {
                let error = $root.lnrpc.AMPRecord.verify(message.amp_record, _depth + 1);
                if (error)
                    return "amp_record." + error;
            }
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records")) {
                if (!$util.isObject(message.custom_records))
                    return "custom_records: object expected";
                let key = $Object.keys(message.custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.custom_records[key[i]] && typeof message.custom_records[key[i]].length === "number" || $util.isString(message.custom_records[key[i]])))
                        return "custom_records: buffer{k:uint64} expected";
                }
            }
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                if (!(message.metadata && typeof message.metadata.length === "number" || $util.isString(message.metadata)))
                    return "metadata: buffer expected";
            if (message.blinding_point != null && $Object.hasOwnProperty.call(message, "blinding_point"))
                if (!(message.blinding_point && typeof message.blinding_point.length === "number" || $util.isString(message.blinding_point)))
                    return "blinding_point: buffer expected";
            if (message.encrypted_data != null && $Object.hasOwnProperty.call(message, "encrypted_data"))
                if (!(message.encrypted_data && typeof message.encrypted_data.length === "number" || $util.isString(message.encrypted_data)))
                    return "encrypted_data: buffer expected";
            if (message.total_amt_msat != null && $Object.hasOwnProperty.call(message, "total_amt_msat"))
                if (!$util.isInteger(message.total_amt_msat) && !(message.total_amt_msat && $util.isInteger(message.total_amt_msat.low) && $util.isInteger(message.total_amt_msat.high)))
                    return "total_amt_msat: integer|Long expected";
            return null;
        };

        /**
         * Creates a Hop message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Hop
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Hop} Hop
         */
        Hop.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Hop)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Hop: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Hop();
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.chan_capacity != null)
                if (typeof object.chan_capacity === "object" ? object.chan_capacity.low || object.chan_capacity.high : $Number(object.chan_capacity) !== 0)
                    if ($util.Long)
                        message.chan_capacity = $util.Long.fromValue(object.chan_capacity, false);
                    else if (typeof object.chan_capacity === "string")
                        message.chan_capacity = $parseInt(object.chan_capacity, 10);
                    else if (typeof object.chan_capacity === "number")
                        message.chan_capacity = object.chan_capacity;
                    else if (typeof object.chan_capacity === "object")
                        message.chan_capacity = new $util.LongBits(object.chan_capacity.low >>> 0, object.chan_capacity.high >>> 0).toNumber();
            if (object.amt_to_forward != null)
                if (typeof object.amt_to_forward === "object" ? object.amt_to_forward.low || object.amt_to_forward.high : $Number(object.amt_to_forward) !== 0)
                    if ($util.Long)
                        message.amt_to_forward = $util.Long.fromValue(object.amt_to_forward, false);
                    else if (typeof object.amt_to_forward === "string")
                        message.amt_to_forward = $parseInt(object.amt_to_forward, 10);
                    else if (typeof object.amt_to_forward === "number")
                        message.amt_to_forward = object.amt_to_forward;
                    else if (typeof object.amt_to_forward === "object")
                        message.amt_to_forward = new $util.LongBits(object.amt_to_forward.low >>> 0, object.amt_to_forward.high >>> 0).toNumber();
            if (object.fee != null)
                if (typeof object.fee === "object" ? object.fee.low || object.fee.high : $Number(object.fee) !== 0)
                    if ($util.Long)
                        message.fee = $util.Long.fromValue(object.fee, false);
                    else if (typeof object.fee === "string")
                        message.fee = $parseInt(object.fee, 10);
                    else if (typeof object.fee === "number")
                        message.fee = object.fee;
                    else if (typeof object.fee === "object")
                        message.fee = new $util.LongBits(object.fee.low >>> 0, object.fee.high >>> 0).toNumber();
            if (object.expiry != null)
                if ($Number(object.expiry) !== 0)
                    message.expiry = object.expiry >>> 0;
            if (object.amt_to_forward_msat != null)
                if (typeof object.amt_to_forward_msat === "object" ? object.amt_to_forward_msat.low || object.amt_to_forward_msat.high : $Number(object.amt_to_forward_msat) !== 0)
                    if ($util.Long)
                        message.amt_to_forward_msat = $util.Long.fromValue(object.amt_to_forward_msat, false);
                    else if (typeof object.amt_to_forward_msat === "string")
                        message.amt_to_forward_msat = $parseInt(object.amt_to_forward_msat, 10);
                    else if (typeof object.amt_to_forward_msat === "number")
                        message.amt_to_forward_msat = object.amt_to_forward_msat;
                    else if (typeof object.amt_to_forward_msat === "object")
                        message.amt_to_forward_msat = new $util.LongBits(object.amt_to_forward_msat.low >>> 0, object.amt_to_forward_msat.high >>> 0).toNumber();
            if (object.fee_msat != null)
                if (typeof object.fee_msat === "object" ? object.fee_msat.low || object.fee_msat.high : $Number(object.fee_msat) !== 0)
                    if ($util.Long)
                        message.fee_msat = $util.Long.fromValue(object.fee_msat, false);
                    else if (typeof object.fee_msat === "string")
                        message.fee_msat = $parseInt(object.fee_msat, 10);
                    else if (typeof object.fee_msat === "number")
                        message.fee_msat = object.fee_msat;
                    else if (typeof object.fee_msat === "object")
                        message.fee_msat = new $util.LongBits(object.fee_msat.low >>> 0, object.fee_msat.high >>> 0).toNumber();
            if (object.pub_key != null)
                if (typeof object.pub_key !== "string" || object.pub_key.length)
                    message.pub_key = $String(object.pub_key);
            if (object.tlv_payload != null)
                if (object.tlv_payload)
                    message.tlv_payload = $Boolean(object.tlv_payload);
            if (object.mpp_record != null) {
                if (!$util.isObject(object.mpp_record))
                    throw $TypeError(".lnrpc.Hop.mpp_record: object expected");
                message.mpp_record = $root.lnrpc.MPPRecord.fromObject(object.mpp_record, _depth + 1);
            }
            if (object.amp_record != null) {
                if (!$util.isObject(object.amp_record))
                    throw $TypeError(".lnrpc.Hop.amp_record: object expected");
                message.amp_record = $root.lnrpc.AMPRecord.fromObject(object.amp_record, _depth + 1);
            }
            if (object.custom_records) {
                if (!$util.isObject(object.custom_records))
                    throw $TypeError(".lnrpc.Hop.custom_records: object expected");
                message.custom_records = {};
                for (let keys = $Object.keys(object.custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.custom_records, keys[i]);
                    if (typeof object.custom_records[keys[i]] === "string")
                        $util.base64.decode(object.custom_records[keys[i]], message.custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.custom_records[keys[i]])), 0);
                    else if (object.custom_records[keys[i]].length >= 0)
                        message.custom_records[keys[i]] = object.custom_records[keys[i]];
                }
            }
            if (object.metadata != null)
                if (object.metadata.length)
                    if (typeof object.metadata === "string")
                        $util.base64.decode(object.metadata, message.metadata = $util.newBuffer($util.base64.length(object.metadata)), 0);
                    else if (object.metadata.length >= 0)
                        message.metadata = object.metadata;
            if (object.blinding_point != null)
                if (object.blinding_point.length)
                    if (typeof object.blinding_point === "string")
                        $util.base64.decode(object.blinding_point, message.blinding_point = $util.newBuffer($util.base64.length(object.blinding_point)), 0);
                    else if (object.blinding_point.length >= 0)
                        message.blinding_point = object.blinding_point;
            if (object.encrypted_data != null)
                if (object.encrypted_data.length)
                    if (typeof object.encrypted_data === "string")
                        $util.base64.decode(object.encrypted_data, message.encrypted_data = $util.newBuffer($util.base64.length(object.encrypted_data)), 0);
                    else if (object.encrypted_data.length >= 0)
                        message.encrypted_data = object.encrypted_data;
            if (object.total_amt_msat != null)
                if (typeof object.total_amt_msat === "object" ? object.total_amt_msat.low || object.total_amt_msat.high : $Number(object.total_amt_msat) !== 0)
                    if ($util.Long)
                        message.total_amt_msat = $util.Long.fromValue(object.total_amt_msat, true);
                    else if (typeof object.total_amt_msat === "string")
                        message.total_amt_msat = $parseInt(object.total_amt_msat, 10);
                    else if (typeof object.total_amt_msat === "number")
                        message.total_amt_msat = object.total_amt_msat;
                    else if (typeof object.total_amt_msat === "object")
                        message.total_amt_msat = new $util.LongBits(object.total_amt_msat.low >>> 0, object.total_amt_msat.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a Hop message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Hop
         * @static
         * @param {lnrpc.Hop} message Hop
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Hop.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.custom_records = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.chan_capacity = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_capacity = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_to_forward = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_to_forward = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.expiry = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_to_forward_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_to_forward_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.pub_key = "";
                object.tlv_payload = false;
                object.mpp_record = null;
                object.amp_record = null;
                if (options.bytes === $String)
                    object.metadata = "";
                else {
                    object.metadata = [];
                    if (options.bytes !== $Array)
                        object.metadata = $util.newBuffer(object.metadata);
                }
                if (options.bytes === $String)
                    object.blinding_point = "";
                else {
                    object.blinding_point = [];
                    if (options.bytes !== $Array)
                        object.blinding_point = $util.newBuffer(object.blinding_point);
                }
                if (options.bytes === $String)
                    object.encrypted_data = "";
                else {
                    object.encrypted_data = [];
                    if (options.bytes !== $Array)
                        object.encrypted_data = $util.newBuffer(object.encrypted_data);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.total_amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.chan_capacity != null && $Object.hasOwnProperty.call(message, "chan_capacity"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_capacity = typeof message.chan_capacity === "number" ? $BigInt(message.chan_capacity) : $util.Long.fromBits(message.chan_capacity.low >>> 0, message.chan_capacity.high >>> 0, false).toBigInt();
                else if (typeof message.chan_capacity === "number")
                    object.chan_capacity = options.longs === $String ? $String(message.chan_capacity) : message.chan_capacity;
                else
                    object.chan_capacity = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_capacity) : options.longs === $Number ? new $util.LongBits(message.chan_capacity.low >>> 0, message.chan_capacity.high >>> 0).toNumber() : message.chan_capacity;
            if (message.amt_to_forward != null && $Object.hasOwnProperty.call(message, "amt_to_forward"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_to_forward = typeof message.amt_to_forward === "number" ? $BigInt(message.amt_to_forward) : $util.Long.fromBits(message.amt_to_forward.low >>> 0, message.amt_to_forward.high >>> 0, false).toBigInt();
                else if (typeof message.amt_to_forward === "number")
                    object.amt_to_forward = options.longs === $String ? $String(message.amt_to_forward) : message.amt_to_forward;
                else
                    object.amt_to_forward = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_to_forward) : options.longs === $Number ? new $util.LongBits(message.amt_to_forward.low >>> 0, message.amt_to_forward.high >>> 0).toNumber() : message.amt_to_forward;
            if (message.fee != null && $Object.hasOwnProperty.call(message, "fee"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee = typeof message.fee === "number" ? $BigInt(message.fee) : $util.Long.fromBits(message.fee.low >>> 0, message.fee.high >>> 0, false).toBigInt();
                else if (typeof message.fee === "number")
                    object.fee = options.longs === $String ? $String(message.fee) : message.fee;
                else
                    object.fee = options.longs === $String ? $util.Long.prototype.toString.call(message.fee) : options.longs === $Number ? new $util.LongBits(message.fee.low >>> 0, message.fee.high >>> 0).toNumber() : message.fee;
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                object.expiry = message.expiry;
            if (message.amt_to_forward_msat != null && $Object.hasOwnProperty.call(message, "amt_to_forward_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_to_forward_msat = typeof message.amt_to_forward_msat === "number" ? $BigInt(message.amt_to_forward_msat) : $util.Long.fromBits(message.amt_to_forward_msat.low >>> 0, message.amt_to_forward_msat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_to_forward_msat === "number")
                    object.amt_to_forward_msat = options.longs === $String ? $String(message.amt_to_forward_msat) : message.amt_to_forward_msat;
                else
                    object.amt_to_forward_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_to_forward_msat) : options.longs === $Number ? new $util.LongBits(message.amt_to_forward_msat.low >>> 0, message.amt_to_forward_msat.high >>> 0).toNumber() : message.amt_to_forward_msat;
            if (message.fee_msat != null && $Object.hasOwnProperty.call(message, "fee_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_msat = typeof message.fee_msat === "number" ? $BigInt(message.fee_msat) : $util.Long.fromBits(message.fee_msat.low >>> 0, message.fee_msat.high >>> 0, false).toBigInt();
                else if (typeof message.fee_msat === "number")
                    object.fee_msat = options.longs === $String ? $String(message.fee_msat) : message.fee_msat;
                else
                    object.fee_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_msat) : options.longs === $Number ? new $util.LongBits(message.fee_msat.low >>> 0, message.fee_msat.high >>> 0).toNumber() : message.fee_msat;
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                object.pub_key = message.pub_key;
            if (message.tlv_payload != null && $Object.hasOwnProperty.call(message, "tlv_payload"))
                object.tlv_payload = message.tlv_payload;
            if (message.mpp_record != null && $Object.hasOwnProperty.call(message, "mpp_record"))
                object.mpp_record = $root.lnrpc.MPPRecord.toObject(message.mpp_record, options, _depth + 1);
            let keys2;
            if (message.custom_records && (keys2 = $Object.keys(message.custom_records)).length) {
                object.custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.custom_records, keys2[j]);
                    object.custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.custom_records[keys2[j]], 0, message.custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_records[keys2[j]]) : message.custom_records[keys2[j]];
                }
            }
            if (message.amp_record != null && $Object.hasOwnProperty.call(message, "amp_record"))
                object.amp_record = $root.lnrpc.AMPRecord.toObject(message.amp_record, options, _depth + 1);
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = options.bytes === $String ? $util.base64.encode(message.metadata, 0, message.metadata.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.metadata) : message.metadata;
            if (message.blinding_point != null && $Object.hasOwnProperty.call(message, "blinding_point"))
                object.blinding_point = options.bytes === $String ? $util.base64.encode(message.blinding_point, 0, message.blinding_point.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.blinding_point) : message.blinding_point;
            if (message.encrypted_data != null && $Object.hasOwnProperty.call(message, "encrypted_data"))
                object.encrypted_data = options.bytes === $String ? $util.base64.encode(message.encrypted_data, 0, message.encrypted_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.encrypted_data) : message.encrypted_data;
            if (message.total_amt_msat != null && $Object.hasOwnProperty.call(message, "total_amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_amt_msat = typeof message.total_amt_msat === "number" ? $BigInt(message.total_amt_msat) : $util.Long.fromBits(message.total_amt_msat.low >>> 0, message.total_amt_msat.high >>> 0, true).toBigInt();
                else if (typeof message.total_amt_msat === "number")
                    object.total_amt_msat = options.longs === $String ? $String(message.total_amt_msat) : message.total_amt_msat;
                else
                    object.total_amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.total_amt_msat) : options.longs === $Number ? new $util.LongBits(message.total_amt_msat.low >>> 0, message.total_amt_msat.high >>> 0).toNumber(true) : message.total_amt_msat;
            return object;
        };

        /**
         * Converts this Hop to JSON.
         * @function toJSON
         * @memberof lnrpc.Hop
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Hop.prototype.toJSON = function() {
            return Hop.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Hop
         * @function getTypeUrl
         * @memberof lnrpc.Hop
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Hop.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Hop";
        };

        return Hop;
    })();

    lnrpc.MPPRecord = (function() {

        /**
         * Properties of a MPPRecord.
         * @typedef {Object} lnrpc.MPPRecord.$Properties
         * @property {Uint8Array|null} [payment_addr] MPPRecord payment_addr
         * @property {Long|null} [total_amt_msat] MPPRecord total_amt_msat
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MPPRecord.
         * @memberof lnrpc
         * @interface IMPPRecord
         * @augments lnrpc.MPPRecord.$Properties
         * @deprecated Use lnrpc.MPPRecord.$Properties instead.
         */

        /**
         * Shape of a MPPRecord.
         * @typedef {lnrpc.MPPRecord.$Properties} lnrpc.MPPRecord.$Shape
         */

        /**
         * Constructs a new MPPRecord.
         * @memberof lnrpc
         * @classdesc Represents a MPPRecord.
         * @constructor
         * @param {lnrpc.MPPRecord.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MPPRecord = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MPPRecord payment_addr.
         * @member {Uint8Array} payment_addr
         * @memberof lnrpc.MPPRecord
         * @instance
         */
        MPPRecord.prototype.payment_addr = $util.newBuffer([]);

        /**
         * MPPRecord total_amt_msat.
         * @member {Long} total_amt_msat
         * @memberof lnrpc.MPPRecord
         * @instance
         */
        MPPRecord.prototype.total_amt_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new MPPRecord instance using the specified properties.
         * @function create
         * @memberof lnrpc.MPPRecord
         * @static
         * @param {lnrpc.MPPRecord.$Properties=} [properties] Properties to set
         * @returns {lnrpc.MPPRecord} MPPRecord instance
         * @type {{
         *   (properties: lnrpc.MPPRecord.$Shape): lnrpc.MPPRecord & lnrpc.MPPRecord.$Shape;
         *   (properties?: lnrpc.MPPRecord.$Properties): lnrpc.MPPRecord;
         * }}
         */
        MPPRecord.create = function(properties) {
            return new MPPRecord(properties);
        };

        /**
         * Encodes the specified MPPRecord message. Does not implicitly {@link lnrpc.MPPRecord.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.MPPRecord
         * @static
         * @param {lnrpc.MPPRecord.$Properties} message MPPRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MPPRecord.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.total_amt_msat != null && $Object.hasOwnProperty.call(message, "total_amt_msat") && (typeof message.total_amt_msat === "object" ? message.total_amt_msat.low || message.total_amt_msat.high : message.total_amt_msat !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.total_amt_msat);
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr") && message.payment_addr.length)
                writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.payment_addr);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MPPRecord message, length delimited. Does not implicitly {@link lnrpc.MPPRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.MPPRecord
         * @static
         * @param {lnrpc.MPPRecord.$Properties} message MPPRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MPPRecord.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MPPRecord message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.MPPRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.MPPRecord & lnrpc.MPPRecord.$Shape} MPPRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MPPRecord.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.MPPRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_addr = value;
                        else
                            delete message.payment_addr;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_amt_msat = value;
                        else
                            delete message.total_amt_msat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MPPRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.MPPRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.MPPRecord & lnrpc.MPPRecord.$Shape} MPPRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MPPRecord.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MPPRecord message.
         * @function verify
         * @memberof lnrpc.MPPRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MPPRecord.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                if (!(message.payment_addr && typeof message.payment_addr.length === "number" || $util.isString(message.payment_addr)))
                    return "payment_addr: buffer expected";
            if (message.total_amt_msat != null && $Object.hasOwnProperty.call(message, "total_amt_msat"))
                if (!$util.isInteger(message.total_amt_msat) && !(message.total_amt_msat && $util.isInteger(message.total_amt_msat.low) && $util.isInteger(message.total_amt_msat.high)))
                    return "total_amt_msat: integer|Long expected";
            return null;
        };

        /**
         * Creates a MPPRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.MPPRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.MPPRecord} MPPRecord
         */
        MPPRecord.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.MPPRecord)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.MPPRecord: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.MPPRecord();
            if (object.payment_addr != null)
                if (object.payment_addr.length)
                    if (typeof object.payment_addr === "string")
                        $util.base64.decode(object.payment_addr, message.payment_addr = $util.newBuffer($util.base64.length(object.payment_addr)), 0);
                    else if (object.payment_addr.length >= 0)
                        message.payment_addr = object.payment_addr;
            if (object.total_amt_msat != null)
                if (typeof object.total_amt_msat === "object" ? object.total_amt_msat.low || object.total_amt_msat.high : $Number(object.total_amt_msat) !== 0)
                    if ($util.Long)
                        message.total_amt_msat = $util.Long.fromValue(object.total_amt_msat, false);
                    else if (typeof object.total_amt_msat === "string")
                        message.total_amt_msat = $parseInt(object.total_amt_msat, 10);
                    else if (typeof object.total_amt_msat === "number")
                        message.total_amt_msat = object.total_amt_msat;
                    else if (typeof object.total_amt_msat === "object")
                        message.total_amt_msat = new $util.LongBits(object.total_amt_msat.low >>> 0, object.total_amt_msat.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a MPPRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.MPPRecord
         * @static
         * @param {lnrpc.MPPRecord} message MPPRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MPPRecord.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.payment_addr = "";
                else {
                    object.payment_addr = [];
                    if (options.bytes !== $Array)
                        object.payment_addr = $util.newBuffer(object.payment_addr);
                }
            }
            if (message.total_amt_msat != null && $Object.hasOwnProperty.call(message, "total_amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_amt_msat = typeof message.total_amt_msat === "number" ? $BigInt(message.total_amt_msat) : $util.Long.fromBits(message.total_amt_msat.low >>> 0, message.total_amt_msat.high >>> 0, false).toBigInt();
                else if (typeof message.total_amt_msat === "number")
                    object.total_amt_msat = options.longs === $String ? $String(message.total_amt_msat) : message.total_amt_msat;
                else
                    object.total_amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.total_amt_msat) : options.longs === $Number ? new $util.LongBits(message.total_amt_msat.low >>> 0, message.total_amt_msat.high >>> 0).toNumber() : message.total_amt_msat;
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                object.payment_addr = options.bytes === $String ? $util.base64.encode(message.payment_addr, 0, message.payment_addr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_addr) : message.payment_addr;
            return object;
        };

        /**
         * Converts this MPPRecord to JSON.
         * @function toJSON
         * @memberof lnrpc.MPPRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MPPRecord.prototype.toJSON = function() {
            return MPPRecord.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MPPRecord
         * @function getTypeUrl
         * @memberof lnrpc.MPPRecord
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MPPRecord.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.MPPRecord";
        };

        return MPPRecord;
    })();

    lnrpc.AMPRecord = (function() {

        /**
         * Properties of a AMPRecord.
         * @typedef {Object} lnrpc.AMPRecord.$Properties
         * @property {Uint8Array|null} [root_share] AMPRecord root_share
         * @property {Uint8Array|null} [set_id] AMPRecord set_id
         * @property {number|null} [child_index] AMPRecord child_index
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a AMPRecord.
         * @memberof lnrpc
         * @interface IAMPRecord
         * @augments lnrpc.AMPRecord.$Properties
         * @deprecated Use lnrpc.AMPRecord.$Properties instead.
         */

        /**
         * Shape of a AMPRecord.
         * @typedef {lnrpc.AMPRecord.$Properties} lnrpc.AMPRecord.$Shape
         */

        /**
         * Constructs a new AMPRecord.
         * @memberof lnrpc
         * @classdesc Represents a AMPRecord.
         * @constructor
         * @param {lnrpc.AMPRecord.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AMPRecord = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AMPRecord root_share.
         * @member {Uint8Array} root_share
         * @memberof lnrpc.AMPRecord
         * @instance
         */
        AMPRecord.prototype.root_share = $util.newBuffer([]);

        /**
         * AMPRecord set_id.
         * @member {Uint8Array} set_id
         * @memberof lnrpc.AMPRecord
         * @instance
         */
        AMPRecord.prototype.set_id = $util.newBuffer([]);

        /**
         * AMPRecord child_index.
         * @member {number} child_index
         * @memberof lnrpc.AMPRecord
         * @instance
         */
        AMPRecord.prototype.child_index = 0;

        /**
         * Creates a new AMPRecord instance using the specified properties.
         * @function create
         * @memberof lnrpc.AMPRecord
         * @static
         * @param {lnrpc.AMPRecord.$Properties=} [properties] Properties to set
         * @returns {lnrpc.AMPRecord} AMPRecord instance
         * @type {{
         *   (properties: lnrpc.AMPRecord.$Shape): lnrpc.AMPRecord & lnrpc.AMPRecord.$Shape;
         *   (properties?: lnrpc.AMPRecord.$Properties): lnrpc.AMPRecord;
         * }}
         */
        AMPRecord.create = function(properties) {
            return new AMPRecord(properties);
        };

        /**
         * Encodes the specified AMPRecord message. Does not implicitly {@link lnrpc.AMPRecord.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.AMPRecord
         * @static
         * @param {lnrpc.AMPRecord.$Properties} message AMPRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AMPRecord.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.root_share != null && $Object.hasOwnProperty.call(message, "root_share") && message.root_share.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.root_share);
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id") && message.set_id.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.set_id);
            if (message.child_index != null && $Object.hasOwnProperty.call(message, "child_index") && message.child_index !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.child_index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AMPRecord message, length delimited. Does not implicitly {@link lnrpc.AMPRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.AMPRecord
         * @static
         * @param {lnrpc.AMPRecord.$Properties} message AMPRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AMPRecord.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AMPRecord message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.AMPRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.AMPRecord & lnrpc.AMPRecord.$Shape} AMPRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AMPRecord.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.AMPRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.root_share = value;
                        else
                            delete message.root_share;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.set_id = value;
                        else
                            delete message.set_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.child_index = value;
                        else
                            delete message.child_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a AMPRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.AMPRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.AMPRecord & lnrpc.AMPRecord.$Shape} AMPRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AMPRecord.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AMPRecord message.
         * @function verify
         * @memberof lnrpc.AMPRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AMPRecord.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.root_share != null && $Object.hasOwnProperty.call(message, "root_share"))
                if (!(message.root_share && typeof message.root_share.length === "number" || $util.isString(message.root_share)))
                    return "root_share: buffer expected";
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id"))
                if (!(message.set_id && typeof message.set_id.length === "number" || $util.isString(message.set_id)))
                    return "set_id: buffer expected";
            if (message.child_index != null && $Object.hasOwnProperty.call(message, "child_index"))
                if (!$util.isInteger(message.child_index))
                    return "child_index: integer expected";
            return null;
        };

        /**
         * Creates a AMPRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.AMPRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.AMPRecord} AMPRecord
         */
        AMPRecord.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.AMPRecord)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.AMPRecord: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.AMPRecord();
            if (object.root_share != null)
                if (object.root_share.length)
                    if (typeof object.root_share === "string")
                        $util.base64.decode(object.root_share, message.root_share = $util.newBuffer($util.base64.length(object.root_share)), 0);
                    else if (object.root_share.length >= 0)
                        message.root_share = object.root_share;
            if (object.set_id != null)
                if (object.set_id.length)
                    if (typeof object.set_id === "string")
                        $util.base64.decode(object.set_id, message.set_id = $util.newBuffer($util.base64.length(object.set_id)), 0);
                    else if (object.set_id.length >= 0)
                        message.set_id = object.set_id;
            if (object.child_index != null)
                if ($Number(object.child_index) !== 0)
                    message.child_index = object.child_index >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a AMPRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.AMPRecord
         * @static
         * @param {lnrpc.AMPRecord} message AMPRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AMPRecord.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.root_share = "";
                else {
                    object.root_share = [];
                    if (options.bytes !== $Array)
                        object.root_share = $util.newBuffer(object.root_share);
                }
                if (options.bytes === $String)
                    object.set_id = "";
                else {
                    object.set_id = [];
                    if (options.bytes !== $Array)
                        object.set_id = $util.newBuffer(object.set_id);
                }
                object.child_index = 0;
            }
            if (message.root_share != null && $Object.hasOwnProperty.call(message, "root_share"))
                object.root_share = options.bytes === $String ? $util.base64.encode(message.root_share, 0, message.root_share.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.root_share) : message.root_share;
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id"))
                object.set_id = options.bytes === $String ? $util.base64.encode(message.set_id, 0, message.set_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.set_id) : message.set_id;
            if (message.child_index != null && $Object.hasOwnProperty.call(message, "child_index"))
                object.child_index = message.child_index;
            return object;
        };

        /**
         * Converts this AMPRecord to JSON.
         * @function toJSON
         * @memberof lnrpc.AMPRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AMPRecord.prototype.toJSON = function() {
            return AMPRecord.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AMPRecord
         * @function getTypeUrl
         * @memberof lnrpc.AMPRecord
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AMPRecord.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.AMPRecord";
        };

        return AMPRecord;
    })();

    lnrpc.Route = (function() {

        /**
         * Properties of a Route.
         * @typedef {Object} lnrpc.Route.$Properties
         * @property {number|null} [total_time_lock] Route total_time_lock
         * @property {Long|null} [total_fees] Route total_fees
         * @property {Long|null} [total_amt] Route total_amt
         * @property {Array.<lnrpc.Hop.$Properties>|null} [hops] Route hops
         * @property {Long|null} [total_fees_msat] Route total_fees_msat
         * @property {Long|null} [total_amt_msat] Route total_amt_msat
         * @property {Long|null} [first_hop_amount_msat] Route first_hop_amount_msat
         * @property {Uint8Array|null} [custom_channel_data] Route custom_channel_data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Route.
         * @memberof lnrpc
         * @interface IRoute
         * @augments lnrpc.Route.$Properties
         * @deprecated Use lnrpc.Route.$Properties instead.
         */

        /**
         * Shape of a Route.
         * @typedef {lnrpc.Route.$Properties} lnrpc.Route.$Shape
         */

        /**
         * Constructs a new Route.
         * @memberof lnrpc
         * @classdesc Represents a Route.
         * @constructor
         * @param {lnrpc.Route.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Route = function (properties) {
            this.hops = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Route total_time_lock.
         * @member {number} total_time_lock
         * @memberof lnrpc.Route
         * @instance
         */
        Route.prototype.total_time_lock = 0;

        /**
         * Route total_fees.
         * @member {Long} total_fees
         * @memberof lnrpc.Route
         * @instance
         */
        Route.prototype.total_fees = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Route total_amt.
         * @member {Long} total_amt
         * @memberof lnrpc.Route
         * @instance
         */
        Route.prototype.total_amt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Route hops.
         * @member {Array.<lnrpc.Hop.$Properties>} hops
         * @memberof lnrpc.Route
         * @instance
         */
        Route.prototype.hops = $util.emptyArray;

        /**
         * Route total_fees_msat.
         * @member {Long} total_fees_msat
         * @memberof lnrpc.Route
         * @instance
         */
        Route.prototype.total_fees_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Route total_amt_msat.
         * @member {Long} total_amt_msat
         * @memberof lnrpc.Route
         * @instance
         */
        Route.prototype.total_amt_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Route first_hop_amount_msat.
         * @member {Long} first_hop_amount_msat
         * @memberof lnrpc.Route
         * @instance
         */
        Route.prototype.first_hop_amount_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Route custom_channel_data.
         * @member {Uint8Array} custom_channel_data
         * @memberof lnrpc.Route
         * @instance
         */
        Route.prototype.custom_channel_data = $util.newBuffer([]);

        /**
         * Creates a new Route instance using the specified properties.
         * @function create
         * @memberof lnrpc.Route
         * @static
         * @param {lnrpc.Route.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Route} Route instance
         * @type {{
         *   (properties: lnrpc.Route.$Shape): lnrpc.Route & lnrpc.Route.$Shape;
         *   (properties?: lnrpc.Route.$Properties): lnrpc.Route;
         * }}
         */
        Route.create = function(properties) {
            return new Route(properties);
        };

        /**
         * Encodes the specified Route message. Does not implicitly {@link lnrpc.Route.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Route
         * @static
         * @param {lnrpc.Route.$Properties} message Route message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Route.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.total_time_lock != null && $Object.hasOwnProperty.call(message, "total_time_lock") && message.total_time_lock !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.total_time_lock);
            if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees") && (typeof message.total_fees === "object" ? message.total_fees.low || message.total_fees.high : message.total_fees !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.total_fees);
            if (message.total_amt != null && $Object.hasOwnProperty.call(message, "total_amt") && (typeof message.total_amt === "object" ? message.total_amt.low || message.total_amt.high : message.total_amt !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.total_amt);
            if (message.hops != null && message.hops.length)
                for (let i = 0; i < message.hops.length; ++i)
                    $root.lnrpc.Hop.encode(message.hops[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.total_fees_msat != null && $Object.hasOwnProperty.call(message, "total_fees_msat") && (typeof message.total_fees_msat === "object" ? message.total_fees_msat.low || message.total_fees_msat.high : message.total_fees_msat !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.total_fees_msat);
            if (message.total_amt_msat != null && $Object.hasOwnProperty.call(message, "total_amt_msat") && (typeof message.total_amt_msat === "object" ? message.total_amt_msat.low || message.total_amt_msat.high : message.total_amt_msat !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.total_amt_msat);
            if (message.first_hop_amount_msat != null && $Object.hasOwnProperty.call(message, "first_hop_amount_msat") && (typeof message.first_hop_amount_msat === "object" ? message.first_hop_amount_msat.low || message.first_hop_amount_msat.high : message.first_hop_amount_msat !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.first_hop_amount_msat);
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data") && message.custom_channel_data.length)
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.custom_channel_data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Route message, length delimited. Does not implicitly {@link lnrpc.Route.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Route
         * @static
         * @param {lnrpc.Route.$Properties} message Route message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Route.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Route message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Route
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Route & lnrpc.Route.$Shape} Route
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Route.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Route(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_time_lock = value;
                        else
                            delete message.total_time_lock;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_fees = value;
                        else
                            delete message.total_fees;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_amt = value;
                        else
                            delete message.total_amt;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hops && message.hops.length))
                            message.hops = [];
                        message.hops.push($root.lnrpc.Hop.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_fees_msat = value;
                        else
                            delete message.total_fees_msat;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_amt_msat = value;
                        else
                            delete message.total_amt_msat;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.first_hop_amount_msat = value;
                        else
                            delete message.first_hop_amount_msat;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.custom_channel_data = value;
                        else
                            delete message.custom_channel_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Route message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Route
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Route & lnrpc.Route.$Shape} Route
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Route.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Route message.
         * @function verify
         * @memberof lnrpc.Route
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Route.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.total_time_lock != null && $Object.hasOwnProperty.call(message, "total_time_lock"))
                if (!$util.isInteger(message.total_time_lock))
                    return "total_time_lock: integer expected";
            if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees"))
                if (!$util.isInteger(message.total_fees) && !(message.total_fees && $util.isInteger(message.total_fees.low) && $util.isInteger(message.total_fees.high)))
                    return "total_fees: integer|Long expected";
            if (message.total_amt != null && $Object.hasOwnProperty.call(message, "total_amt"))
                if (!$util.isInteger(message.total_amt) && !(message.total_amt && $util.isInteger(message.total_amt.low) && $util.isInteger(message.total_amt.high)))
                    return "total_amt: integer|Long expected";
            if (message.hops != null && $Object.hasOwnProperty.call(message, "hops")) {
                if (!$Array.isArray(message.hops))
                    return "hops: array expected";
                for (let i = 0; i < message.hops.length; ++i) {
                    let error = $root.lnrpc.Hop.verify(message.hops[i], _depth + 1);
                    if (error)
                        return "hops." + error;
                }
            }
            if (message.total_fees_msat != null && $Object.hasOwnProperty.call(message, "total_fees_msat"))
                if (!$util.isInteger(message.total_fees_msat) && !(message.total_fees_msat && $util.isInteger(message.total_fees_msat.low) && $util.isInteger(message.total_fees_msat.high)))
                    return "total_fees_msat: integer|Long expected";
            if (message.total_amt_msat != null && $Object.hasOwnProperty.call(message, "total_amt_msat"))
                if (!$util.isInteger(message.total_amt_msat) && !(message.total_amt_msat && $util.isInteger(message.total_amt_msat.low) && $util.isInteger(message.total_amt_msat.high)))
                    return "total_amt_msat: integer|Long expected";
            if (message.first_hop_amount_msat != null && $Object.hasOwnProperty.call(message, "first_hop_amount_msat"))
                if (!$util.isInteger(message.first_hop_amount_msat) && !(message.first_hop_amount_msat && $util.isInteger(message.first_hop_amount_msat.low) && $util.isInteger(message.first_hop_amount_msat.high)))
                    return "first_hop_amount_msat: integer|Long expected";
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                if (!(message.custom_channel_data && typeof message.custom_channel_data.length === "number" || $util.isString(message.custom_channel_data)))
                    return "custom_channel_data: buffer expected";
            return null;
        };

        /**
         * Creates a Route message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Route
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Route} Route
         */
        Route.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Route)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Route: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Route();
            if (object.total_time_lock != null)
                if ($Number(object.total_time_lock) !== 0)
                    message.total_time_lock = object.total_time_lock >>> 0;
            if (object.total_fees != null)
                if (typeof object.total_fees === "object" ? object.total_fees.low || object.total_fees.high : $Number(object.total_fees) !== 0)
                    if ($util.Long)
                        message.total_fees = $util.Long.fromValue(object.total_fees, false);
                    else if (typeof object.total_fees === "string")
                        message.total_fees = $parseInt(object.total_fees, 10);
                    else if (typeof object.total_fees === "number")
                        message.total_fees = object.total_fees;
                    else if (typeof object.total_fees === "object")
                        message.total_fees = new $util.LongBits(object.total_fees.low >>> 0, object.total_fees.high >>> 0).toNumber();
            if (object.total_amt != null)
                if (typeof object.total_amt === "object" ? object.total_amt.low || object.total_amt.high : $Number(object.total_amt) !== 0)
                    if ($util.Long)
                        message.total_amt = $util.Long.fromValue(object.total_amt, false);
                    else if (typeof object.total_amt === "string")
                        message.total_amt = $parseInt(object.total_amt, 10);
                    else if (typeof object.total_amt === "number")
                        message.total_amt = object.total_amt;
                    else if (typeof object.total_amt === "object")
                        message.total_amt = new $util.LongBits(object.total_amt.low >>> 0, object.total_amt.high >>> 0).toNumber();
            if (object.hops) {
                if (!$Array.isArray(object.hops))
                    throw $TypeError(".lnrpc.Route.hops: array expected");
                message.hops = $Array(object.hops.length);
                for (let i = 0; i < object.hops.length; ++i) {
                    if (!$util.isObject(object.hops[i]))
                        throw $TypeError(".lnrpc.Route.hops: object expected");
                    message.hops[i] = $root.lnrpc.Hop.fromObject(object.hops[i], _depth + 1);
                }
            }
            if (object.total_fees_msat != null)
                if (typeof object.total_fees_msat === "object" ? object.total_fees_msat.low || object.total_fees_msat.high : $Number(object.total_fees_msat) !== 0)
                    if ($util.Long)
                        message.total_fees_msat = $util.Long.fromValue(object.total_fees_msat, false);
                    else if (typeof object.total_fees_msat === "string")
                        message.total_fees_msat = $parseInt(object.total_fees_msat, 10);
                    else if (typeof object.total_fees_msat === "number")
                        message.total_fees_msat = object.total_fees_msat;
                    else if (typeof object.total_fees_msat === "object")
                        message.total_fees_msat = new $util.LongBits(object.total_fees_msat.low >>> 0, object.total_fees_msat.high >>> 0).toNumber();
            if (object.total_amt_msat != null)
                if (typeof object.total_amt_msat === "object" ? object.total_amt_msat.low || object.total_amt_msat.high : $Number(object.total_amt_msat) !== 0)
                    if ($util.Long)
                        message.total_amt_msat = $util.Long.fromValue(object.total_amt_msat, false);
                    else if (typeof object.total_amt_msat === "string")
                        message.total_amt_msat = $parseInt(object.total_amt_msat, 10);
                    else if (typeof object.total_amt_msat === "number")
                        message.total_amt_msat = object.total_amt_msat;
                    else if (typeof object.total_amt_msat === "object")
                        message.total_amt_msat = new $util.LongBits(object.total_amt_msat.low >>> 0, object.total_amt_msat.high >>> 0).toNumber();
            if (object.first_hop_amount_msat != null)
                if (typeof object.first_hop_amount_msat === "object" ? object.first_hop_amount_msat.low || object.first_hop_amount_msat.high : $Number(object.first_hop_amount_msat) !== 0)
                    if ($util.Long)
                        message.first_hop_amount_msat = $util.Long.fromValue(object.first_hop_amount_msat, false);
                    else if (typeof object.first_hop_amount_msat === "string")
                        message.first_hop_amount_msat = $parseInt(object.first_hop_amount_msat, 10);
                    else if (typeof object.first_hop_amount_msat === "number")
                        message.first_hop_amount_msat = object.first_hop_amount_msat;
                    else if (typeof object.first_hop_amount_msat === "object")
                        message.first_hop_amount_msat = new $util.LongBits(object.first_hop_amount_msat.low >>> 0, object.first_hop_amount_msat.high >>> 0).toNumber();
            if (object.custom_channel_data != null)
                if (object.custom_channel_data.length)
                    if (typeof object.custom_channel_data === "string")
                        $util.base64.decode(object.custom_channel_data, message.custom_channel_data = $util.newBuffer($util.base64.length(object.custom_channel_data)), 0);
                    else if (object.custom_channel_data.length >= 0)
                        message.custom_channel_data = object.custom_channel_data;
            return message;
        };

        /**
         * Creates a plain object from a Route message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Route
         * @static
         * @param {lnrpc.Route} message Route
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Route.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.hops = [];
            if (options.defaults) {
                object.total_time_lock = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_fees = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_fees = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_amt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_amt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_fees_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_fees_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.first_hop_amount_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.first_hop_amount_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.custom_channel_data = "";
                else {
                    object.custom_channel_data = [];
                    if (options.bytes !== $Array)
                        object.custom_channel_data = $util.newBuffer(object.custom_channel_data);
                }
            }
            if (message.total_time_lock != null && $Object.hasOwnProperty.call(message, "total_time_lock"))
                object.total_time_lock = message.total_time_lock;
            if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_fees = typeof message.total_fees === "number" ? $BigInt(message.total_fees) : $util.Long.fromBits(message.total_fees.low >>> 0, message.total_fees.high >>> 0, false).toBigInt();
                else if (typeof message.total_fees === "number")
                    object.total_fees = options.longs === $String ? $String(message.total_fees) : message.total_fees;
                else
                    object.total_fees = options.longs === $String ? $util.Long.prototype.toString.call(message.total_fees) : options.longs === $Number ? new $util.LongBits(message.total_fees.low >>> 0, message.total_fees.high >>> 0).toNumber() : message.total_fees;
            if (message.total_amt != null && $Object.hasOwnProperty.call(message, "total_amt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_amt = typeof message.total_amt === "number" ? $BigInt(message.total_amt) : $util.Long.fromBits(message.total_amt.low >>> 0, message.total_amt.high >>> 0, false).toBigInt();
                else if (typeof message.total_amt === "number")
                    object.total_amt = options.longs === $String ? $String(message.total_amt) : message.total_amt;
                else
                    object.total_amt = options.longs === $String ? $util.Long.prototype.toString.call(message.total_amt) : options.longs === $Number ? new $util.LongBits(message.total_amt.low >>> 0, message.total_amt.high >>> 0).toNumber() : message.total_amt;
            if (message.hops && message.hops.length) {
                object.hops = $Array(message.hops.length);
                for (let j = 0; j < message.hops.length; ++j)
                    object.hops[j] = $root.lnrpc.Hop.toObject(message.hops[j], options, _depth + 1);
            }
            if (message.total_fees_msat != null && $Object.hasOwnProperty.call(message, "total_fees_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_fees_msat = typeof message.total_fees_msat === "number" ? $BigInt(message.total_fees_msat) : $util.Long.fromBits(message.total_fees_msat.low >>> 0, message.total_fees_msat.high >>> 0, false).toBigInt();
                else if (typeof message.total_fees_msat === "number")
                    object.total_fees_msat = options.longs === $String ? $String(message.total_fees_msat) : message.total_fees_msat;
                else
                    object.total_fees_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.total_fees_msat) : options.longs === $Number ? new $util.LongBits(message.total_fees_msat.low >>> 0, message.total_fees_msat.high >>> 0).toNumber() : message.total_fees_msat;
            if (message.total_amt_msat != null && $Object.hasOwnProperty.call(message, "total_amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_amt_msat = typeof message.total_amt_msat === "number" ? $BigInt(message.total_amt_msat) : $util.Long.fromBits(message.total_amt_msat.low >>> 0, message.total_amt_msat.high >>> 0, false).toBigInt();
                else if (typeof message.total_amt_msat === "number")
                    object.total_amt_msat = options.longs === $String ? $String(message.total_amt_msat) : message.total_amt_msat;
                else
                    object.total_amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.total_amt_msat) : options.longs === $Number ? new $util.LongBits(message.total_amt_msat.low >>> 0, message.total_amt_msat.high >>> 0).toNumber() : message.total_amt_msat;
            if (message.first_hop_amount_msat != null && $Object.hasOwnProperty.call(message, "first_hop_amount_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.first_hop_amount_msat = typeof message.first_hop_amount_msat === "number" ? $BigInt(message.first_hop_amount_msat) : $util.Long.fromBits(message.first_hop_amount_msat.low >>> 0, message.first_hop_amount_msat.high >>> 0, false).toBigInt();
                else if (typeof message.first_hop_amount_msat === "number")
                    object.first_hop_amount_msat = options.longs === $String ? $String(message.first_hop_amount_msat) : message.first_hop_amount_msat;
                else
                    object.first_hop_amount_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.first_hop_amount_msat) : options.longs === $Number ? new $util.LongBits(message.first_hop_amount_msat.low >>> 0, message.first_hop_amount_msat.high >>> 0).toNumber() : message.first_hop_amount_msat;
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                object.custom_channel_data = options.bytes === $String ? $util.base64.encode(message.custom_channel_data, 0, message.custom_channel_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_channel_data) : message.custom_channel_data;
            return object;
        };

        /**
         * Converts this Route to JSON.
         * @function toJSON
         * @memberof lnrpc.Route
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Route.prototype.toJSON = function() {
            return Route.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Route
         * @function getTypeUrl
         * @memberof lnrpc.Route
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Route.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Route";
        };

        return Route;
    })();

    lnrpc.NodeInfoRequest = (function() {

        /**
         * Properties of a NodeInfoRequest.
         * @typedef {Object} lnrpc.NodeInfoRequest.$Properties
         * @property {string|null} [pub_key] NodeInfoRequest pub_key
         * @property {boolean|null} [include_channels] NodeInfoRequest include_channels
         * @property {boolean|null} [include_auth_proof] NodeInfoRequest include_auth_proof
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NodeInfoRequest.
         * @memberof lnrpc
         * @interface INodeInfoRequest
         * @augments lnrpc.NodeInfoRequest.$Properties
         * @deprecated Use lnrpc.NodeInfoRequest.$Properties instead.
         */

        /**
         * Shape of a NodeInfoRequest.
         * @typedef {lnrpc.NodeInfoRequest.$Properties} lnrpc.NodeInfoRequest.$Shape
         */

        /**
         * Constructs a new NodeInfoRequest.
         * @memberof lnrpc
         * @classdesc Represents a NodeInfoRequest.
         * @constructor
         * @param {lnrpc.NodeInfoRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NodeInfoRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NodeInfoRequest pub_key.
         * @member {string} pub_key
         * @memberof lnrpc.NodeInfoRequest
         * @instance
         */
        NodeInfoRequest.prototype.pub_key = "";

        /**
         * NodeInfoRequest include_channels.
         * @member {boolean} include_channels
         * @memberof lnrpc.NodeInfoRequest
         * @instance
         */
        NodeInfoRequest.prototype.include_channels = false;

        /**
         * NodeInfoRequest include_auth_proof.
         * @member {boolean} include_auth_proof
         * @memberof lnrpc.NodeInfoRequest
         * @instance
         */
        NodeInfoRequest.prototype.include_auth_proof = false;

        /**
         * Creates a new NodeInfoRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.NodeInfoRequest
         * @static
         * @param {lnrpc.NodeInfoRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NodeInfoRequest} NodeInfoRequest instance
         * @type {{
         *   (properties: lnrpc.NodeInfoRequest.$Shape): lnrpc.NodeInfoRequest & lnrpc.NodeInfoRequest.$Shape;
         *   (properties?: lnrpc.NodeInfoRequest.$Properties): lnrpc.NodeInfoRequest;
         * }}
         */
        NodeInfoRequest.create = function(properties) {
            return new NodeInfoRequest(properties);
        };

        /**
         * Encodes the specified NodeInfoRequest message. Does not implicitly {@link lnrpc.NodeInfoRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NodeInfoRequest
         * @static
         * @param {lnrpc.NodeInfoRequest.$Properties} message NodeInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeInfoRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key") && message.pub_key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pub_key);
            if (message.include_channels != null && $Object.hasOwnProperty.call(message, "include_channels") && message.include_channels !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.include_channels);
            if (message.include_auth_proof != null && $Object.hasOwnProperty.call(message, "include_auth_proof") && message.include_auth_proof !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.include_auth_proof);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeInfoRequest message, length delimited. Does not implicitly {@link lnrpc.NodeInfoRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NodeInfoRequest
         * @static
         * @param {lnrpc.NodeInfoRequest.$Properties} message NodeInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeInfoRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NodeInfoRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NodeInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NodeInfoRequest & lnrpc.NodeInfoRequest.$Shape} NodeInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeInfoRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NodeInfoRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pub_key = value;
                        else
                            delete message.pub_key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_channels = value;
                        else
                            delete message.include_channels;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_auth_proof = value;
                        else
                            delete message.include_auth_proof;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeInfoRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NodeInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NodeInfoRequest & lnrpc.NodeInfoRequest.$Shape} NodeInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeInfoRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeInfoRequest message.
         * @function verify
         * @memberof lnrpc.NodeInfoRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeInfoRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                if (!$util.isString(message.pub_key))
                    return "pub_key: string expected";
            if (message.include_channels != null && $Object.hasOwnProperty.call(message, "include_channels"))
                if (typeof message.include_channels !== "boolean")
                    return "include_channels: boolean expected";
            if (message.include_auth_proof != null && $Object.hasOwnProperty.call(message, "include_auth_proof"))
                if (typeof message.include_auth_proof !== "boolean")
                    return "include_auth_proof: boolean expected";
            return null;
        };

        /**
         * Creates a NodeInfoRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NodeInfoRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NodeInfoRequest} NodeInfoRequest
         */
        NodeInfoRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NodeInfoRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NodeInfoRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NodeInfoRequest();
            if (object.pub_key != null)
                if (typeof object.pub_key !== "string" || object.pub_key.length)
                    message.pub_key = $String(object.pub_key);
            if (object.include_channels != null)
                if (object.include_channels)
                    message.include_channels = $Boolean(object.include_channels);
            if (object.include_auth_proof != null)
                if (object.include_auth_proof)
                    message.include_auth_proof = $Boolean(object.include_auth_proof);
            return message;
        };

        /**
         * Creates a plain object from a NodeInfoRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NodeInfoRequest
         * @static
         * @param {lnrpc.NodeInfoRequest} message NodeInfoRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeInfoRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.pub_key = "";
                object.include_channels = false;
                object.include_auth_proof = false;
            }
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                object.pub_key = message.pub_key;
            if (message.include_channels != null && $Object.hasOwnProperty.call(message, "include_channels"))
                object.include_channels = message.include_channels;
            if (message.include_auth_proof != null && $Object.hasOwnProperty.call(message, "include_auth_proof"))
                object.include_auth_proof = message.include_auth_proof;
            return object;
        };

        /**
         * Converts this NodeInfoRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.NodeInfoRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeInfoRequest.prototype.toJSON = function() {
            return NodeInfoRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeInfoRequest
         * @function getTypeUrl
         * @memberof lnrpc.NodeInfoRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeInfoRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NodeInfoRequest";
        };

        return NodeInfoRequest;
    })();

    lnrpc.NodeInfo = (function() {

        /**
         * Properties of a NodeInfo.
         * @typedef {Object} lnrpc.NodeInfo.$Properties
         * @property {lnrpc.LightningNode.$Properties|null} [node] NodeInfo node
         * @property {number|null} [num_channels] NodeInfo num_channels
         * @property {Long|null} [total_capacity] NodeInfo total_capacity
         * @property {Array.<lnrpc.ChannelEdge.$Properties>|null} [channels] NodeInfo channels
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NodeInfo.
         * @memberof lnrpc
         * @interface INodeInfo
         * @augments lnrpc.NodeInfo.$Properties
         * @deprecated Use lnrpc.NodeInfo.$Properties instead.
         */

        /**
         * Shape of a NodeInfo.
         * @typedef {lnrpc.NodeInfo.$Properties} lnrpc.NodeInfo.$Shape
         */

        /**
         * Constructs a new NodeInfo.
         * @memberof lnrpc
         * @classdesc Represents a NodeInfo.
         * @constructor
         * @param {lnrpc.NodeInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NodeInfo = function (properties) {
            this.channels = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NodeInfo node.
         * @member {lnrpc.LightningNode.$Properties|null|undefined} node
         * @memberof lnrpc.NodeInfo
         * @instance
         */
        NodeInfo.prototype.node = null;

        /**
         * NodeInfo num_channels.
         * @member {number} num_channels
         * @memberof lnrpc.NodeInfo
         * @instance
         */
        NodeInfo.prototype.num_channels = 0;

        /**
         * NodeInfo total_capacity.
         * @member {Long} total_capacity
         * @memberof lnrpc.NodeInfo
         * @instance
         */
        NodeInfo.prototype.total_capacity = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * NodeInfo channels.
         * @member {Array.<lnrpc.ChannelEdge.$Properties>} channels
         * @memberof lnrpc.NodeInfo
         * @instance
         */
        NodeInfo.prototype.channels = $util.emptyArray;

        /**
         * Creates a new NodeInfo instance using the specified properties.
         * @function create
         * @memberof lnrpc.NodeInfo
         * @static
         * @param {lnrpc.NodeInfo.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NodeInfo} NodeInfo instance
         * @type {{
         *   (properties: lnrpc.NodeInfo.$Shape): lnrpc.NodeInfo & lnrpc.NodeInfo.$Shape;
         *   (properties?: lnrpc.NodeInfo.$Properties): lnrpc.NodeInfo;
         * }}
         */
        NodeInfo.create = function(properties) {
            return new NodeInfo(properties);
        };

        /**
         * Encodes the specified NodeInfo message. Does not implicitly {@link lnrpc.NodeInfo.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NodeInfo
         * @static
         * @param {lnrpc.NodeInfo.$Properties} message NodeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.node != null && $Object.hasOwnProperty.call(message, "node"))
                $root.lnrpc.LightningNode.encode(message.node, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.num_channels != null && $Object.hasOwnProperty.call(message, "num_channels") && message.num_channels !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.num_channels);
            if (message.total_capacity != null && $Object.hasOwnProperty.call(message, "total_capacity") && (typeof message.total_capacity === "object" ? message.total_capacity.low || message.total_capacity.high : message.total_capacity !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.total_capacity);
            if (message.channels != null && message.channels.length)
                for (let i = 0; i < message.channels.length; ++i)
                    $root.lnrpc.ChannelEdge.encode(message.channels[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeInfo message, length delimited. Does not implicitly {@link lnrpc.NodeInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NodeInfo
         * @static
         * @param {lnrpc.NodeInfo.$Properties} message NodeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NodeInfo message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NodeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NodeInfo & lnrpc.NodeInfo.$Shape} NodeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NodeInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.node = $root.lnrpc.LightningNode.decode(reader, reader.uint32(), $undefined, _depth + 1, message.node);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_channels = value;
                        else
                            delete message.num_channels;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_capacity = value;
                        else
                            delete message.total_capacity;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.channels && message.channels.length))
                            message.channels = [];
                        message.channels.push($root.lnrpc.ChannelEdge.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NodeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NodeInfo & lnrpc.NodeInfo.$Shape} NodeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeInfo message.
         * @function verify
         * @memberof lnrpc.NodeInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.node != null && $Object.hasOwnProperty.call(message, "node")) {
                let error = $root.lnrpc.LightningNode.verify(message.node, _depth + 1);
                if (error)
                    return "node." + error;
            }
            if (message.num_channels != null && $Object.hasOwnProperty.call(message, "num_channels"))
                if (!$util.isInteger(message.num_channels))
                    return "num_channels: integer expected";
            if (message.total_capacity != null && $Object.hasOwnProperty.call(message, "total_capacity"))
                if (!$util.isInteger(message.total_capacity) && !(message.total_capacity && $util.isInteger(message.total_capacity.low) && $util.isInteger(message.total_capacity.high)))
                    return "total_capacity: integer|Long expected";
            if (message.channels != null && $Object.hasOwnProperty.call(message, "channels")) {
                if (!$Array.isArray(message.channels))
                    return "channels: array expected";
                for (let i = 0; i < message.channels.length; ++i) {
                    let error = $root.lnrpc.ChannelEdge.verify(message.channels[i], _depth + 1);
                    if (error)
                        return "channels." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NodeInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NodeInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NodeInfo} NodeInfo
         */
        NodeInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NodeInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NodeInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NodeInfo();
            if (object.node != null) {
                if (!$util.isObject(object.node))
                    throw $TypeError(".lnrpc.NodeInfo.node: object expected");
                message.node = $root.lnrpc.LightningNode.fromObject(object.node, _depth + 1);
            }
            if (object.num_channels != null)
                if ($Number(object.num_channels) !== 0)
                    message.num_channels = object.num_channels >>> 0;
            if (object.total_capacity != null)
                if (typeof object.total_capacity === "object" ? object.total_capacity.low || object.total_capacity.high : $Number(object.total_capacity) !== 0)
                    if ($util.Long)
                        message.total_capacity = $util.Long.fromValue(object.total_capacity, false);
                    else if (typeof object.total_capacity === "string")
                        message.total_capacity = $parseInt(object.total_capacity, 10);
                    else if (typeof object.total_capacity === "number")
                        message.total_capacity = object.total_capacity;
                    else if (typeof object.total_capacity === "object")
                        message.total_capacity = new $util.LongBits(object.total_capacity.low >>> 0, object.total_capacity.high >>> 0).toNumber();
            if (object.channels) {
                if (!$Array.isArray(object.channels))
                    throw $TypeError(".lnrpc.NodeInfo.channels: array expected");
                message.channels = $Array(object.channels.length);
                for (let i = 0; i < object.channels.length; ++i) {
                    if (!$util.isObject(object.channels[i]))
                        throw $TypeError(".lnrpc.NodeInfo.channels: object expected");
                    message.channels[i] = $root.lnrpc.ChannelEdge.fromObject(object.channels[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NodeInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NodeInfo
         * @static
         * @param {lnrpc.NodeInfo} message NodeInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.channels = [];
            if (options.defaults) {
                object.node = null;
                object.num_channels = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_capacity = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_capacity = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.node != null && $Object.hasOwnProperty.call(message, "node"))
                object.node = $root.lnrpc.LightningNode.toObject(message.node, options, _depth + 1);
            if (message.num_channels != null && $Object.hasOwnProperty.call(message, "num_channels"))
                object.num_channels = message.num_channels;
            if (message.total_capacity != null && $Object.hasOwnProperty.call(message, "total_capacity"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_capacity = typeof message.total_capacity === "number" ? $BigInt(message.total_capacity) : $util.Long.fromBits(message.total_capacity.low >>> 0, message.total_capacity.high >>> 0, false).toBigInt();
                else if (typeof message.total_capacity === "number")
                    object.total_capacity = options.longs === $String ? $String(message.total_capacity) : message.total_capacity;
                else
                    object.total_capacity = options.longs === $String ? $util.Long.prototype.toString.call(message.total_capacity) : options.longs === $Number ? new $util.LongBits(message.total_capacity.low >>> 0, message.total_capacity.high >>> 0).toNumber() : message.total_capacity;
            if (message.channels && message.channels.length) {
                object.channels = $Array(message.channels.length);
                for (let j = 0; j < message.channels.length; ++j)
                    object.channels[j] = $root.lnrpc.ChannelEdge.toObject(message.channels[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this NodeInfo to JSON.
         * @function toJSON
         * @memberof lnrpc.NodeInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeInfo.prototype.toJSON = function() {
            return NodeInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeInfo
         * @function getTypeUrl
         * @memberof lnrpc.NodeInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NodeInfo";
        };

        return NodeInfo;
    })();

    lnrpc.LightningNode = (function() {

        /**
         * Properties of a LightningNode.
         * @typedef {Object} lnrpc.LightningNode.$Properties
         * @property {number|null} [last_update] LightningNode last_update
         * @property {string|null} [pub_key] LightningNode pub_key
         * @property {string|null} [alias] LightningNode alias
         * @property {Array.<lnrpc.NodeAddress.$Properties>|null} [addresses] LightningNode addresses
         * @property {string|null} [color] LightningNode color
         * @property {Object.<string,lnrpc.Feature.$Properties>|null} [features] LightningNode features
         * @property {Object.<string,Uint8Array>|null} [custom_records] LightningNode custom_records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LightningNode.
         * @memberof lnrpc
         * @interface ILightningNode
         * @augments lnrpc.LightningNode.$Properties
         * @deprecated Use lnrpc.LightningNode.$Properties instead.
         */

        /**
         * Shape of a LightningNode.
         * @typedef {lnrpc.LightningNode.$Properties} lnrpc.LightningNode.$Shape
         */

        /**
         * Constructs a new LightningNode.
         * @memberof lnrpc
         * @classdesc Represents a LightningNode.
         * @constructor
         * @param {lnrpc.LightningNode.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LightningNode = function (properties) {
            this.addresses = [];
            this.features = {};
            this.custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LightningNode last_update.
         * @member {number} last_update
         * @memberof lnrpc.LightningNode
         * @instance
         */
        LightningNode.prototype.last_update = 0;

        /**
         * LightningNode pub_key.
         * @member {string} pub_key
         * @memberof lnrpc.LightningNode
         * @instance
         */
        LightningNode.prototype.pub_key = "";

        /**
         * LightningNode alias.
         * @member {string} alias
         * @memberof lnrpc.LightningNode
         * @instance
         */
        LightningNode.prototype.alias = "";

        /**
         * LightningNode addresses.
         * @member {Array.<lnrpc.NodeAddress.$Properties>} addresses
         * @memberof lnrpc.LightningNode
         * @instance
         */
        LightningNode.prototype.addresses = $util.emptyArray;

        /**
         * LightningNode color.
         * @member {string} color
         * @memberof lnrpc.LightningNode
         * @instance
         */
        LightningNode.prototype.color = "";

        /**
         * LightningNode features.
         * @member {Object.<string,lnrpc.Feature.$Properties>} features
         * @memberof lnrpc.LightningNode
         * @instance
         */
        LightningNode.prototype.features = $util.emptyObject;

        /**
         * LightningNode custom_records.
         * @member {Object.<string,Uint8Array>} custom_records
         * @memberof lnrpc.LightningNode
         * @instance
         */
        LightningNode.prototype.custom_records = $util.emptyObject;

        /**
         * Creates a new LightningNode instance using the specified properties.
         * @function create
         * @memberof lnrpc.LightningNode
         * @static
         * @param {lnrpc.LightningNode.$Properties=} [properties] Properties to set
         * @returns {lnrpc.LightningNode} LightningNode instance
         * @type {{
         *   (properties: lnrpc.LightningNode.$Shape): lnrpc.LightningNode & lnrpc.LightningNode.$Shape;
         *   (properties?: lnrpc.LightningNode.$Properties): lnrpc.LightningNode;
         * }}
         */
        LightningNode.create = function(properties) {
            return new LightningNode(properties);
        };

        /**
         * Encodes the specified LightningNode message. Does not implicitly {@link lnrpc.LightningNode.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.LightningNode
         * @static
         * @param {lnrpc.LightningNode.$Properties} message LightningNode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LightningNode.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.last_update != null && $Object.hasOwnProperty.call(message, "last_update") && message.last_update !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.last_update);
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key") && message.pub_key !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.pub_key);
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias") && message.alias !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.alias);
            if (message.addresses != null && message.addresses.length)
                for (let i = 0; i < message.addresses.length; ++i)
                    $root.lnrpc.NodeAddress.encode(message.addresses[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.color != null && $Object.hasOwnProperty.call(message, "color") && message.color !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.color);
            if (message.features != null && $Object.hasOwnProperty.call(message, "features"))
                for (let keys = $Object.keys(message.features), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 6, wireType 2 =*/50).fork().uint32(/* id 1, wireType 0 =*/8).uint32(keys[i]);
                    $root.lnrpc.Feature.encode(message.features[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records"))
                for (let keys = $Object.keys(message.custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 7, wireType 2 =*/58).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.custom_records[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LightningNode message, length delimited. Does not implicitly {@link lnrpc.LightningNode.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.LightningNode
         * @static
         * @param {lnrpc.LightningNode.$Properties} message LightningNode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LightningNode.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LightningNode message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.LightningNode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.LightningNode & lnrpc.LightningNode.$Shape} LightningNode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LightningNode.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.LightningNode(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_update = value;
                        else
                            delete message.last_update;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pub_key = value;
                        else
                            delete message.pub_key;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.alias = value;
                        else
                            delete message.alias;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.addresses && message.addresses.length))
                            message.addresses = [];
                        message.addresses.push($root.lnrpc.NodeAddress.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.color = value;
                        else
                            delete message.color;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (message.features === $util.emptyObject)
                            message.features = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint32();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.Feature.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.features[key] = value || new $root.lnrpc.Feature();
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (message.custom_records === $util.emptyObject)
                            message.custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LightningNode message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.LightningNode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.LightningNode & lnrpc.LightningNode.$Shape} LightningNode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LightningNode.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LightningNode message.
         * @function verify
         * @memberof lnrpc.LightningNode
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LightningNode.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.last_update != null && $Object.hasOwnProperty.call(message, "last_update"))
                if (!$util.isInteger(message.last_update))
                    return "last_update: integer expected";
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                if (!$util.isString(message.pub_key))
                    return "pub_key: string expected";
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                if (!$util.isString(message.alias))
                    return "alias: string expected";
            if (message.addresses != null && $Object.hasOwnProperty.call(message, "addresses")) {
                if (!$Array.isArray(message.addresses))
                    return "addresses: array expected";
                for (let i = 0; i < message.addresses.length; ++i) {
                    let error = $root.lnrpc.NodeAddress.verify(message.addresses[i], _depth + 1);
                    if (error)
                        return "addresses." + error;
                }
            }
            if (message.color != null && $Object.hasOwnProperty.call(message, "color"))
                if (!$util.isString(message.color))
                    return "color: string expected";
            if (message.features != null && $Object.hasOwnProperty.call(message, "features")) {
                if (!$util.isObject(message.features))
                    return "features: object expected";
                let key = $Object.keys(message.features);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "features: integer key{k:uint32} expected";
                    {
                        let error = $root.lnrpc.Feature.verify(message.features[key[i]], _depth + 1);
                        if (error)
                            return "features." + error;
                    }
                }
            }
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records")) {
                if (!$util.isObject(message.custom_records))
                    return "custom_records: object expected";
                let key = $Object.keys(message.custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.custom_records[key[i]] && typeof message.custom_records[key[i]].length === "number" || $util.isString(message.custom_records[key[i]])))
                        return "custom_records: buffer{k:uint64} expected";
                }
            }
            return null;
        };

        /**
         * Creates a LightningNode message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.LightningNode
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.LightningNode} LightningNode
         */
        LightningNode.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.LightningNode)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.LightningNode: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.LightningNode();
            if (object.last_update != null)
                if ($Number(object.last_update) !== 0)
                    message.last_update = object.last_update >>> 0;
            if (object.pub_key != null)
                if (typeof object.pub_key !== "string" || object.pub_key.length)
                    message.pub_key = $String(object.pub_key);
            if (object.alias != null)
                if (typeof object.alias !== "string" || object.alias.length)
                    message.alias = $String(object.alias);
            if (object.addresses) {
                if (!$Array.isArray(object.addresses))
                    throw $TypeError(".lnrpc.LightningNode.addresses: array expected");
                message.addresses = $Array(object.addresses.length);
                for (let i = 0; i < object.addresses.length; ++i) {
                    if (!$util.isObject(object.addresses[i]))
                        throw $TypeError(".lnrpc.LightningNode.addresses: object expected");
                    message.addresses[i] = $root.lnrpc.NodeAddress.fromObject(object.addresses[i], _depth + 1);
                }
            }
            if (object.color != null)
                if (typeof object.color !== "string" || object.color.length)
                    message.color = $String(object.color);
            if (object.features) {
                if (!$util.isObject(object.features))
                    throw $TypeError(".lnrpc.LightningNode.features: object expected");
                message.features = {};
                for (let keys = $Object.keys(object.features), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.features, keys[i]);
                    if (!$util.isObject(object.features[keys[i]]))
                        throw $TypeError(".lnrpc.LightningNode.features: object expected");
                    message.features[keys[i]] = $root.lnrpc.Feature.fromObject(object.features[keys[i]], _depth + 1);
                }
            }
            if (object.custom_records) {
                if (!$util.isObject(object.custom_records))
                    throw $TypeError(".lnrpc.LightningNode.custom_records: object expected");
                message.custom_records = {};
                for (let keys = $Object.keys(object.custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.custom_records, keys[i]);
                    if (typeof object.custom_records[keys[i]] === "string")
                        $util.base64.decode(object.custom_records[keys[i]], message.custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.custom_records[keys[i]])), 0);
                    else if (object.custom_records[keys[i]].length >= 0)
                        message.custom_records[keys[i]] = object.custom_records[keys[i]];
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a LightningNode message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.LightningNode
         * @static
         * @param {lnrpc.LightningNode} message LightningNode
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LightningNode.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.addresses = [];
            if (options.objects || options.defaults) {
                object.features = {};
                object.custom_records = {};
            }
            if (options.defaults) {
                object.last_update = 0;
                object.pub_key = "";
                object.alias = "";
                object.color = "";
            }
            if (message.last_update != null && $Object.hasOwnProperty.call(message, "last_update"))
                object.last_update = message.last_update;
            if (message.pub_key != null && $Object.hasOwnProperty.call(message, "pub_key"))
                object.pub_key = message.pub_key;
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                object.alias = message.alias;
            if (message.addresses && message.addresses.length) {
                object.addresses = $Array(message.addresses.length);
                for (let j = 0; j < message.addresses.length; ++j)
                    object.addresses[j] = $root.lnrpc.NodeAddress.toObject(message.addresses[j], options, _depth + 1);
            }
            if (message.color != null && $Object.hasOwnProperty.call(message, "color"))
                object.color = message.color;
            let keys2;
            if (message.features && (keys2 = $Object.keys(message.features)).length) {
                object.features = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.features, keys2[j]);
                    object.features[keys2[j]] = $root.lnrpc.Feature.toObject(message.features[keys2[j]], options, _depth + 1);
                }
            }
            if (message.custom_records && (keys2 = $Object.keys(message.custom_records)).length) {
                object.custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.custom_records, keys2[j]);
                    object.custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.custom_records[keys2[j]], 0, message.custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_records[keys2[j]]) : message.custom_records[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this LightningNode to JSON.
         * @function toJSON
         * @memberof lnrpc.LightningNode
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LightningNode.prototype.toJSON = function() {
            return LightningNode.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LightningNode
         * @function getTypeUrl
         * @memberof lnrpc.LightningNode
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LightningNode.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.LightningNode";
        };

        return LightningNode;
    })();

    lnrpc.NodeAddress = (function() {

        /**
         * Properties of a NodeAddress.
         * @typedef {Object} lnrpc.NodeAddress.$Properties
         * @property {string|null} [network] NodeAddress network
         * @property {string|null} [addr] NodeAddress addr
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NodeAddress.
         * @memberof lnrpc
         * @interface INodeAddress
         * @augments lnrpc.NodeAddress.$Properties
         * @deprecated Use lnrpc.NodeAddress.$Properties instead.
         */

        /**
         * Shape of a NodeAddress.
         * @typedef {lnrpc.NodeAddress.$Properties} lnrpc.NodeAddress.$Shape
         */

        /**
         * Constructs a new NodeAddress.
         * @memberof lnrpc
         * @classdesc Represents a NodeAddress.
         * @constructor
         * @param {lnrpc.NodeAddress.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NodeAddress = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NodeAddress network.
         * @member {string} network
         * @memberof lnrpc.NodeAddress
         * @instance
         */
        NodeAddress.prototype.network = "";

        /**
         * NodeAddress addr.
         * @member {string} addr
         * @memberof lnrpc.NodeAddress
         * @instance
         */
        NodeAddress.prototype.addr = "";

        /**
         * Creates a new NodeAddress instance using the specified properties.
         * @function create
         * @memberof lnrpc.NodeAddress
         * @static
         * @param {lnrpc.NodeAddress.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NodeAddress} NodeAddress instance
         * @type {{
         *   (properties: lnrpc.NodeAddress.$Shape): lnrpc.NodeAddress & lnrpc.NodeAddress.$Shape;
         *   (properties?: lnrpc.NodeAddress.$Properties): lnrpc.NodeAddress;
         * }}
         */
        NodeAddress.create = function(properties) {
            return new NodeAddress(properties);
        };

        /**
         * Encodes the specified NodeAddress message. Does not implicitly {@link lnrpc.NodeAddress.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NodeAddress
         * @static
         * @param {lnrpc.NodeAddress.$Properties} message NodeAddress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeAddress.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.network != null && $Object.hasOwnProperty.call(message, "network") && message.network !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.network);
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr") && message.addr !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.addr);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeAddress message, length delimited. Does not implicitly {@link lnrpc.NodeAddress.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NodeAddress
         * @static
         * @param {lnrpc.NodeAddress.$Properties} message NodeAddress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeAddress.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NodeAddress message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NodeAddress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NodeAddress & lnrpc.NodeAddress.$Shape} NodeAddress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeAddress.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NodeAddress(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.network = value;
                        else
                            delete message.network;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.addr = value;
                        else
                            delete message.addr;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeAddress message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NodeAddress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NodeAddress & lnrpc.NodeAddress.$Shape} NodeAddress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeAddress.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeAddress message.
         * @function verify
         * @memberof lnrpc.NodeAddress
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeAddress.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.network != null && $Object.hasOwnProperty.call(message, "network"))
                if (!$util.isString(message.network))
                    return "network: string expected";
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                if (!$util.isString(message.addr))
                    return "addr: string expected";
            return null;
        };

        /**
         * Creates a NodeAddress message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NodeAddress
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NodeAddress} NodeAddress
         */
        NodeAddress.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NodeAddress)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NodeAddress: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NodeAddress();
            if (object.network != null)
                if (typeof object.network !== "string" || object.network.length)
                    message.network = $String(object.network);
            if (object.addr != null)
                if (typeof object.addr !== "string" || object.addr.length)
                    message.addr = $String(object.addr);
            return message;
        };

        /**
         * Creates a plain object from a NodeAddress message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NodeAddress
         * @static
         * @param {lnrpc.NodeAddress} message NodeAddress
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeAddress.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.network = "";
                object.addr = "";
            }
            if (message.network != null && $Object.hasOwnProperty.call(message, "network"))
                object.network = message.network;
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                object.addr = message.addr;
            return object;
        };

        /**
         * Converts this NodeAddress to JSON.
         * @function toJSON
         * @memberof lnrpc.NodeAddress
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeAddress.prototype.toJSON = function() {
            return NodeAddress.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeAddress
         * @function getTypeUrl
         * @memberof lnrpc.NodeAddress
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeAddress.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NodeAddress";
        };

        return NodeAddress;
    })();

    lnrpc.RoutingPolicy = (function() {

        /**
         * Properties of a RoutingPolicy.
         * @typedef {Object} lnrpc.RoutingPolicy.$Properties
         * @property {number|null} [time_lock_delta] RoutingPolicy time_lock_delta
         * @property {Long|null} [min_htlc] RoutingPolicy min_htlc
         * @property {Long|null} [fee_base_msat] RoutingPolicy fee_base_msat
         * @property {Long|null} [fee_rate_milli_msat] RoutingPolicy fee_rate_milli_msat
         * @property {boolean|null} [disabled] RoutingPolicy disabled
         * @property {Long|null} [max_htlc_msat] RoutingPolicy max_htlc_msat
         * @property {number|null} [last_update] RoutingPolicy last_update
         * @property {Object.<string,Uint8Array>|null} [custom_records] RoutingPolicy custom_records
         * @property {number|null} [inbound_fee_base_msat] RoutingPolicy inbound_fee_base_msat
         * @property {number|null} [inbound_fee_rate_milli_msat] RoutingPolicy inbound_fee_rate_milli_msat
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RoutingPolicy.
         * @memberof lnrpc
         * @interface IRoutingPolicy
         * @augments lnrpc.RoutingPolicy.$Properties
         * @deprecated Use lnrpc.RoutingPolicy.$Properties instead.
         */

        /**
         * Shape of a RoutingPolicy.
         * @typedef {lnrpc.RoutingPolicy.$Properties} lnrpc.RoutingPolicy.$Shape
         */

        /**
         * Constructs a new RoutingPolicy.
         * @memberof lnrpc
         * @classdesc Represents a RoutingPolicy.
         * @constructor
         * @param {lnrpc.RoutingPolicy.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RoutingPolicy = function (properties) {
            this.custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoutingPolicy time_lock_delta.
         * @member {number} time_lock_delta
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.time_lock_delta = 0;

        /**
         * RoutingPolicy min_htlc.
         * @member {Long} min_htlc
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.min_htlc = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoutingPolicy fee_base_msat.
         * @member {Long} fee_base_msat
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.fee_base_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoutingPolicy fee_rate_milli_msat.
         * @member {Long} fee_rate_milli_msat
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.fee_rate_milli_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoutingPolicy disabled.
         * @member {boolean} disabled
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.disabled = false;

        /**
         * RoutingPolicy max_htlc_msat.
         * @member {Long} max_htlc_msat
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.max_htlc_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RoutingPolicy last_update.
         * @member {number} last_update
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.last_update = 0;

        /**
         * RoutingPolicy custom_records.
         * @member {Object.<string,Uint8Array>} custom_records
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.custom_records = $util.emptyObject;

        /**
         * RoutingPolicy inbound_fee_base_msat.
         * @member {number} inbound_fee_base_msat
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.inbound_fee_base_msat = 0;

        /**
         * RoutingPolicy inbound_fee_rate_milli_msat.
         * @member {number} inbound_fee_rate_milli_msat
         * @memberof lnrpc.RoutingPolicy
         * @instance
         */
        RoutingPolicy.prototype.inbound_fee_rate_milli_msat = 0;

        /**
         * Creates a new RoutingPolicy instance using the specified properties.
         * @function create
         * @memberof lnrpc.RoutingPolicy
         * @static
         * @param {lnrpc.RoutingPolicy.$Properties=} [properties] Properties to set
         * @returns {lnrpc.RoutingPolicy} RoutingPolicy instance
         * @type {{
         *   (properties: lnrpc.RoutingPolicy.$Shape): lnrpc.RoutingPolicy & lnrpc.RoutingPolicy.$Shape;
         *   (properties?: lnrpc.RoutingPolicy.$Properties): lnrpc.RoutingPolicy;
         * }}
         */
        RoutingPolicy.create = function(properties) {
            return new RoutingPolicy(properties);
        };

        /**
         * Encodes the specified RoutingPolicy message. Does not implicitly {@link lnrpc.RoutingPolicy.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.RoutingPolicy
         * @static
         * @param {lnrpc.RoutingPolicy.$Properties} message RoutingPolicy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoutingPolicy.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.time_lock_delta != null && $Object.hasOwnProperty.call(message, "time_lock_delta") && message.time_lock_delta !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.time_lock_delta);
            if (message.min_htlc != null && $Object.hasOwnProperty.call(message, "min_htlc") && (typeof message.min_htlc === "object" ? message.min_htlc.low || message.min_htlc.high : message.min_htlc !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.min_htlc);
            if (message.fee_base_msat != null && $Object.hasOwnProperty.call(message, "fee_base_msat") && (typeof message.fee_base_msat === "object" ? message.fee_base_msat.low || message.fee_base_msat.high : message.fee_base_msat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.fee_base_msat);
            if (message.fee_rate_milli_msat != null && $Object.hasOwnProperty.call(message, "fee_rate_milli_msat") && (typeof message.fee_rate_milli_msat === "object" ? message.fee_rate_milli_msat.low || message.fee_rate_milli_msat.high : message.fee_rate_milli_msat !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.fee_rate_milli_msat);
            if (message.disabled != null && $Object.hasOwnProperty.call(message, "disabled") && message.disabled !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.disabled);
            if (message.max_htlc_msat != null && $Object.hasOwnProperty.call(message, "max_htlc_msat") && (typeof message.max_htlc_msat === "object" ? message.max_htlc_msat.low || message.max_htlc_msat.high : message.max_htlc_msat !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.max_htlc_msat);
            if (message.last_update != null && $Object.hasOwnProperty.call(message, "last_update") && message.last_update !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.last_update);
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records"))
                for (let keys = $Object.keys(message.custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.custom_records[keys[i]]).ldelim();
            if (message.inbound_fee_base_msat != null && $Object.hasOwnProperty.call(message, "inbound_fee_base_msat") && message.inbound_fee_base_msat !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.inbound_fee_base_msat);
            if (message.inbound_fee_rate_milli_msat != null && $Object.hasOwnProperty.call(message, "inbound_fee_rate_milli_msat") && message.inbound_fee_rate_milli_msat !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.inbound_fee_rate_milli_msat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RoutingPolicy message, length delimited. Does not implicitly {@link lnrpc.RoutingPolicy.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.RoutingPolicy
         * @static
         * @param {lnrpc.RoutingPolicy.$Properties} message RoutingPolicy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoutingPolicy.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RoutingPolicy message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.RoutingPolicy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.RoutingPolicy & lnrpc.RoutingPolicy.$Shape} RoutingPolicy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoutingPolicy.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.RoutingPolicy(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time_lock_delta = value;
                        else
                            delete message.time_lock_delta;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.min_htlc = value;
                        else
                            delete message.min_htlc;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_base_msat = value;
                        else
                            delete message.fee_base_msat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_rate_milli_msat = value;
                        else
                            delete message.fee_rate_milli_msat;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.disabled = value;
                        else
                            delete message.disabled;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_htlc_msat = value;
                        else
                            delete message.max_htlc_msat;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_update = value;
                        else
                            delete message.last_update;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (message.custom_records === $util.emptyObject)
                            message.custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.inbound_fee_base_msat = value;
                        else
                            delete message.inbound_fee_base_msat;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.inbound_fee_rate_milli_msat = value;
                        else
                            delete message.inbound_fee_rate_milli_msat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RoutingPolicy message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.RoutingPolicy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.RoutingPolicy & lnrpc.RoutingPolicy.$Shape} RoutingPolicy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoutingPolicy.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoutingPolicy message.
         * @function verify
         * @memberof lnrpc.RoutingPolicy
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoutingPolicy.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.time_lock_delta != null && $Object.hasOwnProperty.call(message, "time_lock_delta"))
                if (!$util.isInteger(message.time_lock_delta))
                    return "time_lock_delta: integer expected";
            if (message.min_htlc != null && $Object.hasOwnProperty.call(message, "min_htlc"))
                if (!$util.isInteger(message.min_htlc) && !(message.min_htlc && $util.isInteger(message.min_htlc.low) && $util.isInteger(message.min_htlc.high)))
                    return "min_htlc: integer|Long expected";
            if (message.fee_base_msat != null && $Object.hasOwnProperty.call(message, "fee_base_msat"))
                if (!$util.isInteger(message.fee_base_msat) && !(message.fee_base_msat && $util.isInteger(message.fee_base_msat.low) && $util.isInteger(message.fee_base_msat.high)))
                    return "fee_base_msat: integer|Long expected";
            if (message.fee_rate_milli_msat != null && $Object.hasOwnProperty.call(message, "fee_rate_milli_msat"))
                if (!$util.isInteger(message.fee_rate_milli_msat) && !(message.fee_rate_milli_msat && $util.isInteger(message.fee_rate_milli_msat.low) && $util.isInteger(message.fee_rate_milli_msat.high)))
                    return "fee_rate_milli_msat: integer|Long expected";
            if (message.disabled != null && $Object.hasOwnProperty.call(message, "disabled"))
                if (typeof message.disabled !== "boolean")
                    return "disabled: boolean expected";
            if (message.max_htlc_msat != null && $Object.hasOwnProperty.call(message, "max_htlc_msat"))
                if (!$util.isInteger(message.max_htlc_msat) && !(message.max_htlc_msat && $util.isInteger(message.max_htlc_msat.low) && $util.isInteger(message.max_htlc_msat.high)))
                    return "max_htlc_msat: integer|Long expected";
            if (message.last_update != null && $Object.hasOwnProperty.call(message, "last_update"))
                if (!$util.isInteger(message.last_update))
                    return "last_update: integer expected";
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records")) {
                if (!$util.isObject(message.custom_records))
                    return "custom_records: object expected";
                let key = $Object.keys(message.custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.custom_records[key[i]] && typeof message.custom_records[key[i]].length === "number" || $util.isString(message.custom_records[key[i]])))
                        return "custom_records: buffer{k:uint64} expected";
                }
            }
            if (message.inbound_fee_base_msat != null && $Object.hasOwnProperty.call(message, "inbound_fee_base_msat"))
                if (!$util.isInteger(message.inbound_fee_base_msat))
                    return "inbound_fee_base_msat: integer expected";
            if (message.inbound_fee_rate_milli_msat != null && $Object.hasOwnProperty.call(message, "inbound_fee_rate_milli_msat"))
                if (!$util.isInteger(message.inbound_fee_rate_milli_msat))
                    return "inbound_fee_rate_milli_msat: integer expected";
            return null;
        };

        /**
         * Creates a RoutingPolicy message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.RoutingPolicy
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.RoutingPolicy} RoutingPolicy
         */
        RoutingPolicy.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.RoutingPolicy)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.RoutingPolicy: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.RoutingPolicy();
            if (object.time_lock_delta != null)
                if ($Number(object.time_lock_delta) !== 0)
                    message.time_lock_delta = object.time_lock_delta >>> 0;
            if (object.min_htlc != null)
                if (typeof object.min_htlc === "object" ? object.min_htlc.low || object.min_htlc.high : $Number(object.min_htlc) !== 0)
                    if ($util.Long)
                        message.min_htlc = $util.Long.fromValue(object.min_htlc, false);
                    else if (typeof object.min_htlc === "string")
                        message.min_htlc = $parseInt(object.min_htlc, 10);
                    else if (typeof object.min_htlc === "number")
                        message.min_htlc = object.min_htlc;
                    else if (typeof object.min_htlc === "object")
                        message.min_htlc = new $util.LongBits(object.min_htlc.low >>> 0, object.min_htlc.high >>> 0).toNumber();
            if (object.fee_base_msat != null)
                if (typeof object.fee_base_msat === "object" ? object.fee_base_msat.low || object.fee_base_msat.high : $Number(object.fee_base_msat) !== 0)
                    if ($util.Long)
                        message.fee_base_msat = $util.Long.fromValue(object.fee_base_msat, false);
                    else if (typeof object.fee_base_msat === "string")
                        message.fee_base_msat = $parseInt(object.fee_base_msat, 10);
                    else if (typeof object.fee_base_msat === "number")
                        message.fee_base_msat = object.fee_base_msat;
                    else if (typeof object.fee_base_msat === "object")
                        message.fee_base_msat = new $util.LongBits(object.fee_base_msat.low >>> 0, object.fee_base_msat.high >>> 0).toNumber();
            if (object.fee_rate_milli_msat != null)
                if (typeof object.fee_rate_milli_msat === "object" ? object.fee_rate_milli_msat.low || object.fee_rate_milli_msat.high : $Number(object.fee_rate_milli_msat) !== 0)
                    if ($util.Long)
                        message.fee_rate_milli_msat = $util.Long.fromValue(object.fee_rate_milli_msat, false);
                    else if (typeof object.fee_rate_milli_msat === "string")
                        message.fee_rate_milli_msat = $parseInt(object.fee_rate_milli_msat, 10);
                    else if (typeof object.fee_rate_milli_msat === "number")
                        message.fee_rate_milli_msat = object.fee_rate_milli_msat;
                    else if (typeof object.fee_rate_milli_msat === "object")
                        message.fee_rate_milli_msat = new $util.LongBits(object.fee_rate_milli_msat.low >>> 0, object.fee_rate_milli_msat.high >>> 0).toNumber();
            if (object.disabled != null)
                if (object.disabled)
                    message.disabled = $Boolean(object.disabled);
            if (object.max_htlc_msat != null)
                if (typeof object.max_htlc_msat === "object" ? object.max_htlc_msat.low || object.max_htlc_msat.high : $Number(object.max_htlc_msat) !== 0)
                    if ($util.Long)
                        message.max_htlc_msat = $util.Long.fromValue(object.max_htlc_msat, true);
                    else if (typeof object.max_htlc_msat === "string")
                        message.max_htlc_msat = $parseInt(object.max_htlc_msat, 10);
                    else if (typeof object.max_htlc_msat === "number")
                        message.max_htlc_msat = object.max_htlc_msat;
                    else if (typeof object.max_htlc_msat === "object")
                        message.max_htlc_msat = new $util.LongBits(object.max_htlc_msat.low >>> 0, object.max_htlc_msat.high >>> 0).toNumber(true);
            if (object.last_update != null)
                if ($Number(object.last_update) !== 0)
                    message.last_update = object.last_update >>> 0;
            if (object.custom_records) {
                if (!$util.isObject(object.custom_records))
                    throw $TypeError(".lnrpc.RoutingPolicy.custom_records: object expected");
                message.custom_records = {};
                for (let keys = $Object.keys(object.custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.custom_records, keys[i]);
                    if (typeof object.custom_records[keys[i]] === "string")
                        $util.base64.decode(object.custom_records[keys[i]], message.custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.custom_records[keys[i]])), 0);
                    else if (object.custom_records[keys[i]].length >= 0)
                        message.custom_records[keys[i]] = object.custom_records[keys[i]];
                }
            }
            if (object.inbound_fee_base_msat != null)
                if ($Number(object.inbound_fee_base_msat) !== 0)
                    message.inbound_fee_base_msat = object.inbound_fee_base_msat | 0;
            if (object.inbound_fee_rate_milli_msat != null)
                if ($Number(object.inbound_fee_rate_milli_msat) !== 0)
                    message.inbound_fee_rate_milli_msat = object.inbound_fee_rate_milli_msat | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoutingPolicy message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.RoutingPolicy
         * @static
         * @param {lnrpc.RoutingPolicy} message RoutingPolicy
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoutingPolicy.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.custom_records = {};
            if (options.defaults) {
                object.time_lock_delta = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.min_htlc = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min_htlc = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_base_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_base_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_rate_milli_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_rate_milli_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.disabled = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.max_htlc_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.max_htlc_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.last_update = 0;
                object.inbound_fee_base_msat = 0;
                object.inbound_fee_rate_milli_msat = 0;
            }
            if (message.time_lock_delta != null && $Object.hasOwnProperty.call(message, "time_lock_delta"))
                object.time_lock_delta = message.time_lock_delta;
            if (message.min_htlc != null && $Object.hasOwnProperty.call(message, "min_htlc"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min_htlc = typeof message.min_htlc === "number" ? $BigInt(message.min_htlc) : $util.Long.fromBits(message.min_htlc.low >>> 0, message.min_htlc.high >>> 0, false).toBigInt();
                else if (typeof message.min_htlc === "number")
                    object.min_htlc = options.longs === $String ? $String(message.min_htlc) : message.min_htlc;
                else
                    object.min_htlc = options.longs === $String ? $util.Long.prototype.toString.call(message.min_htlc) : options.longs === $Number ? new $util.LongBits(message.min_htlc.low >>> 0, message.min_htlc.high >>> 0).toNumber() : message.min_htlc;
            if (message.fee_base_msat != null && $Object.hasOwnProperty.call(message, "fee_base_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_base_msat = typeof message.fee_base_msat === "number" ? $BigInt(message.fee_base_msat) : $util.Long.fromBits(message.fee_base_msat.low >>> 0, message.fee_base_msat.high >>> 0, false).toBigInt();
                else if (typeof message.fee_base_msat === "number")
                    object.fee_base_msat = options.longs === $String ? $String(message.fee_base_msat) : message.fee_base_msat;
                else
                    object.fee_base_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_base_msat) : options.longs === $Number ? new $util.LongBits(message.fee_base_msat.low >>> 0, message.fee_base_msat.high >>> 0).toNumber() : message.fee_base_msat;
            if (message.fee_rate_milli_msat != null && $Object.hasOwnProperty.call(message, "fee_rate_milli_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_rate_milli_msat = typeof message.fee_rate_milli_msat === "number" ? $BigInt(message.fee_rate_milli_msat) : $util.Long.fromBits(message.fee_rate_milli_msat.low >>> 0, message.fee_rate_milli_msat.high >>> 0, false).toBigInt();
                else if (typeof message.fee_rate_milli_msat === "number")
                    object.fee_rate_milli_msat = options.longs === $String ? $String(message.fee_rate_milli_msat) : message.fee_rate_milli_msat;
                else
                    object.fee_rate_milli_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_rate_milli_msat) : options.longs === $Number ? new $util.LongBits(message.fee_rate_milli_msat.low >>> 0, message.fee_rate_milli_msat.high >>> 0).toNumber() : message.fee_rate_milli_msat;
            if (message.disabled != null && $Object.hasOwnProperty.call(message, "disabled"))
                object.disabled = message.disabled;
            if (message.max_htlc_msat != null && $Object.hasOwnProperty.call(message, "max_htlc_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.max_htlc_msat = typeof message.max_htlc_msat === "number" ? $BigInt(message.max_htlc_msat) : $util.Long.fromBits(message.max_htlc_msat.low >>> 0, message.max_htlc_msat.high >>> 0, true).toBigInt();
                else if (typeof message.max_htlc_msat === "number")
                    object.max_htlc_msat = options.longs === $String ? $String(message.max_htlc_msat) : message.max_htlc_msat;
                else
                    object.max_htlc_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.max_htlc_msat) : options.longs === $Number ? new $util.LongBits(message.max_htlc_msat.low >>> 0, message.max_htlc_msat.high >>> 0).toNumber(true) : message.max_htlc_msat;
            if (message.last_update != null && $Object.hasOwnProperty.call(message, "last_update"))
                object.last_update = message.last_update;
            let keys2;
            if (message.custom_records && (keys2 = $Object.keys(message.custom_records)).length) {
                object.custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.custom_records, keys2[j]);
                    object.custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.custom_records[keys2[j]], 0, message.custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_records[keys2[j]]) : message.custom_records[keys2[j]];
                }
            }
            if (message.inbound_fee_base_msat != null && $Object.hasOwnProperty.call(message, "inbound_fee_base_msat"))
                object.inbound_fee_base_msat = message.inbound_fee_base_msat;
            if (message.inbound_fee_rate_milli_msat != null && $Object.hasOwnProperty.call(message, "inbound_fee_rate_milli_msat"))
                object.inbound_fee_rate_milli_msat = message.inbound_fee_rate_milli_msat;
            return object;
        };

        /**
         * Converts this RoutingPolicy to JSON.
         * @function toJSON
         * @memberof lnrpc.RoutingPolicy
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoutingPolicy.prototype.toJSON = function() {
            return RoutingPolicy.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RoutingPolicy
         * @function getTypeUrl
         * @memberof lnrpc.RoutingPolicy
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RoutingPolicy.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.RoutingPolicy";
        };

        return RoutingPolicy;
    })();

    lnrpc.ChannelAuthProof = (function() {

        /**
         * Properties of a ChannelAuthProof.
         * @typedef {Object} lnrpc.ChannelAuthProof.$Properties
         * @property {Uint8Array|null} [node_sig1] ChannelAuthProof node_sig1
         * @property {Uint8Array|null} [bitcoin_sig1] ChannelAuthProof bitcoin_sig1
         * @property {Uint8Array|null} [node_sig2] ChannelAuthProof node_sig2
         * @property {Uint8Array|null} [bitcoin_sig2] ChannelAuthProof bitcoin_sig2
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelAuthProof.
         * @memberof lnrpc
         * @interface IChannelAuthProof
         * @augments lnrpc.ChannelAuthProof.$Properties
         * @deprecated Use lnrpc.ChannelAuthProof.$Properties instead.
         */

        /**
         * Shape of a ChannelAuthProof.
         * @typedef {lnrpc.ChannelAuthProof.$Properties} lnrpc.ChannelAuthProof.$Shape
         */

        /**
         * Constructs a new ChannelAuthProof.
         * @memberof lnrpc
         * @classdesc Represents a ChannelAuthProof.
         * @constructor
         * @param {lnrpc.ChannelAuthProof.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelAuthProof = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelAuthProof node_sig1.
         * @member {Uint8Array} node_sig1
         * @memberof lnrpc.ChannelAuthProof
         * @instance
         */
        ChannelAuthProof.prototype.node_sig1 = $util.newBuffer([]);

        /**
         * ChannelAuthProof bitcoin_sig1.
         * @member {Uint8Array} bitcoin_sig1
         * @memberof lnrpc.ChannelAuthProof
         * @instance
         */
        ChannelAuthProof.prototype.bitcoin_sig1 = $util.newBuffer([]);

        /**
         * ChannelAuthProof node_sig2.
         * @member {Uint8Array} node_sig2
         * @memberof lnrpc.ChannelAuthProof
         * @instance
         */
        ChannelAuthProof.prototype.node_sig2 = $util.newBuffer([]);

        /**
         * ChannelAuthProof bitcoin_sig2.
         * @member {Uint8Array} bitcoin_sig2
         * @memberof lnrpc.ChannelAuthProof
         * @instance
         */
        ChannelAuthProof.prototype.bitcoin_sig2 = $util.newBuffer([]);

        /**
         * Creates a new ChannelAuthProof instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelAuthProof
         * @static
         * @param {lnrpc.ChannelAuthProof.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelAuthProof} ChannelAuthProof instance
         * @type {{
         *   (properties: lnrpc.ChannelAuthProof.$Shape): lnrpc.ChannelAuthProof & lnrpc.ChannelAuthProof.$Shape;
         *   (properties?: lnrpc.ChannelAuthProof.$Properties): lnrpc.ChannelAuthProof;
         * }}
         */
        ChannelAuthProof.create = function(properties) {
            return new ChannelAuthProof(properties);
        };

        /**
         * Encodes the specified ChannelAuthProof message. Does not implicitly {@link lnrpc.ChannelAuthProof.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelAuthProof
         * @static
         * @param {lnrpc.ChannelAuthProof.$Properties} message ChannelAuthProof message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelAuthProof.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.node_sig1 != null && $Object.hasOwnProperty.call(message, "node_sig1") && message.node_sig1.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.node_sig1);
            if (message.bitcoin_sig1 != null && $Object.hasOwnProperty.call(message, "bitcoin_sig1") && message.bitcoin_sig1.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.bitcoin_sig1);
            if (message.node_sig2 != null && $Object.hasOwnProperty.call(message, "node_sig2") && message.node_sig2.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.node_sig2);
            if (message.bitcoin_sig2 != null && $Object.hasOwnProperty.call(message, "bitcoin_sig2") && message.bitcoin_sig2.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.bitcoin_sig2);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelAuthProof message, length delimited. Does not implicitly {@link lnrpc.ChannelAuthProof.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelAuthProof
         * @static
         * @param {lnrpc.ChannelAuthProof.$Properties} message ChannelAuthProof message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelAuthProof.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelAuthProof message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelAuthProof
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelAuthProof & lnrpc.ChannelAuthProof.$Shape} ChannelAuthProof
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelAuthProof.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelAuthProof(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.node_sig1 = value;
                        else
                            delete message.node_sig1;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.bitcoin_sig1 = value;
                        else
                            delete message.bitcoin_sig1;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.node_sig2 = value;
                        else
                            delete message.node_sig2;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.bitcoin_sig2 = value;
                        else
                            delete message.bitcoin_sig2;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelAuthProof message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelAuthProof
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelAuthProof & lnrpc.ChannelAuthProof.$Shape} ChannelAuthProof
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelAuthProof.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelAuthProof message.
         * @function verify
         * @memberof lnrpc.ChannelAuthProof
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelAuthProof.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.node_sig1 != null && $Object.hasOwnProperty.call(message, "node_sig1"))
                if (!(message.node_sig1 && typeof message.node_sig1.length === "number" || $util.isString(message.node_sig1)))
                    return "node_sig1: buffer expected";
            if (message.bitcoin_sig1 != null && $Object.hasOwnProperty.call(message, "bitcoin_sig1"))
                if (!(message.bitcoin_sig1 && typeof message.bitcoin_sig1.length === "number" || $util.isString(message.bitcoin_sig1)))
                    return "bitcoin_sig1: buffer expected";
            if (message.node_sig2 != null && $Object.hasOwnProperty.call(message, "node_sig2"))
                if (!(message.node_sig2 && typeof message.node_sig2.length === "number" || $util.isString(message.node_sig2)))
                    return "node_sig2: buffer expected";
            if (message.bitcoin_sig2 != null && $Object.hasOwnProperty.call(message, "bitcoin_sig2"))
                if (!(message.bitcoin_sig2 && typeof message.bitcoin_sig2.length === "number" || $util.isString(message.bitcoin_sig2)))
                    return "bitcoin_sig2: buffer expected";
            return null;
        };

        /**
         * Creates a ChannelAuthProof message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelAuthProof
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelAuthProof} ChannelAuthProof
         */
        ChannelAuthProof.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelAuthProof)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelAuthProof: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelAuthProof();
            if (object.node_sig1 != null)
                if (object.node_sig1.length)
                    if (typeof object.node_sig1 === "string")
                        $util.base64.decode(object.node_sig1, message.node_sig1 = $util.newBuffer($util.base64.length(object.node_sig1)), 0);
                    else if (object.node_sig1.length >= 0)
                        message.node_sig1 = object.node_sig1;
            if (object.bitcoin_sig1 != null)
                if (object.bitcoin_sig1.length)
                    if (typeof object.bitcoin_sig1 === "string")
                        $util.base64.decode(object.bitcoin_sig1, message.bitcoin_sig1 = $util.newBuffer($util.base64.length(object.bitcoin_sig1)), 0);
                    else if (object.bitcoin_sig1.length >= 0)
                        message.bitcoin_sig1 = object.bitcoin_sig1;
            if (object.node_sig2 != null)
                if (object.node_sig2.length)
                    if (typeof object.node_sig2 === "string")
                        $util.base64.decode(object.node_sig2, message.node_sig2 = $util.newBuffer($util.base64.length(object.node_sig2)), 0);
                    else if (object.node_sig2.length >= 0)
                        message.node_sig2 = object.node_sig2;
            if (object.bitcoin_sig2 != null)
                if (object.bitcoin_sig2.length)
                    if (typeof object.bitcoin_sig2 === "string")
                        $util.base64.decode(object.bitcoin_sig2, message.bitcoin_sig2 = $util.newBuffer($util.base64.length(object.bitcoin_sig2)), 0);
                    else if (object.bitcoin_sig2.length >= 0)
                        message.bitcoin_sig2 = object.bitcoin_sig2;
            return message;
        };

        /**
         * Creates a plain object from a ChannelAuthProof message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelAuthProof
         * @static
         * @param {lnrpc.ChannelAuthProof} message ChannelAuthProof
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelAuthProof.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.node_sig1 = "";
                else {
                    object.node_sig1 = [];
                    if (options.bytes !== $Array)
                        object.node_sig1 = $util.newBuffer(object.node_sig1);
                }
                if (options.bytes === $String)
                    object.bitcoin_sig1 = "";
                else {
                    object.bitcoin_sig1 = [];
                    if (options.bytes !== $Array)
                        object.bitcoin_sig1 = $util.newBuffer(object.bitcoin_sig1);
                }
                if (options.bytes === $String)
                    object.node_sig2 = "";
                else {
                    object.node_sig2 = [];
                    if (options.bytes !== $Array)
                        object.node_sig2 = $util.newBuffer(object.node_sig2);
                }
                if (options.bytes === $String)
                    object.bitcoin_sig2 = "";
                else {
                    object.bitcoin_sig2 = [];
                    if (options.bytes !== $Array)
                        object.bitcoin_sig2 = $util.newBuffer(object.bitcoin_sig2);
                }
            }
            if (message.node_sig1 != null && $Object.hasOwnProperty.call(message, "node_sig1"))
                object.node_sig1 = options.bytes === $String ? $util.base64.encode(message.node_sig1, 0, message.node_sig1.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.node_sig1) : message.node_sig1;
            if (message.bitcoin_sig1 != null && $Object.hasOwnProperty.call(message, "bitcoin_sig1"))
                object.bitcoin_sig1 = options.bytes === $String ? $util.base64.encode(message.bitcoin_sig1, 0, message.bitcoin_sig1.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.bitcoin_sig1) : message.bitcoin_sig1;
            if (message.node_sig2 != null && $Object.hasOwnProperty.call(message, "node_sig2"))
                object.node_sig2 = options.bytes === $String ? $util.base64.encode(message.node_sig2, 0, message.node_sig2.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.node_sig2) : message.node_sig2;
            if (message.bitcoin_sig2 != null && $Object.hasOwnProperty.call(message, "bitcoin_sig2"))
                object.bitcoin_sig2 = options.bytes === $String ? $util.base64.encode(message.bitcoin_sig2, 0, message.bitcoin_sig2.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.bitcoin_sig2) : message.bitcoin_sig2;
            return object;
        };

        /**
         * Converts this ChannelAuthProof to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelAuthProof
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelAuthProof.prototype.toJSON = function() {
            return ChannelAuthProof.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelAuthProof
         * @function getTypeUrl
         * @memberof lnrpc.ChannelAuthProof
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelAuthProof.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelAuthProof";
        };

        return ChannelAuthProof;
    })();

    lnrpc.ChannelEdge = (function() {

        /**
         * Properties of a ChannelEdge.
         * @typedef {Object} lnrpc.ChannelEdge.$Properties
         * @property {Long|null} [channel_id] ChannelEdge channel_id
         * @property {string|null} [chan_point] ChannelEdge chan_point
         * @property {number|null} [last_update] ChannelEdge last_update
         * @property {string|null} [node1_pub] ChannelEdge node1_pub
         * @property {string|null} [node2_pub] ChannelEdge node2_pub
         * @property {Long|null} [capacity] ChannelEdge capacity
         * @property {lnrpc.RoutingPolicy.$Properties|null} [node1_policy] ChannelEdge node1_policy
         * @property {lnrpc.RoutingPolicy.$Properties|null} [node2_policy] ChannelEdge node2_policy
         * @property {Object.<string,Uint8Array>|null} [custom_records] ChannelEdge custom_records
         * @property {lnrpc.ChannelAuthProof.$Properties|null} [auth_proof] ChannelEdge auth_proof
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelEdge.
         * @memberof lnrpc
         * @interface IChannelEdge
         * @augments lnrpc.ChannelEdge.$Properties
         * @deprecated Use lnrpc.ChannelEdge.$Properties instead.
         */

        /**
         * Shape of a ChannelEdge.
         * @typedef {lnrpc.ChannelEdge.$Properties} lnrpc.ChannelEdge.$Shape
         */

        /**
         * Constructs a new ChannelEdge.
         * @memberof lnrpc
         * @classdesc Represents a ChannelEdge.
         * @constructor
         * @param {lnrpc.ChannelEdge.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelEdge = function (properties) {
            this.custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelEdge channel_id.
         * @member {Long} channel_id
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.channel_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelEdge chan_point.
         * @member {string} chan_point
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.chan_point = "";

        /**
         * ChannelEdge last_update.
         * @member {number} last_update
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.last_update = 0;

        /**
         * ChannelEdge node1_pub.
         * @member {string} node1_pub
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.node1_pub = "";

        /**
         * ChannelEdge node2_pub.
         * @member {string} node2_pub
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.node2_pub = "";

        /**
         * ChannelEdge capacity.
         * @member {Long} capacity
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.capacity = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChannelEdge node1_policy.
         * @member {lnrpc.RoutingPolicy.$Properties|null|undefined} node1_policy
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.node1_policy = null;

        /**
         * ChannelEdge node2_policy.
         * @member {lnrpc.RoutingPolicy.$Properties|null|undefined} node2_policy
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.node2_policy = null;

        /**
         * ChannelEdge custom_records.
         * @member {Object.<string,Uint8Array>} custom_records
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.custom_records = $util.emptyObject;

        /**
         * ChannelEdge auth_proof.
         * @member {lnrpc.ChannelAuthProof.$Properties|null|undefined} auth_proof
         * @memberof lnrpc.ChannelEdge
         * @instance
         */
        ChannelEdge.prototype.auth_proof = null;

        /**
         * Creates a new ChannelEdge instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelEdge
         * @static
         * @param {lnrpc.ChannelEdge.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelEdge} ChannelEdge instance
         * @type {{
         *   (properties: lnrpc.ChannelEdge.$Shape): lnrpc.ChannelEdge & lnrpc.ChannelEdge.$Shape;
         *   (properties?: lnrpc.ChannelEdge.$Properties): lnrpc.ChannelEdge;
         * }}
         */
        ChannelEdge.create = function(properties) {
            return new ChannelEdge(properties);
        };

        /**
         * Encodes the specified ChannelEdge message. Does not implicitly {@link lnrpc.ChannelEdge.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelEdge
         * @static
         * @param {lnrpc.ChannelEdge.$Properties} message ChannelEdge message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelEdge.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channel_id != null && $Object.hasOwnProperty.call(message, "channel_id") && (typeof message.channel_id === "object" ? message.channel_id.low || message.channel_id.high : message.channel_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.channel_id);
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point") && message.chan_point !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chan_point);
            if (message.last_update != null && $Object.hasOwnProperty.call(message, "last_update") && message.last_update !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.last_update);
            if (message.node1_pub != null && $Object.hasOwnProperty.call(message, "node1_pub") && message.node1_pub !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.node1_pub);
            if (message.node2_pub != null && $Object.hasOwnProperty.call(message, "node2_pub") && message.node2_pub !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.node2_pub);
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity") && (typeof message.capacity === "object" ? message.capacity.low || message.capacity.high : message.capacity !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.capacity);
            if (message.node1_policy != null && $Object.hasOwnProperty.call(message, "node1_policy"))
                $root.lnrpc.RoutingPolicy.encode(message.node1_policy, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.node2_policy != null && $Object.hasOwnProperty.call(message, "node2_policy"))
                $root.lnrpc.RoutingPolicy.encode(message.node2_policy, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records"))
                for (let keys = $Object.keys(message.custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.custom_records[keys[i]]).ldelim();
            if (message.auth_proof != null && $Object.hasOwnProperty.call(message, "auth_proof"))
                $root.lnrpc.ChannelAuthProof.encode(message.auth_proof, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelEdge message, length delimited. Does not implicitly {@link lnrpc.ChannelEdge.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelEdge
         * @static
         * @param {lnrpc.ChannelEdge.$Properties} message ChannelEdge message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelEdge.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelEdge message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelEdge
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelEdge & lnrpc.ChannelEdge.$Shape} ChannelEdge
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelEdge.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelEdge(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.channel_id = value;
                        else
                            delete message.channel_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.chan_point = value;
                        else
                            delete message.chan_point;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_update = value;
                        else
                            delete message.last_update;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.node1_pub = value;
                        else
                            delete message.node1_pub;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.node2_pub = value;
                        else
                            delete message.node2_pub;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.capacity = value;
                        else
                            delete message.capacity;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.node1_policy = $root.lnrpc.RoutingPolicy.decode(reader, reader.uint32(), $undefined, _depth + 1, message.node1_policy);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.node2_policy = $root.lnrpc.RoutingPolicy.decode(reader, reader.uint32(), $undefined, _depth + 1, message.node2_policy);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (message.custom_records === $util.emptyObject)
                            message.custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.auth_proof = $root.lnrpc.ChannelAuthProof.decode(reader, reader.uint32(), $undefined, _depth + 1, message.auth_proof);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelEdge message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelEdge
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelEdge & lnrpc.ChannelEdge.$Shape} ChannelEdge
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelEdge.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelEdge message.
         * @function verify
         * @memberof lnrpc.ChannelEdge
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelEdge.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channel_id != null && $Object.hasOwnProperty.call(message, "channel_id"))
                if (!$util.isInteger(message.channel_id) && !(message.channel_id && $util.isInteger(message.channel_id.low) && $util.isInteger(message.channel_id.high)))
                    return "channel_id: integer|Long expected";
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                if (!$util.isString(message.chan_point))
                    return "chan_point: string expected";
            if (message.last_update != null && $Object.hasOwnProperty.call(message, "last_update"))
                if (!$util.isInteger(message.last_update))
                    return "last_update: integer expected";
            if (message.node1_pub != null && $Object.hasOwnProperty.call(message, "node1_pub"))
                if (!$util.isString(message.node1_pub))
                    return "node1_pub: string expected";
            if (message.node2_pub != null && $Object.hasOwnProperty.call(message, "node2_pub"))
                if (!$util.isString(message.node2_pub))
                    return "node2_pub: string expected";
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (!$util.isInteger(message.capacity) && !(message.capacity && $util.isInteger(message.capacity.low) && $util.isInteger(message.capacity.high)))
                    return "capacity: integer|Long expected";
            if (message.node1_policy != null && $Object.hasOwnProperty.call(message, "node1_policy")) {
                let error = $root.lnrpc.RoutingPolicy.verify(message.node1_policy, _depth + 1);
                if (error)
                    return "node1_policy." + error;
            }
            if (message.node2_policy != null && $Object.hasOwnProperty.call(message, "node2_policy")) {
                let error = $root.lnrpc.RoutingPolicy.verify(message.node2_policy, _depth + 1);
                if (error)
                    return "node2_policy." + error;
            }
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records")) {
                if (!$util.isObject(message.custom_records))
                    return "custom_records: object expected";
                let key = $Object.keys(message.custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.custom_records[key[i]] && typeof message.custom_records[key[i]].length === "number" || $util.isString(message.custom_records[key[i]])))
                        return "custom_records: buffer{k:uint64} expected";
                }
            }
            if (message.auth_proof != null && $Object.hasOwnProperty.call(message, "auth_proof")) {
                let error = $root.lnrpc.ChannelAuthProof.verify(message.auth_proof, _depth + 1);
                if (error)
                    return "auth_proof." + error;
            }
            return null;
        };

        /**
         * Creates a ChannelEdge message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelEdge
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelEdge} ChannelEdge
         */
        ChannelEdge.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelEdge)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelEdge: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelEdge();
            if (object.channel_id != null)
                if (typeof object.channel_id === "object" ? object.channel_id.low || object.channel_id.high : $Number(object.channel_id) !== 0)
                    if ($util.Long)
                        message.channel_id = $util.Long.fromValue(object.channel_id, true);
                    else if (typeof object.channel_id === "string")
                        message.channel_id = $parseInt(object.channel_id, 10);
                    else if (typeof object.channel_id === "number")
                        message.channel_id = object.channel_id;
                    else if (typeof object.channel_id === "object")
                        message.channel_id = new $util.LongBits(object.channel_id.low >>> 0, object.channel_id.high >>> 0).toNumber(true);
            if (object.chan_point != null)
                if (typeof object.chan_point !== "string" || object.chan_point.length)
                    message.chan_point = $String(object.chan_point);
            if (object.last_update != null)
                if ($Number(object.last_update) !== 0)
                    message.last_update = object.last_update >>> 0;
            if (object.node1_pub != null)
                if (typeof object.node1_pub !== "string" || object.node1_pub.length)
                    message.node1_pub = $String(object.node1_pub);
            if (object.node2_pub != null)
                if (typeof object.node2_pub !== "string" || object.node2_pub.length)
                    message.node2_pub = $String(object.node2_pub);
            if (object.capacity != null)
                if (typeof object.capacity === "object" ? object.capacity.low || object.capacity.high : $Number(object.capacity) !== 0)
                    if ($util.Long)
                        message.capacity = $util.Long.fromValue(object.capacity, false);
                    else if (typeof object.capacity === "string")
                        message.capacity = $parseInt(object.capacity, 10);
                    else if (typeof object.capacity === "number")
                        message.capacity = object.capacity;
                    else if (typeof object.capacity === "object")
                        message.capacity = new $util.LongBits(object.capacity.low >>> 0, object.capacity.high >>> 0).toNumber();
            if (object.node1_policy != null) {
                if (!$util.isObject(object.node1_policy))
                    throw $TypeError(".lnrpc.ChannelEdge.node1_policy: object expected");
                message.node1_policy = $root.lnrpc.RoutingPolicy.fromObject(object.node1_policy, _depth + 1);
            }
            if (object.node2_policy != null) {
                if (!$util.isObject(object.node2_policy))
                    throw $TypeError(".lnrpc.ChannelEdge.node2_policy: object expected");
                message.node2_policy = $root.lnrpc.RoutingPolicy.fromObject(object.node2_policy, _depth + 1);
            }
            if (object.custom_records) {
                if (!$util.isObject(object.custom_records))
                    throw $TypeError(".lnrpc.ChannelEdge.custom_records: object expected");
                message.custom_records = {};
                for (let keys = $Object.keys(object.custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.custom_records, keys[i]);
                    if (typeof object.custom_records[keys[i]] === "string")
                        $util.base64.decode(object.custom_records[keys[i]], message.custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.custom_records[keys[i]])), 0);
                    else if (object.custom_records[keys[i]].length >= 0)
                        message.custom_records[keys[i]] = object.custom_records[keys[i]];
                }
            }
            if (object.auth_proof != null) {
                if (!$util.isObject(object.auth_proof))
                    throw $TypeError(".lnrpc.ChannelEdge.auth_proof: object expected");
                message.auth_proof = $root.lnrpc.ChannelAuthProof.fromObject(object.auth_proof, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ChannelEdge message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelEdge
         * @static
         * @param {lnrpc.ChannelEdge} message ChannelEdge
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelEdge.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.custom_records = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.channel_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.channel_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.chan_point = "";
                object.last_update = 0;
                object.node1_pub = "";
                object.node2_pub = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.capacity = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.capacity = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.node1_policy = null;
                object.node2_policy = null;
                object.auth_proof = null;
            }
            if (message.channel_id != null && $Object.hasOwnProperty.call(message, "channel_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.channel_id = typeof message.channel_id === "number" ? $BigInt(message.channel_id) : $util.Long.fromBits(message.channel_id.low >>> 0, message.channel_id.high >>> 0, true).toBigInt();
                else if (typeof message.channel_id === "number")
                    object.channel_id = options.longs === $String ? $String(message.channel_id) : message.channel_id;
                else
                    object.channel_id = options.longs === $String ? $util.Long.prototype.toString.call(message.channel_id) : options.longs === $Number ? new $util.LongBits(message.channel_id.low >>> 0, message.channel_id.high >>> 0).toNumber(true) : message.channel_id;
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                object.chan_point = message.chan_point;
            if (message.last_update != null && $Object.hasOwnProperty.call(message, "last_update"))
                object.last_update = message.last_update;
            if (message.node1_pub != null && $Object.hasOwnProperty.call(message, "node1_pub"))
                object.node1_pub = message.node1_pub;
            if (message.node2_pub != null && $Object.hasOwnProperty.call(message, "node2_pub"))
                object.node2_pub = message.node2_pub;
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.capacity = typeof message.capacity === "number" ? $BigInt(message.capacity) : $util.Long.fromBits(message.capacity.low >>> 0, message.capacity.high >>> 0, false).toBigInt();
                else if (typeof message.capacity === "number")
                    object.capacity = options.longs === $String ? $String(message.capacity) : message.capacity;
                else
                    object.capacity = options.longs === $String ? $util.Long.prototype.toString.call(message.capacity) : options.longs === $Number ? new $util.LongBits(message.capacity.low >>> 0, message.capacity.high >>> 0).toNumber() : message.capacity;
            if (message.node1_policy != null && $Object.hasOwnProperty.call(message, "node1_policy"))
                object.node1_policy = $root.lnrpc.RoutingPolicy.toObject(message.node1_policy, options, _depth + 1);
            if (message.node2_policy != null && $Object.hasOwnProperty.call(message, "node2_policy"))
                object.node2_policy = $root.lnrpc.RoutingPolicy.toObject(message.node2_policy, options, _depth + 1);
            let keys2;
            if (message.custom_records && (keys2 = $Object.keys(message.custom_records)).length) {
                object.custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.custom_records, keys2[j]);
                    object.custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.custom_records[keys2[j]], 0, message.custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_records[keys2[j]]) : message.custom_records[keys2[j]];
                }
            }
            if (message.auth_proof != null && $Object.hasOwnProperty.call(message, "auth_proof"))
                object.auth_proof = $root.lnrpc.ChannelAuthProof.toObject(message.auth_proof, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ChannelEdge to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelEdge
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelEdge.prototype.toJSON = function() {
            return ChannelEdge.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelEdge
         * @function getTypeUrl
         * @memberof lnrpc.ChannelEdge
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelEdge.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelEdge";
        };

        return ChannelEdge;
    })();

    lnrpc.ChannelGraphRequest = (function() {

        /**
         * Properties of a ChannelGraphRequest.
         * @typedef {Object} lnrpc.ChannelGraphRequest.$Properties
         * @property {boolean|null} [include_unannounced] ChannelGraphRequest include_unannounced
         * @property {boolean|null} [include_auth_proof] ChannelGraphRequest include_auth_proof
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelGraphRequest.
         * @memberof lnrpc
         * @interface IChannelGraphRequest
         * @augments lnrpc.ChannelGraphRequest.$Properties
         * @deprecated Use lnrpc.ChannelGraphRequest.$Properties instead.
         */

        /**
         * Shape of a ChannelGraphRequest.
         * @typedef {lnrpc.ChannelGraphRequest.$Properties} lnrpc.ChannelGraphRequest.$Shape
         */

        /**
         * Constructs a new ChannelGraphRequest.
         * @memberof lnrpc
         * @classdesc Represents a ChannelGraphRequest.
         * @constructor
         * @param {lnrpc.ChannelGraphRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelGraphRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelGraphRequest include_unannounced.
         * @member {boolean} include_unannounced
         * @memberof lnrpc.ChannelGraphRequest
         * @instance
         */
        ChannelGraphRequest.prototype.include_unannounced = false;

        /**
         * ChannelGraphRequest include_auth_proof.
         * @member {boolean} include_auth_proof
         * @memberof lnrpc.ChannelGraphRequest
         * @instance
         */
        ChannelGraphRequest.prototype.include_auth_proof = false;

        /**
         * Creates a new ChannelGraphRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelGraphRequest
         * @static
         * @param {lnrpc.ChannelGraphRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelGraphRequest} ChannelGraphRequest instance
         * @type {{
         *   (properties: lnrpc.ChannelGraphRequest.$Shape): lnrpc.ChannelGraphRequest & lnrpc.ChannelGraphRequest.$Shape;
         *   (properties?: lnrpc.ChannelGraphRequest.$Properties): lnrpc.ChannelGraphRequest;
         * }}
         */
        ChannelGraphRequest.create = function(properties) {
            return new ChannelGraphRequest(properties);
        };

        /**
         * Encodes the specified ChannelGraphRequest message. Does not implicitly {@link lnrpc.ChannelGraphRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelGraphRequest
         * @static
         * @param {lnrpc.ChannelGraphRequest.$Properties} message ChannelGraphRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelGraphRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.include_unannounced != null && $Object.hasOwnProperty.call(message, "include_unannounced") && message.include_unannounced !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.include_unannounced);
            if (message.include_auth_proof != null && $Object.hasOwnProperty.call(message, "include_auth_proof") && message.include_auth_proof !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.include_auth_proof);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelGraphRequest message, length delimited. Does not implicitly {@link lnrpc.ChannelGraphRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelGraphRequest
         * @static
         * @param {lnrpc.ChannelGraphRequest.$Properties} message ChannelGraphRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelGraphRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelGraphRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelGraphRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelGraphRequest & lnrpc.ChannelGraphRequest.$Shape} ChannelGraphRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelGraphRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelGraphRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_unannounced = value;
                        else
                            delete message.include_unannounced;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_auth_proof = value;
                        else
                            delete message.include_auth_proof;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelGraphRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelGraphRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelGraphRequest & lnrpc.ChannelGraphRequest.$Shape} ChannelGraphRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelGraphRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelGraphRequest message.
         * @function verify
         * @memberof lnrpc.ChannelGraphRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelGraphRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.include_unannounced != null && $Object.hasOwnProperty.call(message, "include_unannounced"))
                if (typeof message.include_unannounced !== "boolean")
                    return "include_unannounced: boolean expected";
            if (message.include_auth_proof != null && $Object.hasOwnProperty.call(message, "include_auth_proof"))
                if (typeof message.include_auth_proof !== "boolean")
                    return "include_auth_proof: boolean expected";
            return null;
        };

        /**
         * Creates a ChannelGraphRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelGraphRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelGraphRequest} ChannelGraphRequest
         */
        ChannelGraphRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelGraphRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelGraphRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelGraphRequest();
            if (object.include_unannounced != null)
                if (object.include_unannounced)
                    message.include_unannounced = $Boolean(object.include_unannounced);
            if (object.include_auth_proof != null)
                if (object.include_auth_proof)
                    message.include_auth_proof = $Boolean(object.include_auth_proof);
            return message;
        };

        /**
         * Creates a plain object from a ChannelGraphRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelGraphRequest
         * @static
         * @param {lnrpc.ChannelGraphRequest} message ChannelGraphRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelGraphRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.include_unannounced = false;
                object.include_auth_proof = false;
            }
            if (message.include_unannounced != null && $Object.hasOwnProperty.call(message, "include_unannounced"))
                object.include_unannounced = message.include_unannounced;
            if (message.include_auth_proof != null && $Object.hasOwnProperty.call(message, "include_auth_proof"))
                object.include_auth_proof = message.include_auth_proof;
            return object;
        };

        /**
         * Converts this ChannelGraphRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelGraphRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelGraphRequest.prototype.toJSON = function() {
            return ChannelGraphRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelGraphRequest
         * @function getTypeUrl
         * @memberof lnrpc.ChannelGraphRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelGraphRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelGraphRequest";
        };

        return ChannelGraphRequest;
    })();

    lnrpc.ChannelGraph = (function() {

        /**
         * Properties of a ChannelGraph.
         * @typedef {Object} lnrpc.ChannelGraph.$Properties
         * @property {Array.<lnrpc.LightningNode.$Properties>|null} [nodes] ChannelGraph nodes
         * @property {Array.<lnrpc.ChannelEdge.$Properties>|null} [edges] ChannelGraph edges
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelGraph.
         * @memberof lnrpc
         * @interface IChannelGraph
         * @augments lnrpc.ChannelGraph.$Properties
         * @deprecated Use lnrpc.ChannelGraph.$Properties instead.
         */

        /**
         * Shape of a ChannelGraph.
         * @typedef {lnrpc.ChannelGraph.$Properties} lnrpc.ChannelGraph.$Shape
         */

        /**
         * Constructs a new ChannelGraph.
         * @memberof lnrpc
         * @classdesc Represents a ChannelGraph.
         * @constructor
         * @param {lnrpc.ChannelGraph.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelGraph = function (properties) {
            this.nodes = [];
            this.edges = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelGraph nodes.
         * @member {Array.<lnrpc.LightningNode.$Properties>} nodes
         * @memberof lnrpc.ChannelGraph
         * @instance
         */
        ChannelGraph.prototype.nodes = $util.emptyArray;

        /**
         * ChannelGraph edges.
         * @member {Array.<lnrpc.ChannelEdge.$Properties>} edges
         * @memberof lnrpc.ChannelGraph
         * @instance
         */
        ChannelGraph.prototype.edges = $util.emptyArray;

        /**
         * Creates a new ChannelGraph instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelGraph
         * @static
         * @param {lnrpc.ChannelGraph.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelGraph} ChannelGraph instance
         * @type {{
         *   (properties: lnrpc.ChannelGraph.$Shape): lnrpc.ChannelGraph & lnrpc.ChannelGraph.$Shape;
         *   (properties?: lnrpc.ChannelGraph.$Properties): lnrpc.ChannelGraph;
         * }}
         */
        ChannelGraph.create = function(properties) {
            return new ChannelGraph(properties);
        };

        /**
         * Encodes the specified ChannelGraph message. Does not implicitly {@link lnrpc.ChannelGraph.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelGraph
         * @static
         * @param {lnrpc.ChannelGraph.$Properties} message ChannelGraph message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelGraph.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodes != null && message.nodes.length)
                for (let i = 0; i < message.nodes.length; ++i)
                    $root.lnrpc.LightningNode.encode(message.nodes[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.edges != null && message.edges.length)
                for (let i = 0; i < message.edges.length; ++i)
                    $root.lnrpc.ChannelEdge.encode(message.edges[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelGraph message, length delimited. Does not implicitly {@link lnrpc.ChannelGraph.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelGraph
         * @static
         * @param {lnrpc.ChannelGraph.$Properties} message ChannelGraph message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelGraph.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelGraph message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelGraph
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelGraph & lnrpc.ChannelGraph.$Shape} ChannelGraph
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelGraph.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelGraph();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nodes && message.nodes.length))
                            message.nodes = [];
                        message.nodes.push($root.lnrpc.LightningNode.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.edges && message.edges.length))
                            message.edges = [];
                        message.edges.push($root.lnrpc.ChannelEdge.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelGraph message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelGraph
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelGraph & lnrpc.ChannelGraph.$Shape} ChannelGraph
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelGraph.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelGraph message.
         * @function verify
         * @memberof lnrpc.ChannelGraph
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelGraph.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodes != null && $Object.hasOwnProperty.call(message, "nodes")) {
                if (!$Array.isArray(message.nodes))
                    return "nodes: array expected";
                for (let i = 0; i < message.nodes.length; ++i) {
                    let error = $root.lnrpc.LightningNode.verify(message.nodes[i], _depth + 1);
                    if (error)
                        return "nodes." + error;
                }
            }
            if (message.edges != null && $Object.hasOwnProperty.call(message, "edges")) {
                if (!$Array.isArray(message.edges))
                    return "edges: array expected";
                for (let i = 0; i < message.edges.length; ++i) {
                    let error = $root.lnrpc.ChannelEdge.verify(message.edges[i], _depth + 1);
                    if (error)
                        return "edges." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ChannelGraph message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelGraph
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelGraph} ChannelGraph
         */
        ChannelGraph.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelGraph)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelGraph: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelGraph();
            if (object.nodes) {
                if (!$Array.isArray(object.nodes))
                    throw $TypeError(".lnrpc.ChannelGraph.nodes: array expected");
                message.nodes = $Array(object.nodes.length);
                for (let i = 0; i < object.nodes.length; ++i) {
                    if (!$util.isObject(object.nodes[i]))
                        throw $TypeError(".lnrpc.ChannelGraph.nodes: object expected");
                    message.nodes[i] = $root.lnrpc.LightningNode.fromObject(object.nodes[i], _depth + 1);
                }
            }
            if (object.edges) {
                if (!$Array.isArray(object.edges))
                    throw $TypeError(".lnrpc.ChannelGraph.edges: array expected");
                message.edges = $Array(object.edges.length);
                for (let i = 0; i < object.edges.length; ++i) {
                    if (!$util.isObject(object.edges[i]))
                        throw $TypeError(".lnrpc.ChannelGraph.edges: object expected");
                    message.edges[i] = $root.lnrpc.ChannelEdge.fromObject(object.edges[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ChannelGraph message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelGraph
         * @static
         * @param {lnrpc.ChannelGraph} message ChannelGraph
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelGraph.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.nodes = [];
                object.edges = [];
            }
            if (message.nodes && message.nodes.length) {
                object.nodes = $Array(message.nodes.length);
                for (let j = 0; j < message.nodes.length; ++j)
                    object.nodes[j] = $root.lnrpc.LightningNode.toObject(message.nodes[j], options, _depth + 1);
            }
            if (message.edges && message.edges.length) {
                object.edges = $Array(message.edges.length);
                for (let j = 0; j < message.edges.length; ++j)
                    object.edges[j] = $root.lnrpc.ChannelEdge.toObject(message.edges[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ChannelGraph to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelGraph
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelGraph.prototype.toJSON = function() {
            return ChannelGraph.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelGraph
         * @function getTypeUrl
         * @memberof lnrpc.ChannelGraph
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelGraph.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelGraph";
        };

        return ChannelGraph;
    })();

    /**
     * NodeMetricType enum.
     * @name lnrpc.NodeMetricType
     * @enum {number}
     * @property {number} UNKNOWN=0 UNKNOWN value
     * @property {number} BETWEENNESS_CENTRALITY=1 BETWEENNESS_CENTRALITY value
     */
    lnrpc.NodeMetricType = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "BETWEENNESS_CENTRALITY"] = 1;
        return values;
    })();

    lnrpc.NodeMetricsRequest = (function() {

        /**
         * Properties of a NodeMetricsRequest.
         * @typedef {Object} lnrpc.NodeMetricsRequest.$Properties
         * @property {Array.<lnrpc.NodeMetricType>|null} [types] NodeMetricsRequest types
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NodeMetricsRequest.
         * @memberof lnrpc
         * @interface INodeMetricsRequest
         * @augments lnrpc.NodeMetricsRequest.$Properties
         * @deprecated Use lnrpc.NodeMetricsRequest.$Properties instead.
         */

        /**
         * Shape of a NodeMetricsRequest.
         * @typedef {lnrpc.NodeMetricsRequest.$Properties} lnrpc.NodeMetricsRequest.$Shape
         */

        /**
         * Constructs a new NodeMetricsRequest.
         * @memberof lnrpc
         * @classdesc Represents a NodeMetricsRequest.
         * @constructor
         * @param {lnrpc.NodeMetricsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NodeMetricsRequest = function (properties) {
            this.types = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NodeMetricsRequest types.
         * @member {Array.<lnrpc.NodeMetricType>} types
         * @memberof lnrpc.NodeMetricsRequest
         * @instance
         */
        NodeMetricsRequest.prototype.types = $util.emptyArray;

        /**
         * Creates a new NodeMetricsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.NodeMetricsRequest
         * @static
         * @param {lnrpc.NodeMetricsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NodeMetricsRequest} NodeMetricsRequest instance
         * @type {{
         *   (properties: lnrpc.NodeMetricsRequest.$Shape): lnrpc.NodeMetricsRequest & lnrpc.NodeMetricsRequest.$Shape;
         *   (properties?: lnrpc.NodeMetricsRequest.$Properties): lnrpc.NodeMetricsRequest;
         * }}
         */
        NodeMetricsRequest.create = function(properties) {
            return new NodeMetricsRequest(properties);
        };

        /**
         * Encodes the specified NodeMetricsRequest message. Does not implicitly {@link lnrpc.NodeMetricsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NodeMetricsRequest
         * @static
         * @param {lnrpc.NodeMetricsRequest.$Properties} message NodeMetricsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeMetricsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.types != null && message.types.length)
                writer.uint32(/* id 1, wireType 2 =*/10).int32s(message.types);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeMetricsRequest message, length delimited. Does not implicitly {@link lnrpc.NodeMetricsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NodeMetricsRequest
         * @static
         * @param {lnrpc.NodeMetricsRequest.$Properties} message NodeMetricsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeMetricsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NodeMetricsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NodeMetricsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NodeMetricsRequest & lnrpc.NodeMetricsRequest.$Shape} NodeMetricsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeMetricsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NodeMetricsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.types && message.types.length))
                                message.types = [];
                            reader.int32s(message.types);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.types && message.types.length))
                            message.types = [];
                        message.types.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeMetricsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NodeMetricsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NodeMetricsRequest & lnrpc.NodeMetricsRequest.$Shape} NodeMetricsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeMetricsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeMetricsRequest message.
         * @function verify
         * @memberof lnrpc.NodeMetricsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeMetricsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.types != null && $Object.hasOwnProperty.call(message, "types")) {
                if (!$Array.isArray(message.types))
                    return "types: array expected";
                for (let i = 0; i < message.types.length; ++i)
                    if (typeof message.types[i] !== "number" || (message.types[i] | 0) !== message.types[i])
                        return "types: enum value[] expected";
            }
            return null;
        };

        /**
         * Creates a NodeMetricsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NodeMetricsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NodeMetricsRequest} NodeMetricsRequest
         */
        NodeMetricsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NodeMetricsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NodeMetricsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NodeMetricsRequest();
            if (object.types) {
                if (!$Array.isArray(object.types))
                    throw $TypeError(".lnrpc.NodeMetricsRequest.types: array expected");
                message.types = [];
                for (let i = 0; i < object.types.length; ++i)
                    switch (object.types[i]) {
                    case "UNKNOWN":
                    case 0:
                        message.types[message.types.length] = 0;
                        break;
                    case "BETWEENNESS_CENTRALITY":
                    case 1:
                        message.types[message.types.length] = 1;
                        break;
                    default:
                        if (typeof object.types[i] === "number" && (object.types[i] | 0) === object.types[i])
                            message.types[message.types.length] = object.types[i];
                    }
            }
            return message;
        };

        /**
         * Creates a plain object from a NodeMetricsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NodeMetricsRequest
         * @static
         * @param {lnrpc.NodeMetricsRequest} message NodeMetricsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeMetricsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.types = [];
            if (message.types && message.types.length) {
                object.types = $Array(message.types.length);
                for (let j = 0; j < message.types.length; ++j)
                    object.types[j] = options.enums === $String ? $root.lnrpc.NodeMetricType[message.types[j]] === $undefined ? message.types[j] : $root.lnrpc.NodeMetricType[message.types[j]] : message.types[j];
            }
            return object;
        };

        /**
         * Converts this NodeMetricsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.NodeMetricsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeMetricsRequest.prototype.toJSON = function() {
            return NodeMetricsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeMetricsRequest
         * @function getTypeUrl
         * @memberof lnrpc.NodeMetricsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeMetricsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NodeMetricsRequest";
        };

        return NodeMetricsRequest;
    })();

    lnrpc.NodeMetricsResponse = (function() {

        /**
         * Properties of a NodeMetricsResponse.
         * @typedef {Object} lnrpc.NodeMetricsResponse.$Properties
         * @property {Object.<string,lnrpc.FloatMetric.$Properties>|null} [betweenness_centrality] NodeMetricsResponse betweenness_centrality
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NodeMetricsResponse.
         * @memberof lnrpc
         * @interface INodeMetricsResponse
         * @augments lnrpc.NodeMetricsResponse.$Properties
         * @deprecated Use lnrpc.NodeMetricsResponse.$Properties instead.
         */

        /**
         * Shape of a NodeMetricsResponse.
         * @typedef {lnrpc.NodeMetricsResponse.$Properties} lnrpc.NodeMetricsResponse.$Shape
         */

        /**
         * Constructs a new NodeMetricsResponse.
         * @memberof lnrpc
         * @classdesc Represents a NodeMetricsResponse.
         * @constructor
         * @param {lnrpc.NodeMetricsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NodeMetricsResponse = function (properties) {
            this.betweenness_centrality = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NodeMetricsResponse betweenness_centrality.
         * @member {Object.<string,lnrpc.FloatMetric.$Properties>} betweenness_centrality
         * @memberof lnrpc.NodeMetricsResponse
         * @instance
         */
        NodeMetricsResponse.prototype.betweenness_centrality = $util.emptyObject;

        /**
         * Creates a new NodeMetricsResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.NodeMetricsResponse
         * @static
         * @param {lnrpc.NodeMetricsResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NodeMetricsResponse} NodeMetricsResponse instance
         * @type {{
         *   (properties: lnrpc.NodeMetricsResponse.$Shape): lnrpc.NodeMetricsResponse & lnrpc.NodeMetricsResponse.$Shape;
         *   (properties?: lnrpc.NodeMetricsResponse.$Properties): lnrpc.NodeMetricsResponse;
         * }}
         */
        NodeMetricsResponse.create = function(properties) {
            return new NodeMetricsResponse(properties);
        };

        /**
         * Encodes the specified NodeMetricsResponse message. Does not implicitly {@link lnrpc.NodeMetricsResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NodeMetricsResponse
         * @static
         * @param {lnrpc.NodeMetricsResponse.$Properties} message NodeMetricsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeMetricsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.betweenness_centrality != null && $Object.hasOwnProperty.call(message, "betweenness_centrality"))
                for (let keys = $Object.keys(message.betweenness_centrality), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.lnrpc.FloatMetric.encode(message.betweenness_centrality[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeMetricsResponse message, length delimited. Does not implicitly {@link lnrpc.NodeMetricsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NodeMetricsResponse
         * @static
         * @param {lnrpc.NodeMetricsResponse.$Properties} message NodeMetricsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeMetricsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NodeMetricsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NodeMetricsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NodeMetricsResponse & lnrpc.NodeMetricsResponse.$Shape} NodeMetricsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeMetricsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NodeMetricsResponse(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (message.betweenness_centrality === $util.emptyObject)
                            message.betweenness_centrality = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.FloatMetric.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.betweenness_centrality, key);
                        message.betweenness_centrality[key] = value || new $root.lnrpc.FloatMetric();
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeMetricsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NodeMetricsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NodeMetricsResponse & lnrpc.NodeMetricsResponse.$Shape} NodeMetricsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeMetricsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeMetricsResponse message.
         * @function verify
         * @memberof lnrpc.NodeMetricsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeMetricsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.betweenness_centrality != null && $Object.hasOwnProperty.call(message, "betweenness_centrality")) {
                if (!$util.isObject(message.betweenness_centrality))
                    return "betweenness_centrality: object expected";
                let key = $Object.keys(message.betweenness_centrality);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.lnrpc.FloatMetric.verify(message.betweenness_centrality[key[i]], _depth + 1);
                    if (error)
                        return "betweenness_centrality." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NodeMetricsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NodeMetricsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NodeMetricsResponse} NodeMetricsResponse
         */
        NodeMetricsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NodeMetricsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NodeMetricsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NodeMetricsResponse();
            if (object.betweenness_centrality) {
                if (!$util.isObject(object.betweenness_centrality))
                    throw $TypeError(".lnrpc.NodeMetricsResponse.betweenness_centrality: object expected");
                message.betweenness_centrality = {};
                for (let keys = $Object.keys(object.betweenness_centrality), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.betweenness_centrality, keys[i]);
                    if (!$util.isObject(object.betweenness_centrality[keys[i]]))
                        throw $TypeError(".lnrpc.NodeMetricsResponse.betweenness_centrality: object expected");
                    message.betweenness_centrality[keys[i]] = $root.lnrpc.FloatMetric.fromObject(object.betweenness_centrality[keys[i]], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NodeMetricsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NodeMetricsResponse
         * @static
         * @param {lnrpc.NodeMetricsResponse} message NodeMetricsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeMetricsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.betweenness_centrality = {};
            let keys2;
            if (message.betweenness_centrality && (keys2 = $Object.keys(message.betweenness_centrality)).length) {
                object.betweenness_centrality = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.betweenness_centrality, keys2[j]);
                    object.betweenness_centrality[keys2[j]] = $root.lnrpc.FloatMetric.toObject(message.betweenness_centrality[keys2[j]], options, _depth + 1);
                }
            }
            return object;
        };

        /**
         * Converts this NodeMetricsResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.NodeMetricsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeMetricsResponse.prototype.toJSON = function() {
            return NodeMetricsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeMetricsResponse
         * @function getTypeUrl
         * @memberof lnrpc.NodeMetricsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeMetricsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NodeMetricsResponse";
        };

        return NodeMetricsResponse;
    })();

    lnrpc.FloatMetric = (function() {

        /**
         * Properties of a FloatMetric.
         * @typedef {Object} lnrpc.FloatMetric.$Properties
         * @property {number|null} [value] FloatMetric value
         * @property {number|null} [normalized_value] FloatMetric normalized_value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FloatMetric.
         * @memberof lnrpc
         * @interface IFloatMetric
         * @augments lnrpc.FloatMetric.$Properties
         * @deprecated Use lnrpc.FloatMetric.$Properties instead.
         */

        /**
         * Shape of a FloatMetric.
         * @typedef {lnrpc.FloatMetric.$Properties} lnrpc.FloatMetric.$Shape
         */

        /**
         * Constructs a new FloatMetric.
         * @memberof lnrpc
         * @classdesc Represents a FloatMetric.
         * @constructor
         * @param {lnrpc.FloatMetric.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FloatMetric = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FloatMetric value.
         * @member {number} value
         * @memberof lnrpc.FloatMetric
         * @instance
         */
        FloatMetric.prototype.value = 0;

        /**
         * FloatMetric normalized_value.
         * @member {number} normalized_value
         * @memberof lnrpc.FloatMetric
         * @instance
         */
        FloatMetric.prototype.normalized_value = 0;

        /**
         * Creates a new FloatMetric instance using the specified properties.
         * @function create
         * @memberof lnrpc.FloatMetric
         * @static
         * @param {lnrpc.FloatMetric.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FloatMetric} FloatMetric instance
         * @type {{
         *   (properties: lnrpc.FloatMetric.$Shape): lnrpc.FloatMetric & lnrpc.FloatMetric.$Shape;
         *   (properties?: lnrpc.FloatMetric.$Properties): lnrpc.FloatMetric;
         * }}
         */
        FloatMetric.create = function(properties) {
            return new FloatMetric(properties);
        };

        /**
         * Encodes the specified FloatMetric message. Does not implicitly {@link lnrpc.FloatMetric.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FloatMetric
         * @static
         * @param {lnrpc.FloatMetric.$Properties} message FloatMetric message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FloatMetric.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.value);
            if (message.normalized_value != null && $Object.hasOwnProperty.call(message, "normalized_value") && !$Object.is(message.normalized_value, 0))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.normalized_value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FloatMetric message, length delimited. Does not implicitly {@link lnrpc.FloatMetric.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FloatMetric
         * @static
         * @param {lnrpc.FloatMetric.$Properties} message FloatMetric message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FloatMetric.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FloatMetric message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FloatMetric
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FloatMetric & lnrpc.FloatMetric.$Shape} FloatMetric
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FloatMetric.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FloatMetric(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 2: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.normalized_value = value;
                        else
                            delete message.normalized_value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FloatMetric message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FloatMetric
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FloatMetric & lnrpc.FloatMetric.$Shape} FloatMetric
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FloatMetric.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FloatMetric message.
         * @function verify
         * @memberof lnrpc.FloatMetric
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FloatMetric.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof message.value !== "number")
                    return "value: number expected";
            if (message.normalized_value != null && $Object.hasOwnProperty.call(message, "normalized_value"))
                if (typeof message.normalized_value !== "number")
                    return "normalized_value: number expected";
            return null;
        };

        /**
         * Creates a FloatMetric message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FloatMetric
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FloatMetric} FloatMetric
         */
        FloatMetric.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FloatMetric)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FloatMetric: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.FloatMetric();
            if (object.value != null)
                if (!$Object.is($Number(object.value), 0))
                    message.value = $Number(object.value);
            if (object.normalized_value != null)
                if (!$Object.is($Number(object.normalized_value), 0))
                    message.normalized_value = $Number(object.normalized_value);
            return message;
        };

        /**
         * Creates a plain object from a FloatMetric message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FloatMetric
         * @static
         * @param {lnrpc.FloatMetric} message FloatMetric
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FloatMetric.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.value = 0;
                object.normalized_value = 0;
            }
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
            if (message.normalized_value != null && $Object.hasOwnProperty.call(message, "normalized_value"))
                object.normalized_value = options.json && !$isFinite(message.normalized_value) ? $String(message.normalized_value) : message.normalized_value;
            return object;
        };

        /**
         * Converts this FloatMetric to JSON.
         * @function toJSON
         * @memberof lnrpc.FloatMetric
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FloatMetric.prototype.toJSON = function() {
            return FloatMetric.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FloatMetric
         * @function getTypeUrl
         * @memberof lnrpc.FloatMetric
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FloatMetric.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FloatMetric";
        };

        return FloatMetric;
    })();

    lnrpc.ChanInfoRequest = (function() {

        /**
         * Properties of a ChanInfoRequest.
         * @typedef {Object} lnrpc.ChanInfoRequest.$Properties
         * @property {Long|null} [chan_id] ChanInfoRequest chan_id
         * @property {string|null} [chan_point] ChanInfoRequest chan_point
         * @property {boolean|null} [include_auth_proof] ChanInfoRequest include_auth_proof
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChanInfoRequest.
         * @memberof lnrpc
         * @interface IChanInfoRequest
         * @augments lnrpc.ChanInfoRequest.$Properties
         * @deprecated Use lnrpc.ChanInfoRequest.$Properties instead.
         */

        /**
         * Shape of a ChanInfoRequest.
         * @typedef {lnrpc.ChanInfoRequest.$Properties} lnrpc.ChanInfoRequest.$Shape
         */

        /**
         * Constructs a new ChanInfoRequest.
         * @memberof lnrpc
         * @classdesc Represents a ChanInfoRequest.
         * @constructor
         * @param {lnrpc.ChanInfoRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChanInfoRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChanInfoRequest chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.ChanInfoRequest
         * @instance
         */
        ChanInfoRequest.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChanInfoRequest chan_point.
         * @member {string} chan_point
         * @memberof lnrpc.ChanInfoRequest
         * @instance
         */
        ChanInfoRequest.prototype.chan_point = "";

        /**
         * ChanInfoRequest include_auth_proof.
         * @member {boolean} include_auth_proof
         * @memberof lnrpc.ChanInfoRequest
         * @instance
         */
        ChanInfoRequest.prototype.include_auth_proof = false;

        /**
         * Creates a new ChanInfoRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChanInfoRequest
         * @static
         * @param {lnrpc.ChanInfoRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChanInfoRequest} ChanInfoRequest instance
         * @type {{
         *   (properties: lnrpc.ChanInfoRequest.$Shape): lnrpc.ChanInfoRequest & lnrpc.ChanInfoRequest.$Shape;
         *   (properties?: lnrpc.ChanInfoRequest.$Properties): lnrpc.ChanInfoRequest;
         * }}
         */
        ChanInfoRequest.create = function(properties) {
            return new ChanInfoRequest(properties);
        };

        /**
         * Encodes the specified ChanInfoRequest message. Does not implicitly {@link lnrpc.ChanInfoRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChanInfoRequest
         * @static
         * @param {lnrpc.ChanInfoRequest.$Properties} message ChanInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChanInfoRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.chan_id);
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point") && message.chan_point !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chan_point);
            if (message.include_auth_proof != null && $Object.hasOwnProperty.call(message, "include_auth_proof") && message.include_auth_proof !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.include_auth_proof);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChanInfoRequest message, length delimited. Does not implicitly {@link lnrpc.ChanInfoRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChanInfoRequest
         * @static
         * @param {lnrpc.ChanInfoRequest.$Properties} message ChanInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChanInfoRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChanInfoRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChanInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChanInfoRequest & lnrpc.ChanInfoRequest.$Shape} ChanInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChanInfoRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChanInfoRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.chan_point = value;
                        else
                            delete message.chan_point;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_auth_proof = value;
                        else
                            delete message.include_auth_proof;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChanInfoRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChanInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChanInfoRequest & lnrpc.ChanInfoRequest.$Shape} ChanInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChanInfoRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChanInfoRequest message.
         * @function verify
         * @memberof lnrpc.ChanInfoRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChanInfoRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                if (!$util.isString(message.chan_point))
                    return "chan_point: string expected";
            if (message.include_auth_proof != null && $Object.hasOwnProperty.call(message, "include_auth_proof"))
                if (typeof message.include_auth_proof !== "boolean")
                    return "include_auth_proof: boolean expected";
            return null;
        };

        /**
         * Creates a ChanInfoRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChanInfoRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChanInfoRequest} ChanInfoRequest
         */
        ChanInfoRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChanInfoRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChanInfoRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChanInfoRequest();
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.chan_point != null)
                if (typeof object.chan_point !== "string" || object.chan_point.length)
                    message.chan_point = $String(object.chan_point);
            if (object.include_auth_proof != null)
                if (object.include_auth_proof)
                    message.include_auth_proof = $Boolean(object.include_auth_proof);
            return message;
        };

        /**
         * Creates a plain object from a ChanInfoRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChanInfoRequest
         * @static
         * @param {lnrpc.ChanInfoRequest} message ChanInfoRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChanInfoRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.chan_point = "";
                object.include_auth_proof = false;
            }
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                object.chan_point = message.chan_point;
            if (message.include_auth_proof != null && $Object.hasOwnProperty.call(message, "include_auth_proof"))
                object.include_auth_proof = message.include_auth_proof;
            return object;
        };

        /**
         * Converts this ChanInfoRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ChanInfoRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChanInfoRequest.prototype.toJSON = function() {
            return ChanInfoRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChanInfoRequest
         * @function getTypeUrl
         * @memberof lnrpc.ChanInfoRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChanInfoRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChanInfoRequest";
        };

        return ChanInfoRequest;
    })();

    lnrpc.NetworkInfoRequest = (function() {

        /**
         * Properties of a NetworkInfoRequest.
         * @typedef {Object} lnrpc.NetworkInfoRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NetworkInfoRequest.
         * @memberof lnrpc
         * @interface INetworkInfoRequest
         * @augments lnrpc.NetworkInfoRequest.$Properties
         * @deprecated Use lnrpc.NetworkInfoRequest.$Properties instead.
         */

        /**
         * Shape of a NetworkInfoRequest.
         * @typedef {lnrpc.NetworkInfoRequest.$Properties} lnrpc.NetworkInfoRequest.$Shape
         */

        /**
         * Constructs a new NetworkInfoRequest.
         * @memberof lnrpc
         * @classdesc Represents a NetworkInfoRequest.
         * @constructor
         * @param {lnrpc.NetworkInfoRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NetworkInfoRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new NetworkInfoRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.NetworkInfoRequest
         * @static
         * @param {lnrpc.NetworkInfoRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NetworkInfoRequest} NetworkInfoRequest instance
         * @type {{
         *   (properties: lnrpc.NetworkInfoRequest.$Shape): lnrpc.NetworkInfoRequest & lnrpc.NetworkInfoRequest.$Shape;
         *   (properties?: lnrpc.NetworkInfoRequest.$Properties): lnrpc.NetworkInfoRequest;
         * }}
         */
        NetworkInfoRequest.create = function(properties) {
            return new NetworkInfoRequest(properties);
        };

        /**
         * Encodes the specified NetworkInfoRequest message. Does not implicitly {@link lnrpc.NetworkInfoRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NetworkInfoRequest
         * @static
         * @param {lnrpc.NetworkInfoRequest.$Properties} message NetworkInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkInfoRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NetworkInfoRequest message, length delimited. Does not implicitly {@link lnrpc.NetworkInfoRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NetworkInfoRequest
         * @static
         * @param {lnrpc.NetworkInfoRequest.$Properties} message NetworkInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkInfoRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NetworkInfoRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NetworkInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NetworkInfoRequest & lnrpc.NetworkInfoRequest.$Shape} NetworkInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkInfoRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NetworkInfoRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NetworkInfoRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NetworkInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NetworkInfoRequest & lnrpc.NetworkInfoRequest.$Shape} NetworkInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkInfoRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NetworkInfoRequest message.
         * @function verify
         * @memberof lnrpc.NetworkInfoRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NetworkInfoRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a NetworkInfoRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NetworkInfoRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NetworkInfoRequest} NetworkInfoRequest
         */
        NetworkInfoRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NetworkInfoRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NetworkInfoRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.NetworkInfoRequest();
        };

        /**
         * Creates a plain object from a NetworkInfoRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NetworkInfoRequest
         * @static
         * @param {lnrpc.NetworkInfoRequest} message NetworkInfoRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NetworkInfoRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this NetworkInfoRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.NetworkInfoRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NetworkInfoRequest.prototype.toJSON = function() {
            return NetworkInfoRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NetworkInfoRequest
         * @function getTypeUrl
         * @memberof lnrpc.NetworkInfoRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NetworkInfoRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NetworkInfoRequest";
        };

        return NetworkInfoRequest;
    })();

    lnrpc.NetworkInfo = (function() {

        /**
         * Properties of a NetworkInfo.
         * @typedef {Object} lnrpc.NetworkInfo.$Properties
         * @property {number|null} [graph_diameter] NetworkInfo graph_diameter
         * @property {number|null} [avg_out_degree] NetworkInfo avg_out_degree
         * @property {number|null} [max_out_degree] NetworkInfo max_out_degree
         * @property {number|null} [num_nodes] NetworkInfo num_nodes
         * @property {number|null} [num_channels] NetworkInfo num_channels
         * @property {Long|null} [total_network_capacity] NetworkInfo total_network_capacity
         * @property {number|null} [avg_channel_size] NetworkInfo avg_channel_size
         * @property {Long|null} [min_channel_size] NetworkInfo min_channel_size
         * @property {Long|null} [max_channel_size] NetworkInfo max_channel_size
         * @property {Long|null} [median_channel_size_sat] NetworkInfo median_channel_size_sat
         * @property {Long|null} [num_zombie_chans] NetworkInfo num_zombie_chans
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NetworkInfo.
         * @memberof lnrpc
         * @interface INetworkInfo
         * @augments lnrpc.NetworkInfo.$Properties
         * @deprecated Use lnrpc.NetworkInfo.$Properties instead.
         */

        /**
         * Shape of a NetworkInfo.
         * @typedef {lnrpc.NetworkInfo.$Properties} lnrpc.NetworkInfo.$Shape
         */

        /**
         * Constructs a new NetworkInfo.
         * @memberof lnrpc
         * @classdesc Represents a NetworkInfo.
         * @constructor
         * @param {lnrpc.NetworkInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NetworkInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NetworkInfo graph_diameter.
         * @member {number} graph_diameter
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.graph_diameter = 0;

        /**
         * NetworkInfo avg_out_degree.
         * @member {number} avg_out_degree
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.avg_out_degree = 0;

        /**
         * NetworkInfo max_out_degree.
         * @member {number} max_out_degree
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.max_out_degree = 0;

        /**
         * NetworkInfo num_nodes.
         * @member {number} num_nodes
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.num_nodes = 0;

        /**
         * NetworkInfo num_channels.
         * @member {number} num_channels
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.num_channels = 0;

        /**
         * NetworkInfo total_network_capacity.
         * @member {Long} total_network_capacity
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.total_network_capacity = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * NetworkInfo avg_channel_size.
         * @member {number} avg_channel_size
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.avg_channel_size = 0;

        /**
         * NetworkInfo min_channel_size.
         * @member {Long} min_channel_size
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.min_channel_size = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * NetworkInfo max_channel_size.
         * @member {Long} max_channel_size
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.max_channel_size = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * NetworkInfo median_channel_size_sat.
         * @member {Long} median_channel_size_sat
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.median_channel_size_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * NetworkInfo num_zombie_chans.
         * @member {Long} num_zombie_chans
         * @memberof lnrpc.NetworkInfo
         * @instance
         */
        NetworkInfo.prototype.num_zombie_chans = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new NetworkInfo instance using the specified properties.
         * @function create
         * @memberof lnrpc.NetworkInfo
         * @static
         * @param {lnrpc.NetworkInfo.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NetworkInfo} NetworkInfo instance
         * @type {{
         *   (properties: lnrpc.NetworkInfo.$Shape): lnrpc.NetworkInfo & lnrpc.NetworkInfo.$Shape;
         *   (properties?: lnrpc.NetworkInfo.$Properties): lnrpc.NetworkInfo;
         * }}
         */
        NetworkInfo.create = function(properties) {
            return new NetworkInfo(properties);
        };

        /**
         * Encodes the specified NetworkInfo message. Does not implicitly {@link lnrpc.NetworkInfo.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NetworkInfo
         * @static
         * @param {lnrpc.NetworkInfo.$Properties} message NetworkInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.graph_diameter != null && $Object.hasOwnProperty.call(message, "graph_diameter") && message.graph_diameter !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.graph_diameter);
            if (message.avg_out_degree != null && $Object.hasOwnProperty.call(message, "avg_out_degree") && !$Object.is(message.avg_out_degree, 0))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.avg_out_degree);
            if (message.max_out_degree != null && $Object.hasOwnProperty.call(message, "max_out_degree") && message.max_out_degree !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.max_out_degree);
            if (message.num_nodes != null && $Object.hasOwnProperty.call(message, "num_nodes") && message.num_nodes !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.num_nodes);
            if (message.num_channels != null && $Object.hasOwnProperty.call(message, "num_channels") && message.num_channels !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.num_channels);
            if (message.total_network_capacity != null && $Object.hasOwnProperty.call(message, "total_network_capacity") && (typeof message.total_network_capacity === "object" ? message.total_network_capacity.low || message.total_network_capacity.high : message.total_network_capacity !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.total_network_capacity);
            if (message.avg_channel_size != null && $Object.hasOwnProperty.call(message, "avg_channel_size") && !$Object.is(message.avg_channel_size, 0))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.avg_channel_size);
            if (message.min_channel_size != null && $Object.hasOwnProperty.call(message, "min_channel_size") && (typeof message.min_channel_size === "object" ? message.min_channel_size.low || message.min_channel_size.high : message.min_channel_size !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.min_channel_size);
            if (message.max_channel_size != null && $Object.hasOwnProperty.call(message, "max_channel_size") && (typeof message.max_channel_size === "object" ? message.max_channel_size.low || message.max_channel_size.high : message.max_channel_size !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.max_channel_size);
            if (message.median_channel_size_sat != null && $Object.hasOwnProperty.call(message, "median_channel_size_sat") && (typeof message.median_channel_size_sat === "object" ? message.median_channel_size_sat.low || message.median_channel_size_sat.high : message.median_channel_size_sat !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.median_channel_size_sat);
            if (message.num_zombie_chans != null && $Object.hasOwnProperty.call(message, "num_zombie_chans") && (typeof message.num_zombie_chans === "object" ? message.num_zombie_chans.low || message.num_zombie_chans.high : message.num_zombie_chans !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.num_zombie_chans);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NetworkInfo message, length delimited. Does not implicitly {@link lnrpc.NetworkInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NetworkInfo
         * @static
         * @param {lnrpc.NetworkInfo.$Properties} message NetworkInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NetworkInfo message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NetworkInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NetworkInfo & lnrpc.NetworkInfo.$Shape} NetworkInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NetworkInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.graph_diameter = value;
                        else
                            delete message.graph_diameter;
                        continue;
                    }
                case 2: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.avg_out_degree = value;
                        else
                            delete message.avg_out_degree;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_out_degree = value;
                        else
                            delete message.max_out_degree;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_nodes = value;
                        else
                            delete message.num_nodes;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_channels = value;
                        else
                            delete message.num_channels;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_network_capacity = value;
                        else
                            delete message.total_network_capacity;
                        continue;
                    }
                case 7: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.avg_channel_size = value;
                        else
                            delete message.avg_channel_size;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.min_channel_size = value;
                        else
                            delete message.min_channel_size;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_channel_size = value;
                        else
                            delete message.max_channel_size;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.median_channel_size_sat = value;
                        else
                            delete message.median_channel_size_sat;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.num_zombie_chans = value;
                        else
                            delete message.num_zombie_chans;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NetworkInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NetworkInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NetworkInfo & lnrpc.NetworkInfo.$Shape} NetworkInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NetworkInfo message.
         * @function verify
         * @memberof lnrpc.NetworkInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NetworkInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.graph_diameter != null && $Object.hasOwnProperty.call(message, "graph_diameter"))
                if (!$util.isInteger(message.graph_diameter))
                    return "graph_diameter: integer expected";
            if (message.avg_out_degree != null && $Object.hasOwnProperty.call(message, "avg_out_degree"))
                if (typeof message.avg_out_degree !== "number")
                    return "avg_out_degree: number expected";
            if (message.max_out_degree != null && $Object.hasOwnProperty.call(message, "max_out_degree"))
                if (!$util.isInteger(message.max_out_degree))
                    return "max_out_degree: integer expected";
            if (message.num_nodes != null && $Object.hasOwnProperty.call(message, "num_nodes"))
                if (!$util.isInteger(message.num_nodes))
                    return "num_nodes: integer expected";
            if (message.num_channels != null && $Object.hasOwnProperty.call(message, "num_channels"))
                if (!$util.isInteger(message.num_channels))
                    return "num_channels: integer expected";
            if (message.total_network_capacity != null && $Object.hasOwnProperty.call(message, "total_network_capacity"))
                if (!$util.isInteger(message.total_network_capacity) && !(message.total_network_capacity && $util.isInteger(message.total_network_capacity.low) && $util.isInteger(message.total_network_capacity.high)))
                    return "total_network_capacity: integer|Long expected";
            if (message.avg_channel_size != null && $Object.hasOwnProperty.call(message, "avg_channel_size"))
                if (typeof message.avg_channel_size !== "number")
                    return "avg_channel_size: number expected";
            if (message.min_channel_size != null && $Object.hasOwnProperty.call(message, "min_channel_size"))
                if (!$util.isInteger(message.min_channel_size) && !(message.min_channel_size && $util.isInteger(message.min_channel_size.low) && $util.isInteger(message.min_channel_size.high)))
                    return "min_channel_size: integer|Long expected";
            if (message.max_channel_size != null && $Object.hasOwnProperty.call(message, "max_channel_size"))
                if (!$util.isInteger(message.max_channel_size) && !(message.max_channel_size && $util.isInteger(message.max_channel_size.low) && $util.isInteger(message.max_channel_size.high)))
                    return "max_channel_size: integer|Long expected";
            if (message.median_channel_size_sat != null && $Object.hasOwnProperty.call(message, "median_channel_size_sat"))
                if (!$util.isInteger(message.median_channel_size_sat) && !(message.median_channel_size_sat && $util.isInteger(message.median_channel_size_sat.low) && $util.isInteger(message.median_channel_size_sat.high)))
                    return "median_channel_size_sat: integer|Long expected";
            if (message.num_zombie_chans != null && $Object.hasOwnProperty.call(message, "num_zombie_chans"))
                if (!$util.isInteger(message.num_zombie_chans) && !(message.num_zombie_chans && $util.isInteger(message.num_zombie_chans.low) && $util.isInteger(message.num_zombie_chans.high)))
                    return "num_zombie_chans: integer|Long expected";
            return null;
        };

        /**
         * Creates a NetworkInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NetworkInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NetworkInfo} NetworkInfo
         */
        NetworkInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NetworkInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NetworkInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NetworkInfo();
            if (object.graph_diameter != null)
                if ($Number(object.graph_diameter) !== 0)
                    message.graph_diameter = object.graph_diameter >>> 0;
            if (object.avg_out_degree != null)
                if (!$Object.is($Number(object.avg_out_degree), 0))
                    message.avg_out_degree = $Number(object.avg_out_degree);
            if (object.max_out_degree != null)
                if ($Number(object.max_out_degree) !== 0)
                    message.max_out_degree = object.max_out_degree >>> 0;
            if (object.num_nodes != null)
                if ($Number(object.num_nodes) !== 0)
                    message.num_nodes = object.num_nodes >>> 0;
            if (object.num_channels != null)
                if ($Number(object.num_channels) !== 0)
                    message.num_channels = object.num_channels >>> 0;
            if (object.total_network_capacity != null)
                if (typeof object.total_network_capacity === "object" ? object.total_network_capacity.low || object.total_network_capacity.high : $Number(object.total_network_capacity) !== 0)
                    if ($util.Long)
                        message.total_network_capacity = $util.Long.fromValue(object.total_network_capacity, false);
                    else if (typeof object.total_network_capacity === "string")
                        message.total_network_capacity = $parseInt(object.total_network_capacity, 10);
                    else if (typeof object.total_network_capacity === "number")
                        message.total_network_capacity = object.total_network_capacity;
                    else if (typeof object.total_network_capacity === "object")
                        message.total_network_capacity = new $util.LongBits(object.total_network_capacity.low >>> 0, object.total_network_capacity.high >>> 0).toNumber();
            if (object.avg_channel_size != null)
                if (!$Object.is($Number(object.avg_channel_size), 0))
                    message.avg_channel_size = $Number(object.avg_channel_size);
            if (object.min_channel_size != null)
                if (typeof object.min_channel_size === "object" ? object.min_channel_size.low || object.min_channel_size.high : $Number(object.min_channel_size) !== 0)
                    if ($util.Long)
                        message.min_channel_size = $util.Long.fromValue(object.min_channel_size, false);
                    else if (typeof object.min_channel_size === "string")
                        message.min_channel_size = $parseInt(object.min_channel_size, 10);
                    else if (typeof object.min_channel_size === "number")
                        message.min_channel_size = object.min_channel_size;
                    else if (typeof object.min_channel_size === "object")
                        message.min_channel_size = new $util.LongBits(object.min_channel_size.low >>> 0, object.min_channel_size.high >>> 0).toNumber();
            if (object.max_channel_size != null)
                if (typeof object.max_channel_size === "object" ? object.max_channel_size.low || object.max_channel_size.high : $Number(object.max_channel_size) !== 0)
                    if ($util.Long)
                        message.max_channel_size = $util.Long.fromValue(object.max_channel_size, false);
                    else if (typeof object.max_channel_size === "string")
                        message.max_channel_size = $parseInt(object.max_channel_size, 10);
                    else if (typeof object.max_channel_size === "number")
                        message.max_channel_size = object.max_channel_size;
                    else if (typeof object.max_channel_size === "object")
                        message.max_channel_size = new $util.LongBits(object.max_channel_size.low >>> 0, object.max_channel_size.high >>> 0).toNumber();
            if (object.median_channel_size_sat != null)
                if (typeof object.median_channel_size_sat === "object" ? object.median_channel_size_sat.low || object.median_channel_size_sat.high : $Number(object.median_channel_size_sat) !== 0)
                    if ($util.Long)
                        message.median_channel_size_sat = $util.Long.fromValue(object.median_channel_size_sat, false);
                    else if (typeof object.median_channel_size_sat === "string")
                        message.median_channel_size_sat = $parseInt(object.median_channel_size_sat, 10);
                    else if (typeof object.median_channel_size_sat === "number")
                        message.median_channel_size_sat = object.median_channel_size_sat;
                    else if (typeof object.median_channel_size_sat === "object")
                        message.median_channel_size_sat = new $util.LongBits(object.median_channel_size_sat.low >>> 0, object.median_channel_size_sat.high >>> 0).toNumber();
            if (object.num_zombie_chans != null)
                if (typeof object.num_zombie_chans === "object" ? object.num_zombie_chans.low || object.num_zombie_chans.high : $Number(object.num_zombie_chans) !== 0)
                    if ($util.Long)
                        message.num_zombie_chans = $util.Long.fromValue(object.num_zombie_chans, true);
                    else if (typeof object.num_zombie_chans === "string")
                        message.num_zombie_chans = $parseInt(object.num_zombie_chans, 10);
                    else if (typeof object.num_zombie_chans === "number")
                        message.num_zombie_chans = object.num_zombie_chans;
                    else if (typeof object.num_zombie_chans === "object")
                        message.num_zombie_chans = new $util.LongBits(object.num_zombie_chans.low >>> 0, object.num_zombie_chans.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a NetworkInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NetworkInfo
         * @static
         * @param {lnrpc.NetworkInfo} message NetworkInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NetworkInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.graph_diameter = 0;
                object.avg_out_degree = 0;
                object.max_out_degree = 0;
                object.num_nodes = 0;
                object.num_channels = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.total_network_capacity = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_network_capacity = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.avg_channel_size = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.min_channel_size = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min_channel_size = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.max_channel_size = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.max_channel_size = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.median_channel_size_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.median_channel_size_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.num_zombie_chans = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.num_zombie_chans = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.graph_diameter != null && $Object.hasOwnProperty.call(message, "graph_diameter"))
                object.graph_diameter = message.graph_diameter;
            if (message.avg_out_degree != null && $Object.hasOwnProperty.call(message, "avg_out_degree"))
                object.avg_out_degree = options.json && !$isFinite(message.avg_out_degree) ? $String(message.avg_out_degree) : message.avg_out_degree;
            if (message.max_out_degree != null && $Object.hasOwnProperty.call(message, "max_out_degree"))
                object.max_out_degree = message.max_out_degree;
            if (message.num_nodes != null && $Object.hasOwnProperty.call(message, "num_nodes"))
                object.num_nodes = message.num_nodes;
            if (message.num_channels != null && $Object.hasOwnProperty.call(message, "num_channels"))
                object.num_channels = message.num_channels;
            if (message.total_network_capacity != null && $Object.hasOwnProperty.call(message, "total_network_capacity"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_network_capacity = typeof message.total_network_capacity === "number" ? $BigInt(message.total_network_capacity) : $util.Long.fromBits(message.total_network_capacity.low >>> 0, message.total_network_capacity.high >>> 0, false).toBigInt();
                else if (typeof message.total_network_capacity === "number")
                    object.total_network_capacity = options.longs === $String ? $String(message.total_network_capacity) : message.total_network_capacity;
                else
                    object.total_network_capacity = options.longs === $String ? $util.Long.prototype.toString.call(message.total_network_capacity) : options.longs === $Number ? new $util.LongBits(message.total_network_capacity.low >>> 0, message.total_network_capacity.high >>> 0).toNumber() : message.total_network_capacity;
            if (message.avg_channel_size != null && $Object.hasOwnProperty.call(message, "avg_channel_size"))
                object.avg_channel_size = options.json && !$isFinite(message.avg_channel_size) ? $String(message.avg_channel_size) : message.avg_channel_size;
            if (message.min_channel_size != null && $Object.hasOwnProperty.call(message, "min_channel_size"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min_channel_size = typeof message.min_channel_size === "number" ? $BigInt(message.min_channel_size) : $util.Long.fromBits(message.min_channel_size.low >>> 0, message.min_channel_size.high >>> 0, false).toBigInt();
                else if (typeof message.min_channel_size === "number")
                    object.min_channel_size = options.longs === $String ? $String(message.min_channel_size) : message.min_channel_size;
                else
                    object.min_channel_size = options.longs === $String ? $util.Long.prototype.toString.call(message.min_channel_size) : options.longs === $Number ? new $util.LongBits(message.min_channel_size.low >>> 0, message.min_channel_size.high >>> 0).toNumber() : message.min_channel_size;
            if (message.max_channel_size != null && $Object.hasOwnProperty.call(message, "max_channel_size"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.max_channel_size = typeof message.max_channel_size === "number" ? $BigInt(message.max_channel_size) : $util.Long.fromBits(message.max_channel_size.low >>> 0, message.max_channel_size.high >>> 0, false).toBigInt();
                else if (typeof message.max_channel_size === "number")
                    object.max_channel_size = options.longs === $String ? $String(message.max_channel_size) : message.max_channel_size;
                else
                    object.max_channel_size = options.longs === $String ? $util.Long.prototype.toString.call(message.max_channel_size) : options.longs === $Number ? new $util.LongBits(message.max_channel_size.low >>> 0, message.max_channel_size.high >>> 0).toNumber() : message.max_channel_size;
            if (message.median_channel_size_sat != null && $Object.hasOwnProperty.call(message, "median_channel_size_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.median_channel_size_sat = typeof message.median_channel_size_sat === "number" ? $BigInt(message.median_channel_size_sat) : $util.Long.fromBits(message.median_channel_size_sat.low >>> 0, message.median_channel_size_sat.high >>> 0, false).toBigInt();
                else if (typeof message.median_channel_size_sat === "number")
                    object.median_channel_size_sat = options.longs === $String ? $String(message.median_channel_size_sat) : message.median_channel_size_sat;
                else
                    object.median_channel_size_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.median_channel_size_sat) : options.longs === $Number ? new $util.LongBits(message.median_channel_size_sat.low >>> 0, message.median_channel_size_sat.high >>> 0).toNumber() : message.median_channel_size_sat;
            if (message.num_zombie_chans != null && $Object.hasOwnProperty.call(message, "num_zombie_chans"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.num_zombie_chans = typeof message.num_zombie_chans === "number" ? $BigInt(message.num_zombie_chans) : $util.Long.fromBits(message.num_zombie_chans.low >>> 0, message.num_zombie_chans.high >>> 0, true).toBigInt();
                else if (typeof message.num_zombie_chans === "number")
                    object.num_zombie_chans = options.longs === $String ? $String(message.num_zombie_chans) : message.num_zombie_chans;
                else
                    object.num_zombie_chans = options.longs === $String ? $util.Long.prototype.toString.call(message.num_zombie_chans) : options.longs === $Number ? new $util.LongBits(message.num_zombie_chans.low >>> 0, message.num_zombie_chans.high >>> 0).toNumber(true) : message.num_zombie_chans;
            return object;
        };

        /**
         * Converts this NetworkInfo to JSON.
         * @function toJSON
         * @memberof lnrpc.NetworkInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NetworkInfo.prototype.toJSON = function() {
            return NetworkInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NetworkInfo
         * @function getTypeUrl
         * @memberof lnrpc.NetworkInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NetworkInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NetworkInfo";
        };

        return NetworkInfo;
    })();

    lnrpc.StopRequest = (function() {

        /**
         * Properties of a StopRequest.
         * @typedef {Object} lnrpc.StopRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a StopRequest.
         * @memberof lnrpc
         * @interface IStopRequest
         * @augments lnrpc.StopRequest.$Properties
         * @deprecated Use lnrpc.StopRequest.$Properties instead.
         */

        /**
         * Shape of a StopRequest.
         * @typedef {lnrpc.StopRequest.$Properties} lnrpc.StopRequest.$Shape
         */

        /**
         * Constructs a new StopRequest.
         * @memberof lnrpc
         * @classdesc Represents a StopRequest.
         * @constructor
         * @param {lnrpc.StopRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const StopRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new StopRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.StopRequest
         * @static
         * @param {lnrpc.StopRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.StopRequest} StopRequest instance
         * @type {{
         *   (properties: lnrpc.StopRequest.$Shape): lnrpc.StopRequest & lnrpc.StopRequest.$Shape;
         *   (properties?: lnrpc.StopRequest.$Properties): lnrpc.StopRequest;
         * }}
         */
        StopRequest.create = function(properties) {
            return new StopRequest(properties);
        };

        /**
         * Encodes the specified StopRequest message. Does not implicitly {@link lnrpc.StopRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.StopRequest
         * @static
         * @param {lnrpc.StopRequest.$Properties} message StopRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StopRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StopRequest message, length delimited. Does not implicitly {@link lnrpc.StopRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.StopRequest
         * @static
         * @param {lnrpc.StopRequest.$Properties} message StopRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StopRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StopRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.StopRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.StopRequest & lnrpc.StopRequest.$Shape} StopRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StopRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.StopRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StopRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.StopRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.StopRequest & lnrpc.StopRequest.$Shape} StopRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StopRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StopRequest message.
         * @function verify
         * @memberof lnrpc.StopRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StopRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a StopRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.StopRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.StopRequest} StopRequest
         */
        StopRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.StopRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.StopRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.StopRequest();
        };

        /**
         * Creates a plain object from a StopRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.StopRequest
         * @static
         * @param {lnrpc.StopRequest} message StopRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StopRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this StopRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.StopRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StopRequest.prototype.toJSON = function() {
            return StopRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StopRequest
         * @function getTypeUrl
         * @memberof lnrpc.StopRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StopRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.StopRequest";
        };

        return StopRequest;
    })();

    lnrpc.StopResponse = (function() {

        /**
         * Properties of a StopResponse.
         * @typedef {Object} lnrpc.StopResponse.$Properties
         * @property {string|null} [status] StopResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a StopResponse.
         * @memberof lnrpc
         * @interface IStopResponse
         * @augments lnrpc.StopResponse.$Properties
         * @deprecated Use lnrpc.StopResponse.$Properties instead.
         */

        /**
         * Shape of a StopResponse.
         * @typedef {lnrpc.StopResponse.$Properties} lnrpc.StopResponse.$Shape
         */

        /**
         * Constructs a new StopResponse.
         * @memberof lnrpc
         * @classdesc Represents a StopResponse.
         * @constructor
         * @param {lnrpc.StopResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const StopResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StopResponse status.
         * @member {string} status
         * @memberof lnrpc.StopResponse
         * @instance
         */
        StopResponse.prototype.status = "";

        /**
         * Creates a new StopResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.StopResponse
         * @static
         * @param {lnrpc.StopResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.StopResponse} StopResponse instance
         * @type {{
         *   (properties: lnrpc.StopResponse.$Shape): lnrpc.StopResponse & lnrpc.StopResponse.$Shape;
         *   (properties?: lnrpc.StopResponse.$Properties): lnrpc.StopResponse;
         * }}
         */
        StopResponse.create = function(properties) {
            return new StopResponse(properties);
        };

        /**
         * Encodes the specified StopResponse message. Does not implicitly {@link lnrpc.StopResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.StopResponse
         * @static
         * @param {lnrpc.StopResponse.$Properties} message StopResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StopResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StopResponse message, length delimited. Does not implicitly {@link lnrpc.StopResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.StopResponse
         * @static
         * @param {lnrpc.StopResponse.$Properties} message StopResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StopResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StopResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.StopResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.StopResponse & lnrpc.StopResponse.$Shape} StopResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StopResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.StopResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StopResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.StopResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.StopResponse & lnrpc.StopResponse.$Shape} StopResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StopResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StopResponse message.
         * @function verify
         * @memberof lnrpc.StopResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StopResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a StopResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.StopResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.StopResponse} StopResponse
         */
        StopResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.StopResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.StopResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.StopResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a StopResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.StopResponse
         * @static
         * @param {lnrpc.StopResponse} message StopResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StopResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this StopResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.StopResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StopResponse.prototype.toJSON = function() {
            return StopResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StopResponse
         * @function getTypeUrl
         * @memberof lnrpc.StopResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StopResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.StopResponse";
        };

        return StopResponse;
    })();

    lnrpc.GraphTopologySubscription = (function() {

        /**
         * Properties of a GraphTopologySubscription.
         * @typedef {Object} lnrpc.GraphTopologySubscription.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GraphTopologySubscription.
         * @memberof lnrpc
         * @interface IGraphTopologySubscription
         * @augments lnrpc.GraphTopologySubscription.$Properties
         * @deprecated Use lnrpc.GraphTopologySubscription.$Properties instead.
         */

        /**
         * Shape of a GraphTopologySubscription.
         * @typedef {lnrpc.GraphTopologySubscription.$Properties} lnrpc.GraphTopologySubscription.$Shape
         */

        /**
         * Constructs a new GraphTopologySubscription.
         * @memberof lnrpc
         * @classdesc Represents a GraphTopologySubscription.
         * @constructor
         * @param {lnrpc.GraphTopologySubscription.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GraphTopologySubscription = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new GraphTopologySubscription instance using the specified properties.
         * @function create
         * @memberof lnrpc.GraphTopologySubscription
         * @static
         * @param {lnrpc.GraphTopologySubscription.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GraphTopologySubscription} GraphTopologySubscription instance
         * @type {{
         *   (properties: lnrpc.GraphTopologySubscription.$Shape): lnrpc.GraphTopologySubscription & lnrpc.GraphTopologySubscription.$Shape;
         *   (properties?: lnrpc.GraphTopologySubscription.$Properties): lnrpc.GraphTopologySubscription;
         * }}
         */
        GraphTopologySubscription.create = function(properties) {
            return new GraphTopologySubscription(properties);
        };

        /**
         * Encodes the specified GraphTopologySubscription message. Does not implicitly {@link lnrpc.GraphTopologySubscription.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GraphTopologySubscription
         * @static
         * @param {lnrpc.GraphTopologySubscription.$Properties} message GraphTopologySubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GraphTopologySubscription.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GraphTopologySubscription message, length delimited. Does not implicitly {@link lnrpc.GraphTopologySubscription.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GraphTopologySubscription
         * @static
         * @param {lnrpc.GraphTopologySubscription.$Properties} message GraphTopologySubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GraphTopologySubscription.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GraphTopologySubscription message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GraphTopologySubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GraphTopologySubscription & lnrpc.GraphTopologySubscription.$Shape} GraphTopologySubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GraphTopologySubscription.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GraphTopologySubscription();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GraphTopologySubscription message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GraphTopologySubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GraphTopologySubscription & lnrpc.GraphTopologySubscription.$Shape} GraphTopologySubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GraphTopologySubscription.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GraphTopologySubscription message.
         * @function verify
         * @memberof lnrpc.GraphTopologySubscription
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GraphTopologySubscription.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a GraphTopologySubscription message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GraphTopologySubscription
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GraphTopologySubscription} GraphTopologySubscription
         */
        GraphTopologySubscription.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GraphTopologySubscription)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GraphTopologySubscription: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.GraphTopologySubscription();
        };

        /**
         * Creates a plain object from a GraphTopologySubscription message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GraphTopologySubscription
         * @static
         * @param {lnrpc.GraphTopologySubscription} message GraphTopologySubscription
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GraphTopologySubscription.toObject = function () {
            return {};
        };

        /**
         * Converts this GraphTopologySubscription to JSON.
         * @function toJSON
         * @memberof lnrpc.GraphTopologySubscription
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GraphTopologySubscription.prototype.toJSON = function() {
            return GraphTopologySubscription.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GraphTopologySubscription
         * @function getTypeUrl
         * @memberof lnrpc.GraphTopologySubscription
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GraphTopologySubscription.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GraphTopologySubscription";
        };

        return GraphTopologySubscription;
    })();

    lnrpc.GraphTopologyUpdate = (function() {

        /**
         * Properties of a GraphTopologyUpdate.
         * @typedef {Object} lnrpc.GraphTopologyUpdate.$Properties
         * @property {Array.<lnrpc.NodeUpdate.$Properties>|null} [node_updates] GraphTopologyUpdate node_updates
         * @property {Array.<lnrpc.ChannelEdgeUpdate.$Properties>|null} [channel_updates] GraphTopologyUpdate channel_updates
         * @property {Array.<lnrpc.ClosedChannelUpdate.$Properties>|null} [closed_chans] GraphTopologyUpdate closed_chans
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GraphTopologyUpdate.
         * @memberof lnrpc
         * @interface IGraphTopologyUpdate
         * @augments lnrpc.GraphTopologyUpdate.$Properties
         * @deprecated Use lnrpc.GraphTopologyUpdate.$Properties instead.
         */

        /**
         * Shape of a GraphTopologyUpdate.
         * @typedef {{
         *   node_updates?: Array.<lnrpc.NodeUpdate.$Shape>|null;
         *   channel_updates?: Array.<lnrpc.ChannelEdgeUpdate.$Shape>|null;
         *   closed_chans?: Array.<lnrpc.ClosedChannelUpdate.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.GraphTopologyUpdate.$Shape
         */

        /**
         * Constructs a new GraphTopologyUpdate.
         * @memberof lnrpc
         * @classdesc Represents a GraphTopologyUpdate.
         * @constructor
         * @param {lnrpc.GraphTopologyUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GraphTopologyUpdate = function (properties) {
            this.node_updates = [];
            this.channel_updates = [];
            this.closed_chans = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GraphTopologyUpdate node_updates.
         * @member {Array.<lnrpc.NodeUpdate.$Properties>} node_updates
         * @memberof lnrpc.GraphTopologyUpdate
         * @instance
         */
        GraphTopologyUpdate.prototype.node_updates = $util.emptyArray;

        /**
         * GraphTopologyUpdate channel_updates.
         * @member {Array.<lnrpc.ChannelEdgeUpdate.$Properties>} channel_updates
         * @memberof lnrpc.GraphTopologyUpdate
         * @instance
         */
        GraphTopologyUpdate.prototype.channel_updates = $util.emptyArray;

        /**
         * GraphTopologyUpdate closed_chans.
         * @member {Array.<lnrpc.ClosedChannelUpdate.$Properties>} closed_chans
         * @memberof lnrpc.GraphTopologyUpdate
         * @instance
         */
        GraphTopologyUpdate.prototype.closed_chans = $util.emptyArray;

        /**
         * Creates a new GraphTopologyUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.GraphTopologyUpdate
         * @static
         * @param {lnrpc.GraphTopologyUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GraphTopologyUpdate} GraphTopologyUpdate instance
         * @type {{
         *   (properties: lnrpc.GraphTopologyUpdate.$Shape): lnrpc.GraphTopologyUpdate & lnrpc.GraphTopologyUpdate.$Shape;
         *   (properties?: lnrpc.GraphTopologyUpdate.$Properties): lnrpc.GraphTopologyUpdate;
         * }}
         */
        GraphTopologyUpdate.create = function(properties) {
            return new GraphTopologyUpdate(properties);
        };

        /**
         * Encodes the specified GraphTopologyUpdate message. Does not implicitly {@link lnrpc.GraphTopologyUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GraphTopologyUpdate
         * @static
         * @param {lnrpc.GraphTopologyUpdate.$Properties} message GraphTopologyUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GraphTopologyUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.node_updates != null && message.node_updates.length)
                for (let i = 0; i < message.node_updates.length; ++i)
                    $root.lnrpc.NodeUpdate.encode(message.node_updates[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.channel_updates != null && message.channel_updates.length)
                for (let i = 0; i < message.channel_updates.length; ++i)
                    $root.lnrpc.ChannelEdgeUpdate.encode(message.channel_updates[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.closed_chans != null && message.closed_chans.length)
                for (let i = 0; i < message.closed_chans.length; ++i)
                    $root.lnrpc.ClosedChannelUpdate.encode(message.closed_chans[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GraphTopologyUpdate message, length delimited. Does not implicitly {@link lnrpc.GraphTopologyUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GraphTopologyUpdate
         * @static
         * @param {lnrpc.GraphTopologyUpdate.$Properties} message GraphTopologyUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GraphTopologyUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GraphTopologyUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GraphTopologyUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GraphTopologyUpdate & lnrpc.GraphTopologyUpdate.$Shape} GraphTopologyUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GraphTopologyUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GraphTopologyUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.node_updates && message.node_updates.length))
                            message.node_updates = [];
                        message.node_updates.push($root.lnrpc.NodeUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.channel_updates && message.channel_updates.length))
                            message.channel_updates = [];
                        message.channel_updates.push($root.lnrpc.ChannelEdgeUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.closed_chans && message.closed_chans.length))
                            message.closed_chans = [];
                        message.closed_chans.push($root.lnrpc.ClosedChannelUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GraphTopologyUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GraphTopologyUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GraphTopologyUpdate & lnrpc.GraphTopologyUpdate.$Shape} GraphTopologyUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GraphTopologyUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GraphTopologyUpdate message.
         * @function verify
         * @memberof lnrpc.GraphTopologyUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GraphTopologyUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.node_updates != null && $Object.hasOwnProperty.call(message, "node_updates")) {
                if (!$Array.isArray(message.node_updates))
                    return "node_updates: array expected";
                for (let i = 0; i < message.node_updates.length; ++i) {
                    let error = $root.lnrpc.NodeUpdate.verify(message.node_updates[i], _depth + 1);
                    if (error)
                        return "node_updates." + error;
                }
            }
            if (message.channel_updates != null && $Object.hasOwnProperty.call(message, "channel_updates")) {
                if (!$Array.isArray(message.channel_updates))
                    return "channel_updates: array expected";
                for (let i = 0; i < message.channel_updates.length; ++i) {
                    let error = $root.lnrpc.ChannelEdgeUpdate.verify(message.channel_updates[i], _depth + 1);
                    if (error)
                        return "channel_updates." + error;
                }
            }
            if (message.closed_chans != null && $Object.hasOwnProperty.call(message, "closed_chans")) {
                if (!$Array.isArray(message.closed_chans))
                    return "closed_chans: array expected";
                for (let i = 0; i < message.closed_chans.length; ++i) {
                    let error = $root.lnrpc.ClosedChannelUpdate.verify(message.closed_chans[i], _depth + 1);
                    if (error)
                        return "closed_chans." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GraphTopologyUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GraphTopologyUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GraphTopologyUpdate} GraphTopologyUpdate
         */
        GraphTopologyUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GraphTopologyUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GraphTopologyUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.GraphTopologyUpdate();
            if (object.node_updates) {
                if (!$Array.isArray(object.node_updates))
                    throw $TypeError(".lnrpc.GraphTopologyUpdate.node_updates: array expected");
                message.node_updates = $Array(object.node_updates.length);
                for (let i = 0; i < object.node_updates.length; ++i) {
                    if (!$util.isObject(object.node_updates[i]))
                        throw $TypeError(".lnrpc.GraphTopologyUpdate.node_updates: object expected");
                    message.node_updates[i] = $root.lnrpc.NodeUpdate.fromObject(object.node_updates[i], _depth + 1);
                }
            }
            if (object.channel_updates) {
                if (!$Array.isArray(object.channel_updates))
                    throw $TypeError(".lnrpc.GraphTopologyUpdate.channel_updates: array expected");
                message.channel_updates = $Array(object.channel_updates.length);
                for (let i = 0; i < object.channel_updates.length; ++i) {
                    if (!$util.isObject(object.channel_updates[i]))
                        throw $TypeError(".lnrpc.GraphTopologyUpdate.channel_updates: object expected");
                    message.channel_updates[i] = $root.lnrpc.ChannelEdgeUpdate.fromObject(object.channel_updates[i], _depth + 1);
                }
            }
            if (object.closed_chans) {
                if (!$Array.isArray(object.closed_chans))
                    throw $TypeError(".lnrpc.GraphTopologyUpdate.closed_chans: array expected");
                message.closed_chans = $Array(object.closed_chans.length);
                for (let i = 0; i < object.closed_chans.length; ++i) {
                    if (!$util.isObject(object.closed_chans[i]))
                        throw $TypeError(".lnrpc.GraphTopologyUpdate.closed_chans: object expected");
                    message.closed_chans[i] = $root.lnrpc.ClosedChannelUpdate.fromObject(object.closed_chans[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GraphTopologyUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GraphTopologyUpdate
         * @static
         * @param {lnrpc.GraphTopologyUpdate} message GraphTopologyUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GraphTopologyUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.node_updates = [];
                object.channel_updates = [];
                object.closed_chans = [];
            }
            if (message.node_updates && message.node_updates.length) {
                object.node_updates = $Array(message.node_updates.length);
                for (let j = 0; j < message.node_updates.length; ++j)
                    object.node_updates[j] = $root.lnrpc.NodeUpdate.toObject(message.node_updates[j], options, _depth + 1);
            }
            if (message.channel_updates && message.channel_updates.length) {
                object.channel_updates = $Array(message.channel_updates.length);
                for (let j = 0; j < message.channel_updates.length; ++j)
                    object.channel_updates[j] = $root.lnrpc.ChannelEdgeUpdate.toObject(message.channel_updates[j], options, _depth + 1);
            }
            if (message.closed_chans && message.closed_chans.length) {
                object.closed_chans = $Array(message.closed_chans.length);
                for (let j = 0; j < message.closed_chans.length; ++j)
                    object.closed_chans[j] = $root.lnrpc.ClosedChannelUpdate.toObject(message.closed_chans[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this GraphTopologyUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.GraphTopologyUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GraphTopologyUpdate.prototype.toJSON = function() {
            return GraphTopologyUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GraphTopologyUpdate
         * @function getTypeUrl
         * @memberof lnrpc.GraphTopologyUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GraphTopologyUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GraphTopologyUpdate";
        };

        return GraphTopologyUpdate;
    })();

    lnrpc.NodeUpdate = (function() {

        /**
         * Properties of a NodeUpdate.
         * @typedef {Object} lnrpc.NodeUpdate.$Properties
         * @property {Array.<string>|null} [addresses] NodeUpdate addresses
         * @property {string|null} [identity_key] NodeUpdate identity_key
         * @property {Uint8Array|null} [global_features] NodeUpdate global_features
         * @property {string|null} [alias] NodeUpdate alias
         * @property {string|null} [color] NodeUpdate color
         * @property {Array.<lnrpc.NodeAddress.$Properties>|null} [node_addresses] NodeUpdate node_addresses
         * @property {Object.<string,lnrpc.Feature.$Properties>|null} [features] NodeUpdate features
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a NodeUpdate.
         * @memberof lnrpc
         * @interface INodeUpdate
         * @augments lnrpc.NodeUpdate.$Properties
         * @deprecated Use lnrpc.NodeUpdate.$Properties instead.
         */

        /**
         * Shape of a NodeUpdate.
         * @typedef {lnrpc.NodeUpdate.$Properties} lnrpc.NodeUpdate.$Shape
         */

        /**
         * Constructs a new NodeUpdate.
         * @memberof lnrpc
         * @classdesc Represents a NodeUpdate.
         * @constructor
         * @param {lnrpc.NodeUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const NodeUpdate = function (properties) {
            this.addresses = [];
            this.node_addresses = [];
            this.features = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NodeUpdate addresses.
         * @member {Array.<string>} addresses
         * @memberof lnrpc.NodeUpdate
         * @instance
         */
        NodeUpdate.prototype.addresses = $util.emptyArray;

        /**
         * NodeUpdate identity_key.
         * @member {string} identity_key
         * @memberof lnrpc.NodeUpdate
         * @instance
         */
        NodeUpdate.prototype.identity_key = "";

        /**
         * NodeUpdate global_features.
         * @member {Uint8Array} global_features
         * @memberof lnrpc.NodeUpdate
         * @instance
         */
        NodeUpdate.prototype.global_features = $util.newBuffer([]);

        /**
         * NodeUpdate alias.
         * @member {string} alias
         * @memberof lnrpc.NodeUpdate
         * @instance
         */
        NodeUpdate.prototype.alias = "";

        /**
         * NodeUpdate color.
         * @member {string} color
         * @memberof lnrpc.NodeUpdate
         * @instance
         */
        NodeUpdate.prototype.color = "";

        /**
         * NodeUpdate node_addresses.
         * @member {Array.<lnrpc.NodeAddress.$Properties>} node_addresses
         * @memberof lnrpc.NodeUpdate
         * @instance
         */
        NodeUpdate.prototype.node_addresses = $util.emptyArray;

        /**
         * NodeUpdate features.
         * @member {Object.<string,lnrpc.Feature.$Properties>} features
         * @memberof lnrpc.NodeUpdate
         * @instance
         */
        NodeUpdate.prototype.features = $util.emptyObject;

        /**
         * Creates a new NodeUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.NodeUpdate
         * @static
         * @param {lnrpc.NodeUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.NodeUpdate} NodeUpdate instance
         * @type {{
         *   (properties: lnrpc.NodeUpdate.$Shape): lnrpc.NodeUpdate & lnrpc.NodeUpdate.$Shape;
         *   (properties?: lnrpc.NodeUpdate.$Properties): lnrpc.NodeUpdate;
         * }}
         */
        NodeUpdate.create = function(properties) {
            return new NodeUpdate(properties);
        };

        /**
         * Encodes the specified NodeUpdate message. Does not implicitly {@link lnrpc.NodeUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.NodeUpdate
         * @static
         * @param {lnrpc.NodeUpdate.$Properties} message NodeUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.addresses != null && message.addresses.length)
                for (let i = 0; i < message.addresses.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.addresses[i]);
            if (message.identity_key != null && $Object.hasOwnProperty.call(message, "identity_key") && message.identity_key !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.identity_key);
            if (message.global_features != null && $Object.hasOwnProperty.call(message, "global_features") && message.global_features.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.global_features);
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias") && message.alias !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.alias);
            if (message.color != null && $Object.hasOwnProperty.call(message, "color") && message.color !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.color);
            if (message.features != null && $Object.hasOwnProperty.call(message, "features"))
                for (let keys = $Object.keys(message.features), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 6, wireType 2 =*/50).fork().uint32(/* id 1, wireType 0 =*/8).uint32(keys[i]);
                    $root.lnrpc.Feature.encode(message.features[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.node_addresses != null && message.node_addresses.length)
                for (let i = 0; i < message.node_addresses.length; ++i)
                    $root.lnrpc.NodeAddress.encode(message.node_addresses[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeUpdate message, length delimited. Does not implicitly {@link lnrpc.NodeUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.NodeUpdate
         * @static
         * @param {lnrpc.NodeUpdate.$Properties} message NodeUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NodeUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.NodeUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.NodeUpdate & lnrpc.NodeUpdate.$Shape} NodeUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.NodeUpdate(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.addresses && message.addresses.length))
                            message.addresses = [];
                        message.addresses.push(reader.stringVerify());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.identity_key = value;
                        else
                            delete message.identity_key;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.global_features = value;
                        else
                            delete message.global_features;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.alias = value;
                        else
                            delete message.alias;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.color = value;
                        else
                            delete message.color;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.node_addresses && message.node_addresses.length))
                            message.node_addresses = [];
                        message.node_addresses.push($root.lnrpc.NodeAddress.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (message.features === $util.emptyObject)
                            message.features = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint32();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.Feature.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.features[key] = value || new $root.lnrpc.Feature();
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.NodeUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.NodeUpdate & lnrpc.NodeUpdate.$Shape} NodeUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeUpdate message.
         * @function verify
         * @memberof lnrpc.NodeUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.addresses != null && $Object.hasOwnProperty.call(message, "addresses")) {
                if (!$Array.isArray(message.addresses))
                    return "addresses: array expected";
                for (let i = 0; i < message.addresses.length; ++i)
                    if (!$util.isString(message.addresses[i]))
                        return "addresses: string[] expected";
            }
            if (message.identity_key != null && $Object.hasOwnProperty.call(message, "identity_key"))
                if (!$util.isString(message.identity_key))
                    return "identity_key: string expected";
            if (message.global_features != null && $Object.hasOwnProperty.call(message, "global_features"))
                if (!(message.global_features && typeof message.global_features.length === "number" || $util.isString(message.global_features)))
                    return "global_features: buffer expected";
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                if (!$util.isString(message.alias))
                    return "alias: string expected";
            if (message.color != null && $Object.hasOwnProperty.call(message, "color"))
                if (!$util.isString(message.color))
                    return "color: string expected";
            if (message.node_addresses != null && $Object.hasOwnProperty.call(message, "node_addresses")) {
                if (!$Array.isArray(message.node_addresses))
                    return "node_addresses: array expected";
                for (let i = 0; i < message.node_addresses.length; ++i) {
                    let error = $root.lnrpc.NodeAddress.verify(message.node_addresses[i], _depth + 1);
                    if (error)
                        return "node_addresses." + error;
                }
            }
            if (message.features != null && $Object.hasOwnProperty.call(message, "features")) {
                if (!$util.isObject(message.features))
                    return "features: object expected";
                let key = $Object.keys(message.features);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "features: integer key{k:uint32} expected";
                    {
                        let error = $root.lnrpc.Feature.verify(message.features[key[i]], _depth + 1);
                        if (error)
                            return "features." + error;
                    }
                }
            }
            return null;
        };

        /**
         * Creates a NodeUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.NodeUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.NodeUpdate} NodeUpdate
         */
        NodeUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.NodeUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.NodeUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.NodeUpdate();
            if (object.addresses) {
                if (!$Array.isArray(object.addresses))
                    throw $TypeError(".lnrpc.NodeUpdate.addresses: array expected");
                message.addresses = $Array(object.addresses.length);
                for (let i = 0; i < object.addresses.length; ++i)
                    message.addresses[i] = $String(object.addresses[i]);
            }
            if (object.identity_key != null)
                if (typeof object.identity_key !== "string" || object.identity_key.length)
                    message.identity_key = $String(object.identity_key);
            if (object.global_features != null)
                if (object.global_features.length)
                    if (typeof object.global_features === "string")
                        $util.base64.decode(object.global_features, message.global_features = $util.newBuffer($util.base64.length(object.global_features)), 0);
                    else if (object.global_features.length >= 0)
                        message.global_features = object.global_features;
            if (object.alias != null)
                if (typeof object.alias !== "string" || object.alias.length)
                    message.alias = $String(object.alias);
            if (object.color != null)
                if (typeof object.color !== "string" || object.color.length)
                    message.color = $String(object.color);
            if (object.node_addresses) {
                if (!$Array.isArray(object.node_addresses))
                    throw $TypeError(".lnrpc.NodeUpdate.node_addresses: array expected");
                message.node_addresses = $Array(object.node_addresses.length);
                for (let i = 0; i < object.node_addresses.length; ++i) {
                    if (!$util.isObject(object.node_addresses[i]))
                        throw $TypeError(".lnrpc.NodeUpdate.node_addresses: object expected");
                    message.node_addresses[i] = $root.lnrpc.NodeAddress.fromObject(object.node_addresses[i], _depth + 1);
                }
            }
            if (object.features) {
                if (!$util.isObject(object.features))
                    throw $TypeError(".lnrpc.NodeUpdate.features: object expected");
                message.features = {};
                for (let keys = $Object.keys(object.features), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.features, keys[i]);
                    if (!$util.isObject(object.features[keys[i]]))
                        throw $TypeError(".lnrpc.NodeUpdate.features: object expected");
                    message.features[keys[i]] = $root.lnrpc.Feature.fromObject(object.features[keys[i]], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NodeUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.NodeUpdate
         * @static
         * @param {lnrpc.NodeUpdate} message NodeUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.addresses = [];
                object.node_addresses = [];
            }
            if (options.objects || options.defaults)
                object.features = {};
            if (options.defaults) {
                object.identity_key = "";
                if (options.bytes === $String)
                    object.global_features = "";
                else {
                    object.global_features = [];
                    if (options.bytes !== $Array)
                        object.global_features = $util.newBuffer(object.global_features);
                }
                object.alias = "";
                object.color = "";
            }
            if (message.addresses && message.addresses.length) {
                object.addresses = $Array(message.addresses.length);
                for (let j = 0; j < message.addresses.length; ++j)
                    object.addresses[j] = message.addresses[j];
            }
            if (message.identity_key != null && $Object.hasOwnProperty.call(message, "identity_key"))
                object.identity_key = message.identity_key;
            if (message.global_features != null && $Object.hasOwnProperty.call(message, "global_features"))
                object.global_features = options.bytes === $String ? $util.base64.encode(message.global_features, 0, message.global_features.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.global_features) : message.global_features;
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                object.alias = message.alias;
            if (message.color != null && $Object.hasOwnProperty.call(message, "color"))
                object.color = message.color;
            let keys2;
            if (message.features && (keys2 = $Object.keys(message.features)).length) {
                object.features = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.features, keys2[j]);
                    object.features[keys2[j]] = $root.lnrpc.Feature.toObject(message.features[keys2[j]], options, _depth + 1);
                }
            }
            if (message.node_addresses && message.node_addresses.length) {
                object.node_addresses = $Array(message.node_addresses.length);
                for (let j = 0; j < message.node_addresses.length; ++j)
                    object.node_addresses[j] = $root.lnrpc.NodeAddress.toObject(message.node_addresses[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this NodeUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.NodeUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeUpdate.prototype.toJSON = function() {
            return NodeUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeUpdate
         * @function getTypeUrl
         * @memberof lnrpc.NodeUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.NodeUpdate";
        };

        return NodeUpdate;
    })();

    lnrpc.ChannelEdgeUpdate = (function() {

        /**
         * Properties of a ChannelEdgeUpdate.
         * @typedef {Object} lnrpc.ChannelEdgeUpdate.$Properties
         * @property {Long|null} [chan_id] ChannelEdgeUpdate chan_id
         * @property {lnrpc.ChannelPoint.$Properties|null} [chan_point] ChannelEdgeUpdate chan_point
         * @property {Long|null} [capacity] ChannelEdgeUpdate capacity
         * @property {lnrpc.RoutingPolicy.$Properties|null} [routing_policy] ChannelEdgeUpdate routing_policy
         * @property {string|null} [advertising_node] ChannelEdgeUpdate advertising_node
         * @property {string|null} [connecting_node] ChannelEdgeUpdate connecting_node
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelEdgeUpdate.
         * @memberof lnrpc
         * @interface IChannelEdgeUpdate
         * @augments lnrpc.ChannelEdgeUpdate.$Properties
         * @deprecated Use lnrpc.ChannelEdgeUpdate.$Properties instead.
         */

        /**
         * Shape of a ChannelEdgeUpdate.
         * @typedef {{
         *   chan_id?: Long|null;
         *   chan_point?: lnrpc.ChannelPoint.$Shape|null;
         *   capacity?: Long|null;
         *   routing_policy?: lnrpc.RoutingPolicy.$Shape|null;
         *   advertising_node?: string|null;
         *   connecting_node?: string|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.ChannelEdgeUpdate.$Shape
         */

        /**
         * Constructs a new ChannelEdgeUpdate.
         * @memberof lnrpc
         * @classdesc Represents a ChannelEdgeUpdate.
         * @constructor
         * @param {lnrpc.ChannelEdgeUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelEdgeUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelEdgeUpdate chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.ChannelEdgeUpdate
         * @instance
         */
        ChannelEdgeUpdate.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelEdgeUpdate chan_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} chan_point
         * @memberof lnrpc.ChannelEdgeUpdate
         * @instance
         */
        ChannelEdgeUpdate.prototype.chan_point = null;

        /**
         * ChannelEdgeUpdate capacity.
         * @member {Long} capacity
         * @memberof lnrpc.ChannelEdgeUpdate
         * @instance
         */
        ChannelEdgeUpdate.prototype.capacity = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChannelEdgeUpdate routing_policy.
         * @member {lnrpc.RoutingPolicy.$Properties|null|undefined} routing_policy
         * @memberof lnrpc.ChannelEdgeUpdate
         * @instance
         */
        ChannelEdgeUpdate.prototype.routing_policy = null;

        /**
         * ChannelEdgeUpdate advertising_node.
         * @member {string} advertising_node
         * @memberof lnrpc.ChannelEdgeUpdate
         * @instance
         */
        ChannelEdgeUpdate.prototype.advertising_node = "";

        /**
         * ChannelEdgeUpdate connecting_node.
         * @member {string} connecting_node
         * @memberof lnrpc.ChannelEdgeUpdate
         * @instance
         */
        ChannelEdgeUpdate.prototype.connecting_node = "";

        /**
         * Creates a new ChannelEdgeUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelEdgeUpdate
         * @static
         * @param {lnrpc.ChannelEdgeUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelEdgeUpdate} ChannelEdgeUpdate instance
         * @type {{
         *   (properties: lnrpc.ChannelEdgeUpdate.$Shape): lnrpc.ChannelEdgeUpdate & lnrpc.ChannelEdgeUpdate.$Shape;
         *   (properties?: lnrpc.ChannelEdgeUpdate.$Properties): lnrpc.ChannelEdgeUpdate;
         * }}
         */
        ChannelEdgeUpdate.create = function(properties) {
            return new ChannelEdgeUpdate(properties);
        };

        /**
         * Encodes the specified ChannelEdgeUpdate message. Does not implicitly {@link lnrpc.ChannelEdgeUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelEdgeUpdate
         * @static
         * @param {lnrpc.ChannelEdgeUpdate.$Properties} message ChannelEdgeUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelEdgeUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.chan_id);
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                $root.lnrpc.ChannelPoint.encode(message.chan_point, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity") && (typeof message.capacity === "object" ? message.capacity.low || message.capacity.high : message.capacity !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.capacity);
            if (message.routing_policy != null && $Object.hasOwnProperty.call(message, "routing_policy"))
                $root.lnrpc.RoutingPolicy.encode(message.routing_policy, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.advertising_node != null && $Object.hasOwnProperty.call(message, "advertising_node") && message.advertising_node !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.advertising_node);
            if (message.connecting_node != null && $Object.hasOwnProperty.call(message, "connecting_node") && message.connecting_node !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.connecting_node);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelEdgeUpdate message, length delimited. Does not implicitly {@link lnrpc.ChannelEdgeUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelEdgeUpdate
         * @static
         * @param {lnrpc.ChannelEdgeUpdate.$Properties} message ChannelEdgeUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelEdgeUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelEdgeUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelEdgeUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelEdgeUpdate & lnrpc.ChannelEdgeUpdate.$Shape} ChannelEdgeUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelEdgeUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelEdgeUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.chan_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_point);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.capacity = value;
                        else
                            delete message.capacity;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.routing_policy = $root.lnrpc.RoutingPolicy.decode(reader, reader.uint32(), $undefined, _depth + 1, message.routing_policy);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.advertising_node = value;
                        else
                            delete message.advertising_node;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.connecting_node = value;
                        else
                            delete message.connecting_node;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelEdgeUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelEdgeUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelEdgeUpdate & lnrpc.ChannelEdgeUpdate.$Shape} ChannelEdgeUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelEdgeUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelEdgeUpdate message.
         * @function verify
         * @memberof lnrpc.ChannelEdgeUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelEdgeUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.chan_point, _depth + 1);
                if (error)
                    return "chan_point." + error;
            }
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (!$util.isInteger(message.capacity) && !(message.capacity && $util.isInteger(message.capacity.low) && $util.isInteger(message.capacity.high)))
                    return "capacity: integer|Long expected";
            if (message.routing_policy != null && $Object.hasOwnProperty.call(message, "routing_policy")) {
                let error = $root.lnrpc.RoutingPolicy.verify(message.routing_policy, _depth + 1);
                if (error)
                    return "routing_policy." + error;
            }
            if (message.advertising_node != null && $Object.hasOwnProperty.call(message, "advertising_node"))
                if (!$util.isString(message.advertising_node))
                    return "advertising_node: string expected";
            if (message.connecting_node != null && $Object.hasOwnProperty.call(message, "connecting_node"))
                if (!$util.isString(message.connecting_node))
                    return "connecting_node: string expected";
            return null;
        };

        /**
         * Creates a ChannelEdgeUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelEdgeUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelEdgeUpdate} ChannelEdgeUpdate
         */
        ChannelEdgeUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelEdgeUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelEdgeUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelEdgeUpdate();
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.chan_point != null) {
                if (!$util.isObject(object.chan_point))
                    throw $TypeError(".lnrpc.ChannelEdgeUpdate.chan_point: object expected");
                message.chan_point = $root.lnrpc.ChannelPoint.fromObject(object.chan_point, _depth + 1);
            }
            if (object.capacity != null)
                if (typeof object.capacity === "object" ? object.capacity.low || object.capacity.high : $Number(object.capacity) !== 0)
                    if ($util.Long)
                        message.capacity = $util.Long.fromValue(object.capacity, false);
                    else if (typeof object.capacity === "string")
                        message.capacity = $parseInt(object.capacity, 10);
                    else if (typeof object.capacity === "number")
                        message.capacity = object.capacity;
                    else if (typeof object.capacity === "object")
                        message.capacity = new $util.LongBits(object.capacity.low >>> 0, object.capacity.high >>> 0).toNumber();
            if (object.routing_policy != null) {
                if (!$util.isObject(object.routing_policy))
                    throw $TypeError(".lnrpc.ChannelEdgeUpdate.routing_policy: object expected");
                message.routing_policy = $root.lnrpc.RoutingPolicy.fromObject(object.routing_policy, _depth + 1);
            }
            if (object.advertising_node != null)
                if (typeof object.advertising_node !== "string" || object.advertising_node.length)
                    message.advertising_node = $String(object.advertising_node);
            if (object.connecting_node != null)
                if (typeof object.connecting_node !== "string" || object.connecting_node.length)
                    message.connecting_node = $String(object.connecting_node);
            return message;
        };

        /**
         * Creates a plain object from a ChannelEdgeUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelEdgeUpdate
         * @static
         * @param {lnrpc.ChannelEdgeUpdate} message ChannelEdgeUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelEdgeUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.chan_point = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.capacity = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.capacity = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.routing_policy = null;
                object.advertising_node = "";
                object.connecting_node = "";
            }
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                object.chan_point = $root.lnrpc.ChannelPoint.toObject(message.chan_point, options, _depth + 1);
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.capacity = typeof message.capacity === "number" ? $BigInt(message.capacity) : $util.Long.fromBits(message.capacity.low >>> 0, message.capacity.high >>> 0, false).toBigInt();
                else if (typeof message.capacity === "number")
                    object.capacity = options.longs === $String ? $String(message.capacity) : message.capacity;
                else
                    object.capacity = options.longs === $String ? $util.Long.prototype.toString.call(message.capacity) : options.longs === $Number ? new $util.LongBits(message.capacity.low >>> 0, message.capacity.high >>> 0).toNumber() : message.capacity;
            if (message.routing_policy != null && $Object.hasOwnProperty.call(message, "routing_policy"))
                object.routing_policy = $root.lnrpc.RoutingPolicy.toObject(message.routing_policy, options, _depth + 1);
            if (message.advertising_node != null && $Object.hasOwnProperty.call(message, "advertising_node"))
                object.advertising_node = message.advertising_node;
            if (message.connecting_node != null && $Object.hasOwnProperty.call(message, "connecting_node"))
                object.connecting_node = message.connecting_node;
            return object;
        };

        /**
         * Converts this ChannelEdgeUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelEdgeUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelEdgeUpdate.prototype.toJSON = function() {
            return ChannelEdgeUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelEdgeUpdate
         * @function getTypeUrl
         * @memberof lnrpc.ChannelEdgeUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelEdgeUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelEdgeUpdate";
        };

        return ChannelEdgeUpdate;
    })();

    lnrpc.ClosedChannelUpdate = (function() {

        /**
         * Properties of a ClosedChannelUpdate.
         * @typedef {Object} lnrpc.ClosedChannelUpdate.$Properties
         * @property {Long|null} [chan_id] ClosedChannelUpdate chan_id
         * @property {Long|null} [capacity] ClosedChannelUpdate capacity
         * @property {number|null} [closed_height] ClosedChannelUpdate closed_height
         * @property {lnrpc.ChannelPoint.$Properties|null} [chan_point] ClosedChannelUpdate chan_point
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ClosedChannelUpdate.
         * @memberof lnrpc
         * @interface IClosedChannelUpdate
         * @augments lnrpc.ClosedChannelUpdate.$Properties
         * @deprecated Use lnrpc.ClosedChannelUpdate.$Properties instead.
         */

        /**
         * Shape of a ClosedChannelUpdate.
         * @typedef {{
         *   chan_id?: Long|null;
         *   capacity?: Long|null;
         *   closed_height?: number|null;
         *   chan_point?: lnrpc.ChannelPoint.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.ClosedChannelUpdate.$Shape
         */

        /**
         * Constructs a new ClosedChannelUpdate.
         * @memberof lnrpc
         * @classdesc Represents a ClosedChannelUpdate.
         * @constructor
         * @param {lnrpc.ClosedChannelUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ClosedChannelUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ClosedChannelUpdate chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.ClosedChannelUpdate
         * @instance
         */
        ClosedChannelUpdate.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ClosedChannelUpdate capacity.
         * @member {Long} capacity
         * @memberof lnrpc.ClosedChannelUpdate
         * @instance
         */
        ClosedChannelUpdate.prototype.capacity = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ClosedChannelUpdate closed_height.
         * @member {number} closed_height
         * @memberof lnrpc.ClosedChannelUpdate
         * @instance
         */
        ClosedChannelUpdate.prototype.closed_height = 0;

        /**
         * ClosedChannelUpdate chan_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} chan_point
         * @memberof lnrpc.ClosedChannelUpdate
         * @instance
         */
        ClosedChannelUpdate.prototype.chan_point = null;

        /**
         * Creates a new ClosedChannelUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.ClosedChannelUpdate
         * @static
         * @param {lnrpc.ClosedChannelUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ClosedChannelUpdate} ClosedChannelUpdate instance
         * @type {{
         *   (properties: lnrpc.ClosedChannelUpdate.$Shape): lnrpc.ClosedChannelUpdate & lnrpc.ClosedChannelUpdate.$Shape;
         *   (properties?: lnrpc.ClosedChannelUpdate.$Properties): lnrpc.ClosedChannelUpdate;
         * }}
         */
        ClosedChannelUpdate.create = function(properties) {
            return new ClosedChannelUpdate(properties);
        };

        /**
         * Encodes the specified ClosedChannelUpdate message. Does not implicitly {@link lnrpc.ClosedChannelUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ClosedChannelUpdate
         * @static
         * @param {lnrpc.ClosedChannelUpdate.$Properties} message ClosedChannelUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClosedChannelUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.chan_id);
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity") && (typeof message.capacity === "object" ? message.capacity.low || message.capacity.high : message.capacity !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.capacity);
            if (message.closed_height != null && $Object.hasOwnProperty.call(message, "closed_height") && message.closed_height !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.closed_height);
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                $root.lnrpc.ChannelPoint.encode(message.chan_point, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ClosedChannelUpdate message, length delimited. Does not implicitly {@link lnrpc.ClosedChannelUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ClosedChannelUpdate
         * @static
         * @param {lnrpc.ClosedChannelUpdate.$Properties} message ClosedChannelUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClosedChannelUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ClosedChannelUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ClosedChannelUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ClosedChannelUpdate & lnrpc.ClosedChannelUpdate.$Shape} ClosedChannelUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClosedChannelUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ClosedChannelUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.capacity = value;
                        else
                            delete message.capacity;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.closed_height = value;
                        else
                            delete message.closed_height;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.chan_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_point);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ClosedChannelUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ClosedChannelUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ClosedChannelUpdate & lnrpc.ClosedChannelUpdate.$Shape} ClosedChannelUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClosedChannelUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ClosedChannelUpdate message.
         * @function verify
         * @memberof lnrpc.ClosedChannelUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ClosedChannelUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (!$util.isInteger(message.capacity) && !(message.capacity && $util.isInteger(message.capacity.low) && $util.isInteger(message.capacity.high)))
                    return "capacity: integer|Long expected";
            if (message.closed_height != null && $Object.hasOwnProperty.call(message, "closed_height"))
                if (!$util.isInteger(message.closed_height))
                    return "closed_height: integer expected";
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.chan_point, _depth + 1);
                if (error)
                    return "chan_point." + error;
            }
            return null;
        };

        /**
         * Creates a ClosedChannelUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ClosedChannelUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ClosedChannelUpdate} ClosedChannelUpdate
         */
        ClosedChannelUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ClosedChannelUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ClosedChannelUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ClosedChannelUpdate();
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.capacity != null)
                if (typeof object.capacity === "object" ? object.capacity.low || object.capacity.high : $Number(object.capacity) !== 0)
                    if ($util.Long)
                        message.capacity = $util.Long.fromValue(object.capacity, false);
                    else if (typeof object.capacity === "string")
                        message.capacity = $parseInt(object.capacity, 10);
                    else if (typeof object.capacity === "number")
                        message.capacity = object.capacity;
                    else if (typeof object.capacity === "object")
                        message.capacity = new $util.LongBits(object.capacity.low >>> 0, object.capacity.high >>> 0).toNumber();
            if (object.closed_height != null)
                if ($Number(object.closed_height) !== 0)
                    message.closed_height = object.closed_height >>> 0;
            if (object.chan_point != null) {
                if (!$util.isObject(object.chan_point))
                    throw $TypeError(".lnrpc.ClosedChannelUpdate.chan_point: object expected");
                message.chan_point = $root.lnrpc.ChannelPoint.fromObject(object.chan_point, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ClosedChannelUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ClosedChannelUpdate
         * @static
         * @param {lnrpc.ClosedChannelUpdate} message ClosedChannelUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ClosedChannelUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.capacity = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.capacity = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.closed_height = 0;
                object.chan_point = null;
            }
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.capacity != null && $Object.hasOwnProperty.call(message, "capacity"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.capacity = typeof message.capacity === "number" ? $BigInt(message.capacity) : $util.Long.fromBits(message.capacity.low >>> 0, message.capacity.high >>> 0, false).toBigInt();
                else if (typeof message.capacity === "number")
                    object.capacity = options.longs === $String ? $String(message.capacity) : message.capacity;
                else
                    object.capacity = options.longs === $String ? $util.Long.prototype.toString.call(message.capacity) : options.longs === $Number ? new $util.LongBits(message.capacity.low >>> 0, message.capacity.high >>> 0).toNumber() : message.capacity;
            if (message.closed_height != null && $Object.hasOwnProperty.call(message, "closed_height"))
                object.closed_height = message.closed_height;
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                object.chan_point = $root.lnrpc.ChannelPoint.toObject(message.chan_point, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ClosedChannelUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.ClosedChannelUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ClosedChannelUpdate.prototype.toJSON = function() {
            return ClosedChannelUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ClosedChannelUpdate
         * @function getTypeUrl
         * @memberof lnrpc.ClosedChannelUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ClosedChannelUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ClosedChannelUpdate";
        };

        return ClosedChannelUpdate;
    })();

    lnrpc.HopHint = (function() {

        /**
         * Properties of a HopHint.
         * @typedef {Object} lnrpc.HopHint.$Properties
         * @property {string|null} [node_id] HopHint node_id
         * @property {Long|null} [chan_id] HopHint chan_id
         * @property {number|null} [fee_base_msat] HopHint fee_base_msat
         * @property {number|null} [fee_proportional_millionths] HopHint fee_proportional_millionths
         * @property {number|null} [cltv_expiry_delta] HopHint cltv_expiry_delta
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a HopHint.
         * @memberof lnrpc
         * @interface IHopHint
         * @augments lnrpc.HopHint.$Properties
         * @deprecated Use lnrpc.HopHint.$Properties instead.
         */

        /**
         * Shape of a HopHint.
         * @typedef {lnrpc.HopHint.$Properties} lnrpc.HopHint.$Shape
         */

        /**
         * Constructs a new HopHint.
         * @memberof lnrpc
         * @classdesc Represents a HopHint.
         * @constructor
         * @param {lnrpc.HopHint.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const HopHint = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HopHint node_id.
         * @member {string} node_id
         * @memberof lnrpc.HopHint
         * @instance
         */
        HopHint.prototype.node_id = "";

        /**
         * HopHint chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.HopHint
         * @instance
         */
        HopHint.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HopHint fee_base_msat.
         * @member {number} fee_base_msat
         * @memberof lnrpc.HopHint
         * @instance
         */
        HopHint.prototype.fee_base_msat = 0;

        /**
         * HopHint fee_proportional_millionths.
         * @member {number} fee_proportional_millionths
         * @memberof lnrpc.HopHint
         * @instance
         */
        HopHint.prototype.fee_proportional_millionths = 0;

        /**
         * HopHint cltv_expiry_delta.
         * @member {number} cltv_expiry_delta
         * @memberof lnrpc.HopHint
         * @instance
         */
        HopHint.prototype.cltv_expiry_delta = 0;

        /**
         * Creates a new HopHint instance using the specified properties.
         * @function create
         * @memberof lnrpc.HopHint
         * @static
         * @param {lnrpc.HopHint.$Properties=} [properties] Properties to set
         * @returns {lnrpc.HopHint} HopHint instance
         * @type {{
         *   (properties: lnrpc.HopHint.$Shape): lnrpc.HopHint & lnrpc.HopHint.$Shape;
         *   (properties?: lnrpc.HopHint.$Properties): lnrpc.HopHint;
         * }}
         */
        HopHint.create = function(properties) {
            return new HopHint(properties);
        };

        /**
         * Encodes the specified HopHint message. Does not implicitly {@link lnrpc.HopHint.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.HopHint
         * @static
         * @param {lnrpc.HopHint.$Properties} message HopHint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HopHint.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.node_id != null && $Object.hasOwnProperty.call(message, "node_id") && message.node_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.node_id);
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.chan_id);
            if (message.fee_base_msat != null && $Object.hasOwnProperty.call(message, "fee_base_msat") && message.fee_base_msat !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.fee_base_msat);
            if (message.fee_proportional_millionths != null && $Object.hasOwnProperty.call(message, "fee_proportional_millionths") && message.fee_proportional_millionths !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.fee_proportional_millionths);
            if (message.cltv_expiry_delta != null && $Object.hasOwnProperty.call(message, "cltv_expiry_delta") && message.cltv_expiry_delta !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.cltv_expiry_delta);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HopHint message, length delimited. Does not implicitly {@link lnrpc.HopHint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.HopHint
         * @static
         * @param {lnrpc.HopHint.$Properties} message HopHint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HopHint.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HopHint message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.HopHint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.HopHint & lnrpc.HopHint.$Shape} HopHint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HopHint.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.HopHint(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.node_id = value;
                        else
                            delete message.node_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fee_base_msat = value;
                        else
                            delete message.fee_base_msat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fee_proportional_millionths = value;
                        else
                            delete message.fee_proportional_millionths;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.cltv_expiry_delta = value;
                        else
                            delete message.cltv_expiry_delta;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HopHint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.HopHint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.HopHint & lnrpc.HopHint.$Shape} HopHint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HopHint.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HopHint message.
         * @function verify
         * @memberof lnrpc.HopHint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HopHint.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.node_id != null && $Object.hasOwnProperty.call(message, "node_id"))
                if (!$util.isString(message.node_id))
                    return "node_id: string expected";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.fee_base_msat != null && $Object.hasOwnProperty.call(message, "fee_base_msat"))
                if (!$util.isInteger(message.fee_base_msat))
                    return "fee_base_msat: integer expected";
            if (message.fee_proportional_millionths != null && $Object.hasOwnProperty.call(message, "fee_proportional_millionths"))
                if (!$util.isInteger(message.fee_proportional_millionths))
                    return "fee_proportional_millionths: integer expected";
            if (message.cltv_expiry_delta != null && $Object.hasOwnProperty.call(message, "cltv_expiry_delta"))
                if (!$util.isInteger(message.cltv_expiry_delta))
                    return "cltv_expiry_delta: integer expected";
            return null;
        };

        /**
         * Creates a HopHint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.HopHint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.HopHint} HopHint
         */
        HopHint.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.HopHint)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.HopHint: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.HopHint();
            if (object.node_id != null)
                if (typeof object.node_id !== "string" || object.node_id.length)
                    message.node_id = $String(object.node_id);
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.fee_base_msat != null)
                if ($Number(object.fee_base_msat) !== 0)
                    message.fee_base_msat = object.fee_base_msat >>> 0;
            if (object.fee_proportional_millionths != null)
                if ($Number(object.fee_proportional_millionths) !== 0)
                    message.fee_proportional_millionths = object.fee_proportional_millionths >>> 0;
            if (object.cltv_expiry_delta != null)
                if ($Number(object.cltv_expiry_delta) !== 0)
                    message.cltv_expiry_delta = object.cltv_expiry_delta >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a HopHint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.HopHint
         * @static
         * @param {lnrpc.HopHint} message HopHint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HopHint.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.node_id = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.fee_base_msat = 0;
                object.fee_proportional_millionths = 0;
                object.cltv_expiry_delta = 0;
            }
            if (message.node_id != null && $Object.hasOwnProperty.call(message, "node_id"))
                object.node_id = message.node_id;
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.fee_base_msat != null && $Object.hasOwnProperty.call(message, "fee_base_msat"))
                object.fee_base_msat = message.fee_base_msat;
            if (message.fee_proportional_millionths != null && $Object.hasOwnProperty.call(message, "fee_proportional_millionths"))
                object.fee_proportional_millionths = message.fee_proportional_millionths;
            if (message.cltv_expiry_delta != null && $Object.hasOwnProperty.call(message, "cltv_expiry_delta"))
                object.cltv_expiry_delta = message.cltv_expiry_delta;
            return object;
        };

        /**
         * Converts this HopHint to JSON.
         * @function toJSON
         * @memberof lnrpc.HopHint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HopHint.prototype.toJSON = function() {
            return HopHint.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HopHint
         * @function getTypeUrl
         * @memberof lnrpc.HopHint
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HopHint.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.HopHint";
        };

        return HopHint;
    })();

    lnrpc.SetID = (function() {

        /**
         * Properties of a SetID.
         * @typedef {Object} lnrpc.SetID.$Properties
         * @property {Uint8Array|null} [set_id] SetID set_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SetID.
         * @memberof lnrpc
         * @interface ISetID
         * @augments lnrpc.SetID.$Properties
         * @deprecated Use lnrpc.SetID.$Properties instead.
         */

        /**
         * Shape of a SetID.
         * @typedef {lnrpc.SetID.$Properties} lnrpc.SetID.$Shape
         */

        /**
         * Constructs a new SetID.
         * @memberof lnrpc
         * @classdesc Represents a SetID.
         * @constructor
         * @param {lnrpc.SetID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SetID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SetID set_id.
         * @member {Uint8Array} set_id
         * @memberof lnrpc.SetID
         * @instance
         */
        SetID.prototype.set_id = $util.newBuffer([]);

        /**
         * Creates a new SetID instance using the specified properties.
         * @function create
         * @memberof lnrpc.SetID
         * @static
         * @param {lnrpc.SetID.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SetID} SetID instance
         * @type {{
         *   (properties: lnrpc.SetID.$Shape): lnrpc.SetID & lnrpc.SetID.$Shape;
         *   (properties?: lnrpc.SetID.$Properties): lnrpc.SetID;
         * }}
         */
        SetID.create = function(properties) {
            return new SetID(properties);
        };

        /**
         * Encodes the specified SetID message. Does not implicitly {@link lnrpc.SetID.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SetID
         * @static
         * @param {lnrpc.SetID.$Properties} message SetID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id") && message.set_id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.set_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SetID message, length delimited. Does not implicitly {@link lnrpc.SetID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SetID
         * @static
         * @param {lnrpc.SetID.$Properties} message SetID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetID.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SetID message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SetID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SetID & lnrpc.SetID.$Shape} SetID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SetID(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.set_id = value;
                        else
                            delete message.set_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SetID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SetID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SetID & lnrpc.SetID.$Shape} SetID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SetID message.
         * @function verify
         * @memberof lnrpc.SetID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SetID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id"))
                if (!(message.set_id && typeof message.set_id.length === "number" || $util.isString(message.set_id)))
                    return "set_id: buffer expected";
            return null;
        };

        /**
         * Creates a SetID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SetID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SetID} SetID
         */
        SetID.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SetID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SetID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SetID();
            if (object.set_id != null)
                if (object.set_id.length)
                    if (typeof object.set_id === "string")
                        $util.base64.decode(object.set_id, message.set_id = $util.newBuffer($util.base64.length(object.set_id)), 0);
                    else if (object.set_id.length >= 0)
                        message.set_id = object.set_id;
            return message;
        };

        /**
         * Creates a plain object from a SetID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SetID
         * @static
         * @param {lnrpc.SetID} message SetID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.set_id = "";
                else {
                    object.set_id = [];
                    if (options.bytes !== $Array)
                        object.set_id = $util.newBuffer(object.set_id);
                }
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id"))
                object.set_id = options.bytes === $String ? $util.base64.encode(message.set_id, 0, message.set_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.set_id) : message.set_id;
            return object;
        };

        /**
         * Converts this SetID to JSON.
         * @function toJSON
         * @memberof lnrpc.SetID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetID.prototype.toJSON = function() {
            return SetID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SetID
         * @function getTypeUrl
         * @memberof lnrpc.SetID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SetID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SetID";
        };

        return SetID;
    })();

    lnrpc.RouteHint = (function() {

        /**
         * Properties of a RouteHint.
         * @typedef {Object} lnrpc.RouteHint.$Properties
         * @property {Array.<lnrpc.HopHint.$Properties>|null} [hop_hints] RouteHint hop_hints
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RouteHint.
         * @memberof lnrpc
         * @interface IRouteHint
         * @augments lnrpc.RouteHint.$Properties
         * @deprecated Use lnrpc.RouteHint.$Properties instead.
         */

        /**
         * Shape of a RouteHint.
         * @typedef {lnrpc.RouteHint.$Properties} lnrpc.RouteHint.$Shape
         */

        /**
         * Constructs a new RouteHint.
         * @memberof lnrpc
         * @classdesc Represents a RouteHint.
         * @constructor
         * @param {lnrpc.RouteHint.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RouteHint = function (properties) {
            this.hop_hints = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RouteHint hop_hints.
         * @member {Array.<lnrpc.HopHint.$Properties>} hop_hints
         * @memberof lnrpc.RouteHint
         * @instance
         */
        RouteHint.prototype.hop_hints = $util.emptyArray;

        /**
         * Creates a new RouteHint instance using the specified properties.
         * @function create
         * @memberof lnrpc.RouteHint
         * @static
         * @param {lnrpc.RouteHint.$Properties=} [properties] Properties to set
         * @returns {lnrpc.RouteHint} RouteHint instance
         * @type {{
         *   (properties: lnrpc.RouteHint.$Shape): lnrpc.RouteHint & lnrpc.RouteHint.$Shape;
         *   (properties?: lnrpc.RouteHint.$Properties): lnrpc.RouteHint;
         * }}
         */
        RouteHint.create = function(properties) {
            return new RouteHint(properties);
        };

        /**
         * Encodes the specified RouteHint message. Does not implicitly {@link lnrpc.RouteHint.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.RouteHint
         * @static
         * @param {lnrpc.RouteHint.$Properties} message RouteHint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RouteHint.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hop_hints != null && message.hop_hints.length)
                for (let i = 0; i < message.hop_hints.length; ++i)
                    $root.lnrpc.HopHint.encode(message.hop_hints[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RouteHint message, length delimited. Does not implicitly {@link lnrpc.RouteHint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.RouteHint
         * @static
         * @param {lnrpc.RouteHint.$Properties} message RouteHint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RouteHint.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RouteHint message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.RouteHint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.RouteHint & lnrpc.RouteHint.$Shape} RouteHint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RouteHint.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.RouteHint();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hop_hints && message.hop_hints.length))
                            message.hop_hints = [];
                        message.hop_hints.push($root.lnrpc.HopHint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RouteHint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.RouteHint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.RouteHint & lnrpc.RouteHint.$Shape} RouteHint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RouteHint.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RouteHint message.
         * @function verify
         * @memberof lnrpc.RouteHint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RouteHint.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hop_hints != null && $Object.hasOwnProperty.call(message, "hop_hints")) {
                if (!$Array.isArray(message.hop_hints))
                    return "hop_hints: array expected";
                for (let i = 0; i < message.hop_hints.length; ++i) {
                    let error = $root.lnrpc.HopHint.verify(message.hop_hints[i], _depth + 1);
                    if (error)
                        return "hop_hints." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RouteHint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.RouteHint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.RouteHint} RouteHint
         */
        RouteHint.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.RouteHint)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.RouteHint: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.RouteHint();
            if (object.hop_hints) {
                if (!$Array.isArray(object.hop_hints))
                    throw $TypeError(".lnrpc.RouteHint.hop_hints: array expected");
                message.hop_hints = $Array(object.hop_hints.length);
                for (let i = 0; i < object.hop_hints.length; ++i) {
                    if (!$util.isObject(object.hop_hints[i]))
                        throw $TypeError(".lnrpc.RouteHint.hop_hints: object expected");
                    message.hop_hints[i] = $root.lnrpc.HopHint.fromObject(object.hop_hints[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RouteHint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.RouteHint
         * @static
         * @param {lnrpc.RouteHint} message RouteHint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RouteHint.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.hop_hints = [];
            if (message.hop_hints && message.hop_hints.length) {
                object.hop_hints = $Array(message.hop_hints.length);
                for (let j = 0; j < message.hop_hints.length; ++j)
                    object.hop_hints[j] = $root.lnrpc.HopHint.toObject(message.hop_hints[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this RouteHint to JSON.
         * @function toJSON
         * @memberof lnrpc.RouteHint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RouteHint.prototype.toJSON = function() {
            return RouteHint.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RouteHint
         * @function getTypeUrl
         * @memberof lnrpc.RouteHint
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RouteHint.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.RouteHint";
        };

        return RouteHint;
    })();

    lnrpc.BlindedPaymentPath = (function() {

        /**
         * Properties of a BlindedPaymentPath.
         * @typedef {Object} lnrpc.BlindedPaymentPath.$Properties
         * @property {lnrpc.BlindedPath.$Properties|null} [blinded_path] BlindedPaymentPath blinded_path
         * @property {Long|null} [base_fee_msat] BlindedPaymentPath base_fee_msat
         * @property {number|null} [proportional_fee_rate] BlindedPaymentPath proportional_fee_rate
         * @property {number|null} [total_cltv_delta] BlindedPaymentPath total_cltv_delta
         * @property {Long|null} [htlc_min_msat] BlindedPaymentPath htlc_min_msat
         * @property {Long|null} [htlc_max_msat] BlindedPaymentPath htlc_max_msat
         * @property {Array.<lnrpc.FeatureBit>|null} [features] BlindedPaymentPath features
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BlindedPaymentPath.
         * @memberof lnrpc
         * @interface IBlindedPaymentPath
         * @augments lnrpc.BlindedPaymentPath.$Properties
         * @deprecated Use lnrpc.BlindedPaymentPath.$Properties instead.
         */

        /**
         * Shape of a BlindedPaymentPath.
         * @typedef {lnrpc.BlindedPaymentPath.$Properties} lnrpc.BlindedPaymentPath.$Shape
         */

        /**
         * Constructs a new BlindedPaymentPath.
         * @memberof lnrpc
         * @classdesc Represents a BlindedPaymentPath.
         * @constructor
         * @param {lnrpc.BlindedPaymentPath.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BlindedPaymentPath = function (properties) {
            this.features = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BlindedPaymentPath blinded_path.
         * @member {lnrpc.BlindedPath.$Properties|null|undefined} blinded_path
         * @memberof lnrpc.BlindedPaymentPath
         * @instance
         */
        BlindedPaymentPath.prototype.blinded_path = null;

        /**
         * BlindedPaymentPath base_fee_msat.
         * @member {Long} base_fee_msat
         * @memberof lnrpc.BlindedPaymentPath
         * @instance
         */
        BlindedPaymentPath.prototype.base_fee_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BlindedPaymentPath proportional_fee_rate.
         * @member {number} proportional_fee_rate
         * @memberof lnrpc.BlindedPaymentPath
         * @instance
         */
        BlindedPaymentPath.prototype.proportional_fee_rate = 0;

        /**
         * BlindedPaymentPath total_cltv_delta.
         * @member {number} total_cltv_delta
         * @memberof lnrpc.BlindedPaymentPath
         * @instance
         */
        BlindedPaymentPath.prototype.total_cltv_delta = 0;

        /**
         * BlindedPaymentPath htlc_min_msat.
         * @member {Long} htlc_min_msat
         * @memberof lnrpc.BlindedPaymentPath
         * @instance
         */
        BlindedPaymentPath.prototype.htlc_min_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BlindedPaymentPath htlc_max_msat.
         * @member {Long} htlc_max_msat
         * @memberof lnrpc.BlindedPaymentPath
         * @instance
         */
        BlindedPaymentPath.prototype.htlc_max_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BlindedPaymentPath features.
         * @member {Array.<lnrpc.FeatureBit>} features
         * @memberof lnrpc.BlindedPaymentPath
         * @instance
         */
        BlindedPaymentPath.prototype.features = $util.emptyArray;

        /**
         * Creates a new BlindedPaymentPath instance using the specified properties.
         * @function create
         * @memberof lnrpc.BlindedPaymentPath
         * @static
         * @param {lnrpc.BlindedPaymentPath.$Properties=} [properties] Properties to set
         * @returns {lnrpc.BlindedPaymentPath} BlindedPaymentPath instance
         * @type {{
         *   (properties: lnrpc.BlindedPaymentPath.$Shape): lnrpc.BlindedPaymentPath & lnrpc.BlindedPaymentPath.$Shape;
         *   (properties?: lnrpc.BlindedPaymentPath.$Properties): lnrpc.BlindedPaymentPath;
         * }}
         */
        BlindedPaymentPath.create = function(properties) {
            return new BlindedPaymentPath(properties);
        };

        /**
         * Encodes the specified BlindedPaymentPath message. Does not implicitly {@link lnrpc.BlindedPaymentPath.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.BlindedPaymentPath
         * @static
         * @param {lnrpc.BlindedPaymentPath.$Properties} message BlindedPaymentPath message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlindedPaymentPath.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.blinded_path != null && $Object.hasOwnProperty.call(message, "blinded_path"))
                $root.lnrpc.BlindedPath.encode(message.blinded_path, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat") && (typeof message.base_fee_msat === "object" ? message.base_fee_msat.low || message.base_fee_msat.high : message.base_fee_msat !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.base_fee_msat);
            if (message.proportional_fee_rate != null && $Object.hasOwnProperty.call(message, "proportional_fee_rate") && message.proportional_fee_rate !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.proportional_fee_rate);
            if (message.total_cltv_delta != null && $Object.hasOwnProperty.call(message, "total_cltv_delta") && message.total_cltv_delta !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.total_cltv_delta);
            if (message.htlc_min_msat != null && $Object.hasOwnProperty.call(message, "htlc_min_msat") && (typeof message.htlc_min_msat === "object" ? message.htlc_min_msat.low || message.htlc_min_msat.high : message.htlc_min_msat !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.htlc_min_msat);
            if (message.htlc_max_msat != null && $Object.hasOwnProperty.call(message, "htlc_max_msat") && (typeof message.htlc_max_msat === "object" ? message.htlc_max_msat.low || message.htlc_max_msat.high : message.htlc_max_msat !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.htlc_max_msat);
            if (message.features != null && message.features.length)
                writer.uint32(/* id 7, wireType 2 =*/58).int32s(message.features);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BlindedPaymentPath message, length delimited. Does not implicitly {@link lnrpc.BlindedPaymentPath.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.BlindedPaymentPath
         * @static
         * @param {lnrpc.BlindedPaymentPath.$Properties} message BlindedPaymentPath message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlindedPaymentPath.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BlindedPaymentPath message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.BlindedPaymentPath
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.BlindedPaymentPath & lnrpc.BlindedPaymentPath.$Shape} BlindedPaymentPath
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlindedPaymentPath.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.BlindedPaymentPath(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.blinded_path = $root.lnrpc.BlindedPath.decode(reader, reader.uint32(), $undefined, _depth + 1, message.blinded_path);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.base_fee_msat = value;
                        else
                            delete message.base_fee_msat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.proportional_fee_rate = value;
                        else
                            delete message.proportional_fee_rate;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_cltv_delta = value;
                        else
                            delete message.total_cltv_delta;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_min_msat = value;
                        else
                            delete message.htlc_min_msat;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_max_msat = value;
                        else
                            delete message.htlc_max_msat;
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.features && message.features.length))
                                message.features = [];
                            reader.int32s(message.features);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.features && message.features.length))
                            message.features = [];
                        message.features.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BlindedPaymentPath message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.BlindedPaymentPath
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.BlindedPaymentPath & lnrpc.BlindedPaymentPath.$Shape} BlindedPaymentPath
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlindedPaymentPath.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BlindedPaymentPath message.
         * @function verify
         * @memberof lnrpc.BlindedPaymentPath
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BlindedPaymentPath.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.blinded_path != null && $Object.hasOwnProperty.call(message, "blinded_path")) {
                let error = $root.lnrpc.BlindedPath.verify(message.blinded_path, _depth + 1);
                if (error)
                    return "blinded_path." + error;
            }
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat"))
                if (!$util.isInteger(message.base_fee_msat) && !(message.base_fee_msat && $util.isInteger(message.base_fee_msat.low) && $util.isInteger(message.base_fee_msat.high)))
                    return "base_fee_msat: integer|Long expected";
            if (message.proportional_fee_rate != null && $Object.hasOwnProperty.call(message, "proportional_fee_rate"))
                if (!$util.isInteger(message.proportional_fee_rate))
                    return "proportional_fee_rate: integer expected";
            if (message.total_cltv_delta != null && $Object.hasOwnProperty.call(message, "total_cltv_delta"))
                if (!$util.isInteger(message.total_cltv_delta))
                    return "total_cltv_delta: integer expected";
            if (message.htlc_min_msat != null && $Object.hasOwnProperty.call(message, "htlc_min_msat"))
                if (!$util.isInteger(message.htlc_min_msat) && !(message.htlc_min_msat && $util.isInteger(message.htlc_min_msat.low) && $util.isInteger(message.htlc_min_msat.high)))
                    return "htlc_min_msat: integer|Long expected";
            if (message.htlc_max_msat != null && $Object.hasOwnProperty.call(message, "htlc_max_msat"))
                if (!$util.isInteger(message.htlc_max_msat) && !(message.htlc_max_msat && $util.isInteger(message.htlc_max_msat.low) && $util.isInteger(message.htlc_max_msat.high)))
                    return "htlc_max_msat: integer|Long expected";
            if (message.features != null && $Object.hasOwnProperty.call(message, "features")) {
                if (!$Array.isArray(message.features))
                    return "features: array expected";
                for (let i = 0; i < message.features.length; ++i)
                    if (typeof message.features[i] !== "number" || (message.features[i] | 0) !== message.features[i])
                        return "features: enum value[] expected";
            }
            return null;
        };

        /**
         * Creates a BlindedPaymentPath message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.BlindedPaymentPath
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.BlindedPaymentPath} BlindedPaymentPath
         */
        BlindedPaymentPath.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.BlindedPaymentPath)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.BlindedPaymentPath: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.BlindedPaymentPath();
            if (object.blinded_path != null) {
                if (!$util.isObject(object.blinded_path))
                    throw $TypeError(".lnrpc.BlindedPaymentPath.blinded_path: object expected");
                message.blinded_path = $root.lnrpc.BlindedPath.fromObject(object.blinded_path, _depth + 1);
            }
            if (object.base_fee_msat != null)
                if (typeof object.base_fee_msat === "object" ? object.base_fee_msat.low || object.base_fee_msat.high : $Number(object.base_fee_msat) !== 0)
                    if ($util.Long)
                        message.base_fee_msat = $util.Long.fromValue(object.base_fee_msat, true);
                    else if (typeof object.base_fee_msat === "string")
                        message.base_fee_msat = $parseInt(object.base_fee_msat, 10);
                    else if (typeof object.base_fee_msat === "number")
                        message.base_fee_msat = object.base_fee_msat;
                    else if (typeof object.base_fee_msat === "object")
                        message.base_fee_msat = new $util.LongBits(object.base_fee_msat.low >>> 0, object.base_fee_msat.high >>> 0).toNumber(true);
            if (object.proportional_fee_rate != null)
                if ($Number(object.proportional_fee_rate) !== 0)
                    message.proportional_fee_rate = object.proportional_fee_rate >>> 0;
            if (object.total_cltv_delta != null)
                if ($Number(object.total_cltv_delta) !== 0)
                    message.total_cltv_delta = object.total_cltv_delta >>> 0;
            if (object.htlc_min_msat != null)
                if (typeof object.htlc_min_msat === "object" ? object.htlc_min_msat.low || object.htlc_min_msat.high : $Number(object.htlc_min_msat) !== 0)
                    if ($util.Long)
                        message.htlc_min_msat = $util.Long.fromValue(object.htlc_min_msat, true);
                    else if (typeof object.htlc_min_msat === "string")
                        message.htlc_min_msat = $parseInt(object.htlc_min_msat, 10);
                    else if (typeof object.htlc_min_msat === "number")
                        message.htlc_min_msat = object.htlc_min_msat;
                    else if (typeof object.htlc_min_msat === "object")
                        message.htlc_min_msat = new $util.LongBits(object.htlc_min_msat.low >>> 0, object.htlc_min_msat.high >>> 0).toNumber(true);
            if (object.htlc_max_msat != null)
                if (typeof object.htlc_max_msat === "object" ? object.htlc_max_msat.low || object.htlc_max_msat.high : $Number(object.htlc_max_msat) !== 0)
                    if ($util.Long)
                        message.htlc_max_msat = $util.Long.fromValue(object.htlc_max_msat, true);
                    else if (typeof object.htlc_max_msat === "string")
                        message.htlc_max_msat = $parseInt(object.htlc_max_msat, 10);
                    else if (typeof object.htlc_max_msat === "number")
                        message.htlc_max_msat = object.htlc_max_msat;
                    else if (typeof object.htlc_max_msat === "object")
                        message.htlc_max_msat = new $util.LongBits(object.htlc_max_msat.low >>> 0, object.htlc_max_msat.high >>> 0).toNumber(true);
            if (object.features) {
                if (!$Array.isArray(object.features))
                    throw $TypeError(".lnrpc.BlindedPaymentPath.features: array expected");
                message.features = [];
                for (let i = 0; i < object.features.length; ++i)
                    switch (object.features[i]) {
                    case "DATALOSS_PROTECT_REQ":
                    case 0:
                        message.features[message.features.length] = 0;
                        break;
                    case "DATALOSS_PROTECT_OPT":
                    case 1:
                        message.features[message.features.length] = 1;
                        break;
                    case "INITIAL_ROUING_SYNC":
                    case 3:
                        message.features[message.features.length] = 3;
                        break;
                    case "UPFRONT_SHUTDOWN_SCRIPT_REQ":
                    case 4:
                        message.features[message.features.length] = 4;
                        break;
                    case "UPFRONT_SHUTDOWN_SCRIPT_OPT":
                    case 5:
                        message.features[message.features.length] = 5;
                        break;
                    case "GOSSIP_QUERIES_REQ":
                    case 6:
                        message.features[message.features.length] = 6;
                        break;
                    case "GOSSIP_QUERIES_OPT":
                    case 7:
                        message.features[message.features.length] = 7;
                        break;
                    case "TLV_ONION_REQ":
                    case 8:
                        message.features[message.features.length] = 8;
                        break;
                    case "TLV_ONION_OPT":
                    case 9:
                        message.features[message.features.length] = 9;
                        break;
                    case "EXT_GOSSIP_QUERIES_REQ":
                    case 10:
                        message.features[message.features.length] = 10;
                        break;
                    case "EXT_GOSSIP_QUERIES_OPT":
                    case 11:
                        message.features[message.features.length] = 11;
                        break;
                    case "STATIC_REMOTE_KEY_REQ":
                    case 12:
                        message.features[message.features.length] = 12;
                        break;
                    case "STATIC_REMOTE_KEY_OPT":
                    case 13:
                        message.features[message.features.length] = 13;
                        break;
                    case "PAYMENT_ADDR_REQ":
                    case 14:
                        message.features[message.features.length] = 14;
                        break;
                    case "PAYMENT_ADDR_OPT":
                    case 15:
                        message.features[message.features.length] = 15;
                        break;
                    case "MPP_REQ":
                    case 16:
                        message.features[message.features.length] = 16;
                        break;
                    case "MPP_OPT":
                    case 17:
                        message.features[message.features.length] = 17;
                        break;
                    case "WUMBO_CHANNELS_REQ":
                    case 18:
                        message.features[message.features.length] = 18;
                        break;
                    case "WUMBO_CHANNELS_OPT":
                    case 19:
                        message.features[message.features.length] = 19;
                        break;
                    case "ANCHORS_REQ":
                    case 20:
                        message.features[message.features.length] = 20;
                        break;
                    case "ANCHORS_OPT":
                    case 21:
                        message.features[message.features.length] = 21;
                        break;
                    case "ANCHORS_ZERO_FEE_HTLC_REQ":
                    case 22:
                        message.features[message.features.length] = 22;
                        break;
                    case "ANCHORS_ZERO_FEE_HTLC_OPT":
                    case 23:
                        message.features[message.features.length] = 23;
                        break;
                    case "ROUTE_BLINDING_REQUIRED":
                    case 24:
                        message.features[message.features.length] = 24;
                        break;
                    case "ROUTE_BLINDING_OPTIONAL":
                    case 25:
                        message.features[message.features.length] = 25;
                        break;
                    case "AMP_REQ":
                    case 30:
                        message.features[message.features.length] = 30;
                        break;
                    case "AMP_OPT":
                    case 31:
                        message.features[message.features.length] = 31;
                        break;
                    default:
                        if (typeof object.features[i] === "number" && (object.features[i] | 0) === object.features[i])
                            message.features[message.features.length] = object.features[i];
                    }
            }
            return message;
        };

        /**
         * Creates a plain object from a BlindedPaymentPath message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.BlindedPaymentPath
         * @static
         * @param {lnrpc.BlindedPaymentPath} message BlindedPaymentPath
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BlindedPaymentPath.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.features = [];
            if (options.defaults) {
                object.blinded_path = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.base_fee_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.base_fee_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.proportional_fee_rate = 0;
                object.total_cltv_delta = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_min_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_min_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_max_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_max_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.blinded_path != null && $Object.hasOwnProperty.call(message, "blinded_path"))
                object.blinded_path = $root.lnrpc.BlindedPath.toObject(message.blinded_path, options, _depth + 1);
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.base_fee_msat = typeof message.base_fee_msat === "number" ? $BigInt(message.base_fee_msat) : $util.Long.fromBits(message.base_fee_msat.low >>> 0, message.base_fee_msat.high >>> 0, true).toBigInt();
                else if (typeof message.base_fee_msat === "number")
                    object.base_fee_msat = options.longs === $String ? $String(message.base_fee_msat) : message.base_fee_msat;
                else
                    object.base_fee_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.base_fee_msat) : options.longs === $Number ? new $util.LongBits(message.base_fee_msat.low >>> 0, message.base_fee_msat.high >>> 0).toNumber(true) : message.base_fee_msat;
            if (message.proportional_fee_rate != null && $Object.hasOwnProperty.call(message, "proportional_fee_rate"))
                object.proportional_fee_rate = message.proportional_fee_rate;
            if (message.total_cltv_delta != null && $Object.hasOwnProperty.call(message, "total_cltv_delta"))
                object.total_cltv_delta = message.total_cltv_delta;
            if (message.htlc_min_msat != null && $Object.hasOwnProperty.call(message, "htlc_min_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_min_msat = typeof message.htlc_min_msat === "number" ? $BigInt(message.htlc_min_msat) : $util.Long.fromBits(message.htlc_min_msat.low >>> 0, message.htlc_min_msat.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_min_msat === "number")
                    object.htlc_min_msat = options.longs === $String ? $String(message.htlc_min_msat) : message.htlc_min_msat;
                else
                    object.htlc_min_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_min_msat) : options.longs === $Number ? new $util.LongBits(message.htlc_min_msat.low >>> 0, message.htlc_min_msat.high >>> 0).toNumber(true) : message.htlc_min_msat;
            if (message.htlc_max_msat != null && $Object.hasOwnProperty.call(message, "htlc_max_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_max_msat = typeof message.htlc_max_msat === "number" ? $BigInt(message.htlc_max_msat) : $util.Long.fromBits(message.htlc_max_msat.low >>> 0, message.htlc_max_msat.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_max_msat === "number")
                    object.htlc_max_msat = options.longs === $String ? $String(message.htlc_max_msat) : message.htlc_max_msat;
                else
                    object.htlc_max_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_max_msat) : options.longs === $Number ? new $util.LongBits(message.htlc_max_msat.low >>> 0, message.htlc_max_msat.high >>> 0).toNumber(true) : message.htlc_max_msat;
            if (message.features && message.features.length) {
                object.features = $Array(message.features.length);
                for (let j = 0; j < message.features.length; ++j)
                    object.features[j] = options.enums === $String ? $root.lnrpc.FeatureBit[message.features[j]] === $undefined ? message.features[j] : $root.lnrpc.FeatureBit[message.features[j]] : message.features[j];
            }
            return object;
        };

        /**
         * Converts this BlindedPaymentPath to JSON.
         * @function toJSON
         * @memberof lnrpc.BlindedPaymentPath
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BlindedPaymentPath.prototype.toJSON = function() {
            return BlindedPaymentPath.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BlindedPaymentPath
         * @function getTypeUrl
         * @memberof lnrpc.BlindedPaymentPath
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BlindedPaymentPath.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.BlindedPaymentPath";
        };

        return BlindedPaymentPath;
    })();

    lnrpc.BlindedPath = (function() {

        /**
         * Properties of a BlindedPath.
         * @typedef {Object} lnrpc.BlindedPath.$Properties
         * @property {Uint8Array|null} [introduction_node] BlindedPath introduction_node
         * @property {Uint8Array|null} [blinding_point] BlindedPath blinding_point
         * @property {Array.<lnrpc.BlindedHop.$Properties>|null} [blinded_hops] BlindedPath blinded_hops
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BlindedPath.
         * @memberof lnrpc
         * @interface IBlindedPath
         * @augments lnrpc.BlindedPath.$Properties
         * @deprecated Use lnrpc.BlindedPath.$Properties instead.
         */

        /**
         * Shape of a BlindedPath.
         * @typedef {lnrpc.BlindedPath.$Properties} lnrpc.BlindedPath.$Shape
         */

        /**
         * Constructs a new BlindedPath.
         * @memberof lnrpc
         * @classdesc Represents a BlindedPath.
         * @constructor
         * @param {lnrpc.BlindedPath.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BlindedPath = function (properties) {
            this.blinded_hops = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BlindedPath introduction_node.
         * @member {Uint8Array} introduction_node
         * @memberof lnrpc.BlindedPath
         * @instance
         */
        BlindedPath.prototype.introduction_node = $util.newBuffer([]);

        /**
         * BlindedPath blinding_point.
         * @member {Uint8Array} blinding_point
         * @memberof lnrpc.BlindedPath
         * @instance
         */
        BlindedPath.prototype.blinding_point = $util.newBuffer([]);

        /**
         * BlindedPath blinded_hops.
         * @member {Array.<lnrpc.BlindedHop.$Properties>} blinded_hops
         * @memberof lnrpc.BlindedPath
         * @instance
         */
        BlindedPath.prototype.blinded_hops = $util.emptyArray;

        /**
         * Creates a new BlindedPath instance using the specified properties.
         * @function create
         * @memberof lnrpc.BlindedPath
         * @static
         * @param {lnrpc.BlindedPath.$Properties=} [properties] Properties to set
         * @returns {lnrpc.BlindedPath} BlindedPath instance
         * @type {{
         *   (properties: lnrpc.BlindedPath.$Shape): lnrpc.BlindedPath & lnrpc.BlindedPath.$Shape;
         *   (properties?: lnrpc.BlindedPath.$Properties): lnrpc.BlindedPath;
         * }}
         */
        BlindedPath.create = function(properties) {
            return new BlindedPath(properties);
        };

        /**
         * Encodes the specified BlindedPath message. Does not implicitly {@link lnrpc.BlindedPath.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.BlindedPath
         * @static
         * @param {lnrpc.BlindedPath.$Properties} message BlindedPath message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlindedPath.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.introduction_node != null && $Object.hasOwnProperty.call(message, "introduction_node") && message.introduction_node.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.introduction_node);
            if (message.blinding_point != null && $Object.hasOwnProperty.call(message, "blinding_point") && message.blinding_point.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.blinding_point);
            if (message.blinded_hops != null && message.blinded_hops.length)
                for (let i = 0; i < message.blinded_hops.length; ++i)
                    $root.lnrpc.BlindedHop.encode(message.blinded_hops[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BlindedPath message, length delimited. Does not implicitly {@link lnrpc.BlindedPath.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.BlindedPath
         * @static
         * @param {lnrpc.BlindedPath.$Properties} message BlindedPath message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlindedPath.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BlindedPath message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.BlindedPath
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.BlindedPath & lnrpc.BlindedPath.$Shape} BlindedPath
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlindedPath.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.BlindedPath(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.introduction_node = value;
                        else
                            delete message.introduction_node;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.blinding_point = value;
                        else
                            delete message.blinding_point;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.blinded_hops && message.blinded_hops.length))
                            message.blinded_hops = [];
                        message.blinded_hops.push($root.lnrpc.BlindedHop.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BlindedPath message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.BlindedPath
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.BlindedPath & lnrpc.BlindedPath.$Shape} BlindedPath
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlindedPath.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BlindedPath message.
         * @function verify
         * @memberof lnrpc.BlindedPath
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BlindedPath.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.introduction_node != null && $Object.hasOwnProperty.call(message, "introduction_node"))
                if (!(message.introduction_node && typeof message.introduction_node.length === "number" || $util.isString(message.introduction_node)))
                    return "introduction_node: buffer expected";
            if (message.blinding_point != null && $Object.hasOwnProperty.call(message, "blinding_point"))
                if (!(message.blinding_point && typeof message.blinding_point.length === "number" || $util.isString(message.blinding_point)))
                    return "blinding_point: buffer expected";
            if (message.blinded_hops != null && $Object.hasOwnProperty.call(message, "blinded_hops")) {
                if (!$Array.isArray(message.blinded_hops))
                    return "blinded_hops: array expected";
                for (let i = 0; i < message.blinded_hops.length; ++i) {
                    let error = $root.lnrpc.BlindedHop.verify(message.blinded_hops[i], _depth + 1);
                    if (error)
                        return "blinded_hops." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BlindedPath message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.BlindedPath
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.BlindedPath} BlindedPath
         */
        BlindedPath.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.BlindedPath)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.BlindedPath: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.BlindedPath();
            if (object.introduction_node != null)
                if (object.introduction_node.length)
                    if (typeof object.introduction_node === "string")
                        $util.base64.decode(object.introduction_node, message.introduction_node = $util.newBuffer($util.base64.length(object.introduction_node)), 0);
                    else if (object.introduction_node.length >= 0)
                        message.introduction_node = object.introduction_node;
            if (object.blinding_point != null)
                if (object.blinding_point.length)
                    if (typeof object.blinding_point === "string")
                        $util.base64.decode(object.blinding_point, message.blinding_point = $util.newBuffer($util.base64.length(object.blinding_point)), 0);
                    else if (object.blinding_point.length >= 0)
                        message.blinding_point = object.blinding_point;
            if (object.blinded_hops) {
                if (!$Array.isArray(object.blinded_hops))
                    throw $TypeError(".lnrpc.BlindedPath.blinded_hops: array expected");
                message.blinded_hops = $Array(object.blinded_hops.length);
                for (let i = 0; i < object.blinded_hops.length; ++i) {
                    if (!$util.isObject(object.blinded_hops[i]))
                        throw $TypeError(".lnrpc.BlindedPath.blinded_hops: object expected");
                    message.blinded_hops[i] = $root.lnrpc.BlindedHop.fromObject(object.blinded_hops[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BlindedPath message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.BlindedPath
         * @static
         * @param {lnrpc.BlindedPath} message BlindedPath
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BlindedPath.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.blinded_hops = [];
            if (options.defaults) {
                if (options.bytes === $String)
                    object.introduction_node = "";
                else {
                    object.introduction_node = [];
                    if (options.bytes !== $Array)
                        object.introduction_node = $util.newBuffer(object.introduction_node);
                }
                if (options.bytes === $String)
                    object.blinding_point = "";
                else {
                    object.blinding_point = [];
                    if (options.bytes !== $Array)
                        object.blinding_point = $util.newBuffer(object.blinding_point);
                }
            }
            if (message.introduction_node != null && $Object.hasOwnProperty.call(message, "introduction_node"))
                object.introduction_node = options.bytes === $String ? $util.base64.encode(message.introduction_node, 0, message.introduction_node.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.introduction_node) : message.introduction_node;
            if (message.blinding_point != null && $Object.hasOwnProperty.call(message, "blinding_point"))
                object.blinding_point = options.bytes === $String ? $util.base64.encode(message.blinding_point, 0, message.blinding_point.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.blinding_point) : message.blinding_point;
            if (message.blinded_hops && message.blinded_hops.length) {
                object.blinded_hops = $Array(message.blinded_hops.length);
                for (let j = 0; j < message.blinded_hops.length; ++j)
                    object.blinded_hops[j] = $root.lnrpc.BlindedHop.toObject(message.blinded_hops[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this BlindedPath to JSON.
         * @function toJSON
         * @memberof lnrpc.BlindedPath
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BlindedPath.prototype.toJSON = function() {
            return BlindedPath.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BlindedPath
         * @function getTypeUrl
         * @memberof lnrpc.BlindedPath
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BlindedPath.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.BlindedPath";
        };

        return BlindedPath;
    })();

    lnrpc.BlindedHop = (function() {

        /**
         * Properties of a BlindedHop.
         * @typedef {Object} lnrpc.BlindedHop.$Properties
         * @property {Uint8Array|null} [blinded_node] BlindedHop blinded_node
         * @property {Uint8Array|null} [encrypted_data] BlindedHop encrypted_data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BlindedHop.
         * @memberof lnrpc
         * @interface IBlindedHop
         * @augments lnrpc.BlindedHop.$Properties
         * @deprecated Use lnrpc.BlindedHop.$Properties instead.
         */

        /**
         * Shape of a BlindedHop.
         * @typedef {lnrpc.BlindedHop.$Properties} lnrpc.BlindedHop.$Shape
         */

        /**
         * Constructs a new BlindedHop.
         * @memberof lnrpc
         * @classdesc Represents a BlindedHop.
         * @constructor
         * @param {lnrpc.BlindedHop.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BlindedHop = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BlindedHop blinded_node.
         * @member {Uint8Array} blinded_node
         * @memberof lnrpc.BlindedHop
         * @instance
         */
        BlindedHop.prototype.blinded_node = $util.newBuffer([]);

        /**
         * BlindedHop encrypted_data.
         * @member {Uint8Array} encrypted_data
         * @memberof lnrpc.BlindedHop
         * @instance
         */
        BlindedHop.prototype.encrypted_data = $util.newBuffer([]);

        /**
         * Creates a new BlindedHop instance using the specified properties.
         * @function create
         * @memberof lnrpc.BlindedHop
         * @static
         * @param {lnrpc.BlindedHop.$Properties=} [properties] Properties to set
         * @returns {lnrpc.BlindedHop} BlindedHop instance
         * @type {{
         *   (properties: lnrpc.BlindedHop.$Shape): lnrpc.BlindedHop & lnrpc.BlindedHop.$Shape;
         *   (properties?: lnrpc.BlindedHop.$Properties): lnrpc.BlindedHop;
         * }}
         */
        BlindedHop.create = function(properties) {
            return new BlindedHop(properties);
        };

        /**
         * Encodes the specified BlindedHop message. Does not implicitly {@link lnrpc.BlindedHop.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.BlindedHop
         * @static
         * @param {lnrpc.BlindedHop.$Properties} message BlindedHop message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlindedHop.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.blinded_node != null && $Object.hasOwnProperty.call(message, "blinded_node") && message.blinded_node.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.blinded_node);
            if (message.encrypted_data != null && $Object.hasOwnProperty.call(message, "encrypted_data") && message.encrypted_data.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encrypted_data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BlindedHop message, length delimited. Does not implicitly {@link lnrpc.BlindedHop.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.BlindedHop
         * @static
         * @param {lnrpc.BlindedHop.$Properties} message BlindedHop message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlindedHop.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BlindedHop message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.BlindedHop
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.BlindedHop & lnrpc.BlindedHop.$Shape} BlindedHop
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlindedHop.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.BlindedHop(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.blinded_node = value;
                        else
                            delete message.blinded_node;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.encrypted_data = value;
                        else
                            delete message.encrypted_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BlindedHop message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.BlindedHop
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.BlindedHop & lnrpc.BlindedHop.$Shape} BlindedHop
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlindedHop.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BlindedHop message.
         * @function verify
         * @memberof lnrpc.BlindedHop
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BlindedHop.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.blinded_node != null && $Object.hasOwnProperty.call(message, "blinded_node"))
                if (!(message.blinded_node && typeof message.blinded_node.length === "number" || $util.isString(message.blinded_node)))
                    return "blinded_node: buffer expected";
            if (message.encrypted_data != null && $Object.hasOwnProperty.call(message, "encrypted_data"))
                if (!(message.encrypted_data && typeof message.encrypted_data.length === "number" || $util.isString(message.encrypted_data)))
                    return "encrypted_data: buffer expected";
            return null;
        };

        /**
         * Creates a BlindedHop message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.BlindedHop
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.BlindedHop} BlindedHop
         */
        BlindedHop.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.BlindedHop)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.BlindedHop: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.BlindedHop();
            if (object.blinded_node != null)
                if (object.blinded_node.length)
                    if (typeof object.blinded_node === "string")
                        $util.base64.decode(object.blinded_node, message.blinded_node = $util.newBuffer($util.base64.length(object.blinded_node)), 0);
                    else if (object.blinded_node.length >= 0)
                        message.blinded_node = object.blinded_node;
            if (object.encrypted_data != null)
                if (object.encrypted_data.length)
                    if (typeof object.encrypted_data === "string")
                        $util.base64.decode(object.encrypted_data, message.encrypted_data = $util.newBuffer($util.base64.length(object.encrypted_data)), 0);
                    else if (object.encrypted_data.length >= 0)
                        message.encrypted_data = object.encrypted_data;
            return message;
        };

        /**
         * Creates a plain object from a BlindedHop message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.BlindedHop
         * @static
         * @param {lnrpc.BlindedHop} message BlindedHop
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BlindedHop.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.blinded_node = "";
                else {
                    object.blinded_node = [];
                    if (options.bytes !== $Array)
                        object.blinded_node = $util.newBuffer(object.blinded_node);
                }
                if (options.bytes === $String)
                    object.encrypted_data = "";
                else {
                    object.encrypted_data = [];
                    if (options.bytes !== $Array)
                        object.encrypted_data = $util.newBuffer(object.encrypted_data);
                }
            }
            if (message.blinded_node != null && $Object.hasOwnProperty.call(message, "blinded_node"))
                object.blinded_node = options.bytes === $String ? $util.base64.encode(message.blinded_node, 0, message.blinded_node.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.blinded_node) : message.blinded_node;
            if (message.encrypted_data != null && $Object.hasOwnProperty.call(message, "encrypted_data"))
                object.encrypted_data = options.bytes === $String ? $util.base64.encode(message.encrypted_data, 0, message.encrypted_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.encrypted_data) : message.encrypted_data;
            return object;
        };

        /**
         * Converts this BlindedHop to JSON.
         * @function toJSON
         * @memberof lnrpc.BlindedHop
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BlindedHop.prototype.toJSON = function() {
            return BlindedHop.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BlindedHop
         * @function getTypeUrl
         * @memberof lnrpc.BlindedHop
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BlindedHop.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.BlindedHop";
        };

        return BlindedHop;
    })();

    lnrpc.AMPInvoiceState = (function() {

        /**
         * Properties of a AMPInvoiceState.
         * @typedef {Object} lnrpc.AMPInvoiceState.$Properties
         * @property {lnrpc.InvoiceHTLCState|null} [state] AMPInvoiceState state
         * @property {Long|null} [settle_index] AMPInvoiceState settle_index
         * @property {Long|null} [settle_time] AMPInvoiceState settle_time
         * @property {Long|null} [amt_paid_msat] AMPInvoiceState amt_paid_msat
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a AMPInvoiceState.
         * @memberof lnrpc
         * @interface IAMPInvoiceState
         * @augments lnrpc.AMPInvoiceState.$Properties
         * @deprecated Use lnrpc.AMPInvoiceState.$Properties instead.
         */

        /**
         * Shape of a AMPInvoiceState.
         * @typedef {lnrpc.AMPInvoiceState.$Properties} lnrpc.AMPInvoiceState.$Shape
         */

        /**
         * Constructs a new AMPInvoiceState.
         * @memberof lnrpc
         * @classdesc Represents a AMPInvoiceState.
         * @constructor
         * @param {lnrpc.AMPInvoiceState.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AMPInvoiceState = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AMPInvoiceState state.
         * @member {lnrpc.InvoiceHTLCState} state
         * @memberof lnrpc.AMPInvoiceState
         * @instance
         */
        AMPInvoiceState.prototype.state = 0;

        /**
         * AMPInvoiceState settle_index.
         * @member {Long} settle_index
         * @memberof lnrpc.AMPInvoiceState
         * @instance
         */
        AMPInvoiceState.prototype.settle_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * AMPInvoiceState settle_time.
         * @member {Long} settle_time
         * @memberof lnrpc.AMPInvoiceState
         * @instance
         */
        AMPInvoiceState.prototype.settle_time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AMPInvoiceState amt_paid_msat.
         * @member {Long} amt_paid_msat
         * @memberof lnrpc.AMPInvoiceState
         * @instance
         */
        AMPInvoiceState.prototype.amt_paid_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new AMPInvoiceState instance using the specified properties.
         * @function create
         * @memberof lnrpc.AMPInvoiceState
         * @static
         * @param {lnrpc.AMPInvoiceState.$Properties=} [properties] Properties to set
         * @returns {lnrpc.AMPInvoiceState} AMPInvoiceState instance
         * @type {{
         *   (properties: lnrpc.AMPInvoiceState.$Shape): lnrpc.AMPInvoiceState & lnrpc.AMPInvoiceState.$Shape;
         *   (properties?: lnrpc.AMPInvoiceState.$Properties): lnrpc.AMPInvoiceState;
         * }}
         */
        AMPInvoiceState.create = function(properties) {
            return new AMPInvoiceState(properties);
        };

        /**
         * Encodes the specified AMPInvoiceState message. Does not implicitly {@link lnrpc.AMPInvoiceState.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.AMPInvoiceState
         * @static
         * @param {lnrpc.AMPInvoiceState.$Properties} message AMPInvoiceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AMPInvoiceState.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
            if (message.settle_index != null && $Object.hasOwnProperty.call(message, "settle_index") && (typeof message.settle_index === "object" ? message.settle_index.low || message.settle_index.high : message.settle_index !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.settle_index);
            if (message.settle_time != null && $Object.hasOwnProperty.call(message, "settle_time") && (typeof message.settle_time === "object" ? message.settle_time.low || message.settle_time.high : message.settle_time !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.settle_time);
            if (message.amt_paid_msat != null && $Object.hasOwnProperty.call(message, "amt_paid_msat") && (typeof message.amt_paid_msat === "object" ? message.amt_paid_msat.low || message.amt_paid_msat.high : message.amt_paid_msat !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.amt_paid_msat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AMPInvoiceState message, length delimited. Does not implicitly {@link lnrpc.AMPInvoiceState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.AMPInvoiceState
         * @static
         * @param {lnrpc.AMPInvoiceState.$Properties} message AMPInvoiceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AMPInvoiceState.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AMPInvoiceState message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.AMPInvoiceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.AMPInvoiceState & lnrpc.AMPInvoiceState.$Shape} AMPInvoiceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AMPInvoiceState.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.AMPInvoiceState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.settle_index = value;
                        else
                            delete message.settle_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.settle_time = value;
                        else
                            delete message.settle_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_paid_msat = value;
                        else
                            delete message.amt_paid_msat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a AMPInvoiceState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.AMPInvoiceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.AMPInvoiceState & lnrpc.AMPInvoiceState.$Shape} AMPInvoiceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AMPInvoiceState.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AMPInvoiceState message.
         * @function verify
         * @memberof lnrpc.AMPInvoiceState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AMPInvoiceState.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                    return "state: enum value expected";
            if (message.settle_index != null && $Object.hasOwnProperty.call(message, "settle_index"))
                if (!$util.isInteger(message.settle_index) && !(message.settle_index && $util.isInteger(message.settle_index.low) && $util.isInteger(message.settle_index.high)))
                    return "settle_index: integer|Long expected";
            if (message.settle_time != null && $Object.hasOwnProperty.call(message, "settle_time"))
                if (!$util.isInteger(message.settle_time) && !(message.settle_time && $util.isInteger(message.settle_time.low) && $util.isInteger(message.settle_time.high)))
                    return "settle_time: integer|Long expected";
            if (message.amt_paid_msat != null && $Object.hasOwnProperty.call(message, "amt_paid_msat"))
                if (!$util.isInteger(message.amt_paid_msat) && !(message.amt_paid_msat && $util.isInteger(message.amt_paid_msat.low) && $util.isInteger(message.amt_paid_msat.high)))
                    return "amt_paid_msat: integer|Long expected";
            return null;
        };

        /**
         * Creates a AMPInvoiceState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.AMPInvoiceState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.AMPInvoiceState} AMPInvoiceState
         */
        AMPInvoiceState.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.AMPInvoiceState)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.AMPInvoiceState: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.AMPInvoiceState();
            if (object.state !== 0 && (typeof object.state !== "string" || $root.lnrpc.InvoiceHTLCState[object.state] !== 0))
                switch (object.state) {
                case "ACCEPTED":
                case 0:
                    message.state = 0;
                    break;
                case "SETTLED":
                case 1:
                    message.state = 1;
                    break;
                case "CANCELED":
                case 2:
                    message.state = 2;
                    break;
                default:
                    if (typeof object.state === "number" && (object.state | 0) === object.state)
                        message.state = object.state;
                }
            if (object.settle_index != null)
                if (typeof object.settle_index === "object" ? object.settle_index.low || object.settle_index.high : $Number(object.settle_index) !== 0)
                    if ($util.Long)
                        message.settle_index = $util.Long.fromValue(object.settle_index, true);
                    else if (typeof object.settle_index === "string")
                        message.settle_index = $parseInt(object.settle_index, 10);
                    else if (typeof object.settle_index === "number")
                        message.settle_index = object.settle_index;
                    else if (typeof object.settle_index === "object")
                        message.settle_index = new $util.LongBits(object.settle_index.low >>> 0, object.settle_index.high >>> 0).toNumber(true);
            if (object.settle_time != null)
                if (typeof object.settle_time === "object" ? object.settle_time.low || object.settle_time.high : $Number(object.settle_time) !== 0)
                    if ($util.Long)
                        message.settle_time = $util.Long.fromValue(object.settle_time, false);
                    else if (typeof object.settle_time === "string")
                        message.settle_time = $parseInt(object.settle_time, 10);
                    else if (typeof object.settle_time === "number")
                        message.settle_time = object.settle_time;
                    else if (typeof object.settle_time === "object")
                        message.settle_time = new $util.LongBits(object.settle_time.low >>> 0, object.settle_time.high >>> 0).toNumber();
            if (object.amt_paid_msat != null)
                if (typeof object.amt_paid_msat === "object" ? object.amt_paid_msat.low || object.amt_paid_msat.high : $Number(object.amt_paid_msat) !== 0)
                    if ($util.Long)
                        message.amt_paid_msat = $util.Long.fromValue(object.amt_paid_msat, false);
                    else if (typeof object.amt_paid_msat === "string")
                        message.amt_paid_msat = $parseInt(object.amt_paid_msat, 10);
                    else if (typeof object.amt_paid_msat === "number")
                        message.amt_paid_msat = object.amt_paid_msat;
                    else if (typeof object.amt_paid_msat === "object")
                        message.amt_paid_msat = new $util.LongBits(object.amt_paid_msat.low >>> 0, object.amt_paid_msat.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a AMPInvoiceState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.AMPInvoiceState
         * @static
         * @param {lnrpc.AMPInvoiceState} message AMPInvoiceState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AMPInvoiceState.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.state = options.enums === $String ? "ACCEPTED" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.settle_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.settle_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.settle_time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.settle_time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_paid_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_paid_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                object.state = options.enums === $String ? $root.lnrpc.InvoiceHTLCState[message.state] === $undefined ? message.state : $root.lnrpc.InvoiceHTLCState[message.state] : message.state;
            if (message.settle_index != null && $Object.hasOwnProperty.call(message, "settle_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.settle_index = typeof message.settle_index === "number" ? $BigInt(message.settle_index) : $util.Long.fromBits(message.settle_index.low >>> 0, message.settle_index.high >>> 0, true).toBigInt();
                else if (typeof message.settle_index === "number")
                    object.settle_index = options.longs === $String ? $String(message.settle_index) : message.settle_index;
                else
                    object.settle_index = options.longs === $String ? $util.Long.prototype.toString.call(message.settle_index) : options.longs === $Number ? new $util.LongBits(message.settle_index.low >>> 0, message.settle_index.high >>> 0).toNumber(true) : message.settle_index;
            if (message.settle_time != null && $Object.hasOwnProperty.call(message, "settle_time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.settle_time = typeof message.settle_time === "number" ? $BigInt(message.settle_time) : $util.Long.fromBits(message.settle_time.low >>> 0, message.settle_time.high >>> 0, false).toBigInt();
                else if (typeof message.settle_time === "number")
                    object.settle_time = options.longs === $String ? $String(message.settle_time) : message.settle_time;
                else
                    object.settle_time = options.longs === $String ? $util.Long.prototype.toString.call(message.settle_time) : options.longs === $Number ? new $util.LongBits(message.settle_time.low >>> 0, message.settle_time.high >>> 0).toNumber() : message.settle_time;
            if (message.amt_paid_msat != null && $Object.hasOwnProperty.call(message, "amt_paid_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_paid_msat = typeof message.amt_paid_msat === "number" ? $BigInt(message.amt_paid_msat) : $util.Long.fromBits(message.amt_paid_msat.low >>> 0, message.amt_paid_msat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_paid_msat === "number")
                    object.amt_paid_msat = options.longs === $String ? $String(message.amt_paid_msat) : message.amt_paid_msat;
                else
                    object.amt_paid_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_paid_msat) : options.longs === $Number ? new $util.LongBits(message.amt_paid_msat.low >>> 0, message.amt_paid_msat.high >>> 0).toNumber() : message.amt_paid_msat;
            return object;
        };

        /**
         * Converts this AMPInvoiceState to JSON.
         * @function toJSON
         * @memberof lnrpc.AMPInvoiceState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AMPInvoiceState.prototype.toJSON = function() {
            return AMPInvoiceState.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AMPInvoiceState
         * @function getTypeUrl
         * @memberof lnrpc.AMPInvoiceState
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AMPInvoiceState.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.AMPInvoiceState";
        };

        return AMPInvoiceState;
    })();

    lnrpc.Invoice = (function() {

        /**
         * Properties of an Invoice.
         * @typedef {Object} lnrpc.Invoice.$Properties
         * @property {string|null} [memo] Invoice memo
         * @property {Uint8Array|null} [r_preimage] Invoice r_preimage
         * @property {Uint8Array|null} [r_hash] Invoice r_hash
         * @property {Long|null} [value] Invoice value
         * @property {Long|null} [value_msat] Invoice value_msat
         * @property {boolean|null} [settled] Invoice settled
         * @property {Long|null} [creation_date] Invoice creation_date
         * @property {Long|null} [settle_date] Invoice settle_date
         * @property {string|null} [payment_request] Invoice payment_request
         * @property {Uint8Array|null} [description_hash] Invoice description_hash
         * @property {Long|null} [expiry] Invoice expiry
         * @property {string|null} [fallback_addr] Invoice fallback_addr
         * @property {Long|null} [cltv_expiry] Invoice cltv_expiry
         * @property {Array.<lnrpc.RouteHint.$Properties>|null} [route_hints] Invoice route_hints
         * @property {boolean|null} ["private"] Invoice private
         * @property {Long|null} [add_index] Invoice add_index
         * @property {Long|null} [settle_index] Invoice settle_index
         * @property {Long|null} [amt_paid] Invoice amt_paid
         * @property {Long|null} [amt_paid_sat] Invoice amt_paid_sat
         * @property {Long|null} [amt_paid_msat] Invoice amt_paid_msat
         * @property {lnrpc.Invoice.InvoiceState|null} [state] Invoice state
         * @property {Array.<lnrpc.InvoiceHTLC.$Properties>|null} [htlcs] Invoice htlcs
         * @property {Object.<string,lnrpc.Feature.$Properties>|null} [features] Invoice features
         * @property {boolean|null} [is_keysend] Invoice is_keysend
         * @property {Uint8Array|null} [payment_addr] Invoice payment_addr
         * @property {boolean|null} [is_amp] Invoice is_amp
         * @property {Object.<string,lnrpc.AMPInvoiceState.$Properties>|null} [amp_invoice_state] Invoice amp_invoice_state
         * @property {boolean|null} [is_blinded] Invoice is_blinded
         * @property {lnrpc.BlindedPathConfig.$Properties|null} [blinded_path_config] Invoice blinded_path_config
         * @property {number|null} [min_hop_hints] Invoice min_hop_hints
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an Invoice.
         * @memberof lnrpc
         * @interface IInvoice
         * @augments lnrpc.Invoice.$Properties
         * @deprecated Use lnrpc.Invoice.$Properties instead.
         */

        /**
         * Shape of an Invoice.
         * @typedef {lnrpc.Invoice.$Properties} lnrpc.Invoice.$Shape
         */

        /**
         * Constructs a new Invoice.
         * @memberof lnrpc
         * @classdesc Represents an Invoice.
         * @constructor
         * @param {lnrpc.Invoice.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Invoice = function (properties) {
            this.route_hints = [];
            this.htlcs = [];
            this.features = {};
            this.amp_invoice_state = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Invoice memo.
         * @member {string} memo
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.memo = "";

        /**
         * Invoice r_preimage.
         * @member {Uint8Array} r_preimage
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.r_preimage = $util.newBuffer([]);

        /**
         * Invoice r_hash.
         * @member {Uint8Array} r_hash
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.r_hash = $util.newBuffer([]);

        /**
         * Invoice value.
         * @member {Long} value
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Invoice value_msat.
         * @member {Long} value_msat
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.value_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Invoice settled.
         * @member {boolean} settled
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.settled = false;

        /**
         * Invoice creation_date.
         * @member {Long} creation_date
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.creation_date = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Invoice settle_date.
         * @member {Long} settle_date
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.settle_date = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Invoice payment_request.
         * @member {string} payment_request
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.payment_request = "";

        /**
         * Invoice description_hash.
         * @member {Uint8Array} description_hash
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.description_hash = $util.newBuffer([]);

        /**
         * Invoice expiry.
         * @member {Long} expiry
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.expiry = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Invoice fallback_addr.
         * @member {string} fallback_addr
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.fallback_addr = "";

        /**
         * Invoice cltv_expiry.
         * @member {Long} cltv_expiry
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.cltv_expiry = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Invoice route_hints.
         * @member {Array.<lnrpc.RouteHint.$Properties>} route_hints
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.route_hints = $util.emptyArray;

        /**
         * Invoice private.
         * @member {boolean} private
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype["private"] = false;

        /**
         * Invoice add_index.
         * @member {Long} add_index
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.add_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Invoice settle_index.
         * @member {Long} settle_index
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.settle_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Invoice amt_paid.
         * @member {Long} amt_paid
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.amt_paid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Invoice amt_paid_sat.
         * @member {Long} amt_paid_sat
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.amt_paid_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Invoice amt_paid_msat.
         * @member {Long} amt_paid_msat
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.amt_paid_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Invoice state.
         * @member {lnrpc.Invoice.InvoiceState} state
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.state = 0;

        /**
         * Invoice htlcs.
         * @member {Array.<lnrpc.InvoiceHTLC.$Properties>} htlcs
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.htlcs = $util.emptyArray;

        /**
         * Invoice features.
         * @member {Object.<string,lnrpc.Feature.$Properties>} features
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.features = $util.emptyObject;

        /**
         * Invoice is_keysend.
         * @member {boolean} is_keysend
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.is_keysend = false;

        /**
         * Invoice payment_addr.
         * @member {Uint8Array} payment_addr
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.payment_addr = $util.newBuffer([]);

        /**
         * Invoice is_amp.
         * @member {boolean} is_amp
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.is_amp = false;

        /**
         * Invoice amp_invoice_state.
         * @member {Object.<string,lnrpc.AMPInvoiceState.$Properties>} amp_invoice_state
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.amp_invoice_state = $util.emptyObject;

        /**
         * Invoice is_blinded.
         * @member {boolean} is_blinded
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.is_blinded = false;

        /**
         * Invoice blinded_path_config.
         * @member {lnrpc.BlindedPathConfig.$Properties|null|undefined} blinded_path_config
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.blinded_path_config = null;

        /**
         * Invoice min_hop_hints.
         * @member {number} min_hop_hints
         * @memberof lnrpc.Invoice
         * @instance
         */
        Invoice.prototype.min_hop_hints = 0;

        /**
         * Creates a new Invoice instance using the specified properties.
         * @function create
         * @memberof lnrpc.Invoice
         * @static
         * @param {lnrpc.Invoice.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Invoice} Invoice instance
         * @type {{
         *   (properties: lnrpc.Invoice.$Shape): lnrpc.Invoice & lnrpc.Invoice.$Shape;
         *   (properties?: lnrpc.Invoice.$Properties): lnrpc.Invoice;
         * }}
         */
        Invoice.create = function(properties) {
            return new Invoice(properties);
        };

        /**
         * Encodes the specified Invoice message. Does not implicitly {@link lnrpc.Invoice.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Invoice
         * @static
         * @param {lnrpc.Invoice.$Properties} message Invoice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Invoice.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.memo);
            if (message.r_preimage != null && $Object.hasOwnProperty.call(message, "r_preimage") && message.r_preimage.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.r_preimage);
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash") && message.r_hash.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.r_hash);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.value);
            if (message.settled != null && $Object.hasOwnProperty.call(message, "settled") && message.settled !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.settled);
            if (message.creation_date != null && $Object.hasOwnProperty.call(message, "creation_date") && (typeof message.creation_date === "object" ? message.creation_date.low || message.creation_date.high : message.creation_date !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.creation_date);
            if (message.settle_date != null && $Object.hasOwnProperty.call(message, "settle_date") && (typeof message.settle_date === "object" ? message.settle_date.low || message.settle_date.high : message.settle_date !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.settle_date);
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request") && message.payment_request !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.payment_request);
            if (message.description_hash != null && $Object.hasOwnProperty.call(message, "description_hash") && message.description_hash.length)
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.description_hash);
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry") && (typeof message.expiry === "object" ? message.expiry.low || message.expiry.high : message.expiry !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.expiry);
            if (message.fallback_addr != null && $Object.hasOwnProperty.call(message, "fallback_addr") && message.fallback_addr !== "")
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.fallback_addr);
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry") && (typeof message.cltv_expiry === "object" ? message.cltv_expiry.low || message.cltv_expiry.high : message.cltv_expiry !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).uint64(message.cltv_expiry);
            if (message.route_hints != null && message.route_hints.length)
                for (let i = 0; i < message.route_hints.length; ++i)
                    $root.lnrpc.RouteHint.encode(message.route_hints[i], writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private") && message["private"] !== false)
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message["private"]);
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index") && (typeof message.add_index === "object" ? message.add_index.low || message.add_index.high : message.add_index !== 0))
                writer.uint32(/* id 16, wireType 0 =*/128).uint64(message.add_index);
            if (message.settle_index != null && $Object.hasOwnProperty.call(message, "settle_index") && (typeof message.settle_index === "object" ? message.settle_index.low || message.settle_index.high : message.settle_index !== 0))
                writer.uint32(/* id 17, wireType 0 =*/136).uint64(message.settle_index);
            if (message.amt_paid != null && $Object.hasOwnProperty.call(message, "amt_paid") && (typeof message.amt_paid === "object" ? message.amt_paid.low || message.amt_paid.high : message.amt_paid !== 0))
                writer.uint32(/* id 18, wireType 0 =*/144).int64(message.amt_paid);
            if (message.amt_paid_sat != null && $Object.hasOwnProperty.call(message, "amt_paid_sat") && (typeof message.amt_paid_sat === "object" ? message.amt_paid_sat.low || message.amt_paid_sat.high : message.amt_paid_sat !== 0))
                writer.uint32(/* id 19, wireType 0 =*/152).int64(message.amt_paid_sat);
            if (message.amt_paid_msat != null && $Object.hasOwnProperty.call(message, "amt_paid_msat") && (typeof message.amt_paid_msat === "object" ? message.amt_paid_msat.low || message.amt_paid_msat.high : message.amt_paid_msat !== 0))
                writer.uint32(/* id 20, wireType 0 =*/160).int64(message.amt_paid_msat);
            if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                writer.uint32(/* id 21, wireType 0 =*/168).int32(message.state);
            if (message.htlcs != null && message.htlcs.length)
                for (let i = 0; i < message.htlcs.length; ++i)
                    $root.lnrpc.InvoiceHTLC.encode(message.htlcs[i], writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
            if (message.value_msat != null && $Object.hasOwnProperty.call(message, "value_msat") && (typeof message.value_msat === "object" ? message.value_msat.low || message.value_msat.high : message.value_msat !== 0))
                writer.uint32(/* id 23, wireType 0 =*/184).int64(message.value_msat);
            if (message.features != null && $Object.hasOwnProperty.call(message, "features"))
                for (let keys = $Object.keys(message.features), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 24, wireType 2 =*/194).fork().uint32(/* id 1, wireType 0 =*/8).uint32(keys[i]);
                    $root.lnrpc.Feature.encode(message.features[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.is_keysend != null && $Object.hasOwnProperty.call(message, "is_keysend") && message.is_keysend !== false)
                writer.uint32(/* id 25, wireType 0 =*/200).bool(message.is_keysend);
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr") && message.payment_addr.length)
                writer.uint32(/* id 26, wireType 2 =*/210).bytes(message.payment_addr);
            if (message.is_amp != null && $Object.hasOwnProperty.call(message, "is_amp") && message.is_amp !== false)
                writer.uint32(/* id 27, wireType 0 =*/216).bool(message.is_amp);
            if (message.amp_invoice_state != null && $Object.hasOwnProperty.call(message, "amp_invoice_state"))
                for (let keys = $Object.keys(message.amp_invoice_state), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 28, wireType 2 =*/226).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.lnrpc.AMPInvoiceState.encode(message.amp_invoice_state[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.is_blinded != null && $Object.hasOwnProperty.call(message, "is_blinded") && message.is_blinded !== false)
                writer.uint32(/* id 29, wireType 0 =*/232).bool(message.is_blinded);
            if (message.blinded_path_config != null && $Object.hasOwnProperty.call(message, "blinded_path_config"))
                $root.lnrpc.BlindedPathConfig.encode(message.blinded_path_config, writer.uint32(/* id 30, wireType 2 =*/242).fork(), _depth + 1).ldelim();
            if (message.min_hop_hints != null && $Object.hasOwnProperty.call(message, "min_hop_hints") && message.min_hop_hints !== 0)
                writer.uint32(/* id 31, wireType 0 =*/248).int32(message.min_hop_hints);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Invoice message, length delimited. Does not implicitly {@link lnrpc.Invoice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Invoice
         * @static
         * @param {lnrpc.Invoice.$Properties} message Invoice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Invoice.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Invoice message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Invoice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Invoice & lnrpc.Invoice.$Shape} Invoice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Invoice.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Invoice(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.r_preimage = value;
                        else
                            delete message.r_preimage;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.r_hash = value;
                        else
                            delete message.r_hash;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value_msat = value;
                        else
                            delete message.value_msat;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.settled = value;
                        else
                            delete message.settled;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.creation_date = value;
                        else
                            delete message.creation_date;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.settle_date = value;
                        else
                            delete message.settle_date;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_request = value;
                        else
                            delete message.payment_request;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.description_hash = value;
                        else
                            delete message.description_hash;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiry = value;
                        else
                            delete message.expiry;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.fallback_addr = value;
                        else
                            delete message.fallback_addr;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.cltv_expiry = value;
                        else
                            delete message.cltv_expiry;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.route_hints && message.route_hints.length))
                            message.route_hints = [];
                        message.route_hints.push($root.lnrpc.RouteHint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message["private"] = value;
                        else
                            delete message["private"];
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.add_index = value;
                        else
                            delete message.add_index;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.settle_index = value;
                        else
                            delete message.settle_index;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_paid = value;
                        else
                            delete message.amt_paid;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_paid_sat = value;
                        else
                            delete message.amt_paid_sat;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_paid_msat = value;
                        else
                            delete message.amt_paid_msat;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        if (!(message.htlcs && message.htlcs.length))
                            message.htlcs = [];
                        message.htlcs.push($root.lnrpc.InvoiceHTLC.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        if (message.features === $util.emptyObject)
                            message.features = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint32();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.Feature.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.features[key] = value || new $root.lnrpc.Feature();
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_keysend = value;
                        else
                            delete message.is_keysend;
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_addr = value;
                        else
                            delete message.payment_addr;
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_amp = value;
                        else
                            delete message.is_amp;
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        if (message.amp_invoice_state === $util.emptyObject)
                            message.amp_invoice_state = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.AMPInvoiceState.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.amp_invoice_state, key);
                        message.amp_invoice_state[key] = value || new $root.lnrpc.AMPInvoiceState();
                        continue;
                    }
                case 29: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_blinded = value;
                        else
                            delete message.is_blinded;
                        continue;
                    }
                case 30: {
                        if (wireType !== 2)
                            break;
                        message.blinded_path_config = $root.lnrpc.BlindedPathConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.blinded_path_config);
                        continue;
                    }
                case 31: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_hop_hints = value;
                        else
                            delete message.min_hop_hints;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Invoice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Invoice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Invoice & lnrpc.Invoice.$Shape} Invoice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Invoice.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Invoice message.
         * @function verify
         * @memberof lnrpc.Invoice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Invoice.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.r_preimage != null && $Object.hasOwnProperty.call(message, "r_preimage"))
                if (!(message.r_preimage && typeof message.r_preimage.length === "number" || $util.isString(message.r_preimage)))
                    return "r_preimage: buffer expected";
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash"))
                if (!(message.r_hash && typeof message.r_hash.length === "number" || $util.isString(message.r_hash)))
                    return "r_hash: buffer expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                    return "value: integer|Long expected";
            if (message.value_msat != null && $Object.hasOwnProperty.call(message, "value_msat"))
                if (!$util.isInteger(message.value_msat) && !(message.value_msat && $util.isInteger(message.value_msat.low) && $util.isInteger(message.value_msat.high)))
                    return "value_msat: integer|Long expected";
            if (message.settled != null && $Object.hasOwnProperty.call(message, "settled"))
                if (typeof message.settled !== "boolean")
                    return "settled: boolean expected";
            if (message.creation_date != null && $Object.hasOwnProperty.call(message, "creation_date"))
                if (!$util.isInteger(message.creation_date) && !(message.creation_date && $util.isInteger(message.creation_date.low) && $util.isInteger(message.creation_date.high)))
                    return "creation_date: integer|Long expected";
            if (message.settle_date != null && $Object.hasOwnProperty.call(message, "settle_date"))
                if (!$util.isInteger(message.settle_date) && !(message.settle_date && $util.isInteger(message.settle_date.low) && $util.isInteger(message.settle_date.high)))
                    return "settle_date: integer|Long expected";
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                if (!$util.isString(message.payment_request))
                    return "payment_request: string expected";
            if (message.description_hash != null && $Object.hasOwnProperty.call(message, "description_hash"))
                if (!(message.description_hash && typeof message.description_hash.length === "number" || $util.isString(message.description_hash)))
                    return "description_hash: buffer expected";
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                if (!$util.isInteger(message.expiry) && !(message.expiry && $util.isInteger(message.expiry.low) && $util.isInteger(message.expiry.high)))
                    return "expiry: integer|Long expected";
            if (message.fallback_addr != null && $Object.hasOwnProperty.call(message, "fallback_addr"))
                if (!$util.isString(message.fallback_addr))
                    return "fallback_addr: string expected";
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry"))
                if (!$util.isInteger(message.cltv_expiry) && !(message.cltv_expiry && $util.isInteger(message.cltv_expiry.low) && $util.isInteger(message.cltv_expiry.high)))
                    return "cltv_expiry: integer|Long expected";
            if (message.route_hints != null && $Object.hasOwnProperty.call(message, "route_hints")) {
                if (!$Array.isArray(message.route_hints))
                    return "route_hints: array expected";
                for (let i = 0; i < message.route_hints.length; ++i) {
                    let error = $root.lnrpc.RouteHint.verify(message.route_hints[i], _depth + 1);
                    if (error)
                        return "route_hints." + error;
                }
            }
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                if (typeof message["private"] !== "boolean")
                    return "private: boolean expected";
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index"))
                if (!$util.isInteger(message.add_index) && !(message.add_index && $util.isInteger(message.add_index.low) && $util.isInteger(message.add_index.high)))
                    return "add_index: integer|Long expected";
            if (message.settle_index != null && $Object.hasOwnProperty.call(message, "settle_index"))
                if (!$util.isInteger(message.settle_index) && !(message.settle_index && $util.isInteger(message.settle_index.low) && $util.isInteger(message.settle_index.high)))
                    return "settle_index: integer|Long expected";
            if (message.amt_paid != null && $Object.hasOwnProperty.call(message, "amt_paid"))
                if (!$util.isInteger(message.amt_paid) && !(message.amt_paid && $util.isInteger(message.amt_paid.low) && $util.isInteger(message.amt_paid.high)))
                    return "amt_paid: integer|Long expected";
            if (message.amt_paid_sat != null && $Object.hasOwnProperty.call(message, "amt_paid_sat"))
                if (!$util.isInteger(message.amt_paid_sat) && !(message.amt_paid_sat && $util.isInteger(message.amt_paid_sat.low) && $util.isInteger(message.amt_paid_sat.high)))
                    return "amt_paid_sat: integer|Long expected";
            if (message.amt_paid_msat != null && $Object.hasOwnProperty.call(message, "amt_paid_msat"))
                if (!$util.isInteger(message.amt_paid_msat) && !(message.amt_paid_msat && $util.isInteger(message.amt_paid_msat.low) && $util.isInteger(message.amt_paid_msat.high)))
                    return "amt_paid_msat: integer|Long expected";
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                    return "state: enum value expected";
            if (message.htlcs != null && $Object.hasOwnProperty.call(message, "htlcs")) {
                if (!$Array.isArray(message.htlcs))
                    return "htlcs: array expected";
                for (let i = 0; i < message.htlcs.length; ++i) {
                    let error = $root.lnrpc.InvoiceHTLC.verify(message.htlcs[i], _depth + 1);
                    if (error)
                        return "htlcs." + error;
                }
            }
            if (message.features != null && $Object.hasOwnProperty.call(message, "features")) {
                if (!$util.isObject(message.features))
                    return "features: object expected";
                let key = $Object.keys(message.features);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "features: integer key{k:uint32} expected";
                    {
                        let error = $root.lnrpc.Feature.verify(message.features[key[i]], _depth + 1);
                        if (error)
                            return "features." + error;
                    }
                }
            }
            if (message.is_keysend != null && $Object.hasOwnProperty.call(message, "is_keysend"))
                if (typeof message.is_keysend !== "boolean")
                    return "is_keysend: boolean expected";
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                if (!(message.payment_addr && typeof message.payment_addr.length === "number" || $util.isString(message.payment_addr)))
                    return "payment_addr: buffer expected";
            if (message.is_amp != null && $Object.hasOwnProperty.call(message, "is_amp"))
                if (typeof message.is_amp !== "boolean")
                    return "is_amp: boolean expected";
            if (message.amp_invoice_state != null && $Object.hasOwnProperty.call(message, "amp_invoice_state")) {
                if (!$util.isObject(message.amp_invoice_state))
                    return "amp_invoice_state: object expected";
                let key = $Object.keys(message.amp_invoice_state);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.lnrpc.AMPInvoiceState.verify(message.amp_invoice_state[key[i]], _depth + 1);
                    if (error)
                        return "amp_invoice_state." + error;
                }
            }
            if (message.is_blinded != null && $Object.hasOwnProperty.call(message, "is_blinded"))
                if (typeof message.is_blinded !== "boolean")
                    return "is_blinded: boolean expected";
            if (message.blinded_path_config != null && $Object.hasOwnProperty.call(message, "blinded_path_config")) {
                let error = $root.lnrpc.BlindedPathConfig.verify(message.blinded_path_config, _depth + 1);
                if (error)
                    return "blinded_path_config." + error;
            }
            if (message.min_hop_hints != null && $Object.hasOwnProperty.call(message, "min_hop_hints"))
                if (!$util.isInteger(message.min_hop_hints))
                    return "min_hop_hints: integer expected";
            return null;
        };

        /**
         * Creates an Invoice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Invoice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Invoice} Invoice
         */
        Invoice.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Invoice)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Invoice: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Invoice();
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.r_preimage != null)
                if (object.r_preimage.length)
                    if (typeof object.r_preimage === "string")
                        $util.base64.decode(object.r_preimage, message.r_preimage = $util.newBuffer($util.base64.length(object.r_preimage)), 0);
                    else if (object.r_preimage.length >= 0)
                        message.r_preimage = object.r_preimage;
            if (object.r_hash != null)
                if (object.r_hash.length)
                    if (typeof object.r_hash === "string")
                        $util.base64.decode(object.r_hash, message.r_hash = $util.newBuffer($util.base64.length(object.r_hash)), 0);
                    else if (object.r_hash.length >= 0)
                        message.r_hash = object.r_hash;
            if (object.value != null)
                if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                    if ($util.Long)
                        message.value = $util.Long.fromValue(object.value, false);
                    else if (typeof object.value === "string")
                        message.value = $parseInt(object.value, 10);
                    else if (typeof object.value === "number")
                        message.value = object.value;
                    else if (typeof object.value === "object")
                        message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber();
            if (object.value_msat != null)
                if (typeof object.value_msat === "object" ? object.value_msat.low || object.value_msat.high : $Number(object.value_msat) !== 0)
                    if ($util.Long)
                        message.value_msat = $util.Long.fromValue(object.value_msat, false);
                    else if (typeof object.value_msat === "string")
                        message.value_msat = $parseInt(object.value_msat, 10);
                    else if (typeof object.value_msat === "number")
                        message.value_msat = object.value_msat;
                    else if (typeof object.value_msat === "object")
                        message.value_msat = new $util.LongBits(object.value_msat.low >>> 0, object.value_msat.high >>> 0).toNumber();
            if (object.settled != null)
                if (object.settled)
                    message.settled = $Boolean(object.settled);
            if (object.creation_date != null)
                if (typeof object.creation_date === "object" ? object.creation_date.low || object.creation_date.high : $Number(object.creation_date) !== 0)
                    if ($util.Long)
                        message.creation_date = $util.Long.fromValue(object.creation_date, false);
                    else if (typeof object.creation_date === "string")
                        message.creation_date = $parseInt(object.creation_date, 10);
                    else if (typeof object.creation_date === "number")
                        message.creation_date = object.creation_date;
                    else if (typeof object.creation_date === "object")
                        message.creation_date = new $util.LongBits(object.creation_date.low >>> 0, object.creation_date.high >>> 0).toNumber();
            if (object.settle_date != null)
                if (typeof object.settle_date === "object" ? object.settle_date.low || object.settle_date.high : $Number(object.settle_date) !== 0)
                    if ($util.Long)
                        message.settle_date = $util.Long.fromValue(object.settle_date, false);
                    else if (typeof object.settle_date === "string")
                        message.settle_date = $parseInt(object.settle_date, 10);
                    else if (typeof object.settle_date === "number")
                        message.settle_date = object.settle_date;
                    else if (typeof object.settle_date === "object")
                        message.settle_date = new $util.LongBits(object.settle_date.low >>> 0, object.settle_date.high >>> 0).toNumber();
            if (object.payment_request != null)
                if (typeof object.payment_request !== "string" || object.payment_request.length)
                    message.payment_request = $String(object.payment_request);
            if (object.description_hash != null)
                if (object.description_hash.length)
                    if (typeof object.description_hash === "string")
                        $util.base64.decode(object.description_hash, message.description_hash = $util.newBuffer($util.base64.length(object.description_hash)), 0);
                    else if (object.description_hash.length >= 0)
                        message.description_hash = object.description_hash;
            if (object.expiry != null)
                if (typeof object.expiry === "object" ? object.expiry.low || object.expiry.high : $Number(object.expiry) !== 0)
                    if ($util.Long)
                        message.expiry = $util.Long.fromValue(object.expiry, false);
                    else if (typeof object.expiry === "string")
                        message.expiry = $parseInt(object.expiry, 10);
                    else if (typeof object.expiry === "number")
                        message.expiry = object.expiry;
                    else if (typeof object.expiry === "object")
                        message.expiry = new $util.LongBits(object.expiry.low >>> 0, object.expiry.high >>> 0).toNumber();
            if (object.fallback_addr != null)
                if (typeof object.fallback_addr !== "string" || object.fallback_addr.length)
                    message.fallback_addr = $String(object.fallback_addr);
            if (object.cltv_expiry != null)
                if (typeof object.cltv_expiry === "object" ? object.cltv_expiry.low || object.cltv_expiry.high : $Number(object.cltv_expiry) !== 0)
                    if ($util.Long)
                        message.cltv_expiry = $util.Long.fromValue(object.cltv_expiry, true);
                    else if (typeof object.cltv_expiry === "string")
                        message.cltv_expiry = $parseInt(object.cltv_expiry, 10);
                    else if (typeof object.cltv_expiry === "number")
                        message.cltv_expiry = object.cltv_expiry;
                    else if (typeof object.cltv_expiry === "object")
                        message.cltv_expiry = new $util.LongBits(object.cltv_expiry.low >>> 0, object.cltv_expiry.high >>> 0).toNumber(true);
            if (object.route_hints) {
                if (!$Array.isArray(object.route_hints))
                    throw $TypeError(".lnrpc.Invoice.route_hints: array expected");
                message.route_hints = $Array(object.route_hints.length);
                for (let i = 0; i < object.route_hints.length; ++i) {
                    if (!$util.isObject(object.route_hints[i]))
                        throw $TypeError(".lnrpc.Invoice.route_hints: object expected");
                    message.route_hints[i] = $root.lnrpc.RouteHint.fromObject(object.route_hints[i], _depth + 1);
                }
            }
            if (object["private"] != null)
                if (object["private"])
                    message["private"] = $Boolean(object["private"]);
            if (object.add_index != null)
                if (typeof object.add_index === "object" ? object.add_index.low || object.add_index.high : $Number(object.add_index) !== 0)
                    if ($util.Long)
                        message.add_index = $util.Long.fromValue(object.add_index, true);
                    else if (typeof object.add_index === "string")
                        message.add_index = $parseInt(object.add_index, 10);
                    else if (typeof object.add_index === "number")
                        message.add_index = object.add_index;
                    else if (typeof object.add_index === "object")
                        message.add_index = new $util.LongBits(object.add_index.low >>> 0, object.add_index.high >>> 0).toNumber(true);
            if (object.settle_index != null)
                if (typeof object.settle_index === "object" ? object.settle_index.low || object.settle_index.high : $Number(object.settle_index) !== 0)
                    if ($util.Long)
                        message.settle_index = $util.Long.fromValue(object.settle_index, true);
                    else if (typeof object.settle_index === "string")
                        message.settle_index = $parseInt(object.settle_index, 10);
                    else if (typeof object.settle_index === "number")
                        message.settle_index = object.settle_index;
                    else if (typeof object.settle_index === "object")
                        message.settle_index = new $util.LongBits(object.settle_index.low >>> 0, object.settle_index.high >>> 0).toNumber(true);
            if (object.amt_paid != null)
                if (typeof object.amt_paid === "object" ? object.amt_paid.low || object.amt_paid.high : $Number(object.amt_paid) !== 0)
                    if ($util.Long)
                        message.amt_paid = $util.Long.fromValue(object.amt_paid, false);
                    else if (typeof object.amt_paid === "string")
                        message.amt_paid = $parseInt(object.amt_paid, 10);
                    else if (typeof object.amt_paid === "number")
                        message.amt_paid = object.amt_paid;
                    else if (typeof object.amt_paid === "object")
                        message.amt_paid = new $util.LongBits(object.amt_paid.low >>> 0, object.amt_paid.high >>> 0).toNumber();
            if (object.amt_paid_sat != null)
                if (typeof object.amt_paid_sat === "object" ? object.amt_paid_sat.low || object.amt_paid_sat.high : $Number(object.amt_paid_sat) !== 0)
                    if ($util.Long)
                        message.amt_paid_sat = $util.Long.fromValue(object.amt_paid_sat, false);
                    else if (typeof object.amt_paid_sat === "string")
                        message.amt_paid_sat = $parseInt(object.amt_paid_sat, 10);
                    else if (typeof object.amt_paid_sat === "number")
                        message.amt_paid_sat = object.amt_paid_sat;
                    else if (typeof object.amt_paid_sat === "object")
                        message.amt_paid_sat = new $util.LongBits(object.amt_paid_sat.low >>> 0, object.amt_paid_sat.high >>> 0).toNumber();
            if (object.amt_paid_msat != null)
                if (typeof object.amt_paid_msat === "object" ? object.amt_paid_msat.low || object.amt_paid_msat.high : $Number(object.amt_paid_msat) !== 0)
                    if ($util.Long)
                        message.amt_paid_msat = $util.Long.fromValue(object.amt_paid_msat, false);
                    else if (typeof object.amt_paid_msat === "string")
                        message.amt_paid_msat = $parseInt(object.amt_paid_msat, 10);
                    else if (typeof object.amt_paid_msat === "number")
                        message.amt_paid_msat = object.amt_paid_msat;
                    else if (typeof object.amt_paid_msat === "object")
                        message.amt_paid_msat = new $util.LongBits(object.amt_paid_msat.low >>> 0, object.amt_paid_msat.high >>> 0).toNumber();
            if (object.state !== 0 && (typeof object.state !== "string" || $root.lnrpc.Invoice.InvoiceState[object.state] !== 0))
                switch (object.state) {
                case "OPEN":
                case 0:
                    message.state = 0;
                    break;
                case "SETTLED":
                case 1:
                    message.state = 1;
                    break;
                case "CANCELED":
                case 2:
                    message.state = 2;
                    break;
                case "ACCEPTED":
                case 3:
                    message.state = 3;
                    break;
                default:
                    if (typeof object.state === "number" && (object.state | 0) === object.state)
                        message.state = object.state;
                }
            if (object.htlcs) {
                if (!$Array.isArray(object.htlcs))
                    throw $TypeError(".lnrpc.Invoice.htlcs: array expected");
                message.htlcs = $Array(object.htlcs.length);
                for (let i = 0; i < object.htlcs.length; ++i) {
                    if (!$util.isObject(object.htlcs[i]))
                        throw $TypeError(".lnrpc.Invoice.htlcs: object expected");
                    message.htlcs[i] = $root.lnrpc.InvoiceHTLC.fromObject(object.htlcs[i], _depth + 1);
                }
            }
            if (object.features) {
                if (!$util.isObject(object.features))
                    throw $TypeError(".lnrpc.Invoice.features: object expected");
                message.features = {};
                for (let keys = $Object.keys(object.features), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.features, keys[i]);
                    if (!$util.isObject(object.features[keys[i]]))
                        throw $TypeError(".lnrpc.Invoice.features: object expected");
                    message.features[keys[i]] = $root.lnrpc.Feature.fromObject(object.features[keys[i]], _depth + 1);
                }
            }
            if (object.is_keysend != null)
                if (object.is_keysend)
                    message.is_keysend = $Boolean(object.is_keysend);
            if (object.payment_addr != null)
                if (object.payment_addr.length)
                    if (typeof object.payment_addr === "string")
                        $util.base64.decode(object.payment_addr, message.payment_addr = $util.newBuffer($util.base64.length(object.payment_addr)), 0);
                    else if (object.payment_addr.length >= 0)
                        message.payment_addr = object.payment_addr;
            if (object.is_amp != null)
                if (object.is_amp)
                    message.is_amp = $Boolean(object.is_amp);
            if (object.amp_invoice_state) {
                if (!$util.isObject(object.amp_invoice_state))
                    throw $TypeError(".lnrpc.Invoice.amp_invoice_state: object expected");
                message.amp_invoice_state = {};
                for (let keys = $Object.keys(object.amp_invoice_state), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.amp_invoice_state, keys[i]);
                    if (!$util.isObject(object.amp_invoice_state[keys[i]]))
                        throw $TypeError(".lnrpc.Invoice.amp_invoice_state: object expected");
                    message.amp_invoice_state[keys[i]] = $root.lnrpc.AMPInvoiceState.fromObject(object.amp_invoice_state[keys[i]], _depth + 1);
                }
            }
            if (object.is_blinded != null)
                if (object.is_blinded)
                    message.is_blinded = $Boolean(object.is_blinded);
            if (object.blinded_path_config != null) {
                if (!$util.isObject(object.blinded_path_config))
                    throw $TypeError(".lnrpc.Invoice.blinded_path_config: object expected");
                message.blinded_path_config = $root.lnrpc.BlindedPathConfig.fromObject(object.blinded_path_config, _depth + 1);
            }
            if (object.min_hop_hints != null)
                if ($Number(object.min_hop_hints) !== 0)
                    message.min_hop_hints = object.min_hop_hints | 0;
            return message;
        };

        /**
         * Creates a plain object from an Invoice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Invoice
         * @static
         * @param {lnrpc.Invoice} message Invoice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Invoice.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.route_hints = [];
                object.htlcs = [];
            }
            if (options.objects || options.defaults) {
                object.features = {};
                object.amp_invoice_state = {};
            }
            if (options.defaults) {
                object.memo = "";
                if (options.bytes === $String)
                    object.r_preimage = "";
                else {
                    object.r_preimage = [];
                    if (options.bytes !== $Array)
                        object.r_preimage = $util.newBuffer(object.r_preimage);
                }
                if (options.bytes === $String)
                    object.r_hash = "";
                else {
                    object.r_hash = [];
                    if (options.bytes !== $Array)
                        object.r_hash = $util.newBuffer(object.r_hash);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.settled = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation_date = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.creation_date = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.settle_date = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.settle_date = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.payment_request = "";
                if (options.bytes === $String)
                    object.description_hash = "";
                else {
                    object.description_hash = [];
                    if (options.bytes !== $Array)
                        object.description_hash = $util.newBuffer(object.description_hash);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiry = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiry = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.fallback_addr = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.cltv_expiry = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.cltv_expiry = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object["private"] = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.add_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.add_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.settle_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.settle_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_paid = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_paid = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_paid_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_paid_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_paid_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_paid_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.state = options.enums === $String ? "OPEN" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.is_keysend = false;
                if (options.bytes === $String)
                    object.payment_addr = "";
                else {
                    object.payment_addr = [];
                    if (options.bytes !== $Array)
                        object.payment_addr = $util.newBuffer(object.payment_addr);
                }
                object.is_amp = false;
                object.is_blinded = false;
                object.blinded_path_config = null;
                object.min_hop_hints = 0;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.r_preimage != null && $Object.hasOwnProperty.call(message, "r_preimage"))
                object.r_preimage = options.bytes === $String ? $util.base64.encode(message.r_preimage, 0, message.r_preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.r_preimage) : message.r_preimage;
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash"))
                object.r_hash = options.bytes === $String ? $util.base64.encode(message.r_hash, 0, message.r_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.r_hash) : message.r_hash;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, false).toBigInt();
                else if (typeof message.value === "number")
                    object.value = options.longs === $String ? $String(message.value) : message.value;
                else
                    object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber() : message.value;
            if (message.settled != null && $Object.hasOwnProperty.call(message, "settled"))
                object.settled = message.settled;
            if (message.creation_date != null && $Object.hasOwnProperty.call(message, "creation_date"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.creation_date = typeof message.creation_date === "number" ? $BigInt(message.creation_date) : $util.Long.fromBits(message.creation_date.low >>> 0, message.creation_date.high >>> 0, false).toBigInt();
                else if (typeof message.creation_date === "number")
                    object.creation_date = options.longs === $String ? $String(message.creation_date) : message.creation_date;
                else
                    object.creation_date = options.longs === $String ? $util.Long.prototype.toString.call(message.creation_date) : options.longs === $Number ? new $util.LongBits(message.creation_date.low >>> 0, message.creation_date.high >>> 0).toNumber() : message.creation_date;
            if (message.settle_date != null && $Object.hasOwnProperty.call(message, "settle_date"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.settle_date = typeof message.settle_date === "number" ? $BigInt(message.settle_date) : $util.Long.fromBits(message.settle_date.low >>> 0, message.settle_date.high >>> 0, false).toBigInt();
                else if (typeof message.settle_date === "number")
                    object.settle_date = options.longs === $String ? $String(message.settle_date) : message.settle_date;
                else
                    object.settle_date = options.longs === $String ? $util.Long.prototype.toString.call(message.settle_date) : options.longs === $Number ? new $util.LongBits(message.settle_date.low >>> 0, message.settle_date.high >>> 0).toNumber() : message.settle_date;
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                object.payment_request = message.payment_request;
            if (message.description_hash != null && $Object.hasOwnProperty.call(message, "description_hash"))
                object.description_hash = options.bytes === $String ? $util.base64.encode(message.description_hash, 0, message.description_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.description_hash) : message.description_hash;
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiry = typeof message.expiry === "number" ? $BigInt(message.expiry) : $util.Long.fromBits(message.expiry.low >>> 0, message.expiry.high >>> 0, false).toBigInt();
                else if (typeof message.expiry === "number")
                    object.expiry = options.longs === $String ? $String(message.expiry) : message.expiry;
                else
                    object.expiry = options.longs === $String ? $util.Long.prototype.toString.call(message.expiry) : options.longs === $Number ? new $util.LongBits(message.expiry.low >>> 0, message.expiry.high >>> 0).toNumber() : message.expiry;
            if (message.fallback_addr != null && $Object.hasOwnProperty.call(message, "fallback_addr"))
                object.fallback_addr = message.fallback_addr;
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.cltv_expiry = typeof message.cltv_expiry === "number" ? $BigInt(message.cltv_expiry) : $util.Long.fromBits(message.cltv_expiry.low >>> 0, message.cltv_expiry.high >>> 0, true).toBigInt();
                else if (typeof message.cltv_expiry === "number")
                    object.cltv_expiry = options.longs === $String ? $String(message.cltv_expiry) : message.cltv_expiry;
                else
                    object.cltv_expiry = options.longs === $String ? $util.Long.prototype.toString.call(message.cltv_expiry) : options.longs === $Number ? new $util.LongBits(message.cltv_expiry.low >>> 0, message.cltv_expiry.high >>> 0).toNumber(true) : message.cltv_expiry;
            if (message.route_hints && message.route_hints.length) {
                object.route_hints = $Array(message.route_hints.length);
                for (let j = 0; j < message.route_hints.length; ++j)
                    object.route_hints[j] = $root.lnrpc.RouteHint.toObject(message.route_hints[j], options, _depth + 1);
            }
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                object["private"] = message["private"];
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.add_index = typeof message.add_index === "number" ? $BigInt(message.add_index) : $util.Long.fromBits(message.add_index.low >>> 0, message.add_index.high >>> 0, true).toBigInt();
                else if (typeof message.add_index === "number")
                    object.add_index = options.longs === $String ? $String(message.add_index) : message.add_index;
                else
                    object.add_index = options.longs === $String ? $util.Long.prototype.toString.call(message.add_index) : options.longs === $Number ? new $util.LongBits(message.add_index.low >>> 0, message.add_index.high >>> 0).toNumber(true) : message.add_index;
            if (message.settle_index != null && $Object.hasOwnProperty.call(message, "settle_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.settle_index = typeof message.settle_index === "number" ? $BigInt(message.settle_index) : $util.Long.fromBits(message.settle_index.low >>> 0, message.settle_index.high >>> 0, true).toBigInt();
                else if (typeof message.settle_index === "number")
                    object.settle_index = options.longs === $String ? $String(message.settle_index) : message.settle_index;
                else
                    object.settle_index = options.longs === $String ? $util.Long.prototype.toString.call(message.settle_index) : options.longs === $Number ? new $util.LongBits(message.settle_index.low >>> 0, message.settle_index.high >>> 0).toNumber(true) : message.settle_index;
            if (message.amt_paid != null && $Object.hasOwnProperty.call(message, "amt_paid"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_paid = typeof message.amt_paid === "number" ? $BigInt(message.amt_paid) : $util.Long.fromBits(message.amt_paid.low >>> 0, message.amt_paid.high >>> 0, false).toBigInt();
                else if (typeof message.amt_paid === "number")
                    object.amt_paid = options.longs === $String ? $String(message.amt_paid) : message.amt_paid;
                else
                    object.amt_paid = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_paid) : options.longs === $Number ? new $util.LongBits(message.amt_paid.low >>> 0, message.amt_paid.high >>> 0).toNumber() : message.amt_paid;
            if (message.amt_paid_sat != null && $Object.hasOwnProperty.call(message, "amt_paid_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_paid_sat = typeof message.amt_paid_sat === "number" ? $BigInt(message.amt_paid_sat) : $util.Long.fromBits(message.amt_paid_sat.low >>> 0, message.amt_paid_sat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_paid_sat === "number")
                    object.amt_paid_sat = options.longs === $String ? $String(message.amt_paid_sat) : message.amt_paid_sat;
                else
                    object.amt_paid_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_paid_sat) : options.longs === $Number ? new $util.LongBits(message.amt_paid_sat.low >>> 0, message.amt_paid_sat.high >>> 0).toNumber() : message.amt_paid_sat;
            if (message.amt_paid_msat != null && $Object.hasOwnProperty.call(message, "amt_paid_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_paid_msat = typeof message.amt_paid_msat === "number" ? $BigInt(message.amt_paid_msat) : $util.Long.fromBits(message.amt_paid_msat.low >>> 0, message.amt_paid_msat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_paid_msat === "number")
                    object.amt_paid_msat = options.longs === $String ? $String(message.amt_paid_msat) : message.amt_paid_msat;
                else
                    object.amt_paid_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_paid_msat) : options.longs === $Number ? new $util.LongBits(message.amt_paid_msat.low >>> 0, message.amt_paid_msat.high >>> 0).toNumber() : message.amt_paid_msat;
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                object.state = options.enums === $String ? $root.lnrpc.Invoice.InvoiceState[message.state] === $undefined ? message.state : $root.lnrpc.Invoice.InvoiceState[message.state] : message.state;
            if (message.htlcs && message.htlcs.length) {
                object.htlcs = $Array(message.htlcs.length);
                for (let j = 0; j < message.htlcs.length; ++j)
                    object.htlcs[j] = $root.lnrpc.InvoiceHTLC.toObject(message.htlcs[j], options, _depth + 1);
            }
            if (message.value_msat != null && $Object.hasOwnProperty.call(message, "value_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value_msat = typeof message.value_msat === "number" ? $BigInt(message.value_msat) : $util.Long.fromBits(message.value_msat.low >>> 0, message.value_msat.high >>> 0, false).toBigInt();
                else if (typeof message.value_msat === "number")
                    object.value_msat = options.longs === $String ? $String(message.value_msat) : message.value_msat;
                else
                    object.value_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.value_msat) : options.longs === $Number ? new $util.LongBits(message.value_msat.low >>> 0, message.value_msat.high >>> 0).toNumber() : message.value_msat;
            let keys2;
            if (message.features && (keys2 = $Object.keys(message.features)).length) {
                object.features = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.features, keys2[j]);
                    object.features[keys2[j]] = $root.lnrpc.Feature.toObject(message.features[keys2[j]], options, _depth + 1);
                }
            }
            if (message.is_keysend != null && $Object.hasOwnProperty.call(message, "is_keysend"))
                object.is_keysend = message.is_keysend;
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                object.payment_addr = options.bytes === $String ? $util.base64.encode(message.payment_addr, 0, message.payment_addr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_addr) : message.payment_addr;
            if (message.is_amp != null && $Object.hasOwnProperty.call(message, "is_amp"))
                object.is_amp = message.is_amp;
            if (message.amp_invoice_state && (keys2 = $Object.keys(message.amp_invoice_state)).length) {
                object.amp_invoice_state = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.amp_invoice_state, keys2[j]);
                    object.amp_invoice_state[keys2[j]] = $root.lnrpc.AMPInvoiceState.toObject(message.amp_invoice_state[keys2[j]], options, _depth + 1);
                }
            }
            if (message.is_blinded != null && $Object.hasOwnProperty.call(message, "is_blinded"))
                object.is_blinded = message.is_blinded;
            if (message.blinded_path_config != null && $Object.hasOwnProperty.call(message, "blinded_path_config"))
                object.blinded_path_config = $root.lnrpc.BlindedPathConfig.toObject(message.blinded_path_config, options, _depth + 1);
            if (message.min_hop_hints != null && $Object.hasOwnProperty.call(message, "min_hop_hints"))
                object.min_hop_hints = message.min_hop_hints;
            return object;
        };

        /**
         * Converts this Invoice to JSON.
         * @function toJSON
         * @memberof lnrpc.Invoice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Invoice.prototype.toJSON = function() {
            return Invoice.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Invoice
         * @function getTypeUrl
         * @memberof lnrpc.Invoice
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Invoice.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Invoice";
        };

        /**
         * InvoiceState enum.
         * @name lnrpc.Invoice.InvoiceState
         * @enum {number}
         * @property {number} OPEN=0 OPEN value
         * @property {number} SETTLED=1 SETTLED value
         * @property {number} CANCELED=2 CANCELED value
         * @property {number} ACCEPTED=3 ACCEPTED value
         */
        Invoice.InvoiceState = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "OPEN"] = 0;
            values[valuesById[1] = "SETTLED"] = 1;
            values[valuesById[2] = "CANCELED"] = 2;
            values[valuesById[3] = "ACCEPTED"] = 3;
            return values;
        })();

        return Invoice;
    })();

    lnrpc.BlindedPathConfig = (function() {

        /**
         * Properties of a BlindedPathConfig.
         * @typedef {Object} lnrpc.BlindedPathConfig.$Properties
         * @property {number|null} [min_num_real_hops] BlindedPathConfig min_num_real_hops
         * @property {number|null} [num_hops] BlindedPathConfig num_hops
         * @property {number|null} [max_num_paths] BlindedPathConfig max_num_paths
         * @property {Array.<Uint8Array>|null} [node_omission_list] BlindedPathConfig node_omission_list
         * @property {Array.<Long>|null} [incoming_channel_list] BlindedPathConfig incoming_channel_list
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BlindedPathConfig.
         * @memberof lnrpc
         * @interface IBlindedPathConfig
         * @augments lnrpc.BlindedPathConfig.$Properties
         * @deprecated Use lnrpc.BlindedPathConfig.$Properties instead.
         */

        /**
         * Shape of a BlindedPathConfig.
         * @typedef {lnrpc.BlindedPathConfig.$Properties} lnrpc.BlindedPathConfig.$Shape
         */

        /**
         * Constructs a new BlindedPathConfig.
         * @memberof lnrpc
         * @classdesc Represents a BlindedPathConfig.
         * @constructor
         * @param {lnrpc.BlindedPathConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BlindedPathConfig = function (properties) {
            this.node_omission_list = [];
            this.incoming_channel_list = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BlindedPathConfig min_num_real_hops.
         * @member {number|null|undefined} min_num_real_hops
         * @memberof lnrpc.BlindedPathConfig
         * @instance
         */
        BlindedPathConfig.prototype.min_num_real_hops = null;

        /**
         * BlindedPathConfig num_hops.
         * @member {number|null|undefined} num_hops
         * @memberof lnrpc.BlindedPathConfig
         * @instance
         */
        BlindedPathConfig.prototype.num_hops = null;

        /**
         * BlindedPathConfig max_num_paths.
         * @member {number|null|undefined} max_num_paths
         * @memberof lnrpc.BlindedPathConfig
         * @instance
         */
        BlindedPathConfig.prototype.max_num_paths = null;

        /**
         * BlindedPathConfig node_omission_list.
         * @member {Array.<Uint8Array>} node_omission_list
         * @memberof lnrpc.BlindedPathConfig
         * @instance
         */
        BlindedPathConfig.prototype.node_omission_list = $util.emptyArray;

        /**
         * BlindedPathConfig incoming_channel_list.
         * @member {Array.<Long>} incoming_channel_list
         * @memberof lnrpc.BlindedPathConfig
         * @instance
         */
        BlindedPathConfig.prototype.incoming_channel_list = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BlindedPathConfig.prototype, "_min_num_real_hops", {
            get: $util.oneOfGetter($oneOfFields = ["min_num_real_hops"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BlindedPathConfig.prototype, "_num_hops", {
            get: $util.oneOfGetter($oneOfFields = ["num_hops"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BlindedPathConfig.prototype, "_max_num_paths", {
            get: $util.oneOfGetter($oneOfFields = ["max_num_paths"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BlindedPathConfig instance using the specified properties.
         * @function create
         * @memberof lnrpc.BlindedPathConfig
         * @static
         * @param {lnrpc.BlindedPathConfig.$Properties=} [properties] Properties to set
         * @returns {lnrpc.BlindedPathConfig} BlindedPathConfig instance
         * @type {{
         *   (properties: lnrpc.BlindedPathConfig.$Shape): lnrpc.BlindedPathConfig & lnrpc.BlindedPathConfig.$Shape;
         *   (properties?: lnrpc.BlindedPathConfig.$Properties): lnrpc.BlindedPathConfig;
         * }}
         */
        BlindedPathConfig.create = function(properties) {
            return new BlindedPathConfig(properties);
        };

        /**
         * Encodes the specified BlindedPathConfig message. Does not implicitly {@link lnrpc.BlindedPathConfig.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.BlindedPathConfig
         * @static
         * @param {lnrpc.BlindedPathConfig.$Properties} message BlindedPathConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlindedPathConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.min_num_real_hops != null && $Object.hasOwnProperty.call(message, "min_num_real_hops"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.min_num_real_hops);
            if (message.num_hops != null && $Object.hasOwnProperty.call(message, "num_hops"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.num_hops);
            if (message.max_num_paths != null && $Object.hasOwnProperty.call(message, "max_num_paths"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.max_num_paths);
            if (message.node_omission_list != null && message.node_omission_list.length)
                for (let i = 0; i < message.node_omission_list.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.node_omission_list[i]);
            if (message.incoming_channel_list != null && message.incoming_channel_list.length)
                writer.uint32(/* id 5, wireType 2 =*/42).uint64s(message.incoming_channel_list);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BlindedPathConfig message, length delimited. Does not implicitly {@link lnrpc.BlindedPathConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.BlindedPathConfig
         * @static
         * @param {lnrpc.BlindedPathConfig.$Properties} message BlindedPathConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlindedPathConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BlindedPathConfig message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.BlindedPathConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.BlindedPathConfig & lnrpc.BlindedPathConfig.$Shape} BlindedPathConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlindedPathConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.BlindedPathConfig();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        message.min_num_real_hops = reader.uint32();
                        message._min_num_real_hops = "min_num_real_hops";
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        message.num_hops = reader.uint32();
                        message._num_hops = "num_hops";
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        message.max_num_paths = reader.uint32();
                        message._max_num_paths = "max_num_paths";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.node_omission_list && message.node_omission_list.length))
                            message.node_omission_list = [];
                        message.node_omission_list.push(reader.bytes());
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.incoming_channel_list && message.incoming_channel_list.length))
                                message.incoming_channel_list = [];
                            reader.uint64s(message.incoming_channel_list);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.incoming_channel_list && message.incoming_channel_list.length))
                            message.incoming_channel_list = [];
                        message.incoming_channel_list.push(reader.uint64());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BlindedPathConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.BlindedPathConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.BlindedPathConfig & lnrpc.BlindedPathConfig.$Shape} BlindedPathConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlindedPathConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BlindedPathConfig message.
         * @function verify
         * @memberof lnrpc.BlindedPathConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BlindedPathConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.min_num_real_hops != null && $Object.hasOwnProperty.call(message, "min_num_real_hops")) {
                properties._min_num_real_hops = 1;
                if (!$util.isInteger(message.min_num_real_hops))
                    return "min_num_real_hops: integer expected";
            }
            if (message.num_hops != null && $Object.hasOwnProperty.call(message, "num_hops")) {
                properties._num_hops = 1;
                if (!$util.isInteger(message.num_hops))
                    return "num_hops: integer expected";
            }
            if (message.max_num_paths != null && $Object.hasOwnProperty.call(message, "max_num_paths")) {
                properties._max_num_paths = 1;
                if (!$util.isInteger(message.max_num_paths))
                    return "max_num_paths: integer expected";
            }
            if (message.node_omission_list != null && $Object.hasOwnProperty.call(message, "node_omission_list")) {
                if (!$Array.isArray(message.node_omission_list))
                    return "node_omission_list: array expected";
                for (let i = 0; i < message.node_omission_list.length; ++i)
                    if (!(message.node_omission_list[i] && typeof message.node_omission_list[i].length === "number" || $util.isString(message.node_omission_list[i])))
                        return "node_omission_list: buffer[] expected";
            }
            if (message.incoming_channel_list != null && $Object.hasOwnProperty.call(message, "incoming_channel_list")) {
                if (!$Array.isArray(message.incoming_channel_list))
                    return "incoming_channel_list: array expected";
                for (let i = 0; i < message.incoming_channel_list.length; ++i)
                    if (!$util.isInteger(message.incoming_channel_list[i]) && !(message.incoming_channel_list[i] && $util.isInteger(message.incoming_channel_list[i].low) && $util.isInteger(message.incoming_channel_list[i].high)))
                        return "incoming_channel_list: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates a BlindedPathConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.BlindedPathConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.BlindedPathConfig} BlindedPathConfig
         */
        BlindedPathConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.BlindedPathConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.BlindedPathConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.BlindedPathConfig();
            if (object.min_num_real_hops != null)
                message.min_num_real_hops = object.min_num_real_hops >>> 0;
            if (object.num_hops != null)
                message.num_hops = object.num_hops >>> 0;
            if (object.max_num_paths != null)
                message.max_num_paths = object.max_num_paths >>> 0;
            if (object.node_omission_list) {
                if (!$Array.isArray(object.node_omission_list))
                    throw $TypeError(".lnrpc.BlindedPathConfig.node_omission_list: array expected");
                message.node_omission_list = $Array(object.node_omission_list.length);
                for (let i = 0; i < object.node_omission_list.length; ++i)
                    if (typeof object.node_omission_list[i] === "string")
                        $util.base64.decode(object.node_omission_list[i], message.node_omission_list[i] = $util.newBuffer($util.base64.length(object.node_omission_list[i])), 0);
                    else if (object.node_omission_list[i].length >= 0)
                        message.node_omission_list[i] = object.node_omission_list[i];
            }
            if (object.incoming_channel_list) {
                if (!$Array.isArray(object.incoming_channel_list))
                    throw $TypeError(".lnrpc.BlindedPathConfig.incoming_channel_list: array expected");
                message.incoming_channel_list = $Array(object.incoming_channel_list.length);
                for (let i = 0; i < object.incoming_channel_list.length; ++i)
                    if ($util.Long)
                        message.incoming_channel_list[i] = $util.Long.fromValue(object.incoming_channel_list[i], true);
                    else if (typeof object.incoming_channel_list[i] === "string")
                        message.incoming_channel_list[i] = $parseInt(object.incoming_channel_list[i], 10);
                    else if (typeof object.incoming_channel_list[i] === "number")
                        message.incoming_channel_list[i] = object.incoming_channel_list[i];
                    else if (typeof object.incoming_channel_list[i] === "object")
                        message.incoming_channel_list[i] = new $util.LongBits(object.incoming_channel_list[i].low >>> 0, object.incoming_channel_list[i].high >>> 0).toNumber(true);
            }
            return message;
        };

        /**
         * Creates a plain object from a BlindedPathConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.BlindedPathConfig
         * @static
         * @param {lnrpc.BlindedPathConfig} message BlindedPathConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BlindedPathConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.node_omission_list = [];
                object.incoming_channel_list = [];
            }
            if (message.min_num_real_hops != null && $Object.hasOwnProperty.call(message, "min_num_real_hops"))
                object.min_num_real_hops = message.min_num_real_hops;
            if (message.num_hops != null && $Object.hasOwnProperty.call(message, "num_hops"))
                object.num_hops = message.num_hops;
            if (message.max_num_paths != null && $Object.hasOwnProperty.call(message, "max_num_paths"))
                object.max_num_paths = message.max_num_paths;
            if (message.node_omission_list && message.node_omission_list.length) {
                object.node_omission_list = $Array(message.node_omission_list.length);
                for (let j = 0; j < message.node_omission_list.length; ++j)
                    object.node_omission_list[j] = options.bytes === $String ? $util.base64.encode(message.node_omission_list[j], 0, message.node_omission_list[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.node_omission_list[j]) : message.node_omission_list[j];
            }
            if (message.incoming_channel_list && message.incoming_channel_list.length) {
                object.incoming_channel_list = $Array(message.incoming_channel_list.length);
                for (let j = 0; j < message.incoming_channel_list.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.incoming_channel_list[j] = typeof message.incoming_channel_list[j] === "number" ? $BigInt(message.incoming_channel_list[j]) : $util.Long.fromBits(message.incoming_channel_list[j].low >>> 0, message.incoming_channel_list[j].high >>> 0, true).toBigInt();
                    else if (typeof message.incoming_channel_list[j] === "number")
                        object.incoming_channel_list[j] = options.longs === $String ? $String(message.incoming_channel_list[j]) : message.incoming_channel_list[j];
                    else
                        object.incoming_channel_list[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.incoming_channel_list[j]) : options.longs === $Number ? new $util.LongBits(message.incoming_channel_list[j].low >>> 0, message.incoming_channel_list[j].high >>> 0).toNumber(true) : message.incoming_channel_list[j];
            }
            return object;
        };

        /**
         * Converts this BlindedPathConfig to JSON.
         * @function toJSON
         * @memberof lnrpc.BlindedPathConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BlindedPathConfig.prototype.toJSON = function() {
            return BlindedPathConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BlindedPathConfig
         * @function getTypeUrl
         * @memberof lnrpc.BlindedPathConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BlindedPathConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.BlindedPathConfig";
        };

        return BlindedPathConfig;
    })();

    /**
     * InvoiceHTLCState enum.
     * @name lnrpc.InvoiceHTLCState
     * @enum {number}
     * @property {number} ACCEPTED=0 ACCEPTED value
     * @property {number} SETTLED=1 SETTLED value
     * @property {number} CANCELED=2 CANCELED value
     */
    lnrpc.InvoiceHTLCState = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "ACCEPTED"] = 0;
        values[valuesById[1] = "SETTLED"] = 1;
        values[valuesById[2] = "CANCELED"] = 2;
        return values;
    })();

    lnrpc.InvoiceHTLC = (function() {

        /**
         * Properties of an InvoiceHTLC.
         * @typedef {Object} lnrpc.InvoiceHTLC.$Properties
         * @property {Long|null} [chan_id] InvoiceHTLC chan_id
         * @property {Long|null} [htlc_index] InvoiceHTLC htlc_index
         * @property {Long|null} [amt_msat] InvoiceHTLC amt_msat
         * @property {number|null} [accept_height] InvoiceHTLC accept_height
         * @property {Long|null} [accept_time] InvoiceHTLC accept_time
         * @property {Long|null} [resolve_time] InvoiceHTLC resolve_time
         * @property {number|null} [expiry_height] InvoiceHTLC expiry_height
         * @property {lnrpc.InvoiceHTLCState|null} [state] InvoiceHTLC state
         * @property {Object.<string,Uint8Array>|null} [custom_records] InvoiceHTLC custom_records
         * @property {Long|null} [mpp_total_amt_msat] InvoiceHTLC mpp_total_amt_msat
         * @property {lnrpc.AMP.$Properties|null} [amp] InvoiceHTLC amp
         * @property {Uint8Array|null} [custom_channel_data] InvoiceHTLC custom_channel_data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an InvoiceHTLC.
         * @memberof lnrpc
         * @interface IInvoiceHTLC
         * @augments lnrpc.InvoiceHTLC.$Properties
         * @deprecated Use lnrpc.InvoiceHTLC.$Properties instead.
         */

        /**
         * Shape of an InvoiceHTLC.
         * @typedef {lnrpc.InvoiceHTLC.$Properties} lnrpc.InvoiceHTLC.$Shape
         */

        /**
         * Constructs a new InvoiceHTLC.
         * @memberof lnrpc
         * @classdesc Represents an InvoiceHTLC.
         * @constructor
         * @param {lnrpc.InvoiceHTLC.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const InvoiceHTLC = function (properties) {
            this.custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InvoiceHTLC chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * InvoiceHTLC htlc_index.
         * @member {Long} htlc_index
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.htlc_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * InvoiceHTLC amt_msat.
         * @member {Long} amt_msat
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.amt_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * InvoiceHTLC accept_height.
         * @member {number} accept_height
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.accept_height = 0;

        /**
         * InvoiceHTLC accept_time.
         * @member {Long} accept_time
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.accept_time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * InvoiceHTLC resolve_time.
         * @member {Long} resolve_time
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.resolve_time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * InvoiceHTLC expiry_height.
         * @member {number} expiry_height
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.expiry_height = 0;

        /**
         * InvoiceHTLC state.
         * @member {lnrpc.InvoiceHTLCState} state
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.state = 0;

        /**
         * InvoiceHTLC custom_records.
         * @member {Object.<string,Uint8Array>} custom_records
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.custom_records = $util.emptyObject;

        /**
         * InvoiceHTLC mpp_total_amt_msat.
         * @member {Long} mpp_total_amt_msat
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.mpp_total_amt_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * InvoiceHTLC amp.
         * @member {lnrpc.AMP.$Properties|null|undefined} amp
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.amp = null;

        /**
         * InvoiceHTLC custom_channel_data.
         * @member {Uint8Array} custom_channel_data
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         */
        InvoiceHTLC.prototype.custom_channel_data = $util.newBuffer([]);

        /**
         * Creates a new InvoiceHTLC instance using the specified properties.
         * @function create
         * @memberof lnrpc.InvoiceHTLC
         * @static
         * @param {lnrpc.InvoiceHTLC.$Properties=} [properties] Properties to set
         * @returns {lnrpc.InvoiceHTLC} InvoiceHTLC instance
         * @type {{
         *   (properties: lnrpc.InvoiceHTLC.$Shape): lnrpc.InvoiceHTLC & lnrpc.InvoiceHTLC.$Shape;
         *   (properties?: lnrpc.InvoiceHTLC.$Properties): lnrpc.InvoiceHTLC;
         * }}
         */
        InvoiceHTLC.create = function(properties) {
            return new InvoiceHTLC(properties);
        };

        /**
         * Encodes the specified InvoiceHTLC message. Does not implicitly {@link lnrpc.InvoiceHTLC.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.InvoiceHTLC
         * @static
         * @param {lnrpc.InvoiceHTLC.$Properties} message InvoiceHTLC message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InvoiceHTLC.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.chan_id);
            if (message.htlc_index != null && $Object.hasOwnProperty.call(message, "htlc_index") && (typeof message.htlc_index === "object" ? message.htlc_index.low || message.htlc_index.high : message.htlc_index !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.htlc_index);
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat") && (typeof message.amt_msat === "object" ? message.amt_msat.low || message.amt_msat.high : message.amt_msat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.amt_msat);
            if (message.accept_height != null && $Object.hasOwnProperty.call(message, "accept_height") && message.accept_height !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.accept_height);
            if (message.accept_time != null && $Object.hasOwnProperty.call(message, "accept_time") && (typeof message.accept_time === "object" ? message.accept_time.low || message.accept_time.high : message.accept_time !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.accept_time);
            if (message.resolve_time != null && $Object.hasOwnProperty.call(message, "resolve_time") && (typeof message.resolve_time === "object" ? message.resolve_time.low || message.resolve_time.high : message.resolve_time !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.resolve_time);
            if (message.expiry_height != null && $Object.hasOwnProperty.call(message, "expiry_height") && message.expiry_height !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.expiry_height);
            if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.state);
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records"))
                for (let keys = $Object.keys(message.custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.custom_records[keys[i]]).ldelim();
            if (message.mpp_total_amt_msat != null && $Object.hasOwnProperty.call(message, "mpp_total_amt_msat") && (typeof message.mpp_total_amt_msat === "object" ? message.mpp_total_amt_msat.low || message.mpp_total_amt_msat.high : message.mpp_total_amt_msat !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.mpp_total_amt_msat);
            if (message.amp != null && $Object.hasOwnProperty.call(message, "amp"))
                $root.lnrpc.AMP.encode(message.amp, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data") && message.custom_channel_data.length)
                writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.custom_channel_data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InvoiceHTLC message, length delimited. Does not implicitly {@link lnrpc.InvoiceHTLC.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.InvoiceHTLC
         * @static
         * @param {lnrpc.InvoiceHTLC.$Properties} message InvoiceHTLC message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InvoiceHTLC.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InvoiceHTLC message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.InvoiceHTLC
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.InvoiceHTLC & lnrpc.InvoiceHTLC.$Shape} InvoiceHTLC
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InvoiceHTLC.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.InvoiceHTLC(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_index = value;
                        else
                            delete message.htlc_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_msat = value;
                        else
                            delete message.amt_msat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.accept_height = value;
                        else
                            delete message.accept_height;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.accept_time = value;
                        else
                            delete message.accept_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.resolve_time = value;
                        else
                            delete message.resolve_time;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.expiry_height = value;
                        else
                            delete message.expiry_height;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (message.custom_records === $util.emptyObject)
                            message.custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.mpp_total_amt_msat = value;
                        else
                            delete message.mpp_total_amt_msat;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.amp = $root.lnrpc.AMP.decode(reader, reader.uint32(), $undefined, _depth + 1, message.amp);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.custom_channel_data = value;
                        else
                            delete message.custom_channel_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InvoiceHTLC message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.InvoiceHTLC
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.InvoiceHTLC & lnrpc.InvoiceHTLC.$Shape} InvoiceHTLC
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InvoiceHTLC.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InvoiceHTLC message.
         * @function verify
         * @memberof lnrpc.InvoiceHTLC
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InvoiceHTLC.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.htlc_index != null && $Object.hasOwnProperty.call(message, "htlc_index"))
                if (!$util.isInteger(message.htlc_index) && !(message.htlc_index && $util.isInteger(message.htlc_index.low) && $util.isInteger(message.htlc_index.high)))
                    return "htlc_index: integer|Long expected";
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (!$util.isInteger(message.amt_msat) && !(message.amt_msat && $util.isInteger(message.amt_msat.low) && $util.isInteger(message.amt_msat.high)))
                    return "amt_msat: integer|Long expected";
            if (message.accept_height != null && $Object.hasOwnProperty.call(message, "accept_height"))
                if (!$util.isInteger(message.accept_height))
                    return "accept_height: integer expected";
            if (message.accept_time != null && $Object.hasOwnProperty.call(message, "accept_time"))
                if (!$util.isInteger(message.accept_time) && !(message.accept_time && $util.isInteger(message.accept_time.low) && $util.isInteger(message.accept_time.high)))
                    return "accept_time: integer|Long expected";
            if (message.resolve_time != null && $Object.hasOwnProperty.call(message, "resolve_time"))
                if (!$util.isInteger(message.resolve_time) && !(message.resolve_time && $util.isInteger(message.resolve_time.low) && $util.isInteger(message.resolve_time.high)))
                    return "resolve_time: integer|Long expected";
            if (message.expiry_height != null && $Object.hasOwnProperty.call(message, "expiry_height"))
                if (!$util.isInteger(message.expiry_height))
                    return "expiry_height: integer expected";
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                    return "state: enum value expected";
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records")) {
                if (!$util.isObject(message.custom_records))
                    return "custom_records: object expected";
                let key = $Object.keys(message.custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.custom_records[key[i]] && typeof message.custom_records[key[i]].length === "number" || $util.isString(message.custom_records[key[i]])))
                        return "custom_records: buffer{k:uint64} expected";
                }
            }
            if (message.mpp_total_amt_msat != null && $Object.hasOwnProperty.call(message, "mpp_total_amt_msat"))
                if (!$util.isInteger(message.mpp_total_amt_msat) && !(message.mpp_total_amt_msat && $util.isInteger(message.mpp_total_amt_msat.low) && $util.isInteger(message.mpp_total_amt_msat.high)))
                    return "mpp_total_amt_msat: integer|Long expected";
            if (message.amp != null && $Object.hasOwnProperty.call(message, "amp")) {
                let error = $root.lnrpc.AMP.verify(message.amp, _depth + 1);
                if (error)
                    return "amp." + error;
            }
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                if (!(message.custom_channel_data && typeof message.custom_channel_data.length === "number" || $util.isString(message.custom_channel_data)))
                    return "custom_channel_data: buffer expected";
            return null;
        };

        /**
         * Creates an InvoiceHTLC message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.InvoiceHTLC
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.InvoiceHTLC} InvoiceHTLC
         */
        InvoiceHTLC.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.InvoiceHTLC)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.InvoiceHTLC: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.InvoiceHTLC();
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.htlc_index != null)
                if (typeof object.htlc_index === "object" ? object.htlc_index.low || object.htlc_index.high : $Number(object.htlc_index) !== 0)
                    if ($util.Long)
                        message.htlc_index = $util.Long.fromValue(object.htlc_index, true);
                    else if (typeof object.htlc_index === "string")
                        message.htlc_index = $parseInt(object.htlc_index, 10);
                    else if (typeof object.htlc_index === "number")
                        message.htlc_index = object.htlc_index;
                    else if (typeof object.htlc_index === "object")
                        message.htlc_index = new $util.LongBits(object.htlc_index.low >>> 0, object.htlc_index.high >>> 0).toNumber(true);
            if (object.amt_msat != null)
                if (typeof object.amt_msat === "object" ? object.amt_msat.low || object.amt_msat.high : $Number(object.amt_msat) !== 0)
                    if ($util.Long)
                        message.amt_msat = $util.Long.fromValue(object.amt_msat, true);
                    else if (typeof object.amt_msat === "string")
                        message.amt_msat = $parseInt(object.amt_msat, 10);
                    else if (typeof object.amt_msat === "number")
                        message.amt_msat = object.amt_msat;
                    else if (typeof object.amt_msat === "object")
                        message.amt_msat = new $util.LongBits(object.amt_msat.low >>> 0, object.amt_msat.high >>> 0).toNumber(true);
            if (object.accept_height != null)
                if ($Number(object.accept_height) !== 0)
                    message.accept_height = object.accept_height | 0;
            if (object.accept_time != null)
                if (typeof object.accept_time === "object" ? object.accept_time.low || object.accept_time.high : $Number(object.accept_time) !== 0)
                    if ($util.Long)
                        message.accept_time = $util.Long.fromValue(object.accept_time, false);
                    else if (typeof object.accept_time === "string")
                        message.accept_time = $parseInt(object.accept_time, 10);
                    else if (typeof object.accept_time === "number")
                        message.accept_time = object.accept_time;
                    else if (typeof object.accept_time === "object")
                        message.accept_time = new $util.LongBits(object.accept_time.low >>> 0, object.accept_time.high >>> 0).toNumber();
            if (object.resolve_time != null)
                if (typeof object.resolve_time === "object" ? object.resolve_time.low || object.resolve_time.high : $Number(object.resolve_time) !== 0)
                    if ($util.Long)
                        message.resolve_time = $util.Long.fromValue(object.resolve_time, false);
                    else if (typeof object.resolve_time === "string")
                        message.resolve_time = $parseInt(object.resolve_time, 10);
                    else if (typeof object.resolve_time === "number")
                        message.resolve_time = object.resolve_time;
                    else if (typeof object.resolve_time === "object")
                        message.resolve_time = new $util.LongBits(object.resolve_time.low >>> 0, object.resolve_time.high >>> 0).toNumber();
            if (object.expiry_height != null)
                if ($Number(object.expiry_height) !== 0)
                    message.expiry_height = object.expiry_height | 0;
            if (object.state !== 0 && (typeof object.state !== "string" || $root.lnrpc.InvoiceHTLCState[object.state] !== 0))
                switch (object.state) {
                case "ACCEPTED":
                case 0:
                    message.state = 0;
                    break;
                case "SETTLED":
                case 1:
                    message.state = 1;
                    break;
                case "CANCELED":
                case 2:
                    message.state = 2;
                    break;
                default:
                    if (typeof object.state === "number" && (object.state | 0) === object.state)
                        message.state = object.state;
                }
            if (object.custom_records) {
                if (!$util.isObject(object.custom_records))
                    throw $TypeError(".lnrpc.InvoiceHTLC.custom_records: object expected");
                message.custom_records = {};
                for (let keys = $Object.keys(object.custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.custom_records, keys[i]);
                    if (typeof object.custom_records[keys[i]] === "string")
                        $util.base64.decode(object.custom_records[keys[i]], message.custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.custom_records[keys[i]])), 0);
                    else if (object.custom_records[keys[i]].length >= 0)
                        message.custom_records[keys[i]] = object.custom_records[keys[i]];
                }
            }
            if (object.mpp_total_amt_msat != null)
                if (typeof object.mpp_total_amt_msat === "object" ? object.mpp_total_amt_msat.low || object.mpp_total_amt_msat.high : $Number(object.mpp_total_amt_msat) !== 0)
                    if ($util.Long)
                        message.mpp_total_amt_msat = $util.Long.fromValue(object.mpp_total_amt_msat, true);
                    else if (typeof object.mpp_total_amt_msat === "string")
                        message.mpp_total_amt_msat = $parseInt(object.mpp_total_amt_msat, 10);
                    else if (typeof object.mpp_total_amt_msat === "number")
                        message.mpp_total_amt_msat = object.mpp_total_amt_msat;
                    else if (typeof object.mpp_total_amt_msat === "object")
                        message.mpp_total_amt_msat = new $util.LongBits(object.mpp_total_amt_msat.low >>> 0, object.mpp_total_amt_msat.high >>> 0).toNumber(true);
            if (object.amp != null) {
                if (!$util.isObject(object.amp))
                    throw $TypeError(".lnrpc.InvoiceHTLC.amp: object expected");
                message.amp = $root.lnrpc.AMP.fromObject(object.amp, _depth + 1);
            }
            if (object.custom_channel_data != null)
                if (object.custom_channel_data.length)
                    if (typeof object.custom_channel_data === "string")
                        $util.base64.decode(object.custom_channel_data, message.custom_channel_data = $util.newBuffer($util.base64.length(object.custom_channel_data)), 0);
                    else if (object.custom_channel_data.length >= 0)
                        message.custom_channel_data = object.custom_channel_data;
            return message;
        };

        /**
         * Creates a plain object from an InvoiceHTLC message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.InvoiceHTLC
         * @static
         * @param {lnrpc.InvoiceHTLC} message InvoiceHTLC
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InvoiceHTLC.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.custom_records = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.accept_height = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.accept_time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.accept_time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.resolve_time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.resolve_time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.expiry_height = 0;
                object.state = options.enums === $String ? "ACCEPTED" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.mpp_total_amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.mpp_total_amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.amp = null;
                if (options.bytes === $String)
                    object.custom_channel_data = "";
                else {
                    object.custom_channel_data = [];
                    if (options.bytes !== $Array)
                        object.custom_channel_data = $util.newBuffer(object.custom_channel_data);
                }
            }
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.htlc_index != null && $Object.hasOwnProperty.call(message, "htlc_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_index = typeof message.htlc_index === "number" ? $BigInt(message.htlc_index) : $util.Long.fromBits(message.htlc_index.low >>> 0, message.htlc_index.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_index === "number")
                    object.htlc_index = options.longs === $String ? $String(message.htlc_index) : message.htlc_index;
                else
                    object.htlc_index = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_index) : options.longs === $Number ? new $util.LongBits(message.htlc_index.low >>> 0, message.htlc_index.high >>> 0).toNumber(true) : message.htlc_index;
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_msat = typeof message.amt_msat === "number" ? $BigInt(message.amt_msat) : $util.Long.fromBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0, true).toBigInt();
                else if (typeof message.amt_msat === "number")
                    object.amt_msat = options.longs === $String ? $String(message.amt_msat) : message.amt_msat;
                else
                    object.amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_msat) : options.longs === $Number ? new $util.LongBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0).toNumber(true) : message.amt_msat;
            if (message.accept_height != null && $Object.hasOwnProperty.call(message, "accept_height"))
                object.accept_height = message.accept_height;
            if (message.accept_time != null && $Object.hasOwnProperty.call(message, "accept_time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.accept_time = typeof message.accept_time === "number" ? $BigInt(message.accept_time) : $util.Long.fromBits(message.accept_time.low >>> 0, message.accept_time.high >>> 0, false).toBigInt();
                else if (typeof message.accept_time === "number")
                    object.accept_time = options.longs === $String ? $String(message.accept_time) : message.accept_time;
                else
                    object.accept_time = options.longs === $String ? $util.Long.prototype.toString.call(message.accept_time) : options.longs === $Number ? new $util.LongBits(message.accept_time.low >>> 0, message.accept_time.high >>> 0).toNumber() : message.accept_time;
            if (message.resolve_time != null && $Object.hasOwnProperty.call(message, "resolve_time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.resolve_time = typeof message.resolve_time === "number" ? $BigInt(message.resolve_time) : $util.Long.fromBits(message.resolve_time.low >>> 0, message.resolve_time.high >>> 0, false).toBigInt();
                else if (typeof message.resolve_time === "number")
                    object.resolve_time = options.longs === $String ? $String(message.resolve_time) : message.resolve_time;
                else
                    object.resolve_time = options.longs === $String ? $util.Long.prototype.toString.call(message.resolve_time) : options.longs === $Number ? new $util.LongBits(message.resolve_time.low >>> 0, message.resolve_time.high >>> 0).toNumber() : message.resolve_time;
            if (message.expiry_height != null && $Object.hasOwnProperty.call(message, "expiry_height"))
                object.expiry_height = message.expiry_height;
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                object.state = options.enums === $String ? $root.lnrpc.InvoiceHTLCState[message.state] === $undefined ? message.state : $root.lnrpc.InvoiceHTLCState[message.state] : message.state;
            let keys2;
            if (message.custom_records && (keys2 = $Object.keys(message.custom_records)).length) {
                object.custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.custom_records, keys2[j]);
                    object.custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.custom_records[keys2[j]], 0, message.custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_records[keys2[j]]) : message.custom_records[keys2[j]];
                }
            }
            if (message.mpp_total_amt_msat != null && $Object.hasOwnProperty.call(message, "mpp_total_amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.mpp_total_amt_msat = typeof message.mpp_total_amt_msat === "number" ? $BigInt(message.mpp_total_amt_msat) : $util.Long.fromBits(message.mpp_total_amt_msat.low >>> 0, message.mpp_total_amt_msat.high >>> 0, true).toBigInt();
                else if (typeof message.mpp_total_amt_msat === "number")
                    object.mpp_total_amt_msat = options.longs === $String ? $String(message.mpp_total_amt_msat) : message.mpp_total_amt_msat;
                else
                    object.mpp_total_amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.mpp_total_amt_msat) : options.longs === $Number ? new $util.LongBits(message.mpp_total_amt_msat.low >>> 0, message.mpp_total_amt_msat.high >>> 0).toNumber(true) : message.mpp_total_amt_msat;
            if (message.amp != null && $Object.hasOwnProperty.call(message, "amp"))
                object.amp = $root.lnrpc.AMP.toObject(message.amp, options, _depth + 1);
            if (message.custom_channel_data != null && $Object.hasOwnProperty.call(message, "custom_channel_data"))
                object.custom_channel_data = options.bytes === $String ? $util.base64.encode(message.custom_channel_data, 0, message.custom_channel_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_channel_data) : message.custom_channel_data;
            return object;
        };

        /**
         * Converts this InvoiceHTLC to JSON.
         * @function toJSON
         * @memberof lnrpc.InvoiceHTLC
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InvoiceHTLC.prototype.toJSON = function() {
            return InvoiceHTLC.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InvoiceHTLC
         * @function getTypeUrl
         * @memberof lnrpc.InvoiceHTLC
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InvoiceHTLC.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.InvoiceHTLC";
        };

        return InvoiceHTLC;
    })();

    lnrpc.AMP = (function() {

        /**
         * Properties of a AMP.
         * @typedef {Object} lnrpc.AMP.$Properties
         * @property {Uint8Array|null} [root_share] AMP root_share
         * @property {Uint8Array|null} [set_id] AMP set_id
         * @property {number|null} [child_index] AMP child_index
         * @property {Uint8Array|null} [hash] AMP hash
         * @property {Uint8Array|null} [preimage] AMP preimage
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a AMP.
         * @memberof lnrpc
         * @interface IAMP
         * @augments lnrpc.AMP.$Properties
         * @deprecated Use lnrpc.AMP.$Properties instead.
         */

        /**
         * Shape of a AMP.
         * @typedef {lnrpc.AMP.$Properties} lnrpc.AMP.$Shape
         */

        /**
         * Constructs a new AMP.
         * @memberof lnrpc
         * @classdesc Represents a AMP.
         * @constructor
         * @param {lnrpc.AMP.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AMP = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AMP root_share.
         * @member {Uint8Array} root_share
         * @memberof lnrpc.AMP
         * @instance
         */
        AMP.prototype.root_share = $util.newBuffer([]);

        /**
         * AMP set_id.
         * @member {Uint8Array} set_id
         * @memberof lnrpc.AMP
         * @instance
         */
        AMP.prototype.set_id = $util.newBuffer([]);

        /**
         * AMP child_index.
         * @member {number} child_index
         * @memberof lnrpc.AMP
         * @instance
         */
        AMP.prototype.child_index = 0;

        /**
         * AMP hash.
         * @member {Uint8Array} hash
         * @memberof lnrpc.AMP
         * @instance
         */
        AMP.prototype.hash = $util.newBuffer([]);

        /**
         * AMP preimage.
         * @member {Uint8Array} preimage
         * @memberof lnrpc.AMP
         * @instance
         */
        AMP.prototype.preimage = $util.newBuffer([]);

        /**
         * Creates a new AMP instance using the specified properties.
         * @function create
         * @memberof lnrpc.AMP
         * @static
         * @param {lnrpc.AMP.$Properties=} [properties] Properties to set
         * @returns {lnrpc.AMP} AMP instance
         * @type {{
         *   (properties: lnrpc.AMP.$Shape): lnrpc.AMP & lnrpc.AMP.$Shape;
         *   (properties?: lnrpc.AMP.$Properties): lnrpc.AMP;
         * }}
         */
        AMP.create = function(properties) {
            return new AMP(properties);
        };

        /**
         * Encodes the specified AMP message. Does not implicitly {@link lnrpc.AMP.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.AMP
         * @static
         * @param {lnrpc.AMP.$Properties} message AMP message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AMP.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.root_share != null && $Object.hasOwnProperty.call(message, "root_share") && message.root_share.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.root_share);
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id") && message.set_id.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.set_id);
            if (message.child_index != null && $Object.hasOwnProperty.call(message, "child_index") && message.child_index !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.child_index);
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.hash);
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage") && message.preimage.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.preimage);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AMP message, length delimited. Does not implicitly {@link lnrpc.AMP.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.AMP
         * @static
         * @param {lnrpc.AMP.$Properties} message AMP message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AMP.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AMP message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.AMP
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.AMP & lnrpc.AMP.$Shape} AMP
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AMP.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.AMP(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.root_share = value;
                        else
                            delete message.root_share;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.set_id = value;
                        else
                            delete message.set_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.child_index = value;
                        else
                            delete message.child_index;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.preimage = value;
                        else
                            delete message.preimage;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a AMP message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.AMP
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.AMP & lnrpc.AMP.$Shape} AMP
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AMP.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AMP message.
         * @function verify
         * @memberof lnrpc.AMP
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AMP.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.root_share != null && $Object.hasOwnProperty.call(message, "root_share"))
                if (!(message.root_share && typeof message.root_share.length === "number" || $util.isString(message.root_share)))
                    return "root_share: buffer expected";
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id"))
                if (!(message.set_id && typeof message.set_id.length === "number" || $util.isString(message.set_id)))
                    return "set_id: buffer expected";
            if (message.child_index != null && $Object.hasOwnProperty.call(message, "child_index"))
                if (!$util.isInteger(message.child_index))
                    return "child_index: integer expected";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                    return "hash: buffer expected";
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                if (!(message.preimage && typeof message.preimage.length === "number" || $util.isString(message.preimage)))
                    return "preimage: buffer expected";
            return null;
        };

        /**
         * Creates a AMP message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.AMP
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.AMP} AMP
         */
        AMP.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.AMP)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.AMP: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.AMP();
            if (object.root_share != null)
                if (object.root_share.length)
                    if (typeof object.root_share === "string")
                        $util.base64.decode(object.root_share, message.root_share = $util.newBuffer($util.base64.length(object.root_share)), 0);
                    else if (object.root_share.length >= 0)
                        message.root_share = object.root_share;
            if (object.set_id != null)
                if (object.set_id.length)
                    if (typeof object.set_id === "string")
                        $util.base64.decode(object.set_id, message.set_id = $util.newBuffer($util.base64.length(object.set_id)), 0);
                    else if (object.set_id.length >= 0)
                        message.set_id = object.set_id;
            if (object.child_index != null)
                if ($Number(object.child_index) !== 0)
                    message.child_index = object.child_index >>> 0;
            if (object.hash != null)
                if (object.hash.length)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
            if (object.preimage != null)
                if (object.preimage.length)
                    if (typeof object.preimage === "string")
                        $util.base64.decode(object.preimage, message.preimage = $util.newBuffer($util.base64.length(object.preimage)), 0);
                    else if (object.preimage.length >= 0)
                        message.preimage = object.preimage;
            return message;
        };

        /**
         * Creates a plain object from a AMP message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.AMP
         * @static
         * @param {lnrpc.AMP} message AMP
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AMP.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.root_share = "";
                else {
                    object.root_share = [];
                    if (options.bytes !== $Array)
                        object.root_share = $util.newBuffer(object.root_share);
                }
                if (options.bytes === $String)
                    object.set_id = "";
                else {
                    object.set_id = [];
                    if (options.bytes !== $Array)
                        object.set_id = $util.newBuffer(object.set_id);
                }
                object.child_index = 0;
                if (options.bytes === $String)
                    object.hash = "";
                else {
                    object.hash = [];
                    if (options.bytes !== $Array)
                        object.hash = $util.newBuffer(object.hash);
                }
                if (options.bytes === $String)
                    object.preimage = "";
                else {
                    object.preimage = [];
                    if (options.bytes !== $Array)
                        object.preimage = $util.newBuffer(object.preimage);
                }
            }
            if (message.root_share != null && $Object.hasOwnProperty.call(message, "root_share"))
                object.root_share = options.bytes === $String ? $util.base64.encode(message.root_share, 0, message.root_share.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.root_share) : message.root_share;
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id"))
                object.set_id = options.bytes === $String ? $util.base64.encode(message.set_id, 0, message.set_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.set_id) : message.set_id;
            if (message.child_index != null && $Object.hasOwnProperty.call(message, "child_index"))
                object.child_index = message.child_index;
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = options.bytes === $String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash) : message.hash;
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                object.preimage = options.bytes === $String ? $util.base64.encode(message.preimage, 0, message.preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.preimage) : message.preimage;
            return object;
        };

        /**
         * Converts this AMP to JSON.
         * @function toJSON
         * @memberof lnrpc.AMP
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AMP.prototype.toJSON = function() {
            return AMP.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AMP
         * @function getTypeUrl
         * @memberof lnrpc.AMP
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AMP.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.AMP";
        };

        return AMP;
    })();

    lnrpc.AddInvoiceResponse = (function() {

        /**
         * Properties of an AddInvoiceResponse.
         * @typedef {Object} lnrpc.AddInvoiceResponse.$Properties
         * @property {Uint8Array|null} [r_hash] AddInvoiceResponse r_hash
         * @property {string|null} [payment_request] AddInvoiceResponse payment_request
         * @property {Long|null} [add_index] AddInvoiceResponse add_index
         * @property {Uint8Array|null} [payment_addr] AddInvoiceResponse payment_addr
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddInvoiceResponse.
         * @memberof lnrpc
         * @interface IAddInvoiceResponse
         * @augments lnrpc.AddInvoiceResponse.$Properties
         * @deprecated Use lnrpc.AddInvoiceResponse.$Properties instead.
         */

        /**
         * Shape of an AddInvoiceResponse.
         * @typedef {lnrpc.AddInvoiceResponse.$Properties} lnrpc.AddInvoiceResponse.$Shape
         */

        /**
         * Constructs a new AddInvoiceResponse.
         * @memberof lnrpc
         * @classdesc Represents an AddInvoiceResponse.
         * @constructor
         * @param {lnrpc.AddInvoiceResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddInvoiceResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddInvoiceResponse r_hash.
         * @member {Uint8Array} r_hash
         * @memberof lnrpc.AddInvoiceResponse
         * @instance
         */
        AddInvoiceResponse.prototype.r_hash = $util.newBuffer([]);

        /**
         * AddInvoiceResponse payment_request.
         * @member {string} payment_request
         * @memberof lnrpc.AddInvoiceResponse
         * @instance
         */
        AddInvoiceResponse.prototype.payment_request = "";

        /**
         * AddInvoiceResponse add_index.
         * @member {Long} add_index
         * @memberof lnrpc.AddInvoiceResponse
         * @instance
         */
        AddInvoiceResponse.prototype.add_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * AddInvoiceResponse payment_addr.
         * @member {Uint8Array} payment_addr
         * @memberof lnrpc.AddInvoiceResponse
         * @instance
         */
        AddInvoiceResponse.prototype.payment_addr = $util.newBuffer([]);

        /**
         * Creates a new AddInvoiceResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.AddInvoiceResponse
         * @static
         * @param {lnrpc.AddInvoiceResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.AddInvoiceResponse} AddInvoiceResponse instance
         * @type {{
         *   (properties: lnrpc.AddInvoiceResponse.$Shape): lnrpc.AddInvoiceResponse & lnrpc.AddInvoiceResponse.$Shape;
         *   (properties?: lnrpc.AddInvoiceResponse.$Properties): lnrpc.AddInvoiceResponse;
         * }}
         */
        AddInvoiceResponse.create = function(properties) {
            return new AddInvoiceResponse(properties);
        };

        /**
         * Encodes the specified AddInvoiceResponse message. Does not implicitly {@link lnrpc.AddInvoiceResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.AddInvoiceResponse
         * @static
         * @param {lnrpc.AddInvoiceResponse.$Properties} message AddInvoiceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddInvoiceResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash") && message.r_hash.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.r_hash);
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request") && message.payment_request !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.payment_request);
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index") && (typeof message.add_index === "object" ? message.add_index.low || message.add_index.high : message.add_index !== 0))
                writer.uint32(/* id 16, wireType 0 =*/128).uint64(message.add_index);
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr") && message.payment_addr.length)
                writer.uint32(/* id 17, wireType 2 =*/138).bytes(message.payment_addr);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddInvoiceResponse message, length delimited. Does not implicitly {@link lnrpc.AddInvoiceResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.AddInvoiceResponse
         * @static
         * @param {lnrpc.AddInvoiceResponse.$Properties} message AddInvoiceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddInvoiceResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddInvoiceResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.AddInvoiceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.AddInvoiceResponse & lnrpc.AddInvoiceResponse.$Shape} AddInvoiceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddInvoiceResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.AddInvoiceResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.r_hash = value;
                        else
                            delete message.r_hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_request = value;
                        else
                            delete message.payment_request;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.add_index = value;
                        else
                            delete message.add_index;
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_addr = value;
                        else
                            delete message.payment_addr;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddInvoiceResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.AddInvoiceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.AddInvoiceResponse & lnrpc.AddInvoiceResponse.$Shape} AddInvoiceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddInvoiceResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddInvoiceResponse message.
         * @function verify
         * @memberof lnrpc.AddInvoiceResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddInvoiceResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash"))
                if (!(message.r_hash && typeof message.r_hash.length === "number" || $util.isString(message.r_hash)))
                    return "r_hash: buffer expected";
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                if (!$util.isString(message.payment_request))
                    return "payment_request: string expected";
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index"))
                if (!$util.isInteger(message.add_index) && !(message.add_index && $util.isInteger(message.add_index.low) && $util.isInteger(message.add_index.high)))
                    return "add_index: integer|Long expected";
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                if (!(message.payment_addr && typeof message.payment_addr.length === "number" || $util.isString(message.payment_addr)))
                    return "payment_addr: buffer expected";
            return null;
        };

        /**
         * Creates an AddInvoiceResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.AddInvoiceResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.AddInvoiceResponse} AddInvoiceResponse
         */
        AddInvoiceResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.AddInvoiceResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.AddInvoiceResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.AddInvoiceResponse();
            if (object.r_hash != null)
                if (object.r_hash.length)
                    if (typeof object.r_hash === "string")
                        $util.base64.decode(object.r_hash, message.r_hash = $util.newBuffer($util.base64.length(object.r_hash)), 0);
                    else if (object.r_hash.length >= 0)
                        message.r_hash = object.r_hash;
            if (object.payment_request != null)
                if (typeof object.payment_request !== "string" || object.payment_request.length)
                    message.payment_request = $String(object.payment_request);
            if (object.add_index != null)
                if (typeof object.add_index === "object" ? object.add_index.low || object.add_index.high : $Number(object.add_index) !== 0)
                    if ($util.Long)
                        message.add_index = $util.Long.fromValue(object.add_index, true);
                    else if (typeof object.add_index === "string")
                        message.add_index = $parseInt(object.add_index, 10);
                    else if (typeof object.add_index === "number")
                        message.add_index = object.add_index;
                    else if (typeof object.add_index === "object")
                        message.add_index = new $util.LongBits(object.add_index.low >>> 0, object.add_index.high >>> 0).toNumber(true);
            if (object.payment_addr != null)
                if (object.payment_addr.length)
                    if (typeof object.payment_addr === "string")
                        $util.base64.decode(object.payment_addr, message.payment_addr = $util.newBuffer($util.base64.length(object.payment_addr)), 0);
                    else if (object.payment_addr.length >= 0)
                        message.payment_addr = object.payment_addr;
            return message;
        };

        /**
         * Creates a plain object from an AddInvoiceResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.AddInvoiceResponse
         * @static
         * @param {lnrpc.AddInvoiceResponse} message AddInvoiceResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddInvoiceResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.r_hash = "";
                else {
                    object.r_hash = [];
                    if (options.bytes !== $Array)
                        object.r_hash = $util.newBuffer(object.r_hash);
                }
                object.payment_request = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.add_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.add_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.payment_addr = "";
                else {
                    object.payment_addr = [];
                    if (options.bytes !== $Array)
                        object.payment_addr = $util.newBuffer(object.payment_addr);
                }
            }
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash"))
                object.r_hash = options.bytes === $String ? $util.base64.encode(message.r_hash, 0, message.r_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.r_hash) : message.r_hash;
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                object.payment_request = message.payment_request;
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.add_index = typeof message.add_index === "number" ? $BigInt(message.add_index) : $util.Long.fromBits(message.add_index.low >>> 0, message.add_index.high >>> 0, true).toBigInt();
                else if (typeof message.add_index === "number")
                    object.add_index = options.longs === $String ? $String(message.add_index) : message.add_index;
                else
                    object.add_index = options.longs === $String ? $util.Long.prototype.toString.call(message.add_index) : options.longs === $Number ? new $util.LongBits(message.add_index.low >>> 0, message.add_index.high >>> 0).toNumber(true) : message.add_index;
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                object.payment_addr = options.bytes === $String ? $util.base64.encode(message.payment_addr, 0, message.payment_addr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_addr) : message.payment_addr;
            return object;
        };

        /**
         * Converts this AddInvoiceResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.AddInvoiceResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddInvoiceResponse.prototype.toJSON = function() {
            return AddInvoiceResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddInvoiceResponse
         * @function getTypeUrl
         * @memberof lnrpc.AddInvoiceResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddInvoiceResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.AddInvoiceResponse";
        };

        return AddInvoiceResponse;
    })();

    lnrpc.PaymentHash = (function() {

        /**
         * Properties of a PaymentHash.
         * @typedef {Object} lnrpc.PaymentHash.$Properties
         * @property {string|null} [r_hash_str] PaymentHash r_hash_str
         * @property {Uint8Array|null} [r_hash] PaymentHash r_hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PaymentHash.
         * @memberof lnrpc
         * @interface IPaymentHash
         * @augments lnrpc.PaymentHash.$Properties
         * @deprecated Use lnrpc.PaymentHash.$Properties instead.
         */

        /**
         * Shape of a PaymentHash.
         * @typedef {lnrpc.PaymentHash.$Properties} lnrpc.PaymentHash.$Shape
         */

        /**
         * Constructs a new PaymentHash.
         * @memberof lnrpc
         * @classdesc Represents a PaymentHash.
         * @constructor
         * @param {lnrpc.PaymentHash.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PaymentHash = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PaymentHash r_hash_str.
         * @member {string} r_hash_str
         * @memberof lnrpc.PaymentHash
         * @instance
         */
        PaymentHash.prototype.r_hash_str = "";

        /**
         * PaymentHash r_hash.
         * @member {Uint8Array} r_hash
         * @memberof lnrpc.PaymentHash
         * @instance
         */
        PaymentHash.prototype.r_hash = $util.newBuffer([]);

        /**
         * Creates a new PaymentHash instance using the specified properties.
         * @function create
         * @memberof lnrpc.PaymentHash
         * @static
         * @param {lnrpc.PaymentHash.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PaymentHash} PaymentHash instance
         * @type {{
         *   (properties: lnrpc.PaymentHash.$Shape): lnrpc.PaymentHash & lnrpc.PaymentHash.$Shape;
         *   (properties?: lnrpc.PaymentHash.$Properties): lnrpc.PaymentHash;
         * }}
         */
        PaymentHash.create = function(properties) {
            return new PaymentHash(properties);
        };

        /**
         * Encodes the specified PaymentHash message. Does not implicitly {@link lnrpc.PaymentHash.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PaymentHash
         * @static
         * @param {lnrpc.PaymentHash.$Properties} message PaymentHash message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentHash.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.r_hash_str != null && $Object.hasOwnProperty.call(message, "r_hash_str") && message.r_hash_str !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.r_hash_str);
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash") && message.r_hash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.r_hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PaymentHash message, length delimited. Does not implicitly {@link lnrpc.PaymentHash.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PaymentHash
         * @static
         * @param {lnrpc.PaymentHash.$Properties} message PaymentHash message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentHash.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PaymentHash message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PaymentHash
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PaymentHash & lnrpc.PaymentHash.$Shape} PaymentHash
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentHash.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PaymentHash(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.r_hash_str = value;
                        else
                            delete message.r_hash_str;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.r_hash = value;
                        else
                            delete message.r_hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PaymentHash message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PaymentHash
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PaymentHash & lnrpc.PaymentHash.$Shape} PaymentHash
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentHash.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PaymentHash message.
         * @function verify
         * @memberof lnrpc.PaymentHash
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PaymentHash.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.r_hash_str != null && $Object.hasOwnProperty.call(message, "r_hash_str"))
                if (!$util.isString(message.r_hash_str))
                    return "r_hash_str: string expected";
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash"))
                if (!(message.r_hash && typeof message.r_hash.length === "number" || $util.isString(message.r_hash)))
                    return "r_hash: buffer expected";
            return null;
        };

        /**
         * Creates a PaymentHash message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PaymentHash
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PaymentHash} PaymentHash
         */
        PaymentHash.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PaymentHash)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PaymentHash: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PaymentHash();
            if (object.r_hash_str != null)
                if (typeof object.r_hash_str !== "string" || object.r_hash_str.length)
                    message.r_hash_str = $String(object.r_hash_str);
            if (object.r_hash != null)
                if (object.r_hash.length)
                    if (typeof object.r_hash === "string")
                        $util.base64.decode(object.r_hash, message.r_hash = $util.newBuffer($util.base64.length(object.r_hash)), 0);
                    else if (object.r_hash.length >= 0)
                        message.r_hash = object.r_hash;
            return message;
        };

        /**
         * Creates a plain object from a PaymentHash message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PaymentHash
         * @static
         * @param {lnrpc.PaymentHash} message PaymentHash
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PaymentHash.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.r_hash_str = "";
                if (options.bytes === $String)
                    object.r_hash = "";
                else {
                    object.r_hash = [];
                    if (options.bytes !== $Array)
                        object.r_hash = $util.newBuffer(object.r_hash);
                }
            }
            if (message.r_hash_str != null && $Object.hasOwnProperty.call(message, "r_hash_str"))
                object.r_hash_str = message.r_hash_str;
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash"))
                object.r_hash = options.bytes === $String ? $util.base64.encode(message.r_hash, 0, message.r_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.r_hash) : message.r_hash;
            return object;
        };

        /**
         * Converts this PaymentHash to JSON.
         * @function toJSON
         * @memberof lnrpc.PaymentHash
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PaymentHash.prototype.toJSON = function() {
            return PaymentHash.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PaymentHash
         * @function getTypeUrl
         * @memberof lnrpc.PaymentHash
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PaymentHash.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PaymentHash";
        };

        return PaymentHash;
    })();

    lnrpc.ListInvoiceRequest = (function() {

        /**
         * Properties of a ListInvoiceRequest.
         * @typedef {Object} lnrpc.ListInvoiceRequest.$Properties
         * @property {boolean|null} [pending_only] ListInvoiceRequest pending_only
         * @property {Long|null} [index_offset] ListInvoiceRequest index_offset
         * @property {Long|null} [num_max_invoices] ListInvoiceRequest num_max_invoices
         * @property {boolean|null} [reversed] ListInvoiceRequest reversed
         * @property {Long|null} [creation_date_start] ListInvoiceRequest creation_date_start
         * @property {Long|null} [creation_date_end] ListInvoiceRequest creation_date_end
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListInvoiceRequest.
         * @memberof lnrpc
         * @interface IListInvoiceRequest
         * @augments lnrpc.ListInvoiceRequest.$Properties
         * @deprecated Use lnrpc.ListInvoiceRequest.$Properties instead.
         */

        /**
         * Shape of a ListInvoiceRequest.
         * @typedef {lnrpc.ListInvoiceRequest.$Properties} lnrpc.ListInvoiceRequest.$Shape
         */

        /**
         * Constructs a new ListInvoiceRequest.
         * @memberof lnrpc
         * @classdesc Represents a ListInvoiceRequest.
         * @constructor
         * @param {lnrpc.ListInvoiceRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListInvoiceRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListInvoiceRequest pending_only.
         * @member {boolean} pending_only
         * @memberof lnrpc.ListInvoiceRequest
         * @instance
         */
        ListInvoiceRequest.prototype.pending_only = false;

        /**
         * ListInvoiceRequest index_offset.
         * @member {Long} index_offset
         * @memberof lnrpc.ListInvoiceRequest
         * @instance
         */
        ListInvoiceRequest.prototype.index_offset = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ListInvoiceRequest num_max_invoices.
         * @member {Long} num_max_invoices
         * @memberof lnrpc.ListInvoiceRequest
         * @instance
         */
        ListInvoiceRequest.prototype.num_max_invoices = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ListInvoiceRequest reversed.
         * @member {boolean} reversed
         * @memberof lnrpc.ListInvoiceRequest
         * @instance
         */
        ListInvoiceRequest.prototype.reversed = false;

        /**
         * ListInvoiceRequest creation_date_start.
         * @member {Long} creation_date_start
         * @memberof lnrpc.ListInvoiceRequest
         * @instance
         */
        ListInvoiceRequest.prototype.creation_date_start = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ListInvoiceRequest creation_date_end.
         * @member {Long} creation_date_end
         * @memberof lnrpc.ListInvoiceRequest
         * @instance
         */
        ListInvoiceRequest.prototype.creation_date_end = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new ListInvoiceRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListInvoiceRequest
         * @static
         * @param {lnrpc.ListInvoiceRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListInvoiceRequest} ListInvoiceRequest instance
         * @type {{
         *   (properties: lnrpc.ListInvoiceRequest.$Shape): lnrpc.ListInvoiceRequest & lnrpc.ListInvoiceRequest.$Shape;
         *   (properties?: lnrpc.ListInvoiceRequest.$Properties): lnrpc.ListInvoiceRequest;
         * }}
         */
        ListInvoiceRequest.create = function(properties) {
            return new ListInvoiceRequest(properties);
        };

        /**
         * Encodes the specified ListInvoiceRequest message. Does not implicitly {@link lnrpc.ListInvoiceRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListInvoiceRequest
         * @static
         * @param {lnrpc.ListInvoiceRequest.$Properties} message ListInvoiceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListInvoiceRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pending_only != null && $Object.hasOwnProperty.call(message, "pending_only") && message.pending_only !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.pending_only);
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset") && (typeof message.index_offset === "object" ? message.index_offset.low || message.index_offset.high : message.index_offset !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.index_offset);
            if (message.num_max_invoices != null && $Object.hasOwnProperty.call(message, "num_max_invoices") && (typeof message.num_max_invoices === "object" ? message.num_max_invoices.low || message.num_max_invoices.high : message.num_max_invoices !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.num_max_invoices);
            if (message.reversed != null && $Object.hasOwnProperty.call(message, "reversed") && message.reversed !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.reversed);
            if (message.creation_date_start != null && $Object.hasOwnProperty.call(message, "creation_date_start") && (typeof message.creation_date_start === "object" ? message.creation_date_start.low || message.creation_date_start.high : message.creation_date_start !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.creation_date_start);
            if (message.creation_date_end != null && $Object.hasOwnProperty.call(message, "creation_date_end") && (typeof message.creation_date_end === "object" ? message.creation_date_end.low || message.creation_date_end.high : message.creation_date_end !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.creation_date_end);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListInvoiceRequest message, length delimited. Does not implicitly {@link lnrpc.ListInvoiceRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListInvoiceRequest
         * @static
         * @param {lnrpc.ListInvoiceRequest.$Properties} message ListInvoiceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListInvoiceRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListInvoiceRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListInvoiceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListInvoiceRequest & lnrpc.ListInvoiceRequest.$Shape} ListInvoiceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListInvoiceRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListInvoiceRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.pending_only = value;
                        else
                            delete message.pending_only;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.index_offset = value;
                        else
                            delete message.index_offset;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.num_max_invoices = value;
                        else
                            delete message.num_max_invoices;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.reversed = value;
                        else
                            delete message.reversed;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.creation_date_start = value;
                        else
                            delete message.creation_date_start;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.creation_date_end = value;
                        else
                            delete message.creation_date_end;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListInvoiceRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListInvoiceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListInvoiceRequest & lnrpc.ListInvoiceRequest.$Shape} ListInvoiceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListInvoiceRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListInvoiceRequest message.
         * @function verify
         * @memberof lnrpc.ListInvoiceRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListInvoiceRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pending_only != null && $Object.hasOwnProperty.call(message, "pending_only"))
                if (typeof message.pending_only !== "boolean")
                    return "pending_only: boolean expected";
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset"))
                if (!$util.isInteger(message.index_offset) && !(message.index_offset && $util.isInteger(message.index_offset.low) && $util.isInteger(message.index_offset.high)))
                    return "index_offset: integer|Long expected";
            if (message.num_max_invoices != null && $Object.hasOwnProperty.call(message, "num_max_invoices"))
                if (!$util.isInteger(message.num_max_invoices) && !(message.num_max_invoices && $util.isInteger(message.num_max_invoices.low) && $util.isInteger(message.num_max_invoices.high)))
                    return "num_max_invoices: integer|Long expected";
            if (message.reversed != null && $Object.hasOwnProperty.call(message, "reversed"))
                if (typeof message.reversed !== "boolean")
                    return "reversed: boolean expected";
            if (message.creation_date_start != null && $Object.hasOwnProperty.call(message, "creation_date_start"))
                if (!$util.isInteger(message.creation_date_start) && !(message.creation_date_start && $util.isInteger(message.creation_date_start.low) && $util.isInteger(message.creation_date_start.high)))
                    return "creation_date_start: integer|Long expected";
            if (message.creation_date_end != null && $Object.hasOwnProperty.call(message, "creation_date_end"))
                if (!$util.isInteger(message.creation_date_end) && !(message.creation_date_end && $util.isInteger(message.creation_date_end.low) && $util.isInteger(message.creation_date_end.high)))
                    return "creation_date_end: integer|Long expected";
            return null;
        };

        /**
         * Creates a ListInvoiceRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListInvoiceRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListInvoiceRequest} ListInvoiceRequest
         */
        ListInvoiceRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListInvoiceRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListInvoiceRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListInvoiceRequest();
            if (object.pending_only != null)
                if (object.pending_only)
                    message.pending_only = $Boolean(object.pending_only);
            if (object.index_offset != null)
                if (typeof object.index_offset === "object" ? object.index_offset.low || object.index_offset.high : $Number(object.index_offset) !== 0)
                    if ($util.Long)
                        message.index_offset = $util.Long.fromValue(object.index_offset, true);
                    else if (typeof object.index_offset === "string")
                        message.index_offset = $parseInt(object.index_offset, 10);
                    else if (typeof object.index_offset === "number")
                        message.index_offset = object.index_offset;
                    else if (typeof object.index_offset === "object")
                        message.index_offset = new $util.LongBits(object.index_offset.low >>> 0, object.index_offset.high >>> 0).toNumber(true);
            if (object.num_max_invoices != null)
                if (typeof object.num_max_invoices === "object" ? object.num_max_invoices.low || object.num_max_invoices.high : $Number(object.num_max_invoices) !== 0)
                    if ($util.Long)
                        message.num_max_invoices = $util.Long.fromValue(object.num_max_invoices, true);
                    else if (typeof object.num_max_invoices === "string")
                        message.num_max_invoices = $parseInt(object.num_max_invoices, 10);
                    else if (typeof object.num_max_invoices === "number")
                        message.num_max_invoices = object.num_max_invoices;
                    else if (typeof object.num_max_invoices === "object")
                        message.num_max_invoices = new $util.LongBits(object.num_max_invoices.low >>> 0, object.num_max_invoices.high >>> 0).toNumber(true);
            if (object.reversed != null)
                if (object.reversed)
                    message.reversed = $Boolean(object.reversed);
            if (object.creation_date_start != null)
                if (typeof object.creation_date_start === "object" ? object.creation_date_start.low || object.creation_date_start.high : $Number(object.creation_date_start) !== 0)
                    if ($util.Long)
                        message.creation_date_start = $util.Long.fromValue(object.creation_date_start, true);
                    else if (typeof object.creation_date_start === "string")
                        message.creation_date_start = $parseInt(object.creation_date_start, 10);
                    else if (typeof object.creation_date_start === "number")
                        message.creation_date_start = object.creation_date_start;
                    else if (typeof object.creation_date_start === "object")
                        message.creation_date_start = new $util.LongBits(object.creation_date_start.low >>> 0, object.creation_date_start.high >>> 0).toNumber(true);
            if (object.creation_date_end != null)
                if (typeof object.creation_date_end === "object" ? object.creation_date_end.low || object.creation_date_end.high : $Number(object.creation_date_end) !== 0)
                    if ($util.Long)
                        message.creation_date_end = $util.Long.fromValue(object.creation_date_end, true);
                    else if (typeof object.creation_date_end === "string")
                        message.creation_date_end = $parseInt(object.creation_date_end, 10);
                    else if (typeof object.creation_date_end === "number")
                        message.creation_date_end = object.creation_date_end;
                    else if (typeof object.creation_date_end === "object")
                        message.creation_date_end = new $util.LongBits(object.creation_date_end.low >>> 0, object.creation_date_end.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ListInvoiceRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListInvoiceRequest
         * @static
         * @param {lnrpc.ListInvoiceRequest} message ListInvoiceRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListInvoiceRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.pending_only = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.index_offset = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.index_offset = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.num_max_invoices = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.num_max_invoices = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.reversed = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.creation_date_start = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.creation_date_start = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.creation_date_end = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.creation_date_end = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.pending_only != null && $Object.hasOwnProperty.call(message, "pending_only"))
                object.pending_only = message.pending_only;
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.index_offset = typeof message.index_offset === "number" ? $BigInt(message.index_offset) : $util.Long.fromBits(message.index_offset.low >>> 0, message.index_offset.high >>> 0, true).toBigInt();
                else if (typeof message.index_offset === "number")
                    object.index_offset = options.longs === $String ? $String(message.index_offset) : message.index_offset;
                else
                    object.index_offset = options.longs === $String ? $util.Long.prototype.toString.call(message.index_offset) : options.longs === $Number ? new $util.LongBits(message.index_offset.low >>> 0, message.index_offset.high >>> 0).toNumber(true) : message.index_offset;
            if (message.num_max_invoices != null && $Object.hasOwnProperty.call(message, "num_max_invoices"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.num_max_invoices = typeof message.num_max_invoices === "number" ? $BigInt(message.num_max_invoices) : $util.Long.fromBits(message.num_max_invoices.low >>> 0, message.num_max_invoices.high >>> 0, true).toBigInt();
                else if (typeof message.num_max_invoices === "number")
                    object.num_max_invoices = options.longs === $String ? $String(message.num_max_invoices) : message.num_max_invoices;
                else
                    object.num_max_invoices = options.longs === $String ? $util.Long.prototype.toString.call(message.num_max_invoices) : options.longs === $Number ? new $util.LongBits(message.num_max_invoices.low >>> 0, message.num_max_invoices.high >>> 0).toNumber(true) : message.num_max_invoices;
            if (message.reversed != null && $Object.hasOwnProperty.call(message, "reversed"))
                object.reversed = message.reversed;
            if (message.creation_date_start != null && $Object.hasOwnProperty.call(message, "creation_date_start"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.creation_date_start = typeof message.creation_date_start === "number" ? $BigInt(message.creation_date_start) : $util.Long.fromBits(message.creation_date_start.low >>> 0, message.creation_date_start.high >>> 0, true).toBigInt();
                else if (typeof message.creation_date_start === "number")
                    object.creation_date_start = options.longs === $String ? $String(message.creation_date_start) : message.creation_date_start;
                else
                    object.creation_date_start = options.longs === $String ? $util.Long.prototype.toString.call(message.creation_date_start) : options.longs === $Number ? new $util.LongBits(message.creation_date_start.low >>> 0, message.creation_date_start.high >>> 0).toNumber(true) : message.creation_date_start;
            if (message.creation_date_end != null && $Object.hasOwnProperty.call(message, "creation_date_end"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.creation_date_end = typeof message.creation_date_end === "number" ? $BigInt(message.creation_date_end) : $util.Long.fromBits(message.creation_date_end.low >>> 0, message.creation_date_end.high >>> 0, true).toBigInt();
                else if (typeof message.creation_date_end === "number")
                    object.creation_date_end = options.longs === $String ? $String(message.creation_date_end) : message.creation_date_end;
                else
                    object.creation_date_end = options.longs === $String ? $util.Long.prototype.toString.call(message.creation_date_end) : options.longs === $Number ? new $util.LongBits(message.creation_date_end.low >>> 0, message.creation_date_end.high >>> 0).toNumber(true) : message.creation_date_end;
            return object;
        };

        /**
         * Converts this ListInvoiceRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ListInvoiceRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListInvoiceRequest.prototype.toJSON = function() {
            return ListInvoiceRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListInvoiceRequest
         * @function getTypeUrl
         * @memberof lnrpc.ListInvoiceRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListInvoiceRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListInvoiceRequest";
        };

        return ListInvoiceRequest;
    })();

    lnrpc.ListInvoiceResponse = (function() {

        /**
         * Properties of a ListInvoiceResponse.
         * @typedef {Object} lnrpc.ListInvoiceResponse.$Properties
         * @property {Array.<lnrpc.Invoice.$Properties>|null} [invoices] ListInvoiceResponse invoices
         * @property {Long|null} [last_index_offset] ListInvoiceResponse last_index_offset
         * @property {Long|null} [first_index_offset] ListInvoiceResponse first_index_offset
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListInvoiceResponse.
         * @memberof lnrpc
         * @interface IListInvoiceResponse
         * @augments lnrpc.ListInvoiceResponse.$Properties
         * @deprecated Use lnrpc.ListInvoiceResponse.$Properties instead.
         */

        /**
         * Shape of a ListInvoiceResponse.
         * @typedef {lnrpc.ListInvoiceResponse.$Properties} lnrpc.ListInvoiceResponse.$Shape
         */

        /**
         * Constructs a new ListInvoiceResponse.
         * @memberof lnrpc
         * @classdesc Represents a ListInvoiceResponse.
         * @constructor
         * @param {lnrpc.ListInvoiceResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListInvoiceResponse = function (properties) {
            this.invoices = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListInvoiceResponse invoices.
         * @member {Array.<lnrpc.Invoice.$Properties>} invoices
         * @memberof lnrpc.ListInvoiceResponse
         * @instance
         */
        ListInvoiceResponse.prototype.invoices = $util.emptyArray;

        /**
         * ListInvoiceResponse last_index_offset.
         * @member {Long} last_index_offset
         * @memberof lnrpc.ListInvoiceResponse
         * @instance
         */
        ListInvoiceResponse.prototype.last_index_offset = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ListInvoiceResponse first_index_offset.
         * @member {Long} first_index_offset
         * @memberof lnrpc.ListInvoiceResponse
         * @instance
         */
        ListInvoiceResponse.prototype.first_index_offset = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new ListInvoiceResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListInvoiceResponse
         * @static
         * @param {lnrpc.ListInvoiceResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListInvoiceResponse} ListInvoiceResponse instance
         * @type {{
         *   (properties: lnrpc.ListInvoiceResponse.$Shape): lnrpc.ListInvoiceResponse & lnrpc.ListInvoiceResponse.$Shape;
         *   (properties?: lnrpc.ListInvoiceResponse.$Properties): lnrpc.ListInvoiceResponse;
         * }}
         */
        ListInvoiceResponse.create = function(properties) {
            return new ListInvoiceResponse(properties);
        };

        /**
         * Encodes the specified ListInvoiceResponse message. Does not implicitly {@link lnrpc.ListInvoiceResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListInvoiceResponse
         * @static
         * @param {lnrpc.ListInvoiceResponse.$Properties} message ListInvoiceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListInvoiceResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.invoices != null && message.invoices.length)
                for (let i = 0; i < message.invoices.length; ++i)
                    $root.lnrpc.Invoice.encode(message.invoices[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.last_index_offset != null && $Object.hasOwnProperty.call(message, "last_index_offset") && (typeof message.last_index_offset === "object" ? message.last_index_offset.low || message.last_index_offset.high : message.last_index_offset !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.last_index_offset);
            if (message.first_index_offset != null && $Object.hasOwnProperty.call(message, "first_index_offset") && (typeof message.first_index_offset === "object" ? message.first_index_offset.low || message.first_index_offset.high : message.first_index_offset !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.first_index_offset);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListInvoiceResponse message, length delimited. Does not implicitly {@link lnrpc.ListInvoiceResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListInvoiceResponse
         * @static
         * @param {lnrpc.ListInvoiceResponse.$Properties} message ListInvoiceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListInvoiceResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListInvoiceResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListInvoiceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListInvoiceResponse & lnrpc.ListInvoiceResponse.$Shape} ListInvoiceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListInvoiceResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListInvoiceResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.invoices && message.invoices.length))
                            message.invoices = [];
                        message.invoices.push($root.lnrpc.Invoice.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.last_index_offset = value;
                        else
                            delete message.last_index_offset;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.first_index_offset = value;
                        else
                            delete message.first_index_offset;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListInvoiceResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListInvoiceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListInvoiceResponse & lnrpc.ListInvoiceResponse.$Shape} ListInvoiceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListInvoiceResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListInvoiceResponse message.
         * @function verify
         * @memberof lnrpc.ListInvoiceResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListInvoiceResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.invoices != null && $Object.hasOwnProperty.call(message, "invoices")) {
                if (!$Array.isArray(message.invoices))
                    return "invoices: array expected";
                for (let i = 0; i < message.invoices.length; ++i) {
                    let error = $root.lnrpc.Invoice.verify(message.invoices[i], _depth + 1);
                    if (error)
                        return "invoices." + error;
                }
            }
            if (message.last_index_offset != null && $Object.hasOwnProperty.call(message, "last_index_offset"))
                if (!$util.isInteger(message.last_index_offset) && !(message.last_index_offset && $util.isInteger(message.last_index_offset.low) && $util.isInteger(message.last_index_offset.high)))
                    return "last_index_offset: integer|Long expected";
            if (message.first_index_offset != null && $Object.hasOwnProperty.call(message, "first_index_offset"))
                if (!$util.isInteger(message.first_index_offset) && !(message.first_index_offset && $util.isInteger(message.first_index_offset.low) && $util.isInteger(message.first_index_offset.high)))
                    return "first_index_offset: integer|Long expected";
            return null;
        };

        /**
         * Creates a ListInvoiceResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListInvoiceResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListInvoiceResponse} ListInvoiceResponse
         */
        ListInvoiceResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListInvoiceResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListInvoiceResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListInvoiceResponse();
            if (object.invoices) {
                if (!$Array.isArray(object.invoices))
                    throw $TypeError(".lnrpc.ListInvoiceResponse.invoices: array expected");
                message.invoices = $Array(object.invoices.length);
                for (let i = 0; i < object.invoices.length; ++i) {
                    if (!$util.isObject(object.invoices[i]))
                        throw $TypeError(".lnrpc.ListInvoiceResponse.invoices: object expected");
                    message.invoices[i] = $root.lnrpc.Invoice.fromObject(object.invoices[i], _depth + 1);
                }
            }
            if (object.last_index_offset != null)
                if (typeof object.last_index_offset === "object" ? object.last_index_offset.low || object.last_index_offset.high : $Number(object.last_index_offset) !== 0)
                    if ($util.Long)
                        message.last_index_offset = $util.Long.fromValue(object.last_index_offset, true);
                    else if (typeof object.last_index_offset === "string")
                        message.last_index_offset = $parseInt(object.last_index_offset, 10);
                    else if (typeof object.last_index_offset === "number")
                        message.last_index_offset = object.last_index_offset;
                    else if (typeof object.last_index_offset === "object")
                        message.last_index_offset = new $util.LongBits(object.last_index_offset.low >>> 0, object.last_index_offset.high >>> 0).toNumber(true);
            if (object.first_index_offset != null)
                if (typeof object.first_index_offset === "object" ? object.first_index_offset.low || object.first_index_offset.high : $Number(object.first_index_offset) !== 0)
                    if ($util.Long)
                        message.first_index_offset = $util.Long.fromValue(object.first_index_offset, true);
                    else if (typeof object.first_index_offset === "string")
                        message.first_index_offset = $parseInt(object.first_index_offset, 10);
                    else if (typeof object.first_index_offset === "number")
                        message.first_index_offset = object.first_index_offset;
                    else if (typeof object.first_index_offset === "object")
                        message.first_index_offset = new $util.LongBits(object.first_index_offset.low >>> 0, object.first_index_offset.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ListInvoiceResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListInvoiceResponse
         * @static
         * @param {lnrpc.ListInvoiceResponse} message ListInvoiceResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListInvoiceResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.invoices = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.last_index_offset = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.last_index_offset = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.first_index_offset = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.first_index_offset = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.invoices && message.invoices.length) {
                object.invoices = $Array(message.invoices.length);
                for (let j = 0; j < message.invoices.length; ++j)
                    object.invoices[j] = $root.lnrpc.Invoice.toObject(message.invoices[j], options, _depth + 1);
            }
            if (message.last_index_offset != null && $Object.hasOwnProperty.call(message, "last_index_offset"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.last_index_offset = typeof message.last_index_offset === "number" ? $BigInt(message.last_index_offset) : $util.Long.fromBits(message.last_index_offset.low >>> 0, message.last_index_offset.high >>> 0, true).toBigInt();
                else if (typeof message.last_index_offset === "number")
                    object.last_index_offset = options.longs === $String ? $String(message.last_index_offset) : message.last_index_offset;
                else
                    object.last_index_offset = options.longs === $String ? $util.Long.prototype.toString.call(message.last_index_offset) : options.longs === $Number ? new $util.LongBits(message.last_index_offset.low >>> 0, message.last_index_offset.high >>> 0).toNumber(true) : message.last_index_offset;
            if (message.first_index_offset != null && $Object.hasOwnProperty.call(message, "first_index_offset"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.first_index_offset = typeof message.first_index_offset === "number" ? $BigInt(message.first_index_offset) : $util.Long.fromBits(message.first_index_offset.low >>> 0, message.first_index_offset.high >>> 0, true).toBigInt();
                else if (typeof message.first_index_offset === "number")
                    object.first_index_offset = options.longs === $String ? $String(message.first_index_offset) : message.first_index_offset;
                else
                    object.first_index_offset = options.longs === $String ? $util.Long.prototype.toString.call(message.first_index_offset) : options.longs === $Number ? new $util.LongBits(message.first_index_offset.low >>> 0, message.first_index_offset.high >>> 0).toNumber(true) : message.first_index_offset;
            return object;
        };

        /**
         * Converts this ListInvoiceResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ListInvoiceResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListInvoiceResponse.prototype.toJSON = function() {
            return ListInvoiceResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListInvoiceResponse
         * @function getTypeUrl
         * @memberof lnrpc.ListInvoiceResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListInvoiceResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListInvoiceResponse";
        };

        return ListInvoiceResponse;
    })();

    lnrpc.InvoiceSubscription = (function() {

        /**
         * Properties of an InvoiceSubscription.
         * @typedef {Object} lnrpc.InvoiceSubscription.$Properties
         * @property {Long|null} [add_index] InvoiceSubscription add_index
         * @property {Long|null} [settle_index] InvoiceSubscription settle_index
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an InvoiceSubscription.
         * @memberof lnrpc
         * @interface IInvoiceSubscription
         * @augments lnrpc.InvoiceSubscription.$Properties
         * @deprecated Use lnrpc.InvoiceSubscription.$Properties instead.
         */

        /**
         * Shape of an InvoiceSubscription.
         * @typedef {lnrpc.InvoiceSubscription.$Properties} lnrpc.InvoiceSubscription.$Shape
         */

        /**
         * Constructs a new InvoiceSubscription.
         * @memberof lnrpc
         * @classdesc Represents an InvoiceSubscription.
         * @constructor
         * @param {lnrpc.InvoiceSubscription.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const InvoiceSubscription = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InvoiceSubscription add_index.
         * @member {Long} add_index
         * @memberof lnrpc.InvoiceSubscription
         * @instance
         */
        InvoiceSubscription.prototype.add_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * InvoiceSubscription settle_index.
         * @member {Long} settle_index
         * @memberof lnrpc.InvoiceSubscription
         * @instance
         */
        InvoiceSubscription.prototype.settle_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new InvoiceSubscription instance using the specified properties.
         * @function create
         * @memberof lnrpc.InvoiceSubscription
         * @static
         * @param {lnrpc.InvoiceSubscription.$Properties=} [properties] Properties to set
         * @returns {lnrpc.InvoiceSubscription} InvoiceSubscription instance
         * @type {{
         *   (properties: lnrpc.InvoiceSubscription.$Shape): lnrpc.InvoiceSubscription & lnrpc.InvoiceSubscription.$Shape;
         *   (properties?: lnrpc.InvoiceSubscription.$Properties): lnrpc.InvoiceSubscription;
         * }}
         */
        InvoiceSubscription.create = function(properties) {
            return new InvoiceSubscription(properties);
        };

        /**
         * Encodes the specified InvoiceSubscription message. Does not implicitly {@link lnrpc.InvoiceSubscription.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.InvoiceSubscription
         * @static
         * @param {lnrpc.InvoiceSubscription.$Properties} message InvoiceSubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InvoiceSubscription.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index") && (typeof message.add_index === "object" ? message.add_index.low || message.add_index.high : message.add_index !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.add_index);
            if (message.settle_index != null && $Object.hasOwnProperty.call(message, "settle_index") && (typeof message.settle_index === "object" ? message.settle_index.low || message.settle_index.high : message.settle_index !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.settle_index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InvoiceSubscription message, length delimited. Does not implicitly {@link lnrpc.InvoiceSubscription.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.InvoiceSubscription
         * @static
         * @param {lnrpc.InvoiceSubscription.$Properties} message InvoiceSubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InvoiceSubscription.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InvoiceSubscription message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.InvoiceSubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.InvoiceSubscription & lnrpc.InvoiceSubscription.$Shape} InvoiceSubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InvoiceSubscription.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.InvoiceSubscription(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.add_index = value;
                        else
                            delete message.add_index;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.settle_index = value;
                        else
                            delete message.settle_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InvoiceSubscription message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.InvoiceSubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.InvoiceSubscription & lnrpc.InvoiceSubscription.$Shape} InvoiceSubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InvoiceSubscription.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InvoiceSubscription message.
         * @function verify
         * @memberof lnrpc.InvoiceSubscription
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InvoiceSubscription.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index"))
                if (!$util.isInteger(message.add_index) && !(message.add_index && $util.isInteger(message.add_index.low) && $util.isInteger(message.add_index.high)))
                    return "add_index: integer|Long expected";
            if (message.settle_index != null && $Object.hasOwnProperty.call(message, "settle_index"))
                if (!$util.isInteger(message.settle_index) && !(message.settle_index && $util.isInteger(message.settle_index.low) && $util.isInteger(message.settle_index.high)))
                    return "settle_index: integer|Long expected";
            return null;
        };

        /**
         * Creates an InvoiceSubscription message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.InvoiceSubscription
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.InvoiceSubscription} InvoiceSubscription
         */
        InvoiceSubscription.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.InvoiceSubscription)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.InvoiceSubscription: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.InvoiceSubscription();
            if (object.add_index != null)
                if (typeof object.add_index === "object" ? object.add_index.low || object.add_index.high : $Number(object.add_index) !== 0)
                    if ($util.Long)
                        message.add_index = $util.Long.fromValue(object.add_index, true);
                    else if (typeof object.add_index === "string")
                        message.add_index = $parseInt(object.add_index, 10);
                    else if (typeof object.add_index === "number")
                        message.add_index = object.add_index;
                    else if (typeof object.add_index === "object")
                        message.add_index = new $util.LongBits(object.add_index.low >>> 0, object.add_index.high >>> 0).toNumber(true);
            if (object.settle_index != null)
                if (typeof object.settle_index === "object" ? object.settle_index.low || object.settle_index.high : $Number(object.settle_index) !== 0)
                    if ($util.Long)
                        message.settle_index = $util.Long.fromValue(object.settle_index, true);
                    else if (typeof object.settle_index === "string")
                        message.settle_index = $parseInt(object.settle_index, 10);
                    else if (typeof object.settle_index === "number")
                        message.settle_index = object.settle_index;
                    else if (typeof object.settle_index === "object")
                        message.settle_index = new $util.LongBits(object.settle_index.low >>> 0, object.settle_index.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from an InvoiceSubscription message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.InvoiceSubscription
         * @static
         * @param {lnrpc.InvoiceSubscription} message InvoiceSubscription
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InvoiceSubscription.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.add_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.add_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.settle_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.settle_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.add_index = typeof message.add_index === "number" ? $BigInt(message.add_index) : $util.Long.fromBits(message.add_index.low >>> 0, message.add_index.high >>> 0, true).toBigInt();
                else if (typeof message.add_index === "number")
                    object.add_index = options.longs === $String ? $String(message.add_index) : message.add_index;
                else
                    object.add_index = options.longs === $String ? $util.Long.prototype.toString.call(message.add_index) : options.longs === $Number ? new $util.LongBits(message.add_index.low >>> 0, message.add_index.high >>> 0).toNumber(true) : message.add_index;
            if (message.settle_index != null && $Object.hasOwnProperty.call(message, "settle_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.settle_index = typeof message.settle_index === "number" ? $BigInt(message.settle_index) : $util.Long.fromBits(message.settle_index.low >>> 0, message.settle_index.high >>> 0, true).toBigInt();
                else if (typeof message.settle_index === "number")
                    object.settle_index = options.longs === $String ? $String(message.settle_index) : message.settle_index;
                else
                    object.settle_index = options.longs === $String ? $util.Long.prototype.toString.call(message.settle_index) : options.longs === $Number ? new $util.LongBits(message.settle_index.low >>> 0, message.settle_index.high >>> 0).toNumber(true) : message.settle_index;
            return object;
        };

        /**
         * Converts this InvoiceSubscription to JSON.
         * @function toJSON
         * @memberof lnrpc.InvoiceSubscription
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InvoiceSubscription.prototype.toJSON = function() {
            return InvoiceSubscription.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InvoiceSubscription
         * @function getTypeUrl
         * @memberof lnrpc.InvoiceSubscription
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InvoiceSubscription.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.InvoiceSubscription";
        };

        return InvoiceSubscription;
    })();

    lnrpc.DelCanceledInvoiceReq = (function() {

        /**
         * Properties of a DelCanceledInvoiceReq.
         * @typedef {Object} lnrpc.DelCanceledInvoiceReq.$Properties
         * @property {string|null} [invoice_hash] DelCanceledInvoiceReq invoice_hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DelCanceledInvoiceReq.
         * @memberof lnrpc
         * @interface IDelCanceledInvoiceReq
         * @augments lnrpc.DelCanceledInvoiceReq.$Properties
         * @deprecated Use lnrpc.DelCanceledInvoiceReq.$Properties instead.
         */

        /**
         * Shape of a DelCanceledInvoiceReq.
         * @typedef {lnrpc.DelCanceledInvoiceReq.$Properties} lnrpc.DelCanceledInvoiceReq.$Shape
         */

        /**
         * Constructs a new DelCanceledInvoiceReq.
         * @memberof lnrpc
         * @classdesc Represents a DelCanceledInvoiceReq.
         * @constructor
         * @param {lnrpc.DelCanceledInvoiceReq.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DelCanceledInvoiceReq = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DelCanceledInvoiceReq invoice_hash.
         * @member {string} invoice_hash
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @instance
         */
        DelCanceledInvoiceReq.prototype.invoice_hash = "";

        /**
         * Creates a new DelCanceledInvoiceReq instance using the specified properties.
         * @function create
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @static
         * @param {lnrpc.DelCanceledInvoiceReq.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DelCanceledInvoiceReq} DelCanceledInvoiceReq instance
         * @type {{
         *   (properties: lnrpc.DelCanceledInvoiceReq.$Shape): lnrpc.DelCanceledInvoiceReq & lnrpc.DelCanceledInvoiceReq.$Shape;
         *   (properties?: lnrpc.DelCanceledInvoiceReq.$Properties): lnrpc.DelCanceledInvoiceReq;
         * }}
         */
        DelCanceledInvoiceReq.create = function(properties) {
            return new DelCanceledInvoiceReq(properties);
        };

        /**
         * Encodes the specified DelCanceledInvoiceReq message. Does not implicitly {@link lnrpc.DelCanceledInvoiceReq.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @static
         * @param {lnrpc.DelCanceledInvoiceReq.$Properties} message DelCanceledInvoiceReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DelCanceledInvoiceReq.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.invoice_hash != null && $Object.hasOwnProperty.call(message, "invoice_hash") && message.invoice_hash !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.invoice_hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DelCanceledInvoiceReq message, length delimited. Does not implicitly {@link lnrpc.DelCanceledInvoiceReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @static
         * @param {lnrpc.DelCanceledInvoiceReq.$Properties} message DelCanceledInvoiceReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DelCanceledInvoiceReq.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DelCanceledInvoiceReq message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DelCanceledInvoiceReq & lnrpc.DelCanceledInvoiceReq.$Shape} DelCanceledInvoiceReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DelCanceledInvoiceReq.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DelCanceledInvoiceReq(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.invoice_hash = value;
                        else
                            delete message.invoice_hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DelCanceledInvoiceReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DelCanceledInvoiceReq & lnrpc.DelCanceledInvoiceReq.$Shape} DelCanceledInvoiceReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DelCanceledInvoiceReq.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DelCanceledInvoiceReq message.
         * @function verify
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DelCanceledInvoiceReq.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.invoice_hash != null && $Object.hasOwnProperty.call(message, "invoice_hash"))
                if (!$util.isString(message.invoice_hash))
                    return "invoice_hash: string expected";
            return null;
        };

        /**
         * Creates a DelCanceledInvoiceReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DelCanceledInvoiceReq} DelCanceledInvoiceReq
         */
        DelCanceledInvoiceReq.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DelCanceledInvoiceReq)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DelCanceledInvoiceReq: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DelCanceledInvoiceReq();
            if (object.invoice_hash != null)
                if (typeof object.invoice_hash !== "string" || object.invoice_hash.length)
                    message.invoice_hash = $String(object.invoice_hash);
            return message;
        };

        /**
         * Creates a plain object from a DelCanceledInvoiceReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @static
         * @param {lnrpc.DelCanceledInvoiceReq} message DelCanceledInvoiceReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DelCanceledInvoiceReq.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.invoice_hash = "";
            if (message.invoice_hash != null && $Object.hasOwnProperty.call(message, "invoice_hash"))
                object.invoice_hash = message.invoice_hash;
            return object;
        };

        /**
         * Converts this DelCanceledInvoiceReq to JSON.
         * @function toJSON
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DelCanceledInvoiceReq.prototype.toJSON = function() {
            return DelCanceledInvoiceReq.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DelCanceledInvoiceReq
         * @function getTypeUrl
         * @memberof lnrpc.DelCanceledInvoiceReq
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DelCanceledInvoiceReq.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DelCanceledInvoiceReq";
        };

        return DelCanceledInvoiceReq;
    })();

    lnrpc.DelCanceledInvoiceResp = (function() {

        /**
         * Properties of a DelCanceledInvoiceResp.
         * @typedef {Object} lnrpc.DelCanceledInvoiceResp.$Properties
         * @property {string|null} [status] DelCanceledInvoiceResp status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DelCanceledInvoiceResp.
         * @memberof lnrpc
         * @interface IDelCanceledInvoiceResp
         * @augments lnrpc.DelCanceledInvoiceResp.$Properties
         * @deprecated Use lnrpc.DelCanceledInvoiceResp.$Properties instead.
         */

        /**
         * Shape of a DelCanceledInvoiceResp.
         * @typedef {lnrpc.DelCanceledInvoiceResp.$Properties} lnrpc.DelCanceledInvoiceResp.$Shape
         */

        /**
         * Constructs a new DelCanceledInvoiceResp.
         * @memberof lnrpc
         * @classdesc Represents a DelCanceledInvoiceResp.
         * @constructor
         * @param {lnrpc.DelCanceledInvoiceResp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DelCanceledInvoiceResp = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DelCanceledInvoiceResp status.
         * @member {string} status
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @instance
         */
        DelCanceledInvoiceResp.prototype.status = "";

        /**
         * Creates a new DelCanceledInvoiceResp instance using the specified properties.
         * @function create
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @static
         * @param {lnrpc.DelCanceledInvoiceResp.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DelCanceledInvoiceResp} DelCanceledInvoiceResp instance
         * @type {{
         *   (properties: lnrpc.DelCanceledInvoiceResp.$Shape): lnrpc.DelCanceledInvoiceResp & lnrpc.DelCanceledInvoiceResp.$Shape;
         *   (properties?: lnrpc.DelCanceledInvoiceResp.$Properties): lnrpc.DelCanceledInvoiceResp;
         * }}
         */
        DelCanceledInvoiceResp.create = function(properties) {
            return new DelCanceledInvoiceResp(properties);
        };

        /**
         * Encodes the specified DelCanceledInvoiceResp message. Does not implicitly {@link lnrpc.DelCanceledInvoiceResp.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @static
         * @param {lnrpc.DelCanceledInvoiceResp.$Properties} message DelCanceledInvoiceResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DelCanceledInvoiceResp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DelCanceledInvoiceResp message, length delimited. Does not implicitly {@link lnrpc.DelCanceledInvoiceResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @static
         * @param {lnrpc.DelCanceledInvoiceResp.$Properties} message DelCanceledInvoiceResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DelCanceledInvoiceResp.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DelCanceledInvoiceResp message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DelCanceledInvoiceResp & lnrpc.DelCanceledInvoiceResp.$Shape} DelCanceledInvoiceResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DelCanceledInvoiceResp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DelCanceledInvoiceResp(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DelCanceledInvoiceResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DelCanceledInvoiceResp & lnrpc.DelCanceledInvoiceResp.$Shape} DelCanceledInvoiceResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DelCanceledInvoiceResp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DelCanceledInvoiceResp message.
         * @function verify
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DelCanceledInvoiceResp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a DelCanceledInvoiceResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DelCanceledInvoiceResp} DelCanceledInvoiceResp
         */
        DelCanceledInvoiceResp.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DelCanceledInvoiceResp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DelCanceledInvoiceResp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DelCanceledInvoiceResp();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a DelCanceledInvoiceResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @static
         * @param {lnrpc.DelCanceledInvoiceResp} message DelCanceledInvoiceResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DelCanceledInvoiceResp.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this DelCanceledInvoiceResp to JSON.
         * @function toJSON
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DelCanceledInvoiceResp.prototype.toJSON = function() {
            return DelCanceledInvoiceResp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DelCanceledInvoiceResp
         * @function getTypeUrl
         * @memberof lnrpc.DelCanceledInvoiceResp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DelCanceledInvoiceResp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DelCanceledInvoiceResp";
        };

        return DelCanceledInvoiceResp;
    })();

    /**
     * PaymentFailureReason enum.
     * @name lnrpc.PaymentFailureReason
     * @enum {number}
     * @property {number} FAILURE_REASON_NONE=0 FAILURE_REASON_NONE value
     * @property {number} FAILURE_REASON_TIMEOUT=1 FAILURE_REASON_TIMEOUT value
     * @property {number} FAILURE_REASON_NO_ROUTE=2 FAILURE_REASON_NO_ROUTE value
     * @property {number} FAILURE_REASON_ERROR=3 FAILURE_REASON_ERROR value
     * @property {number} FAILURE_REASON_INCORRECT_PAYMENT_DETAILS=4 FAILURE_REASON_INCORRECT_PAYMENT_DETAILS value
     * @property {number} FAILURE_REASON_INSUFFICIENT_BALANCE=5 FAILURE_REASON_INSUFFICIENT_BALANCE value
     * @property {number} FAILURE_REASON_CANCELED=6 FAILURE_REASON_CANCELED value
     */
    lnrpc.PaymentFailureReason = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "FAILURE_REASON_NONE"] = 0;
        values[valuesById[1] = "FAILURE_REASON_TIMEOUT"] = 1;
        values[valuesById[2] = "FAILURE_REASON_NO_ROUTE"] = 2;
        values[valuesById[3] = "FAILURE_REASON_ERROR"] = 3;
        values[valuesById[4] = "FAILURE_REASON_INCORRECT_PAYMENT_DETAILS"] = 4;
        values[valuesById[5] = "FAILURE_REASON_INSUFFICIENT_BALANCE"] = 5;
        values[valuesById[6] = "FAILURE_REASON_CANCELED"] = 6;
        return values;
    })();

    lnrpc.Payment = (function() {

        /**
         * Properties of a Payment.
         * @typedef {Object} lnrpc.Payment.$Properties
         * @property {string|null} [payment_hash] Payment payment_hash
         * @property {Long|null} [value] Payment value
         * @property {Long|null} [creation_date] Payment creation_date
         * @property {Long|null} [fee] Payment fee
         * @property {string|null} [payment_preimage] Payment payment_preimage
         * @property {Long|null} [value_sat] Payment value_sat
         * @property {Long|null} [value_msat] Payment value_msat
         * @property {string|null} [payment_request] Payment payment_request
         * @property {lnrpc.Payment.PaymentStatus|null} [status] Payment status
         * @property {Long|null} [fee_sat] Payment fee_sat
         * @property {Long|null} [fee_msat] Payment fee_msat
         * @property {Long|null} [creation_time_ns] Payment creation_time_ns
         * @property {Array.<lnrpc.HTLCAttempt.$Properties>|null} [htlcs] Payment htlcs
         * @property {Long|null} [payment_index] Payment payment_index
         * @property {lnrpc.PaymentFailureReason|null} [failure_reason] Payment failure_reason
         * @property {Object.<string,Uint8Array>|null} [first_hop_custom_records] Payment first_hop_custom_records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Payment.
         * @memberof lnrpc
         * @interface IPayment
         * @augments lnrpc.Payment.$Properties
         * @deprecated Use lnrpc.Payment.$Properties instead.
         */

        /**
         * Shape of a Payment.
         * @typedef {lnrpc.Payment.$Properties} lnrpc.Payment.$Shape
         */

        /**
         * Constructs a new Payment.
         * @memberof lnrpc
         * @classdesc Represents a Payment.
         * @constructor
         * @param {lnrpc.Payment.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Payment = function (properties) {
            this.htlcs = [];
            this.first_hop_custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Payment payment_hash.
         * @member {string} payment_hash
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.payment_hash = "";

        /**
         * Payment value.
         * @member {Long} value
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Payment creation_date.
         * @member {Long} creation_date
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.creation_date = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Payment fee.
         * @member {Long} fee
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.fee = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Payment payment_preimage.
         * @member {string} payment_preimage
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.payment_preimage = "";

        /**
         * Payment value_sat.
         * @member {Long} value_sat
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.value_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Payment value_msat.
         * @member {Long} value_msat
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.value_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Payment payment_request.
         * @member {string} payment_request
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.payment_request = "";

        /**
         * Payment status.
         * @member {lnrpc.Payment.PaymentStatus} status
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.status = 0;

        /**
         * Payment fee_sat.
         * @member {Long} fee_sat
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.fee_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Payment fee_msat.
         * @member {Long} fee_msat
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.fee_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Payment creation_time_ns.
         * @member {Long} creation_time_ns
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.creation_time_ns = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Payment htlcs.
         * @member {Array.<lnrpc.HTLCAttempt.$Properties>} htlcs
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.htlcs = $util.emptyArray;

        /**
         * Payment payment_index.
         * @member {Long} payment_index
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.payment_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Payment failure_reason.
         * @member {lnrpc.PaymentFailureReason} failure_reason
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.failure_reason = 0;

        /**
         * Payment first_hop_custom_records.
         * @member {Object.<string,Uint8Array>} first_hop_custom_records
         * @memberof lnrpc.Payment
         * @instance
         */
        Payment.prototype.first_hop_custom_records = $util.emptyObject;

        /**
         * Creates a new Payment instance using the specified properties.
         * @function create
         * @memberof lnrpc.Payment
         * @static
         * @param {lnrpc.Payment.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Payment} Payment instance
         * @type {{
         *   (properties: lnrpc.Payment.$Shape): lnrpc.Payment & lnrpc.Payment.$Shape;
         *   (properties?: lnrpc.Payment.$Properties): lnrpc.Payment;
         * }}
         */
        Payment.create = function(properties) {
            return new Payment(properties);
        };

        /**
         * Encodes the specified Payment message. Does not implicitly {@link lnrpc.Payment.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Payment
         * @static
         * @param {lnrpc.Payment.$Properties} message Payment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Payment.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.payment_hash);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.value);
            if (message.creation_date != null && $Object.hasOwnProperty.call(message, "creation_date") && (typeof message.creation_date === "object" ? message.creation_date.low || message.creation_date.high : message.creation_date !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.creation_date);
            if (message.fee != null && $Object.hasOwnProperty.call(message, "fee") && (typeof message.fee === "object" ? message.fee.low || message.fee.high : message.fee !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.fee);
            if (message.payment_preimage != null && $Object.hasOwnProperty.call(message, "payment_preimage") && message.payment_preimage !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.payment_preimage);
            if (message.value_sat != null && $Object.hasOwnProperty.call(message, "value_sat") && (typeof message.value_sat === "object" ? message.value_sat.low || message.value_sat.high : message.value_sat !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.value_sat);
            if (message.value_msat != null && $Object.hasOwnProperty.call(message, "value_msat") && (typeof message.value_msat === "object" ? message.value_msat.low || message.value_msat.high : message.value_msat !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.value_msat);
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request") && message.payment_request !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.payment_request);
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.status);
            if (message.fee_sat != null && $Object.hasOwnProperty.call(message, "fee_sat") && (typeof message.fee_sat === "object" ? message.fee_sat.low || message.fee_sat.high : message.fee_sat !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.fee_sat);
            if (message.fee_msat != null && $Object.hasOwnProperty.call(message, "fee_msat") && (typeof message.fee_msat === "object" ? message.fee_msat.low || message.fee_msat.high : message.fee_msat !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.fee_msat);
            if (message.creation_time_ns != null && $Object.hasOwnProperty.call(message, "creation_time_ns") && (typeof message.creation_time_ns === "object" ? message.creation_time_ns.low || message.creation_time_ns.high : message.creation_time_ns !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.creation_time_ns);
            if (message.htlcs != null && message.htlcs.length)
                for (let i = 0; i < message.htlcs.length; ++i)
                    $root.lnrpc.HTLCAttempt.encode(message.htlcs[i], writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.payment_index != null && $Object.hasOwnProperty.call(message, "payment_index") && (typeof message.payment_index === "object" ? message.payment_index.low || message.payment_index.high : message.payment_index !== 0))
                writer.uint32(/* id 15, wireType 0 =*/120).uint64(message.payment_index);
            if (message.failure_reason != null && $Object.hasOwnProperty.call(message, "failure_reason") && message.failure_reason !== 0)
                writer.uint32(/* id 16, wireType 0 =*/128).int32(message.failure_reason);
            if (message.first_hop_custom_records != null && $Object.hasOwnProperty.call(message, "first_hop_custom_records"))
                for (let keys = $Object.keys(message.first_hop_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 17, wireType 2 =*/138).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.first_hop_custom_records[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Payment message, length delimited. Does not implicitly {@link lnrpc.Payment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Payment
         * @static
         * @param {lnrpc.Payment.$Properties} message Payment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Payment.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Payment message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Payment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Payment & lnrpc.Payment.$Shape} Payment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Payment.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Payment(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.creation_date = value;
                        else
                            delete message.creation_date;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee = value;
                        else
                            delete message.fee;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_preimage = value;
                        else
                            delete message.payment_preimage;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value_sat = value;
                        else
                            delete message.value_sat;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value_msat = value;
                        else
                            delete message.value_msat;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_request = value;
                        else
                            delete message.payment_request;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_sat = value;
                        else
                            delete message.fee_sat;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_msat = value;
                        else
                            delete message.fee_msat;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.creation_time_ns = value;
                        else
                            delete message.creation_time_ns;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.htlcs && message.htlcs.length))
                            message.htlcs = [];
                        message.htlcs.push($root.lnrpc.HTLCAttempt.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.payment_index = value;
                        else
                            delete message.payment_index;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.failure_reason = value;
                        else
                            delete message.failure_reason;
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if (message.first_hop_custom_records === $util.emptyObject)
                            message.first_hop_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.first_hop_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Payment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Payment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Payment & lnrpc.Payment.$Shape} Payment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Payment.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Payment message.
         * @function verify
         * @memberof lnrpc.Payment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Payment.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!$util.isString(message.payment_hash))
                    return "payment_hash: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                    return "value: integer|Long expected";
            if (message.creation_date != null && $Object.hasOwnProperty.call(message, "creation_date"))
                if (!$util.isInteger(message.creation_date) && !(message.creation_date && $util.isInteger(message.creation_date.low) && $util.isInteger(message.creation_date.high)))
                    return "creation_date: integer|Long expected";
            if (message.fee != null && $Object.hasOwnProperty.call(message, "fee"))
                if (!$util.isInteger(message.fee) && !(message.fee && $util.isInteger(message.fee.low) && $util.isInteger(message.fee.high)))
                    return "fee: integer|Long expected";
            if (message.payment_preimage != null && $Object.hasOwnProperty.call(message, "payment_preimage"))
                if (!$util.isString(message.payment_preimage))
                    return "payment_preimage: string expected";
            if (message.value_sat != null && $Object.hasOwnProperty.call(message, "value_sat"))
                if (!$util.isInteger(message.value_sat) && !(message.value_sat && $util.isInteger(message.value_sat.low) && $util.isInteger(message.value_sat.high)))
                    return "value_sat: integer|Long expected";
            if (message.value_msat != null && $Object.hasOwnProperty.call(message, "value_msat"))
                if (!$util.isInteger(message.value_msat) && !(message.value_msat && $util.isInteger(message.value_msat.low) && $util.isInteger(message.value_msat.high)))
                    return "value_msat: integer|Long expected";
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                if (!$util.isString(message.payment_request))
                    return "payment_request: string expected";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                    return "status: enum value expected";
            if (message.fee_sat != null && $Object.hasOwnProperty.call(message, "fee_sat"))
                if (!$util.isInteger(message.fee_sat) && !(message.fee_sat && $util.isInteger(message.fee_sat.low) && $util.isInteger(message.fee_sat.high)))
                    return "fee_sat: integer|Long expected";
            if (message.fee_msat != null && $Object.hasOwnProperty.call(message, "fee_msat"))
                if (!$util.isInteger(message.fee_msat) && !(message.fee_msat && $util.isInteger(message.fee_msat.low) && $util.isInteger(message.fee_msat.high)))
                    return "fee_msat: integer|Long expected";
            if (message.creation_time_ns != null && $Object.hasOwnProperty.call(message, "creation_time_ns"))
                if (!$util.isInteger(message.creation_time_ns) && !(message.creation_time_ns && $util.isInteger(message.creation_time_ns.low) && $util.isInteger(message.creation_time_ns.high)))
                    return "creation_time_ns: integer|Long expected";
            if (message.htlcs != null && $Object.hasOwnProperty.call(message, "htlcs")) {
                if (!$Array.isArray(message.htlcs))
                    return "htlcs: array expected";
                for (let i = 0; i < message.htlcs.length; ++i) {
                    let error = $root.lnrpc.HTLCAttempt.verify(message.htlcs[i], _depth + 1);
                    if (error)
                        return "htlcs." + error;
                }
            }
            if (message.payment_index != null && $Object.hasOwnProperty.call(message, "payment_index"))
                if (!$util.isInteger(message.payment_index) && !(message.payment_index && $util.isInteger(message.payment_index.low) && $util.isInteger(message.payment_index.high)))
                    return "payment_index: integer|Long expected";
            if (message.failure_reason != null && $Object.hasOwnProperty.call(message, "failure_reason"))
                if (typeof message.failure_reason !== "number" || (message.failure_reason | 0) !== message.failure_reason)
                    return "failure_reason: enum value expected";
            if (message.first_hop_custom_records != null && $Object.hasOwnProperty.call(message, "first_hop_custom_records")) {
                if (!$util.isObject(message.first_hop_custom_records))
                    return "first_hop_custom_records: object expected";
                let key = $Object.keys(message.first_hop_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "first_hop_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.first_hop_custom_records[key[i]] && typeof message.first_hop_custom_records[key[i]].length === "number" || $util.isString(message.first_hop_custom_records[key[i]])))
                        return "first_hop_custom_records: buffer{k:uint64} expected";
                }
            }
            return null;
        };

        /**
         * Creates a Payment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Payment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Payment} Payment
         */
        Payment.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Payment)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Payment: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Payment();
            if (object.payment_hash != null)
                if (typeof object.payment_hash !== "string" || object.payment_hash.length)
                    message.payment_hash = $String(object.payment_hash);
            if (object.value != null)
                if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                    if ($util.Long)
                        message.value = $util.Long.fromValue(object.value, false);
                    else if (typeof object.value === "string")
                        message.value = $parseInt(object.value, 10);
                    else if (typeof object.value === "number")
                        message.value = object.value;
                    else if (typeof object.value === "object")
                        message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber();
            if (object.creation_date != null)
                if (typeof object.creation_date === "object" ? object.creation_date.low || object.creation_date.high : $Number(object.creation_date) !== 0)
                    if ($util.Long)
                        message.creation_date = $util.Long.fromValue(object.creation_date, false);
                    else if (typeof object.creation_date === "string")
                        message.creation_date = $parseInt(object.creation_date, 10);
                    else if (typeof object.creation_date === "number")
                        message.creation_date = object.creation_date;
                    else if (typeof object.creation_date === "object")
                        message.creation_date = new $util.LongBits(object.creation_date.low >>> 0, object.creation_date.high >>> 0).toNumber();
            if (object.fee != null)
                if (typeof object.fee === "object" ? object.fee.low || object.fee.high : $Number(object.fee) !== 0)
                    if ($util.Long)
                        message.fee = $util.Long.fromValue(object.fee, false);
                    else if (typeof object.fee === "string")
                        message.fee = $parseInt(object.fee, 10);
                    else if (typeof object.fee === "number")
                        message.fee = object.fee;
                    else if (typeof object.fee === "object")
                        message.fee = new $util.LongBits(object.fee.low >>> 0, object.fee.high >>> 0).toNumber();
            if (object.payment_preimage != null)
                if (typeof object.payment_preimage !== "string" || object.payment_preimage.length)
                    message.payment_preimage = $String(object.payment_preimage);
            if (object.value_sat != null)
                if (typeof object.value_sat === "object" ? object.value_sat.low || object.value_sat.high : $Number(object.value_sat) !== 0)
                    if ($util.Long)
                        message.value_sat = $util.Long.fromValue(object.value_sat, false);
                    else if (typeof object.value_sat === "string")
                        message.value_sat = $parseInt(object.value_sat, 10);
                    else if (typeof object.value_sat === "number")
                        message.value_sat = object.value_sat;
                    else if (typeof object.value_sat === "object")
                        message.value_sat = new $util.LongBits(object.value_sat.low >>> 0, object.value_sat.high >>> 0).toNumber();
            if (object.value_msat != null)
                if (typeof object.value_msat === "object" ? object.value_msat.low || object.value_msat.high : $Number(object.value_msat) !== 0)
                    if ($util.Long)
                        message.value_msat = $util.Long.fromValue(object.value_msat, false);
                    else if (typeof object.value_msat === "string")
                        message.value_msat = $parseInt(object.value_msat, 10);
                    else if (typeof object.value_msat === "number")
                        message.value_msat = object.value_msat;
                    else if (typeof object.value_msat === "object")
                        message.value_msat = new $util.LongBits(object.value_msat.low >>> 0, object.value_msat.high >>> 0).toNumber();
            if (object.payment_request != null)
                if (typeof object.payment_request !== "string" || object.payment_request.length)
                    message.payment_request = $String(object.payment_request);
            if (object.status !== 0 && (typeof object.status !== "string" || $root.lnrpc.Payment.PaymentStatus[object.status] !== 0))
                switch (object.status) {
                case "UNKNOWN":
                case 0:
                    message.status = 0;
                    break;
                case "IN_FLIGHT":
                case 1:
                    message.status = 1;
                    break;
                case "SUCCEEDED":
                case 2:
                    message.status = 2;
                    break;
                case "FAILED":
                case 3:
                    message.status = 3;
                    break;
                case "INITIATED":
                case 4:
                    message.status = 4;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
            if (object.fee_sat != null)
                if (typeof object.fee_sat === "object" ? object.fee_sat.low || object.fee_sat.high : $Number(object.fee_sat) !== 0)
                    if ($util.Long)
                        message.fee_sat = $util.Long.fromValue(object.fee_sat, false);
                    else if (typeof object.fee_sat === "string")
                        message.fee_sat = $parseInt(object.fee_sat, 10);
                    else if (typeof object.fee_sat === "number")
                        message.fee_sat = object.fee_sat;
                    else if (typeof object.fee_sat === "object")
                        message.fee_sat = new $util.LongBits(object.fee_sat.low >>> 0, object.fee_sat.high >>> 0).toNumber();
            if (object.fee_msat != null)
                if (typeof object.fee_msat === "object" ? object.fee_msat.low || object.fee_msat.high : $Number(object.fee_msat) !== 0)
                    if ($util.Long)
                        message.fee_msat = $util.Long.fromValue(object.fee_msat, false);
                    else if (typeof object.fee_msat === "string")
                        message.fee_msat = $parseInt(object.fee_msat, 10);
                    else if (typeof object.fee_msat === "number")
                        message.fee_msat = object.fee_msat;
                    else if (typeof object.fee_msat === "object")
                        message.fee_msat = new $util.LongBits(object.fee_msat.low >>> 0, object.fee_msat.high >>> 0).toNumber();
            if (object.creation_time_ns != null)
                if (typeof object.creation_time_ns === "object" ? object.creation_time_ns.low || object.creation_time_ns.high : $Number(object.creation_time_ns) !== 0)
                    if ($util.Long)
                        message.creation_time_ns = $util.Long.fromValue(object.creation_time_ns, false);
                    else if (typeof object.creation_time_ns === "string")
                        message.creation_time_ns = $parseInt(object.creation_time_ns, 10);
                    else if (typeof object.creation_time_ns === "number")
                        message.creation_time_ns = object.creation_time_ns;
                    else if (typeof object.creation_time_ns === "object")
                        message.creation_time_ns = new $util.LongBits(object.creation_time_ns.low >>> 0, object.creation_time_ns.high >>> 0).toNumber();
            if (object.htlcs) {
                if (!$Array.isArray(object.htlcs))
                    throw $TypeError(".lnrpc.Payment.htlcs: array expected");
                message.htlcs = $Array(object.htlcs.length);
                for (let i = 0; i < object.htlcs.length; ++i) {
                    if (!$util.isObject(object.htlcs[i]))
                        throw $TypeError(".lnrpc.Payment.htlcs: object expected");
                    message.htlcs[i] = $root.lnrpc.HTLCAttempt.fromObject(object.htlcs[i], _depth + 1);
                }
            }
            if (object.payment_index != null)
                if (typeof object.payment_index === "object" ? object.payment_index.low || object.payment_index.high : $Number(object.payment_index) !== 0)
                    if ($util.Long)
                        message.payment_index = $util.Long.fromValue(object.payment_index, true);
                    else if (typeof object.payment_index === "string")
                        message.payment_index = $parseInt(object.payment_index, 10);
                    else if (typeof object.payment_index === "number")
                        message.payment_index = object.payment_index;
                    else if (typeof object.payment_index === "object")
                        message.payment_index = new $util.LongBits(object.payment_index.low >>> 0, object.payment_index.high >>> 0).toNumber(true);
            if (object.failure_reason !== 0 && (typeof object.failure_reason !== "string" || $root.lnrpc.PaymentFailureReason[object.failure_reason] !== 0))
                switch (object.failure_reason) {
                case "FAILURE_REASON_NONE":
                case 0:
                    message.failure_reason = 0;
                    break;
                case "FAILURE_REASON_TIMEOUT":
                case 1:
                    message.failure_reason = 1;
                    break;
                case "FAILURE_REASON_NO_ROUTE":
                case 2:
                    message.failure_reason = 2;
                    break;
                case "FAILURE_REASON_ERROR":
                case 3:
                    message.failure_reason = 3;
                    break;
                case "FAILURE_REASON_INCORRECT_PAYMENT_DETAILS":
                case 4:
                    message.failure_reason = 4;
                    break;
                case "FAILURE_REASON_INSUFFICIENT_BALANCE":
                case 5:
                    message.failure_reason = 5;
                    break;
                case "FAILURE_REASON_CANCELED":
                case 6:
                    message.failure_reason = 6;
                    break;
                default:
                    if (typeof object.failure_reason === "number" && (object.failure_reason | 0) === object.failure_reason)
                        message.failure_reason = object.failure_reason;
                }
            if (object.first_hop_custom_records) {
                if (!$util.isObject(object.first_hop_custom_records))
                    throw $TypeError(".lnrpc.Payment.first_hop_custom_records: object expected");
                message.first_hop_custom_records = {};
                for (let keys = $Object.keys(object.first_hop_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.first_hop_custom_records, keys[i]);
                    if (typeof object.first_hop_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.first_hop_custom_records[keys[i]], message.first_hop_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.first_hop_custom_records[keys[i]])), 0);
                    else if (object.first_hop_custom_records[keys[i]].length >= 0)
                        message.first_hop_custom_records[keys[i]] = object.first_hop_custom_records[keys[i]];
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Payment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Payment
         * @static
         * @param {lnrpc.Payment} message Payment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Payment.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.htlcs = [];
            if (options.objects || options.defaults)
                object.first_hop_custom_records = {};
            if (options.defaults) {
                object.payment_hash = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation_date = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.creation_date = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.payment_preimage = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.payment_request = "";
                object.status = options.enums === $String ? "UNKNOWN" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation_time_ns = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.creation_time_ns = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.payment_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.payment_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.failure_reason = options.enums === $String ? "FAILURE_REASON_NONE" : 0;
            }
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = message.payment_hash;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, false).toBigInt();
                else if (typeof message.value === "number")
                    object.value = options.longs === $String ? $String(message.value) : message.value;
                else
                    object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber() : message.value;
            if (message.creation_date != null && $Object.hasOwnProperty.call(message, "creation_date"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.creation_date = typeof message.creation_date === "number" ? $BigInt(message.creation_date) : $util.Long.fromBits(message.creation_date.low >>> 0, message.creation_date.high >>> 0, false).toBigInt();
                else if (typeof message.creation_date === "number")
                    object.creation_date = options.longs === $String ? $String(message.creation_date) : message.creation_date;
                else
                    object.creation_date = options.longs === $String ? $util.Long.prototype.toString.call(message.creation_date) : options.longs === $Number ? new $util.LongBits(message.creation_date.low >>> 0, message.creation_date.high >>> 0).toNumber() : message.creation_date;
            if (message.fee != null && $Object.hasOwnProperty.call(message, "fee"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee = typeof message.fee === "number" ? $BigInt(message.fee) : $util.Long.fromBits(message.fee.low >>> 0, message.fee.high >>> 0, false).toBigInt();
                else if (typeof message.fee === "number")
                    object.fee = options.longs === $String ? $String(message.fee) : message.fee;
                else
                    object.fee = options.longs === $String ? $util.Long.prototype.toString.call(message.fee) : options.longs === $Number ? new $util.LongBits(message.fee.low >>> 0, message.fee.high >>> 0).toNumber() : message.fee;
            if (message.payment_preimage != null && $Object.hasOwnProperty.call(message, "payment_preimage"))
                object.payment_preimage = message.payment_preimage;
            if (message.value_sat != null && $Object.hasOwnProperty.call(message, "value_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value_sat = typeof message.value_sat === "number" ? $BigInt(message.value_sat) : $util.Long.fromBits(message.value_sat.low >>> 0, message.value_sat.high >>> 0, false).toBigInt();
                else if (typeof message.value_sat === "number")
                    object.value_sat = options.longs === $String ? $String(message.value_sat) : message.value_sat;
                else
                    object.value_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.value_sat) : options.longs === $Number ? new $util.LongBits(message.value_sat.low >>> 0, message.value_sat.high >>> 0).toNumber() : message.value_sat;
            if (message.value_msat != null && $Object.hasOwnProperty.call(message, "value_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value_msat = typeof message.value_msat === "number" ? $BigInt(message.value_msat) : $util.Long.fromBits(message.value_msat.low >>> 0, message.value_msat.high >>> 0, false).toBigInt();
                else if (typeof message.value_msat === "number")
                    object.value_msat = options.longs === $String ? $String(message.value_msat) : message.value_msat;
                else
                    object.value_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.value_msat) : options.longs === $Number ? new $util.LongBits(message.value_msat.low >>> 0, message.value_msat.high >>> 0).toNumber() : message.value_msat;
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                object.payment_request = message.payment_request;
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === $String ? $root.lnrpc.Payment.PaymentStatus[message.status] === $undefined ? message.status : $root.lnrpc.Payment.PaymentStatus[message.status] : message.status;
            if (message.fee_sat != null && $Object.hasOwnProperty.call(message, "fee_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_sat = typeof message.fee_sat === "number" ? $BigInt(message.fee_sat) : $util.Long.fromBits(message.fee_sat.low >>> 0, message.fee_sat.high >>> 0, false).toBigInt();
                else if (typeof message.fee_sat === "number")
                    object.fee_sat = options.longs === $String ? $String(message.fee_sat) : message.fee_sat;
                else
                    object.fee_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_sat) : options.longs === $Number ? new $util.LongBits(message.fee_sat.low >>> 0, message.fee_sat.high >>> 0).toNumber() : message.fee_sat;
            if (message.fee_msat != null && $Object.hasOwnProperty.call(message, "fee_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_msat = typeof message.fee_msat === "number" ? $BigInt(message.fee_msat) : $util.Long.fromBits(message.fee_msat.low >>> 0, message.fee_msat.high >>> 0, false).toBigInt();
                else if (typeof message.fee_msat === "number")
                    object.fee_msat = options.longs === $String ? $String(message.fee_msat) : message.fee_msat;
                else
                    object.fee_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_msat) : options.longs === $Number ? new $util.LongBits(message.fee_msat.low >>> 0, message.fee_msat.high >>> 0).toNumber() : message.fee_msat;
            if (message.creation_time_ns != null && $Object.hasOwnProperty.call(message, "creation_time_ns"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.creation_time_ns = typeof message.creation_time_ns === "number" ? $BigInt(message.creation_time_ns) : $util.Long.fromBits(message.creation_time_ns.low >>> 0, message.creation_time_ns.high >>> 0, false).toBigInt();
                else if (typeof message.creation_time_ns === "number")
                    object.creation_time_ns = options.longs === $String ? $String(message.creation_time_ns) : message.creation_time_ns;
                else
                    object.creation_time_ns = options.longs === $String ? $util.Long.prototype.toString.call(message.creation_time_ns) : options.longs === $Number ? new $util.LongBits(message.creation_time_ns.low >>> 0, message.creation_time_ns.high >>> 0).toNumber() : message.creation_time_ns;
            if (message.htlcs && message.htlcs.length) {
                object.htlcs = $Array(message.htlcs.length);
                for (let j = 0; j < message.htlcs.length; ++j)
                    object.htlcs[j] = $root.lnrpc.HTLCAttempt.toObject(message.htlcs[j], options, _depth + 1);
            }
            if (message.payment_index != null && $Object.hasOwnProperty.call(message, "payment_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.payment_index = typeof message.payment_index === "number" ? $BigInt(message.payment_index) : $util.Long.fromBits(message.payment_index.low >>> 0, message.payment_index.high >>> 0, true).toBigInt();
                else if (typeof message.payment_index === "number")
                    object.payment_index = options.longs === $String ? $String(message.payment_index) : message.payment_index;
                else
                    object.payment_index = options.longs === $String ? $util.Long.prototype.toString.call(message.payment_index) : options.longs === $Number ? new $util.LongBits(message.payment_index.low >>> 0, message.payment_index.high >>> 0).toNumber(true) : message.payment_index;
            if (message.failure_reason != null && $Object.hasOwnProperty.call(message, "failure_reason"))
                object.failure_reason = options.enums === $String ? $root.lnrpc.PaymentFailureReason[message.failure_reason] === $undefined ? message.failure_reason : $root.lnrpc.PaymentFailureReason[message.failure_reason] : message.failure_reason;
            let keys2;
            if (message.first_hop_custom_records && (keys2 = $Object.keys(message.first_hop_custom_records)).length) {
                object.first_hop_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.first_hop_custom_records, keys2[j]);
                    object.first_hop_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.first_hop_custom_records[keys2[j]], 0, message.first_hop_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.first_hop_custom_records[keys2[j]]) : message.first_hop_custom_records[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this Payment to JSON.
         * @function toJSON
         * @memberof lnrpc.Payment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Payment.prototype.toJSON = function() {
            return Payment.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Payment
         * @function getTypeUrl
         * @memberof lnrpc.Payment
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Payment.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Payment";
        };

        /**
         * PaymentStatus enum.
         * @name lnrpc.Payment.PaymentStatus
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} IN_FLIGHT=1 IN_FLIGHT value
         * @property {number} SUCCEEDED=2 SUCCEEDED value
         * @property {number} FAILED=3 FAILED value
         * @property {number} INITIATED=4 INITIATED value
         */
        Payment.PaymentStatus = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "IN_FLIGHT"] = 1;
            values[valuesById[2] = "SUCCEEDED"] = 2;
            values[valuesById[3] = "FAILED"] = 3;
            values[valuesById[4] = "INITIATED"] = 4;
            return values;
        })();

        return Payment;
    })();

    lnrpc.HTLCAttempt = (function() {

        /**
         * Properties of a HTLCAttempt.
         * @typedef {Object} lnrpc.HTLCAttempt.$Properties
         * @property {Long|null} [attempt_id] HTLCAttempt attempt_id
         * @property {lnrpc.HTLCAttempt.HTLCStatus|null} [status] HTLCAttempt status
         * @property {lnrpc.Route.$Properties|null} [route] HTLCAttempt route
         * @property {Long|null} [attempt_time_ns] HTLCAttempt attempt_time_ns
         * @property {Long|null} [resolve_time_ns] HTLCAttempt resolve_time_ns
         * @property {lnrpc.Failure.$Properties|null} [failure] HTLCAttempt failure
         * @property {Uint8Array|null} [preimage] HTLCAttempt preimage
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a HTLCAttempt.
         * @memberof lnrpc
         * @interface IHTLCAttempt
         * @augments lnrpc.HTLCAttempt.$Properties
         * @deprecated Use lnrpc.HTLCAttempt.$Properties instead.
         */

        /**
         * Shape of a HTLCAttempt.
         * @typedef {lnrpc.HTLCAttempt.$Properties} lnrpc.HTLCAttempt.$Shape
         */

        /**
         * Constructs a new HTLCAttempt.
         * @memberof lnrpc
         * @classdesc Represents a HTLCAttempt.
         * @constructor
         * @param {lnrpc.HTLCAttempt.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const HTLCAttempt = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HTLCAttempt attempt_id.
         * @member {Long} attempt_id
         * @memberof lnrpc.HTLCAttempt
         * @instance
         */
        HTLCAttempt.prototype.attempt_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HTLCAttempt status.
         * @member {lnrpc.HTLCAttempt.HTLCStatus} status
         * @memberof lnrpc.HTLCAttempt
         * @instance
         */
        HTLCAttempt.prototype.status = 0;

        /**
         * HTLCAttempt route.
         * @member {lnrpc.Route.$Properties|null|undefined} route
         * @memberof lnrpc.HTLCAttempt
         * @instance
         */
        HTLCAttempt.prototype.route = null;

        /**
         * HTLCAttempt attempt_time_ns.
         * @member {Long} attempt_time_ns
         * @memberof lnrpc.HTLCAttempt
         * @instance
         */
        HTLCAttempt.prototype.attempt_time_ns = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * HTLCAttempt resolve_time_ns.
         * @member {Long} resolve_time_ns
         * @memberof lnrpc.HTLCAttempt
         * @instance
         */
        HTLCAttempt.prototype.resolve_time_ns = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * HTLCAttempt failure.
         * @member {lnrpc.Failure.$Properties|null|undefined} failure
         * @memberof lnrpc.HTLCAttempt
         * @instance
         */
        HTLCAttempt.prototype.failure = null;

        /**
         * HTLCAttempt preimage.
         * @member {Uint8Array} preimage
         * @memberof lnrpc.HTLCAttempt
         * @instance
         */
        HTLCAttempt.prototype.preimage = $util.newBuffer([]);

        /**
         * Creates a new HTLCAttempt instance using the specified properties.
         * @function create
         * @memberof lnrpc.HTLCAttempt
         * @static
         * @param {lnrpc.HTLCAttempt.$Properties=} [properties] Properties to set
         * @returns {lnrpc.HTLCAttempt} HTLCAttempt instance
         * @type {{
         *   (properties: lnrpc.HTLCAttempt.$Shape): lnrpc.HTLCAttempt & lnrpc.HTLCAttempt.$Shape;
         *   (properties?: lnrpc.HTLCAttempt.$Properties): lnrpc.HTLCAttempt;
         * }}
         */
        HTLCAttempt.create = function(properties) {
            return new HTLCAttempt(properties);
        };

        /**
         * Encodes the specified HTLCAttempt message. Does not implicitly {@link lnrpc.HTLCAttempt.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.HTLCAttempt
         * @static
         * @param {lnrpc.HTLCAttempt.$Properties} message HTLCAttempt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HTLCAttempt.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            if (message.route != null && $Object.hasOwnProperty.call(message, "route"))
                $root.lnrpc.Route.encode(message.route, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.attempt_time_ns != null && $Object.hasOwnProperty.call(message, "attempt_time_ns") && (typeof message.attempt_time_ns === "object" ? message.attempt_time_ns.low || message.attempt_time_ns.high : message.attempt_time_ns !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.attempt_time_ns);
            if (message.resolve_time_ns != null && $Object.hasOwnProperty.call(message, "resolve_time_ns") && (typeof message.resolve_time_ns === "object" ? message.resolve_time_ns.low || message.resolve_time_ns.high : message.resolve_time_ns !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.resolve_time_ns);
            if (message.failure != null && $Object.hasOwnProperty.call(message, "failure"))
                $root.lnrpc.Failure.encode(message.failure, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage") && message.preimage.length)
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.preimage);
            if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id") && (typeof message.attempt_id === "object" ? message.attempt_id.low || message.attempt_id.high : message.attempt_id !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.attempt_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HTLCAttempt message, length delimited. Does not implicitly {@link lnrpc.HTLCAttempt.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.HTLCAttempt
         * @static
         * @param {lnrpc.HTLCAttempt.$Properties} message HTLCAttempt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HTLCAttempt.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HTLCAttempt message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.HTLCAttempt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.HTLCAttempt & lnrpc.HTLCAttempt.$Shape} HTLCAttempt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HTLCAttempt.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.HTLCAttempt(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.attempt_id = value;
                        else
                            delete message.attempt_id;
                        continue;
                    }
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.route = $root.lnrpc.Route.decode(reader, reader.uint32(), $undefined, _depth + 1, message.route);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.attempt_time_ns = value;
                        else
                            delete message.attempt_time_ns;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.resolve_time_ns = value;
                        else
                            delete message.resolve_time_ns;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.failure = $root.lnrpc.Failure.decode(reader, reader.uint32(), $undefined, _depth + 1, message.failure);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.preimage = value;
                        else
                            delete message.preimage;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HTLCAttempt message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.HTLCAttempt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.HTLCAttempt & lnrpc.HTLCAttempt.$Shape} HTLCAttempt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HTLCAttempt.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HTLCAttempt message.
         * @function verify
         * @memberof lnrpc.HTLCAttempt
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HTLCAttempt.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id"))
                if (!$util.isInteger(message.attempt_id) && !(message.attempt_id && $util.isInteger(message.attempt_id.low) && $util.isInteger(message.attempt_id.high)))
                    return "attempt_id: integer|Long expected";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                    return "status: enum value expected";
            if (message.route != null && $Object.hasOwnProperty.call(message, "route")) {
                let error = $root.lnrpc.Route.verify(message.route, _depth + 1);
                if (error)
                    return "route." + error;
            }
            if (message.attempt_time_ns != null && $Object.hasOwnProperty.call(message, "attempt_time_ns"))
                if (!$util.isInteger(message.attempt_time_ns) && !(message.attempt_time_ns && $util.isInteger(message.attempt_time_ns.low) && $util.isInteger(message.attempt_time_ns.high)))
                    return "attempt_time_ns: integer|Long expected";
            if (message.resolve_time_ns != null && $Object.hasOwnProperty.call(message, "resolve_time_ns"))
                if (!$util.isInteger(message.resolve_time_ns) && !(message.resolve_time_ns && $util.isInteger(message.resolve_time_ns.low) && $util.isInteger(message.resolve_time_ns.high)))
                    return "resolve_time_ns: integer|Long expected";
            if (message.failure != null && $Object.hasOwnProperty.call(message, "failure")) {
                let error = $root.lnrpc.Failure.verify(message.failure, _depth + 1);
                if (error)
                    return "failure." + error;
            }
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                if (!(message.preimage && typeof message.preimage.length === "number" || $util.isString(message.preimage)))
                    return "preimage: buffer expected";
            return null;
        };

        /**
         * Creates a HTLCAttempt message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.HTLCAttempt
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.HTLCAttempt} HTLCAttempt
         */
        HTLCAttempt.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.HTLCAttempt)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.HTLCAttempt: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.HTLCAttempt();
            if (object.attempt_id != null)
                if (typeof object.attempt_id === "object" ? object.attempt_id.low || object.attempt_id.high : $Number(object.attempt_id) !== 0)
                    if ($util.Long)
                        message.attempt_id = $util.Long.fromValue(object.attempt_id, true);
                    else if (typeof object.attempt_id === "string")
                        message.attempt_id = $parseInt(object.attempt_id, 10);
                    else if (typeof object.attempt_id === "number")
                        message.attempt_id = object.attempt_id;
                    else if (typeof object.attempt_id === "object")
                        message.attempt_id = new $util.LongBits(object.attempt_id.low >>> 0, object.attempt_id.high >>> 0).toNumber(true);
            if (object.status !== 0 && (typeof object.status !== "string" || $root.lnrpc.HTLCAttempt.HTLCStatus[object.status] !== 0))
                switch (object.status) {
                case "IN_FLIGHT":
                case 0:
                    message.status = 0;
                    break;
                case "SUCCEEDED":
                case 1:
                    message.status = 1;
                    break;
                case "FAILED":
                case 2:
                    message.status = 2;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
            if (object.route != null) {
                if (!$util.isObject(object.route))
                    throw $TypeError(".lnrpc.HTLCAttempt.route: object expected");
                message.route = $root.lnrpc.Route.fromObject(object.route, _depth + 1);
            }
            if (object.attempt_time_ns != null)
                if (typeof object.attempt_time_ns === "object" ? object.attempt_time_ns.low || object.attempt_time_ns.high : $Number(object.attempt_time_ns) !== 0)
                    if ($util.Long)
                        message.attempt_time_ns = $util.Long.fromValue(object.attempt_time_ns, false);
                    else if (typeof object.attempt_time_ns === "string")
                        message.attempt_time_ns = $parseInt(object.attempt_time_ns, 10);
                    else if (typeof object.attempt_time_ns === "number")
                        message.attempt_time_ns = object.attempt_time_ns;
                    else if (typeof object.attempt_time_ns === "object")
                        message.attempt_time_ns = new $util.LongBits(object.attempt_time_ns.low >>> 0, object.attempt_time_ns.high >>> 0).toNumber();
            if (object.resolve_time_ns != null)
                if (typeof object.resolve_time_ns === "object" ? object.resolve_time_ns.low || object.resolve_time_ns.high : $Number(object.resolve_time_ns) !== 0)
                    if ($util.Long)
                        message.resolve_time_ns = $util.Long.fromValue(object.resolve_time_ns, false);
                    else if (typeof object.resolve_time_ns === "string")
                        message.resolve_time_ns = $parseInt(object.resolve_time_ns, 10);
                    else if (typeof object.resolve_time_ns === "number")
                        message.resolve_time_ns = object.resolve_time_ns;
                    else if (typeof object.resolve_time_ns === "object")
                        message.resolve_time_ns = new $util.LongBits(object.resolve_time_ns.low >>> 0, object.resolve_time_ns.high >>> 0).toNumber();
            if (object.failure != null) {
                if (!$util.isObject(object.failure))
                    throw $TypeError(".lnrpc.HTLCAttempt.failure: object expected");
                message.failure = $root.lnrpc.Failure.fromObject(object.failure, _depth + 1);
            }
            if (object.preimage != null)
                if (object.preimage.length)
                    if (typeof object.preimage === "string")
                        $util.base64.decode(object.preimage, message.preimage = $util.newBuffer($util.base64.length(object.preimage)), 0);
                    else if (object.preimage.length >= 0)
                        message.preimage = object.preimage;
            return message;
        };

        /**
         * Creates a plain object from a HTLCAttempt message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.HTLCAttempt
         * @static
         * @param {lnrpc.HTLCAttempt} message HTLCAttempt
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HTLCAttempt.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.status = options.enums === $String ? "IN_FLIGHT" : 0;
                object.route = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.attempt_time_ns = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.attempt_time_ns = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.resolve_time_ns = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.resolve_time_ns = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.failure = null;
                if (options.bytes === $String)
                    object.preimage = "";
                else {
                    object.preimage = [];
                    if (options.bytes !== $Array)
                        object.preimage = $util.newBuffer(object.preimage);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.attempt_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.attempt_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === $String ? $root.lnrpc.HTLCAttempt.HTLCStatus[message.status] === $undefined ? message.status : $root.lnrpc.HTLCAttempt.HTLCStatus[message.status] : message.status;
            if (message.route != null && $Object.hasOwnProperty.call(message, "route"))
                object.route = $root.lnrpc.Route.toObject(message.route, options, _depth + 1);
            if (message.attempt_time_ns != null && $Object.hasOwnProperty.call(message, "attempt_time_ns"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.attempt_time_ns = typeof message.attempt_time_ns === "number" ? $BigInt(message.attempt_time_ns) : $util.Long.fromBits(message.attempt_time_ns.low >>> 0, message.attempt_time_ns.high >>> 0, false).toBigInt();
                else if (typeof message.attempt_time_ns === "number")
                    object.attempt_time_ns = options.longs === $String ? $String(message.attempt_time_ns) : message.attempt_time_ns;
                else
                    object.attempt_time_ns = options.longs === $String ? $util.Long.prototype.toString.call(message.attempt_time_ns) : options.longs === $Number ? new $util.LongBits(message.attempt_time_ns.low >>> 0, message.attempt_time_ns.high >>> 0).toNumber() : message.attempt_time_ns;
            if (message.resolve_time_ns != null && $Object.hasOwnProperty.call(message, "resolve_time_ns"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.resolve_time_ns = typeof message.resolve_time_ns === "number" ? $BigInt(message.resolve_time_ns) : $util.Long.fromBits(message.resolve_time_ns.low >>> 0, message.resolve_time_ns.high >>> 0, false).toBigInt();
                else if (typeof message.resolve_time_ns === "number")
                    object.resolve_time_ns = options.longs === $String ? $String(message.resolve_time_ns) : message.resolve_time_ns;
                else
                    object.resolve_time_ns = options.longs === $String ? $util.Long.prototype.toString.call(message.resolve_time_ns) : options.longs === $Number ? new $util.LongBits(message.resolve_time_ns.low >>> 0, message.resolve_time_ns.high >>> 0).toNumber() : message.resolve_time_ns;
            if (message.failure != null && $Object.hasOwnProperty.call(message, "failure"))
                object.failure = $root.lnrpc.Failure.toObject(message.failure, options, _depth + 1);
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                object.preimage = options.bytes === $String ? $util.base64.encode(message.preimage, 0, message.preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.preimage) : message.preimage;
            if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.attempt_id = typeof message.attempt_id === "number" ? $BigInt(message.attempt_id) : $util.Long.fromBits(message.attempt_id.low >>> 0, message.attempt_id.high >>> 0, true).toBigInt();
                else if (typeof message.attempt_id === "number")
                    object.attempt_id = options.longs === $String ? $String(message.attempt_id) : message.attempt_id;
                else
                    object.attempt_id = options.longs === $String ? $util.Long.prototype.toString.call(message.attempt_id) : options.longs === $Number ? new $util.LongBits(message.attempt_id.low >>> 0, message.attempt_id.high >>> 0).toNumber(true) : message.attempt_id;
            return object;
        };

        /**
         * Converts this HTLCAttempt to JSON.
         * @function toJSON
         * @memberof lnrpc.HTLCAttempt
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HTLCAttempt.prototype.toJSON = function() {
            return HTLCAttempt.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HTLCAttempt
         * @function getTypeUrl
         * @memberof lnrpc.HTLCAttempt
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HTLCAttempt.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.HTLCAttempt";
        };

        /**
         * HTLCStatus enum.
         * @name lnrpc.HTLCAttempt.HTLCStatus
         * @enum {number}
         * @property {number} IN_FLIGHT=0 IN_FLIGHT value
         * @property {number} SUCCEEDED=1 SUCCEEDED value
         * @property {number} FAILED=2 FAILED value
         */
        HTLCAttempt.HTLCStatus = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "IN_FLIGHT"] = 0;
            values[valuesById[1] = "SUCCEEDED"] = 1;
            values[valuesById[2] = "FAILED"] = 2;
            return values;
        })();

        return HTLCAttempt;
    })();

    lnrpc.ListPaymentsRequest = (function() {

        /**
         * Properties of a ListPaymentsRequest.
         * @typedef {Object} lnrpc.ListPaymentsRequest.$Properties
         * @property {boolean|null} [include_incomplete] ListPaymentsRequest include_incomplete
         * @property {Long|null} [index_offset] ListPaymentsRequest index_offset
         * @property {Long|null} [max_payments] ListPaymentsRequest max_payments
         * @property {boolean|null} [reversed] ListPaymentsRequest reversed
         * @property {boolean|null} [count_total_payments] ListPaymentsRequest count_total_payments
         * @property {Long|null} [creation_date_start] ListPaymentsRequest creation_date_start
         * @property {Long|null} [creation_date_end] ListPaymentsRequest creation_date_end
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListPaymentsRequest.
         * @memberof lnrpc
         * @interface IListPaymentsRequest
         * @augments lnrpc.ListPaymentsRequest.$Properties
         * @deprecated Use lnrpc.ListPaymentsRequest.$Properties instead.
         */

        /**
         * Shape of a ListPaymentsRequest.
         * @typedef {lnrpc.ListPaymentsRequest.$Properties} lnrpc.ListPaymentsRequest.$Shape
         */

        /**
         * Constructs a new ListPaymentsRequest.
         * @memberof lnrpc
         * @classdesc Represents a ListPaymentsRequest.
         * @constructor
         * @param {lnrpc.ListPaymentsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListPaymentsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListPaymentsRequest include_incomplete.
         * @member {boolean} include_incomplete
         * @memberof lnrpc.ListPaymentsRequest
         * @instance
         */
        ListPaymentsRequest.prototype.include_incomplete = false;

        /**
         * ListPaymentsRequest index_offset.
         * @member {Long} index_offset
         * @memberof lnrpc.ListPaymentsRequest
         * @instance
         */
        ListPaymentsRequest.prototype.index_offset = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ListPaymentsRequest max_payments.
         * @member {Long} max_payments
         * @memberof lnrpc.ListPaymentsRequest
         * @instance
         */
        ListPaymentsRequest.prototype.max_payments = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ListPaymentsRequest reversed.
         * @member {boolean} reversed
         * @memberof lnrpc.ListPaymentsRequest
         * @instance
         */
        ListPaymentsRequest.prototype.reversed = false;

        /**
         * ListPaymentsRequest count_total_payments.
         * @member {boolean} count_total_payments
         * @memberof lnrpc.ListPaymentsRequest
         * @instance
         */
        ListPaymentsRequest.prototype.count_total_payments = false;

        /**
         * ListPaymentsRequest creation_date_start.
         * @member {Long} creation_date_start
         * @memberof lnrpc.ListPaymentsRequest
         * @instance
         */
        ListPaymentsRequest.prototype.creation_date_start = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ListPaymentsRequest creation_date_end.
         * @member {Long} creation_date_end
         * @memberof lnrpc.ListPaymentsRequest
         * @instance
         */
        ListPaymentsRequest.prototype.creation_date_end = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new ListPaymentsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListPaymentsRequest
         * @static
         * @param {lnrpc.ListPaymentsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListPaymentsRequest} ListPaymentsRequest instance
         * @type {{
         *   (properties: lnrpc.ListPaymentsRequest.$Shape): lnrpc.ListPaymentsRequest & lnrpc.ListPaymentsRequest.$Shape;
         *   (properties?: lnrpc.ListPaymentsRequest.$Properties): lnrpc.ListPaymentsRequest;
         * }}
         */
        ListPaymentsRequest.create = function(properties) {
            return new ListPaymentsRequest(properties);
        };

        /**
         * Encodes the specified ListPaymentsRequest message. Does not implicitly {@link lnrpc.ListPaymentsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListPaymentsRequest
         * @static
         * @param {lnrpc.ListPaymentsRequest.$Properties} message ListPaymentsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPaymentsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.include_incomplete != null && $Object.hasOwnProperty.call(message, "include_incomplete") && message.include_incomplete !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.include_incomplete);
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset") && (typeof message.index_offset === "object" ? message.index_offset.low || message.index_offset.high : message.index_offset !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.index_offset);
            if (message.max_payments != null && $Object.hasOwnProperty.call(message, "max_payments") && (typeof message.max_payments === "object" ? message.max_payments.low || message.max_payments.high : message.max_payments !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.max_payments);
            if (message.reversed != null && $Object.hasOwnProperty.call(message, "reversed") && message.reversed !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.reversed);
            if (message.count_total_payments != null && $Object.hasOwnProperty.call(message, "count_total_payments") && message.count_total_payments !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.count_total_payments);
            if (message.creation_date_start != null && $Object.hasOwnProperty.call(message, "creation_date_start") && (typeof message.creation_date_start === "object" ? message.creation_date_start.low || message.creation_date_start.high : message.creation_date_start !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.creation_date_start);
            if (message.creation_date_end != null && $Object.hasOwnProperty.call(message, "creation_date_end") && (typeof message.creation_date_end === "object" ? message.creation_date_end.low || message.creation_date_end.high : message.creation_date_end !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.creation_date_end);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListPaymentsRequest message, length delimited. Does not implicitly {@link lnrpc.ListPaymentsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListPaymentsRequest
         * @static
         * @param {lnrpc.ListPaymentsRequest.$Properties} message ListPaymentsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPaymentsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListPaymentsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListPaymentsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListPaymentsRequest & lnrpc.ListPaymentsRequest.$Shape} ListPaymentsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPaymentsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListPaymentsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_incomplete = value;
                        else
                            delete message.include_incomplete;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.index_offset = value;
                        else
                            delete message.index_offset;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_payments = value;
                        else
                            delete message.max_payments;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.reversed = value;
                        else
                            delete message.reversed;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.count_total_payments = value;
                        else
                            delete message.count_total_payments;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.creation_date_start = value;
                        else
                            delete message.creation_date_start;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.creation_date_end = value;
                        else
                            delete message.creation_date_end;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListPaymentsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListPaymentsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListPaymentsRequest & lnrpc.ListPaymentsRequest.$Shape} ListPaymentsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPaymentsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListPaymentsRequest message.
         * @function verify
         * @memberof lnrpc.ListPaymentsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListPaymentsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.include_incomplete != null && $Object.hasOwnProperty.call(message, "include_incomplete"))
                if (typeof message.include_incomplete !== "boolean")
                    return "include_incomplete: boolean expected";
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset"))
                if (!$util.isInteger(message.index_offset) && !(message.index_offset && $util.isInteger(message.index_offset.low) && $util.isInteger(message.index_offset.high)))
                    return "index_offset: integer|Long expected";
            if (message.max_payments != null && $Object.hasOwnProperty.call(message, "max_payments"))
                if (!$util.isInteger(message.max_payments) && !(message.max_payments && $util.isInteger(message.max_payments.low) && $util.isInteger(message.max_payments.high)))
                    return "max_payments: integer|Long expected";
            if (message.reversed != null && $Object.hasOwnProperty.call(message, "reversed"))
                if (typeof message.reversed !== "boolean")
                    return "reversed: boolean expected";
            if (message.count_total_payments != null && $Object.hasOwnProperty.call(message, "count_total_payments"))
                if (typeof message.count_total_payments !== "boolean")
                    return "count_total_payments: boolean expected";
            if (message.creation_date_start != null && $Object.hasOwnProperty.call(message, "creation_date_start"))
                if (!$util.isInteger(message.creation_date_start) && !(message.creation_date_start && $util.isInteger(message.creation_date_start.low) && $util.isInteger(message.creation_date_start.high)))
                    return "creation_date_start: integer|Long expected";
            if (message.creation_date_end != null && $Object.hasOwnProperty.call(message, "creation_date_end"))
                if (!$util.isInteger(message.creation_date_end) && !(message.creation_date_end && $util.isInteger(message.creation_date_end.low) && $util.isInteger(message.creation_date_end.high)))
                    return "creation_date_end: integer|Long expected";
            return null;
        };

        /**
         * Creates a ListPaymentsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListPaymentsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListPaymentsRequest} ListPaymentsRequest
         */
        ListPaymentsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListPaymentsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListPaymentsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListPaymentsRequest();
            if (object.include_incomplete != null)
                if (object.include_incomplete)
                    message.include_incomplete = $Boolean(object.include_incomplete);
            if (object.index_offset != null)
                if (typeof object.index_offset === "object" ? object.index_offset.low || object.index_offset.high : $Number(object.index_offset) !== 0)
                    if ($util.Long)
                        message.index_offset = $util.Long.fromValue(object.index_offset, true);
                    else if (typeof object.index_offset === "string")
                        message.index_offset = $parseInt(object.index_offset, 10);
                    else if (typeof object.index_offset === "number")
                        message.index_offset = object.index_offset;
                    else if (typeof object.index_offset === "object")
                        message.index_offset = new $util.LongBits(object.index_offset.low >>> 0, object.index_offset.high >>> 0).toNumber(true);
            if (object.max_payments != null)
                if (typeof object.max_payments === "object" ? object.max_payments.low || object.max_payments.high : $Number(object.max_payments) !== 0)
                    if ($util.Long)
                        message.max_payments = $util.Long.fromValue(object.max_payments, true);
                    else if (typeof object.max_payments === "string")
                        message.max_payments = $parseInt(object.max_payments, 10);
                    else if (typeof object.max_payments === "number")
                        message.max_payments = object.max_payments;
                    else if (typeof object.max_payments === "object")
                        message.max_payments = new $util.LongBits(object.max_payments.low >>> 0, object.max_payments.high >>> 0).toNumber(true);
            if (object.reversed != null)
                if (object.reversed)
                    message.reversed = $Boolean(object.reversed);
            if (object.count_total_payments != null)
                if (object.count_total_payments)
                    message.count_total_payments = $Boolean(object.count_total_payments);
            if (object.creation_date_start != null)
                if (typeof object.creation_date_start === "object" ? object.creation_date_start.low || object.creation_date_start.high : $Number(object.creation_date_start) !== 0)
                    if ($util.Long)
                        message.creation_date_start = $util.Long.fromValue(object.creation_date_start, true);
                    else if (typeof object.creation_date_start === "string")
                        message.creation_date_start = $parseInt(object.creation_date_start, 10);
                    else if (typeof object.creation_date_start === "number")
                        message.creation_date_start = object.creation_date_start;
                    else if (typeof object.creation_date_start === "object")
                        message.creation_date_start = new $util.LongBits(object.creation_date_start.low >>> 0, object.creation_date_start.high >>> 0).toNumber(true);
            if (object.creation_date_end != null)
                if (typeof object.creation_date_end === "object" ? object.creation_date_end.low || object.creation_date_end.high : $Number(object.creation_date_end) !== 0)
                    if ($util.Long)
                        message.creation_date_end = $util.Long.fromValue(object.creation_date_end, true);
                    else if (typeof object.creation_date_end === "string")
                        message.creation_date_end = $parseInt(object.creation_date_end, 10);
                    else if (typeof object.creation_date_end === "number")
                        message.creation_date_end = object.creation_date_end;
                    else if (typeof object.creation_date_end === "object")
                        message.creation_date_end = new $util.LongBits(object.creation_date_end.low >>> 0, object.creation_date_end.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ListPaymentsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListPaymentsRequest
         * @static
         * @param {lnrpc.ListPaymentsRequest} message ListPaymentsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListPaymentsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.include_incomplete = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.index_offset = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.index_offset = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.max_payments = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.max_payments = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.reversed = false;
                object.count_total_payments = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.creation_date_start = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.creation_date_start = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.creation_date_end = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.creation_date_end = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.include_incomplete != null && $Object.hasOwnProperty.call(message, "include_incomplete"))
                object.include_incomplete = message.include_incomplete;
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.index_offset = typeof message.index_offset === "number" ? $BigInt(message.index_offset) : $util.Long.fromBits(message.index_offset.low >>> 0, message.index_offset.high >>> 0, true).toBigInt();
                else if (typeof message.index_offset === "number")
                    object.index_offset = options.longs === $String ? $String(message.index_offset) : message.index_offset;
                else
                    object.index_offset = options.longs === $String ? $util.Long.prototype.toString.call(message.index_offset) : options.longs === $Number ? new $util.LongBits(message.index_offset.low >>> 0, message.index_offset.high >>> 0).toNumber(true) : message.index_offset;
            if (message.max_payments != null && $Object.hasOwnProperty.call(message, "max_payments"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.max_payments = typeof message.max_payments === "number" ? $BigInt(message.max_payments) : $util.Long.fromBits(message.max_payments.low >>> 0, message.max_payments.high >>> 0, true).toBigInt();
                else if (typeof message.max_payments === "number")
                    object.max_payments = options.longs === $String ? $String(message.max_payments) : message.max_payments;
                else
                    object.max_payments = options.longs === $String ? $util.Long.prototype.toString.call(message.max_payments) : options.longs === $Number ? new $util.LongBits(message.max_payments.low >>> 0, message.max_payments.high >>> 0).toNumber(true) : message.max_payments;
            if (message.reversed != null && $Object.hasOwnProperty.call(message, "reversed"))
                object.reversed = message.reversed;
            if (message.count_total_payments != null && $Object.hasOwnProperty.call(message, "count_total_payments"))
                object.count_total_payments = message.count_total_payments;
            if (message.creation_date_start != null && $Object.hasOwnProperty.call(message, "creation_date_start"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.creation_date_start = typeof message.creation_date_start === "number" ? $BigInt(message.creation_date_start) : $util.Long.fromBits(message.creation_date_start.low >>> 0, message.creation_date_start.high >>> 0, true).toBigInt();
                else if (typeof message.creation_date_start === "number")
                    object.creation_date_start = options.longs === $String ? $String(message.creation_date_start) : message.creation_date_start;
                else
                    object.creation_date_start = options.longs === $String ? $util.Long.prototype.toString.call(message.creation_date_start) : options.longs === $Number ? new $util.LongBits(message.creation_date_start.low >>> 0, message.creation_date_start.high >>> 0).toNumber(true) : message.creation_date_start;
            if (message.creation_date_end != null && $Object.hasOwnProperty.call(message, "creation_date_end"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.creation_date_end = typeof message.creation_date_end === "number" ? $BigInt(message.creation_date_end) : $util.Long.fromBits(message.creation_date_end.low >>> 0, message.creation_date_end.high >>> 0, true).toBigInt();
                else if (typeof message.creation_date_end === "number")
                    object.creation_date_end = options.longs === $String ? $String(message.creation_date_end) : message.creation_date_end;
                else
                    object.creation_date_end = options.longs === $String ? $util.Long.prototype.toString.call(message.creation_date_end) : options.longs === $Number ? new $util.LongBits(message.creation_date_end.low >>> 0, message.creation_date_end.high >>> 0).toNumber(true) : message.creation_date_end;
            return object;
        };

        /**
         * Converts this ListPaymentsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ListPaymentsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListPaymentsRequest.prototype.toJSON = function() {
            return ListPaymentsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListPaymentsRequest
         * @function getTypeUrl
         * @memberof lnrpc.ListPaymentsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListPaymentsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListPaymentsRequest";
        };

        return ListPaymentsRequest;
    })();

    lnrpc.ListPaymentsResponse = (function() {

        /**
         * Properties of a ListPaymentsResponse.
         * @typedef {Object} lnrpc.ListPaymentsResponse.$Properties
         * @property {Array.<lnrpc.Payment.$Properties>|null} [payments] ListPaymentsResponse payments
         * @property {Long|null} [first_index_offset] ListPaymentsResponse first_index_offset
         * @property {Long|null} [last_index_offset] ListPaymentsResponse last_index_offset
         * @property {Long|null} [total_num_payments] ListPaymentsResponse total_num_payments
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListPaymentsResponse.
         * @memberof lnrpc
         * @interface IListPaymentsResponse
         * @augments lnrpc.ListPaymentsResponse.$Properties
         * @deprecated Use lnrpc.ListPaymentsResponse.$Properties instead.
         */

        /**
         * Shape of a ListPaymentsResponse.
         * @typedef {lnrpc.ListPaymentsResponse.$Properties} lnrpc.ListPaymentsResponse.$Shape
         */

        /**
         * Constructs a new ListPaymentsResponse.
         * @memberof lnrpc
         * @classdesc Represents a ListPaymentsResponse.
         * @constructor
         * @param {lnrpc.ListPaymentsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListPaymentsResponse = function (properties) {
            this.payments = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListPaymentsResponse payments.
         * @member {Array.<lnrpc.Payment.$Properties>} payments
         * @memberof lnrpc.ListPaymentsResponse
         * @instance
         */
        ListPaymentsResponse.prototype.payments = $util.emptyArray;

        /**
         * ListPaymentsResponse first_index_offset.
         * @member {Long} first_index_offset
         * @memberof lnrpc.ListPaymentsResponse
         * @instance
         */
        ListPaymentsResponse.prototype.first_index_offset = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ListPaymentsResponse last_index_offset.
         * @member {Long} last_index_offset
         * @memberof lnrpc.ListPaymentsResponse
         * @instance
         */
        ListPaymentsResponse.prototype.last_index_offset = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ListPaymentsResponse total_num_payments.
         * @member {Long} total_num_payments
         * @memberof lnrpc.ListPaymentsResponse
         * @instance
         */
        ListPaymentsResponse.prototype.total_num_payments = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new ListPaymentsResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListPaymentsResponse
         * @static
         * @param {lnrpc.ListPaymentsResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListPaymentsResponse} ListPaymentsResponse instance
         * @type {{
         *   (properties: lnrpc.ListPaymentsResponse.$Shape): lnrpc.ListPaymentsResponse & lnrpc.ListPaymentsResponse.$Shape;
         *   (properties?: lnrpc.ListPaymentsResponse.$Properties): lnrpc.ListPaymentsResponse;
         * }}
         */
        ListPaymentsResponse.create = function(properties) {
            return new ListPaymentsResponse(properties);
        };

        /**
         * Encodes the specified ListPaymentsResponse message. Does not implicitly {@link lnrpc.ListPaymentsResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListPaymentsResponse
         * @static
         * @param {lnrpc.ListPaymentsResponse.$Properties} message ListPaymentsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPaymentsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payments != null && message.payments.length)
                for (let i = 0; i < message.payments.length; ++i)
                    $root.lnrpc.Payment.encode(message.payments[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.first_index_offset != null && $Object.hasOwnProperty.call(message, "first_index_offset") && (typeof message.first_index_offset === "object" ? message.first_index_offset.low || message.first_index_offset.high : message.first_index_offset !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.first_index_offset);
            if (message.last_index_offset != null && $Object.hasOwnProperty.call(message, "last_index_offset") && (typeof message.last_index_offset === "object" ? message.last_index_offset.low || message.last_index_offset.high : message.last_index_offset !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.last_index_offset);
            if (message.total_num_payments != null && $Object.hasOwnProperty.call(message, "total_num_payments") && (typeof message.total_num_payments === "object" ? message.total_num_payments.low || message.total_num_payments.high : message.total_num_payments !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.total_num_payments);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListPaymentsResponse message, length delimited. Does not implicitly {@link lnrpc.ListPaymentsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListPaymentsResponse
         * @static
         * @param {lnrpc.ListPaymentsResponse.$Properties} message ListPaymentsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPaymentsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListPaymentsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListPaymentsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListPaymentsResponse & lnrpc.ListPaymentsResponse.$Shape} ListPaymentsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPaymentsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListPaymentsResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.payments && message.payments.length))
                            message.payments = [];
                        message.payments.push($root.lnrpc.Payment.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.first_index_offset = value;
                        else
                            delete message.first_index_offset;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.last_index_offset = value;
                        else
                            delete message.last_index_offset;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_num_payments = value;
                        else
                            delete message.total_num_payments;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListPaymentsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListPaymentsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListPaymentsResponse & lnrpc.ListPaymentsResponse.$Shape} ListPaymentsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPaymentsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListPaymentsResponse message.
         * @function verify
         * @memberof lnrpc.ListPaymentsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListPaymentsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payments != null && $Object.hasOwnProperty.call(message, "payments")) {
                if (!$Array.isArray(message.payments))
                    return "payments: array expected";
                for (let i = 0; i < message.payments.length; ++i) {
                    let error = $root.lnrpc.Payment.verify(message.payments[i], _depth + 1);
                    if (error)
                        return "payments." + error;
                }
            }
            if (message.first_index_offset != null && $Object.hasOwnProperty.call(message, "first_index_offset"))
                if (!$util.isInteger(message.first_index_offset) && !(message.first_index_offset && $util.isInteger(message.first_index_offset.low) && $util.isInteger(message.first_index_offset.high)))
                    return "first_index_offset: integer|Long expected";
            if (message.last_index_offset != null && $Object.hasOwnProperty.call(message, "last_index_offset"))
                if (!$util.isInteger(message.last_index_offset) && !(message.last_index_offset && $util.isInteger(message.last_index_offset.low) && $util.isInteger(message.last_index_offset.high)))
                    return "last_index_offset: integer|Long expected";
            if (message.total_num_payments != null && $Object.hasOwnProperty.call(message, "total_num_payments"))
                if (!$util.isInteger(message.total_num_payments) && !(message.total_num_payments && $util.isInteger(message.total_num_payments.low) && $util.isInteger(message.total_num_payments.high)))
                    return "total_num_payments: integer|Long expected";
            return null;
        };

        /**
         * Creates a ListPaymentsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListPaymentsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListPaymentsResponse} ListPaymentsResponse
         */
        ListPaymentsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListPaymentsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListPaymentsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListPaymentsResponse();
            if (object.payments) {
                if (!$Array.isArray(object.payments))
                    throw $TypeError(".lnrpc.ListPaymentsResponse.payments: array expected");
                message.payments = $Array(object.payments.length);
                for (let i = 0; i < object.payments.length; ++i) {
                    if (!$util.isObject(object.payments[i]))
                        throw $TypeError(".lnrpc.ListPaymentsResponse.payments: object expected");
                    message.payments[i] = $root.lnrpc.Payment.fromObject(object.payments[i], _depth + 1);
                }
            }
            if (object.first_index_offset != null)
                if (typeof object.first_index_offset === "object" ? object.first_index_offset.low || object.first_index_offset.high : $Number(object.first_index_offset) !== 0)
                    if ($util.Long)
                        message.first_index_offset = $util.Long.fromValue(object.first_index_offset, true);
                    else if (typeof object.first_index_offset === "string")
                        message.first_index_offset = $parseInt(object.first_index_offset, 10);
                    else if (typeof object.first_index_offset === "number")
                        message.first_index_offset = object.first_index_offset;
                    else if (typeof object.first_index_offset === "object")
                        message.first_index_offset = new $util.LongBits(object.first_index_offset.low >>> 0, object.first_index_offset.high >>> 0).toNumber(true);
            if (object.last_index_offset != null)
                if (typeof object.last_index_offset === "object" ? object.last_index_offset.low || object.last_index_offset.high : $Number(object.last_index_offset) !== 0)
                    if ($util.Long)
                        message.last_index_offset = $util.Long.fromValue(object.last_index_offset, true);
                    else if (typeof object.last_index_offset === "string")
                        message.last_index_offset = $parseInt(object.last_index_offset, 10);
                    else if (typeof object.last_index_offset === "number")
                        message.last_index_offset = object.last_index_offset;
                    else if (typeof object.last_index_offset === "object")
                        message.last_index_offset = new $util.LongBits(object.last_index_offset.low >>> 0, object.last_index_offset.high >>> 0).toNumber(true);
            if (object.total_num_payments != null)
                if (typeof object.total_num_payments === "object" ? object.total_num_payments.low || object.total_num_payments.high : $Number(object.total_num_payments) !== 0)
                    if ($util.Long)
                        message.total_num_payments = $util.Long.fromValue(object.total_num_payments, true);
                    else if (typeof object.total_num_payments === "string")
                        message.total_num_payments = $parseInt(object.total_num_payments, 10);
                    else if (typeof object.total_num_payments === "number")
                        message.total_num_payments = object.total_num_payments;
                    else if (typeof object.total_num_payments === "object")
                        message.total_num_payments = new $util.LongBits(object.total_num_payments.low >>> 0, object.total_num_payments.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ListPaymentsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListPaymentsResponse
         * @static
         * @param {lnrpc.ListPaymentsResponse} message ListPaymentsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListPaymentsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.payments = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.first_index_offset = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.first_index_offset = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.last_index_offset = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.last_index_offset = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.total_num_payments = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.total_num_payments = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.payments && message.payments.length) {
                object.payments = $Array(message.payments.length);
                for (let j = 0; j < message.payments.length; ++j)
                    object.payments[j] = $root.lnrpc.Payment.toObject(message.payments[j], options, _depth + 1);
            }
            if (message.first_index_offset != null && $Object.hasOwnProperty.call(message, "first_index_offset"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.first_index_offset = typeof message.first_index_offset === "number" ? $BigInt(message.first_index_offset) : $util.Long.fromBits(message.first_index_offset.low >>> 0, message.first_index_offset.high >>> 0, true).toBigInt();
                else if (typeof message.first_index_offset === "number")
                    object.first_index_offset = options.longs === $String ? $String(message.first_index_offset) : message.first_index_offset;
                else
                    object.first_index_offset = options.longs === $String ? $util.Long.prototype.toString.call(message.first_index_offset) : options.longs === $Number ? new $util.LongBits(message.first_index_offset.low >>> 0, message.first_index_offset.high >>> 0).toNumber(true) : message.first_index_offset;
            if (message.last_index_offset != null && $Object.hasOwnProperty.call(message, "last_index_offset"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.last_index_offset = typeof message.last_index_offset === "number" ? $BigInt(message.last_index_offset) : $util.Long.fromBits(message.last_index_offset.low >>> 0, message.last_index_offset.high >>> 0, true).toBigInt();
                else if (typeof message.last_index_offset === "number")
                    object.last_index_offset = options.longs === $String ? $String(message.last_index_offset) : message.last_index_offset;
                else
                    object.last_index_offset = options.longs === $String ? $util.Long.prototype.toString.call(message.last_index_offset) : options.longs === $Number ? new $util.LongBits(message.last_index_offset.low >>> 0, message.last_index_offset.high >>> 0).toNumber(true) : message.last_index_offset;
            if (message.total_num_payments != null && $Object.hasOwnProperty.call(message, "total_num_payments"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.total_num_payments = typeof message.total_num_payments === "number" ? $BigInt(message.total_num_payments) : $util.Long.fromBits(message.total_num_payments.low >>> 0, message.total_num_payments.high >>> 0, true).toBigInt();
                else if (typeof message.total_num_payments === "number")
                    object.total_num_payments = options.longs === $String ? $String(message.total_num_payments) : message.total_num_payments;
                else
                    object.total_num_payments = options.longs === $String ? $util.Long.prototype.toString.call(message.total_num_payments) : options.longs === $Number ? new $util.LongBits(message.total_num_payments.low >>> 0, message.total_num_payments.high >>> 0).toNumber(true) : message.total_num_payments;
            return object;
        };

        /**
         * Converts this ListPaymentsResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ListPaymentsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListPaymentsResponse.prototype.toJSON = function() {
            return ListPaymentsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListPaymentsResponse
         * @function getTypeUrl
         * @memberof lnrpc.ListPaymentsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListPaymentsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListPaymentsResponse";
        };

        return ListPaymentsResponse;
    })();

    lnrpc.DeletePaymentRequest = (function() {

        /**
         * Properties of a DeletePaymentRequest.
         * @typedef {Object} lnrpc.DeletePaymentRequest.$Properties
         * @property {Uint8Array|null} [payment_hash] DeletePaymentRequest payment_hash
         * @property {boolean|null} [failed_htlcs_only] DeletePaymentRequest failed_htlcs_only
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeletePaymentRequest.
         * @memberof lnrpc
         * @interface IDeletePaymentRequest
         * @augments lnrpc.DeletePaymentRequest.$Properties
         * @deprecated Use lnrpc.DeletePaymentRequest.$Properties instead.
         */

        /**
         * Shape of a DeletePaymentRequest.
         * @typedef {lnrpc.DeletePaymentRequest.$Properties} lnrpc.DeletePaymentRequest.$Shape
         */

        /**
         * Constructs a new DeletePaymentRequest.
         * @memberof lnrpc
         * @classdesc Represents a DeletePaymentRequest.
         * @constructor
         * @param {lnrpc.DeletePaymentRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeletePaymentRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeletePaymentRequest payment_hash.
         * @member {Uint8Array} payment_hash
         * @memberof lnrpc.DeletePaymentRequest
         * @instance
         */
        DeletePaymentRequest.prototype.payment_hash = $util.newBuffer([]);

        /**
         * DeletePaymentRequest failed_htlcs_only.
         * @member {boolean} failed_htlcs_only
         * @memberof lnrpc.DeletePaymentRequest
         * @instance
         */
        DeletePaymentRequest.prototype.failed_htlcs_only = false;

        /**
         * Creates a new DeletePaymentRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.DeletePaymentRequest
         * @static
         * @param {lnrpc.DeletePaymentRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DeletePaymentRequest} DeletePaymentRequest instance
         * @type {{
         *   (properties: lnrpc.DeletePaymentRequest.$Shape): lnrpc.DeletePaymentRequest & lnrpc.DeletePaymentRequest.$Shape;
         *   (properties?: lnrpc.DeletePaymentRequest.$Properties): lnrpc.DeletePaymentRequest;
         * }}
         */
        DeletePaymentRequest.create = function(properties) {
            return new DeletePaymentRequest(properties);
        };

        /**
         * Encodes the specified DeletePaymentRequest message. Does not implicitly {@link lnrpc.DeletePaymentRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DeletePaymentRequest
         * @static
         * @param {lnrpc.DeletePaymentRequest.$Properties} message DeletePaymentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeletePaymentRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.payment_hash);
            if (message.failed_htlcs_only != null && $Object.hasOwnProperty.call(message, "failed_htlcs_only") && message.failed_htlcs_only !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.failed_htlcs_only);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeletePaymentRequest message, length delimited. Does not implicitly {@link lnrpc.DeletePaymentRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DeletePaymentRequest
         * @static
         * @param {lnrpc.DeletePaymentRequest.$Properties} message DeletePaymentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeletePaymentRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeletePaymentRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DeletePaymentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DeletePaymentRequest & lnrpc.DeletePaymentRequest.$Shape} DeletePaymentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeletePaymentRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DeletePaymentRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.failed_htlcs_only = value;
                        else
                            delete message.failed_htlcs_only;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeletePaymentRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DeletePaymentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DeletePaymentRequest & lnrpc.DeletePaymentRequest.$Shape} DeletePaymentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeletePaymentRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeletePaymentRequest message.
         * @function verify
         * @memberof lnrpc.DeletePaymentRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeletePaymentRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            if (message.failed_htlcs_only != null && $Object.hasOwnProperty.call(message, "failed_htlcs_only"))
                if (typeof message.failed_htlcs_only !== "boolean")
                    return "failed_htlcs_only: boolean expected";
            return null;
        };

        /**
         * Creates a DeletePaymentRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DeletePaymentRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DeletePaymentRequest} DeletePaymentRequest
         */
        DeletePaymentRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DeletePaymentRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DeletePaymentRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DeletePaymentRequest();
            if (object.payment_hash != null)
                if (object.payment_hash.length)
                    if (typeof object.payment_hash === "string")
                        $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                    else if (object.payment_hash.length >= 0)
                        message.payment_hash = object.payment_hash;
            if (object.failed_htlcs_only != null)
                if (object.failed_htlcs_only)
                    message.failed_htlcs_only = $Boolean(object.failed_htlcs_only);
            return message;
        };

        /**
         * Creates a plain object from a DeletePaymentRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DeletePaymentRequest
         * @static
         * @param {lnrpc.DeletePaymentRequest} message DeletePaymentRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeletePaymentRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.payment_hash = "";
                else {
                    object.payment_hash = [];
                    if (options.bytes !== $Array)
                        object.payment_hash = $util.newBuffer(object.payment_hash);
                }
                object.failed_htlcs_only = false;
            }
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
            if (message.failed_htlcs_only != null && $Object.hasOwnProperty.call(message, "failed_htlcs_only"))
                object.failed_htlcs_only = message.failed_htlcs_only;
            return object;
        };

        /**
         * Converts this DeletePaymentRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.DeletePaymentRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeletePaymentRequest.prototype.toJSON = function() {
            return DeletePaymentRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeletePaymentRequest
         * @function getTypeUrl
         * @memberof lnrpc.DeletePaymentRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeletePaymentRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DeletePaymentRequest";
        };

        return DeletePaymentRequest;
    })();

    lnrpc.DeleteAllPaymentsRequest = (function() {

        /**
         * Properties of a DeleteAllPaymentsRequest.
         * @typedef {Object} lnrpc.DeleteAllPaymentsRequest.$Properties
         * @property {boolean|null} [failed_payments_only] DeleteAllPaymentsRequest failed_payments_only
         * @property {boolean|null} [failed_htlcs_only] DeleteAllPaymentsRequest failed_htlcs_only
         * @property {boolean|null} [all_payments] DeleteAllPaymentsRequest all_payments
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeleteAllPaymentsRequest.
         * @memberof lnrpc
         * @interface IDeleteAllPaymentsRequest
         * @augments lnrpc.DeleteAllPaymentsRequest.$Properties
         * @deprecated Use lnrpc.DeleteAllPaymentsRequest.$Properties instead.
         */

        /**
         * Shape of a DeleteAllPaymentsRequest.
         * @typedef {lnrpc.DeleteAllPaymentsRequest.$Properties} lnrpc.DeleteAllPaymentsRequest.$Shape
         */

        /**
         * Constructs a new DeleteAllPaymentsRequest.
         * @memberof lnrpc
         * @classdesc Represents a DeleteAllPaymentsRequest.
         * @constructor
         * @param {lnrpc.DeleteAllPaymentsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeleteAllPaymentsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeleteAllPaymentsRequest failed_payments_only.
         * @member {boolean} failed_payments_only
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @instance
         */
        DeleteAllPaymentsRequest.prototype.failed_payments_only = false;

        /**
         * DeleteAllPaymentsRequest failed_htlcs_only.
         * @member {boolean} failed_htlcs_only
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @instance
         */
        DeleteAllPaymentsRequest.prototype.failed_htlcs_only = false;

        /**
         * DeleteAllPaymentsRequest all_payments.
         * @member {boolean} all_payments
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @instance
         */
        DeleteAllPaymentsRequest.prototype.all_payments = false;

        /**
         * Creates a new DeleteAllPaymentsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @static
         * @param {lnrpc.DeleteAllPaymentsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DeleteAllPaymentsRequest} DeleteAllPaymentsRequest instance
         * @type {{
         *   (properties: lnrpc.DeleteAllPaymentsRequest.$Shape): lnrpc.DeleteAllPaymentsRequest & lnrpc.DeleteAllPaymentsRequest.$Shape;
         *   (properties?: lnrpc.DeleteAllPaymentsRequest.$Properties): lnrpc.DeleteAllPaymentsRequest;
         * }}
         */
        DeleteAllPaymentsRequest.create = function(properties) {
            return new DeleteAllPaymentsRequest(properties);
        };

        /**
         * Encodes the specified DeleteAllPaymentsRequest message. Does not implicitly {@link lnrpc.DeleteAllPaymentsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @static
         * @param {lnrpc.DeleteAllPaymentsRequest.$Properties} message DeleteAllPaymentsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteAllPaymentsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.failed_payments_only != null && $Object.hasOwnProperty.call(message, "failed_payments_only") && message.failed_payments_only !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.failed_payments_only);
            if (message.failed_htlcs_only != null && $Object.hasOwnProperty.call(message, "failed_htlcs_only") && message.failed_htlcs_only !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.failed_htlcs_only);
            if (message.all_payments != null && $Object.hasOwnProperty.call(message, "all_payments") && message.all_payments !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.all_payments);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeleteAllPaymentsRequest message, length delimited. Does not implicitly {@link lnrpc.DeleteAllPaymentsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @static
         * @param {lnrpc.DeleteAllPaymentsRequest.$Properties} message DeleteAllPaymentsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteAllPaymentsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeleteAllPaymentsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DeleteAllPaymentsRequest & lnrpc.DeleteAllPaymentsRequest.$Shape} DeleteAllPaymentsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteAllPaymentsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DeleteAllPaymentsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.failed_payments_only = value;
                        else
                            delete message.failed_payments_only;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.failed_htlcs_only = value;
                        else
                            delete message.failed_htlcs_only;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.all_payments = value;
                        else
                            delete message.all_payments;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeleteAllPaymentsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DeleteAllPaymentsRequest & lnrpc.DeleteAllPaymentsRequest.$Shape} DeleteAllPaymentsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteAllPaymentsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeleteAllPaymentsRequest message.
         * @function verify
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeleteAllPaymentsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.failed_payments_only != null && $Object.hasOwnProperty.call(message, "failed_payments_only"))
                if (typeof message.failed_payments_only !== "boolean")
                    return "failed_payments_only: boolean expected";
            if (message.failed_htlcs_only != null && $Object.hasOwnProperty.call(message, "failed_htlcs_only"))
                if (typeof message.failed_htlcs_only !== "boolean")
                    return "failed_htlcs_only: boolean expected";
            if (message.all_payments != null && $Object.hasOwnProperty.call(message, "all_payments"))
                if (typeof message.all_payments !== "boolean")
                    return "all_payments: boolean expected";
            return null;
        };

        /**
         * Creates a DeleteAllPaymentsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DeleteAllPaymentsRequest} DeleteAllPaymentsRequest
         */
        DeleteAllPaymentsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DeleteAllPaymentsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DeleteAllPaymentsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DeleteAllPaymentsRequest();
            if (object.failed_payments_only != null)
                if (object.failed_payments_only)
                    message.failed_payments_only = $Boolean(object.failed_payments_only);
            if (object.failed_htlcs_only != null)
                if (object.failed_htlcs_only)
                    message.failed_htlcs_only = $Boolean(object.failed_htlcs_only);
            if (object.all_payments != null)
                if (object.all_payments)
                    message.all_payments = $Boolean(object.all_payments);
            return message;
        };

        /**
         * Creates a plain object from a DeleteAllPaymentsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @static
         * @param {lnrpc.DeleteAllPaymentsRequest} message DeleteAllPaymentsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeleteAllPaymentsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.failed_payments_only = false;
                object.failed_htlcs_only = false;
                object.all_payments = false;
            }
            if (message.failed_payments_only != null && $Object.hasOwnProperty.call(message, "failed_payments_only"))
                object.failed_payments_only = message.failed_payments_only;
            if (message.failed_htlcs_only != null && $Object.hasOwnProperty.call(message, "failed_htlcs_only"))
                object.failed_htlcs_only = message.failed_htlcs_only;
            if (message.all_payments != null && $Object.hasOwnProperty.call(message, "all_payments"))
                object.all_payments = message.all_payments;
            return object;
        };

        /**
         * Converts this DeleteAllPaymentsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeleteAllPaymentsRequest.prototype.toJSON = function() {
            return DeleteAllPaymentsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeleteAllPaymentsRequest
         * @function getTypeUrl
         * @memberof lnrpc.DeleteAllPaymentsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeleteAllPaymentsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DeleteAllPaymentsRequest";
        };

        return DeleteAllPaymentsRequest;
    })();

    lnrpc.DeletePaymentResponse = (function() {

        /**
         * Properties of a DeletePaymentResponse.
         * @typedef {Object} lnrpc.DeletePaymentResponse.$Properties
         * @property {string|null} [status] DeletePaymentResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeletePaymentResponse.
         * @memberof lnrpc
         * @interface IDeletePaymentResponse
         * @augments lnrpc.DeletePaymentResponse.$Properties
         * @deprecated Use lnrpc.DeletePaymentResponse.$Properties instead.
         */

        /**
         * Shape of a DeletePaymentResponse.
         * @typedef {lnrpc.DeletePaymentResponse.$Properties} lnrpc.DeletePaymentResponse.$Shape
         */

        /**
         * Constructs a new DeletePaymentResponse.
         * @memberof lnrpc
         * @classdesc Represents a DeletePaymentResponse.
         * @constructor
         * @param {lnrpc.DeletePaymentResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeletePaymentResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeletePaymentResponse status.
         * @member {string} status
         * @memberof lnrpc.DeletePaymentResponse
         * @instance
         */
        DeletePaymentResponse.prototype.status = "";

        /**
         * Creates a new DeletePaymentResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.DeletePaymentResponse
         * @static
         * @param {lnrpc.DeletePaymentResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DeletePaymentResponse} DeletePaymentResponse instance
         * @type {{
         *   (properties: lnrpc.DeletePaymentResponse.$Shape): lnrpc.DeletePaymentResponse & lnrpc.DeletePaymentResponse.$Shape;
         *   (properties?: lnrpc.DeletePaymentResponse.$Properties): lnrpc.DeletePaymentResponse;
         * }}
         */
        DeletePaymentResponse.create = function(properties) {
            return new DeletePaymentResponse(properties);
        };

        /**
         * Encodes the specified DeletePaymentResponse message. Does not implicitly {@link lnrpc.DeletePaymentResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DeletePaymentResponse
         * @static
         * @param {lnrpc.DeletePaymentResponse.$Properties} message DeletePaymentResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeletePaymentResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeletePaymentResponse message, length delimited. Does not implicitly {@link lnrpc.DeletePaymentResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DeletePaymentResponse
         * @static
         * @param {lnrpc.DeletePaymentResponse.$Properties} message DeletePaymentResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeletePaymentResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeletePaymentResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DeletePaymentResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DeletePaymentResponse & lnrpc.DeletePaymentResponse.$Shape} DeletePaymentResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeletePaymentResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DeletePaymentResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeletePaymentResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DeletePaymentResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DeletePaymentResponse & lnrpc.DeletePaymentResponse.$Shape} DeletePaymentResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeletePaymentResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeletePaymentResponse message.
         * @function verify
         * @memberof lnrpc.DeletePaymentResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeletePaymentResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a DeletePaymentResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DeletePaymentResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DeletePaymentResponse} DeletePaymentResponse
         */
        DeletePaymentResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DeletePaymentResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DeletePaymentResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DeletePaymentResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a DeletePaymentResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DeletePaymentResponse
         * @static
         * @param {lnrpc.DeletePaymentResponse} message DeletePaymentResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeletePaymentResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this DeletePaymentResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.DeletePaymentResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeletePaymentResponse.prototype.toJSON = function() {
            return DeletePaymentResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeletePaymentResponse
         * @function getTypeUrl
         * @memberof lnrpc.DeletePaymentResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeletePaymentResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DeletePaymentResponse";
        };

        return DeletePaymentResponse;
    })();

    lnrpc.DeleteAllPaymentsResponse = (function() {

        /**
         * Properties of a DeleteAllPaymentsResponse.
         * @typedef {Object} lnrpc.DeleteAllPaymentsResponse.$Properties
         * @property {string|null} [status] DeleteAllPaymentsResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeleteAllPaymentsResponse.
         * @memberof lnrpc
         * @interface IDeleteAllPaymentsResponse
         * @augments lnrpc.DeleteAllPaymentsResponse.$Properties
         * @deprecated Use lnrpc.DeleteAllPaymentsResponse.$Properties instead.
         */

        /**
         * Shape of a DeleteAllPaymentsResponse.
         * @typedef {lnrpc.DeleteAllPaymentsResponse.$Properties} lnrpc.DeleteAllPaymentsResponse.$Shape
         */

        /**
         * Constructs a new DeleteAllPaymentsResponse.
         * @memberof lnrpc
         * @classdesc Represents a DeleteAllPaymentsResponse.
         * @constructor
         * @param {lnrpc.DeleteAllPaymentsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeleteAllPaymentsResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeleteAllPaymentsResponse status.
         * @member {string} status
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @instance
         */
        DeleteAllPaymentsResponse.prototype.status = "";

        /**
         * Creates a new DeleteAllPaymentsResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @static
         * @param {lnrpc.DeleteAllPaymentsResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DeleteAllPaymentsResponse} DeleteAllPaymentsResponse instance
         * @type {{
         *   (properties: lnrpc.DeleteAllPaymentsResponse.$Shape): lnrpc.DeleteAllPaymentsResponse & lnrpc.DeleteAllPaymentsResponse.$Shape;
         *   (properties?: lnrpc.DeleteAllPaymentsResponse.$Properties): lnrpc.DeleteAllPaymentsResponse;
         * }}
         */
        DeleteAllPaymentsResponse.create = function(properties) {
            return new DeleteAllPaymentsResponse(properties);
        };

        /**
         * Encodes the specified DeleteAllPaymentsResponse message. Does not implicitly {@link lnrpc.DeleteAllPaymentsResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @static
         * @param {lnrpc.DeleteAllPaymentsResponse.$Properties} message DeleteAllPaymentsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteAllPaymentsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeleteAllPaymentsResponse message, length delimited. Does not implicitly {@link lnrpc.DeleteAllPaymentsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @static
         * @param {lnrpc.DeleteAllPaymentsResponse.$Properties} message DeleteAllPaymentsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteAllPaymentsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeleteAllPaymentsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DeleteAllPaymentsResponse & lnrpc.DeleteAllPaymentsResponse.$Shape} DeleteAllPaymentsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteAllPaymentsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DeleteAllPaymentsResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeleteAllPaymentsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DeleteAllPaymentsResponse & lnrpc.DeleteAllPaymentsResponse.$Shape} DeleteAllPaymentsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteAllPaymentsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeleteAllPaymentsResponse message.
         * @function verify
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeleteAllPaymentsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a DeleteAllPaymentsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DeleteAllPaymentsResponse} DeleteAllPaymentsResponse
         */
        DeleteAllPaymentsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DeleteAllPaymentsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DeleteAllPaymentsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DeleteAllPaymentsResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a DeleteAllPaymentsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @static
         * @param {lnrpc.DeleteAllPaymentsResponse} message DeleteAllPaymentsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeleteAllPaymentsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this DeleteAllPaymentsResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeleteAllPaymentsResponse.prototype.toJSON = function() {
            return DeleteAllPaymentsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeleteAllPaymentsResponse
         * @function getTypeUrl
         * @memberof lnrpc.DeleteAllPaymentsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeleteAllPaymentsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DeleteAllPaymentsResponse";
        };

        return DeleteAllPaymentsResponse;
    })();

    lnrpc.AbandonChannelRequest = (function() {

        /**
         * Properties of an AbandonChannelRequest.
         * @typedef {Object} lnrpc.AbandonChannelRequest.$Properties
         * @property {lnrpc.ChannelPoint.$Properties|null} [channel_point] AbandonChannelRequest channel_point
         * @property {boolean|null} [pending_funding_shim_only] AbandonChannelRequest pending_funding_shim_only
         * @property {boolean|null} [i_know_what_i_am_doing] AbandonChannelRequest i_know_what_i_am_doing
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AbandonChannelRequest.
         * @memberof lnrpc
         * @interface IAbandonChannelRequest
         * @augments lnrpc.AbandonChannelRequest.$Properties
         * @deprecated Use lnrpc.AbandonChannelRequest.$Properties instead.
         */

        /**
         * Shape of an AbandonChannelRequest.
         * @typedef {{
         *   channel_point?: lnrpc.ChannelPoint.$Shape|null;
         *   pending_funding_shim_only?: boolean|null;
         *   i_know_what_i_am_doing?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.AbandonChannelRequest.$Shape
         */

        /**
         * Constructs a new AbandonChannelRequest.
         * @memberof lnrpc
         * @classdesc Represents an AbandonChannelRequest.
         * @constructor
         * @param {lnrpc.AbandonChannelRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AbandonChannelRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AbandonChannelRequest channel_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} channel_point
         * @memberof lnrpc.AbandonChannelRequest
         * @instance
         */
        AbandonChannelRequest.prototype.channel_point = null;

        /**
         * AbandonChannelRequest pending_funding_shim_only.
         * @member {boolean} pending_funding_shim_only
         * @memberof lnrpc.AbandonChannelRequest
         * @instance
         */
        AbandonChannelRequest.prototype.pending_funding_shim_only = false;

        /**
         * AbandonChannelRequest i_know_what_i_am_doing.
         * @member {boolean} i_know_what_i_am_doing
         * @memberof lnrpc.AbandonChannelRequest
         * @instance
         */
        AbandonChannelRequest.prototype.i_know_what_i_am_doing = false;

        /**
         * Creates a new AbandonChannelRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.AbandonChannelRequest
         * @static
         * @param {lnrpc.AbandonChannelRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.AbandonChannelRequest} AbandonChannelRequest instance
         * @type {{
         *   (properties: lnrpc.AbandonChannelRequest.$Shape): lnrpc.AbandonChannelRequest & lnrpc.AbandonChannelRequest.$Shape;
         *   (properties?: lnrpc.AbandonChannelRequest.$Properties): lnrpc.AbandonChannelRequest;
         * }}
         */
        AbandonChannelRequest.create = function(properties) {
            return new AbandonChannelRequest(properties);
        };

        /**
         * Encodes the specified AbandonChannelRequest message. Does not implicitly {@link lnrpc.AbandonChannelRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.AbandonChannelRequest
         * @static
         * @param {lnrpc.AbandonChannelRequest.$Properties} message AbandonChannelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AbandonChannelRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                $root.lnrpc.ChannelPoint.encode(message.channel_point, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.pending_funding_shim_only != null && $Object.hasOwnProperty.call(message, "pending_funding_shim_only") && message.pending_funding_shim_only !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.pending_funding_shim_only);
            if (message.i_know_what_i_am_doing != null && $Object.hasOwnProperty.call(message, "i_know_what_i_am_doing") && message.i_know_what_i_am_doing !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.i_know_what_i_am_doing);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AbandonChannelRequest message, length delimited. Does not implicitly {@link lnrpc.AbandonChannelRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.AbandonChannelRequest
         * @static
         * @param {lnrpc.AbandonChannelRequest.$Properties} message AbandonChannelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AbandonChannelRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AbandonChannelRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.AbandonChannelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.AbandonChannelRequest & lnrpc.AbandonChannelRequest.$Shape} AbandonChannelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AbandonChannelRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.AbandonChannelRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.channel_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel_point);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.pending_funding_shim_only = value;
                        else
                            delete message.pending_funding_shim_only;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.i_know_what_i_am_doing = value;
                        else
                            delete message.i_know_what_i_am_doing;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AbandonChannelRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.AbandonChannelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.AbandonChannelRequest & lnrpc.AbandonChannelRequest.$Shape} AbandonChannelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AbandonChannelRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AbandonChannelRequest message.
         * @function verify
         * @memberof lnrpc.AbandonChannelRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AbandonChannelRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.channel_point, _depth + 1);
                if (error)
                    return "channel_point." + error;
            }
            if (message.pending_funding_shim_only != null && $Object.hasOwnProperty.call(message, "pending_funding_shim_only"))
                if (typeof message.pending_funding_shim_only !== "boolean")
                    return "pending_funding_shim_only: boolean expected";
            if (message.i_know_what_i_am_doing != null && $Object.hasOwnProperty.call(message, "i_know_what_i_am_doing"))
                if (typeof message.i_know_what_i_am_doing !== "boolean")
                    return "i_know_what_i_am_doing: boolean expected";
            return null;
        };

        /**
         * Creates an AbandonChannelRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.AbandonChannelRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.AbandonChannelRequest} AbandonChannelRequest
         */
        AbandonChannelRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.AbandonChannelRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.AbandonChannelRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.AbandonChannelRequest();
            if (object.channel_point != null) {
                if (!$util.isObject(object.channel_point))
                    throw $TypeError(".lnrpc.AbandonChannelRequest.channel_point: object expected");
                message.channel_point = $root.lnrpc.ChannelPoint.fromObject(object.channel_point, _depth + 1);
            }
            if (object.pending_funding_shim_only != null)
                if (object.pending_funding_shim_only)
                    message.pending_funding_shim_only = $Boolean(object.pending_funding_shim_only);
            if (object.i_know_what_i_am_doing != null)
                if (object.i_know_what_i_am_doing)
                    message.i_know_what_i_am_doing = $Boolean(object.i_know_what_i_am_doing);
            return message;
        };

        /**
         * Creates a plain object from an AbandonChannelRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.AbandonChannelRequest
         * @static
         * @param {lnrpc.AbandonChannelRequest} message AbandonChannelRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AbandonChannelRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.channel_point = null;
                object.pending_funding_shim_only = false;
                object.i_know_what_i_am_doing = false;
            }
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                object.channel_point = $root.lnrpc.ChannelPoint.toObject(message.channel_point, options, _depth + 1);
            if (message.pending_funding_shim_only != null && $Object.hasOwnProperty.call(message, "pending_funding_shim_only"))
                object.pending_funding_shim_only = message.pending_funding_shim_only;
            if (message.i_know_what_i_am_doing != null && $Object.hasOwnProperty.call(message, "i_know_what_i_am_doing"))
                object.i_know_what_i_am_doing = message.i_know_what_i_am_doing;
            return object;
        };

        /**
         * Converts this AbandonChannelRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.AbandonChannelRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AbandonChannelRequest.prototype.toJSON = function() {
            return AbandonChannelRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AbandonChannelRequest
         * @function getTypeUrl
         * @memberof lnrpc.AbandonChannelRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AbandonChannelRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.AbandonChannelRequest";
        };

        return AbandonChannelRequest;
    })();

    lnrpc.AbandonChannelResponse = (function() {

        /**
         * Properties of an AbandonChannelResponse.
         * @typedef {Object} lnrpc.AbandonChannelResponse.$Properties
         * @property {string|null} [status] AbandonChannelResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AbandonChannelResponse.
         * @memberof lnrpc
         * @interface IAbandonChannelResponse
         * @augments lnrpc.AbandonChannelResponse.$Properties
         * @deprecated Use lnrpc.AbandonChannelResponse.$Properties instead.
         */

        /**
         * Shape of an AbandonChannelResponse.
         * @typedef {lnrpc.AbandonChannelResponse.$Properties} lnrpc.AbandonChannelResponse.$Shape
         */

        /**
         * Constructs a new AbandonChannelResponse.
         * @memberof lnrpc
         * @classdesc Represents an AbandonChannelResponse.
         * @constructor
         * @param {lnrpc.AbandonChannelResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AbandonChannelResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AbandonChannelResponse status.
         * @member {string} status
         * @memberof lnrpc.AbandonChannelResponse
         * @instance
         */
        AbandonChannelResponse.prototype.status = "";

        /**
         * Creates a new AbandonChannelResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.AbandonChannelResponse
         * @static
         * @param {lnrpc.AbandonChannelResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.AbandonChannelResponse} AbandonChannelResponse instance
         * @type {{
         *   (properties: lnrpc.AbandonChannelResponse.$Shape): lnrpc.AbandonChannelResponse & lnrpc.AbandonChannelResponse.$Shape;
         *   (properties?: lnrpc.AbandonChannelResponse.$Properties): lnrpc.AbandonChannelResponse;
         * }}
         */
        AbandonChannelResponse.create = function(properties) {
            return new AbandonChannelResponse(properties);
        };

        /**
         * Encodes the specified AbandonChannelResponse message. Does not implicitly {@link lnrpc.AbandonChannelResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.AbandonChannelResponse
         * @static
         * @param {lnrpc.AbandonChannelResponse.$Properties} message AbandonChannelResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AbandonChannelResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AbandonChannelResponse message, length delimited. Does not implicitly {@link lnrpc.AbandonChannelResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.AbandonChannelResponse
         * @static
         * @param {lnrpc.AbandonChannelResponse.$Properties} message AbandonChannelResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AbandonChannelResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AbandonChannelResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.AbandonChannelResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.AbandonChannelResponse & lnrpc.AbandonChannelResponse.$Shape} AbandonChannelResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AbandonChannelResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.AbandonChannelResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AbandonChannelResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.AbandonChannelResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.AbandonChannelResponse & lnrpc.AbandonChannelResponse.$Shape} AbandonChannelResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AbandonChannelResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AbandonChannelResponse message.
         * @function verify
         * @memberof lnrpc.AbandonChannelResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AbandonChannelResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates an AbandonChannelResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.AbandonChannelResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.AbandonChannelResponse} AbandonChannelResponse
         */
        AbandonChannelResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.AbandonChannelResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.AbandonChannelResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.AbandonChannelResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from an AbandonChannelResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.AbandonChannelResponse
         * @static
         * @param {lnrpc.AbandonChannelResponse} message AbandonChannelResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AbandonChannelResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this AbandonChannelResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.AbandonChannelResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AbandonChannelResponse.prototype.toJSON = function() {
            return AbandonChannelResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AbandonChannelResponse
         * @function getTypeUrl
         * @memberof lnrpc.AbandonChannelResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AbandonChannelResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.AbandonChannelResponse";
        };

        return AbandonChannelResponse;
    })();

    lnrpc.DebugLevelRequest = (function() {

        /**
         * Properties of a DebugLevelRequest.
         * @typedef {Object} lnrpc.DebugLevelRequest.$Properties
         * @property {boolean|null} [show] DebugLevelRequest show
         * @property {string|null} [level_spec] DebugLevelRequest level_spec
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DebugLevelRequest.
         * @memberof lnrpc
         * @interface IDebugLevelRequest
         * @augments lnrpc.DebugLevelRequest.$Properties
         * @deprecated Use lnrpc.DebugLevelRequest.$Properties instead.
         */

        /**
         * Shape of a DebugLevelRequest.
         * @typedef {lnrpc.DebugLevelRequest.$Properties} lnrpc.DebugLevelRequest.$Shape
         */

        /**
         * Constructs a new DebugLevelRequest.
         * @memberof lnrpc
         * @classdesc Represents a DebugLevelRequest.
         * @constructor
         * @param {lnrpc.DebugLevelRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DebugLevelRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DebugLevelRequest show.
         * @member {boolean} show
         * @memberof lnrpc.DebugLevelRequest
         * @instance
         */
        DebugLevelRequest.prototype.show = false;

        /**
         * DebugLevelRequest level_spec.
         * @member {string} level_spec
         * @memberof lnrpc.DebugLevelRequest
         * @instance
         */
        DebugLevelRequest.prototype.level_spec = "";

        /**
         * Creates a new DebugLevelRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.DebugLevelRequest
         * @static
         * @param {lnrpc.DebugLevelRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DebugLevelRequest} DebugLevelRequest instance
         * @type {{
         *   (properties: lnrpc.DebugLevelRequest.$Shape): lnrpc.DebugLevelRequest & lnrpc.DebugLevelRequest.$Shape;
         *   (properties?: lnrpc.DebugLevelRequest.$Properties): lnrpc.DebugLevelRequest;
         * }}
         */
        DebugLevelRequest.create = function(properties) {
            return new DebugLevelRequest(properties);
        };

        /**
         * Encodes the specified DebugLevelRequest message. Does not implicitly {@link lnrpc.DebugLevelRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DebugLevelRequest
         * @static
         * @param {lnrpc.DebugLevelRequest.$Properties} message DebugLevelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DebugLevelRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.show != null && $Object.hasOwnProperty.call(message, "show") && message.show !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.show);
            if (message.level_spec != null && $Object.hasOwnProperty.call(message, "level_spec") && message.level_spec !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.level_spec);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DebugLevelRequest message, length delimited. Does not implicitly {@link lnrpc.DebugLevelRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DebugLevelRequest
         * @static
         * @param {lnrpc.DebugLevelRequest.$Properties} message DebugLevelRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DebugLevelRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DebugLevelRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DebugLevelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DebugLevelRequest & lnrpc.DebugLevelRequest.$Shape} DebugLevelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DebugLevelRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DebugLevelRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.show = value;
                        else
                            delete message.show;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.level_spec = value;
                        else
                            delete message.level_spec;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DebugLevelRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DebugLevelRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DebugLevelRequest & lnrpc.DebugLevelRequest.$Shape} DebugLevelRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DebugLevelRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DebugLevelRequest message.
         * @function verify
         * @memberof lnrpc.DebugLevelRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DebugLevelRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.show != null && $Object.hasOwnProperty.call(message, "show"))
                if (typeof message.show !== "boolean")
                    return "show: boolean expected";
            if (message.level_spec != null && $Object.hasOwnProperty.call(message, "level_spec"))
                if (!$util.isString(message.level_spec))
                    return "level_spec: string expected";
            return null;
        };

        /**
         * Creates a DebugLevelRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DebugLevelRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DebugLevelRequest} DebugLevelRequest
         */
        DebugLevelRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DebugLevelRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DebugLevelRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DebugLevelRequest();
            if (object.show != null)
                if (object.show)
                    message.show = $Boolean(object.show);
            if (object.level_spec != null)
                if (typeof object.level_spec !== "string" || object.level_spec.length)
                    message.level_spec = $String(object.level_spec);
            return message;
        };

        /**
         * Creates a plain object from a DebugLevelRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DebugLevelRequest
         * @static
         * @param {lnrpc.DebugLevelRequest} message DebugLevelRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DebugLevelRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.show = false;
                object.level_spec = "";
            }
            if (message.show != null && $Object.hasOwnProperty.call(message, "show"))
                object.show = message.show;
            if (message.level_spec != null && $Object.hasOwnProperty.call(message, "level_spec"))
                object.level_spec = message.level_spec;
            return object;
        };

        /**
         * Converts this DebugLevelRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.DebugLevelRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DebugLevelRequest.prototype.toJSON = function() {
            return DebugLevelRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DebugLevelRequest
         * @function getTypeUrl
         * @memberof lnrpc.DebugLevelRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DebugLevelRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DebugLevelRequest";
        };

        return DebugLevelRequest;
    })();

    lnrpc.DebugLevelResponse = (function() {

        /**
         * Properties of a DebugLevelResponse.
         * @typedef {Object} lnrpc.DebugLevelResponse.$Properties
         * @property {string|null} [sub_systems] DebugLevelResponse sub_systems
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DebugLevelResponse.
         * @memberof lnrpc
         * @interface IDebugLevelResponse
         * @augments lnrpc.DebugLevelResponse.$Properties
         * @deprecated Use lnrpc.DebugLevelResponse.$Properties instead.
         */

        /**
         * Shape of a DebugLevelResponse.
         * @typedef {lnrpc.DebugLevelResponse.$Properties} lnrpc.DebugLevelResponse.$Shape
         */

        /**
         * Constructs a new DebugLevelResponse.
         * @memberof lnrpc
         * @classdesc Represents a DebugLevelResponse.
         * @constructor
         * @param {lnrpc.DebugLevelResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DebugLevelResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DebugLevelResponse sub_systems.
         * @member {string} sub_systems
         * @memberof lnrpc.DebugLevelResponse
         * @instance
         */
        DebugLevelResponse.prototype.sub_systems = "";

        /**
         * Creates a new DebugLevelResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.DebugLevelResponse
         * @static
         * @param {lnrpc.DebugLevelResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DebugLevelResponse} DebugLevelResponse instance
         * @type {{
         *   (properties: lnrpc.DebugLevelResponse.$Shape): lnrpc.DebugLevelResponse & lnrpc.DebugLevelResponse.$Shape;
         *   (properties?: lnrpc.DebugLevelResponse.$Properties): lnrpc.DebugLevelResponse;
         * }}
         */
        DebugLevelResponse.create = function(properties) {
            return new DebugLevelResponse(properties);
        };

        /**
         * Encodes the specified DebugLevelResponse message. Does not implicitly {@link lnrpc.DebugLevelResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DebugLevelResponse
         * @static
         * @param {lnrpc.DebugLevelResponse.$Properties} message DebugLevelResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DebugLevelResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sub_systems != null && $Object.hasOwnProperty.call(message, "sub_systems") && message.sub_systems !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.sub_systems);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DebugLevelResponse message, length delimited. Does not implicitly {@link lnrpc.DebugLevelResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DebugLevelResponse
         * @static
         * @param {lnrpc.DebugLevelResponse.$Properties} message DebugLevelResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DebugLevelResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DebugLevelResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DebugLevelResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DebugLevelResponse & lnrpc.DebugLevelResponse.$Shape} DebugLevelResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DebugLevelResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DebugLevelResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.sub_systems = value;
                        else
                            delete message.sub_systems;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DebugLevelResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DebugLevelResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DebugLevelResponse & lnrpc.DebugLevelResponse.$Shape} DebugLevelResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DebugLevelResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DebugLevelResponse message.
         * @function verify
         * @memberof lnrpc.DebugLevelResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DebugLevelResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sub_systems != null && $Object.hasOwnProperty.call(message, "sub_systems"))
                if (!$util.isString(message.sub_systems))
                    return "sub_systems: string expected";
            return null;
        };

        /**
         * Creates a DebugLevelResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DebugLevelResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DebugLevelResponse} DebugLevelResponse
         */
        DebugLevelResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DebugLevelResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DebugLevelResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DebugLevelResponse();
            if (object.sub_systems != null)
                if (typeof object.sub_systems !== "string" || object.sub_systems.length)
                    message.sub_systems = $String(object.sub_systems);
            return message;
        };

        /**
         * Creates a plain object from a DebugLevelResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DebugLevelResponse
         * @static
         * @param {lnrpc.DebugLevelResponse} message DebugLevelResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DebugLevelResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.sub_systems = "";
            if (message.sub_systems != null && $Object.hasOwnProperty.call(message, "sub_systems"))
                object.sub_systems = message.sub_systems;
            return object;
        };

        /**
         * Converts this DebugLevelResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.DebugLevelResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DebugLevelResponse.prototype.toJSON = function() {
            return DebugLevelResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DebugLevelResponse
         * @function getTypeUrl
         * @memberof lnrpc.DebugLevelResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DebugLevelResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DebugLevelResponse";
        };

        return DebugLevelResponse;
    })();

    lnrpc.PayReqString = (function() {

        /**
         * Properties of a PayReqString.
         * @typedef {Object} lnrpc.PayReqString.$Properties
         * @property {string|null} [pay_req] PayReqString pay_req
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PayReqString.
         * @memberof lnrpc
         * @interface IPayReqString
         * @augments lnrpc.PayReqString.$Properties
         * @deprecated Use lnrpc.PayReqString.$Properties instead.
         */

        /**
         * Shape of a PayReqString.
         * @typedef {lnrpc.PayReqString.$Properties} lnrpc.PayReqString.$Shape
         */

        /**
         * Constructs a new PayReqString.
         * @memberof lnrpc
         * @classdesc Represents a PayReqString.
         * @constructor
         * @param {lnrpc.PayReqString.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PayReqString = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PayReqString pay_req.
         * @member {string} pay_req
         * @memberof lnrpc.PayReqString
         * @instance
         */
        PayReqString.prototype.pay_req = "";

        /**
         * Creates a new PayReqString instance using the specified properties.
         * @function create
         * @memberof lnrpc.PayReqString
         * @static
         * @param {lnrpc.PayReqString.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PayReqString} PayReqString instance
         * @type {{
         *   (properties: lnrpc.PayReqString.$Shape): lnrpc.PayReqString & lnrpc.PayReqString.$Shape;
         *   (properties?: lnrpc.PayReqString.$Properties): lnrpc.PayReqString;
         * }}
         */
        PayReqString.create = function(properties) {
            return new PayReqString(properties);
        };

        /**
         * Encodes the specified PayReqString message. Does not implicitly {@link lnrpc.PayReqString.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PayReqString
         * @static
         * @param {lnrpc.PayReqString.$Properties} message PayReqString message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PayReqString.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pay_req != null && $Object.hasOwnProperty.call(message, "pay_req") && message.pay_req !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pay_req);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PayReqString message, length delimited. Does not implicitly {@link lnrpc.PayReqString.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PayReqString
         * @static
         * @param {lnrpc.PayReqString.$Properties} message PayReqString message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PayReqString.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PayReqString message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PayReqString
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PayReqString & lnrpc.PayReqString.$Shape} PayReqString
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PayReqString.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PayReqString(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pay_req = value;
                        else
                            delete message.pay_req;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PayReqString message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PayReqString
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PayReqString & lnrpc.PayReqString.$Shape} PayReqString
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PayReqString.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PayReqString message.
         * @function verify
         * @memberof lnrpc.PayReqString
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PayReqString.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pay_req != null && $Object.hasOwnProperty.call(message, "pay_req"))
                if (!$util.isString(message.pay_req))
                    return "pay_req: string expected";
            return null;
        };

        /**
         * Creates a PayReqString message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PayReqString
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PayReqString} PayReqString
         */
        PayReqString.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PayReqString)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PayReqString: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PayReqString();
            if (object.pay_req != null)
                if (typeof object.pay_req !== "string" || object.pay_req.length)
                    message.pay_req = $String(object.pay_req);
            return message;
        };

        /**
         * Creates a plain object from a PayReqString message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PayReqString
         * @static
         * @param {lnrpc.PayReqString} message PayReqString
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PayReqString.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.pay_req = "";
            if (message.pay_req != null && $Object.hasOwnProperty.call(message, "pay_req"))
                object.pay_req = message.pay_req;
            return object;
        };

        /**
         * Converts this PayReqString to JSON.
         * @function toJSON
         * @memberof lnrpc.PayReqString
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PayReqString.prototype.toJSON = function() {
            return PayReqString.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PayReqString
         * @function getTypeUrl
         * @memberof lnrpc.PayReqString
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PayReqString.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PayReqString";
        };

        return PayReqString;
    })();

    lnrpc.PayReq = (function() {

        /**
         * Properties of a PayReq.
         * @typedef {Object} lnrpc.PayReq.$Properties
         * @property {string|null} [destination] PayReq destination
         * @property {string|null} [payment_hash] PayReq payment_hash
         * @property {Long|null} [num_satoshis] PayReq num_satoshis
         * @property {Long|null} [timestamp] PayReq timestamp
         * @property {Long|null} [expiry] PayReq expiry
         * @property {string|null} [description] PayReq description
         * @property {string|null} [description_hash] PayReq description_hash
         * @property {string|null} [fallback_addr] PayReq fallback_addr
         * @property {Long|null} [cltv_expiry] PayReq cltv_expiry
         * @property {Array.<lnrpc.RouteHint.$Properties>|null} [route_hints] PayReq route_hints
         * @property {Uint8Array|null} [payment_addr] PayReq payment_addr
         * @property {Long|null} [num_msat] PayReq num_msat
         * @property {Object.<string,lnrpc.Feature.$Properties>|null} [features] PayReq features
         * @property {Array.<lnrpc.BlindedPaymentPath.$Properties>|null} [blinded_paths] PayReq blinded_paths
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PayReq.
         * @memberof lnrpc
         * @interface IPayReq
         * @augments lnrpc.PayReq.$Properties
         * @deprecated Use lnrpc.PayReq.$Properties instead.
         */

        /**
         * Shape of a PayReq.
         * @typedef {lnrpc.PayReq.$Properties} lnrpc.PayReq.$Shape
         */

        /**
         * Constructs a new PayReq.
         * @memberof lnrpc
         * @classdesc Represents a PayReq.
         * @constructor
         * @param {lnrpc.PayReq.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PayReq = function (properties) {
            this.route_hints = [];
            this.features = {};
            this.blinded_paths = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PayReq destination.
         * @member {string} destination
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.destination = "";

        /**
         * PayReq payment_hash.
         * @member {string} payment_hash
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.payment_hash = "";

        /**
         * PayReq num_satoshis.
         * @member {Long} num_satoshis
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.num_satoshis = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PayReq timestamp.
         * @member {Long} timestamp
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PayReq expiry.
         * @member {Long} expiry
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.expiry = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PayReq description.
         * @member {string} description
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.description = "";

        /**
         * PayReq description_hash.
         * @member {string} description_hash
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.description_hash = "";

        /**
         * PayReq fallback_addr.
         * @member {string} fallback_addr
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.fallback_addr = "";

        /**
         * PayReq cltv_expiry.
         * @member {Long} cltv_expiry
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.cltv_expiry = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PayReq route_hints.
         * @member {Array.<lnrpc.RouteHint.$Properties>} route_hints
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.route_hints = $util.emptyArray;

        /**
         * PayReq payment_addr.
         * @member {Uint8Array} payment_addr
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.payment_addr = $util.newBuffer([]);

        /**
         * PayReq num_msat.
         * @member {Long} num_msat
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.num_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PayReq features.
         * @member {Object.<string,lnrpc.Feature.$Properties>} features
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.features = $util.emptyObject;

        /**
         * PayReq blinded_paths.
         * @member {Array.<lnrpc.BlindedPaymentPath.$Properties>} blinded_paths
         * @memberof lnrpc.PayReq
         * @instance
         */
        PayReq.prototype.blinded_paths = $util.emptyArray;

        /**
         * Creates a new PayReq instance using the specified properties.
         * @function create
         * @memberof lnrpc.PayReq
         * @static
         * @param {lnrpc.PayReq.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PayReq} PayReq instance
         * @type {{
         *   (properties: lnrpc.PayReq.$Shape): lnrpc.PayReq & lnrpc.PayReq.$Shape;
         *   (properties?: lnrpc.PayReq.$Properties): lnrpc.PayReq;
         * }}
         */
        PayReq.create = function(properties) {
            return new PayReq(properties);
        };

        /**
         * Encodes the specified PayReq message. Does not implicitly {@link lnrpc.PayReq.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PayReq
         * @static
         * @param {lnrpc.PayReq.$Properties} message PayReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PayReq.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.destination != null && $Object.hasOwnProperty.call(message, "destination") && message.destination !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.destination);
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.payment_hash);
            if (message.num_satoshis != null && $Object.hasOwnProperty.call(message, "num_satoshis") && (typeof message.num_satoshis === "object" ? message.num_satoshis.low || message.num_satoshis.high : message.num_satoshis !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.num_satoshis);
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp") && (typeof message.timestamp === "object" ? message.timestamp.low || message.timestamp.high : message.timestamp !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.timestamp);
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry") && (typeof message.expiry === "object" ? message.expiry.low || message.expiry.high : message.expiry !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.expiry);
            if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.description);
            if (message.description_hash != null && $Object.hasOwnProperty.call(message, "description_hash") && message.description_hash !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.description_hash);
            if (message.fallback_addr != null && $Object.hasOwnProperty.call(message, "fallback_addr") && message.fallback_addr !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.fallback_addr);
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry") && (typeof message.cltv_expiry === "object" ? message.cltv_expiry.low || message.cltv_expiry.high : message.cltv_expiry !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.cltv_expiry);
            if (message.route_hints != null && message.route_hints.length)
                for (let i = 0; i < message.route_hints.length; ++i)
                    $root.lnrpc.RouteHint.encode(message.route_hints[i], writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr") && message.payment_addr.length)
                writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.payment_addr);
            if (message.num_msat != null && $Object.hasOwnProperty.call(message, "num_msat") && (typeof message.num_msat === "object" ? message.num_msat.low || message.num_msat.high : message.num_msat !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.num_msat);
            if (message.features != null && $Object.hasOwnProperty.call(message, "features"))
                for (let keys = $Object.keys(message.features), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 13, wireType 2 =*/106).fork().uint32(/* id 1, wireType 0 =*/8).uint32(keys[i]);
                    $root.lnrpc.Feature.encode(message.features[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.blinded_paths != null && message.blinded_paths.length)
                for (let i = 0; i < message.blinded_paths.length; ++i)
                    $root.lnrpc.BlindedPaymentPath.encode(message.blinded_paths[i], writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PayReq message, length delimited. Does not implicitly {@link lnrpc.PayReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PayReq
         * @static
         * @param {lnrpc.PayReq.$Properties} message PayReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PayReq.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PayReq message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PayReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PayReq & lnrpc.PayReq.$Shape} PayReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PayReq.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PayReq(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.destination = value;
                        else
                            delete message.destination;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.num_satoshis = value;
                        else
                            delete message.num_satoshis;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiry = value;
                        else
                            delete message.expiry;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.description = value;
                        else
                            delete message.description;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.description_hash = value;
                        else
                            delete message.description_hash;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.fallback_addr = value;
                        else
                            delete message.fallback_addr;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.cltv_expiry = value;
                        else
                            delete message.cltv_expiry;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.route_hints && message.route_hints.length))
                            message.route_hints = [];
                        message.route_hints.push($root.lnrpc.RouteHint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_addr = value;
                        else
                            delete message.payment_addr;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.num_msat = value;
                        else
                            delete message.num_msat;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if (message.features === $util.emptyObject)
                            message.features = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint32();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.Feature.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.features[key] = value || new $root.lnrpc.Feature();
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.blinded_paths && message.blinded_paths.length))
                            message.blinded_paths = [];
                        message.blinded_paths.push($root.lnrpc.BlindedPaymentPath.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PayReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PayReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PayReq & lnrpc.PayReq.$Shape} PayReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PayReq.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PayReq message.
         * @function verify
         * @memberof lnrpc.PayReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PayReq.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.destination != null && $Object.hasOwnProperty.call(message, "destination"))
                if (!$util.isString(message.destination))
                    return "destination: string expected";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!$util.isString(message.payment_hash))
                    return "payment_hash: string expected";
            if (message.num_satoshis != null && $Object.hasOwnProperty.call(message, "num_satoshis"))
                if (!$util.isInteger(message.num_satoshis) && !(message.num_satoshis && $util.isInteger(message.num_satoshis.low) && $util.isInteger(message.num_satoshis.high)))
                    return "num_satoshis: integer|Long expected";
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                if (!$util.isInteger(message.expiry) && !(message.expiry && $util.isInteger(message.expiry.low) && $util.isInteger(message.expiry.high)))
                    return "expiry: integer|Long expected";
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            if (message.description_hash != null && $Object.hasOwnProperty.call(message, "description_hash"))
                if (!$util.isString(message.description_hash))
                    return "description_hash: string expected";
            if (message.fallback_addr != null && $Object.hasOwnProperty.call(message, "fallback_addr"))
                if (!$util.isString(message.fallback_addr))
                    return "fallback_addr: string expected";
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry"))
                if (!$util.isInteger(message.cltv_expiry) && !(message.cltv_expiry && $util.isInteger(message.cltv_expiry.low) && $util.isInteger(message.cltv_expiry.high)))
                    return "cltv_expiry: integer|Long expected";
            if (message.route_hints != null && $Object.hasOwnProperty.call(message, "route_hints")) {
                if (!$Array.isArray(message.route_hints))
                    return "route_hints: array expected";
                for (let i = 0; i < message.route_hints.length; ++i) {
                    let error = $root.lnrpc.RouteHint.verify(message.route_hints[i], _depth + 1);
                    if (error)
                        return "route_hints." + error;
                }
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                if (!(message.payment_addr && typeof message.payment_addr.length === "number" || $util.isString(message.payment_addr)))
                    return "payment_addr: buffer expected";
            if (message.num_msat != null && $Object.hasOwnProperty.call(message, "num_msat"))
                if (!$util.isInteger(message.num_msat) && !(message.num_msat && $util.isInteger(message.num_msat.low) && $util.isInteger(message.num_msat.high)))
                    return "num_msat: integer|Long expected";
            if (message.features != null && $Object.hasOwnProperty.call(message, "features")) {
                if (!$util.isObject(message.features))
                    return "features: object expected";
                let key = $Object.keys(message.features);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "features: integer key{k:uint32} expected";
                    {
                        let error = $root.lnrpc.Feature.verify(message.features[key[i]], _depth + 1);
                        if (error)
                            return "features." + error;
                    }
                }
            }
            if (message.blinded_paths != null && $Object.hasOwnProperty.call(message, "blinded_paths")) {
                if (!$Array.isArray(message.blinded_paths))
                    return "blinded_paths: array expected";
                for (let i = 0; i < message.blinded_paths.length; ++i) {
                    let error = $root.lnrpc.BlindedPaymentPath.verify(message.blinded_paths[i], _depth + 1);
                    if (error)
                        return "blinded_paths." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PayReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PayReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PayReq} PayReq
         */
        PayReq.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PayReq)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PayReq: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PayReq();
            if (object.destination != null)
                if (typeof object.destination !== "string" || object.destination.length)
                    message.destination = $String(object.destination);
            if (object.payment_hash != null)
                if (typeof object.payment_hash !== "string" || object.payment_hash.length)
                    message.payment_hash = $String(object.payment_hash);
            if (object.num_satoshis != null)
                if (typeof object.num_satoshis === "object" ? object.num_satoshis.low || object.num_satoshis.high : $Number(object.num_satoshis) !== 0)
                    if ($util.Long)
                        message.num_satoshis = $util.Long.fromValue(object.num_satoshis, false);
                    else if (typeof object.num_satoshis === "string")
                        message.num_satoshis = $parseInt(object.num_satoshis, 10);
                    else if (typeof object.num_satoshis === "number")
                        message.num_satoshis = object.num_satoshis;
                    else if (typeof object.num_satoshis === "object")
                        message.num_satoshis = new $util.LongBits(object.num_satoshis.low >>> 0, object.num_satoshis.high >>> 0).toNumber();
            if (object.timestamp != null)
                if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = $parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
            if (object.expiry != null)
                if (typeof object.expiry === "object" ? object.expiry.low || object.expiry.high : $Number(object.expiry) !== 0)
                    if ($util.Long)
                        message.expiry = $util.Long.fromValue(object.expiry, false);
                    else if (typeof object.expiry === "string")
                        message.expiry = $parseInt(object.expiry, 10);
                    else if (typeof object.expiry === "number")
                        message.expiry = object.expiry;
                    else if (typeof object.expiry === "object")
                        message.expiry = new $util.LongBits(object.expiry.low >>> 0, object.expiry.high >>> 0).toNumber();
            if (object.description != null)
                if (typeof object.description !== "string" || object.description.length)
                    message.description = $String(object.description);
            if (object.description_hash != null)
                if (typeof object.description_hash !== "string" || object.description_hash.length)
                    message.description_hash = $String(object.description_hash);
            if (object.fallback_addr != null)
                if (typeof object.fallback_addr !== "string" || object.fallback_addr.length)
                    message.fallback_addr = $String(object.fallback_addr);
            if (object.cltv_expiry != null)
                if (typeof object.cltv_expiry === "object" ? object.cltv_expiry.low || object.cltv_expiry.high : $Number(object.cltv_expiry) !== 0)
                    if ($util.Long)
                        message.cltv_expiry = $util.Long.fromValue(object.cltv_expiry, false);
                    else if (typeof object.cltv_expiry === "string")
                        message.cltv_expiry = $parseInt(object.cltv_expiry, 10);
                    else if (typeof object.cltv_expiry === "number")
                        message.cltv_expiry = object.cltv_expiry;
                    else if (typeof object.cltv_expiry === "object")
                        message.cltv_expiry = new $util.LongBits(object.cltv_expiry.low >>> 0, object.cltv_expiry.high >>> 0).toNumber();
            if (object.route_hints) {
                if (!$Array.isArray(object.route_hints))
                    throw $TypeError(".lnrpc.PayReq.route_hints: array expected");
                message.route_hints = $Array(object.route_hints.length);
                for (let i = 0; i < object.route_hints.length; ++i) {
                    if (!$util.isObject(object.route_hints[i]))
                        throw $TypeError(".lnrpc.PayReq.route_hints: object expected");
                    message.route_hints[i] = $root.lnrpc.RouteHint.fromObject(object.route_hints[i], _depth + 1);
                }
            }
            if (object.payment_addr != null)
                if (object.payment_addr.length)
                    if (typeof object.payment_addr === "string")
                        $util.base64.decode(object.payment_addr, message.payment_addr = $util.newBuffer($util.base64.length(object.payment_addr)), 0);
                    else if (object.payment_addr.length >= 0)
                        message.payment_addr = object.payment_addr;
            if (object.num_msat != null)
                if (typeof object.num_msat === "object" ? object.num_msat.low || object.num_msat.high : $Number(object.num_msat) !== 0)
                    if ($util.Long)
                        message.num_msat = $util.Long.fromValue(object.num_msat, false);
                    else if (typeof object.num_msat === "string")
                        message.num_msat = $parseInt(object.num_msat, 10);
                    else if (typeof object.num_msat === "number")
                        message.num_msat = object.num_msat;
                    else if (typeof object.num_msat === "object")
                        message.num_msat = new $util.LongBits(object.num_msat.low >>> 0, object.num_msat.high >>> 0).toNumber();
            if (object.features) {
                if (!$util.isObject(object.features))
                    throw $TypeError(".lnrpc.PayReq.features: object expected");
                message.features = {};
                for (let keys = $Object.keys(object.features), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.features, keys[i]);
                    if (!$util.isObject(object.features[keys[i]]))
                        throw $TypeError(".lnrpc.PayReq.features: object expected");
                    message.features[keys[i]] = $root.lnrpc.Feature.fromObject(object.features[keys[i]], _depth + 1);
                }
            }
            if (object.blinded_paths) {
                if (!$Array.isArray(object.blinded_paths))
                    throw $TypeError(".lnrpc.PayReq.blinded_paths: array expected");
                message.blinded_paths = $Array(object.blinded_paths.length);
                for (let i = 0; i < object.blinded_paths.length; ++i) {
                    if (!$util.isObject(object.blinded_paths[i]))
                        throw $TypeError(".lnrpc.PayReq.blinded_paths: object expected");
                    message.blinded_paths[i] = $root.lnrpc.BlindedPaymentPath.fromObject(object.blinded_paths[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PayReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PayReq
         * @static
         * @param {lnrpc.PayReq} message PayReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PayReq.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.route_hints = [];
                object.blinded_paths = [];
            }
            if (options.objects || options.defaults)
                object.features = {};
            if (options.defaults) {
                object.destination = "";
                object.payment_hash = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.num_satoshis = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.num_satoshis = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiry = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiry = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.description = "";
                object.description_hash = "";
                object.fallback_addr = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.cltv_expiry = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.cltv_expiry = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.payment_addr = "";
                else {
                    object.payment_addr = [];
                    if (options.bytes !== $Array)
                        object.payment_addr = $util.newBuffer(object.payment_addr);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.num_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.num_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.destination != null && $Object.hasOwnProperty.call(message, "destination"))
                object.destination = message.destination;
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = message.payment_hash;
            if (message.num_satoshis != null && $Object.hasOwnProperty.call(message, "num_satoshis"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.num_satoshis = typeof message.num_satoshis === "number" ? $BigInt(message.num_satoshis) : $util.Long.fromBits(message.num_satoshis.low >>> 0, message.num_satoshis.high >>> 0, false).toBigInt();
                else if (typeof message.num_satoshis === "number")
                    object.num_satoshis = options.longs === $String ? $String(message.num_satoshis) : message.num_satoshis;
                else
                    object.num_satoshis = options.longs === $String ? $util.Long.prototype.toString.call(message.num_satoshis) : options.longs === $Number ? new $util.LongBits(message.num_satoshis.low >>> 0, message.num_satoshis.high >>> 0).toNumber() : message.num_satoshis;
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiry = typeof message.expiry === "number" ? $BigInt(message.expiry) : $util.Long.fromBits(message.expiry.low >>> 0, message.expiry.high >>> 0, false).toBigInt();
                else if (typeof message.expiry === "number")
                    object.expiry = options.longs === $String ? $String(message.expiry) : message.expiry;
                else
                    object.expiry = options.longs === $String ? $util.Long.prototype.toString.call(message.expiry) : options.longs === $Number ? new $util.LongBits(message.expiry.low >>> 0, message.expiry.high >>> 0).toNumber() : message.expiry;
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.description_hash != null && $Object.hasOwnProperty.call(message, "description_hash"))
                object.description_hash = message.description_hash;
            if (message.fallback_addr != null && $Object.hasOwnProperty.call(message, "fallback_addr"))
                object.fallback_addr = message.fallback_addr;
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.cltv_expiry = typeof message.cltv_expiry === "number" ? $BigInt(message.cltv_expiry) : $util.Long.fromBits(message.cltv_expiry.low >>> 0, message.cltv_expiry.high >>> 0, false).toBigInt();
                else if (typeof message.cltv_expiry === "number")
                    object.cltv_expiry = options.longs === $String ? $String(message.cltv_expiry) : message.cltv_expiry;
                else
                    object.cltv_expiry = options.longs === $String ? $util.Long.prototype.toString.call(message.cltv_expiry) : options.longs === $Number ? new $util.LongBits(message.cltv_expiry.low >>> 0, message.cltv_expiry.high >>> 0).toNumber() : message.cltv_expiry;
            if (message.route_hints && message.route_hints.length) {
                object.route_hints = $Array(message.route_hints.length);
                for (let j = 0; j < message.route_hints.length; ++j)
                    object.route_hints[j] = $root.lnrpc.RouteHint.toObject(message.route_hints[j], options, _depth + 1);
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                object.payment_addr = options.bytes === $String ? $util.base64.encode(message.payment_addr, 0, message.payment_addr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_addr) : message.payment_addr;
            if (message.num_msat != null && $Object.hasOwnProperty.call(message, "num_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.num_msat = typeof message.num_msat === "number" ? $BigInt(message.num_msat) : $util.Long.fromBits(message.num_msat.low >>> 0, message.num_msat.high >>> 0, false).toBigInt();
                else if (typeof message.num_msat === "number")
                    object.num_msat = options.longs === $String ? $String(message.num_msat) : message.num_msat;
                else
                    object.num_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.num_msat) : options.longs === $Number ? new $util.LongBits(message.num_msat.low >>> 0, message.num_msat.high >>> 0).toNumber() : message.num_msat;
            let keys2;
            if (message.features && (keys2 = $Object.keys(message.features)).length) {
                object.features = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.features, keys2[j]);
                    object.features[keys2[j]] = $root.lnrpc.Feature.toObject(message.features[keys2[j]], options, _depth + 1);
                }
            }
            if (message.blinded_paths && message.blinded_paths.length) {
                object.blinded_paths = $Array(message.blinded_paths.length);
                for (let j = 0; j < message.blinded_paths.length; ++j)
                    object.blinded_paths[j] = $root.lnrpc.BlindedPaymentPath.toObject(message.blinded_paths[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this PayReq to JSON.
         * @function toJSON
         * @memberof lnrpc.PayReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PayReq.prototype.toJSON = function() {
            return PayReq.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PayReq
         * @function getTypeUrl
         * @memberof lnrpc.PayReq
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PayReq.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PayReq";
        };

        return PayReq;
    })();

    /**
     * FeatureBit enum.
     * @name lnrpc.FeatureBit
     * @enum {number}
     * @property {number} DATALOSS_PROTECT_REQ=0 DATALOSS_PROTECT_REQ value
     * @property {number} DATALOSS_PROTECT_OPT=1 DATALOSS_PROTECT_OPT value
     * @property {number} INITIAL_ROUING_SYNC=3 INITIAL_ROUING_SYNC value
     * @property {number} UPFRONT_SHUTDOWN_SCRIPT_REQ=4 UPFRONT_SHUTDOWN_SCRIPT_REQ value
     * @property {number} UPFRONT_SHUTDOWN_SCRIPT_OPT=5 UPFRONT_SHUTDOWN_SCRIPT_OPT value
     * @property {number} GOSSIP_QUERIES_REQ=6 GOSSIP_QUERIES_REQ value
     * @property {number} GOSSIP_QUERIES_OPT=7 GOSSIP_QUERIES_OPT value
     * @property {number} TLV_ONION_REQ=8 TLV_ONION_REQ value
     * @property {number} TLV_ONION_OPT=9 TLV_ONION_OPT value
     * @property {number} EXT_GOSSIP_QUERIES_REQ=10 EXT_GOSSIP_QUERIES_REQ value
     * @property {number} EXT_GOSSIP_QUERIES_OPT=11 EXT_GOSSIP_QUERIES_OPT value
     * @property {number} STATIC_REMOTE_KEY_REQ=12 STATIC_REMOTE_KEY_REQ value
     * @property {number} STATIC_REMOTE_KEY_OPT=13 STATIC_REMOTE_KEY_OPT value
     * @property {number} PAYMENT_ADDR_REQ=14 PAYMENT_ADDR_REQ value
     * @property {number} PAYMENT_ADDR_OPT=15 PAYMENT_ADDR_OPT value
     * @property {number} MPP_REQ=16 MPP_REQ value
     * @property {number} MPP_OPT=17 MPP_OPT value
     * @property {number} WUMBO_CHANNELS_REQ=18 WUMBO_CHANNELS_REQ value
     * @property {number} WUMBO_CHANNELS_OPT=19 WUMBO_CHANNELS_OPT value
     * @property {number} ANCHORS_REQ=20 ANCHORS_REQ value
     * @property {number} ANCHORS_OPT=21 ANCHORS_OPT value
     * @property {number} ANCHORS_ZERO_FEE_HTLC_REQ=22 ANCHORS_ZERO_FEE_HTLC_REQ value
     * @property {number} ANCHORS_ZERO_FEE_HTLC_OPT=23 ANCHORS_ZERO_FEE_HTLC_OPT value
     * @property {number} ROUTE_BLINDING_REQUIRED=24 ROUTE_BLINDING_REQUIRED value
     * @property {number} ROUTE_BLINDING_OPTIONAL=25 ROUTE_BLINDING_OPTIONAL value
     * @property {number} AMP_REQ=30 AMP_REQ value
     * @property {number} AMP_OPT=31 AMP_OPT value
     */
    lnrpc.FeatureBit = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "DATALOSS_PROTECT_REQ"] = 0;
        values[valuesById[1] = "DATALOSS_PROTECT_OPT"] = 1;
        values[valuesById[3] = "INITIAL_ROUING_SYNC"] = 3;
        values[valuesById[4] = "UPFRONT_SHUTDOWN_SCRIPT_REQ"] = 4;
        values[valuesById[5] = "UPFRONT_SHUTDOWN_SCRIPT_OPT"] = 5;
        values[valuesById[6] = "GOSSIP_QUERIES_REQ"] = 6;
        values[valuesById[7] = "GOSSIP_QUERIES_OPT"] = 7;
        values[valuesById[8] = "TLV_ONION_REQ"] = 8;
        values[valuesById[9] = "TLV_ONION_OPT"] = 9;
        values[valuesById[10] = "EXT_GOSSIP_QUERIES_REQ"] = 10;
        values[valuesById[11] = "EXT_GOSSIP_QUERIES_OPT"] = 11;
        values[valuesById[12] = "STATIC_REMOTE_KEY_REQ"] = 12;
        values[valuesById[13] = "STATIC_REMOTE_KEY_OPT"] = 13;
        values[valuesById[14] = "PAYMENT_ADDR_REQ"] = 14;
        values[valuesById[15] = "PAYMENT_ADDR_OPT"] = 15;
        values[valuesById[16] = "MPP_REQ"] = 16;
        values[valuesById[17] = "MPP_OPT"] = 17;
        values[valuesById[18] = "WUMBO_CHANNELS_REQ"] = 18;
        values[valuesById[19] = "WUMBO_CHANNELS_OPT"] = 19;
        values[valuesById[20] = "ANCHORS_REQ"] = 20;
        values[valuesById[21] = "ANCHORS_OPT"] = 21;
        values[valuesById[22] = "ANCHORS_ZERO_FEE_HTLC_REQ"] = 22;
        values[valuesById[23] = "ANCHORS_ZERO_FEE_HTLC_OPT"] = 23;
        values[valuesById[24] = "ROUTE_BLINDING_REQUIRED"] = 24;
        values[valuesById[25] = "ROUTE_BLINDING_OPTIONAL"] = 25;
        values[valuesById[30] = "AMP_REQ"] = 30;
        values[valuesById[31] = "AMP_OPT"] = 31;
        return values;
    })();

    lnrpc.Feature = (function() {

        /**
         * Properties of a Feature.
         * @typedef {Object} lnrpc.Feature.$Properties
         * @property {string|null} [name] Feature name
         * @property {boolean|null} [is_required] Feature is_required
         * @property {boolean|null} [is_known] Feature is_known
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Feature.
         * @memberof lnrpc
         * @interface IFeature
         * @augments lnrpc.Feature.$Properties
         * @deprecated Use lnrpc.Feature.$Properties instead.
         */

        /**
         * Shape of a Feature.
         * @typedef {lnrpc.Feature.$Properties} lnrpc.Feature.$Shape
         */

        /**
         * Constructs a new Feature.
         * @memberof lnrpc
         * @classdesc Represents a Feature.
         * @constructor
         * @param {lnrpc.Feature.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Feature = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Feature name.
         * @member {string} name
         * @memberof lnrpc.Feature
         * @instance
         */
        Feature.prototype.name = "";

        /**
         * Feature is_required.
         * @member {boolean} is_required
         * @memberof lnrpc.Feature
         * @instance
         */
        Feature.prototype.is_required = false;

        /**
         * Feature is_known.
         * @member {boolean} is_known
         * @memberof lnrpc.Feature
         * @instance
         */
        Feature.prototype.is_known = false;

        /**
         * Creates a new Feature instance using the specified properties.
         * @function create
         * @memberof lnrpc.Feature
         * @static
         * @param {lnrpc.Feature.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Feature} Feature instance
         * @type {{
         *   (properties: lnrpc.Feature.$Shape): lnrpc.Feature & lnrpc.Feature.$Shape;
         *   (properties?: lnrpc.Feature.$Properties): lnrpc.Feature;
         * }}
         */
        Feature.create = function(properties) {
            return new Feature(properties);
        };

        /**
         * Encodes the specified Feature message. Does not implicitly {@link lnrpc.Feature.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Feature
         * @static
         * @param {lnrpc.Feature.$Properties} message Feature message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Feature.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.is_required != null && $Object.hasOwnProperty.call(message, "is_required") && message.is_required !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.is_required);
            if (message.is_known != null && $Object.hasOwnProperty.call(message, "is_known") && message.is_known !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.is_known);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Feature message, length delimited. Does not implicitly {@link lnrpc.Feature.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Feature
         * @static
         * @param {lnrpc.Feature.$Properties} message Feature message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Feature.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Feature message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Feature
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Feature & lnrpc.Feature.$Shape} Feature
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Feature.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Feature(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_required = value;
                        else
                            delete message.is_required;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_known = value;
                        else
                            delete message.is_known;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Feature message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Feature
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Feature & lnrpc.Feature.$Shape} Feature
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Feature.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Feature message.
         * @function verify
         * @memberof lnrpc.Feature
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Feature.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.is_required != null && $Object.hasOwnProperty.call(message, "is_required"))
                if (typeof message.is_required !== "boolean")
                    return "is_required: boolean expected";
            if (message.is_known != null && $Object.hasOwnProperty.call(message, "is_known"))
                if (typeof message.is_known !== "boolean")
                    return "is_known: boolean expected";
            return null;
        };

        /**
         * Creates a Feature message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Feature
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Feature} Feature
         */
        Feature.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Feature)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Feature: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Feature();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.is_required != null)
                if (object.is_required)
                    message.is_required = $Boolean(object.is_required);
            if (object.is_known != null)
                if (object.is_known)
                    message.is_known = $Boolean(object.is_known);
            return message;
        };

        /**
         * Creates a plain object from a Feature message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Feature
         * @static
         * @param {lnrpc.Feature} message Feature
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Feature.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.is_required = false;
                object.is_known = false;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.is_required != null && $Object.hasOwnProperty.call(message, "is_required"))
                object.is_required = message.is_required;
            if (message.is_known != null && $Object.hasOwnProperty.call(message, "is_known"))
                object.is_known = message.is_known;
            return object;
        };

        /**
         * Converts this Feature to JSON.
         * @function toJSON
         * @memberof lnrpc.Feature
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Feature.prototype.toJSON = function() {
            return Feature.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Feature
         * @function getTypeUrl
         * @memberof lnrpc.Feature
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Feature.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Feature";
        };

        return Feature;
    })();

    lnrpc.FeeReportRequest = (function() {

        /**
         * Properties of a FeeReportRequest.
         * @typedef {Object} lnrpc.FeeReportRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FeeReportRequest.
         * @memberof lnrpc
         * @interface IFeeReportRequest
         * @augments lnrpc.FeeReportRequest.$Properties
         * @deprecated Use lnrpc.FeeReportRequest.$Properties instead.
         */

        /**
         * Shape of a FeeReportRequest.
         * @typedef {lnrpc.FeeReportRequest.$Properties} lnrpc.FeeReportRequest.$Shape
         */

        /**
         * Constructs a new FeeReportRequest.
         * @memberof lnrpc
         * @classdesc Represents a FeeReportRequest.
         * @constructor
         * @param {lnrpc.FeeReportRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FeeReportRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new FeeReportRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.FeeReportRequest
         * @static
         * @param {lnrpc.FeeReportRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FeeReportRequest} FeeReportRequest instance
         * @type {{
         *   (properties: lnrpc.FeeReportRequest.$Shape): lnrpc.FeeReportRequest & lnrpc.FeeReportRequest.$Shape;
         *   (properties?: lnrpc.FeeReportRequest.$Properties): lnrpc.FeeReportRequest;
         * }}
         */
        FeeReportRequest.create = function(properties) {
            return new FeeReportRequest(properties);
        };

        /**
         * Encodes the specified FeeReportRequest message. Does not implicitly {@link lnrpc.FeeReportRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FeeReportRequest
         * @static
         * @param {lnrpc.FeeReportRequest.$Properties} message FeeReportRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeReportRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FeeReportRequest message, length delimited. Does not implicitly {@link lnrpc.FeeReportRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FeeReportRequest
         * @static
         * @param {lnrpc.FeeReportRequest.$Properties} message FeeReportRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeReportRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FeeReportRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FeeReportRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FeeReportRequest & lnrpc.FeeReportRequest.$Shape} FeeReportRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeReportRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FeeReportRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FeeReportRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FeeReportRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FeeReportRequest & lnrpc.FeeReportRequest.$Shape} FeeReportRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeReportRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeeReportRequest message.
         * @function verify
         * @memberof lnrpc.FeeReportRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeeReportRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a FeeReportRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FeeReportRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FeeReportRequest} FeeReportRequest
         */
        FeeReportRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FeeReportRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FeeReportRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.FeeReportRequest();
        };

        /**
         * Creates a plain object from a FeeReportRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FeeReportRequest
         * @static
         * @param {lnrpc.FeeReportRequest} message FeeReportRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeeReportRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this FeeReportRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.FeeReportRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeeReportRequest.prototype.toJSON = function() {
            return FeeReportRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FeeReportRequest
         * @function getTypeUrl
         * @memberof lnrpc.FeeReportRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FeeReportRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FeeReportRequest";
        };

        return FeeReportRequest;
    })();

    lnrpc.ChannelFeeReport = (function() {

        /**
         * Properties of a ChannelFeeReport.
         * @typedef {Object} lnrpc.ChannelFeeReport.$Properties
         * @property {Long|null} [chan_id] ChannelFeeReport chan_id
         * @property {string|null} [channel_point] ChannelFeeReport channel_point
         * @property {Long|null} [base_fee_msat] ChannelFeeReport base_fee_msat
         * @property {Long|null} [fee_per_mil] ChannelFeeReport fee_per_mil
         * @property {number|null} [fee_rate] ChannelFeeReport fee_rate
         * @property {number|null} [inbound_base_fee_msat] ChannelFeeReport inbound_base_fee_msat
         * @property {number|null} [inbound_fee_per_mil] ChannelFeeReport inbound_fee_per_mil
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelFeeReport.
         * @memberof lnrpc
         * @interface IChannelFeeReport
         * @augments lnrpc.ChannelFeeReport.$Properties
         * @deprecated Use lnrpc.ChannelFeeReport.$Properties instead.
         */

        /**
         * Shape of a ChannelFeeReport.
         * @typedef {lnrpc.ChannelFeeReport.$Properties} lnrpc.ChannelFeeReport.$Shape
         */

        /**
         * Constructs a new ChannelFeeReport.
         * @memberof lnrpc
         * @classdesc Represents a ChannelFeeReport.
         * @constructor
         * @param {lnrpc.ChannelFeeReport.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelFeeReport = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelFeeReport chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.ChannelFeeReport
         * @instance
         */
        ChannelFeeReport.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelFeeReport channel_point.
         * @member {string} channel_point
         * @memberof lnrpc.ChannelFeeReport
         * @instance
         */
        ChannelFeeReport.prototype.channel_point = "";

        /**
         * ChannelFeeReport base_fee_msat.
         * @member {Long} base_fee_msat
         * @memberof lnrpc.ChannelFeeReport
         * @instance
         */
        ChannelFeeReport.prototype.base_fee_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChannelFeeReport fee_per_mil.
         * @member {Long} fee_per_mil
         * @memberof lnrpc.ChannelFeeReport
         * @instance
         */
        ChannelFeeReport.prototype.fee_per_mil = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChannelFeeReport fee_rate.
         * @member {number} fee_rate
         * @memberof lnrpc.ChannelFeeReport
         * @instance
         */
        ChannelFeeReport.prototype.fee_rate = 0;

        /**
         * ChannelFeeReport inbound_base_fee_msat.
         * @member {number} inbound_base_fee_msat
         * @memberof lnrpc.ChannelFeeReport
         * @instance
         */
        ChannelFeeReport.prototype.inbound_base_fee_msat = 0;

        /**
         * ChannelFeeReport inbound_fee_per_mil.
         * @member {number} inbound_fee_per_mil
         * @memberof lnrpc.ChannelFeeReport
         * @instance
         */
        ChannelFeeReport.prototype.inbound_fee_per_mil = 0;

        /**
         * Creates a new ChannelFeeReport instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelFeeReport
         * @static
         * @param {lnrpc.ChannelFeeReport.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelFeeReport} ChannelFeeReport instance
         * @type {{
         *   (properties: lnrpc.ChannelFeeReport.$Shape): lnrpc.ChannelFeeReport & lnrpc.ChannelFeeReport.$Shape;
         *   (properties?: lnrpc.ChannelFeeReport.$Properties): lnrpc.ChannelFeeReport;
         * }}
         */
        ChannelFeeReport.create = function(properties) {
            return new ChannelFeeReport(properties);
        };

        /**
         * Encodes the specified ChannelFeeReport message. Does not implicitly {@link lnrpc.ChannelFeeReport.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelFeeReport
         * @static
         * @param {lnrpc.ChannelFeeReport.$Properties} message ChannelFeeReport message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelFeeReport.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point") && message.channel_point !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.channel_point);
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat") && (typeof message.base_fee_msat === "object" ? message.base_fee_msat.low || message.base_fee_msat.high : message.base_fee_msat !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.base_fee_msat);
            if (message.fee_per_mil != null && $Object.hasOwnProperty.call(message, "fee_per_mil") && (typeof message.fee_per_mil === "object" ? message.fee_per_mil.low || message.fee_per_mil.high : message.fee_per_mil !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.fee_per_mil);
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate") && !$Object.is(message.fee_rate, 0))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.fee_rate);
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.chan_id);
            if (message.inbound_base_fee_msat != null && $Object.hasOwnProperty.call(message, "inbound_base_fee_msat") && message.inbound_base_fee_msat !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.inbound_base_fee_msat);
            if (message.inbound_fee_per_mil != null && $Object.hasOwnProperty.call(message, "inbound_fee_per_mil") && message.inbound_fee_per_mil !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.inbound_fee_per_mil);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelFeeReport message, length delimited. Does not implicitly {@link lnrpc.ChannelFeeReport.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelFeeReport
         * @static
         * @param {lnrpc.ChannelFeeReport.$Properties} message ChannelFeeReport message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelFeeReport.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelFeeReport message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelFeeReport
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelFeeReport & lnrpc.ChannelFeeReport.$Shape} ChannelFeeReport
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelFeeReport.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelFeeReport(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.channel_point = value;
                        else
                            delete message.channel_point;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.base_fee_msat = value;
                        else
                            delete message.base_fee_msat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_per_mil = value;
                        else
                            delete message.fee_per_mil;
                        continue;
                    }
                case 4: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.fee_rate = value;
                        else
                            delete message.fee_rate;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.inbound_base_fee_msat = value;
                        else
                            delete message.inbound_base_fee_msat;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.inbound_fee_per_mil = value;
                        else
                            delete message.inbound_fee_per_mil;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelFeeReport message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelFeeReport
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelFeeReport & lnrpc.ChannelFeeReport.$Shape} ChannelFeeReport
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelFeeReport.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelFeeReport message.
         * @function verify
         * @memberof lnrpc.ChannelFeeReport
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelFeeReport.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                if (!$util.isString(message.channel_point))
                    return "channel_point: string expected";
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat"))
                if (!$util.isInteger(message.base_fee_msat) && !(message.base_fee_msat && $util.isInteger(message.base_fee_msat.low) && $util.isInteger(message.base_fee_msat.high)))
                    return "base_fee_msat: integer|Long expected";
            if (message.fee_per_mil != null && $Object.hasOwnProperty.call(message, "fee_per_mil"))
                if (!$util.isInteger(message.fee_per_mil) && !(message.fee_per_mil && $util.isInteger(message.fee_per_mil.low) && $util.isInteger(message.fee_per_mil.high)))
                    return "fee_per_mil: integer|Long expected";
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                if (typeof message.fee_rate !== "number")
                    return "fee_rate: number expected";
            if (message.inbound_base_fee_msat != null && $Object.hasOwnProperty.call(message, "inbound_base_fee_msat"))
                if (!$util.isInteger(message.inbound_base_fee_msat))
                    return "inbound_base_fee_msat: integer expected";
            if (message.inbound_fee_per_mil != null && $Object.hasOwnProperty.call(message, "inbound_fee_per_mil"))
                if (!$util.isInteger(message.inbound_fee_per_mil))
                    return "inbound_fee_per_mil: integer expected";
            return null;
        };

        /**
         * Creates a ChannelFeeReport message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelFeeReport
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelFeeReport} ChannelFeeReport
         */
        ChannelFeeReport.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelFeeReport)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelFeeReport: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelFeeReport();
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.channel_point != null)
                if (typeof object.channel_point !== "string" || object.channel_point.length)
                    message.channel_point = $String(object.channel_point);
            if (object.base_fee_msat != null)
                if (typeof object.base_fee_msat === "object" ? object.base_fee_msat.low || object.base_fee_msat.high : $Number(object.base_fee_msat) !== 0)
                    if ($util.Long)
                        message.base_fee_msat = $util.Long.fromValue(object.base_fee_msat, false);
                    else if (typeof object.base_fee_msat === "string")
                        message.base_fee_msat = $parseInt(object.base_fee_msat, 10);
                    else if (typeof object.base_fee_msat === "number")
                        message.base_fee_msat = object.base_fee_msat;
                    else if (typeof object.base_fee_msat === "object")
                        message.base_fee_msat = new $util.LongBits(object.base_fee_msat.low >>> 0, object.base_fee_msat.high >>> 0).toNumber();
            if (object.fee_per_mil != null)
                if (typeof object.fee_per_mil === "object" ? object.fee_per_mil.low || object.fee_per_mil.high : $Number(object.fee_per_mil) !== 0)
                    if ($util.Long)
                        message.fee_per_mil = $util.Long.fromValue(object.fee_per_mil, false);
                    else if (typeof object.fee_per_mil === "string")
                        message.fee_per_mil = $parseInt(object.fee_per_mil, 10);
                    else if (typeof object.fee_per_mil === "number")
                        message.fee_per_mil = object.fee_per_mil;
                    else if (typeof object.fee_per_mil === "object")
                        message.fee_per_mil = new $util.LongBits(object.fee_per_mil.low >>> 0, object.fee_per_mil.high >>> 0).toNumber();
            if (object.fee_rate != null)
                if (!$Object.is($Number(object.fee_rate), 0))
                    message.fee_rate = $Number(object.fee_rate);
            if (object.inbound_base_fee_msat != null)
                if ($Number(object.inbound_base_fee_msat) !== 0)
                    message.inbound_base_fee_msat = object.inbound_base_fee_msat | 0;
            if (object.inbound_fee_per_mil != null)
                if ($Number(object.inbound_fee_per_mil) !== 0)
                    message.inbound_fee_per_mil = object.inbound_fee_per_mil | 0;
            return message;
        };

        /**
         * Creates a plain object from a ChannelFeeReport message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelFeeReport
         * @static
         * @param {lnrpc.ChannelFeeReport} message ChannelFeeReport
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelFeeReport.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.channel_point = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.base_fee_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.base_fee_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_per_mil = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_per_mil = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.fee_rate = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.inbound_base_fee_msat = 0;
                object.inbound_fee_per_mil = 0;
            }
            if (message.channel_point != null && $Object.hasOwnProperty.call(message, "channel_point"))
                object.channel_point = message.channel_point;
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.base_fee_msat = typeof message.base_fee_msat === "number" ? $BigInt(message.base_fee_msat) : $util.Long.fromBits(message.base_fee_msat.low >>> 0, message.base_fee_msat.high >>> 0, false).toBigInt();
                else if (typeof message.base_fee_msat === "number")
                    object.base_fee_msat = options.longs === $String ? $String(message.base_fee_msat) : message.base_fee_msat;
                else
                    object.base_fee_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.base_fee_msat) : options.longs === $Number ? new $util.LongBits(message.base_fee_msat.low >>> 0, message.base_fee_msat.high >>> 0).toNumber() : message.base_fee_msat;
            if (message.fee_per_mil != null && $Object.hasOwnProperty.call(message, "fee_per_mil"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_per_mil = typeof message.fee_per_mil === "number" ? $BigInt(message.fee_per_mil) : $util.Long.fromBits(message.fee_per_mil.low >>> 0, message.fee_per_mil.high >>> 0, false).toBigInt();
                else if (typeof message.fee_per_mil === "number")
                    object.fee_per_mil = options.longs === $String ? $String(message.fee_per_mil) : message.fee_per_mil;
                else
                    object.fee_per_mil = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_per_mil) : options.longs === $Number ? new $util.LongBits(message.fee_per_mil.low >>> 0, message.fee_per_mil.high >>> 0).toNumber() : message.fee_per_mil;
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                object.fee_rate = options.json && !$isFinite(message.fee_rate) ? $String(message.fee_rate) : message.fee_rate;
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.inbound_base_fee_msat != null && $Object.hasOwnProperty.call(message, "inbound_base_fee_msat"))
                object.inbound_base_fee_msat = message.inbound_base_fee_msat;
            if (message.inbound_fee_per_mil != null && $Object.hasOwnProperty.call(message, "inbound_fee_per_mil"))
                object.inbound_fee_per_mil = message.inbound_fee_per_mil;
            return object;
        };

        /**
         * Converts this ChannelFeeReport to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelFeeReport
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelFeeReport.prototype.toJSON = function() {
            return ChannelFeeReport.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelFeeReport
         * @function getTypeUrl
         * @memberof lnrpc.ChannelFeeReport
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelFeeReport.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelFeeReport";
        };

        return ChannelFeeReport;
    })();

    lnrpc.FeeReportResponse = (function() {

        /**
         * Properties of a FeeReportResponse.
         * @typedef {Object} lnrpc.FeeReportResponse.$Properties
         * @property {Array.<lnrpc.ChannelFeeReport.$Properties>|null} [channel_fees] FeeReportResponse channel_fees
         * @property {Long|null} [day_fee_sum] FeeReportResponse day_fee_sum
         * @property {Long|null} [week_fee_sum] FeeReportResponse week_fee_sum
         * @property {Long|null} [month_fee_sum] FeeReportResponse month_fee_sum
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FeeReportResponse.
         * @memberof lnrpc
         * @interface IFeeReportResponse
         * @augments lnrpc.FeeReportResponse.$Properties
         * @deprecated Use lnrpc.FeeReportResponse.$Properties instead.
         */

        /**
         * Shape of a FeeReportResponse.
         * @typedef {lnrpc.FeeReportResponse.$Properties} lnrpc.FeeReportResponse.$Shape
         */

        /**
         * Constructs a new FeeReportResponse.
         * @memberof lnrpc
         * @classdesc Represents a FeeReportResponse.
         * @constructor
         * @param {lnrpc.FeeReportResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FeeReportResponse = function (properties) {
            this.channel_fees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FeeReportResponse channel_fees.
         * @member {Array.<lnrpc.ChannelFeeReport.$Properties>} channel_fees
         * @memberof lnrpc.FeeReportResponse
         * @instance
         */
        FeeReportResponse.prototype.channel_fees = $util.emptyArray;

        /**
         * FeeReportResponse day_fee_sum.
         * @member {Long} day_fee_sum
         * @memberof lnrpc.FeeReportResponse
         * @instance
         */
        FeeReportResponse.prototype.day_fee_sum = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FeeReportResponse week_fee_sum.
         * @member {Long} week_fee_sum
         * @memberof lnrpc.FeeReportResponse
         * @instance
         */
        FeeReportResponse.prototype.week_fee_sum = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FeeReportResponse month_fee_sum.
         * @member {Long} month_fee_sum
         * @memberof lnrpc.FeeReportResponse
         * @instance
         */
        FeeReportResponse.prototype.month_fee_sum = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new FeeReportResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.FeeReportResponse
         * @static
         * @param {lnrpc.FeeReportResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FeeReportResponse} FeeReportResponse instance
         * @type {{
         *   (properties: lnrpc.FeeReportResponse.$Shape): lnrpc.FeeReportResponse & lnrpc.FeeReportResponse.$Shape;
         *   (properties?: lnrpc.FeeReportResponse.$Properties): lnrpc.FeeReportResponse;
         * }}
         */
        FeeReportResponse.create = function(properties) {
            return new FeeReportResponse(properties);
        };

        /**
         * Encodes the specified FeeReportResponse message. Does not implicitly {@link lnrpc.FeeReportResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FeeReportResponse
         * @static
         * @param {lnrpc.FeeReportResponse.$Properties} message FeeReportResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeReportResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.channel_fees != null && message.channel_fees.length)
                for (let i = 0; i < message.channel_fees.length; ++i)
                    $root.lnrpc.ChannelFeeReport.encode(message.channel_fees[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.day_fee_sum != null && $Object.hasOwnProperty.call(message, "day_fee_sum") && (typeof message.day_fee_sum === "object" ? message.day_fee_sum.low || message.day_fee_sum.high : message.day_fee_sum !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.day_fee_sum);
            if (message.week_fee_sum != null && $Object.hasOwnProperty.call(message, "week_fee_sum") && (typeof message.week_fee_sum === "object" ? message.week_fee_sum.low || message.week_fee_sum.high : message.week_fee_sum !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.week_fee_sum);
            if (message.month_fee_sum != null && $Object.hasOwnProperty.call(message, "month_fee_sum") && (typeof message.month_fee_sum === "object" ? message.month_fee_sum.low || message.month_fee_sum.high : message.month_fee_sum !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.month_fee_sum);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FeeReportResponse message, length delimited. Does not implicitly {@link lnrpc.FeeReportResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FeeReportResponse
         * @static
         * @param {lnrpc.FeeReportResponse.$Properties} message FeeReportResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeReportResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FeeReportResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FeeReportResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FeeReportResponse & lnrpc.FeeReportResponse.$Shape} FeeReportResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeReportResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FeeReportResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.channel_fees && message.channel_fees.length))
                            message.channel_fees = [];
                        message.channel_fees.push($root.lnrpc.ChannelFeeReport.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.day_fee_sum = value;
                        else
                            delete message.day_fee_sum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.week_fee_sum = value;
                        else
                            delete message.week_fee_sum;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.month_fee_sum = value;
                        else
                            delete message.month_fee_sum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FeeReportResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FeeReportResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FeeReportResponse & lnrpc.FeeReportResponse.$Shape} FeeReportResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeReportResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeeReportResponse message.
         * @function verify
         * @memberof lnrpc.FeeReportResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeeReportResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.channel_fees != null && $Object.hasOwnProperty.call(message, "channel_fees")) {
                if (!$Array.isArray(message.channel_fees))
                    return "channel_fees: array expected";
                for (let i = 0; i < message.channel_fees.length; ++i) {
                    let error = $root.lnrpc.ChannelFeeReport.verify(message.channel_fees[i], _depth + 1);
                    if (error)
                        return "channel_fees." + error;
                }
            }
            if (message.day_fee_sum != null && $Object.hasOwnProperty.call(message, "day_fee_sum"))
                if (!$util.isInteger(message.day_fee_sum) && !(message.day_fee_sum && $util.isInteger(message.day_fee_sum.low) && $util.isInteger(message.day_fee_sum.high)))
                    return "day_fee_sum: integer|Long expected";
            if (message.week_fee_sum != null && $Object.hasOwnProperty.call(message, "week_fee_sum"))
                if (!$util.isInteger(message.week_fee_sum) && !(message.week_fee_sum && $util.isInteger(message.week_fee_sum.low) && $util.isInteger(message.week_fee_sum.high)))
                    return "week_fee_sum: integer|Long expected";
            if (message.month_fee_sum != null && $Object.hasOwnProperty.call(message, "month_fee_sum"))
                if (!$util.isInteger(message.month_fee_sum) && !(message.month_fee_sum && $util.isInteger(message.month_fee_sum.low) && $util.isInteger(message.month_fee_sum.high)))
                    return "month_fee_sum: integer|Long expected";
            return null;
        };

        /**
         * Creates a FeeReportResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FeeReportResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FeeReportResponse} FeeReportResponse
         */
        FeeReportResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FeeReportResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FeeReportResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.FeeReportResponse();
            if (object.channel_fees) {
                if (!$Array.isArray(object.channel_fees))
                    throw $TypeError(".lnrpc.FeeReportResponse.channel_fees: array expected");
                message.channel_fees = $Array(object.channel_fees.length);
                for (let i = 0; i < object.channel_fees.length; ++i) {
                    if (!$util.isObject(object.channel_fees[i]))
                        throw $TypeError(".lnrpc.FeeReportResponse.channel_fees: object expected");
                    message.channel_fees[i] = $root.lnrpc.ChannelFeeReport.fromObject(object.channel_fees[i], _depth + 1);
                }
            }
            if (object.day_fee_sum != null)
                if (typeof object.day_fee_sum === "object" ? object.day_fee_sum.low || object.day_fee_sum.high : $Number(object.day_fee_sum) !== 0)
                    if ($util.Long)
                        message.day_fee_sum = $util.Long.fromValue(object.day_fee_sum, true);
                    else if (typeof object.day_fee_sum === "string")
                        message.day_fee_sum = $parseInt(object.day_fee_sum, 10);
                    else if (typeof object.day_fee_sum === "number")
                        message.day_fee_sum = object.day_fee_sum;
                    else if (typeof object.day_fee_sum === "object")
                        message.day_fee_sum = new $util.LongBits(object.day_fee_sum.low >>> 0, object.day_fee_sum.high >>> 0).toNumber(true);
            if (object.week_fee_sum != null)
                if (typeof object.week_fee_sum === "object" ? object.week_fee_sum.low || object.week_fee_sum.high : $Number(object.week_fee_sum) !== 0)
                    if ($util.Long)
                        message.week_fee_sum = $util.Long.fromValue(object.week_fee_sum, true);
                    else if (typeof object.week_fee_sum === "string")
                        message.week_fee_sum = $parseInt(object.week_fee_sum, 10);
                    else if (typeof object.week_fee_sum === "number")
                        message.week_fee_sum = object.week_fee_sum;
                    else if (typeof object.week_fee_sum === "object")
                        message.week_fee_sum = new $util.LongBits(object.week_fee_sum.low >>> 0, object.week_fee_sum.high >>> 0).toNumber(true);
            if (object.month_fee_sum != null)
                if (typeof object.month_fee_sum === "object" ? object.month_fee_sum.low || object.month_fee_sum.high : $Number(object.month_fee_sum) !== 0)
                    if ($util.Long)
                        message.month_fee_sum = $util.Long.fromValue(object.month_fee_sum, true);
                    else if (typeof object.month_fee_sum === "string")
                        message.month_fee_sum = $parseInt(object.month_fee_sum, 10);
                    else if (typeof object.month_fee_sum === "number")
                        message.month_fee_sum = object.month_fee_sum;
                    else if (typeof object.month_fee_sum === "object")
                        message.month_fee_sum = new $util.LongBits(object.month_fee_sum.low >>> 0, object.month_fee_sum.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FeeReportResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FeeReportResponse
         * @static
         * @param {lnrpc.FeeReportResponse} message FeeReportResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeeReportResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.channel_fees = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.day_fee_sum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.day_fee_sum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.week_fee_sum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.week_fee_sum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.month_fee_sum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.month_fee_sum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.channel_fees && message.channel_fees.length) {
                object.channel_fees = $Array(message.channel_fees.length);
                for (let j = 0; j < message.channel_fees.length; ++j)
                    object.channel_fees[j] = $root.lnrpc.ChannelFeeReport.toObject(message.channel_fees[j], options, _depth + 1);
            }
            if (message.day_fee_sum != null && $Object.hasOwnProperty.call(message, "day_fee_sum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.day_fee_sum = typeof message.day_fee_sum === "number" ? $BigInt(message.day_fee_sum) : $util.Long.fromBits(message.day_fee_sum.low >>> 0, message.day_fee_sum.high >>> 0, true).toBigInt();
                else if (typeof message.day_fee_sum === "number")
                    object.day_fee_sum = options.longs === $String ? $String(message.day_fee_sum) : message.day_fee_sum;
                else
                    object.day_fee_sum = options.longs === $String ? $util.Long.prototype.toString.call(message.day_fee_sum) : options.longs === $Number ? new $util.LongBits(message.day_fee_sum.low >>> 0, message.day_fee_sum.high >>> 0).toNumber(true) : message.day_fee_sum;
            if (message.week_fee_sum != null && $Object.hasOwnProperty.call(message, "week_fee_sum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.week_fee_sum = typeof message.week_fee_sum === "number" ? $BigInt(message.week_fee_sum) : $util.Long.fromBits(message.week_fee_sum.low >>> 0, message.week_fee_sum.high >>> 0, true).toBigInt();
                else if (typeof message.week_fee_sum === "number")
                    object.week_fee_sum = options.longs === $String ? $String(message.week_fee_sum) : message.week_fee_sum;
                else
                    object.week_fee_sum = options.longs === $String ? $util.Long.prototype.toString.call(message.week_fee_sum) : options.longs === $Number ? new $util.LongBits(message.week_fee_sum.low >>> 0, message.week_fee_sum.high >>> 0).toNumber(true) : message.week_fee_sum;
            if (message.month_fee_sum != null && $Object.hasOwnProperty.call(message, "month_fee_sum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.month_fee_sum = typeof message.month_fee_sum === "number" ? $BigInt(message.month_fee_sum) : $util.Long.fromBits(message.month_fee_sum.low >>> 0, message.month_fee_sum.high >>> 0, true).toBigInt();
                else if (typeof message.month_fee_sum === "number")
                    object.month_fee_sum = options.longs === $String ? $String(message.month_fee_sum) : message.month_fee_sum;
                else
                    object.month_fee_sum = options.longs === $String ? $util.Long.prototype.toString.call(message.month_fee_sum) : options.longs === $Number ? new $util.LongBits(message.month_fee_sum.low >>> 0, message.month_fee_sum.high >>> 0).toNumber(true) : message.month_fee_sum;
            return object;
        };

        /**
         * Converts this FeeReportResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.FeeReportResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeeReportResponse.prototype.toJSON = function() {
            return FeeReportResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FeeReportResponse
         * @function getTypeUrl
         * @memberof lnrpc.FeeReportResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FeeReportResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FeeReportResponse";
        };

        return FeeReportResponse;
    })();

    lnrpc.InboundFee = (function() {

        /**
         * Properties of an InboundFee.
         * @typedef {Object} lnrpc.InboundFee.$Properties
         * @property {number|null} [base_fee_msat] InboundFee base_fee_msat
         * @property {number|null} [fee_rate_ppm] InboundFee fee_rate_ppm
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an InboundFee.
         * @memberof lnrpc
         * @interface IInboundFee
         * @augments lnrpc.InboundFee.$Properties
         * @deprecated Use lnrpc.InboundFee.$Properties instead.
         */

        /**
         * Shape of an InboundFee.
         * @typedef {lnrpc.InboundFee.$Properties} lnrpc.InboundFee.$Shape
         */

        /**
         * Constructs a new InboundFee.
         * @memberof lnrpc
         * @classdesc Represents an InboundFee.
         * @constructor
         * @param {lnrpc.InboundFee.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const InboundFee = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InboundFee base_fee_msat.
         * @member {number} base_fee_msat
         * @memberof lnrpc.InboundFee
         * @instance
         */
        InboundFee.prototype.base_fee_msat = 0;

        /**
         * InboundFee fee_rate_ppm.
         * @member {number} fee_rate_ppm
         * @memberof lnrpc.InboundFee
         * @instance
         */
        InboundFee.prototype.fee_rate_ppm = 0;

        /**
         * Creates a new InboundFee instance using the specified properties.
         * @function create
         * @memberof lnrpc.InboundFee
         * @static
         * @param {lnrpc.InboundFee.$Properties=} [properties] Properties to set
         * @returns {lnrpc.InboundFee} InboundFee instance
         * @type {{
         *   (properties: lnrpc.InboundFee.$Shape): lnrpc.InboundFee & lnrpc.InboundFee.$Shape;
         *   (properties?: lnrpc.InboundFee.$Properties): lnrpc.InboundFee;
         * }}
         */
        InboundFee.create = function(properties) {
            return new InboundFee(properties);
        };

        /**
         * Encodes the specified InboundFee message. Does not implicitly {@link lnrpc.InboundFee.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.InboundFee
         * @static
         * @param {lnrpc.InboundFee.$Properties} message InboundFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InboundFee.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat") && message.base_fee_msat !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.base_fee_msat);
            if (message.fee_rate_ppm != null && $Object.hasOwnProperty.call(message, "fee_rate_ppm") && message.fee_rate_ppm !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.fee_rate_ppm);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InboundFee message, length delimited. Does not implicitly {@link lnrpc.InboundFee.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.InboundFee
         * @static
         * @param {lnrpc.InboundFee.$Properties} message InboundFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InboundFee.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InboundFee message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.InboundFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.InboundFee & lnrpc.InboundFee.$Shape} InboundFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InboundFee.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.InboundFee(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.base_fee_msat = value;
                        else
                            delete message.base_fee_msat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.fee_rate_ppm = value;
                        else
                            delete message.fee_rate_ppm;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InboundFee message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.InboundFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.InboundFee & lnrpc.InboundFee.$Shape} InboundFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InboundFee.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InboundFee message.
         * @function verify
         * @memberof lnrpc.InboundFee
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InboundFee.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat"))
                if (!$util.isInteger(message.base_fee_msat))
                    return "base_fee_msat: integer expected";
            if (message.fee_rate_ppm != null && $Object.hasOwnProperty.call(message, "fee_rate_ppm"))
                if (!$util.isInteger(message.fee_rate_ppm))
                    return "fee_rate_ppm: integer expected";
            return null;
        };

        /**
         * Creates an InboundFee message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.InboundFee
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.InboundFee} InboundFee
         */
        InboundFee.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.InboundFee)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.InboundFee: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.InboundFee();
            if (object.base_fee_msat != null)
                if ($Number(object.base_fee_msat) !== 0)
                    message.base_fee_msat = object.base_fee_msat | 0;
            if (object.fee_rate_ppm != null)
                if ($Number(object.fee_rate_ppm) !== 0)
                    message.fee_rate_ppm = object.fee_rate_ppm | 0;
            return message;
        };

        /**
         * Creates a plain object from an InboundFee message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.InboundFee
         * @static
         * @param {lnrpc.InboundFee} message InboundFee
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InboundFee.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.base_fee_msat = 0;
                object.fee_rate_ppm = 0;
            }
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat"))
                object.base_fee_msat = message.base_fee_msat;
            if (message.fee_rate_ppm != null && $Object.hasOwnProperty.call(message, "fee_rate_ppm"))
                object.fee_rate_ppm = message.fee_rate_ppm;
            return object;
        };

        /**
         * Converts this InboundFee to JSON.
         * @function toJSON
         * @memberof lnrpc.InboundFee
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InboundFee.prototype.toJSON = function() {
            return InboundFee.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InboundFee
         * @function getTypeUrl
         * @memberof lnrpc.InboundFee
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InboundFee.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.InboundFee";
        };

        return InboundFee;
    })();

    lnrpc.PolicyUpdateRequest = (function() {

        /**
         * Properties of a PolicyUpdateRequest.
         * @typedef {Object} lnrpc.PolicyUpdateRequest.$Properties
         * @property {boolean|null} [global] PolicyUpdateRequest global
         * @property {lnrpc.ChannelPoint.$Properties|null} [chan_point] PolicyUpdateRequest chan_point
         * @property {Long|null} [base_fee_msat] PolicyUpdateRequest base_fee_msat
         * @property {number|null} [fee_rate] PolicyUpdateRequest fee_rate
         * @property {number|null} [fee_rate_ppm] PolicyUpdateRequest fee_rate_ppm
         * @property {number|null} [time_lock_delta] PolicyUpdateRequest time_lock_delta
         * @property {Long|null} [max_htlc_msat] PolicyUpdateRequest max_htlc_msat
         * @property {Long|null} [min_htlc_msat] PolicyUpdateRequest min_htlc_msat
         * @property {boolean|null} [min_htlc_msat_specified] PolicyUpdateRequest min_htlc_msat_specified
         * @property {lnrpc.InboundFee.$Properties|null} [inbound_fee] PolicyUpdateRequest inbound_fee
         * @property {boolean|null} [create_missing_edge] PolicyUpdateRequest create_missing_edge
         * @property {"global"|"chan_point"} [scope] PolicyUpdateRequest scope
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PolicyUpdateRequest.
         * @memberof lnrpc
         * @interface IPolicyUpdateRequest
         * @augments lnrpc.PolicyUpdateRequest.$Properties
         * @deprecated Use lnrpc.PolicyUpdateRequest.$Properties instead.
         */

        /**
         * Narrowed shape of a PolicyUpdateRequest.
         * @typedef {{
         *   global?: boolean|null;
         *   chan_point?: lnrpc.ChannelPoint.$Shape|null;
         *   base_fee_msat?: Long|null;
         *   fee_rate?: number|null;
         *   fee_rate_ppm?: number|null;
         *   time_lock_delta?: number|null;
         *   max_htlc_msat?: Long|null;
         *   min_htlc_msat?: Long|null;
         *   min_htlc_msat_specified?: boolean|null;
         *   inbound_fee?: lnrpc.InboundFee.$Shape|null;
         *   create_missing_edge?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ scope?: undefined; global?: null; chan_point?: null }|{ scope?: "global"; global: boolean; chan_point?: null }|{ scope?: "chan_point"; global?: null; chan_point: lnrpc.ChannelPoint.$Shape })
         * )} lnrpc.PolicyUpdateRequest.$Shape
         */

        /**
         * Constructs a new PolicyUpdateRequest.
         * @memberof lnrpc
         * @classdesc Represents a PolicyUpdateRequest.
         * @constructor
         * @param {lnrpc.PolicyUpdateRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PolicyUpdateRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PolicyUpdateRequest global.
         * @member {boolean|null|undefined} global
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.global = null;

        /**
         * PolicyUpdateRequest chan_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} chan_point
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.chan_point = null;

        /**
         * PolicyUpdateRequest base_fee_msat.
         * @member {Long} base_fee_msat
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.base_fee_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PolicyUpdateRequest fee_rate.
         * @member {number} fee_rate
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.fee_rate = 0;

        /**
         * PolicyUpdateRequest fee_rate_ppm.
         * @member {number} fee_rate_ppm
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.fee_rate_ppm = 0;

        /**
         * PolicyUpdateRequest time_lock_delta.
         * @member {number} time_lock_delta
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.time_lock_delta = 0;

        /**
         * PolicyUpdateRequest max_htlc_msat.
         * @member {Long} max_htlc_msat
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.max_htlc_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PolicyUpdateRequest min_htlc_msat.
         * @member {Long} min_htlc_msat
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.min_htlc_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PolicyUpdateRequest min_htlc_msat_specified.
         * @member {boolean} min_htlc_msat_specified
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.min_htlc_msat_specified = false;

        /**
         * PolicyUpdateRequest inbound_fee.
         * @member {lnrpc.InboundFee.$Properties|null|undefined} inbound_fee
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.inbound_fee = null;

        /**
         * PolicyUpdateRequest create_missing_edge.
         * @member {boolean} create_missing_edge
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        PolicyUpdateRequest.prototype.create_missing_edge = false;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * PolicyUpdateRequest scope.
         * @member {"global"|"chan_point"|undefined} scope
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         */
        $Object.defineProperty(PolicyUpdateRequest.prototype, "scope", {
            get: $util.oneOfGetter($oneOfFields = ["global", "chan_point"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PolicyUpdateRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.PolicyUpdateRequest
         * @static
         * @param {lnrpc.PolicyUpdateRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PolicyUpdateRequest} PolicyUpdateRequest instance
         * @type {{
         *   (properties: lnrpc.PolicyUpdateRequest.$Shape): lnrpc.PolicyUpdateRequest & lnrpc.PolicyUpdateRequest.$Shape;
         *   (properties?: lnrpc.PolicyUpdateRequest.$Properties): lnrpc.PolicyUpdateRequest;
         * }}
         */
        PolicyUpdateRequest.create = function(properties) {
            return new PolicyUpdateRequest(properties);
        };

        /**
         * Encodes the specified PolicyUpdateRequest message. Does not implicitly {@link lnrpc.PolicyUpdateRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PolicyUpdateRequest
         * @static
         * @param {lnrpc.PolicyUpdateRequest.$Properties} message PolicyUpdateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PolicyUpdateRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.global != null && $Object.hasOwnProperty.call(message, "global"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.global);
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                $root.lnrpc.ChannelPoint.encode(message.chan_point, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat") && (typeof message.base_fee_msat === "object" ? message.base_fee_msat.low || message.base_fee_msat.high : message.base_fee_msat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.base_fee_msat);
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate") && !$Object.is(message.fee_rate, 0))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.fee_rate);
            if (message.time_lock_delta != null && $Object.hasOwnProperty.call(message, "time_lock_delta") && message.time_lock_delta !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.time_lock_delta);
            if (message.max_htlc_msat != null && $Object.hasOwnProperty.call(message, "max_htlc_msat") && (typeof message.max_htlc_msat === "object" ? message.max_htlc_msat.low || message.max_htlc_msat.high : message.max_htlc_msat !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.max_htlc_msat);
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat") && (typeof message.min_htlc_msat === "object" ? message.min_htlc_msat.low || message.min_htlc_msat.high : message.min_htlc_msat !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.min_htlc_msat);
            if (message.min_htlc_msat_specified != null && $Object.hasOwnProperty.call(message, "min_htlc_msat_specified") && message.min_htlc_msat_specified !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.min_htlc_msat_specified);
            if (message.fee_rate_ppm != null && $Object.hasOwnProperty.call(message, "fee_rate_ppm") && message.fee_rate_ppm !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.fee_rate_ppm);
            if (message.inbound_fee != null && $Object.hasOwnProperty.call(message, "inbound_fee"))
                $root.lnrpc.InboundFee.encode(message.inbound_fee, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.create_missing_edge != null && $Object.hasOwnProperty.call(message, "create_missing_edge") && message.create_missing_edge !== false)
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.create_missing_edge);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PolicyUpdateRequest message, length delimited. Does not implicitly {@link lnrpc.PolicyUpdateRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PolicyUpdateRequest
         * @static
         * @param {lnrpc.PolicyUpdateRequest.$Properties} message PolicyUpdateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PolicyUpdateRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PolicyUpdateRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PolicyUpdateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PolicyUpdateRequest & lnrpc.PolicyUpdateRequest.$Shape} PolicyUpdateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PolicyUpdateRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PolicyUpdateRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        message.global = reader.bool();
                        message.scope = "global";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.chan_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_point);
                        message.scope = "chan_point";
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.base_fee_msat = value;
                        else
                            delete message.base_fee_msat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.fee_rate = value;
                        else
                            delete message.fee_rate;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fee_rate_ppm = value;
                        else
                            delete message.fee_rate_ppm;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time_lock_delta = value;
                        else
                            delete message.time_lock_delta;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_htlc_msat = value;
                        else
                            delete message.max_htlc_msat;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.min_htlc_msat = value;
                        else
                            delete message.min_htlc_msat;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.min_htlc_msat_specified = value;
                        else
                            delete message.min_htlc_msat_specified;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.inbound_fee = $root.lnrpc.InboundFee.decode(reader, reader.uint32(), $undefined, _depth + 1, message.inbound_fee);
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.create_missing_edge = value;
                        else
                            delete message.create_missing_edge;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PolicyUpdateRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PolicyUpdateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PolicyUpdateRequest & lnrpc.PolicyUpdateRequest.$Shape} PolicyUpdateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PolicyUpdateRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PolicyUpdateRequest message.
         * @function verify
         * @memberof lnrpc.PolicyUpdateRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PolicyUpdateRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.global != null && $Object.hasOwnProperty.call(message, "global")) {
                properties.scope = 1;
                if (typeof message.global !== "boolean")
                    return "global: boolean expected";
            }
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point")) {
                if (properties.scope === 1)
                    return "scope: multiple values";
                properties.scope = 1;
                {
                    let error = $root.lnrpc.ChannelPoint.verify(message.chan_point, _depth + 1);
                    if (error)
                        return "chan_point." + error;
                }
            }
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat"))
                if (!$util.isInteger(message.base_fee_msat) && !(message.base_fee_msat && $util.isInteger(message.base_fee_msat.low) && $util.isInteger(message.base_fee_msat.high)))
                    return "base_fee_msat: integer|Long expected";
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                if (typeof message.fee_rate !== "number")
                    return "fee_rate: number expected";
            if (message.fee_rate_ppm != null && $Object.hasOwnProperty.call(message, "fee_rate_ppm"))
                if (!$util.isInteger(message.fee_rate_ppm))
                    return "fee_rate_ppm: integer expected";
            if (message.time_lock_delta != null && $Object.hasOwnProperty.call(message, "time_lock_delta"))
                if (!$util.isInteger(message.time_lock_delta))
                    return "time_lock_delta: integer expected";
            if (message.max_htlc_msat != null && $Object.hasOwnProperty.call(message, "max_htlc_msat"))
                if (!$util.isInteger(message.max_htlc_msat) && !(message.max_htlc_msat && $util.isInteger(message.max_htlc_msat.low) && $util.isInteger(message.max_htlc_msat.high)))
                    return "max_htlc_msat: integer|Long expected";
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat"))
                if (!$util.isInteger(message.min_htlc_msat) && !(message.min_htlc_msat && $util.isInteger(message.min_htlc_msat.low) && $util.isInteger(message.min_htlc_msat.high)))
                    return "min_htlc_msat: integer|Long expected";
            if (message.min_htlc_msat_specified != null && $Object.hasOwnProperty.call(message, "min_htlc_msat_specified"))
                if (typeof message.min_htlc_msat_specified !== "boolean")
                    return "min_htlc_msat_specified: boolean expected";
            if (message.inbound_fee != null && $Object.hasOwnProperty.call(message, "inbound_fee")) {
                let error = $root.lnrpc.InboundFee.verify(message.inbound_fee, _depth + 1);
                if (error)
                    return "inbound_fee." + error;
            }
            if (message.create_missing_edge != null && $Object.hasOwnProperty.call(message, "create_missing_edge"))
                if (typeof message.create_missing_edge !== "boolean")
                    return "create_missing_edge: boolean expected";
            return null;
        };

        /**
         * Creates a PolicyUpdateRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PolicyUpdateRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PolicyUpdateRequest} PolicyUpdateRequest
         */
        PolicyUpdateRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PolicyUpdateRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PolicyUpdateRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PolicyUpdateRequest();
            if (object.global != null)
                message.global = $Boolean(object.global);
            if (object.chan_point != null) {
                if (!$util.isObject(object.chan_point))
                    throw $TypeError(".lnrpc.PolicyUpdateRequest.chan_point: object expected");
                message.chan_point = $root.lnrpc.ChannelPoint.fromObject(object.chan_point, _depth + 1);
            }
            if (object.base_fee_msat != null)
                if (typeof object.base_fee_msat === "object" ? object.base_fee_msat.low || object.base_fee_msat.high : $Number(object.base_fee_msat) !== 0)
                    if ($util.Long)
                        message.base_fee_msat = $util.Long.fromValue(object.base_fee_msat, false);
                    else if (typeof object.base_fee_msat === "string")
                        message.base_fee_msat = $parseInt(object.base_fee_msat, 10);
                    else if (typeof object.base_fee_msat === "number")
                        message.base_fee_msat = object.base_fee_msat;
                    else if (typeof object.base_fee_msat === "object")
                        message.base_fee_msat = new $util.LongBits(object.base_fee_msat.low >>> 0, object.base_fee_msat.high >>> 0).toNumber();
            if (object.fee_rate != null)
                if (!$Object.is($Number(object.fee_rate), 0))
                    message.fee_rate = $Number(object.fee_rate);
            if (object.fee_rate_ppm != null)
                if ($Number(object.fee_rate_ppm) !== 0)
                    message.fee_rate_ppm = object.fee_rate_ppm >>> 0;
            if (object.time_lock_delta != null)
                if ($Number(object.time_lock_delta) !== 0)
                    message.time_lock_delta = object.time_lock_delta >>> 0;
            if (object.max_htlc_msat != null)
                if (typeof object.max_htlc_msat === "object" ? object.max_htlc_msat.low || object.max_htlc_msat.high : $Number(object.max_htlc_msat) !== 0)
                    if ($util.Long)
                        message.max_htlc_msat = $util.Long.fromValue(object.max_htlc_msat, true);
                    else if (typeof object.max_htlc_msat === "string")
                        message.max_htlc_msat = $parseInt(object.max_htlc_msat, 10);
                    else if (typeof object.max_htlc_msat === "number")
                        message.max_htlc_msat = object.max_htlc_msat;
                    else if (typeof object.max_htlc_msat === "object")
                        message.max_htlc_msat = new $util.LongBits(object.max_htlc_msat.low >>> 0, object.max_htlc_msat.high >>> 0).toNumber(true);
            if (object.min_htlc_msat != null)
                if (typeof object.min_htlc_msat === "object" ? object.min_htlc_msat.low || object.min_htlc_msat.high : $Number(object.min_htlc_msat) !== 0)
                    if ($util.Long)
                        message.min_htlc_msat = $util.Long.fromValue(object.min_htlc_msat, true);
                    else if (typeof object.min_htlc_msat === "string")
                        message.min_htlc_msat = $parseInt(object.min_htlc_msat, 10);
                    else if (typeof object.min_htlc_msat === "number")
                        message.min_htlc_msat = object.min_htlc_msat;
                    else if (typeof object.min_htlc_msat === "object")
                        message.min_htlc_msat = new $util.LongBits(object.min_htlc_msat.low >>> 0, object.min_htlc_msat.high >>> 0).toNumber(true);
            if (object.min_htlc_msat_specified != null)
                if (object.min_htlc_msat_specified)
                    message.min_htlc_msat_specified = $Boolean(object.min_htlc_msat_specified);
            if (object.inbound_fee != null) {
                if (!$util.isObject(object.inbound_fee))
                    throw $TypeError(".lnrpc.PolicyUpdateRequest.inbound_fee: object expected");
                message.inbound_fee = $root.lnrpc.InboundFee.fromObject(object.inbound_fee, _depth + 1);
            }
            if (object.create_missing_edge != null)
                if (object.create_missing_edge)
                    message.create_missing_edge = $Boolean(object.create_missing_edge);
            return message;
        };

        /**
         * Creates a plain object from a PolicyUpdateRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PolicyUpdateRequest
         * @static
         * @param {lnrpc.PolicyUpdateRequest} message PolicyUpdateRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PolicyUpdateRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.base_fee_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.base_fee_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.fee_rate = 0;
                object.time_lock_delta = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.max_htlc_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.max_htlc_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.min_htlc_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min_htlc_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.min_htlc_msat_specified = false;
                object.fee_rate_ppm = 0;
                object.inbound_fee = null;
                object.create_missing_edge = false;
            }
            if (message.global != null && $Object.hasOwnProperty.call(message, "global")) {
                object.global = message.global;
                if (options.oneofs)
                    object.scope = "global";
            }
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point")) {
                object.chan_point = $root.lnrpc.ChannelPoint.toObject(message.chan_point, options, _depth + 1);
                if (options.oneofs)
                    object.scope = "chan_point";
            }
            if (message.base_fee_msat != null && $Object.hasOwnProperty.call(message, "base_fee_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.base_fee_msat = typeof message.base_fee_msat === "number" ? $BigInt(message.base_fee_msat) : $util.Long.fromBits(message.base_fee_msat.low >>> 0, message.base_fee_msat.high >>> 0, false).toBigInt();
                else if (typeof message.base_fee_msat === "number")
                    object.base_fee_msat = options.longs === $String ? $String(message.base_fee_msat) : message.base_fee_msat;
                else
                    object.base_fee_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.base_fee_msat) : options.longs === $Number ? new $util.LongBits(message.base_fee_msat.low >>> 0, message.base_fee_msat.high >>> 0).toNumber() : message.base_fee_msat;
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                object.fee_rate = options.json && !$isFinite(message.fee_rate) ? $String(message.fee_rate) : message.fee_rate;
            if (message.time_lock_delta != null && $Object.hasOwnProperty.call(message, "time_lock_delta"))
                object.time_lock_delta = message.time_lock_delta;
            if (message.max_htlc_msat != null && $Object.hasOwnProperty.call(message, "max_htlc_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.max_htlc_msat = typeof message.max_htlc_msat === "number" ? $BigInt(message.max_htlc_msat) : $util.Long.fromBits(message.max_htlc_msat.low >>> 0, message.max_htlc_msat.high >>> 0, true).toBigInt();
                else if (typeof message.max_htlc_msat === "number")
                    object.max_htlc_msat = options.longs === $String ? $String(message.max_htlc_msat) : message.max_htlc_msat;
                else
                    object.max_htlc_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.max_htlc_msat) : options.longs === $Number ? new $util.LongBits(message.max_htlc_msat.low >>> 0, message.max_htlc_msat.high >>> 0).toNumber(true) : message.max_htlc_msat;
            if (message.min_htlc_msat != null && $Object.hasOwnProperty.call(message, "min_htlc_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min_htlc_msat = typeof message.min_htlc_msat === "number" ? $BigInt(message.min_htlc_msat) : $util.Long.fromBits(message.min_htlc_msat.low >>> 0, message.min_htlc_msat.high >>> 0, true).toBigInt();
                else if (typeof message.min_htlc_msat === "number")
                    object.min_htlc_msat = options.longs === $String ? $String(message.min_htlc_msat) : message.min_htlc_msat;
                else
                    object.min_htlc_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.min_htlc_msat) : options.longs === $Number ? new $util.LongBits(message.min_htlc_msat.low >>> 0, message.min_htlc_msat.high >>> 0).toNumber(true) : message.min_htlc_msat;
            if (message.min_htlc_msat_specified != null && $Object.hasOwnProperty.call(message, "min_htlc_msat_specified"))
                object.min_htlc_msat_specified = message.min_htlc_msat_specified;
            if (message.fee_rate_ppm != null && $Object.hasOwnProperty.call(message, "fee_rate_ppm"))
                object.fee_rate_ppm = message.fee_rate_ppm;
            if (message.inbound_fee != null && $Object.hasOwnProperty.call(message, "inbound_fee"))
                object.inbound_fee = $root.lnrpc.InboundFee.toObject(message.inbound_fee, options, _depth + 1);
            if (message.create_missing_edge != null && $Object.hasOwnProperty.call(message, "create_missing_edge"))
                object.create_missing_edge = message.create_missing_edge;
            return object;
        };

        /**
         * Converts this PolicyUpdateRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.PolicyUpdateRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PolicyUpdateRequest.prototype.toJSON = function() {
            return PolicyUpdateRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PolicyUpdateRequest
         * @function getTypeUrl
         * @memberof lnrpc.PolicyUpdateRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PolicyUpdateRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PolicyUpdateRequest";
        };

        return PolicyUpdateRequest;
    })();

    /**
     * UpdateFailure enum.
     * @name lnrpc.UpdateFailure
     * @enum {number}
     * @property {number} UPDATE_FAILURE_UNKNOWN=0 UPDATE_FAILURE_UNKNOWN value
     * @property {number} UPDATE_FAILURE_PENDING=1 UPDATE_FAILURE_PENDING value
     * @property {number} UPDATE_FAILURE_NOT_FOUND=2 UPDATE_FAILURE_NOT_FOUND value
     * @property {number} UPDATE_FAILURE_INTERNAL_ERR=3 UPDATE_FAILURE_INTERNAL_ERR value
     * @property {number} UPDATE_FAILURE_INVALID_PARAMETER=4 UPDATE_FAILURE_INVALID_PARAMETER value
     */
    lnrpc.UpdateFailure = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "UPDATE_FAILURE_UNKNOWN"] = 0;
        values[valuesById[1] = "UPDATE_FAILURE_PENDING"] = 1;
        values[valuesById[2] = "UPDATE_FAILURE_NOT_FOUND"] = 2;
        values[valuesById[3] = "UPDATE_FAILURE_INTERNAL_ERR"] = 3;
        values[valuesById[4] = "UPDATE_FAILURE_INVALID_PARAMETER"] = 4;
        return values;
    })();

    lnrpc.FailedUpdate = (function() {

        /**
         * Properties of a FailedUpdate.
         * @typedef {Object} lnrpc.FailedUpdate.$Properties
         * @property {lnrpc.OutPoint.$Properties|null} [outpoint] FailedUpdate outpoint
         * @property {lnrpc.UpdateFailure|null} [reason] FailedUpdate reason
         * @property {string|null} [update_error] FailedUpdate update_error
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FailedUpdate.
         * @memberof lnrpc
         * @interface IFailedUpdate
         * @augments lnrpc.FailedUpdate.$Properties
         * @deprecated Use lnrpc.FailedUpdate.$Properties instead.
         */

        /**
         * Shape of a FailedUpdate.
         * @typedef {lnrpc.FailedUpdate.$Properties} lnrpc.FailedUpdate.$Shape
         */

        /**
         * Constructs a new FailedUpdate.
         * @memberof lnrpc
         * @classdesc Represents a FailedUpdate.
         * @constructor
         * @param {lnrpc.FailedUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FailedUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FailedUpdate outpoint.
         * @member {lnrpc.OutPoint.$Properties|null|undefined} outpoint
         * @memberof lnrpc.FailedUpdate
         * @instance
         */
        FailedUpdate.prototype.outpoint = null;

        /**
         * FailedUpdate reason.
         * @member {lnrpc.UpdateFailure} reason
         * @memberof lnrpc.FailedUpdate
         * @instance
         */
        FailedUpdate.prototype.reason = 0;

        /**
         * FailedUpdate update_error.
         * @member {string} update_error
         * @memberof lnrpc.FailedUpdate
         * @instance
         */
        FailedUpdate.prototype.update_error = "";

        /**
         * Creates a new FailedUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.FailedUpdate
         * @static
         * @param {lnrpc.FailedUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.FailedUpdate} FailedUpdate instance
         * @type {{
         *   (properties: lnrpc.FailedUpdate.$Shape): lnrpc.FailedUpdate & lnrpc.FailedUpdate.$Shape;
         *   (properties?: lnrpc.FailedUpdate.$Properties): lnrpc.FailedUpdate;
         * }}
         */
        FailedUpdate.create = function(properties) {
            return new FailedUpdate(properties);
        };

        /**
         * Encodes the specified FailedUpdate message. Does not implicitly {@link lnrpc.FailedUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.FailedUpdate
         * @static
         * @param {lnrpc.FailedUpdate.$Properties} message FailedUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FailedUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                $root.lnrpc.OutPoint.encode(message.outpoint, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.reason != null && $Object.hasOwnProperty.call(message, "reason") && message.reason !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.reason);
            if (message.update_error != null && $Object.hasOwnProperty.call(message, "update_error") && message.update_error !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.update_error);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FailedUpdate message, length delimited. Does not implicitly {@link lnrpc.FailedUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.FailedUpdate
         * @static
         * @param {lnrpc.FailedUpdate.$Properties} message FailedUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FailedUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FailedUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.FailedUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.FailedUpdate & lnrpc.FailedUpdate.$Shape} FailedUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FailedUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.FailedUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.outpoint = $root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.outpoint);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.reason = value;
                        else
                            delete message.reason;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.update_error = value;
                        else
                            delete message.update_error;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FailedUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.FailedUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.FailedUpdate & lnrpc.FailedUpdate.$Shape} FailedUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FailedUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FailedUpdate message.
         * @function verify
         * @memberof lnrpc.FailedUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FailedUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint")) {
                let error = $root.lnrpc.OutPoint.verify(message.outpoint, _depth + 1);
                if (error)
                    return "outpoint." + error;
            }
            if (message.reason != null && $Object.hasOwnProperty.call(message, "reason"))
                if (typeof message.reason !== "number" || (message.reason | 0) !== message.reason)
                    return "reason: enum value expected";
            if (message.update_error != null && $Object.hasOwnProperty.call(message, "update_error"))
                if (!$util.isString(message.update_error))
                    return "update_error: string expected";
            return null;
        };

        /**
         * Creates a FailedUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.FailedUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.FailedUpdate} FailedUpdate
         */
        FailedUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.FailedUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.FailedUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.FailedUpdate();
            if (object.outpoint != null) {
                if (!$util.isObject(object.outpoint))
                    throw $TypeError(".lnrpc.FailedUpdate.outpoint: object expected");
                message.outpoint = $root.lnrpc.OutPoint.fromObject(object.outpoint, _depth + 1);
            }
            if (object.reason !== 0 && (typeof object.reason !== "string" || $root.lnrpc.UpdateFailure[object.reason] !== 0))
                switch (object.reason) {
                case "UPDATE_FAILURE_UNKNOWN":
                case 0:
                    message.reason = 0;
                    break;
                case "UPDATE_FAILURE_PENDING":
                case 1:
                    message.reason = 1;
                    break;
                case "UPDATE_FAILURE_NOT_FOUND":
                case 2:
                    message.reason = 2;
                    break;
                case "UPDATE_FAILURE_INTERNAL_ERR":
                case 3:
                    message.reason = 3;
                    break;
                case "UPDATE_FAILURE_INVALID_PARAMETER":
                case 4:
                    message.reason = 4;
                    break;
                default:
                    if (typeof object.reason === "number" && (object.reason | 0) === object.reason)
                        message.reason = object.reason;
                }
            if (object.update_error != null)
                if (typeof object.update_error !== "string" || object.update_error.length)
                    message.update_error = $String(object.update_error);
            return message;
        };

        /**
         * Creates a plain object from a FailedUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.FailedUpdate
         * @static
         * @param {lnrpc.FailedUpdate} message FailedUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FailedUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.outpoint = null;
                object.reason = options.enums === $String ? "UPDATE_FAILURE_UNKNOWN" : 0;
                object.update_error = "";
            }
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = $root.lnrpc.OutPoint.toObject(message.outpoint, options, _depth + 1);
            if (message.reason != null && $Object.hasOwnProperty.call(message, "reason"))
                object.reason = options.enums === $String ? $root.lnrpc.UpdateFailure[message.reason] === $undefined ? message.reason : $root.lnrpc.UpdateFailure[message.reason] : message.reason;
            if (message.update_error != null && $Object.hasOwnProperty.call(message, "update_error"))
                object.update_error = message.update_error;
            return object;
        };

        /**
         * Converts this FailedUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.FailedUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FailedUpdate.prototype.toJSON = function() {
            return FailedUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FailedUpdate
         * @function getTypeUrl
         * @memberof lnrpc.FailedUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FailedUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.FailedUpdate";
        };

        return FailedUpdate;
    })();

    lnrpc.PolicyUpdateResponse = (function() {

        /**
         * Properties of a PolicyUpdateResponse.
         * @typedef {Object} lnrpc.PolicyUpdateResponse.$Properties
         * @property {Array.<lnrpc.FailedUpdate.$Properties>|null} [failed_updates] PolicyUpdateResponse failed_updates
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PolicyUpdateResponse.
         * @memberof lnrpc
         * @interface IPolicyUpdateResponse
         * @augments lnrpc.PolicyUpdateResponse.$Properties
         * @deprecated Use lnrpc.PolicyUpdateResponse.$Properties instead.
         */

        /**
         * Shape of a PolicyUpdateResponse.
         * @typedef {lnrpc.PolicyUpdateResponse.$Properties} lnrpc.PolicyUpdateResponse.$Shape
         */

        /**
         * Constructs a new PolicyUpdateResponse.
         * @memberof lnrpc
         * @classdesc Represents a PolicyUpdateResponse.
         * @constructor
         * @param {lnrpc.PolicyUpdateResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PolicyUpdateResponse = function (properties) {
            this.failed_updates = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PolicyUpdateResponse failed_updates.
         * @member {Array.<lnrpc.FailedUpdate.$Properties>} failed_updates
         * @memberof lnrpc.PolicyUpdateResponse
         * @instance
         */
        PolicyUpdateResponse.prototype.failed_updates = $util.emptyArray;

        /**
         * Creates a new PolicyUpdateResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.PolicyUpdateResponse
         * @static
         * @param {lnrpc.PolicyUpdateResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.PolicyUpdateResponse} PolicyUpdateResponse instance
         * @type {{
         *   (properties: lnrpc.PolicyUpdateResponse.$Shape): lnrpc.PolicyUpdateResponse & lnrpc.PolicyUpdateResponse.$Shape;
         *   (properties?: lnrpc.PolicyUpdateResponse.$Properties): lnrpc.PolicyUpdateResponse;
         * }}
         */
        PolicyUpdateResponse.create = function(properties) {
            return new PolicyUpdateResponse(properties);
        };

        /**
         * Encodes the specified PolicyUpdateResponse message. Does not implicitly {@link lnrpc.PolicyUpdateResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.PolicyUpdateResponse
         * @static
         * @param {lnrpc.PolicyUpdateResponse.$Properties} message PolicyUpdateResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PolicyUpdateResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.failed_updates != null && message.failed_updates.length)
                for (let i = 0; i < message.failed_updates.length; ++i)
                    $root.lnrpc.FailedUpdate.encode(message.failed_updates[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PolicyUpdateResponse message, length delimited. Does not implicitly {@link lnrpc.PolicyUpdateResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.PolicyUpdateResponse
         * @static
         * @param {lnrpc.PolicyUpdateResponse.$Properties} message PolicyUpdateResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PolicyUpdateResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PolicyUpdateResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.PolicyUpdateResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.PolicyUpdateResponse & lnrpc.PolicyUpdateResponse.$Shape} PolicyUpdateResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PolicyUpdateResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.PolicyUpdateResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.failed_updates && message.failed_updates.length))
                            message.failed_updates = [];
                        message.failed_updates.push($root.lnrpc.FailedUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PolicyUpdateResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.PolicyUpdateResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.PolicyUpdateResponse & lnrpc.PolicyUpdateResponse.$Shape} PolicyUpdateResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PolicyUpdateResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PolicyUpdateResponse message.
         * @function verify
         * @memberof lnrpc.PolicyUpdateResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PolicyUpdateResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.failed_updates != null && $Object.hasOwnProperty.call(message, "failed_updates")) {
                if (!$Array.isArray(message.failed_updates))
                    return "failed_updates: array expected";
                for (let i = 0; i < message.failed_updates.length; ++i) {
                    let error = $root.lnrpc.FailedUpdate.verify(message.failed_updates[i], _depth + 1);
                    if (error)
                        return "failed_updates." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PolicyUpdateResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.PolicyUpdateResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.PolicyUpdateResponse} PolicyUpdateResponse
         */
        PolicyUpdateResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.PolicyUpdateResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.PolicyUpdateResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.PolicyUpdateResponse();
            if (object.failed_updates) {
                if (!$Array.isArray(object.failed_updates))
                    throw $TypeError(".lnrpc.PolicyUpdateResponse.failed_updates: array expected");
                message.failed_updates = $Array(object.failed_updates.length);
                for (let i = 0; i < object.failed_updates.length; ++i) {
                    if (!$util.isObject(object.failed_updates[i]))
                        throw $TypeError(".lnrpc.PolicyUpdateResponse.failed_updates: object expected");
                    message.failed_updates[i] = $root.lnrpc.FailedUpdate.fromObject(object.failed_updates[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PolicyUpdateResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.PolicyUpdateResponse
         * @static
         * @param {lnrpc.PolicyUpdateResponse} message PolicyUpdateResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PolicyUpdateResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.failed_updates = [];
            if (message.failed_updates && message.failed_updates.length) {
                object.failed_updates = $Array(message.failed_updates.length);
                for (let j = 0; j < message.failed_updates.length; ++j)
                    object.failed_updates[j] = $root.lnrpc.FailedUpdate.toObject(message.failed_updates[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this PolicyUpdateResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.PolicyUpdateResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PolicyUpdateResponse.prototype.toJSON = function() {
            return PolicyUpdateResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PolicyUpdateResponse
         * @function getTypeUrl
         * @memberof lnrpc.PolicyUpdateResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PolicyUpdateResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.PolicyUpdateResponse";
        };

        return PolicyUpdateResponse;
    })();

    lnrpc.ForwardingHistoryRequest = (function() {

        /**
         * Properties of a ForwardingHistoryRequest.
         * @typedef {Object} lnrpc.ForwardingHistoryRequest.$Properties
         * @property {Long|null} [start_time] ForwardingHistoryRequest start_time
         * @property {Long|null} [end_time] ForwardingHistoryRequest end_time
         * @property {number|null} [index_offset] ForwardingHistoryRequest index_offset
         * @property {number|null} [num_max_events] ForwardingHistoryRequest num_max_events
         * @property {boolean|null} [peer_alias_lookup] ForwardingHistoryRequest peer_alias_lookup
         * @property {Array.<Long>|null} [incoming_chan_ids] ForwardingHistoryRequest incoming_chan_ids
         * @property {Array.<Long>|null} [outgoing_chan_ids] ForwardingHistoryRequest outgoing_chan_ids
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ForwardingHistoryRequest.
         * @memberof lnrpc
         * @interface IForwardingHistoryRequest
         * @augments lnrpc.ForwardingHistoryRequest.$Properties
         * @deprecated Use lnrpc.ForwardingHistoryRequest.$Properties instead.
         */

        /**
         * Shape of a ForwardingHistoryRequest.
         * @typedef {lnrpc.ForwardingHistoryRequest.$Properties} lnrpc.ForwardingHistoryRequest.$Shape
         */

        /**
         * Constructs a new ForwardingHistoryRequest.
         * @memberof lnrpc
         * @classdesc Represents a ForwardingHistoryRequest.
         * @constructor
         * @param {lnrpc.ForwardingHistoryRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ForwardingHistoryRequest = function (properties) {
            this.incoming_chan_ids = [];
            this.outgoing_chan_ids = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ForwardingHistoryRequest start_time.
         * @member {Long} start_time
         * @memberof lnrpc.ForwardingHistoryRequest
         * @instance
         */
        ForwardingHistoryRequest.prototype.start_time = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingHistoryRequest end_time.
         * @member {Long} end_time
         * @memberof lnrpc.ForwardingHistoryRequest
         * @instance
         */
        ForwardingHistoryRequest.prototype.end_time = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingHistoryRequest index_offset.
         * @member {number} index_offset
         * @memberof lnrpc.ForwardingHistoryRequest
         * @instance
         */
        ForwardingHistoryRequest.prototype.index_offset = 0;

        /**
         * ForwardingHistoryRequest num_max_events.
         * @member {number} num_max_events
         * @memberof lnrpc.ForwardingHistoryRequest
         * @instance
         */
        ForwardingHistoryRequest.prototype.num_max_events = 0;

        /**
         * ForwardingHistoryRequest peer_alias_lookup.
         * @member {boolean} peer_alias_lookup
         * @memberof lnrpc.ForwardingHistoryRequest
         * @instance
         */
        ForwardingHistoryRequest.prototype.peer_alias_lookup = false;

        /**
         * ForwardingHistoryRequest incoming_chan_ids.
         * @member {Array.<Long>} incoming_chan_ids
         * @memberof lnrpc.ForwardingHistoryRequest
         * @instance
         */
        ForwardingHistoryRequest.prototype.incoming_chan_ids = $util.emptyArray;

        /**
         * ForwardingHistoryRequest outgoing_chan_ids.
         * @member {Array.<Long>} outgoing_chan_ids
         * @memberof lnrpc.ForwardingHistoryRequest
         * @instance
         */
        ForwardingHistoryRequest.prototype.outgoing_chan_ids = $util.emptyArray;

        /**
         * Creates a new ForwardingHistoryRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ForwardingHistoryRequest
         * @static
         * @param {lnrpc.ForwardingHistoryRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ForwardingHistoryRequest} ForwardingHistoryRequest instance
         * @type {{
         *   (properties: lnrpc.ForwardingHistoryRequest.$Shape): lnrpc.ForwardingHistoryRequest & lnrpc.ForwardingHistoryRequest.$Shape;
         *   (properties?: lnrpc.ForwardingHistoryRequest.$Properties): lnrpc.ForwardingHistoryRequest;
         * }}
         */
        ForwardingHistoryRequest.create = function(properties) {
            return new ForwardingHistoryRequest(properties);
        };

        /**
         * Encodes the specified ForwardingHistoryRequest message. Does not implicitly {@link lnrpc.ForwardingHistoryRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ForwardingHistoryRequest
         * @static
         * @param {lnrpc.ForwardingHistoryRequest.$Properties} message ForwardingHistoryRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardingHistoryRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.start_time != null && $Object.hasOwnProperty.call(message, "start_time") && (typeof message.start_time === "object" ? message.start_time.low || message.start_time.high : message.start_time !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.start_time);
            if (message.end_time != null && $Object.hasOwnProperty.call(message, "end_time") && (typeof message.end_time === "object" ? message.end_time.low || message.end_time.high : message.end_time !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.end_time);
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset") && message.index_offset !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.index_offset);
            if (message.num_max_events != null && $Object.hasOwnProperty.call(message, "num_max_events") && message.num_max_events !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.num_max_events);
            if (message.peer_alias_lookup != null && $Object.hasOwnProperty.call(message, "peer_alias_lookup") && message.peer_alias_lookup !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.peer_alias_lookup);
            if (message.incoming_chan_ids != null && message.incoming_chan_ids.length)
                writer.uint32(/* id 6, wireType 2 =*/50).uint64s(message.incoming_chan_ids);
            if (message.outgoing_chan_ids != null && message.outgoing_chan_ids.length)
                writer.uint32(/* id 7, wireType 2 =*/58).uint64s(message.outgoing_chan_ids);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ForwardingHistoryRequest message, length delimited. Does not implicitly {@link lnrpc.ForwardingHistoryRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ForwardingHistoryRequest
         * @static
         * @param {lnrpc.ForwardingHistoryRequest.$Properties} message ForwardingHistoryRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardingHistoryRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ForwardingHistoryRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ForwardingHistoryRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ForwardingHistoryRequest & lnrpc.ForwardingHistoryRequest.$Shape} ForwardingHistoryRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardingHistoryRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ForwardingHistoryRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index_offset = value;
                        else
                            delete message.index_offset;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_max_events = value;
                        else
                            delete message.num_max_events;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.peer_alias_lookup = value;
                        else
                            delete message.peer_alias_lookup;
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.incoming_chan_ids && message.incoming_chan_ids.length))
                                message.incoming_chan_ids = [];
                            reader.uint64s(message.incoming_chan_ids);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.incoming_chan_ids && message.incoming_chan_ids.length))
                            message.incoming_chan_ids = [];
                        message.incoming_chan_ids.push(reader.uint64());
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.outgoing_chan_ids && message.outgoing_chan_ids.length))
                                message.outgoing_chan_ids = [];
                            reader.uint64s(message.outgoing_chan_ids);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.outgoing_chan_ids && message.outgoing_chan_ids.length))
                            message.outgoing_chan_ids = [];
                        message.outgoing_chan_ids.push(reader.uint64());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ForwardingHistoryRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ForwardingHistoryRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ForwardingHistoryRequest & lnrpc.ForwardingHistoryRequest.$Shape} ForwardingHistoryRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardingHistoryRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForwardingHistoryRequest message.
         * @function verify
         * @memberof lnrpc.ForwardingHistoryRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForwardingHistoryRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.start_time != null && $Object.hasOwnProperty.call(message, "start_time"))
                if (!$util.isInteger(message.start_time) && !(message.start_time && $util.isInteger(message.start_time.low) && $util.isInteger(message.start_time.high)))
                    return "start_time: integer|Long expected";
            if (message.end_time != null && $Object.hasOwnProperty.call(message, "end_time"))
                if (!$util.isInteger(message.end_time) && !(message.end_time && $util.isInteger(message.end_time.low) && $util.isInteger(message.end_time.high)))
                    return "end_time: integer|Long expected";
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset"))
                if (!$util.isInteger(message.index_offset))
                    return "index_offset: integer expected";
            if (message.num_max_events != null && $Object.hasOwnProperty.call(message, "num_max_events"))
                if (!$util.isInteger(message.num_max_events))
                    return "num_max_events: integer expected";
            if (message.peer_alias_lookup != null && $Object.hasOwnProperty.call(message, "peer_alias_lookup"))
                if (typeof message.peer_alias_lookup !== "boolean")
                    return "peer_alias_lookup: boolean expected";
            if (message.incoming_chan_ids != null && $Object.hasOwnProperty.call(message, "incoming_chan_ids")) {
                if (!$Array.isArray(message.incoming_chan_ids))
                    return "incoming_chan_ids: array expected";
                for (let i = 0; i < message.incoming_chan_ids.length; ++i)
                    if (!$util.isInteger(message.incoming_chan_ids[i]) && !(message.incoming_chan_ids[i] && $util.isInteger(message.incoming_chan_ids[i].low) && $util.isInteger(message.incoming_chan_ids[i].high)))
                        return "incoming_chan_ids: integer|Long[] expected";
            }
            if (message.outgoing_chan_ids != null && $Object.hasOwnProperty.call(message, "outgoing_chan_ids")) {
                if (!$Array.isArray(message.outgoing_chan_ids))
                    return "outgoing_chan_ids: array expected";
                for (let i = 0; i < message.outgoing_chan_ids.length; ++i)
                    if (!$util.isInteger(message.outgoing_chan_ids[i]) && !(message.outgoing_chan_ids[i] && $util.isInteger(message.outgoing_chan_ids[i].low) && $util.isInteger(message.outgoing_chan_ids[i].high)))
                        return "outgoing_chan_ids: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates a ForwardingHistoryRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ForwardingHistoryRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ForwardingHistoryRequest} ForwardingHistoryRequest
         */
        ForwardingHistoryRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ForwardingHistoryRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ForwardingHistoryRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ForwardingHistoryRequest();
            if (object.start_time != null)
                if (typeof object.start_time === "object" ? object.start_time.low || object.start_time.high : $Number(object.start_time) !== 0)
                    if ($util.Long)
                        message.start_time = $util.Long.fromValue(object.start_time, true);
                    else if (typeof object.start_time === "string")
                        message.start_time = $parseInt(object.start_time, 10);
                    else if (typeof object.start_time === "number")
                        message.start_time = object.start_time;
                    else if (typeof object.start_time === "object")
                        message.start_time = new $util.LongBits(object.start_time.low >>> 0, object.start_time.high >>> 0).toNumber(true);
            if (object.end_time != null)
                if (typeof object.end_time === "object" ? object.end_time.low || object.end_time.high : $Number(object.end_time) !== 0)
                    if ($util.Long)
                        message.end_time = $util.Long.fromValue(object.end_time, true);
                    else if (typeof object.end_time === "string")
                        message.end_time = $parseInt(object.end_time, 10);
                    else if (typeof object.end_time === "number")
                        message.end_time = object.end_time;
                    else if (typeof object.end_time === "object")
                        message.end_time = new $util.LongBits(object.end_time.low >>> 0, object.end_time.high >>> 0).toNumber(true);
            if (object.index_offset != null)
                if ($Number(object.index_offset) !== 0)
                    message.index_offset = object.index_offset >>> 0;
            if (object.num_max_events != null)
                if ($Number(object.num_max_events) !== 0)
                    message.num_max_events = object.num_max_events >>> 0;
            if (object.peer_alias_lookup != null)
                if (object.peer_alias_lookup)
                    message.peer_alias_lookup = $Boolean(object.peer_alias_lookup);
            if (object.incoming_chan_ids) {
                if (!$Array.isArray(object.incoming_chan_ids))
                    throw $TypeError(".lnrpc.ForwardingHistoryRequest.incoming_chan_ids: array expected");
                message.incoming_chan_ids = $Array(object.incoming_chan_ids.length);
                for (let i = 0; i < object.incoming_chan_ids.length; ++i)
                    if ($util.Long)
                        message.incoming_chan_ids[i] = $util.Long.fromValue(object.incoming_chan_ids[i], true);
                    else if (typeof object.incoming_chan_ids[i] === "string")
                        message.incoming_chan_ids[i] = $parseInt(object.incoming_chan_ids[i], 10);
                    else if (typeof object.incoming_chan_ids[i] === "number")
                        message.incoming_chan_ids[i] = object.incoming_chan_ids[i];
                    else if (typeof object.incoming_chan_ids[i] === "object")
                        message.incoming_chan_ids[i] = new $util.LongBits(object.incoming_chan_ids[i].low >>> 0, object.incoming_chan_ids[i].high >>> 0).toNumber(true);
            }
            if (object.outgoing_chan_ids) {
                if (!$Array.isArray(object.outgoing_chan_ids))
                    throw $TypeError(".lnrpc.ForwardingHistoryRequest.outgoing_chan_ids: array expected");
                message.outgoing_chan_ids = $Array(object.outgoing_chan_ids.length);
                for (let i = 0; i < object.outgoing_chan_ids.length; ++i)
                    if ($util.Long)
                        message.outgoing_chan_ids[i] = $util.Long.fromValue(object.outgoing_chan_ids[i], true);
                    else if (typeof object.outgoing_chan_ids[i] === "string")
                        message.outgoing_chan_ids[i] = $parseInt(object.outgoing_chan_ids[i], 10);
                    else if (typeof object.outgoing_chan_ids[i] === "number")
                        message.outgoing_chan_ids[i] = object.outgoing_chan_ids[i];
                    else if (typeof object.outgoing_chan_ids[i] === "object")
                        message.outgoing_chan_ids[i] = new $util.LongBits(object.outgoing_chan_ids[i].low >>> 0, object.outgoing_chan_ids[i].high >>> 0).toNumber(true);
            }
            return message;
        };

        /**
         * Creates a plain object from a ForwardingHistoryRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ForwardingHistoryRequest
         * @static
         * @param {lnrpc.ForwardingHistoryRequest} message ForwardingHistoryRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForwardingHistoryRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.incoming_chan_ids = [];
                object.outgoing_chan_ids = [];
            }
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.start_time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.start_time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.end_time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.end_time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.index_offset = 0;
                object.num_max_events = 0;
                object.peer_alias_lookup = false;
            }
            if (message.start_time != null && $Object.hasOwnProperty.call(message, "start_time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.start_time = typeof message.start_time === "number" ? $BigInt(message.start_time) : $util.Long.fromBits(message.start_time.low >>> 0, message.start_time.high >>> 0, true).toBigInt();
                else if (typeof message.start_time === "number")
                    object.start_time = options.longs === $String ? $String(message.start_time) : message.start_time;
                else
                    object.start_time = options.longs === $String ? $util.Long.prototype.toString.call(message.start_time) : options.longs === $Number ? new $util.LongBits(message.start_time.low >>> 0, message.start_time.high >>> 0).toNumber(true) : message.start_time;
            if (message.end_time != null && $Object.hasOwnProperty.call(message, "end_time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.end_time = typeof message.end_time === "number" ? $BigInt(message.end_time) : $util.Long.fromBits(message.end_time.low >>> 0, message.end_time.high >>> 0, true).toBigInt();
                else if (typeof message.end_time === "number")
                    object.end_time = options.longs === $String ? $String(message.end_time) : message.end_time;
                else
                    object.end_time = options.longs === $String ? $util.Long.prototype.toString.call(message.end_time) : options.longs === $Number ? new $util.LongBits(message.end_time.low >>> 0, message.end_time.high >>> 0).toNumber(true) : message.end_time;
            if (message.index_offset != null && $Object.hasOwnProperty.call(message, "index_offset"))
                object.index_offset = message.index_offset;
            if (message.num_max_events != null && $Object.hasOwnProperty.call(message, "num_max_events"))
                object.num_max_events = message.num_max_events;
            if (message.peer_alias_lookup != null && $Object.hasOwnProperty.call(message, "peer_alias_lookup"))
                object.peer_alias_lookup = message.peer_alias_lookup;
            if (message.incoming_chan_ids && message.incoming_chan_ids.length) {
                object.incoming_chan_ids = $Array(message.incoming_chan_ids.length);
                for (let j = 0; j < message.incoming_chan_ids.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.incoming_chan_ids[j] = typeof message.incoming_chan_ids[j] === "number" ? $BigInt(message.incoming_chan_ids[j]) : $util.Long.fromBits(message.incoming_chan_ids[j].low >>> 0, message.incoming_chan_ids[j].high >>> 0, true).toBigInt();
                    else if (typeof message.incoming_chan_ids[j] === "number")
                        object.incoming_chan_ids[j] = options.longs === $String ? $String(message.incoming_chan_ids[j]) : message.incoming_chan_ids[j];
                    else
                        object.incoming_chan_ids[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.incoming_chan_ids[j]) : options.longs === $Number ? new $util.LongBits(message.incoming_chan_ids[j].low >>> 0, message.incoming_chan_ids[j].high >>> 0).toNumber(true) : message.incoming_chan_ids[j];
            }
            if (message.outgoing_chan_ids && message.outgoing_chan_ids.length) {
                object.outgoing_chan_ids = $Array(message.outgoing_chan_ids.length);
                for (let j = 0; j < message.outgoing_chan_ids.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.outgoing_chan_ids[j] = typeof message.outgoing_chan_ids[j] === "number" ? $BigInt(message.outgoing_chan_ids[j]) : $util.Long.fromBits(message.outgoing_chan_ids[j].low >>> 0, message.outgoing_chan_ids[j].high >>> 0, true).toBigInt();
                    else if (typeof message.outgoing_chan_ids[j] === "number")
                        object.outgoing_chan_ids[j] = options.longs === $String ? $String(message.outgoing_chan_ids[j]) : message.outgoing_chan_ids[j];
                    else
                        object.outgoing_chan_ids[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_chan_ids[j]) : options.longs === $Number ? new $util.LongBits(message.outgoing_chan_ids[j].low >>> 0, message.outgoing_chan_ids[j].high >>> 0).toNumber(true) : message.outgoing_chan_ids[j];
            }
            return object;
        };

        /**
         * Converts this ForwardingHistoryRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ForwardingHistoryRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForwardingHistoryRequest.prototype.toJSON = function() {
            return ForwardingHistoryRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ForwardingHistoryRequest
         * @function getTypeUrl
         * @memberof lnrpc.ForwardingHistoryRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ForwardingHistoryRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ForwardingHistoryRequest";
        };

        return ForwardingHistoryRequest;
    })();

    lnrpc.ForwardingEvent = (function() {

        /**
         * Properties of a ForwardingEvent.
         * @typedef {Object} lnrpc.ForwardingEvent.$Properties
         * @property {Long|null} [timestamp] ForwardingEvent timestamp
         * @property {Long|null} [chan_id_in] ForwardingEvent chan_id_in
         * @property {Long|null} [chan_id_out] ForwardingEvent chan_id_out
         * @property {Long|null} [amt_in] ForwardingEvent amt_in
         * @property {Long|null} [amt_out] ForwardingEvent amt_out
         * @property {Long|null} [fee] ForwardingEvent fee
         * @property {Long|null} [fee_msat] ForwardingEvent fee_msat
         * @property {Long|null} [amt_in_msat] ForwardingEvent amt_in_msat
         * @property {Long|null} [amt_out_msat] ForwardingEvent amt_out_msat
         * @property {Long|null} [timestamp_ns] ForwardingEvent timestamp_ns
         * @property {string|null} [peer_alias_in] ForwardingEvent peer_alias_in
         * @property {string|null} [peer_alias_out] ForwardingEvent peer_alias_out
         * @property {Long|null} [incoming_htlc_id] ForwardingEvent incoming_htlc_id
         * @property {Long|null} [outgoing_htlc_id] ForwardingEvent outgoing_htlc_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ForwardingEvent.
         * @memberof lnrpc
         * @interface IForwardingEvent
         * @augments lnrpc.ForwardingEvent.$Properties
         * @deprecated Use lnrpc.ForwardingEvent.$Properties instead.
         */

        /**
         * Shape of a ForwardingEvent.
         * @typedef {lnrpc.ForwardingEvent.$Properties} lnrpc.ForwardingEvent.$Shape
         */

        /**
         * Constructs a new ForwardingEvent.
         * @memberof lnrpc
         * @classdesc Represents a ForwardingEvent.
         * @constructor
         * @param {lnrpc.ForwardingEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ForwardingEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ForwardingEvent timestamp.
         * @member {Long} timestamp
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent chan_id_in.
         * @member {Long} chan_id_in
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.chan_id_in = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent chan_id_out.
         * @member {Long} chan_id_out
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.chan_id_out = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent amt_in.
         * @member {Long} amt_in
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.amt_in = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent amt_out.
         * @member {Long} amt_out
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.amt_out = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent fee.
         * @member {Long} fee
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.fee = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent fee_msat.
         * @member {Long} fee_msat
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.fee_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent amt_in_msat.
         * @member {Long} amt_in_msat
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.amt_in_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent amt_out_msat.
         * @member {Long} amt_out_msat
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.amt_out_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent timestamp_ns.
         * @member {Long} timestamp_ns
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.timestamp_ns = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardingEvent peer_alias_in.
         * @member {string} peer_alias_in
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.peer_alias_in = "";

        /**
         * ForwardingEvent peer_alias_out.
         * @member {string} peer_alias_out
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.peer_alias_out = "";

        /**
         * ForwardingEvent incoming_htlc_id.
         * @member {Long|null|undefined} incoming_htlc_id
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.incoming_htlc_id = null;

        /**
         * ForwardingEvent outgoing_htlc_id.
         * @member {Long|null|undefined} outgoing_htlc_id
         * @memberof lnrpc.ForwardingEvent
         * @instance
         */
        ForwardingEvent.prototype.outgoing_htlc_id = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ForwardingEvent.prototype, "_incoming_htlc_id", {
            get: $util.oneOfGetter($oneOfFields = ["incoming_htlc_id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ForwardingEvent.prototype, "_outgoing_htlc_id", {
            get: $util.oneOfGetter($oneOfFields = ["outgoing_htlc_id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ForwardingEvent instance using the specified properties.
         * @function create
         * @memberof lnrpc.ForwardingEvent
         * @static
         * @param {lnrpc.ForwardingEvent.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ForwardingEvent} ForwardingEvent instance
         * @type {{
         *   (properties: lnrpc.ForwardingEvent.$Shape): lnrpc.ForwardingEvent & lnrpc.ForwardingEvent.$Shape;
         *   (properties?: lnrpc.ForwardingEvent.$Properties): lnrpc.ForwardingEvent;
         * }}
         */
        ForwardingEvent.create = function(properties) {
            return new ForwardingEvent(properties);
        };

        /**
         * Encodes the specified ForwardingEvent message. Does not implicitly {@link lnrpc.ForwardingEvent.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ForwardingEvent
         * @static
         * @param {lnrpc.ForwardingEvent.$Properties} message ForwardingEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardingEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp") && (typeof message.timestamp === "object" ? message.timestamp.low || message.timestamp.high : message.timestamp !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.timestamp);
            if (message.chan_id_in != null && $Object.hasOwnProperty.call(message, "chan_id_in") && (typeof message.chan_id_in === "object" ? message.chan_id_in.low || message.chan_id_in.high : message.chan_id_in !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.chan_id_in);
            if (message.chan_id_out != null && $Object.hasOwnProperty.call(message, "chan_id_out") && (typeof message.chan_id_out === "object" ? message.chan_id_out.low || message.chan_id_out.high : message.chan_id_out !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.chan_id_out);
            if (message.amt_in != null && $Object.hasOwnProperty.call(message, "amt_in") && (typeof message.amt_in === "object" ? message.amt_in.low || message.amt_in.high : message.amt_in !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.amt_in);
            if (message.amt_out != null && $Object.hasOwnProperty.call(message, "amt_out") && (typeof message.amt_out === "object" ? message.amt_out.low || message.amt_out.high : message.amt_out !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.amt_out);
            if (message.fee != null && $Object.hasOwnProperty.call(message, "fee") && (typeof message.fee === "object" ? message.fee.low || message.fee.high : message.fee !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.fee);
            if (message.fee_msat != null && $Object.hasOwnProperty.call(message, "fee_msat") && (typeof message.fee_msat === "object" ? message.fee_msat.low || message.fee_msat.high : message.fee_msat !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.fee_msat);
            if (message.amt_in_msat != null && $Object.hasOwnProperty.call(message, "amt_in_msat") && (typeof message.amt_in_msat === "object" ? message.amt_in_msat.low || message.amt_in_msat.high : message.amt_in_msat !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.amt_in_msat);
            if (message.amt_out_msat != null && $Object.hasOwnProperty.call(message, "amt_out_msat") && (typeof message.amt_out_msat === "object" ? message.amt_out_msat.low || message.amt_out_msat.high : message.amt_out_msat !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.amt_out_msat);
            if (message.timestamp_ns != null && $Object.hasOwnProperty.call(message, "timestamp_ns") && (typeof message.timestamp_ns === "object" ? message.timestamp_ns.low || message.timestamp_ns.high : message.timestamp_ns !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.timestamp_ns);
            if (message.peer_alias_in != null && $Object.hasOwnProperty.call(message, "peer_alias_in") && message.peer_alias_in !== "")
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.peer_alias_in);
            if (message.peer_alias_out != null && $Object.hasOwnProperty.call(message, "peer_alias_out") && message.peer_alias_out !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.peer_alias_out);
            if (message.incoming_htlc_id != null && $Object.hasOwnProperty.call(message, "incoming_htlc_id"))
                writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.incoming_htlc_id);
            if (message.outgoing_htlc_id != null && $Object.hasOwnProperty.call(message, "outgoing_htlc_id"))
                writer.uint32(/* id 15, wireType 0 =*/120).uint64(message.outgoing_htlc_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ForwardingEvent message, length delimited. Does not implicitly {@link lnrpc.ForwardingEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ForwardingEvent
         * @static
         * @param {lnrpc.ForwardingEvent.$Properties} message ForwardingEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardingEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ForwardingEvent message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ForwardingEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ForwardingEvent & lnrpc.ForwardingEvent.$Shape} ForwardingEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardingEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ForwardingEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id_in = value;
                        else
                            delete message.chan_id_in;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id_out = value;
                        else
                            delete message.chan_id_out;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_in = value;
                        else
                            delete message.amt_in;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_out = value;
                        else
                            delete message.amt_out;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee = value;
                        else
                            delete message.fee;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_msat = value;
                        else
                            delete message.fee_msat;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_in_msat = value;
                        else
                            delete message.amt_in_msat;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_out_msat = value;
                        else
                            delete message.amt_out_msat;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.timestamp_ns = value;
                        else
                            delete message.timestamp_ns;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.peer_alias_in = value;
                        else
                            delete message.peer_alias_in;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.peer_alias_out = value;
                        else
                            delete message.peer_alias_out;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        message.incoming_htlc_id = reader.uint64();
                        message._incoming_htlc_id = "incoming_htlc_id";
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        message.outgoing_htlc_id = reader.uint64();
                        message._outgoing_htlc_id = "outgoing_htlc_id";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ForwardingEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ForwardingEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ForwardingEvent & lnrpc.ForwardingEvent.$Shape} ForwardingEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardingEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForwardingEvent message.
         * @function verify
         * @memberof lnrpc.ForwardingEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForwardingEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            if (message.chan_id_in != null && $Object.hasOwnProperty.call(message, "chan_id_in"))
                if (!$util.isInteger(message.chan_id_in) && !(message.chan_id_in && $util.isInteger(message.chan_id_in.low) && $util.isInteger(message.chan_id_in.high)))
                    return "chan_id_in: integer|Long expected";
            if (message.chan_id_out != null && $Object.hasOwnProperty.call(message, "chan_id_out"))
                if (!$util.isInteger(message.chan_id_out) && !(message.chan_id_out && $util.isInteger(message.chan_id_out.low) && $util.isInteger(message.chan_id_out.high)))
                    return "chan_id_out: integer|Long expected";
            if (message.amt_in != null && $Object.hasOwnProperty.call(message, "amt_in"))
                if (!$util.isInteger(message.amt_in) && !(message.amt_in && $util.isInteger(message.amt_in.low) && $util.isInteger(message.amt_in.high)))
                    return "amt_in: integer|Long expected";
            if (message.amt_out != null && $Object.hasOwnProperty.call(message, "amt_out"))
                if (!$util.isInteger(message.amt_out) && !(message.amt_out && $util.isInteger(message.amt_out.low) && $util.isInteger(message.amt_out.high)))
                    return "amt_out: integer|Long expected";
            if (message.fee != null && $Object.hasOwnProperty.call(message, "fee"))
                if (!$util.isInteger(message.fee) && !(message.fee && $util.isInteger(message.fee.low) && $util.isInteger(message.fee.high)))
                    return "fee: integer|Long expected";
            if (message.fee_msat != null && $Object.hasOwnProperty.call(message, "fee_msat"))
                if (!$util.isInteger(message.fee_msat) && !(message.fee_msat && $util.isInteger(message.fee_msat.low) && $util.isInteger(message.fee_msat.high)))
                    return "fee_msat: integer|Long expected";
            if (message.amt_in_msat != null && $Object.hasOwnProperty.call(message, "amt_in_msat"))
                if (!$util.isInteger(message.amt_in_msat) && !(message.amt_in_msat && $util.isInteger(message.amt_in_msat.low) && $util.isInteger(message.amt_in_msat.high)))
                    return "amt_in_msat: integer|Long expected";
            if (message.amt_out_msat != null && $Object.hasOwnProperty.call(message, "amt_out_msat"))
                if (!$util.isInteger(message.amt_out_msat) && !(message.amt_out_msat && $util.isInteger(message.amt_out_msat.low) && $util.isInteger(message.amt_out_msat.high)))
                    return "amt_out_msat: integer|Long expected";
            if (message.timestamp_ns != null && $Object.hasOwnProperty.call(message, "timestamp_ns"))
                if (!$util.isInteger(message.timestamp_ns) && !(message.timestamp_ns && $util.isInteger(message.timestamp_ns.low) && $util.isInteger(message.timestamp_ns.high)))
                    return "timestamp_ns: integer|Long expected";
            if (message.peer_alias_in != null && $Object.hasOwnProperty.call(message, "peer_alias_in"))
                if (!$util.isString(message.peer_alias_in))
                    return "peer_alias_in: string expected";
            if (message.peer_alias_out != null && $Object.hasOwnProperty.call(message, "peer_alias_out"))
                if (!$util.isString(message.peer_alias_out))
                    return "peer_alias_out: string expected";
            if (message.incoming_htlc_id != null && $Object.hasOwnProperty.call(message, "incoming_htlc_id")) {
                properties._incoming_htlc_id = 1;
                if (!$util.isInteger(message.incoming_htlc_id) && !(message.incoming_htlc_id && $util.isInteger(message.incoming_htlc_id.low) && $util.isInteger(message.incoming_htlc_id.high)))
                    return "incoming_htlc_id: integer|Long expected";
            }
            if (message.outgoing_htlc_id != null && $Object.hasOwnProperty.call(message, "outgoing_htlc_id")) {
                properties._outgoing_htlc_id = 1;
                if (!$util.isInteger(message.outgoing_htlc_id) && !(message.outgoing_htlc_id && $util.isInteger(message.outgoing_htlc_id.low) && $util.isInteger(message.outgoing_htlc_id.high)))
                    return "outgoing_htlc_id: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a ForwardingEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ForwardingEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ForwardingEvent} ForwardingEvent
         */
        ForwardingEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ForwardingEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ForwardingEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ForwardingEvent();
            if (object.timestamp != null)
                if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, true);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = $parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            if (object.chan_id_in != null)
                if (typeof object.chan_id_in === "object" ? object.chan_id_in.low || object.chan_id_in.high : $Number(object.chan_id_in) !== 0)
                    if ($util.Long)
                        message.chan_id_in = $util.Long.fromValue(object.chan_id_in, true);
                    else if (typeof object.chan_id_in === "string")
                        message.chan_id_in = $parseInt(object.chan_id_in, 10);
                    else if (typeof object.chan_id_in === "number")
                        message.chan_id_in = object.chan_id_in;
                    else if (typeof object.chan_id_in === "object")
                        message.chan_id_in = new $util.LongBits(object.chan_id_in.low >>> 0, object.chan_id_in.high >>> 0).toNumber(true);
            if (object.chan_id_out != null)
                if (typeof object.chan_id_out === "object" ? object.chan_id_out.low || object.chan_id_out.high : $Number(object.chan_id_out) !== 0)
                    if ($util.Long)
                        message.chan_id_out = $util.Long.fromValue(object.chan_id_out, true);
                    else if (typeof object.chan_id_out === "string")
                        message.chan_id_out = $parseInt(object.chan_id_out, 10);
                    else if (typeof object.chan_id_out === "number")
                        message.chan_id_out = object.chan_id_out;
                    else if (typeof object.chan_id_out === "object")
                        message.chan_id_out = new $util.LongBits(object.chan_id_out.low >>> 0, object.chan_id_out.high >>> 0).toNumber(true);
            if (object.amt_in != null)
                if (typeof object.amt_in === "object" ? object.amt_in.low || object.amt_in.high : $Number(object.amt_in) !== 0)
                    if ($util.Long)
                        message.amt_in = $util.Long.fromValue(object.amt_in, true);
                    else if (typeof object.amt_in === "string")
                        message.amt_in = $parseInt(object.amt_in, 10);
                    else if (typeof object.amt_in === "number")
                        message.amt_in = object.amt_in;
                    else if (typeof object.amt_in === "object")
                        message.amt_in = new $util.LongBits(object.amt_in.low >>> 0, object.amt_in.high >>> 0).toNumber(true);
            if (object.amt_out != null)
                if (typeof object.amt_out === "object" ? object.amt_out.low || object.amt_out.high : $Number(object.amt_out) !== 0)
                    if ($util.Long)
                        message.amt_out = $util.Long.fromValue(object.amt_out, true);
                    else if (typeof object.amt_out === "string")
                        message.amt_out = $parseInt(object.amt_out, 10);
                    else if (typeof object.amt_out === "number")
                        message.amt_out = object.amt_out;
                    else if (typeof object.amt_out === "object")
                        message.amt_out = new $util.LongBits(object.amt_out.low >>> 0, object.amt_out.high >>> 0).toNumber(true);
            if (object.fee != null)
                if (typeof object.fee === "object" ? object.fee.low || object.fee.high : $Number(object.fee) !== 0)
                    if ($util.Long)
                        message.fee = $util.Long.fromValue(object.fee, true);
                    else if (typeof object.fee === "string")
                        message.fee = $parseInt(object.fee, 10);
                    else if (typeof object.fee === "number")
                        message.fee = object.fee;
                    else if (typeof object.fee === "object")
                        message.fee = new $util.LongBits(object.fee.low >>> 0, object.fee.high >>> 0).toNumber(true);
            if (object.fee_msat != null)
                if (typeof object.fee_msat === "object" ? object.fee_msat.low || object.fee_msat.high : $Number(object.fee_msat) !== 0)
                    if ($util.Long)
                        message.fee_msat = $util.Long.fromValue(object.fee_msat, true);
                    else if (typeof object.fee_msat === "string")
                        message.fee_msat = $parseInt(object.fee_msat, 10);
                    else if (typeof object.fee_msat === "number")
                        message.fee_msat = object.fee_msat;
                    else if (typeof object.fee_msat === "object")
                        message.fee_msat = new $util.LongBits(object.fee_msat.low >>> 0, object.fee_msat.high >>> 0).toNumber(true);
            if (object.amt_in_msat != null)
                if (typeof object.amt_in_msat === "object" ? object.amt_in_msat.low || object.amt_in_msat.high : $Number(object.amt_in_msat) !== 0)
                    if ($util.Long)
                        message.amt_in_msat = $util.Long.fromValue(object.amt_in_msat, true);
                    else if (typeof object.amt_in_msat === "string")
                        message.amt_in_msat = $parseInt(object.amt_in_msat, 10);
                    else if (typeof object.amt_in_msat === "number")
                        message.amt_in_msat = object.amt_in_msat;
                    else if (typeof object.amt_in_msat === "object")
                        message.amt_in_msat = new $util.LongBits(object.amt_in_msat.low >>> 0, object.amt_in_msat.high >>> 0).toNumber(true);
            if (object.amt_out_msat != null)
                if (typeof object.amt_out_msat === "object" ? object.amt_out_msat.low || object.amt_out_msat.high : $Number(object.amt_out_msat) !== 0)
                    if ($util.Long)
                        message.amt_out_msat = $util.Long.fromValue(object.amt_out_msat, true);
                    else if (typeof object.amt_out_msat === "string")
                        message.amt_out_msat = $parseInt(object.amt_out_msat, 10);
                    else if (typeof object.amt_out_msat === "number")
                        message.amt_out_msat = object.amt_out_msat;
                    else if (typeof object.amt_out_msat === "object")
                        message.amt_out_msat = new $util.LongBits(object.amt_out_msat.low >>> 0, object.amt_out_msat.high >>> 0).toNumber(true);
            if (object.timestamp_ns != null)
                if (typeof object.timestamp_ns === "object" ? object.timestamp_ns.low || object.timestamp_ns.high : $Number(object.timestamp_ns) !== 0)
                    if ($util.Long)
                        message.timestamp_ns = $util.Long.fromValue(object.timestamp_ns, true);
                    else if (typeof object.timestamp_ns === "string")
                        message.timestamp_ns = $parseInt(object.timestamp_ns, 10);
                    else if (typeof object.timestamp_ns === "number")
                        message.timestamp_ns = object.timestamp_ns;
                    else if (typeof object.timestamp_ns === "object")
                        message.timestamp_ns = new $util.LongBits(object.timestamp_ns.low >>> 0, object.timestamp_ns.high >>> 0).toNumber(true);
            if (object.peer_alias_in != null)
                if (typeof object.peer_alias_in !== "string" || object.peer_alias_in.length)
                    message.peer_alias_in = $String(object.peer_alias_in);
            if (object.peer_alias_out != null)
                if (typeof object.peer_alias_out !== "string" || object.peer_alias_out.length)
                    message.peer_alias_out = $String(object.peer_alias_out);
            if (object.incoming_htlc_id != null)
                if ($util.Long)
                    message.incoming_htlc_id = $util.Long.fromValue(object.incoming_htlc_id, true);
                else if (typeof object.incoming_htlc_id === "string")
                    message.incoming_htlc_id = $parseInt(object.incoming_htlc_id, 10);
                else if (typeof object.incoming_htlc_id === "number")
                    message.incoming_htlc_id = object.incoming_htlc_id;
                else if (typeof object.incoming_htlc_id === "object")
                    message.incoming_htlc_id = new $util.LongBits(object.incoming_htlc_id.low >>> 0, object.incoming_htlc_id.high >>> 0).toNumber(true);
            if (object.outgoing_htlc_id != null)
                if ($util.Long)
                    message.outgoing_htlc_id = $util.Long.fromValue(object.outgoing_htlc_id, true);
                else if (typeof object.outgoing_htlc_id === "string")
                    message.outgoing_htlc_id = $parseInt(object.outgoing_htlc_id, 10);
                else if (typeof object.outgoing_htlc_id === "number")
                    message.outgoing_htlc_id = object.outgoing_htlc_id;
                else if (typeof object.outgoing_htlc_id === "object")
                    message.outgoing_htlc_id = new $util.LongBits(object.outgoing_htlc_id.low >>> 0, object.outgoing_htlc_id.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ForwardingEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ForwardingEvent
         * @static
         * @param {lnrpc.ForwardingEvent} message ForwardingEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForwardingEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id_in = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id_in = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id_out = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id_out = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amt_in = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_in = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amt_out = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_out = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.fee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.fee_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amt_in_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_in_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amt_out_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_out_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.timestamp_ns = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.timestamp_ns = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.peer_alias_in = "";
                object.peer_alias_out = "";
            }
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            if (message.chan_id_in != null && $Object.hasOwnProperty.call(message, "chan_id_in"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id_in = typeof message.chan_id_in === "number" ? $BigInt(message.chan_id_in) : $util.Long.fromBits(message.chan_id_in.low >>> 0, message.chan_id_in.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id_in === "number")
                    object.chan_id_in = options.longs === $String ? $String(message.chan_id_in) : message.chan_id_in;
                else
                    object.chan_id_in = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id_in) : options.longs === $Number ? new $util.LongBits(message.chan_id_in.low >>> 0, message.chan_id_in.high >>> 0).toNumber(true) : message.chan_id_in;
            if (message.chan_id_out != null && $Object.hasOwnProperty.call(message, "chan_id_out"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id_out = typeof message.chan_id_out === "number" ? $BigInt(message.chan_id_out) : $util.Long.fromBits(message.chan_id_out.low >>> 0, message.chan_id_out.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id_out === "number")
                    object.chan_id_out = options.longs === $String ? $String(message.chan_id_out) : message.chan_id_out;
                else
                    object.chan_id_out = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id_out) : options.longs === $Number ? new $util.LongBits(message.chan_id_out.low >>> 0, message.chan_id_out.high >>> 0).toNumber(true) : message.chan_id_out;
            if (message.amt_in != null && $Object.hasOwnProperty.call(message, "amt_in"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_in = typeof message.amt_in === "number" ? $BigInt(message.amt_in) : $util.Long.fromBits(message.amt_in.low >>> 0, message.amt_in.high >>> 0, true).toBigInt();
                else if (typeof message.amt_in === "number")
                    object.amt_in = options.longs === $String ? $String(message.amt_in) : message.amt_in;
                else
                    object.amt_in = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_in) : options.longs === $Number ? new $util.LongBits(message.amt_in.low >>> 0, message.amt_in.high >>> 0).toNumber(true) : message.amt_in;
            if (message.amt_out != null && $Object.hasOwnProperty.call(message, "amt_out"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_out = typeof message.amt_out === "number" ? $BigInt(message.amt_out) : $util.Long.fromBits(message.amt_out.low >>> 0, message.amt_out.high >>> 0, true).toBigInt();
                else if (typeof message.amt_out === "number")
                    object.amt_out = options.longs === $String ? $String(message.amt_out) : message.amt_out;
                else
                    object.amt_out = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_out) : options.longs === $Number ? new $util.LongBits(message.amt_out.low >>> 0, message.amt_out.high >>> 0).toNumber(true) : message.amt_out;
            if (message.fee != null && $Object.hasOwnProperty.call(message, "fee"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee = typeof message.fee === "number" ? $BigInt(message.fee) : $util.Long.fromBits(message.fee.low >>> 0, message.fee.high >>> 0, true).toBigInt();
                else if (typeof message.fee === "number")
                    object.fee = options.longs === $String ? $String(message.fee) : message.fee;
                else
                    object.fee = options.longs === $String ? $util.Long.prototype.toString.call(message.fee) : options.longs === $Number ? new $util.LongBits(message.fee.low >>> 0, message.fee.high >>> 0).toNumber(true) : message.fee;
            if (message.fee_msat != null && $Object.hasOwnProperty.call(message, "fee_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_msat = typeof message.fee_msat === "number" ? $BigInt(message.fee_msat) : $util.Long.fromBits(message.fee_msat.low >>> 0, message.fee_msat.high >>> 0, true).toBigInt();
                else if (typeof message.fee_msat === "number")
                    object.fee_msat = options.longs === $String ? $String(message.fee_msat) : message.fee_msat;
                else
                    object.fee_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_msat) : options.longs === $Number ? new $util.LongBits(message.fee_msat.low >>> 0, message.fee_msat.high >>> 0).toNumber(true) : message.fee_msat;
            if (message.amt_in_msat != null && $Object.hasOwnProperty.call(message, "amt_in_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_in_msat = typeof message.amt_in_msat === "number" ? $BigInt(message.amt_in_msat) : $util.Long.fromBits(message.amt_in_msat.low >>> 0, message.amt_in_msat.high >>> 0, true).toBigInt();
                else if (typeof message.amt_in_msat === "number")
                    object.amt_in_msat = options.longs === $String ? $String(message.amt_in_msat) : message.amt_in_msat;
                else
                    object.amt_in_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_in_msat) : options.longs === $Number ? new $util.LongBits(message.amt_in_msat.low >>> 0, message.amt_in_msat.high >>> 0).toNumber(true) : message.amt_in_msat;
            if (message.amt_out_msat != null && $Object.hasOwnProperty.call(message, "amt_out_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_out_msat = typeof message.amt_out_msat === "number" ? $BigInt(message.amt_out_msat) : $util.Long.fromBits(message.amt_out_msat.low >>> 0, message.amt_out_msat.high >>> 0, true).toBigInt();
                else if (typeof message.amt_out_msat === "number")
                    object.amt_out_msat = options.longs === $String ? $String(message.amt_out_msat) : message.amt_out_msat;
                else
                    object.amt_out_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_out_msat) : options.longs === $Number ? new $util.LongBits(message.amt_out_msat.low >>> 0, message.amt_out_msat.high >>> 0).toNumber(true) : message.amt_out_msat;
            if (message.timestamp_ns != null && $Object.hasOwnProperty.call(message, "timestamp_ns"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.timestamp_ns = typeof message.timestamp_ns === "number" ? $BigInt(message.timestamp_ns) : $util.Long.fromBits(message.timestamp_ns.low >>> 0, message.timestamp_ns.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp_ns === "number")
                    object.timestamp_ns = options.longs === $String ? $String(message.timestamp_ns) : message.timestamp_ns;
                else
                    object.timestamp_ns = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp_ns) : options.longs === $Number ? new $util.LongBits(message.timestamp_ns.low >>> 0, message.timestamp_ns.high >>> 0).toNumber(true) : message.timestamp_ns;
            if (message.peer_alias_in != null && $Object.hasOwnProperty.call(message, "peer_alias_in"))
                object.peer_alias_in = message.peer_alias_in;
            if (message.peer_alias_out != null && $Object.hasOwnProperty.call(message, "peer_alias_out"))
                object.peer_alias_out = message.peer_alias_out;
            if (message.incoming_htlc_id != null && $Object.hasOwnProperty.call(message, "incoming_htlc_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.incoming_htlc_id = typeof message.incoming_htlc_id === "number" ? $BigInt(message.incoming_htlc_id) : $util.Long.fromBits(message.incoming_htlc_id.low >>> 0, message.incoming_htlc_id.high >>> 0, true).toBigInt();
                else if (typeof message.incoming_htlc_id === "number")
                    object.incoming_htlc_id = options.longs === $String ? $String(message.incoming_htlc_id) : message.incoming_htlc_id;
                else
                    object.incoming_htlc_id = options.longs === $String ? $util.Long.prototype.toString.call(message.incoming_htlc_id) : options.longs === $Number ? new $util.LongBits(message.incoming_htlc_id.low >>> 0, message.incoming_htlc_id.high >>> 0).toNumber(true) : message.incoming_htlc_id;
            if (message.outgoing_htlc_id != null && $Object.hasOwnProperty.call(message, "outgoing_htlc_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_htlc_id = typeof message.outgoing_htlc_id === "number" ? $BigInt(message.outgoing_htlc_id) : $util.Long.fromBits(message.outgoing_htlc_id.low >>> 0, message.outgoing_htlc_id.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_htlc_id === "number")
                    object.outgoing_htlc_id = options.longs === $String ? $String(message.outgoing_htlc_id) : message.outgoing_htlc_id;
                else
                    object.outgoing_htlc_id = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_htlc_id) : options.longs === $Number ? new $util.LongBits(message.outgoing_htlc_id.low >>> 0, message.outgoing_htlc_id.high >>> 0).toNumber(true) : message.outgoing_htlc_id;
            return object;
        };

        /**
         * Converts this ForwardingEvent to JSON.
         * @function toJSON
         * @memberof lnrpc.ForwardingEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForwardingEvent.prototype.toJSON = function() {
            return ForwardingEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ForwardingEvent
         * @function getTypeUrl
         * @memberof lnrpc.ForwardingEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ForwardingEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ForwardingEvent";
        };

        return ForwardingEvent;
    })();

    lnrpc.ForwardingHistoryResponse = (function() {

        /**
         * Properties of a ForwardingHistoryResponse.
         * @typedef {Object} lnrpc.ForwardingHistoryResponse.$Properties
         * @property {Array.<lnrpc.ForwardingEvent.$Properties>|null} [forwarding_events] ForwardingHistoryResponse forwarding_events
         * @property {number|null} [last_offset_index] ForwardingHistoryResponse last_offset_index
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ForwardingHistoryResponse.
         * @memberof lnrpc
         * @interface IForwardingHistoryResponse
         * @augments lnrpc.ForwardingHistoryResponse.$Properties
         * @deprecated Use lnrpc.ForwardingHistoryResponse.$Properties instead.
         */

        /**
         * Shape of a ForwardingHistoryResponse.
         * @typedef {lnrpc.ForwardingHistoryResponse.$Properties} lnrpc.ForwardingHistoryResponse.$Shape
         */

        /**
         * Constructs a new ForwardingHistoryResponse.
         * @memberof lnrpc
         * @classdesc Represents a ForwardingHistoryResponse.
         * @constructor
         * @param {lnrpc.ForwardingHistoryResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ForwardingHistoryResponse = function (properties) {
            this.forwarding_events = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ForwardingHistoryResponse forwarding_events.
         * @member {Array.<lnrpc.ForwardingEvent.$Properties>} forwarding_events
         * @memberof lnrpc.ForwardingHistoryResponse
         * @instance
         */
        ForwardingHistoryResponse.prototype.forwarding_events = $util.emptyArray;

        /**
         * ForwardingHistoryResponse last_offset_index.
         * @member {number} last_offset_index
         * @memberof lnrpc.ForwardingHistoryResponse
         * @instance
         */
        ForwardingHistoryResponse.prototype.last_offset_index = 0;

        /**
         * Creates a new ForwardingHistoryResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ForwardingHistoryResponse
         * @static
         * @param {lnrpc.ForwardingHistoryResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ForwardingHistoryResponse} ForwardingHistoryResponse instance
         * @type {{
         *   (properties: lnrpc.ForwardingHistoryResponse.$Shape): lnrpc.ForwardingHistoryResponse & lnrpc.ForwardingHistoryResponse.$Shape;
         *   (properties?: lnrpc.ForwardingHistoryResponse.$Properties): lnrpc.ForwardingHistoryResponse;
         * }}
         */
        ForwardingHistoryResponse.create = function(properties) {
            return new ForwardingHistoryResponse(properties);
        };

        /**
         * Encodes the specified ForwardingHistoryResponse message. Does not implicitly {@link lnrpc.ForwardingHistoryResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ForwardingHistoryResponse
         * @static
         * @param {lnrpc.ForwardingHistoryResponse.$Properties} message ForwardingHistoryResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardingHistoryResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.forwarding_events != null && message.forwarding_events.length)
                for (let i = 0; i < message.forwarding_events.length; ++i)
                    $root.lnrpc.ForwardingEvent.encode(message.forwarding_events[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.last_offset_index != null && $Object.hasOwnProperty.call(message, "last_offset_index") && message.last_offset_index !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.last_offset_index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ForwardingHistoryResponse message, length delimited. Does not implicitly {@link lnrpc.ForwardingHistoryResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ForwardingHistoryResponse
         * @static
         * @param {lnrpc.ForwardingHistoryResponse.$Properties} message ForwardingHistoryResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardingHistoryResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ForwardingHistoryResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ForwardingHistoryResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ForwardingHistoryResponse & lnrpc.ForwardingHistoryResponse.$Shape} ForwardingHistoryResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardingHistoryResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ForwardingHistoryResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.forwarding_events && message.forwarding_events.length))
                            message.forwarding_events = [];
                        message.forwarding_events.push($root.lnrpc.ForwardingEvent.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_offset_index = value;
                        else
                            delete message.last_offset_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ForwardingHistoryResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ForwardingHistoryResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ForwardingHistoryResponse & lnrpc.ForwardingHistoryResponse.$Shape} ForwardingHistoryResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardingHistoryResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForwardingHistoryResponse message.
         * @function verify
         * @memberof lnrpc.ForwardingHistoryResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForwardingHistoryResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.forwarding_events != null && $Object.hasOwnProperty.call(message, "forwarding_events")) {
                if (!$Array.isArray(message.forwarding_events))
                    return "forwarding_events: array expected";
                for (let i = 0; i < message.forwarding_events.length; ++i) {
                    let error = $root.lnrpc.ForwardingEvent.verify(message.forwarding_events[i], _depth + 1);
                    if (error)
                        return "forwarding_events." + error;
                }
            }
            if (message.last_offset_index != null && $Object.hasOwnProperty.call(message, "last_offset_index"))
                if (!$util.isInteger(message.last_offset_index))
                    return "last_offset_index: integer expected";
            return null;
        };

        /**
         * Creates a ForwardingHistoryResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ForwardingHistoryResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ForwardingHistoryResponse} ForwardingHistoryResponse
         */
        ForwardingHistoryResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ForwardingHistoryResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ForwardingHistoryResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ForwardingHistoryResponse();
            if (object.forwarding_events) {
                if (!$Array.isArray(object.forwarding_events))
                    throw $TypeError(".lnrpc.ForwardingHistoryResponse.forwarding_events: array expected");
                message.forwarding_events = $Array(object.forwarding_events.length);
                for (let i = 0; i < object.forwarding_events.length; ++i) {
                    if (!$util.isObject(object.forwarding_events[i]))
                        throw $TypeError(".lnrpc.ForwardingHistoryResponse.forwarding_events: object expected");
                    message.forwarding_events[i] = $root.lnrpc.ForwardingEvent.fromObject(object.forwarding_events[i], _depth + 1);
                }
            }
            if (object.last_offset_index != null)
                if ($Number(object.last_offset_index) !== 0)
                    message.last_offset_index = object.last_offset_index >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ForwardingHistoryResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ForwardingHistoryResponse
         * @static
         * @param {lnrpc.ForwardingHistoryResponse} message ForwardingHistoryResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForwardingHistoryResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.forwarding_events = [];
            if (options.defaults)
                object.last_offset_index = 0;
            if (message.forwarding_events && message.forwarding_events.length) {
                object.forwarding_events = $Array(message.forwarding_events.length);
                for (let j = 0; j < message.forwarding_events.length; ++j)
                    object.forwarding_events[j] = $root.lnrpc.ForwardingEvent.toObject(message.forwarding_events[j], options, _depth + 1);
            }
            if (message.last_offset_index != null && $Object.hasOwnProperty.call(message, "last_offset_index"))
                object.last_offset_index = message.last_offset_index;
            return object;
        };

        /**
         * Converts this ForwardingHistoryResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ForwardingHistoryResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForwardingHistoryResponse.prototype.toJSON = function() {
            return ForwardingHistoryResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ForwardingHistoryResponse
         * @function getTypeUrl
         * @memberof lnrpc.ForwardingHistoryResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ForwardingHistoryResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ForwardingHistoryResponse";
        };

        return ForwardingHistoryResponse;
    })();

    lnrpc.ExportChannelBackupRequest = (function() {

        /**
         * Properties of an ExportChannelBackupRequest.
         * @typedef {Object} lnrpc.ExportChannelBackupRequest.$Properties
         * @property {lnrpc.ChannelPoint.$Properties|null} [chan_point] ExportChannelBackupRequest chan_point
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an ExportChannelBackupRequest.
         * @memberof lnrpc
         * @interface IExportChannelBackupRequest
         * @augments lnrpc.ExportChannelBackupRequest.$Properties
         * @deprecated Use lnrpc.ExportChannelBackupRequest.$Properties instead.
         */

        /**
         * Shape of an ExportChannelBackupRequest.
         * @typedef {{
         *   chan_point?: lnrpc.ChannelPoint.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.ExportChannelBackupRequest.$Shape
         */

        /**
         * Constructs a new ExportChannelBackupRequest.
         * @memberof lnrpc
         * @classdesc Represents an ExportChannelBackupRequest.
         * @constructor
         * @param {lnrpc.ExportChannelBackupRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ExportChannelBackupRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ExportChannelBackupRequest chan_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} chan_point
         * @memberof lnrpc.ExportChannelBackupRequest
         * @instance
         */
        ExportChannelBackupRequest.prototype.chan_point = null;

        /**
         * Creates a new ExportChannelBackupRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ExportChannelBackupRequest
         * @static
         * @param {lnrpc.ExportChannelBackupRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ExportChannelBackupRequest} ExportChannelBackupRequest instance
         * @type {{
         *   (properties: lnrpc.ExportChannelBackupRequest.$Shape): lnrpc.ExportChannelBackupRequest & lnrpc.ExportChannelBackupRequest.$Shape;
         *   (properties?: lnrpc.ExportChannelBackupRequest.$Properties): lnrpc.ExportChannelBackupRequest;
         * }}
         */
        ExportChannelBackupRequest.create = function(properties) {
            return new ExportChannelBackupRequest(properties);
        };

        /**
         * Encodes the specified ExportChannelBackupRequest message. Does not implicitly {@link lnrpc.ExportChannelBackupRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ExportChannelBackupRequest
         * @static
         * @param {lnrpc.ExportChannelBackupRequest.$Properties} message ExportChannelBackupRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExportChannelBackupRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                $root.lnrpc.ChannelPoint.encode(message.chan_point, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ExportChannelBackupRequest message, length delimited. Does not implicitly {@link lnrpc.ExportChannelBackupRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ExportChannelBackupRequest
         * @static
         * @param {lnrpc.ExportChannelBackupRequest.$Properties} message ExportChannelBackupRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExportChannelBackupRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ExportChannelBackupRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ExportChannelBackupRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ExportChannelBackupRequest & lnrpc.ExportChannelBackupRequest.$Shape} ExportChannelBackupRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExportChannelBackupRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ExportChannelBackupRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.chan_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_point);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ExportChannelBackupRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ExportChannelBackupRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ExportChannelBackupRequest & lnrpc.ExportChannelBackupRequest.$Shape} ExportChannelBackupRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExportChannelBackupRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExportChannelBackupRequest message.
         * @function verify
         * @memberof lnrpc.ExportChannelBackupRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExportChannelBackupRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.chan_point, _depth + 1);
                if (error)
                    return "chan_point." + error;
            }
            return null;
        };

        /**
         * Creates an ExportChannelBackupRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ExportChannelBackupRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ExportChannelBackupRequest} ExportChannelBackupRequest
         */
        ExportChannelBackupRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ExportChannelBackupRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ExportChannelBackupRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ExportChannelBackupRequest();
            if (object.chan_point != null) {
                if (!$util.isObject(object.chan_point))
                    throw $TypeError(".lnrpc.ExportChannelBackupRequest.chan_point: object expected");
                message.chan_point = $root.lnrpc.ChannelPoint.fromObject(object.chan_point, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ExportChannelBackupRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ExportChannelBackupRequest
         * @static
         * @param {lnrpc.ExportChannelBackupRequest} message ExportChannelBackupRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExportChannelBackupRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.chan_point = null;
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                object.chan_point = $root.lnrpc.ChannelPoint.toObject(message.chan_point, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ExportChannelBackupRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ExportChannelBackupRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExportChannelBackupRequest.prototype.toJSON = function() {
            return ExportChannelBackupRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ExportChannelBackupRequest
         * @function getTypeUrl
         * @memberof lnrpc.ExportChannelBackupRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ExportChannelBackupRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ExportChannelBackupRequest";
        };

        return ExportChannelBackupRequest;
    })();

    lnrpc.ChannelBackup = (function() {

        /**
         * Properties of a ChannelBackup.
         * @typedef {Object} lnrpc.ChannelBackup.$Properties
         * @property {lnrpc.ChannelPoint.$Properties|null} [chan_point] ChannelBackup chan_point
         * @property {Uint8Array|null} [chan_backup] ChannelBackup chan_backup
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelBackup.
         * @memberof lnrpc
         * @interface IChannelBackup
         * @augments lnrpc.ChannelBackup.$Properties
         * @deprecated Use lnrpc.ChannelBackup.$Properties instead.
         */

        /**
         * Shape of a ChannelBackup.
         * @typedef {{
         *   chan_point?: lnrpc.ChannelPoint.$Shape|null;
         *   chan_backup?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.ChannelBackup.$Shape
         */

        /**
         * Constructs a new ChannelBackup.
         * @memberof lnrpc
         * @classdesc Represents a ChannelBackup.
         * @constructor
         * @param {lnrpc.ChannelBackup.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelBackup = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelBackup chan_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} chan_point
         * @memberof lnrpc.ChannelBackup
         * @instance
         */
        ChannelBackup.prototype.chan_point = null;

        /**
         * ChannelBackup chan_backup.
         * @member {Uint8Array} chan_backup
         * @memberof lnrpc.ChannelBackup
         * @instance
         */
        ChannelBackup.prototype.chan_backup = $util.newBuffer([]);

        /**
         * Creates a new ChannelBackup instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelBackup
         * @static
         * @param {lnrpc.ChannelBackup.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelBackup} ChannelBackup instance
         * @type {{
         *   (properties: lnrpc.ChannelBackup.$Shape): lnrpc.ChannelBackup & lnrpc.ChannelBackup.$Shape;
         *   (properties?: lnrpc.ChannelBackup.$Properties): lnrpc.ChannelBackup;
         * }}
         */
        ChannelBackup.create = function(properties) {
            return new ChannelBackup(properties);
        };

        /**
         * Encodes the specified ChannelBackup message. Does not implicitly {@link lnrpc.ChannelBackup.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelBackup
         * @static
         * @param {lnrpc.ChannelBackup.$Properties} message ChannelBackup message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBackup.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                $root.lnrpc.ChannelPoint.encode(message.chan_point, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.chan_backup != null && $Object.hasOwnProperty.call(message, "chan_backup") && message.chan_backup.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.chan_backup);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelBackup message, length delimited. Does not implicitly {@link lnrpc.ChannelBackup.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelBackup
         * @static
         * @param {lnrpc.ChannelBackup.$Properties} message ChannelBackup message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBackup.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelBackup message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelBackup
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelBackup & lnrpc.ChannelBackup.$Shape} ChannelBackup
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBackup.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelBackup(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.chan_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_point);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.chan_backup = value;
                        else
                            delete message.chan_backup;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelBackup message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelBackup
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelBackup & lnrpc.ChannelBackup.$Shape} ChannelBackup
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBackup.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelBackup message.
         * @function verify
         * @memberof lnrpc.ChannelBackup
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelBackup.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.chan_point, _depth + 1);
                if (error)
                    return "chan_point." + error;
            }
            if (message.chan_backup != null && $Object.hasOwnProperty.call(message, "chan_backup"))
                if (!(message.chan_backup && typeof message.chan_backup.length === "number" || $util.isString(message.chan_backup)))
                    return "chan_backup: buffer expected";
            return null;
        };

        /**
         * Creates a ChannelBackup message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelBackup
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelBackup} ChannelBackup
         */
        ChannelBackup.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelBackup)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelBackup: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelBackup();
            if (object.chan_point != null) {
                if (!$util.isObject(object.chan_point))
                    throw $TypeError(".lnrpc.ChannelBackup.chan_point: object expected");
                message.chan_point = $root.lnrpc.ChannelPoint.fromObject(object.chan_point, _depth + 1);
            }
            if (object.chan_backup != null)
                if (object.chan_backup.length)
                    if (typeof object.chan_backup === "string")
                        $util.base64.decode(object.chan_backup, message.chan_backup = $util.newBuffer($util.base64.length(object.chan_backup)), 0);
                    else if (object.chan_backup.length >= 0)
                        message.chan_backup = object.chan_backup;
            return message;
        };

        /**
         * Creates a plain object from a ChannelBackup message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelBackup
         * @static
         * @param {lnrpc.ChannelBackup} message ChannelBackup
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelBackup.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.chan_point = null;
                if (options.bytes === $String)
                    object.chan_backup = "";
                else {
                    object.chan_backup = [];
                    if (options.bytes !== $Array)
                        object.chan_backup = $util.newBuffer(object.chan_backup);
                }
            }
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                object.chan_point = $root.lnrpc.ChannelPoint.toObject(message.chan_point, options, _depth + 1);
            if (message.chan_backup != null && $Object.hasOwnProperty.call(message, "chan_backup"))
                object.chan_backup = options.bytes === $String ? $util.base64.encode(message.chan_backup, 0, message.chan_backup.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.chan_backup) : message.chan_backup;
            return object;
        };

        /**
         * Converts this ChannelBackup to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelBackup
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelBackup.prototype.toJSON = function() {
            return ChannelBackup.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelBackup
         * @function getTypeUrl
         * @memberof lnrpc.ChannelBackup
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelBackup.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelBackup";
        };

        return ChannelBackup;
    })();

    lnrpc.MultiChanBackup = (function() {

        /**
         * Properties of a MultiChanBackup.
         * @typedef {Object} lnrpc.MultiChanBackup.$Properties
         * @property {Array.<lnrpc.ChannelPoint.$Properties>|null} [chan_points] MultiChanBackup chan_points
         * @property {Uint8Array|null} [multi_chan_backup] MultiChanBackup multi_chan_backup
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MultiChanBackup.
         * @memberof lnrpc
         * @interface IMultiChanBackup
         * @augments lnrpc.MultiChanBackup.$Properties
         * @deprecated Use lnrpc.MultiChanBackup.$Properties instead.
         */

        /**
         * Shape of a MultiChanBackup.
         * @typedef {{
         *   chan_points?: Array.<lnrpc.ChannelPoint.$Shape>|null;
         *   multi_chan_backup?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.MultiChanBackup.$Shape
         */

        /**
         * Constructs a new MultiChanBackup.
         * @memberof lnrpc
         * @classdesc Represents a MultiChanBackup.
         * @constructor
         * @param {lnrpc.MultiChanBackup.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MultiChanBackup = function (properties) {
            this.chan_points = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MultiChanBackup chan_points.
         * @member {Array.<lnrpc.ChannelPoint.$Properties>} chan_points
         * @memberof lnrpc.MultiChanBackup
         * @instance
         */
        MultiChanBackup.prototype.chan_points = $util.emptyArray;

        /**
         * MultiChanBackup multi_chan_backup.
         * @member {Uint8Array} multi_chan_backup
         * @memberof lnrpc.MultiChanBackup
         * @instance
         */
        MultiChanBackup.prototype.multi_chan_backup = $util.newBuffer([]);

        /**
         * Creates a new MultiChanBackup instance using the specified properties.
         * @function create
         * @memberof lnrpc.MultiChanBackup
         * @static
         * @param {lnrpc.MultiChanBackup.$Properties=} [properties] Properties to set
         * @returns {lnrpc.MultiChanBackup} MultiChanBackup instance
         * @type {{
         *   (properties: lnrpc.MultiChanBackup.$Shape): lnrpc.MultiChanBackup & lnrpc.MultiChanBackup.$Shape;
         *   (properties?: lnrpc.MultiChanBackup.$Properties): lnrpc.MultiChanBackup;
         * }}
         */
        MultiChanBackup.create = function(properties) {
            return new MultiChanBackup(properties);
        };

        /**
         * Encodes the specified MultiChanBackup message. Does not implicitly {@link lnrpc.MultiChanBackup.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.MultiChanBackup
         * @static
         * @param {lnrpc.MultiChanBackup.$Properties} message MultiChanBackup message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MultiChanBackup.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_points != null && message.chan_points.length)
                for (let i = 0; i < message.chan_points.length; ++i)
                    $root.lnrpc.ChannelPoint.encode(message.chan_points[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.multi_chan_backup != null && $Object.hasOwnProperty.call(message, "multi_chan_backup") && message.multi_chan_backup.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.multi_chan_backup);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MultiChanBackup message, length delimited. Does not implicitly {@link lnrpc.MultiChanBackup.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.MultiChanBackup
         * @static
         * @param {lnrpc.MultiChanBackup.$Properties} message MultiChanBackup message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MultiChanBackup.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MultiChanBackup message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.MultiChanBackup
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.MultiChanBackup & lnrpc.MultiChanBackup.$Shape} MultiChanBackup
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MultiChanBackup.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.MultiChanBackup(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.chan_points && message.chan_points.length))
                            message.chan_points = [];
                        message.chan_points.push($root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.multi_chan_backup = value;
                        else
                            delete message.multi_chan_backup;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MultiChanBackup message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.MultiChanBackup
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.MultiChanBackup & lnrpc.MultiChanBackup.$Shape} MultiChanBackup
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MultiChanBackup.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MultiChanBackup message.
         * @function verify
         * @memberof lnrpc.MultiChanBackup
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MultiChanBackup.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_points != null && $Object.hasOwnProperty.call(message, "chan_points")) {
                if (!$Array.isArray(message.chan_points))
                    return "chan_points: array expected";
                for (let i = 0; i < message.chan_points.length; ++i) {
                    let error = $root.lnrpc.ChannelPoint.verify(message.chan_points[i], _depth + 1);
                    if (error)
                        return "chan_points." + error;
                }
            }
            if (message.multi_chan_backup != null && $Object.hasOwnProperty.call(message, "multi_chan_backup"))
                if (!(message.multi_chan_backup && typeof message.multi_chan_backup.length === "number" || $util.isString(message.multi_chan_backup)))
                    return "multi_chan_backup: buffer expected";
            return null;
        };

        /**
         * Creates a MultiChanBackup message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.MultiChanBackup
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.MultiChanBackup} MultiChanBackup
         */
        MultiChanBackup.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.MultiChanBackup)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.MultiChanBackup: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.MultiChanBackup();
            if (object.chan_points) {
                if (!$Array.isArray(object.chan_points))
                    throw $TypeError(".lnrpc.MultiChanBackup.chan_points: array expected");
                message.chan_points = $Array(object.chan_points.length);
                for (let i = 0; i < object.chan_points.length; ++i) {
                    if (!$util.isObject(object.chan_points[i]))
                        throw $TypeError(".lnrpc.MultiChanBackup.chan_points: object expected");
                    message.chan_points[i] = $root.lnrpc.ChannelPoint.fromObject(object.chan_points[i], _depth + 1);
                }
            }
            if (object.multi_chan_backup != null)
                if (object.multi_chan_backup.length)
                    if (typeof object.multi_chan_backup === "string")
                        $util.base64.decode(object.multi_chan_backup, message.multi_chan_backup = $util.newBuffer($util.base64.length(object.multi_chan_backup)), 0);
                    else if (object.multi_chan_backup.length >= 0)
                        message.multi_chan_backup = object.multi_chan_backup;
            return message;
        };

        /**
         * Creates a plain object from a MultiChanBackup message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.MultiChanBackup
         * @static
         * @param {lnrpc.MultiChanBackup} message MultiChanBackup
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MultiChanBackup.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.chan_points = [];
            if (options.defaults)
                if (options.bytes === $String)
                    object.multi_chan_backup = "";
                else {
                    object.multi_chan_backup = [];
                    if (options.bytes !== $Array)
                        object.multi_chan_backup = $util.newBuffer(object.multi_chan_backup);
                }
            if (message.chan_points && message.chan_points.length) {
                object.chan_points = $Array(message.chan_points.length);
                for (let j = 0; j < message.chan_points.length; ++j)
                    object.chan_points[j] = $root.lnrpc.ChannelPoint.toObject(message.chan_points[j], options, _depth + 1);
            }
            if (message.multi_chan_backup != null && $Object.hasOwnProperty.call(message, "multi_chan_backup"))
                object.multi_chan_backup = options.bytes === $String ? $util.base64.encode(message.multi_chan_backup, 0, message.multi_chan_backup.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.multi_chan_backup) : message.multi_chan_backup;
            return object;
        };

        /**
         * Converts this MultiChanBackup to JSON.
         * @function toJSON
         * @memberof lnrpc.MultiChanBackup
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MultiChanBackup.prototype.toJSON = function() {
            return MultiChanBackup.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MultiChanBackup
         * @function getTypeUrl
         * @memberof lnrpc.MultiChanBackup
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MultiChanBackup.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.MultiChanBackup";
        };

        return MultiChanBackup;
    })();

    lnrpc.ChanBackupExportRequest = (function() {

        /**
         * Properties of a ChanBackupExportRequest.
         * @typedef {Object} lnrpc.ChanBackupExportRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChanBackupExportRequest.
         * @memberof lnrpc
         * @interface IChanBackupExportRequest
         * @augments lnrpc.ChanBackupExportRequest.$Properties
         * @deprecated Use lnrpc.ChanBackupExportRequest.$Properties instead.
         */

        /**
         * Shape of a ChanBackupExportRequest.
         * @typedef {lnrpc.ChanBackupExportRequest.$Properties} lnrpc.ChanBackupExportRequest.$Shape
         */

        /**
         * Constructs a new ChanBackupExportRequest.
         * @memberof lnrpc
         * @classdesc Represents a ChanBackupExportRequest.
         * @constructor
         * @param {lnrpc.ChanBackupExportRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChanBackupExportRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ChanBackupExportRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChanBackupExportRequest
         * @static
         * @param {lnrpc.ChanBackupExportRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChanBackupExportRequest} ChanBackupExportRequest instance
         * @type {{
         *   (properties: lnrpc.ChanBackupExportRequest.$Shape): lnrpc.ChanBackupExportRequest & lnrpc.ChanBackupExportRequest.$Shape;
         *   (properties?: lnrpc.ChanBackupExportRequest.$Properties): lnrpc.ChanBackupExportRequest;
         * }}
         */
        ChanBackupExportRequest.create = function(properties) {
            return new ChanBackupExportRequest(properties);
        };

        /**
         * Encodes the specified ChanBackupExportRequest message. Does not implicitly {@link lnrpc.ChanBackupExportRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChanBackupExportRequest
         * @static
         * @param {lnrpc.ChanBackupExportRequest.$Properties} message ChanBackupExportRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChanBackupExportRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChanBackupExportRequest message, length delimited. Does not implicitly {@link lnrpc.ChanBackupExportRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChanBackupExportRequest
         * @static
         * @param {lnrpc.ChanBackupExportRequest.$Properties} message ChanBackupExportRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChanBackupExportRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChanBackupExportRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChanBackupExportRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChanBackupExportRequest & lnrpc.ChanBackupExportRequest.$Shape} ChanBackupExportRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChanBackupExportRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChanBackupExportRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChanBackupExportRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChanBackupExportRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChanBackupExportRequest & lnrpc.ChanBackupExportRequest.$Shape} ChanBackupExportRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChanBackupExportRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChanBackupExportRequest message.
         * @function verify
         * @memberof lnrpc.ChanBackupExportRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChanBackupExportRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ChanBackupExportRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChanBackupExportRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChanBackupExportRequest} ChanBackupExportRequest
         */
        ChanBackupExportRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChanBackupExportRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChanBackupExportRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.ChanBackupExportRequest();
        };

        /**
         * Creates a plain object from a ChanBackupExportRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChanBackupExportRequest
         * @static
         * @param {lnrpc.ChanBackupExportRequest} message ChanBackupExportRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChanBackupExportRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this ChanBackupExportRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ChanBackupExportRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChanBackupExportRequest.prototype.toJSON = function() {
            return ChanBackupExportRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChanBackupExportRequest
         * @function getTypeUrl
         * @memberof lnrpc.ChanBackupExportRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChanBackupExportRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChanBackupExportRequest";
        };

        return ChanBackupExportRequest;
    })();

    lnrpc.ChanBackupSnapshot = (function() {

        /**
         * Properties of a ChanBackupSnapshot.
         * @typedef {Object} lnrpc.ChanBackupSnapshot.$Properties
         * @property {lnrpc.ChannelBackups.$Properties|null} [single_chan_backups] ChanBackupSnapshot single_chan_backups
         * @property {lnrpc.MultiChanBackup.$Properties|null} [multi_chan_backup] ChanBackupSnapshot multi_chan_backup
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChanBackupSnapshot.
         * @memberof lnrpc
         * @interface IChanBackupSnapshot
         * @augments lnrpc.ChanBackupSnapshot.$Properties
         * @deprecated Use lnrpc.ChanBackupSnapshot.$Properties instead.
         */

        /**
         * Shape of a ChanBackupSnapshot.
         * @typedef {{
         *   single_chan_backups?: lnrpc.ChannelBackups.$Shape|null;
         *   multi_chan_backup?: lnrpc.MultiChanBackup.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.ChanBackupSnapshot.$Shape
         */

        /**
         * Constructs a new ChanBackupSnapshot.
         * @memberof lnrpc
         * @classdesc Represents a ChanBackupSnapshot.
         * @constructor
         * @param {lnrpc.ChanBackupSnapshot.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChanBackupSnapshot = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChanBackupSnapshot single_chan_backups.
         * @member {lnrpc.ChannelBackups.$Properties|null|undefined} single_chan_backups
         * @memberof lnrpc.ChanBackupSnapshot
         * @instance
         */
        ChanBackupSnapshot.prototype.single_chan_backups = null;

        /**
         * ChanBackupSnapshot multi_chan_backup.
         * @member {lnrpc.MultiChanBackup.$Properties|null|undefined} multi_chan_backup
         * @memberof lnrpc.ChanBackupSnapshot
         * @instance
         */
        ChanBackupSnapshot.prototype.multi_chan_backup = null;

        /**
         * Creates a new ChanBackupSnapshot instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChanBackupSnapshot
         * @static
         * @param {lnrpc.ChanBackupSnapshot.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChanBackupSnapshot} ChanBackupSnapshot instance
         * @type {{
         *   (properties: lnrpc.ChanBackupSnapshot.$Shape): lnrpc.ChanBackupSnapshot & lnrpc.ChanBackupSnapshot.$Shape;
         *   (properties?: lnrpc.ChanBackupSnapshot.$Properties): lnrpc.ChanBackupSnapshot;
         * }}
         */
        ChanBackupSnapshot.create = function(properties) {
            return new ChanBackupSnapshot(properties);
        };

        /**
         * Encodes the specified ChanBackupSnapshot message. Does not implicitly {@link lnrpc.ChanBackupSnapshot.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChanBackupSnapshot
         * @static
         * @param {lnrpc.ChanBackupSnapshot.$Properties} message ChanBackupSnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChanBackupSnapshot.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.single_chan_backups != null && $Object.hasOwnProperty.call(message, "single_chan_backups"))
                $root.lnrpc.ChannelBackups.encode(message.single_chan_backups, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.multi_chan_backup != null && $Object.hasOwnProperty.call(message, "multi_chan_backup"))
                $root.lnrpc.MultiChanBackup.encode(message.multi_chan_backup, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChanBackupSnapshot message, length delimited. Does not implicitly {@link lnrpc.ChanBackupSnapshot.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChanBackupSnapshot
         * @static
         * @param {lnrpc.ChanBackupSnapshot.$Properties} message ChanBackupSnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChanBackupSnapshot.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChanBackupSnapshot message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChanBackupSnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChanBackupSnapshot & lnrpc.ChanBackupSnapshot.$Shape} ChanBackupSnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChanBackupSnapshot.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChanBackupSnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.single_chan_backups = $root.lnrpc.ChannelBackups.decode(reader, reader.uint32(), $undefined, _depth + 1, message.single_chan_backups);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.multi_chan_backup = $root.lnrpc.MultiChanBackup.decode(reader, reader.uint32(), $undefined, _depth + 1, message.multi_chan_backup);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChanBackupSnapshot message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChanBackupSnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChanBackupSnapshot & lnrpc.ChanBackupSnapshot.$Shape} ChanBackupSnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChanBackupSnapshot.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChanBackupSnapshot message.
         * @function verify
         * @memberof lnrpc.ChanBackupSnapshot
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChanBackupSnapshot.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.single_chan_backups != null && $Object.hasOwnProperty.call(message, "single_chan_backups")) {
                let error = $root.lnrpc.ChannelBackups.verify(message.single_chan_backups, _depth + 1);
                if (error)
                    return "single_chan_backups." + error;
            }
            if (message.multi_chan_backup != null && $Object.hasOwnProperty.call(message, "multi_chan_backup")) {
                let error = $root.lnrpc.MultiChanBackup.verify(message.multi_chan_backup, _depth + 1);
                if (error)
                    return "multi_chan_backup." + error;
            }
            return null;
        };

        /**
         * Creates a ChanBackupSnapshot message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChanBackupSnapshot
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChanBackupSnapshot} ChanBackupSnapshot
         */
        ChanBackupSnapshot.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChanBackupSnapshot)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChanBackupSnapshot: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChanBackupSnapshot();
            if (object.single_chan_backups != null) {
                if (!$util.isObject(object.single_chan_backups))
                    throw $TypeError(".lnrpc.ChanBackupSnapshot.single_chan_backups: object expected");
                message.single_chan_backups = $root.lnrpc.ChannelBackups.fromObject(object.single_chan_backups, _depth + 1);
            }
            if (object.multi_chan_backup != null) {
                if (!$util.isObject(object.multi_chan_backup))
                    throw $TypeError(".lnrpc.ChanBackupSnapshot.multi_chan_backup: object expected");
                message.multi_chan_backup = $root.lnrpc.MultiChanBackup.fromObject(object.multi_chan_backup, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ChanBackupSnapshot message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChanBackupSnapshot
         * @static
         * @param {lnrpc.ChanBackupSnapshot} message ChanBackupSnapshot
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChanBackupSnapshot.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.single_chan_backups = null;
                object.multi_chan_backup = null;
            }
            if (message.single_chan_backups != null && $Object.hasOwnProperty.call(message, "single_chan_backups"))
                object.single_chan_backups = $root.lnrpc.ChannelBackups.toObject(message.single_chan_backups, options, _depth + 1);
            if (message.multi_chan_backup != null && $Object.hasOwnProperty.call(message, "multi_chan_backup"))
                object.multi_chan_backup = $root.lnrpc.MultiChanBackup.toObject(message.multi_chan_backup, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ChanBackupSnapshot to JSON.
         * @function toJSON
         * @memberof lnrpc.ChanBackupSnapshot
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChanBackupSnapshot.prototype.toJSON = function() {
            return ChanBackupSnapshot.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChanBackupSnapshot
         * @function getTypeUrl
         * @memberof lnrpc.ChanBackupSnapshot
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChanBackupSnapshot.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChanBackupSnapshot";
        };

        return ChanBackupSnapshot;
    })();

    lnrpc.ChannelBackups = (function() {

        /**
         * Properties of a ChannelBackups.
         * @typedef {Object} lnrpc.ChannelBackups.$Properties
         * @property {Array.<lnrpc.ChannelBackup.$Properties>|null} [chan_backups] ChannelBackups chan_backups
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelBackups.
         * @memberof lnrpc
         * @interface IChannelBackups
         * @augments lnrpc.ChannelBackups.$Properties
         * @deprecated Use lnrpc.ChannelBackups.$Properties instead.
         */

        /**
         * Shape of a ChannelBackups.
         * @typedef {{
         *   chan_backups?: Array.<lnrpc.ChannelBackup.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.ChannelBackups.$Shape
         */

        /**
         * Constructs a new ChannelBackups.
         * @memberof lnrpc
         * @classdesc Represents a ChannelBackups.
         * @constructor
         * @param {lnrpc.ChannelBackups.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelBackups = function (properties) {
            this.chan_backups = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelBackups chan_backups.
         * @member {Array.<lnrpc.ChannelBackup.$Properties>} chan_backups
         * @memberof lnrpc.ChannelBackups
         * @instance
         */
        ChannelBackups.prototype.chan_backups = $util.emptyArray;

        /**
         * Creates a new ChannelBackups instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelBackups
         * @static
         * @param {lnrpc.ChannelBackups.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelBackups} ChannelBackups instance
         * @type {{
         *   (properties: lnrpc.ChannelBackups.$Shape): lnrpc.ChannelBackups & lnrpc.ChannelBackups.$Shape;
         *   (properties?: lnrpc.ChannelBackups.$Properties): lnrpc.ChannelBackups;
         * }}
         */
        ChannelBackups.create = function(properties) {
            return new ChannelBackups(properties);
        };

        /**
         * Encodes the specified ChannelBackups message. Does not implicitly {@link lnrpc.ChannelBackups.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelBackups
         * @static
         * @param {lnrpc.ChannelBackups.$Properties} message ChannelBackups message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBackups.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_backups != null && message.chan_backups.length)
                for (let i = 0; i < message.chan_backups.length; ++i)
                    $root.lnrpc.ChannelBackup.encode(message.chan_backups[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelBackups message, length delimited. Does not implicitly {@link lnrpc.ChannelBackups.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelBackups
         * @static
         * @param {lnrpc.ChannelBackups.$Properties} message ChannelBackups message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBackups.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelBackups message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelBackups
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelBackups & lnrpc.ChannelBackups.$Shape} ChannelBackups
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBackups.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelBackups();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.chan_backups && message.chan_backups.length))
                            message.chan_backups = [];
                        message.chan_backups.push($root.lnrpc.ChannelBackup.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelBackups message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelBackups
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelBackups & lnrpc.ChannelBackups.$Shape} ChannelBackups
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBackups.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelBackups message.
         * @function verify
         * @memberof lnrpc.ChannelBackups
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelBackups.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_backups != null && $Object.hasOwnProperty.call(message, "chan_backups")) {
                if (!$Array.isArray(message.chan_backups))
                    return "chan_backups: array expected";
                for (let i = 0; i < message.chan_backups.length; ++i) {
                    let error = $root.lnrpc.ChannelBackup.verify(message.chan_backups[i], _depth + 1);
                    if (error)
                        return "chan_backups." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ChannelBackups message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelBackups
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelBackups} ChannelBackups
         */
        ChannelBackups.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelBackups)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelBackups: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelBackups();
            if (object.chan_backups) {
                if (!$Array.isArray(object.chan_backups))
                    throw $TypeError(".lnrpc.ChannelBackups.chan_backups: array expected");
                message.chan_backups = $Array(object.chan_backups.length);
                for (let i = 0; i < object.chan_backups.length; ++i) {
                    if (!$util.isObject(object.chan_backups[i]))
                        throw $TypeError(".lnrpc.ChannelBackups.chan_backups: object expected");
                    message.chan_backups[i] = $root.lnrpc.ChannelBackup.fromObject(object.chan_backups[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ChannelBackups message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelBackups
         * @static
         * @param {lnrpc.ChannelBackups} message ChannelBackups
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelBackups.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.chan_backups = [];
            if (message.chan_backups && message.chan_backups.length) {
                object.chan_backups = $Array(message.chan_backups.length);
                for (let j = 0; j < message.chan_backups.length; ++j)
                    object.chan_backups[j] = $root.lnrpc.ChannelBackup.toObject(message.chan_backups[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ChannelBackups to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelBackups
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelBackups.prototype.toJSON = function() {
            return ChannelBackups.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelBackups
         * @function getTypeUrl
         * @memberof lnrpc.ChannelBackups
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelBackups.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelBackups";
        };

        return ChannelBackups;
    })();

    lnrpc.RestoreChanBackupRequest = (function() {

        /**
         * Properties of a RestoreChanBackupRequest.
         * @typedef {Object} lnrpc.RestoreChanBackupRequest.$Properties
         * @property {lnrpc.ChannelBackups.$Properties|null} [chan_backups] RestoreChanBackupRequest chan_backups
         * @property {Uint8Array|null} [multi_chan_backup] RestoreChanBackupRequest multi_chan_backup
         * @property {"chan_backups"|"multi_chan_backup"} [backup] RestoreChanBackupRequest backup
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RestoreChanBackupRequest.
         * @memberof lnrpc
         * @interface IRestoreChanBackupRequest
         * @augments lnrpc.RestoreChanBackupRequest.$Properties
         * @deprecated Use lnrpc.RestoreChanBackupRequest.$Properties instead.
         */

        /**
         * Narrowed shape of a RestoreChanBackupRequest.
         * @typedef {{
         *   chan_backups?: lnrpc.ChannelBackups.$Shape|null;
         *   multi_chan_backup?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ backup?: undefined; chan_backups?: null; multi_chan_backup?: null }|{ backup?: "chan_backups"; chan_backups: lnrpc.ChannelBackups.$Shape; multi_chan_backup?: null }|{ backup?: "multi_chan_backup"; chan_backups?: null; multi_chan_backup: Uint8Array })
         * )} lnrpc.RestoreChanBackupRequest.$Shape
         */

        /**
         * Constructs a new RestoreChanBackupRequest.
         * @memberof lnrpc
         * @classdesc Represents a RestoreChanBackupRequest.
         * @constructor
         * @param {lnrpc.RestoreChanBackupRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RestoreChanBackupRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RestoreChanBackupRequest chan_backups.
         * @member {lnrpc.ChannelBackups.$Properties|null|undefined} chan_backups
         * @memberof lnrpc.RestoreChanBackupRequest
         * @instance
         */
        RestoreChanBackupRequest.prototype.chan_backups = null;

        /**
         * RestoreChanBackupRequest multi_chan_backup.
         * @member {Uint8Array|null|undefined} multi_chan_backup
         * @memberof lnrpc.RestoreChanBackupRequest
         * @instance
         */
        RestoreChanBackupRequest.prototype.multi_chan_backup = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * RestoreChanBackupRequest backup.
         * @member {"chan_backups"|"multi_chan_backup"|undefined} backup
         * @memberof lnrpc.RestoreChanBackupRequest
         * @instance
         */
        $Object.defineProperty(RestoreChanBackupRequest.prototype, "backup", {
            get: $util.oneOfGetter($oneOfFields = ["chan_backups", "multi_chan_backup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new RestoreChanBackupRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.RestoreChanBackupRequest
         * @static
         * @param {lnrpc.RestoreChanBackupRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.RestoreChanBackupRequest} RestoreChanBackupRequest instance
         * @type {{
         *   (properties: lnrpc.RestoreChanBackupRequest.$Shape): lnrpc.RestoreChanBackupRequest & lnrpc.RestoreChanBackupRequest.$Shape;
         *   (properties?: lnrpc.RestoreChanBackupRequest.$Properties): lnrpc.RestoreChanBackupRequest;
         * }}
         */
        RestoreChanBackupRequest.create = function(properties) {
            return new RestoreChanBackupRequest(properties);
        };

        /**
         * Encodes the specified RestoreChanBackupRequest message. Does not implicitly {@link lnrpc.RestoreChanBackupRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.RestoreChanBackupRequest
         * @static
         * @param {lnrpc.RestoreChanBackupRequest.$Properties} message RestoreChanBackupRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RestoreChanBackupRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_backups != null && $Object.hasOwnProperty.call(message, "chan_backups"))
                $root.lnrpc.ChannelBackups.encode(message.chan_backups, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.multi_chan_backup != null && $Object.hasOwnProperty.call(message, "multi_chan_backup"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.multi_chan_backup);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RestoreChanBackupRequest message, length delimited. Does not implicitly {@link lnrpc.RestoreChanBackupRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.RestoreChanBackupRequest
         * @static
         * @param {lnrpc.RestoreChanBackupRequest.$Properties} message RestoreChanBackupRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RestoreChanBackupRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RestoreChanBackupRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.RestoreChanBackupRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.RestoreChanBackupRequest & lnrpc.RestoreChanBackupRequest.$Shape} RestoreChanBackupRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RestoreChanBackupRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.RestoreChanBackupRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.chan_backups = $root.lnrpc.ChannelBackups.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_backups);
                        message.backup = "chan_backups";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.multi_chan_backup = reader.bytes();
                        message.backup = "multi_chan_backup";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RestoreChanBackupRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.RestoreChanBackupRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.RestoreChanBackupRequest & lnrpc.RestoreChanBackupRequest.$Shape} RestoreChanBackupRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RestoreChanBackupRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RestoreChanBackupRequest message.
         * @function verify
         * @memberof lnrpc.RestoreChanBackupRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RestoreChanBackupRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.chan_backups != null && $Object.hasOwnProperty.call(message, "chan_backups")) {
                properties.backup = 1;
                {
                    let error = $root.lnrpc.ChannelBackups.verify(message.chan_backups, _depth + 1);
                    if (error)
                        return "chan_backups." + error;
                }
            }
            if (message.multi_chan_backup != null && $Object.hasOwnProperty.call(message, "multi_chan_backup")) {
                if (properties.backup === 1)
                    return "backup: multiple values";
                properties.backup = 1;
                if (!(message.multi_chan_backup && typeof message.multi_chan_backup.length === "number" || $util.isString(message.multi_chan_backup)))
                    return "multi_chan_backup: buffer expected";
            }
            return null;
        };

        /**
         * Creates a RestoreChanBackupRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.RestoreChanBackupRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.RestoreChanBackupRequest} RestoreChanBackupRequest
         */
        RestoreChanBackupRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.RestoreChanBackupRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.RestoreChanBackupRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.RestoreChanBackupRequest();
            if (object.chan_backups != null) {
                if (!$util.isObject(object.chan_backups))
                    throw $TypeError(".lnrpc.RestoreChanBackupRequest.chan_backups: object expected");
                message.chan_backups = $root.lnrpc.ChannelBackups.fromObject(object.chan_backups, _depth + 1);
            }
            if (object.multi_chan_backup != null)
                if (typeof object.multi_chan_backup === "string")
                    $util.base64.decode(object.multi_chan_backup, message.multi_chan_backup = $util.newBuffer($util.base64.length(object.multi_chan_backup)), 0);
                else if (object.multi_chan_backup.length >= 0)
                    message.multi_chan_backup = object.multi_chan_backup;
            return message;
        };

        /**
         * Creates a plain object from a RestoreChanBackupRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.RestoreChanBackupRequest
         * @static
         * @param {lnrpc.RestoreChanBackupRequest} message RestoreChanBackupRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RestoreChanBackupRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.chan_backups != null && $Object.hasOwnProperty.call(message, "chan_backups")) {
                object.chan_backups = $root.lnrpc.ChannelBackups.toObject(message.chan_backups, options, _depth + 1);
                if (options.oneofs)
                    object.backup = "chan_backups";
            }
            if (message.multi_chan_backup != null && $Object.hasOwnProperty.call(message, "multi_chan_backup")) {
                object.multi_chan_backup = options.bytes === $String ? $util.base64.encode(message.multi_chan_backup, 0, message.multi_chan_backup.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.multi_chan_backup) : message.multi_chan_backup;
                if (options.oneofs)
                    object.backup = "multi_chan_backup";
            }
            return object;
        };

        /**
         * Converts this RestoreChanBackupRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.RestoreChanBackupRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RestoreChanBackupRequest.prototype.toJSON = function() {
            return RestoreChanBackupRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RestoreChanBackupRequest
         * @function getTypeUrl
         * @memberof lnrpc.RestoreChanBackupRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RestoreChanBackupRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.RestoreChanBackupRequest";
        };

        return RestoreChanBackupRequest;
    })();

    lnrpc.RestoreBackupResponse = (function() {

        /**
         * Properties of a RestoreBackupResponse.
         * @typedef {Object} lnrpc.RestoreBackupResponse.$Properties
         * @property {number|null} [num_restored] RestoreBackupResponse num_restored
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RestoreBackupResponse.
         * @memberof lnrpc
         * @interface IRestoreBackupResponse
         * @augments lnrpc.RestoreBackupResponse.$Properties
         * @deprecated Use lnrpc.RestoreBackupResponse.$Properties instead.
         */

        /**
         * Shape of a RestoreBackupResponse.
         * @typedef {lnrpc.RestoreBackupResponse.$Properties} lnrpc.RestoreBackupResponse.$Shape
         */

        /**
         * Constructs a new RestoreBackupResponse.
         * @memberof lnrpc
         * @classdesc Represents a RestoreBackupResponse.
         * @constructor
         * @param {lnrpc.RestoreBackupResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RestoreBackupResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RestoreBackupResponse num_restored.
         * @member {number} num_restored
         * @memberof lnrpc.RestoreBackupResponse
         * @instance
         */
        RestoreBackupResponse.prototype.num_restored = 0;

        /**
         * Creates a new RestoreBackupResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.RestoreBackupResponse
         * @static
         * @param {lnrpc.RestoreBackupResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.RestoreBackupResponse} RestoreBackupResponse instance
         * @type {{
         *   (properties: lnrpc.RestoreBackupResponse.$Shape): lnrpc.RestoreBackupResponse & lnrpc.RestoreBackupResponse.$Shape;
         *   (properties?: lnrpc.RestoreBackupResponse.$Properties): lnrpc.RestoreBackupResponse;
         * }}
         */
        RestoreBackupResponse.create = function(properties) {
            return new RestoreBackupResponse(properties);
        };

        /**
         * Encodes the specified RestoreBackupResponse message. Does not implicitly {@link lnrpc.RestoreBackupResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.RestoreBackupResponse
         * @static
         * @param {lnrpc.RestoreBackupResponse.$Properties} message RestoreBackupResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RestoreBackupResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.num_restored != null && $Object.hasOwnProperty.call(message, "num_restored") && message.num_restored !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.num_restored);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RestoreBackupResponse message, length delimited. Does not implicitly {@link lnrpc.RestoreBackupResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.RestoreBackupResponse
         * @static
         * @param {lnrpc.RestoreBackupResponse.$Properties} message RestoreBackupResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RestoreBackupResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RestoreBackupResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.RestoreBackupResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.RestoreBackupResponse & lnrpc.RestoreBackupResponse.$Shape} RestoreBackupResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RestoreBackupResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.RestoreBackupResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_restored = value;
                        else
                            delete message.num_restored;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RestoreBackupResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.RestoreBackupResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.RestoreBackupResponse & lnrpc.RestoreBackupResponse.$Shape} RestoreBackupResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RestoreBackupResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RestoreBackupResponse message.
         * @function verify
         * @memberof lnrpc.RestoreBackupResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RestoreBackupResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.num_restored != null && $Object.hasOwnProperty.call(message, "num_restored"))
                if (!$util.isInteger(message.num_restored))
                    return "num_restored: integer expected";
            return null;
        };

        /**
         * Creates a RestoreBackupResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.RestoreBackupResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.RestoreBackupResponse} RestoreBackupResponse
         */
        RestoreBackupResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.RestoreBackupResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.RestoreBackupResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.RestoreBackupResponse();
            if (object.num_restored != null)
                if ($Number(object.num_restored) !== 0)
                    message.num_restored = object.num_restored >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a RestoreBackupResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.RestoreBackupResponse
         * @static
         * @param {lnrpc.RestoreBackupResponse} message RestoreBackupResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RestoreBackupResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.num_restored = 0;
            if (message.num_restored != null && $Object.hasOwnProperty.call(message, "num_restored"))
                object.num_restored = message.num_restored;
            return object;
        };

        /**
         * Converts this RestoreBackupResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.RestoreBackupResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RestoreBackupResponse.prototype.toJSON = function() {
            return RestoreBackupResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RestoreBackupResponse
         * @function getTypeUrl
         * @memberof lnrpc.RestoreBackupResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RestoreBackupResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.RestoreBackupResponse";
        };

        return RestoreBackupResponse;
    })();

    lnrpc.ChannelBackupSubscription = (function() {

        /**
         * Properties of a ChannelBackupSubscription.
         * @typedef {Object} lnrpc.ChannelBackupSubscription.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelBackupSubscription.
         * @memberof lnrpc
         * @interface IChannelBackupSubscription
         * @augments lnrpc.ChannelBackupSubscription.$Properties
         * @deprecated Use lnrpc.ChannelBackupSubscription.$Properties instead.
         */

        /**
         * Shape of a ChannelBackupSubscription.
         * @typedef {lnrpc.ChannelBackupSubscription.$Properties} lnrpc.ChannelBackupSubscription.$Shape
         */

        /**
         * Constructs a new ChannelBackupSubscription.
         * @memberof lnrpc
         * @classdesc Represents a ChannelBackupSubscription.
         * @constructor
         * @param {lnrpc.ChannelBackupSubscription.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelBackupSubscription = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ChannelBackupSubscription instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelBackupSubscription
         * @static
         * @param {lnrpc.ChannelBackupSubscription.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelBackupSubscription} ChannelBackupSubscription instance
         * @type {{
         *   (properties: lnrpc.ChannelBackupSubscription.$Shape): lnrpc.ChannelBackupSubscription & lnrpc.ChannelBackupSubscription.$Shape;
         *   (properties?: lnrpc.ChannelBackupSubscription.$Properties): lnrpc.ChannelBackupSubscription;
         * }}
         */
        ChannelBackupSubscription.create = function(properties) {
            return new ChannelBackupSubscription(properties);
        };

        /**
         * Encodes the specified ChannelBackupSubscription message. Does not implicitly {@link lnrpc.ChannelBackupSubscription.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelBackupSubscription
         * @static
         * @param {lnrpc.ChannelBackupSubscription.$Properties} message ChannelBackupSubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBackupSubscription.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelBackupSubscription message, length delimited. Does not implicitly {@link lnrpc.ChannelBackupSubscription.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelBackupSubscription
         * @static
         * @param {lnrpc.ChannelBackupSubscription.$Properties} message ChannelBackupSubscription message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelBackupSubscription.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelBackupSubscription message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelBackupSubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelBackupSubscription & lnrpc.ChannelBackupSubscription.$Shape} ChannelBackupSubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBackupSubscription.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelBackupSubscription();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelBackupSubscription message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelBackupSubscription
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelBackupSubscription & lnrpc.ChannelBackupSubscription.$Shape} ChannelBackupSubscription
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelBackupSubscription.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelBackupSubscription message.
         * @function verify
         * @memberof lnrpc.ChannelBackupSubscription
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelBackupSubscription.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ChannelBackupSubscription message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelBackupSubscription
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelBackupSubscription} ChannelBackupSubscription
         */
        ChannelBackupSubscription.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelBackupSubscription)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelBackupSubscription: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.ChannelBackupSubscription();
        };

        /**
         * Creates a plain object from a ChannelBackupSubscription message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelBackupSubscription
         * @static
         * @param {lnrpc.ChannelBackupSubscription} message ChannelBackupSubscription
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelBackupSubscription.toObject = function () {
            return {};
        };

        /**
         * Converts this ChannelBackupSubscription to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelBackupSubscription
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelBackupSubscription.prototype.toJSON = function() {
            return ChannelBackupSubscription.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelBackupSubscription
         * @function getTypeUrl
         * @memberof lnrpc.ChannelBackupSubscription
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelBackupSubscription.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelBackupSubscription";
        };

        return ChannelBackupSubscription;
    })();

    lnrpc.VerifyChanBackupResponse = (function() {

        /**
         * Properties of a VerifyChanBackupResponse.
         * @typedef {Object} lnrpc.VerifyChanBackupResponse.$Properties
         * @property {Array.<string>|null} [chan_points] VerifyChanBackupResponse chan_points
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a VerifyChanBackupResponse.
         * @memberof lnrpc
         * @interface IVerifyChanBackupResponse
         * @augments lnrpc.VerifyChanBackupResponse.$Properties
         * @deprecated Use lnrpc.VerifyChanBackupResponse.$Properties instead.
         */

        /**
         * Shape of a VerifyChanBackupResponse.
         * @typedef {lnrpc.VerifyChanBackupResponse.$Properties} lnrpc.VerifyChanBackupResponse.$Shape
         */

        /**
         * Constructs a new VerifyChanBackupResponse.
         * @memberof lnrpc
         * @classdesc Represents a VerifyChanBackupResponse.
         * @constructor
         * @param {lnrpc.VerifyChanBackupResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const VerifyChanBackupResponse = function (properties) {
            this.chan_points = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * VerifyChanBackupResponse chan_points.
         * @member {Array.<string>} chan_points
         * @memberof lnrpc.VerifyChanBackupResponse
         * @instance
         */
        VerifyChanBackupResponse.prototype.chan_points = $util.emptyArray;

        /**
         * Creates a new VerifyChanBackupResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.VerifyChanBackupResponse
         * @static
         * @param {lnrpc.VerifyChanBackupResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.VerifyChanBackupResponse} VerifyChanBackupResponse instance
         * @type {{
         *   (properties: lnrpc.VerifyChanBackupResponse.$Shape): lnrpc.VerifyChanBackupResponse & lnrpc.VerifyChanBackupResponse.$Shape;
         *   (properties?: lnrpc.VerifyChanBackupResponse.$Properties): lnrpc.VerifyChanBackupResponse;
         * }}
         */
        VerifyChanBackupResponse.create = function(properties) {
            return new VerifyChanBackupResponse(properties);
        };

        /**
         * Encodes the specified VerifyChanBackupResponse message. Does not implicitly {@link lnrpc.VerifyChanBackupResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.VerifyChanBackupResponse
         * @static
         * @param {lnrpc.VerifyChanBackupResponse.$Properties} message VerifyChanBackupResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyChanBackupResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_points != null && message.chan_points.length)
                for (let i = 0; i < message.chan_points.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.chan_points[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VerifyChanBackupResponse message, length delimited. Does not implicitly {@link lnrpc.VerifyChanBackupResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.VerifyChanBackupResponse
         * @static
         * @param {lnrpc.VerifyChanBackupResponse.$Properties} message VerifyChanBackupResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyChanBackupResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VerifyChanBackupResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.VerifyChanBackupResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.VerifyChanBackupResponse & lnrpc.VerifyChanBackupResponse.$Shape} VerifyChanBackupResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyChanBackupResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.VerifyChanBackupResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.chan_points && message.chan_points.length))
                            message.chan_points = [];
                        message.chan_points.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a VerifyChanBackupResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.VerifyChanBackupResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.VerifyChanBackupResponse & lnrpc.VerifyChanBackupResponse.$Shape} VerifyChanBackupResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyChanBackupResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VerifyChanBackupResponse message.
         * @function verify
         * @memberof lnrpc.VerifyChanBackupResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VerifyChanBackupResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_points != null && $Object.hasOwnProperty.call(message, "chan_points")) {
                if (!$Array.isArray(message.chan_points))
                    return "chan_points: array expected";
                for (let i = 0; i < message.chan_points.length; ++i)
                    if (!$util.isString(message.chan_points[i]))
                        return "chan_points: string[] expected";
            }
            return null;
        };

        /**
         * Creates a VerifyChanBackupResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.VerifyChanBackupResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.VerifyChanBackupResponse} VerifyChanBackupResponse
         */
        VerifyChanBackupResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.VerifyChanBackupResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.VerifyChanBackupResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.VerifyChanBackupResponse();
            if (object.chan_points) {
                if (!$Array.isArray(object.chan_points))
                    throw $TypeError(".lnrpc.VerifyChanBackupResponse.chan_points: array expected");
                message.chan_points = $Array(object.chan_points.length);
                for (let i = 0; i < object.chan_points.length; ++i)
                    message.chan_points[i] = $String(object.chan_points[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a VerifyChanBackupResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.VerifyChanBackupResponse
         * @static
         * @param {lnrpc.VerifyChanBackupResponse} message VerifyChanBackupResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VerifyChanBackupResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.chan_points = [];
            if (message.chan_points && message.chan_points.length) {
                object.chan_points = $Array(message.chan_points.length);
                for (let j = 0; j < message.chan_points.length; ++j)
                    object.chan_points[j] = message.chan_points[j];
            }
            return object;
        };

        /**
         * Converts this VerifyChanBackupResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.VerifyChanBackupResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VerifyChanBackupResponse.prototype.toJSON = function() {
            return VerifyChanBackupResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for VerifyChanBackupResponse
         * @function getTypeUrl
         * @memberof lnrpc.VerifyChanBackupResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        VerifyChanBackupResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.VerifyChanBackupResponse";
        };

        return VerifyChanBackupResponse;
    })();

    lnrpc.MacaroonPermission = (function() {

        /**
         * Properties of a MacaroonPermission.
         * @typedef {Object} lnrpc.MacaroonPermission.$Properties
         * @property {string|null} [entity] MacaroonPermission entity
         * @property {string|null} [action] MacaroonPermission action
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MacaroonPermission.
         * @memberof lnrpc
         * @interface IMacaroonPermission
         * @augments lnrpc.MacaroonPermission.$Properties
         * @deprecated Use lnrpc.MacaroonPermission.$Properties instead.
         */

        /**
         * Shape of a MacaroonPermission.
         * @typedef {lnrpc.MacaroonPermission.$Properties} lnrpc.MacaroonPermission.$Shape
         */

        /**
         * Constructs a new MacaroonPermission.
         * @memberof lnrpc
         * @classdesc Represents a MacaroonPermission.
         * @constructor
         * @param {lnrpc.MacaroonPermission.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MacaroonPermission = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MacaroonPermission entity.
         * @member {string} entity
         * @memberof lnrpc.MacaroonPermission
         * @instance
         */
        MacaroonPermission.prototype.entity = "";

        /**
         * MacaroonPermission action.
         * @member {string} action
         * @memberof lnrpc.MacaroonPermission
         * @instance
         */
        MacaroonPermission.prototype.action = "";

        /**
         * Creates a new MacaroonPermission instance using the specified properties.
         * @function create
         * @memberof lnrpc.MacaroonPermission
         * @static
         * @param {lnrpc.MacaroonPermission.$Properties=} [properties] Properties to set
         * @returns {lnrpc.MacaroonPermission} MacaroonPermission instance
         * @type {{
         *   (properties: lnrpc.MacaroonPermission.$Shape): lnrpc.MacaroonPermission & lnrpc.MacaroonPermission.$Shape;
         *   (properties?: lnrpc.MacaroonPermission.$Properties): lnrpc.MacaroonPermission;
         * }}
         */
        MacaroonPermission.create = function(properties) {
            return new MacaroonPermission(properties);
        };

        /**
         * Encodes the specified MacaroonPermission message. Does not implicitly {@link lnrpc.MacaroonPermission.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.MacaroonPermission
         * @static
         * @param {lnrpc.MacaroonPermission.$Properties} message MacaroonPermission message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MacaroonPermission.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.entity != null && $Object.hasOwnProperty.call(message, "entity") && message.entity !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.entity);
            if (message.action != null && $Object.hasOwnProperty.call(message, "action") && message.action !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.action);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MacaroonPermission message, length delimited. Does not implicitly {@link lnrpc.MacaroonPermission.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.MacaroonPermission
         * @static
         * @param {lnrpc.MacaroonPermission.$Properties} message MacaroonPermission message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MacaroonPermission.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MacaroonPermission message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.MacaroonPermission
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.MacaroonPermission & lnrpc.MacaroonPermission.$Shape} MacaroonPermission
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MacaroonPermission.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.MacaroonPermission(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.entity = value;
                        else
                            delete message.entity;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.action = value;
                        else
                            delete message.action;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MacaroonPermission message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.MacaroonPermission
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.MacaroonPermission & lnrpc.MacaroonPermission.$Shape} MacaroonPermission
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MacaroonPermission.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MacaroonPermission message.
         * @function verify
         * @memberof lnrpc.MacaroonPermission
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MacaroonPermission.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.entity != null && $Object.hasOwnProperty.call(message, "entity"))
                if (!$util.isString(message.entity))
                    return "entity: string expected";
            if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
                if (!$util.isString(message.action))
                    return "action: string expected";
            return null;
        };

        /**
         * Creates a MacaroonPermission message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.MacaroonPermission
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.MacaroonPermission} MacaroonPermission
         */
        MacaroonPermission.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.MacaroonPermission)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.MacaroonPermission: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.MacaroonPermission();
            if (object.entity != null)
                if (typeof object.entity !== "string" || object.entity.length)
                    message.entity = $String(object.entity);
            if (object.action != null)
                if (typeof object.action !== "string" || object.action.length)
                    message.action = $String(object.action);
            return message;
        };

        /**
         * Creates a plain object from a MacaroonPermission message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.MacaroonPermission
         * @static
         * @param {lnrpc.MacaroonPermission} message MacaroonPermission
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MacaroonPermission.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.entity = "";
                object.action = "";
            }
            if (message.entity != null && $Object.hasOwnProperty.call(message, "entity"))
                object.entity = message.entity;
            if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
                object.action = message.action;
            return object;
        };

        /**
         * Converts this MacaroonPermission to JSON.
         * @function toJSON
         * @memberof lnrpc.MacaroonPermission
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MacaroonPermission.prototype.toJSON = function() {
            return MacaroonPermission.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MacaroonPermission
         * @function getTypeUrl
         * @memberof lnrpc.MacaroonPermission
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MacaroonPermission.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.MacaroonPermission";
        };

        return MacaroonPermission;
    })();

    lnrpc.BakeMacaroonRequest = (function() {

        /**
         * Properties of a BakeMacaroonRequest.
         * @typedef {Object} lnrpc.BakeMacaroonRequest.$Properties
         * @property {Array.<lnrpc.MacaroonPermission.$Properties>|null} [permissions] BakeMacaroonRequest permissions
         * @property {Long|null} [root_key_id] BakeMacaroonRequest root_key_id
         * @property {boolean|null} [allow_external_permissions] BakeMacaroonRequest allow_external_permissions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BakeMacaroonRequest.
         * @memberof lnrpc
         * @interface IBakeMacaroonRequest
         * @augments lnrpc.BakeMacaroonRequest.$Properties
         * @deprecated Use lnrpc.BakeMacaroonRequest.$Properties instead.
         */

        /**
         * Shape of a BakeMacaroonRequest.
         * @typedef {lnrpc.BakeMacaroonRequest.$Properties} lnrpc.BakeMacaroonRequest.$Shape
         */

        /**
         * Constructs a new BakeMacaroonRequest.
         * @memberof lnrpc
         * @classdesc Represents a BakeMacaroonRequest.
         * @constructor
         * @param {lnrpc.BakeMacaroonRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BakeMacaroonRequest = function (properties) {
            this.permissions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BakeMacaroonRequest permissions.
         * @member {Array.<lnrpc.MacaroonPermission.$Properties>} permissions
         * @memberof lnrpc.BakeMacaroonRequest
         * @instance
         */
        BakeMacaroonRequest.prototype.permissions = $util.emptyArray;

        /**
         * BakeMacaroonRequest root_key_id.
         * @member {Long} root_key_id
         * @memberof lnrpc.BakeMacaroonRequest
         * @instance
         */
        BakeMacaroonRequest.prototype.root_key_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BakeMacaroonRequest allow_external_permissions.
         * @member {boolean} allow_external_permissions
         * @memberof lnrpc.BakeMacaroonRequest
         * @instance
         */
        BakeMacaroonRequest.prototype.allow_external_permissions = false;

        /**
         * Creates a new BakeMacaroonRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.BakeMacaroonRequest
         * @static
         * @param {lnrpc.BakeMacaroonRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.BakeMacaroonRequest} BakeMacaroonRequest instance
         * @type {{
         *   (properties: lnrpc.BakeMacaroonRequest.$Shape): lnrpc.BakeMacaroonRequest & lnrpc.BakeMacaroonRequest.$Shape;
         *   (properties?: lnrpc.BakeMacaroonRequest.$Properties): lnrpc.BakeMacaroonRequest;
         * }}
         */
        BakeMacaroonRequest.create = function(properties) {
            return new BakeMacaroonRequest(properties);
        };

        /**
         * Encodes the specified BakeMacaroonRequest message. Does not implicitly {@link lnrpc.BakeMacaroonRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.BakeMacaroonRequest
         * @static
         * @param {lnrpc.BakeMacaroonRequest.$Properties} message BakeMacaroonRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BakeMacaroonRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.permissions != null && message.permissions.length)
                for (let i = 0; i < message.permissions.length; ++i)
                    $root.lnrpc.MacaroonPermission.encode(message.permissions[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.root_key_id != null && $Object.hasOwnProperty.call(message, "root_key_id") && (typeof message.root_key_id === "object" ? message.root_key_id.low || message.root_key_id.high : message.root_key_id !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.root_key_id);
            if (message.allow_external_permissions != null && $Object.hasOwnProperty.call(message, "allow_external_permissions") && message.allow_external_permissions !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.allow_external_permissions);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BakeMacaroonRequest message, length delimited. Does not implicitly {@link lnrpc.BakeMacaroonRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.BakeMacaroonRequest
         * @static
         * @param {lnrpc.BakeMacaroonRequest.$Properties} message BakeMacaroonRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BakeMacaroonRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BakeMacaroonRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.BakeMacaroonRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.BakeMacaroonRequest & lnrpc.BakeMacaroonRequest.$Shape} BakeMacaroonRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BakeMacaroonRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.BakeMacaroonRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.permissions && message.permissions.length))
                            message.permissions = [];
                        message.permissions.push($root.lnrpc.MacaroonPermission.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.root_key_id = value;
                        else
                            delete message.root_key_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.allow_external_permissions = value;
                        else
                            delete message.allow_external_permissions;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BakeMacaroonRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.BakeMacaroonRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.BakeMacaroonRequest & lnrpc.BakeMacaroonRequest.$Shape} BakeMacaroonRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BakeMacaroonRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BakeMacaroonRequest message.
         * @function verify
         * @memberof lnrpc.BakeMacaroonRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BakeMacaroonRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.permissions != null && $Object.hasOwnProperty.call(message, "permissions")) {
                if (!$Array.isArray(message.permissions))
                    return "permissions: array expected";
                for (let i = 0; i < message.permissions.length; ++i) {
                    let error = $root.lnrpc.MacaroonPermission.verify(message.permissions[i], _depth + 1);
                    if (error)
                        return "permissions." + error;
                }
            }
            if (message.root_key_id != null && $Object.hasOwnProperty.call(message, "root_key_id"))
                if (!$util.isInteger(message.root_key_id) && !(message.root_key_id && $util.isInteger(message.root_key_id.low) && $util.isInteger(message.root_key_id.high)))
                    return "root_key_id: integer|Long expected";
            if (message.allow_external_permissions != null && $Object.hasOwnProperty.call(message, "allow_external_permissions"))
                if (typeof message.allow_external_permissions !== "boolean")
                    return "allow_external_permissions: boolean expected";
            return null;
        };

        /**
         * Creates a BakeMacaroonRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.BakeMacaroonRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.BakeMacaroonRequest} BakeMacaroonRequest
         */
        BakeMacaroonRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.BakeMacaroonRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.BakeMacaroonRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.BakeMacaroonRequest();
            if (object.permissions) {
                if (!$Array.isArray(object.permissions))
                    throw $TypeError(".lnrpc.BakeMacaroonRequest.permissions: array expected");
                message.permissions = $Array(object.permissions.length);
                for (let i = 0; i < object.permissions.length; ++i) {
                    if (!$util.isObject(object.permissions[i]))
                        throw $TypeError(".lnrpc.BakeMacaroonRequest.permissions: object expected");
                    message.permissions[i] = $root.lnrpc.MacaroonPermission.fromObject(object.permissions[i], _depth + 1);
                }
            }
            if (object.root_key_id != null)
                if (typeof object.root_key_id === "object" ? object.root_key_id.low || object.root_key_id.high : $Number(object.root_key_id) !== 0)
                    if ($util.Long)
                        message.root_key_id = $util.Long.fromValue(object.root_key_id, true);
                    else if (typeof object.root_key_id === "string")
                        message.root_key_id = $parseInt(object.root_key_id, 10);
                    else if (typeof object.root_key_id === "number")
                        message.root_key_id = object.root_key_id;
                    else if (typeof object.root_key_id === "object")
                        message.root_key_id = new $util.LongBits(object.root_key_id.low >>> 0, object.root_key_id.high >>> 0).toNumber(true);
            if (object.allow_external_permissions != null)
                if (object.allow_external_permissions)
                    message.allow_external_permissions = $Boolean(object.allow_external_permissions);
            return message;
        };

        /**
         * Creates a plain object from a BakeMacaroonRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.BakeMacaroonRequest
         * @static
         * @param {lnrpc.BakeMacaroonRequest} message BakeMacaroonRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BakeMacaroonRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.permissions = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.root_key_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.root_key_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.allow_external_permissions = false;
            }
            if (message.permissions && message.permissions.length) {
                object.permissions = $Array(message.permissions.length);
                for (let j = 0; j < message.permissions.length; ++j)
                    object.permissions[j] = $root.lnrpc.MacaroonPermission.toObject(message.permissions[j], options, _depth + 1);
            }
            if (message.root_key_id != null && $Object.hasOwnProperty.call(message, "root_key_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.root_key_id = typeof message.root_key_id === "number" ? $BigInt(message.root_key_id) : $util.Long.fromBits(message.root_key_id.low >>> 0, message.root_key_id.high >>> 0, true).toBigInt();
                else if (typeof message.root_key_id === "number")
                    object.root_key_id = options.longs === $String ? $String(message.root_key_id) : message.root_key_id;
                else
                    object.root_key_id = options.longs === $String ? $util.Long.prototype.toString.call(message.root_key_id) : options.longs === $Number ? new $util.LongBits(message.root_key_id.low >>> 0, message.root_key_id.high >>> 0).toNumber(true) : message.root_key_id;
            if (message.allow_external_permissions != null && $Object.hasOwnProperty.call(message, "allow_external_permissions"))
                object.allow_external_permissions = message.allow_external_permissions;
            return object;
        };

        /**
         * Converts this BakeMacaroonRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.BakeMacaroonRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BakeMacaroonRequest.prototype.toJSON = function() {
            return BakeMacaroonRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BakeMacaroonRequest
         * @function getTypeUrl
         * @memberof lnrpc.BakeMacaroonRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BakeMacaroonRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.BakeMacaroonRequest";
        };

        return BakeMacaroonRequest;
    })();

    lnrpc.BakeMacaroonResponse = (function() {

        /**
         * Properties of a BakeMacaroonResponse.
         * @typedef {Object} lnrpc.BakeMacaroonResponse.$Properties
         * @property {string|null} [macaroon] BakeMacaroonResponse macaroon
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BakeMacaroonResponse.
         * @memberof lnrpc
         * @interface IBakeMacaroonResponse
         * @augments lnrpc.BakeMacaroonResponse.$Properties
         * @deprecated Use lnrpc.BakeMacaroonResponse.$Properties instead.
         */

        /**
         * Shape of a BakeMacaroonResponse.
         * @typedef {lnrpc.BakeMacaroonResponse.$Properties} lnrpc.BakeMacaroonResponse.$Shape
         */

        /**
         * Constructs a new BakeMacaroonResponse.
         * @memberof lnrpc
         * @classdesc Represents a BakeMacaroonResponse.
         * @constructor
         * @param {lnrpc.BakeMacaroonResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BakeMacaroonResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BakeMacaroonResponse macaroon.
         * @member {string} macaroon
         * @memberof lnrpc.BakeMacaroonResponse
         * @instance
         */
        BakeMacaroonResponse.prototype.macaroon = "";

        /**
         * Creates a new BakeMacaroonResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.BakeMacaroonResponse
         * @static
         * @param {lnrpc.BakeMacaroonResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.BakeMacaroonResponse} BakeMacaroonResponse instance
         * @type {{
         *   (properties: lnrpc.BakeMacaroonResponse.$Shape): lnrpc.BakeMacaroonResponse & lnrpc.BakeMacaroonResponse.$Shape;
         *   (properties?: lnrpc.BakeMacaroonResponse.$Properties): lnrpc.BakeMacaroonResponse;
         * }}
         */
        BakeMacaroonResponse.create = function(properties) {
            return new BakeMacaroonResponse(properties);
        };

        /**
         * Encodes the specified BakeMacaroonResponse message. Does not implicitly {@link lnrpc.BakeMacaroonResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.BakeMacaroonResponse
         * @static
         * @param {lnrpc.BakeMacaroonResponse.$Properties} message BakeMacaroonResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BakeMacaroonResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.macaroon != null && $Object.hasOwnProperty.call(message, "macaroon") && message.macaroon !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.macaroon);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BakeMacaroonResponse message, length delimited. Does not implicitly {@link lnrpc.BakeMacaroonResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.BakeMacaroonResponse
         * @static
         * @param {lnrpc.BakeMacaroonResponse.$Properties} message BakeMacaroonResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BakeMacaroonResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BakeMacaroonResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.BakeMacaroonResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.BakeMacaroonResponse & lnrpc.BakeMacaroonResponse.$Shape} BakeMacaroonResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BakeMacaroonResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.BakeMacaroonResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.macaroon = value;
                        else
                            delete message.macaroon;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BakeMacaroonResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.BakeMacaroonResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.BakeMacaroonResponse & lnrpc.BakeMacaroonResponse.$Shape} BakeMacaroonResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BakeMacaroonResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BakeMacaroonResponse message.
         * @function verify
         * @memberof lnrpc.BakeMacaroonResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BakeMacaroonResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.macaroon != null && $Object.hasOwnProperty.call(message, "macaroon"))
                if (!$util.isString(message.macaroon))
                    return "macaroon: string expected";
            return null;
        };

        /**
         * Creates a BakeMacaroonResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.BakeMacaroonResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.BakeMacaroonResponse} BakeMacaroonResponse
         */
        BakeMacaroonResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.BakeMacaroonResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.BakeMacaroonResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.BakeMacaroonResponse();
            if (object.macaroon != null)
                if (typeof object.macaroon !== "string" || object.macaroon.length)
                    message.macaroon = $String(object.macaroon);
            return message;
        };

        /**
         * Creates a plain object from a BakeMacaroonResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.BakeMacaroonResponse
         * @static
         * @param {lnrpc.BakeMacaroonResponse} message BakeMacaroonResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BakeMacaroonResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.macaroon = "";
            if (message.macaroon != null && $Object.hasOwnProperty.call(message, "macaroon"))
                object.macaroon = message.macaroon;
            return object;
        };

        /**
         * Converts this BakeMacaroonResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.BakeMacaroonResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BakeMacaroonResponse.prototype.toJSON = function() {
            return BakeMacaroonResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BakeMacaroonResponse
         * @function getTypeUrl
         * @memberof lnrpc.BakeMacaroonResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BakeMacaroonResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.BakeMacaroonResponse";
        };

        return BakeMacaroonResponse;
    })();

    lnrpc.ListMacaroonIDsRequest = (function() {

        /**
         * Properties of a ListMacaroonIDsRequest.
         * @typedef {Object} lnrpc.ListMacaroonIDsRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListMacaroonIDsRequest.
         * @memberof lnrpc
         * @interface IListMacaroonIDsRequest
         * @augments lnrpc.ListMacaroonIDsRequest.$Properties
         * @deprecated Use lnrpc.ListMacaroonIDsRequest.$Properties instead.
         */

        /**
         * Shape of a ListMacaroonIDsRequest.
         * @typedef {lnrpc.ListMacaroonIDsRequest.$Properties} lnrpc.ListMacaroonIDsRequest.$Shape
         */

        /**
         * Constructs a new ListMacaroonIDsRequest.
         * @memberof lnrpc
         * @classdesc Represents a ListMacaroonIDsRequest.
         * @constructor
         * @param {lnrpc.ListMacaroonIDsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListMacaroonIDsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ListMacaroonIDsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @static
         * @param {lnrpc.ListMacaroonIDsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListMacaroonIDsRequest} ListMacaroonIDsRequest instance
         * @type {{
         *   (properties: lnrpc.ListMacaroonIDsRequest.$Shape): lnrpc.ListMacaroonIDsRequest & lnrpc.ListMacaroonIDsRequest.$Shape;
         *   (properties?: lnrpc.ListMacaroonIDsRequest.$Properties): lnrpc.ListMacaroonIDsRequest;
         * }}
         */
        ListMacaroonIDsRequest.create = function(properties) {
            return new ListMacaroonIDsRequest(properties);
        };

        /**
         * Encodes the specified ListMacaroonIDsRequest message. Does not implicitly {@link lnrpc.ListMacaroonIDsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @static
         * @param {lnrpc.ListMacaroonIDsRequest.$Properties} message ListMacaroonIDsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListMacaroonIDsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListMacaroonIDsRequest message, length delimited. Does not implicitly {@link lnrpc.ListMacaroonIDsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @static
         * @param {lnrpc.ListMacaroonIDsRequest.$Properties} message ListMacaroonIDsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListMacaroonIDsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListMacaroonIDsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListMacaroonIDsRequest & lnrpc.ListMacaroonIDsRequest.$Shape} ListMacaroonIDsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListMacaroonIDsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListMacaroonIDsRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListMacaroonIDsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListMacaroonIDsRequest & lnrpc.ListMacaroonIDsRequest.$Shape} ListMacaroonIDsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListMacaroonIDsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListMacaroonIDsRequest message.
         * @function verify
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListMacaroonIDsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ListMacaroonIDsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListMacaroonIDsRequest} ListMacaroonIDsRequest
         */
        ListMacaroonIDsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListMacaroonIDsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListMacaroonIDsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.ListMacaroonIDsRequest();
        };

        /**
         * Creates a plain object from a ListMacaroonIDsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @static
         * @param {lnrpc.ListMacaroonIDsRequest} message ListMacaroonIDsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListMacaroonIDsRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this ListMacaroonIDsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListMacaroonIDsRequest.prototype.toJSON = function() {
            return ListMacaroonIDsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListMacaroonIDsRequest
         * @function getTypeUrl
         * @memberof lnrpc.ListMacaroonIDsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListMacaroonIDsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListMacaroonIDsRequest";
        };

        return ListMacaroonIDsRequest;
    })();

    lnrpc.ListMacaroonIDsResponse = (function() {

        /**
         * Properties of a ListMacaroonIDsResponse.
         * @typedef {Object} lnrpc.ListMacaroonIDsResponse.$Properties
         * @property {Array.<Long>|null} [root_key_ids] ListMacaroonIDsResponse root_key_ids
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListMacaroonIDsResponse.
         * @memberof lnrpc
         * @interface IListMacaroonIDsResponse
         * @augments lnrpc.ListMacaroonIDsResponse.$Properties
         * @deprecated Use lnrpc.ListMacaroonIDsResponse.$Properties instead.
         */

        /**
         * Shape of a ListMacaroonIDsResponse.
         * @typedef {lnrpc.ListMacaroonIDsResponse.$Properties} lnrpc.ListMacaroonIDsResponse.$Shape
         */

        /**
         * Constructs a new ListMacaroonIDsResponse.
         * @memberof lnrpc
         * @classdesc Represents a ListMacaroonIDsResponse.
         * @constructor
         * @param {lnrpc.ListMacaroonIDsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListMacaroonIDsResponse = function (properties) {
            this.root_key_ids = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListMacaroonIDsResponse root_key_ids.
         * @member {Array.<Long>} root_key_ids
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @instance
         */
        ListMacaroonIDsResponse.prototype.root_key_ids = $util.emptyArray;

        /**
         * Creates a new ListMacaroonIDsResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @static
         * @param {lnrpc.ListMacaroonIDsResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListMacaroonIDsResponse} ListMacaroonIDsResponse instance
         * @type {{
         *   (properties: lnrpc.ListMacaroonIDsResponse.$Shape): lnrpc.ListMacaroonIDsResponse & lnrpc.ListMacaroonIDsResponse.$Shape;
         *   (properties?: lnrpc.ListMacaroonIDsResponse.$Properties): lnrpc.ListMacaroonIDsResponse;
         * }}
         */
        ListMacaroonIDsResponse.create = function(properties) {
            return new ListMacaroonIDsResponse(properties);
        };

        /**
         * Encodes the specified ListMacaroonIDsResponse message. Does not implicitly {@link lnrpc.ListMacaroonIDsResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @static
         * @param {lnrpc.ListMacaroonIDsResponse.$Properties} message ListMacaroonIDsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListMacaroonIDsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.root_key_ids != null && message.root_key_ids.length)
                writer.uint32(/* id 1, wireType 2 =*/10).uint64s(message.root_key_ids);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListMacaroonIDsResponse message, length delimited. Does not implicitly {@link lnrpc.ListMacaroonIDsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @static
         * @param {lnrpc.ListMacaroonIDsResponse.$Properties} message ListMacaroonIDsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListMacaroonIDsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListMacaroonIDsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListMacaroonIDsResponse & lnrpc.ListMacaroonIDsResponse.$Shape} ListMacaroonIDsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListMacaroonIDsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListMacaroonIDsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.root_key_ids && message.root_key_ids.length))
                                message.root_key_ids = [];
                            reader.uint64s(message.root_key_ids);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.root_key_ids && message.root_key_ids.length))
                            message.root_key_ids = [];
                        message.root_key_ids.push(reader.uint64());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListMacaroonIDsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListMacaroonIDsResponse & lnrpc.ListMacaroonIDsResponse.$Shape} ListMacaroonIDsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListMacaroonIDsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListMacaroonIDsResponse message.
         * @function verify
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListMacaroonIDsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.root_key_ids != null && $Object.hasOwnProperty.call(message, "root_key_ids")) {
                if (!$Array.isArray(message.root_key_ids))
                    return "root_key_ids: array expected";
                for (let i = 0; i < message.root_key_ids.length; ++i)
                    if (!$util.isInteger(message.root_key_ids[i]) && !(message.root_key_ids[i] && $util.isInteger(message.root_key_ids[i].low) && $util.isInteger(message.root_key_ids[i].high)))
                        return "root_key_ids: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates a ListMacaroonIDsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListMacaroonIDsResponse} ListMacaroonIDsResponse
         */
        ListMacaroonIDsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListMacaroonIDsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListMacaroonIDsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListMacaroonIDsResponse();
            if (object.root_key_ids) {
                if (!$Array.isArray(object.root_key_ids))
                    throw $TypeError(".lnrpc.ListMacaroonIDsResponse.root_key_ids: array expected");
                message.root_key_ids = $Array(object.root_key_ids.length);
                for (let i = 0; i < object.root_key_ids.length; ++i)
                    if ($util.Long)
                        message.root_key_ids[i] = $util.Long.fromValue(object.root_key_ids[i], true);
                    else if (typeof object.root_key_ids[i] === "string")
                        message.root_key_ids[i] = $parseInt(object.root_key_ids[i], 10);
                    else if (typeof object.root_key_ids[i] === "number")
                        message.root_key_ids[i] = object.root_key_ids[i];
                    else if (typeof object.root_key_ids[i] === "object")
                        message.root_key_ids[i] = new $util.LongBits(object.root_key_ids[i].low >>> 0, object.root_key_ids[i].high >>> 0).toNumber(true);
            }
            return message;
        };

        /**
         * Creates a plain object from a ListMacaroonIDsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @static
         * @param {lnrpc.ListMacaroonIDsResponse} message ListMacaroonIDsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListMacaroonIDsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.root_key_ids = [];
            if (message.root_key_ids && message.root_key_ids.length) {
                object.root_key_ids = $Array(message.root_key_ids.length);
                for (let j = 0; j < message.root_key_ids.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.root_key_ids[j] = typeof message.root_key_ids[j] === "number" ? $BigInt(message.root_key_ids[j]) : $util.Long.fromBits(message.root_key_ids[j].low >>> 0, message.root_key_ids[j].high >>> 0, true).toBigInt();
                    else if (typeof message.root_key_ids[j] === "number")
                        object.root_key_ids[j] = options.longs === $String ? $String(message.root_key_ids[j]) : message.root_key_ids[j];
                    else
                        object.root_key_ids[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.root_key_ids[j]) : options.longs === $Number ? new $util.LongBits(message.root_key_ids[j].low >>> 0, message.root_key_ids[j].high >>> 0).toNumber(true) : message.root_key_ids[j];
            }
            return object;
        };

        /**
         * Converts this ListMacaroonIDsResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListMacaroonIDsResponse.prototype.toJSON = function() {
            return ListMacaroonIDsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListMacaroonIDsResponse
         * @function getTypeUrl
         * @memberof lnrpc.ListMacaroonIDsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListMacaroonIDsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListMacaroonIDsResponse";
        };

        return ListMacaroonIDsResponse;
    })();

    lnrpc.DeleteMacaroonIDRequest = (function() {

        /**
         * Properties of a DeleteMacaroonIDRequest.
         * @typedef {Object} lnrpc.DeleteMacaroonIDRequest.$Properties
         * @property {Long|null} [root_key_id] DeleteMacaroonIDRequest root_key_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeleteMacaroonIDRequest.
         * @memberof lnrpc
         * @interface IDeleteMacaroonIDRequest
         * @augments lnrpc.DeleteMacaroonIDRequest.$Properties
         * @deprecated Use lnrpc.DeleteMacaroonIDRequest.$Properties instead.
         */

        /**
         * Shape of a DeleteMacaroonIDRequest.
         * @typedef {lnrpc.DeleteMacaroonIDRequest.$Properties} lnrpc.DeleteMacaroonIDRequest.$Shape
         */

        /**
         * Constructs a new DeleteMacaroonIDRequest.
         * @memberof lnrpc
         * @classdesc Represents a DeleteMacaroonIDRequest.
         * @constructor
         * @param {lnrpc.DeleteMacaroonIDRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeleteMacaroonIDRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeleteMacaroonIDRequest root_key_id.
         * @member {Long} root_key_id
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @instance
         */
        DeleteMacaroonIDRequest.prototype.root_key_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new DeleteMacaroonIDRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @static
         * @param {lnrpc.DeleteMacaroonIDRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DeleteMacaroonIDRequest} DeleteMacaroonIDRequest instance
         * @type {{
         *   (properties: lnrpc.DeleteMacaroonIDRequest.$Shape): lnrpc.DeleteMacaroonIDRequest & lnrpc.DeleteMacaroonIDRequest.$Shape;
         *   (properties?: lnrpc.DeleteMacaroonIDRequest.$Properties): lnrpc.DeleteMacaroonIDRequest;
         * }}
         */
        DeleteMacaroonIDRequest.create = function(properties) {
            return new DeleteMacaroonIDRequest(properties);
        };

        /**
         * Encodes the specified DeleteMacaroonIDRequest message. Does not implicitly {@link lnrpc.DeleteMacaroonIDRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @static
         * @param {lnrpc.DeleteMacaroonIDRequest.$Properties} message DeleteMacaroonIDRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteMacaroonIDRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.root_key_id != null && $Object.hasOwnProperty.call(message, "root_key_id") && (typeof message.root_key_id === "object" ? message.root_key_id.low || message.root_key_id.high : message.root_key_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.root_key_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeleteMacaroonIDRequest message, length delimited. Does not implicitly {@link lnrpc.DeleteMacaroonIDRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @static
         * @param {lnrpc.DeleteMacaroonIDRequest.$Properties} message DeleteMacaroonIDRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteMacaroonIDRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeleteMacaroonIDRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DeleteMacaroonIDRequest & lnrpc.DeleteMacaroonIDRequest.$Shape} DeleteMacaroonIDRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteMacaroonIDRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DeleteMacaroonIDRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.root_key_id = value;
                        else
                            delete message.root_key_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeleteMacaroonIDRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DeleteMacaroonIDRequest & lnrpc.DeleteMacaroonIDRequest.$Shape} DeleteMacaroonIDRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteMacaroonIDRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeleteMacaroonIDRequest message.
         * @function verify
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeleteMacaroonIDRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.root_key_id != null && $Object.hasOwnProperty.call(message, "root_key_id"))
                if (!$util.isInteger(message.root_key_id) && !(message.root_key_id && $util.isInteger(message.root_key_id.low) && $util.isInteger(message.root_key_id.high)))
                    return "root_key_id: integer|Long expected";
            return null;
        };

        /**
         * Creates a DeleteMacaroonIDRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DeleteMacaroonIDRequest} DeleteMacaroonIDRequest
         */
        DeleteMacaroonIDRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DeleteMacaroonIDRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DeleteMacaroonIDRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DeleteMacaroonIDRequest();
            if (object.root_key_id != null)
                if (typeof object.root_key_id === "object" ? object.root_key_id.low || object.root_key_id.high : $Number(object.root_key_id) !== 0)
                    if ($util.Long)
                        message.root_key_id = $util.Long.fromValue(object.root_key_id, true);
                    else if (typeof object.root_key_id === "string")
                        message.root_key_id = $parseInt(object.root_key_id, 10);
                    else if (typeof object.root_key_id === "number")
                        message.root_key_id = object.root_key_id;
                    else if (typeof object.root_key_id === "object")
                        message.root_key_id = new $util.LongBits(object.root_key_id.low >>> 0, object.root_key_id.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a DeleteMacaroonIDRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @static
         * @param {lnrpc.DeleteMacaroonIDRequest} message DeleteMacaroonIDRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeleteMacaroonIDRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.root_key_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.root_key_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.root_key_id != null && $Object.hasOwnProperty.call(message, "root_key_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.root_key_id = typeof message.root_key_id === "number" ? $BigInt(message.root_key_id) : $util.Long.fromBits(message.root_key_id.low >>> 0, message.root_key_id.high >>> 0, true).toBigInt();
                else if (typeof message.root_key_id === "number")
                    object.root_key_id = options.longs === $String ? $String(message.root_key_id) : message.root_key_id;
                else
                    object.root_key_id = options.longs === $String ? $util.Long.prototype.toString.call(message.root_key_id) : options.longs === $Number ? new $util.LongBits(message.root_key_id.low >>> 0, message.root_key_id.high >>> 0).toNumber(true) : message.root_key_id;
            return object;
        };

        /**
         * Converts this DeleteMacaroonIDRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeleteMacaroonIDRequest.prototype.toJSON = function() {
            return DeleteMacaroonIDRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeleteMacaroonIDRequest
         * @function getTypeUrl
         * @memberof lnrpc.DeleteMacaroonIDRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeleteMacaroonIDRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DeleteMacaroonIDRequest";
        };

        return DeleteMacaroonIDRequest;
    })();

    lnrpc.DeleteMacaroonIDResponse = (function() {

        /**
         * Properties of a DeleteMacaroonIDResponse.
         * @typedef {Object} lnrpc.DeleteMacaroonIDResponse.$Properties
         * @property {boolean|null} [deleted] DeleteMacaroonIDResponse deleted
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeleteMacaroonIDResponse.
         * @memberof lnrpc
         * @interface IDeleteMacaroonIDResponse
         * @augments lnrpc.DeleteMacaroonIDResponse.$Properties
         * @deprecated Use lnrpc.DeleteMacaroonIDResponse.$Properties instead.
         */

        /**
         * Shape of a DeleteMacaroonIDResponse.
         * @typedef {lnrpc.DeleteMacaroonIDResponse.$Properties} lnrpc.DeleteMacaroonIDResponse.$Shape
         */

        /**
         * Constructs a new DeleteMacaroonIDResponse.
         * @memberof lnrpc
         * @classdesc Represents a DeleteMacaroonIDResponse.
         * @constructor
         * @param {lnrpc.DeleteMacaroonIDResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeleteMacaroonIDResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeleteMacaroonIDResponse deleted.
         * @member {boolean} deleted
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @instance
         */
        DeleteMacaroonIDResponse.prototype.deleted = false;

        /**
         * Creates a new DeleteMacaroonIDResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @static
         * @param {lnrpc.DeleteMacaroonIDResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.DeleteMacaroonIDResponse} DeleteMacaroonIDResponse instance
         * @type {{
         *   (properties: lnrpc.DeleteMacaroonIDResponse.$Shape): lnrpc.DeleteMacaroonIDResponse & lnrpc.DeleteMacaroonIDResponse.$Shape;
         *   (properties?: lnrpc.DeleteMacaroonIDResponse.$Properties): lnrpc.DeleteMacaroonIDResponse;
         * }}
         */
        DeleteMacaroonIDResponse.create = function(properties) {
            return new DeleteMacaroonIDResponse(properties);
        };

        /**
         * Encodes the specified DeleteMacaroonIDResponse message. Does not implicitly {@link lnrpc.DeleteMacaroonIDResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @static
         * @param {lnrpc.DeleteMacaroonIDResponse.$Properties} message DeleteMacaroonIDResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteMacaroonIDResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.deleted);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeleteMacaroonIDResponse message, length delimited. Does not implicitly {@link lnrpc.DeleteMacaroonIDResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @static
         * @param {lnrpc.DeleteMacaroonIDResponse.$Properties} message DeleteMacaroonIDResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteMacaroonIDResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeleteMacaroonIDResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.DeleteMacaroonIDResponse & lnrpc.DeleteMacaroonIDResponse.$Shape} DeleteMacaroonIDResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteMacaroonIDResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.DeleteMacaroonIDResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.deleted = value;
                        else
                            delete message.deleted;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeleteMacaroonIDResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.DeleteMacaroonIDResponse & lnrpc.DeleteMacaroonIDResponse.$Shape} DeleteMacaroonIDResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteMacaroonIDResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeleteMacaroonIDResponse message.
         * @function verify
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeleteMacaroonIDResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                if (typeof message.deleted !== "boolean")
                    return "deleted: boolean expected";
            return null;
        };

        /**
         * Creates a DeleteMacaroonIDResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.DeleteMacaroonIDResponse} DeleteMacaroonIDResponse
         */
        DeleteMacaroonIDResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.DeleteMacaroonIDResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.DeleteMacaroonIDResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.DeleteMacaroonIDResponse();
            if (object.deleted != null)
                if (object.deleted)
                    message.deleted = $Boolean(object.deleted);
            return message;
        };

        /**
         * Creates a plain object from a DeleteMacaroonIDResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @static
         * @param {lnrpc.DeleteMacaroonIDResponse} message DeleteMacaroonIDResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeleteMacaroonIDResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.deleted = false;
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                object.deleted = message.deleted;
            return object;
        };

        /**
         * Converts this DeleteMacaroonIDResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeleteMacaroonIDResponse.prototype.toJSON = function() {
            return DeleteMacaroonIDResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeleteMacaroonIDResponse
         * @function getTypeUrl
         * @memberof lnrpc.DeleteMacaroonIDResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeleteMacaroonIDResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.DeleteMacaroonIDResponse";
        };

        return DeleteMacaroonIDResponse;
    })();

    lnrpc.MacaroonPermissionList = (function() {

        /**
         * Properties of a MacaroonPermissionList.
         * @typedef {Object} lnrpc.MacaroonPermissionList.$Properties
         * @property {Array.<lnrpc.MacaroonPermission.$Properties>|null} [permissions] MacaroonPermissionList permissions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MacaroonPermissionList.
         * @memberof lnrpc
         * @interface IMacaroonPermissionList
         * @augments lnrpc.MacaroonPermissionList.$Properties
         * @deprecated Use lnrpc.MacaroonPermissionList.$Properties instead.
         */

        /**
         * Shape of a MacaroonPermissionList.
         * @typedef {lnrpc.MacaroonPermissionList.$Properties} lnrpc.MacaroonPermissionList.$Shape
         */

        /**
         * Constructs a new MacaroonPermissionList.
         * @memberof lnrpc
         * @classdesc Represents a MacaroonPermissionList.
         * @constructor
         * @param {lnrpc.MacaroonPermissionList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MacaroonPermissionList = function (properties) {
            this.permissions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MacaroonPermissionList permissions.
         * @member {Array.<lnrpc.MacaroonPermission.$Properties>} permissions
         * @memberof lnrpc.MacaroonPermissionList
         * @instance
         */
        MacaroonPermissionList.prototype.permissions = $util.emptyArray;

        /**
         * Creates a new MacaroonPermissionList instance using the specified properties.
         * @function create
         * @memberof lnrpc.MacaroonPermissionList
         * @static
         * @param {lnrpc.MacaroonPermissionList.$Properties=} [properties] Properties to set
         * @returns {lnrpc.MacaroonPermissionList} MacaroonPermissionList instance
         * @type {{
         *   (properties: lnrpc.MacaroonPermissionList.$Shape): lnrpc.MacaroonPermissionList & lnrpc.MacaroonPermissionList.$Shape;
         *   (properties?: lnrpc.MacaroonPermissionList.$Properties): lnrpc.MacaroonPermissionList;
         * }}
         */
        MacaroonPermissionList.create = function(properties) {
            return new MacaroonPermissionList(properties);
        };

        /**
         * Encodes the specified MacaroonPermissionList message. Does not implicitly {@link lnrpc.MacaroonPermissionList.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.MacaroonPermissionList
         * @static
         * @param {lnrpc.MacaroonPermissionList.$Properties} message MacaroonPermissionList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MacaroonPermissionList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.permissions != null && message.permissions.length)
                for (let i = 0; i < message.permissions.length; ++i)
                    $root.lnrpc.MacaroonPermission.encode(message.permissions[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MacaroonPermissionList message, length delimited. Does not implicitly {@link lnrpc.MacaroonPermissionList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.MacaroonPermissionList
         * @static
         * @param {lnrpc.MacaroonPermissionList.$Properties} message MacaroonPermissionList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MacaroonPermissionList.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MacaroonPermissionList message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.MacaroonPermissionList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.MacaroonPermissionList & lnrpc.MacaroonPermissionList.$Shape} MacaroonPermissionList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MacaroonPermissionList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.MacaroonPermissionList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.permissions && message.permissions.length))
                            message.permissions = [];
                        message.permissions.push($root.lnrpc.MacaroonPermission.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MacaroonPermissionList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.MacaroonPermissionList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.MacaroonPermissionList & lnrpc.MacaroonPermissionList.$Shape} MacaroonPermissionList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MacaroonPermissionList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MacaroonPermissionList message.
         * @function verify
         * @memberof lnrpc.MacaroonPermissionList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MacaroonPermissionList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.permissions != null && $Object.hasOwnProperty.call(message, "permissions")) {
                if (!$Array.isArray(message.permissions))
                    return "permissions: array expected";
                for (let i = 0; i < message.permissions.length; ++i) {
                    let error = $root.lnrpc.MacaroonPermission.verify(message.permissions[i], _depth + 1);
                    if (error)
                        return "permissions." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MacaroonPermissionList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.MacaroonPermissionList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.MacaroonPermissionList} MacaroonPermissionList
         */
        MacaroonPermissionList.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.MacaroonPermissionList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.MacaroonPermissionList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.MacaroonPermissionList();
            if (object.permissions) {
                if (!$Array.isArray(object.permissions))
                    throw $TypeError(".lnrpc.MacaroonPermissionList.permissions: array expected");
                message.permissions = $Array(object.permissions.length);
                for (let i = 0; i < object.permissions.length; ++i) {
                    if (!$util.isObject(object.permissions[i]))
                        throw $TypeError(".lnrpc.MacaroonPermissionList.permissions: object expected");
                    message.permissions[i] = $root.lnrpc.MacaroonPermission.fromObject(object.permissions[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MacaroonPermissionList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.MacaroonPermissionList
         * @static
         * @param {lnrpc.MacaroonPermissionList} message MacaroonPermissionList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MacaroonPermissionList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.permissions = [];
            if (message.permissions && message.permissions.length) {
                object.permissions = $Array(message.permissions.length);
                for (let j = 0; j < message.permissions.length; ++j)
                    object.permissions[j] = $root.lnrpc.MacaroonPermission.toObject(message.permissions[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this MacaroonPermissionList to JSON.
         * @function toJSON
         * @memberof lnrpc.MacaroonPermissionList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MacaroonPermissionList.prototype.toJSON = function() {
            return MacaroonPermissionList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MacaroonPermissionList
         * @function getTypeUrl
         * @memberof lnrpc.MacaroonPermissionList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MacaroonPermissionList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.MacaroonPermissionList";
        };

        return MacaroonPermissionList;
    })();

    lnrpc.ListPermissionsRequest = (function() {

        /**
         * Properties of a ListPermissionsRequest.
         * @typedef {Object} lnrpc.ListPermissionsRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListPermissionsRequest.
         * @memberof lnrpc
         * @interface IListPermissionsRequest
         * @augments lnrpc.ListPermissionsRequest.$Properties
         * @deprecated Use lnrpc.ListPermissionsRequest.$Properties instead.
         */

        /**
         * Shape of a ListPermissionsRequest.
         * @typedef {lnrpc.ListPermissionsRequest.$Properties} lnrpc.ListPermissionsRequest.$Shape
         */

        /**
         * Constructs a new ListPermissionsRequest.
         * @memberof lnrpc
         * @classdesc Represents a ListPermissionsRequest.
         * @constructor
         * @param {lnrpc.ListPermissionsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListPermissionsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ListPermissionsRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListPermissionsRequest
         * @static
         * @param {lnrpc.ListPermissionsRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListPermissionsRequest} ListPermissionsRequest instance
         * @type {{
         *   (properties: lnrpc.ListPermissionsRequest.$Shape): lnrpc.ListPermissionsRequest & lnrpc.ListPermissionsRequest.$Shape;
         *   (properties?: lnrpc.ListPermissionsRequest.$Properties): lnrpc.ListPermissionsRequest;
         * }}
         */
        ListPermissionsRequest.create = function(properties) {
            return new ListPermissionsRequest(properties);
        };

        /**
         * Encodes the specified ListPermissionsRequest message. Does not implicitly {@link lnrpc.ListPermissionsRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListPermissionsRequest
         * @static
         * @param {lnrpc.ListPermissionsRequest.$Properties} message ListPermissionsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPermissionsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListPermissionsRequest message, length delimited. Does not implicitly {@link lnrpc.ListPermissionsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListPermissionsRequest
         * @static
         * @param {lnrpc.ListPermissionsRequest.$Properties} message ListPermissionsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPermissionsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListPermissionsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListPermissionsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListPermissionsRequest & lnrpc.ListPermissionsRequest.$Shape} ListPermissionsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPermissionsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListPermissionsRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListPermissionsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListPermissionsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListPermissionsRequest & lnrpc.ListPermissionsRequest.$Shape} ListPermissionsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPermissionsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListPermissionsRequest message.
         * @function verify
         * @memberof lnrpc.ListPermissionsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListPermissionsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ListPermissionsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListPermissionsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListPermissionsRequest} ListPermissionsRequest
         */
        ListPermissionsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListPermissionsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListPermissionsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.ListPermissionsRequest();
        };

        /**
         * Creates a plain object from a ListPermissionsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListPermissionsRequest
         * @static
         * @param {lnrpc.ListPermissionsRequest} message ListPermissionsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListPermissionsRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this ListPermissionsRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ListPermissionsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListPermissionsRequest.prototype.toJSON = function() {
            return ListPermissionsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListPermissionsRequest
         * @function getTypeUrl
         * @memberof lnrpc.ListPermissionsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListPermissionsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListPermissionsRequest";
        };

        return ListPermissionsRequest;
    })();

    lnrpc.ListPermissionsResponse = (function() {

        /**
         * Properties of a ListPermissionsResponse.
         * @typedef {Object} lnrpc.ListPermissionsResponse.$Properties
         * @property {Object.<string,lnrpc.MacaroonPermissionList.$Properties>|null} [method_permissions] ListPermissionsResponse method_permissions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListPermissionsResponse.
         * @memberof lnrpc
         * @interface IListPermissionsResponse
         * @augments lnrpc.ListPermissionsResponse.$Properties
         * @deprecated Use lnrpc.ListPermissionsResponse.$Properties instead.
         */

        /**
         * Shape of a ListPermissionsResponse.
         * @typedef {lnrpc.ListPermissionsResponse.$Properties} lnrpc.ListPermissionsResponse.$Shape
         */

        /**
         * Constructs a new ListPermissionsResponse.
         * @memberof lnrpc
         * @classdesc Represents a ListPermissionsResponse.
         * @constructor
         * @param {lnrpc.ListPermissionsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListPermissionsResponse = function (properties) {
            this.method_permissions = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListPermissionsResponse method_permissions.
         * @member {Object.<string,lnrpc.MacaroonPermissionList.$Properties>} method_permissions
         * @memberof lnrpc.ListPermissionsResponse
         * @instance
         */
        ListPermissionsResponse.prototype.method_permissions = $util.emptyObject;

        /**
         * Creates a new ListPermissionsResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ListPermissionsResponse
         * @static
         * @param {lnrpc.ListPermissionsResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ListPermissionsResponse} ListPermissionsResponse instance
         * @type {{
         *   (properties: lnrpc.ListPermissionsResponse.$Shape): lnrpc.ListPermissionsResponse & lnrpc.ListPermissionsResponse.$Shape;
         *   (properties?: lnrpc.ListPermissionsResponse.$Properties): lnrpc.ListPermissionsResponse;
         * }}
         */
        ListPermissionsResponse.create = function(properties) {
            return new ListPermissionsResponse(properties);
        };

        /**
         * Encodes the specified ListPermissionsResponse message. Does not implicitly {@link lnrpc.ListPermissionsResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ListPermissionsResponse
         * @static
         * @param {lnrpc.ListPermissionsResponse.$Properties} message ListPermissionsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPermissionsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.method_permissions != null && $Object.hasOwnProperty.call(message, "method_permissions"))
                for (let keys = $Object.keys(message.method_permissions), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.lnrpc.MacaroonPermissionList.encode(message.method_permissions[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListPermissionsResponse message, length delimited. Does not implicitly {@link lnrpc.ListPermissionsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ListPermissionsResponse
         * @static
         * @param {lnrpc.ListPermissionsResponse.$Properties} message ListPermissionsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListPermissionsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListPermissionsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ListPermissionsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ListPermissionsResponse & lnrpc.ListPermissionsResponse.$Shape} ListPermissionsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPermissionsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ListPermissionsResponse(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (message.method_permissions === $util.emptyObject)
                            message.method_permissions = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.MacaroonPermissionList.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.method_permissions, key);
                        message.method_permissions[key] = value || new $root.lnrpc.MacaroonPermissionList();
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListPermissionsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ListPermissionsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ListPermissionsResponse & lnrpc.ListPermissionsResponse.$Shape} ListPermissionsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListPermissionsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListPermissionsResponse message.
         * @function verify
         * @memberof lnrpc.ListPermissionsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListPermissionsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.method_permissions != null && $Object.hasOwnProperty.call(message, "method_permissions")) {
                if (!$util.isObject(message.method_permissions))
                    return "method_permissions: object expected";
                let key = $Object.keys(message.method_permissions);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.lnrpc.MacaroonPermissionList.verify(message.method_permissions[key[i]], _depth + 1);
                    if (error)
                        return "method_permissions." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListPermissionsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ListPermissionsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ListPermissionsResponse} ListPermissionsResponse
         */
        ListPermissionsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ListPermissionsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ListPermissionsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ListPermissionsResponse();
            if (object.method_permissions) {
                if (!$util.isObject(object.method_permissions))
                    throw $TypeError(".lnrpc.ListPermissionsResponse.method_permissions: object expected");
                message.method_permissions = {};
                for (let keys = $Object.keys(object.method_permissions), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.method_permissions, keys[i]);
                    if (!$util.isObject(object.method_permissions[keys[i]]))
                        throw $TypeError(".lnrpc.ListPermissionsResponse.method_permissions: object expected");
                    message.method_permissions[keys[i]] = $root.lnrpc.MacaroonPermissionList.fromObject(object.method_permissions[keys[i]], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListPermissionsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ListPermissionsResponse
         * @static
         * @param {lnrpc.ListPermissionsResponse} message ListPermissionsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListPermissionsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.method_permissions = {};
            let keys2;
            if (message.method_permissions && (keys2 = $Object.keys(message.method_permissions)).length) {
                object.method_permissions = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.method_permissions, keys2[j]);
                    object.method_permissions[keys2[j]] = $root.lnrpc.MacaroonPermissionList.toObject(message.method_permissions[keys2[j]], options, _depth + 1);
                }
            }
            return object;
        };

        /**
         * Converts this ListPermissionsResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ListPermissionsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListPermissionsResponse.prototype.toJSON = function() {
            return ListPermissionsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListPermissionsResponse
         * @function getTypeUrl
         * @memberof lnrpc.ListPermissionsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListPermissionsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ListPermissionsResponse";
        };

        return ListPermissionsResponse;
    })();

    lnrpc.Failure = (function() {

        /**
         * Properties of a Failure.
         * @typedef {Object} lnrpc.Failure.$Properties
         * @property {lnrpc.Failure.FailureCode|null} [code] Failure code
         * @property {lnrpc.ChannelUpdate.$Properties|null} [channel_update] Failure channel_update
         * @property {Long|null} [htlc_msat] Failure htlc_msat
         * @property {Uint8Array|null} [onion_sha_256] Failure onion_sha_256
         * @property {number|null} [cltv_expiry] Failure cltv_expiry
         * @property {number|null} [flags] Failure flags
         * @property {number|null} [failure_source_index] Failure failure_source_index
         * @property {number|null} [height] Failure height
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Failure.
         * @memberof lnrpc
         * @interface IFailure
         * @augments lnrpc.Failure.$Properties
         * @deprecated Use lnrpc.Failure.$Properties instead.
         */

        /**
         * Shape of a Failure.
         * @typedef {lnrpc.Failure.$Properties} lnrpc.Failure.$Shape
         */

        /**
         * Constructs a new Failure.
         * @memberof lnrpc
         * @classdesc Represents a Failure.
         * @constructor
         * @param {lnrpc.Failure.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Failure = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Failure code.
         * @member {lnrpc.Failure.FailureCode} code
         * @memberof lnrpc.Failure
         * @instance
         */
        Failure.prototype.code = 0;

        /**
         * Failure channel_update.
         * @member {lnrpc.ChannelUpdate.$Properties|null|undefined} channel_update
         * @memberof lnrpc.Failure
         * @instance
         */
        Failure.prototype.channel_update = null;

        /**
         * Failure htlc_msat.
         * @member {Long} htlc_msat
         * @memberof lnrpc.Failure
         * @instance
         */
        Failure.prototype.htlc_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Failure onion_sha_256.
         * @member {Uint8Array} onion_sha_256
         * @memberof lnrpc.Failure
         * @instance
         */
        Failure.prototype.onion_sha_256 = $util.newBuffer([]);

        /**
         * Failure cltv_expiry.
         * @member {number} cltv_expiry
         * @memberof lnrpc.Failure
         * @instance
         */
        Failure.prototype.cltv_expiry = 0;

        /**
         * Failure flags.
         * @member {number} flags
         * @memberof lnrpc.Failure
         * @instance
         */
        Failure.prototype.flags = 0;

        /**
         * Failure failure_source_index.
         * @member {number} failure_source_index
         * @memberof lnrpc.Failure
         * @instance
         */
        Failure.prototype.failure_source_index = 0;

        /**
         * Failure height.
         * @member {number} height
         * @memberof lnrpc.Failure
         * @instance
         */
        Failure.prototype.height = 0;

        /**
         * Creates a new Failure instance using the specified properties.
         * @function create
         * @memberof lnrpc.Failure
         * @static
         * @param {lnrpc.Failure.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Failure} Failure instance
         * @type {{
         *   (properties: lnrpc.Failure.$Shape): lnrpc.Failure & lnrpc.Failure.$Shape;
         *   (properties?: lnrpc.Failure.$Properties): lnrpc.Failure;
         * }}
         */
        Failure.create = function(properties) {
            return new Failure(properties);
        };

        /**
         * Encodes the specified Failure message. Does not implicitly {@link lnrpc.Failure.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Failure
         * @static
         * @param {lnrpc.Failure.$Properties} message Failure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Failure.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.code);
            if (message.channel_update != null && $Object.hasOwnProperty.call(message, "channel_update"))
                $root.lnrpc.ChannelUpdate.encode(message.channel_update, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.htlc_msat != null && $Object.hasOwnProperty.call(message, "htlc_msat") && (typeof message.htlc_msat === "object" ? message.htlc_msat.low || message.htlc_msat.high : message.htlc_msat !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.htlc_msat);
            if (message.onion_sha_256 != null && $Object.hasOwnProperty.call(message, "onion_sha_256") && message.onion_sha_256.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.onion_sha_256);
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry") && message.cltv_expiry !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.cltv_expiry);
            if (message.flags != null && $Object.hasOwnProperty.call(message, "flags") && message.flags !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.flags);
            if (message.failure_source_index != null && $Object.hasOwnProperty.call(message, "failure_source_index") && message.failure_source_index !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.failure_source_index);
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Failure message, length delimited. Does not implicitly {@link lnrpc.Failure.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Failure
         * @static
         * @param {lnrpc.Failure.$Properties} message Failure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Failure.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Failure message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Failure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Failure & lnrpc.Failure.$Shape} Failure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Failure.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Failure(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.channel_update = $root.lnrpc.ChannelUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel_update);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_msat = value;
                        else
                            delete message.htlc_msat;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.onion_sha_256 = value;
                        else
                            delete message.onion_sha_256;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.cltv_expiry = value;
                        else
                            delete message.cltv_expiry;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.flags = value;
                        else
                            delete message.flags;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.failure_source_index = value;
                        else
                            delete message.failure_source_index;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Failure message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Failure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Failure & lnrpc.Failure.$Shape} Failure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Failure.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Failure message.
         * @function verify
         * @memberof lnrpc.Failure
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Failure.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                if (typeof message.code !== "number" || (message.code | 0) !== message.code)
                    return "code: enum value expected";
            if (message.channel_update != null && $Object.hasOwnProperty.call(message, "channel_update")) {
                let error = $root.lnrpc.ChannelUpdate.verify(message.channel_update, _depth + 1);
                if (error)
                    return "channel_update." + error;
            }
            if (message.htlc_msat != null && $Object.hasOwnProperty.call(message, "htlc_msat"))
                if (!$util.isInteger(message.htlc_msat) && !(message.htlc_msat && $util.isInteger(message.htlc_msat.low) && $util.isInteger(message.htlc_msat.high)))
                    return "htlc_msat: integer|Long expected";
            if (message.onion_sha_256 != null && $Object.hasOwnProperty.call(message, "onion_sha_256"))
                if (!(message.onion_sha_256 && typeof message.onion_sha_256.length === "number" || $util.isString(message.onion_sha_256)))
                    return "onion_sha_256: buffer expected";
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry"))
                if (!$util.isInteger(message.cltv_expiry))
                    return "cltv_expiry: integer expected";
            if (message.flags != null && $Object.hasOwnProperty.call(message, "flags"))
                if (!$util.isInteger(message.flags))
                    return "flags: integer expected";
            if (message.failure_source_index != null && $Object.hasOwnProperty.call(message, "failure_source_index"))
                if (!$util.isInteger(message.failure_source_index))
                    return "failure_source_index: integer expected";
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            return null;
        };

        /**
         * Creates a Failure message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Failure
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Failure} Failure
         */
        Failure.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Failure)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Failure: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Failure();
            if (object.code !== 0 && (typeof object.code !== "string" || $root.lnrpc.Failure.FailureCode[object.code] !== 0))
                switch (object.code) {
                case "RESERVED":
                case 0:
                    message.code = 0;
                    break;
                case "INCORRECT_OR_UNKNOWN_PAYMENT_DETAILS":
                case 1:
                    message.code = 1;
                    break;
                case "INCORRECT_PAYMENT_AMOUNT":
                case 2:
                    message.code = 2;
                    break;
                case "FINAL_INCORRECT_CLTV_EXPIRY":
                case 3:
                    message.code = 3;
                    break;
                case "FINAL_INCORRECT_HTLC_AMOUNT":
                case 4:
                    message.code = 4;
                    break;
                case "FINAL_EXPIRY_TOO_SOON":
                case 5:
                    message.code = 5;
                    break;
                case "INVALID_REALM":
                case 6:
                    message.code = 6;
                    break;
                case "EXPIRY_TOO_SOON":
                case 7:
                    message.code = 7;
                    break;
                case "INVALID_ONION_VERSION":
                case 8:
                    message.code = 8;
                    break;
                case "INVALID_ONION_HMAC":
                case 9:
                    message.code = 9;
                    break;
                case "INVALID_ONION_KEY":
                case 10:
                    message.code = 10;
                    break;
                case "AMOUNT_BELOW_MINIMUM":
                case 11:
                    message.code = 11;
                    break;
                case "FEE_INSUFFICIENT":
                case 12:
                    message.code = 12;
                    break;
                case "INCORRECT_CLTV_EXPIRY":
                case 13:
                    message.code = 13;
                    break;
                case "CHANNEL_DISABLED":
                case 14:
                    message.code = 14;
                    break;
                case "TEMPORARY_CHANNEL_FAILURE":
                case 15:
                    message.code = 15;
                    break;
                case "REQUIRED_NODE_FEATURE_MISSING":
                case 16:
                    message.code = 16;
                    break;
                case "REQUIRED_CHANNEL_FEATURE_MISSING":
                case 17:
                    message.code = 17;
                    break;
                case "UNKNOWN_NEXT_PEER":
                case 18:
                    message.code = 18;
                    break;
                case "TEMPORARY_NODE_FAILURE":
                case 19:
                    message.code = 19;
                    break;
                case "PERMANENT_NODE_FAILURE":
                case 20:
                    message.code = 20;
                    break;
                case "PERMANENT_CHANNEL_FAILURE":
                case 21:
                    message.code = 21;
                    break;
                case "EXPIRY_TOO_FAR":
                case 22:
                    message.code = 22;
                    break;
                case "MPP_TIMEOUT":
                case 23:
                    message.code = 23;
                    break;
                case "INVALID_ONION_PAYLOAD":
                case 24:
                    message.code = 24;
                    break;
                case "INVALID_ONION_BLINDING":
                case 25:
                    message.code = 25;
                    break;
                case "INTERNAL_FAILURE":
                case 997:
                    message.code = 997;
                    break;
                case "UNKNOWN_FAILURE":
                case 998:
                    message.code = 998;
                    break;
                case "UNREADABLE_FAILURE":
                case 999:
                    message.code = 999;
                    break;
                default:
                    if (typeof object.code === "number" && (object.code | 0) === object.code)
                        message.code = object.code;
                }
            if (object.channel_update != null) {
                if (!$util.isObject(object.channel_update))
                    throw $TypeError(".lnrpc.Failure.channel_update: object expected");
                message.channel_update = $root.lnrpc.ChannelUpdate.fromObject(object.channel_update, _depth + 1);
            }
            if (object.htlc_msat != null)
                if (typeof object.htlc_msat === "object" ? object.htlc_msat.low || object.htlc_msat.high : $Number(object.htlc_msat) !== 0)
                    if ($util.Long)
                        message.htlc_msat = $util.Long.fromValue(object.htlc_msat, true);
                    else if (typeof object.htlc_msat === "string")
                        message.htlc_msat = $parseInt(object.htlc_msat, 10);
                    else if (typeof object.htlc_msat === "number")
                        message.htlc_msat = object.htlc_msat;
                    else if (typeof object.htlc_msat === "object")
                        message.htlc_msat = new $util.LongBits(object.htlc_msat.low >>> 0, object.htlc_msat.high >>> 0).toNumber(true);
            if (object.onion_sha_256 != null)
                if (object.onion_sha_256.length)
                    if (typeof object.onion_sha_256 === "string")
                        $util.base64.decode(object.onion_sha_256, message.onion_sha_256 = $util.newBuffer($util.base64.length(object.onion_sha_256)), 0);
                    else if (object.onion_sha_256.length >= 0)
                        message.onion_sha_256 = object.onion_sha_256;
            if (object.cltv_expiry != null)
                if ($Number(object.cltv_expiry) !== 0)
                    message.cltv_expiry = object.cltv_expiry >>> 0;
            if (object.flags != null)
                if ($Number(object.flags) !== 0)
                    message.flags = object.flags >>> 0;
            if (object.failure_source_index != null)
                if ($Number(object.failure_source_index) !== 0)
                    message.failure_source_index = object.failure_source_index >>> 0;
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Failure message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Failure
         * @static
         * @param {lnrpc.Failure} message Failure
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Failure.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.code = options.enums === $String ? "RESERVED" : 0;
                object.channel_update = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.onion_sha_256 = "";
                else {
                    object.onion_sha_256 = [];
                    if (options.bytes !== $Array)
                        object.onion_sha_256 = $util.newBuffer(object.onion_sha_256);
                }
                object.cltv_expiry = 0;
                object.flags = 0;
                object.failure_source_index = 0;
                object.height = 0;
            }
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                object.code = options.enums === $String ? $root.lnrpc.Failure.FailureCode[message.code] === $undefined ? message.code : $root.lnrpc.Failure.FailureCode[message.code] : message.code;
            if (message.channel_update != null && $Object.hasOwnProperty.call(message, "channel_update"))
                object.channel_update = $root.lnrpc.ChannelUpdate.toObject(message.channel_update, options, _depth + 1);
            if (message.htlc_msat != null && $Object.hasOwnProperty.call(message, "htlc_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_msat = typeof message.htlc_msat === "number" ? $BigInt(message.htlc_msat) : $util.Long.fromBits(message.htlc_msat.low >>> 0, message.htlc_msat.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_msat === "number")
                    object.htlc_msat = options.longs === $String ? $String(message.htlc_msat) : message.htlc_msat;
                else
                    object.htlc_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_msat) : options.longs === $Number ? new $util.LongBits(message.htlc_msat.low >>> 0, message.htlc_msat.high >>> 0).toNumber(true) : message.htlc_msat;
            if (message.onion_sha_256 != null && $Object.hasOwnProperty.call(message, "onion_sha_256"))
                object.onion_sha_256 = options.bytes === $String ? $util.base64.encode(message.onion_sha_256, 0, message.onion_sha_256.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.onion_sha_256) : message.onion_sha_256;
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry"))
                object.cltv_expiry = message.cltv_expiry;
            if (message.flags != null && $Object.hasOwnProperty.call(message, "flags"))
                object.flags = message.flags;
            if (message.failure_source_index != null && $Object.hasOwnProperty.call(message, "failure_source_index"))
                object.failure_source_index = message.failure_source_index;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            return object;
        };

        /**
         * Converts this Failure to JSON.
         * @function toJSON
         * @memberof lnrpc.Failure
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Failure.prototype.toJSON = function() {
            return Failure.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Failure
         * @function getTypeUrl
         * @memberof lnrpc.Failure
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Failure.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Failure";
        };

        /**
         * FailureCode enum.
         * @name lnrpc.Failure.FailureCode
         * @enum {number}
         * @property {number} RESERVED=0 RESERVED value
         * @property {number} INCORRECT_OR_UNKNOWN_PAYMENT_DETAILS=1 INCORRECT_OR_UNKNOWN_PAYMENT_DETAILS value
         * @property {number} INCORRECT_PAYMENT_AMOUNT=2 INCORRECT_PAYMENT_AMOUNT value
         * @property {number} FINAL_INCORRECT_CLTV_EXPIRY=3 FINAL_INCORRECT_CLTV_EXPIRY value
         * @property {number} FINAL_INCORRECT_HTLC_AMOUNT=4 FINAL_INCORRECT_HTLC_AMOUNT value
         * @property {number} FINAL_EXPIRY_TOO_SOON=5 FINAL_EXPIRY_TOO_SOON value
         * @property {number} INVALID_REALM=6 INVALID_REALM value
         * @property {number} EXPIRY_TOO_SOON=7 EXPIRY_TOO_SOON value
         * @property {number} INVALID_ONION_VERSION=8 INVALID_ONION_VERSION value
         * @property {number} INVALID_ONION_HMAC=9 INVALID_ONION_HMAC value
         * @property {number} INVALID_ONION_KEY=10 INVALID_ONION_KEY value
         * @property {number} AMOUNT_BELOW_MINIMUM=11 AMOUNT_BELOW_MINIMUM value
         * @property {number} FEE_INSUFFICIENT=12 FEE_INSUFFICIENT value
         * @property {number} INCORRECT_CLTV_EXPIRY=13 INCORRECT_CLTV_EXPIRY value
         * @property {number} CHANNEL_DISABLED=14 CHANNEL_DISABLED value
         * @property {number} TEMPORARY_CHANNEL_FAILURE=15 TEMPORARY_CHANNEL_FAILURE value
         * @property {number} REQUIRED_NODE_FEATURE_MISSING=16 REQUIRED_NODE_FEATURE_MISSING value
         * @property {number} REQUIRED_CHANNEL_FEATURE_MISSING=17 REQUIRED_CHANNEL_FEATURE_MISSING value
         * @property {number} UNKNOWN_NEXT_PEER=18 UNKNOWN_NEXT_PEER value
         * @property {number} TEMPORARY_NODE_FAILURE=19 TEMPORARY_NODE_FAILURE value
         * @property {number} PERMANENT_NODE_FAILURE=20 PERMANENT_NODE_FAILURE value
         * @property {number} PERMANENT_CHANNEL_FAILURE=21 PERMANENT_CHANNEL_FAILURE value
         * @property {number} EXPIRY_TOO_FAR=22 EXPIRY_TOO_FAR value
         * @property {number} MPP_TIMEOUT=23 MPP_TIMEOUT value
         * @property {number} INVALID_ONION_PAYLOAD=24 INVALID_ONION_PAYLOAD value
         * @property {number} INVALID_ONION_BLINDING=25 INVALID_ONION_BLINDING value
         * @property {number} INTERNAL_FAILURE=997 INTERNAL_FAILURE value
         * @property {number} UNKNOWN_FAILURE=998 UNKNOWN_FAILURE value
         * @property {number} UNREADABLE_FAILURE=999 UNREADABLE_FAILURE value
         */
        Failure.FailureCode = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "RESERVED"] = 0;
            values[valuesById[1] = "INCORRECT_OR_UNKNOWN_PAYMENT_DETAILS"] = 1;
            values[valuesById[2] = "INCORRECT_PAYMENT_AMOUNT"] = 2;
            values[valuesById[3] = "FINAL_INCORRECT_CLTV_EXPIRY"] = 3;
            values[valuesById[4] = "FINAL_INCORRECT_HTLC_AMOUNT"] = 4;
            values[valuesById[5] = "FINAL_EXPIRY_TOO_SOON"] = 5;
            values[valuesById[6] = "INVALID_REALM"] = 6;
            values[valuesById[7] = "EXPIRY_TOO_SOON"] = 7;
            values[valuesById[8] = "INVALID_ONION_VERSION"] = 8;
            values[valuesById[9] = "INVALID_ONION_HMAC"] = 9;
            values[valuesById[10] = "INVALID_ONION_KEY"] = 10;
            values[valuesById[11] = "AMOUNT_BELOW_MINIMUM"] = 11;
            values[valuesById[12] = "FEE_INSUFFICIENT"] = 12;
            values[valuesById[13] = "INCORRECT_CLTV_EXPIRY"] = 13;
            values[valuesById[14] = "CHANNEL_DISABLED"] = 14;
            values[valuesById[15] = "TEMPORARY_CHANNEL_FAILURE"] = 15;
            values[valuesById[16] = "REQUIRED_NODE_FEATURE_MISSING"] = 16;
            values[valuesById[17] = "REQUIRED_CHANNEL_FEATURE_MISSING"] = 17;
            values[valuesById[18] = "UNKNOWN_NEXT_PEER"] = 18;
            values[valuesById[19] = "TEMPORARY_NODE_FAILURE"] = 19;
            values[valuesById[20] = "PERMANENT_NODE_FAILURE"] = 20;
            values[valuesById[21] = "PERMANENT_CHANNEL_FAILURE"] = 21;
            values[valuesById[22] = "EXPIRY_TOO_FAR"] = 22;
            values[valuesById[23] = "MPP_TIMEOUT"] = 23;
            values[valuesById[24] = "INVALID_ONION_PAYLOAD"] = 24;
            values[valuesById[25] = "INVALID_ONION_BLINDING"] = 25;
            values[valuesById[997] = "INTERNAL_FAILURE"] = 997;
            values[valuesById[998] = "UNKNOWN_FAILURE"] = 998;
            values[valuesById[999] = "UNREADABLE_FAILURE"] = 999;
            return values;
        })();

        return Failure;
    })();

    lnrpc.ChannelUpdate = (function() {

        /**
         * Properties of a ChannelUpdate.
         * @typedef {Object} lnrpc.ChannelUpdate.$Properties
         * @property {Uint8Array|null} [signature] ChannelUpdate signature
         * @property {Uint8Array|null} [chain_hash] ChannelUpdate chain_hash
         * @property {Long|null} [chan_id] ChannelUpdate chan_id
         * @property {number|null} [timestamp] ChannelUpdate timestamp
         * @property {number|null} [message_flags] ChannelUpdate message_flags
         * @property {number|null} [channel_flags] ChannelUpdate channel_flags
         * @property {number|null} [time_lock_delta] ChannelUpdate time_lock_delta
         * @property {Long|null} [htlc_minimum_msat] ChannelUpdate htlc_minimum_msat
         * @property {number|null} [base_fee] ChannelUpdate base_fee
         * @property {number|null} [fee_rate] ChannelUpdate fee_rate
         * @property {Long|null} [htlc_maximum_msat] ChannelUpdate htlc_maximum_msat
         * @property {Uint8Array|null} [extra_opaque_data] ChannelUpdate extra_opaque_data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChannelUpdate.
         * @memberof lnrpc
         * @interface IChannelUpdate
         * @augments lnrpc.ChannelUpdate.$Properties
         * @deprecated Use lnrpc.ChannelUpdate.$Properties instead.
         */

        /**
         * Shape of a ChannelUpdate.
         * @typedef {lnrpc.ChannelUpdate.$Properties} lnrpc.ChannelUpdate.$Shape
         */

        /**
         * Constructs a new ChannelUpdate.
         * @memberof lnrpc
         * @classdesc Represents a ChannelUpdate.
         * @constructor
         * @param {lnrpc.ChannelUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChannelUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChannelUpdate signature.
         * @member {Uint8Array} signature
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.signature = $util.newBuffer([]);

        /**
         * ChannelUpdate chain_hash.
         * @member {Uint8Array} chain_hash
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.chain_hash = $util.newBuffer([]);

        /**
         * ChannelUpdate chan_id.
         * @member {Long} chan_id
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelUpdate timestamp.
         * @member {number} timestamp
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.timestamp = 0;

        /**
         * ChannelUpdate message_flags.
         * @member {number} message_flags
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.message_flags = 0;

        /**
         * ChannelUpdate channel_flags.
         * @member {number} channel_flags
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.channel_flags = 0;

        /**
         * ChannelUpdate time_lock_delta.
         * @member {number} time_lock_delta
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.time_lock_delta = 0;

        /**
         * ChannelUpdate htlc_minimum_msat.
         * @member {Long} htlc_minimum_msat
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.htlc_minimum_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelUpdate base_fee.
         * @member {number} base_fee
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.base_fee = 0;

        /**
         * ChannelUpdate fee_rate.
         * @member {number} fee_rate
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.fee_rate = 0;

        /**
         * ChannelUpdate htlc_maximum_msat.
         * @member {Long} htlc_maximum_msat
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.htlc_maximum_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChannelUpdate extra_opaque_data.
         * @member {Uint8Array} extra_opaque_data
         * @memberof lnrpc.ChannelUpdate
         * @instance
         */
        ChannelUpdate.prototype.extra_opaque_data = $util.newBuffer([]);

        /**
         * Creates a new ChannelUpdate instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChannelUpdate
         * @static
         * @param {lnrpc.ChannelUpdate.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChannelUpdate} ChannelUpdate instance
         * @type {{
         *   (properties: lnrpc.ChannelUpdate.$Shape): lnrpc.ChannelUpdate & lnrpc.ChannelUpdate.$Shape;
         *   (properties?: lnrpc.ChannelUpdate.$Properties): lnrpc.ChannelUpdate;
         * }}
         */
        ChannelUpdate.create = function(properties) {
            return new ChannelUpdate(properties);
        };

        /**
         * Encodes the specified ChannelUpdate message. Does not implicitly {@link lnrpc.ChannelUpdate.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChannelUpdate
         * @static
         * @param {lnrpc.ChannelUpdate.$Properties} message ChannelUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.signature);
            if (message.chain_hash != null && $Object.hasOwnProperty.call(message, "chain_hash") && message.chain_hash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.chain_hash);
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.chan_id);
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp") && message.timestamp !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.timestamp);
            if (message.channel_flags != null && $Object.hasOwnProperty.call(message, "channel_flags") && message.channel_flags !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.channel_flags);
            if (message.time_lock_delta != null && $Object.hasOwnProperty.call(message, "time_lock_delta") && message.time_lock_delta !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.time_lock_delta);
            if (message.htlc_minimum_msat != null && $Object.hasOwnProperty.call(message, "htlc_minimum_msat") && (typeof message.htlc_minimum_msat === "object" ? message.htlc_minimum_msat.low || message.htlc_minimum_msat.high : message.htlc_minimum_msat !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.htlc_minimum_msat);
            if (message.base_fee != null && $Object.hasOwnProperty.call(message, "base_fee") && message.base_fee !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.base_fee);
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate") && message.fee_rate !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.fee_rate);
            if (message.message_flags != null && $Object.hasOwnProperty.call(message, "message_flags") && message.message_flags !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.message_flags);
            if (message.htlc_maximum_msat != null && $Object.hasOwnProperty.call(message, "htlc_maximum_msat") && (typeof message.htlc_maximum_msat === "object" ? message.htlc_maximum_msat.low || message.htlc_maximum_msat.high : message.htlc_maximum_msat !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.htlc_maximum_msat);
            if (message.extra_opaque_data != null && $Object.hasOwnProperty.call(message, "extra_opaque_data") && message.extra_opaque_data.length)
                writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.extra_opaque_data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChannelUpdate message, length delimited. Does not implicitly {@link lnrpc.ChannelUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChannelUpdate
         * @static
         * @param {lnrpc.ChannelUpdate.$Properties} message ChannelUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChannelUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChannelUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChannelUpdate & lnrpc.ChannelUpdate.$Shape} ChannelUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChannelUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.chain_hash = value;
                        else
                            delete message.chain_hash;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.message_flags = value;
                        else
                            delete message.message_flags;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.channel_flags = value;
                        else
                            delete message.channel_flags;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time_lock_delta = value;
                        else
                            delete message.time_lock_delta;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_minimum_msat = value;
                        else
                            delete message.htlc_minimum_msat;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.base_fee = value;
                        else
                            delete message.base_fee;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fee_rate = value;
                        else
                            delete message.fee_rate;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_maximum_msat = value;
                        else
                            delete message.htlc_maximum_msat;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.extra_opaque_data = value;
                        else
                            delete message.extra_opaque_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChannelUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChannelUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChannelUpdate & lnrpc.ChannelUpdate.$Shape} ChannelUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelUpdate message.
         * @function verify
         * @memberof lnrpc.ChannelUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            if (message.chain_hash != null && $Object.hasOwnProperty.call(message, "chain_hash"))
                if (!(message.chain_hash && typeof message.chain_hash.length === "number" || $util.isString(message.chain_hash)))
                    return "chain_hash: buffer expected";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                if (!$util.isInteger(message.timestamp))
                    return "timestamp: integer expected";
            if (message.message_flags != null && $Object.hasOwnProperty.call(message, "message_flags"))
                if (!$util.isInteger(message.message_flags))
                    return "message_flags: integer expected";
            if (message.channel_flags != null && $Object.hasOwnProperty.call(message, "channel_flags"))
                if (!$util.isInteger(message.channel_flags))
                    return "channel_flags: integer expected";
            if (message.time_lock_delta != null && $Object.hasOwnProperty.call(message, "time_lock_delta"))
                if (!$util.isInteger(message.time_lock_delta))
                    return "time_lock_delta: integer expected";
            if (message.htlc_minimum_msat != null && $Object.hasOwnProperty.call(message, "htlc_minimum_msat"))
                if (!$util.isInteger(message.htlc_minimum_msat) && !(message.htlc_minimum_msat && $util.isInteger(message.htlc_minimum_msat.low) && $util.isInteger(message.htlc_minimum_msat.high)))
                    return "htlc_minimum_msat: integer|Long expected";
            if (message.base_fee != null && $Object.hasOwnProperty.call(message, "base_fee"))
                if (!$util.isInteger(message.base_fee))
                    return "base_fee: integer expected";
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                if (!$util.isInteger(message.fee_rate))
                    return "fee_rate: integer expected";
            if (message.htlc_maximum_msat != null && $Object.hasOwnProperty.call(message, "htlc_maximum_msat"))
                if (!$util.isInteger(message.htlc_maximum_msat) && !(message.htlc_maximum_msat && $util.isInteger(message.htlc_maximum_msat.low) && $util.isInteger(message.htlc_maximum_msat.high)))
                    return "htlc_maximum_msat: integer|Long expected";
            if (message.extra_opaque_data != null && $Object.hasOwnProperty.call(message, "extra_opaque_data"))
                if (!(message.extra_opaque_data && typeof message.extra_opaque_data.length === "number" || $util.isString(message.extra_opaque_data)))
                    return "extra_opaque_data: buffer expected";
            return null;
        };

        /**
         * Creates a ChannelUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChannelUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChannelUpdate} ChannelUpdate
         */
        ChannelUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChannelUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChannelUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChannelUpdate();
            if (object.signature != null)
                if (object.signature.length)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
            if (object.chain_hash != null)
                if (object.chain_hash.length)
                    if (typeof object.chain_hash === "string")
                        $util.base64.decode(object.chain_hash, message.chain_hash = $util.newBuffer($util.base64.length(object.chain_hash)), 0);
                    else if (object.chain_hash.length >= 0)
                        message.chain_hash = object.chain_hash;
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.timestamp != null)
                if ($Number(object.timestamp) !== 0)
                    message.timestamp = object.timestamp >>> 0;
            if (object.message_flags != null)
                if ($Number(object.message_flags) !== 0)
                    message.message_flags = object.message_flags >>> 0;
            if (object.channel_flags != null)
                if ($Number(object.channel_flags) !== 0)
                    message.channel_flags = object.channel_flags >>> 0;
            if (object.time_lock_delta != null)
                if ($Number(object.time_lock_delta) !== 0)
                    message.time_lock_delta = object.time_lock_delta >>> 0;
            if (object.htlc_minimum_msat != null)
                if (typeof object.htlc_minimum_msat === "object" ? object.htlc_minimum_msat.low || object.htlc_minimum_msat.high : $Number(object.htlc_minimum_msat) !== 0)
                    if ($util.Long)
                        message.htlc_minimum_msat = $util.Long.fromValue(object.htlc_minimum_msat, true);
                    else if (typeof object.htlc_minimum_msat === "string")
                        message.htlc_minimum_msat = $parseInt(object.htlc_minimum_msat, 10);
                    else if (typeof object.htlc_minimum_msat === "number")
                        message.htlc_minimum_msat = object.htlc_minimum_msat;
                    else if (typeof object.htlc_minimum_msat === "object")
                        message.htlc_minimum_msat = new $util.LongBits(object.htlc_minimum_msat.low >>> 0, object.htlc_minimum_msat.high >>> 0).toNumber(true);
            if (object.base_fee != null)
                if ($Number(object.base_fee) !== 0)
                    message.base_fee = object.base_fee >>> 0;
            if (object.fee_rate != null)
                if ($Number(object.fee_rate) !== 0)
                    message.fee_rate = object.fee_rate >>> 0;
            if (object.htlc_maximum_msat != null)
                if (typeof object.htlc_maximum_msat === "object" ? object.htlc_maximum_msat.low || object.htlc_maximum_msat.high : $Number(object.htlc_maximum_msat) !== 0)
                    if ($util.Long)
                        message.htlc_maximum_msat = $util.Long.fromValue(object.htlc_maximum_msat, true);
                    else if (typeof object.htlc_maximum_msat === "string")
                        message.htlc_maximum_msat = $parseInt(object.htlc_maximum_msat, 10);
                    else if (typeof object.htlc_maximum_msat === "number")
                        message.htlc_maximum_msat = object.htlc_maximum_msat;
                    else if (typeof object.htlc_maximum_msat === "object")
                        message.htlc_maximum_msat = new $util.LongBits(object.htlc_maximum_msat.low >>> 0, object.htlc_maximum_msat.high >>> 0).toNumber(true);
            if (object.extra_opaque_data != null)
                if (object.extra_opaque_data.length)
                    if (typeof object.extra_opaque_data === "string")
                        $util.base64.decode(object.extra_opaque_data, message.extra_opaque_data = $util.newBuffer($util.base64.length(object.extra_opaque_data)), 0);
                    else if (object.extra_opaque_data.length >= 0)
                        message.extra_opaque_data = object.extra_opaque_data;
            return message;
        };

        /**
         * Creates a plain object from a ChannelUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChannelUpdate
         * @static
         * @param {lnrpc.ChannelUpdate} message ChannelUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== $Array)
                        object.signature = $util.newBuffer(object.signature);
                }
                if (options.bytes === $String)
                    object.chain_hash = "";
                else {
                    object.chain_hash = [];
                    if (options.bytes !== $Array)
                        object.chain_hash = $util.newBuffer(object.chain_hash);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.timestamp = 0;
                object.channel_flags = 0;
                object.time_lock_delta = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_minimum_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_minimum_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.base_fee = 0;
                object.fee_rate = 0;
                object.message_flags = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_maximum_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_maximum_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.extra_opaque_data = "";
                else {
                    object.extra_opaque_data = [];
                    if (options.bytes !== $Array)
                        object.extra_opaque_data = $util.newBuffer(object.extra_opaque_data);
                }
            }
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                object.signature = options.bytes === $String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signature) : message.signature;
            if (message.chain_hash != null && $Object.hasOwnProperty.call(message, "chain_hash"))
                object.chain_hash = options.bytes === $String ? $util.base64.encode(message.chain_hash, 0, message.chain_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.chain_hash) : message.chain_hash;
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                object.timestamp = message.timestamp;
            if (message.channel_flags != null && $Object.hasOwnProperty.call(message, "channel_flags"))
                object.channel_flags = message.channel_flags;
            if (message.time_lock_delta != null && $Object.hasOwnProperty.call(message, "time_lock_delta"))
                object.time_lock_delta = message.time_lock_delta;
            if (message.htlc_minimum_msat != null && $Object.hasOwnProperty.call(message, "htlc_minimum_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_minimum_msat = typeof message.htlc_minimum_msat === "number" ? $BigInt(message.htlc_minimum_msat) : $util.Long.fromBits(message.htlc_minimum_msat.low >>> 0, message.htlc_minimum_msat.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_minimum_msat === "number")
                    object.htlc_minimum_msat = options.longs === $String ? $String(message.htlc_minimum_msat) : message.htlc_minimum_msat;
                else
                    object.htlc_minimum_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_minimum_msat) : options.longs === $Number ? new $util.LongBits(message.htlc_minimum_msat.low >>> 0, message.htlc_minimum_msat.high >>> 0).toNumber(true) : message.htlc_minimum_msat;
            if (message.base_fee != null && $Object.hasOwnProperty.call(message, "base_fee"))
                object.base_fee = message.base_fee;
            if (message.fee_rate != null && $Object.hasOwnProperty.call(message, "fee_rate"))
                object.fee_rate = message.fee_rate;
            if (message.message_flags != null && $Object.hasOwnProperty.call(message, "message_flags"))
                object.message_flags = message.message_flags;
            if (message.htlc_maximum_msat != null && $Object.hasOwnProperty.call(message, "htlc_maximum_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_maximum_msat = typeof message.htlc_maximum_msat === "number" ? $BigInt(message.htlc_maximum_msat) : $util.Long.fromBits(message.htlc_maximum_msat.low >>> 0, message.htlc_maximum_msat.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_maximum_msat === "number")
                    object.htlc_maximum_msat = options.longs === $String ? $String(message.htlc_maximum_msat) : message.htlc_maximum_msat;
                else
                    object.htlc_maximum_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_maximum_msat) : options.longs === $Number ? new $util.LongBits(message.htlc_maximum_msat.low >>> 0, message.htlc_maximum_msat.high >>> 0).toNumber(true) : message.htlc_maximum_msat;
            if (message.extra_opaque_data != null && $Object.hasOwnProperty.call(message, "extra_opaque_data"))
                object.extra_opaque_data = options.bytes === $String ? $util.base64.encode(message.extra_opaque_data, 0, message.extra_opaque_data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.extra_opaque_data) : message.extra_opaque_data;
            return object;
        };

        /**
         * Converts this ChannelUpdate to JSON.
         * @function toJSON
         * @memberof lnrpc.ChannelUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelUpdate.prototype.toJSON = function() {
            return ChannelUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChannelUpdate
         * @function getTypeUrl
         * @memberof lnrpc.ChannelUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChannelUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChannelUpdate";
        };

        return ChannelUpdate;
    })();

    lnrpc.MacaroonId = (function() {

        /**
         * Properties of a MacaroonId.
         * @typedef {Object} lnrpc.MacaroonId.$Properties
         * @property {Uint8Array|null} [nonce] MacaroonId nonce
         * @property {Uint8Array|null} [storageId] MacaroonId storageId
         * @property {Array.<lnrpc.Op.$Properties>|null} [ops] MacaroonId ops
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MacaroonId.
         * @memberof lnrpc
         * @interface IMacaroonId
         * @augments lnrpc.MacaroonId.$Properties
         * @deprecated Use lnrpc.MacaroonId.$Properties instead.
         */

        /**
         * Shape of a MacaroonId.
         * @typedef {lnrpc.MacaroonId.$Properties} lnrpc.MacaroonId.$Shape
         */

        /**
         * Constructs a new MacaroonId.
         * @memberof lnrpc
         * @classdesc Represents a MacaroonId.
         * @constructor
         * @param {lnrpc.MacaroonId.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MacaroonId = function (properties) {
            this.ops = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MacaroonId nonce.
         * @member {Uint8Array} nonce
         * @memberof lnrpc.MacaroonId
         * @instance
         */
        MacaroonId.prototype.nonce = $util.newBuffer([]);

        /**
         * MacaroonId storageId.
         * @member {Uint8Array} storageId
         * @memberof lnrpc.MacaroonId
         * @instance
         */
        MacaroonId.prototype.storageId = $util.newBuffer([]);

        /**
         * MacaroonId ops.
         * @member {Array.<lnrpc.Op.$Properties>} ops
         * @memberof lnrpc.MacaroonId
         * @instance
         */
        MacaroonId.prototype.ops = $util.emptyArray;

        /**
         * Creates a new MacaroonId instance using the specified properties.
         * @function create
         * @memberof lnrpc.MacaroonId
         * @static
         * @param {lnrpc.MacaroonId.$Properties=} [properties] Properties to set
         * @returns {lnrpc.MacaroonId} MacaroonId instance
         * @type {{
         *   (properties: lnrpc.MacaroonId.$Shape): lnrpc.MacaroonId & lnrpc.MacaroonId.$Shape;
         *   (properties?: lnrpc.MacaroonId.$Properties): lnrpc.MacaroonId;
         * }}
         */
        MacaroonId.create = function(properties) {
            return new MacaroonId(properties);
        };

        /**
         * Encodes the specified MacaroonId message. Does not implicitly {@link lnrpc.MacaroonId.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.MacaroonId
         * @static
         * @param {lnrpc.MacaroonId.$Properties} message MacaroonId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MacaroonId.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce") && message.nonce.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.nonce);
            if (message.storageId != null && $Object.hasOwnProperty.call(message, "storageId") && message.storageId.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.storageId);
            if (message.ops != null && message.ops.length)
                for (let i = 0; i < message.ops.length; ++i)
                    $root.lnrpc.Op.encode(message.ops[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MacaroonId message, length delimited. Does not implicitly {@link lnrpc.MacaroonId.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.MacaroonId
         * @static
         * @param {lnrpc.MacaroonId.$Properties} message MacaroonId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MacaroonId.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MacaroonId message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.MacaroonId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.MacaroonId & lnrpc.MacaroonId.$Shape} MacaroonId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MacaroonId.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.MacaroonId(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.nonce = value;
                        else
                            delete message.nonce;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.storageId = value;
                        else
                            delete message.storageId;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ops && message.ops.length))
                            message.ops = [];
                        message.ops.push($root.lnrpc.Op.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MacaroonId message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.MacaroonId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.MacaroonId & lnrpc.MacaroonId.$Shape} MacaroonId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MacaroonId.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MacaroonId message.
         * @function verify
         * @memberof lnrpc.MacaroonId
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MacaroonId.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                if (!(message.nonce && typeof message.nonce.length === "number" || $util.isString(message.nonce)))
                    return "nonce: buffer expected";
            if (message.storageId != null && $Object.hasOwnProperty.call(message, "storageId"))
                if (!(message.storageId && typeof message.storageId.length === "number" || $util.isString(message.storageId)))
                    return "storageId: buffer expected";
            if (message.ops != null && $Object.hasOwnProperty.call(message, "ops")) {
                if (!$Array.isArray(message.ops))
                    return "ops: array expected";
                for (let i = 0; i < message.ops.length; ++i) {
                    let error = $root.lnrpc.Op.verify(message.ops[i], _depth + 1);
                    if (error)
                        return "ops." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MacaroonId message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.MacaroonId
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.MacaroonId} MacaroonId
         */
        MacaroonId.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.MacaroonId)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.MacaroonId: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.MacaroonId();
            if (object.nonce != null)
                if (object.nonce.length)
                    if (typeof object.nonce === "string")
                        $util.base64.decode(object.nonce, message.nonce = $util.newBuffer($util.base64.length(object.nonce)), 0);
                    else if (object.nonce.length >= 0)
                        message.nonce = object.nonce;
            if (object.storageId != null)
                if (object.storageId.length)
                    if (typeof object.storageId === "string")
                        $util.base64.decode(object.storageId, message.storageId = $util.newBuffer($util.base64.length(object.storageId)), 0);
                    else if (object.storageId.length >= 0)
                        message.storageId = object.storageId;
            if (object.ops) {
                if (!$Array.isArray(object.ops))
                    throw $TypeError(".lnrpc.MacaroonId.ops: array expected");
                message.ops = $Array(object.ops.length);
                for (let i = 0; i < object.ops.length; ++i) {
                    if (!$util.isObject(object.ops[i]))
                        throw $TypeError(".lnrpc.MacaroonId.ops: object expected");
                    message.ops[i] = $root.lnrpc.Op.fromObject(object.ops[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MacaroonId message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.MacaroonId
         * @static
         * @param {lnrpc.MacaroonId} message MacaroonId
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MacaroonId.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.ops = [];
            if (options.defaults) {
                if (options.bytes === $String)
                    object.nonce = "";
                else {
                    object.nonce = [];
                    if (options.bytes !== $Array)
                        object.nonce = $util.newBuffer(object.nonce);
                }
                if (options.bytes === $String)
                    object.storageId = "";
                else {
                    object.storageId = [];
                    if (options.bytes !== $Array)
                        object.storageId = $util.newBuffer(object.storageId);
                }
            }
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                object.nonce = options.bytes === $String ? $util.base64.encode(message.nonce, 0, message.nonce.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nonce) : message.nonce;
            if (message.storageId != null && $Object.hasOwnProperty.call(message, "storageId"))
                object.storageId = options.bytes === $String ? $util.base64.encode(message.storageId, 0, message.storageId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.storageId) : message.storageId;
            if (message.ops && message.ops.length) {
                object.ops = $Array(message.ops.length);
                for (let j = 0; j < message.ops.length; ++j)
                    object.ops[j] = $root.lnrpc.Op.toObject(message.ops[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this MacaroonId to JSON.
         * @function toJSON
         * @memberof lnrpc.MacaroonId
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MacaroonId.prototype.toJSON = function() {
            return MacaroonId.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MacaroonId
         * @function getTypeUrl
         * @memberof lnrpc.MacaroonId
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MacaroonId.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.MacaroonId";
        };

        return MacaroonId;
    })();

    lnrpc.Op = (function() {

        /**
         * Properties of an Op.
         * @typedef {Object} lnrpc.Op.$Properties
         * @property {string|null} [entity] Op entity
         * @property {Array.<string>|null} [actions] Op actions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an Op.
         * @memberof lnrpc
         * @interface IOp
         * @augments lnrpc.Op.$Properties
         * @deprecated Use lnrpc.Op.$Properties instead.
         */

        /**
         * Shape of an Op.
         * @typedef {lnrpc.Op.$Properties} lnrpc.Op.$Shape
         */

        /**
         * Constructs a new Op.
         * @memberof lnrpc
         * @classdesc Represents an Op.
         * @constructor
         * @param {lnrpc.Op.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Op = function (properties) {
            this.actions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Op entity.
         * @member {string} entity
         * @memberof lnrpc.Op
         * @instance
         */
        Op.prototype.entity = "";

        /**
         * Op actions.
         * @member {Array.<string>} actions
         * @memberof lnrpc.Op
         * @instance
         */
        Op.prototype.actions = $util.emptyArray;

        /**
         * Creates a new Op instance using the specified properties.
         * @function create
         * @memberof lnrpc.Op
         * @static
         * @param {lnrpc.Op.$Properties=} [properties] Properties to set
         * @returns {lnrpc.Op} Op instance
         * @type {{
         *   (properties: lnrpc.Op.$Shape): lnrpc.Op & lnrpc.Op.$Shape;
         *   (properties?: lnrpc.Op.$Properties): lnrpc.Op;
         * }}
         */
        Op.create = function(properties) {
            return new Op(properties);
        };

        /**
         * Encodes the specified Op message. Does not implicitly {@link lnrpc.Op.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.Op
         * @static
         * @param {lnrpc.Op.$Properties} message Op message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Op.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.entity != null && $Object.hasOwnProperty.call(message, "entity") && message.entity !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.entity);
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.actions[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Op message, length delimited. Does not implicitly {@link lnrpc.Op.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.Op
         * @static
         * @param {lnrpc.Op.$Properties} message Op message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Op.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Op message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.Op
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.Op & lnrpc.Op.$Shape} Op
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Op.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.Op(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.entity = value;
                        else
                            delete message.entity;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Op message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.Op
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.Op & lnrpc.Op.$Shape} Op
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Op.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Op message.
         * @function verify
         * @memberof lnrpc.Op
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Op.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.entity != null && $Object.hasOwnProperty.call(message, "entity"))
                if (!$util.isString(message.entity))
                    return "entity: string expected";
            if (message.actions != null && $Object.hasOwnProperty.call(message, "actions")) {
                if (!$Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i)
                    if (!$util.isString(message.actions[i]))
                        return "actions: string[] expected";
            }
            return null;
        };

        /**
         * Creates an Op message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.Op
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.Op} Op
         */
        Op.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.Op)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.Op: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.Op();
            if (object.entity != null)
                if (typeof object.entity !== "string" || object.entity.length)
                    message.entity = $String(object.entity);
            if (object.actions) {
                if (!$Array.isArray(object.actions))
                    throw $TypeError(".lnrpc.Op.actions: array expected");
                message.actions = $Array(object.actions.length);
                for (let i = 0; i < object.actions.length; ++i)
                    message.actions[i] = $String(object.actions[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from an Op message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.Op
         * @static
         * @param {lnrpc.Op} message Op
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Op.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.actions = [];
            if (options.defaults)
                object.entity = "";
            if (message.entity != null && $Object.hasOwnProperty.call(message, "entity"))
                object.entity = message.entity;
            if (message.actions && message.actions.length) {
                object.actions = $Array(message.actions.length);
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = message.actions[j];
            }
            return object;
        };

        /**
         * Converts this Op to JSON.
         * @function toJSON
         * @memberof lnrpc.Op
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Op.prototype.toJSON = function() {
            return Op.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Op
         * @function getTypeUrl
         * @memberof lnrpc.Op
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Op.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.Op";
        };

        return Op;
    })();

    lnrpc.CheckMacPermRequest = (function() {

        /**
         * Properties of a CheckMacPermRequest.
         * @typedef {Object} lnrpc.CheckMacPermRequest.$Properties
         * @property {Uint8Array|null} [macaroon] CheckMacPermRequest macaroon
         * @property {Array.<lnrpc.MacaroonPermission.$Properties>|null} [permissions] CheckMacPermRequest permissions
         * @property {string|null} [fullMethod] CheckMacPermRequest fullMethod
         * @property {boolean|null} [check_default_perms_from_full_method] CheckMacPermRequest check_default_perms_from_full_method
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CheckMacPermRequest.
         * @memberof lnrpc
         * @interface ICheckMacPermRequest
         * @augments lnrpc.CheckMacPermRequest.$Properties
         * @deprecated Use lnrpc.CheckMacPermRequest.$Properties instead.
         */

        /**
         * Shape of a CheckMacPermRequest.
         * @typedef {lnrpc.CheckMacPermRequest.$Properties} lnrpc.CheckMacPermRequest.$Shape
         */

        /**
         * Constructs a new CheckMacPermRequest.
         * @memberof lnrpc
         * @classdesc Represents a CheckMacPermRequest.
         * @constructor
         * @param {lnrpc.CheckMacPermRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CheckMacPermRequest = function (properties) {
            this.permissions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CheckMacPermRequest macaroon.
         * @member {Uint8Array} macaroon
         * @memberof lnrpc.CheckMacPermRequest
         * @instance
         */
        CheckMacPermRequest.prototype.macaroon = $util.newBuffer([]);

        /**
         * CheckMacPermRequest permissions.
         * @member {Array.<lnrpc.MacaroonPermission.$Properties>} permissions
         * @memberof lnrpc.CheckMacPermRequest
         * @instance
         */
        CheckMacPermRequest.prototype.permissions = $util.emptyArray;

        /**
         * CheckMacPermRequest fullMethod.
         * @member {string} fullMethod
         * @memberof lnrpc.CheckMacPermRequest
         * @instance
         */
        CheckMacPermRequest.prototype.fullMethod = "";

        /**
         * CheckMacPermRequest check_default_perms_from_full_method.
         * @member {boolean} check_default_perms_from_full_method
         * @memberof lnrpc.CheckMacPermRequest
         * @instance
         */
        CheckMacPermRequest.prototype.check_default_perms_from_full_method = false;

        /**
         * Creates a new CheckMacPermRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.CheckMacPermRequest
         * @static
         * @param {lnrpc.CheckMacPermRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.CheckMacPermRequest} CheckMacPermRequest instance
         * @type {{
         *   (properties: lnrpc.CheckMacPermRequest.$Shape): lnrpc.CheckMacPermRequest & lnrpc.CheckMacPermRequest.$Shape;
         *   (properties?: lnrpc.CheckMacPermRequest.$Properties): lnrpc.CheckMacPermRequest;
         * }}
         */
        CheckMacPermRequest.create = function(properties) {
            return new CheckMacPermRequest(properties);
        };

        /**
         * Encodes the specified CheckMacPermRequest message. Does not implicitly {@link lnrpc.CheckMacPermRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.CheckMacPermRequest
         * @static
         * @param {lnrpc.CheckMacPermRequest.$Properties} message CheckMacPermRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMacPermRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.macaroon != null && $Object.hasOwnProperty.call(message, "macaroon") && message.macaroon.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.macaroon);
            if (message.permissions != null && message.permissions.length)
                for (let i = 0; i < message.permissions.length; ++i)
                    $root.lnrpc.MacaroonPermission.encode(message.permissions[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.fullMethod != null && $Object.hasOwnProperty.call(message, "fullMethod") && message.fullMethod !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.fullMethod);
            if (message.check_default_perms_from_full_method != null && $Object.hasOwnProperty.call(message, "check_default_perms_from_full_method") && message.check_default_perms_from_full_method !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.check_default_perms_from_full_method);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CheckMacPermRequest message, length delimited. Does not implicitly {@link lnrpc.CheckMacPermRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.CheckMacPermRequest
         * @static
         * @param {lnrpc.CheckMacPermRequest.$Properties} message CheckMacPermRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMacPermRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CheckMacPermRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.CheckMacPermRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.CheckMacPermRequest & lnrpc.CheckMacPermRequest.$Shape} CheckMacPermRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMacPermRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.CheckMacPermRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.macaroon = value;
                        else
                            delete message.macaroon;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.permissions && message.permissions.length))
                            message.permissions = [];
                        message.permissions.push($root.lnrpc.MacaroonPermission.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.fullMethod = value;
                        else
                            delete message.fullMethod;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.check_default_perms_from_full_method = value;
                        else
                            delete message.check_default_perms_from_full_method;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CheckMacPermRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.CheckMacPermRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.CheckMacPermRequest & lnrpc.CheckMacPermRequest.$Shape} CheckMacPermRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMacPermRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CheckMacPermRequest message.
         * @function verify
         * @memberof lnrpc.CheckMacPermRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CheckMacPermRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.macaroon != null && $Object.hasOwnProperty.call(message, "macaroon"))
                if (!(message.macaroon && typeof message.macaroon.length === "number" || $util.isString(message.macaroon)))
                    return "macaroon: buffer expected";
            if (message.permissions != null && $Object.hasOwnProperty.call(message, "permissions")) {
                if (!$Array.isArray(message.permissions))
                    return "permissions: array expected";
                for (let i = 0; i < message.permissions.length; ++i) {
                    let error = $root.lnrpc.MacaroonPermission.verify(message.permissions[i], _depth + 1);
                    if (error)
                        return "permissions." + error;
                }
            }
            if (message.fullMethod != null && $Object.hasOwnProperty.call(message, "fullMethod"))
                if (!$util.isString(message.fullMethod))
                    return "fullMethod: string expected";
            if (message.check_default_perms_from_full_method != null && $Object.hasOwnProperty.call(message, "check_default_perms_from_full_method"))
                if (typeof message.check_default_perms_from_full_method !== "boolean")
                    return "check_default_perms_from_full_method: boolean expected";
            return null;
        };

        /**
         * Creates a CheckMacPermRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.CheckMacPermRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.CheckMacPermRequest} CheckMacPermRequest
         */
        CheckMacPermRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.CheckMacPermRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.CheckMacPermRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.CheckMacPermRequest();
            if (object.macaroon != null)
                if (object.macaroon.length)
                    if (typeof object.macaroon === "string")
                        $util.base64.decode(object.macaroon, message.macaroon = $util.newBuffer($util.base64.length(object.macaroon)), 0);
                    else if (object.macaroon.length >= 0)
                        message.macaroon = object.macaroon;
            if (object.permissions) {
                if (!$Array.isArray(object.permissions))
                    throw $TypeError(".lnrpc.CheckMacPermRequest.permissions: array expected");
                message.permissions = $Array(object.permissions.length);
                for (let i = 0; i < object.permissions.length; ++i) {
                    if (!$util.isObject(object.permissions[i]))
                        throw $TypeError(".lnrpc.CheckMacPermRequest.permissions: object expected");
                    message.permissions[i] = $root.lnrpc.MacaroonPermission.fromObject(object.permissions[i], _depth + 1);
                }
            }
            if (object.fullMethod != null)
                if (typeof object.fullMethod !== "string" || object.fullMethod.length)
                    message.fullMethod = $String(object.fullMethod);
            if (object.check_default_perms_from_full_method != null)
                if (object.check_default_perms_from_full_method)
                    message.check_default_perms_from_full_method = $Boolean(object.check_default_perms_from_full_method);
            return message;
        };

        /**
         * Creates a plain object from a CheckMacPermRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.CheckMacPermRequest
         * @static
         * @param {lnrpc.CheckMacPermRequest} message CheckMacPermRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CheckMacPermRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.permissions = [];
            if (options.defaults) {
                if (options.bytes === $String)
                    object.macaroon = "";
                else {
                    object.macaroon = [];
                    if (options.bytes !== $Array)
                        object.macaroon = $util.newBuffer(object.macaroon);
                }
                object.fullMethod = "";
                object.check_default_perms_from_full_method = false;
            }
            if (message.macaroon != null && $Object.hasOwnProperty.call(message, "macaroon"))
                object.macaroon = options.bytes === $String ? $util.base64.encode(message.macaroon, 0, message.macaroon.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.macaroon) : message.macaroon;
            if (message.permissions && message.permissions.length) {
                object.permissions = $Array(message.permissions.length);
                for (let j = 0; j < message.permissions.length; ++j)
                    object.permissions[j] = $root.lnrpc.MacaroonPermission.toObject(message.permissions[j], options, _depth + 1);
            }
            if (message.fullMethod != null && $Object.hasOwnProperty.call(message, "fullMethod"))
                object.fullMethod = message.fullMethod;
            if (message.check_default_perms_from_full_method != null && $Object.hasOwnProperty.call(message, "check_default_perms_from_full_method"))
                object.check_default_perms_from_full_method = message.check_default_perms_from_full_method;
            return object;
        };

        /**
         * Converts this CheckMacPermRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.CheckMacPermRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CheckMacPermRequest.prototype.toJSON = function() {
            return CheckMacPermRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CheckMacPermRequest
         * @function getTypeUrl
         * @memberof lnrpc.CheckMacPermRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CheckMacPermRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.CheckMacPermRequest";
        };

        return CheckMacPermRequest;
    })();

    lnrpc.CheckMacPermResponse = (function() {

        /**
         * Properties of a CheckMacPermResponse.
         * @typedef {Object} lnrpc.CheckMacPermResponse.$Properties
         * @property {boolean|null} [valid] CheckMacPermResponse valid
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CheckMacPermResponse.
         * @memberof lnrpc
         * @interface ICheckMacPermResponse
         * @augments lnrpc.CheckMacPermResponse.$Properties
         * @deprecated Use lnrpc.CheckMacPermResponse.$Properties instead.
         */

        /**
         * Shape of a CheckMacPermResponse.
         * @typedef {lnrpc.CheckMacPermResponse.$Properties} lnrpc.CheckMacPermResponse.$Shape
         */

        /**
         * Constructs a new CheckMacPermResponse.
         * @memberof lnrpc
         * @classdesc Represents a CheckMacPermResponse.
         * @constructor
         * @param {lnrpc.CheckMacPermResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CheckMacPermResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CheckMacPermResponse valid.
         * @member {boolean} valid
         * @memberof lnrpc.CheckMacPermResponse
         * @instance
         */
        CheckMacPermResponse.prototype.valid = false;

        /**
         * Creates a new CheckMacPermResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.CheckMacPermResponse
         * @static
         * @param {lnrpc.CheckMacPermResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.CheckMacPermResponse} CheckMacPermResponse instance
         * @type {{
         *   (properties: lnrpc.CheckMacPermResponse.$Shape): lnrpc.CheckMacPermResponse & lnrpc.CheckMacPermResponse.$Shape;
         *   (properties?: lnrpc.CheckMacPermResponse.$Properties): lnrpc.CheckMacPermResponse;
         * }}
         */
        CheckMacPermResponse.create = function(properties) {
            return new CheckMacPermResponse(properties);
        };

        /**
         * Encodes the specified CheckMacPermResponse message. Does not implicitly {@link lnrpc.CheckMacPermResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.CheckMacPermResponse
         * @static
         * @param {lnrpc.CheckMacPermResponse.$Properties} message CheckMacPermResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMacPermResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid") && message.valid !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.valid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CheckMacPermResponse message, length delimited. Does not implicitly {@link lnrpc.CheckMacPermResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.CheckMacPermResponse
         * @static
         * @param {lnrpc.CheckMacPermResponse.$Properties} message CheckMacPermResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMacPermResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CheckMacPermResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.CheckMacPermResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.CheckMacPermResponse & lnrpc.CheckMacPermResponse.$Shape} CheckMacPermResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMacPermResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.CheckMacPermResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.valid = value;
                        else
                            delete message.valid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CheckMacPermResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.CheckMacPermResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.CheckMacPermResponse & lnrpc.CheckMacPermResponse.$Shape} CheckMacPermResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMacPermResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CheckMacPermResponse message.
         * @function verify
         * @memberof lnrpc.CheckMacPermResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CheckMacPermResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid"))
                if (typeof message.valid !== "boolean")
                    return "valid: boolean expected";
            return null;
        };

        /**
         * Creates a CheckMacPermResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.CheckMacPermResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.CheckMacPermResponse} CheckMacPermResponse
         */
        CheckMacPermResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.CheckMacPermResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.CheckMacPermResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.CheckMacPermResponse();
            if (object.valid != null)
                if (object.valid)
                    message.valid = $Boolean(object.valid);
            return message;
        };

        /**
         * Creates a plain object from a CheckMacPermResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.CheckMacPermResponse
         * @static
         * @param {lnrpc.CheckMacPermResponse} message CheckMacPermResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CheckMacPermResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.valid = false;
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid"))
                object.valid = message.valid;
            return object;
        };

        /**
         * Converts this CheckMacPermResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.CheckMacPermResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CheckMacPermResponse.prototype.toJSON = function() {
            return CheckMacPermResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CheckMacPermResponse
         * @function getTypeUrl
         * @memberof lnrpc.CheckMacPermResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CheckMacPermResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.CheckMacPermResponse";
        };

        return CheckMacPermResponse;
    })();

    lnrpc.RPCMiddlewareRequest = (function() {

        /**
         * Properties of a RPCMiddlewareRequest.
         * @typedef {Object} lnrpc.RPCMiddlewareRequest.$Properties
         * @property {Long|null} [request_id] RPCMiddlewareRequest request_id
         * @property {Uint8Array|null} [raw_macaroon] RPCMiddlewareRequest raw_macaroon
         * @property {string|null} [custom_caveat_condition] RPCMiddlewareRequest custom_caveat_condition
         * @property {lnrpc.StreamAuth.$Properties|null} [stream_auth] RPCMiddlewareRequest stream_auth
         * @property {lnrpc.RPCMessage.$Properties|null} [request] RPCMiddlewareRequest request
         * @property {lnrpc.RPCMessage.$Properties|null} [response] RPCMiddlewareRequest response
         * @property {boolean|null} [reg_complete] RPCMiddlewareRequest reg_complete
         * @property {Long|null} [msg_id] RPCMiddlewareRequest msg_id
         * @property {Object.<string,lnrpc.MetadataValues.$Properties>|null} [metadata_pairs] RPCMiddlewareRequest metadata_pairs
         * @property {"stream_auth"|"request"|"response"|"reg_complete"} [intercept_type] RPCMiddlewareRequest intercept_type
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RPCMiddlewareRequest.
         * @memberof lnrpc
         * @interface IRPCMiddlewareRequest
         * @augments lnrpc.RPCMiddlewareRequest.$Properties
         * @deprecated Use lnrpc.RPCMiddlewareRequest.$Properties instead.
         */

        /**
         * Narrowed shape of a RPCMiddlewareRequest.
         * @typedef {{
         *   request_id?: Long|null;
         *   raw_macaroon?: Uint8Array|null;
         *   custom_caveat_condition?: string|null;
         *   stream_auth?: lnrpc.StreamAuth.$Shape|null;
         *   request?: lnrpc.RPCMessage.$Shape|null;
         *   response?: lnrpc.RPCMessage.$Shape|null;
         *   reg_complete?: boolean|null;
         *   msg_id?: Long|null;
         *   metadata_pairs?: Object.<string,lnrpc.MetadataValues.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ intercept_type?: undefined; stream_auth?: null; request?: null; response?: null; reg_complete?: null }|{ intercept_type?: "stream_auth"; stream_auth: lnrpc.StreamAuth.$Shape; request?: null; response?: null; reg_complete?: null }|{ intercept_type?: "request"; stream_auth?: null; request: lnrpc.RPCMessage.$Shape; response?: null; reg_complete?: null }|{ intercept_type?: "response"; stream_auth?: null; request?: null; response: lnrpc.RPCMessage.$Shape; reg_complete?: null }|{ intercept_type?: "reg_complete"; stream_auth?: null; request?: null; response?: null; reg_complete: boolean })
         * )} lnrpc.RPCMiddlewareRequest.$Shape
         */

        /**
         * Constructs a new RPCMiddlewareRequest.
         * @memberof lnrpc
         * @classdesc Represents a RPCMiddlewareRequest.
         * @constructor
         * @param {lnrpc.RPCMiddlewareRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RPCMiddlewareRequest = function (properties) {
            this.metadata_pairs = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RPCMiddlewareRequest request_id.
         * @member {Long} request_id
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        RPCMiddlewareRequest.prototype.request_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RPCMiddlewareRequest raw_macaroon.
         * @member {Uint8Array} raw_macaroon
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        RPCMiddlewareRequest.prototype.raw_macaroon = $util.newBuffer([]);

        /**
         * RPCMiddlewareRequest custom_caveat_condition.
         * @member {string} custom_caveat_condition
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        RPCMiddlewareRequest.prototype.custom_caveat_condition = "";

        /**
         * RPCMiddlewareRequest stream_auth.
         * @member {lnrpc.StreamAuth.$Properties|null|undefined} stream_auth
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        RPCMiddlewareRequest.prototype.stream_auth = null;

        /**
         * RPCMiddlewareRequest request.
         * @member {lnrpc.RPCMessage.$Properties|null|undefined} request
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        RPCMiddlewareRequest.prototype.request = null;

        /**
         * RPCMiddlewareRequest response.
         * @member {lnrpc.RPCMessage.$Properties|null|undefined} response
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        RPCMiddlewareRequest.prototype.response = null;

        /**
         * RPCMiddlewareRequest reg_complete.
         * @member {boolean|null|undefined} reg_complete
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        RPCMiddlewareRequest.prototype.reg_complete = null;

        /**
         * RPCMiddlewareRequest msg_id.
         * @member {Long} msg_id
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        RPCMiddlewareRequest.prototype.msg_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RPCMiddlewareRequest metadata_pairs.
         * @member {Object.<string,lnrpc.MetadataValues.$Properties>} metadata_pairs
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        RPCMiddlewareRequest.prototype.metadata_pairs = $util.emptyObject;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * RPCMiddlewareRequest intercept_type.
         * @member {"stream_auth"|"request"|"response"|"reg_complete"|undefined} intercept_type
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         */
        $Object.defineProperty(RPCMiddlewareRequest.prototype, "intercept_type", {
            get: $util.oneOfGetter($oneOfFields = ["stream_auth", "request", "response", "reg_complete"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new RPCMiddlewareRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.RPCMiddlewareRequest
         * @static
         * @param {lnrpc.RPCMiddlewareRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.RPCMiddlewareRequest} RPCMiddlewareRequest instance
         * @type {{
         *   (properties: lnrpc.RPCMiddlewareRequest.$Shape): lnrpc.RPCMiddlewareRequest & lnrpc.RPCMiddlewareRequest.$Shape;
         *   (properties?: lnrpc.RPCMiddlewareRequest.$Properties): lnrpc.RPCMiddlewareRequest;
         * }}
         */
        RPCMiddlewareRequest.create = function(properties) {
            return new RPCMiddlewareRequest(properties);
        };

        /**
         * Encodes the specified RPCMiddlewareRequest message. Does not implicitly {@link lnrpc.RPCMiddlewareRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.RPCMiddlewareRequest
         * @static
         * @param {lnrpc.RPCMiddlewareRequest.$Properties} message RPCMiddlewareRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RPCMiddlewareRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id") && (typeof message.request_id === "object" ? message.request_id.low || message.request_id.high : message.request_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.request_id);
            if (message.raw_macaroon != null && $Object.hasOwnProperty.call(message, "raw_macaroon") && message.raw_macaroon.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.raw_macaroon);
            if (message.custom_caveat_condition != null && $Object.hasOwnProperty.call(message, "custom_caveat_condition") && message.custom_caveat_condition !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.custom_caveat_condition);
            if (message.stream_auth != null && $Object.hasOwnProperty.call(message, "stream_auth"))
                $root.lnrpc.StreamAuth.encode(message.stream_auth, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.request != null && $Object.hasOwnProperty.call(message, "request"))
                $root.lnrpc.RPCMessage.encode(message.request, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.response != null && $Object.hasOwnProperty.call(message, "response"))
                $root.lnrpc.RPCMessage.encode(message.response, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.msg_id != null && $Object.hasOwnProperty.call(message, "msg_id") && (typeof message.msg_id === "object" ? message.msg_id.low || message.msg_id.high : message.msg_id !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.msg_id);
            if (message.reg_complete != null && $Object.hasOwnProperty.call(message, "reg_complete"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.reg_complete);
            if (message.metadata_pairs != null && $Object.hasOwnProperty.call(message, "metadata_pairs"))
                for (let keys = $Object.keys(message.metadata_pairs), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 9, wireType 2 =*/74).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.lnrpc.MetadataValues.encode(message.metadata_pairs[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RPCMiddlewareRequest message, length delimited. Does not implicitly {@link lnrpc.RPCMiddlewareRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.RPCMiddlewareRequest
         * @static
         * @param {lnrpc.RPCMiddlewareRequest.$Properties} message RPCMiddlewareRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RPCMiddlewareRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RPCMiddlewareRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.RPCMiddlewareRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.RPCMiddlewareRequest & lnrpc.RPCMiddlewareRequest.$Shape} RPCMiddlewareRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RPCMiddlewareRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.RPCMiddlewareRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.request_id = value;
                        else
                            delete message.request_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_macaroon = value;
                        else
                            delete message.raw_macaroon;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.custom_caveat_condition = value;
                        else
                            delete message.custom_caveat_condition;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.stream_auth = $root.lnrpc.StreamAuth.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stream_auth);
                        message.intercept_type = "stream_auth";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.request = $root.lnrpc.RPCMessage.decode(reader, reader.uint32(), $undefined, _depth + 1, message.request);
                        message.intercept_type = "request";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.response = $root.lnrpc.RPCMessage.decode(reader, reader.uint32(), $undefined, _depth + 1, message.response);
                        message.intercept_type = "response";
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        message.reg_complete = reader.bool();
                        message.intercept_type = "reg_complete";
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.msg_id = value;
                        else
                            delete message.msg_id;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (message.metadata_pairs === $util.emptyObject)
                            message.metadata_pairs = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.lnrpc.MetadataValues.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.metadata_pairs, key);
                        message.metadata_pairs[key] = value || new $root.lnrpc.MetadataValues();
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RPCMiddlewareRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.RPCMiddlewareRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.RPCMiddlewareRequest & lnrpc.RPCMiddlewareRequest.$Shape} RPCMiddlewareRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RPCMiddlewareRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RPCMiddlewareRequest message.
         * @function verify
         * @memberof lnrpc.RPCMiddlewareRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RPCMiddlewareRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                if (!$util.isInteger(message.request_id) && !(message.request_id && $util.isInteger(message.request_id.low) && $util.isInteger(message.request_id.high)))
                    return "request_id: integer|Long expected";
            if (message.raw_macaroon != null && $Object.hasOwnProperty.call(message, "raw_macaroon"))
                if (!(message.raw_macaroon && typeof message.raw_macaroon.length === "number" || $util.isString(message.raw_macaroon)))
                    return "raw_macaroon: buffer expected";
            if (message.custom_caveat_condition != null && $Object.hasOwnProperty.call(message, "custom_caveat_condition"))
                if (!$util.isString(message.custom_caveat_condition))
                    return "custom_caveat_condition: string expected";
            if (message.stream_auth != null && $Object.hasOwnProperty.call(message, "stream_auth")) {
                properties.intercept_type = 1;
                {
                    let error = $root.lnrpc.StreamAuth.verify(message.stream_auth, _depth + 1);
                    if (error)
                        return "stream_auth." + error;
                }
            }
            if (message.request != null && $Object.hasOwnProperty.call(message, "request")) {
                if (properties.intercept_type === 1)
                    return "intercept_type: multiple values";
                properties.intercept_type = 1;
                {
                    let error = $root.lnrpc.RPCMessage.verify(message.request, _depth + 1);
                    if (error)
                        return "request." + error;
                }
            }
            if (message.response != null && $Object.hasOwnProperty.call(message, "response")) {
                if (properties.intercept_type === 1)
                    return "intercept_type: multiple values";
                properties.intercept_type = 1;
                {
                    let error = $root.lnrpc.RPCMessage.verify(message.response, _depth + 1);
                    if (error)
                        return "response." + error;
                }
            }
            if (message.reg_complete != null && $Object.hasOwnProperty.call(message, "reg_complete")) {
                if (properties.intercept_type === 1)
                    return "intercept_type: multiple values";
                properties.intercept_type = 1;
                if (typeof message.reg_complete !== "boolean")
                    return "reg_complete: boolean expected";
            }
            if (message.msg_id != null && $Object.hasOwnProperty.call(message, "msg_id"))
                if (!$util.isInteger(message.msg_id) && !(message.msg_id && $util.isInteger(message.msg_id.low) && $util.isInteger(message.msg_id.high)))
                    return "msg_id: integer|Long expected";
            if (message.metadata_pairs != null && $Object.hasOwnProperty.call(message, "metadata_pairs")) {
                if (!$util.isObject(message.metadata_pairs))
                    return "metadata_pairs: object expected";
                let key = $Object.keys(message.metadata_pairs);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.lnrpc.MetadataValues.verify(message.metadata_pairs[key[i]], _depth + 1);
                    if (error)
                        return "metadata_pairs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RPCMiddlewareRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.RPCMiddlewareRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.RPCMiddlewareRequest} RPCMiddlewareRequest
         */
        RPCMiddlewareRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.RPCMiddlewareRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.RPCMiddlewareRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.RPCMiddlewareRequest();
            if (object.request_id != null)
                if (typeof object.request_id === "object" ? object.request_id.low || object.request_id.high : $Number(object.request_id) !== 0)
                    if ($util.Long)
                        message.request_id = $util.Long.fromValue(object.request_id, true);
                    else if (typeof object.request_id === "string")
                        message.request_id = $parseInt(object.request_id, 10);
                    else if (typeof object.request_id === "number")
                        message.request_id = object.request_id;
                    else if (typeof object.request_id === "object")
                        message.request_id = new $util.LongBits(object.request_id.low >>> 0, object.request_id.high >>> 0).toNumber(true);
            if (object.raw_macaroon != null)
                if (object.raw_macaroon.length)
                    if (typeof object.raw_macaroon === "string")
                        $util.base64.decode(object.raw_macaroon, message.raw_macaroon = $util.newBuffer($util.base64.length(object.raw_macaroon)), 0);
                    else if (object.raw_macaroon.length >= 0)
                        message.raw_macaroon = object.raw_macaroon;
            if (object.custom_caveat_condition != null)
                if (typeof object.custom_caveat_condition !== "string" || object.custom_caveat_condition.length)
                    message.custom_caveat_condition = $String(object.custom_caveat_condition);
            if (object.stream_auth != null) {
                if (!$util.isObject(object.stream_auth))
                    throw $TypeError(".lnrpc.RPCMiddlewareRequest.stream_auth: object expected");
                message.stream_auth = $root.lnrpc.StreamAuth.fromObject(object.stream_auth, _depth + 1);
            }
            if (object.request != null) {
                if (!$util.isObject(object.request))
                    throw $TypeError(".lnrpc.RPCMiddlewareRequest.request: object expected");
                message.request = $root.lnrpc.RPCMessage.fromObject(object.request, _depth + 1);
            }
            if (object.response != null) {
                if (!$util.isObject(object.response))
                    throw $TypeError(".lnrpc.RPCMiddlewareRequest.response: object expected");
                message.response = $root.lnrpc.RPCMessage.fromObject(object.response, _depth + 1);
            }
            if (object.reg_complete != null)
                message.reg_complete = $Boolean(object.reg_complete);
            if (object.msg_id != null)
                if (typeof object.msg_id === "object" ? object.msg_id.low || object.msg_id.high : $Number(object.msg_id) !== 0)
                    if ($util.Long)
                        message.msg_id = $util.Long.fromValue(object.msg_id, true);
                    else if (typeof object.msg_id === "string")
                        message.msg_id = $parseInt(object.msg_id, 10);
                    else if (typeof object.msg_id === "number")
                        message.msg_id = object.msg_id;
                    else if (typeof object.msg_id === "object")
                        message.msg_id = new $util.LongBits(object.msg_id.low >>> 0, object.msg_id.high >>> 0).toNumber(true);
            if (object.metadata_pairs) {
                if (!$util.isObject(object.metadata_pairs))
                    throw $TypeError(".lnrpc.RPCMiddlewareRequest.metadata_pairs: object expected");
                message.metadata_pairs = {};
                for (let keys = $Object.keys(object.metadata_pairs), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.metadata_pairs, keys[i]);
                    if (!$util.isObject(object.metadata_pairs[keys[i]]))
                        throw $TypeError(".lnrpc.RPCMiddlewareRequest.metadata_pairs: object expected");
                    message.metadata_pairs[keys[i]] = $root.lnrpc.MetadataValues.fromObject(object.metadata_pairs[keys[i]], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RPCMiddlewareRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.RPCMiddlewareRequest
         * @static
         * @param {lnrpc.RPCMiddlewareRequest} message RPCMiddlewareRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RPCMiddlewareRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.metadata_pairs = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.request_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.request_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.raw_macaroon = "";
                else {
                    object.raw_macaroon = [];
                    if (options.bytes !== $Array)
                        object.raw_macaroon = $util.newBuffer(object.raw_macaroon);
                }
                object.custom_caveat_condition = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.msg_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.msg_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.request_id = typeof message.request_id === "number" ? $BigInt(message.request_id) : $util.Long.fromBits(message.request_id.low >>> 0, message.request_id.high >>> 0, true).toBigInt();
                else if (typeof message.request_id === "number")
                    object.request_id = options.longs === $String ? $String(message.request_id) : message.request_id;
                else
                    object.request_id = options.longs === $String ? $util.Long.prototype.toString.call(message.request_id) : options.longs === $Number ? new $util.LongBits(message.request_id.low >>> 0, message.request_id.high >>> 0).toNumber(true) : message.request_id;
            if (message.raw_macaroon != null && $Object.hasOwnProperty.call(message, "raw_macaroon"))
                object.raw_macaroon = options.bytes === $String ? $util.base64.encode(message.raw_macaroon, 0, message.raw_macaroon.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_macaroon) : message.raw_macaroon;
            if (message.custom_caveat_condition != null && $Object.hasOwnProperty.call(message, "custom_caveat_condition"))
                object.custom_caveat_condition = message.custom_caveat_condition;
            if (message.stream_auth != null && $Object.hasOwnProperty.call(message, "stream_auth")) {
                object.stream_auth = $root.lnrpc.StreamAuth.toObject(message.stream_auth, options, _depth + 1);
                if (options.oneofs)
                    object.intercept_type = "stream_auth";
            }
            if (message.request != null && $Object.hasOwnProperty.call(message, "request")) {
                object.request = $root.lnrpc.RPCMessage.toObject(message.request, options, _depth + 1);
                if (options.oneofs)
                    object.intercept_type = "request";
            }
            if (message.response != null && $Object.hasOwnProperty.call(message, "response")) {
                object.response = $root.lnrpc.RPCMessage.toObject(message.response, options, _depth + 1);
                if (options.oneofs)
                    object.intercept_type = "response";
            }
            if (message.msg_id != null && $Object.hasOwnProperty.call(message, "msg_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.msg_id = typeof message.msg_id === "number" ? $BigInt(message.msg_id) : $util.Long.fromBits(message.msg_id.low >>> 0, message.msg_id.high >>> 0, true).toBigInt();
                else if (typeof message.msg_id === "number")
                    object.msg_id = options.longs === $String ? $String(message.msg_id) : message.msg_id;
                else
                    object.msg_id = options.longs === $String ? $util.Long.prototype.toString.call(message.msg_id) : options.longs === $Number ? new $util.LongBits(message.msg_id.low >>> 0, message.msg_id.high >>> 0).toNumber(true) : message.msg_id;
            if (message.reg_complete != null && $Object.hasOwnProperty.call(message, "reg_complete")) {
                object.reg_complete = message.reg_complete;
                if (options.oneofs)
                    object.intercept_type = "reg_complete";
            }
            let keys2;
            if (message.metadata_pairs && (keys2 = $Object.keys(message.metadata_pairs)).length) {
                object.metadata_pairs = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.metadata_pairs, keys2[j]);
                    object.metadata_pairs[keys2[j]] = $root.lnrpc.MetadataValues.toObject(message.metadata_pairs[keys2[j]], options, _depth + 1);
                }
            }
            return object;
        };

        /**
         * Converts this RPCMiddlewareRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.RPCMiddlewareRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RPCMiddlewareRequest.prototype.toJSON = function() {
            return RPCMiddlewareRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RPCMiddlewareRequest
         * @function getTypeUrl
         * @memberof lnrpc.RPCMiddlewareRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RPCMiddlewareRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.RPCMiddlewareRequest";
        };

        return RPCMiddlewareRequest;
    })();

    lnrpc.MetadataValues = (function() {

        /**
         * Properties of a MetadataValues.
         * @typedef {Object} lnrpc.MetadataValues.$Properties
         * @property {Array.<string>|null} [values] MetadataValues values
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MetadataValues.
         * @memberof lnrpc
         * @interface IMetadataValues
         * @augments lnrpc.MetadataValues.$Properties
         * @deprecated Use lnrpc.MetadataValues.$Properties instead.
         */

        /**
         * Shape of a MetadataValues.
         * @typedef {lnrpc.MetadataValues.$Properties} lnrpc.MetadataValues.$Shape
         */

        /**
         * Constructs a new MetadataValues.
         * @memberof lnrpc
         * @classdesc Represents a MetadataValues.
         * @constructor
         * @param {lnrpc.MetadataValues.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MetadataValues = function (properties) {
            this.values = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MetadataValues values.
         * @member {Array.<string>} values
         * @memberof lnrpc.MetadataValues
         * @instance
         */
        MetadataValues.prototype.values = $util.emptyArray;

        /**
         * Creates a new MetadataValues instance using the specified properties.
         * @function create
         * @memberof lnrpc.MetadataValues
         * @static
         * @param {lnrpc.MetadataValues.$Properties=} [properties] Properties to set
         * @returns {lnrpc.MetadataValues} MetadataValues instance
         * @type {{
         *   (properties: lnrpc.MetadataValues.$Shape): lnrpc.MetadataValues & lnrpc.MetadataValues.$Shape;
         *   (properties?: lnrpc.MetadataValues.$Properties): lnrpc.MetadataValues;
         * }}
         */
        MetadataValues.create = function(properties) {
            return new MetadataValues(properties);
        };

        /**
         * Encodes the specified MetadataValues message. Does not implicitly {@link lnrpc.MetadataValues.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.MetadataValues
         * @static
         * @param {lnrpc.MetadataValues.$Properties} message MetadataValues message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MetadataValues.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.values != null && message.values.length)
                for (let i = 0; i < message.values.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.values[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MetadataValues message, length delimited. Does not implicitly {@link lnrpc.MetadataValues.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.MetadataValues
         * @static
         * @param {lnrpc.MetadataValues.$Properties} message MetadataValues message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MetadataValues.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MetadataValues message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.MetadataValues
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.MetadataValues & lnrpc.MetadataValues.$Shape} MetadataValues
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MetadataValues.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.MetadataValues();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.values && message.values.length))
                            message.values = [];
                        message.values.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MetadataValues message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.MetadataValues
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.MetadataValues & lnrpc.MetadataValues.$Shape} MetadataValues
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MetadataValues.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MetadataValues message.
         * @function verify
         * @memberof lnrpc.MetadataValues
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MetadataValues.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                if (!$Array.isArray(message.values))
                    return "values: array expected";
                for (let i = 0; i < message.values.length; ++i)
                    if (!$util.isString(message.values[i]))
                        return "values: string[] expected";
            }
            return null;
        };

        /**
         * Creates a MetadataValues message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.MetadataValues
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.MetadataValues} MetadataValues
         */
        MetadataValues.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.MetadataValues)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.MetadataValues: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.MetadataValues();
            if (object.values) {
                if (!$Array.isArray(object.values))
                    throw $TypeError(".lnrpc.MetadataValues.values: array expected");
                message.values = $Array(object.values.length);
                for (let i = 0; i < object.values.length; ++i)
                    message.values[i] = $String(object.values[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a MetadataValues message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.MetadataValues
         * @static
         * @param {lnrpc.MetadataValues} message MetadataValues
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MetadataValues.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.values = [];
            if (message.values && message.values.length) {
                object.values = $Array(message.values.length);
                for (let j = 0; j < message.values.length; ++j)
                    object.values[j] = message.values[j];
            }
            return object;
        };

        /**
         * Converts this MetadataValues to JSON.
         * @function toJSON
         * @memberof lnrpc.MetadataValues
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MetadataValues.prototype.toJSON = function() {
            return MetadataValues.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MetadataValues
         * @function getTypeUrl
         * @memberof lnrpc.MetadataValues
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MetadataValues.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.MetadataValues";
        };

        return MetadataValues;
    })();

    lnrpc.StreamAuth = (function() {

        /**
         * Properties of a StreamAuth.
         * @typedef {Object} lnrpc.StreamAuth.$Properties
         * @property {string|null} [method_full_uri] StreamAuth method_full_uri
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a StreamAuth.
         * @memberof lnrpc
         * @interface IStreamAuth
         * @augments lnrpc.StreamAuth.$Properties
         * @deprecated Use lnrpc.StreamAuth.$Properties instead.
         */

        /**
         * Shape of a StreamAuth.
         * @typedef {lnrpc.StreamAuth.$Properties} lnrpc.StreamAuth.$Shape
         */

        /**
         * Constructs a new StreamAuth.
         * @memberof lnrpc
         * @classdesc Represents a StreamAuth.
         * @constructor
         * @param {lnrpc.StreamAuth.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const StreamAuth = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StreamAuth method_full_uri.
         * @member {string} method_full_uri
         * @memberof lnrpc.StreamAuth
         * @instance
         */
        StreamAuth.prototype.method_full_uri = "";

        /**
         * Creates a new StreamAuth instance using the specified properties.
         * @function create
         * @memberof lnrpc.StreamAuth
         * @static
         * @param {lnrpc.StreamAuth.$Properties=} [properties] Properties to set
         * @returns {lnrpc.StreamAuth} StreamAuth instance
         * @type {{
         *   (properties: lnrpc.StreamAuth.$Shape): lnrpc.StreamAuth & lnrpc.StreamAuth.$Shape;
         *   (properties?: lnrpc.StreamAuth.$Properties): lnrpc.StreamAuth;
         * }}
         */
        StreamAuth.create = function(properties) {
            return new StreamAuth(properties);
        };

        /**
         * Encodes the specified StreamAuth message. Does not implicitly {@link lnrpc.StreamAuth.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.StreamAuth
         * @static
         * @param {lnrpc.StreamAuth.$Properties} message StreamAuth message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StreamAuth.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.method_full_uri != null && $Object.hasOwnProperty.call(message, "method_full_uri") && message.method_full_uri !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.method_full_uri);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StreamAuth message, length delimited. Does not implicitly {@link lnrpc.StreamAuth.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.StreamAuth
         * @static
         * @param {lnrpc.StreamAuth.$Properties} message StreamAuth message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StreamAuth.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StreamAuth message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.StreamAuth
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.StreamAuth & lnrpc.StreamAuth.$Shape} StreamAuth
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StreamAuth.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.StreamAuth(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.method_full_uri = value;
                        else
                            delete message.method_full_uri;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StreamAuth message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.StreamAuth
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.StreamAuth & lnrpc.StreamAuth.$Shape} StreamAuth
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StreamAuth.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StreamAuth message.
         * @function verify
         * @memberof lnrpc.StreamAuth
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StreamAuth.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.method_full_uri != null && $Object.hasOwnProperty.call(message, "method_full_uri"))
                if (!$util.isString(message.method_full_uri))
                    return "method_full_uri: string expected";
            return null;
        };

        /**
         * Creates a StreamAuth message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.StreamAuth
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.StreamAuth} StreamAuth
         */
        StreamAuth.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.StreamAuth)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.StreamAuth: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.StreamAuth();
            if (object.method_full_uri != null)
                if (typeof object.method_full_uri !== "string" || object.method_full_uri.length)
                    message.method_full_uri = $String(object.method_full_uri);
            return message;
        };

        /**
         * Creates a plain object from a StreamAuth message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.StreamAuth
         * @static
         * @param {lnrpc.StreamAuth} message StreamAuth
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StreamAuth.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.method_full_uri = "";
            if (message.method_full_uri != null && $Object.hasOwnProperty.call(message, "method_full_uri"))
                object.method_full_uri = message.method_full_uri;
            return object;
        };

        /**
         * Converts this StreamAuth to JSON.
         * @function toJSON
         * @memberof lnrpc.StreamAuth
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StreamAuth.prototype.toJSON = function() {
            return StreamAuth.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StreamAuth
         * @function getTypeUrl
         * @memberof lnrpc.StreamAuth
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StreamAuth.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.StreamAuth";
        };

        return StreamAuth;
    })();

    lnrpc.RPCMessage = (function() {

        /**
         * Properties of a RPCMessage.
         * @typedef {Object} lnrpc.RPCMessage.$Properties
         * @property {string|null} [method_full_uri] RPCMessage method_full_uri
         * @property {boolean|null} [stream_rpc] RPCMessage stream_rpc
         * @property {string|null} [type_name] RPCMessage type_name
         * @property {Uint8Array|null} [serialized] RPCMessage serialized
         * @property {boolean|null} [is_error] RPCMessage is_error
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RPCMessage.
         * @memberof lnrpc
         * @interface IRPCMessage
         * @augments lnrpc.RPCMessage.$Properties
         * @deprecated Use lnrpc.RPCMessage.$Properties instead.
         */

        /**
         * Shape of a RPCMessage.
         * @typedef {lnrpc.RPCMessage.$Properties} lnrpc.RPCMessage.$Shape
         */

        /**
         * Constructs a new RPCMessage.
         * @memberof lnrpc
         * @classdesc Represents a RPCMessage.
         * @constructor
         * @param {lnrpc.RPCMessage.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RPCMessage = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RPCMessage method_full_uri.
         * @member {string} method_full_uri
         * @memberof lnrpc.RPCMessage
         * @instance
         */
        RPCMessage.prototype.method_full_uri = "";

        /**
         * RPCMessage stream_rpc.
         * @member {boolean} stream_rpc
         * @memberof lnrpc.RPCMessage
         * @instance
         */
        RPCMessage.prototype.stream_rpc = false;

        /**
         * RPCMessage type_name.
         * @member {string} type_name
         * @memberof lnrpc.RPCMessage
         * @instance
         */
        RPCMessage.prototype.type_name = "";

        /**
         * RPCMessage serialized.
         * @member {Uint8Array} serialized
         * @memberof lnrpc.RPCMessage
         * @instance
         */
        RPCMessage.prototype.serialized = $util.newBuffer([]);

        /**
         * RPCMessage is_error.
         * @member {boolean} is_error
         * @memberof lnrpc.RPCMessage
         * @instance
         */
        RPCMessage.prototype.is_error = false;

        /**
         * Creates a new RPCMessage instance using the specified properties.
         * @function create
         * @memberof lnrpc.RPCMessage
         * @static
         * @param {lnrpc.RPCMessage.$Properties=} [properties] Properties to set
         * @returns {lnrpc.RPCMessage} RPCMessage instance
         * @type {{
         *   (properties: lnrpc.RPCMessage.$Shape): lnrpc.RPCMessage & lnrpc.RPCMessage.$Shape;
         *   (properties?: lnrpc.RPCMessage.$Properties): lnrpc.RPCMessage;
         * }}
         */
        RPCMessage.create = function(properties) {
            return new RPCMessage(properties);
        };

        /**
         * Encodes the specified RPCMessage message. Does not implicitly {@link lnrpc.RPCMessage.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.RPCMessage
         * @static
         * @param {lnrpc.RPCMessage.$Properties} message RPCMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RPCMessage.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.method_full_uri != null && $Object.hasOwnProperty.call(message, "method_full_uri") && message.method_full_uri !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.method_full_uri);
            if (message.stream_rpc != null && $Object.hasOwnProperty.call(message, "stream_rpc") && message.stream_rpc !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.stream_rpc);
            if (message.type_name != null && $Object.hasOwnProperty.call(message, "type_name") && message.type_name !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.type_name);
            if (message.serialized != null && $Object.hasOwnProperty.call(message, "serialized") && message.serialized.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.serialized);
            if (message.is_error != null && $Object.hasOwnProperty.call(message, "is_error") && message.is_error !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.is_error);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RPCMessage message, length delimited. Does not implicitly {@link lnrpc.RPCMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.RPCMessage
         * @static
         * @param {lnrpc.RPCMessage.$Properties} message RPCMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RPCMessage.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RPCMessage message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.RPCMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.RPCMessage & lnrpc.RPCMessage.$Shape} RPCMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RPCMessage.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.RPCMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.method_full_uri = value;
                        else
                            delete message.method_full_uri;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.stream_rpc = value;
                        else
                            delete message.stream_rpc;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.type_name = value;
                        else
                            delete message.type_name;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.serialized = value;
                        else
                            delete message.serialized;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_error = value;
                        else
                            delete message.is_error;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RPCMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.RPCMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.RPCMessage & lnrpc.RPCMessage.$Shape} RPCMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RPCMessage.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RPCMessage message.
         * @function verify
         * @memberof lnrpc.RPCMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RPCMessage.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.method_full_uri != null && $Object.hasOwnProperty.call(message, "method_full_uri"))
                if (!$util.isString(message.method_full_uri))
                    return "method_full_uri: string expected";
            if (message.stream_rpc != null && $Object.hasOwnProperty.call(message, "stream_rpc"))
                if (typeof message.stream_rpc !== "boolean")
                    return "stream_rpc: boolean expected";
            if (message.type_name != null && $Object.hasOwnProperty.call(message, "type_name"))
                if (!$util.isString(message.type_name))
                    return "type_name: string expected";
            if (message.serialized != null && $Object.hasOwnProperty.call(message, "serialized"))
                if (!(message.serialized && typeof message.serialized.length === "number" || $util.isString(message.serialized)))
                    return "serialized: buffer expected";
            if (message.is_error != null && $Object.hasOwnProperty.call(message, "is_error"))
                if (typeof message.is_error !== "boolean")
                    return "is_error: boolean expected";
            return null;
        };

        /**
         * Creates a RPCMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.RPCMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.RPCMessage} RPCMessage
         */
        RPCMessage.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.RPCMessage)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.RPCMessage: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.RPCMessage();
            if (object.method_full_uri != null)
                if (typeof object.method_full_uri !== "string" || object.method_full_uri.length)
                    message.method_full_uri = $String(object.method_full_uri);
            if (object.stream_rpc != null)
                if (object.stream_rpc)
                    message.stream_rpc = $Boolean(object.stream_rpc);
            if (object.type_name != null)
                if (typeof object.type_name !== "string" || object.type_name.length)
                    message.type_name = $String(object.type_name);
            if (object.serialized != null)
                if (object.serialized.length)
                    if (typeof object.serialized === "string")
                        $util.base64.decode(object.serialized, message.serialized = $util.newBuffer($util.base64.length(object.serialized)), 0);
                    else if (object.serialized.length >= 0)
                        message.serialized = object.serialized;
            if (object.is_error != null)
                if (object.is_error)
                    message.is_error = $Boolean(object.is_error);
            return message;
        };

        /**
         * Creates a plain object from a RPCMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.RPCMessage
         * @static
         * @param {lnrpc.RPCMessage} message RPCMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RPCMessage.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.method_full_uri = "";
                object.stream_rpc = false;
                object.type_name = "";
                if (options.bytes === $String)
                    object.serialized = "";
                else {
                    object.serialized = [];
                    if (options.bytes !== $Array)
                        object.serialized = $util.newBuffer(object.serialized);
                }
                object.is_error = false;
            }
            if (message.method_full_uri != null && $Object.hasOwnProperty.call(message, "method_full_uri"))
                object.method_full_uri = message.method_full_uri;
            if (message.stream_rpc != null && $Object.hasOwnProperty.call(message, "stream_rpc"))
                object.stream_rpc = message.stream_rpc;
            if (message.type_name != null && $Object.hasOwnProperty.call(message, "type_name"))
                object.type_name = message.type_name;
            if (message.serialized != null && $Object.hasOwnProperty.call(message, "serialized"))
                object.serialized = options.bytes === $String ? $util.base64.encode(message.serialized, 0, message.serialized.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.serialized) : message.serialized;
            if (message.is_error != null && $Object.hasOwnProperty.call(message, "is_error"))
                object.is_error = message.is_error;
            return object;
        };

        /**
         * Converts this RPCMessage to JSON.
         * @function toJSON
         * @memberof lnrpc.RPCMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RPCMessage.prototype.toJSON = function() {
            return RPCMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RPCMessage
         * @function getTypeUrl
         * @memberof lnrpc.RPCMessage
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RPCMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.RPCMessage";
        };

        return RPCMessage;
    })();

    lnrpc.RPCMiddlewareResponse = (function() {

        /**
         * Properties of a RPCMiddlewareResponse.
         * @typedef {Object} lnrpc.RPCMiddlewareResponse.$Properties
         * @property {Long|null} [ref_msg_id] RPCMiddlewareResponse ref_msg_id
         * @property {lnrpc.MiddlewareRegistration.$Properties|null} [register] RPCMiddlewareResponse register
         * @property {lnrpc.InterceptFeedback.$Properties|null} [feedback] RPCMiddlewareResponse feedback
         * @property {"register"|"feedback"} [middleware_message] RPCMiddlewareResponse middleware_message
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RPCMiddlewareResponse.
         * @memberof lnrpc
         * @interface IRPCMiddlewareResponse
         * @augments lnrpc.RPCMiddlewareResponse.$Properties
         * @deprecated Use lnrpc.RPCMiddlewareResponse.$Properties instead.
         */

        /**
         * Narrowed shape of a RPCMiddlewareResponse.
         * @typedef {{
         *   ref_msg_id?: Long|null;
         *   register?: lnrpc.MiddlewareRegistration.$Shape|null;
         *   feedback?: lnrpc.InterceptFeedback.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ middleware_message?: undefined; register?: null; feedback?: null }|{ middleware_message?: "register"; register: lnrpc.MiddlewareRegistration.$Shape; feedback?: null }|{ middleware_message?: "feedback"; register?: null; feedback: lnrpc.InterceptFeedback.$Shape })
         * )} lnrpc.RPCMiddlewareResponse.$Shape
         */

        /**
         * Constructs a new RPCMiddlewareResponse.
         * @memberof lnrpc
         * @classdesc Represents a RPCMiddlewareResponse.
         * @constructor
         * @param {lnrpc.RPCMiddlewareResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RPCMiddlewareResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RPCMiddlewareResponse ref_msg_id.
         * @member {Long} ref_msg_id
         * @memberof lnrpc.RPCMiddlewareResponse
         * @instance
         */
        RPCMiddlewareResponse.prototype.ref_msg_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RPCMiddlewareResponse register.
         * @member {lnrpc.MiddlewareRegistration.$Properties|null|undefined} register
         * @memberof lnrpc.RPCMiddlewareResponse
         * @instance
         */
        RPCMiddlewareResponse.prototype.register = null;

        /**
         * RPCMiddlewareResponse feedback.
         * @member {lnrpc.InterceptFeedback.$Properties|null|undefined} feedback
         * @memberof lnrpc.RPCMiddlewareResponse
         * @instance
         */
        RPCMiddlewareResponse.prototype.feedback = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * RPCMiddlewareResponse middleware_message.
         * @member {"register"|"feedback"|undefined} middleware_message
         * @memberof lnrpc.RPCMiddlewareResponse
         * @instance
         */
        $Object.defineProperty(RPCMiddlewareResponse.prototype, "middleware_message", {
            get: $util.oneOfGetter($oneOfFields = ["register", "feedback"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new RPCMiddlewareResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.RPCMiddlewareResponse
         * @static
         * @param {lnrpc.RPCMiddlewareResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.RPCMiddlewareResponse} RPCMiddlewareResponse instance
         * @type {{
         *   (properties: lnrpc.RPCMiddlewareResponse.$Shape): lnrpc.RPCMiddlewareResponse & lnrpc.RPCMiddlewareResponse.$Shape;
         *   (properties?: lnrpc.RPCMiddlewareResponse.$Properties): lnrpc.RPCMiddlewareResponse;
         * }}
         */
        RPCMiddlewareResponse.create = function(properties) {
            return new RPCMiddlewareResponse(properties);
        };

        /**
         * Encodes the specified RPCMiddlewareResponse message. Does not implicitly {@link lnrpc.RPCMiddlewareResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.RPCMiddlewareResponse
         * @static
         * @param {lnrpc.RPCMiddlewareResponse.$Properties} message RPCMiddlewareResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RPCMiddlewareResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.ref_msg_id != null && $Object.hasOwnProperty.call(message, "ref_msg_id") && (typeof message.ref_msg_id === "object" ? message.ref_msg_id.low || message.ref_msg_id.high : message.ref_msg_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.ref_msg_id);
            if (message.register != null && $Object.hasOwnProperty.call(message, "register"))
                $root.lnrpc.MiddlewareRegistration.encode(message.register, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.feedback != null && $Object.hasOwnProperty.call(message, "feedback"))
                $root.lnrpc.InterceptFeedback.encode(message.feedback, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RPCMiddlewareResponse message, length delimited. Does not implicitly {@link lnrpc.RPCMiddlewareResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.RPCMiddlewareResponse
         * @static
         * @param {lnrpc.RPCMiddlewareResponse.$Properties} message RPCMiddlewareResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RPCMiddlewareResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RPCMiddlewareResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.RPCMiddlewareResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.RPCMiddlewareResponse & lnrpc.RPCMiddlewareResponse.$Shape} RPCMiddlewareResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RPCMiddlewareResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.RPCMiddlewareResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.ref_msg_id = value;
                        else
                            delete message.ref_msg_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.register = $root.lnrpc.MiddlewareRegistration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.register);
                        message.middleware_message = "register";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.feedback = $root.lnrpc.InterceptFeedback.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feedback);
                        message.middleware_message = "feedback";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RPCMiddlewareResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.RPCMiddlewareResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.RPCMiddlewareResponse & lnrpc.RPCMiddlewareResponse.$Shape} RPCMiddlewareResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RPCMiddlewareResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RPCMiddlewareResponse message.
         * @function verify
         * @memberof lnrpc.RPCMiddlewareResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RPCMiddlewareResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.ref_msg_id != null && $Object.hasOwnProperty.call(message, "ref_msg_id"))
                if (!$util.isInteger(message.ref_msg_id) && !(message.ref_msg_id && $util.isInteger(message.ref_msg_id.low) && $util.isInteger(message.ref_msg_id.high)))
                    return "ref_msg_id: integer|Long expected";
            if (message.register != null && $Object.hasOwnProperty.call(message, "register")) {
                properties.middleware_message = 1;
                {
                    let error = $root.lnrpc.MiddlewareRegistration.verify(message.register, _depth + 1);
                    if (error)
                        return "register." + error;
                }
            }
            if (message.feedback != null && $Object.hasOwnProperty.call(message, "feedback")) {
                if (properties.middleware_message === 1)
                    return "middleware_message: multiple values";
                properties.middleware_message = 1;
                {
                    let error = $root.lnrpc.InterceptFeedback.verify(message.feedback, _depth + 1);
                    if (error)
                        return "feedback." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RPCMiddlewareResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.RPCMiddlewareResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.RPCMiddlewareResponse} RPCMiddlewareResponse
         */
        RPCMiddlewareResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.RPCMiddlewareResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.RPCMiddlewareResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.RPCMiddlewareResponse();
            if (object.ref_msg_id != null)
                if (typeof object.ref_msg_id === "object" ? object.ref_msg_id.low || object.ref_msg_id.high : $Number(object.ref_msg_id) !== 0)
                    if ($util.Long)
                        message.ref_msg_id = $util.Long.fromValue(object.ref_msg_id, true);
                    else if (typeof object.ref_msg_id === "string")
                        message.ref_msg_id = $parseInt(object.ref_msg_id, 10);
                    else if (typeof object.ref_msg_id === "number")
                        message.ref_msg_id = object.ref_msg_id;
                    else if (typeof object.ref_msg_id === "object")
                        message.ref_msg_id = new $util.LongBits(object.ref_msg_id.low >>> 0, object.ref_msg_id.high >>> 0).toNumber(true);
            if (object.register != null) {
                if (!$util.isObject(object.register))
                    throw $TypeError(".lnrpc.RPCMiddlewareResponse.register: object expected");
                message.register = $root.lnrpc.MiddlewareRegistration.fromObject(object.register, _depth + 1);
            }
            if (object.feedback != null) {
                if (!$util.isObject(object.feedback))
                    throw $TypeError(".lnrpc.RPCMiddlewareResponse.feedback: object expected");
                message.feedback = $root.lnrpc.InterceptFeedback.fromObject(object.feedback, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RPCMiddlewareResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.RPCMiddlewareResponse
         * @static
         * @param {lnrpc.RPCMiddlewareResponse} message RPCMiddlewareResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RPCMiddlewareResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.ref_msg_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.ref_msg_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.ref_msg_id != null && $Object.hasOwnProperty.call(message, "ref_msg_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.ref_msg_id = typeof message.ref_msg_id === "number" ? $BigInt(message.ref_msg_id) : $util.Long.fromBits(message.ref_msg_id.low >>> 0, message.ref_msg_id.high >>> 0, true).toBigInt();
                else if (typeof message.ref_msg_id === "number")
                    object.ref_msg_id = options.longs === $String ? $String(message.ref_msg_id) : message.ref_msg_id;
                else
                    object.ref_msg_id = options.longs === $String ? $util.Long.prototype.toString.call(message.ref_msg_id) : options.longs === $Number ? new $util.LongBits(message.ref_msg_id.low >>> 0, message.ref_msg_id.high >>> 0).toNumber(true) : message.ref_msg_id;
            if (message.register != null && $Object.hasOwnProperty.call(message, "register")) {
                object.register = $root.lnrpc.MiddlewareRegistration.toObject(message.register, options, _depth + 1);
                if (options.oneofs)
                    object.middleware_message = "register";
            }
            if (message.feedback != null && $Object.hasOwnProperty.call(message, "feedback")) {
                object.feedback = $root.lnrpc.InterceptFeedback.toObject(message.feedback, options, _depth + 1);
                if (options.oneofs)
                    object.middleware_message = "feedback";
            }
            return object;
        };

        /**
         * Converts this RPCMiddlewareResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.RPCMiddlewareResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RPCMiddlewareResponse.prototype.toJSON = function() {
            return RPCMiddlewareResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RPCMiddlewareResponse
         * @function getTypeUrl
         * @memberof lnrpc.RPCMiddlewareResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RPCMiddlewareResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.RPCMiddlewareResponse";
        };

        return RPCMiddlewareResponse;
    })();

    lnrpc.MiddlewareRegistration = (function() {

        /**
         * Properties of a MiddlewareRegistration.
         * @typedef {Object} lnrpc.MiddlewareRegistration.$Properties
         * @property {string|null} [middleware_name] MiddlewareRegistration middleware_name
         * @property {string|null} [custom_macaroon_caveat_name] MiddlewareRegistration custom_macaroon_caveat_name
         * @property {boolean|null} [read_only_mode] MiddlewareRegistration read_only_mode
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MiddlewareRegistration.
         * @memberof lnrpc
         * @interface IMiddlewareRegistration
         * @augments lnrpc.MiddlewareRegistration.$Properties
         * @deprecated Use lnrpc.MiddlewareRegistration.$Properties instead.
         */

        /**
         * Shape of a MiddlewareRegistration.
         * @typedef {lnrpc.MiddlewareRegistration.$Properties} lnrpc.MiddlewareRegistration.$Shape
         */

        /**
         * Constructs a new MiddlewareRegistration.
         * @memberof lnrpc
         * @classdesc Represents a MiddlewareRegistration.
         * @constructor
         * @param {lnrpc.MiddlewareRegistration.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MiddlewareRegistration = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MiddlewareRegistration middleware_name.
         * @member {string} middleware_name
         * @memberof lnrpc.MiddlewareRegistration
         * @instance
         */
        MiddlewareRegistration.prototype.middleware_name = "";

        /**
         * MiddlewareRegistration custom_macaroon_caveat_name.
         * @member {string} custom_macaroon_caveat_name
         * @memberof lnrpc.MiddlewareRegistration
         * @instance
         */
        MiddlewareRegistration.prototype.custom_macaroon_caveat_name = "";

        /**
         * MiddlewareRegistration read_only_mode.
         * @member {boolean} read_only_mode
         * @memberof lnrpc.MiddlewareRegistration
         * @instance
         */
        MiddlewareRegistration.prototype.read_only_mode = false;

        /**
         * Creates a new MiddlewareRegistration instance using the specified properties.
         * @function create
         * @memberof lnrpc.MiddlewareRegistration
         * @static
         * @param {lnrpc.MiddlewareRegistration.$Properties=} [properties] Properties to set
         * @returns {lnrpc.MiddlewareRegistration} MiddlewareRegistration instance
         * @type {{
         *   (properties: lnrpc.MiddlewareRegistration.$Shape): lnrpc.MiddlewareRegistration & lnrpc.MiddlewareRegistration.$Shape;
         *   (properties?: lnrpc.MiddlewareRegistration.$Properties): lnrpc.MiddlewareRegistration;
         * }}
         */
        MiddlewareRegistration.create = function(properties) {
            return new MiddlewareRegistration(properties);
        };

        /**
         * Encodes the specified MiddlewareRegistration message. Does not implicitly {@link lnrpc.MiddlewareRegistration.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.MiddlewareRegistration
         * @static
         * @param {lnrpc.MiddlewareRegistration.$Properties} message MiddlewareRegistration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MiddlewareRegistration.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.middleware_name != null && $Object.hasOwnProperty.call(message, "middleware_name") && message.middleware_name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.middleware_name);
            if (message.custom_macaroon_caveat_name != null && $Object.hasOwnProperty.call(message, "custom_macaroon_caveat_name") && message.custom_macaroon_caveat_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.custom_macaroon_caveat_name);
            if (message.read_only_mode != null && $Object.hasOwnProperty.call(message, "read_only_mode") && message.read_only_mode !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.read_only_mode);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MiddlewareRegistration message, length delimited. Does not implicitly {@link lnrpc.MiddlewareRegistration.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.MiddlewareRegistration
         * @static
         * @param {lnrpc.MiddlewareRegistration.$Properties} message MiddlewareRegistration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MiddlewareRegistration.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MiddlewareRegistration message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.MiddlewareRegistration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.MiddlewareRegistration & lnrpc.MiddlewareRegistration.$Shape} MiddlewareRegistration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MiddlewareRegistration.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.MiddlewareRegistration(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.middleware_name = value;
                        else
                            delete message.middleware_name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.custom_macaroon_caveat_name = value;
                        else
                            delete message.custom_macaroon_caveat_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.read_only_mode = value;
                        else
                            delete message.read_only_mode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MiddlewareRegistration message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.MiddlewareRegistration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.MiddlewareRegistration & lnrpc.MiddlewareRegistration.$Shape} MiddlewareRegistration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MiddlewareRegistration.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MiddlewareRegistration message.
         * @function verify
         * @memberof lnrpc.MiddlewareRegistration
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MiddlewareRegistration.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.middleware_name != null && $Object.hasOwnProperty.call(message, "middleware_name"))
                if (!$util.isString(message.middleware_name))
                    return "middleware_name: string expected";
            if (message.custom_macaroon_caveat_name != null && $Object.hasOwnProperty.call(message, "custom_macaroon_caveat_name"))
                if (!$util.isString(message.custom_macaroon_caveat_name))
                    return "custom_macaroon_caveat_name: string expected";
            if (message.read_only_mode != null && $Object.hasOwnProperty.call(message, "read_only_mode"))
                if (typeof message.read_only_mode !== "boolean")
                    return "read_only_mode: boolean expected";
            return null;
        };

        /**
         * Creates a MiddlewareRegistration message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.MiddlewareRegistration
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.MiddlewareRegistration} MiddlewareRegistration
         */
        MiddlewareRegistration.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.MiddlewareRegistration)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.MiddlewareRegistration: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.MiddlewareRegistration();
            if (object.middleware_name != null)
                if (typeof object.middleware_name !== "string" || object.middleware_name.length)
                    message.middleware_name = $String(object.middleware_name);
            if (object.custom_macaroon_caveat_name != null)
                if (typeof object.custom_macaroon_caveat_name !== "string" || object.custom_macaroon_caveat_name.length)
                    message.custom_macaroon_caveat_name = $String(object.custom_macaroon_caveat_name);
            if (object.read_only_mode != null)
                if (object.read_only_mode)
                    message.read_only_mode = $Boolean(object.read_only_mode);
            return message;
        };

        /**
         * Creates a plain object from a MiddlewareRegistration message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.MiddlewareRegistration
         * @static
         * @param {lnrpc.MiddlewareRegistration} message MiddlewareRegistration
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MiddlewareRegistration.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.middleware_name = "";
                object.custom_macaroon_caveat_name = "";
                object.read_only_mode = false;
            }
            if (message.middleware_name != null && $Object.hasOwnProperty.call(message, "middleware_name"))
                object.middleware_name = message.middleware_name;
            if (message.custom_macaroon_caveat_name != null && $Object.hasOwnProperty.call(message, "custom_macaroon_caveat_name"))
                object.custom_macaroon_caveat_name = message.custom_macaroon_caveat_name;
            if (message.read_only_mode != null && $Object.hasOwnProperty.call(message, "read_only_mode"))
                object.read_only_mode = message.read_only_mode;
            return object;
        };

        /**
         * Converts this MiddlewareRegistration to JSON.
         * @function toJSON
         * @memberof lnrpc.MiddlewareRegistration
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MiddlewareRegistration.prototype.toJSON = function() {
            return MiddlewareRegistration.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MiddlewareRegistration
         * @function getTypeUrl
         * @memberof lnrpc.MiddlewareRegistration
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MiddlewareRegistration.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.MiddlewareRegistration";
        };

        return MiddlewareRegistration;
    })();

    lnrpc.InterceptFeedback = (function() {

        /**
         * Properties of an InterceptFeedback.
         * @typedef {Object} lnrpc.InterceptFeedback.$Properties
         * @property {string|null} [error] InterceptFeedback error
         * @property {boolean|null} [replace_response] InterceptFeedback replace_response
         * @property {Uint8Array|null} [replacement_serialized] InterceptFeedback replacement_serialized
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an InterceptFeedback.
         * @memberof lnrpc
         * @interface IInterceptFeedback
         * @augments lnrpc.InterceptFeedback.$Properties
         * @deprecated Use lnrpc.InterceptFeedback.$Properties instead.
         */

        /**
         * Shape of an InterceptFeedback.
         * @typedef {lnrpc.InterceptFeedback.$Properties} lnrpc.InterceptFeedback.$Shape
         */

        /**
         * Constructs a new InterceptFeedback.
         * @memberof lnrpc
         * @classdesc Represents an InterceptFeedback.
         * @constructor
         * @param {lnrpc.InterceptFeedback.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const InterceptFeedback = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InterceptFeedback error.
         * @member {string} error
         * @memberof lnrpc.InterceptFeedback
         * @instance
         */
        InterceptFeedback.prototype.error = "";

        /**
         * InterceptFeedback replace_response.
         * @member {boolean} replace_response
         * @memberof lnrpc.InterceptFeedback
         * @instance
         */
        InterceptFeedback.prototype.replace_response = false;

        /**
         * InterceptFeedback replacement_serialized.
         * @member {Uint8Array} replacement_serialized
         * @memberof lnrpc.InterceptFeedback
         * @instance
         */
        InterceptFeedback.prototype.replacement_serialized = $util.newBuffer([]);

        /**
         * Creates a new InterceptFeedback instance using the specified properties.
         * @function create
         * @memberof lnrpc.InterceptFeedback
         * @static
         * @param {lnrpc.InterceptFeedback.$Properties=} [properties] Properties to set
         * @returns {lnrpc.InterceptFeedback} InterceptFeedback instance
         * @type {{
         *   (properties: lnrpc.InterceptFeedback.$Shape): lnrpc.InterceptFeedback & lnrpc.InterceptFeedback.$Shape;
         *   (properties?: lnrpc.InterceptFeedback.$Properties): lnrpc.InterceptFeedback;
         * }}
         */
        InterceptFeedback.create = function(properties) {
            return new InterceptFeedback(properties);
        };

        /**
         * Encodes the specified InterceptFeedback message. Does not implicitly {@link lnrpc.InterceptFeedback.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.InterceptFeedback
         * @static
         * @param {lnrpc.InterceptFeedback.$Properties} message InterceptFeedback message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InterceptFeedback.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.error != null && $Object.hasOwnProperty.call(message, "error") && message.error !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.error);
            if (message.replace_response != null && $Object.hasOwnProperty.call(message, "replace_response") && message.replace_response !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.replace_response);
            if (message.replacement_serialized != null && $Object.hasOwnProperty.call(message, "replacement_serialized") && message.replacement_serialized.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.replacement_serialized);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InterceptFeedback message, length delimited. Does not implicitly {@link lnrpc.InterceptFeedback.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.InterceptFeedback
         * @static
         * @param {lnrpc.InterceptFeedback.$Properties} message InterceptFeedback message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InterceptFeedback.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InterceptFeedback message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.InterceptFeedback
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.InterceptFeedback & lnrpc.InterceptFeedback.$Shape} InterceptFeedback
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InterceptFeedback.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.InterceptFeedback(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.error = value;
                        else
                            delete message.error;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.replace_response = value;
                        else
                            delete message.replace_response;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.replacement_serialized = value;
                        else
                            delete message.replacement_serialized;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InterceptFeedback message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.InterceptFeedback
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.InterceptFeedback & lnrpc.InterceptFeedback.$Shape} InterceptFeedback
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InterceptFeedback.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InterceptFeedback message.
         * @function verify
         * @memberof lnrpc.InterceptFeedback
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InterceptFeedback.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            if (message.replace_response != null && $Object.hasOwnProperty.call(message, "replace_response"))
                if (typeof message.replace_response !== "boolean")
                    return "replace_response: boolean expected";
            if (message.replacement_serialized != null && $Object.hasOwnProperty.call(message, "replacement_serialized"))
                if (!(message.replacement_serialized && typeof message.replacement_serialized.length === "number" || $util.isString(message.replacement_serialized)))
                    return "replacement_serialized: buffer expected";
            return null;
        };

        /**
         * Creates an InterceptFeedback message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.InterceptFeedback
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.InterceptFeedback} InterceptFeedback
         */
        InterceptFeedback.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.InterceptFeedback)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.InterceptFeedback: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.InterceptFeedback();
            if (object.error != null)
                if (typeof object.error !== "string" || object.error.length)
                    message.error = $String(object.error);
            if (object.replace_response != null)
                if (object.replace_response)
                    message.replace_response = $Boolean(object.replace_response);
            if (object.replacement_serialized != null)
                if (object.replacement_serialized.length)
                    if (typeof object.replacement_serialized === "string")
                        $util.base64.decode(object.replacement_serialized, message.replacement_serialized = $util.newBuffer($util.base64.length(object.replacement_serialized)), 0);
                    else if (object.replacement_serialized.length >= 0)
                        message.replacement_serialized = object.replacement_serialized;
            return message;
        };

        /**
         * Creates a plain object from an InterceptFeedback message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.InterceptFeedback
         * @static
         * @param {lnrpc.InterceptFeedback} message InterceptFeedback
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InterceptFeedback.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.error = "";
                object.replace_response = false;
                if (options.bytes === $String)
                    object.replacement_serialized = "";
                else {
                    object.replacement_serialized = [];
                    if (options.bytes !== $Array)
                        object.replacement_serialized = $util.newBuffer(object.replacement_serialized);
                }
            }
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                object.error = message.error;
            if (message.replace_response != null && $Object.hasOwnProperty.call(message, "replace_response"))
                object.replace_response = message.replace_response;
            if (message.replacement_serialized != null && $Object.hasOwnProperty.call(message, "replacement_serialized"))
                object.replacement_serialized = options.bytes === $String ? $util.base64.encode(message.replacement_serialized, 0, message.replacement_serialized.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.replacement_serialized) : message.replacement_serialized;
            return object;
        };

        /**
         * Converts this InterceptFeedback to JSON.
         * @function toJSON
         * @memberof lnrpc.InterceptFeedback
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InterceptFeedback.prototype.toJSON = function() {
            return InterceptFeedback.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InterceptFeedback
         * @function getTypeUrl
         * @memberof lnrpc.InterceptFeedback
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InterceptFeedback.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.InterceptFeedback";
        };

        return InterceptFeedback;
    })();

    lnrpc.State = (function() {

        /**
         * Constructs a new State service.
         * @memberof lnrpc
         * @classdesc Represents a State
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const State = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (State.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = State;

        /**
         * Creates new State service using the specified rpc implementation.
         * @function create
         * @memberof lnrpc.State
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {State} RPC service. Useful where requests and/or responses are streamed.
         */
        State.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link lnrpc.State#subscribeState}.
         * @memberof lnrpc.State
         * @typedef SubscribeStateCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.SubscribeStateResponse} [response] SubscribeStateResponse
         */

        /**
         * Calls SubscribeState.
         * @memberof lnrpc.State
         * @typedef SubscribeState
         * @type {{
         *   (request: lnrpc.ISubscribeStateRequest, callback: lnrpc.State.SubscribeStateCallback): void;
         *   (request: lnrpc.ISubscribeStateRequest): Promise<lnrpc.SubscribeStateResponse>;
         *   readonly name: "SubscribeState";
         *   readonly path: "/lnrpc.State/SubscribeState";
         *   readonly requestType: "SubscribeStateRequest";
         *   readonly responseType: "SubscribeStateResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribeState.
         * @name lnrpc.State#subscribeState
         * @type {lnrpc.State.SubscribeState}
         */
        $Object.defineProperties(State.prototype.subscribeState = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, State.prototype.subscribeState, $root.lnrpc.SubscribeStateRequest, $root.lnrpc.SubscribeStateResponse, request, callback);
        }, {
            name: { value: "SubscribeState" },
            path: { value: "/lnrpc.State/SubscribeState" },
            requestType: { value: "SubscribeStateRequest" },
            responseType: { value: "SubscribeStateResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link lnrpc.State#getState}.
         * @memberof lnrpc.State
         * @typedef GetStateCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.GetStateResponse} [response] GetStateResponse
         */

        /**
         * Calls GetState.
         * @memberof lnrpc.State
         * @typedef GetState
         * @type {{
         *   (request: lnrpc.IGetStateRequest, callback: lnrpc.State.GetStateCallback): void;
         *   (request: lnrpc.IGetStateRequest): Promise<lnrpc.GetStateResponse>;
         *   readonly name: "GetState";
         *   readonly path: "/lnrpc.State/GetState";
         *   readonly requestType: "GetStateRequest";
         *   readonly responseType: "GetStateResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetState.
         * @name lnrpc.State#getState
         * @type {lnrpc.State.GetState}
         */
        $Object.defineProperties(State.prototype.getState = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, State.prototype.getState, $root.lnrpc.GetStateRequest, $root.lnrpc.GetStateResponse, request, callback);
        }, {
            name: { value: "GetState" },
            path: { value: "/lnrpc.State/GetState" },
            requestType: { value: "GetStateRequest" },
            responseType: { value: "GetStateResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return State;
    })();

    /**
     * WalletState enum.
     * @name lnrpc.WalletState
     * @enum {number}
     * @property {number} NON_EXISTING=0 NON_EXISTING value
     * @property {number} LOCKED=1 LOCKED value
     * @property {number} UNLOCKED=2 UNLOCKED value
     * @property {number} RPC_ACTIVE=3 RPC_ACTIVE value
     * @property {number} SERVER_ACTIVE=4 SERVER_ACTIVE value
     * @property {number} WAITING_TO_START=255 WAITING_TO_START value
     */
    lnrpc.WalletState = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "NON_EXISTING"] = 0;
        values[valuesById[1] = "LOCKED"] = 1;
        values[valuesById[2] = "UNLOCKED"] = 2;
        values[valuesById[3] = "RPC_ACTIVE"] = 3;
        values[valuesById[4] = "SERVER_ACTIVE"] = 4;
        values[valuesById[255] = "WAITING_TO_START"] = 255;
        return values;
    })();

    lnrpc.SubscribeStateRequest = (function() {

        /**
         * Properties of a SubscribeStateRequest.
         * @typedef {Object} lnrpc.SubscribeStateRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SubscribeStateRequest.
         * @memberof lnrpc
         * @interface ISubscribeStateRequest
         * @augments lnrpc.SubscribeStateRequest.$Properties
         * @deprecated Use lnrpc.SubscribeStateRequest.$Properties instead.
         */

        /**
         * Shape of a SubscribeStateRequest.
         * @typedef {lnrpc.SubscribeStateRequest.$Properties} lnrpc.SubscribeStateRequest.$Shape
         */

        /**
         * Constructs a new SubscribeStateRequest.
         * @memberof lnrpc
         * @classdesc Represents a SubscribeStateRequest.
         * @constructor
         * @param {lnrpc.SubscribeStateRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SubscribeStateRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new SubscribeStateRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.SubscribeStateRequest
         * @static
         * @param {lnrpc.SubscribeStateRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SubscribeStateRequest} SubscribeStateRequest instance
         * @type {{
         *   (properties: lnrpc.SubscribeStateRequest.$Shape): lnrpc.SubscribeStateRequest & lnrpc.SubscribeStateRequest.$Shape;
         *   (properties?: lnrpc.SubscribeStateRequest.$Properties): lnrpc.SubscribeStateRequest;
         * }}
         */
        SubscribeStateRequest.create = function(properties) {
            return new SubscribeStateRequest(properties);
        };

        /**
         * Encodes the specified SubscribeStateRequest message. Does not implicitly {@link lnrpc.SubscribeStateRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SubscribeStateRequest
         * @static
         * @param {lnrpc.SubscribeStateRequest.$Properties} message SubscribeStateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeStateRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SubscribeStateRequest message, length delimited. Does not implicitly {@link lnrpc.SubscribeStateRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SubscribeStateRequest
         * @static
         * @param {lnrpc.SubscribeStateRequest.$Properties} message SubscribeStateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeStateRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SubscribeStateRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SubscribeStateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SubscribeStateRequest & lnrpc.SubscribeStateRequest.$Shape} SubscribeStateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeStateRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SubscribeStateRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SubscribeStateRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SubscribeStateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SubscribeStateRequest & lnrpc.SubscribeStateRequest.$Shape} SubscribeStateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeStateRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SubscribeStateRequest message.
         * @function verify
         * @memberof lnrpc.SubscribeStateRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SubscribeStateRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a SubscribeStateRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SubscribeStateRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SubscribeStateRequest} SubscribeStateRequest
         */
        SubscribeStateRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SubscribeStateRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SubscribeStateRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.SubscribeStateRequest();
        };

        /**
         * Creates a plain object from a SubscribeStateRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SubscribeStateRequest
         * @static
         * @param {lnrpc.SubscribeStateRequest} message SubscribeStateRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SubscribeStateRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this SubscribeStateRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.SubscribeStateRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SubscribeStateRequest.prototype.toJSON = function() {
            return SubscribeStateRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SubscribeStateRequest
         * @function getTypeUrl
         * @memberof lnrpc.SubscribeStateRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SubscribeStateRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SubscribeStateRequest";
        };

        return SubscribeStateRequest;
    })();

    lnrpc.SubscribeStateResponse = (function() {

        /**
         * Properties of a SubscribeStateResponse.
         * @typedef {Object} lnrpc.SubscribeStateResponse.$Properties
         * @property {lnrpc.WalletState|null} [state] SubscribeStateResponse state
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SubscribeStateResponse.
         * @memberof lnrpc
         * @interface ISubscribeStateResponse
         * @augments lnrpc.SubscribeStateResponse.$Properties
         * @deprecated Use lnrpc.SubscribeStateResponse.$Properties instead.
         */

        /**
         * Shape of a SubscribeStateResponse.
         * @typedef {lnrpc.SubscribeStateResponse.$Properties} lnrpc.SubscribeStateResponse.$Shape
         */

        /**
         * Constructs a new SubscribeStateResponse.
         * @memberof lnrpc
         * @classdesc Represents a SubscribeStateResponse.
         * @constructor
         * @param {lnrpc.SubscribeStateResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SubscribeStateResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SubscribeStateResponse state.
         * @member {lnrpc.WalletState} state
         * @memberof lnrpc.SubscribeStateResponse
         * @instance
         */
        SubscribeStateResponse.prototype.state = 0;

        /**
         * Creates a new SubscribeStateResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.SubscribeStateResponse
         * @static
         * @param {lnrpc.SubscribeStateResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.SubscribeStateResponse} SubscribeStateResponse instance
         * @type {{
         *   (properties: lnrpc.SubscribeStateResponse.$Shape): lnrpc.SubscribeStateResponse & lnrpc.SubscribeStateResponse.$Shape;
         *   (properties?: lnrpc.SubscribeStateResponse.$Properties): lnrpc.SubscribeStateResponse;
         * }}
         */
        SubscribeStateResponse.create = function(properties) {
            return new SubscribeStateResponse(properties);
        };

        /**
         * Encodes the specified SubscribeStateResponse message. Does not implicitly {@link lnrpc.SubscribeStateResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.SubscribeStateResponse
         * @static
         * @param {lnrpc.SubscribeStateResponse.$Properties} message SubscribeStateResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeStateResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SubscribeStateResponse message, length delimited. Does not implicitly {@link lnrpc.SubscribeStateResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.SubscribeStateResponse
         * @static
         * @param {lnrpc.SubscribeStateResponse.$Properties} message SubscribeStateResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeStateResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SubscribeStateResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.SubscribeStateResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.SubscribeStateResponse & lnrpc.SubscribeStateResponse.$Shape} SubscribeStateResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeStateResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.SubscribeStateResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SubscribeStateResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.SubscribeStateResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.SubscribeStateResponse & lnrpc.SubscribeStateResponse.$Shape} SubscribeStateResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeStateResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SubscribeStateResponse message.
         * @function verify
         * @memberof lnrpc.SubscribeStateResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SubscribeStateResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                    return "state: enum value expected";
            return null;
        };

        /**
         * Creates a SubscribeStateResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.SubscribeStateResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.SubscribeStateResponse} SubscribeStateResponse
         */
        SubscribeStateResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.SubscribeStateResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.SubscribeStateResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.SubscribeStateResponse();
            if (object.state !== 0 && (typeof object.state !== "string" || $root.lnrpc.WalletState[object.state] !== 0))
                switch (object.state) {
                case "NON_EXISTING":
                case 0:
                    message.state = 0;
                    break;
                case "LOCKED":
                case 1:
                    message.state = 1;
                    break;
                case "UNLOCKED":
                case 2:
                    message.state = 2;
                    break;
                case "RPC_ACTIVE":
                case 3:
                    message.state = 3;
                    break;
                case "SERVER_ACTIVE":
                case 4:
                    message.state = 4;
                    break;
                case "WAITING_TO_START":
                case 255:
                    message.state = 255;
                    break;
                default:
                    if (typeof object.state === "number" && (object.state | 0) === object.state)
                        message.state = object.state;
                }
            return message;
        };

        /**
         * Creates a plain object from a SubscribeStateResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.SubscribeStateResponse
         * @static
         * @param {lnrpc.SubscribeStateResponse} message SubscribeStateResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SubscribeStateResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.state = options.enums === $String ? "NON_EXISTING" : 0;
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                object.state = options.enums === $String ? $root.lnrpc.WalletState[message.state] === $undefined ? message.state : $root.lnrpc.WalletState[message.state] : message.state;
            return object;
        };

        /**
         * Converts this SubscribeStateResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.SubscribeStateResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SubscribeStateResponse.prototype.toJSON = function() {
            return SubscribeStateResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SubscribeStateResponse
         * @function getTypeUrl
         * @memberof lnrpc.SubscribeStateResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SubscribeStateResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.SubscribeStateResponse";
        };

        return SubscribeStateResponse;
    })();

    lnrpc.GetStateRequest = (function() {

        /**
         * Properties of a GetStateRequest.
         * @typedef {Object} lnrpc.GetStateRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetStateRequest.
         * @memberof lnrpc
         * @interface IGetStateRequest
         * @augments lnrpc.GetStateRequest.$Properties
         * @deprecated Use lnrpc.GetStateRequest.$Properties instead.
         */

        /**
         * Shape of a GetStateRequest.
         * @typedef {lnrpc.GetStateRequest.$Properties} lnrpc.GetStateRequest.$Shape
         */

        /**
         * Constructs a new GetStateRequest.
         * @memberof lnrpc
         * @classdesc Represents a GetStateRequest.
         * @constructor
         * @param {lnrpc.GetStateRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetStateRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new GetStateRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.GetStateRequest
         * @static
         * @param {lnrpc.GetStateRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GetStateRequest} GetStateRequest instance
         * @type {{
         *   (properties: lnrpc.GetStateRequest.$Shape): lnrpc.GetStateRequest & lnrpc.GetStateRequest.$Shape;
         *   (properties?: lnrpc.GetStateRequest.$Properties): lnrpc.GetStateRequest;
         * }}
         */
        GetStateRequest.create = function(properties) {
            return new GetStateRequest(properties);
        };

        /**
         * Encodes the specified GetStateRequest message. Does not implicitly {@link lnrpc.GetStateRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GetStateRequest
         * @static
         * @param {lnrpc.GetStateRequest.$Properties} message GetStateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetStateRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetStateRequest message, length delimited. Does not implicitly {@link lnrpc.GetStateRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GetStateRequest
         * @static
         * @param {lnrpc.GetStateRequest.$Properties} message GetStateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetStateRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetStateRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GetStateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GetStateRequest & lnrpc.GetStateRequest.$Shape} GetStateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetStateRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GetStateRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetStateRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GetStateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GetStateRequest & lnrpc.GetStateRequest.$Shape} GetStateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetStateRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetStateRequest message.
         * @function verify
         * @memberof lnrpc.GetStateRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetStateRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a GetStateRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GetStateRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GetStateRequest} GetStateRequest
         */
        GetStateRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GetStateRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GetStateRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.GetStateRequest();
        };

        /**
         * Creates a plain object from a GetStateRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GetStateRequest
         * @static
         * @param {lnrpc.GetStateRequest} message GetStateRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetStateRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this GetStateRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.GetStateRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetStateRequest.prototype.toJSON = function() {
            return GetStateRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetStateRequest
         * @function getTypeUrl
         * @memberof lnrpc.GetStateRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetStateRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GetStateRequest";
        };

        return GetStateRequest;
    })();

    lnrpc.GetStateResponse = (function() {

        /**
         * Properties of a GetStateResponse.
         * @typedef {Object} lnrpc.GetStateResponse.$Properties
         * @property {lnrpc.WalletState|null} [state] GetStateResponse state
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetStateResponse.
         * @memberof lnrpc
         * @interface IGetStateResponse
         * @augments lnrpc.GetStateResponse.$Properties
         * @deprecated Use lnrpc.GetStateResponse.$Properties instead.
         */

        /**
         * Shape of a GetStateResponse.
         * @typedef {lnrpc.GetStateResponse.$Properties} lnrpc.GetStateResponse.$Shape
         */

        /**
         * Constructs a new GetStateResponse.
         * @memberof lnrpc
         * @classdesc Represents a GetStateResponse.
         * @constructor
         * @param {lnrpc.GetStateResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetStateResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetStateResponse state.
         * @member {lnrpc.WalletState} state
         * @memberof lnrpc.GetStateResponse
         * @instance
         */
        GetStateResponse.prototype.state = 0;

        /**
         * Creates a new GetStateResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.GetStateResponse
         * @static
         * @param {lnrpc.GetStateResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GetStateResponse} GetStateResponse instance
         * @type {{
         *   (properties: lnrpc.GetStateResponse.$Shape): lnrpc.GetStateResponse & lnrpc.GetStateResponse.$Shape;
         *   (properties?: lnrpc.GetStateResponse.$Properties): lnrpc.GetStateResponse;
         * }}
         */
        GetStateResponse.create = function(properties) {
            return new GetStateResponse(properties);
        };

        /**
         * Encodes the specified GetStateResponse message. Does not implicitly {@link lnrpc.GetStateResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GetStateResponse
         * @static
         * @param {lnrpc.GetStateResponse.$Properties} message GetStateResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetStateResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetStateResponse message, length delimited. Does not implicitly {@link lnrpc.GetStateResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GetStateResponse
         * @static
         * @param {lnrpc.GetStateResponse.$Properties} message GetStateResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetStateResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetStateResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GetStateResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GetStateResponse & lnrpc.GetStateResponse.$Shape} GetStateResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetStateResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GetStateResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetStateResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GetStateResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GetStateResponse & lnrpc.GetStateResponse.$Shape} GetStateResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetStateResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetStateResponse message.
         * @function verify
         * @memberof lnrpc.GetStateResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetStateResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                    return "state: enum value expected";
            return null;
        };

        /**
         * Creates a GetStateResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GetStateResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GetStateResponse} GetStateResponse
         */
        GetStateResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GetStateResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GetStateResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.GetStateResponse();
            if (object.state !== 0 && (typeof object.state !== "string" || $root.lnrpc.WalletState[object.state] !== 0))
                switch (object.state) {
                case "NON_EXISTING":
                case 0:
                    message.state = 0;
                    break;
                case "LOCKED":
                case 1:
                    message.state = 1;
                    break;
                case "UNLOCKED":
                case 2:
                    message.state = 2;
                    break;
                case "RPC_ACTIVE":
                case 3:
                    message.state = 3;
                    break;
                case "SERVER_ACTIVE":
                case 4:
                    message.state = 4;
                    break;
                case "WAITING_TO_START":
                case 255:
                    message.state = 255;
                    break;
                default:
                    if (typeof object.state === "number" && (object.state | 0) === object.state)
                        message.state = object.state;
                }
            return message;
        };

        /**
         * Creates a plain object from a GetStateResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GetStateResponse
         * @static
         * @param {lnrpc.GetStateResponse} message GetStateResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetStateResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.state = options.enums === $String ? "NON_EXISTING" : 0;
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                object.state = options.enums === $String ? $root.lnrpc.WalletState[message.state] === $undefined ? message.state : $root.lnrpc.WalletState[message.state] : message.state;
            return object;
        };

        /**
         * Converts this GetStateResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.GetStateResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetStateResponse.prototype.toJSON = function() {
            return GetStateResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetStateResponse
         * @function getTypeUrl
         * @memberof lnrpc.GetStateResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetStateResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GetStateResponse";
        };

        return GetStateResponse;
    })();

    lnrpc.WalletUnlocker = (function() {

        /**
         * Constructs a new WalletUnlocker service.
         * @memberof lnrpc
         * @classdesc Represents a WalletUnlocker
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const WalletUnlocker = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (WalletUnlocker.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = WalletUnlocker;

        /**
         * Creates new WalletUnlocker service using the specified rpc implementation.
         * @function create
         * @memberof lnrpc.WalletUnlocker
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {WalletUnlocker} RPC service. Useful where requests and/or responses are streamed.
         */
        WalletUnlocker.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link lnrpc.WalletUnlocker#genSeed}.
         * @memberof lnrpc.WalletUnlocker
         * @typedef GenSeedCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.GenSeedResponse} [response] GenSeedResponse
         */

        /**
         * Calls GenSeed.
         * @memberof lnrpc.WalletUnlocker
         * @typedef GenSeed
         * @type {{
         *   (request: lnrpc.IGenSeedRequest, callback: lnrpc.WalletUnlocker.GenSeedCallback): void;
         *   (request: lnrpc.IGenSeedRequest): Promise<lnrpc.GenSeedResponse>;
         *   readonly name: "GenSeed";
         *   readonly path: "/lnrpc.WalletUnlocker/GenSeed";
         *   readonly requestType: "GenSeedRequest";
         *   readonly responseType: "GenSeedResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GenSeed.
         * @name lnrpc.WalletUnlocker#genSeed
         * @type {lnrpc.WalletUnlocker.GenSeed}
         */
        $Object.defineProperties(WalletUnlocker.prototype.genSeed = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletUnlocker.prototype.genSeed, $root.lnrpc.GenSeedRequest, $root.lnrpc.GenSeedResponse, request, callback);
        }, {
            name: { value: "GenSeed" },
            path: { value: "/lnrpc.WalletUnlocker/GenSeed" },
            requestType: { value: "GenSeedRequest" },
            responseType: { value: "GenSeedResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.WalletUnlocker#initWallet}.
         * @memberof lnrpc.WalletUnlocker
         * @typedef InitWalletCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.InitWalletResponse} [response] InitWalletResponse
         */

        /**
         * Calls InitWallet.
         * @memberof lnrpc.WalletUnlocker
         * @typedef InitWallet
         * @type {{
         *   (request: lnrpc.IInitWalletRequest, callback: lnrpc.WalletUnlocker.InitWalletCallback): void;
         *   (request: lnrpc.IInitWalletRequest): Promise<lnrpc.InitWalletResponse>;
         *   readonly name: "InitWallet";
         *   readonly path: "/lnrpc.WalletUnlocker/InitWallet";
         *   readonly requestType: "InitWalletRequest";
         *   readonly responseType: "InitWalletResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls InitWallet.
         * @name lnrpc.WalletUnlocker#initWallet
         * @type {lnrpc.WalletUnlocker.InitWallet}
         */
        $Object.defineProperties(WalletUnlocker.prototype.initWallet = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletUnlocker.prototype.initWallet, $root.lnrpc.InitWalletRequest, $root.lnrpc.InitWalletResponse, request, callback);
        }, {
            name: { value: "InitWallet" },
            path: { value: "/lnrpc.WalletUnlocker/InitWallet" },
            requestType: { value: "InitWalletRequest" },
            responseType: { value: "InitWalletResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.WalletUnlocker#unlockWallet}.
         * @memberof lnrpc.WalletUnlocker
         * @typedef UnlockWalletCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.UnlockWalletResponse} [response] UnlockWalletResponse
         */

        /**
         * Calls UnlockWallet.
         * @memberof lnrpc.WalletUnlocker
         * @typedef UnlockWallet
         * @type {{
         *   (request: lnrpc.IUnlockWalletRequest, callback: lnrpc.WalletUnlocker.UnlockWalletCallback): void;
         *   (request: lnrpc.IUnlockWalletRequest): Promise<lnrpc.UnlockWalletResponse>;
         *   readonly name: "UnlockWallet";
         *   readonly path: "/lnrpc.WalletUnlocker/UnlockWallet";
         *   readonly requestType: "UnlockWalletRequest";
         *   readonly responseType: "UnlockWalletResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls UnlockWallet.
         * @name lnrpc.WalletUnlocker#unlockWallet
         * @type {lnrpc.WalletUnlocker.UnlockWallet}
         */
        $Object.defineProperties(WalletUnlocker.prototype.unlockWallet = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletUnlocker.prototype.unlockWallet, $root.lnrpc.UnlockWalletRequest, $root.lnrpc.UnlockWalletResponse, request, callback);
        }, {
            name: { value: "UnlockWallet" },
            path: { value: "/lnrpc.WalletUnlocker/UnlockWallet" },
            requestType: { value: "UnlockWalletRequest" },
            responseType: { value: "UnlockWalletResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link lnrpc.WalletUnlocker#changePassword}.
         * @memberof lnrpc.WalletUnlocker
         * @typedef ChangePasswordCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.ChangePasswordResponse} [response] ChangePasswordResponse
         */

        /**
         * Calls ChangePassword.
         * @memberof lnrpc.WalletUnlocker
         * @typedef ChangePassword
         * @type {{
         *   (request: lnrpc.IChangePasswordRequest, callback: lnrpc.WalletUnlocker.ChangePasswordCallback): void;
         *   (request: lnrpc.IChangePasswordRequest): Promise<lnrpc.ChangePasswordResponse>;
         *   readonly name: "ChangePassword";
         *   readonly path: "/lnrpc.WalletUnlocker/ChangePassword";
         *   readonly requestType: "ChangePasswordRequest";
         *   readonly responseType: "ChangePasswordResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ChangePassword.
         * @name lnrpc.WalletUnlocker#changePassword
         * @type {lnrpc.WalletUnlocker.ChangePassword}
         */
        $Object.defineProperties(WalletUnlocker.prototype.changePassword = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletUnlocker.prototype.changePassword, $root.lnrpc.ChangePasswordRequest, $root.lnrpc.ChangePasswordResponse, request, callback);
        }, {
            name: { value: "ChangePassword" },
            path: { value: "/lnrpc.WalletUnlocker/ChangePassword" },
            requestType: { value: "ChangePasswordRequest" },
            responseType: { value: "ChangePasswordResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return WalletUnlocker;
    })();

    lnrpc.GenSeedRequest = (function() {

        /**
         * Properties of a GenSeedRequest.
         * @typedef {Object} lnrpc.GenSeedRequest.$Properties
         * @property {Uint8Array|null} [aezeed_passphrase] GenSeedRequest aezeed_passphrase
         * @property {Uint8Array|null} [seed_entropy] GenSeedRequest seed_entropy
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GenSeedRequest.
         * @memberof lnrpc
         * @interface IGenSeedRequest
         * @augments lnrpc.GenSeedRequest.$Properties
         * @deprecated Use lnrpc.GenSeedRequest.$Properties instead.
         */

        /**
         * Shape of a GenSeedRequest.
         * @typedef {lnrpc.GenSeedRequest.$Properties} lnrpc.GenSeedRequest.$Shape
         */

        /**
         * Constructs a new GenSeedRequest.
         * @memberof lnrpc
         * @classdesc Represents a GenSeedRequest.
         * @constructor
         * @param {lnrpc.GenSeedRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GenSeedRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GenSeedRequest aezeed_passphrase.
         * @member {Uint8Array} aezeed_passphrase
         * @memberof lnrpc.GenSeedRequest
         * @instance
         */
        GenSeedRequest.prototype.aezeed_passphrase = $util.newBuffer([]);

        /**
         * GenSeedRequest seed_entropy.
         * @member {Uint8Array} seed_entropy
         * @memberof lnrpc.GenSeedRequest
         * @instance
         */
        GenSeedRequest.prototype.seed_entropy = $util.newBuffer([]);

        /**
         * Creates a new GenSeedRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.GenSeedRequest
         * @static
         * @param {lnrpc.GenSeedRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GenSeedRequest} GenSeedRequest instance
         * @type {{
         *   (properties: lnrpc.GenSeedRequest.$Shape): lnrpc.GenSeedRequest & lnrpc.GenSeedRequest.$Shape;
         *   (properties?: lnrpc.GenSeedRequest.$Properties): lnrpc.GenSeedRequest;
         * }}
         */
        GenSeedRequest.create = function(properties) {
            return new GenSeedRequest(properties);
        };

        /**
         * Encodes the specified GenSeedRequest message. Does not implicitly {@link lnrpc.GenSeedRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GenSeedRequest
         * @static
         * @param {lnrpc.GenSeedRequest.$Properties} message GenSeedRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenSeedRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.aezeed_passphrase != null && $Object.hasOwnProperty.call(message, "aezeed_passphrase") && message.aezeed_passphrase.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.aezeed_passphrase);
            if (message.seed_entropy != null && $Object.hasOwnProperty.call(message, "seed_entropy") && message.seed_entropy.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.seed_entropy);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GenSeedRequest message, length delimited. Does not implicitly {@link lnrpc.GenSeedRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GenSeedRequest
         * @static
         * @param {lnrpc.GenSeedRequest.$Properties} message GenSeedRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenSeedRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GenSeedRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GenSeedRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GenSeedRequest & lnrpc.GenSeedRequest.$Shape} GenSeedRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenSeedRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GenSeedRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.aezeed_passphrase = value;
                        else
                            delete message.aezeed_passphrase;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.seed_entropy = value;
                        else
                            delete message.seed_entropy;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GenSeedRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GenSeedRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GenSeedRequest & lnrpc.GenSeedRequest.$Shape} GenSeedRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenSeedRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GenSeedRequest message.
         * @function verify
         * @memberof lnrpc.GenSeedRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GenSeedRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.aezeed_passphrase != null && $Object.hasOwnProperty.call(message, "aezeed_passphrase"))
                if (!(message.aezeed_passphrase && typeof message.aezeed_passphrase.length === "number" || $util.isString(message.aezeed_passphrase)))
                    return "aezeed_passphrase: buffer expected";
            if (message.seed_entropy != null && $Object.hasOwnProperty.call(message, "seed_entropy"))
                if (!(message.seed_entropy && typeof message.seed_entropy.length === "number" || $util.isString(message.seed_entropy)))
                    return "seed_entropy: buffer expected";
            return null;
        };

        /**
         * Creates a GenSeedRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GenSeedRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GenSeedRequest} GenSeedRequest
         */
        GenSeedRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GenSeedRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GenSeedRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.GenSeedRequest();
            if (object.aezeed_passphrase != null)
                if (object.aezeed_passphrase.length)
                    if (typeof object.aezeed_passphrase === "string")
                        $util.base64.decode(object.aezeed_passphrase, message.aezeed_passphrase = $util.newBuffer($util.base64.length(object.aezeed_passphrase)), 0);
                    else if (object.aezeed_passphrase.length >= 0)
                        message.aezeed_passphrase = object.aezeed_passphrase;
            if (object.seed_entropy != null)
                if (object.seed_entropy.length)
                    if (typeof object.seed_entropy === "string")
                        $util.base64.decode(object.seed_entropy, message.seed_entropy = $util.newBuffer($util.base64.length(object.seed_entropy)), 0);
                    else if (object.seed_entropy.length >= 0)
                        message.seed_entropy = object.seed_entropy;
            return message;
        };

        /**
         * Creates a plain object from a GenSeedRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GenSeedRequest
         * @static
         * @param {lnrpc.GenSeedRequest} message GenSeedRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GenSeedRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.aezeed_passphrase = "";
                else {
                    object.aezeed_passphrase = [];
                    if (options.bytes !== $Array)
                        object.aezeed_passphrase = $util.newBuffer(object.aezeed_passphrase);
                }
                if (options.bytes === $String)
                    object.seed_entropy = "";
                else {
                    object.seed_entropy = [];
                    if (options.bytes !== $Array)
                        object.seed_entropy = $util.newBuffer(object.seed_entropy);
                }
            }
            if (message.aezeed_passphrase != null && $Object.hasOwnProperty.call(message, "aezeed_passphrase"))
                object.aezeed_passphrase = options.bytes === $String ? $util.base64.encode(message.aezeed_passphrase, 0, message.aezeed_passphrase.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.aezeed_passphrase) : message.aezeed_passphrase;
            if (message.seed_entropy != null && $Object.hasOwnProperty.call(message, "seed_entropy"))
                object.seed_entropy = options.bytes === $String ? $util.base64.encode(message.seed_entropy, 0, message.seed_entropy.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.seed_entropy) : message.seed_entropy;
            return object;
        };

        /**
         * Converts this GenSeedRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.GenSeedRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GenSeedRequest.prototype.toJSON = function() {
            return GenSeedRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GenSeedRequest
         * @function getTypeUrl
         * @memberof lnrpc.GenSeedRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GenSeedRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GenSeedRequest";
        };

        return GenSeedRequest;
    })();

    lnrpc.GenSeedResponse = (function() {

        /**
         * Properties of a GenSeedResponse.
         * @typedef {Object} lnrpc.GenSeedResponse.$Properties
         * @property {Array.<string>|null} [cipher_seed_mnemonic] GenSeedResponse cipher_seed_mnemonic
         * @property {Uint8Array|null} [enciphered_seed] GenSeedResponse enciphered_seed
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GenSeedResponse.
         * @memberof lnrpc
         * @interface IGenSeedResponse
         * @augments lnrpc.GenSeedResponse.$Properties
         * @deprecated Use lnrpc.GenSeedResponse.$Properties instead.
         */

        /**
         * Shape of a GenSeedResponse.
         * @typedef {lnrpc.GenSeedResponse.$Properties} lnrpc.GenSeedResponse.$Shape
         */

        /**
         * Constructs a new GenSeedResponse.
         * @memberof lnrpc
         * @classdesc Represents a GenSeedResponse.
         * @constructor
         * @param {lnrpc.GenSeedResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GenSeedResponse = function (properties) {
            this.cipher_seed_mnemonic = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GenSeedResponse cipher_seed_mnemonic.
         * @member {Array.<string>} cipher_seed_mnemonic
         * @memberof lnrpc.GenSeedResponse
         * @instance
         */
        GenSeedResponse.prototype.cipher_seed_mnemonic = $util.emptyArray;

        /**
         * GenSeedResponse enciphered_seed.
         * @member {Uint8Array} enciphered_seed
         * @memberof lnrpc.GenSeedResponse
         * @instance
         */
        GenSeedResponse.prototype.enciphered_seed = $util.newBuffer([]);

        /**
         * Creates a new GenSeedResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.GenSeedResponse
         * @static
         * @param {lnrpc.GenSeedResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.GenSeedResponse} GenSeedResponse instance
         * @type {{
         *   (properties: lnrpc.GenSeedResponse.$Shape): lnrpc.GenSeedResponse & lnrpc.GenSeedResponse.$Shape;
         *   (properties?: lnrpc.GenSeedResponse.$Properties): lnrpc.GenSeedResponse;
         * }}
         */
        GenSeedResponse.create = function(properties) {
            return new GenSeedResponse(properties);
        };

        /**
         * Encodes the specified GenSeedResponse message. Does not implicitly {@link lnrpc.GenSeedResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.GenSeedResponse
         * @static
         * @param {lnrpc.GenSeedResponse.$Properties} message GenSeedResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenSeedResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.cipher_seed_mnemonic != null && message.cipher_seed_mnemonic.length)
                for (let i = 0; i < message.cipher_seed_mnemonic.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.cipher_seed_mnemonic[i]);
            if (message.enciphered_seed != null && $Object.hasOwnProperty.call(message, "enciphered_seed") && message.enciphered_seed.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.enciphered_seed);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GenSeedResponse message, length delimited. Does not implicitly {@link lnrpc.GenSeedResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.GenSeedResponse
         * @static
         * @param {lnrpc.GenSeedResponse.$Properties} message GenSeedResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenSeedResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GenSeedResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.GenSeedResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.GenSeedResponse & lnrpc.GenSeedResponse.$Shape} GenSeedResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenSeedResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.GenSeedResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cipher_seed_mnemonic && message.cipher_seed_mnemonic.length))
                            message.cipher_seed_mnemonic = [];
                        message.cipher_seed_mnemonic.push(reader.stringVerify());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.enciphered_seed = value;
                        else
                            delete message.enciphered_seed;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GenSeedResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.GenSeedResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.GenSeedResponse & lnrpc.GenSeedResponse.$Shape} GenSeedResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenSeedResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GenSeedResponse message.
         * @function verify
         * @memberof lnrpc.GenSeedResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GenSeedResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.cipher_seed_mnemonic != null && $Object.hasOwnProperty.call(message, "cipher_seed_mnemonic")) {
                if (!$Array.isArray(message.cipher_seed_mnemonic))
                    return "cipher_seed_mnemonic: array expected";
                for (let i = 0; i < message.cipher_seed_mnemonic.length; ++i)
                    if (!$util.isString(message.cipher_seed_mnemonic[i]))
                        return "cipher_seed_mnemonic: string[] expected";
            }
            if (message.enciphered_seed != null && $Object.hasOwnProperty.call(message, "enciphered_seed"))
                if (!(message.enciphered_seed && typeof message.enciphered_seed.length === "number" || $util.isString(message.enciphered_seed)))
                    return "enciphered_seed: buffer expected";
            return null;
        };

        /**
         * Creates a GenSeedResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.GenSeedResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.GenSeedResponse} GenSeedResponse
         */
        GenSeedResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.GenSeedResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.GenSeedResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.GenSeedResponse();
            if (object.cipher_seed_mnemonic) {
                if (!$Array.isArray(object.cipher_seed_mnemonic))
                    throw $TypeError(".lnrpc.GenSeedResponse.cipher_seed_mnemonic: array expected");
                message.cipher_seed_mnemonic = $Array(object.cipher_seed_mnemonic.length);
                for (let i = 0; i < object.cipher_seed_mnemonic.length; ++i)
                    message.cipher_seed_mnemonic[i] = $String(object.cipher_seed_mnemonic[i]);
            }
            if (object.enciphered_seed != null)
                if (object.enciphered_seed.length)
                    if (typeof object.enciphered_seed === "string")
                        $util.base64.decode(object.enciphered_seed, message.enciphered_seed = $util.newBuffer($util.base64.length(object.enciphered_seed)), 0);
                    else if (object.enciphered_seed.length >= 0)
                        message.enciphered_seed = object.enciphered_seed;
            return message;
        };

        /**
         * Creates a plain object from a GenSeedResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.GenSeedResponse
         * @static
         * @param {lnrpc.GenSeedResponse} message GenSeedResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GenSeedResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.cipher_seed_mnemonic = [];
            if (options.defaults)
                if (options.bytes === $String)
                    object.enciphered_seed = "";
                else {
                    object.enciphered_seed = [];
                    if (options.bytes !== $Array)
                        object.enciphered_seed = $util.newBuffer(object.enciphered_seed);
                }
            if (message.cipher_seed_mnemonic && message.cipher_seed_mnemonic.length) {
                object.cipher_seed_mnemonic = $Array(message.cipher_seed_mnemonic.length);
                for (let j = 0; j < message.cipher_seed_mnemonic.length; ++j)
                    object.cipher_seed_mnemonic[j] = message.cipher_seed_mnemonic[j];
            }
            if (message.enciphered_seed != null && $Object.hasOwnProperty.call(message, "enciphered_seed"))
                object.enciphered_seed = options.bytes === $String ? $util.base64.encode(message.enciphered_seed, 0, message.enciphered_seed.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.enciphered_seed) : message.enciphered_seed;
            return object;
        };

        /**
         * Converts this GenSeedResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.GenSeedResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GenSeedResponse.prototype.toJSON = function() {
            return GenSeedResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GenSeedResponse
         * @function getTypeUrl
         * @memberof lnrpc.GenSeedResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GenSeedResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.GenSeedResponse";
        };

        return GenSeedResponse;
    })();

    lnrpc.InitWalletRequest = (function() {

        /**
         * Properties of an InitWalletRequest.
         * @typedef {Object} lnrpc.InitWalletRequest.$Properties
         * @property {Uint8Array|null} [wallet_password] InitWalletRequest wallet_password
         * @property {Array.<string>|null} [cipher_seed_mnemonic] InitWalletRequest cipher_seed_mnemonic
         * @property {Uint8Array|null} [aezeed_passphrase] InitWalletRequest aezeed_passphrase
         * @property {number|null} [recovery_window] InitWalletRequest recovery_window
         * @property {lnrpc.ChanBackupSnapshot.$Properties|null} [channel_backups] InitWalletRequest channel_backups
         * @property {boolean|null} [stateless_init] InitWalletRequest stateless_init
         * @property {string|null} [extended_master_key] InitWalletRequest extended_master_key
         * @property {Long|null} [extended_master_key_birthday_timestamp] InitWalletRequest extended_master_key_birthday_timestamp
         * @property {lnrpc.WatchOnly.$Properties|null} [watch_only] InitWalletRequest watch_only
         * @property {Uint8Array|null} [macaroon_root_key] InitWalletRequest macaroon_root_key
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an InitWalletRequest.
         * @memberof lnrpc
         * @interface IInitWalletRequest
         * @augments lnrpc.InitWalletRequest.$Properties
         * @deprecated Use lnrpc.InitWalletRequest.$Properties instead.
         */

        /**
         * Shape of an InitWalletRequest.
         * @typedef {{
         *   wallet_password?: Uint8Array|null;
         *   cipher_seed_mnemonic?: Array.<string>|null;
         *   aezeed_passphrase?: Uint8Array|null;
         *   recovery_window?: number|null;
         *   channel_backups?: lnrpc.ChanBackupSnapshot.$Shape|null;
         *   stateless_init?: boolean|null;
         *   extended_master_key?: string|null;
         *   extended_master_key_birthday_timestamp?: Long|null;
         *   watch_only?: lnrpc.WatchOnly.$Shape|null;
         *   macaroon_root_key?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.InitWalletRequest.$Shape
         */

        /**
         * Constructs a new InitWalletRequest.
         * @memberof lnrpc
         * @classdesc Represents an InitWalletRequest.
         * @constructor
         * @param {lnrpc.InitWalletRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const InitWalletRequest = function (properties) {
            this.cipher_seed_mnemonic = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InitWalletRequest wallet_password.
         * @member {Uint8Array} wallet_password
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.wallet_password = $util.newBuffer([]);

        /**
         * InitWalletRequest cipher_seed_mnemonic.
         * @member {Array.<string>} cipher_seed_mnemonic
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.cipher_seed_mnemonic = $util.emptyArray;

        /**
         * InitWalletRequest aezeed_passphrase.
         * @member {Uint8Array} aezeed_passphrase
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.aezeed_passphrase = $util.newBuffer([]);

        /**
         * InitWalletRequest recovery_window.
         * @member {number} recovery_window
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.recovery_window = 0;

        /**
         * InitWalletRequest channel_backups.
         * @member {lnrpc.ChanBackupSnapshot.$Properties|null|undefined} channel_backups
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.channel_backups = null;

        /**
         * InitWalletRequest stateless_init.
         * @member {boolean} stateless_init
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.stateless_init = false;

        /**
         * InitWalletRequest extended_master_key.
         * @member {string} extended_master_key
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.extended_master_key = "";

        /**
         * InitWalletRequest extended_master_key_birthday_timestamp.
         * @member {Long} extended_master_key_birthday_timestamp
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.extended_master_key_birthday_timestamp = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * InitWalletRequest watch_only.
         * @member {lnrpc.WatchOnly.$Properties|null|undefined} watch_only
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.watch_only = null;

        /**
         * InitWalletRequest macaroon_root_key.
         * @member {Uint8Array} macaroon_root_key
         * @memberof lnrpc.InitWalletRequest
         * @instance
         */
        InitWalletRequest.prototype.macaroon_root_key = $util.newBuffer([]);

        /**
         * Creates a new InitWalletRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.InitWalletRequest
         * @static
         * @param {lnrpc.InitWalletRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.InitWalletRequest} InitWalletRequest instance
         * @type {{
         *   (properties: lnrpc.InitWalletRequest.$Shape): lnrpc.InitWalletRequest & lnrpc.InitWalletRequest.$Shape;
         *   (properties?: lnrpc.InitWalletRequest.$Properties): lnrpc.InitWalletRequest;
         * }}
         */
        InitWalletRequest.create = function(properties) {
            return new InitWalletRequest(properties);
        };

        /**
         * Encodes the specified InitWalletRequest message. Does not implicitly {@link lnrpc.InitWalletRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.InitWalletRequest
         * @static
         * @param {lnrpc.InitWalletRequest.$Properties} message InitWalletRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InitWalletRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.wallet_password != null && $Object.hasOwnProperty.call(message, "wallet_password") && message.wallet_password.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.wallet_password);
            if (message.cipher_seed_mnemonic != null && message.cipher_seed_mnemonic.length)
                for (let i = 0; i < message.cipher_seed_mnemonic.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.cipher_seed_mnemonic[i]);
            if (message.aezeed_passphrase != null && $Object.hasOwnProperty.call(message, "aezeed_passphrase") && message.aezeed_passphrase.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.aezeed_passphrase);
            if (message.recovery_window != null && $Object.hasOwnProperty.call(message, "recovery_window") && message.recovery_window !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.recovery_window);
            if (message.channel_backups != null && $Object.hasOwnProperty.call(message, "channel_backups"))
                $root.lnrpc.ChanBackupSnapshot.encode(message.channel_backups, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.stateless_init != null && $Object.hasOwnProperty.call(message, "stateless_init") && message.stateless_init !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.stateless_init);
            if (message.extended_master_key != null && $Object.hasOwnProperty.call(message, "extended_master_key") && message.extended_master_key !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.extended_master_key);
            if (message.extended_master_key_birthday_timestamp != null && $Object.hasOwnProperty.call(message, "extended_master_key_birthday_timestamp") && (typeof message.extended_master_key_birthday_timestamp === "object" ? message.extended_master_key_birthday_timestamp.low || message.extended_master_key_birthday_timestamp.high : message.extended_master_key_birthday_timestamp !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.extended_master_key_birthday_timestamp);
            if (message.watch_only != null && $Object.hasOwnProperty.call(message, "watch_only"))
                $root.lnrpc.WatchOnly.encode(message.watch_only, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.macaroon_root_key != null && $Object.hasOwnProperty.call(message, "macaroon_root_key") && message.macaroon_root_key.length)
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.macaroon_root_key);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InitWalletRequest message, length delimited. Does not implicitly {@link lnrpc.InitWalletRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.InitWalletRequest
         * @static
         * @param {lnrpc.InitWalletRequest.$Properties} message InitWalletRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InitWalletRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InitWalletRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.InitWalletRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.InitWalletRequest & lnrpc.InitWalletRequest.$Shape} InitWalletRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InitWalletRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.InitWalletRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.wallet_password = value;
                        else
                            delete message.wallet_password;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cipher_seed_mnemonic && message.cipher_seed_mnemonic.length))
                            message.cipher_seed_mnemonic = [];
                        message.cipher_seed_mnemonic.push(reader.stringVerify());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.aezeed_passphrase = value;
                        else
                            delete message.aezeed_passphrase;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.recovery_window = value;
                        else
                            delete message.recovery_window;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.channel_backups = $root.lnrpc.ChanBackupSnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel_backups);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.stateless_init = value;
                        else
                            delete message.stateless_init;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.extended_master_key = value;
                        else
                            delete message.extended_master_key;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.extended_master_key_birthday_timestamp = value;
                        else
                            delete message.extended_master_key_birthday_timestamp;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.watch_only = $root.lnrpc.WatchOnly.decode(reader, reader.uint32(), $undefined, _depth + 1, message.watch_only);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.macaroon_root_key = value;
                        else
                            delete message.macaroon_root_key;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InitWalletRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.InitWalletRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.InitWalletRequest & lnrpc.InitWalletRequest.$Shape} InitWalletRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InitWalletRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InitWalletRequest message.
         * @function verify
         * @memberof lnrpc.InitWalletRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InitWalletRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.wallet_password != null && $Object.hasOwnProperty.call(message, "wallet_password"))
                if (!(message.wallet_password && typeof message.wallet_password.length === "number" || $util.isString(message.wallet_password)))
                    return "wallet_password: buffer expected";
            if (message.cipher_seed_mnemonic != null && $Object.hasOwnProperty.call(message, "cipher_seed_mnemonic")) {
                if (!$Array.isArray(message.cipher_seed_mnemonic))
                    return "cipher_seed_mnemonic: array expected";
                for (let i = 0; i < message.cipher_seed_mnemonic.length; ++i)
                    if (!$util.isString(message.cipher_seed_mnemonic[i]))
                        return "cipher_seed_mnemonic: string[] expected";
            }
            if (message.aezeed_passphrase != null && $Object.hasOwnProperty.call(message, "aezeed_passphrase"))
                if (!(message.aezeed_passphrase && typeof message.aezeed_passphrase.length === "number" || $util.isString(message.aezeed_passphrase)))
                    return "aezeed_passphrase: buffer expected";
            if (message.recovery_window != null && $Object.hasOwnProperty.call(message, "recovery_window"))
                if (!$util.isInteger(message.recovery_window))
                    return "recovery_window: integer expected";
            if (message.channel_backups != null && $Object.hasOwnProperty.call(message, "channel_backups")) {
                let error = $root.lnrpc.ChanBackupSnapshot.verify(message.channel_backups, _depth + 1);
                if (error)
                    return "channel_backups." + error;
            }
            if (message.stateless_init != null && $Object.hasOwnProperty.call(message, "stateless_init"))
                if (typeof message.stateless_init !== "boolean")
                    return "stateless_init: boolean expected";
            if (message.extended_master_key != null && $Object.hasOwnProperty.call(message, "extended_master_key"))
                if (!$util.isString(message.extended_master_key))
                    return "extended_master_key: string expected";
            if (message.extended_master_key_birthday_timestamp != null && $Object.hasOwnProperty.call(message, "extended_master_key_birthday_timestamp"))
                if (!$util.isInteger(message.extended_master_key_birthday_timestamp) && !(message.extended_master_key_birthday_timestamp && $util.isInteger(message.extended_master_key_birthday_timestamp.low) && $util.isInteger(message.extended_master_key_birthday_timestamp.high)))
                    return "extended_master_key_birthday_timestamp: integer|Long expected";
            if (message.watch_only != null && $Object.hasOwnProperty.call(message, "watch_only")) {
                let error = $root.lnrpc.WatchOnly.verify(message.watch_only, _depth + 1);
                if (error)
                    return "watch_only." + error;
            }
            if (message.macaroon_root_key != null && $Object.hasOwnProperty.call(message, "macaroon_root_key"))
                if (!(message.macaroon_root_key && typeof message.macaroon_root_key.length === "number" || $util.isString(message.macaroon_root_key)))
                    return "macaroon_root_key: buffer expected";
            return null;
        };

        /**
         * Creates an InitWalletRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.InitWalletRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.InitWalletRequest} InitWalletRequest
         */
        InitWalletRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.InitWalletRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.InitWalletRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.InitWalletRequest();
            if (object.wallet_password != null)
                if (object.wallet_password.length)
                    if (typeof object.wallet_password === "string")
                        $util.base64.decode(object.wallet_password, message.wallet_password = $util.newBuffer($util.base64.length(object.wallet_password)), 0);
                    else if (object.wallet_password.length >= 0)
                        message.wallet_password = object.wallet_password;
            if (object.cipher_seed_mnemonic) {
                if (!$Array.isArray(object.cipher_seed_mnemonic))
                    throw $TypeError(".lnrpc.InitWalletRequest.cipher_seed_mnemonic: array expected");
                message.cipher_seed_mnemonic = $Array(object.cipher_seed_mnemonic.length);
                for (let i = 0; i < object.cipher_seed_mnemonic.length; ++i)
                    message.cipher_seed_mnemonic[i] = $String(object.cipher_seed_mnemonic[i]);
            }
            if (object.aezeed_passphrase != null)
                if (object.aezeed_passphrase.length)
                    if (typeof object.aezeed_passphrase === "string")
                        $util.base64.decode(object.aezeed_passphrase, message.aezeed_passphrase = $util.newBuffer($util.base64.length(object.aezeed_passphrase)), 0);
                    else if (object.aezeed_passphrase.length >= 0)
                        message.aezeed_passphrase = object.aezeed_passphrase;
            if (object.recovery_window != null)
                if ($Number(object.recovery_window) !== 0)
                    message.recovery_window = object.recovery_window | 0;
            if (object.channel_backups != null) {
                if (!$util.isObject(object.channel_backups))
                    throw $TypeError(".lnrpc.InitWalletRequest.channel_backups: object expected");
                message.channel_backups = $root.lnrpc.ChanBackupSnapshot.fromObject(object.channel_backups, _depth + 1);
            }
            if (object.stateless_init != null)
                if (object.stateless_init)
                    message.stateless_init = $Boolean(object.stateless_init);
            if (object.extended_master_key != null)
                if (typeof object.extended_master_key !== "string" || object.extended_master_key.length)
                    message.extended_master_key = $String(object.extended_master_key);
            if (object.extended_master_key_birthday_timestamp != null)
                if (typeof object.extended_master_key_birthday_timestamp === "object" ? object.extended_master_key_birthday_timestamp.low || object.extended_master_key_birthday_timestamp.high : $Number(object.extended_master_key_birthday_timestamp) !== 0)
                    if ($util.Long)
                        message.extended_master_key_birthday_timestamp = $util.Long.fromValue(object.extended_master_key_birthday_timestamp, true);
                    else if (typeof object.extended_master_key_birthday_timestamp === "string")
                        message.extended_master_key_birthday_timestamp = $parseInt(object.extended_master_key_birthday_timestamp, 10);
                    else if (typeof object.extended_master_key_birthday_timestamp === "number")
                        message.extended_master_key_birthday_timestamp = object.extended_master_key_birthday_timestamp;
                    else if (typeof object.extended_master_key_birthday_timestamp === "object")
                        message.extended_master_key_birthday_timestamp = new $util.LongBits(object.extended_master_key_birthday_timestamp.low >>> 0, object.extended_master_key_birthday_timestamp.high >>> 0).toNumber(true);
            if (object.watch_only != null) {
                if (!$util.isObject(object.watch_only))
                    throw $TypeError(".lnrpc.InitWalletRequest.watch_only: object expected");
                message.watch_only = $root.lnrpc.WatchOnly.fromObject(object.watch_only, _depth + 1);
            }
            if (object.macaroon_root_key != null)
                if (object.macaroon_root_key.length)
                    if (typeof object.macaroon_root_key === "string")
                        $util.base64.decode(object.macaroon_root_key, message.macaroon_root_key = $util.newBuffer($util.base64.length(object.macaroon_root_key)), 0);
                    else if (object.macaroon_root_key.length >= 0)
                        message.macaroon_root_key = object.macaroon_root_key;
            return message;
        };

        /**
         * Creates a plain object from an InitWalletRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.InitWalletRequest
         * @static
         * @param {lnrpc.InitWalletRequest} message InitWalletRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InitWalletRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.cipher_seed_mnemonic = [];
            if (options.defaults) {
                if (options.bytes === $String)
                    object.wallet_password = "";
                else {
                    object.wallet_password = [];
                    if (options.bytes !== $Array)
                        object.wallet_password = $util.newBuffer(object.wallet_password);
                }
                if (options.bytes === $String)
                    object.aezeed_passphrase = "";
                else {
                    object.aezeed_passphrase = [];
                    if (options.bytes !== $Array)
                        object.aezeed_passphrase = $util.newBuffer(object.aezeed_passphrase);
                }
                object.recovery_window = 0;
                object.channel_backups = null;
                object.stateless_init = false;
                object.extended_master_key = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.extended_master_key_birthday_timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.extended_master_key_birthday_timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.watch_only = null;
                if (options.bytes === $String)
                    object.macaroon_root_key = "";
                else {
                    object.macaroon_root_key = [];
                    if (options.bytes !== $Array)
                        object.macaroon_root_key = $util.newBuffer(object.macaroon_root_key);
                }
            }
            if (message.wallet_password != null && $Object.hasOwnProperty.call(message, "wallet_password"))
                object.wallet_password = options.bytes === $String ? $util.base64.encode(message.wallet_password, 0, message.wallet_password.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.wallet_password) : message.wallet_password;
            if (message.cipher_seed_mnemonic && message.cipher_seed_mnemonic.length) {
                object.cipher_seed_mnemonic = $Array(message.cipher_seed_mnemonic.length);
                for (let j = 0; j < message.cipher_seed_mnemonic.length; ++j)
                    object.cipher_seed_mnemonic[j] = message.cipher_seed_mnemonic[j];
            }
            if (message.aezeed_passphrase != null && $Object.hasOwnProperty.call(message, "aezeed_passphrase"))
                object.aezeed_passphrase = options.bytes === $String ? $util.base64.encode(message.aezeed_passphrase, 0, message.aezeed_passphrase.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.aezeed_passphrase) : message.aezeed_passphrase;
            if (message.recovery_window != null && $Object.hasOwnProperty.call(message, "recovery_window"))
                object.recovery_window = message.recovery_window;
            if (message.channel_backups != null && $Object.hasOwnProperty.call(message, "channel_backups"))
                object.channel_backups = $root.lnrpc.ChanBackupSnapshot.toObject(message.channel_backups, options, _depth + 1);
            if (message.stateless_init != null && $Object.hasOwnProperty.call(message, "stateless_init"))
                object.stateless_init = message.stateless_init;
            if (message.extended_master_key != null && $Object.hasOwnProperty.call(message, "extended_master_key"))
                object.extended_master_key = message.extended_master_key;
            if (message.extended_master_key_birthday_timestamp != null && $Object.hasOwnProperty.call(message, "extended_master_key_birthday_timestamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.extended_master_key_birthday_timestamp = typeof message.extended_master_key_birthday_timestamp === "number" ? $BigInt(message.extended_master_key_birthday_timestamp) : $util.Long.fromBits(message.extended_master_key_birthday_timestamp.low >>> 0, message.extended_master_key_birthday_timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.extended_master_key_birthday_timestamp === "number")
                    object.extended_master_key_birthday_timestamp = options.longs === $String ? $String(message.extended_master_key_birthday_timestamp) : message.extended_master_key_birthday_timestamp;
                else
                    object.extended_master_key_birthday_timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.extended_master_key_birthday_timestamp) : options.longs === $Number ? new $util.LongBits(message.extended_master_key_birthday_timestamp.low >>> 0, message.extended_master_key_birthday_timestamp.high >>> 0).toNumber(true) : message.extended_master_key_birthday_timestamp;
            if (message.watch_only != null && $Object.hasOwnProperty.call(message, "watch_only"))
                object.watch_only = $root.lnrpc.WatchOnly.toObject(message.watch_only, options, _depth + 1);
            if (message.macaroon_root_key != null && $Object.hasOwnProperty.call(message, "macaroon_root_key"))
                object.macaroon_root_key = options.bytes === $String ? $util.base64.encode(message.macaroon_root_key, 0, message.macaroon_root_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.macaroon_root_key) : message.macaroon_root_key;
            return object;
        };

        /**
         * Converts this InitWalletRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.InitWalletRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InitWalletRequest.prototype.toJSON = function() {
            return InitWalletRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InitWalletRequest
         * @function getTypeUrl
         * @memberof lnrpc.InitWalletRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InitWalletRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.InitWalletRequest";
        };

        return InitWalletRequest;
    })();

    lnrpc.InitWalletResponse = (function() {

        /**
         * Properties of an InitWalletResponse.
         * @typedef {Object} lnrpc.InitWalletResponse.$Properties
         * @property {Uint8Array|null} [admin_macaroon] InitWalletResponse admin_macaroon
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an InitWalletResponse.
         * @memberof lnrpc
         * @interface IInitWalletResponse
         * @augments lnrpc.InitWalletResponse.$Properties
         * @deprecated Use lnrpc.InitWalletResponse.$Properties instead.
         */

        /**
         * Shape of an InitWalletResponse.
         * @typedef {lnrpc.InitWalletResponse.$Properties} lnrpc.InitWalletResponse.$Shape
         */

        /**
         * Constructs a new InitWalletResponse.
         * @memberof lnrpc
         * @classdesc Represents an InitWalletResponse.
         * @constructor
         * @param {lnrpc.InitWalletResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const InitWalletResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InitWalletResponse admin_macaroon.
         * @member {Uint8Array} admin_macaroon
         * @memberof lnrpc.InitWalletResponse
         * @instance
         */
        InitWalletResponse.prototype.admin_macaroon = $util.newBuffer([]);

        /**
         * Creates a new InitWalletResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.InitWalletResponse
         * @static
         * @param {lnrpc.InitWalletResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.InitWalletResponse} InitWalletResponse instance
         * @type {{
         *   (properties: lnrpc.InitWalletResponse.$Shape): lnrpc.InitWalletResponse & lnrpc.InitWalletResponse.$Shape;
         *   (properties?: lnrpc.InitWalletResponse.$Properties): lnrpc.InitWalletResponse;
         * }}
         */
        InitWalletResponse.create = function(properties) {
            return new InitWalletResponse(properties);
        };

        /**
         * Encodes the specified InitWalletResponse message. Does not implicitly {@link lnrpc.InitWalletResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.InitWalletResponse
         * @static
         * @param {lnrpc.InitWalletResponse.$Properties} message InitWalletResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InitWalletResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.admin_macaroon != null && $Object.hasOwnProperty.call(message, "admin_macaroon") && message.admin_macaroon.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.admin_macaroon);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InitWalletResponse message, length delimited. Does not implicitly {@link lnrpc.InitWalletResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.InitWalletResponse
         * @static
         * @param {lnrpc.InitWalletResponse.$Properties} message InitWalletResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InitWalletResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InitWalletResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.InitWalletResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.InitWalletResponse & lnrpc.InitWalletResponse.$Shape} InitWalletResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InitWalletResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.InitWalletResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.admin_macaroon = value;
                        else
                            delete message.admin_macaroon;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InitWalletResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.InitWalletResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.InitWalletResponse & lnrpc.InitWalletResponse.$Shape} InitWalletResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InitWalletResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InitWalletResponse message.
         * @function verify
         * @memberof lnrpc.InitWalletResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InitWalletResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.admin_macaroon != null && $Object.hasOwnProperty.call(message, "admin_macaroon"))
                if (!(message.admin_macaroon && typeof message.admin_macaroon.length === "number" || $util.isString(message.admin_macaroon)))
                    return "admin_macaroon: buffer expected";
            return null;
        };

        /**
         * Creates an InitWalletResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.InitWalletResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.InitWalletResponse} InitWalletResponse
         */
        InitWalletResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.InitWalletResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.InitWalletResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.InitWalletResponse();
            if (object.admin_macaroon != null)
                if (object.admin_macaroon.length)
                    if (typeof object.admin_macaroon === "string")
                        $util.base64.decode(object.admin_macaroon, message.admin_macaroon = $util.newBuffer($util.base64.length(object.admin_macaroon)), 0);
                    else if (object.admin_macaroon.length >= 0)
                        message.admin_macaroon = object.admin_macaroon;
            return message;
        };

        /**
         * Creates a plain object from an InitWalletResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.InitWalletResponse
         * @static
         * @param {lnrpc.InitWalletResponse} message InitWalletResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InitWalletResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.admin_macaroon = "";
                else {
                    object.admin_macaroon = [];
                    if (options.bytes !== $Array)
                        object.admin_macaroon = $util.newBuffer(object.admin_macaroon);
                }
            if (message.admin_macaroon != null && $Object.hasOwnProperty.call(message, "admin_macaroon"))
                object.admin_macaroon = options.bytes === $String ? $util.base64.encode(message.admin_macaroon, 0, message.admin_macaroon.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.admin_macaroon) : message.admin_macaroon;
            return object;
        };

        /**
         * Converts this InitWalletResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.InitWalletResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InitWalletResponse.prototype.toJSON = function() {
            return InitWalletResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InitWalletResponse
         * @function getTypeUrl
         * @memberof lnrpc.InitWalletResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InitWalletResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.InitWalletResponse";
        };

        return InitWalletResponse;
    })();

    lnrpc.WatchOnly = (function() {

        /**
         * Properties of a WatchOnly.
         * @typedef {Object} lnrpc.WatchOnly.$Properties
         * @property {Long|null} [master_key_birthday_timestamp] WatchOnly master_key_birthday_timestamp
         * @property {Uint8Array|null} [master_key_fingerprint] WatchOnly master_key_fingerprint
         * @property {Array.<lnrpc.WatchOnlyAccount.$Properties>|null} [accounts] WatchOnly accounts
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a WatchOnly.
         * @memberof lnrpc
         * @interface IWatchOnly
         * @augments lnrpc.WatchOnly.$Properties
         * @deprecated Use lnrpc.WatchOnly.$Properties instead.
         */

        /**
         * Shape of a WatchOnly.
         * @typedef {lnrpc.WatchOnly.$Properties} lnrpc.WatchOnly.$Shape
         */

        /**
         * Constructs a new WatchOnly.
         * @memberof lnrpc
         * @classdesc Represents a WatchOnly.
         * @constructor
         * @param {lnrpc.WatchOnly.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const WatchOnly = function (properties) {
            this.accounts = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * WatchOnly master_key_birthday_timestamp.
         * @member {Long} master_key_birthday_timestamp
         * @memberof lnrpc.WatchOnly
         * @instance
         */
        WatchOnly.prototype.master_key_birthday_timestamp = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WatchOnly master_key_fingerprint.
         * @member {Uint8Array} master_key_fingerprint
         * @memberof lnrpc.WatchOnly
         * @instance
         */
        WatchOnly.prototype.master_key_fingerprint = $util.newBuffer([]);

        /**
         * WatchOnly accounts.
         * @member {Array.<lnrpc.WatchOnlyAccount.$Properties>} accounts
         * @memberof lnrpc.WatchOnly
         * @instance
         */
        WatchOnly.prototype.accounts = $util.emptyArray;

        /**
         * Creates a new WatchOnly instance using the specified properties.
         * @function create
         * @memberof lnrpc.WatchOnly
         * @static
         * @param {lnrpc.WatchOnly.$Properties=} [properties] Properties to set
         * @returns {lnrpc.WatchOnly} WatchOnly instance
         * @type {{
         *   (properties: lnrpc.WatchOnly.$Shape): lnrpc.WatchOnly & lnrpc.WatchOnly.$Shape;
         *   (properties?: lnrpc.WatchOnly.$Properties): lnrpc.WatchOnly;
         * }}
         */
        WatchOnly.create = function(properties) {
            return new WatchOnly(properties);
        };

        /**
         * Encodes the specified WatchOnly message. Does not implicitly {@link lnrpc.WatchOnly.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.WatchOnly
         * @static
         * @param {lnrpc.WatchOnly.$Properties} message WatchOnly message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WatchOnly.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.master_key_birthday_timestamp != null && $Object.hasOwnProperty.call(message, "master_key_birthday_timestamp") && (typeof message.master_key_birthday_timestamp === "object" ? message.master_key_birthday_timestamp.low || message.master_key_birthday_timestamp.high : message.master_key_birthday_timestamp !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.master_key_birthday_timestamp);
            if (message.master_key_fingerprint != null && $Object.hasOwnProperty.call(message, "master_key_fingerprint") && message.master_key_fingerprint.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.master_key_fingerprint);
            if (message.accounts != null && message.accounts.length)
                for (let i = 0; i < message.accounts.length; ++i)
                    $root.lnrpc.WatchOnlyAccount.encode(message.accounts[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified WatchOnly message, length delimited. Does not implicitly {@link lnrpc.WatchOnly.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.WatchOnly
         * @static
         * @param {lnrpc.WatchOnly.$Properties} message WatchOnly message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WatchOnly.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WatchOnly message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.WatchOnly
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.WatchOnly & lnrpc.WatchOnly.$Shape} WatchOnly
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WatchOnly.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.WatchOnly(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.master_key_birthday_timestamp = value;
                        else
                            delete message.master_key_birthday_timestamp;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.master_key_fingerprint = value;
                        else
                            delete message.master_key_fingerprint;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.accounts && message.accounts.length))
                            message.accounts = [];
                        message.accounts.push($root.lnrpc.WatchOnlyAccount.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a WatchOnly message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.WatchOnly
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.WatchOnly & lnrpc.WatchOnly.$Shape} WatchOnly
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WatchOnly.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WatchOnly message.
         * @function verify
         * @memberof lnrpc.WatchOnly
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WatchOnly.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.master_key_birthday_timestamp != null && $Object.hasOwnProperty.call(message, "master_key_birthday_timestamp"))
                if (!$util.isInteger(message.master_key_birthday_timestamp) && !(message.master_key_birthday_timestamp && $util.isInteger(message.master_key_birthday_timestamp.low) && $util.isInteger(message.master_key_birthday_timestamp.high)))
                    return "master_key_birthday_timestamp: integer|Long expected";
            if (message.master_key_fingerprint != null && $Object.hasOwnProperty.call(message, "master_key_fingerprint"))
                if (!(message.master_key_fingerprint && typeof message.master_key_fingerprint.length === "number" || $util.isString(message.master_key_fingerprint)))
                    return "master_key_fingerprint: buffer expected";
            if (message.accounts != null && $Object.hasOwnProperty.call(message, "accounts")) {
                if (!$Array.isArray(message.accounts))
                    return "accounts: array expected";
                for (let i = 0; i < message.accounts.length; ++i) {
                    let error = $root.lnrpc.WatchOnlyAccount.verify(message.accounts[i], _depth + 1);
                    if (error)
                        return "accounts." + error;
                }
            }
            return null;
        };

        /**
         * Creates a WatchOnly message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.WatchOnly
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.WatchOnly} WatchOnly
         */
        WatchOnly.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.WatchOnly)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.WatchOnly: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.WatchOnly();
            if (object.master_key_birthday_timestamp != null)
                if (typeof object.master_key_birthday_timestamp === "object" ? object.master_key_birthday_timestamp.low || object.master_key_birthday_timestamp.high : $Number(object.master_key_birthday_timestamp) !== 0)
                    if ($util.Long)
                        message.master_key_birthday_timestamp = $util.Long.fromValue(object.master_key_birthday_timestamp, true);
                    else if (typeof object.master_key_birthday_timestamp === "string")
                        message.master_key_birthday_timestamp = $parseInt(object.master_key_birthday_timestamp, 10);
                    else if (typeof object.master_key_birthday_timestamp === "number")
                        message.master_key_birthday_timestamp = object.master_key_birthday_timestamp;
                    else if (typeof object.master_key_birthday_timestamp === "object")
                        message.master_key_birthday_timestamp = new $util.LongBits(object.master_key_birthday_timestamp.low >>> 0, object.master_key_birthday_timestamp.high >>> 0).toNumber(true);
            if (object.master_key_fingerprint != null)
                if (object.master_key_fingerprint.length)
                    if (typeof object.master_key_fingerprint === "string")
                        $util.base64.decode(object.master_key_fingerprint, message.master_key_fingerprint = $util.newBuffer($util.base64.length(object.master_key_fingerprint)), 0);
                    else if (object.master_key_fingerprint.length >= 0)
                        message.master_key_fingerprint = object.master_key_fingerprint;
            if (object.accounts) {
                if (!$Array.isArray(object.accounts))
                    throw $TypeError(".lnrpc.WatchOnly.accounts: array expected");
                message.accounts = $Array(object.accounts.length);
                for (let i = 0; i < object.accounts.length; ++i) {
                    if (!$util.isObject(object.accounts[i]))
                        throw $TypeError(".lnrpc.WatchOnly.accounts: object expected");
                    message.accounts[i] = $root.lnrpc.WatchOnlyAccount.fromObject(object.accounts[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a WatchOnly message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.WatchOnly
         * @static
         * @param {lnrpc.WatchOnly} message WatchOnly
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WatchOnly.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.accounts = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.master_key_birthday_timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.master_key_birthday_timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.master_key_fingerprint = "";
                else {
                    object.master_key_fingerprint = [];
                    if (options.bytes !== $Array)
                        object.master_key_fingerprint = $util.newBuffer(object.master_key_fingerprint);
                }
            }
            if (message.master_key_birthday_timestamp != null && $Object.hasOwnProperty.call(message, "master_key_birthday_timestamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.master_key_birthday_timestamp = typeof message.master_key_birthday_timestamp === "number" ? $BigInt(message.master_key_birthday_timestamp) : $util.Long.fromBits(message.master_key_birthday_timestamp.low >>> 0, message.master_key_birthday_timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.master_key_birthday_timestamp === "number")
                    object.master_key_birthday_timestamp = options.longs === $String ? $String(message.master_key_birthday_timestamp) : message.master_key_birthday_timestamp;
                else
                    object.master_key_birthday_timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.master_key_birthday_timestamp) : options.longs === $Number ? new $util.LongBits(message.master_key_birthday_timestamp.low >>> 0, message.master_key_birthday_timestamp.high >>> 0).toNumber(true) : message.master_key_birthday_timestamp;
            if (message.master_key_fingerprint != null && $Object.hasOwnProperty.call(message, "master_key_fingerprint"))
                object.master_key_fingerprint = options.bytes === $String ? $util.base64.encode(message.master_key_fingerprint, 0, message.master_key_fingerprint.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.master_key_fingerprint) : message.master_key_fingerprint;
            if (message.accounts && message.accounts.length) {
                object.accounts = $Array(message.accounts.length);
                for (let j = 0; j < message.accounts.length; ++j)
                    object.accounts[j] = $root.lnrpc.WatchOnlyAccount.toObject(message.accounts[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this WatchOnly to JSON.
         * @function toJSON
         * @memberof lnrpc.WatchOnly
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WatchOnly.prototype.toJSON = function() {
            return WatchOnly.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for WatchOnly
         * @function getTypeUrl
         * @memberof lnrpc.WatchOnly
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        WatchOnly.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.WatchOnly";
        };

        return WatchOnly;
    })();

    lnrpc.WatchOnlyAccount = (function() {

        /**
         * Properties of a WatchOnlyAccount.
         * @typedef {Object} lnrpc.WatchOnlyAccount.$Properties
         * @property {number|null} [purpose] WatchOnlyAccount purpose
         * @property {number|null} [coin_type] WatchOnlyAccount coin_type
         * @property {number|null} [account] WatchOnlyAccount account
         * @property {string|null} [xpub] WatchOnlyAccount xpub
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a WatchOnlyAccount.
         * @memberof lnrpc
         * @interface IWatchOnlyAccount
         * @augments lnrpc.WatchOnlyAccount.$Properties
         * @deprecated Use lnrpc.WatchOnlyAccount.$Properties instead.
         */

        /**
         * Shape of a WatchOnlyAccount.
         * @typedef {lnrpc.WatchOnlyAccount.$Properties} lnrpc.WatchOnlyAccount.$Shape
         */

        /**
         * Constructs a new WatchOnlyAccount.
         * @memberof lnrpc
         * @classdesc Represents a WatchOnlyAccount.
         * @constructor
         * @param {lnrpc.WatchOnlyAccount.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const WatchOnlyAccount = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * WatchOnlyAccount purpose.
         * @member {number} purpose
         * @memberof lnrpc.WatchOnlyAccount
         * @instance
         */
        WatchOnlyAccount.prototype.purpose = 0;

        /**
         * WatchOnlyAccount coin_type.
         * @member {number} coin_type
         * @memberof lnrpc.WatchOnlyAccount
         * @instance
         */
        WatchOnlyAccount.prototype.coin_type = 0;

        /**
         * WatchOnlyAccount account.
         * @member {number} account
         * @memberof lnrpc.WatchOnlyAccount
         * @instance
         */
        WatchOnlyAccount.prototype.account = 0;

        /**
         * WatchOnlyAccount xpub.
         * @member {string} xpub
         * @memberof lnrpc.WatchOnlyAccount
         * @instance
         */
        WatchOnlyAccount.prototype.xpub = "";

        /**
         * Creates a new WatchOnlyAccount instance using the specified properties.
         * @function create
         * @memberof lnrpc.WatchOnlyAccount
         * @static
         * @param {lnrpc.WatchOnlyAccount.$Properties=} [properties] Properties to set
         * @returns {lnrpc.WatchOnlyAccount} WatchOnlyAccount instance
         * @type {{
         *   (properties: lnrpc.WatchOnlyAccount.$Shape): lnrpc.WatchOnlyAccount & lnrpc.WatchOnlyAccount.$Shape;
         *   (properties?: lnrpc.WatchOnlyAccount.$Properties): lnrpc.WatchOnlyAccount;
         * }}
         */
        WatchOnlyAccount.create = function(properties) {
            return new WatchOnlyAccount(properties);
        };

        /**
         * Encodes the specified WatchOnlyAccount message. Does not implicitly {@link lnrpc.WatchOnlyAccount.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.WatchOnlyAccount
         * @static
         * @param {lnrpc.WatchOnlyAccount.$Properties} message WatchOnlyAccount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WatchOnlyAccount.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.purpose != null && $Object.hasOwnProperty.call(message, "purpose") && message.purpose !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.purpose);
            if (message.coin_type != null && $Object.hasOwnProperty.call(message, "coin_type") && message.coin_type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.coin_type);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account") && message.account !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.account);
            if (message.xpub != null && $Object.hasOwnProperty.call(message, "xpub") && message.xpub !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.xpub);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified WatchOnlyAccount message, length delimited. Does not implicitly {@link lnrpc.WatchOnlyAccount.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.WatchOnlyAccount
         * @static
         * @param {lnrpc.WatchOnlyAccount.$Properties} message WatchOnlyAccount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WatchOnlyAccount.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WatchOnlyAccount message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.WatchOnlyAccount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.WatchOnlyAccount & lnrpc.WatchOnlyAccount.$Shape} WatchOnlyAccount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WatchOnlyAccount.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.WatchOnlyAccount(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.purpose = value;
                        else
                            delete message.purpose;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.coin_type = value;
                        else
                            delete message.coin_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.xpub = value;
                        else
                            delete message.xpub;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a WatchOnlyAccount message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.WatchOnlyAccount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.WatchOnlyAccount & lnrpc.WatchOnlyAccount.$Shape} WatchOnlyAccount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WatchOnlyAccount.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WatchOnlyAccount message.
         * @function verify
         * @memberof lnrpc.WatchOnlyAccount
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WatchOnlyAccount.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.purpose != null && $Object.hasOwnProperty.call(message, "purpose"))
                if (!$util.isInteger(message.purpose))
                    return "purpose: integer expected";
            if (message.coin_type != null && $Object.hasOwnProperty.call(message, "coin_type"))
                if (!$util.isInteger(message.coin_type))
                    return "coin_type: integer expected";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                if (!$util.isInteger(message.account))
                    return "account: integer expected";
            if (message.xpub != null && $Object.hasOwnProperty.call(message, "xpub"))
                if (!$util.isString(message.xpub))
                    return "xpub: string expected";
            return null;
        };

        /**
         * Creates a WatchOnlyAccount message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.WatchOnlyAccount
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.WatchOnlyAccount} WatchOnlyAccount
         */
        WatchOnlyAccount.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.WatchOnlyAccount)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.WatchOnlyAccount: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.WatchOnlyAccount();
            if (object.purpose != null)
                if ($Number(object.purpose) !== 0)
                    message.purpose = object.purpose >>> 0;
            if (object.coin_type != null)
                if ($Number(object.coin_type) !== 0)
                    message.coin_type = object.coin_type >>> 0;
            if (object.account != null)
                if ($Number(object.account) !== 0)
                    message.account = object.account >>> 0;
            if (object.xpub != null)
                if (typeof object.xpub !== "string" || object.xpub.length)
                    message.xpub = $String(object.xpub);
            return message;
        };

        /**
         * Creates a plain object from a WatchOnlyAccount message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.WatchOnlyAccount
         * @static
         * @param {lnrpc.WatchOnlyAccount} message WatchOnlyAccount
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WatchOnlyAccount.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.purpose = 0;
                object.coin_type = 0;
                object.account = 0;
                object.xpub = "";
            }
            if (message.purpose != null && $Object.hasOwnProperty.call(message, "purpose"))
                object.purpose = message.purpose;
            if (message.coin_type != null && $Object.hasOwnProperty.call(message, "coin_type"))
                object.coin_type = message.coin_type;
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = message.account;
            if (message.xpub != null && $Object.hasOwnProperty.call(message, "xpub"))
                object.xpub = message.xpub;
            return object;
        };

        /**
         * Converts this WatchOnlyAccount to JSON.
         * @function toJSON
         * @memberof lnrpc.WatchOnlyAccount
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WatchOnlyAccount.prototype.toJSON = function() {
            return WatchOnlyAccount.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for WatchOnlyAccount
         * @function getTypeUrl
         * @memberof lnrpc.WatchOnlyAccount
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        WatchOnlyAccount.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.WatchOnlyAccount";
        };

        return WatchOnlyAccount;
    })();

    lnrpc.UnlockWalletRequest = (function() {

        /**
         * Properties of an UnlockWalletRequest.
         * @typedef {Object} lnrpc.UnlockWalletRequest.$Properties
         * @property {Uint8Array|null} [wallet_password] UnlockWalletRequest wallet_password
         * @property {number|null} [recovery_window] UnlockWalletRequest recovery_window
         * @property {lnrpc.ChanBackupSnapshot.$Properties|null} [channel_backups] UnlockWalletRequest channel_backups
         * @property {boolean|null} [stateless_init] UnlockWalletRequest stateless_init
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an UnlockWalletRequest.
         * @memberof lnrpc
         * @interface IUnlockWalletRequest
         * @augments lnrpc.UnlockWalletRequest.$Properties
         * @deprecated Use lnrpc.UnlockWalletRequest.$Properties instead.
         */

        /**
         * Shape of an UnlockWalletRequest.
         * @typedef {{
         *   wallet_password?: Uint8Array|null;
         *   recovery_window?: number|null;
         *   channel_backups?: lnrpc.ChanBackupSnapshot.$Shape|null;
         *   stateless_init?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} lnrpc.UnlockWalletRequest.$Shape
         */

        /**
         * Constructs a new UnlockWalletRequest.
         * @memberof lnrpc
         * @classdesc Represents an UnlockWalletRequest.
         * @constructor
         * @param {lnrpc.UnlockWalletRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const UnlockWalletRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * UnlockWalletRequest wallet_password.
         * @member {Uint8Array} wallet_password
         * @memberof lnrpc.UnlockWalletRequest
         * @instance
         */
        UnlockWalletRequest.prototype.wallet_password = $util.newBuffer([]);

        /**
         * UnlockWalletRequest recovery_window.
         * @member {number} recovery_window
         * @memberof lnrpc.UnlockWalletRequest
         * @instance
         */
        UnlockWalletRequest.prototype.recovery_window = 0;

        /**
         * UnlockWalletRequest channel_backups.
         * @member {lnrpc.ChanBackupSnapshot.$Properties|null|undefined} channel_backups
         * @memberof lnrpc.UnlockWalletRequest
         * @instance
         */
        UnlockWalletRequest.prototype.channel_backups = null;

        /**
         * UnlockWalletRequest stateless_init.
         * @member {boolean} stateless_init
         * @memberof lnrpc.UnlockWalletRequest
         * @instance
         */
        UnlockWalletRequest.prototype.stateless_init = false;

        /**
         * Creates a new UnlockWalletRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.UnlockWalletRequest
         * @static
         * @param {lnrpc.UnlockWalletRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.UnlockWalletRequest} UnlockWalletRequest instance
         * @type {{
         *   (properties: lnrpc.UnlockWalletRequest.$Shape): lnrpc.UnlockWalletRequest & lnrpc.UnlockWalletRequest.$Shape;
         *   (properties?: lnrpc.UnlockWalletRequest.$Properties): lnrpc.UnlockWalletRequest;
         * }}
         */
        UnlockWalletRequest.create = function(properties) {
            return new UnlockWalletRequest(properties);
        };

        /**
         * Encodes the specified UnlockWalletRequest message. Does not implicitly {@link lnrpc.UnlockWalletRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.UnlockWalletRequest
         * @static
         * @param {lnrpc.UnlockWalletRequest.$Properties} message UnlockWalletRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UnlockWalletRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.wallet_password != null && $Object.hasOwnProperty.call(message, "wallet_password") && message.wallet_password.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.wallet_password);
            if (message.recovery_window != null && $Object.hasOwnProperty.call(message, "recovery_window") && message.recovery_window !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.recovery_window);
            if (message.channel_backups != null && $Object.hasOwnProperty.call(message, "channel_backups"))
                $root.lnrpc.ChanBackupSnapshot.encode(message.channel_backups, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.stateless_init != null && $Object.hasOwnProperty.call(message, "stateless_init") && message.stateless_init !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.stateless_init);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UnlockWalletRequest message, length delimited. Does not implicitly {@link lnrpc.UnlockWalletRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.UnlockWalletRequest
         * @static
         * @param {lnrpc.UnlockWalletRequest.$Properties} message UnlockWalletRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UnlockWalletRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an UnlockWalletRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.UnlockWalletRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.UnlockWalletRequest & lnrpc.UnlockWalletRequest.$Shape} UnlockWalletRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UnlockWalletRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.UnlockWalletRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.wallet_password = value;
                        else
                            delete message.wallet_password;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.recovery_window = value;
                        else
                            delete message.recovery_window;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.channel_backups = $root.lnrpc.ChanBackupSnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.channel_backups);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.stateless_init = value;
                        else
                            delete message.stateless_init;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UnlockWalletRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.UnlockWalletRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.UnlockWalletRequest & lnrpc.UnlockWalletRequest.$Shape} UnlockWalletRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UnlockWalletRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UnlockWalletRequest message.
         * @function verify
         * @memberof lnrpc.UnlockWalletRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UnlockWalletRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.wallet_password != null && $Object.hasOwnProperty.call(message, "wallet_password"))
                if (!(message.wallet_password && typeof message.wallet_password.length === "number" || $util.isString(message.wallet_password)))
                    return "wallet_password: buffer expected";
            if (message.recovery_window != null && $Object.hasOwnProperty.call(message, "recovery_window"))
                if (!$util.isInteger(message.recovery_window))
                    return "recovery_window: integer expected";
            if (message.channel_backups != null && $Object.hasOwnProperty.call(message, "channel_backups")) {
                let error = $root.lnrpc.ChanBackupSnapshot.verify(message.channel_backups, _depth + 1);
                if (error)
                    return "channel_backups." + error;
            }
            if (message.stateless_init != null && $Object.hasOwnProperty.call(message, "stateless_init"))
                if (typeof message.stateless_init !== "boolean")
                    return "stateless_init: boolean expected";
            return null;
        };

        /**
         * Creates an UnlockWalletRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.UnlockWalletRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.UnlockWalletRequest} UnlockWalletRequest
         */
        UnlockWalletRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.UnlockWalletRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.UnlockWalletRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.UnlockWalletRequest();
            if (object.wallet_password != null)
                if (object.wallet_password.length)
                    if (typeof object.wallet_password === "string")
                        $util.base64.decode(object.wallet_password, message.wallet_password = $util.newBuffer($util.base64.length(object.wallet_password)), 0);
                    else if (object.wallet_password.length >= 0)
                        message.wallet_password = object.wallet_password;
            if (object.recovery_window != null)
                if ($Number(object.recovery_window) !== 0)
                    message.recovery_window = object.recovery_window | 0;
            if (object.channel_backups != null) {
                if (!$util.isObject(object.channel_backups))
                    throw $TypeError(".lnrpc.UnlockWalletRequest.channel_backups: object expected");
                message.channel_backups = $root.lnrpc.ChanBackupSnapshot.fromObject(object.channel_backups, _depth + 1);
            }
            if (object.stateless_init != null)
                if (object.stateless_init)
                    message.stateless_init = $Boolean(object.stateless_init);
            return message;
        };

        /**
         * Creates a plain object from an UnlockWalletRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.UnlockWalletRequest
         * @static
         * @param {lnrpc.UnlockWalletRequest} message UnlockWalletRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UnlockWalletRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.wallet_password = "";
                else {
                    object.wallet_password = [];
                    if (options.bytes !== $Array)
                        object.wallet_password = $util.newBuffer(object.wallet_password);
                }
                object.recovery_window = 0;
                object.channel_backups = null;
                object.stateless_init = false;
            }
            if (message.wallet_password != null && $Object.hasOwnProperty.call(message, "wallet_password"))
                object.wallet_password = options.bytes === $String ? $util.base64.encode(message.wallet_password, 0, message.wallet_password.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.wallet_password) : message.wallet_password;
            if (message.recovery_window != null && $Object.hasOwnProperty.call(message, "recovery_window"))
                object.recovery_window = message.recovery_window;
            if (message.channel_backups != null && $Object.hasOwnProperty.call(message, "channel_backups"))
                object.channel_backups = $root.lnrpc.ChanBackupSnapshot.toObject(message.channel_backups, options, _depth + 1);
            if (message.stateless_init != null && $Object.hasOwnProperty.call(message, "stateless_init"))
                object.stateless_init = message.stateless_init;
            return object;
        };

        /**
         * Converts this UnlockWalletRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.UnlockWalletRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UnlockWalletRequest.prototype.toJSON = function() {
            return UnlockWalletRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UnlockWalletRequest
         * @function getTypeUrl
         * @memberof lnrpc.UnlockWalletRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UnlockWalletRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.UnlockWalletRequest";
        };

        return UnlockWalletRequest;
    })();

    lnrpc.UnlockWalletResponse = (function() {

        /**
         * Properties of an UnlockWalletResponse.
         * @typedef {Object} lnrpc.UnlockWalletResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an UnlockWalletResponse.
         * @memberof lnrpc
         * @interface IUnlockWalletResponse
         * @augments lnrpc.UnlockWalletResponse.$Properties
         * @deprecated Use lnrpc.UnlockWalletResponse.$Properties instead.
         */

        /**
         * Shape of an UnlockWalletResponse.
         * @typedef {lnrpc.UnlockWalletResponse.$Properties} lnrpc.UnlockWalletResponse.$Shape
         */

        /**
         * Constructs a new UnlockWalletResponse.
         * @memberof lnrpc
         * @classdesc Represents an UnlockWalletResponse.
         * @constructor
         * @param {lnrpc.UnlockWalletResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const UnlockWalletResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new UnlockWalletResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.UnlockWalletResponse
         * @static
         * @param {lnrpc.UnlockWalletResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.UnlockWalletResponse} UnlockWalletResponse instance
         * @type {{
         *   (properties: lnrpc.UnlockWalletResponse.$Shape): lnrpc.UnlockWalletResponse & lnrpc.UnlockWalletResponse.$Shape;
         *   (properties?: lnrpc.UnlockWalletResponse.$Properties): lnrpc.UnlockWalletResponse;
         * }}
         */
        UnlockWalletResponse.create = function(properties) {
            return new UnlockWalletResponse(properties);
        };

        /**
         * Encodes the specified UnlockWalletResponse message. Does not implicitly {@link lnrpc.UnlockWalletResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.UnlockWalletResponse
         * @static
         * @param {lnrpc.UnlockWalletResponse.$Properties} message UnlockWalletResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UnlockWalletResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UnlockWalletResponse message, length delimited. Does not implicitly {@link lnrpc.UnlockWalletResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.UnlockWalletResponse
         * @static
         * @param {lnrpc.UnlockWalletResponse.$Properties} message UnlockWalletResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UnlockWalletResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an UnlockWalletResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.UnlockWalletResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.UnlockWalletResponse & lnrpc.UnlockWalletResponse.$Shape} UnlockWalletResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UnlockWalletResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.UnlockWalletResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UnlockWalletResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.UnlockWalletResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.UnlockWalletResponse & lnrpc.UnlockWalletResponse.$Shape} UnlockWalletResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UnlockWalletResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UnlockWalletResponse message.
         * @function verify
         * @memberof lnrpc.UnlockWalletResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UnlockWalletResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates an UnlockWalletResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.UnlockWalletResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.UnlockWalletResponse} UnlockWalletResponse
         */
        UnlockWalletResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.UnlockWalletResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.UnlockWalletResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.lnrpc.UnlockWalletResponse();
        };

        /**
         * Creates a plain object from an UnlockWalletResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.UnlockWalletResponse
         * @static
         * @param {lnrpc.UnlockWalletResponse} message UnlockWalletResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UnlockWalletResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this UnlockWalletResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.UnlockWalletResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UnlockWalletResponse.prototype.toJSON = function() {
            return UnlockWalletResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UnlockWalletResponse
         * @function getTypeUrl
         * @memberof lnrpc.UnlockWalletResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UnlockWalletResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.UnlockWalletResponse";
        };

        return UnlockWalletResponse;
    })();

    lnrpc.ChangePasswordRequest = (function() {

        /**
         * Properties of a ChangePasswordRequest.
         * @typedef {Object} lnrpc.ChangePasswordRequest.$Properties
         * @property {Uint8Array|null} [current_password] ChangePasswordRequest current_password
         * @property {Uint8Array|null} [new_password] ChangePasswordRequest new_password
         * @property {boolean|null} [stateless_init] ChangePasswordRequest stateless_init
         * @property {boolean|null} [new_macaroon_root_key] ChangePasswordRequest new_macaroon_root_key
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChangePasswordRequest.
         * @memberof lnrpc
         * @interface IChangePasswordRequest
         * @augments lnrpc.ChangePasswordRequest.$Properties
         * @deprecated Use lnrpc.ChangePasswordRequest.$Properties instead.
         */

        /**
         * Shape of a ChangePasswordRequest.
         * @typedef {lnrpc.ChangePasswordRequest.$Properties} lnrpc.ChangePasswordRequest.$Shape
         */

        /**
         * Constructs a new ChangePasswordRequest.
         * @memberof lnrpc
         * @classdesc Represents a ChangePasswordRequest.
         * @constructor
         * @param {lnrpc.ChangePasswordRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChangePasswordRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChangePasswordRequest current_password.
         * @member {Uint8Array} current_password
         * @memberof lnrpc.ChangePasswordRequest
         * @instance
         */
        ChangePasswordRequest.prototype.current_password = $util.newBuffer([]);

        /**
         * ChangePasswordRequest new_password.
         * @member {Uint8Array} new_password
         * @memberof lnrpc.ChangePasswordRequest
         * @instance
         */
        ChangePasswordRequest.prototype.new_password = $util.newBuffer([]);

        /**
         * ChangePasswordRequest stateless_init.
         * @member {boolean} stateless_init
         * @memberof lnrpc.ChangePasswordRequest
         * @instance
         */
        ChangePasswordRequest.prototype.stateless_init = false;

        /**
         * ChangePasswordRequest new_macaroon_root_key.
         * @member {boolean} new_macaroon_root_key
         * @memberof lnrpc.ChangePasswordRequest
         * @instance
         */
        ChangePasswordRequest.prototype.new_macaroon_root_key = false;

        /**
         * Creates a new ChangePasswordRequest instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChangePasswordRequest
         * @static
         * @param {lnrpc.ChangePasswordRequest.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChangePasswordRequest} ChangePasswordRequest instance
         * @type {{
         *   (properties: lnrpc.ChangePasswordRequest.$Shape): lnrpc.ChangePasswordRequest & lnrpc.ChangePasswordRequest.$Shape;
         *   (properties?: lnrpc.ChangePasswordRequest.$Properties): lnrpc.ChangePasswordRequest;
         * }}
         */
        ChangePasswordRequest.create = function(properties) {
            return new ChangePasswordRequest(properties);
        };

        /**
         * Encodes the specified ChangePasswordRequest message. Does not implicitly {@link lnrpc.ChangePasswordRequest.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChangePasswordRequest
         * @static
         * @param {lnrpc.ChangePasswordRequest.$Properties} message ChangePasswordRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangePasswordRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.current_password != null && $Object.hasOwnProperty.call(message, "current_password") && message.current_password.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.current_password);
            if (message.new_password != null && $Object.hasOwnProperty.call(message, "new_password") && message.new_password.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.new_password);
            if (message.stateless_init != null && $Object.hasOwnProperty.call(message, "stateless_init") && message.stateless_init !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.stateless_init);
            if (message.new_macaroon_root_key != null && $Object.hasOwnProperty.call(message, "new_macaroon_root_key") && message.new_macaroon_root_key !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.new_macaroon_root_key);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChangePasswordRequest message, length delimited. Does not implicitly {@link lnrpc.ChangePasswordRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChangePasswordRequest
         * @static
         * @param {lnrpc.ChangePasswordRequest.$Properties} message ChangePasswordRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangePasswordRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChangePasswordRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChangePasswordRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChangePasswordRequest & lnrpc.ChangePasswordRequest.$Shape} ChangePasswordRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangePasswordRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChangePasswordRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.current_password = value;
                        else
                            delete message.current_password;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.new_password = value;
                        else
                            delete message.new_password;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.stateless_init = value;
                        else
                            delete message.stateless_init;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.new_macaroon_root_key = value;
                        else
                            delete message.new_macaroon_root_key;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChangePasswordRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChangePasswordRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChangePasswordRequest & lnrpc.ChangePasswordRequest.$Shape} ChangePasswordRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangePasswordRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChangePasswordRequest message.
         * @function verify
         * @memberof lnrpc.ChangePasswordRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChangePasswordRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.current_password != null && $Object.hasOwnProperty.call(message, "current_password"))
                if (!(message.current_password && typeof message.current_password.length === "number" || $util.isString(message.current_password)))
                    return "current_password: buffer expected";
            if (message.new_password != null && $Object.hasOwnProperty.call(message, "new_password"))
                if (!(message.new_password && typeof message.new_password.length === "number" || $util.isString(message.new_password)))
                    return "new_password: buffer expected";
            if (message.stateless_init != null && $Object.hasOwnProperty.call(message, "stateless_init"))
                if (typeof message.stateless_init !== "boolean")
                    return "stateless_init: boolean expected";
            if (message.new_macaroon_root_key != null && $Object.hasOwnProperty.call(message, "new_macaroon_root_key"))
                if (typeof message.new_macaroon_root_key !== "boolean")
                    return "new_macaroon_root_key: boolean expected";
            return null;
        };

        /**
         * Creates a ChangePasswordRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChangePasswordRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChangePasswordRequest} ChangePasswordRequest
         */
        ChangePasswordRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChangePasswordRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChangePasswordRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChangePasswordRequest();
            if (object.current_password != null)
                if (object.current_password.length)
                    if (typeof object.current_password === "string")
                        $util.base64.decode(object.current_password, message.current_password = $util.newBuffer($util.base64.length(object.current_password)), 0);
                    else if (object.current_password.length >= 0)
                        message.current_password = object.current_password;
            if (object.new_password != null)
                if (object.new_password.length)
                    if (typeof object.new_password === "string")
                        $util.base64.decode(object.new_password, message.new_password = $util.newBuffer($util.base64.length(object.new_password)), 0);
                    else if (object.new_password.length >= 0)
                        message.new_password = object.new_password;
            if (object.stateless_init != null)
                if (object.stateless_init)
                    message.stateless_init = $Boolean(object.stateless_init);
            if (object.new_macaroon_root_key != null)
                if (object.new_macaroon_root_key)
                    message.new_macaroon_root_key = $Boolean(object.new_macaroon_root_key);
            return message;
        };

        /**
         * Creates a plain object from a ChangePasswordRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChangePasswordRequest
         * @static
         * @param {lnrpc.ChangePasswordRequest} message ChangePasswordRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangePasswordRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.current_password = "";
                else {
                    object.current_password = [];
                    if (options.bytes !== $Array)
                        object.current_password = $util.newBuffer(object.current_password);
                }
                if (options.bytes === $String)
                    object.new_password = "";
                else {
                    object.new_password = [];
                    if (options.bytes !== $Array)
                        object.new_password = $util.newBuffer(object.new_password);
                }
                object.stateless_init = false;
                object.new_macaroon_root_key = false;
            }
            if (message.current_password != null && $Object.hasOwnProperty.call(message, "current_password"))
                object.current_password = options.bytes === $String ? $util.base64.encode(message.current_password, 0, message.current_password.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.current_password) : message.current_password;
            if (message.new_password != null && $Object.hasOwnProperty.call(message, "new_password"))
                object.new_password = options.bytes === $String ? $util.base64.encode(message.new_password, 0, message.new_password.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.new_password) : message.new_password;
            if (message.stateless_init != null && $Object.hasOwnProperty.call(message, "stateless_init"))
                object.stateless_init = message.stateless_init;
            if (message.new_macaroon_root_key != null && $Object.hasOwnProperty.call(message, "new_macaroon_root_key"))
                object.new_macaroon_root_key = message.new_macaroon_root_key;
            return object;
        };

        /**
         * Converts this ChangePasswordRequest to JSON.
         * @function toJSON
         * @memberof lnrpc.ChangePasswordRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangePasswordRequest.prototype.toJSON = function() {
            return ChangePasswordRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChangePasswordRequest
         * @function getTypeUrl
         * @memberof lnrpc.ChangePasswordRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChangePasswordRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChangePasswordRequest";
        };

        return ChangePasswordRequest;
    })();

    lnrpc.ChangePasswordResponse = (function() {

        /**
         * Properties of a ChangePasswordResponse.
         * @typedef {Object} lnrpc.ChangePasswordResponse.$Properties
         * @property {Uint8Array|null} [admin_macaroon] ChangePasswordResponse admin_macaroon
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ChangePasswordResponse.
         * @memberof lnrpc
         * @interface IChangePasswordResponse
         * @augments lnrpc.ChangePasswordResponse.$Properties
         * @deprecated Use lnrpc.ChangePasswordResponse.$Properties instead.
         */

        /**
         * Shape of a ChangePasswordResponse.
         * @typedef {lnrpc.ChangePasswordResponse.$Properties} lnrpc.ChangePasswordResponse.$Shape
         */

        /**
         * Constructs a new ChangePasswordResponse.
         * @memberof lnrpc
         * @classdesc Represents a ChangePasswordResponse.
         * @constructor
         * @param {lnrpc.ChangePasswordResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ChangePasswordResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChangePasswordResponse admin_macaroon.
         * @member {Uint8Array} admin_macaroon
         * @memberof lnrpc.ChangePasswordResponse
         * @instance
         */
        ChangePasswordResponse.prototype.admin_macaroon = $util.newBuffer([]);

        /**
         * Creates a new ChangePasswordResponse instance using the specified properties.
         * @function create
         * @memberof lnrpc.ChangePasswordResponse
         * @static
         * @param {lnrpc.ChangePasswordResponse.$Properties=} [properties] Properties to set
         * @returns {lnrpc.ChangePasswordResponse} ChangePasswordResponse instance
         * @type {{
         *   (properties: lnrpc.ChangePasswordResponse.$Shape): lnrpc.ChangePasswordResponse & lnrpc.ChangePasswordResponse.$Shape;
         *   (properties?: lnrpc.ChangePasswordResponse.$Properties): lnrpc.ChangePasswordResponse;
         * }}
         */
        ChangePasswordResponse.create = function(properties) {
            return new ChangePasswordResponse(properties);
        };

        /**
         * Encodes the specified ChangePasswordResponse message. Does not implicitly {@link lnrpc.ChangePasswordResponse.verify|verify} messages.
         * @function encode
         * @memberof lnrpc.ChangePasswordResponse
         * @static
         * @param {lnrpc.ChangePasswordResponse.$Properties} message ChangePasswordResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangePasswordResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.admin_macaroon != null && $Object.hasOwnProperty.call(message, "admin_macaroon") && message.admin_macaroon.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.admin_macaroon);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChangePasswordResponse message, length delimited. Does not implicitly {@link lnrpc.ChangePasswordResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lnrpc.ChangePasswordResponse
         * @static
         * @param {lnrpc.ChangePasswordResponse.$Properties} message ChangePasswordResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangePasswordResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChangePasswordResponse message from the specified reader or buffer.
         * @function decode
         * @memberof lnrpc.ChangePasswordResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lnrpc.ChangePasswordResponse & lnrpc.ChangePasswordResponse.$Shape} ChangePasswordResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangePasswordResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.lnrpc.ChangePasswordResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.admin_macaroon = value;
                        else
                            delete message.admin_macaroon;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChangePasswordResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lnrpc.ChangePasswordResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lnrpc.ChangePasswordResponse & lnrpc.ChangePasswordResponse.$Shape} ChangePasswordResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangePasswordResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChangePasswordResponse message.
         * @function verify
         * @memberof lnrpc.ChangePasswordResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChangePasswordResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.admin_macaroon != null && $Object.hasOwnProperty.call(message, "admin_macaroon"))
                if (!(message.admin_macaroon && typeof message.admin_macaroon.length === "number" || $util.isString(message.admin_macaroon)))
                    return "admin_macaroon: buffer expected";
            return null;
        };

        /**
         * Creates a ChangePasswordResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lnrpc.ChangePasswordResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lnrpc.ChangePasswordResponse} ChangePasswordResponse
         */
        ChangePasswordResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.lnrpc.ChangePasswordResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lnrpc.ChangePasswordResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.lnrpc.ChangePasswordResponse();
            if (object.admin_macaroon != null)
                if (object.admin_macaroon.length)
                    if (typeof object.admin_macaroon === "string")
                        $util.base64.decode(object.admin_macaroon, message.admin_macaroon = $util.newBuffer($util.base64.length(object.admin_macaroon)), 0);
                    else if (object.admin_macaroon.length >= 0)
                        message.admin_macaroon = object.admin_macaroon;
            return message;
        };

        /**
         * Creates a plain object from a ChangePasswordResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lnrpc.ChangePasswordResponse
         * @static
         * @param {lnrpc.ChangePasswordResponse} message ChangePasswordResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangePasswordResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.admin_macaroon = "";
                else {
                    object.admin_macaroon = [];
                    if (options.bytes !== $Array)
                        object.admin_macaroon = $util.newBuffer(object.admin_macaroon);
                }
            if (message.admin_macaroon != null && $Object.hasOwnProperty.call(message, "admin_macaroon"))
                object.admin_macaroon = options.bytes === $String ? $util.base64.encode(message.admin_macaroon, 0, message.admin_macaroon.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.admin_macaroon) : message.admin_macaroon;
            return object;
        };

        /**
         * Converts this ChangePasswordResponse to JSON.
         * @function toJSON
         * @memberof lnrpc.ChangePasswordResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangePasswordResponse.prototype.toJSON = function() {
            return ChangePasswordResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChangePasswordResponse
         * @function getTypeUrl
         * @memberof lnrpc.ChangePasswordResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChangePasswordResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lnrpc.ChangePasswordResponse";
        };

        return ChangePasswordResponse;
    })();

    return lnrpc;
})();

export const autopilotrpc = $root.autopilotrpc = (() => {

    /**
     * Namespace autopilotrpc.
     * @exports autopilotrpc
     * @namespace
     */
    const autopilotrpc = {};

    autopilotrpc.Autopilot = (function() {

        /**
         * Constructs a new Autopilot service.
         * @memberof autopilotrpc
         * @classdesc Represents an Autopilot
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const Autopilot = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (Autopilot.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = Autopilot;

        /**
         * Creates new Autopilot service using the specified rpc implementation.
         * @function create
         * @memberof autopilotrpc.Autopilot
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {Autopilot} RPC service. Useful where requests and/or responses are streamed.
         */
        Autopilot.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link autopilotrpc.Autopilot#status}.
         * @memberof autopilotrpc.Autopilot
         * @typedef StatusCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {autopilotrpc.StatusResponse} [response] StatusResponse
         */

        /**
         * Calls Status.
         * @memberof autopilotrpc.Autopilot
         * @typedef Status
         * @type {{
         *   (request: autopilotrpc.IStatusRequest, callback: autopilotrpc.Autopilot.StatusCallback): void;
         *   (request: autopilotrpc.IStatusRequest): Promise<autopilotrpc.StatusResponse>;
         *   readonly name: "Status";
         *   readonly path: "/autopilotrpc.Autopilot/Status";
         *   readonly requestType: "StatusRequest";
         *   readonly responseType: "StatusResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls Status.
         * @name autopilotrpc.Autopilot#status
         * @type {autopilotrpc.Autopilot.Status}
         */
        $Object.defineProperties(Autopilot.prototype.status = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Autopilot.prototype.status, $root.autopilotrpc.StatusRequest, $root.autopilotrpc.StatusResponse, request, callback);
        }, {
            name: { value: "Status" },
            path: { value: "/autopilotrpc.Autopilot/Status" },
            requestType: { value: "StatusRequest" },
            responseType: { value: "StatusResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link autopilotrpc.Autopilot#modifyStatus}.
         * @memberof autopilotrpc.Autopilot
         * @typedef ModifyStatusCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {autopilotrpc.ModifyStatusResponse} [response] ModifyStatusResponse
         */

        /**
         * Calls ModifyStatus.
         * @memberof autopilotrpc.Autopilot
         * @typedef ModifyStatus
         * @type {{
         *   (request: autopilotrpc.IModifyStatusRequest, callback: autopilotrpc.Autopilot.ModifyStatusCallback): void;
         *   (request: autopilotrpc.IModifyStatusRequest): Promise<autopilotrpc.ModifyStatusResponse>;
         *   readonly name: "ModifyStatus";
         *   readonly path: "/autopilotrpc.Autopilot/ModifyStatus";
         *   readonly requestType: "ModifyStatusRequest";
         *   readonly responseType: "ModifyStatusResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ModifyStatus.
         * @name autopilotrpc.Autopilot#modifyStatus
         * @type {autopilotrpc.Autopilot.ModifyStatus}
         */
        $Object.defineProperties(Autopilot.prototype.modifyStatus = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Autopilot.prototype.modifyStatus, $root.autopilotrpc.ModifyStatusRequest, $root.autopilotrpc.ModifyStatusResponse, request, callback);
        }, {
            name: { value: "ModifyStatus" },
            path: { value: "/autopilotrpc.Autopilot/ModifyStatus" },
            requestType: { value: "ModifyStatusRequest" },
            responseType: { value: "ModifyStatusResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link autopilotrpc.Autopilot#queryScores}.
         * @memberof autopilotrpc.Autopilot
         * @typedef QueryScoresCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {autopilotrpc.QueryScoresResponse} [response] QueryScoresResponse
         */

        /**
         * Calls QueryScores.
         * @memberof autopilotrpc.Autopilot
         * @typedef QueryScores
         * @type {{
         *   (request: autopilotrpc.IQueryScoresRequest, callback: autopilotrpc.Autopilot.QueryScoresCallback): void;
         *   (request: autopilotrpc.IQueryScoresRequest): Promise<autopilotrpc.QueryScoresResponse>;
         *   readonly name: "QueryScores";
         *   readonly path: "/autopilotrpc.Autopilot/QueryScores";
         *   readonly requestType: "QueryScoresRequest";
         *   readonly responseType: "QueryScoresResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls QueryScores.
         * @name autopilotrpc.Autopilot#queryScores
         * @type {autopilotrpc.Autopilot.QueryScores}
         */
        $Object.defineProperties(Autopilot.prototype.queryScores = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Autopilot.prototype.queryScores, $root.autopilotrpc.QueryScoresRequest, $root.autopilotrpc.QueryScoresResponse, request, callback);
        }, {
            name: { value: "QueryScores" },
            path: { value: "/autopilotrpc.Autopilot/QueryScores" },
            requestType: { value: "QueryScoresRequest" },
            responseType: { value: "QueryScoresResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link autopilotrpc.Autopilot#setScores}.
         * @memberof autopilotrpc.Autopilot
         * @typedef SetScoresCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {autopilotrpc.SetScoresResponse} [response] SetScoresResponse
         */

        /**
         * Calls SetScores.
         * @memberof autopilotrpc.Autopilot
         * @typedef SetScores
         * @type {{
         *   (request: autopilotrpc.ISetScoresRequest, callback: autopilotrpc.Autopilot.SetScoresCallback): void;
         *   (request: autopilotrpc.ISetScoresRequest): Promise<autopilotrpc.SetScoresResponse>;
         *   readonly name: "SetScores";
         *   readonly path: "/autopilotrpc.Autopilot/SetScores";
         *   readonly requestType: "SetScoresRequest";
         *   readonly responseType: "SetScoresResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SetScores.
         * @name autopilotrpc.Autopilot#setScores
         * @type {autopilotrpc.Autopilot.SetScores}
         */
        $Object.defineProperties(Autopilot.prototype.setScores = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Autopilot.prototype.setScores, $root.autopilotrpc.SetScoresRequest, $root.autopilotrpc.SetScoresResponse, request, callback);
        }, {
            name: { value: "SetScores" },
            path: { value: "/autopilotrpc.Autopilot/SetScores" },
            requestType: { value: "SetScoresRequest" },
            responseType: { value: "SetScoresResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return Autopilot;
    })();

    autopilotrpc.StatusRequest = (function() {

        /**
         * Properties of a StatusRequest.
         * @typedef {Object} autopilotrpc.StatusRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a StatusRequest.
         * @memberof autopilotrpc
         * @interface IStatusRequest
         * @augments autopilotrpc.StatusRequest.$Properties
         * @deprecated Use autopilotrpc.StatusRequest.$Properties instead.
         */

        /**
         * Shape of a StatusRequest.
         * @typedef {autopilotrpc.StatusRequest.$Properties} autopilotrpc.StatusRequest.$Shape
         */

        /**
         * Constructs a new StatusRequest.
         * @memberof autopilotrpc
         * @classdesc Represents a StatusRequest.
         * @constructor
         * @param {autopilotrpc.StatusRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const StatusRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new StatusRequest instance using the specified properties.
         * @function create
         * @memberof autopilotrpc.StatusRequest
         * @static
         * @param {autopilotrpc.StatusRequest.$Properties=} [properties] Properties to set
         * @returns {autopilotrpc.StatusRequest} StatusRequest instance
         * @type {{
         *   (properties: autopilotrpc.StatusRequest.$Shape): autopilotrpc.StatusRequest & autopilotrpc.StatusRequest.$Shape;
         *   (properties?: autopilotrpc.StatusRequest.$Properties): autopilotrpc.StatusRequest;
         * }}
         */
        StatusRequest.create = function(properties) {
            return new StatusRequest(properties);
        };

        /**
         * Encodes the specified StatusRequest message. Does not implicitly {@link autopilotrpc.StatusRequest.verify|verify} messages.
         * @function encode
         * @memberof autopilotrpc.StatusRequest
         * @static
         * @param {autopilotrpc.StatusRequest.$Properties} message StatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StatusRequest message, length delimited. Does not implicitly {@link autopilotrpc.StatusRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof autopilotrpc.StatusRequest
         * @static
         * @param {autopilotrpc.StatusRequest.$Properties} message StatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StatusRequest message from the specified reader or buffer.
         * @function decode
         * @memberof autopilotrpc.StatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {autopilotrpc.StatusRequest & autopilotrpc.StatusRequest.$Shape} StatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.autopilotrpc.StatusRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StatusRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof autopilotrpc.StatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {autopilotrpc.StatusRequest & autopilotrpc.StatusRequest.$Shape} StatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatusRequest message.
         * @function verify
         * @memberof autopilotrpc.StatusRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatusRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a StatusRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof autopilotrpc.StatusRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {autopilotrpc.StatusRequest} StatusRequest
         */
        StatusRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.autopilotrpc.StatusRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".autopilotrpc.StatusRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.autopilotrpc.StatusRequest();
        };

        /**
         * Creates a plain object from a StatusRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof autopilotrpc.StatusRequest
         * @static
         * @param {autopilotrpc.StatusRequest} message StatusRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatusRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this StatusRequest to JSON.
         * @function toJSON
         * @memberof autopilotrpc.StatusRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatusRequest.prototype.toJSON = function() {
            return StatusRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StatusRequest
         * @function getTypeUrl
         * @memberof autopilotrpc.StatusRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StatusRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/autopilotrpc.StatusRequest";
        };

        return StatusRequest;
    })();

    autopilotrpc.StatusResponse = (function() {

        /**
         * Properties of a StatusResponse.
         * @typedef {Object} autopilotrpc.StatusResponse.$Properties
         * @property {boolean|null} [active] StatusResponse active
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a StatusResponse.
         * @memberof autopilotrpc
         * @interface IStatusResponse
         * @augments autopilotrpc.StatusResponse.$Properties
         * @deprecated Use autopilotrpc.StatusResponse.$Properties instead.
         */

        /**
         * Shape of a StatusResponse.
         * @typedef {autopilotrpc.StatusResponse.$Properties} autopilotrpc.StatusResponse.$Shape
         */

        /**
         * Constructs a new StatusResponse.
         * @memberof autopilotrpc
         * @classdesc Represents a StatusResponse.
         * @constructor
         * @param {autopilotrpc.StatusResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const StatusResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StatusResponse active.
         * @member {boolean} active
         * @memberof autopilotrpc.StatusResponse
         * @instance
         */
        StatusResponse.prototype.active = false;

        /**
         * Creates a new StatusResponse instance using the specified properties.
         * @function create
         * @memberof autopilotrpc.StatusResponse
         * @static
         * @param {autopilotrpc.StatusResponse.$Properties=} [properties] Properties to set
         * @returns {autopilotrpc.StatusResponse} StatusResponse instance
         * @type {{
         *   (properties: autopilotrpc.StatusResponse.$Shape): autopilotrpc.StatusResponse & autopilotrpc.StatusResponse.$Shape;
         *   (properties?: autopilotrpc.StatusResponse.$Properties): autopilotrpc.StatusResponse;
         * }}
         */
        StatusResponse.create = function(properties) {
            return new StatusResponse(properties);
        };

        /**
         * Encodes the specified StatusResponse message. Does not implicitly {@link autopilotrpc.StatusResponse.verify|verify} messages.
         * @function encode
         * @memberof autopilotrpc.StatusResponse
         * @static
         * @param {autopilotrpc.StatusResponse.$Properties} message StatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.active != null && $Object.hasOwnProperty.call(message, "active") && message.active !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.active);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StatusResponse message, length delimited. Does not implicitly {@link autopilotrpc.StatusResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof autopilotrpc.StatusResponse
         * @static
         * @param {autopilotrpc.StatusResponse.$Properties} message StatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StatusResponse message from the specified reader or buffer.
         * @function decode
         * @memberof autopilotrpc.StatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {autopilotrpc.StatusResponse & autopilotrpc.StatusResponse.$Shape} StatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.autopilotrpc.StatusResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.active = value;
                        else
                            delete message.active;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StatusResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof autopilotrpc.StatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {autopilotrpc.StatusResponse & autopilotrpc.StatusResponse.$Shape} StatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatusResponse message.
         * @function verify
         * @memberof autopilotrpc.StatusResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatusResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.active != null && $Object.hasOwnProperty.call(message, "active"))
                if (typeof message.active !== "boolean")
                    return "active: boolean expected";
            return null;
        };

        /**
         * Creates a StatusResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof autopilotrpc.StatusResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {autopilotrpc.StatusResponse} StatusResponse
         */
        StatusResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.autopilotrpc.StatusResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".autopilotrpc.StatusResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.autopilotrpc.StatusResponse();
            if (object.active != null)
                if (object.active)
                    message.active = $Boolean(object.active);
            return message;
        };

        /**
         * Creates a plain object from a StatusResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof autopilotrpc.StatusResponse
         * @static
         * @param {autopilotrpc.StatusResponse} message StatusResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatusResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.active = false;
            if (message.active != null && $Object.hasOwnProperty.call(message, "active"))
                object.active = message.active;
            return object;
        };

        /**
         * Converts this StatusResponse to JSON.
         * @function toJSON
         * @memberof autopilotrpc.StatusResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatusResponse.prototype.toJSON = function() {
            return StatusResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StatusResponse
         * @function getTypeUrl
         * @memberof autopilotrpc.StatusResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StatusResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/autopilotrpc.StatusResponse";
        };

        return StatusResponse;
    })();

    autopilotrpc.ModifyStatusRequest = (function() {

        /**
         * Properties of a ModifyStatusRequest.
         * @typedef {Object} autopilotrpc.ModifyStatusRequest.$Properties
         * @property {boolean|null} [enable] ModifyStatusRequest enable
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ModifyStatusRequest.
         * @memberof autopilotrpc
         * @interface IModifyStatusRequest
         * @augments autopilotrpc.ModifyStatusRequest.$Properties
         * @deprecated Use autopilotrpc.ModifyStatusRequest.$Properties instead.
         */

        /**
         * Shape of a ModifyStatusRequest.
         * @typedef {autopilotrpc.ModifyStatusRequest.$Properties} autopilotrpc.ModifyStatusRequest.$Shape
         */

        /**
         * Constructs a new ModifyStatusRequest.
         * @memberof autopilotrpc
         * @classdesc Represents a ModifyStatusRequest.
         * @constructor
         * @param {autopilotrpc.ModifyStatusRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ModifyStatusRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ModifyStatusRequest enable.
         * @member {boolean} enable
         * @memberof autopilotrpc.ModifyStatusRequest
         * @instance
         */
        ModifyStatusRequest.prototype.enable = false;

        /**
         * Creates a new ModifyStatusRequest instance using the specified properties.
         * @function create
         * @memberof autopilotrpc.ModifyStatusRequest
         * @static
         * @param {autopilotrpc.ModifyStatusRequest.$Properties=} [properties] Properties to set
         * @returns {autopilotrpc.ModifyStatusRequest} ModifyStatusRequest instance
         * @type {{
         *   (properties: autopilotrpc.ModifyStatusRequest.$Shape): autopilotrpc.ModifyStatusRequest & autopilotrpc.ModifyStatusRequest.$Shape;
         *   (properties?: autopilotrpc.ModifyStatusRequest.$Properties): autopilotrpc.ModifyStatusRequest;
         * }}
         */
        ModifyStatusRequest.create = function(properties) {
            return new ModifyStatusRequest(properties);
        };

        /**
         * Encodes the specified ModifyStatusRequest message. Does not implicitly {@link autopilotrpc.ModifyStatusRequest.verify|verify} messages.
         * @function encode
         * @memberof autopilotrpc.ModifyStatusRequest
         * @static
         * @param {autopilotrpc.ModifyStatusRequest.$Properties} message ModifyStatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ModifyStatusRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.enable != null && $Object.hasOwnProperty.call(message, "enable") && message.enable !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.enable);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ModifyStatusRequest message, length delimited. Does not implicitly {@link autopilotrpc.ModifyStatusRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof autopilotrpc.ModifyStatusRequest
         * @static
         * @param {autopilotrpc.ModifyStatusRequest.$Properties} message ModifyStatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ModifyStatusRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ModifyStatusRequest message from the specified reader or buffer.
         * @function decode
         * @memberof autopilotrpc.ModifyStatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {autopilotrpc.ModifyStatusRequest & autopilotrpc.ModifyStatusRequest.$Shape} ModifyStatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ModifyStatusRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.autopilotrpc.ModifyStatusRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enable = value;
                        else
                            delete message.enable;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ModifyStatusRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof autopilotrpc.ModifyStatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {autopilotrpc.ModifyStatusRequest & autopilotrpc.ModifyStatusRequest.$Shape} ModifyStatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ModifyStatusRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ModifyStatusRequest message.
         * @function verify
         * @memberof autopilotrpc.ModifyStatusRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ModifyStatusRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enable != null && $Object.hasOwnProperty.call(message, "enable"))
                if (typeof message.enable !== "boolean")
                    return "enable: boolean expected";
            return null;
        };

        /**
         * Creates a ModifyStatusRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof autopilotrpc.ModifyStatusRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {autopilotrpc.ModifyStatusRequest} ModifyStatusRequest
         */
        ModifyStatusRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.autopilotrpc.ModifyStatusRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".autopilotrpc.ModifyStatusRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.autopilotrpc.ModifyStatusRequest();
            if (object.enable != null)
                if (object.enable)
                    message.enable = $Boolean(object.enable);
            return message;
        };

        /**
         * Creates a plain object from a ModifyStatusRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof autopilotrpc.ModifyStatusRequest
         * @static
         * @param {autopilotrpc.ModifyStatusRequest} message ModifyStatusRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ModifyStatusRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.enable = false;
            if (message.enable != null && $Object.hasOwnProperty.call(message, "enable"))
                object.enable = message.enable;
            return object;
        };

        /**
         * Converts this ModifyStatusRequest to JSON.
         * @function toJSON
         * @memberof autopilotrpc.ModifyStatusRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ModifyStatusRequest.prototype.toJSON = function() {
            return ModifyStatusRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ModifyStatusRequest
         * @function getTypeUrl
         * @memberof autopilotrpc.ModifyStatusRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ModifyStatusRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/autopilotrpc.ModifyStatusRequest";
        };

        return ModifyStatusRequest;
    })();

    autopilotrpc.ModifyStatusResponse = (function() {

        /**
         * Properties of a ModifyStatusResponse.
         * @typedef {Object} autopilotrpc.ModifyStatusResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ModifyStatusResponse.
         * @memberof autopilotrpc
         * @interface IModifyStatusResponse
         * @augments autopilotrpc.ModifyStatusResponse.$Properties
         * @deprecated Use autopilotrpc.ModifyStatusResponse.$Properties instead.
         */

        /**
         * Shape of a ModifyStatusResponse.
         * @typedef {autopilotrpc.ModifyStatusResponse.$Properties} autopilotrpc.ModifyStatusResponse.$Shape
         */

        /**
         * Constructs a new ModifyStatusResponse.
         * @memberof autopilotrpc
         * @classdesc Represents a ModifyStatusResponse.
         * @constructor
         * @param {autopilotrpc.ModifyStatusResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ModifyStatusResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ModifyStatusResponse instance using the specified properties.
         * @function create
         * @memberof autopilotrpc.ModifyStatusResponse
         * @static
         * @param {autopilotrpc.ModifyStatusResponse.$Properties=} [properties] Properties to set
         * @returns {autopilotrpc.ModifyStatusResponse} ModifyStatusResponse instance
         * @type {{
         *   (properties: autopilotrpc.ModifyStatusResponse.$Shape): autopilotrpc.ModifyStatusResponse & autopilotrpc.ModifyStatusResponse.$Shape;
         *   (properties?: autopilotrpc.ModifyStatusResponse.$Properties): autopilotrpc.ModifyStatusResponse;
         * }}
         */
        ModifyStatusResponse.create = function(properties) {
            return new ModifyStatusResponse(properties);
        };

        /**
         * Encodes the specified ModifyStatusResponse message. Does not implicitly {@link autopilotrpc.ModifyStatusResponse.verify|verify} messages.
         * @function encode
         * @memberof autopilotrpc.ModifyStatusResponse
         * @static
         * @param {autopilotrpc.ModifyStatusResponse.$Properties} message ModifyStatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ModifyStatusResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ModifyStatusResponse message, length delimited. Does not implicitly {@link autopilotrpc.ModifyStatusResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof autopilotrpc.ModifyStatusResponse
         * @static
         * @param {autopilotrpc.ModifyStatusResponse.$Properties} message ModifyStatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ModifyStatusResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ModifyStatusResponse message from the specified reader or buffer.
         * @function decode
         * @memberof autopilotrpc.ModifyStatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {autopilotrpc.ModifyStatusResponse & autopilotrpc.ModifyStatusResponse.$Shape} ModifyStatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ModifyStatusResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.autopilotrpc.ModifyStatusResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ModifyStatusResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof autopilotrpc.ModifyStatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {autopilotrpc.ModifyStatusResponse & autopilotrpc.ModifyStatusResponse.$Shape} ModifyStatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ModifyStatusResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ModifyStatusResponse message.
         * @function verify
         * @memberof autopilotrpc.ModifyStatusResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ModifyStatusResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ModifyStatusResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof autopilotrpc.ModifyStatusResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {autopilotrpc.ModifyStatusResponse} ModifyStatusResponse
         */
        ModifyStatusResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.autopilotrpc.ModifyStatusResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".autopilotrpc.ModifyStatusResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.autopilotrpc.ModifyStatusResponse();
        };

        /**
         * Creates a plain object from a ModifyStatusResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof autopilotrpc.ModifyStatusResponse
         * @static
         * @param {autopilotrpc.ModifyStatusResponse} message ModifyStatusResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ModifyStatusResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this ModifyStatusResponse to JSON.
         * @function toJSON
         * @memberof autopilotrpc.ModifyStatusResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ModifyStatusResponse.prototype.toJSON = function() {
            return ModifyStatusResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ModifyStatusResponse
         * @function getTypeUrl
         * @memberof autopilotrpc.ModifyStatusResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ModifyStatusResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/autopilotrpc.ModifyStatusResponse";
        };

        return ModifyStatusResponse;
    })();

    autopilotrpc.QueryScoresRequest = (function() {

        /**
         * Properties of a QueryScoresRequest.
         * @typedef {Object} autopilotrpc.QueryScoresRequest.$Properties
         * @property {Array.<string>|null} [pubkeys] QueryScoresRequest pubkeys
         * @property {boolean|null} [ignore_local_state] QueryScoresRequest ignore_local_state
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a QueryScoresRequest.
         * @memberof autopilotrpc
         * @interface IQueryScoresRequest
         * @augments autopilotrpc.QueryScoresRequest.$Properties
         * @deprecated Use autopilotrpc.QueryScoresRequest.$Properties instead.
         */

        /**
         * Shape of a QueryScoresRequest.
         * @typedef {autopilotrpc.QueryScoresRequest.$Properties} autopilotrpc.QueryScoresRequest.$Shape
         */

        /**
         * Constructs a new QueryScoresRequest.
         * @memberof autopilotrpc
         * @classdesc Represents a QueryScoresRequest.
         * @constructor
         * @param {autopilotrpc.QueryScoresRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const QueryScoresRequest = function (properties) {
            this.pubkeys = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * QueryScoresRequest pubkeys.
         * @member {Array.<string>} pubkeys
         * @memberof autopilotrpc.QueryScoresRequest
         * @instance
         */
        QueryScoresRequest.prototype.pubkeys = $util.emptyArray;

        /**
         * QueryScoresRequest ignore_local_state.
         * @member {boolean} ignore_local_state
         * @memberof autopilotrpc.QueryScoresRequest
         * @instance
         */
        QueryScoresRequest.prototype.ignore_local_state = false;

        /**
         * Creates a new QueryScoresRequest instance using the specified properties.
         * @function create
         * @memberof autopilotrpc.QueryScoresRequest
         * @static
         * @param {autopilotrpc.QueryScoresRequest.$Properties=} [properties] Properties to set
         * @returns {autopilotrpc.QueryScoresRequest} QueryScoresRequest instance
         * @type {{
         *   (properties: autopilotrpc.QueryScoresRequest.$Shape): autopilotrpc.QueryScoresRequest & autopilotrpc.QueryScoresRequest.$Shape;
         *   (properties?: autopilotrpc.QueryScoresRequest.$Properties): autopilotrpc.QueryScoresRequest;
         * }}
         */
        QueryScoresRequest.create = function(properties) {
            return new QueryScoresRequest(properties);
        };

        /**
         * Encodes the specified QueryScoresRequest message. Does not implicitly {@link autopilotrpc.QueryScoresRequest.verify|verify} messages.
         * @function encode
         * @memberof autopilotrpc.QueryScoresRequest
         * @static
         * @param {autopilotrpc.QueryScoresRequest.$Properties} message QueryScoresRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryScoresRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pubkeys != null && message.pubkeys.length)
                for (let i = 0; i < message.pubkeys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.pubkeys[i]);
            if (message.ignore_local_state != null && $Object.hasOwnProperty.call(message, "ignore_local_state") && message.ignore_local_state !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.ignore_local_state);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QueryScoresRequest message, length delimited. Does not implicitly {@link autopilotrpc.QueryScoresRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof autopilotrpc.QueryScoresRequest
         * @static
         * @param {autopilotrpc.QueryScoresRequest.$Properties} message QueryScoresRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryScoresRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryScoresRequest message from the specified reader or buffer.
         * @function decode
         * @memberof autopilotrpc.QueryScoresRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {autopilotrpc.QueryScoresRequest & autopilotrpc.QueryScoresRequest.$Shape} QueryScoresRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryScoresRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.autopilotrpc.QueryScoresRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pubkeys && message.pubkeys.length))
                            message.pubkeys = [];
                        message.pubkeys.push(reader.stringVerify());
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ignore_local_state = value;
                        else
                            delete message.ignore_local_state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QueryScoresRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof autopilotrpc.QueryScoresRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {autopilotrpc.QueryScoresRequest & autopilotrpc.QueryScoresRequest.$Shape} QueryScoresRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryScoresRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryScoresRequest message.
         * @function verify
         * @memberof autopilotrpc.QueryScoresRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryScoresRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pubkeys != null && $Object.hasOwnProperty.call(message, "pubkeys")) {
                if (!$Array.isArray(message.pubkeys))
                    return "pubkeys: array expected";
                for (let i = 0; i < message.pubkeys.length; ++i)
                    if (!$util.isString(message.pubkeys[i]))
                        return "pubkeys: string[] expected";
            }
            if (message.ignore_local_state != null && $Object.hasOwnProperty.call(message, "ignore_local_state"))
                if (typeof message.ignore_local_state !== "boolean")
                    return "ignore_local_state: boolean expected";
            return null;
        };

        /**
         * Creates a QueryScoresRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof autopilotrpc.QueryScoresRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {autopilotrpc.QueryScoresRequest} QueryScoresRequest
         */
        QueryScoresRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.autopilotrpc.QueryScoresRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".autopilotrpc.QueryScoresRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.autopilotrpc.QueryScoresRequest();
            if (object.pubkeys) {
                if (!$Array.isArray(object.pubkeys))
                    throw $TypeError(".autopilotrpc.QueryScoresRequest.pubkeys: array expected");
                message.pubkeys = $Array(object.pubkeys.length);
                for (let i = 0; i < object.pubkeys.length; ++i)
                    message.pubkeys[i] = $String(object.pubkeys[i]);
            }
            if (object.ignore_local_state != null)
                if (object.ignore_local_state)
                    message.ignore_local_state = $Boolean(object.ignore_local_state);
            return message;
        };

        /**
         * Creates a plain object from a QueryScoresRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof autopilotrpc.QueryScoresRequest
         * @static
         * @param {autopilotrpc.QueryScoresRequest} message QueryScoresRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryScoresRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.pubkeys = [];
            if (options.defaults)
                object.ignore_local_state = false;
            if (message.pubkeys && message.pubkeys.length) {
                object.pubkeys = $Array(message.pubkeys.length);
                for (let j = 0; j < message.pubkeys.length; ++j)
                    object.pubkeys[j] = message.pubkeys[j];
            }
            if (message.ignore_local_state != null && $Object.hasOwnProperty.call(message, "ignore_local_state"))
                object.ignore_local_state = message.ignore_local_state;
            return object;
        };

        /**
         * Converts this QueryScoresRequest to JSON.
         * @function toJSON
         * @memberof autopilotrpc.QueryScoresRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryScoresRequest.prototype.toJSON = function() {
            return QueryScoresRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QueryScoresRequest
         * @function getTypeUrl
         * @memberof autopilotrpc.QueryScoresRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QueryScoresRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/autopilotrpc.QueryScoresRequest";
        };

        return QueryScoresRequest;
    })();

    autopilotrpc.QueryScoresResponse = (function() {

        /**
         * Properties of a QueryScoresResponse.
         * @typedef {Object} autopilotrpc.QueryScoresResponse.$Properties
         * @property {Array.<autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties>|null} [results] QueryScoresResponse results
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a QueryScoresResponse.
         * @memberof autopilotrpc
         * @interface IQueryScoresResponse
         * @augments autopilotrpc.QueryScoresResponse.$Properties
         * @deprecated Use autopilotrpc.QueryScoresResponse.$Properties instead.
         */

        /**
         * Shape of a QueryScoresResponse.
         * @typedef {autopilotrpc.QueryScoresResponse.$Properties} autopilotrpc.QueryScoresResponse.$Shape
         */

        /**
         * Constructs a new QueryScoresResponse.
         * @memberof autopilotrpc
         * @classdesc Represents a QueryScoresResponse.
         * @constructor
         * @param {autopilotrpc.QueryScoresResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const QueryScoresResponse = function (properties) {
            this.results = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * QueryScoresResponse results.
         * @member {Array.<autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties>} results
         * @memberof autopilotrpc.QueryScoresResponse
         * @instance
         */
        QueryScoresResponse.prototype.results = $util.emptyArray;

        /**
         * Creates a new QueryScoresResponse instance using the specified properties.
         * @function create
         * @memberof autopilotrpc.QueryScoresResponse
         * @static
         * @param {autopilotrpc.QueryScoresResponse.$Properties=} [properties] Properties to set
         * @returns {autopilotrpc.QueryScoresResponse} QueryScoresResponse instance
         * @type {{
         *   (properties: autopilotrpc.QueryScoresResponse.$Shape): autopilotrpc.QueryScoresResponse & autopilotrpc.QueryScoresResponse.$Shape;
         *   (properties?: autopilotrpc.QueryScoresResponse.$Properties): autopilotrpc.QueryScoresResponse;
         * }}
         */
        QueryScoresResponse.create = function(properties) {
            return new QueryScoresResponse(properties);
        };

        /**
         * Encodes the specified QueryScoresResponse message. Does not implicitly {@link autopilotrpc.QueryScoresResponse.verify|verify} messages.
         * @function encode
         * @memberof autopilotrpc.QueryScoresResponse
         * @static
         * @param {autopilotrpc.QueryScoresResponse.$Properties} message QueryScoresResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryScoresResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.results != null && message.results.length)
                for (let i = 0; i < message.results.length; ++i)
                    $root.autopilotrpc.QueryScoresResponse.HeuristicResult.encode(message.results[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QueryScoresResponse message, length delimited. Does not implicitly {@link autopilotrpc.QueryScoresResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof autopilotrpc.QueryScoresResponse
         * @static
         * @param {autopilotrpc.QueryScoresResponse.$Properties} message QueryScoresResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryScoresResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryScoresResponse message from the specified reader or buffer.
         * @function decode
         * @memberof autopilotrpc.QueryScoresResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {autopilotrpc.QueryScoresResponse & autopilotrpc.QueryScoresResponse.$Shape} QueryScoresResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryScoresResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.autopilotrpc.QueryScoresResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.results && message.results.length))
                            message.results = [];
                        message.results.push($root.autopilotrpc.QueryScoresResponse.HeuristicResult.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QueryScoresResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof autopilotrpc.QueryScoresResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {autopilotrpc.QueryScoresResponse & autopilotrpc.QueryScoresResponse.$Shape} QueryScoresResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryScoresResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryScoresResponse message.
         * @function verify
         * @memberof autopilotrpc.QueryScoresResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryScoresResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.results != null && $Object.hasOwnProperty.call(message, "results")) {
                if (!$Array.isArray(message.results))
                    return "results: array expected";
                for (let i = 0; i < message.results.length; ++i) {
                    let error = $root.autopilotrpc.QueryScoresResponse.HeuristicResult.verify(message.results[i], _depth + 1);
                    if (error)
                        return "results." + error;
                }
            }
            return null;
        };

        /**
         * Creates a QueryScoresResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof autopilotrpc.QueryScoresResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {autopilotrpc.QueryScoresResponse} QueryScoresResponse
         */
        QueryScoresResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.autopilotrpc.QueryScoresResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".autopilotrpc.QueryScoresResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.autopilotrpc.QueryScoresResponse();
            if (object.results) {
                if (!$Array.isArray(object.results))
                    throw $TypeError(".autopilotrpc.QueryScoresResponse.results: array expected");
                message.results = $Array(object.results.length);
                for (let i = 0; i < object.results.length; ++i) {
                    if (!$util.isObject(object.results[i]))
                        throw $TypeError(".autopilotrpc.QueryScoresResponse.results: object expected");
                    message.results[i] = $root.autopilotrpc.QueryScoresResponse.HeuristicResult.fromObject(object.results[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a QueryScoresResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof autopilotrpc.QueryScoresResponse
         * @static
         * @param {autopilotrpc.QueryScoresResponse} message QueryScoresResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryScoresResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.results = [];
            if (message.results && message.results.length) {
                object.results = $Array(message.results.length);
                for (let j = 0; j < message.results.length; ++j)
                    object.results[j] = $root.autopilotrpc.QueryScoresResponse.HeuristicResult.toObject(message.results[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this QueryScoresResponse to JSON.
         * @function toJSON
         * @memberof autopilotrpc.QueryScoresResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryScoresResponse.prototype.toJSON = function() {
            return QueryScoresResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QueryScoresResponse
         * @function getTypeUrl
         * @memberof autopilotrpc.QueryScoresResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QueryScoresResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/autopilotrpc.QueryScoresResponse";
        };

        QueryScoresResponse.HeuristicResult = (function() {

            /**
             * Properties of a HeuristicResult.
             * @typedef {Object} autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties
             * @property {string|null} [heuristic] HeuristicResult heuristic
             * @property {Object.<string,number>|null} [scores] HeuristicResult scores
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */

            /**
             * Properties of a HeuristicResult.
             * @memberof autopilotrpc.QueryScoresResponse
             * @interface IHeuristicResult
             * @augments autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties
             * @deprecated Use autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties instead.
             */

            /**
             * Shape of a HeuristicResult.
             * @typedef {autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties} autopilotrpc.QueryScoresResponse.HeuristicResult.$Shape
             */

            /**
             * Constructs a new HeuristicResult.
             * @memberof autopilotrpc.QueryScoresResponse
             * @classdesc Represents a HeuristicResult.
             * @constructor
             * @param {autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */
            const HeuristicResult = function (properties) {
                this.scores = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * HeuristicResult heuristic.
             * @member {string} heuristic
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @instance
             */
            HeuristicResult.prototype.heuristic = "";

            /**
             * HeuristicResult scores.
             * @member {Object.<string,number>} scores
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @instance
             */
            HeuristicResult.prototype.scores = $util.emptyObject;

            /**
             * Creates a new HeuristicResult instance using the specified properties.
             * @function create
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @static
             * @param {autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties=} [properties] Properties to set
             * @returns {autopilotrpc.QueryScoresResponse.HeuristicResult} HeuristicResult instance
             * @type {{
             *   (properties: autopilotrpc.QueryScoresResponse.HeuristicResult.$Shape): autopilotrpc.QueryScoresResponse.HeuristicResult & autopilotrpc.QueryScoresResponse.HeuristicResult.$Shape;
             *   (properties?: autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties): autopilotrpc.QueryScoresResponse.HeuristicResult;
             * }}
             */
            HeuristicResult.create = function(properties) {
                return new HeuristicResult(properties);
            };

            /**
             * Encodes the specified HeuristicResult message. Does not implicitly {@link autopilotrpc.QueryScoresResponse.HeuristicResult.verify|verify} messages.
             * @function encode
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @static
             * @param {autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties} message HeuristicResult message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HeuristicResult.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.heuristic != null && $Object.hasOwnProperty.call(message, "heuristic") && message.heuristic !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.heuristic);
                if (message.scores != null && $Object.hasOwnProperty.call(message, "scores"))
                    for (let keys = $Object.keys(message.scores), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 1 =*/17).double(message.scores[keys[i]]).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HeuristicResult message, length delimited. Does not implicitly {@link autopilotrpc.QueryScoresResponse.HeuristicResult.verify|verify} messages.
             * @function encodeDelimited
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @static
             * @param {autopilotrpc.QueryScoresResponse.HeuristicResult.$Properties} message HeuristicResult message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HeuristicResult.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a HeuristicResult message from the specified reader or buffer.
             * @function decode
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {autopilotrpc.QueryScoresResponse.HeuristicResult & autopilotrpc.QueryScoresResponse.HeuristicResult.$Shape} HeuristicResult
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HeuristicResult.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.autopilotrpc.QueryScoresResponse.HeuristicResult(), key, value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.heuristic = value;
                            else
                                delete message.heuristic;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (message.scores === $util.emptyObject)
                                message.scores = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = 0;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 1)
                                        break;
                                    value = reader.double();
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.scores, key);
                            message.scores[key] = value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a HeuristicResult message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {autopilotrpc.QueryScoresResponse.HeuristicResult & autopilotrpc.QueryScoresResponse.HeuristicResult.$Shape} HeuristicResult
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HeuristicResult.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HeuristicResult message.
             * @function verify
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HeuristicResult.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.heuristic != null && $Object.hasOwnProperty.call(message, "heuristic"))
                    if (!$util.isString(message.heuristic))
                        return "heuristic: string expected";
                if (message.scores != null && $Object.hasOwnProperty.call(message, "scores")) {
                    if (!$util.isObject(message.scores))
                        return "scores: object expected";
                    let key = $Object.keys(message.scores);
                    for (let i = 0; i < key.length; ++i)
                        if (typeof message.scores[key[i]] !== "number")
                            return "scores: number{k:string} expected";
                }
                return null;
            };

            /**
             * Creates a HeuristicResult message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {autopilotrpc.QueryScoresResponse.HeuristicResult} HeuristicResult
             */
            HeuristicResult.fromObject = function (object, _depth) {
                if (object instanceof $root.autopilotrpc.QueryScoresResponse.HeuristicResult)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".autopilotrpc.QueryScoresResponse.HeuristicResult: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.autopilotrpc.QueryScoresResponse.HeuristicResult();
                if (object.heuristic != null)
                    if (typeof object.heuristic !== "string" || object.heuristic.length)
                        message.heuristic = $String(object.heuristic);
                if (object.scores) {
                    if (!$util.isObject(object.scores))
                        throw $TypeError(".autopilotrpc.QueryScoresResponse.HeuristicResult.scores: object expected");
                    message.scores = {};
                    for (let keys = $Object.keys(object.scores), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.scores, keys[i]);
                        message.scores[keys[i]] = $Number(object.scores[keys[i]]);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a HeuristicResult message. Also converts values to other types if specified.
             * @function toObject
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @static
             * @param {autopilotrpc.QueryScoresResponse.HeuristicResult} message HeuristicResult
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HeuristicResult.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.scores = {};
                if (options.defaults)
                    object.heuristic = "";
                if (message.heuristic != null && $Object.hasOwnProperty.call(message, "heuristic"))
                    object.heuristic = message.heuristic;
                let keys2;
                if (message.scores && (keys2 = $Object.keys(message.scores)).length) {
                    object.scores = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.scores, keys2[j]);
                        object.scores[keys2[j]] = options.json && !$isFinite(message.scores[keys2[j]]) ? $String(message.scores[keys2[j]]) : message.scores[keys2[j]];
                    }
                }
                return object;
            };

            /**
             * Converts this HeuristicResult to JSON.
             * @function toJSON
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HeuristicResult.prototype.toJSON = function() {
                return HeuristicResult.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for HeuristicResult
             * @function getTypeUrl
             * @memberof autopilotrpc.QueryScoresResponse.HeuristicResult
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            HeuristicResult.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/autopilotrpc.QueryScoresResponse.HeuristicResult";
            };

            return HeuristicResult;
        })();

        return QueryScoresResponse;
    })();

    autopilotrpc.SetScoresRequest = (function() {

        /**
         * Properties of a SetScoresRequest.
         * @typedef {Object} autopilotrpc.SetScoresRequest.$Properties
         * @property {string|null} [heuristic] SetScoresRequest heuristic
         * @property {Object.<string,number>|null} [scores] SetScoresRequest scores
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SetScoresRequest.
         * @memberof autopilotrpc
         * @interface ISetScoresRequest
         * @augments autopilotrpc.SetScoresRequest.$Properties
         * @deprecated Use autopilotrpc.SetScoresRequest.$Properties instead.
         */

        /**
         * Shape of a SetScoresRequest.
         * @typedef {autopilotrpc.SetScoresRequest.$Properties} autopilotrpc.SetScoresRequest.$Shape
         */

        /**
         * Constructs a new SetScoresRequest.
         * @memberof autopilotrpc
         * @classdesc Represents a SetScoresRequest.
         * @constructor
         * @param {autopilotrpc.SetScoresRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SetScoresRequest = function (properties) {
            this.scores = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SetScoresRequest heuristic.
         * @member {string} heuristic
         * @memberof autopilotrpc.SetScoresRequest
         * @instance
         */
        SetScoresRequest.prototype.heuristic = "";

        /**
         * SetScoresRequest scores.
         * @member {Object.<string,number>} scores
         * @memberof autopilotrpc.SetScoresRequest
         * @instance
         */
        SetScoresRequest.prototype.scores = $util.emptyObject;

        /**
         * Creates a new SetScoresRequest instance using the specified properties.
         * @function create
         * @memberof autopilotrpc.SetScoresRequest
         * @static
         * @param {autopilotrpc.SetScoresRequest.$Properties=} [properties] Properties to set
         * @returns {autopilotrpc.SetScoresRequest} SetScoresRequest instance
         * @type {{
         *   (properties: autopilotrpc.SetScoresRequest.$Shape): autopilotrpc.SetScoresRequest & autopilotrpc.SetScoresRequest.$Shape;
         *   (properties?: autopilotrpc.SetScoresRequest.$Properties): autopilotrpc.SetScoresRequest;
         * }}
         */
        SetScoresRequest.create = function(properties) {
            return new SetScoresRequest(properties);
        };

        /**
         * Encodes the specified SetScoresRequest message. Does not implicitly {@link autopilotrpc.SetScoresRequest.verify|verify} messages.
         * @function encode
         * @memberof autopilotrpc.SetScoresRequest
         * @static
         * @param {autopilotrpc.SetScoresRequest.$Properties} message SetScoresRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetScoresRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.heuristic != null && $Object.hasOwnProperty.call(message, "heuristic") && message.heuristic !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.heuristic);
            if (message.scores != null && $Object.hasOwnProperty.call(message, "scores"))
                for (let keys = $Object.keys(message.scores), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 1 =*/17).double(message.scores[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SetScoresRequest message, length delimited. Does not implicitly {@link autopilotrpc.SetScoresRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof autopilotrpc.SetScoresRequest
         * @static
         * @param {autopilotrpc.SetScoresRequest.$Properties} message SetScoresRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetScoresRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SetScoresRequest message from the specified reader or buffer.
         * @function decode
         * @memberof autopilotrpc.SetScoresRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {autopilotrpc.SetScoresRequest & autopilotrpc.SetScoresRequest.$Shape} SetScoresRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetScoresRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.autopilotrpc.SetScoresRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.heuristic = value;
                        else
                            delete message.heuristic;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (message.scores === $util.emptyObject)
                            message.scores = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 1)
                                    break;
                                value = reader.double();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.scores, key);
                        message.scores[key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SetScoresRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof autopilotrpc.SetScoresRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {autopilotrpc.SetScoresRequest & autopilotrpc.SetScoresRequest.$Shape} SetScoresRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetScoresRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SetScoresRequest message.
         * @function verify
         * @memberof autopilotrpc.SetScoresRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SetScoresRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.heuristic != null && $Object.hasOwnProperty.call(message, "heuristic"))
                if (!$util.isString(message.heuristic))
                    return "heuristic: string expected";
            if (message.scores != null && $Object.hasOwnProperty.call(message, "scores")) {
                if (!$util.isObject(message.scores))
                    return "scores: object expected";
                let key = $Object.keys(message.scores);
                for (let i = 0; i < key.length; ++i)
                    if (typeof message.scores[key[i]] !== "number")
                        return "scores: number{k:string} expected";
            }
            return null;
        };

        /**
         * Creates a SetScoresRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof autopilotrpc.SetScoresRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {autopilotrpc.SetScoresRequest} SetScoresRequest
         */
        SetScoresRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.autopilotrpc.SetScoresRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".autopilotrpc.SetScoresRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.autopilotrpc.SetScoresRequest();
            if (object.heuristic != null)
                if (typeof object.heuristic !== "string" || object.heuristic.length)
                    message.heuristic = $String(object.heuristic);
            if (object.scores) {
                if (!$util.isObject(object.scores))
                    throw $TypeError(".autopilotrpc.SetScoresRequest.scores: object expected");
                message.scores = {};
                for (let keys = $Object.keys(object.scores), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.scores, keys[i]);
                    message.scores[keys[i]] = $Number(object.scores[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SetScoresRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof autopilotrpc.SetScoresRequest
         * @static
         * @param {autopilotrpc.SetScoresRequest} message SetScoresRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetScoresRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.scores = {};
            if (options.defaults)
                object.heuristic = "";
            if (message.heuristic != null && $Object.hasOwnProperty.call(message, "heuristic"))
                object.heuristic = message.heuristic;
            let keys2;
            if (message.scores && (keys2 = $Object.keys(message.scores)).length) {
                object.scores = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.scores, keys2[j]);
                    object.scores[keys2[j]] = options.json && !$isFinite(message.scores[keys2[j]]) ? $String(message.scores[keys2[j]]) : message.scores[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this SetScoresRequest to JSON.
         * @function toJSON
         * @memberof autopilotrpc.SetScoresRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetScoresRequest.prototype.toJSON = function() {
            return SetScoresRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SetScoresRequest
         * @function getTypeUrl
         * @memberof autopilotrpc.SetScoresRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SetScoresRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/autopilotrpc.SetScoresRequest";
        };

        return SetScoresRequest;
    })();

    autopilotrpc.SetScoresResponse = (function() {

        /**
         * Properties of a SetScoresResponse.
         * @typedef {Object} autopilotrpc.SetScoresResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SetScoresResponse.
         * @memberof autopilotrpc
         * @interface ISetScoresResponse
         * @augments autopilotrpc.SetScoresResponse.$Properties
         * @deprecated Use autopilotrpc.SetScoresResponse.$Properties instead.
         */

        /**
         * Shape of a SetScoresResponse.
         * @typedef {autopilotrpc.SetScoresResponse.$Properties} autopilotrpc.SetScoresResponse.$Shape
         */

        /**
         * Constructs a new SetScoresResponse.
         * @memberof autopilotrpc
         * @classdesc Represents a SetScoresResponse.
         * @constructor
         * @param {autopilotrpc.SetScoresResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SetScoresResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new SetScoresResponse instance using the specified properties.
         * @function create
         * @memberof autopilotrpc.SetScoresResponse
         * @static
         * @param {autopilotrpc.SetScoresResponse.$Properties=} [properties] Properties to set
         * @returns {autopilotrpc.SetScoresResponse} SetScoresResponse instance
         * @type {{
         *   (properties: autopilotrpc.SetScoresResponse.$Shape): autopilotrpc.SetScoresResponse & autopilotrpc.SetScoresResponse.$Shape;
         *   (properties?: autopilotrpc.SetScoresResponse.$Properties): autopilotrpc.SetScoresResponse;
         * }}
         */
        SetScoresResponse.create = function(properties) {
            return new SetScoresResponse(properties);
        };

        /**
         * Encodes the specified SetScoresResponse message. Does not implicitly {@link autopilotrpc.SetScoresResponse.verify|verify} messages.
         * @function encode
         * @memberof autopilotrpc.SetScoresResponse
         * @static
         * @param {autopilotrpc.SetScoresResponse.$Properties} message SetScoresResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetScoresResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SetScoresResponse message, length delimited. Does not implicitly {@link autopilotrpc.SetScoresResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof autopilotrpc.SetScoresResponse
         * @static
         * @param {autopilotrpc.SetScoresResponse.$Properties} message SetScoresResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetScoresResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SetScoresResponse message from the specified reader or buffer.
         * @function decode
         * @memberof autopilotrpc.SetScoresResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {autopilotrpc.SetScoresResponse & autopilotrpc.SetScoresResponse.$Shape} SetScoresResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetScoresResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.autopilotrpc.SetScoresResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SetScoresResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof autopilotrpc.SetScoresResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {autopilotrpc.SetScoresResponse & autopilotrpc.SetScoresResponse.$Shape} SetScoresResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetScoresResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SetScoresResponse message.
         * @function verify
         * @memberof autopilotrpc.SetScoresResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SetScoresResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a SetScoresResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof autopilotrpc.SetScoresResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {autopilotrpc.SetScoresResponse} SetScoresResponse
         */
        SetScoresResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.autopilotrpc.SetScoresResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".autopilotrpc.SetScoresResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.autopilotrpc.SetScoresResponse();
        };

        /**
         * Creates a plain object from a SetScoresResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof autopilotrpc.SetScoresResponse
         * @static
         * @param {autopilotrpc.SetScoresResponse} message SetScoresResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetScoresResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this SetScoresResponse to JSON.
         * @function toJSON
         * @memberof autopilotrpc.SetScoresResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetScoresResponse.prototype.toJSON = function() {
            return SetScoresResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SetScoresResponse
         * @function getTypeUrl
         * @memberof autopilotrpc.SetScoresResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SetScoresResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/autopilotrpc.SetScoresResponse";
        };

        return SetScoresResponse;
    })();

    return autopilotrpc;
})();

export const chainrpc = $root.chainrpc = (() => {

    /**
     * Namespace chainrpc.
     * @exports chainrpc
     * @namespace
     */
    const chainrpc = {};

    chainrpc.ChainNotifier = (function() {

        /**
         * Constructs a new ChainNotifier service.
         * @memberof chainrpc
         * @classdesc Represents a ChainNotifier
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const ChainNotifier = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (ChainNotifier.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = ChainNotifier;

        /**
         * Creates new ChainNotifier service using the specified rpc implementation.
         * @function create
         * @memberof chainrpc.ChainNotifier
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {ChainNotifier} RPC service. Useful where requests and/or responses are streamed.
         */
        ChainNotifier.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link chainrpc.ChainNotifier#registerConfirmationsNtfn}.
         * @memberof chainrpc.ChainNotifier
         * @typedef RegisterConfirmationsNtfnCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {chainrpc.ConfEvent} [response] ConfEvent
         */

        /**
         * Calls RegisterConfirmationsNtfn.
         * @memberof chainrpc.ChainNotifier
         * @typedef RegisterConfirmationsNtfn
         * @type {{
         *   (request: chainrpc.IConfRequest, callback: chainrpc.ChainNotifier.RegisterConfirmationsNtfnCallback): void;
         *   (request: chainrpc.IConfRequest): Promise<chainrpc.ConfEvent>;
         *   readonly name: "RegisterConfirmationsNtfn";
         *   readonly path: "/chainrpc.ChainNotifier/RegisterConfirmationsNtfn";
         *   readonly requestType: "ConfRequest";
         *   readonly responseType: "ConfEvent";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls RegisterConfirmationsNtfn.
         * @name chainrpc.ChainNotifier#registerConfirmationsNtfn
         * @type {chainrpc.ChainNotifier.RegisterConfirmationsNtfn}
         */
        $Object.defineProperties(ChainNotifier.prototype.registerConfirmationsNtfn = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ChainNotifier.prototype.registerConfirmationsNtfn, $root.chainrpc.ConfRequest, $root.chainrpc.ConfEvent, request, callback);
        }, {
            name: { value: "RegisterConfirmationsNtfn" },
            path: { value: "/chainrpc.ChainNotifier/RegisterConfirmationsNtfn" },
            requestType: { value: "ConfRequest" },
            responseType: { value: "ConfEvent" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link chainrpc.ChainNotifier#registerSpendNtfn}.
         * @memberof chainrpc.ChainNotifier
         * @typedef RegisterSpendNtfnCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {chainrpc.SpendEvent} [response] SpendEvent
         */

        /**
         * Calls RegisterSpendNtfn.
         * @memberof chainrpc.ChainNotifier
         * @typedef RegisterSpendNtfn
         * @type {{
         *   (request: chainrpc.ISpendRequest, callback: chainrpc.ChainNotifier.RegisterSpendNtfnCallback): void;
         *   (request: chainrpc.ISpendRequest): Promise<chainrpc.SpendEvent>;
         *   readonly name: "RegisterSpendNtfn";
         *   readonly path: "/chainrpc.ChainNotifier/RegisterSpendNtfn";
         *   readonly requestType: "SpendRequest";
         *   readonly responseType: "SpendEvent";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls RegisterSpendNtfn.
         * @name chainrpc.ChainNotifier#registerSpendNtfn
         * @type {chainrpc.ChainNotifier.RegisterSpendNtfn}
         */
        $Object.defineProperties(ChainNotifier.prototype.registerSpendNtfn = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ChainNotifier.prototype.registerSpendNtfn, $root.chainrpc.SpendRequest, $root.chainrpc.SpendEvent, request, callback);
        }, {
            name: { value: "RegisterSpendNtfn" },
            path: { value: "/chainrpc.ChainNotifier/RegisterSpendNtfn" },
            requestType: { value: "SpendRequest" },
            responseType: { value: "SpendEvent" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link chainrpc.ChainNotifier#registerBlockEpochNtfn}.
         * @memberof chainrpc.ChainNotifier
         * @typedef RegisterBlockEpochNtfnCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {chainrpc.BlockEpoch} [response] BlockEpoch
         */

        /**
         * Calls RegisterBlockEpochNtfn.
         * @memberof chainrpc.ChainNotifier
         * @typedef RegisterBlockEpochNtfn
         * @type {{
         *   (request: chainrpc.IBlockEpoch, callback: chainrpc.ChainNotifier.RegisterBlockEpochNtfnCallback): void;
         *   (request: chainrpc.IBlockEpoch): Promise<chainrpc.BlockEpoch>;
         *   readonly name: "RegisterBlockEpochNtfn";
         *   readonly path: "/chainrpc.ChainNotifier/RegisterBlockEpochNtfn";
         *   readonly requestType: "BlockEpoch";
         *   readonly responseType: "BlockEpoch";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls RegisterBlockEpochNtfn.
         * @name chainrpc.ChainNotifier#registerBlockEpochNtfn
         * @type {chainrpc.ChainNotifier.RegisterBlockEpochNtfn}
         */
        $Object.defineProperties(ChainNotifier.prototype.registerBlockEpochNtfn = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ChainNotifier.prototype.registerBlockEpochNtfn, $root.chainrpc.BlockEpoch, $root.chainrpc.BlockEpoch, request, callback);
        }, {
            name: { value: "RegisterBlockEpochNtfn" },
            path: { value: "/chainrpc.ChainNotifier/RegisterBlockEpochNtfn" },
            requestType: { value: "BlockEpoch" },
            responseType: { value: "BlockEpoch" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        return ChainNotifier;
    })();

    chainrpc.ConfRequest = (function() {

        /**
         * Properties of a ConfRequest.
         * @typedef {Object} chainrpc.ConfRequest.$Properties
         * @property {Uint8Array|null} [txid] ConfRequest txid
         * @property {Uint8Array|null} [script] ConfRequest script
         * @property {number|null} [num_confs] ConfRequest num_confs
         * @property {number|null} [height_hint] ConfRequest height_hint
         * @property {boolean|null} [include_block] ConfRequest include_block
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ConfRequest.
         * @memberof chainrpc
         * @interface IConfRequest
         * @augments chainrpc.ConfRequest.$Properties
         * @deprecated Use chainrpc.ConfRequest.$Properties instead.
         */

        /**
         * Shape of a ConfRequest.
         * @typedef {chainrpc.ConfRequest.$Properties} chainrpc.ConfRequest.$Shape
         */

        /**
         * Constructs a new ConfRequest.
         * @memberof chainrpc
         * @classdesc Represents a ConfRequest.
         * @constructor
         * @param {chainrpc.ConfRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ConfRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ConfRequest txid.
         * @member {Uint8Array} txid
         * @memberof chainrpc.ConfRequest
         * @instance
         */
        ConfRequest.prototype.txid = $util.newBuffer([]);

        /**
         * ConfRequest script.
         * @member {Uint8Array} script
         * @memberof chainrpc.ConfRequest
         * @instance
         */
        ConfRequest.prototype.script = $util.newBuffer([]);

        /**
         * ConfRequest num_confs.
         * @member {number} num_confs
         * @memberof chainrpc.ConfRequest
         * @instance
         */
        ConfRequest.prototype.num_confs = 0;

        /**
         * ConfRequest height_hint.
         * @member {number} height_hint
         * @memberof chainrpc.ConfRequest
         * @instance
         */
        ConfRequest.prototype.height_hint = 0;

        /**
         * ConfRequest include_block.
         * @member {boolean} include_block
         * @memberof chainrpc.ConfRequest
         * @instance
         */
        ConfRequest.prototype.include_block = false;

        /**
         * Creates a new ConfRequest instance using the specified properties.
         * @function create
         * @memberof chainrpc.ConfRequest
         * @static
         * @param {chainrpc.ConfRequest.$Properties=} [properties] Properties to set
         * @returns {chainrpc.ConfRequest} ConfRequest instance
         * @type {{
         *   (properties: chainrpc.ConfRequest.$Shape): chainrpc.ConfRequest & chainrpc.ConfRequest.$Shape;
         *   (properties?: chainrpc.ConfRequest.$Properties): chainrpc.ConfRequest;
         * }}
         */
        ConfRequest.create = function(properties) {
            return new ConfRequest(properties);
        };

        /**
         * Encodes the specified ConfRequest message. Does not implicitly {@link chainrpc.ConfRequest.verify|verify} messages.
         * @function encode
         * @memberof chainrpc.ConfRequest
         * @static
         * @param {chainrpc.ConfRequest.$Properties} message ConfRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConfRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid") && message.txid.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.txid);
            if (message.script != null && $Object.hasOwnProperty.call(message, "script") && message.script.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.script);
            if (message.num_confs != null && $Object.hasOwnProperty.call(message, "num_confs") && message.num_confs !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.num_confs);
            if (message.height_hint != null && $Object.hasOwnProperty.call(message, "height_hint") && message.height_hint !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.height_hint);
            if (message.include_block != null && $Object.hasOwnProperty.call(message, "include_block") && message.include_block !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.include_block);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConfRequest message, length delimited. Does not implicitly {@link chainrpc.ConfRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof chainrpc.ConfRequest
         * @static
         * @param {chainrpc.ConfRequest.$Properties} message ConfRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConfRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ConfRequest message from the specified reader or buffer.
         * @function decode
         * @memberof chainrpc.ConfRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {chainrpc.ConfRequest & chainrpc.ConfRequest.$Shape} ConfRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConfRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.chainrpc.ConfRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.txid = value;
                        else
                            delete message.txid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.script = value;
                        else
                            delete message.script;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_confs = value;
                        else
                            delete message.num_confs;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.height_hint = value;
                        else
                            delete message.height_hint;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_block = value;
                        else
                            delete message.include_block;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConfRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof chainrpc.ConfRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {chainrpc.ConfRequest & chainrpc.ConfRequest.$Shape} ConfRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConfRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConfRequest message.
         * @function verify
         * @memberof chainrpc.ConfRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConfRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                if (!(message.txid && typeof message.txid.length === "number" || $util.isString(message.txid)))
                    return "txid: buffer expected";
            if (message.script != null && $Object.hasOwnProperty.call(message, "script"))
                if (!(message.script && typeof message.script.length === "number" || $util.isString(message.script)))
                    return "script: buffer expected";
            if (message.num_confs != null && $Object.hasOwnProperty.call(message, "num_confs"))
                if (!$util.isInteger(message.num_confs))
                    return "num_confs: integer expected";
            if (message.height_hint != null && $Object.hasOwnProperty.call(message, "height_hint"))
                if (!$util.isInteger(message.height_hint))
                    return "height_hint: integer expected";
            if (message.include_block != null && $Object.hasOwnProperty.call(message, "include_block"))
                if (typeof message.include_block !== "boolean")
                    return "include_block: boolean expected";
            return null;
        };

        /**
         * Creates a ConfRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof chainrpc.ConfRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {chainrpc.ConfRequest} ConfRequest
         */
        ConfRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.chainrpc.ConfRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".chainrpc.ConfRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.chainrpc.ConfRequest();
            if (object.txid != null)
                if (object.txid.length)
                    if (typeof object.txid === "string")
                        $util.base64.decode(object.txid, message.txid = $util.newBuffer($util.base64.length(object.txid)), 0);
                    else if (object.txid.length >= 0)
                        message.txid = object.txid;
            if (object.script != null)
                if (object.script.length)
                    if (typeof object.script === "string")
                        $util.base64.decode(object.script, message.script = $util.newBuffer($util.base64.length(object.script)), 0);
                    else if (object.script.length >= 0)
                        message.script = object.script;
            if (object.num_confs != null)
                if ($Number(object.num_confs) !== 0)
                    message.num_confs = object.num_confs >>> 0;
            if (object.height_hint != null)
                if ($Number(object.height_hint) !== 0)
                    message.height_hint = object.height_hint >>> 0;
            if (object.include_block != null)
                if (object.include_block)
                    message.include_block = $Boolean(object.include_block);
            return message;
        };

        /**
         * Creates a plain object from a ConfRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof chainrpc.ConfRequest
         * @static
         * @param {chainrpc.ConfRequest} message ConfRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConfRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.txid = "";
                else {
                    object.txid = [];
                    if (options.bytes !== $Array)
                        object.txid = $util.newBuffer(object.txid);
                }
                if (options.bytes === $String)
                    object.script = "";
                else {
                    object.script = [];
                    if (options.bytes !== $Array)
                        object.script = $util.newBuffer(object.script);
                }
                object.num_confs = 0;
                object.height_hint = 0;
                object.include_block = false;
            }
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                object.txid = options.bytes === $String ? $util.base64.encode(message.txid, 0, message.txid.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.txid) : message.txid;
            if (message.script != null && $Object.hasOwnProperty.call(message, "script"))
                object.script = options.bytes === $String ? $util.base64.encode(message.script, 0, message.script.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.script) : message.script;
            if (message.num_confs != null && $Object.hasOwnProperty.call(message, "num_confs"))
                object.num_confs = message.num_confs;
            if (message.height_hint != null && $Object.hasOwnProperty.call(message, "height_hint"))
                object.height_hint = message.height_hint;
            if (message.include_block != null && $Object.hasOwnProperty.call(message, "include_block"))
                object.include_block = message.include_block;
            return object;
        };

        /**
         * Converts this ConfRequest to JSON.
         * @function toJSON
         * @memberof chainrpc.ConfRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConfRequest.prototype.toJSON = function() {
            return ConfRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConfRequest
         * @function getTypeUrl
         * @memberof chainrpc.ConfRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConfRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/chainrpc.ConfRequest";
        };

        return ConfRequest;
    })();

    chainrpc.ConfDetails = (function() {

        /**
         * Properties of a ConfDetails.
         * @typedef {Object} chainrpc.ConfDetails.$Properties
         * @property {Uint8Array|null} [raw_tx] ConfDetails raw_tx
         * @property {Uint8Array|null} [block_hash] ConfDetails block_hash
         * @property {number|null} [block_height] ConfDetails block_height
         * @property {number|null} [tx_index] ConfDetails tx_index
         * @property {Uint8Array|null} [raw_block] ConfDetails raw_block
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ConfDetails.
         * @memberof chainrpc
         * @interface IConfDetails
         * @augments chainrpc.ConfDetails.$Properties
         * @deprecated Use chainrpc.ConfDetails.$Properties instead.
         */

        /**
         * Shape of a ConfDetails.
         * @typedef {chainrpc.ConfDetails.$Properties} chainrpc.ConfDetails.$Shape
         */

        /**
         * Constructs a new ConfDetails.
         * @memberof chainrpc
         * @classdesc Represents a ConfDetails.
         * @constructor
         * @param {chainrpc.ConfDetails.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ConfDetails = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ConfDetails raw_tx.
         * @member {Uint8Array} raw_tx
         * @memberof chainrpc.ConfDetails
         * @instance
         */
        ConfDetails.prototype.raw_tx = $util.newBuffer([]);

        /**
         * ConfDetails block_hash.
         * @member {Uint8Array} block_hash
         * @memberof chainrpc.ConfDetails
         * @instance
         */
        ConfDetails.prototype.block_hash = $util.newBuffer([]);

        /**
         * ConfDetails block_height.
         * @member {number} block_height
         * @memberof chainrpc.ConfDetails
         * @instance
         */
        ConfDetails.prototype.block_height = 0;

        /**
         * ConfDetails tx_index.
         * @member {number} tx_index
         * @memberof chainrpc.ConfDetails
         * @instance
         */
        ConfDetails.prototype.tx_index = 0;

        /**
         * ConfDetails raw_block.
         * @member {Uint8Array} raw_block
         * @memberof chainrpc.ConfDetails
         * @instance
         */
        ConfDetails.prototype.raw_block = $util.newBuffer([]);

        /**
         * Creates a new ConfDetails instance using the specified properties.
         * @function create
         * @memberof chainrpc.ConfDetails
         * @static
         * @param {chainrpc.ConfDetails.$Properties=} [properties] Properties to set
         * @returns {chainrpc.ConfDetails} ConfDetails instance
         * @type {{
         *   (properties: chainrpc.ConfDetails.$Shape): chainrpc.ConfDetails & chainrpc.ConfDetails.$Shape;
         *   (properties?: chainrpc.ConfDetails.$Properties): chainrpc.ConfDetails;
         * }}
         */
        ConfDetails.create = function(properties) {
            return new ConfDetails(properties);
        };

        /**
         * Encodes the specified ConfDetails message. Does not implicitly {@link chainrpc.ConfDetails.verify|verify} messages.
         * @function encode
         * @memberof chainrpc.ConfDetails
         * @static
         * @param {chainrpc.ConfDetails.$Properties} message ConfDetails message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConfDetails.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.raw_tx != null && $Object.hasOwnProperty.call(message, "raw_tx") && message.raw_tx.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.raw_tx);
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash") && message.block_hash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.block_hash);
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height") && message.block_height !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.block_height);
            if (message.tx_index != null && $Object.hasOwnProperty.call(message, "tx_index") && message.tx_index !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.tx_index);
            if (message.raw_block != null && $Object.hasOwnProperty.call(message, "raw_block") && message.raw_block.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.raw_block);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConfDetails message, length delimited. Does not implicitly {@link chainrpc.ConfDetails.verify|verify} messages.
         * @function encodeDelimited
         * @memberof chainrpc.ConfDetails
         * @static
         * @param {chainrpc.ConfDetails.$Properties} message ConfDetails message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConfDetails.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ConfDetails message from the specified reader or buffer.
         * @function decode
         * @memberof chainrpc.ConfDetails
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {chainrpc.ConfDetails & chainrpc.ConfDetails.$Shape} ConfDetails
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConfDetails.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.chainrpc.ConfDetails(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_tx = value;
                        else
                            delete message.raw_tx;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.block_hash = value;
                        else
                            delete message.block_hash;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.block_height = value;
                        else
                            delete message.block_height;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tx_index = value;
                        else
                            delete message.tx_index;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_block = value;
                        else
                            delete message.raw_block;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConfDetails message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof chainrpc.ConfDetails
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {chainrpc.ConfDetails & chainrpc.ConfDetails.$Shape} ConfDetails
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConfDetails.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConfDetails message.
         * @function verify
         * @memberof chainrpc.ConfDetails
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConfDetails.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.raw_tx != null && $Object.hasOwnProperty.call(message, "raw_tx"))
                if (!(message.raw_tx && typeof message.raw_tx.length === "number" || $util.isString(message.raw_tx)))
                    return "raw_tx: buffer expected";
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash"))
                if (!(message.block_hash && typeof message.block_hash.length === "number" || $util.isString(message.block_hash)))
                    return "block_hash: buffer expected";
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height"))
                if (!$util.isInteger(message.block_height))
                    return "block_height: integer expected";
            if (message.tx_index != null && $Object.hasOwnProperty.call(message, "tx_index"))
                if (!$util.isInteger(message.tx_index))
                    return "tx_index: integer expected";
            if (message.raw_block != null && $Object.hasOwnProperty.call(message, "raw_block"))
                if (!(message.raw_block && typeof message.raw_block.length === "number" || $util.isString(message.raw_block)))
                    return "raw_block: buffer expected";
            return null;
        };

        /**
         * Creates a ConfDetails message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof chainrpc.ConfDetails
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {chainrpc.ConfDetails} ConfDetails
         */
        ConfDetails.fromObject = function (object, _depth) {
            if (object instanceof $root.chainrpc.ConfDetails)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".chainrpc.ConfDetails: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.chainrpc.ConfDetails();
            if (object.raw_tx != null)
                if (object.raw_tx.length)
                    if (typeof object.raw_tx === "string")
                        $util.base64.decode(object.raw_tx, message.raw_tx = $util.newBuffer($util.base64.length(object.raw_tx)), 0);
                    else if (object.raw_tx.length >= 0)
                        message.raw_tx = object.raw_tx;
            if (object.block_hash != null)
                if (object.block_hash.length)
                    if (typeof object.block_hash === "string")
                        $util.base64.decode(object.block_hash, message.block_hash = $util.newBuffer($util.base64.length(object.block_hash)), 0);
                    else if (object.block_hash.length >= 0)
                        message.block_hash = object.block_hash;
            if (object.block_height != null)
                if ($Number(object.block_height) !== 0)
                    message.block_height = object.block_height >>> 0;
            if (object.tx_index != null)
                if ($Number(object.tx_index) !== 0)
                    message.tx_index = object.tx_index >>> 0;
            if (object.raw_block != null)
                if (object.raw_block.length)
                    if (typeof object.raw_block === "string")
                        $util.base64.decode(object.raw_block, message.raw_block = $util.newBuffer($util.base64.length(object.raw_block)), 0);
                    else if (object.raw_block.length >= 0)
                        message.raw_block = object.raw_block;
            return message;
        };

        /**
         * Creates a plain object from a ConfDetails message. Also converts values to other types if specified.
         * @function toObject
         * @memberof chainrpc.ConfDetails
         * @static
         * @param {chainrpc.ConfDetails} message ConfDetails
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConfDetails.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.raw_tx = "";
                else {
                    object.raw_tx = [];
                    if (options.bytes !== $Array)
                        object.raw_tx = $util.newBuffer(object.raw_tx);
                }
                if (options.bytes === $String)
                    object.block_hash = "";
                else {
                    object.block_hash = [];
                    if (options.bytes !== $Array)
                        object.block_hash = $util.newBuffer(object.block_hash);
                }
                object.block_height = 0;
                object.tx_index = 0;
                if (options.bytes === $String)
                    object.raw_block = "";
                else {
                    object.raw_block = [];
                    if (options.bytes !== $Array)
                        object.raw_block = $util.newBuffer(object.raw_block);
                }
            }
            if (message.raw_tx != null && $Object.hasOwnProperty.call(message, "raw_tx"))
                object.raw_tx = options.bytes === $String ? $util.base64.encode(message.raw_tx, 0, message.raw_tx.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_tx) : message.raw_tx;
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash"))
                object.block_hash = options.bytes === $String ? $util.base64.encode(message.block_hash, 0, message.block_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.block_hash) : message.block_hash;
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height"))
                object.block_height = message.block_height;
            if (message.tx_index != null && $Object.hasOwnProperty.call(message, "tx_index"))
                object.tx_index = message.tx_index;
            if (message.raw_block != null && $Object.hasOwnProperty.call(message, "raw_block"))
                object.raw_block = options.bytes === $String ? $util.base64.encode(message.raw_block, 0, message.raw_block.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_block) : message.raw_block;
            return object;
        };

        /**
         * Converts this ConfDetails to JSON.
         * @function toJSON
         * @memberof chainrpc.ConfDetails
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConfDetails.prototype.toJSON = function() {
            return ConfDetails.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConfDetails
         * @function getTypeUrl
         * @memberof chainrpc.ConfDetails
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConfDetails.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/chainrpc.ConfDetails";
        };

        return ConfDetails;
    })();

    chainrpc.Reorg = (function() {

        /**
         * Properties of a Reorg.
         * @typedef {Object} chainrpc.Reorg.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Reorg.
         * @memberof chainrpc
         * @interface IReorg
         * @augments chainrpc.Reorg.$Properties
         * @deprecated Use chainrpc.Reorg.$Properties instead.
         */

        /**
         * Shape of a Reorg.
         * @typedef {chainrpc.Reorg.$Properties} chainrpc.Reorg.$Shape
         */

        /**
         * Constructs a new Reorg.
         * @memberof chainrpc
         * @classdesc Represents a Reorg.
         * @constructor
         * @param {chainrpc.Reorg.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Reorg = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new Reorg instance using the specified properties.
         * @function create
         * @memberof chainrpc.Reorg
         * @static
         * @param {chainrpc.Reorg.$Properties=} [properties] Properties to set
         * @returns {chainrpc.Reorg} Reorg instance
         * @type {{
         *   (properties: chainrpc.Reorg.$Shape): chainrpc.Reorg & chainrpc.Reorg.$Shape;
         *   (properties?: chainrpc.Reorg.$Properties): chainrpc.Reorg;
         * }}
         */
        Reorg.create = function(properties) {
            return new Reorg(properties);
        };

        /**
         * Encodes the specified Reorg message. Does not implicitly {@link chainrpc.Reorg.verify|verify} messages.
         * @function encode
         * @memberof chainrpc.Reorg
         * @static
         * @param {chainrpc.Reorg.$Properties} message Reorg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Reorg.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Reorg message, length delimited. Does not implicitly {@link chainrpc.Reorg.verify|verify} messages.
         * @function encodeDelimited
         * @memberof chainrpc.Reorg
         * @static
         * @param {chainrpc.Reorg.$Properties} message Reorg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Reorg.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Reorg message from the specified reader or buffer.
         * @function decode
         * @memberof chainrpc.Reorg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {chainrpc.Reorg & chainrpc.Reorg.$Shape} Reorg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Reorg.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.chainrpc.Reorg();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Reorg message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof chainrpc.Reorg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {chainrpc.Reorg & chainrpc.Reorg.$Shape} Reorg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Reorg.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Reorg message.
         * @function verify
         * @memberof chainrpc.Reorg
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Reorg.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a Reorg message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof chainrpc.Reorg
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {chainrpc.Reorg} Reorg
         */
        Reorg.fromObject = function (object, _depth) {
            if (object instanceof $root.chainrpc.Reorg)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".chainrpc.Reorg: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.chainrpc.Reorg();
        };

        /**
         * Creates a plain object from a Reorg message. Also converts values to other types if specified.
         * @function toObject
         * @memberof chainrpc.Reorg
         * @static
         * @param {chainrpc.Reorg} message Reorg
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Reorg.toObject = function () {
            return {};
        };

        /**
         * Converts this Reorg to JSON.
         * @function toJSON
         * @memberof chainrpc.Reorg
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Reorg.prototype.toJSON = function() {
            return Reorg.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Reorg
         * @function getTypeUrl
         * @memberof chainrpc.Reorg
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Reorg.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/chainrpc.Reorg";
        };

        return Reorg;
    })();

    chainrpc.ConfEvent = (function() {

        /**
         * Properties of a ConfEvent.
         * @typedef {Object} chainrpc.ConfEvent.$Properties
         * @property {chainrpc.ConfDetails.$Properties|null} [conf] ConfEvent conf
         * @property {chainrpc.Reorg.$Properties|null} [reorg] ConfEvent reorg
         * @property {"conf"|"reorg"} [event] ConfEvent event
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ConfEvent.
         * @memberof chainrpc
         * @interface IConfEvent
         * @augments chainrpc.ConfEvent.$Properties
         * @deprecated Use chainrpc.ConfEvent.$Properties instead.
         */

        /**
         * Narrowed shape of a ConfEvent.
         * @typedef {{
         *   conf?: chainrpc.ConfDetails.$Shape|null;
         *   reorg?: chainrpc.Reorg.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ event?: undefined; conf?: null; reorg?: null }|{ event?: "conf"; conf: chainrpc.ConfDetails.$Shape; reorg?: null }|{ event?: "reorg"; conf?: null; reorg: chainrpc.Reorg.$Shape })
         * )} chainrpc.ConfEvent.$Shape
         */

        /**
         * Constructs a new ConfEvent.
         * @memberof chainrpc
         * @classdesc Represents a ConfEvent.
         * @constructor
         * @param {chainrpc.ConfEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ConfEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ConfEvent conf.
         * @member {chainrpc.ConfDetails.$Properties|null|undefined} conf
         * @memberof chainrpc.ConfEvent
         * @instance
         */
        ConfEvent.prototype.conf = null;

        /**
         * ConfEvent reorg.
         * @member {chainrpc.Reorg.$Properties|null|undefined} reorg
         * @memberof chainrpc.ConfEvent
         * @instance
         */
        ConfEvent.prototype.reorg = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ConfEvent event.
         * @member {"conf"|"reorg"|undefined} event
         * @memberof chainrpc.ConfEvent
         * @instance
         */
        $Object.defineProperty(ConfEvent.prototype, "event", {
            get: $util.oneOfGetter($oneOfFields = ["conf", "reorg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ConfEvent instance using the specified properties.
         * @function create
         * @memberof chainrpc.ConfEvent
         * @static
         * @param {chainrpc.ConfEvent.$Properties=} [properties] Properties to set
         * @returns {chainrpc.ConfEvent} ConfEvent instance
         * @type {{
         *   (properties: chainrpc.ConfEvent.$Shape): chainrpc.ConfEvent & chainrpc.ConfEvent.$Shape;
         *   (properties?: chainrpc.ConfEvent.$Properties): chainrpc.ConfEvent;
         * }}
         */
        ConfEvent.create = function(properties) {
            return new ConfEvent(properties);
        };

        /**
         * Encodes the specified ConfEvent message. Does not implicitly {@link chainrpc.ConfEvent.verify|verify} messages.
         * @function encode
         * @memberof chainrpc.ConfEvent
         * @static
         * @param {chainrpc.ConfEvent.$Properties} message ConfEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConfEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.conf != null && $Object.hasOwnProperty.call(message, "conf"))
                $root.chainrpc.ConfDetails.encode(message.conf, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.reorg != null && $Object.hasOwnProperty.call(message, "reorg"))
                $root.chainrpc.Reorg.encode(message.reorg, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConfEvent message, length delimited. Does not implicitly {@link chainrpc.ConfEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof chainrpc.ConfEvent
         * @static
         * @param {chainrpc.ConfEvent.$Properties} message ConfEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConfEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ConfEvent message from the specified reader or buffer.
         * @function decode
         * @memberof chainrpc.ConfEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {chainrpc.ConfEvent & chainrpc.ConfEvent.$Shape} ConfEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConfEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.chainrpc.ConfEvent();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.conf = $root.chainrpc.ConfDetails.decode(reader, reader.uint32(), $undefined, _depth + 1, message.conf);
                        message.event = "conf";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.reorg = $root.chainrpc.Reorg.decode(reader, reader.uint32(), $undefined, _depth + 1, message.reorg);
                        message.event = "reorg";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConfEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof chainrpc.ConfEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {chainrpc.ConfEvent & chainrpc.ConfEvent.$Shape} ConfEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConfEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConfEvent message.
         * @function verify
         * @memberof chainrpc.ConfEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConfEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.conf != null && $Object.hasOwnProperty.call(message, "conf")) {
                properties.event = 1;
                {
                    let error = $root.chainrpc.ConfDetails.verify(message.conf, _depth + 1);
                    if (error)
                        return "conf." + error;
                }
            }
            if (message.reorg != null && $Object.hasOwnProperty.call(message, "reorg")) {
                if (properties.event === 1)
                    return "event: multiple values";
                properties.event = 1;
                {
                    let error = $root.chainrpc.Reorg.verify(message.reorg, _depth + 1);
                    if (error)
                        return "reorg." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ConfEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof chainrpc.ConfEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {chainrpc.ConfEvent} ConfEvent
         */
        ConfEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.chainrpc.ConfEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".chainrpc.ConfEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.chainrpc.ConfEvent();
            if (object.conf != null) {
                if (!$util.isObject(object.conf))
                    throw $TypeError(".chainrpc.ConfEvent.conf: object expected");
                message.conf = $root.chainrpc.ConfDetails.fromObject(object.conf, _depth + 1);
            }
            if (object.reorg != null) {
                if (!$util.isObject(object.reorg))
                    throw $TypeError(".chainrpc.ConfEvent.reorg: object expected");
                message.reorg = $root.chainrpc.Reorg.fromObject(object.reorg, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ConfEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof chainrpc.ConfEvent
         * @static
         * @param {chainrpc.ConfEvent} message ConfEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConfEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.conf != null && $Object.hasOwnProperty.call(message, "conf")) {
                object.conf = $root.chainrpc.ConfDetails.toObject(message.conf, options, _depth + 1);
                if (options.oneofs)
                    object.event = "conf";
            }
            if (message.reorg != null && $Object.hasOwnProperty.call(message, "reorg")) {
                object.reorg = $root.chainrpc.Reorg.toObject(message.reorg, options, _depth + 1);
                if (options.oneofs)
                    object.event = "reorg";
            }
            return object;
        };

        /**
         * Converts this ConfEvent to JSON.
         * @function toJSON
         * @memberof chainrpc.ConfEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConfEvent.prototype.toJSON = function() {
            return ConfEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConfEvent
         * @function getTypeUrl
         * @memberof chainrpc.ConfEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConfEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/chainrpc.ConfEvent";
        };

        return ConfEvent;
    })();

    chainrpc.Outpoint = (function() {

        /**
         * Properties of an Outpoint.
         * @typedef {Object} chainrpc.Outpoint.$Properties
         * @property {Uint8Array|null} [hash] Outpoint hash
         * @property {number|null} [index] Outpoint index
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an Outpoint.
         * @memberof chainrpc
         * @interface IOutpoint
         * @augments chainrpc.Outpoint.$Properties
         * @deprecated Use chainrpc.Outpoint.$Properties instead.
         */

        /**
         * Shape of an Outpoint.
         * @typedef {chainrpc.Outpoint.$Properties} chainrpc.Outpoint.$Shape
         */

        /**
         * Constructs a new Outpoint.
         * @memberof chainrpc
         * @classdesc Represents an Outpoint.
         * @constructor
         * @param {chainrpc.Outpoint.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Outpoint = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Outpoint hash.
         * @member {Uint8Array} hash
         * @memberof chainrpc.Outpoint
         * @instance
         */
        Outpoint.prototype.hash = $util.newBuffer([]);

        /**
         * Outpoint index.
         * @member {number} index
         * @memberof chainrpc.Outpoint
         * @instance
         */
        Outpoint.prototype.index = 0;

        /**
         * Creates a new Outpoint instance using the specified properties.
         * @function create
         * @memberof chainrpc.Outpoint
         * @static
         * @param {chainrpc.Outpoint.$Properties=} [properties] Properties to set
         * @returns {chainrpc.Outpoint} Outpoint instance
         * @type {{
         *   (properties: chainrpc.Outpoint.$Shape): chainrpc.Outpoint & chainrpc.Outpoint.$Shape;
         *   (properties?: chainrpc.Outpoint.$Properties): chainrpc.Outpoint;
         * }}
         */
        Outpoint.create = function(properties) {
            return new Outpoint(properties);
        };

        /**
         * Encodes the specified Outpoint message. Does not implicitly {@link chainrpc.Outpoint.verify|verify} messages.
         * @function encode
         * @memberof chainrpc.Outpoint
         * @static
         * @param {chainrpc.Outpoint.$Properties} message Outpoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Outpoint.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.hash);
            if (message.index != null && $Object.hasOwnProperty.call(message, "index") && message.index !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Outpoint message, length delimited. Does not implicitly {@link chainrpc.Outpoint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof chainrpc.Outpoint
         * @static
         * @param {chainrpc.Outpoint.$Properties} message Outpoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Outpoint.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Outpoint message from the specified reader or buffer.
         * @function decode
         * @memberof chainrpc.Outpoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {chainrpc.Outpoint & chainrpc.Outpoint.$Shape} Outpoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Outpoint.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.chainrpc.Outpoint(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index = value;
                        else
                            delete message.index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Outpoint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof chainrpc.Outpoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {chainrpc.Outpoint & chainrpc.Outpoint.$Shape} Outpoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Outpoint.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Outpoint message.
         * @function verify
         * @memberof chainrpc.Outpoint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Outpoint.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                    return "hash: buffer expected";
            if (message.index != null && $Object.hasOwnProperty.call(message, "index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            return null;
        };

        /**
         * Creates an Outpoint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof chainrpc.Outpoint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {chainrpc.Outpoint} Outpoint
         */
        Outpoint.fromObject = function (object, _depth) {
            if (object instanceof $root.chainrpc.Outpoint)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".chainrpc.Outpoint: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.chainrpc.Outpoint();
            if (object.hash != null)
                if (object.hash.length)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
            if (object.index != null)
                if ($Number(object.index) !== 0)
                    message.index = object.index >>> 0;
            return message;
        };

        /**
         * Creates a plain object from an Outpoint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof chainrpc.Outpoint
         * @static
         * @param {chainrpc.Outpoint} message Outpoint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Outpoint.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.hash = "";
                else {
                    object.hash = [];
                    if (options.bytes !== $Array)
                        object.hash = $util.newBuffer(object.hash);
                }
                object.index = 0;
            }
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = options.bytes === $String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash) : message.hash;
            if (message.index != null && $Object.hasOwnProperty.call(message, "index"))
                object.index = message.index;
            return object;
        };

        /**
         * Converts this Outpoint to JSON.
         * @function toJSON
         * @memberof chainrpc.Outpoint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Outpoint.prototype.toJSON = function() {
            return Outpoint.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Outpoint
         * @function getTypeUrl
         * @memberof chainrpc.Outpoint
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Outpoint.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/chainrpc.Outpoint";
        };

        return Outpoint;
    })();

    chainrpc.SpendRequest = (function() {

        /**
         * Properties of a SpendRequest.
         * @typedef {Object} chainrpc.SpendRequest.$Properties
         * @property {chainrpc.Outpoint.$Properties|null} [outpoint] SpendRequest outpoint
         * @property {Uint8Array|null} [script] SpendRequest script
         * @property {number|null} [height_hint] SpendRequest height_hint
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SpendRequest.
         * @memberof chainrpc
         * @interface ISpendRequest
         * @augments chainrpc.SpendRequest.$Properties
         * @deprecated Use chainrpc.SpendRequest.$Properties instead.
         */

        /**
         * Shape of a SpendRequest.
         * @typedef {chainrpc.SpendRequest.$Properties} chainrpc.SpendRequest.$Shape
         */

        /**
         * Constructs a new SpendRequest.
         * @memberof chainrpc
         * @classdesc Represents a SpendRequest.
         * @constructor
         * @param {chainrpc.SpendRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SpendRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SpendRequest outpoint.
         * @member {chainrpc.Outpoint.$Properties|null|undefined} outpoint
         * @memberof chainrpc.SpendRequest
         * @instance
         */
        SpendRequest.prototype.outpoint = null;

        /**
         * SpendRequest script.
         * @member {Uint8Array} script
         * @memberof chainrpc.SpendRequest
         * @instance
         */
        SpendRequest.prototype.script = $util.newBuffer([]);

        /**
         * SpendRequest height_hint.
         * @member {number} height_hint
         * @memberof chainrpc.SpendRequest
         * @instance
         */
        SpendRequest.prototype.height_hint = 0;

        /**
         * Creates a new SpendRequest instance using the specified properties.
         * @function create
         * @memberof chainrpc.SpendRequest
         * @static
         * @param {chainrpc.SpendRequest.$Properties=} [properties] Properties to set
         * @returns {chainrpc.SpendRequest} SpendRequest instance
         * @type {{
         *   (properties: chainrpc.SpendRequest.$Shape): chainrpc.SpendRequest & chainrpc.SpendRequest.$Shape;
         *   (properties?: chainrpc.SpendRequest.$Properties): chainrpc.SpendRequest;
         * }}
         */
        SpendRequest.create = function(properties) {
            return new SpendRequest(properties);
        };

        /**
         * Encodes the specified SpendRequest message. Does not implicitly {@link chainrpc.SpendRequest.verify|verify} messages.
         * @function encode
         * @memberof chainrpc.SpendRequest
         * @static
         * @param {chainrpc.SpendRequest.$Properties} message SpendRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpendRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                $root.chainrpc.Outpoint.encode(message.outpoint, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.script != null && $Object.hasOwnProperty.call(message, "script") && message.script.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.script);
            if (message.height_hint != null && $Object.hasOwnProperty.call(message, "height_hint") && message.height_hint !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.height_hint);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SpendRequest message, length delimited. Does not implicitly {@link chainrpc.SpendRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof chainrpc.SpendRequest
         * @static
         * @param {chainrpc.SpendRequest.$Properties} message SpendRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpendRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SpendRequest message from the specified reader or buffer.
         * @function decode
         * @memberof chainrpc.SpendRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {chainrpc.SpendRequest & chainrpc.SpendRequest.$Shape} SpendRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpendRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.chainrpc.SpendRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.outpoint = $root.chainrpc.Outpoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.outpoint);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.script = value;
                        else
                            delete message.script;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.height_hint = value;
                        else
                            delete message.height_hint;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SpendRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof chainrpc.SpendRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {chainrpc.SpendRequest & chainrpc.SpendRequest.$Shape} SpendRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpendRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpendRequest message.
         * @function verify
         * @memberof chainrpc.SpendRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpendRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint")) {
                let error = $root.chainrpc.Outpoint.verify(message.outpoint, _depth + 1);
                if (error)
                    return "outpoint." + error;
            }
            if (message.script != null && $Object.hasOwnProperty.call(message, "script"))
                if (!(message.script && typeof message.script.length === "number" || $util.isString(message.script)))
                    return "script: buffer expected";
            if (message.height_hint != null && $Object.hasOwnProperty.call(message, "height_hint"))
                if (!$util.isInteger(message.height_hint))
                    return "height_hint: integer expected";
            return null;
        };

        /**
         * Creates a SpendRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof chainrpc.SpendRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {chainrpc.SpendRequest} SpendRequest
         */
        SpendRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.chainrpc.SpendRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".chainrpc.SpendRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.chainrpc.SpendRequest();
            if (object.outpoint != null) {
                if (!$util.isObject(object.outpoint))
                    throw $TypeError(".chainrpc.SpendRequest.outpoint: object expected");
                message.outpoint = $root.chainrpc.Outpoint.fromObject(object.outpoint, _depth + 1);
            }
            if (object.script != null)
                if (object.script.length)
                    if (typeof object.script === "string")
                        $util.base64.decode(object.script, message.script = $util.newBuffer($util.base64.length(object.script)), 0);
                    else if (object.script.length >= 0)
                        message.script = object.script;
            if (object.height_hint != null)
                if ($Number(object.height_hint) !== 0)
                    message.height_hint = object.height_hint >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a SpendRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof chainrpc.SpendRequest
         * @static
         * @param {chainrpc.SpendRequest} message SpendRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpendRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.outpoint = null;
                if (options.bytes === $String)
                    object.script = "";
                else {
                    object.script = [];
                    if (options.bytes !== $Array)
                        object.script = $util.newBuffer(object.script);
                }
                object.height_hint = 0;
            }
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = $root.chainrpc.Outpoint.toObject(message.outpoint, options, _depth + 1);
            if (message.script != null && $Object.hasOwnProperty.call(message, "script"))
                object.script = options.bytes === $String ? $util.base64.encode(message.script, 0, message.script.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.script) : message.script;
            if (message.height_hint != null && $Object.hasOwnProperty.call(message, "height_hint"))
                object.height_hint = message.height_hint;
            return object;
        };

        /**
         * Converts this SpendRequest to JSON.
         * @function toJSON
         * @memberof chainrpc.SpendRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpendRequest.prototype.toJSON = function() {
            return SpendRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SpendRequest
         * @function getTypeUrl
         * @memberof chainrpc.SpendRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SpendRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/chainrpc.SpendRequest";
        };

        return SpendRequest;
    })();

    chainrpc.SpendDetails = (function() {

        /**
         * Properties of a SpendDetails.
         * @typedef {Object} chainrpc.SpendDetails.$Properties
         * @property {chainrpc.Outpoint.$Properties|null} [spending_outpoint] SpendDetails spending_outpoint
         * @property {Uint8Array|null} [raw_spending_tx] SpendDetails raw_spending_tx
         * @property {Uint8Array|null} [spending_tx_hash] SpendDetails spending_tx_hash
         * @property {number|null} [spending_input_index] SpendDetails spending_input_index
         * @property {number|null} [spending_height] SpendDetails spending_height
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SpendDetails.
         * @memberof chainrpc
         * @interface ISpendDetails
         * @augments chainrpc.SpendDetails.$Properties
         * @deprecated Use chainrpc.SpendDetails.$Properties instead.
         */

        /**
         * Shape of a SpendDetails.
         * @typedef {chainrpc.SpendDetails.$Properties} chainrpc.SpendDetails.$Shape
         */

        /**
         * Constructs a new SpendDetails.
         * @memberof chainrpc
         * @classdesc Represents a SpendDetails.
         * @constructor
         * @param {chainrpc.SpendDetails.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SpendDetails = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SpendDetails spending_outpoint.
         * @member {chainrpc.Outpoint.$Properties|null|undefined} spending_outpoint
         * @memberof chainrpc.SpendDetails
         * @instance
         */
        SpendDetails.prototype.spending_outpoint = null;

        /**
         * SpendDetails raw_spending_tx.
         * @member {Uint8Array} raw_spending_tx
         * @memberof chainrpc.SpendDetails
         * @instance
         */
        SpendDetails.prototype.raw_spending_tx = $util.newBuffer([]);

        /**
         * SpendDetails spending_tx_hash.
         * @member {Uint8Array} spending_tx_hash
         * @memberof chainrpc.SpendDetails
         * @instance
         */
        SpendDetails.prototype.spending_tx_hash = $util.newBuffer([]);

        /**
         * SpendDetails spending_input_index.
         * @member {number} spending_input_index
         * @memberof chainrpc.SpendDetails
         * @instance
         */
        SpendDetails.prototype.spending_input_index = 0;

        /**
         * SpendDetails spending_height.
         * @member {number} spending_height
         * @memberof chainrpc.SpendDetails
         * @instance
         */
        SpendDetails.prototype.spending_height = 0;

        /**
         * Creates a new SpendDetails instance using the specified properties.
         * @function create
         * @memberof chainrpc.SpendDetails
         * @static
         * @param {chainrpc.SpendDetails.$Properties=} [properties] Properties to set
         * @returns {chainrpc.SpendDetails} SpendDetails instance
         * @type {{
         *   (properties: chainrpc.SpendDetails.$Shape): chainrpc.SpendDetails & chainrpc.SpendDetails.$Shape;
         *   (properties?: chainrpc.SpendDetails.$Properties): chainrpc.SpendDetails;
         * }}
         */
        SpendDetails.create = function(properties) {
            return new SpendDetails(properties);
        };

        /**
         * Encodes the specified SpendDetails message. Does not implicitly {@link chainrpc.SpendDetails.verify|verify} messages.
         * @function encode
         * @memberof chainrpc.SpendDetails
         * @static
         * @param {chainrpc.SpendDetails.$Properties} message SpendDetails message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpendDetails.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.spending_outpoint != null && $Object.hasOwnProperty.call(message, "spending_outpoint"))
                $root.chainrpc.Outpoint.encode(message.spending_outpoint, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.raw_spending_tx != null && $Object.hasOwnProperty.call(message, "raw_spending_tx") && message.raw_spending_tx.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.raw_spending_tx);
            if (message.spending_tx_hash != null && $Object.hasOwnProperty.call(message, "spending_tx_hash") && message.spending_tx_hash.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.spending_tx_hash);
            if (message.spending_input_index != null && $Object.hasOwnProperty.call(message, "spending_input_index") && message.spending_input_index !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.spending_input_index);
            if (message.spending_height != null && $Object.hasOwnProperty.call(message, "spending_height") && message.spending_height !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.spending_height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SpendDetails message, length delimited. Does not implicitly {@link chainrpc.SpendDetails.verify|verify} messages.
         * @function encodeDelimited
         * @memberof chainrpc.SpendDetails
         * @static
         * @param {chainrpc.SpendDetails.$Properties} message SpendDetails message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpendDetails.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SpendDetails message from the specified reader or buffer.
         * @function decode
         * @memberof chainrpc.SpendDetails
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {chainrpc.SpendDetails & chainrpc.SpendDetails.$Shape} SpendDetails
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpendDetails.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.chainrpc.SpendDetails(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.spending_outpoint = $root.chainrpc.Outpoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spending_outpoint);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_spending_tx = value;
                        else
                            delete message.raw_spending_tx;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.spending_tx_hash = value;
                        else
                            delete message.spending_tx_hash;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.spending_input_index = value;
                        else
                            delete message.spending_input_index;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.spending_height = value;
                        else
                            delete message.spending_height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SpendDetails message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof chainrpc.SpendDetails
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {chainrpc.SpendDetails & chainrpc.SpendDetails.$Shape} SpendDetails
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpendDetails.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpendDetails message.
         * @function verify
         * @memberof chainrpc.SpendDetails
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpendDetails.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.spending_outpoint != null && $Object.hasOwnProperty.call(message, "spending_outpoint")) {
                let error = $root.chainrpc.Outpoint.verify(message.spending_outpoint, _depth + 1);
                if (error)
                    return "spending_outpoint." + error;
            }
            if (message.raw_spending_tx != null && $Object.hasOwnProperty.call(message, "raw_spending_tx"))
                if (!(message.raw_spending_tx && typeof message.raw_spending_tx.length === "number" || $util.isString(message.raw_spending_tx)))
                    return "raw_spending_tx: buffer expected";
            if (message.spending_tx_hash != null && $Object.hasOwnProperty.call(message, "spending_tx_hash"))
                if (!(message.spending_tx_hash && typeof message.spending_tx_hash.length === "number" || $util.isString(message.spending_tx_hash)))
                    return "spending_tx_hash: buffer expected";
            if (message.spending_input_index != null && $Object.hasOwnProperty.call(message, "spending_input_index"))
                if (!$util.isInteger(message.spending_input_index))
                    return "spending_input_index: integer expected";
            if (message.spending_height != null && $Object.hasOwnProperty.call(message, "spending_height"))
                if (!$util.isInteger(message.spending_height))
                    return "spending_height: integer expected";
            return null;
        };

        /**
         * Creates a SpendDetails message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof chainrpc.SpendDetails
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {chainrpc.SpendDetails} SpendDetails
         */
        SpendDetails.fromObject = function (object, _depth) {
            if (object instanceof $root.chainrpc.SpendDetails)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".chainrpc.SpendDetails: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.chainrpc.SpendDetails();
            if (object.spending_outpoint != null) {
                if (!$util.isObject(object.spending_outpoint))
                    throw $TypeError(".chainrpc.SpendDetails.spending_outpoint: object expected");
                message.spending_outpoint = $root.chainrpc.Outpoint.fromObject(object.spending_outpoint, _depth + 1);
            }
            if (object.raw_spending_tx != null)
                if (object.raw_spending_tx.length)
                    if (typeof object.raw_spending_tx === "string")
                        $util.base64.decode(object.raw_spending_tx, message.raw_spending_tx = $util.newBuffer($util.base64.length(object.raw_spending_tx)), 0);
                    else if (object.raw_spending_tx.length >= 0)
                        message.raw_spending_tx = object.raw_spending_tx;
            if (object.spending_tx_hash != null)
                if (object.spending_tx_hash.length)
                    if (typeof object.spending_tx_hash === "string")
                        $util.base64.decode(object.spending_tx_hash, message.spending_tx_hash = $util.newBuffer($util.base64.length(object.spending_tx_hash)), 0);
                    else if (object.spending_tx_hash.length >= 0)
                        message.spending_tx_hash = object.spending_tx_hash;
            if (object.spending_input_index != null)
                if ($Number(object.spending_input_index) !== 0)
                    message.spending_input_index = object.spending_input_index >>> 0;
            if (object.spending_height != null)
                if ($Number(object.spending_height) !== 0)
                    message.spending_height = object.spending_height >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a SpendDetails message. Also converts values to other types if specified.
         * @function toObject
         * @memberof chainrpc.SpendDetails
         * @static
         * @param {chainrpc.SpendDetails} message SpendDetails
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpendDetails.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.spending_outpoint = null;
                if (options.bytes === $String)
                    object.raw_spending_tx = "";
                else {
                    object.raw_spending_tx = [];
                    if (options.bytes !== $Array)
                        object.raw_spending_tx = $util.newBuffer(object.raw_spending_tx);
                }
                if (options.bytes === $String)
                    object.spending_tx_hash = "";
                else {
                    object.spending_tx_hash = [];
                    if (options.bytes !== $Array)
                        object.spending_tx_hash = $util.newBuffer(object.spending_tx_hash);
                }
                object.spending_input_index = 0;
                object.spending_height = 0;
            }
            if (message.spending_outpoint != null && $Object.hasOwnProperty.call(message, "spending_outpoint"))
                object.spending_outpoint = $root.chainrpc.Outpoint.toObject(message.spending_outpoint, options, _depth + 1);
            if (message.raw_spending_tx != null && $Object.hasOwnProperty.call(message, "raw_spending_tx"))
                object.raw_spending_tx = options.bytes === $String ? $util.base64.encode(message.raw_spending_tx, 0, message.raw_spending_tx.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_spending_tx) : message.raw_spending_tx;
            if (message.spending_tx_hash != null && $Object.hasOwnProperty.call(message, "spending_tx_hash"))
                object.spending_tx_hash = options.bytes === $String ? $util.base64.encode(message.spending_tx_hash, 0, message.spending_tx_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.spending_tx_hash) : message.spending_tx_hash;
            if (message.spending_input_index != null && $Object.hasOwnProperty.call(message, "spending_input_index"))
                object.spending_input_index = message.spending_input_index;
            if (message.spending_height != null && $Object.hasOwnProperty.call(message, "spending_height"))
                object.spending_height = message.spending_height;
            return object;
        };

        /**
         * Converts this SpendDetails to JSON.
         * @function toJSON
         * @memberof chainrpc.SpendDetails
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpendDetails.prototype.toJSON = function() {
            return SpendDetails.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SpendDetails
         * @function getTypeUrl
         * @memberof chainrpc.SpendDetails
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SpendDetails.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/chainrpc.SpendDetails";
        };

        return SpendDetails;
    })();

    chainrpc.SpendEvent = (function() {

        /**
         * Properties of a SpendEvent.
         * @typedef {Object} chainrpc.SpendEvent.$Properties
         * @property {chainrpc.SpendDetails.$Properties|null} [spend] SpendEvent spend
         * @property {chainrpc.Reorg.$Properties|null} [reorg] SpendEvent reorg
         * @property {"spend"|"reorg"} [event] SpendEvent event
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SpendEvent.
         * @memberof chainrpc
         * @interface ISpendEvent
         * @augments chainrpc.SpendEvent.$Properties
         * @deprecated Use chainrpc.SpendEvent.$Properties instead.
         */

        /**
         * Narrowed shape of a SpendEvent.
         * @typedef {{
         *   spend?: chainrpc.SpendDetails.$Shape|null;
         *   reorg?: chainrpc.Reorg.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ event?: undefined; spend?: null; reorg?: null }|{ event?: "spend"; spend: chainrpc.SpendDetails.$Shape; reorg?: null }|{ event?: "reorg"; spend?: null; reorg: chainrpc.Reorg.$Shape })
         * )} chainrpc.SpendEvent.$Shape
         */

        /**
         * Constructs a new SpendEvent.
         * @memberof chainrpc
         * @classdesc Represents a SpendEvent.
         * @constructor
         * @param {chainrpc.SpendEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SpendEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SpendEvent spend.
         * @member {chainrpc.SpendDetails.$Properties|null|undefined} spend
         * @memberof chainrpc.SpendEvent
         * @instance
         */
        SpendEvent.prototype.spend = null;

        /**
         * SpendEvent reorg.
         * @member {chainrpc.Reorg.$Properties|null|undefined} reorg
         * @memberof chainrpc.SpendEvent
         * @instance
         */
        SpendEvent.prototype.reorg = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * SpendEvent event.
         * @member {"spend"|"reorg"|undefined} event
         * @memberof chainrpc.SpendEvent
         * @instance
         */
        $Object.defineProperty(SpendEvent.prototype, "event", {
            get: $util.oneOfGetter($oneOfFields = ["spend", "reorg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SpendEvent instance using the specified properties.
         * @function create
         * @memberof chainrpc.SpendEvent
         * @static
         * @param {chainrpc.SpendEvent.$Properties=} [properties] Properties to set
         * @returns {chainrpc.SpendEvent} SpendEvent instance
         * @type {{
         *   (properties: chainrpc.SpendEvent.$Shape): chainrpc.SpendEvent & chainrpc.SpendEvent.$Shape;
         *   (properties?: chainrpc.SpendEvent.$Properties): chainrpc.SpendEvent;
         * }}
         */
        SpendEvent.create = function(properties) {
            return new SpendEvent(properties);
        };

        /**
         * Encodes the specified SpendEvent message. Does not implicitly {@link chainrpc.SpendEvent.verify|verify} messages.
         * @function encode
         * @memberof chainrpc.SpendEvent
         * @static
         * @param {chainrpc.SpendEvent.$Properties} message SpendEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpendEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.spend != null && $Object.hasOwnProperty.call(message, "spend"))
                $root.chainrpc.SpendDetails.encode(message.spend, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.reorg != null && $Object.hasOwnProperty.call(message, "reorg"))
                $root.chainrpc.Reorg.encode(message.reorg, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SpendEvent message, length delimited. Does not implicitly {@link chainrpc.SpendEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof chainrpc.SpendEvent
         * @static
         * @param {chainrpc.SpendEvent.$Properties} message SpendEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpendEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SpendEvent message from the specified reader or buffer.
         * @function decode
         * @memberof chainrpc.SpendEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {chainrpc.SpendEvent & chainrpc.SpendEvent.$Shape} SpendEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpendEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.chainrpc.SpendEvent();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.spend = $root.chainrpc.SpendDetails.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spend);
                        message.event = "spend";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.reorg = $root.chainrpc.Reorg.decode(reader, reader.uint32(), $undefined, _depth + 1, message.reorg);
                        message.event = "reorg";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SpendEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof chainrpc.SpendEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {chainrpc.SpendEvent & chainrpc.SpendEvent.$Shape} SpendEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpendEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpendEvent message.
         * @function verify
         * @memberof chainrpc.SpendEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpendEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.spend != null && $Object.hasOwnProperty.call(message, "spend")) {
                properties.event = 1;
                {
                    let error = $root.chainrpc.SpendDetails.verify(message.spend, _depth + 1);
                    if (error)
                        return "spend." + error;
                }
            }
            if (message.reorg != null && $Object.hasOwnProperty.call(message, "reorg")) {
                if (properties.event === 1)
                    return "event: multiple values";
                properties.event = 1;
                {
                    let error = $root.chainrpc.Reorg.verify(message.reorg, _depth + 1);
                    if (error)
                        return "reorg." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SpendEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof chainrpc.SpendEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {chainrpc.SpendEvent} SpendEvent
         */
        SpendEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.chainrpc.SpendEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".chainrpc.SpendEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.chainrpc.SpendEvent();
            if (object.spend != null) {
                if (!$util.isObject(object.spend))
                    throw $TypeError(".chainrpc.SpendEvent.spend: object expected");
                message.spend = $root.chainrpc.SpendDetails.fromObject(object.spend, _depth + 1);
            }
            if (object.reorg != null) {
                if (!$util.isObject(object.reorg))
                    throw $TypeError(".chainrpc.SpendEvent.reorg: object expected");
                message.reorg = $root.chainrpc.Reorg.fromObject(object.reorg, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SpendEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof chainrpc.SpendEvent
         * @static
         * @param {chainrpc.SpendEvent} message SpendEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpendEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.spend != null && $Object.hasOwnProperty.call(message, "spend")) {
                object.spend = $root.chainrpc.SpendDetails.toObject(message.spend, options, _depth + 1);
                if (options.oneofs)
                    object.event = "spend";
            }
            if (message.reorg != null && $Object.hasOwnProperty.call(message, "reorg")) {
                object.reorg = $root.chainrpc.Reorg.toObject(message.reorg, options, _depth + 1);
                if (options.oneofs)
                    object.event = "reorg";
            }
            return object;
        };

        /**
         * Converts this SpendEvent to JSON.
         * @function toJSON
         * @memberof chainrpc.SpendEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpendEvent.prototype.toJSON = function() {
            return SpendEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SpendEvent
         * @function getTypeUrl
         * @memberof chainrpc.SpendEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SpendEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/chainrpc.SpendEvent";
        };

        return SpendEvent;
    })();

    chainrpc.BlockEpoch = (function() {

        /**
         * Properties of a BlockEpoch.
         * @typedef {Object} chainrpc.BlockEpoch.$Properties
         * @property {Uint8Array|null} [hash] BlockEpoch hash
         * @property {number|null} [height] BlockEpoch height
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BlockEpoch.
         * @memberof chainrpc
         * @interface IBlockEpoch
         * @augments chainrpc.BlockEpoch.$Properties
         * @deprecated Use chainrpc.BlockEpoch.$Properties instead.
         */

        /**
         * Shape of a BlockEpoch.
         * @typedef {chainrpc.BlockEpoch.$Properties} chainrpc.BlockEpoch.$Shape
         */

        /**
         * Constructs a new BlockEpoch.
         * @memberof chainrpc
         * @classdesc Represents a BlockEpoch.
         * @constructor
         * @param {chainrpc.BlockEpoch.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BlockEpoch = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BlockEpoch hash.
         * @member {Uint8Array} hash
         * @memberof chainrpc.BlockEpoch
         * @instance
         */
        BlockEpoch.prototype.hash = $util.newBuffer([]);

        /**
         * BlockEpoch height.
         * @member {number} height
         * @memberof chainrpc.BlockEpoch
         * @instance
         */
        BlockEpoch.prototype.height = 0;

        /**
         * Creates a new BlockEpoch instance using the specified properties.
         * @function create
         * @memberof chainrpc.BlockEpoch
         * @static
         * @param {chainrpc.BlockEpoch.$Properties=} [properties] Properties to set
         * @returns {chainrpc.BlockEpoch} BlockEpoch instance
         * @type {{
         *   (properties: chainrpc.BlockEpoch.$Shape): chainrpc.BlockEpoch & chainrpc.BlockEpoch.$Shape;
         *   (properties?: chainrpc.BlockEpoch.$Properties): chainrpc.BlockEpoch;
         * }}
         */
        BlockEpoch.create = function(properties) {
            return new BlockEpoch(properties);
        };

        /**
         * Encodes the specified BlockEpoch message. Does not implicitly {@link chainrpc.BlockEpoch.verify|verify} messages.
         * @function encode
         * @memberof chainrpc.BlockEpoch
         * @static
         * @param {chainrpc.BlockEpoch.$Properties} message BlockEpoch message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockEpoch.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.hash);
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BlockEpoch message, length delimited. Does not implicitly {@link chainrpc.BlockEpoch.verify|verify} messages.
         * @function encodeDelimited
         * @memberof chainrpc.BlockEpoch
         * @static
         * @param {chainrpc.BlockEpoch.$Properties} message BlockEpoch message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockEpoch.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BlockEpoch message from the specified reader or buffer.
         * @function decode
         * @memberof chainrpc.BlockEpoch
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {chainrpc.BlockEpoch & chainrpc.BlockEpoch.$Shape} BlockEpoch
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockEpoch.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.chainrpc.BlockEpoch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BlockEpoch message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof chainrpc.BlockEpoch
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {chainrpc.BlockEpoch & chainrpc.BlockEpoch.$Shape} BlockEpoch
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockEpoch.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BlockEpoch message.
         * @function verify
         * @memberof chainrpc.BlockEpoch
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BlockEpoch.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                    return "hash: buffer expected";
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            return null;
        };

        /**
         * Creates a BlockEpoch message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof chainrpc.BlockEpoch
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {chainrpc.BlockEpoch} BlockEpoch
         */
        BlockEpoch.fromObject = function (object, _depth) {
            if (object instanceof $root.chainrpc.BlockEpoch)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".chainrpc.BlockEpoch: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.chainrpc.BlockEpoch();
            if (object.hash != null)
                if (object.hash.length)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a BlockEpoch message. Also converts values to other types if specified.
         * @function toObject
         * @memberof chainrpc.BlockEpoch
         * @static
         * @param {chainrpc.BlockEpoch} message BlockEpoch
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BlockEpoch.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.hash = "";
                else {
                    object.hash = [];
                    if (options.bytes !== $Array)
                        object.hash = $util.newBuffer(object.hash);
                }
                object.height = 0;
            }
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = options.bytes === $String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash) : message.hash;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            return object;
        };

        /**
         * Converts this BlockEpoch to JSON.
         * @function toJSON
         * @memberof chainrpc.BlockEpoch
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BlockEpoch.prototype.toJSON = function() {
            return BlockEpoch.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BlockEpoch
         * @function getTypeUrl
         * @memberof chainrpc.BlockEpoch
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BlockEpoch.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/chainrpc.BlockEpoch";
        };

        return BlockEpoch;
    })();

    return chainrpc;
})();

export const invoicesrpc = $root.invoicesrpc = (() => {

    /**
     * Namespace invoicesrpc.
     * @exports invoicesrpc
     * @namespace
     */
    const invoicesrpc = {};

    invoicesrpc.Invoices = (function() {

        /**
         * Constructs a new Invoices service.
         * @memberof invoicesrpc
         * @classdesc Represents an Invoices
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const Invoices = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (Invoices.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = Invoices;

        /**
         * Creates new Invoices service using the specified rpc implementation.
         * @function create
         * @memberof invoicesrpc.Invoices
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {Invoices} RPC service. Useful where requests and/or responses are streamed.
         */
        Invoices.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link invoicesrpc.Invoices#subscribeSingleInvoice}.
         * @memberof invoicesrpc.Invoices
         * @typedef SubscribeSingleInvoiceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.Invoice} [response] Invoice
         */

        /**
         * Calls SubscribeSingleInvoice.
         * @memberof invoicesrpc.Invoices
         * @typedef SubscribeSingleInvoice
         * @type {{
         *   (request: invoicesrpc.ISubscribeSingleInvoiceRequest, callback: invoicesrpc.Invoices.SubscribeSingleInvoiceCallback): void;
         *   (request: invoicesrpc.ISubscribeSingleInvoiceRequest): Promise<lnrpc.Invoice>;
         *   readonly name: "SubscribeSingleInvoice";
         *   readonly path: "/invoicesrpc.Invoices/SubscribeSingleInvoice";
         *   readonly requestType: "SubscribeSingleInvoiceRequest";
         *   readonly responseType: "lnrpc.Invoice";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribeSingleInvoice.
         * @name invoicesrpc.Invoices#subscribeSingleInvoice
         * @type {invoicesrpc.Invoices.SubscribeSingleInvoice}
         */
        $Object.defineProperties(Invoices.prototype.subscribeSingleInvoice = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Invoices.prototype.subscribeSingleInvoice, $root.invoicesrpc.SubscribeSingleInvoiceRequest, $root.lnrpc.Invoice, request, callback);
        }, {
            name: { value: "SubscribeSingleInvoice" },
            path: { value: "/invoicesrpc.Invoices/SubscribeSingleInvoice" },
            requestType: { value: "SubscribeSingleInvoiceRequest" },
            responseType: { value: "lnrpc.Invoice" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link invoicesrpc.Invoices#cancelInvoice}.
         * @memberof invoicesrpc.Invoices
         * @typedef CancelInvoiceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {invoicesrpc.CancelInvoiceResp} [response] CancelInvoiceResp
         */

        /**
         * Calls CancelInvoice.
         * @memberof invoicesrpc.Invoices
         * @typedef CancelInvoice
         * @type {{
         *   (request: invoicesrpc.ICancelInvoiceMsg, callback: invoicesrpc.Invoices.CancelInvoiceCallback): void;
         *   (request: invoicesrpc.ICancelInvoiceMsg): Promise<invoicesrpc.CancelInvoiceResp>;
         *   readonly name: "CancelInvoice";
         *   readonly path: "/invoicesrpc.Invoices/CancelInvoice";
         *   readonly requestType: "CancelInvoiceMsg";
         *   readonly responseType: "CancelInvoiceResp";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls CancelInvoice.
         * @name invoicesrpc.Invoices#cancelInvoice
         * @type {invoicesrpc.Invoices.CancelInvoice}
         */
        $Object.defineProperties(Invoices.prototype.cancelInvoice = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Invoices.prototype.cancelInvoice, $root.invoicesrpc.CancelInvoiceMsg, $root.invoicesrpc.CancelInvoiceResp, request, callback);
        }, {
            name: { value: "CancelInvoice" },
            path: { value: "/invoicesrpc.Invoices/CancelInvoice" },
            requestType: { value: "CancelInvoiceMsg" },
            responseType: { value: "CancelInvoiceResp" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link invoicesrpc.Invoices#addHoldInvoice}.
         * @memberof invoicesrpc.Invoices
         * @typedef AddHoldInvoiceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {invoicesrpc.AddHoldInvoiceResp} [response] AddHoldInvoiceResp
         */

        /**
         * Calls AddHoldInvoice.
         * @memberof invoicesrpc.Invoices
         * @typedef AddHoldInvoice
         * @type {{
         *   (request: invoicesrpc.IAddHoldInvoiceRequest, callback: invoicesrpc.Invoices.AddHoldInvoiceCallback): void;
         *   (request: invoicesrpc.IAddHoldInvoiceRequest): Promise<invoicesrpc.AddHoldInvoiceResp>;
         *   readonly name: "AddHoldInvoice";
         *   readonly path: "/invoicesrpc.Invoices/AddHoldInvoice";
         *   readonly requestType: "AddHoldInvoiceRequest";
         *   readonly responseType: "AddHoldInvoiceResp";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls AddHoldInvoice.
         * @name invoicesrpc.Invoices#addHoldInvoice
         * @type {invoicesrpc.Invoices.AddHoldInvoice}
         */
        $Object.defineProperties(Invoices.prototype.addHoldInvoice = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Invoices.prototype.addHoldInvoice, $root.invoicesrpc.AddHoldInvoiceRequest, $root.invoicesrpc.AddHoldInvoiceResp, request, callback);
        }, {
            name: { value: "AddHoldInvoice" },
            path: { value: "/invoicesrpc.Invoices/AddHoldInvoice" },
            requestType: { value: "AddHoldInvoiceRequest" },
            responseType: { value: "AddHoldInvoiceResp" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link invoicesrpc.Invoices#settleInvoice}.
         * @memberof invoicesrpc.Invoices
         * @typedef SettleInvoiceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {invoicesrpc.SettleInvoiceResp} [response] SettleInvoiceResp
         */

        /**
         * Calls SettleInvoice.
         * @memberof invoicesrpc.Invoices
         * @typedef SettleInvoice
         * @type {{
         *   (request: invoicesrpc.ISettleInvoiceMsg, callback: invoicesrpc.Invoices.SettleInvoiceCallback): void;
         *   (request: invoicesrpc.ISettleInvoiceMsg): Promise<invoicesrpc.SettleInvoiceResp>;
         *   readonly name: "SettleInvoice";
         *   readonly path: "/invoicesrpc.Invoices/SettleInvoice";
         *   readonly requestType: "SettleInvoiceMsg";
         *   readonly responseType: "SettleInvoiceResp";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SettleInvoice.
         * @name invoicesrpc.Invoices#settleInvoice
         * @type {invoicesrpc.Invoices.SettleInvoice}
         */
        $Object.defineProperties(Invoices.prototype.settleInvoice = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Invoices.prototype.settleInvoice, $root.invoicesrpc.SettleInvoiceMsg, $root.invoicesrpc.SettleInvoiceResp, request, callback);
        }, {
            name: { value: "SettleInvoice" },
            path: { value: "/invoicesrpc.Invoices/SettleInvoice" },
            requestType: { value: "SettleInvoiceMsg" },
            responseType: { value: "SettleInvoiceResp" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link invoicesrpc.Invoices#lookupInvoiceV2}.
         * @memberof invoicesrpc.Invoices
         * @typedef LookupInvoiceV2Callback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.Invoice} [response] Invoice
         */

        /**
         * Calls LookupInvoiceV2.
         * @memberof invoicesrpc.Invoices
         * @typedef LookupInvoiceV2
         * @type {{
         *   (request: invoicesrpc.ILookupInvoiceMsg, callback: invoicesrpc.Invoices.LookupInvoiceV2Callback): void;
         *   (request: invoicesrpc.ILookupInvoiceMsg): Promise<lnrpc.Invoice>;
         *   readonly name: "LookupInvoiceV2";
         *   readonly path: "/invoicesrpc.Invoices/LookupInvoiceV2";
         *   readonly requestType: "LookupInvoiceMsg";
         *   readonly responseType: "lnrpc.Invoice";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls LookupInvoiceV2.
         * @name invoicesrpc.Invoices#lookupInvoiceV2
         * @type {invoicesrpc.Invoices.LookupInvoiceV2}
         */
        $Object.defineProperties(Invoices.prototype.lookupInvoiceV2 = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Invoices.prototype.lookupInvoiceV2, $root.invoicesrpc.LookupInvoiceMsg, $root.lnrpc.Invoice, request, callback);
        }, {
            name: { value: "LookupInvoiceV2" },
            path: { value: "/invoicesrpc.Invoices/LookupInvoiceV2" },
            requestType: { value: "LookupInvoiceMsg" },
            responseType: { value: "lnrpc.Invoice" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link invoicesrpc.Invoices#htlcModifier}.
         * @memberof invoicesrpc.Invoices
         * @typedef HtlcModifierCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {invoicesrpc.HtlcModifyRequest} [response] HtlcModifyRequest
         */

        /**
         * Calls HtlcModifier.
         * @memberof invoicesrpc.Invoices
         * @typedef HtlcModifier
         * @type {{
         *   (request: invoicesrpc.IHtlcModifyResponse, callback: invoicesrpc.Invoices.HtlcModifierCallback): void;
         *   (request: invoicesrpc.IHtlcModifyResponse): Promise<invoicesrpc.HtlcModifyRequest>;
         *   readonly name: "HtlcModifier";
         *   readonly path: "/invoicesrpc.Invoices/HtlcModifier";
         *   readonly requestType: "HtlcModifyResponse";
         *   readonly responseType: "HtlcModifyRequest";
         *   readonly requestStream: true;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls HtlcModifier.
         * @name invoicesrpc.Invoices#htlcModifier
         * @type {invoicesrpc.Invoices.HtlcModifier}
         */
        $Object.defineProperties(Invoices.prototype.htlcModifier = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Invoices.prototype.htlcModifier, $root.invoicesrpc.HtlcModifyResponse, $root.invoicesrpc.HtlcModifyRequest, request, callback);
        }, {
            name: { value: "HtlcModifier" },
            path: { value: "/invoicesrpc.Invoices/HtlcModifier" },
            requestType: { value: "HtlcModifyResponse" },
            responseType: { value: "HtlcModifyRequest" },
            requestStream: { value: true },
            responseStream: { value: true }
        });

        return Invoices;
    })();

    invoicesrpc.CancelInvoiceMsg = (function() {

        /**
         * Properties of a CancelInvoiceMsg.
         * @typedef {Object} invoicesrpc.CancelInvoiceMsg.$Properties
         * @property {Uint8Array|null} [payment_hash] CancelInvoiceMsg payment_hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CancelInvoiceMsg.
         * @memberof invoicesrpc
         * @interface ICancelInvoiceMsg
         * @augments invoicesrpc.CancelInvoiceMsg.$Properties
         * @deprecated Use invoicesrpc.CancelInvoiceMsg.$Properties instead.
         */

        /**
         * Shape of a CancelInvoiceMsg.
         * @typedef {invoicesrpc.CancelInvoiceMsg.$Properties} invoicesrpc.CancelInvoiceMsg.$Shape
         */

        /**
         * Constructs a new CancelInvoiceMsg.
         * @memberof invoicesrpc
         * @classdesc Represents a CancelInvoiceMsg.
         * @constructor
         * @param {invoicesrpc.CancelInvoiceMsg.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CancelInvoiceMsg = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CancelInvoiceMsg payment_hash.
         * @member {Uint8Array} payment_hash
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @instance
         */
        CancelInvoiceMsg.prototype.payment_hash = $util.newBuffer([]);

        /**
         * Creates a new CancelInvoiceMsg instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @static
         * @param {invoicesrpc.CancelInvoiceMsg.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.CancelInvoiceMsg} CancelInvoiceMsg instance
         * @type {{
         *   (properties: invoicesrpc.CancelInvoiceMsg.$Shape): invoicesrpc.CancelInvoiceMsg & invoicesrpc.CancelInvoiceMsg.$Shape;
         *   (properties?: invoicesrpc.CancelInvoiceMsg.$Properties): invoicesrpc.CancelInvoiceMsg;
         * }}
         */
        CancelInvoiceMsg.create = function(properties) {
            return new CancelInvoiceMsg(properties);
        };

        /**
         * Encodes the specified CancelInvoiceMsg message. Does not implicitly {@link invoicesrpc.CancelInvoiceMsg.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @static
         * @param {invoicesrpc.CancelInvoiceMsg.$Properties} message CancelInvoiceMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CancelInvoiceMsg.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.payment_hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CancelInvoiceMsg message, length delimited. Does not implicitly {@link invoicesrpc.CancelInvoiceMsg.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @static
         * @param {invoicesrpc.CancelInvoiceMsg.$Properties} message CancelInvoiceMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CancelInvoiceMsg.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CancelInvoiceMsg message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.CancelInvoiceMsg & invoicesrpc.CancelInvoiceMsg.$Shape} CancelInvoiceMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CancelInvoiceMsg.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.CancelInvoiceMsg(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CancelInvoiceMsg message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.CancelInvoiceMsg & invoicesrpc.CancelInvoiceMsg.$Shape} CancelInvoiceMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CancelInvoiceMsg.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CancelInvoiceMsg message.
         * @function verify
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CancelInvoiceMsg.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            return null;
        };

        /**
         * Creates a CancelInvoiceMsg message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.CancelInvoiceMsg} CancelInvoiceMsg
         */
        CancelInvoiceMsg.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.CancelInvoiceMsg)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.CancelInvoiceMsg: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.invoicesrpc.CancelInvoiceMsg();
            if (object.payment_hash != null)
                if (object.payment_hash.length)
                    if (typeof object.payment_hash === "string")
                        $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                    else if (object.payment_hash.length >= 0)
                        message.payment_hash = object.payment_hash;
            return message;
        };

        /**
         * Creates a plain object from a CancelInvoiceMsg message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @static
         * @param {invoicesrpc.CancelInvoiceMsg} message CancelInvoiceMsg
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CancelInvoiceMsg.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.payment_hash = "";
                else {
                    object.payment_hash = [];
                    if (options.bytes !== $Array)
                        object.payment_hash = $util.newBuffer(object.payment_hash);
                }
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
            return object;
        };

        /**
         * Converts this CancelInvoiceMsg to JSON.
         * @function toJSON
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CancelInvoiceMsg.prototype.toJSON = function() {
            return CancelInvoiceMsg.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CancelInvoiceMsg
         * @function getTypeUrl
         * @memberof invoicesrpc.CancelInvoiceMsg
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CancelInvoiceMsg.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.CancelInvoiceMsg";
        };

        return CancelInvoiceMsg;
    })();

    invoicesrpc.CancelInvoiceResp = (function() {

        /**
         * Properties of a CancelInvoiceResp.
         * @typedef {Object} invoicesrpc.CancelInvoiceResp.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CancelInvoiceResp.
         * @memberof invoicesrpc
         * @interface ICancelInvoiceResp
         * @augments invoicesrpc.CancelInvoiceResp.$Properties
         * @deprecated Use invoicesrpc.CancelInvoiceResp.$Properties instead.
         */

        /**
         * Shape of a CancelInvoiceResp.
         * @typedef {invoicesrpc.CancelInvoiceResp.$Properties} invoicesrpc.CancelInvoiceResp.$Shape
         */

        /**
         * Constructs a new CancelInvoiceResp.
         * @memberof invoicesrpc
         * @classdesc Represents a CancelInvoiceResp.
         * @constructor
         * @param {invoicesrpc.CancelInvoiceResp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CancelInvoiceResp = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new CancelInvoiceResp instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.CancelInvoiceResp
         * @static
         * @param {invoicesrpc.CancelInvoiceResp.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.CancelInvoiceResp} CancelInvoiceResp instance
         * @type {{
         *   (properties: invoicesrpc.CancelInvoiceResp.$Shape): invoicesrpc.CancelInvoiceResp & invoicesrpc.CancelInvoiceResp.$Shape;
         *   (properties?: invoicesrpc.CancelInvoiceResp.$Properties): invoicesrpc.CancelInvoiceResp;
         * }}
         */
        CancelInvoiceResp.create = function(properties) {
            return new CancelInvoiceResp(properties);
        };

        /**
         * Encodes the specified CancelInvoiceResp message. Does not implicitly {@link invoicesrpc.CancelInvoiceResp.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.CancelInvoiceResp
         * @static
         * @param {invoicesrpc.CancelInvoiceResp.$Properties} message CancelInvoiceResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CancelInvoiceResp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CancelInvoiceResp message, length delimited. Does not implicitly {@link invoicesrpc.CancelInvoiceResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.CancelInvoiceResp
         * @static
         * @param {invoicesrpc.CancelInvoiceResp.$Properties} message CancelInvoiceResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CancelInvoiceResp.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CancelInvoiceResp message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.CancelInvoiceResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.CancelInvoiceResp & invoicesrpc.CancelInvoiceResp.$Shape} CancelInvoiceResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CancelInvoiceResp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.CancelInvoiceResp();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CancelInvoiceResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.CancelInvoiceResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.CancelInvoiceResp & invoicesrpc.CancelInvoiceResp.$Shape} CancelInvoiceResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CancelInvoiceResp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CancelInvoiceResp message.
         * @function verify
         * @memberof invoicesrpc.CancelInvoiceResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CancelInvoiceResp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a CancelInvoiceResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.CancelInvoiceResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.CancelInvoiceResp} CancelInvoiceResp
         */
        CancelInvoiceResp.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.CancelInvoiceResp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.CancelInvoiceResp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.invoicesrpc.CancelInvoiceResp();
        };

        /**
         * Creates a plain object from a CancelInvoiceResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.CancelInvoiceResp
         * @static
         * @param {invoicesrpc.CancelInvoiceResp} message CancelInvoiceResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CancelInvoiceResp.toObject = function () {
            return {};
        };

        /**
         * Converts this CancelInvoiceResp to JSON.
         * @function toJSON
         * @memberof invoicesrpc.CancelInvoiceResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CancelInvoiceResp.prototype.toJSON = function() {
            return CancelInvoiceResp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CancelInvoiceResp
         * @function getTypeUrl
         * @memberof invoicesrpc.CancelInvoiceResp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CancelInvoiceResp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.CancelInvoiceResp";
        };

        return CancelInvoiceResp;
    })();

    invoicesrpc.AddHoldInvoiceRequest = (function() {

        /**
         * Properties of an AddHoldInvoiceRequest.
         * @typedef {Object} invoicesrpc.AddHoldInvoiceRequest.$Properties
         * @property {string|null} [memo] AddHoldInvoiceRequest memo
         * @property {Uint8Array|null} [hash] AddHoldInvoiceRequest hash
         * @property {Long|null} [value] AddHoldInvoiceRequest value
         * @property {Long|null} [value_msat] AddHoldInvoiceRequest value_msat
         * @property {Uint8Array|null} [description_hash] AddHoldInvoiceRequest description_hash
         * @property {Long|null} [expiry] AddHoldInvoiceRequest expiry
         * @property {string|null} [fallback_addr] AddHoldInvoiceRequest fallback_addr
         * @property {Long|null} [cltv_expiry] AddHoldInvoiceRequest cltv_expiry
         * @property {Array.<lnrpc.RouteHint.$Properties>|null} [route_hints] AddHoldInvoiceRequest route_hints
         * @property {boolean|null} ["private"] AddHoldInvoiceRequest private
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddHoldInvoiceRequest.
         * @memberof invoicesrpc
         * @interface IAddHoldInvoiceRequest
         * @augments invoicesrpc.AddHoldInvoiceRequest.$Properties
         * @deprecated Use invoicesrpc.AddHoldInvoiceRequest.$Properties instead.
         */

        /**
         * Shape of an AddHoldInvoiceRequest.
         * @typedef {invoicesrpc.AddHoldInvoiceRequest.$Properties} invoicesrpc.AddHoldInvoiceRequest.$Shape
         */

        /**
         * Constructs a new AddHoldInvoiceRequest.
         * @memberof invoicesrpc
         * @classdesc Represents an AddHoldInvoiceRequest.
         * @constructor
         * @param {invoicesrpc.AddHoldInvoiceRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddHoldInvoiceRequest = function (properties) {
            this.route_hints = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddHoldInvoiceRequest memo.
         * @member {string} memo
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype.memo = "";

        /**
         * AddHoldInvoiceRequest hash.
         * @member {Uint8Array} hash
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype.hash = $util.newBuffer([]);

        /**
         * AddHoldInvoiceRequest value.
         * @member {Long} value
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype.value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AddHoldInvoiceRequest value_msat.
         * @member {Long} value_msat
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype.value_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AddHoldInvoiceRequest description_hash.
         * @member {Uint8Array} description_hash
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype.description_hash = $util.newBuffer([]);

        /**
         * AddHoldInvoiceRequest expiry.
         * @member {Long} expiry
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype.expiry = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AddHoldInvoiceRequest fallback_addr.
         * @member {string} fallback_addr
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype.fallback_addr = "";

        /**
         * AddHoldInvoiceRequest cltv_expiry.
         * @member {Long} cltv_expiry
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype.cltv_expiry = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * AddHoldInvoiceRequest route_hints.
         * @member {Array.<lnrpc.RouteHint.$Properties>} route_hints
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype.route_hints = $util.emptyArray;

        /**
         * AddHoldInvoiceRequest private.
         * @member {boolean} private
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         */
        AddHoldInvoiceRequest.prototype["private"] = false;

        /**
         * Creates a new AddHoldInvoiceRequest instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @static
         * @param {invoicesrpc.AddHoldInvoiceRequest.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.AddHoldInvoiceRequest} AddHoldInvoiceRequest instance
         * @type {{
         *   (properties: invoicesrpc.AddHoldInvoiceRequest.$Shape): invoicesrpc.AddHoldInvoiceRequest & invoicesrpc.AddHoldInvoiceRequest.$Shape;
         *   (properties?: invoicesrpc.AddHoldInvoiceRequest.$Properties): invoicesrpc.AddHoldInvoiceRequest;
         * }}
         */
        AddHoldInvoiceRequest.create = function(properties) {
            return new AddHoldInvoiceRequest(properties);
        };

        /**
         * Encodes the specified AddHoldInvoiceRequest message. Does not implicitly {@link invoicesrpc.AddHoldInvoiceRequest.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @static
         * @param {invoicesrpc.AddHoldInvoiceRequest.$Properties} message AddHoldInvoiceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddHoldInvoiceRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.memo);
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.hash);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.value);
            if (message.description_hash != null && $Object.hasOwnProperty.call(message, "description_hash") && message.description_hash.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.description_hash);
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry") && (typeof message.expiry === "object" ? message.expiry.low || message.expiry.high : message.expiry !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.expiry);
            if (message.fallback_addr != null && $Object.hasOwnProperty.call(message, "fallback_addr") && message.fallback_addr !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.fallback_addr);
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry") && (typeof message.cltv_expiry === "object" ? message.cltv_expiry.low || message.cltv_expiry.high : message.cltv_expiry !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.cltv_expiry);
            if (message.route_hints != null && message.route_hints.length)
                for (let i = 0; i < message.route_hints.length; ++i)
                    $root.lnrpc.RouteHint.encode(message.route_hints[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private") && message["private"] !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message["private"]);
            if (message.value_msat != null && $Object.hasOwnProperty.call(message, "value_msat") && (typeof message.value_msat === "object" ? message.value_msat.low || message.value_msat.high : message.value_msat !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.value_msat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddHoldInvoiceRequest message, length delimited. Does not implicitly {@link invoicesrpc.AddHoldInvoiceRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @static
         * @param {invoicesrpc.AddHoldInvoiceRequest.$Properties} message AddHoldInvoiceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddHoldInvoiceRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddHoldInvoiceRequest message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.AddHoldInvoiceRequest & invoicesrpc.AddHoldInvoiceRequest.$Shape} AddHoldInvoiceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddHoldInvoiceRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.AddHoldInvoiceRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value_msat = value;
                        else
                            delete message.value_msat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.description_hash = value;
                        else
                            delete message.description_hash;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiry = value;
                        else
                            delete message.expiry;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.fallback_addr = value;
                        else
                            delete message.fallback_addr;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.cltv_expiry = value;
                        else
                            delete message.cltv_expiry;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.route_hints && message.route_hints.length))
                            message.route_hints = [];
                        message.route_hints.push($root.lnrpc.RouteHint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message["private"] = value;
                        else
                            delete message["private"];
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddHoldInvoiceRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.AddHoldInvoiceRequest & invoicesrpc.AddHoldInvoiceRequest.$Shape} AddHoldInvoiceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddHoldInvoiceRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddHoldInvoiceRequest message.
         * @function verify
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddHoldInvoiceRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                    return "hash: buffer expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                    return "value: integer|Long expected";
            if (message.value_msat != null && $Object.hasOwnProperty.call(message, "value_msat"))
                if (!$util.isInteger(message.value_msat) && !(message.value_msat && $util.isInteger(message.value_msat.low) && $util.isInteger(message.value_msat.high)))
                    return "value_msat: integer|Long expected";
            if (message.description_hash != null && $Object.hasOwnProperty.call(message, "description_hash"))
                if (!(message.description_hash && typeof message.description_hash.length === "number" || $util.isString(message.description_hash)))
                    return "description_hash: buffer expected";
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                if (!$util.isInteger(message.expiry) && !(message.expiry && $util.isInteger(message.expiry.low) && $util.isInteger(message.expiry.high)))
                    return "expiry: integer|Long expected";
            if (message.fallback_addr != null && $Object.hasOwnProperty.call(message, "fallback_addr"))
                if (!$util.isString(message.fallback_addr))
                    return "fallback_addr: string expected";
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry"))
                if (!$util.isInteger(message.cltv_expiry) && !(message.cltv_expiry && $util.isInteger(message.cltv_expiry.low) && $util.isInteger(message.cltv_expiry.high)))
                    return "cltv_expiry: integer|Long expected";
            if (message.route_hints != null && $Object.hasOwnProperty.call(message, "route_hints")) {
                if (!$Array.isArray(message.route_hints))
                    return "route_hints: array expected";
                for (let i = 0; i < message.route_hints.length; ++i) {
                    let error = $root.lnrpc.RouteHint.verify(message.route_hints[i], _depth + 1);
                    if (error)
                        return "route_hints." + error;
                }
            }
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                if (typeof message["private"] !== "boolean")
                    return "private: boolean expected";
            return null;
        };

        /**
         * Creates an AddHoldInvoiceRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.AddHoldInvoiceRequest} AddHoldInvoiceRequest
         */
        AddHoldInvoiceRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.AddHoldInvoiceRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.AddHoldInvoiceRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.invoicesrpc.AddHoldInvoiceRequest();
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.hash != null)
                if (object.hash.length)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
            if (object.value != null)
                if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                    if ($util.Long)
                        message.value = $util.Long.fromValue(object.value, false);
                    else if (typeof object.value === "string")
                        message.value = $parseInt(object.value, 10);
                    else if (typeof object.value === "number")
                        message.value = object.value;
                    else if (typeof object.value === "object")
                        message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber();
            if (object.value_msat != null)
                if (typeof object.value_msat === "object" ? object.value_msat.low || object.value_msat.high : $Number(object.value_msat) !== 0)
                    if ($util.Long)
                        message.value_msat = $util.Long.fromValue(object.value_msat, false);
                    else if (typeof object.value_msat === "string")
                        message.value_msat = $parseInt(object.value_msat, 10);
                    else if (typeof object.value_msat === "number")
                        message.value_msat = object.value_msat;
                    else if (typeof object.value_msat === "object")
                        message.value_msat = new $util.LongBits(object.value_msat.low >>> 0, object.value_msat.high >>> 0).toNumber();
            if (object.description_hash != null)
                if (object.description_hash.length)
                    if (typeof object.description_hash === "string")
                        $util.base64.decode(object.description_hash, message.description_hash = $util.newBuffer($util.base64.length(object.description_hash)), 0);
                    else if (object.description_hash.length >= 0)
                        message.description_hash = object.description_hash;
            if (object.expiry != null)
                if (typeof object.expiry === "object" ? object.expiry.low || object.expiry.high : $Number(object.expiry) !== 0)
                    if ($util.Long)
                        message.expiry = $util.Long.fromValue(object.expiry, false);
                    else if (typeof object.expiry === "string")
                        message.expiry = $parseInt(object.expiry, 10);
                    else if (typeof object.expiry === "number")
                        message.expiry = object.expiry;
                    else if (typeof object.expiry === "object")
                        message.expiry = new $util.LongBits(object.expiry.low >>> 0, object.expiry.high >>> 0).toNumber();
            if (object.fallback_addr != null)
                if (typeof object.fallback_addr !== "string" || object.fallback_addr.length)
                    message.fallback_addr = $String(object.fallback_addr);
            if (object.cltv_expiry != null)
                if (typeof object.cltv_expiry === "object" ? object.cltv_expiry.low || object.cltv_expiry.high : $Number(object.cltv_expiry) !== 0)
                    if ($util.Long)
                        message.cltv_expiry = $util.Long.fromValue(object.cltv_expiry, true);
                    else if (typeof object.cltv_expiry === "string")
                        message.cltv_expiry = $parseInt(object.cltv_expiry, 10);
                    else if (typeof object.cltv_expiry === "number")
                        message.cltv_expiry = object.cltv_expiry;
                    else if (typeof object.cltv_expiry === "object")
                        message.cltv_expiry = new $util.LongBits(object.cltv_expiry.low >>> 0, object.cltv_expiry.high >>> 0).toNumber(true);
            if (object.route_hints) {
                if (!$Array.isArray(object.route_hints))
                    throw $TypeError(".invoicesrpc.AddHoldInvoiceRequest.route_hints: array expected");
                message.route_hints = $Array(object.route_hints.length);
                for (let i = 0; i < object.route_hints.length; ++i) {
                    if (!$util.isObject(object.route_hints[i]))
                        throw $TypeError(".invoicesrpc.AddHoldInvoiceRequest.route_hints: object expected");
                    message.route_hints[i] = $root.lnrpc.RouteHint.fromObject(object.route_hints[i], _depth + 1);
                }
            }
            if (object["private"] != null)
                if (object["private"])
                    message["private"] = $Boolean(object["private"]);
            return message;
        };

        /**
         * Creates a plain object from an AddHoldInvoiceRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @static
         * @param {invoicesrpc.AddHoldInvoiceRequest} message AddHoldInvoiceRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddHoldInvoiceRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.route_hints = [];
            if (options.defaults) {
                object.memo = "";
                if (options.bytes === $String)
                    object.hash = "";
                else {
                    object.hash = [];
                    if (options.bytes !== $Array)
                        object.hash = $util.newBuffer(object.hash);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.description_hash = "";
                else {
                    object.description_hash = [];
                    if (options.bytes !== $Array)
                        object.description_hash = $util.newBuffer(object.description_hash);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiry = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiry = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.fallback_addr = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.cltv_expiry = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.cltv_expiry = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object["private"] = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = options.bytes === $String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash) : message.hash;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, false).toBigInt();
                else if (typeof message.value === "number")
                    object.value = options.longs === $String ? $String(message.value) : message.value;
                else
                    object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber() : message.value;
            if (message.description_hash != null && $Object.hasOwnProperty.call(message, "description_hash"))
                object.description_hash = options.bytes === $String ? $util.base64.encode(message.description_hash, 0, message.description_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.description_hash) : message.description_hash;
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiry = typeof message.expiry === "number" ? $BigInt(message.expiry) : $util.Long.fromBits(message.expiry.low >>> 0, message.expiry.high >>> 0, false).toBigInt();
                else if (typeof message.expiry === "number")
                    object.expiry = options.longs === $String ? $String(message.expiry) : message.expiry;
                else
                    object.expiry = options.longs === $String ? $util.Long.prototype.toString.call(message.expiry) : options.longs === $Number ? new $util.LongBits(message.expiry.low >>> 0, message.expiry.high >>> 0).toNumber() : message.expiry;
            if (message.fallback_addr != null && $Object.hasOwnProperty.call(message, "fallback_addr"))
                object.fallback_addr = message.fallback_addr;
            if (message.cltv_expiry != null && $Object.hasOwnProperty.call(message, "cltv_expiry"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.cltv_expiry = typeof message.cltv_expiry === "number" ? $BigInt(message.cltv_expiry) : $util.Long.fromBits(message.cltv_expiry.low >>> 0, message.cltv_expiry.high >>> 0, true).toBigInt();
                else if (typeof message.cltv_expiry === "number")
                    object.cltv_expiry = options.longs === $String ? $String(message.cltv_expiry) : message.cltv_expiry;
                else
                    object.cltv_expiry = options.longs === $String ? $util.Long.prototype.toString.call(message.cltv_expiry) : options.longs === $Number ? new $util.LongBits(message.cltv_expiry.low >>> 0, message.cltv_expiry.high >>> 0).toNumber(true) : message.cltv_expiry;
            if (message.route_hints && message.route_hints.length) {
                object.route_hints = $Array(message.route_hints.length);
                for (let j = 0; j < message.route_hints.length; ++j)
                    object.route_hints[j] = $root.lnrpc.RouteHint.toObject(message.route_hints[j], options, _depth + 1);
            }
            if (message["private"] != null && $Object.hasOwnProperty.call(message, "private"))
                object["private"] = message["private"];
            if (message.value_msat != null && $Object.hasOwnProperty.call(message, "value_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value_msat = typeof message.value_msat === "number" ? $BigInt(message.value_msat) : $util.Long.fromBits(message.value_msat.low >>> 0, message.value_msat.high >>> 0, false).toBigInt();
                else if (typeof message.value_msat === "number")
                    object.value_msat = options.longs === $String ? $String(message.value_msat) : message.value_msat;
                else
                    object.value_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.value_msat) : options.longs === $Number ? new $util.LongBits(message.value_msat.low >>> 0, message.value_msat.high >>> 0).toNumber() : message.value_msat;
            return object;
        };

        /**
         * Converts this AddHoldInvoiceRequest to JSON.
         * @function toJSON
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddHoldInvoiceRequest.prototype.toJSON = function() {
            return AddHoldInvoiceRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddHoldInvoiceRequest
         * @function getTypeUrl
         * @memberof invoicesrpc.AddHoldInvoiceRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddHoldInvoiceRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.AddHoldInvoiceRequest";
        };

        return AddHoldInvoiceRequest;
    })();

    invoicesrpc.AddHoldInvoiceResp = (function() {

        /**
         * Properties of an AddHoldInvoiceResp.
         * @typedef {Object} invoicesrpc.AddHoldInvoiceResp.$Properties
         * @property {string|null} [payment_request] AddHoldInvoiceResp payment_request
         * @property {Long|null} [add_index] AddHoldInvoiceResp add_index
         * @property {Uint8Array|null} [payment_addr] AddHoldInvoiceResp payment_addr
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddHoldInvoiceResp.
         * @memberof invoicesrpc
         * @interface IAddHoldInvoiceResp
         * @augments invoicesrpc.AddHoldInvoiceResp.$Properties
         * @deprecated Use invoicesrpc.AddHoldInvoiceResp.$Properties instead.
         */

        /**
         * Shape of an AddHoldInvoiceResp.
         * @typedef {invoicesrpc.AddHoldInvoiceResp.$Properties} invoicesrpc.AddHoldInvoiceResp.$Shape
         */

        /**
         * Constructs a new AddHoldInvoiceResp.
         * @memberof invoicesrpc
         * @classdesc Represents an AddHoldInvoiceResp.
         * @constructor
         * @param {invoicesrpc.AddHoldInvoiceResp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddHoldInvoiceResp = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddHoldInvoiceResp payment_request.
         * @member {string} payment_request
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @instance
         */
        AddHoldInvoiceResp.prototype.payment_request = "";

        /**
         * AddHoldInvoiceResp add_index.
         * @member {Long} add_index
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @instance
         */
        AddHoldInvoiceResp.prototype.add_index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * AddHoldInvoiceResp payment_addr.
         * @member {Uint8Array} payment_addr
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @instance
         */
        AddHoldInvoiceResp.prototype.payment_addr = $util.newBuffer([]);

        /**
         * Creates a new AddHoldInvoiceResp instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @static
         * @param {invoicesrpc.AddHoldInvoiceResp.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.AddHoldInvoiceResp} AddHoldInvoiceResp instance
         * @type {{
         *   (properties: invoicesrpc.AddHoldInvoiceResp.$Shape): invoicesrpc.AddHoldInvoiceResp & invoicesrpc.AddHoldInvoiceResp.$Shape;
         *   (properties?: invoicesrpc.AddHoldInvoiceResp.$Properties): invoicesrpc.AddHoldInvoiceResp;
         * }}
         */
        AddHoldInvoiceResp.create = function(properties) {
            return new AddHoldInvoiceResp(properties);
        };

        /**
         * Encodes the specified AddHoldInvoiceResp message. Does not implicitly {@link invoicesrpc.AddHoldInvoiceResp.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @static
         * @param {invoicesrpc.AddHoldInvoiceResp.$Properties} message AddHoldInvoiceResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddHoldInvoiceResp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request") && message.payment_request !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.payment_request);
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index") && (typeof message.add_index === "object" ? message.add_index.low || message.add_index.high : message.add_index !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.add_index);
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr") && message.payment_addr.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.payment_addr);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddHoldInvoiceResp message, length delimited. Does not implicitly {@link invoicesrpc.AddHoldInvoiceResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @static
         * @param {invoicesrpc.AddHoldInvoiceResp.$Properties} message AddHoldInvoiceResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddHoldInvoiceResp.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddHoldInvoiceResp message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.AddHoldInvoiceResp & invoicesrpc.AddHoldInvoiceResp.$Shape} AddHoldInvoiceResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddHoldInvoiceResp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.AddHoldInvoiceResp(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_request = value;
                        else
                            delete message.payment_request;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.add_index = value;
                        else
                            delete message.add_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_addr = value;
                        else
                            delete message.payment_addr;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddHoldInvoiceResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.AddHoldInvoiceResp & invoicesrpc.AddHoldInvoiceResp.$Shape} AddHoldInvoiceResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddHoldInvoiceResp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddHoldInvoiceResp message.
         * @function verify
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddHoldInvoiceResp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                if (!$util.isString(message.payment_request))
                    return "payment_request: string expected";
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index"))
                if (!$util.isInteger(message.add_index) && !(message.add_index && $util.isInteger(message.add_index.low) && $util.isInteger(message.add_index.high)))
                    return "add_index: integer|Long expected";
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                if (!(message.payment_addr && typeof message.payment_addr.length === "number" || $util.isString(message.payment_addr)))
                    return "payment_addr: buffer expected";
            return null;
        };

        /**
         * Creates an AddHoldInvoiceResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.AddHoldInvoiceResp} AddHoldInvoiceResp
         */
        AddHoldInvoiceResp.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.AddHoldInvoiceResp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.AddHoldInvoiceResp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.invoicesrpc.AddHoldInvoiceResp();
            if (object.payment_request != null)
                if (typeof object.payment_request !== "string" || object.payment_request.length)
                    message.payment_request = $String(object.payment_request);
            if (object.add_index != null)
                if (typeof object.add_index === "object" ? object.add_index.low || object.add_index.high : $Number(object.add_index) !== 0)
                    if ($util.Long)
                        message.add_index = $util.Long.fromValue(object.add_index, true);
                    else if (typeof object.add_index === "string")
                        message.add_index = $parseInt(object.add_index, 10);
                    else if (typeof object.add_index === "number")
                        message.add_index = object.add_index;
                    else if (typeof object.add_index === "object")
                        message.add_index = new $util.LongBits(object.add_index.low >>> 0, object.add_index.high >>> 0).toNumber(true);
            if (object.payment_addr != null)
                if (object.payment_addr.length)
                    if (typeof object.payment_addr === "string")
                        $util.base64.decode(object.payment_addr, message.payment_addr = $util.newBuffer($util.base64.length(object.payment_addr)), 0);
                    else if (object.payment_addr.length >= 0)
                        message.payment_addr = object.payment_addr;
            return message;
        };

        /**
         * Creates a plain object from an AddHoldInvoiceResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @static
         * @param {invoicesrpc.AddHoldInvoiceResp} message AddHoldInvoiceResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddHoldInvoiceResp.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.payment_request = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.add_index = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.add_index = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.payment_addr = "";
                else {
                    object.payment_addr = [];
                    if (options.bytes !== $Array)
                        object.payment_addr = $util.newBuffer(object.payment_addr);
                }
            }
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                object.payment_request = message.payment_request;
            if (message.add_index != null && $Object.hasOwnProperty.call(message, "add_index"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.add_index = typeof message.add_index === "number" ? $BigInt(message.add_index) : $util.Long.fromBits(message.add_index.low >>> 0, message.add_index.high >>> 0, true).toBigInt();
                else if (typeof message.add_index === "number")
                    object.add_index = options.longs === $String ? $String(message.add_index) : message.add_index;
                else
                    object.add_index = options.longs === $String ? $util.Long.prototype.toString.call(message.add_index) : options.longs === $Number ? new $util.LongBits(message.add_index.low >>> 0, message.add_index.high >>> 0).toNumber(true) : message.add_index;
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                object.payment_addr = options.bytes === $String ? $util.base64.encode(message.payment_addr, 0, message.payment_addr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_addr) : message.payment_addr;
            return object;
        };

        /**
         * Converts this AddHoldInvoiceResp to JSON.
         * @function toJSON
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddHoldInvoiceResp.prototype.toJSON = function() {
            return AddHoldInvoiceResp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddHoldInvoiceResp
         * @function getTypeUrl
         * @memberof invoicesrpc.AddHoldInvoiceResp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddHoldInvoiceResp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.AddHoldInvoiceResp";
        };

        return AddHoldInvoiceResp;
    })();

    invoicesrpc.SettleInvoiceMsg = (function() {

        /**
         * Properties of a SettleInvoiceMsg.
         * @typedef {Object} invoicesrpc.SettleInvoiceMsg.$Properties
         * @property {Uint8Array|null} [preimage] SettleInvoiceMsg preimage
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SettleInvoiceMsg.
         * @memberof invoicesrpc
         * @interface ISettleInvoiceMsg
         * @augments invoicesrpc.SettleInvoiceMsg.$Properties
         * @deprecated Use invoicesrpc.SettleInvoiceMsg.$Properties instead.
         */

        /**
         * Shape of a SettleInvoiceMsg.
         * @typedef {invoicesrpc.SettleInvoiceMsg.$Properties} invoicesrpc.SettleInvoiceMsg.$Shape
         */

        /**
         * Constructs a new SettleInvoiceMsg.
         * @memberof invoicesrpc
         * @classdesc Represents a SettleInvoiceMsg.
         * @constructor
         * @param {invoicesrpc.SettleInvoiceMsg.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SettleInvoiceMsg = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SettleInvoiceMsg preimage.
         * @member {Uint8Array} preimage
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @instance
         */
        SettleInvoiceMsg.prototype.preimage = $util.newBuffer([]);

        /**
         * Creates a new SettleInvoiceMsg instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @static
         * @param {invoicesrpc.SettleInvoiceMsg.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.SettleInvoiceMsg} SettleInvoiceMsg instance
         * @type {{
         *   (properties: invoicesrpc.SettleInvoiceMsg.$Shape): invoicesrpc.SettleInvoiceMsg & invoicesrpc.SettleInvoiceMsg.$Shape;
         *   (properties?: invoicesrpc.SettleInvoiceMsg.$Properties): invoicesrpc.SettleInvoiceMsg;
         * }}
         */
        SettleInvoiceMsg.create = function(properties) {
            return new SettleInvoiceMsg(properties);
        };

        /**
         * Encodes the specified SettleInvoiceMsg message. Does not implicitly {@link invoicesrpc.SettleInvoiceMsg.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @static
         * @param {invoicesrpc.SettleInvoiceMsg.$Properties} message SettleInvoiceMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SettleInvoiceMsg.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage") && message.preimage.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.preimage);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SettleInvoiceMsg message, length delimited. Does not implicitly {@link invoicesrpc.SettleInvoiceMsg.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @static
         * @param {invoicesrpc.SettleInvoiceMsg.$Properties} message SettleInvoiceMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SettleInvoiceMsg.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SettleInvoiceMsg message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.SettleInvoiceMsg & invoicesrpc.SettleInvoiceMsg.$Shape} SettleInvoiceMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SettleInvoiceMsg.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.SettleInvoiceMsg(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.preimage = value;
                        else
                            delete message.preimage;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SettleInvoiceMsg message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.SettleInvoiceMsg & invoicesrpc.SettleInvoiceMsg.$Shape} SettleInvoiceMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SettleInvoiceMsg.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SettleInvoiceMsg message.
         * @function verify
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SettleInvoiceMsg.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                if (!(message.preimage && typeof message.preimage.length === "number" || $util.isString(message.preimage)))
                    return "preimage: buffer expected";
            return null;
        };

        /**
         * Creates a SettleInvoiceMsg message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.SettleInvoiceMsg} SettleInvoiceMsg
         */
        SettleInvoiceMsg.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.SettleInvoiceMsg)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.SettleInvoiceMsg: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.invoicesrpc.SettleInvoiceMsg();
            if (object.preimage != null)
                if (object.preimage.length)
                    if (typeof object.preimage === "string")
                        $util.base64.decode(object.preimage, message.preimage = $util.newBuffer($util.base64.length(object.preimage)), 0);
                    else if (object.preimage.length >= 0)
                        message.preimage = object.preimage;
            return message;
        };

        /**
         * Creates a plain object from a SettleInvoiceMsg message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @static
         * @param {invoicesrpc.SettleInvoiceMsg} message SettleInvoiceMsg
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SettleInvoiceMsg.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.preimage = "";
                else {
                    object.preimage = [];
                    if (options.bytes !== $Array)
                        object.preimage = $util.newBuffer(object.preimage);
                }
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                object.preimage = options.bytes === $String ? $util.base64.encode(message.preimage, 0, message.preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.preimage) : message.preimage;
            return object;
        };

        /**
         * Converts this SettleInvoiceMsg to JSON.
         * @function toJSON
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SettleInvoiceMsg.prototype.toJSON = function() {
            return SettleInvoiceMsg.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SettleInvoiceMsg
         * @function getTypeUrl
         * @memberof invoicesrpc.SettleInvoiceMsg
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SettleInvoiceMsg.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.SettleInvoiceMsg";
        };

        return SettleInvoiceMsg;
    })();

    invoicesrpc.SettleInvoiceResp = (function() {

        /**
         * Properties of a SettleInvoiceResp.
         * @typedef {Object} invoicesrpc.SettleInvoiceResp.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SettleInvoiceResp.
         * @memberof invoicesrpc
         * @interface ISettleInvoiceResp
         * @augments invoicesrpc.SettleInvoiceResp.$Properties
         * @deprecated Use invoicesrpc.SettleInvoiceResp.$Properties instead.
         */

        /**
         * Shape of a SettleInvoiceResp.
         * @typedef {invoicesrpc.SettleInvoiceResp.$Properties} invoicesrpc.SettleInvoiceResp.$Shape
         */

        /**
         * Constructs a new SettleInvoiceResp.
         * @memberof invoicesrpc
         * @classdesc Represents a SettleInvoiceResp.
         * @constructor
         * @param {invoicesrpc.SettleInvoiceResp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SettleInvoiceResp = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new SettleInvoiceResp instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.SettleInvoiceResp
         * @static
         * @param {invoicesrpc.SettleInvoiceResp.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.SettleInvoiceResp} SettleInvoiceResp instance
         * @type {{
         *   (properties: invoicesrpc.SettleInvoiceResp.$Shape): invoicesrpc.SettleInvoiceResp & invoicesrpc.SettleInvoiceResp.$Shape;
         *   (properties?: invoicesrpc.SettleInvoiceResp.$Properties): invoicesrpc.SettleInvoiceResp;
         * }}
         */
        SettleInvoiceResp.create = function(properties) {
            return new SettleInvoiceResp(properties);
        };

        /**
         * Encodes the specified SettleInvoiceResp message. Does not implicitly {@link invoicesrpc.SettleInvoiceResp.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.SettleInvoiceResp
         * @static
         * @param {invoicesrpc.SettleInvoiceResp.$Properties} message SettleInvoiceResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SettleInvoiceResp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SettleInvoiceResp message, length delimited. Does not implicitly {@link invoicesrpc.SettleInvoiceResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.SettleInvoiceResp
         * @static
         * @param {invoicesrpc.SettleInvoiceResp.$Properties} message SettleInvoiceResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SettleInvoiceResp.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SettleInvoiceResp message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.SettleInvoiceResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.SettleInvoiceResp & invoicesrpc.SettleInvoiceResp.$Shape} SettleInvoiceResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SettleInvoiceResp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.SettleInvoiceResp();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SettleInvoiceResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.SettleInvoiceResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.SettleInvoiceResp & invoicesrpc.SettleInvoiceResp.$Shape} SettleInvoiceResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SettleInvoiceResp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SettleInvoiceResp message.
         * @function verify
         * @memberof invoicesrpc.SettleInvoiceResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SettleInvoiceResp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a SettleInvoiceResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.SettleInvoiceResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.SettleInvoiceResp} SettleInvoiceResp
         */
        SettleInvoiceResp.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.SettleInvoiceResp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.SettleInvoiceResp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.invoicesrpc.SettleInvoiceResp();
        };

        /**
         * Creates a plain object from a SettleInvoiceResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.SettleInvoiceResp
         * @static
         * @param {invoicesrpc.SettleInvoiceResp} message SettleInvoiceResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SettleInvoiceResp.toObject = function () {
            return {};
        };

        /**
         * Converts this SettleInvoiceResp to JSON.
         * @function toJSON
         * @memberof invoicesrpc.SettleInvoiceResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SettleInvoiceResp.prototype.toJSON = function() {
            return SettleInvoiceResp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SettleInvoiceResp
         * @function getTypeUrl
         * @memberof invoicesrpc.SettleInvoiceResp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SettleInvoiceResp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.SettleInvoiceResp";
        };

        return SettleInvoiceResp;
    })();

    invoicesrpc.SubscribeSingleInvoiceRequest = (function() {

        /**
         * Properties of a SubscribeSingleInvoiceRequest.
         * @typedef {Object} invoicesrpc.SubscribeSingleInvoiceRequest.$Properties
         * @property {Uint8Array|null} [r_hash] SubscribeSingleInvoiceRequest r_hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SubscribeSingleInvoiceRequest.
         * @memberof invoicesrpc
         * @interface ISubscribeSingleInvoiceRequest
         * @augments invoicesrpc.SubscribeSingleInvoiceRequest.$Properties
         * @deprecated Use invoicesrpc.SubscribeSingleInvoiceRequest.$Properties instead.
         */

        /**
         * Shape of a SubscribeSingleInvoiceRequest.
         * @typedef {invoicesrpc.SubscribeSingleInvoiceRequest.$Properties} invoicesrpc.SubscribeSingleInvoiceRequest.$Shape
         */

        /**
         * Constructs a new SubscribeSingleInvoiceRequest.
         * @memberof invoicesrpc
         * @classdesc Represents a SubscribeSingleInvoiceRequest.
         * @constructor
         * @param {invoicesrpc.SubscribeSingleInvoiceRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SubscribeSingleInvoiceRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SubscribeSingleInvoiceRequest r_hash.
         * @member {Uint8Array} r_hash
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @instance
         */
        SubscribeSingleInvoiceRequest.prototype.r_hash = $util.newBuffer([]);

        /**
         * Creates a new SubscribeSingleInvoiceRequest instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @static
         * @param {invoicesrpc.SubscribeSingleInvoiceRequest.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.SubscribeSingleInvoiceRequest} SubscribeSingleInvoiceRequest instance
         * @type {{
         *   (properties: invoicesrpc.SubscribeSingleInvoiceRequest.$Shape): invoicesrpc.SubscribeSingleInvoiceRequest & invoicesrpc.SubscribeSingleInvoiceRequest.$Shape;
         *   (properties?: invoicesrpc.SubscribeSingleInvoiceRequest.$Properties): invoicesrpc.SubscribeSingleInvoiceRequest;
         * }}
         */
        SubscribeSingleInvoiceRequest.create = function(properties) {
            return new SubscribeSingleInvoiceRequest(properties);
        };

        /**
         * Encodes the specified SubscribeSingleInvoiceRequest message. Does not implicitly {@link invoicesrpc.SubscribeSingleInvoiceRequest.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @static
         * @param {invoicesrpc.SubscribeSingleInvoiceRequest.$Properties} message SubscribeSingleInvoiceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeSingleInvoiceRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash") && message.r_hash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.r_hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SubscribeSingleInvoiceRequest message, length delimited. Does not implicitly {@link invoicesrpc.SubscribeSingleInvoiceRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @static
         * @param {invoicesrpc.SubscribeSingleInvoiceRequest.$Properties} message SubscribeSingleInvoiceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeSingleInvoiceRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SubscribeSingleInvoiceRequest message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.SubscribeSingleInvoiceRequest & invoicesrpc.SubscribeSingleInvoiceRequest.$Shape} SubscribeSingleInvoiceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeSingleInvoiceRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.SubscribeSingleInvoiceRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.r_hash = value;
                        else
                            delete message.r_hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SubscribeSingleInvoiceRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.SubscribeSingleInvoiceRequest & invoicesrpc.SubscribeSingleInvoiceRequest.$Shape} SubscribeSingleInvoiceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeSingleInvoiceRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SubscribeSingleInvoiceRequest message.
         * @function verify
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SubscribeSingleInvoiceRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash"))
                if (!(message.r_hash && typeof message.r_hash.length === "number" || $util.isString(message.r_hash)))
                    return "r_hash: buffer expected";
            return null;
        };

        /**
         * Creates a SubscribeSingleInvoiceRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.SubscribeSingleInvoiceRequest} SubscribeSingleInvoiceRequest
         */
        SubscribeSingleInvoiceRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.SubscribeSingleInvoiceRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.SubscribeSingleInvoiceRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.invoicesrpc.SubscribeSingleInvoiceRequest();
            if (object.r_hash != null)
                if (object.r_hash.length)
                    if (typeof object.r_hash === "string")
                        $util.base64.decode(object.r_hash, message.r_hash = $util.newBuffer($util.base64.length(object.r_hash)), 0);
                    else if (object.r_hash.length >= 0)
                        message.r_hash = object.r_hash;
            return message;
        };

        /**
         * Creates a plain object from a SubscribeSingleInvoiceRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @static
         * @param {invoicesrpc.SubscribeSingleInvoiceRequest} message SubscribeSingleInvoiceRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SubscribeSingleInvoiceRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.r_hash = "";
                else {
                    object.r_hash = [];
                    if (options.bytes !== $Array)
                        object.r_hash = $util.newBuffer(object.r_hash);
                }
            if (message.r_hash != null && $Object.hasOwnProperty.call(message, "r_hash"))
                object.r_hash = options.bytes === $String ? $util.base64.encode(message.r_hash, 0, message.r_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.r_hash) : message.r_hash;
            return object;
        };

        /**
         * Converts this SubscribeSingleInvoiceRequest to JSON.
         * @function toJSON
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SubscribeSingleInvoiceRequest.prototype.toJSON = function() {
            return SubscribeSingleInvoiceRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SubscribeSingleInvoiceRequest
         * @function getTypeUrl
         * @memberof invoicesrpc.SubscribeSingleInvoiceRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SubscribeSingleInvoiceRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.SubscribeSingleInvoiceRequest";
        };

        return SubscribeSingleInvoiceRequest;
    })();

    /**
     * LookupModifier enum.
     * @name invoicesrpc.LookupModifier
     * @enum {number}
     * @property {number} DEFAULT=0 DEFAULT value
     * @property {number} HTLC_SET_ONLY=1 HTLC_SET_ONLY value
     * @property {number} HTLC_SET_BLANK=2 HTLC_SET_BLANK value
     */
    invoicesrpc.LookupModifier = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "DEFAULT"] = 0;
        values[valuesById[1] = "HTLC_SET_ONLY"] = 1;
        values[valuesById[2] = "HTLC_SET_BLANK"] = 2;
        return values;
    })();

    invoicesrpc.LookupInvoiceMsg = (function() {

        /**
         * Properties of a LookupInvoiceMsg.
         * @typedef {Object} invoicesrpc.LookupInvoiceMsg.$Properties
         * @property {Uint8Array|null} [payment_hash] LookupInvoiceMsg payment_hash
         * @property {Uint8Array|null} [payment_addr] LookupInvoiceMsg payment_addr
         * @property {Uint8Array|null} [set_id] LookupInvoiceMsg set_id
         * @property {invoicesrpc.LookupModifier|null} [lookup_modifier] LookupInvoiceMsg lookup_modifier
         * @property {"payment_hash"|"payment_addr"|"set_id"} [invoice_ref] LookupInvoiceMsg invoice_ref
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LookupInvoiceMsg.
         * @memberof invoicesrpc
         * @interface ILookupInvoiceMsg
         * @augments invoicesrpc.LookupInvoiceMsg.$Properties
         * @deprecated Use invoicesrpc.LookupInvoiceMsg.$Properties instead.
         */

        /**
         * Narrowed shape of a LookupInvoiceMsg.
         * @typedef {{
         *   payment_hash?: Uint8Array|null;
         *   payment_addr?: Uint8Array|null;
         *   set_id?: Uint8Array|null;
         *   lookup_modifier?: invoicesrpc.LookupModifier|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ invoice_ref?: undefined; payment_hash?: null; payment_addr?: null; set_id?: null }|{ invoice_ref?: "payment_hash"; payment_hash: Uint8Array; payment_addr?: null; set_id?: null }|{ invoice_ref?: "payment_addr"; payment_hash?: null; payment_addr: Uint8Array; set_id?: null }|{ invoice_ref?: "set_id"; payment_hash?: null; payment_addr?: null; set_id: Uint8Array })
         * )} invoicesrpc.LookupInvoiceMsg.$Shape
         */

        /**
         * Constructs a new LookupInvoiceMsg.
         * @memberof invoicesrpc
         * @classdesc Represents a LookupInvoiceMsg.
         * @constructor
         * @param {invoicesrpc.LookupInvoiceMsg.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LookupInvoiceMsg = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LookupInvoiceMsg payment_hash.
         * @member {Uint8Array|null|undefined} payment_hash
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @instance
         */
        LookupInvoiceMsg.prototype.payment_hash = null;

        /**
         * LookupInvoiceMsg payment_addr.
         * @member {Uint8Array|null|undefined} payment_addr
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @instance
         */
        LookupInvoiceMsg.prototype.payment_addr = null;

        /**
         * LookupInvoiceMsg set_id.
         * @member {Uint8Array|null|undefined} set_id
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @instance
         */
        LookupInvoiceMsg.prototype.set_id = null;

        /**
         * LookupInvoiceMsg lookup_modifier.
         * @member {invoicesrpc.LookupModifier} lookup_modifier
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @instance
         */
        LookupInvoiceMsg.prototype.lookup_modifier = 0;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * LookupInvoiceMsg invoice_ref.
         * @member {"payment_hash"|"payment_addr"|"set_id"|undefined} invoice_ref
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @instance
         */
        $Object.defineProperty(LookupInvoiceMsg.prototype, "invoice_ref", {
            get: $util.oneOfGetter($oneOfFields = ["payment_hash", "payment_addr", "set_id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new LookupInvoiceMsg instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @static
         * @param {invoicesrpc.LookupInvoiceMsg.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.LookupInvoiceMsg} LookupInvoiceMsg instance
         * @type {{
         *   (properties: invoicesrpc.LookupInvoiceMsg.$Shape): invoicesrpc.LookupInvoiceMsg & invoicesrpc.LookupInvoiceMsg.$Shape;
         *   (properties?: invoicesrpc.LookupInvoiceMsg.$Properties): invoicesrpc.LookupInvoiceMsg;
         * }}
         */
        LookupInvoiceMsg.create = function(properties) {
            return new LookupInvoiceMsg(properties);
        };

        /**
         * Encodes the specified LookupInvoiceMsg message. Does not implicitly {@link invoicesrpc.LookupInvoiceMsg.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @static
         * @param {invoicesrpc.LookupInvoiceMsg.$Properties} message LookupInvoiceMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LookupInvoiceMsg.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.payment_hash);
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.payment_addr);
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.set_id);
            if (message.lookup_modifier != null && $Object.hasOwnProperty.call(message, "lookup_modifier") && message.lookup_modifier !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.lookup_modifier);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LookupInvoiceMsg message, length delimited. Does not implicitly {@link invoicesrpc.LookupInvoiceMsg.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @static
         * @param {invoicesrpc.LookupInvoiceMsg.$Properties} message LookupInvoiceMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LookupInvoiceMsg.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LookupInvoiceMsg message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.LookupInvoiceMsg & invoicesrpc.LookupInvoiceMsg.$Shape} LookupInvoiceMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LookupInvoiceMsg.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.LookupInvoiceMsg(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.payment_hash = reader.bytes();
                        message.invoice_ref = "payment_hash";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.payment_addr = reader.bytes();
                        message.invoice_ref = "payment_addr";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.set_id = reader.bytes();
                        message.invoice_ref = "set_id";
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.lookup_modifier = value;
                        else
                            delete message.lookup_modifier;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LookupInvoiceMsg message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.LookupInvoiceMsg & invoicesrpc.LookupInvoiceMsg.$Shape} LookupInvoiceMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LookupInvoiceMsg.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LookupInvoiceMsg message.
         * @function verify
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LookupInvoiceMsg.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash")) {
                properties.invoice_ref = 1;
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr")) {
                if (properties.invoice_ref === 1)
                    return "invoice_ref: multiple values";
                properties.invoice_ref = 1;
                if (!(message.payment_addr && typeof message.payment_addr.length === "number" || $util.isString(message.payment_addr)))
                    return "payment_addr: buffer expected";
            }
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id")) {
                if (properties.invoice_ref === 1)
                    return "invoice_ref: multiple values";
                properties.invoice_ref = 1;
                if (!(message.set_id && typeof message.set_id.length === "number" || $util.isString(message.set_id)))
                    return "set_id: buffer expected";
            }
            if (message.lookup_modifier != null && $Object.hasOwnProperty.call(message, "lookup_modifier"))
                if (typeof message.lookup_modifier !== "number" || (message.lookup_modifier | 0) !== message.lookup_modifier)
                    return "lookup_modifier: enum value expected";
            return null;
        };

        /**
         * Creates a LookupInvoiceMsg message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.LookupInvoiceMsg} LookupInvoiceMsg
         */
        LookupInvoiceMsg.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.LookupInvoiceMsg)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.LookupInvoiceMsg: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.invoicesrpc.LookupInvoiceMsg();
            if (object.payment_hash != null)
                if (typeof object.payment_hash === "string")
                    $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                else if (object.payment_hash.length >= 0)
                    message.payment_hash = object.payment_hash;
            if (object.payment_addr != null)
                if (typeof object.payment_addr === "string")
                    $util.base64.decode(object.payment_addr, message.payment_addr = $util.newBuffer($util.base64.length(object.payment_addr)), 0);
                else if (object.payment_addr.length >= 0)
                    message.payment_addr = object.payment_addr;
            if (object.set_id != null)
                if (typeof object.set_id === "string")
                    $util.base64.decode(object.set_id, message.set_id = $util.newBuffer($util.base64.length(object.set_id)), 0);
                else if (object.set_id.length >= 0)
                    message.set_id = object.set_id;
            if (object.lookup_modifier !== 0 && (typeof object.lookup_modifier !== "string" || $root.invoicesrpc.LookupModifier[object.lookup_modifier] !== 0))
                switch (object.lookup_modifier) {
                case "DEFAULT":
                case 0:
                    message.lookup_modifier = 0;
                    break;
                case "HTLC_SET_ONLY":
                case 1:
                    message.lookup_modifier = 1;
                    break;
                case "HTLC_SET_BLANK":
                case 2:
                    message.lookup_modifier = 2;
                    break;
                default:
                    if (typeof object.lookup_modifier === "number" && (object.lookup_modifier | 0) === object.lookup_modifier)
                        message.lookup_modifier = object.lookup_modifier;
                }
            return message;
        };

        /**
         * Creates a plain object from a LookupInvoiceMsg message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @static
         * @param {invoicesrpc.LookupInvoiceMsg} message LookupInvoiceMsg
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LookupInvoiceMsg.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.lookup_modifier = options.enums === $String ? "DEFAULT" : 0;
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash")) {
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
                if (options.oneofs)
                    object.invoice_ref = "payment_hash";
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr")) {
                object.payment_addr = options.bytes === $String ? $util.base64.encode(message.payment_addr, 0, message.payment_addr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_addr) : message.payment_addr;
                if (options.oneofs)
                    object.invoice_ref = "payment_addr";
            }
            if (message.set_id != null && $Object.hasOwnProperty.call(message, "set_id")) {
                object.set_id = options.bytes === $String ? $util.base64.encode(message.set_id, 0, message.set_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.set_id) : message.set_id;
                if (options.oneofs)
                    object.invoice_ref = "set_id";
            }
            if (message.lookup_modifier != null && $Object.hasOwnProperty.call(message, "lookup_modifier"))
                object.lookup_modifier = options.enums === $String ? $root.invoicesrpc.LookupModifier[message.lookup_modifier] === $undefined ? message.lookup_modifier : $root.invoicesrpc.LookupModifier[message.lookup_modifier] : message.lookup_modifier;
            return object;
        };

        /**
         * Converts this LookupInvoiceMsg to JSON.
         * @function toJSON
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LookupInvoiceMsg.prototype.toJSON = function() {
            return LookupInvoiceMsg.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LookupInvoiceMsg
         * @function getTypeUrl
         * @memberof invoicesrpc.LookupInvoiceMsg
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LookupInvoiceMsg.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.LookupInvoiceMsg";
        };

        return LookupInvoiceMsg;
    })();

    invoicesrpc.CircuitKey = (function() {

        /**
         * Properties of a CircuitKey.
         * @typedef {Object} invoicesrpc.CircuitKey.$Properties
         * @property {Long|null} [chan_id] CircuitKey chan_id
         * @property {Long|null} [htlc_id] CircuitKey htlc_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CircuitKey.
         * @memberof invoicesrpc
         * @interface ICircuitKey
         * @augments invoicesrpc.CircuitKey.$Properties
         * @deprecated Use invoicesrpc.CircuitKey.$Properties instead.
         */

        /**
         * Shape of a CircuitKey.
         * @typedef {invoicesrpc.CircuitKey.$Properties} invoicesrpc.CircuitKey.$Shape
         */

        /**
         * Constructs a new CircuitKey.
         * @memberof invoicesrpc
         * @classdesc Represents a CircuitKey.
         * @constructor
         * @param {invoicesrpc.CircuitKey.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CircuitKey = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CircuitKey chan_id.
         * @member {Long} chan_id
         * @memberof invoicesrpc.CircuitKey
         * @instance
         */
        CircuitKey.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CircuitKey htlc_id.
         * @member {Long} htlc_id
         * @memberof invoicesrpc.CircuitKey
         * @instance
         */
        CircuitKey.prototype.htlc_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new CircuitKey instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.CircuitKey
         * @static
         * @param {invoicesrpc.CircuitKey.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.CircuitKey} CircuitKey instance
         * @type {{
         *   (properties: invoicesrpc.CircuitKey.$Shape): invoicesrpc.CircuitKey & invoicesrpc.CircuitKey.$Shape;
         *   (properties?: invoicesrpc.CircuitKey.$Properties): invoicesrpc.CircuitKey;
         * }}
         */
        CircuitKey.create = function(properties) {
            return new CircuitKey(properties);
        };

        /**
         * Encodes the specified CircuitKey message. Does not implicitly {@link invoicesrpc.CircuitKey.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.CircuitKey
         * @static
         * @param {invoicesrpc.CircuitKey.$Properties} message CircuitKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CircuitKey.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.chan_id);
            if (message.htlc_id != null && $Object.hasOwnProperty.call(message, "htlc_id") && (typeof message.htlc_id === "object" ? message.htlc_id.low || message.htlc_id.high : message.htlc_id !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.htlc_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CircuitKey message, length delimited. Does not implicitly {@link invoicesrpc.CircuitKey.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.CircuitKey
         * @static
         * @param {invoicesrpc.CircuitKey.$Properties} message CircuitKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CircuitKey.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CircuitKey message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.CircuitKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.CircuitKey & invoicesrpc.CircuitKey.$Shape} CircuitKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CircuitKey.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.CircuitKey(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_id = value;
                        else
                            delete message.htlc_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CircuitKey message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.CircuitKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.CircuitKey & invoicesrpc.CircuitKey.$Shape} CircuitKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CircuitKey.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CircuitKey message.
         * @function verify
         * @memberof invoicesrpc.CircuitKey
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CircuitKey.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.htlc_id != null && $Object.hasOwnProperty.call(message, "htlc_id"))
                if (!$util.isInteger(message.htlc_id) && !(message.htlc_id && $util.isInteger(message.htlc_id.low) && $util.isInteger(message.htlc_id.high)))
                    return "htlc_id: integer|Long expected";
            return null;
        };

        /**
         * Creates a CircuitKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.CircuitKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.CircuitKey} CircuitKey
         */
        CircuitKey.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.CircuitKey)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.CircuitKey: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.invoicesrpc.CircuitKey();
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.htlc_id != null)
                if (typeof object.htlc_id === "object" ? object.htlc_id.low || object.htlc_id.high : $Number(object.htlc_id) !== 0)
                    if ($util.Long)
                        message.htlc_id = $util.Long.fromValue(object.htlc_id, true);
                    else if (typeof object.htlc_id === "string")
                        message.htlc_id = $parseInt(object.htlc_id, 10);
                    else if (typeof object.htlc_id === "number")
                        message.htlc_id = object.htlc_id;
                    else if (typeof object.htlc_id === "object")
                        message.htlc_id = new $util.LongBits(object.htlc_id.low >>> 0, object.htlc_id.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a CircuitKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.CircuitKey
         * @static
         * @param {invoicesrpc.CircuitKey} message CircuitKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CircuitKey.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.htlc_id != null && $Object.hasOwnProperty.call(message, "htlc_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_id = typeof message.htlc_id === "number" ? $BigInt(message.htlc_id) : $util.Long.fromBits(message.htlc_id.low >>> 0, message.htlc_id.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_id === "number")
                    object.htlc_id = options.longs === $String ? $String(message.htlc_id) : message.htlc_id;
                else
                    object.htlc_id = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_id) : options.longs === $Number ? new $util.LongBits(message.htlc_id.low >>> 0, message.htlc_id.high >>> 0).toNumber(true) : message.htlc_id;
            return object;
        };

        /**
         * Converts this CircuitKey to JSON.
         * @function toJSON
         * @memberof invoicesrpc.CircuitKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CircuitKey.prototype.toJSON = function() {
            return CircuitKey.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CircuitKey
         * @function getTypeUrl
         * @memberof invoicesrpc.CircuitKey
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CircuitKey.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.CircuitKey";
        };

        return CircuitKey;
    })();

    invoicesrpc.HtlcModifyRequest = (function() {

        /**
         * Properties of a HtlcModifyRequest.
         * @typedef {Object} invoicesrpc.HtlcModifyRequest.$Properties
         * @property {lnrpc.Invoice.$Properties|null} [invoice] HtlcModifyRequest invoice
         * @property {invoicesrpc.CircuitKey.$Properties|null} [exit_htlc_circuit_key] HtlcModifyRequest exit_htlc_circuit_key
         * @property {Long|null} [exit_htlc_amt] HtlcModifyRequest exit_htlc_amt
         * @property {number|null} [exit_htlc_expiry] HtlcModifyRequest exit_htlc_expiry
         * @property {number|null} [current_height] HtlcModifyRequest current_height
         * @property {Object.<string,Uint8Array>|null} [exit_htlc_wire_custom_records] HtlcModifyRequest exit_htlc_wire_custom_records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a HtlcModifyRequest.
         * @memberof invoicesrpc
         * @interface IHtlcModifyRequest
         * @augments invoicesrpc.HtlcModifyRequest.$Properties
         * @deprecated Use invoicesrpc.HtlcModifyRequest.$Properties instead.
         */

        /**
         * Shape of a HtlcModifyRequest.
         * @typedef {invoicesrpc.HtlcModifyRequest.$Properties} invoicesrpc.HtlcModifyRequest.$Shape
         */

        /**
         * Constructs a new HtlcModifyRequest.
         * @memberof invoicesrpc
         * @classdesc Represents a HtlcModifyRequest.
         * @constructor
         * @param {invoicesrpc.HtlcModifyRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const HtlcModifyRequest = function (properties) {
            this.exit_htlc_wire_custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HtlcModifyRequest invoice.
         * @member {lnrpc.Invoice.$Properties|null|undefined} invoice
         * @memberof invoicesrpc.HtlcModifyRequest
         * @instance
         */
        HtlcModifyRequest.prototype.invoice = null;

        /**
         * HtlcModifyRequest exit_htlc_circuit_key.
         * @member {invoicesrpc.CircuitKey.$Properties|null|undefined} exit_htlc_circuit_key
         * @memberof invoicesrpc.HtlcModifyRequest
         * @instance
         */
        HtlcModifyRequest.prototype.exit_htlc_circuit_key = null;

        /**
         * HtlcModifyRequest exit_htlc_amt.
         * @member {Long} exit_htlc_amt
         * @memberof invoicesrpc.HtlcModifyRequest
         * @instance
         */
        HtlcModifyRequest.prototype.exit_htlc_amt = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HtlcModifyRequest exit_htlc_expiry.
         * @member {number} exit_htlc_expiry
         * @memberof invoicesrpc.HtlcModifyRequest
         * @instance
         */
        HtlcModifyRequest.prototype.exit_htlc_expiry = 0;

        /**
         * HtlcModifyRequest current_height.
         * @member {number} current_height
         * @memberof invoicesrpc.HtlcModifyRequest
         * @instance
         */
        HtlcModifyRequest.prototype.current_height = 0;

        /**
         * HtlcModifyRequest exit_htlc_wire_custom_records.
         * @member {Object.<string,Uint8Array>} exit_htlc_wire_custom_records
         * @memberof invoicesrpc.HtlcModifyRequest
         * @instance
         */
        HtlcModifyRequest.prototype.exit_htlc_wire_custom_records = $util.emptyObject;

        /**
         * Creates a new HtlcModifyRequest instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.HtlcModifyRequest
         * @static
         * @param {invoicesrpc.HtlcModifyRequest.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.HtlcModifyRequest} HtlcModifyRequest instance
         * @type {{
         *   (properties: invoicesrpc.HtlcModifyRequest.$Shape): invoicesrpc.HtlcModifyRequest & invoicesrpc.HtlcModifyRequest.$Shape;
         *   (properties?: invoicesrpc.HtlcModifyRequest.$Properties): invoicesrpc.HtlcModifyRequest;
         * }}
         */
        HtlcModifyRequest.create = function(properties) {
            return new HtlcModifyRequest(properties);
        };

        /**
         * Encodes the specified HtlcModifyRequest message. Does not implicitly {@link invoicesrpc.HtlcModifyRequest.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.HtlcModifyRequest
         * @static
         * @param {invoicesrpc.HtlcModifyRequest.$Properties} message HtlcModifyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HtlcModifyRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.invoice != null && $Object.hasOwnProperty.call(message, "invoice"))
                $root.lnrpc.Invoice.encode(message.invoice, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.exit_htlc_circuit_key != null && $Object.hasOwnProperty.call(message, "exit_htlc_circuit_key"))
                $root.invoicesrpc.CircuitKey.encode(message.exit_htlc_circuit_key, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.exit_htlc_amt != null && $Object.hasOwnProperty.call(message, "exit_htlc_amt") && (typeof message.exit_htlc_amt === "object" ? message.exit_htlc_amt.low || message.exit_htlc_amt.high : message.exit_htlc_amt !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.exit_htlc_amt);
            if (message.exit_htlc_expiry != null && $Object.hasOwnProperty.call(message, "exit_htlc_expiry") && message.exit_htlc_expiry !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.exit_htlc_expiry);
            if (message.current_height != null && $Object.hasOwnProperty.call(message, "current_height") && message.current_height !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.current_height);
            if (message.exit_htlc_wire_custom_records != null && $Object.hasOwnProperty.call(message, "exit_htlc_wire_custom_records"))
                for (let keys = $Object.keys(message.exit_htlc_wire_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.exit_htlc_wire_custom_records[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HtlcModifyRequest message, length delimited. Does not implicitly {@link invoicesrpc.HtlcModifyRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.HtlcModifyRequest
         * @static
         * @param {invoicesrpc.HtlcModifyRequest.$Properties} message HtlcModifyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HtlcModifyRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HtlcModifyRequest message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.HtlcModifyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.HtlcModifyRequest & invoicesrpc.HtlcModifyRequest.$Shape} HtlcModifyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HtlcModifyRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.HtlcModifyRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.invoice = $root.lnrpc.Invoice.decode(reader, reader.uint32(), $undefined, _depth + 1, message.invoice);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.exit_htlc_circuit_key = $root.invoicesrpc.CircuitKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.exit_htlc_circuit_key);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.exit_htlc_amt = value;
                        else
                            delete message.exit_htlc_amt;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.exit_htlc_expiry = value;
                        else
                            delete message.exit_htlc_expiry;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.current_height = value;
                        else
                            delete message.current_height;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (message.exit_htlc_wire_custom_records === $util.emptyObject)
                            message.exit_htlc_wire_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.exit_htlc_wire_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HtlcModifyRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.HtlcModifyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.HtlcModifyRequest & invoicesrpc.HtlcModifyRequest.$Shape} HtlcModifyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HtlcModifyRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HtlcModifyRequest message.
         * @function verify
         * @memberof invoicesrpc.HtlcModifyRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HtlcModifyRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.invoice != null && $Object.hasOwnProperty.call(message, "invoice")) {
                let error = $root.lnrpc.Invoice.verify(message.invoice, _depth + 1);
                if (error)
                    return "invoice." + error;
            }
            if (message.exit_htlc_circuit_key != null && $Object.hasOwnProperty.call(message, "exit_htlc_circuit_key")) {
                let error = $root.invoicesrpc.CircuitKey.verify(message.exit_htlc_circuit_key, _depth + 1);
                if (error)
                    return "exit_htlc_circuit_key." + error;
            }
            if (message.exit_htlc_amt != null && $Object.hasOwnProperty.call(message, "exit_htlc_amt"))
                if (!$util.isInteger(message.exit_htlc_amt) && !(message.exit_htlc_amt && $util.isInteger(message.exit_htlc_amt.low) && $util.isInteger(message.exit_htlc_amt.high)))
                    return "exit_htlc_amt: integer|Long expected";
            if (message.exit_htlc_expiry != null && $Object.hasOwnProperty.call(message, "exit_htlc_expiry"))
                if (!$util.isInteger(message.exit_htlc_expiry))
                    return "exit_htlc_expiry: integer expected";
            if (message.current_height != null && $Object.hasOwnProperty.call(message, "current_height"))
                if (!$util.isInteger(message.current_height))
                    return "current_height: integer expected";
            if (message.exit_htlc_wire_custom_records != null && $Object.hasOwnProperty.call(message, "exit_htlc_wire_custom_records")) {
                if (!$util.isObject(message.exit_htlc_wire_custom_records))
                    return "exit_htlc_wire_custom_records: object expected";
                let key = $Object.keys(message.exit_htlc_wire_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "exit_htlc_wire_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.exit_htlc_wire_custom_records[key[i]] && typeof message.exit_htlc_wire_custom_records[key[i]].length === "number" || $util.isString(message.exit_htlc_wire_custom_records[key[i]])))
                        return "exit_htlc_wire_custom_records: buffer{k:uint64} expected";
                }
            }
            return null;
        };

        /**
         * Creates a HtlcModifyRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.HtlcModifyRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.HtlcModifyRequest} HtlcModifyRequest
         */
        HtlcModifyRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.HtlcModifyRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.HtlcModifyRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.invoicesrpc.HtlcModifyRequest();
            if (object.invoice != null) {
                if (!$util.isObject(object.invoice))
                    throw $TypeError(".invoicesrpc.HtlcModifyRequest.invoice: object expected");
                message.invoice = $root.lnrpc.Invoice.fromObject(object.invoice, _depth + 1);
            }
            if (object.exit_htlc_circuit_key != null) {
                if (!$util.isObject(object.exit_htlc_circuit_key))
                    throw $TypeError(".invoicesrpc.HtlcModifyRequest.exit_htlc_circuit_key: object expected");
                message.exit_htlc_circuit_key = $root.invoicesrpc.CircuitKey.fromObject(object.exit_htlc_circuit_key, _depth + 1);
            }
            if (object.exit_htlc_amt != null)
                if (typeof object.exit_htlc_amt === "object" ? object.exit_htlc_amt.low || object.exit_htlc_amt.high : $Number(object.exit_htlc_amt) !== 0)
                    if ($util.Long)
                        message.exit_htlc_amt = $util.Long.fromValue(object.exit_htlc_amt, true);
                    else if (typeof object.exit_htlc_amt === "string")
                        message.exit_htlc_amt = $parseInt(object.exit_htlc_amt, 10);
                    else if (typeof object.exit_htlc_amt === "number")
                        message.exit_htlc_amt = object.exit_htlc_amt;
                    else if (typeof object.exit_htlc_amt === "object")
                        message.exit_htlc_amt = new $util.LongBits(object.exit_htlc_amt.low >>> 0, object.exit_htlc_amt.high >>> 0).toNumber(true);
            if (object.exit_htlc_expiry != null)
                if ($Number(object.exit_htlc_expiry) !== 0)
                    message.exit_htlc_expiry = object.exit_htlc_expiry >>> 0;
            if (object.current_height != null)
                if ($Number(object.current_height) !== 0)
                    message.current_height = object.current_height >>> 0;
            if (object.exit_htlc_wire_custom_records) {
                if (!$util.isObject(object.exit_htlc_wire_custom_records))
                    throw $TypeError(".invoicesrpc.HtlcModifyRequest.exit_htlc_wire_custom_records: object expected");
                message.exit_htlc_wire_custom_records = {};
                for (let keys = $Object.keys(object.exit_htlc_wire_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.exit_htlc_wire_custom_records, keys[i]);
                    if (typeof object.exit_htlc_wire_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.exit_htlc_wire_custom_records[keys[i]], message.exit_htlc_wire_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.exit_htlc_wire_custom_records[keys[i]])), 0);
                    else if (object.exit_htlc_wire_custom_records[keys[i]].length >= 0)
                        message.exit_htlc_wire_custom_records[keys[i]] = object.exit_htlc_wire_custom_records[keys[i]];
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a HtlcModifyRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.HtlcModifyRequest
         * @static
         * @param {invoicesrpc.HtlcModifyRequest} message HtlcModifyRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HtlcModifyRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.exit_htlc_wire_custom_records = {};
            if (options.defaults) {
                object.invoice = null;
                object.exit_htlc_circuit_key = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.exit_htlc_amt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.exit_htlc_amt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.exit_htlc_expiry = 0;
                object.current_height = 0;
            }
            if (message.invoice != null && $Object.hasOwnProperty.call(message, "invoice"))
                object.invoice = $root.lnrpc.Invoice.toObject(message.invoice, options, _depth + 1);
            if (message.exit_htlc_circuit_key != null && $Object.hasOwnProperty.call(message, "exit_htlc_circuit_key"))
                object.exit_htlc_circuit_key = $root.invoicesrpc.CircuitKey.toObject(message.exit_htlc_circuit_key, options, _depth + 1);
            if (message.exit_htlc_amt != null && $Object.hasOwnProperty.call(message, "exit_htlc_amt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.exit_htlc_amt = typeof message.exit_htlc_amt === "number" ? $BigInt(message.exit_htlc_amt) : $util.Long.fromBits(message.exit_htlc_amt.low >>> 0, message.exit_htlc_amt.high >>> 0, true).toBigInt();
                else if (typeof message.exit_htlc_amt === "number")
                    object.exit_htlc_amt = options.longs === $String ? $String(message.exit_htlc_amt) : message.exit_htlc_amt;
                else
                    object.exit_htlc_amt = options.longs === $String ? $util.Long.prototype.toString.call(message.exit_htlc_amt) : options.longs === $Number ? new $util.LongBits(message.exit_htlc_amt.low >>> 0, message.exit_htlc_amt.high >>> 0).toNumber(true) : message.exit_htlc_amt;
            if (message.exit_htlc_expiry != null && $Object.hasOwnProperty.call(message, "exit_htlc_expiry"))
                object.exit_htlc_expiry = message.exit_htlc_expiry;
            if (message.current_height != null && $Object.hasOwnProperty.call(message, "current_height"))
                object.current_height = message.current_height;
            let keys2;
            if (message.exit_htlc_wire_custom_records && (keys2 = $Object.keys(message.exit_htlc_wire_custom_records)).length) {
                object.exit_htlc_wire_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.exit_htlc_wire_custom_records, keys2[j]);
                    object.exit_htlc_wire_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.exit_htlc_wire_custom_records[keys2[j]], 0, message.exit_htlc_wire_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.exit_htlc_wire_custom_records[keys2[j]]) : message.exit_htlc_wire_custom_records[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this HtlcModifyRequest to JSON.
         * @function toJSON
         * @memberof invoicesrpc.HtlcModifyRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HtlcModifyRequest.prototype.toJSON = function() {
            return HtlcModifyRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HtlcModifyRequest
         * @function getTypeUrl
         * @memberof invoicesrpc.HtlcModifyRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HtlcModifyRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.HtlcModifyRequest";
        };

        return HtlcModifyRequest;
    })();

    invoicesrpc.HtlcModifyResponse = (function() {

        /**
         * Properties of a HtlcModifyResponse.
         * @typedef {Object} invoicesrpc.HtlcModifyResponse.$Properties
         * @property {invoicesrpc.CircuitKey.$Properties|null} [circuit_key] HtlcModifyResponse circuit_key
         * @property {Long|null} [amt_paid] HtlcModifyResponse amt_paid
         * @property {boolean|null} [cancel_set] HtlcModifyResponse cancel_set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a HtlcModifyResponse.
         * @memberof invoicesrpc
         * @interface IHtlcModifyResponse
         * @augments invoicesrpc.HtlcModifyResponse.$Properties
         * @deprecated Use invoicesrpc.HtlcModifyResponse.$Properties instead.
         */

        /**
         * Shape of a HtlcModifyResponse.
         * @typedef {invoicesrpc.HtlcModifyResponse.$Properties} invoicesrpc.HtlcModifyResponse.$Shape
         */

        /**
         * Constructs a new HtlcModifyResponse.
         * @memberof invoicesrpc
         * @classdesc Represents a HtlcModifyResponse.
         * @constructor
         * @param {invoicesrpc.HtlcModifyResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const HtlcModifyResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HtlcModifyResponse circuit_key.
         * @member {invoicesrpc.CircuitKey.$Properties|null|undefined} circuit_key
         * @memberof invoicesrpc.HtlcModifyResponse
         * @instance
         */
        HtlcModifyResponse.prototype.circuit_key = null;

        /**
         * HtlcModifyResponse amt_paid.
         * @member {Long|null|undefined} amt_paid
         * @memberof invoicesrpc.HtlcModifyResponse
         * @instance
         */
        HtlcModifyResponse.prototype.amt_paid = null;

        /**
         * HtlcModifyResponse cancel_set.
         * @member {boolean} cancel_set
         * @memberof invoicesrpc.HtlcModifyResponse
         * @instance
         */
        HtlcModifyResponse.prototype.cancel_set = false;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HtlcModifyResponse.prototype, "_amt_paid", {
            get: $util.oneOfGetter($oneOfFields = ["amt_paid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new HtlcModifyResponse instance using the specified properties.
         * @function create
         * @memberof invoicesrpc.HtlcModifyResponse
         * @static
         * @param {invoicesrpc.HtlcModifyResponse.$Properties=} [properties] Properties to set
         * @returns {invoicesrpc.HtlcModifyResponse} HtlcModifyResponse instance
         * @type {{
         *   (properties: invoicesrpc.HtlcModifyResponse.$Shape): invoicesrpc.HtlcModifyResponse & invoicesrpc.HtlcModifyResponse.$Shape;
         *   (properties?: invoicesrpc.HtlcModifyResponse.$Properties): invoicesrpc.HtlcModifyResponse;
         * }}
         */
        HtlcModifyResponse.create = function(properties) {
            return new HtlcModifyResponse(properties);
        };

        /**
         * Encodes the specified HtlcModifyResponse message. Does not implicitly {@link invoicesrpc.HtlcModifyResponse.verify|verify} messages.
         * @function encode
         * @memberof invoicesrpc.HtlcModifyResponse
         * @static
         * @param {invoicesrpc.HtlcModifyResponse.$Properties} message HtlcModifyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HtlcModifyResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.circuit_key != null && $Object.hasOwnProperty.call(message, "circuit_key"))
                $root.invoicesrpc.CircuitKey.encode(message.circuit_key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.amt_paid != null && $Object.hasOwnProperty.call(message, "amt_paid"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.amt_paid);
            if (message.cancel_set != null && $Object.hasOwnProperty.call(message, "cancel_set") && message.cancel_set !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.cancel_set);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HtlcModifyResponse message, length delimited. Does not implicitly {@link invoicesrpc.HtlcModifyResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof invoicesrpc.HtlcModifyResponse
         * @static
         * @param {invoicesrpc.HtlcModifyResponse.$Properties} message HtlcModifyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HtlcModifyResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HtlcModifyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof invoicesrpc.HtlcModifyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {invoicesrpc.HtlcModifyResponse & invoicesrpc.HtlcModifyResponse.$Shape} HtlcModifyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HtlcModifyResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.invoicesrpc.HtlcModifyResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.circuit_key = $root.invoicesrpc.CircuitKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.circuit_key);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        message.amt_paid = reader.uint64();
                        message._amt_paid = "amt_paid";
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cancel_set = value;
                        else
                            delete message.cancel_set;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HtlcModifyResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof invoicesrpc.HtlcModifyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {invoicesrpc.HtlcModifyResponse & invoicesrpc.HtlcModifyResponse.$Shape} HtlcModifyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HtlcModifyResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HtlcModifyResponse message.
         * @function verify
         * @memberof invoicesrpc.HtlcModifyResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HtlcModifyResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.circuit_key != null && $Object.hasOwnProperty.call(message, "circuit_key")) {
                let error = $root.invoicesrpc.CircuitKey.verify(message.circuit_key, _depth + 1);
                if (error)
                    return "circuit_key." + error;
            }
            if (message.amt_paid != null && $Object.hasOwnProperty.call(message, "amt_paid")) {
                properties._amt_paid = 1;
                if (!$util.isInteger(message.amt_paid) && !(message.amt_paid && $util.isInteger(message.amt_paid.low) && $util.isInteger(message.amt_paid.high)))
                    return "amt_paid: integer|Long expected";
            }
            if (message.cancel_set != null && $Object.hasOwnProperty.call(message, "cancel_set"))
                if (typeof message.cancel_set !== "boolean")
                    return "cancel_set: boolean expected";
            return null;
        };

        /**
         * Creates a HtlcModifyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof invoicesrpc.HtlcModifyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {invoicesrpc.HtlcModifyResponse} HtlcModifyResponse
         */
        HtlcModifyResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.invoicesrpc.HtlcModifyResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".invoicesrpc.HtlcModifyResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.invoicesrpc.HtlcModifyResponse();
            if (object.circuit_key != null) {
                if (!$util.isObject(object.circuit_key))
                    throw $TypeError(".invoicesrpc.HtlcModifyResponse.circuit_key: object expected");
                message.circuit_key = $root.invoicesrpc.CircuitKey.fromObject(object.circuit_key, _depth + 1);
            }
            if (object.amt_paid != null)
                if ($util.Long)
                    message.amt_paid = $util.Long.fromValue(object.amt_paid, true);
                else if (typeof object.amt_paid === "string")
                    message.amt_paid = $parseInt(object.amt_paid, 10);
                else if (typeof object.amt_paid === "number")
                    message.amt_paid = object.amt_paid;
                else if (typeof object.amt_paid === "object")
                    message.amt_paid = new $util.LongBits(object.amt_paid.low >>> 0, object.amt_paid.high >>> 0).toNumber(true);
            if (object.cancel_set != null)
                if (object.cancel_set)
                    message.cancel_set = $Boolean(object.cancel_set);
            return message;
        };

        /**
         * Creates a plain object from a HtlcModifyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof invoicesrpc.HtlcModifyResponse
         * @static
         * @param {invoicesrpc.HtlcModifyResponse} message HtlcModifyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HtlcModifyResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.circuit_key = null;
                object.cancel_set = false;
            }
            if (message.circuit_key != null && $Object.hasOwnProperty.call(message, "circuit_key"))
                object.circuit_key = $root.invoicesrpc.CircuitKey.toObject(message.circuit_key, options, _depth + 1);
            if (message.amt_paid != null && $Object.hasOwnProperty.call(message, "amt_paid"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_paid = typeof message.amt_paid === "number" ? $BigInt(message.amt_paid) : $util.Long.fromBits(message.amt_paid.low >>> 0, message.amt_paid.high >>> 0, true).toBigInt();
                else if (typeof message.amt_paid === "number")
                    object.amt_paid = options.longs === $String ? $String(message.amt_paid) : message.amt_paid;
                else
                    object.amt_paid = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_paid) : options.longs === $Number ? new $util.LongBits(message.amt_paid.low >>> 0, message.amt_paid.high >>> 0).toNumber(true) : message.amt_paid;
            if (message.cancel_set != null && $Object.hasOwnProperty.call(message, "cancel_set"))
                object.cancel_set = message.cancel_set;
            return object;
        };

        /**
         * Converts this HtlcModifyResponse to JSON.
         * @function toJSON
         * @memberof invoicesrpc.HtlcModifyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HtlcModifyResponse.prototype.toJSON = function() {
            return HtlcModifyResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HtlcModifyResponse
         * @function getTypeUrl
         * @memberof invoicesrpc.HtlcModifyResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HtlcModifyResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/invoicesrpc.HtlcModifyResponse";
        };

        return HtlcModifyResponse;
    })();

    return invoicesrpc;
})();

export const neutrinorpc = $root.neutrinorpc = (() => {

    /**
     * Namespace neutrinorpc.
     * @exports neutrinorpc
     * @namespace
     */
    const neutrinorpc = {};

    neutrinorpc.NeutrinoKit = (function() {

        /**
         * Constructs a new NeutrinoKit service.
         * @memberof neutrinorpc
         * @classdesc Represents a NeutrinoKit
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const NeutrinoKit = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (NeutrinoKit.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = NeutrinoKit;

        /**
         * Creates new NeutrinoKit service using the specified rpc implementation.
         * @function create
         * @memberof neutrinorpc.NeutrinoKit
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {NeutrinoKit} RPC service. Useful where requests and/or responses are streamed.
         */
        NeutrinoKit.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link neutrinorpc.NeutrinoKit#status}.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef StatusCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {neutrinorpc.StatusResponse} [response] StatusResponse
         */

        /**
         * Calls Status.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef Status
         * @type {{
         *   (request: neutrinorpc.IStatusRequest, callback: neutrinorpc.NeutrinoKit.StatusCallback): void;
         *   (request: neutrinorpc.IStatusRequest): Promise<neutrinorpc.StatusResponse>;
         *   readonly name: "Status";
         *   readonly path: "/neutrinorpc.NeutrinoKit/Status";
         *   readonly requestType: "StatusRequest";
         *   readonly responseType: "StatusResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls Status.
         * @name neutrinorpc.NeutrinoKit#status
         * @type {neutrinorpc.NeutrinoKit.Status}
         */
        $Object.defineProperties(NeutrinoKit.prototype.status = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NeutrinoKit.prototype.status, $root.neutrinorpc.StatusRequest, $root.neutrinorpc.StatusResponse, request, callback);
        }, {
            name: { value: "Status" },
            path: { value: "/neutrinorpc.NeutrinoKit/Status" },
            requestType: { value: "StatusRequest" },
            responseType: { value: "StatusResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link neutrinorpc.NeutrinoKit#addPeer}.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef AddPeerCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {neutrinorpc.AddPeerResponse} [response] AddPeerResponse
         */

        /**
         * Calls AddPeer.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef AddPeer
         * @type {{
         *   (request: neutrinorpc.IAddPeerRequest, callback: neutrinorpc.NeutrinoKit.AddPeerCallback): void;
         *   (request: neutrinorpc.IAddPeerRequest): Promise<neutrinorpc.AddPeerResponse>;
         *   readonly name: "AddPeer";
         *   readonly path: "/neutrinorpc.NeutrinoKit/AddPeer";
         *   readonly requestType: "AddPeerRequest";
         *   readonly responseType: "AddPeerResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls AddPeer.
         * @name neutrinorpc.NeutrinoKit#addPeer
         * @type {neutrinorpc.NeutrinoKit.AddPeer}
         */
        $Object.defineProperties(NeutrinoKit.prototype.addPeer = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NeutrinoKit.prototype.addPeer, $root.neutrinorpc.AddPeerRequest, $root.neutrinorpc.AddPeerResponse, request, callback);
        }, {
            name: { value: "AddPeer" },
            path: { value: "/neutrinorpc.NeutrinoKit/AddPeer" },
            requestType: { value: "AddPeerRequest" },
            responseType: { value: "AddPeerResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link neutrinorpc.NeutrinoKit#disconnectPeer}.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef DisconnectPeerCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {neutrinorpc.DisconnectPeerResponse} [response] DisconnectPeerResponse
         */

        /**
         * Calls DisconnectPeer.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef DisconnectPeer
         * @type {{
         *   (request: neutrinorpc.IDisconnectPeerRequest, callback: neutrinorpc.NeutrinoKit.DisconnectPeerCallback): void;
         *   (request: neutrinorpc.IDisconnectPeerRequest): Promise<neutrinorpc.DisconnectPeerResponse>;
         *   readonly name: "DisconnectPeer";
         *   readonly path: "/neutrinorpc.NeutrinoKit/DisconnectPeer";
         *   readonly requestType: "DisconnectPeerRequest";
         *   readonly responseType: "DisconnectPeerResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DisconnectPeer.
         * @name neutrinorpc.NeutrinoKit#disconnectPeer
         * @type {neutrinorpc.NeutrinoKit.DisconnectPeer}
         */
        $Object.defineProperties(NeutrinoKit.prototype.disconnectPeer = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NeutrinoKit.prototype.disconnectPeer, $root.neutrinorpc.DisconnectPeerRequest, $root.neutrinorpc.DisconnectPeerResponse, request, callback);
        }, {
            name: { value: "DisconnectPeer" },
            path: { value: "/neutrinorpc.NeutrinoKit/DisconnectPeer" },
            requestType: { value: "DisconnectPeerRequest" },
            responseType: { value: "DisconnectPeerResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link neutrinorpc.NeutrinoKit#isBanned}.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef IsBannedCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {neutrinorpc.IsBannedResponse} [response] IsBannedResponse
         */

        /**
         * Calls IsBanned.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef IsBanned
         * @type {{
         *   (request: neutrinorpc.IIsBannedRequest, callback: neutrinorpc.NeutrinoKit.IsBannedCallback): void;
         *   (request: neutrinorpc.IIsBannedRequest): Promise<neutrinorpc.IsBannedResponse>;
         *   readonly name: "IsBanned";
         *   readonly path: "/neutrinorpc.NeutrinoKit/IsBanned";
         *   readonly requestType: "IsBannedRequest";
         *   readonly responseType: "IsBannedResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls IsBanned.
         * @name neutrinorpc.NeutrinoKit#isBanned
         * @type {neutrinorpc.NeutrinoKit.IsBanned}
         */
        $Object.defineProperties(NeutrinoKit.prototype.isBanned = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NeutrinoKit.prototype.isBanned, $root.neutrinorpc.IsBannedRequest, $root.neutrinorpc.IsBannedResponse, request, callback);
        }, {
            name: { value: "IsBanned" },
            path: { value: "/neutrinorpc.NeutrinoKit/IsBanned" },
            requestType: { value: "IsBannedRequest" },
            responseType: { value: "IsBannedResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link neutrinorpc.NeutrinoKit#getBlockHeader}.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef GetBlockHeaderCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {neutrinorpc.GetBlockHeaderResponse} [response] GetBlockHeaderResponse
         */

        /**
         * Calls GetBlockHeader.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef GetBlockHeader
         * @type {{
         *   (request: neutrinorpc.IGetBlockHeaderRequest, callback: neutrinorpc.NeutrinoKit.GetBlockHeaderCallback): void;
         *   (request: neutrinorpc.IGetBlockHeaderRequest): Promise<neutrinorpc.GetBlockHeaderResponse>;
         *   readonly name: "GetBlockHeader";
         *   readonly path: "/neutrinorpc.NeutrinoKit/GetBlockHeader";
         *   readonly requestType: "GetBlockHeaderRequest";
         *   readonly responseType: "GetBlockHeaderResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetBlockHeader.
         * @name neutrinorpc.NeutrinoKit#getBlockHeader
         * @type {neutrinorpc.NeutrinoKit.GetBlockHeader}
         */
        $Object.defineProperties(NeutrinoKit.prototype.getBlockHeader = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NeutrinoKit.prototype.getBlockHeader, $root.neutrinorpc.GetBlockHeaderRequest, $root.neutrinorpc.GetBlockHeaderResponse, request, callback);
        }, {
            name: { value: "GetBlockHeader" },
            path: { value: "/neutrinorpc.NeutrinoKit/GetBlockHeader" },
            requestType: { value: "GetBlockHeaderRequest" },
            responseType: { value: "GetBlockHeaderResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link neutrinorpc.NeutrinoKit#getBlock}.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef GetBlockCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {neutrinorpc.GetBlockResponse} [response] GetBlockResponse
         */

        /**
         * Calls GetBlock.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef GetBlock
         * @type {{
         *   (request: neutrinorpc.IGetBlockRequest, callback: neutrinorpc.NeutrinoKit.GetBlockCallback): void;
         *   (request: neutrinorpc.IGetBlockRequest): Promise<neutrinorpc.GetBlockResponse>;
         *   readonly name: "GetBlock";
         *   readonly path: "/neutrinorpc.NeutrinoKit/GetBlock";
         *   readonly requestType: "GetBlockRequest";
         *   readonly responseType: "GetBlockResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetBlock.
         * @name neutrinorpc.NeutrinoKit#getBlock
         * @type {neutrinorpc.NeutrinoKit.GetBlock}
         */
        $Object.defineProperties(NeutrinoKit.prototype.getBlock = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NeutrinoKit.prototype.getBlock, $root.neutrinorpc.GetBlockRequest, $root.neutrinorpc.GetBlockResponse, request, callback);
        }, {
            name: { value: "GetBlock" },
            path: { value: "/neutrinorpc.NeutrinoKit/GetBlock" },
            requestType: { value: "GetBlockRequest" },
            responseType: { value: "GetBlockResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link neutrinorpc.NeutrinoKit#getCFilter}.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef GetCFilterCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {neutrinorpc.GetCFilterResponse} [response] GetCFilterResponse
         */

        /**
         * Calls GetCFilter.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef GetCFilter
         * @type {{
         *   (request: neutrinorpc.IGetCFilterRequest, callback: neutrinorpc.NeutrinoKit.GetCFilterCallback): void;
         *   (request: neutrinorpc.IGetCFilterRequest): Promise<neutrinorpc.GetCFilterResponse>;
         *   readonly name: "GetCFilter";
         *   readonly path: "/neutrinorpc.NeutrinoKit/GetCFilter";
         *   readonly requestType: "GetCFilterRequest";
         *   readonly responseType: "GetCFilterResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetCFilter.
         * @name neutrinorpc.NeutrinoKit#getCFilter
         * @type {neutrinorpc.NeutrinoKit.GetCFilter}
         */
        $Object.defineProperties(NeutrinoKit.prototype.getCFilter = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NeutrinoKit.prototype.getCFilter, $root.neutrinorpc.GetCFilterRequest, $root.neutrinorpc.GetCFilterResponse, request, callback);
        }, {
            name: { value: "GetCFilter" },
            path: { value: "/neutrinorpc.NeutrinoKit/GetCFilter" },
            requestType: { value: "GetCFilterRequest" },
            responseType: { value: "GetCFilterResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link neutrinorpc.NeutrinoKit#getBlockHash}.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef GetBlockHashCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {neutrinorpc.GetBlockHashResponse} [response] GetBlockHashResponse
         */

        /**
         * Calls GetBlockHash.
         * @memberof neutrinorpc.NeutrinoKit
         * @typedef GetBlockHash
         * @type {{
         *   (request: neutrinorpc.IGetBlockHashRequest, callback: neutrinorpc.NeutrinoKit.GetBlockHashCallback): void;
         *   (request: neutrinorpc.IGetBlockHashRequest): Promise<neutrinorpc.GetBlockHashResponse>;
         *   readonly name: "GetBlockHash";
         *   readonly path: "/neutrinorpc.NeutrinoKit/GetBlockHash";
         *   readonly requestType: "GetBlockHashRequest";
         *   readonly responseType: "GetBlockHashResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetBlockHash.
         * @name neutrinorpc.NeutrinoKit#getBlockHash
         * @type {neutrinorpc.NeutrinoKit.GetBlockHash}
         */
        $Object.defineProperties(NeutrinoKit.prototype.getBlockHash = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NeutrinoKit.prototype.getBlockHash, $root.neutrinorpc.GetBlockHashRequest, $root.neutrinorpc.GetBlockHashResponse, request, callback);
        }, {
            name: { value: "GetBlockHash" },
            path: { value: "/neutrinorpc.NeutrinoKit/GetBlockHash" },
            requestType: { value: "GetBlockHashRequest" },
            responseType: { value: "GetBlockHashResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return NeutrinoKit;
    })();

    neutrinorpc.StatusRequest = (function() {

        /**
         * Properties of a StatusRequest.
         * @typedef {Object} neutrinorpc.StatusRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a StatusRequest.
         * @memberof neutrinorpc
         * @interface IStatusRequest
         * @augments neutrinorpc.StatusRequest.$Properties
         * @deprecated Use neutrinorpc.StatusRequest.$Properties instead.
         */

        /**
         * Shape of a StatusRequest.
         * @typedef {neutrinorpc.StatusRequest.$Properties} neutrinorpc.StatusRequest.$Shape
         */

        /**
         * Constructs a new StatusRequest.
         * @memberof neutrinorpc
         * @classdesc Represents a StatusRequest.
         * @constructor
         * @param {neutrinorpc.StatusRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const StatusRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new StatusRequest instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.StatusRequest
         * @static
         * @param {neutrinorpc.StatusRequest.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.StatusRequest} StatusRequest instance
         * @type {{
         *   (properties: neutrinorpc.StatusRequest.$Shape): neutrinorpc.StatusRequest & neutrinorpc.StatusRequest.$Shape;
         *   (properties?: neutrinorpc.StatusRequest.$Properties): neutrinorpc.StatusRequest;
         * }}
         */
        StatusRequest.create = function(properties) {
            return new StatusRequest(properties);
        };

        /**
         * Encodes the specified StatusRequest message. Does not implicitly {@link neutrinorpc.StatusRequest.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.StatusRequest
         * @static
         * @param {neutrinorpc.StatusRequest.$Properties} message StatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StatusRequest message, length delimited. Does not implicitly {@link neutrinorpc.StatusRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.StatusRequest
         * @static
         * @param {neutrinorpc.StatusRequest.$Properties} message StatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StatusRequest message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.StatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.StatusRequest & neutrinorpc.StatusRequest.$Shape} StatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.StatusRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StatusRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.StatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.StatusRequest & neutrinorpc.StatusRequest.$Shape} StatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatusRequest message.
         * @function verify
         * @memberof neutrinorpc.StatusRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatusRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a StatusRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.StatusRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.StatusRequest} StatusRequest
         */
        StatusRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.StatusRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.StatusRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.neutrinorpc.StatusRequest();
        };

        /**
         * Creates a plain object from a StatusRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.StatusRequest
         * @static
         * @param {neutrinorpc.StatusRequest} message StatusRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatusRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this StatusRequest to JSON.
         * @function toJSON
         * @memberof neutrinorpc.StatusRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatusRequest.prototype.toJSON = function() {
            return StatusRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StatusRequest
         * @function getTypeUrl
         * @memberof neutrinorpc.StatusRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StatusRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.StatusRequest";
        };

        return StatusRequest;
    })();

    neutrinorpc.StatusResponse = (function() {

        /**
         * Properties of a StatusResponse.
         * @typedef {Object} neutrinorpc.StatusResponse.$Properties
         * @property {boolean|null} [active] StatusResponse active
         * @property {boolean|null} [synced] StatusResponse synced
         * @property {number|null} [block_height] StatusResponse block_height
         * @property {string|null} [block_hash] StatusResponse block_hash
         * @property {Array.<string>|null} [peers] StatusResponse peers
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a StatusResponse.
         * @memberof neutrinorpc
         * @interface IStatusResponse
         * @augments neutrinorpc.StatusResponse.$Properties
         * @deprecated Use neutrinorpc.StatusResponse.$Properties instead.
         */

        /**
         * Shape of a StatusResponse.
         * @typedef {neutrinorpc.StatusResponse.$Properties} neutrinorpc.StatusResponse.$Shape
         */

        /**
         * Constructs a new StatusResponse.
         * @memberof neutrinorpc
         * @classdesc Represents a StatusResponse.
         * @constructor
         * @param {neutrinorpc.StatusResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const StatusResponse = function (properties) {
            this.peers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StatusResponse active.
         * @member {boolean} active
         * @memberof neutrinorpc.StatusResponse
         * @instance
         */
        StatusResponse.prototype.active = false;

        /**
         * StatusResponse synced.
         * @member {boolean} synced
         * @memberof neutrinorpc.StatusResponse
         * @instance
         */
        StatusResponse.prototype.synced = false;

        /**
         * StatusResponse block_height.
         * @member {number} block_height
         * @memberof neutrinorpc.StatusResponse
         * @instance
         */
        StatusResponse.prototype.block_height = 0;

        /**
         * StatusResponse block_hash.
         * @member {string} block_hash
         * @memberof neutrinorpc.StatusResponse
         * @instance
         */
        StatusResponse.prototype.block_hash = "";

        /**
         * StatusResponse peers.
         * @member {Array.<string>} peers
         * @memberof neutrinorpc.StatusResponse
         * @instance
         */
        StatusResponse.prototype.peers = $util.emptyArray;

        /**
         * Creates a new StatusResponse instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.StatusResponse
         * @static
         * @param {neutrinorpc.StatusResponse.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.StatusResponse} StatusResponse instance
         * @type {{
         *   (properties: neutrinorpc.StatusResponse.$Shape): neutrinorpc.StatusResponse & neutrinorpc.StatusResponse.$Shape;
         *   (properties?: neutrinorpc.StatusResponse.$Properties): neutrinorpc.StatusResponse;
         * }}
         */
        StatusResponse.create = function(properties) {
            return new StatusResponse(properties);
        };

        /**
         * Encodes the specified StatusResponse message. Does not implicitly {@link neutrinorpc.StatusResponse.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.StatusResponse
         * @static
         * @param {neutrinorpc.StatusResponse.$Properties} message StatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.active != null && $Object.hasOwnProperty.call(message, "active") && message.active !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.active);
            if (message.synced != null && $Object.hasOwnProperty.call(message, "synced") && message.synced !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.synced);
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height") && message.block_height !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.block_height);
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash") && message.block_hash !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.block_hash);
            if (message.peers != null && message.peers.length)
                for (let i = 0; i < message.peers.length; ++i)
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.peers[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StatusResponse message, length delimited. Does not implicitly {@link neutrinorpc.StatusResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.StatusResponse
         * @static
         * @param {neutrinorpc.StatusResponse.$Properties} message StatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StatusResponse message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.StatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.StatusResponse & neutrinorpc.StatusResponse.$Shape} StatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.StatusResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.active = value;
                        else
                            delete message.active;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.synced = value;
                        else
                            delete message.synced;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.block_height = value;
                        else
                            delete message.block_height;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.block_hash = value;
                        else
                            delete message.block_hash;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.peers && message.peers.length))
                            message.peers = [];
                        message.peers.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StatusResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.StatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.StatusResponse & neutrinorpc.StatusResponse.$Shape} StatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatusResponse message.
         * @function verify
         * @memberof neutrinorpc.StatusResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatusResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.active != null && $Object.hasOwnProperty.call(message, "active"))
                if (typeof message.active !== "boolean")
                    return "active: boolean expected";
            if (message.synced != null && $Object.hasOwnProperty.call(message, "synced"))
                if (typeof message.synced !== "boolean")
                    return "synced: boolean expected";
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height"))
                if (!$util.isInteger(message.block_height))
                    return "block_height: integer expected";
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash"))
                if (!$util.isString(message.block_hash))
                    return "block_hash: string expected";
            if (message.peers != null && $Object.hasOwnProperty.call(message, "peers")) {
                if (!$Array.isArray(message.peers))
                    return "peers: array expected";
                for (let i = 0; i < message.peers.length; ++i)
                    if (!$util.isString(message.peers[i]))
                        return "peers: string[] expected";
            }
            return null;
        };

        /**
         * Creates a StatusResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.StatusResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.StatusResponse} StatusResponse
         */
        StatusResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.StatusResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.StatusResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.StatusResponse();
            if (object.active != null)
                if (object.active)
                    message.active = $Boolean(object.active);
            if (object.synced != null)
                if (object.synced)
                    message.synced = $Boolean(object.synced);
            if (object.block_height != null)
                if ($Number(object.block_height) !== 0)
                    message.block_height = object.block_height | 0;
            if (object.block_hash != null)
                if (typeof object.block_hash !== "string" || object.block_hash.length)
                    message.block_hash = $String(object.block_hash);
            if (object.peers) {
                if (!$Array.isArray(object.peers))
                    throw $TypeError(".neutrinorpc.StatusResponse.peers: array expected");
                message.peers = $Array(object.peers.length);
                for (let i = 0; i < object.peers.length; ++i)
                    message.peers[i] = $String(object.peers[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a StatusResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.StatusResponse
         * @static
         * @param {neutrinorpc.StatusResponse} message StatusResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatusResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.peers = [];
            if (options.defaults) {
                object.active = false;
                object.synced = false;
                object.block_height = 0;
                object.block_hash = "";
            }
            if (message.active != null && $Object.hasOwnProperty.call(message, "active"))
                object.active = message.active;
            if (message.synced != null && $Object.hasOwnProperty.call(message, "synced"))
                object.synced = message.synced;
            if (message.block_height != null && $Object.hasOwnProperty.call(message, "block_height"))
                object.block_height = message.block_height;
            if (message.block_hash != null && $Object.hasOwnProperty.call(message, "block_hash"))
                object.block_hash = message.block_hash;
            if (message.peers && message.peers.length) {
                object.peers = $Array(message.peers.length);
                for (let j = 0; j < message.peers.length; ++j)
                    object.peers[j] = message.peers[j];
            }
            return object;
        };

        /**
         * Converts this StatusResponse to JSON.
         * @function toJSON
         * @memberof neutrinorpc.StatusResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatusResponse.prototype.toJSON = function() {
            return StatusResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StatusResponse
         * @function getTypeUrl
         * @memberof neutrinorpc.StatusResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StatusResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.StatusResponse";
        };

        return StatusResponse;
    })();

    neutrinorpc.AddPeerRequest = (function() {

        /**
         * Properties of an AddPeerRequest.
         * @typedef {Object} neutrinorpc.AddPeerRequest.$Properties
         * @property {string|null} [peer_addrs] AddPeerRequest peer_addrs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddPeerRequest.
         * @memberof neutrinorpc
         * @interface IAddPeerRequest
         * @augments neutrinorpc.AddPeerRequest.$Properties
         * @deprecated Use neutrinorpc.AddPeerRequest.$Properties instead.
         */

        /**
         * Shape of an AddPeerRequest.
         * @typedef {neutrinorpc.AddPeerRequest.$Properties} neutrinorpc.AddPeerRequest.$Shape
         */

        /**
         * Constructs a new AddPeerRequest.
         * @memberof neutrinorpc
         * @classdesc Represents an AddPeerRequest.
         * @constructor
         * @param {neutrinorpc.AddPeerRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddPeerRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddPeerRequest peer_addrs.
         * @member {string} peer_addrs
         * @memberof neutrinorpc.AddPeerRequest
         * @instance
         */
        AddPeerRequest.prototype.peer_addrs = "";

        /**
         * Creates a new AddPeerRequest instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.AddPeerRequest
         * @static
         * @param {neutrinorpc.AddPeerRequest.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.AddPeerRequest} AddPeerRequest instance
         * @type {{
         *   (properties: neutrinorpc.AddPeerRequest.$Shape): neutrinorpc.AddPeerRequest & neutrinorpc.AddPeerRequest.$Shape;
         *   (properties?: neutrinorpc.AddPeerRequest.$Properties): neutrinorpc.AddPeerRequest;
         * }}
         */
        AddPeerRequest.create = function(properties) {
            return new AddPeerRequest(properties);
        };

        /**
         * Encodes the specified AddPeerRequest message. Does not implicitly {@link neutrinorpc.AddPeerRequest.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.AddPeerRequest
         * @static
         * @param {neutrinorpc.AddPeerRequest.$Properties} message AddPeerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddPeerRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.peer_addrs != null && $Object.hasOwnProperty.call(message, "peer_addrs") && message.peer_addrs !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.peer_addrs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddPeerRequest message, length delimited. Does not implicitly {@link neutrinorpc.AddPeerRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.AddPeerRequest
         * @static
         * @param {neutrinorpc.AddPeerRequest.$Properties} message AddPeerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddPeerRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddPeerRequest message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.AddPeerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.AddPeerRequest & neutrinorpc.AddPeerRequest.$Shape} AddPeerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddPeerRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.AddPeerRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.peer_addrs = value;
                        else
                            delete message.peer_addrs;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddPeerRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.AddPeerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.AddPeerRequest & neutrinorpc.AddPeerRequest.$Shape} AddPeerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddPeerRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddPeerRequest message.
         * @function verify
         * @memberof neutrinorpc.AddPeerRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddPeerRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.peer_addrs != null && $Object.hasOwnProperty.call(message, "peer_addrs"))
                if (!$util.isString(message.peer_addrs))
                    return "peer_addrs: string expected";
            return null;
        };

        /**
         * Creates an AddPeerRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.AddPeerRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.AddPeerRequest} AddPeerRequest
         */
        AddPeerRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.AddPeerRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.AddPeerRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.AddPeerRequest();
            if (object.peer_addrs != null)
                if (typeof object.peer_addrs !== "string" || object.peer_addrs.length)
                    message.peer_addrs = $String(object.peer_addrs);
            return message;
        };

        /**
         * Creates a plain object from an AddPeerRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.AddPeerRequest
         * @static
         * @param {neutrinorpc.AddPeerRequest} message AddPeerRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddPeerRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.peer_addrs = "";
            if (message.peer_addrs != null && $Object.hasOwnProperty.call(message, "peer_addrs"))
                object.peer_addrs = message.peer_addrs;
            return object;
        };

        /**
         * Converts this AddPeerRequest to JSON.
         * @function toJSON
         * @memberof neutrinorpc.AddPeerRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddPeerRequest.prototype.toJSON = function() {
            return AddPeerRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddPeerRequest
         * @function getTypeUrl
         * @memberof neutrinorpc.AddPeerRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddPeerRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.AddPeerRequest";
        };

        return AddPeerRequest;
    })();

    neutrinorpc.AddPeerResponse = (function() {

        /**
         * Properties of an AddPeerResponse.
         * @typedef {Object} neutrinorpc.AddPeerResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddPeerResponse.
         * @memberof neutrinorpc
         * @interface IAddPeerResponse
         * @augments neutrinorpc.AddPeerResponse.$Properties
         * @deprecated Use neutrinorpc.AddPeerResponse.$Properties instead.
         */

        /**
         * Shape of an AddPeerResponse.
         * @typedef {neutrinorpc.AddPeerResponse.$Properties} neutrinorpc.AddPeerResponse.$Shape
         */

        /**
         * Constructs a new AddPeerResponse.
         * @memberof neutrinorpc
         * @classdesc Represents an AddPeerResponse.
         * @constructor
         * @param {neutrinorpc.AddPeerResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddPeerResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new AddPeerResponse instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.AddPeerResponse
         * @static
         * @param {neutrinorpc.AddPeerResponse.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.AddPeerResponse} AddPeerResponse instance
         * @type {{
         *   (properties: neutrinorpc.AddPeerResponse.$Shape): neutrinorpc.AddPeerResponse & neutrinorpc.AddPeerResponse.$Shape;
         *   (properties?: neutrinorpc.AddPeerResponse.$Properties): neutrinorpc.AddPeerResponse;
         * }}
         */
        AddPeerResponse.create = function(properties) {
            return new AddPeerResponse(properties);
        };

        /**
         * Encodes the specified AddPeerResponse message. Does not implicitly {@link neutrinorpc.AddPeerResponse.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.AddPeerResponse
         * @static
         * @param {neutrinorpc.AddPeerResponse.$Properties} message AddPeerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddPeerResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddPeerResponse message, length delimited. Does not implicitly {@link neutrinorpc.AddPeerResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.AddPeerResponse
         * @static
         * @param {neutrinorpc.AddPeerResponse.$Properties} message AddPeerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddPeerResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddPeerResponse message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.AddPeerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.AddPeerResponse & neutrinorpc.AddPeerResponse.$Shape} AddPeerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddPeerResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.AddPeerResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddPeerResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.AddPeerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.AddPeerResponse & neutrinorpc.AddPeerResponse.$Shape} AddPeerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddPeerResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddPeerResponse message.
         * @function verify
         * @memberof neutrinorpc.AddPeerResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddPeerResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates an AddPeerResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.AddPeerResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.AddPeerResponse} AddPeerResponse
         */
        AddPeerResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.AddPeerResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.AddPeerResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.neutrinorpc.AddPeerResponse();
        };

        /**
         * Creates a plain object from an AddPeerResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.AddPeerResponse
         * @static
         * @param {neutrinorpc.AddPeerResponse} message AddPeerResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddPeerResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this AddPeerResponse to JSON.
         * @function toJSON
         * @memberof neutrinorpc.AddPeerResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddPeerResponse.prototype.toJSON = function() {
            return AddPeerResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddPeerResponse
         * @function getTypeUrl
         * @memberof neutrinorpc.AddPeerResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddPeerResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.AddPeerResponse";
        };

        return AddPeerResponse;
    })();

    neutrinorpc.DisconnectPeerRequest = (function() {

        /**
         * Properties of a DisconnectPeerRequest.
         * @typedef {Object} neutrinorpc.DisconnectPeerRequest.$Properties
         * @property {string|null} [peer_addrs] DisconnectPeerRequest peer_addrs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DisconnectPeerRequest.
         * @memberof neutrinorpc
         * @interface IDisconnectPeerRequest
         * @augments neutrinorpc.DisconnectPeerRequest.$Properties
         * @deprecated Use neutrinorpc.DisconnectPeerRequest.$Properties instead.
         */

        /**
         * Shape of a DisconnectPeerRequest.
         * @typedef {neutrinorpc.DisconnectPeerRequest.$Properties} neutrinorpc.DisconnectPeerRequest.$Shape
         */

        /**
         * Constructs a new DisconnectPeerRequest.
         * @memberof neutrinorpc
         * @classdesc Represents a DisconnectPeerRequest.
         * @constructor
         * @param {neutrinorpc.DisconnectPeerRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DisconnectPeerRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DisconnectPeerRequest peer_addrs.
         * @member {string} peer_addrs
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @instance
         */
        DisconnectPeerRequest.prototype.peer_addrs = "";

        /**
         * Creates a new DisconnectPeerRequest instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @static
         * @param {neutrinorpc.DisconnectPeerRequest.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.DisconnectPeerRequest} DisconnectPeerRequest instance
         * @type {{
         *   (properties: neutrinorpc.DisconnectPeerRequest.$Shape): neutrinorpc.DisconnectPeerRequest & neutrinorpc.DisconnectPeerRequest.$Shape;
         *   (properties?: neutrinorpc.DisconnectPeerRequest.$Properties): neutrinorpc.DisconnectPeerRequest;
         * }}
         */
        DisconnectPeerRequest.create = function(properties) {
            return new DisconnectPeerRequest(properties);
        };

        /**
         * Encodes the specified DisconnectPeerRequest message. Does not implicitly {@link neutrinorpc.DisconnectPeerRequest.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @static
         * @param {neutrinorpc.DisconnectPeerRequest.$Properties} message DisconnectPeerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisconnectPeerRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.peer_addrs != null && $Object.hasOwnProperty.call(message, "peer_addrs") && message.peer_addrs !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.peer_addrs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DisconnectPeerRequest message, length delimited. Does not implicitly {@link neutrinorpc.DisconnectPeerRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @static
         * @param {neutrinorpc.DisconnectPeerRequest.$Properties} message DisconnectPeerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisconnectPeerRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DisconnectPeerRequest message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.DisconnectPeerRequest & neutrinorpc.DisconnectPeerRequest.$Shape} DisconnectPeerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisconnectPeerRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.DisconnectPeerRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.peer_addrs = value;
                        else
                            delete message.peer_addrs;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DisconnectPeerRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.DisconnectPeerRequest & neutrinorpc.DisconnectPeerRequest.$Shape} DisconnectPeerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisconnectPeerRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DisconnectPeerRequest message.
         * @function verify
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DisconnectPeerRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.peer_addrs != null && $Object.hasOwnProperty.call(message, "peer_addrs"))
                if (!$util.isString(message.peer_addrs))
                    return "peer_addrs: string expected";
            return null;
        };

        /**
         * Creates a DisconnectPeerRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.DisconnectPeerRequest} DisconnectPeerRequest
         */
        DisconnectPeerRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.DisconnectPeerRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.DisconnectPeerRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.DisconnectPeerRequest();
            if (object.peer_addrs != null)
                if (typeof object.peer_addrs !== "string" || object.peer_addrs.length)
                    message.peer_addrs = $String(object.peer_addrs);
            return message;
        };

        /**
         * Creates a plain object from a DisconnectPeerRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @static
         * @param {neutrinorpc.DisconnectPeerRequest} message DisconnectPeerRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DisconnectPeerRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.peer_addrs = "";
            if (message.peer_addrs != null && $Object.hasOwnProperty.call(message, "peer_addrs"))
                object.peer_addrs = message.peer_addrs;
            return object;
        };

        /**
         * Converts this DisconnectPeerRequest to JSON.
         * @function toJSON
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DisconnectPeerRequest.prototype.toJSON = function() {
            return DisconnectPeerRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DisconnectPeerRequest
         * @function getTypeUrl
         * @memberof neutrinorpc.DisconnectPeerRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DisconnectPeerRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.DisconnectPeerRequest";
        };

        return DisconnectPeerRequest;
    })();

    neutrinorpc.DisconnectPeerResponse = (function() {

        /**
         * Properties of a DisconnectPeerResponse.
         * @typedef {Object} neutrinorpc.DisconnectPeerResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DisconnectPeerResponse.
         * @memberof neutrinorpc
         * @interface IDisconnectPeerResponse
         * @augments neutrinorpc.DisconnectPeerResponse.$Properties
         * @deprecated Use neutrinorpc.DisconnectPeerResponse.$Properties instead.
         */

        /**
         * Shape of a DisconnectPeerResponse.
         * @typedef {neutrinorpc.DisconnectPeerResponse.$Properties} neutrinorpc.DisconnectPeerResponse.$Shape
         */

        /**
         * Constructs a new DisconnectPeerResponse.
         * @memberof neutrinorpc
         * @classdesc Represents a DisconnectPeerResponse.
         * @constructor
         * @param {neutrinorpc.DisconnectPeerResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DisconnectPeerResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new DisconnectPeerResponse instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @static
         * @param {neutrinorpc.DisconnectPeerResponse.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.DisconnectPeerResponse} DisconnectPeerResponse instance
         * @type {{
         *   (properties: neutrinorpc.DisconnectPeerResponse.$Shape): neutrinorpc.DisconnectPeerResponse & neutrinorpc.DisconnectPeerResponse.$Shape;
         *   (properties?: neutrinorpc.DisconnectPeerResponse.$Properties): neutrinorpc.DisconnectPeerResponse;
         * }}
         */
        DisconnectPeerResponse.create = function(properties) {
            return new DisconnectPeerResponse(properties);
        };

        /**
         * Encodes the specified DisconnectPeerResponse message. Does not implicitly {@link neutrinorpc.DisconnectPeerResponse.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @static
         * @param {neutrinorpc.DisconnectPeerResponse.$Properties} message DisconnectPeerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisconnectPeerResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DisconnectPeerResponse message, length delimited. Does not implicitly {@link neutrinorpc.DisconnectPeerResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @static
         * @param {neutrinorpc.DisconnectPeerResponse.$Properties} message DisconnectPeerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisconnectPeerResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DisconnectPeerResponse message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.DisconnectPeerResponse & neutrinorpc.DisconnectPeerResponse.$Shape} DisconnectPeerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisconnectPeerResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.DisconnectPeerResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DisconnectPeerResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.DisconnectPeerResponse & neutrinorpc.DisconnectPeerResponse.$Shape} DisconnectPeerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisconnectPeerResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DisconnectPeerResponse message.
         * @function verify
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DisconnectPeerResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a DisconnectPeerResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.DisconnectPeerResponse} DisconnectPeerResponse
         */
        DisconnectPeerResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.DisconnectPeerResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.DisconnectPeerResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.neutrinorpc.DisconnectPeerResponse();
        };

        /**
         * Creates a plain object from a DisconnectPeerResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @static
         * @param {neutrinorpc.DisconnectPeerResponse} message DisconnectPeerResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DisconnectPeerResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this DisconnectPeerResponse to JSON.
         * @function toJSON
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DisconnectPeerResponse.prototype.toJSON = function() {
            return DisconnectPeerResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DisconnectPeerResponse
         * @function getTypeUrl
         * @memberof neutrinorpc.DisconnectPeerResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DisconnectPeerResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.DisconnectPeerResponse";
        };

        return DisconnectPeerResponse;
    })();

    neutrinorpc.IsBannedRequest = (function() {

        /**
         * Properties of an IsBannedRequest.
         * @typedef {Object} neutrinorpc.IsBannedRequest.$Properties
         * @property {string|null} [peer_addrs] IsBannedRequest peer_addrs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an IsBannedRequest.
         * @memberof neutrinorpc
         * @interface IIsBannedRequest
         * @augments neutrinorpc.IsBannedRequest.$Properties
         * @deprecated Use neutrinorpc.IsBannedRequest.$Properties instead.
         */

        /**
         * Shape of an IsBannedRequest.
         * @typedef {neutrinorpc.IsBannedRequest.$Properties} neutrinorpc.IsBannedRequest.$Shape
         */

        /**
         * Constructs a new IsBannedRequest.
         * @memberof neutrinorpc
         * @classdesc Represents an IsBannedRequest.
         * @constructor
         * @param {neutrinorpc.IsBannedRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const IsBannedRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * IsBannedRequest peer_addrs.
         * @member {string} peer_addrs
         * @memberof neutrinorpc.IsBannedRequest
         * @instance
         */
        IsBannedRequest.prototype.peer_addrs = "";

        /**
         * Creates a new IsBannedRequest instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.IsBannedRequest
         * @static
         * @param {neutrinorpc.IsBannedRequest.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.IsBannedRequest} IsBannedRequest instance
         * @type {{
         *   (properties: neutrinorpc.IsBannedRequest.$Shape): neutrinorpc.IsBannedRequest & neutrinorpc.IsBannedRequest.$Shape;
         *   (properties?: neutrinorpc.IsBannedRequest.$Properties): neutrinorpc.IsBannedRequest;
         * }}
         */
        IsBannedRequest.create = function(properties) {
            return new IsBannedRequest(properties);
        };

        /**
         * Encodes the specified IsBannedRequest message. Does not implicitly {@link neutrinorpc.IsBannedRequest.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.IsBannedRequest
         * @static
         * @param {neutrinorpc.IsBannedRequest.$Properties} message IsBannedRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IsBannedRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.peer_addrs != null && $Object.hasOwnProperty.call(message, "peer_addrs") && message.peer_addrs !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.peer_addrs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified IsBannedRequest message, length delimited. Does not implicitly {@link neutrinorpc.IsBannedRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.IsBannedRequest
         * @static
         * @param {neutrinorpc.IsBannedRequest.$Properties} message IsBannedRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IsBannedRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an IsBannedRequest message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.IsBannedRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.IsBannedRequest & neutrinorpc.IsBannedRequest.$Shape} IsBannedRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IsBannedRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.IsBannedRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.peer_addrs = value;
                        else
                            delete message.peer_addrs;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an IsBannedRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.IsBannedRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.IsBannedRequest & neutrinorpc.IsBannedRequest.$Shape} IsBannedRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IsBannedRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an IsBannedRequest message.
         * @function verify
         * @memberof neutrinorpc.IsBannedRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IsBannedRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.peer_addrs != null && $Object.hasOwnProperty.call(message, "peer_addrs"))
                if (!$util.isString(message.peer_addrs))
                    return "peer_addrs: string expected";
            return null;
        };

        /**
         * Creates an IsBannedRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.IsBannedRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.IsBannedRequest} IsBannedRequest
         */
        IsBannedRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.IsBannedRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.IsBannedRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.IsBannedRequest();
            if (object.peer_addrs != null)
                if (typeof object.peer_addrs !== "string" || object.peer_addrs.length)
                    message.peer_addrs = $String(object.peer_addrs);
            return message;
        };

        /**
         * Creates a plain object from an IsBannedRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.IsBannedRequest
         * @static
         * @param {neutrinorpc.IsBannedRequest} message IsBannedRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IsBannedRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.peer_addrs = "";
            if (message.peer_addrs != null && $Object.hasOwnProperty.call(message, "peer_addrs"))
                object.peer_addrs = message.peer_addrs;
            return object;
        };

        /**
         * Converts this IsBannedRequest to JSON.
         * @function toJSON
         * @memberof neutrinorpc.IsBannedRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IsBannedRequest.prototype.toJSON = function() {
            return IsBannedRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for IsBannedRequest
         * @function getTypeUrl
         * @memberof neutrinorpc.IsBannedRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        IsBannedRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.IsBannedRequest";
        };

        return IsBannedRequest;
    })();

    neutrinorpc.IsBannedResponse = (function() {

        /**
         * Properties of an IsBannedResponse.
         * @typedef {Object} neutrinorpc.IsBannedResponse.$Properties
         * @property {boolean|null} [banned] IsBannedResponse banned
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an IsBannedResponse.
         * @memberof neutrinorpc
         * @interface IIsBannedResponse
         * @augments neutrinorpc.IsBannedResponse.$Properties
         * @deprecated Use neutrinorpc.IsBannedResponse.$Properties instead.
         */

        /**
         * Shape of an IsBannedResponse.
         * @typedef {neutrinorpc.IsBannedResponse.$Properties} neutrinorpc.IsBannedResponse.$Shape
         */

        /**
         * Constructs a new IsBannedResponse.
         * @memberof neutrinorpc
         * @classdesc Represents an IsBannedResponse.
         * @constructor
         * @param {neutrinorpc.IsBannedResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const IsBannedResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * IsBannedResponse banned.
         * @member {boolean} banned
         * @memberof neutrinorpc.IsBannedResponse
         * @instance
         */
        IsBannedResponse.prototype.banned = false;

        /**
         * Creates a new IsBannedResponse instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.IsBannedResponse
         * @static
         * @param {neutrinorpc.IsBannedResponse.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.IsBannedResponse} IsBannedResponse instance
         * @type {{
         *   (properties: neutrinorpc.IsBannedResponse.$Shape): neutrinorpc.IsBannedResponse & neutrinorpc.IsBannedResponse.$Shape;
         *   (properties?: neutrinorpc.IsBannedResponse.$Properties): neutrinorpc.IsBannedResponse;
         * }}
         */
        IsBannedResponse.create = function(properties) {
            return new IsBannedResponse(properties);
        };

        /**
         * Encodes the specified IsBannedResponse message. Does not implicitly {@link neutrinorpc.IsBannedResponse.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.IsBannedResponse
         * @static
         * @param {neutrinorpc.IsBannedResponse.$Properties} message IsBannedResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IsBannedResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.banned != null && $Object.hasOwnProperty.call(message, "banned") && message.banned !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.banned);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified IsBannedResponse message, length delimited. Does not implicitly {@link neutrinorpc.IsBannedResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.IsBannedResponse
         * @static
         * @param {neutrinorpc.IsBannedResponse.$Properties} message IsBannedResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IsBannedResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an IsBannedResponse message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.IsBannedResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.IsBannedResponse & neutrinorpc.IsBannedResponse.$Shape} IsBannedResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IsBannedResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.IsBannedResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.banned = value;
                        else
                            delete message.banned;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an IsBannedResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.IsBannedResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.IsBannedResponse & neutrinorpc.IsBannedResponse.$Shape} IsBannedResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IsBannedResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an IsBannedResponse message.
         * @function verify
         * @memberof neutrinorpc.IsBannedResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IsBannedResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.banned != null && $Object.hasOwnProperty.call(message, "banned"))
                if (typeof message.banned !== "boolean")
                    return "banned: boolean expected";
            return null;
        };

        /**
         * Creates an IsBannedResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.IsBannedResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.IsBannedResponse} IsBannedResponse
         */
        IsBannedResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.IsBannedResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.IsBannedResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.IsBannedResponse();
            if (object.banned != null)
                if (object.banned)
                    message.banned = $Boolean(object.banned);
            return message;
        };

        /**
         * Creates a plain object from an IsBannedResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.IsBannedResponse
         * @static
         * @param {neutrinorpc.IsBannedResponse} message IsBannedResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IsBannedResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.banned = false;
            if (message.banned != null && $Object.hasOwnProperty.call(message, "banned"))
                object.banned = message.banned;
            return object;
        };

        /**
         * Converts this IsBannedResponse to JSON.
         * @function toJSON
         * @memberof neutrinorpc.IsBannedResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IsBannedResponse.prototype.toJSON = function() {
            return IsBannedResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for IsBannedResponse
         * @function getTypeUrl
         * @memberof neutrinorpc.IsBannedResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        IsBannedResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.IsBannedResponse";
        };

        return IsBannedResponse;
    })();

    neutrinorpc.GetBlockHeaderRequest = (function() {

        /**
         * Properties of a GetBlockHeaderRequest.
         * @typedef {Object} neutrinorpc.GetBlockHeaderRequest.$Properties
         * @property {string|null} [hash] GetBlockHeaderRequest hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetBlockHeaderRequest.
         * @memberof neutrinorpc
         * @interface IGetBlockHeaderRequest
         * @augments neutrinorpc.GetBlockHeaderRequest.$Properties
         * @deprecated Use neutrinorpc.GetBlockHeaderRequest.$Properties instead.
         */

        /**
         * Shape of a GetBlockHeaderRequest.
         * @typedef {neutrinorpc.GetBlockHeaderRequest.$Properties} neutrinorpc.GetBlockHeaderRequest.$Shape
         */

        /**
         * Constructs a new GetBlockHeaderRequest.
         * @memberof neutrinorpc
         * @classdesc Represents a GetBlockHeaderRequest.
         * @constructor
         * @param {neutrinorpc.GetBlockHeaderRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetBlockHeaderRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetBlockHeaderRequest hash.
         * @member {string} hash
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @instance
         */
        GetBlockHeaderRequest.prototype.hash = "";

        /**
         * Creates a new GetBlockHeaderRequest instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @static
         * @param {neutrinorpc.GetBlockHeaderRequest.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.GetBlockHeaderRequest} GetBlockHeaderRequest instance
         * @type {{
         *   (properties: neutrinorpc.GetBlockHeaderRequest.$Shape): neutrinorpc.GetBlockHeaderRequest & neutrinorpc.GetBlockHeaderRequest.$Shape;
         *   (properties?: neutrinorpc.GetBlockHeaderRequest.$Properties): neutrinorpc.GetBlockHeaderRequest;
         * }}
         */
        GetBlockHeaderRequest.create = function(properties) {
            return new GetBlockHeaderRequest(properties);
        };

        /**
         * Encodes the specified GetBlockHeaderRequest message. Does not implicitly {@link neutrinorpc.GetBlockHeaderRequest.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @static
         * @param {neutrinorpc.GetBlockHeaderRequest.$Properties} message GetBlockHeaderRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockHeaderRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetBlockHeaderRequest message, length delimited. Does not implicitly {@link neutrinorpc.GetBlockHeaderRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @static
         * @param {neutrinorpc.GetBlockHeaderRequest.$Properties} message GetBlockHeaderRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockHeaderRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetBlockHeaderRequest message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.GetBlockHeaderRequest & neutrinorpc.GetBlockHeaderRequest.$Shape} GetBlockHeaderRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockHeaderRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.GetBlockHeaderRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetBlockHeaderRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.GetBlockHeaderRequest & neutrinorpc.GetBlockHeaderRequest.$Shape} GetBlockHeaderRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockHeaderRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBlockHeaderRequest message.
         * @function verify
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBlockHeaderRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!$util.isString(message.hash))
                    return "hash: string expected";
            return null;
        };

        /**
         * Creates a GetBlockHeaderRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.GetBlockHeaderRequest} GetBlockHeaderRequest
         */
        GetBlockHeaderRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.GetBlockHeaderRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.GetBlockHeaderRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.GetBlockHeaderRequest();
            if (object.hash != null)
                if (typeof object.hash !== "string" || object.hash.length)
                    message.hash = $String(object.hash);
            return message;
        };

        /**
         * Creates a plain object from a GetBlockHeaderRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @static
         * @param {neutrinorpc.GetBlockHeaderRequest} message GetBlockHeaderRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBlockHeaderRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.hash = "";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = message.hash;
            return object;
        };

        /**
         * Converts this GetBlockHeaderRequest to JSON.
         * @function toJSON
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBlockHeaderRequest.prototype.toJSON = function() {
            return GetBlockHeaderRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetBlockHeaderRequest
         * @function getTypeUrl
         * @memberof neutrinorpc.GetBlockHeaderRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetBlockHeaderRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.GetBlockHeaderRequest";
        };

        return GetBlockHeaderRequest;
    })();

    neutrinorpc.GetBlockHeaderResponse = (function() {

        /**
         * Properties of a GetBlockHeaderResponse.
         * @typedef {Object} neutrinorpc.GetBlockHeaderResponse.$Properties
         * @property {string|null} [hash] GetBlockHeaderResponse hash
         * @property {Long|null} [confirmations] GetBlockHeaderResponse confirmations
         * @property {Long|null} [stripped_size] GetBlockHeaderResponse stripped_size
         * @property {Long|null} [size] GetBlockHeaderResponse size
         * @property {Long|null} [weight] GetBlockHeaderResponse weight
         * @property {number|null} [height] GetBlockHeaderResponse height
         * @property {number|null} [version] GetBlockHeaderResponse version
         * @property {string|null} [version_hex] GetBlockHeaderResponse version_hex
         * @property {string|null} [merkleroot] GetBlockHeaderResponse merkleroot
         * @property {Long|null} [time] GetBlockHeaderResponse time
         * @property {number|null} [nonce] GetBlockHeaderResponse nonce
         * @property {string|null} [bits] GetBlockHeaderResponse bits
         * @property {number|null} [ntx] GetBlockHeaderResponse ntx
         * @property {string|null} [previous_block_hash] GetBlockHeaderResponse previous_block_hash
         * @property {Uint8Array|null} [raw_hex] GetBlockHeaderResponse raw_hex
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetBlockHeaderResponse.
         * @memberof neutrinorpc
         * @interface IGetBlockHeaderResponse
         * @augments neutrinorpc.GetBlockHeaderResponse.$Properties
         * @deprecated Use neutrinorpc.GetBlockHeaderResponse.$Properties instead.
         */

        /**
         * Shape of a GetBlockHeaderResponse.
         * @typedef {neutrinorpc.GetBlockHeaderResponse.$Properties} neutrinorpc.GetBlockHeaderResponse.$Shape
         */

        /**
         * Constructs a new GetBlockHeaderResponse.
         * @memberof neutrinorpc
         * @classdesc Represents a GetBlockHeaderResponse.
         * @constructor
         * @param {neutrinorpc.GetBlockHeaderResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetBlockHeaderResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetBlockHeaderResponse hash.
         * @member {string} hash
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.hash = "";

        /**
         * GetBlockHeaderResponse confirmations.
         * @member {Long} confirmations
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.confirmations = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockHeaderResponse stripped_size.
         * @member {Long} stripped_size
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.stripped_size = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockHeaderResponse size.
         * @member {Long} size
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.size = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockHeaderResponse weight.
         * @member {Long} weight
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.weight = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockHeaderResponse height.
         * @member {number} height
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.height = 0;

        /**
         * GetBlockHeaderResponse version.
         * @member {number} version
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.version = 0;

        /**
         * GetBlockHeaderResponse version_hex.
         * @member {string} version_hex
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.version_hex = "";

        /**
         * GetBlockHeaderResponse merkleroot.
         * @member {string} merkleroot
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.merkleroot = "";

        /**
         * GetBlockHeaderResponse time.
         * @member {Long} time
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockHeaderResponse nonce.
         * @member {number} nonce
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.nonce = 0;

        /**
         * GetBlockHeaderResponse bits.
         * @member {string} bits
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.bits = "";

        /**
         * GetBlockHeaderResponse ntx.
         * @member {number} ntx
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.ntx = 0;

        /**
         * GetBlockHeaderResponse previous_block_hash.
         * @member {string} previous_block_hash
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.previous_block_hash = "";

        /**
         * GetBlockHeaderResponse raw_hex.
         * @member {Uint8Array} raw_hex
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         */
        GetBlockHeaderResponse.prototype.raw_hex = $util.newBuffer([]);

        /**
         * Creates a new GetBlockHeaderResponse instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @static
         * @param {neutrinorpc.GetBlockHeaderResponse.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.GetBlockHeaderResponse} GetBlockHeaderResponse instance
         * @type {{
         *   (properties: neutrinorpc.GetBlockHeaderResponse.$Shape): neutrinorpc.GetBlockHeaderResponse & neutrinorpc.GetBlockHeaderResponse.$Shape;
         *   (properties?: neutrinorpc.GetBlockHeaderResponse.$Properties): neutrinorpc.GetBlockHeaderResponse;
         * }}
         */
        GetBlockHeaderResponse.create = function(properties) {
            return new GetBlockHeaderResponse(properties);
        };

        /**
         * Encodes the specified GetBlockHeaderResponse message. Does not implicitly {@link neutrinorpc.GetBlockHeaderResponse.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @static
         * @param {neutrinorpc.GetBlockHeaderResponse.$Properties} message GetBlockHeaderResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockHeaderResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.hash);
            if (message.confirmations != null && $Object.hasOwnProperty.call(message, "confirmations") && (typeof message.confirmations === "object" ? message.confirmations.low || message.confirmations.high : message.confirmations !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.confirmations);
            if (message.stripped_size != null && $Object.hasOwnProperty.call(message, "stripped_size") && (typeof message.stripped_size === "object" ? message.stripped_size.low || message.stripped_size.high : message.stripped_size !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.stripped_size);
            if (message.size != null && $Object.hasOwnProperty.call(message, "size") && (typeof message.size === "object" ? message.size.low || message.size.high : message.size !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.size);
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && (typeof message.weight === "object" ? message.weight.low || message.weight.high : message.weight !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.weight);
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.height);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.version);
            if (message.version_hex != null && $Object.hasOwnProperty.call(message, "version_hex") && message.version_hex !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.version_hex);
            if (message.merkleroot != null && $Object.hasOwnProperty.call(message, "merkleroot") && message.merkleroot !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.merkleroot);
            if (message.time != null && $Object.hasOwnProperty.call(message, "time") && (typeof message.time === "object" ? message.time.low || message.time.high : message.time !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.time);
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce") && message.nonce !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.nonce);
            if (message.bits != null && $Object.hasOwnProperty.call(message, "bits") && message.bits !== "")
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.bits);
            if (message.ntx != null && $Object.hasOwnProperty.call(message, "ntx") && message.ntx !== 0)
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.ntx);
            if (message.previous_block_hash != null && $Object.hasOwnProperty.call(message, "previous_block_hash") && message.previous_block_hash !== "")
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.previous_block_hash);
            if (message.raw_hex != null && $Object.hasOwnProperty.call(message, "raw_hex") && message.raw_hex.length)
                writer.uint32(/* id 15, wireType 2 =*/122).bytes(message.raw_hex);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetBlockHeaderResponse message, length delimited. Does not implicitly {@link neutrinorpc.GetBlockHeaderResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @static
         * @param {neutrinorpc.GetBlockHeaderResponse.$Properties} message GetBlockHeaderResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockHeaderResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetBlockHeaderResponse message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.GetBlockHeaderResponse & neutrinorpc.GetBlockHeaderResponse.$Shape} GetBlockHeaderResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockHeaderResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.GetBlockHeaderResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.confirmations = value;
                        else
                            delete message.confirmations;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stripped_size = value;
                        else
                            delete message.stripped_size;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.size = value;
                        else
                            delete message.size;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.weight = value;
                        else
                            delete message.weight;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.version_hex = value;
                        else
                            delete message.version_hex;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.merkleroot = value;
                        else
                            delete message.merkleroot;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.time = value;
                        else
                            delete message.time;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.nonce = value;
                        else
                            delete message.nonce;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.bits = value;
                        else
                            delete message.bits;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.ntx = value;
                        else
                            delete message.ntx;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.previous_block_hash = value;
                        else
                            delete message.previous_block_hash;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_hex = value;
                        else
                            delete message.raw_hex;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetBlockHeaderResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.GetBlockHeaderResponse & neutrinorpc.GetBlockHeaderResponse.$Shape} GetBlockHeaderResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockHeaderResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBlockHeaderResponse message.
         * @function verify
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBlockHeaderResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!$util.isString(message.hash))
                    return "hash: string expected";
            if (message.confirmations != null && $Object.hasOwnProperty.call(message, "confirmations"))
                if (!$util.isInteger(message.confirmations) && !(message.confirmations && $util.isInteger(message.confirmations.low) && $util.isInteger(message.confirmations.high)))
                    return "confirmations: integer|Long expected";
            if (message.stripped_size != null && $Object.hasOwnProperty.call(message, "stripped_size"))
                if (!$util.isInteger(message.stripped_size) && !(message.stripped_size && $util.isInteger(message.stripped_size.low) && $util.isInteger(message.stripped_size.high)))
                    return "stripped_size: integer|Long expected";
            if (message.size != null && $Object.hasOwnProperty.call(message, "size"))
                if (!$util.isInteger(message.size) && !(message.size && $util.isInteger(message.size.low) && $util.isInteger(message.size.high)))
                    return "size: integer|Long expected";
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                if (!$util.isInteger(message.weight) && !(message.weight && $util.isInteger(message.weight.low) && $util.isInteger(message.weight.high)))
                    return "weight: integer|Long expected";
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            if (message.version_hex != null && $Object.hasOwnProperty.call(message, "version_hex"))
                if (!$util.isString(message.version_hex))
                    return "version_hex: string expected";
            if (message.merkleroot != null && $Object.hasOwnProperty.call(message, "merkleroot"))
                if (!$util.isString(message.merkleroot))
                    return "merkleroot: string expected";
            if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                    return "time: integer|Long expected";
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                if (!$util.isInteger(message.nonce))
                    return "nonce: integer expected";
            if (message.bits != null && $Object.hasOwnProperty.call(message, "bits"))
                if (!$util.isString(message.bits))
                    return "bits: string expected";
            if (message.ntx != null && $Object.hasOwnProperty.call(message, "ntx"))
                if (!$util.isInteger(message.ntx))
                    return "ntx: integer expected";
            if (message.previous_block_hash != null && $Object.hasOwnProperty.call(message, "previous_block_hash"))
                if (!$util.isString(message.previous_block_hash))
                    return "previous_block_hash: string expected";
            if (message.raw_hex != null && $Object.hasOwnProperty.call(message, "raw_hex"))
                if (!(message.raw_hex && typeof message.raw_hex.length === "number" || $util.isString(message.raw_hex)))
                    return "raw_hex: buffer expected";
            return null;
        };

        /**
         * Creates a GetBlockHeaderResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.GetBlockHeaderResponse} GetBlockHeaderResponse
         */
        GetBlockHeaderResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.GetBlockHeaderResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.GetBlockHeaderResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.GetBlockHeaderResponse();
            if (object.hash != null)
                if (typeof object.hash !== "string" || object.hash.length)
                    message.hash = $String(object.hash);
            if (object.confirmations != null)
                if (typeof object.confirmations === "object" ? object.confirmations.low || object.confirmations.high : $Number(object.confirmations) !== 0)
                    if ($util.Long)
                        message.confirmations = $util.Long.fromValue(object.confirmations, false);
                    else if (typeof object.confirmations === "string")
                        message.confirmations = $parseInt(object.confirmations, 10);
                    else if (typeof object.confirmations === "number")
                        message.confirmations = object.confirmations;
                    else if (typeof object.confirmations === "object")
                        message.confirmations = new $util.LongBits(object.confirmations.low >>> 0, object.confirmations.high >>> 0).toNumber();
            if (object.stripped_size != null)
                if (typeof object.stripped_size === "object" ? object.stripped_size.low || object.stripped_size.high : $Number(object.stripped_size) !== 0)
                    if ($util.Long)
                        message.stripped_size = $util.Long.fromValue(object.stripped_size, false);
                    else if (typeof object.stripped_size === "string")
                        message.stripped_size = $parseInt(object.stripped_size, 10);
                    else if (typeof object.stripped_size === "number")
                        message.stripped_size = object.stripped_size;
                    else if (typeof object.stripped_size === "object")
                        message.stripped_size = new $util.LongBits(object.stripped_size.low >>> 0, object.stripped_size.high >>> 0).toNumber();
            if (object.size != null)
                if (typeof object.size === "object" ? object.size.low || object.size.high : $Number(object.size) !== 0)
                    if ($util.Long)
                        message.size = $util.Long.fromValue(object.size, false);
                    else if (typeof object.size === "string")
                        message.size = $parseInt(object.size, 10);
                    else if (typeof object.size === "number")
                        message.size = object.size;
                    else if (typeof object.size === "object")
                        message.size = new $util.LongBits(object.size.low >>> 0, object.size.high >>> 0).toNumber();
            if (object.weight != null)
                if (typeof object.weight === "object" ? object.weight.low || object.weight.high : $Number(object.weight) !== 0)
                    if ($util.Long)
                        message.weight = $util.Long.fromValue(object.weight, false);
                    else if (typeof object.weight === "string")
                        message.weight = $parseInt(object.weight, 10);
                    else if (typeof object.weight === "number")
                        message.weight = object.weight;
                    else if (typeof object.weight === "object")
                        message.weight = new $util.LongBits(object.weight.low >>> 0, object.weight.high >>> 0).toNumber();
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height | 0;
            if (object.version != null)
                if ($Number(object.version) !== 0)
                    message.version = object.version | 0;
            if (object.version_hex != null)
                if (typeof object.version_hex !== "string" || object.version_hex.length)
                    message.version_hex = $String(object.version_hex);
            if (object.merkleroot != null)
                if (typeof object.merkleroot !== "string" || object.merkleroot.length)
                    message.merkleroot = $String(object.merkleroot);
            if (object.time != null)
                if (typeof object.time === "object" ? object.time.low || object.time.high : $Number(object.time) !== 0)
                    if ($util.Long)
                        message.time = $util.Long.fromValue(object.time, false);
                    else if (typeof object.time === "string")
                        message.time = $parseInt(object.time, 10);
                    else if (typeof object.time === "number")
                        message.time = object.time;
                    else if (typeof object.time === "object")
                        message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
            if (object.nonce != null)
                if ($Number(object.nonce) !== 0)
                    message.nonce = object.nonce >>> 0;
            if (object.bits != null)
                if (typeof object.bits !== "string" || object.bits.length)
                    message.bits = $String(object.bits);
            if (object.ntx != null)
                if ($Number(object.ntx) !== 0)
                    message.ntx = object.ntx | 0;
            if (object.previous_block_hash != null)
                if (typeof object.previous_block_hash !== "string" || object.previous_block_hash.length)
                    message.previous_block_hash = $String(object.previous_block_hash);
            if (object.raw_hex != null)
                if (object.raw_hex.length)
                    if (typeof object.raw_hex === "string")
                        $util.base64.decode(object.raw_hex, message.raw_hex = $util.newBuffer($util.base64.length(object.raw_hex)), 0);
                    else if (object.raw_hex.length >= 0)
                        message.raw_hex = object.raw_hex;
            return message;
        };

        /**
         * Creates a plain object from a GetBlockHeaderResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @static
         * @param {neutrinorpc.GetBlockHeaderResponse} message GetBlockHeaderResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBlockHeaderResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.hash = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.confirmations = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.confirmations = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stripped_size = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stripped_size = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.size = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.size = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.weight = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.weight = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.height = 0;
                object.version = 0;
                object.version_hex = "";
                object.merkleroot = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nonce = 0;
                object.bits = "";
                object.ntx = 0;
                object.previous_block_hash = "";
                if (options.bytes === $String)
                    object.raw_hex = "";
                else {
                    object.raw_hex = [];
                    if (options.bytes !== $Array)
                        object.raw_hex = $util.newBuffer(object.raw_hex);
                }
            }
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = message.hash;
            if (message.confirmations != null && $Object.hasOwnProperty.call(message, "confirmations"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.confirmations = typeof message.confirmations === "number" ? $BigInt(message.confirmations) : $util.Long.fromBits(message.confirmations.low >>> 0, message.confirmations.high >>> 0, false).toBigInt();
                else if (typeof message.confirmations === "number")
                    object.confirmations = options.longs === $String ? $String(message.confirmations) : message.confirmations;
                else
                    object.confirmations = options.longs === $String ? $util.Long.prototype.toString.call(message.confirmations) : options.longs === $Number ? new $util.LongBits(message.confirmations.low >>> 0, message.confirmations.high >>> 0).toNumber() : message.confirmations;
            if (message.stripped_size != null && $Object.hasOwnProperty.call(message, "stripped_size"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stripped_size = typeof message.stripped_size === "number" ? $BigInt(message.stripped_size) : $util.Long.fromBits(message.stripped_size.low >>> 0, message.stripped_size.high >>> 0, false).toBigInt();
                else if (typeof message.stripped_size === "number")
                    object.stripped_size = options.longs === $String ? $String(message.stripped_size) : message.stripped_size;
                else
                    object.stripped_size = options.longs === $String ? $util.Long.prototype.toString.call(message.stripped_size) : options.longs === $Number ? new $util.LongBits(message.stripped_size.low >>> 0, message.stripped_size.high >>> 0).toNumber() : message.stripped_size;
            if (message.size != null && $Object.hasOwnProperty.call(message, "size"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.size = typeof message.size === "number" ? $BigInt(message.size) : $util.Long.fromBits(message.size.low >>> 0, message.size.high >>> 0, false).toBigInt();
                else if (typeof message.size === "number")
                    object.size = options.longs === $String ? $String(message.size) : message.size;
                else
                    object.size = options.longs === $String ? $util.Long.prototype.toString.call(message.size) : options.longs === $Number ? new $util.LongBits(message.size.low >>> 0, message.size.high >>> 0).toNumber() : message.size;
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.weight = typeof message.weight === "number" ? $BigInt(message.weight) : $util.Long.fromBits(message.weight.low >>> 0, message.weight.high >>> 0, false).toBigInt();
                else if (typeof message.weight === "number")
                    object.weight = options.longs === $String ? $String(message.weight) : message.weight;
                else
                    object.weight = options.longs === $String ? $util.Long.prototype.toString.call(message.weight) : options.longs === $Number ? new $util.LongBits(message.weight.low >>> 0, message.weight.high >>> 0).toNumber() : message.weight;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.version_hex != null && $Object.hasOwnProperty.call(message, "version_hex"))
                object.version_hex = message.version_hex;
            if (message.merkleroot != null && $Object.hasOwnProperty.call(message, "merkleroot"))
                object.merkleroot = message.merkleroot;
            if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.time = typeof message.time === "number" ? $BigInt(message.time) : $util.Long.fromBits(message.time.low >>> 0, message.time.high >>> 0, false).toBigInt();
                else if (typeof message.time === "number")
                    object.time = options.longs === $String ? $String(message.time) : message.time;
                else
                    object.time = options.longs === $String ? $util.Long.prototype.toString.call(message.time) : options.longs === $Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                object.nonce = message.nonce;
            if (message.bits != null && $Object.hasOwnProperty.call(message, "bits"))
                object.bits = message.bits;
            if (message.ntx != null && $Object.hasOwnProperty.call(message, "ntx"))
                object.ntx = message.ntx;
            if (message.previous_block_hash != null && $Object.hasOwnProperty.call(message, "previous_block_hash"))
                object.previous_block_hash = message.previous_block_hash;
            if (message.raw_hex != null && $Object.hasOwnProperty.call(message, "raw_hex"))
                object.raw_hex = options.bytes === $String ? $util.base64.encode(message.raw_hex, 0, message.raw_hex.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_hex) : message.raw_hex;
            return object;
        };

        /**
         * Converts this GetBlockHeaderResponse to JSON.
         * @function toJSON
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBlockHeaderResponse.prototype.toJSON = function() {
            return GetBlockHeaderResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetBlockHeaderResponse
         * @function getTypeUrl
         * @memberof neutrinorpc.GetBlockHeaderResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetBlockHeaderResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.GetBlockHeaderResponse";
        };

        return GetBlockHeaderResponse;
    })();

    neutrinorpc.GetBlockRequest = (function() {

        /**
         * Properties of a GetBlockRequest.
         * @typedef {Object} neutrinorpc.GetBlockRequest.$Properties
         * @property {string|null} [hash] GetBlockRequest hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetBlockRequest.
         * @memberof neutrinorpc
         * @interface IGetBlockRequest
         * @augments neutrinorpc.GetBlockRequest.$Properties
         * @deprecated Use neutrinorpc.GetBlockRequest.$Properties instead.
         */

        /**
         * Shape of a GetBlockRequest.
         * @typedef {neutrinorpc.GetBlockRequest.$Properties} neutrinorpc.GetBlockRequest.$Shape
         */

        /**
         * Constructs a new GetBlockRequest.
         * @memberof neutrinorpc
         * @classdesc Represents a GetBlockRequest.
         * @constructor
         * @param {neutrinorpc.GetBlockRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetBlockRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetBlockRequest hash.
         * @member {string} hash
         * @memberof neutrinorpc.GetBlockRequest
         * @instance
         */
        GetBlockRequest.prototype.hash = "";

        /**
         * Creates a new GetBlockRequest instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.GetBlockRequest
         * @static
         * @param {neutrinorpc.GetBlockRequest.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.GetBlockRequest} GetBlockRequest instance
         * @type {{
         *   (properties: neutrinorpc.GetBlockRequest.$Shape): neutrinorpc.GetBlockRequest & neutrinorpc.GetBlockRequest.$Shape;
         *   (properties?: neutrinorpc.GetBlockRequest.$Properties): neutrinorpc.GetBlockRequest;
         * }}
         */
        GetBlockRequest.create = function(properties) {
            return new GetBlockRequest(properties);
        };

        /**
         * Encodes the specified GetBlockRequest message. Does not implicitly {@link neutrinorpc.GetBlockRequest.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.GetBlockRequest
         * @static
         * @param {neutrinorpc.GetBlockRequest.$Properties} message GetBlockRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetBlockRequest message, length delimited. Does not implicitly {@link neutrinorpc.GetBlockRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.GetBlockRequest
         * @static
         * @param {neutrinorpc.GetBlockRequest.$Properties} message GetBlockRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetBlockRequest message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.GetBlockRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.GetBlockRequest & neutrinorpc.GetBlockRequest.$Shape} GetBlockRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.GetBlockRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetBlockRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.GetBlockRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.GetBlockRequest & neutrinorpc.GetBlockRequest.$Shape} GetBlockRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBlockRequest message.
         * @function verify
         * @memberof neutrinorpc.GetBlockRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBlockRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!$util.isString(message.hash))
                    return "hash: string expected";
            return null;
        };

        /**
         * Creates a GetBlockRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.GetBlockRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.GetBlockRequest} GetBlockRequest
         */
        GetBlockRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.GetBlockRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.GetBlockRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.GetBlockRequest();
            if (object.hash != null)
                if (typeof object.hash !== "string" || object.hash.length)
                    message.hash = $String(object.hash);
            return message;
        };

        /**
         * Creates a plain object from a GetBlockRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.GetBlockRequest
         * @static
         * @param {neutrinorpc.GetBlockRequest} message GetBlockRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBlockRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.hash = "";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = message.hash;
            return object;
        };

        /**
         * Converts this GetBlockRequest to JSON.
         * @function toJSON
         * @memberof neutrinorpc.GetBlockRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBlockRequest.prototype.toJSON = function() {
            return GetBlockRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetBlockRequest
         * @function getTypeUrl
         * @memberof neutrinorpc.GetBlockRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetBlockRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.GetBlockRequest";
        };

        return GetBlockRequest;
    })();

    neutrinorpc.GetBlockResponse = (function() {

        /**
         * Properties of a GetBlockResponse.
         * @typedef {Object} neutrinorpc.GetBlockResponse.$Properties
         * @property {string|null} [hash] GetBlockResponse hash
         * @property {Long|null} [confirmations] GetBlockResponse confirmations
         * @property {Long|null} [stripped_size] GetBlockResponse stripped_size
         * @property {Long|null} [size] GetBlockResponse size
         * @property {Long|null} [weight] GetBlockResponse weight
         * @property {number|null} [height] GetBlockResponse height
         * @property {number|null} [version] GetBlockResponse version
         * @property {string|null} [version_hex] GetBlockResponse version_hex
         * @property {string|null} [merkleroot] GetBlockResponse merkleroot
         * @property {Array.<string>|null} [tx] GetBlockResponse tx
         * @property {Long|null} [time] GetBlockResponse time
         * @property {number|null} [nonce] GetBlockResponse nonce
         * @property {string|null} [bits] GetBlockResponse bits
         * @property {number|null} [ntx] GetBlockResponse ntx
         * @property {string|null} [previous_block_hash] GetBlockResponse previous_block_hash
         * @property {Uint8Array|null} [raw_hex] GetBlockResponse raw_hex
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetBlockResponse.
         * @memberof neutrinorpc
         * @interface IGetBlockResponse
         * @augments neutrinorpc.GetBlockResponse.$Properties
         * @deprecated Use neutrinorpc.GetBlockResponse.$Properties instead.
         */

        /**
         * Shape of a GetBlockResponse.
         * @typedef {neutrinorpc.GetBlockResponse.$Properties} neutrinorpc.GetBlockResponse.$Shape
         */

        /**
         * Constructs a new GetBlockResponse.
         * @memberof neutrinorpc
         * @classdesc Represents a GetBlockResponse.
         * @constructor
         * @param {neutrinorpc.GetBlockResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetBlockResponse = function (properties) {
            this.tx = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetBlockResponse hash.
         * @member {string} hash
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.hash = "";

        /**
         * GetBlockResponse confirmations.
         * @member {Long} confirmations
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.confirmations = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockResponse stripped_size.
         * @member {Long} stripped_size
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.stripped_size = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockResponse size.
         * @member {Long} size
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.size = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockResponse weight.
         * @member {Long} weight
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.weight = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockResponse height.
         * @member {number} height
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.height = 0;

        /**
         * GetBlockResponse version.
         * @member {number} version
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.version = 0;

        /**
         * GetBlockResponse version_hex.
         * @member {string} version_hex
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.version_hex = "";

        /**
         * GetBlockResponse merkleroot.
         * @member {string} merkleroot
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.merkleroot = "";

        /**
         * GetBlockResponse tx.
         * @member {Array.<string>} tx
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.tx = $util.emptyArray;

        /**
         * GetBlockResponse time.
         * @member {Long} time
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetBlockResponse nonce.
         * @member {number} nonce
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.nonce = 0;

        /**
         * GetBlockResponse bits.
         * @member {string} bits
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.bits = "";

        /**
         * GetBlockResponse ntx.
         * @member {number} ntx
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.ntx = 0;

        /**
         * GetBlockResponse previous_block_hash.
         * @member {string} previous_block_hash
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.previous_block_hash = "";

        /**
         * GetBlockResponse raw_hex.
         * @member {Uint8Array} raw_hex
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.raw_hex = $util.newBuffer([]);

        /**
         * Creates a new GetBlockResponse instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.GetBlockResponse
         * @static
         * @param {neutrinorpc.GetBlockResponse.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.GetBlockResponse} GetBlockResponse instance
         * @type {{
         *   (properties: neutrinorpc.GetBlockResponse.$Shape): neutrinorpc.GetBlockResponse & neutrinorpc.GetBlockResponse.$Shape;
         *   (properties?: neutrinorpc.GetBlockResponse.$Properties): neutrinorpc.GetBlockResponse;
         * }}
         */
        GetBlockResponse.create = function(properties) {
            return new GetBlockResponse(properties);
        };

        /**
         * Encodes the specified GetBlockResponse message. Does not implicitly {@link neutrinorpc.GetBlockResponse.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.GetBlockResponse
         * @static
         * @param {neutrinorpc.GetBlockResponse.$Properties} message GetBlockResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.hash);
            if (message.confirmations != null && $Object.hasOwnProperty.call(message, "confirmations") && (typeof message.confirmations === "object" ? message.confirmations.low || message.confirmations.high : message.confirmations !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.confirmations);
            if (message.stripped_size != null && $Object.hasOwnProperty.call(message, "stripped_size") && (typeof message.stripped_size === "object" ? message.stripped_size.low || message.stripped_size.high : message.stripped_size !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.stripped_size);
            if (message.size != null && $Object.hasOwnProperty.call(message, "size") && (typeof message.size === "object" ? message.size.low || message.size.high : message.size !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.size);
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && (typeof message.weight === "object" ? message.weight.low || message.weight.high : message.weight !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.weight);
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.height);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.version);
            if (message.version_hex != null && $Object.hasOwnProperty.call(message, "version_hex") && message.version_hex !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.version_hex);
            if (message.merkleroot != null && $Object.hasOwnProperty.call(message, "merkleroot") && message.merkleroot !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.merkleroot);
            if (message.tx != null && message.tx.length)
                for (let i = 0; i < message.tx.length; ++i)
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.tx[i]);
            if (message.time != null && $Object.hasOwnProperty.call(message, "time") && (typeof message.time === "object" ? message.time.low || message.time.high : message.time !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.time);
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce") && message.nonce !== 0)
                writer.uint32(/* id 12, wireType 0 =*/96).uint32(message.nonce);
            if (message.bits != null && $Object.hasOwnProperty.call(message, "bits") && message.bits !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.bits);
            if (message.ntx != null && $Object.hasOwnProperty.call(message, "ntx") && message.ntx !== 0)
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.ntx);
            if (message.previous_block_hash != null && $Object.hasOwnProperty.call(message, "previous_block_hash") && message.previous_block_hash !== "")
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.previous_block_hash);
            if (message.raw_hex != null && $Object.hasOwnProperty.call(message, "raw_hex") && message.raw_hex.length)
                writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.raw_hex);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetBlockResponse message, length delimited. Does not implicitly {@link neutrinorpc.GetBlockResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.GetBlockResponse
         * @static
         * @param {neutrinorpc.GetBlockResponse.$Properties} message GetBlockResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetBlockResponse message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.GetBlockResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.GetBlockResponse & neutrinorpc.GetBlockResponse.$Shape} GetBlockResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.GetBlockResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.confirmations = value;
                        else
                            delete message.confirmations;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stripped_size = value;
                        else
                            delete message.stripped_size;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.size = value;
                        else
                            delete message.size;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.weight = value;
                        else
                            delete message.weight;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.version_hex = value;
                        else
                            delete message.version_hex;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.merkleroot = value;
                        else
                            delete message.merkleroot;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tx && message.tx.length))
                            message.tx = [];
                        message.tx.push(reader.stringVerify());
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.time = value;
                        else
                            delete message.time;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.nonce = value;
                        else
                            delete message.nonce;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.bits = value;
                        else
                            delete message.bits;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.ntx = value;
                        else
                            delete message.ntx;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.previous_block_hash = value;
                        else
                            delete message.previous_block_hash;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_hex = value;
                        else
                            delete message.raw_hex;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetBlockResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.GetBlockResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.GetBlockResponse & neutrinorpc.GetBlockResponse.$Shape} GetBlockResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBlockResponse message.
         * @function verify
         * @memberof neutrinorpc.GetBlockResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBlockResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!$util.isString(message.hash))
                    return "hash: string expected";
            if (message.confirmations != null && $Object.hasOwnProperty.call(message, "confirmations"))
                if (!$util.isInteger(message.confirmations) && !(message.confirmations && $util.isInteger(message.confirmations.low) && $util.isInteger(message.confirmations.high)))
                    return "confirmations: integer|Long expected";
            if (message.stripped_size != null && $Object.hasOwnProperty.call(message, "stripped_size"))
                if (!$util.isInteger(message.stripped_size) && !(message.stripped_size && $util.isInteger(message.stripped_size.low) && $util.isInteger(message.stripped_size.high)))
                    return "stripped_size: integer|Long expected";
            if (message.size != null && $Object.hasOwnProperty.call(message, "size"))
                if (!$util.isInteger(message.size) && !(message.size && $util.isInteger(message.size.low) && $util.isInteger(message.size.high)))
                    return "size: integer|Long expected";
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                if (!$util.isInteger(message.weight) && !(message.weight && $util.isInteger(message.weight.low) && $util.isInteger(message.weight.high)))
                    return "weight: integer|Long expected";
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            if (message.version_hex != null && $Object.hasOwnProperty.call(message, "version_hex"))
                if (!$util.isString(message.version_hex))
                    return "version_hex: string expected";
            if (message.merkleroot != null && $Object.hasOwnProperty.call(message, "merkleroot"))
                if (!$util.isString(message.merkleroot))
                    return "merkleroot: string expected";
            if (message.tx != null && $Object.hasOwnProperty.call(message, "tx")) {
                if (!$Array.isArray(message.tx))
                    return "tx: array expected";
                for (let i = 0; i < message.tx.length; ++i)
                    if (!$util.isString(message.tx[i]))
                        return "tx: string[] expected";
            }
            if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                    return "time: integer|Long expected";
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                if (!$util.isInteger(message.nonce))
                    return "nonce: integer expected";
            if (message.bits != null && $Object.hasOwnProperty.call(message, "bits"))
                if (!$util.isString(message.bits))
                    return "bits: string expected";
            if (message.ntx != null && $Object.hasOwnProperty.call(message, "ntx"))
                if (!$util.isInteger(message.ntx))
                    return "ntx: integer expected";
            if (message.previous_block_hash != null && $Object.hasOwnProperty.call(message, "previous_block_hash"))
                if (!$util.isString(message.previous_block_hash))
                    return "previous_block_hash: string expected";
            if (message.raw_hex != null && $Object.hasOwnProperty.call(message, "raw_hex"))
                if (!(message.raw_hex && typeof message.raw_hex.length === "number" || $util.isString(message.raw_hex)))
                    return "raw_hex: buffer expected";
            return null;
        };

        /**
         * Creates a GetBlockResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.GetBlockResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.GetBlockResponse} GetBlockResponse
         */
        GetBlockResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.GetBlockResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.GetBlockResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.GetBlockResponse();
            if (object.hash != null)
                if (typeof object.hash !== "string" || object.hash.length)
                    message.hash = $String(object.hash);
            if (object.confirmations != null)
                if (typeof object.confirmations === "object" ? object.confirmations.low || object.confirmations.high : $Number(object.confirmations) !== 0)
                    if ($util.Long)
                        message.confirmations = $util.Long.fromValue(object.confirmations, false);
                    else if (typeof object.confirmations === "string")
                        message.confirmations = $parseInt(object.confirmations, 10);
                    else if (typeof object.confirmations === "number")
                        message.confirmations = object.confirmations;
                    else if (typeof object.confirmations === "object")
                        message.confirmations = new $util.LongBits(object.confirmations.low >>> 0, object.confirmations.high >>> 0).toNumber();
            if (object.stripped_size != null)
                if (typeof object.stripped_size === "object" ? object.stripped_size.low || object.stripped_size.high : $Number(object.stripped_size) !== 0)
                    if ($util.Long)
                        message.stripped_size = $util.Long.fromValue(object.stripped_size, false);
                    else if (typeof object.stripped_size === "string")
                        message.stripped_size = $parseInt(object.stripped_size, 10);
                    else if (typeof object.stripped_size === "number")
                        message.stripped_size = object.stripped_size;
                    else if (typeof object.stripped_size === "object")
                        message.stripped_size = new $util.LongBits(object.stripped_size.low >>> 0, object.stripped_size.high >>> 0).toNumber();
            if (object.size != null)
                if (typeof object.size === "object" ? object.size.low || object.size.high : $Number(object.size) !== 0)
                    if ($util.Long)
                        message.size = $util.Long.fromValue(object.size, false);
                    else if (typeof object.size === "string")
                        message.size = $parseInt(object.size, 10);
                    else if (typeof object.size === "number")
                        message.size = object.size;
                    else if (typeof object.size === "object")
                        message.size = new $util.LongBits(object.size.low >>> 0, object.size.high >>> 0).toNumber();
            if (object.weight != null)
                if (typeof object.weight === "object" ? object.weight.low || object.weight.high : $Number(object.weight) !== 0)
                    if ($util.Long)
                        message.weight = $util.Long.fromValue(object.weight, false);
                    else if (typeof object.weight === "string")
                        message.weight = $parseInt(object.weight, 10);
                    else if (typeof object.weight === "number")
                        message.weight = object.weight;
                    else if (typeof object.weight === "object")
                        message.weight = new $util.LongBits(object.weight.low >>> 0, object.weight.high >>> 0).toNumber();
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height | 0;
            if (object.version != null)
                if ($Number(object.version) !== 0)
                    message.version = object.version | 0;
            if (object.version_hex != null)
                if (typeof object.version_hex !== "string" || object.version_hex.length)
                    message.version_hex = $String(object.version_hex);
            if (object.merkleroot != null)
                if (typeof object.merkleroot !== "string" || object.merkleroot.length)
                    message.merkleroot = $String(object.merkleroot);
            if (object.tx) {
                if (!$Array.isArray(object.tx))
                    throw $TypeError(".neutrinorpc.GetBlockResponse.tx: array expected");
                message.tx = $Array(object.tx.length);
                for (let i = 0; i < object.tx.length; ++i)
                    message.tx[i] = $String(object.tx[i]);
            }
            if (object.time != null)
                if (typeof object.time === "object" ? object.time.low || object.time.high : $Number(object.time) !== 0)
                    if ($util.Long)
                        message.time = $util.Long.fromValue(object.time, false);
                    else if (typeof object.time === "string")
                        message.time = $parseInt(object.time, 10);
                    else if (typeof object.time === "number")
                        message.time = object.time;
                    else if (typeof object.time === "object")
                        message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
            if (object.nonce != null)
                if ($Number(object.nonce) !== 0)
                    message.nonce = object.nonce >>> 0;
            if (object.bits != null)
                if (typeof object.bits !== "string" || object.bits.length)
                    message.bits = $String(object.bits);
            if (object.ntx != null)
                if ($Number(object.ntx) !== 0)
                    message.ntx = object.ntx | 0;
            if (object.previous_block_hash != null)
                if (typeof object.previous_block_hash !== "string" || object.previous_block_hash.length)
                    message.previous_block_hash = $String(object.previous_block_hash);
            if (object.raw_hex != null)
                if (object.raw_hex.length)
                    if (typeof object.raw_hex === "string")
                        $util.base64.decode(object.raw_hex, message.raw_hex = $util.newBuffer($util.base64.length(object.raw_hex)), 0);
                    else if (object.raw_hex.length >= 0)
                        message.raw_hex = object.raw_hex;
            return message;
        };

        /**
         * Creates a plain object from a GetBlockResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.GetBlockResponse
         * @static
         * @param {neutrinorpc.GetBlockResponse} message GetBlockResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBlockResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tx = [];
            if (options.defaults) {
                object.hash = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.confirmations = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.confirmations = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stripped_size = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stripped_size = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.size = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.size = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.weight = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.weight = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.height = 0;
                object.version = 0;
                object.version_hex = "";
                object.merkleroot = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nonce = 0;
                object.bits = "";
                object.ntx = 0;
                object.previous_block_hash = "";
                if (options.bytes === $String)
                    object.raw_hex = "";
                else {
                    object.raw_hex = [];
                    if (options.bytes !== $Array)
                        object.raw_hex = $util.newBuffer(object.raw_hex);
                }
            }
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = message.hash;
            if (message.confirmations != null && $Object.hasOwnProperty.call(message, "confirmations"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.confirmations = typeof message.confirmations === "number" ? $BigInt(message.confirmations) : $util.Long.fromBits(message.confirmations.low >>> 0, message.confirmations.high >>> 0, false).toBigInt();
                else if (typeof message.confirmations === "number")
                    object.confirmations = options.longs === $String ? $String(message.confirmations) : message.confirmations;
                else
                    object.confirmations = options.longs === $String ? $util.Long.prototype.toString.call(message.confirmations) : options.longs === $Number ? new $util.LongBits(message.confirmations.low >>> 0, message.confirmations.high >>> 0).toNumber() : message.confirmations;
            if (message.stripped_size != null && $Object.hasOwnProperty.call(message, "stripped_size"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stripped_size = typeof message.stripped_size === "number" ? $BigInt(message.stripped_size) : $util.Long.fromBits(message.stripped_size.low >>> 0, message.stripped_size.high >>> 0, false).toBigInt();
                else if (typeof message.stripped_size === "number")
                    object.stripped_size = options.longs === $String ? $String(message.stripped_size) : message.stripped_size;
                else
                    object.stripped_size = options.longs === $String ? $util.Long.prototype.toString.call(message.stripped_size) : options.longs === $Number ? new $util.LongBits(message.stripped_size.low >>> 0, message.stripped_size.high >>> 0).toNumber() : message.stripped_size;
            if (message.size != null && $Object.hasOwnProperty.call(message, "size"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.size = typeof message.size === "number" ? $BigInt(message.size) : $util.Long.fromBits(message.size.low >>> 0, message.size.high >>> 0, false).toBigInt();
                else if (typeof message.size === "number")
                    object.size = options.longs === $String ? $String(message.size) : message.size;
                else
                    object.size = options.longs === $String ? $util.Long.prototype.toString.call(message.size) : options.longs === $Number ? new $util.LongBits(message.size.low >>> 0, message.size.high >>> 0).toNumber() : message.size;
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.weight = typeof message.weight === "number" ? $BigInt(message.weight) : $util.Long.fromBits(message.weight.low >>> 0, message.weight.high >>> 0, false).toBigInt();
                else if (typeof message.weight === "number")
                    object.weight = options.longs === $String ? $String(message.weight) : message.weight;
                else
                    object.weight = options.longs === $String ? $util.Long.prototype.toString.call(message.weight) : options.longs === $Number ? new $util.LongBits(message.weight.low >>> 0, message.weight.high >>> 0).toNumber() : message.weight;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.version_hex != null && $Object.hasOwnProperty.call(message, "version_hex"))
                object.version_hex = message.version_hex;
            if (message.merkleroot != null && $Object.hasOwnProperty.call(message, "merkleroot"))
                object.merkleroot = message.merkleroot;
            if (message.tx && message.tx.length) {
                object.tx = $Array(message.tx.length);
                for (let j = 0; j < message.tx.length; ++j)
                    object.tx[j] = message.tx[j];
            }
            if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.time = typeof message.time === "number" ? $BigInt(message.time) : $util.Long.fromBits(message.time.low >>> 0, message.time.high >>> 0, false).toBigInt();
                else if (typeof message.time === "number")
                    object.time = options.longs === $String ? $String(message.time) : message.time;
                else
                    object.time = options.longs === $String ? $util.Long.prototype.toString.call(message.time) : options.longs === $Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                object.nonce = message.nonce;
            if (message.bits != null && $Object.hasOwnProperty.call(message, "bits"))
                object.bits = message.bits;
            if (message.ntx != null && $Object.hasOwnProperty.call(message, "ntx"))
                object.ntx = message.ntx;
            if (message.previous_block_hash != null && $Object.hasOwnProperty.call(message, "previous_block_hash"))
                object.previous_block_hash = message.previous_block_hash;
            if (message.raw_hex != null && $Object.hasOwnProperty.call(message, "raw_hex"))
                object.raw_hex = options.bytes === $String ? $util.base64.encode(message.raw_hex, 0, message.raw_hex.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_hex) : message.raw_hex;
            return object;
        };

        /**
         * Converts this GetBlockResponse to JSON.
         * @function toJSON
         * @memberof neutrinorpc.GetBlockResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBlockResponse.prototype.toJSON = function() {
            return GetBlockResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetBlockResponse
         * @function getTypeUrl
         * @memberof neutrinorpc.GetBlockResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetBlockResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.GetBlockResponse";
        };

        return GetBlockResponse;
    })();

    neutrinorpc.GetCFilterRequest = (function() {

        /**
         * Properties of a GetCFilterRequest.
         * @typedef {Object} neutrinorpc.GetCFilterRequest.$Properties
         * @property {string|null} [hash] GetCFilterRequest hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetCFilterRequest.
         * @memberof neutrinorpc
         * @interface IGetCFilterRequest
         * @augments neutrinorpc.GetCFilterRequest.$Properties
         * @deprecated Use neutrinorpc.GetCFilterRequest.$Properties instead.
         */

        /**
         * Shape of a GetCFilterRequest.
         * @typedef {neutrinorpc.GetCFilterRequest.$Properties} neutrinorpc.GetCFilterRequest.$Shape
         */

        /**
         * Constructs a new GetCFilterRequest.
         * @memberof neutrinorpc
         * @classdesc Represents a GetCFilterRequest.
         * @constructor
         * @param {neutrinorpc.GetCFilterRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetCFilterRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetCFilterRequest hash.
         * @member {string} hash
         * @memberof neutrinorpc.GetCFilterRequest
         * @instance
         */
        GetCFilterRequest.prototype.hash = "";

        /**
         * Creates a new GetCFilterRequest instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.GetCFilterRequest
         * @static
         * @param {neutrinorpc.GetCFilterRequest.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.GetCFilterRequest} GetCFilterRequest instance
         * @type {{
         *   (properties: neutrinorpc.GetCFilterRequest.$Shape): neutrinorpc.GetCFilterRequest & neutrinorpc.GetCFilterRequest.$Shape;
         *   (properties?: neutrinorpc.GetCFilterRequest.$Properties): neutrinorpc.GetCFilterRequest;
         * }}
         */
        GetCFilterRequest.create = function(properties) {
            return new GetCFilterRequest(properties);
        };

        /**
         * Encodes the specified GetCFilterRequest message. Does not implicitly {@link neutrinorpc.GetCFilterRequest.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.GetCFilterRequest
         * @static
         * @param {neutrinorpc.GetCFilterRequest.$Properties} message GetCFilterRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetCFilterRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetCFilterRequest message, length delimited. Does not implicitly {@link neutrinorpc.GetCFilterRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.GetCFilterRequest
         * @static
         * @param {neutrinorpc.GetCFilterRequest.$Properties} message GetCFilterRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetCFilterRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetCFilterRequest message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.GetCFilterRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.GetCFilterRequest & neutrinorpc.GetCFilterRequest.$Shape} GetCFilterRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetCFilterRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.GetCFilterRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetCFilterRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.GetCFilterRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.GetCFilterRequest & neutrinorpc.GetCFilterRequest.$Shape} GetCFilterRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetCFilterRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetCFilterRequest message.
         * @function verify
         * @memberof neutrinorpc.GetCFilterRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetCFilterRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!$util.isString(message.hash))
                    return "hash: string expected";
            return null;
        };

        /**
         * Creates a GetCFilterRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.GetCFilterRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.GetCFilterRequest} GetCFilterRequest
         */
        GetCFilterRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.GetCFilterRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.GetCFilterRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.GetCFilterRequest();
            if (object.hash != null)
                if (typeof object.hash !== "string" || object.hash.length)
                    message.hash = $String(object.hash);
            return message;
        };

        /**
         * Creates a plain object from a GetCFilterRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.GetCFilterRequest
         * @static
         * @param {neutrinorpc.GetCFilterRequest} message GetCFilterRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetCFilterRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.hash = "";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = message.hash;
            return object;
        };

        /**
         * Converts this GetCFilterRequest to JSON.
         * @function toJSON
         * @memberof neutrinorpc.GetCFilterRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetCFilterRequest.prototype.toJSON = function() {
            return GetCFilterRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetCFilterRequest
         * @function getTypeUrl
         * @memberof neutrinorpc.GetCFilterRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetCFilterRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.GetCFilterRequest";
        };

        return GetCFilterRequest;
    })();

    neutrinorpc.GetCFilterResponse = (function() {

        /**
         * Properties of a GetCFilterResponse.
         * @typedef {Object} neutrinorpc.GetCFilterResponse.$Properties
         * @property {Uint8Array|null} [filter] GetCFilterResponse filter
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetCFilterResponse.
         * @memberof neutrinorpc
         * @interface IGetCFilterResponse
         * @augments neutrinorpc.GetCFilterResponse.$Properties
         * @deprecated Use neutrinorpc.GetCFilterResponse.$Properties instead.
         */

        /**
         * Shape of a GetCFilterResponse.
         * @typedef {neutrinorpc.GetCFilterResponse.$Properties} neutrinorpc.GetCFilterResponse.$Shape
         */

        /**
         * Constructs a new GetCFilterResponse.
         * @memberof neutrinorpc
         * @classdesc Represents a GetCFilterResponse.
         * @constructor
         * @param {neutrinorpc.GetCFilterResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetCFilterResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetCFilterResponse filter.
         * @member {Uint8Array} filter
         * @memberof neutrinorpc.GetCFilterResponse
         * @instance
         */
        GetCFilterResponse.prototype.filter = $util.newBuffer([]);

        /**
         * Creates a new GetCFilterResponse instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.GetCFilterResponse
         * @static
         * @param {neutrinorpc.GetCFilterResponse.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.GetCFilterResponse} GetCFilterResponse instance
         * @type {{
         *   (properties: neutrinorpc.GetCFilterResponse.$Shape): neutrinorpc.GetCFilterResponse & neutrinorpc.GetCFilterResponse.$Shape;
         *   (properties?: neutrinorpc.GetCFilterResponse.$Properties): neutrinorpc.GetCFilterResponse;
         * }}
         */
        GetCFilterResponse.create = function(properties) {
            return new GetCFilterResponse(properties);
        };

        /**
         * Encodes the specified GetCFilterResponse message. Does not implicitly {@link neutrinorpc.GetCFilterResponse.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.GetCFilterResponse
         * @static
         * @param {neutrinorpc.GetCFilterResponse.$Properties} message GetCFilterResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetCFilterResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.filter != null && $Object.hasOwnProperty.call(message, "filter") && message.filter.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.filter);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetCFilterResponse message, length delimited. Does not implicitly {@link neutrinorpc.GetCFilterResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.GetCFilterResponse
         * @static
         * @param {neutrinorpc.GetCFilterResponse.$Properties} message GetCFilterResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetCFilterResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetCFilterResponse message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.GetCFilterResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.GetCFilterResponse & neutrinorpc.GetCFilterResponse.$Shape} GetCFilterResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetCFilterResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.GetCFilterResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.filter = value;
                        else
                            delete message.filter;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetCFilterResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.GetCFilterResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.GetCFilterResponse & neutrinorpc.GetCFilterResponse.$Shape} GetCFilterResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetCFilterResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetCFilterResponse message.
         * @function verify
         * @memberof neutrinorpc.GetCFilterResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetCFilterResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.filter != null && $Object.hasOwnProperty.call(message, "filter"))
                if (!(message.filter && typeof message.filter.length === "number" || $util.isString(message.filter)))
                    return "filter: buffer expected";
            return null;
        };

        /**
         * Creates a GetCFilterResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.GetCFilterResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.GetCFilterResponse} GetCFilterResponse
         */
        GetCFilterResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.GetCFilterResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.GetCFilterResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.GetCFilterResponse();
            if (object.filter != null)
                if (object.filter.length)
                    if (typeof object.filter === "string")
                        $util.base64.decode(object.filter, message.filter = $util.newBuffer($util.base64.length(object.filter)), 0);
                    else if (object.filter.length >= 0)
                        message.filter = object.filter;
            return message;
        };

        /**
         * Creates a plain object from a GetCFilterResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.GetCFilterResponse
         * @static
         * @param {neutrinorpc.GetCFilterResponse} message GetCFilterResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetCFilterResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.filter = "";
                else {
                    object.filter = [];
                    if (options.bytes !== $Array)
                        object.filter = $util.newBuffer(object.filter);
                }
            if (message.filter != null && $Object.hasOwnProperty.call(message, "filter"))
                object.filter = options.bytes === $String ? $util.base64.encode(message.filter, 0, message.filter.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.filter) : message.filter;
            return object;
        };

        /**
         * Converts this GetCFilterResponse to JSON.
         * @function toJSON
         * @memberof neutrinorpc.GetCFilterResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetCFilterResponse.prototype.toJSON = function() {
            return GetCFilterResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetCFilterResponse
         * @function getTypeUrl
         * @memberof neutrinorpc.GetCFilterResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetCFilterResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.GetCFilterResponse";
        };

        return GetCFilterResponse;
    })();

    neutrinorpc.GetBlockHashRequest = (function() {

        /**
         * Properties of a GetBlockHashRequest.
         * @typedef {Object} neutrinorpc.GetBlockHashRequest.$Properties
         * @property {number|null} [height] GetBlockHashRequest height
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetBlockHashRequest.
         * @memberof neutrinorpc
         * @interface IGetBlockHashRequest
         * @augments neutrinorpc.GetBlockHashRequest.$Properties
         * @deprecated Use neutrinorpc.GetBlockHashRequest.$Properties instead.
         */

        /**
         * Shape of a GetBlockHashRequest.
         * @typedef {neutrinorpc.GetBlockHashRequest.$Properties} neutrinorpc.GetBlockHashRequest.$Shape
         */

        /**
         * Constructs a new GetBlockHashRequest.
         * @memberof neutrinorpc
         * @classdesc Represents a GetBlockHashRequest.
         * @constructor
         * @param {neutrinorpc.GetBlockHashRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetBlockHashRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetBlockHashRequest height.
         * @member {number} height
         * @memberof neutrinorpc.GetBlockHashRequest
         * @instance
         */
        GetBlockHashRequest.prototype.height = 0;

        /**
         * Creates a new GetBlockHashRequest instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.GetBlockHashRequest
         * @static
         * @param {neutrinorpc.GetBlockHashRequest.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.GetBlockHashRequest} GetBlockHashRequest instance
         * @type {{
         *   (properties: neutrinorpc.GetBlockHashRequest.$Shape): neutrinorpc.GetBlockHashRequest & neutrinorpc.GetBlockHashRequest.$Shape;
         *   (properties?: neutrinorpc.GetBlockHashRequest.$Properties): neutrinorpc.GetBlockHashRequest;
         * }}
         */
        GetBlockHashRequest.create = function(properties) {
            return new GetBlockHashRequest(properties);
        };

        /**
         * Encodes the specified GetBlockHashRequest message. Does not implicitly {@link neutrinorpc.GetBlockHashRequest.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.GetBlockHashRequest
         * @static
         * @param {neutrinorpc.GetBlockHashRequest.$Properties} message GetBlockHashRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockHashRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetBlockHashRequest message, length delimited. Does not implicitly {@link neutrinorpc.GetBlockHashRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.GetBlockHashRequest
         * @static
         * @param {neutrinorpc.GetBlockHashRequest.$Properties} message GetBlockHashRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockHashRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetBlockHashRequest message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.GetBlockHashRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.GetBlockHashRequest & neutrinorpc.GetBlockHashRequest.$Shape} GetBlockHashRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockHashRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.GetBlockHashRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetBlockHashRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.GetBlockHashRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.GetBlockHashRequest & neutrinorpc.GetBlockHashRequest.$Shape} GetBlockHashRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockHashRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBlockHashRequest message.
         * @function verify
         * @memberof neutrinorpc.GetBlockHashRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBlockHashRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            return null;
        };

        /**
         * Creates a GetBlockHashRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.GetBlockHashRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.GetBlockHashRequest} GetBlockHashRequest
         */
        GetBlockHashRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.GetBlockHashRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.GetBlockHashRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.GetBlockHashRequest();
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height | 0;
            return message;
        };

        /**
         * Creates a plain object from a GetBlockHashRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.GetBlockHashRequest
         * @static
         * @param {neutrinorpc.GetBlockHashRequest} message GetBlockHashRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBlockHashRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.height = 0;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            return object;
        };

        /**
         * Converts this GetBlockHashRequest to JSON.
         * @function toJSON
         * @memberof neutrinorpc.GetBlockHashRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBlockHashRequest.prototype.toJSON = function() {
            return GetBlockHashRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetBlockHashRequest
         * @function getTypeUrl
         * @memberof neutrinorpc.GetBlockHashRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetBlockHashRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.GetBlockHashRequest";
        };

        return GetBlockHashRequest;
    })();

    neutrinorpc.GetBlockHashResponse = (function() {

        /**
         * Properties of a GetBlockHashResponse.
         * @typedef {Object} neutrinorpc.GetBlockHashResponse.$Properties
         * @property {string|null} [hash] GetBlockHashResponse hash
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetBlockHashResponse.
         * @memberof neutrinorpc
         * @interface IGetBlockHashResponse
         * @augments neutrinorpc.GetBlockHashResponse.$Properties
         * @deprecated Use neutrinorpc.GetBlockHashResponse.$Properties instead.
         */

        /**
         * Shape of a GetBlockHashResponse.
         * @typedef {neutrinorpc.GetBlockHashResponse.$Properties} neutrinorpc.GetBlockHashResponse.$Shape
         */

        /**
         * Constructs a new GetBlockHashResponse.
         * @memberof neutrinorpc
         * @classdesc Represents a GetBlockHashResponse.
         * @constructor
         * @param {neutrinorpc.GetBlockHashResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetBlockHashResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetBlockHashResponse hash.
         * @member {string} hash
         * @memberof neutrinorpc.GetBlockHashResponse
         * @instance
         */
        GetBlockHashResponse.prototype.hash = "";

        /**
         * Creates a new GetBlockHashResponse instance using the specified properties.
         * @function create
         * @memberof neutrinorpc.GetBlockHashResponse
         * @static
         * @param {neutrinorpc.GetBlockHashResponse.$Properties=} [properties] Properties to set
         * @returns {neutrinorpc.GetBlockHashResponse} GetBlockHashResponse instance
         * @type {{
         *   (properties: neutrinorpc.GetBlockHashResponse.$Shape): neutrinorpc.GetBlockHashResponse & neutrinorpc.GetBlockHashResponse.$Shape;
         *   (properties?: neutrinorpc.GetBlockHashResponse.$Properties): neutrinorpc.GetBlockHashResponse;
         * }}
         */
        GetBlockHashResponse.create = function(properties) {
            return new GetBlockHashResponse(properties);
        };

        /**
         * Encodes the specified GetBlockHashResponse message. Does not implicitly {@link neutrinorpc.GetBlockHashResponse.verify|verify} messages.
         * @function encode
         * @memberof neutrinorpc.GetBlockHashResponse
         * @static
         * @param {neutrinorpc.GetBlockHashResponse.$Properties} message GetBlockHashResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockHashResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetBlockHashResponse message, length delimited. Does not implicitly {@link neutrinorpc.GetBlockHashResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof neutrinorpc.GetBlockHashResponse
         * @static
         * @param {neutrinorpc.GetBlockHashResponse.$Properties} message GetBlockHashResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockHashResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetBlockHashResponse message from the specified reader or buffer.
         * @function decode
         * @memberof neutrinorpc.GetBlockHashResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {neutrinorpc.GetBlockHashResponse & neutrinorpc.GetBlockHashResponse.$Shape} GetBlockHashResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockHashResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.neutrinorpc.GetBlockHashResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetBlockHashResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof neutrinorpc.GetBlockHashResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {neutrinorpc.GetBlockHashResponse & neutrinorpc.GetBlockHashResponse.$Shape} GetBlockHashResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockHashResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBlockHashResponse message.
         * @function verify
         * @memberof neutrinorpc.GetBlockHashResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBlockHashResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!$util.isString(message.hash))
                    return "hash: string expected";
            return null;
        };

        /**
         * Creates a GetBlockHashResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof neutrinorpc.GetBlockHashResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {neutrinorpc.GetBlockHashResponse} GetBlockHashResponse
         */
        GetBlockHashResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.neutrinorpc.GetBlockHashResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".neutrinorpc.GetBlockHashResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.neutrinorpc.GetBlockHashResponse();
            if (object.hash != null)
                if (typeof object.hash !== "string" || object.hash.length)
                    message.hash = $String(object.hash);
            return message;
        };

        /**
         * Creates a plain object from a GetBlockHashResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof neutrinorpc.GetBlockHashResponse
         * @static
         * @param {neutrinorpc.GetBlockHashResponse} message GetBlockHashResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBlockHashResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.hash = "";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = message.hash;
            return object;
        };

        /**
         * Converts this GetBlockHashResponse to JSON.
         * @function toJSON
         * @memberof neutrinorpc.GetBlockHashResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBlockHashResponse.prototype.toJSON = function() {
            return GetBlockHashResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetBlockHashResponse
         * @function getTypeUrl
         * @memberof neutrinorpc.GetBlockHashResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetBlockHashResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/neutrinorpc.GetBlockHashResponse";
        };

        return GetBlockHashResponse;
    })();

    return neutrinorpc;
})();

export const routerrpc = $root.routerrpc = (() => {

    /**
     * Namespace routerrpc.
     * @exports routerrpc
     * @namespace
     */
    const routerrpc = {};

    routerrpc.Router = (function() {

        /**
         * Constructs a new Router service.
         * @memberof routerrpc
         * @classdesc Represents a Router
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const Router = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (Router.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = Router;

        /**
         * Creates new Router service using the specified rpc implementation.
         * @function create
         * @memberof routerrpc.Router
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {Router} RPC service. Useful where requests and/or responses are streamed.
         */
        Router.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link routerrpc.Router#sendPaymentV2}.
         * @memberof routerrpc.Router
         * @typedef SendPaymentV2Callback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.Payment} [response] Payment
         */

        /**
         * Calls SendPaymentV2.
         * @memberof routerrpc.Router
         * @typedef SendPaymentV2
         * @type {{
         *   (request: routerrpc.ISendPaymentRequest, callback: routerrpc.Router.SendPaymentV2Callback): void;
         *   (request: routerrpc.ISendPaymentRequest): Promise<lnrpc.Payment>;
         *   readonly name: "SendPaymentV2";
         *   readonly path: "/routerrpc.Router/SendPaymentV2";
         *   readonly requestType: "SendPaymentRequest";
         *   readonly responseType: "lnrpc.Payment";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SendPaymentV2.
         * @name routerrpc.Router#sendPaymentV2
         * @type {routerrpc.Router.SendPaymentV2}
         */
        $Object.defineProperties(Router.prototype.sendPaymentV2 = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.sendPaymentV2, $root.routerrpc.SendPaymentRequest, $root.lnrpc.Payment, request, callback);
        }, {
            name: { value: "SendPaymentV2" },
            path: { value: "/routerrpc.Router/SendPaymentV2" },
            requestType: { value: "SendPaymentRequest" },
            responseType: { value: "lnrpc.Payment" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link routerrpc.Router#trackPaymentV2}.
         * @memberof routerrpc.Router
         * @typedef TrackPaymentV2Callback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.Payment} [response] Payment
         */

        /**
         * Calls TrackPaymentV2.
         * @memberof routerrpc.Router
         * @typedef TrackPaymentV2
         * @type {{
         *   (request: routerrpc.ITrackPaymentRequest, callback: routerrpc.Router.TrackPaymentV2Callback): void;
         *   (request: routerrpc.ITrackPaymentRequest): Promise<lnrpc.Payment>;
         *   readonly name: "TrackPaymentV2";
         *   readonly path: "/routerrpc.Router/TrackPaymentV2";
         *   readonly requestType: "TrackPaymentRequest";
         *   readonly responseType: "lnrpc.Payment";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls TrackPaymentV2.
         * @name routerrpc.Router#trackPaymentV2
         * @type {routerrpc.Router.TrackPaymentV2}
         */
        $Object.defineProperties(Router.prototype.trackPaymentV2 = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.trackPaymentV2, $root.routerrpc.TrackPaymentRequest, $root.lnrpc.Payment, request, callback);
        }, {
            name: { value: "TrackPaymentV2" },
            path: { value: "/routerrpc.Router/TrackPaymentV2" },
            requestType: { value: "TrackPaymentRequest" },
            responseType: { value: "lnrpc.Payment" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link routerrpc.Router#trackPayments}.
         * @memberof routerrpc.Router
         * @typedef TrackPaymentsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.Payment} [response] Payment
         */

        /**
         * Calls TrackPayments.
         * @memberof routerrpc.Router
         * @typedef TrackPayments
         * @type {{
         *   (request: routerrpc.ITrackPaymentsRequest, callback: routerrpc.Router.TrackPaymentsCallback): void;
         *   (request: routerrpc.ITrackPaymentsRequest): Promise<lnrpc.Payment>;
         *   readonly name: "TrackPayments";
         *   readonly path: "/routerrpc.Router/TrackPayments";
         *   readonly requestType: "TrackPaymentsRequest";
         *   readonly responseType: "lnrpc.Payment";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls TrackPayments.
         * @name routerrpc.Router#trackPayments
         * @type {routerrpc.Router.TrackPayments}
         */
        $Object.defineProperties(Router.prototype.trackPayments = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.trackPayments, $root.routerrpc.TrackPaymentsRequest, $root.lnrpc.Payment, request, callback);
        }, {
            name: { value: "TrackPayments" },
            path: { value: "/routerrpc.Router/TrackPayments" },
            requestType: { value: "TrackPaymentsRequest" },
            responseType: { value: "lnrpc.Payment" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link routerrpc.Router#estimateRouteFee}.
         * @memberof routerrpc.Router
         * @typedef EstimateRouteFeeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.RouteFeeResponse} [response] RouteFeeResponse
         */

        /**
         * Calls EstimateRouteFee.
         * @memberof routerrpc.Router
         * @typedef EstimateRouteFee
         * @type {{
         *   (request: routerrpc.IRouteFeeRequest, callback: routerrpc.Router.EstimateRouteFeeCallback): void;
         *   (request: routerrpc.IRouteFeeRequest): Promise<routerrpc.RouteFeeResponse>;
         *   readonly name: "EstimateRouteFee";
         *   readonly path: "/routerrpc.Router/EstimateRouteFee";
         *   readonly requestType: "RouteFeeRequest";
         *   readonly responseType: "RouteFeeResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls EstimateRouteFee.
         * @name routerrpc.Router#estimateRouteFee
         * @type {routerrpc.Router.EstimateRouteFee}
         */
        $Object.defineProperties(Router.prototype.estimateRouteFee = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.estimateRouteFee, $root.routerrpc.RouteFeeRequest, $root.routerrpc.RouteFeeResponse, request, callback);
        }, {
            name: { value: "EstimateRouteFee" },
            path: { value: "/routerrpc.Router/EstimateRouteFee" },
            requestType: { value: "RouteFeeRequest" },
            responseType: { value: "RouteFeeResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#sendToRoute}.
         * @memberof routerrpc.Router
         * @typedef SendToRouteCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.SendToRouteResponse} [response] SendToRouteResponse
         */

        /**
         * Calls SendToRoute.
         * @memberof routerrpc.Router
         * @typedef SendToRoute
         * @type {{
         *   (request: routerrpc.ISendToRouteRequest, callback: routerrpc.Router.SendToRouteCallback): void;
         *   (request: routerrpc.ISendToRouteRequest): Promise<routerrpc.SendToRouteResponse>;
         *   readonly name: "SendToRoute";
         *   readonly path: "/routerrpc.Router/SendToRoute";
         *   readonly requestType: "SendToRouteRequest";
         *   readonly responseType: "SendToRouteResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SendToRoute.
         * @name routerrpc.Router#sendToRoute
         * @type {routerrpc.Router.SendToRoute}
         */
        $Object.defineProperties(Router.prototype.sendToRoute = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.sendToRoute, $root.routerrpc.SendToRouteRequest, $root.routerrpc.SendToRouteResponse, request, callback);
        }, {
            name: { value: "SendToRoute" },
            path: { value: "/routerrpc.Router/SendToRoute" },
            requestType: { value: "SendToRouteRequest" },
            responseType: { value: "SendToRouteResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#sendToRouteV2}.
         * @memberof routerrpc.Router
         * @typedef SendToRouteV2Callback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.HTLCAttempt} [response] HTLCAttempt
         */

        /**
         * Calls SendToRouteV2.
         * @memberof routerrpc.Router
         * @typedef SendToRouteV2
         * @type {{
         *   (request: routerrpc.ISendToRouteRequest, callback: routerrpc.Router.SendToRouteV2Callback): void;
         *   (request: routerrpc.ISendToRouteRequest): Promise<lnrpc.HTLCAttempt>;
         *   readonly name: "SendToRouteV2";
         *   readonly path: "/routerrpc.Router/SendToRouteV2";
         *   readonly requestType: "SendToRouteRequest";
         *   readonly responseType: "lnrpc.HTLCAttempt";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SendToRouteV2.
         * @name routerrpc.Router#sendToRouteV2
         * @type {routerrpc.Router.SendToRouteV2}
         */
        $Object.defineProperties(Router.prototype.sendToRouteV2 = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.sendToRouteV2, $root.routerrpc.SendToRouteRequest, $root.lnrpc.HTLCAttempt, request, callback);
        }, {
            name: { value: "SendToRouteV2" },
            path: { value: "/routerrpc.Router/SendToRouteV2" },
            requestType: { value: "SendToRouteRequest" },
            responseType: { value: "lnrpc.HTLCAttempt" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#resetMissionControl}.
         * @memberof routerrpc.Router
         * @typedef ResetMissionControlCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.ResetMissionControlResponse} [response] ResetMissionControlResponse
         */

        /**
         * Calls ResetMissionControl.
         * @memberof routerrpc.Router
         * @typedef ResetMissionControl
         * @type {{
         *   (request: routerrpc.IResetMissionControlRequest, callback: routerrpc.Router.ResetMissionControlCallback): void;
         *   (request: routerrpc.IResetMissionControlRequest): Promise<routerrpc.ResetMissionControlResponse>;
         *   readonly name: "ResetMissionControl";
         *   readonly path: "/routerrpc.Router/ResetMissionControl";
         *   readonly requestType: "ResetMissionControlRequest";
         *   readonly responseType: "ResetMissionControlResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ResetMissionControl.
         * @name routerrpc.Router#resetMissionControl
         * @type {routerrpc.Router.ResetMissionControl}
         */
        $Object.defineProperties(Router.prototype.resetMissionControl = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.resetMissionControl, $root.routerrpc.ResetMissionControlRequest, $root.routerrpc.ResetMissionControlResponse, request, callback);
        }, {
            name: { value: "ResetMissionControl" },
            path: { value: "/routerrpc.Router/ResetMissionControl" },
            requestType: { value: "ResetMissionControlRequest" },
            responseType: { value: "ResetMissionControlResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#queryMissionControl}.
         * @memberof routerrpc.Router
         * @typedef QueryMissionControlCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.QueryMissionControlResponse} [response] QueryMissionControlResponse
         */

        /**
         * Calls QueryMissionControl.
         * @memberof routerrpc.Router
         * @typedef QueryMissionControl
         * @type {{
         *   (request: routerrpc.IQueryMissionControlRequest, callback: routerrpc.Router.QueryMissionControlCallback): void;
         *   (request: routerrpc.IQueryMissionControlRequest): Promise<routerrpc.QueryMissionControlResponse>;
         *   readonly name: "QueryMissionControl";
         *   readonly path: "/routerrpc.Router/QueryMissionControl";
         *   readonly requestType: "QueryMissionControlRequest";
         *   readonly responseType: "QueryMissionControlResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls QueryMissionControl.
         * @name routerrpc.Router#queryMissionControl
         * @type {routerrpc.Router.QueryMissionControl}
         */
        $Object.defineProperties(Router.prototype.queryMissionControl = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.queryMissionControl, $root.routerrpc.QueryMissionControlRequest, $root.routerrpc.QueryMissionControlResponse, request, callback);
        }, {
            name: { value: "QueryMissionControl" },
            path: { value: "/routerrpc.Router/QueryMissionControl" },
            requestType: { value: "QueryMissionControlRequest" },
            responseType: { value: "QueryMissionControlResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#xImportMissionControl}.
         * @memberof routerrpc.Router
         * @typedef XImportMissionControlCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.XImportMissionControlResponse} [response] XImportMissionControlResponse
         */

        /**
         * Calls XImportMissionControl.
         * @memberof routerrpc.Router
         * @typedef XImportMissionControl
         * @type {{
         *   (request: routerrpc.IXImportMissionControlRequest, callback: routerrpc.Router.XImportMissionControlCallback): void;
         *   (request: routerrpc.IXImportMissionControlRequest): Promise<routerrpc.XImportMissionControlResponse>;
         *   readonly name: "XImportMissionControl";
         *   readonly path: "/routerrpc.Router/XImportMissionControl";
         *   readonly requestType: "XImportMissionControlRequest";
         *   readonly responseType: "XImportMissionControlResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls XImportMissionControl.
         * @name routerrpc.Router#xImportMissionControl
         * @type {routerrpc.Router.XImportMissionControl}
         */
        $Object.defineProperties(Router.prototype.xImportMissionControl = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.xImportMissionControl, $root.routerrpc.XImportMissionControlRequest, $root.routerrpc.XImportMissionControlResponse, request, callback);
        }, {
            name: { value: "XImportMissionControl" },
            path: { value: "/routerrpc.Router/XImportMissionControl" },
            requestType: { value: "XImportMissionControlRequest" },
            responseType: { value: "XImportMissionControlResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#getMissionControlConfig}.
         * @memberof routerrpc.Router
         * @typedef GetMissionControlConfigCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.GetMissionControlConfigResponse} [response] GetMissionControlConfigResponse
         */

        /**
         * Calls GetMissionControlConfig.
         * @memberof routerrpc.Router
         * @typedef GetMissionControlConfig
         * @type {{
         *   (request: routerrpc.IGetMissionControlConfigRequest, callback: routerrpc.Router.GetMissionControlConfigCallback): void;
         *   (request: routerrpc.IGetMissionControlConfigRequest): Promise<routerrpc.GetMissionControlConfigResponse>;
         *   readonly name: "GetMissionControlConfig";
         *   readonly path: "/routerrpc.Router/GetMissionControlConfig";
         *   readonly requestType: "GetMissionControlConfigRequest";
         *   readonly responseType: "GetMissionControlConfigResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetMissionControlConfig.
         * @name routerrpc.Router#getMissionControlConfig
         * @type {routerrpc.Router.GetMissionControlConfig}
         */
        $Object.defineProperties(Router.prototype.getMissionControlConfig = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.getMissionControlConfig, $root.routerrpc.GetMissionControlConfigRequest, $root.routerrpc.GetMissionControlConfigResponse, request, callback);
        }, {
            name: { value: "GetMissionControlConfig" },
            path: { value: "/routerrpc.Router/GetMissionControlConfig" },
            requestType: { value: "GetMissionControlConfigRequest" },
            responseType: { value: "GetMissionControlConfigResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#setMissionControlConfig}.
         * @memberof routerrpc.Router
         * @typedef SetMissionControlConfigCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.SetMissionControlConfigResponse} [response] SetMissionControlConfigResponse
         */

        /**
         * Calls SetMissionControlConfig.
         * @memberof routerrpc.Router
         * @typedef SetMissionControlConfig
         * @type {{
         *   (request: routerrpc.ISetMissionControlConfigRequest, callback: routerrpc.Router.SetMissionControlConfigCallback): void;
         *   (request: routerrpc.ISetMissionControlConfigRequest): Promise<routerrpc.SetMissionControlConfigResponse>;
         *   readonly name: "SetMissionControlConfig";
         *   readonly path: "/routerrpc.Router/SetMissionControlConfig";
         *   readonly requestType: "SetMissionControlConfigRequest";
         *   readonly responseType: "SetMissionControlConfigResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SetMissionControlConfig.
         * @name routerrpc.Router#setMissionControlConfig
         * @type {routerrpc.Router.SetMissionControlConfig}
         */
        $Object.defineProperties(Router.prototype.setMissionControlConfig = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.setMissionControlConfig, $root.routerrpc.SetMissionControlConfigRequest, $root.routerrpc.SetMissionControlConfigResponse, request, callback);
        }, {
            name: { value: "SetMissionControlConfig" },
            path: { value: "/routerrpc.Router/SetMissionControlConfig" },
            requestType: { value: "SetMissionControlConfigRequest" },
            responseType: { value: "SetMissionControlConfigResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#queryProbability}.
         * @memberof routerrpc.Router
         * @typedef QueryProbabilityCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.QueryProbabilityResponse} [response] QueryProbabilityResponse
         */

        /**
         * Calls QueryProbability.
         * @memberof routerrpc.Router
         * @typedef QueryProbability
         * @type {{
         *   (request: routerrpc.IQueryProbabilityRequest, callback: routerrpc.Router.QueryProbabilityCallback): void;
         *   (request: routerrpc.IQueryProbabilityRequest): Promise<routerrpc.QueryProbabilityResponse>;
         *   readonly name: "QueryProbability";
         *   readonly path: "/routerrpc.Router/QueryProbability";
         *   readonly requestType: "QueryProbabilityRequest";
         *   readonly responseType: "QueryProbabilityResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls QueryProbability.
         * @name routerrpc.Router#queryProbability
         * @type {routerrpc.Router.QueryProbability}
         */
        $Object.defineProperties(Router.prototype.queryProbability = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.queryProbability, $root.routerrpc.QueryProbabilityRequest, $root.routerrpc.QueryProbabilityResponse, request, callback);
        }, {
            name: { value: "QueryProbability" },
            path: { value: "/routerrpc.Router/QueryProbability" },
            requestType: { value: "QueryProbabilityRequest" },
            responseType: { value: "QueryProbabilityResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#buildRoute}.
         * @memberof routerrpc.Router
         * @typedef BuildRouteCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.BuildRouteResponse} [response] BuildRouteResponse
         */

        /**
         * Calls BuildRoute.
         * @memberof routerrpc.Router
         * @typedef BuildRoute
         * @type {{
         *   (request: routerrpc.IBuildRouteRequest, callback: routerrpc.Router.BuildRouteCallback): void;
         *   (request: routerrpc.IBuildRouteRequest): Promise<routerrpc.BuildRouteResponse>;
         *   readonly name: "BuildRoute";
         *   readonly path: "/routerrpc.Router/BuildRoute";
         *   readonly requestType: "BuildRouteRequest";
         *   readonly responseType: "BuildRouteResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls BuildRoute.
         * @name routerrpc.Router#buildRoute
         * @type {routerrpc.Router.BuildRoute}
         */
        $Object.defineProperties(Router.prototype.buildRoute = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.buildRoute, $root.routerrpc.BuildRouteRequest, $root.routerrpc.BuildRouteResponse, request, callback);
        }, {
            name: { value: "BuildRoute" },
            path: { value: "/routerrpc.Router/BuildRoute" },
            requestType: { value: "BuildRouteRequest" },
            responseType: { value: "BuildRouteResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#subscribeHtlcEvents}.
         * @memberof routerrpc.Router
         * @typedef SubscribeHtlcEventsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.HtlcEvent} [response] HtlcEvent
         */

        /**
         * Calls SubscribeHtlcEvents.
         * @memberof routerrpc.Router
         * @typedef SubscribeHtlcEvents
         * @type {{
         *   (request: routerrpc.ISubscribeHtlcEventsRequest, callback: routerrpc.Router.SubscribeHtlcEventsCallback): void;
         *   (request: routerrpc.ISubscribeHtlcEventsRequest): Promise<routerrpc.HtlcEvent>;
         *   readonly name: "SubscribeHtlcEvents";
         *   readonly path: "/routerrpc.Router/SubscribeHtlcEvents";
         *   readonly requestType: "SubscribeHtlcEventsRequest";
         *   readonly responseType: "HtlcEvent";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SubscribeHtlcEvents.
         * @name routerrpc.Router#subscribeHtlcEvents
         * @type {routerrpc.Router.SubscribeHtlcEvents}
         */
        $Object.defineProperties(Router.prototype.subscribeHtlcEvents = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.subscribeHtlcEvents, $root.routerrpc.SubscribeHtlcEventsRequest, $root.routerrpc.HtlcEvent, request, callback);
        }, {
            name: { value: "SubscribeHtlcEvents" },
            path: { value: "/routerrpc.Router/SubscribeHtlcEvents" },
            requestType: { value: "SubscribeHtlcEventsRequest" },
            responseType: { value: "HtlcEvent" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link routerrpc.Router#sendPayment}.
         * @memberof routerrpc.Router
         * @typedef SendPaymentCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.PaymentStatus} [response] PaymentStatus
         */

        /**
         * Calls SendPayment.
         * @memberof routerrpc.Router
         * @typedef SendPayment
         * @type {{
         *   (request: routerrpc.ISendPaymentRequest, callback: routerrpc.Router.SendPaymentCallback): void;
         *   (request: routerrpc.ISendPaymentRequest): Promise<routerrpc.PaymentStatus>;
         *   readonly name: "SendPayment";
         *   readonly path: "/routerrpc.Router/SendPayment";
         *   readonly requestType: "SendPaymentRequest";
         *   readonly responseType: "PaymentStatus";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls SendPayment.
         * @name routerrpc.Router#sendPayment
         * @type {routerrpc.Router.SendPayment}
         */
        $Object.defineProperties(Router.prototype.sendPayment = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.sendPayment, $root.routerrpc.SendPaymentRequest, $root.routerrpc.PaymentStatus, request, callback);
        }, {
            name: { value: "SendPayment" },
            path: { value: "/routerrpc.Router/SendPayment" },
            requestType: { value: "SendPaymentRequest" },
            responseType: { value: "PaymentStatus" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link routerrpc.Router#trackPayment}.
         * @memberof routerrpc.Router
         * @typedef TrackPaymentCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.PaymentStatus} [response] PaymentStatus
         */

        /**
         * Calls TrackPayment.
         * @memberof routerrpc.Router
         * @typedef TrackPayment
         * @type {{
         *   (request: routerrpc.ITrackPaymentRequest, callback: routerrpc.Router.TrackPaymentCallback): void;
         *   (request: routerrpc.ITrackPaymentRequest): Promise<routerrpc.PaymentStatus>;
         *   readonly name: "TrackPayment";
         *   readonly path: "/routerrpc.Router/TrackPayment";
         *   readonly requestType: "TrackPaymentRequest";
         *   readonly responseType: "PaymentStatus";
         *   readonly requestStream: undefined;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * Calls TrackPayment.
         * @name routerrpc.Router#trackPayment
         * @type {routerrpc.Router.TrackPayment}
         */
        $Object.defineProperties(Router.prototype.trackPayment = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.trackPayment, $root.routerrpc.TrackPaymentRequest, $root.routerrpc.PaymentStatus, request, callback);
        }, {
            name: { value: "TrackPayment" },
            path: { value: "/routerrpc.Router/TrackPayment" },
            requestType: { value: "TrackPaymentRequest" },
            responseType: { value: "PaymentStatus" },
            requestStream: { value: $undefined },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link routerrpc.Router#htlcInterceptor}.
         * @memberof routerrpc.Router
         * @typedef HtlcInterceptorCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.ForwardHtlcInterceptRequest} [response] ForwardHtlcInterceptRequest
         */

        /**
         * HtlcInterceptor dispatches a bi-directional streaming RPC in which
         * Forwarded HTLC requests are sent to the client and the client responds with
         * a boolean that tells LND if this htlc should be intercepted.
         * In case of interception, the htlc can be either settled, cancelled or
         * resumed later by using the ResolveHoldForward endpoint.
         * @memberof routerrpc.Router
         * @typedef HtlcInterceptor
         * @type {{
         *   (request: routerrpc.IForwardHtlcInterceptResponse, callback: routerrpc.Router.HtlcInterceptorCallback): void;
         *   (request: routerrpc.IForwardHtlcInterceptResponse): Promise<routerrpc.ForwardHtlcInterceptRequest>;
         *   readonly name: "HtlcInterceptor";
         *   readonly path: "/routerrpc.Router/HtlcInterceptor";
         *   readonly requestType: "ForwardHtlcInterceptResponse";
         *   readonly responseType: "ForwardHtlcInterceptRequest";
         *   readonly requestStream: true;
         *   readonly responseStream: true;
         * }}
         */

        /**
         * HtlcInterceptor dispatches a bi-directional streaming RPC in which
         * Forwarded HTLC requests are sent to the client and the client responds with
         * a boolean that tells LND if this htlc should be intercepted.
         * In case of interception, the htlc can be either settled, cancelled or
         * resumed later by using the ResolveHoldForward endpoint.
         * @name routerrpc.Router#htlcInterceptor
         * @type {routerrpc.Router.HtlcInterceptor}
         */
        $Object.defineProperties(Router.prototype.htlcInterceptor = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.htlcInterceptor, $root.routerrpc.ForwardHtlcInterceptResponse, $root.routerrpc.ForwardHtlcInterceptRequest, request, callback);
        }, {
            name: { value: "HtlcInterceptor" },
            path: { value: "/routerrpc.Router/HtlcInterceptor" },
            requestType: { value: "ForwardHtlcInterceptResponse" },
            responseType: { value: "ForwardHtlcInterceptRequest" },
            requestStream: { value: true },
            responseStream: { value: true }
        });

        /**
         * Callback as used by {@link routerrpc.Router#updateChanStatus}.
         * @memberof routerrpc.Router
         * @typedef UpdateChanStatusCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.UpdateChanStatusResponse} [response] UpdateChanStatusResponse
         */

        /**
         * Calls UpdateChanStatus.
         * @memberof routerrpc.Router
         * @typedef UpdateChanStatus
         * @type {{
         *   (request: routerrpc.IUpdateChanStatusRequest, callback: routerrpc.Router.UpdateChanStatusCallback): void;
         *   (request: routerrpc.IUpdateChanStatusRequest): Promise<routerrpc.UpdateChanStatusResponse>;
         *   readonly name: "UpdateChanStatus";
         *   readonly path: "/routerrpc.Router/UpdateChanStatus";
         *   readonly requestType: "UpdateChanStatusRequest";
         *   readonly responseType: "UpdateChanStatusResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls UpdateChanStatus.
         * @name routerrpc.Router#updateChanStatus
         * @type {routerrpc.Router.UpdateChanStatus}
         */
        $Object.defineProperties(Router.prototype.updateChanStatus = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.updateChanStatus, $root.routerrpc.UpdateChanStatusRequest, $root.routerrpc.UpdateChanStatusResponse, request, callback);
        }, {
            name: { value: "UpdateChanStatus" },
            path: { value: "/routerrpc.Router/UpdateChanStatus" },
            requestType: { value: "UpdateChanStatusRequest" },
            responseType: { value: "UpdateChanStatusResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#xAddLocalChanAliases}.
         * @memberof routerrpc.Router
         * @typedef XAddLocalChanAliasesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.AddAliasesResponse} [response] AddAliasesResponse
         */

        /**
         * Calls XAddLocalChanAliases.
         * @memberof routerrpc.Router
         * @typedef XAddLocalChanAliases
         * @type {{
         *   (request: routerrpc.IAddAliasesRequest, callback: routerrpc.Router.XAddLocalChanAliasesCallback): void;
         *   (request: routerrpc.IAddAliasesRequest): Promise<routerrpc.AddAliasesResponse>;
         *   readonly name: "XAddLocalChanAliases";
         *   readonly path: "/routerrpc.Router/XAddLocalChanAliases";
         *   readonly requestType: "AddAliasesRequest";
         *   readonly responseType: "AddAliasesResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls XAddLocalChanAliases.
         * @name routerrpc.Router#xAddLocalChanAliases
         * @type {routerrpc.Router.XAddLocalChanAliases}
         */
        $Object.defineProperties(Router.prototype.xAddLocalChanAliases = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.xAddLocalChanAliases, $root.routerrpc.AddAliasesRequest, $root.routerrpc.AddAliasesResponse, request, callback);
        }, {
            name: { value: "XAddLocalChanAliases" },
            path: { value: "/routerrpc.Router/XAddLocalChanAliases" },
            requestType: { value: "AddAliasesRequest" },
            responseType: { value: "AddAliasesResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#xDeleteLocalChanAliases}.
         * @memberof routerrpc.Router
         * @typedef XDeleteLocalChanAliasesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.DeleteAliasesResponse} [response] DeleteAliasesResponse
         */

        /**
         * Calls XDeleteLocalChanAliases.
         * @memberof routerrpc.Router
         * @typedef XDeleteLocalChanAliases
         * @type {{
         *   (request: routerrpc.IDeleteAliasesRequest, callback: routerrpc.Router.XDeleteLocalChanAliasesCallback): void;
         *   (request: routerrpc.IDeleteAliasesRequest): Promise<routerrpc.DeleteAliasesResponse>;
         *   readonly name: "XDeleteLocalChanAliases";
         *   readonly path: "/routerrpc.Router/XDeleteLocalChanAliases";
         *   readonly requestType: "DeleteAliasesRequest";
         *   readonly responseType: "DeleteAliasesResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls XDeleteLocalChanAliases.
         * @name routerrpc.Router#xDeleteLocalChanAliases
         * @type {routerrpc.Router.XDeleteLocalChanAliases}
         */
        $Object.defineProperties(Router.prototype.xDeleteLocalChanAliases = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.xDeleteLocalChanAliases, $root.routerrpc.DeleteAliasesRequest, $root.routerrpc.DeleteAliasesResponse, request, callback);
        }, {
            name: { value: "XDeleteLocalChanAliases" },
            path: { value: "/routerrpc.Router/XDeleteLocalChanAliases" },
            requestType: { value: "DeleteAliasesRequest" },
            responseType: { value: "DeleteAliasesResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link routerrpc.Router#xFindBaseLocalChanAlias}.
         * @memberof routerrpc.Router
         * @typedef XFindBaseLocalChanAliasCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {routerrpc.FindBaseAliasResponse} [response] FindBaseAliasResponse
         */

        /**
         * Calls XFindBaseLocalChanAlias.
         * @memberof routerrpc.Router
         * @typedef XFindBaseLocalChanAlias
         * @type {{
         *   (request: routerrpc.IFindBaseAliasRequest, callback: routerrpc.Router.XFindBaseLocalChanAliasCallback): void;
         *   (request: routerrpc.IFindBaseAliasRequest): Promise<routerrpc.FindBaseAliasResponse>;
         *   readonly name: "XFindBaseLocalChanAlias";
         *   readonly path: "/routerrpc.Router/XFindBaseLocalChanAlias";
         *   readonly requestType: "FindBaseAliasRequest";
         *   readonly responseType: "FindBaseAliasResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls XFindBaseLocalChanAlias.
         * @name routerrpc.Router#xFindBaseLocalChanAlias
         * @type {routerrpc.Router.XFindBaseLocalChanAlias}
         */
        $Object.defineProperties(Router.prototype.xFindBaseLocalChanAlias = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Router.prototype.xFindBaseLocalChanAlias, $root.routerrpc.FindBaseAliasRequest, $root.routerrpc.FindBaseAliasResponse, request, callback);
        }, {
            name: { value: "XFindBaseLocalChanAlias" },
            path: { value: "/routerrpc.Router/XFindBaseLocalChanAlias" },
            requestType: { value: "FindBaseAliasRequest" },
            responseType: { value: "FindBaseAliasResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return Router;
    })();

    routerrpc.SendPaymentRequest = (function() {

        /**
         * Properties of a SendPaymentRequest.
         * @typedef {Object} routerrpc.SendPaymentRequest.$Properties
         * @property {Uint8Array|null} [dest] SendPaymentRequest dest
         * @property {Long|null} [amt] SendPaymentRequest amt
         * @property {Uint8Array|null} [payment_hash] SendPaymentRequest payment_hash
         * @property {number|null} [final_cltv_delta] SendPaymentRequest final_cltv_delta
         * @property {string|null} [payment_request] SendPaymentRequest payment_request
         * @property {number|null} [timeout_seconds] SendPaymentRequest timeout_seconds
         * @property {Long|null} [fee_limit_sat] SendPaymentRequest fee_limit_sat
         * @property {Long|null} [outgoing_chan_id] SendPaymentRequest outgoing_chan_id
         * @property {number|null} [cltv_limit] SendPaymentRequest cltv_limit
         * @property {Array.<lnrpc.RouteHint.$Properties>|null} [route_hints] SendPaymentRequest route_hints
         * @property {Object.<string,Uint8Array>|null} [dest_custom_records] SendPaymentRequest dest_custom_records
         * @property {Long|null} [amt_msat] SendPaymentRequest amt_msat
         * @property {Long|null} [fee_limit_msat] SendPaymentRequest fee_limit_msat
         * @property {Uint8Array|null} [last_hop_pubkey] SendPaymentRequest last_hop_pubkey
         * @property {boolean|null} [allow_self_payment] SendPaymentRequest allow_self_payment
         * @property {Array.<lnrpc.FeatureBit>|null} [dest_features] SendPaymentRequest dest_features
         * @property {number|null} [max_parts] SendPaymentRequest max_parts
         * @property {boolean|null} [no_inflight_updates] SendPaymentRequest no_inflight_updates
         * @property {Array.<Long>|null} [outgoing_chan_ids] SendPaymentRequest outgoing_chan_ids
         * @property {Uint8Array|null} [payment_addr] SendPaymentRequest payment_addr
         * @property {Long|null} [max_shard_size_msat] SendPaymentRequest max_shard_size_msat
         * @property {boolean|null} [amp] SendPaymentRequest amp
         * @property {number|null} [time_pref] SendPaymentRequest time_pref
         * @property {boolean|null} [cancelable] SendPaymentRequest cancelable
         * @property {Object.<string,Uint8Array>|null} [first_hop_custom_records] SendPaymentRequest first_hop_custom_records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendPaymentRequest.
         * @memberof routerrpc
         * @interface ISendPaymentRequest
         * @augments routerrpc.SendPaymentRequest.$Properties
         * @deprecated Use routerrpc.SendPaymentRequest.$Properties instead.
         */

        /**
         * Shape of a SendPaymentRequest.
         * @typedef {routerrpc.SendPaymentRequest.$Properties} routerrpc.SendPaymentRequest.$Shape
         */

        /**
         * Constructs a new SendPaymentRequest.
         * @memberof routerrpc
         * @classdesc Represents a SendPaymentRequest.
         * @constructor
         * @param {routerrpc.SendPaymentRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendPaymentRequest = function (properties) {
            this.route_hints = [];
            this.dest_custom_records = {};
            this.dest_features = [];
            this.outgoing_chan_ids = [];
            this.first_hop_custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendPaymentRequest dest.
         * @member {Uint8Array} dest
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.dest = $util.newBuffer([]);

        /**
         * SendPaymentRequest amt.
         * @member {Long} amt
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.amt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendPaymentRequest payment_hash.
         * @member {Uint8Array} payment_hash
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.payment_hash = $util.newBuffer([]);

        /**
         * SendPaymentRequest final_cltv_delta.
         * @member {number} final_cltv_delta
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.final_cltv_delta = 0;

        /**
         * SendPaymentRequest payment_request.
         * @member {string} payment_request
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.payment_request = "";

        /**
         * SendPaymentRequest timeout_seconds.
         * @member {number} timeout_seconds
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.timeout_seconds = 0;

        /**
         * SendPaymentRequest fee_limit_sat.
         * @member {Long} fee_limit_sat
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.fee_limit_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendPaymentRequest outgoing_chan_id.
         * @member {Long} outgoing_chan_id
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.outgoing_chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * SendPaymentRequest cltv_limit.
         * @member {number} cltv_limit
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.cltv_limit = 0;

        /**
         * SendPaymentRequest route_hints.
         * @member {Array.<lnrpc.RouteHint.$Properties>} route_hints
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.route_hints = $util.emptyArray;

        /**
         * SendPaymentRequest dest_custom_records.
         * @member {Object.<string,Uint8Array>} dest_custom_records
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.dest_custom_records = $util.emptyObject;

        /**
         * SendPaymentRequest amt_msat.
         * @member {Long} amt_msat
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.amt_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendPaymentRequest fee_limit_msat.
         * @member {Long} fee_limit_msat
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.fee_limit_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendPaymentRequest last_hop_pubkey.
         * @member {Uint8Array} last_hop_pubkey
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.last_hop_pubkey = $util.newBuffer([]);

        /**
         * SendPaymentRequest allow_self_payment.
         * @member {boolean} allow_self_payment
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.allow_self_payment = false;

        /**
         * SendPaymentRequest dest_features.
         * @member {Array.<lnrpc.FeatureBit>} dest_features
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.dest_features = $util.emptyArray;

        /**
         * SendPaymentRequest max_parts.
         * @member {number} max_parts
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.max_parts = 0;

        /**
         * SendPaymentRequest no_inflight_updates.
         * @member {boolean} no_inflight_updates
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.no_inflight_updates = false;

        /**
         * SendPaymentRequest outgoing_chan_ids.
         * @member {Array.<Long>} outgoing_chan_ids
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.outgoing_chan_ids = $util.emptyArray;

        /**
         * SendPaymentRequest payment_addr.
         * @member {Uint8Array} payment_addr
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.payment_addr = $util.newBuffer([]);

        /**
         * SendPaymentRequest max_shard_size_msat.
         * @member {Long} max_shard_size_msat
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.max_shard_size_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * SendPaymentRequest amp.
         * @member {boolean} amp
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.amp = false;

        /**
         * SendPaymentRequest time_pref.
         * @member {number} time_pref
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.time_pref = 0;

        /**
         * SendPaymentRequest cancelable.
         * @member {boolean} cancelable
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.cancelable = false;

        /**
         * SendPaymentRequest first_hop_custom_records.
         * @member {Object.<string,Uint8Array>} first_hop_custom_records
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         */
        SendPaymentRequest.prototype.first_hop_custom_records = $util.emptyObject;

        /**
         * Creates a new SendPaymentRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.SendPaymentRequest
         * @static
         * @param {routerrpc.SendPaymentRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.SendPaymentRequest} SendPaymentRequest instance
         * @type {{
         *   (properties: routerrpc.SendPaymentRequest.$Shape): routerrpc.SendPaymentRequest & routerrpc.SendPaymentRequest.$Shape;
         *   (properties?: routerrpc.SendPaymentRequest.$Properties): routerrpc.SendPaymentRequest;
         * }}
         */
        SendPaymentRequest.create = function(properties) {
            return new SendPaymentRequest(properties);
        };

        /**
         * Encodes the specified SendPaymentRequest message. Does not implicitly {@link routerrpc.SendPaymentRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.SendPaymentRequest
         * @static
         * @param {routerrpc.SendPaymentRequest.$Properties} message SendPaymentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendPaymentRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.dest != null && $Object.hasOwnProperty.call(message, "dest") && message.dest.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.dest);
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt") && (typeof message.amt === "object" ? message.amt.low || message.amt.high : message.amt !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amt);
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.payment_hash);
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta") && message.final_cltv_delta !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.final_cltv_delta);
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request") && message.payment_request !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.payment_request);
            if (message.timeout_seconds != null && $Object.hasOwnProperty.call(message, "timeout_seconds") && message.timeout_seconds !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.timeout_seconds);
            if (message.fee_limit_sat != null && $Object.hasOwnProperty.call(message, "fee_limit_sat") && (typeof message.fee_limit_sat === "object" ? message.fee_limit_sat.low || message.fee_limit_sat.high : message.fee_limit_sat !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.fee_limit_sat);
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id") && (typeof message.outgoing_chan_id === "object" ? message.outgoing_chan_id.low || message.outgoing_chan_id.high : message.outgoing_chan_id !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.outgoing_chan_id);
            if (message.cltv_limit != null && $Object.hasOwnProperty.call(message, "cltv_limit") && message.cltv_limit !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.cltv_limit);
            if (message.route_hints != null && message.route_hints.length)
                for (let i = 0; i < message.route_hints.length; ++i)
                    $root.lnrpc.RouteHint.encode(message.route_hints[i], writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.dest_custom_records != null && $Object.hasOwnProperty.call(message, "dest_custom_records"))
                for (let keys = $Object.keys(message.dest_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 11, wireType 2 =*/90).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.dest_custom_records[keys[i]]).ldelim();
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat") && (typeof message.amt_msat === "object" ? message.amt_msat.low || message.amt_msat.high : message.amt_msat !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.amt_msat);
            if (message.fee_limit_msat != null && $Object.hasOwnProperty.call(message, "fee_limit_msat") && (typeof message.fee_limit_msat === "object" ? message.fee_limit_msat.low || message.fee_limit_msat.high : message.fee_limit_msat !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.fee_limit_msat);
            if (message.last_hop_pubkey != null && $Object.hasOwnProperty.call(message, "last_hop_pubkey") && message.last_hop_pubkey.length)
                writer.uint32(/* id 14, wireType 2 =*/114).bytes(message.last_hop_pubkey);
            if (message.allow_self_payment != null && $Object.hasOwnProperty.call(message, "allow_self_payment") && message.allow_self_payment !== false)
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.allow_self_payment);
            if (message.dest_features != null && message.dest_features.length)
                writer.uint32(/* id 16, wireType 2 =*/130).int32s(message.dest_features);
            if (message.max_parts != null && $Object.hasOwnProperty.call(message, "max_parts") && message.max_parts !== 0)
                writer.uint32(/* id 17, wireType 0 =*/136).uint32(message.max_parts);
            if (message.no_inflight_updates != null && $Object.hasOwnProperty.call(message, "no_inflight_updates") && message.no_inflight_updates !== false)
                writer.uint32(/* id 18, wireType 0 =*/144).bool(message.no_inflight_updates);
            if (message.outgoing_chan_ids != null && message.outgoing_chan_ids.length)
                writer.uint32(/* id 19, wireType 2 =*/154).uint64s(message.outgoing_chan_ids);
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr") && message.payment_addr.length)
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.payment_addr);
            if (message.max_shard_size_msat != null && $Object.hasOwnProperty.call(message, "max_shard_size_msat") && (typeof message.max_shard_size_msat === "object" ? message.max_shard_size_msat.low || message.max_shard_size_msat.high : message.max_shard_size_msat !== 0))
                writer.uint32(/* id 21, wireType 0 =*/168).uint64(message.max_shard_size_msat);
            if (message.amp != null && $Object.hasOwnProperty.call(message, "amp") && message.amp !== false)
                writer.uint32(/* id 22, wireType 0 =*/176).bool(message.amp);
            if (message.time_pref != null && $Object.hasOwnProperty.call(message, "time_pref") && !$Object.is(message.time_pref, 0))
                writer.uint32(/* id 23, wireType 1 =*/185).double(message.time_pref);
            if (message.cancelable != null && $Object.hasOwnProperty.call(message, "cancelable") && message.cancelable !== false)
                writer.uint32(/* id 24, wireType 0 =*/192).bool(message.cancelable);
            if (message.first_hop_custom_records != null && $Object.hasOwnProperty.call(message, "first_hop_custom_records"))
                for (let keys = $Object.keys(message.first_hop_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 25, wireType 2 =*/202).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.first_hop_custom_records[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendPaymentRequest message, length delimited. Does not implicitly {@link routerrpc.SendPaymentRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.SendPaymentRequest
         * @static
         * @param {routerrpc.SendPaymentRequest.$Properties} message SendPaymentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendPaymentRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendPaymentRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.SendPaymentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.SendPaymentRequest & routerrpc.SendPaymentRequest.$Shape} SendPaymentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendPaymentRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.SendPaymentRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.dest = value;
                        else
                            delete message.dest;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt = value;
                        else
                            delete message.amt;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.final_cltv_delta = value;
                        else
                            delete message.final_cltv_delta;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_request = value;
                        else
                            delete message.payment_request;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.timeout_seconds = value;
                        else
                            delete message.timeout_seconds;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_limit_sat = value;
                        else
                            delete message.fee_limit_sat;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.outgoing_chan_id = value;
                        else
                            delete message.outgoing_chan_id;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.cltv_limit = value;
                        else
                            delete message.cltv_limit;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.route_hints && message.route_hints.length))
                            message.route_hints = [];
                        message.route_hints.push($root.lnrpc.RouteHint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (message.dest_custom_records === $util.emptyObject)
                            message.dest_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.dest_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_msat = value;
                        else
                            delete message.amt_msat;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fee_limit_msat = value;
                        else
                            delete message.fee_limit_msat;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.last_hop_pubkey = value;
                        else
                            delete message.last_hop_pubkey;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.allow_self_payment = value;
                        else
                            delete message.allow_self_payment;
                        continue;
                    }
                case 16: {
                        if (wireType === 2) {
                            if (!(message.dest_features && message.dest_features.length))
                                message.dest_features = [];
                            reader.int32s(message.dest_features);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.dest_features && message.dest_features.length))
                            message.dest_features = [];
                        message.dest_features.push(reader.int32());
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_parts = value;
                        else
                            delete message.max_parts;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.no_inflight_updates = value;
                        else
                            delete message.no_inflight_updates;
                        continue;
                    }
                case 19: {
                        if (wireType === 2) {
                            if (!(message.outgoing_chan_ids && message.outgoing_chan_ids.length))
                                message.outgoing_chan_ids = [];
                            reader.uint64s(message.outgoing_chan_ids);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.outgoing_chan_ids && message.outgoing_chan_ids.length))
                            message.outgoing_chan_ids = [];
                        message.outgoing_chan_ids.push(reader.uint64());
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_addr = value;
                        else
                            delete message.payment_addr;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_shard_size_msat = value;
                        else
                            delete message.max_shard_size_msat;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.amp = value;
                        else
                            delete message.amp;
                        continue;
                    }
                case 23: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.time_pref = value;
                        else
                            delete message.time_pref;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cancelable = value;
                        else
                            delete message.cancelable;
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        if (message.first_hop_custom_records === $util.emptyObject)
                            message.first_hop_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.first_hop_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendPaymentRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.SendPaymentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.SendPaymentRequest & routerrpc.SendPaymentRequest.$Shape} SendPaymentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendPaymentRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendPaymentRequest message.
         * @function verify
         * @memberof routerrpc.SendPaymentRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendPaymentRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dest != null && $Object.hasOwnProperty.call(message, "dest"))
                if (!(message.dest && typeof message.dest.length === "number" || $util.isString(message.dest)))
                    return "dest: buffer expected";
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt"))
                if (!$util.isInteger(message.amt) && !(message.amt && $util.isInteger(message.amt.low) && $util.isInteger(message.amt.high)))
                    return "amt: integer|Long expected";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta"))
                if (!$util.isInteger(message.final_cltv_delta))
                    return "final_cltv_delta: integer expected";
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                if (!$util.isString(message.payment_request))
                    return "payment_request: string expected";
            if (message.timeout_seconds != null && $Object.hasOwnProperty.call(message, "timeout_seconds"))
                if (!$util.isInteger(message.timeout_seconds))
                    return "timeout_seconds: integer expected";
            if (message.fee_limit_sat != null && $Object.hasOwnProperty.call(message, "fee_limit_sat"))
                if (!$util.isInteger(message.fee_limit_sat) && !(message.fee_limit_sat && $util.isInteger(message.fee_limit_sat.low) && $util.isInteger(message.fee_limit_sat.high)))
                    return "fee_limit_sat: integer|Long expected";
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id"))
                if (!$util.isInteger(message.outgoing_chan_id) && !(message.outgoing_chan_id && $util.isInteger(message.outgoing_chan_id.low) && $util.isInteger(message.outgoing_chan_id.high)))
                    return "outgoing_chan_id: integer|Long expected";
            if (message.cltv_limit != null && $Object.hasOwnProperty.call(message, "cltv_limit"))
                if (!$util.isInteger(message.cltv_limit))
                    return "cltv_limit: integer expected";
            if (message.route_hints != null && $Object.hasOwnProperty.call(message, "route_hints")) {
                if (!$Array.isArray(message.route_hints))
                    return "route_hints: array expected";
                for (let i = 0; i < message.route_hints.length; ++i) {
                    let error = $root.lnrpc.RouteHint.verify(message.route_hints[i], _depth + 1);
                    if (error)
                        return "route_hints." + error;
                }
            }
            if (message.dest_custom_records != null && $Object.hasOwnProperty.call(message, "dest_custom_records")) {
                if (!$util.isObject(message.dest_custom_records))
                    return "dest_custom_records: object expected";
                let key = $Object.keys(message.dest_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "dest_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.dest_custom_records[key[i]] && typeof message.dest_custom_records[key[i]].length === "number" || $util.isString(message.dest_custom_records[key[i]])))
                        return "dest_custom_records: buffer{k:uint64} expected";
                }
            }
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (!$util.isInteger(message.amt_msat) && !(message.amt_msat && $util.isInteger(message.amt_msat.low) && $util.isInteger(message.amt_msat.high)))
                    return "amt_msat: integer|Long expected";
            if (message.fee_limit_msat != null && $Object.hasOwnProperty.call(message, "fee_limit_msat"))
                if (!$util.isInteger(message.fee_limit_msat) && !(message.fee_limit_msat && $util.isInteger(message.fee_limit_msat.low) && $util.isInteger(message.fee_limit_msat.high)))
                    return "fee_limit_msat: integer|Long expected";
            if (message.last_hop_pubkey != null && $Object.hasOwnProperty.call(message, "last_hop_pubkey"))
                if (!(message.last_hop_pubkey && typeof message.last_hop_pubkey.length === "number" || $util.isString(message.last_hop_pubkey)))
                    return "last_hop_pubkey: buffer expected";
            if (message.allow_self_payment != null && $Object.hasOwnProperty.call(message, "allow_self_payment"))
                if (typeof message.allow_self_payment !== "boolean")
                    return "allow_self_payment: boolean expected";
            if (message.dest_features != null && $Object.hasOwnProperty.call(message, "dest_features")) {
                if (!$Array.isArray(message.dest_features))
                    return "dest_features: array expected";
                for (let i = 0; i < message.dest_features.length; ++i)
                    if (typeof message.dest_features[i] !== "number" || (message.dest_features[i] | 0) !== message.dest_features[i])
                        return "dest_features: enum value[] expected";
            }
            if (message.max_parts != null && $Object.hasOwnProperty.call(message, "max_parts"))
                if (!$util.isInteger(message.max_parts))
                    return "max_parts: integer expected";
            if (message.no_inflight_updates != null && $Object.hasOwnProperty.call(message, "no_inflight_updates"))
                if (typeof message.no_inflight_updates !== "boolean")
                    return "no_inflight_updates: boolean expected";
            if (message.outgoing_chan_ids != null && $Object.hasOwnProperty.call(message, "outgoing_chan_ids")) {
                if (!$Array.isArray(message.outgoing_chan_ids))
                    return "outgoing_chan_ids: array expected";
                for (let i = 0; i < message.outgoing_chan_ids.length; ++i)
                    if (!$util.isInteger(message.outgoing_chan_ids[i]) && !(message.outgoing_chan_ids[i] && $util.isInteger(message.outgoing_chan_ids[i].low) && $util.isInteger(message.outgoing_chan_ids[i].high)))
                        return "outgoing_chan_ids: integer|Long[] expected";
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                if (!(message.payment_addr && typeof message.payment_addr.length === "number" || $util.isString(message.payment_addr)))
                    return "payment_addr: buffer expected";
            if (message.max_shard_size_msat != null && $Object.hasOwnProperty.call(message, "max_shard_size_msat"))
                if (!$util.isInteger(message.max_shard_size_msat) && !(message.max_shard_size_msat && $util.isInteger(message.max_shard_size_msat.low) && $util.isInteger(message.max_shard_size_msat.high)))
                    return "max_shard_size_msat: integer|Long expected";
            if (message.amp != null && $Object.hasOwnProperty.call(message, "amp"))
                if (typeof message.amp !== "boolean")
                    return "amp: boolean expected";
            if (message.time_pref != null && $Object.hasOwnProperty.call(message, "time_pref"))
                if (typeof message.time_pref !== "number")
                    return "time_pref: number expected";
            if (message.cancelable != null && $Object.hasOwnProperty.call(message, "cancelable"))
                if (typeof message.cancelable !== "boolean")
                    return "cancelable: boolean expected";
            if (message.first_hop_custom_records != null && $Object.hasOwnProperty.call(message, "first_hop_custom_records")) {
                if (!$util.isObject(message.first_hop_custom_records))
                    return "first_hop_custom_records: object expected";
                let key = $Object.keys(message.first_hop_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "first_hop_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.first_hop_custom_records[key[i]] && typeof message.first_hop_custom_records[key[i]].length === "number" || $util.isString(message.first_hop_custom_records[key[i]])))
                        return "first_hop_custom_records: buffer{k:uint64} expected";
                }
            }
            return null;
        };

        /**
         * Creates a SendPaymentRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.SendPaymentRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.SendPaymentRequest} SendPaymentRequest
         */
        SendPaymentRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.SendPaymentRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.SendPaymentRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.SendPaymentRequest();
            if (object.dest != null)
                if (object.dest.length)
                    if (typeof object.dest === "string")
                        $util.base64.decode(object.dest, message.dest = $util.newBuffer($util.base64.length(object.dest)), 0);
                    else if (object.dest.length >= 0)
                        message.dest = object.dest;
            if (object.amt != null)
                if (typeof object.amt === "object" ? object.amt.low || object.amt.high : $Number(object.amt) !== 0)
                    if ($util.Long)
                        message.amt = $util.Long.fromValue(object.amt, false);
                    else if (typeof object.amt === "string")
                        message.amt = $parseInt(object.amt, 10);
                    else if (typeof object.amt === "number")
                        message.amt = object.amt;
                    else if (typeof object.amt === "object")
                        message.amt = new $util.LongBits(object.amt.low >>> 0, object.amt.high >>> 0).toNumber();
            if (object.payment_hash != null)
                if (object.payment_hash.length)
                    if (typeof object.payment_hash === "string")
                        $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                    else if (object.payment_hash.length >= 0)
                        message.payment_hash = object.payment_hash;
            if (object.final_cltv_delta != null)
                if ($Number(object.final_cltv_delta) !== 0)
                    message.final_cltv_delta = object.final_cltv_delta | 0;
            if (object.payment_request != null)
                if (typeof object.payment_request !== "string" || object.payment_request.length)
                    message.payment_request = $String(object.payment_request);
            if (object.timeout_seconds != null)
                if ($Number(object.timeout_seconds) !== 0)
                    message.timeout_seconds = object.timeout_seconds | 0;
            if (object.fee_limit_sat != null)
                if (typeof object.fee_limit_sat === "object" ? object.fee_limit_sat.low || object.fee_limit_sat.high : $Number(object.fee_limit_sat) !== 0)
                    if ($util.Long)
                        message.fee_limit_sat = $util.Long.fromValue(object.fee_limit_sat, false);
                    else if (typeof object.fee_limit_sat === "string")
                        message.fee_limit_sat = $parseInt(object.fee_limit_sat, 10);
                    else if (typeof object.fee_limit_sat === "number")
                        message.fee_limit_sat = object.fee_limit_sat;
                    else if (typeof object.fee_limit_sat === "object")
                        message.fee_limit_sat = new $util.LongBits(object.fee_limit_sat.low >>> 0, object.fee_limit_sat.high >>> 0).toNumber();
            if (object.outgoing_chan_id != null)
                if (typeof object.outgoing_chan_id === "object" ? object.outgoing_chan_id.low || object.outgoing_chan_id.high : $Number(object.outgoing_chan_id) !== 0)
                    if ($util.Long)
                        message.outgoing_chan_id = $util.Long.fromValue(object.outgoing_chan_id, true);
                    else if (typeof object.outgoing_chan_id === "string")
                        message.outgoing_chan_id = $parseInt(object.outgoing_chan_id, 10);
                    else if (typeof object.outgoing_chan_id === "number")
                        message.outgoing_chan_id = object.outgoing_chan_id;
                    else if (typeof object.outgoing_chan_id === "object")
                        message.outgoing_chan_id = new $util.LongBits(object.outgoing_chan_id.low >>> 0, object.outgoing_chan_id.high >>> 0).toNumber(true);
            if (object.cltv_limit != null)
                if ($Number(object.cltv_limit) !== 0)
                    message.cltv_limit = object.cltv_limit | 0;
            if (object.route_hints) {
                if (!$Array.isArray(object.route_hints))
                    throw $TypeError(".routerrpc.SendPaymentRequest.route_hints: array expected");
                message.route_hints = $Array(object.route_hints.length);
                for (let i = 0; i < object.route_hints.length; ++i) {
                    if (!$util.isObject(object.route_hints[i]))
                        throw $TypeError(".routerrpc.SendPaymentRequest.route_hints: object expected");
                    message.route_hints[i] = $root.lnrpc.RouteHint.fromObject(object.route_hints[i], _depth + 1);
                }
            }
            if (object.dest_custom_records) {
                if (!$util.isObject(object.dest_custom_records))
                    throw $TypeError(".routerrpc.SendPaymentRequest.dest_custom_records: object expected");
                message.dest_custom_records = {};
                for (let keys = $Object.keys(object.dest_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.dest_custom_records, keys[i]);
                    if (typeof object.dest_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.dest_custom_records[keys[i]], message.dest_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.dest_custom_records[keys[i]])), 0);
                    else if (object.dest_custom_records[keys[i]].length >= 0)
                        message.dest_custom_records[keys[i]] = object.dest_custom_records[keys[i]];
                }
            }
            if (object.amt_msat != null)
                if (typeof object.amt_msat === "object" ? object.amt_msat.low || object.amt_msat.high : $Number(object.amt_msat) !== 0)
                    if ($util.Long)
                        message.amt_msat = $util.Long.fromValue(object.amt_msat, false);
                    else if (typeof object.amt_msat === "string")
                        message.amt_msat = $parseInt(object.amt_msat, 10);
                    else if (typeof object.amt_msat === "number")
                        message.amt_msat = object.amt_msat;
                    else if (typeof object.amt_msat === "object")
                        message.amt_msat = new $util.LongBits(object.amt_msat.low >>> 0, object.amt_msat.high >>> 0).toNumber();
            if (object.fee_limit_msat != null)
                if (typeof object.fee_limit_msat === "object" ? object.fee_limit_msat.low || object.fee_limit_msat.high : $Number(object.fee_limit_msat) !== 0)
                    if ($util.Long)
                        message.fee_limit_msat = $util.Long.fromValue(object.fee_limit_msat, false);
                    else if (typeof object.fee_limit_msat === "string")
                        message.fee_limit_msat = $parseInt(object.fee_limit_msat, 10);
                    else if (typeof object.fee_limit_msat === "number")
                        message.fee_limit_msat = object.fee_limit_msat;
                    else if (typeof object.fee_limit_msat === "object")
                        message.fee_limit_msat = new $util.LongBits(object.fee_limit_msat.low >>> 0, object.fee_limit_msat.high >>> 0).toNumber();
            if (object.last_hop_pubkey != null)
                if (object.last_hop_pubkey.length)
                    if (typeof object.last_hop_pubkey === "string")
                        $util.base64.decode(object.last_hop_pubkey, message.last_hop_pubkey = $util.newBuffer($util.base64.length(object.last_hop_pubkey)), 0);
                    else if (object.last_hop_pubkey.length >= 0)
                        message.last_hop_pubkey = object.last_hop_pubkey;
            if (object.allow_self_payment != null)
                if (object.allow_self_payment)
                    message.allow_self_payment = $Boolean(object.allow_self_payment);
            if (object.dest_features) {
                if (!$Array.isArray(object.dest_features))
                    throw $TypeError(".routerrpc.SendPaymentRequest.dest_features: array expected");
                message.dest_features = [];
                for (let i = 0; i < object.dest_features.length; ++i)
                    switch (object.dest_features[i]) {
                    case "DATALOSS_PROTECT_REQ":
                    case 0:
                        message.dest_features[message.dest_features.length] = 0;
                        break;
                    case "DATALOSS_PROTECT_OPT":
                    case 1:
                        message.dest_features[message.dest_features.length] = 1;
                        break;
                    case "INITIAL_ROUING_SYNC":
                    case 3:
                        message.dest_features[message.dest_features.length] = 3;
                        break;
                    case "UPFRONT_SHUTDOWN_SCRIPT_REQ":
                    case 4:
                        message.dest_features[message.dest_features.length] = 4;
                        break;
                    case "UPFRONT_SHUTDOWN_SCRIPT_OPT":
                    case 5:
                        message.dest_features[message.dest_features.length] = 5;
                        break;
                    case "GOSSIP_QUERIES_REQ":
                    case 6:
                        message.dest_features[message.dest_features.length] = 6;
                        break;
                    case "GOSSIP_QUERIES_OPT":
                    case 7:
                        message.dest_features[message.dest_features.length] = 7;
                        break;
                    case "TLV_ONION_REQ":
                    case 8:
                        message.dest_features[message.dest_features.length] = 8;
                        break;
                    case "TLV_ONION_OPT":
                    case 9:
                        message.dest_features[message.dest_features.length] = 9;
                        break;
                    case "EXT_GOSSIP_QUERIES_REQ":
                    case 10:
                        message.dest_features[message.dest_features.length] = 10;
                        break;
                    case "EXT_GOSSIP_QUERIES_OPT":
                    case 11:
                        message.dest_features[message.dest_features.length] = 11;
                        break;
                    case "STATIC_REMOTE_KEY_REQ":
                    case 12:
                        message.dest_features[message.dest_features.length] = 12;
                        break;
                    case "STATIC_REMOTE_KEY_OPT":
                    case 13:
                        message.dest_features[message.dest_features.length] = 13;
                        break;
                    case "PAYMENT_ADDR_REQ":
                    case 14:
                        message.dest_features[message.dest_features.length] = 14;
                        break;
                    case "PAYMENT_ADDR_OPT":
                    case 15:
                        message.dest_features[message.dest_features.length] = 15;
                        break;
                    case "MPP_REQ":
                    case 16:
                        message.dest_features[message.dest_features.length] = 16;
                        break;
                    case "MPP_OPT":
                    case 17:
                        message.dest_features[message.dest_features.length] = 17;
                        break;
                    case "WUMBO_CHANNELS_REQ":
                    case 18:
                        message.dest_features[message.dest_features.length] = 18;
                        break;
                    case "WUMBO_CHANNELS_OPT":
                    case 19:
                        message.dest_features[message.dest_features.length] = 19;
                        break;
                    case "ANCHORS_REQ":
                    case 20:
                        message.dest_features[message.dest_features.length] = 20;
                        break;
                    case "ANCHORS_OPT":
                    case 21:
                        message.dest_features[message.dest_features.length] = 21;
                        break;
                    case "ANCHORS_ZERO_FEE_HTLC_REQ":
                    case 22:
                        message.dest_features[message.dest_features.length] = 22;
                        break;
                    case "ANCHORS_ZERO_FEE_HTLC_OPT":
                    case 23:
                        message.dest_features[message.dest_features.length] = 23;
                        break;
                    case "ROUTE_BLINDING_REQUIRED":
                    case 24:
                        message.dest_features[message.dest_features.length] = 24;
                        break;
                    case "ROUTE_BLINDING_OPTIONAL":
                    case 25:
                        message.dest_features[message.dest_features.length] = 25;
                        break;
                    case "AMP_REQ":
                    case 30:
                        message.dest_features[message.dest_features.length] = 30;
                        break;
                    case "AMP_OPT":
                    case 31:
                        message.dest_features[message.dest_features.length] = 31;
                        break;
                    default:
                        if (typeof object.dest_features[i] === "number" && (object.dest_features[i] | 0) === object.dest_features[i])
                            message.dest_features[message.dest_features.length] = object.dest_features[i];
                    }
            }
            if (object.max_parts != null)
                if ($Number(object.max_parts) !== 0)
                    message.max_parts = object.max_parts >>> 0;
            if (object.no_inflight_updates != null)
                if (object.no_inflight_updates)
                    message.no_inflight_updates = $Boolean(object.no_inflight_updates);
            if (object.outgoing_chan_ids) {
                if (!$Array.isArray(object.outgoing_chan_ids))
                    throw $TypeError(".routerrpc.SendPaymentRequest.outgoing_chan_ids: array expected");
                message.outgoing_chan_ids = $Array(object.outgoing_chan_ids.length);
                for (let i = 0; i < object.outgoing_chan_ids.length; ++i)
                    if ($util.Long)
                        message.outgoing_chan_ids[i] = $util.Long.fromValue(object.outgoing_chan_ids[i], true);
                    else if (typeof object.outgoing_chan_ids[i] === "string")
                        message.outgoing_chan_ids[i] = $parseInt(object.outgoing_chan_ids[i], 10);
                    else if (typeof object.outgoing_chan_ids[i] === "number")
                        message.outgoing_chan_ids[i] = object.outgoing_chan_ids[i];
                    else if (typeof object.outgoing_chan_ids[i] === "object")
                        message.outgoing_chan_ids[i] = new $util.LongBits(object.outgoing_chan_ids[i].low >>> 0, object.outgoing_chan_ids[i].high >>> 0).toNumber(true);
            }
            if (object.payment_addr != null)
                if (object.payment_addr.length)
                    if (typeof object.payment_addr === "string")
                        $util.base64.decode(object.payment_addr, message.payment_addr = $util.newBuffer($util.base64.length(object.payment_addr)), 0);
                    else if (object.payment_addr.length >= 0)
                        message.payment_addr = object.payment_addr;
            if (object.max_shard_size_msat != null)
                if (typeof object.max_shard_size_msat === "object" ? object.max_shard_size_msat.low || object.max_shard_size_msat.high : $Number(object.max_shard_size_msat) !== 0)
                    if ($util.Long)
                        message.max_shard_size_msat = $util.Long.fromValue(object.max_shard_size_msat, true);
                    else if (typeof object.max_shard_size_msat === "string")
                        message.max_shard_size_msat = $parseInt(object.max_shard_size_msat, 10);
                    else if (typeof object.max_shard_size_msat === "number")
                        message.max_shard_size_msat = object.max_shard_size_msat;
                    else if (typeof object.max_shard_size_msat === "object")
                        message.max_shard_size_msat = new $util.LongBits(object.max_shard_size_msat.low >>> 0, object.max_shard_size_msat.high >>> 0).toNumber(true);
            if (object.amp != null)
                if (object.amp)
                    message.amp = $Boolean(object.amp);
            if (object.time_pref != null)
                if (!$Object.is($Number(object.time_pref), 0))
                    message.time_pref = $Number(object.time_pref);
            if (object.cancelable != null)
                if (object.cancelable)
                    message.cancelable = $Boolean(object.cancelable);
            if (object.first_hop_custom_records) {
                if (!$util.isObject(object.first_hop_custom_records))
                    throw $TypeError(".routerrpc.SendPaymentRequest.first_hop_custom_records: object expected");
                message.first_hop_custom_records = {};
                for (let keys = $Object.keys(object.first_hop_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.first_hop_custom_records, keys[i]);
                    if (typeof object.first_hop_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.first_hop_custom_records[keys[i]], message.first_hop_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.first_hop_custom_records[keys[i]])), 0);
                    else if (object.first_hop_custom_records[keys[i]].length >= 0)
                        message.first_hop_custom_records[keys[i]] = object.first_hop_custom_records[keys[i]];
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SendPaymentRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.SendPaymentRequest
         * @static
         * @param {routerrpc.SendPaymentRequest} message SendPaymentRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendPaymentRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.route_hints = [];
                object.dest_features = [];
                object.outgoing_chan_ids = [];
            }
            if (options.objects || options.defaults) {
                object.dest_custom_records = {};
                object.first_hop_custom_records = {};
            }
            if (options.defaults) {
                if (options.bytes === $String)
                    object.dest = "";
                else {
                    object.dest = [];
                    if (options.bytes !== $Array)
                        object.dest = $util.newBuffer(object.dest);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.payment_hash = "";
                else {
                    object.payment_hash = [];
                    if (options.bytes !== $Array)
                        object.payment_hash = $util.newBuffer(object.payment_hash);
                }
                object.final_cltv_delta = 0;
                object.payment_request = "";
                object.timeout_seconds = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_limit_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_limit_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.outgoing_chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.outgoing_chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.cltv_limit = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fee_limit_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fee_limit_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.last_hop_pubkey = "";
                else {
                    object.last_hop_pubkey = [];
                    if (options.bytes !== $Array)
                        object.last_hop_pubkey = $util.newBuffer(object.last_hop_pubkey);
                }
                object.allow_self_payment = false;
                object.max_parts = 0;
                object.no_inflight_updates = false;
                if (options.bytes === $String)
                    object.payment_addr = "";
                else {
                    object.payment_addr = [];
                    if (options.bytes !== $Array)
                        object.payment_addr = $util.newBuffer(object.payment_addr);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.max_shard_size_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.max_shard_size_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.amp = false;
                object.time_pref = 0;
                object.cancelable = false;
            }
            if (message.dest != null && $Object.hasOwnProperty.call(message, "dest"))
                object.dest = options.bytes === $String ? $util.base64.encode(message.dest, 0, message.dest.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.dest) : message.dest;
            if (message.amt != null && $Object.hasOwnProperty.call(message, "amt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt = typeof message.amt === "number" ? $BigInt(message.amt) : $util.Long.fromBits(message.amt.low >>> 0, message.amt.high >>> 0, false).toBigInt();
                else if (typeof message.amt === "number")
                    object.amt = options.longs === $String ? $String(message.amt) : message.amt;
                else
                    object.amt = options.longs === $String ? $util.Long.prototype.toString.call(message.amt) : options.longs === $Number ? new $util.LongBits(message.amt.low >>> 0, message.amt.high >>> 0).toNumber() : message.amt;
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta"))
                object.final_cltv_delta = message.final_cltv_delta;
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                object.payment_request = message.payment_request;
            if (message.timeout_seconds != null && $Object.hasOwnProperty.call(message, "timeout_seconds"))
                object.timeout_seconds = message.timeout_seconds;
            if (message.fee_limit_sat != null && $Object.hasOwnProperty.call(message, "fee_limit_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_limit_sat = typeof message.fee_limit_sat === "number" ? $BigInt(message.fee_limit_sat) : $util.Long.fromBits(message.fee_limit_sat.low >>> 0, message.fee_limit_sat.high >>> 0, false).toBigInt();
                else if (typeof message.fee_limit_sat === "number")
                    object.fee_limit_sat = options.longs === $String ? $String(message.fee_limit_sat) : message.fee_limit_sat;
                else
                    object.fee_limit_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_limit_sat) : options.longs === $Number ? new $util.LongBits(message.fee_limit_sat.low >>> 0, message.fee_limit_sat.high >>> 0).toNumber() : message.fee_limit_sat;
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_chan_id = typeof message.outgoing_chan_id === "number" ? $BigInt(message.outgoing_chan_id) : $util.Long.fromBits(message.outgoing_chan_id.low >>> 0, message.outgoing_chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_chan_id === "number")
                    object.outgoing_chan_id = options.longs === $String ? $String(message.outgoing_chan_id) : message.outgoing_chan_id;
                else
                    object.outgoing_chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_chan_id) : options.longs === $Number ? new $util.LongBits(message.outgoing_chan_id.low >>> 0, message.outgoing_chan_id.high >>> 0).toNumber(true) : message.outgoing_chan_id;
            if (message.cltv_limit != null && $Object.hasOwnProperty.call(message, "cltv_limit"))
                object.cltv_limit = message.cltv_limit;
            if (message.route_hints && message.route_hints.length) {
                object.route_hints = $Array(message.route_hints.length);
                for (let j = 0; j < message.route_hints.length; ++j)
                    object.route_hints[j] = $root.lnrpc.RouteHint.toObject(message.route_hints[j], options, _depth + 1);
            }
            let keys2;
            if (message.dest_custom_records && (keys2 = $Object.keys(message.dest_custom_records)).length) {
                object.dest_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.dest_custom_records, keys2[j]);
                    object.dest_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.dest_custom_records[keys2[j]], 0, message.dest_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.dest_custom_records[keys2[j]]) : message.dest_custom_records[keys2[j]];
                }
            }
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_msat = typeof message.amt_msat === "number" ? $BigInt(message.amt_msat) : $util.Long.fromBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_msat === "number")
                    object.amt_msat = options.longs === $String ? $String(message.amt_msat) : message.amt_msat;
                else
                    object.amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_msat) : options.longs === $Number ? new $util.LongBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0).toNumber() : message.amt_msat;
            if (message.fee_limit_msat != null && $Object.hasOwnProperty.call(message, "fee_limit_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fee_limit_msat = typeof message.fee_limit_msat === "number" ? $BigInt(message.fee_limit_msat) : $util.Long.fromBits(message.fee_limit_msat.low >>> 0, message.fee_limit_msat.high >>> 0, false).toBigInt();
                else if (typeof message.fee_limit_msat === "number")
                    object.fee_limit_msat = options.longs === $String ? $String(message.fee_limit_msat) : message.fee_limit_msat;
                else
                    object.fee_limit_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.fee_limit_msat) : options.longs === $Number ? new $util.LongBits(message.fee_limit_msat.low >>> 0, message.fee_limit_msat.high >>> 0).toNumber() : message.fee_limit_msat;
            if (message.last_hop_pubkey != null && $Object.hasOwnProperty.call(message, "last_hop_pubkey"))
                object.last_hop_pubkey = options.bytes === $String ? $util.base64.encode(message.last_hop_pubkey, 0, message.last_hop_pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.last_hop_pubkey) : message.last_hop_pubkey;
            if (message.allow_self_payment != null && $Object.hasOwnProperty.call(message, "allow_self_payment"))
                object.allow_self_payment = message.allow_self_payment;
            if (message.dest_features && message.dest_features.length) {
                object.dest_features = $Array(message.dest_features.length);
                for (let j = 0; j < message.dest_features.length; ++j)
                    object.dest_features[j] = options.enums === $String ? $root.lnrpc.FeatureBit[message.dest_features[j]] === $undefined ? message.dest_features[j] : $root.lnrpc.FeatureBit[message.dest_features[j]] : message.dest_features[j];
            }
            if (message.max_parts != null && $Object.hasOwnProperty.call(message, "max_parts"))
                object.max_parts = message.max_parts;
            if (message.no_inflight_updates != null && $Object.hasOwnProperty.call(message, "no_inflight_updates"))
                object.no_inflight_updates = message.no_inflight_updates;
            if (message.outgoing_chan_ids && message.outgoing_chan_ids.length) {
                object.outgoing_chan_ids = $Array(message.outgoing_chan_ids.length);
                for (let j = 0; j < message.outgoing_chan_ids.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.outgoing_chan_ids[j] = typeof message.outgoing_chan_ids[j] === "number" ? $BigInt(message.outgoing_chan_ids[j]) : $util.Long.fromBits(message.outgoing_chan_ids[j].low >>> 0, message.outgoing_chan_ids[j].high >>> 0, true).toBigInt();
                    else if (typeof message.outgoing_chan_ids[j] === "number")
                        object.outgoing_chan_ids[j] = options.longs === $String ? $String(message.outgoing_chan_ids[j]) : message.outgoing_chan_ids[j];
                    else
                        object.outgoing_chan_ids[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_chan_ids[j]) : options.longs === $Number ? new $util.LongBits(message.outgoing_chan_ids[j].low >>> 0, message.outgoing_chan_ids[j].high >>> 0).toNumber(true) : message.outgoing_chan_ids[j];
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                object.payment_addr = options.bytes === $String ? $util.base64.encode(message.payment_addr, 0, message.payment_addr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_addr) : message.payment_addr;
            if (message.max_shard_size_msat != null && $Object.hasOwnProperty.call(message, "max_shard_size_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.max_shard_size_msat = typeof message.max_shard_size_msat === "number" ? $BigInt(message.max_shard_size_msat) : $util.Long.fromBits(message.max_shard_size_msat.low >>> 0, message.max_shard_size_msat.high >>> 0, true).toBigInt();
                else if (typeof message.max_shard_size_msat === "number")
                    object.max_shard_size_msat = options.longs === $String ? $String(message.max_shard_size_msat) : message.max_shard_size_msat;
                else
                    object.max_shard_size_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.max_shard_size_msat) : options.longs === $Number ? new $util.LongBits(message.max_shard_size_msat.low >>> 0, message.max_shard_size_msat.high >>> 0).toNumber(true) : message.max_shard_size_msat;
            if (message.amp != null && $Object.hasOwnProperty.call(message, "amp"))
                object.amp = message.amp;
            if (message.time_pref != null && $Object.hasOwnProperty.call(message, "time_pref"))
                object.time_pref = options.json && !$isFinite(message.time_pref) ? $String(message.time_pref) : message.time_pref;
            if (message.cancelable != null && $Object.hasOwnProperty.call(message, "cancelable"))
                object.cancelable = message.cancelable;
            if (message.first_hop_custom_records && (keys2 = $Object.keys(message.first_hop_custom_records)).length) {
                object.first_hop_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.first_hop_custom_records, keys2[j]);
                    object.first_hop_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.first_hop_custom_records[keys2[j]], 0, message.first_hop_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.first_hop_custom_records[keys2[j]]) : message.first_hop_custom_records[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this SendPaymentRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.SendPaymentRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendPaymentRequest.prototype.toJSON = function() {
            return SendPaymentRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendPaymentRequest
         * @function getTypeUrl
         * @memberof routerrpc.SendPaymentRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendPaymentRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.SendPaymentRequest";
        };

        return SendPaymentRequest;
    })();

    routerrpc.TrackPaymentRequest = (function() {

        /**
         * Properties of a TrackPaymentRequest.
         * @typedef {Object} routerrpc.TrackPaymentRequest.$Properties
         * @property {Uint8Array|null} [payment_hash] TrackPaymentRequest payment_hash
         * @property {boolean|null} [no_inflight_updates] TrackPaymentRequest no_inflight_updates
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TrackPaymentRequest.
         * @memberof routerrpc
         * @interface ITrackPaymentRequest
         * @augments routerrpc.TrackPaymentRequest.$Properties
         * @deprecated Use routerrpc.TrackPaymentRequest.$Properties instead.
         */

        /**
         * Shape of a TrackPaymentRequest.
         * @typedef {routerrpc.TrackPaymentRequest.$Properties} routerrpc.TrackPaymentRequest.$Shape
         */

        /**
         * Constructs a new TrackPaymentRequest.
         * @memberof routerrpc
         * @classdesc Represents a TrackPaymentRequest.
         * @constructor
         * @param {routerrpc.TrackPaymentRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TrackPaymentRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TrackPaymentRequest payment_hash.
         * @member {Uint8Array} payment_hash
         * @memberof routerrpc.TrackPaymentRequest
         * @instance
         */
        TrackPaymentRequest.prototype.payment_hash = $util.newBuffer([]);

        /**
         * TrackPaymentRequest no_inflight_updates.
         * @member {boolean} no_inflight_updates
         * @memberof routerrpc.TrackPaymentRequest
         * @instance
         */
        TrackPaymentRequest.prototype.no_inflight_updates = false;

        /**
         * Creates a new TrackPaymentRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.TrackPaymentRequest
         * @static
         * @param {routerrpc.TrackPaymentRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.TrackPaymentRequest} TrackPaymentRequest instance
         * @type {{
         *   (properties: routerrpc.TrackPaymentRequest.$Shape): routerrpc.TrackPaymentRequest & routerrpc.TrackPaymentRequest.$Shape;
         *   (properties?: routerrpc.TrackPaymentRequest.$Properties): routerrpc.TrackPaymentRequest;
         * }}
         */
        TrackPaymentRequest.create = function(properties) {
            return new TrackPaymentRequest(properties);
        };

        /**
         * Encodes the specified TrackPaymentRequest message. Does not implicitly {@link routerrpc.TrackPaymentRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.TrackPaymentRequest
         * @static
         * @param {routerrpc.TrackPaymentRequest.$Properties} message TrackPaymentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TrackPaymentRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.payment_hash);
            if (message.no_inflight_updates != null && $Object.hasOwnProperty.call(message, "no_inflight_updates") && message.no_inflight_updates !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.no_inflight_updates);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TrackPaymentRequest message, length delimited. Does not implicitly {@link routerrpc.TrackPaymentRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.TrackPaymentRequest
         * @static
         * @param {routerrpc.TrackPaymentRequest.$Properties} message TrackPaymentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TrackPaymentRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TrackPaymentRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.TrackPaymentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.TrackPaymentRequest & routerrpc.TrackPaymentRequest.$Shape} TrackPaymentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TrackPaymentRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.TrackPaymentRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.no_inflight_updates = value;
                        else
                            delete message.no_inflight_updates;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TrackPaymentRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.TrackPaymentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.TrackPaymentRequest & routerrpc.TrackPaymentRequest.$Shape} TrackPaymentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TrackPaymentRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TrackPaymentRequest message.
         * @function verify
         * @memberof routerrpc.TrackPaymentRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TrackPaymentRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            if (message.no_inflight_updates != null && $Object.hasOwnProperty.call(message, "no_inflight_updates"))
                if (typeof message.no_inflight_updates !== "boolean")
                    return "no_inflight_updates: boolean expected";
            return null;
        };

        /**
         * Creates a TrackPaymentRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.TrackPaymentRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.TrackPaymentRequest} TrackPaymentRequest
         */
        TrackPaymentRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.TrackPaymentRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.TrackPaymentRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.TrackPaymentRequest();
            if (object.payment_hash != null)
                if (object.payment_hash.length)
                    if (typeof object.payment_hash === "string")
                        $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                    else if (object.payment_hash.length >= 0)
                        message.payment_hash = object.payment_hash;
            if (object.no_inflight_updates != null)
                if (object.no_inflight_updates)
                    message.no_inflight_updates = $Boolean(object.no_inflight_updates);
            return message;
        };

        /**
         * Creates a plain object from a TrackPaymentRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.TrackPaymentRequest
         * @static
         * @param {routerrpc.TrackPaymentRequest} message TrackPaymentRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TrackPaymentRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.payment_hash = "";
                else {
                    object.payment_hash = [];
                    if (options.bytes !== $Array)
                        object.payment_hash = $util.newBuffer(object.payment_hash);
                }
                object.no_inflight_updates = false;
            }
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
            if (message.no_inflight_updates != null && $Object.hasOwnProperty.call(message, "no_inflight_updates"))
                object.no_inflight_updates = message.no_inflight_updates;
            return object;
        };

        /**
         * Converts this TrackPaymentRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.TrackPaymentRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TrackPaymentRequest.prototype.toJSON = function() {
            return TrackPaymentRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TrackPaymentRequest
         * @function getTypeUrl
         * @memberof routerrpc.TrackPaymentRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TrackPaymentRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.TrackPaymentRequest";
        };

        return TrackPaymentRequest;
    })();

    routerrpc.TrackPaymentsRequest = (function() {

        /**
         * Properties of a TrackPaymentsRequest.
         * @typedef {Object} routerrpc.TrackPaymentsRequest.$Properties
         * @property {boolean|null} [no_inflight_updates] TrackPaymentsRequest no_inflight_updates
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TrackPaymentsRequest.
         * @memberof routerrpc
         * @interface ITrackPaymentsRequest
         * @augments routerrpc.TrackPaymentsRequest.$Properties
         * @deprecated Use routerrpc.TrackPaymentsRequest.$Properties instead.
         */

        /**
         * Shape of a TrackPaymentsRequest.
         * @typedef {routerrpc.TrackPaymentsRequest.$Properties} routerrpc.TrackPaymentsRequest.$Shape
         */

        /**
         * Constructs a new TrackPaymentsRequest.
         * @memberof routerrpc
         * @classdesc Represents a TrackPaymentsRequest.
         * @constructor
         * @param {routerrpc.TrackPaymentsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TrackPaymentsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TrackPaymentsRequest no_inflight_updates.
         * @member {boolean} no_inflight_updates
         * @memberof routerrpc.TrackPaymentsRequest
         * @instance
         */
        TrackPaymentsRequest.prototype.no_inflight_updates = false;

        /**
         * Creates a new TrackPaymentsRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.TrackPaymentsRequest
         * @static
         * @param {routerrpc.TrackPaymentsRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.TrackPaymentsRequest} TrackPaymentsRequest instance
         * @type {{
         *   (properties: routerrpc.TrackPaymentsRequest.$Shape): routerrpc.TrackPaymentsRequest & routerrpc.TrackPaymentsRequest.$Shape;
         *   (properties?: routerrpc.TrackPaymentsRequest.$Properties): routerrpc.TrackPaymentsRequest;
         * }}
         */
        TrackPaymentsRequest.create = function(properties) {
            return new TrackPaymentsRequest(properties);
        };

        /**
         * Encodes the specified TrackPaymentsRequest message. Does not implicitly {@link routerrpc.TrackPaymentsRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.TrackPaymentsRequest
         * @static
         * @param {routerrpc.TrackPaymentsRequest.$Properties} message TrackPaymentsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TrackPaymentsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.no_inflight_updates != null && $Object.hasOwnProperty.call(message, "no_inflight_updates") && message.no_inflight_updates !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.no_inflight_updates);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TrackPaymentsRequest message, length delimited. Does not implicitly {@link routerrpc.TrackPaymentsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.TrackPaymentsRequest
         * @static
         * @param {routerrpc.TrackPaymentsRequest.$Properties} message TrackPaymentsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TrackPaymentsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TrackPaymentsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.TrackPaymentsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.TrackPaymentsRequest & routerrpc.TrackPaymentsRequest.$Shape} TrackPaymentsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TrackPaymentsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.TrackPaymentsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.no_inflight_updates = value;
                        else
                            delete message.no_inflight_updates;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TrackPaymentsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.TrackPaymentsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.TrackPaymentsRequest & routerrpc.TrackPaymentsRequest.$Shape} TrackPaymentsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TrackPaymentsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TrackPaymentsRequest message.
         * @function verify
         * @memberof routerrpc.TrackPaymentsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TrackPaymentsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.no_inflight_updates != null && $Object.hasOwnProperty.call(message, "no_inflight_updates"))
                if (typeof message.no_inflight_updates !== "boolean")
                    return "no_inflight_updates: boolean expected";
            return null;
        };

        /**
         * Creates a TrackPaymentsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.TrackPaymentsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.TrackPaymentsRequest} TrackPaymentsRequest
         */
        TrackPaymentsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.TrackPaymentsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.TrackPaymentsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.TrackPaymentsRequest();
            if (object.no_inflight_updates != null)
                if (object.no_inflight_updates)
                    message.no_inflight_updates = $Boolean(object.no_inflight_updates);
            return message;
        };

        /**
         * Creates a plain object from a TrackPaymentsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.TrackPaymentsRequest
         * @static
         * @param {routerrpc.TrackPaymentsRequest} message TrackPaymentsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TrackPaymentsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.no_inflight_updates = false;
            if (message.no_inflight_updates != null && $Object.hasOwnProperty.call(message, "no_inflight_updates"))
                object.no_inflight_updates = message.no_inflight_updates;
            return object;
        };

        /**
         * Converts this TrackPaymentsRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.TrackPaymentsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TrackPaymentsRequest.prototype.toJSON = function() {
            return TrackPaymentsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TrackPaymentsRequest
         * @function getTypeUrl
         * @memberof routerrpc.TrackPaymentsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TrackPaymentsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.TrackPaymentsRequest";
        };

        return TrackPaymentsRequest;
    })();

    routerrpc.RouteFeeRequest = (function() {

        /**
         * Properties of a RouteFeeRequest.
         * @typedef {Object} routerrpc.RouteFeeRequest.$Properties
         * @property {Uint8Array|null} [dest] RouteFeeRequest dest
         * @property {Long|null} [amt_sat] RouteFeeRequest amt_sat
         * @property {string|null} [payment_request] RouteFeeRequest payment_request
         * @property {number|null} [timeout] RouteFeeRequest timeout
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RouteFeeRequest.
         * @memberof routerrpc
         * @interface IRouteFeeRequest
         * @augments routerrpc.RouteFeeRequest.$Properties
         * @deprecated Use routerrpc.RouteFeeRequest.$Properties instead.
         */

        /**
         * Shape of a RouteFeeRequest.
         * @typedef {routerrpc.RouteFeeRequest.$Properties} routerrpc.RouteFeeRequest.$Shape
         */

        /**
         * Constructs a new RouteFeeRequest.
         * @memberof routerrpc
         * @classdesc Represents a RouteFeeRequest.
         * @constructor
         * @param {routerrpc.RouteFeeRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RouteFeeRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RouteFeeRequest dest.
         * @member {Uint8Array} dest
         * @memberof routerrpc.RouteFeeRequest
         * @instance
         */
        RouteFeeRequest.prototype.dest = $util.newBuffer([]);

        /**
         * RouteFeeRequest amt_sat.
         * @member {Long} amt_sat
         * @memberof routerrpc.RouteFeeRequest
         * @instance
         */
        RouteFeeRequest.prototype.amt_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RouteFeeRequest payment_request.
         * @member {string} payment_request
         * @memberof routerrpc.RouteFeeRequest
         * @instance
         */
        RouteFeeRequest.prototype.payment_request = "";

        /**
         * RouteFeeRequest timeout.
         * @member {number} timeout
         * @memberof routerrpc.RouteFeeRequest
         * @instance
         */
        RouteFeeRequest.prototype.timeout = 0;

        /**
         * Creates a new RouteFeeRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.RouteFeeRequest
         * @static
         * @param {routerrpc.RouteFeeRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.RouteFeeRequest} RouteFeeRequest instance
         * @type {{
         *   (properties: routerrpc.RouteFeeRequest.$Shape): routerrpc.RouteFeeRequest & routerrpc.RouteFeeRequest.$Shape;
         *   (properties?: routerrpc.RouteFeeRequest.$Properties): routerrpc.RouteFeeRequest;
         * }}
         */
        RouteFeeRequest.create = function(properties) {
            return new RouteFeeRequest(properties);
        };

        /**
         * Encodes the specified RouteFeeRequest message. Does not implicitly {@link routerrpc.RouteFeeRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.RouteFeeRequest
         * @static
         * @param {routerrpc.RouteFeeRequest.$Properties} message RouteFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RouteFeeRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.dest != null && $Object.hasOwnProperty.call(message, "dest") && message.dest.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.dest);
            if (message.amt_sat != null && $Object.hasOwnProperty.call(message, "amt_sat") && (typeof message.amt_sat === "object" ? message.amt_sat.low || message.amt_sat.high : message.amt_sat !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amt_sat);
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request") && message.payment_request !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.payment_request);
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout") && message.timeout !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.timeout);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RouteFeeRequest message, length delimited. Does not implicitly {@link routerrpc.RouteFeeRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.RouteFeeRequest
         * @static
         * @param {routerrpc.RouteFeeRequest.$Properties} message RouteFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RouteFeeRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RouteFeeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.RouteFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.RouteFeeRequest & routerrpc.RouteFeeRequest.$Shape} RouteFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RouteFeeRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.RouteFeeRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.dest = value;
                        else
                            delete message.dest;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_sat = value;
                        else
                            delete message.amt_sat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.payment_request = value;
                        else
                            delete message.payment_request;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.timeout = value;
                        else
                            delete message.timeout;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RouteFeeRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.RouteFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.RouteFeeRequest & routerrpc.RouteFeeRequest.$Shape} RouteFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RouteFeeRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RouteFeeRequest message.
         * @function verify
         * @memberof routerrpc.RouteFeeRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RouteFeeRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dest != null && $Object.hasOwnProperty.call(message, "dest"))
                if (!(message.dest && typeof message.dest.length === "number" || $util.isString(message.dest)))
                    return "dest: buffer expected";
            if (message.amt_sat != null && $Object.hasOwnProperty.call(message, "amt_sat"))
                if (!$util.isInteger(message.amt_sat) && !(message.amt_sat && $util.isInteger(message.amt_sat.low) && $util.isInteger(message.amt_sat.high)))
                    return "amt_sat: integer|Long expected";
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                if (!$util.isString(message.payment_request))
                    return "payment_request: string expected";
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                if (!$util.isInteger(message.timeout))
                    return "timeout: integer expected";
            return null;
        };

        /**
         * Creates a RouteFeeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.RouteFeeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.RouteFeeRequest} RouteFeeRequest
         */
        RouteFeeRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.RouteFeeRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.RouteFeeRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.RouteFeeRequest();
            if (object.dest != null)
                if (object.dest.length)
                    if (typeof object.dest === "string")
                        $util.base64.decode(object.dest, message.dest = $util.newBuffer($util.base64.length(object.dest)), 0);
                    else if (object.dest.length >= 0)
                        message.dest = object.dest;
            if (object.amt_sat != null)
                if (typeof object.amt_sat === "object" ? object.amt_sat.low || object.amt_sat.high : $Number(object.amt_sat) !== 0)
                    if ($util.Long)
                        message.amt_sat = $util.Long.fromValue(object.amt_sat, false);
                    else if (typeof object.amt_sat === "string")
                        message.amt_sat = $parseInt(object.amt_sat, 10);
                    else if (typeof object.amt_sat === "number")
                        message.amt_sat = object.amt_sat;
                    else if (typeof object.amt_sat === "object")
                        message.amt_sat = new $util.LongBits(object.amt_sat.low >>> 0, object.amt_sat.high >>> 0).toNumber();
            if (object.payment_request != null)
                if (typeof object.payment_request !== "string" || object.payment_request.length)
                    message.payment_request = $String(object.payment_request);
            if (object.timeout != null)
                if ($Number(object.timeout) !== 0)
                    message.timeout = object.timeout >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a RouteFeeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.RouteFeeRequest
         * @static
         * @param {routerrpc.RouteFeeRequest} message RouteFeeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RouteFeeRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.dest = "";
                else {
                    object.dest = [];
                    if (options.bytes !== $Array)
                        object.dest = $util.newBuffer(object.dest);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.payment_request = "";
                object.timeout = 0;
            }
            if (message.dest != null && $Object.hasOwnProperty.call(message, "dest"))
                object.dest = options.bytes === $String ? $util.base64.encode(message.dest, 0, message.dest.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.dest) : message.dest;
            if (message.amt_sat != null && $Object.hasOwnProperty.call(message, "amt_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_sat = typeof message.amt_sat === "number" ? $BigInt(message.amt_sat) : $util.Long.fromBits(message.amt_sat.low >>> 0, message.amt_sat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_sat === "number")
                    object.amt_sat = options.longs === $String ? $String(message.amt_sat) : message.amt_sat;
                else
                    object.amt_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_sat) : options.longs === $Number ? new $util.LongBits(message.amt_sat.low >>> 0, message.amt_sat.high >>> 0).toNumber() : message.amt_sat;
            if (message.payment_request != null && $Object.hasOwnProperty.call(message, "payment_request"))
                object.payment_request = message.payment_request;
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                object.timeout = message.timeout;
            return object;
        };

        /**
         * Converts this RouteFeeRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.RouteFeeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RouteFeeRequest.prototype.toJSON = function() {
            return RouteFeeRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RouteFeeRequest
         * @function getTypeUrl
         * @memberof routerrpc.RouteFeeRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RouteFeeRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.RouteFeeRequest";
        };

        return RouteFeeRequest;
    })();

    routerrpc.RouteFeeResponse = (function() {

        /**
         * Properties of a RouteFeeResponse.
         * @typedef {Object} routerrpc.RouteFeeResponse.$Properties
         * @property {Long|null} [routing_fee_msat] RouteFeeResponse routing_fee_msat
         * @property {Long|null} [time_lock_delay] RouteFeeResponse time_lock_delay
         * @property {lnrpc.PaymentFailureReason|null} [failure_reason] RouteFeeResponse failure_reason
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RouteFeeResponse.
         * @memberof routerrpc
         * @interface IRouteFeeResponse
         * @augments routerrpc.RouteFeeResponse.$Properties
         * @deprecated Use routerrpc.RouteFeeResponse.$Properties instead.
         */

        /**
         * Shape of a RouteFeeResponse.
         * @typedef {routerrpc.RouteFeeResponse.$Properties} routerrpc.RouteFeeResponse.$Shape
         */

        /**
         * Constructs a new RouteFeeResponse.
         * @memberof routerrpc
         * @classdesc Represents a RouteFeeResponse.
         * @constructor
         * @param {routerrpc.RouteFeeResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RouteFeeResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RouteFeeResponse routing_fee_msat.
         * @member {Long} routing_fee_msat
         * @memberof routerrpc.RouteFeeResponse
         * @instance
         */
        RouteFeeResponse.prototype.routing_fee_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RouteFeeResponse time_lock_delay.
         * @member {Long} time_lock_delay
         * @memberof routerrpc.RouteFeeResponse
         * @instance
         */
        RouteFeeResponse.prototype.time_lock_delay = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RouteFeeResponse failure_reason.
         * @member {lnrpc.PaymentFailureReason} failure_reason
         * @memberof routerrpc.RouteFeeResponse
         * @instance
         */
        RouteFeeResponse.prototype.failure_reason = 0;

        /**
         * Creates a new RouteFeeResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.RouteFeeResponse
         * @static
         * @param {routerrpc.RouteFeeResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.RouteFeeResponse} RouteFeeResponse instance
         * @type {{
         *   (properties: routerrpc.RouteFeeResponse.$Shape): routerrpc.RouteFeeResponse & routerrpc.RouteFeeResponse.$Shape;
         *   (properties?: routerrpc.RouteFeeResponse.$Properties): routerrpc.RouteFeeResponse;
         * }}
         */
        RouteFeeResponse.create = function(properties) {
            return new RouteFeeResponse(properties);
        };

        /**
         * Encodes the specified RouteFeeResponse message. Does not implicitly {@link routerrpc.RouteFeeResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.RouteFeeResponse
         * @static
         * @param {routerrpc.RouteFeeResponse.$Properties} message RouteFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RouteFeeResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.routing_fee_msat != null && $Object.hasOwnProperty.call(message, "routing_fee_msat") && (typeof message.routing_fee_msat === "object" ? message.routing_fee_msat.low || message.routing_fee_msat.high : message.routing_fee_msat !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.routing_fee_msat);
            if (message.time_lock_delay != null && $Object.hasOwnProperty.call(message, "time_lock_delay") && (typeof message.time_lock_delay === "object" ? message.time_lock_delay.low || message.time_lock_delay.high : message.time_lock_delay !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.time_lock_delay);
            if (message.failure_reason != null && $Object.hasOwnProperty.call(message, "failure_reason") && message.failure_reason !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.failure_reason);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RouteFeeResponse message, length delimited. Does not implicitly {@link routerrpc.RouteFeeResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.RouteFeeResponse
         * @static
         * @param {routerrpc.RouteFeeResponse.$Properties} message RouteFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RouteFeeResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RouteFeeResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.RouteFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.RouteFeeResponse & routerrpc.RouteFeeResponse.$Shape} RouteFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RouteFeeResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.RouteFeeResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.routing_fee_msat = value;
                        else
                            delete message.routing_fee_msat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.time_lock_delay = value;
                        else
                            delete message.time_lock_delay;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.failure_reason = value;
                        else
                            delete message.failure_reason;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RouteFeeResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.RouteFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.RouteFeeResponse & routerrpc.RouteFeeResponse.$Shape} RouteFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RouteFeeResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RouteFeeResponse message.
         * @function verify
         * @memberof routerrpc.RouteFeeResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RouteFeeResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.routing_fee_msat != null && $Object.hasOwnProperty.call(message, "routing_fee_msat"))
                if (!$util.isInteger(message.routing_fee_msat) && !(message.routing_fee_msat && $util.isInteger(message.routing_fee_msat.low) && $util.isInteger(message.routing_fee_msat.high)))
                    return "routing_fee_msat: integer|Long expected";
            if (message.time_lock_delay != null && $Object.hasOwnProperty.call(message, "time_lock_delay"))
                if (!$util.isInteger(message.time_lock_delay) && !(message.time_lock_delay && $util.isInteger(message.time_lock_delay.low) && $util.isInteger(message.time_lock_delay.high)))
                    return "time_lock_delay: integer|Long expected";
            if (message.failure_reason != null && $Object.hasOwnProperty.call(message, "failure_reason"))
                if (typeof message.failure_reason !== "number" || (message.failure_reason | 0) !== message.failure_reason)
                    return "failure_reason: enum value expected";
            return null;
        };

        /**
         * Creates a RouteFeeResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.RouteFeeResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.RouteFeeResponse} RouteFeeResponse
         */
        RouteFeeResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.RouteFeeResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.RouteFeeResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.RouteFeeResponse();
            if (object.routing_fee_msat != null)
                if (typeof object.routing_fee_msat === "object" ? object.routing_fee_msat.low || object.routing_fee_msat.high : $Number(object.routing_fee_msat) !== 0)
                    if ($util.Long)
                        message.routing_fee_msat = $util.Long.fromValue(object.routing_fee_msat, false);
                    else if (typeof object.routing_fee_msat === "string")
                        message.routing_fee_msat = $parseInt(object.routing_fee_msat, 10);
                    else if (typeof object.routing_fee_msat === "number")
                        message.routing_fee_msat = object.routing_fee_msat;
                    else if (typeof object.routing_fee_msat === "object")
                        message.routing_fee_msat = new $util.LongBits(object.routing_fee_msat.low >>> 0, object.routing_fee_msat.high >>> 0).toNumber();
            if (object.time_lock_delay != null)
                if (typeof object.time_lock_delay === "object" ? object.time_lock_delay.low || object.time_lock_delay.high : $Number(object.time_lock_delay) !== 0)
                    if ($util.Long)
                        message.time_lock_delay = $util.Long.fromValue(object.time_lock_delay, false);
                    else if (typeof object.time_lock_delay === "string")
                        message.time_lock_delay = $parseInt(object.time_lock_delay, 10);
                    else if (typeof object.time_lock_delay === "number")
                        message.time_lock_delay = object.time_lock_delay;
                    else if (typeof object.time_lock_delay === "object")
                        message.time_lock_delay = new $util.LongBits(object.time_lock_delay.low >>> 0, object.time_lock_delay.high >>> 0).toNumber();
            if (object.failure_reason !== 0 && (typeof object.failure_reason !== "string" || $root.lnrpc.PaymentFailureReason[object.failure_reason] !== 0))
                switch (object.failure_reason) {
                case "FAILURE_REASON_NONE":
                case 0:
                    message.failure_reason = 0;
                    break;
                case "FAILURE_REASON_TIMEOUT":
                case 1:
                    message.failure_reason = 1;
                    break;
                case "FAILURE_REASON_NO_ROUTE":
                case 2:
                    message.failure_reason = 2;
                    break;
                case "FAILURE_REASON_ERROR":
                case 3:
                    message.failure_reason = 3;
                    break;
                case "FAILURE_REASON_INCORRECT_PAYMENT_DETAILS":
                case 4:
                    message.failure_reason = 4;
                    break;
                case "FAILURE_REASON_INSUFFICIENT_BALANCE":
                case 5:
                    message.failure_reason = 5;
                    break;
                case "FAILURE_REASON_CANCELED":
                case 6:
                    message.failure_reason = 6;
                    break;
                default:
                    if (typeof object.failure_reason === "number" && (object.failure_reason | 0) === object.failure_reason)
                        message.failure_reason = object.failure_reason;
                }
            return message;
        };

        /**
         * Creates a plain object from a RouteFeeResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.RouteFeeResponse
         * @static
         * @param {routerrpc.RouteFeeResponse} message RouteFeeResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RouteFeeResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.routing_fee_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.routing_fee_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.time_lock_delay = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.time_lock_delay = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.failure_reason = options.enums === $String ? "FAILURE_REASON_NONE" : 0;
            }
            if (message.routing_fee_msat != null && $Object.hasOwnProperty.call(message, "routing_fee_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.routing_fee_msat = typeof message.routing_fee_msat === "number" ? $BigInt(message.routing_fee_msat) : $util.Long.fromBits(message.routing_fee_msat.low >>> 0, message.routing_fee_msat.high >>> 0, false).toBigInt();
                else if (typeof message.routing_fee_msat === "number")
                    object.routing_fee_msat = options.longs === $String ? $String(message.routing_fee_msat) : message.routing_fee_msat;
                else
                    object.routing_fee_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.routing_fee_msat) : options.longs === $Number ? new $util.LongBits(message.routing_fee_msat.low >>> 0, message.routing_fee_msat.high >>> 0).toNumber() : message.routing_fee_msat;
            if (message.time_lock_delay != null && $Object.hasOwnProperty.call(message, "time_lock_delay"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.time_lock_delay = typeof message.time_lock_delay === "number" ? $BigInt(message.time_lock_delay) : $util.Long.fromBits(message.time_lock_delay.low >>> 0, message.time_lock_delay.high >>> 0, false).toBigInt();
                else if (typeof message.time_lock_delay === "number")
                    object.time_lock_delay = options.longs === $String ? $String(message.time_lock_delay) : message.time_lock_delay;
                else
                    object.time_lock_delay = options.longs === $String ? $util.Long.prototype.toString.call(message.time_lock_delay) : options.longs === $Number ? new $util.LongBits(message.time_lock_delay.low >>> 0, message.time_lock_delay.high >>> 0).toNumber() : message.time_lock_delay;
            if (message.failure_reason != null && $Object.hasOwnProperty.call(message, "failure_reason"))
                object.failure_reason = options.enums === $String ? $root.lnrpc.PaymentFailureReason[message.failure_reason] === $undefined ? message.failure_reason : $root.lnrpc.PaymentFailureReason[message.failure_reason] : message.failure_reason;
            return object;
        };

        /**
         * Converts this RouteFeeResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.RouteFeeResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RouteFeeResponse.prototype.toJSON = function() {
            return RouteFeeResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RouteFeeResponse
         * @function getTypeUrl
         * @memberof routerrpc.RouteFeeResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RouteFeeResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.RouteFeeResponse";
        };

        return RouteFeeResponse;
    })();

    routerrpc.SendToRouteRequest = (function() {

        /**
         * Properties of a SendToRouteRequest.
         * @typedef {Object} routerrpc.SendToRouteRequest.$Properties
         * @property {Uint8Array|null} [payment_hash] SendToRouteRequest payment_hash
         * @property {lnrpc.Route.$Properties|null} [route] SendToRouteRequest route
         * @property {boolean|null} [skip_temp_err] SendToRouteRequest skip_temp_err
         * @property {Object.<string,Uint8Array>|null} [first_hop_custom_records] SendToRouteRequest first_hop_custom_records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendToRouteRequest.
         * @memberof routerrpc
         * @interface ISendToRouteRequest
         * @augments routerrpc.SendToRouteRequest.$Properties
         * @deprecated Use routerrpc.SendToRouteRequest.$Properties instead.
         */

        /**
         * Shape of a SendToRouteRequest.
         * @typedef {routerrpc.SendToRouteRequest.$Properties} routerrpc.SendToRouteRequest.$Shape
         */

        /**
         * Constructs a new SendToRouteRequest.
         * @memberof routerrpc
         * @classdesc Represents a SendToRouteRequest.
         * @constructor
         * @param {routerrpc.SendToRouteRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendToRouteRequest = function (properties) {
            this.first_hop_custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendToRouteRequest payment_hash.
         * @member {Uint8Array} payment_hash
         * @memberof routerrpc.SendToRouteRequest
         * @instance
         */
        SendToRouteRequest.prototype.payment_hash = $util.newBuffer([]);

        /**
         * SendToRouteRequest route.
         * @member {lnrpc.Route.$Properties|null|undefined} route
         * @memberof routerrpc.SendToRouteRequest
         * @instance
         */
        SendToRouteRequest.prototype.route = null;

        /**
         * SendToRouteRequest skip_temp_err.
         * @member {boolean} skip_temp_err
         * @memberof routerrpc.SendToRouteRequest
         * @instance
         */
        SendToRouteRequest.prototype.skip_temp_err = false;

        /**
         * SendToRouteRequest first_hop_custom_records.
         * @member {Object.<string,Uint8Array>} first_hop_custom_records
         * @memberof routerrpc.SendToRouteRequest
         * @instance
         */
        SendToRouteRequest.prototype.first_hop_custom_records = $util.emptyObject;

        /**
         * Creates a new SendToRouteRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.SendToRouteRequest
         * @static
         * @param {routerrpc.SendToRouteRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.SendToRouteRequest} SendToRouteRequest instance
         * @type {{
         *   (properties: routerrpc.SendToRouteRequest.$Shape): routerrpc.SendToRouteRequest & routerrpc.SendToRouteRequest.$Shape;
         *   (properties?: routerrpc.SendToRouteRequest.$Properties): routerrpc.SendToRouteRequest;
         * }}
         */
        SendToRouteRequest.create = function(properties) {
            return new SendToRouteRequest(properties);
        };

        /**
         * Encodes the specified SendToRouteRequest message. Does not implicitly {@link routerrpc.SendToRouteRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.SendToRouteRequest
         * @static
         * @param {routerrpc.SendToRouteRequest.$Properties} message SendToRouteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendToRouteRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.payment_hash);
            if (message.route != null && $Object.hasOwnProperty.call(message, "route"))
                $root.lnrpc.Route.encode(message.route, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.skip_temp_err != null && $Object.hasOwnProperty.call(message, "skip_temp_err") && message.skip_temp_err !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.skip_temp_err);
            if (message.first_hop_custom_records != null && $Object.hasOwnProperty.call(message, "first_hop_custom_records"))
                for (let keys = $Object.keys(message.first_hop_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.first_hop_custom_records[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendToRouteRequest message, length delimited. Does not implicitly {@link routerrpc.SendToRouteRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.SendToRouteRequest
         * @static
         * @param {routerrpc.SendToRouteRequest.$Properties} message SendToRouteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendToRouteRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendToRouteRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.SendToRouteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.SendToRouteRequest & routerrpc.SendToRouteRequest.$Shape} SendToRouteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendToRouteRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.SendToRouteRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.route = $root.lnrpc.Route.decode(reader, reader.uint32(), $undefined, _depth + 1, message.route);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.skip_temp_err = value;
                        else
                            delete message.skip_temp_err;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (message.first_hop_custom_records === $util.emptyObject)
                            message.first_hop_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.first_hop_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendToRouteRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.SendToRouteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.SendToRouteRequest & routerrpc.SendToRouteRequest.$Shape} SendToRouteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendToRouteRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendToRouteRequest message.
         * @function verify
         * @memberof routerrpc.SendToRouteRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendToRouteRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            if (message.route != null && $Object.hasOwnProperty.call(message, "route")) {
                let error = $root.lnrpc.Route.verify(message.route, _depth + 1);
                if (error)
                    return "route." + error;
            }
            if (message.skip_temp_err != null && $Object.hasOwnProperty.call(message, "skip_temp_err"))
                if (typeof message.skip_temp_err !== "boolean")
                    return "skip_temp_err: boolean expected";
            if (message.first_hop_custom_records != null && $Object.hasOwnProperty.call(message, "first_hop_custom_records")) {
                if (!$util.isObject(message.first_hop_custom_records))
                    return "first_hop_custom_records: object expected";
                let key = $Object.keys(message.first_hop_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "first_hop_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.first_hop_custom_records[key[i]] && typeof message.first_hop_custom_records[key[i]].length === "number" || $util.isString(message.first_hop_custom_records[key[i]])))
                        return "first_hop_custom_records: buffer{k:uint64} expected";
                }
            }
            return null;
        };

        /**
         * Creates a SendToRouteRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.SendToRouteRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.SendToRouteRequest} SendToRouteRequest
         */
        SendToRouteRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.SendToRouteRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.SendToRouteRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.SendToRouteRequest();
            if (object.payment_hash != null)
                if (object.payment_hash.length)
                    if (typeof object.payment_hash === "string")
                        $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                    else if (object.payment_hash.length >= 0)
                        message.payment_hash = object.payment_hash;
            if (object.route != null) {
                if (!$util.isObject(object.route))
                    throw $TypeError(".routerrpc.SendToRouteRequest.route: object expected");
                message.route = $root.lnrpc.Route.fromObject(object.route, _depth + 1);
            }
            if (object.skip_temp_err != null)
                if (object.skip_temp_err)
                    message.skip_temp_err = $Boolean(object.skip_temp_err);
            if (object.first_hop_custom_records) {
                if (!$util.isObject(object.first_hop_custom_records))
                    throw $TypeError(".routerrpc.SendToRouteRequest.first_hop_custom_records: object expected");
                message.first_hop_custom_records = {};
                for (let keys = $Object.keys(object.first_hop_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.first_hop_custom_records, keys[i]);
                    if (typeof object.first_hop_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.first_hop_custom_records[keys[i]], message.first_hop_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.first_hop_custom_records[keys[i]])), 0);
                    else if (object.first_hop_custom_records[keys[i]].length >= 0)
                        message.first_hop_custom_records[keys[i]] = object.first_hop_custom_records[keys[i]];
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SendToRouteRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.SendToRouteRequest
         * @static
         * @param {routerrpc.SendToRouteRequest} message SendToRouteRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendToRouteRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.first_hop_custom_records = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.payment_hash = "";
                else {
                    object.payment_hash = [];
                    if (options.bytes !== $Array)
                        object.payment_hash = $util.newBuffer(object.payment_hash);
                }
                object.route = null;
                object.skip_temp_err = false;
            }
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
            if (message.route != null && $Object.hasOwnProperty.call(message, "route"))
                object.route = $root.lnrpc.Route.toObject(message.route, options, _depth + 1);
            if (message.skip_temp_err != null && $Object.hasOwnProperty.call(message, "skip_temp_err"))
                object.skip_temp_err = message.skip_temp_err;
            let keys2;
            if (message.first_hop_custom_records && (keys2 = $Object.keys(message.first_hop_custom_records)).length) {
                object.first_hop_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.first_hop_custom_records, keys2[j]);
                    object.first_hop_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.first_hop_custom_records[keys2[j]], 0, message.first_hop_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.first_hop_custom_records[keys2[j]]) : message.first_hop_custom_records[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this SendToRouteRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.SendToRouteRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendToRouteRequest.prototype.toJSON = function() {
            return SendToRouteRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendToRouteRequest
         * @function getTypeUrl
         * @memberof routerrpc.SendToRouteRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendToRouteRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.SendToRouteRequest";
        };

        return SendToRouteRequest;
    })();

    routerrpc.SendToRouteResponse = (function() {

        /**
         * Properties of a SendToRouteResponse.
         * @typedef {Object} routerrpc.SendToRouteResponse.$Properties
         * @property {Uint8Array|null} [preimage] SendToRouteResponse preimage
         * @property {lnrpc.Failure.$Properties|null} [failure] SendToRouteResponse failure
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendToRouteResponse.
         * @memberof routerrpc
         * @interface ISendToRouteResponse
         * @augments routerrpc.SendToRouteResponse.$Properties
         * @deprecated Use routerrpc.SendToRouteResponse.$Properties instead.
         */

        /**
         * Shape of a SendToRouteResponse.
         * @typedef {routerrpc.SendToRouteResponse.$Properties} routerrpc.SendToRouteResponse.$Shape
         */

        /**
         * Constructs a new SendToRouteResponse.
         * @memberof routerrpc
         * @classdesc Represents a SendToRouteResponse.
         * @constructor
         * @param {routerrpc.SendToRouteResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendToRouteResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendToRouteResponse preimage.
         * @member {Uint8Array} preimage
         * @memberof routerrpc.SendToRouteResponse
         * @instance
         */
        SendToRouteResponse.prototype.preimage = $util.newBuffer([]);

        /**
         * SendToRouteResponse failure.
         * @member {lnrpc.Failure.$Properties|null|undefined} failure
         * @memberof routerrpc.SendToRouteResponse
         * @instance
         */
        SendToRouteResponse.prototype.failure = null;

        /**
         * Creates a new SendToRouteResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.SendToRouteResponse
         * @static
         * @param {routerrpc.SendToRouteResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.SendToRouteResponse} SendToRouteResponse instance
         * @type {{
         *   (properties: routerrpc.SendToRouteResponse.$Shape): routerrpc.SendToRouteResponse & routerrpc.SendToRouteResponse.$Shape;
         *   (properties?: routerrpc.SendToRouteResponse.$Properties): routerrpc.SendToRouteResponse;
         * }}
         */
        SendToRouteResponse.create = function(properties) {
            return new SendToRouteResponse(properties);
        };

        /**
         * Encodes the specified SendToRouteResponse message. Does not implicitly {@link routerrpc.SendToRouteResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.SendToRouteResponse
         * @static
         * @param {routerrpc.SendToRouteResponse.$Properties} message SendToRouteResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendToRouteResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage") && message.preimage.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.preimage);
            if (message.failure != null && $Object.hasOwnProperty.call(message, "failure"))
                $root.lnrpc.Failure.encode(message.failure, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendToRouteResponse message, length delimited. Does not implicitly {@link routerrpc.SendToRouteResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.SendToRouteResponse
         * @static
         * @param {routerrpc.SendToRouteResponse.$Properties} message SendToRouteResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendToRouteResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendToRouteResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.SendToRouteResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.SendToRouteResponse & routerrpc.SendToRouteResponse.$Shape} SendToRouteResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendToRouteResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.SendToRouteResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.preimage = value;
                        else
                            delete message.preimage;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.failure = $root.lnrpc.Failure.decode(reader, reader.uint32(), $undefined, _depth + 1, message.failure);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendToRouteResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.SendToRouteResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.SendToRouteResponse & routerrpc.SendToRouteResponse.$Shape} SendToRouteResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendToRouteResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendToRouteResponse message.
         * @function verify
         * @memberof routerrpc.SendToRouteResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendToRouteResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                if (!(message.preimage && typeof message.preimage.length === "number" || $util.isString(message.preimage)))
                    return "preimage: buffer expected";
            if (message.failure != null && $Object.hasOwnProperty.call(message, "failure")) {
                let error = $root.lnrpc.Failure.verify(message.failure, _depth + 1);
                if (error)
                    return "failure." + error;
            }
            return null;
        };

        /**
         * Creates a SendToRouteResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.SendToRouteResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.SendToRouteResponse} SendToRouteResponse
         */
        SendToRouteResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.SendToRouteResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.SendToRouteResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.SendToRouteResponse();
            if (object.preimage != null)
                if (object.preimage.length)
                    if (typeof object.preimage === "string")
                        $util.base64.decode(object.preimage, message.preimage = $util.newBuffer($util.base64.length(object.preimage)), 0);
                    else if (object.preimage.length >= 0)
                        message.preimage = object.preimage;
            if (object.failure != null) {
                if (!$util.isObject(object.failure))
                    throw $TypeError(".routerrpc.SendToRouteResponse.failure: object expected");
                message.failure = $root.lnrpc.Failure.fromObject(object.failure, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SendToRouteResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.SendToRouteResponse
         * @static
         * @param {routerrpc.SendToRouteResponse} message SendToRouteResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendToRouteResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.preimage = "";
                else {
                    object.preimage = [];
                    if (options.bytes !== $Array)
                        object.preimage = $util.newBuffer(object.preimage);
                }
                object.failure = null;
            }
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                object.preimage = options.bytes === $String ? $util.base64.encode(message.preimage, 0, message.preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.preimage) : message.preimage;
            if (message.failure != null && $Object.hasOwnProperty.call(message, "failure"))
                object.failure = $root.lnrpc.Failure.toObject(message.failure, options, _depth + 1);
            return object;
        };

        /**
         * Converts this SendToRouteResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.SendToRouteResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendToRouteResponse.prototype.toJSON = function() {
            return SendToRouteResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendToRouteResponse
         * @function getTypeUrl
         * @memberof routerrpc.SendToRouteResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendToRouteResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.SendToRouteResponse";
        };

        return SendToRouteResponse;
    })();

    routerrpc.ResetMissionControlRequest = (function() {

        /**
         * Properties of a ResetMissionControlRequest.
         * @typedef {Object} routerrpc.ResetMissionControlRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ResetMissionControlRequest.
         * @memberof routerrpc
         * @interface IResetMissionControlRequest
         * @augments routerrpc.ResetMissionControlRequest.$Properties
         * @deprecated Use routerrpc.ResetMissionControlRequest.$Properties instead.
         */

        /**
         * Shape of a ResetMissionControlRequest.
         * @typedef {routerrpc.ResetMissionControlRequest.$Properties} routerrpc.ResetMissionControlRequest.$Shape
         */

        /**
         * Constructs a new ResetMissionControlRequest.
         * @memberof routerrpc
         * @classdesc Represents a ResetMissionControlRequest.
         * @constructor
         * @param {routerrpc.ResetMissionControlRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ResetMissionControlRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ResetMissionControlRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.ResetMissionControlRequest
         * @static
         * @param {routerrpc.ResetMissionControlRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.ResetMissionControlRequest} ResetMissionControlRequest instance
         * @type {{
         *   (properties: routerrpc.ResetMissionControlRequest.$Shape): routerrpc.ResetMissionControlRequest & routerrpc.ResetMissionControlRequest.$Shape;
         *   (properties?: routerrpc.ResetMissionControlRequest.$Properties): routerrpc.ResetMissionControlRequest;
         * }}
         */
        ResetMissionControlRequest.create = function(properties) {
            return new ResetMissionControlRequest(properties);
        };

        /**
         * Encodes the specified ResetMissionControlRequest message. Does not implicitly {@link routerrpc.ResetMissionControlRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.ResetMissionControlRequest
         * @static
         * @param {routerrpc.ResetMissionControlRequest.$Properties} message ResetMissionControlRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResetMissionControlRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ResetMissionControlRequest message, length delimited. Does not implicitly {@link routerrpc.ResetMissionControlRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.ResetMissionControlRequest
         * @static
         * @param {routerrpc.ResetMissionControlRequest.$Properties} message ResetMissionControlRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResetMissionControlRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ResetMissionControlRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.ResetMissionControlRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.ResetMissionControlRequest & routerrpc.ResetMissionControlRequest.$Shape} ResetMissionControlRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResetMissionControlRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.ResetMissionControlRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ResetMissionControlRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.ResetMissionControlRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.ResetMissionControlRequest & routerrpc.ResetMissionControlRequest.$Shape} ResetMissionControlRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResetMissionControlRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ResetMissionControlRequest message.
         * @function verify
         * @memberof routerrpc.ResetMissionControlRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ResetMissionControlRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ResetMissionControlRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.ResetMissionControlRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.ResetMissionControlRequest} ResetMissionControlRequest
         */
        ResetMissionControlRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.ResetMissionControlRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.ResetMissionControlRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.ResetMissionControlRequest();
        };

        /**
         * Creates a plain object from a ResetMissionControlRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.ResetMissionControlRequest
         * @static
         * @param {routerrpc.ResetMissionControlRequest} message ResetMissionControlRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ResetMissionControlRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this ResetMissionControlRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.ResetMissionControlRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ResetMissionControlRequest.prototype.toJSON = function() {
            return ResetMissionControlRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ResetMissionControlRequest
         * @function getTypeUrl
         * @memberof routerrpc.ResetMissionControlRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ResetMissionControlRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.ResetMissionControlRequest";
        };

        return ResetMissionControlRequest;
    })();

    routerrpc.ResetMissionControlResponse = (function() {

        /**
         * Properties of a ResetMissionControlResponse.
         * @typedef {Object} routerrpc.ResetMissionControlResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ResetMissionControlResponse.
         * @memberof routerrpc
         * @interface IResetMissionControlResponse
         * @augments routerrpc.ResetMissionControlResponse.$Properties
         * @deprecated Use routerrpc.ResetMissionControlResponse.$Properties instead.
         */

        /**
         * Shape of a ResetMissionControlResponse.
         * @typedef {routerrpc.ResetMissionControlResponse.$Properties} routerrpc.ResetMissionControlResponse.$Shape
         */

        /**
         * Constructs a new ResetMissionControlResponse.
         * @memberof routerrpc
         * @classdesc Represents a ResetMissionControlResponse.
         * @constructor
         * @param {routerrpc.ResetMissionControlResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ResetMissionControlResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ResetMissionControlResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.ResetMissionControlResponse
         * @static
         * @param {routerrpc.ResetMissionControlResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.ResetMissionControlResponse} ResetMissionControlResponse instance
         * @type {{
         *   (properties: routerrpc.ResetMissionControlResponse.$Shape): routerrpc.ResetMissionControlResponse & routerrpc.ResetMissionControlResponse.$Shape;
         *   (properties?: routerrpc.ResetMissionControlResponse.$Properties): routerrpc.ResetMissionControlResponse;
         * }}
         */
        ResetMissionControlResponse.create = function(properties) {
            return new ResetMissionControlResponse(properties);
        };

        /**
         * Encodes the specified ResetMissionControlResponse message. Does not implicitly {@link routerrpc.ResetMissionControlResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.ResetMissionControlResponse
         * @static
         * @param {routerrpc.ResetMissionControlResponse.$Properties} message ResetMissionControlResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResetMissionControlResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ResetMissionControlResponse message, length delimited. Does not implicitly {@link routerrpc.ResetMissionControlResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.ResetMissionControlResponse
         * @static
         * @param {routerrpc.ResetMissionControlResponse.$Properties} message ResetMissionControlResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResetMissionControlResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ResetMissionControlResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.ResetMissionControlResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.ResetMissionControlResponse & routerrpc.ResetMissionControlResponse.$Shape} ResetMissionControlResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResetMissionControlResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.ResetMissionControlResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ResetMissionControlResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.ResetMissionControlResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.ResetMissionControlResponse & routerrpc.ResetMissionControlResponse.$Shape} ResetMissionControlResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResetMissionControlResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ResetMissionControlResponse message.
         * @function verify
         * @memberof routerrpc.ResetMissionControlResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ResetMissionControlResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ResetMissionControlResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.ResetMissionControlResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.ResetMissionControlResponse} ResetMissionControlResponse
         */
        ResetMissionControlResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.ResetMissionControlResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.ResetMissionControlResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.ResetMissionControlResponse();
        };

        /**
         * Creates a plain object from a ResetMissionControlResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.ResetMissionControlResponse
         * @static
         * @param {routerrpc.ResetMissionControlResponse} message ResetMissionControlResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ResetMissionControlResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this ResetMissionControlResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.ResetMissionControlResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ResetMissionControlResponse.prototype.toJSON = function() {
            return ResetMissionControlResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ResetMissionControlResponse
         * @function getTypeUrl
         * @memberof routerrpc.ResetMissionControlResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ResetMissionControlResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.ResetMissionControlResponse";
        };

        return ResetMissionControlResponse;
    })();

    routerrpc.QueryMissionControlRequest = (function() {

        /**
         * Properties of a QueryMissionControlRequest.
         * @typedef {Object} routerrpc.QueryMissionControlRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a QueryMissionControlRequest.
         * @memberof routerrpc
         * @interface IQueryMissionControlRequest
         * @augments routerrpc.QueryMissionControlRequest.$Properties
         * @deprecated Use routerrpc.QueryMissionControlRequest.$Properties instead.
         */

        /**
         * Shape of a QueryMissionControlRequest.
         * @typedef {routerrpc.QueryMissionControlRequest.$Properties} routerrpc.QueryMissionControlRequest.$Shape
         */

        /**
         * Constructs a new QueryMissionControlRequest.
         * @memberof routerrpc
         * @classdesc Represents a QueryMissionControlRequest.
         * @constructor
         * @param {routerrpc.QueryMissionControlRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const QueryMissionControlRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new QueryMissionControlRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.QueryMissionControlRequest
         * @static
         * @param {routerrpc.QueryMissionControlRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.QueryMissionControlRequest} QueryMissionControlRequest instance
         * @type {{
         *   (properties: routerrpc.QueryMissionControlRequest.$Shape): routerrpc.QueryMissionControlRequest & routerrpc.QueryMissionControlRequest.$Shape;
         *   (properties?: routerrpc.QueryMissionControlRequest.$Properties): routerrpc.QueryMissionControlRequest;
         * }}
         */
        QueryMissionControlRequest.create = function(properties) {
            return new QueryMissionControlRequest(properties);
        };

        /**
         * Encodes the specified QueryMissionControlRequest message. Does not implicitly {@link routerrpc.QueryMissionControlRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.QueryMissionControlRequest
         * @static
         * @param {routerrpc.QueryMissionControlRequest.$Properties} message QueryMissionControlRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryMissionControlRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QueryMissionControlRequest message, length delimited. Does not implicitly {@link routerrpc.QueryMissionControlRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.QueryMissionControlRequest
         * @static
         * @param {routerrpc.QueryMissionControlRequest.$Properties} message QueryMissionControlRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryMissionControlRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryMissionControlRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.QueryMissionControlRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.QueryMissionControlRequest & routerrpc.QueryMissionControlRequest.$Shape} QueryMissionControlRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryMissionControlRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.QueryMissionControlRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QueryMissionControlRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.QueryMissionControlRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.QueryMissionControlRequest & routerrpc.QueryMissionControlRequest.$Shape} QueryMissionControlRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryMissionControlRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryMissionControlRequest message.
         * @function verify
         * @memberof routerrpc.QueryMissionControlRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryMissionControlRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a QueryMissionControlRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.QueryMissionControlRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.QueryMissionControlRequest} QueryMissionControlRequest
         */
        QueryMissionControlRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.QueryMissionControlRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.QueryMissionControlRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.QueryMissionControlRequest();
        };

        /**
         * Creates a plain object from a QueryMissionControlRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.QueryMissionControlRequest
         * @static
         * @param {routerrpc.QueryMissionControlRequest} message QueryMissionControlRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryMissionControlRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this QueryMissionControlRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.QueryMissionControlRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryMissionControlRequest.prototype.toJSON = function() {
            return QueryMissionControlRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QueryMissionControlRequest
         * @function getTypeUrl
         * @memberof routerrpc.QueryMissionControlRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QueryMissionControlRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.QueryMissionControlRequest";
        };

        return QueryMissionControlRequest;
    })();

    routerrpc.QueryMissionControlResponse = (function() {

        /**
         * Properties of a QueryMissionControlResponse.
         * @typedef {Object} routerrpc.QueryMissionControlResponse.$Properties
         * @property {Array.<routerrpc.PairHistory.$Properties>|null} [pairs] QueryMissionControlResponse pairs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a QueryMissionControlResponse.
         * @memberof routerrpc
         * @interface IQueryMissionControlResponse
         * @augments routerrpc.QueryMissionControlResponse.$Properties
         * @deprecated Use routerrpc.QueryMissionControlResponse.$Properties instead.
         */

        /**
         * Shape of a QueryMissionControlResponse.
         * @typedef {routerrpc.QueryMissionControlResponse.$Properties} routerrpc.QueryMissionControlResponse.$Shape
         */

        /**
         * Constructs a new QueryMissionControlResponse.
         * @memberof routerrpc
         * @classdesc Represents a QueryMissionControlResponse.
         * @constructor
         * @param {routerrpc.QueryMissionControlResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const QueryMissionControlResponse = function (properties) {
            this.pairs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * QueryMissionControlResponse pairs.
         * @member {Array.<routerrpc.PairHistory.$Properties>} pairs
         * @memberof routerrpc.QueryMissionControlResponse
         * @instance
         */
        QueryMissionControlResponse.prototype.pairs = $util.emptyArray;

        /**
         * Creates a new QueryMissionControlResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.QueryMissionControlResponse
         * @static
         * @param {routerrpc.QueryMissionControlResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.QueryMissionControlResponse} QueryMissionControlResponse instance
         * @type {{
         *   (properties: routerrpc.QueryMissionControlResponse.$Shape): routerrpc.QueryMissionControlResponse & routerrpc.QueryMissionControlResponse.$Shape;
         *   (properties?: routerrpc.QueryMissionControlResponse.$Properties): routerrpc.QueryMissionControlResponse;
         * }}
         */
        QueryMissionControlResponse.create = function(properties) {
            return new QueryMissionControlResponse(properties);
        };

        /**
         * Encodes the specified QueryMissionControlResponse message. Does not implicitly {@link routerrpc.QueryMissionControlResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.QueryMissionControlResponse
         * @static
         * @param {routerrpc.QueryMissionControlResponse.$Properties} message QueryMissionControlResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryMissionControlResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pairs != null && message.pairs.length)
                for (let i = 0; i < message.pairs.length; ++i)
                    $root.routerrpc.PairHistory.encode(message.pairs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QueryMissionControlResponse message, length delimited. Does not implicitly {@link routerrpc.QueryMissionControlResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.QueryMissionControlResponse
         * @static
         * @param {routerrpc.QueryMissionControlResponse.$Properties} message QueryMissionControlResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryMissionControlResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryMissionControlResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.QueryMissionControlResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.QueryMissionControlResponse & routerrpc.QueryMissionControlResponse.$Shape} QueryMissionControlResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryMissionControlResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.QueryMissionControlResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pairs && message.pairs.length))
                            message.pairs = [];
                        message.pairs.push($root.routerrpc.PairHistory.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QueryMissionControlResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.QueryMissionControlResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.QueryMissionControlResponse & routerrpc.QueryMissionControlResponse.$Shape} QueryMissionControlResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryMissionControlResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryMissionControlResponse message.
         * @function verify
         * @memberof routerrpc.QueryMissionControlResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryMissionControlResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pairs != null && $Object.hasOwnProperty.call(message, "pairs")) {
                if (!$Array.isArray(message.pairs))
                    return "pairs: array expected";
                for (let i = 0; i < message.pairs.length; ++i) {
                    let error = $root.routerrpc.PairHistory.verify(message.pairs[i], _depth + 1);
                    if (error)
                        return "pairs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a QueryMissionControlResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.QueryMissionControlResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.QueryMissionControlResponse} QueryMissionControlResponse
         */
        QueryMissionControlResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.QueryMissionControlResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.QueryMissionControlResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.QueryMissionControlResponse();
            if (object.pairs) {
                if (!$Array.isArray(object.pairs))
                    throw $TypeError(".routerrpc.QueryMissionControlResponse.pairs: array expected");
                message.pairs = $Array(object.pairs.length);
                for (let i = 0; i < object.pairs.length; ++i) {
                    if (!$util.isObject(object.pairs[i]))
                        throw $TypeError(".routerrpc.QueryMissionControlResponse.pairs: object expected");
                    message.pairs[i] = $root.routerrpc.PairHistory.fromObject(object.pairs[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a QueryMissionControlResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.QueryMissionControlResponse
         * @static
         * @param {routerrpc.QueryMissionControlResponse} message QueryMissionControlResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryMissionControlResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.pairs = [];
            if (message.pairs && message.pairs.length) {
                object.pairs = $Array(message.pairs.length);
                for (let j = 0; j < message.pairs.length; ++j)
                    object.pairs[j] = $root.routerrpc.PairHistory.toObject(message.pairs[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this QueryMissionControlResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.QueryMissionControlResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryMissionControlResponse.prototype.toJSON = function() {
            return QueryMissionControlResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QueryMissionControlResponse
         * @function getTypeUrl
         * @memberof routerrpc.QueryMissionControlResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QueryMissionControlResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.QueryMissionControlResponse";
        };

        return QueryMissionControlResponse;
    })();

    routerrpc.XImportMissionControlRequest = (function() {

        /**
         * Properties of a XImportMissionControlRequest.
         * @typedef {Object} routerrpc.XImportMissionControlRequest.$Properties
         * @property {Array.<routerrpc.PairHistory.$Properties>|null} [pairs] XImportMissionControlRequest pairs
         * @property {boolean|null} [force] XImportMissionControlRequest force
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a XImportMissionControlRequest.
         * @memberof routerrpc
         * @interface IXImportMissionControlRequest
         * @augments routerrpc.XImportMissionControlRequest.$Properties
         * @deprecated Use routerrpc.XImportMissionControlRequest.$Properties instead.
         */

        /**
         * Shape of a XImportMissionControlRequest.
         * @typedef {routerrpc.XImportMissionControlRequest.$Properties} routerrpc.XImportMissionControlRequest.$Shape
         */

        /**
         * Constructs a new XImportMissionControlRequest.
         * @memberof routerrpc
         * @classdesc Represents a XImportMissionControlRequest.
         * @constructor
         * @param {routerrpc.XImportMissionControlRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const XImportMissionControlRequest = function (properties) {
            this.pairs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * XImportMissionControlRequest pairs.
         * @member {Array.<routerrpc.PairHistory.$Properties>} pairs
         * @memberof routerrpc.XImportMissionControlRequest
         * @instance
         */
        XImportMissionControlRequest.prototype.pairs = $util.emptyArray;

        /**
         * XImportMissionControlRequest force.
         * @member {boolean} force
         * @memberof routerrpc.XImportMissionControlRequest
         * @instance
         */
        XImportMissionControlRequest.prototype.force = false;

        /**
         * Creates a new XImportMissionControlRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.XImportMissionControlRequest
         * @static
         * @param {routerrpc.XImportMissionControlRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.XImportMissionControlRequest} XImportMissionControlRequest instance
         * @type {{
         *   (properties: routerrpc.XImportMissionControlRequest.$Shape): routerrpc.XImportMissionControlRequest & routerrpc.XImportMissionControlRequest.$Shape;
         *   (properties?: routerrpc.XImportMissionControlRequest.$Properties): routerrpc.XImportMissionControlRequest;
         * }}
         */
        XImportMissionControlRequest.create = function(properties) {
            return new XImportMissionControlRequest(properties);
        };

        /**
         * Encodes the specified XImportMissionControlRequest message. Does not implicitly {@link routerrpc.XImportMissionControlRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.XImportMissionControlRequest
         * @static
         * @param {routerrpc.XImportMissionControlRequest.$Properties} message XImportMissionControlRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        XImportMissionControlRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pairs != null && message.pairs.length)
                for (let i = 0; i < message.pairs.length; ++i)
                    $root.routerrpc.PairHistory.encode(message.pairs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.force != null && $Object.hasOwnProperty.call(message, "force") && message.force !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.force);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified XImportMissionControlRequest message, length delimited. Does not implicitly {@link routerrpc.XImportMissionControlRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.XImportMissionControlRequest
         * @static
         * @param {routerrpc.XImportMissionControlRequest.$Properties} message XImportMissionControlRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        XImportMissionControlRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a XImportMissionControlRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.XImportMissionControlRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.XImportMissionControlRequest & routerrpc.XImportMissionControlRequest.$Shape} XImportMissionControlRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        XImportMissionControlRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.XImportMissionControlRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pairs && message.pairs.length))
                            message.pairs = [];
                        message.pairs.push($root.routerrpc.PairHistory.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.force = value;
                        else
                            delete message.force;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a XImportMissionControlRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.XImportMissionControlRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.XImportMissionControlRequest & routerrpc.XImportMissionControlRequest.$Shape} XImportMissionControlRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        XImportMissionControlRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a XImportMissionControlRequest message.
         * @function verify
         * @memberof routerrpc.XImportMissionControlRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        XImportMissionControlRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pairs != null && $Object.hasOwnProperty.call(message, "pairs")) {
                if (!$Array.isArray(message.pairs))
                    return "pairs: array expected";
                for (let i = 0; i < message.pairs.length; ++i) {
                    let error = $root.routerrpc.PairHistory.verify(message.pairs[i], _depth + 1);
                    if (error)
                        return "pairs." + error;
                }
            }
            if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                if (typeof message.force !== "boolean")
                    return "force: boolean expected";
            return null;
        };

        /**
         * Creates a XImportMissionControlRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.XImportMissionControlRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.XImportMissionControlRequest} XImportMissionControlRequest
         */
        XImportMissionControlRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.XImportMissionControlRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.XImportMissionControlRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.XImportMissionControlRequest();
            if (object.pairs) {
                if (!$Array.isArray(object.pairs))
                    throw $TypeError(".routerrpc.XImportMissionControlRequest.pairs: array expected");
                message.pairs = $Array(object.pairs.length);
                for (let i = 0; i < object.pairs.length; ++i) {
                    if (!$util.isObject(object.pairs[i]))
                        throw $TypeError(".routerrpc.XImportMissionControlRequest.pairs: object expected");
                    message.pairs[i] = $root.routerrpc.PairHistory.fromObject(object.pairs[i], _depth + 1);
                }
            }
            if (object.force != null)
                if (object.force)
                    message.force = $Boolean(object.force);
            return message;
        };

        /**
         * Creates a plain object from a XImportMissionControlRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.XImportMissionControlRequest
         * @static
         * @param {routerrpc.XImportMissionControlRequest} message XImportMissionControlRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        XImportMissionControlRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.pairs = [];
            if (options.defaults)
                object.force = false;
            if (message.pairs && message.pairs.length) {
                object.pairs = $Array(message.pairs.length);
                for (let j = 0; j < message.pairs.length; ++j)
                    object.pairs[j] = $root.routerrpc.PairHistory.toObject(message.pairs[j], options, _depth + 1);
            }
            if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                object.force = message.force;
            return object;
        };

        /**
         * Converts this XImportMissionControlRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.XImportMissionControlRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        XImportMissionControlRequest.prototype.toJSON = function() {
            return XImportMissionControlRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for XImportMissionControlRequest
         * @function getTypeUrl
         * @memberof routerrpc.XImportMissionControlRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        XImportMissionControlRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.XImportMissionControlRequest";
        };

        return XImportMissionControlRequest;
    })();

    routerrpc.XImportMissionControlResponse = (function() {

        /**
         * Properties of a XImportMissionControlResponse.
         * @typedef {Object} routerrpc.XImportMissionControlResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a XImportMissionControlResponse.
         * @memberof routerrpc
         * @interface IXImportMissionControlResponse
         * @augments routerrpc.XImportMissionControlResponse.$Properties
         * @deprecated Use routerrpc.XImportMissionControlResponse.$Properties instead.
         */

        /**
         * Shape of a XImportMissionControlResponse.
         * @typedef {routerrpc.XImportMissionControlResponse.$Properties} routerrpc.XImportMissionControlResponse.$Shape
         */

        /**
         * Constructs a new XImportMissionControlResponse.
         * @memberof routerrpc
         * @classdesc Represents a XImportMissionControlResponse.
         * @constructor
         * @param {routerrpc.XImportMissionControlResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const XImportMissionControlResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new XImportMissionControlResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.XImportMissionControlResponse
         * @static
         * @param {routerrpc.XImportMissionControlResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.XImportMissionControlResponse} XImportMissionControlResponse instance
         * @type {{
         *   (properties: routerrpc.XImportMissionControlResponse.$Shape): routerrpc.XImportMissionControlResponse & routerrpc.XImportMissionControlResponse.$Shape;
         *   (properties?: routerrpc.XImportMissionControlResponse.$Properties): routerrpc.XImportMissionControlResponse;
         * }}
         */
        XImportMissionControlResponse.create = function(properties) {
            return new XImportMissionControlResponse(properties);
        };

        /**
         * Encodes the specified XImportMissionControlResponse message. Does not implicitly {@link routerrpc.XImportMissionControlResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.XImportMissionControlResponse
         * @static
         * @param {routerrpc.XImportMissionControlResponse.$Properties} message XImportMissionControlResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        XImportMissionControlResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified XImportMissionControlResponse message, length delimited. Does not implicitly {@link routerrpc.XImportMissionControlResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.XImportMissionControlResponse
         * @static
         * @param {routerrpc.XImportMissionControlResponse.$Properties} message XImportMissionControlResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        XImportMissionControlResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a XImportMissionControlResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.XImportMissionControlResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.XImportMissionControlResponse & routerrpc.XImportMissionControlResponse.$Shape} XImportMissionControlResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        XImportMissionControlResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.XImportMissionControlResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a XImportMissionControlResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.XImportMissionControlResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.XImportMissionControlResponse & routerrpc.XImportMissionControlResponse.$Shape} XImportMissionControlResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        XImportMissionControlResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a XImportMissionControlResponse message.
         * @function verify
         * @memberof routerrpc.XImportMissionControlResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        XImportMissionControlResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a XImportMissionControlResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.XImportMissionControlResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.XImportMissionControlResponse} XImportMissionControlResponse
         */
        XImportMissionControlResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.XImportMissionControlResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.XImportMissionControlResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.XImportMissionControlResponse();
        };

        /**
         * Creates a plain object from a XImportMissionControlResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.XImportMissionControlResponse
         * @static
         * @param {routerrpc.XImportMissionControlResponse} message XImportMissionControlResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        XImportMissionControlResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this XImportMissionControlResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.XImportMissionControlResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        XImportMissionControlResponse.prototype.toJSON = function() {
            return XImportMissionControlResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for XImportMissionControlResponse
         * @function getTypeUrl
         * @memberof routerrpc.XImportMissionControlResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        XImportMissionControlResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.XImportMissionControlResponse";
        };

        return XImportMissionControlResponse;
    })();

    routerrpc.PairHistory = (function() {

        /**
         * Properties of a PairHistory.
         * @typedef {Object} routerrpc.PairHistory.$Properties
         * @property {Uint8Array|null} [node_from] PairHistory node_from
         * @property {Uint8Array|null} [node_to] PairHistory node_to
         * @property {routerrpc.PairData.$Properties|null} [history] PairHistory history
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PairHistory.
         * @memberof routerrpc
         * @interface IPairHistory
         * @augments routerrpc.PairHistory.$Properties
         * @deprecated Use routerrpc.PairHistory.$Properties instead.
         */

        /**
         * Shape of a PairHistory.
         * @typedef {routerrpc.PairHistory.$Properties} routerrpc.PairHistory.$Shape
         */

        /**
         * Constructs a new PairHistory.
         * @memberof routerrpc
         * @classdesc Represents a PairHistory.
         * @constructor
         * @param {routerrpc.PairHistory.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PairHistory = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PairHistory node_from.
         * @member {Uint8Array} node_from
         * @memberof routerrpc.PairHistory
         * @instance
         */
        PairHistory.prototype.node_from = $util.newBuffer([]);

        /**
         * PairHistory node_to.
         * @member {Uint8Array} node_to
         * @memberof routerrpc.PairHistory
         * @instance
         */
        PairHistory.prototype.node_to = $util.newBuffer([]);

        /**
         * PairHistory history.
         * @member {routerrpc.PairData.$Properties|null|undefined} history
         * @memberof routerrpc.PairHistory
         * @instance
         */
        PairHistory.prototype.history = null;

        /**
         * Creates a new PairHistory instance using the specified properties.
         * @function create
         * @memberof routerrpc.PairHistory
         * @static
         * @param {routerrpc.PairHistory.$Properties=} [properties] Properties to set
         * @returns {routerrpc.PairHistory} PairHistory instance
         * @type {{
         *   (properties: routerrpc.PairHistory.$Shape): routerrpc.PairHistory & routerrpc.PairHistory.$Shape;
         *   (properties?: routerrpc.PairHistory.$Properties): routerrpc.PairHistory;
         * }}
         */
        PairHistory.create = function(properties) {
            return new PairHistory(properties);
        };

        /**
         * Encodes the specified PairHistory message. Does not implicitly {@link routerrpc.PairHistory.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.PairHistory
         * @static
         * @param {routerrpc.PairHistory.$Properties} message PairHistory message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PairHistory.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.node_from != null && $Object.hasOwnProperty.call(message, "node_from") && message.node_from.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.node_from);
            if (message.node_to != null && $Object.hasOwnProperty.call(message, "node_to") && message.node_to.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.node_to);
            if (message.history != null && $Object.hasOwnProperty.call(message, "history"))
                $root.routerrpc.PairData.encode(message.history, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PairHistory message, length delimited. Does not implicitly {@link routerrpc.PairHistory.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.PairHistory
         * @static
         * @param {routerrpc.PairHistory.$Properties} message PairHistory message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PairHistory.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PairHistory message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.PairHistory
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.PairHistory & routerrpc.PairHistory.$Shape} PairHistory
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PairHistory.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.PairHistory(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.node_from = value;
                        else
                            delete message.node_from;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.node_to = value;
                        else
                            delete message.node_to;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.history = $root.routerrpc.PairData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.history);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PairHistory message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.PairHistory
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.PairHistory & routerrpc.PairHistory.$Shape} PairHistory
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PairHistory.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PairHistory message.
         * @function verify
         * @memberof routerrpc.PairHistory
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PairHistory.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.node_from != null && $Object.hasOwnProperty.call(message, "node_from"))
                if (!(message.node_from && typeof message.node_from.length === "number" || $util.isString(message.node_from)))
                    return "node_from: buffer expected";
            if (message.node_to != null && $Object.hasOwnProperty.call(message, "node_to"))
                if (!(message.node_to && typeof message.node_to.length === "number" || $util.isString(message.node_to)))
                    return "node_to: buffer expected";
            if (message.history != null && $Object.hasOwnProperty.call(message, "history")) {
                let error = $root.routerrpc.PairData.verify(message.history, _depth + 1);
                if (error)
                    return "history." + error;
            }
            return null;
        };

        /**
         * Creates a PairHistory message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.PairHistory
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.PairHistory} PairHistory
         */
        PairHistory.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.PairHistory)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.PairHistory: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.PairHistory();
            if (object.node_from != null)
                if (object.node_from.length)
                    if (typeof object.node_from === "string")
                        $util.base64.decode(object.node_from, message.node_from = $util.newBuffer($util.base64.length(object.node_from)), 0);
                    else if (object.node_from.length >= 0)
                        message.node_from = object.node_from;
            if (object.node_to != null)
                if (object.node_to.length)
                    if (typeof object.node_to === "string")
                        $util.base64.decode(object.node_to, message.node_to = $util.newBuffer($util.base64.length(object.node_to)), 0);
                    else if (object.node_to.length >= 0)
                        message.node_to = object.node_to;
            if (object.history != null) {
                if (!$util.isObject(object.history))
                    throw $TypeError(".routerrpc.PairHistory.history: object expected");
                message.history = $root.routerrpc.PairData.fromObject(object.history, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PairHistory message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.PairHistory
         * @static
         * @param {routerrpc.PairHistory} message PairHistory
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PairHistory.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.node_from = "";
                else {
                    object.node_from = [];
                    if (options.bytes !== $Array)
                        object.node_from = $util.newBuffer(object.node_from);
                }
                if (options.bytes === $String)
                    object.node_to = "";
                else {
                    object.node_to = [];
                    if (options.bytes !== $Array)
                        object.node_to = $util.newBuffer(object.node_to);
                }
                object.history = null;
            }
            if (message.node_from != null && $Object.hasOwnProperty.call(message, "node_from"))
                object.node_from = options.bytes === $String ? $util.base64.encode(message.node_from, 0, message.node_from.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.node_from) : message.node_from;
            if (message.node_to != null && $Object.hasOwnProperty.call(message, "node_to"))
                object.node_to = options.bytes === $String ? $util.base64.encode(message.node_to, 0, message.node_to.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.node_to) : message.node_to;
            if (message.history != null && $Object.hasOwnProperty.call(message, "history"))
                object.history = $root.routerrpc.PairData.toObject(message.history, options, _depth + 1);
            return object;
        };

        /**
         * Converts this PairHistory to JSON.
         * @function toJSON
         * @memberof routerrpc.PairHistory
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PairHistory.prototype.toJSON = function() {
            return PairHistory.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PairHistory
         * @function getTypeUrl
         * @memberof routerrpc.PairHistory
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PairHistory.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.PairHistory";
        };

        return PairHistory;
    })();

    routerrpc.PairData = (function() {

        /**
         * Properties of a PairData.
         * @typedef {Object} routerrpc.PairData.$Properties
         * @property {Long|null} [fail_time] PairData fail_time
         * @property {Long|null} [fail_amt_sat] PairData fail_amt_sat
         * @property {Long|null} [fail_amt_msat] PairData fail_amt_msat
         * @property {Long|null} [success_time] PairData success_time
         * @property {Long|null} [success_amt_sat] PairData success_amt_sat
         * @property {Long|null} [success_amt_msat] PairData success_amt_msat
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PairData.
         * @memberof routerrpc
         * @interface IPairData
         * @augments routerrpc.PairData.$Properties
         * @deprecated Use routerrpc.PairData.$Properties instead.
         */

        /**
         * Shape of a PairData.
         * @typedef {routerrpc.PairData.$Properties} routerrpc.PairData.$Shape
         */

        /**
         * Constructs a new PairData.
         * @memberof routerrpc
         * @classdesc Represents a PairData.
         * @constructor
         * @param {routerrpc.PairData.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PairData = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PairData fail_time.
         * @member {Long} fail_time
         * @memberof routerrpc.PairData
         * @instance
         */
        PairData.prototype.fail_time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PairData fail_amt_sat.
         * @member {Long} fail_amt_sat
         * @memberof routerrpc.PairData
         * @instance
         */
        PairData.prototype.fail_amt_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PairData fail_amt_msat.
         * @member {Long} fail_amt_msat
         * @memberof routerrpc.PairData
         * @instance
         */
        PairData.prototype.fail_amt_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PairData success_time.
         * @member {Long} success_time
         * @memberof routerrpc.PairData
         * @instance
         */
        PairData.prototype.success_time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PairData success_amt_sat.
         * @member {Long} success_amt_sat
         * @memberof routerrpc.PairData
         * @instance
         */
        PairData.prototype.success_amt_sat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PairData success_amt_msat.
         * @member {Long} success_amt_msat
         * @memberof routerrpc.PairData
         * @instance
         */
        PairData.prototype.success_amt_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new PairData instance using the specified properties.
         * @function create
         * @memberof routerrpc.PairData
         * @static
         * @param {routerrpc.PairData.$Properties=} [properties] Properties to set
         * @returns {routerrpc.PairData} PairData instance
         * @type {{
         *   (properties: routerrpc.PairData.$Shape): routerrpc.PairData & routerrpc.PairData.$Shape;
         *   (properties?: routerrpc.PairData.$Properties): routerrpc.PairData;
         * }}
         */
        PairData.create = function(properties) {
            return new PairData(properties);
        };

        /**
         * Encodes the specified PairData message. Does not implicitly {@link routerrpc.PairData.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.PairData
         * @static
         * @param {routerrpc.PairData.$Properties} message PairData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PairData.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fail_time != null && $Object.hasOwnProperty.call(message, "fail_time") && (typeof message.fail_time === "object" ? message.fail_time.low || message.fail_time.high : message.fail_time !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.fail_time);
            if (message.fail_amt_sat != null && $Object.hasOwnProperty.call(message, "fail_amt_sat") && (typeof message.fail_amt_sat === "object" ? message.fail_amt_sat.low || message.fail_amt_sat.high : message.fail_amt_sat !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.fail_amt_sat);
            if (message.fail_amt_msat != null && $Object.hasOwnProperty.call(message, "fail_amt_msat") && (typeof message.fail_amt_msat === "object" ? message.fail_amt_msat.low || message.fail_amt_msat.high : message.fail_amt_msat !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.fail_amt_msat);
            if (message.success_time != null && $Object.hasOwnProperty.call(message, "success_time") && (typeof message.success_time === "object" ? message.success_time.low || message.success_time.high : message.success_time !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.success_time);
            if (message.success_amt_sat != null && $Object.hasOwnProperty.call(message, "success_amt_sat") && (typeof message.success_amt_sat === "object" ? message.success_amt_sat.low || message.success_amt_sat.high : message.success_amt_sat !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.success_amt_sat);
            if (message.success_amt_msat != null && $Object.hasOwnProperty.call(message, "success_amt_msat") && (typeof message.success_amt_msat === "object" ? message.success_amt_msat.low || message.success_amt_msat.high : message.success_amt_msat !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.success_amt_msat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PairData message, length delimited. Does not implicitly {@link routerrpc.PairData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.PairData
         * @static
         * @param {routerrpc.PairData.$Properties} message PairData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PairData.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PairData message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.PairData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.PairData & routerrpc.PairData.$Shape} PairData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PairData.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.PairData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fail_time = value;
                        else
                            delete message.fail_time;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fail_amt_sat = value;
                        else
                            delete message.fail_amt_sat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fail_amt_msat = value;
                        else
                            delete message.fail_amt_msat;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.success_time = value;
                        else
                            delete message.success_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.success_amt_sat = value;
                        else
                            delete message.success_amt_sat;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.success_amt_msat = value;
                        else
                            delete message.success_amt_msat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PairData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.PairData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.PairData & routerrpc.PairData.$Shape} PairData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PairData.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PairData message.
         * @function verify
         * @memberof routerrpc.PairData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PairData.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fail_time != null && $Object.hasOwnProperty.call(message, "fail_time"))
                if (!$util.isInteger(message.fail_time) && !(message.fail_time && $util.isInteger(message.fail_time.low) && $util.isInteger(message.fail_time.high)))
                    return "fail_time: integer|Long expected";
            if (message.fail_amt_sat != null && $Object.hasOwnProperty.call(message, "fail_amt_sat"))
                if (!$util.isInteger(message.fail_amt_sat) && !(message.fail_amt_sat && $util.isInteger(message.fail_amt_sat.low) && $util.isInteger(message.fail_amt_sat.high)))
                    return "fail_amt_sat: integer|Long expected";
            if (message.fail_amt_msat != null && $Object.hasOwnProperty.call(message, "fail_amt_msat"))
                if (!$util.isInteger(message.fail_amt_msat) && !(message.fail_amt_msat && $util.isInteger(message.fail_amt_msat.low) && $util.isInteger(message.fail_amt_msat.high)))
                    return "fail_amt_msat: integer|Long expected";
            if (message.success_time != null && $Object.hasOwnProperty.call(message, "success_time"))
                if (!$util.isInteger(message.success_time) && !(message.success_time && $util.isInteger(message.success_time.low) && $util.isInteger(message.success_time.high)))
                    return "success_time: integer|Long expected";
            if (message.success_amt_sat != null && $Object.hasOwnProperty.call(message, "success_amt_sat"))
                if (!$util.isInteger(message.success_amt_sat) && !(message.success_amt_sat && $util.isInteger(message.success_amt_sat.low) && $util.isInteger(message.success_amt_sat.high)))
                    return "success_amt_sat: integer|Long expected";
            if (message.success_amt_msat != null && $Object.hasOwnProperty.call(message, "success_amt_msat"))
                if (!$util.isInteger(message.success_amt_msat) && !(message.success_amt_msat && $util.isInteger(message.success_amt_msat.low) && $util.isInteger(message.success_amt_msat.high)))
                    return "success_amt_msat: integer|Long expected";
            return null;
        };

        /**
         * Creates a PairData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.PairData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.PairData} PairData
         */
        PairData.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.PairData)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.PairData: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.PairData();
            if (object.fail_time != null)
                if (typeof object.fail_time === "object" ? object.fail_time.low || object.fail_time.high : $Number(object.fail_time) !== 0)
                    if ($util.Long)
                        message.fail_time = $util.Long.fromValue(object.fail_time, false);
                    else if (typeof object.fail_time === "string")
                        message.fail_time = $parseInt(object.fail_time, 10);
                    else if (typeof object.fail_time === "number")
                        message.fail_time = object.fail_time;
                    else if (typeof object.fail_time === "object")
                        message.fail_time = new $util.LongBits(object.fail_time.low >>> 0, object.fail_time.high >>> 0).toNumber();
            if (object.fail_amt_sat != null)
                if (typeof object.fail_amt_sat === "object" ? object.fail_amt_sat.low || object.fail_amt_sat.high : $Number(object.fail_amt_sat) !== 0)
                    if ($util.Long)
                        message.fail_amt_sat = $util.Long.fromValue(object.fail_amt_sat, false);
                    else if (typeof object.fail_amt_sat === "string")
                        message.fail_amt_sat = $parseInt(object.fail_amt_sat, 10);
                    else if (typeof object.fail_amt_sat === "number")
                        message.fail_amt_sat = object.fail_amt_sat;
                    else if (typeof object.fail_amt_sat === "object")
                        message.fail_amt_sat = new $util.LongBits(object.fail_amt_sat.low >>> 0, object.fail_amt_sat.high >>> 0).toNumber();
            if (object.fail_amt_msat != null)
                if (typeof object.fail_amt_msat === "object" ? object.fail_amt_msat.low || object.fail_amt_msat.high : $Number(object.fail_amt_msat) !== 0)
                    if ($util.Long)
                        message.fail_amt_msat = $util.Long.fromValue(object.fail_amt_msat, false);
                    else if (typeof object.fail_amt_msat === "string")
                        message.fail_amt_msat = $parseInt(object.fail_amt_msat, 10);
                    else if (typeof object.fail_amt_msat === "number")
                        message.fail_amt_msat = object.fail_amt_msat;
                    else if (typeof object.fail_amt_msat === "object")
                        message.fail_amt_msat = new $util.LongBits(object.fail_amt_msat.low >>> 0, object.fail_amt_msat.high >>> 0).toNumber();
            if (object.success_time != null)
                if (typeof object.success_time === "object" ? object.success_time.low || object.success_time.high : $Number(object.success_time) !== 0)
                    if ($util.Long)
                        message.success_time = $util.Long.fromValue(object.success_time, false);
                    else if (typeof object.success_time === "string")
                        message.success_time = $parseInt(object.success_time, 10);
                    else if (typeof object.success_time === "number")
                        message.success_time = object.success_time;
                    else if (typeof object.success_time === "object")
                        message.success_time = new $util.LongBits(object.success_time.low >>> 0, object.success_time.high >>> 0).toNumber();
            if (object.success_amt_sat != null)
                if (typeof object.success_amt_sat === "object" ? object.success_amt_sat.low || object.success_amt_sat.high : $Number(object.success_amt_sat) !== 0)
                    if ($util.Long)
                        message.success_amt_sat = $util.Long.fromValue(object.success_amt_sat, false);
                    else if (typeof object.success_amt_sat === "string")
                        message.success_amt_sat = $parseInt(object.success_amt_sat, 10);
                    else if (typeof object.success_amt_sat === "number")
                        message.success_amt_sat = object.success_amt_sat;
                    else if (typeof object.success_amt_sat === "object")
                        message.success_amt_sat = new $util.LongBits(object.success_amt_sat.low >>> 0, object.success_amt_sat.high >>> 0).toNumber();
            if (object.success_amt_msat != null)
                if (typeof object.success_amt_msat === "object" ? object.success_amt_msat.low || object.success_amt_msat.high : $Number(object.success_amt_msat) !== 0)
                    if ($util.Long)
                        message.success_amt_msat = $util.Long.fromValue(object.success_amt_msat, false);
                    else if (typeof object.success_amt_msat === "string")
                        message.success_amt_msat = $parseInt(object.success_amt_msat, 10);
                    else if (typeof object.success_amt_msat === "number")
                        message.success_amt_msat = object.success_amt_msat;
                    else if (typeof object.success_amt_msat === "object")
                        message.success_amt_msat = new $util.LongBits(object.success_amt_msat.low >>> 0, object.success_amt_msat.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a PairData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.PairData
         * @static
         * @param {routerrpc.PairData} message PairData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PairData.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fail_time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fail_time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fail_amt_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fail_amt_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fail_amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fail_amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.success_time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.success_time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.success_amt_sat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.success_amt_sat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.success_amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.success_amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.fail_time != null && $Object.hasOwnProperty.call(message, "fail_time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fail_time = typeof message.fail_time === "number" ? $BigInt(message.fail_time) : $util.Long.fromBits(message.fail_time.low >>> 0, message.fail_time.high >>> 0, false).toBigInt();
                else if (typeof message.fail_time === "number")
                    object.fail_time = options.longs === $String ? $String(message.fail_time) : message.fail_time;
                else
                    object.fail_time = options.longs === $String ? $util.Long.prototype.toString.call(message.fail_time) : options.longs === $Number ? new $util.LongBits(message.fail_time.low >>> 0, message.fail_time.high >>> 0).toNumber() : message.fail_time;
            if (message.fail_amt_sat != null && $Object.hasOwnProperty.call(message, "fail_amt_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fail_amt_sat = typeof message.fail_amt_sat === "number" ? $BigInt(message.fail_amt_sat) : $util.Long.fromBits(message.fail_amt_sat.low >>> 0, message.fail_amt_sat.high >>> 0, false).toBigInt();
                else if (typeof message.fail_amt_sat === "number")
                    object.fail_amt_sat = options.longs === $String ? $String(message.fail_amt_sat) : message.fail_amt_sat;
                else
                    object.fail_amt_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.fail_amt_sat) : options.longs === $Number ? new $util.LongBits(message.fail_amt_sat.low >>> 0, message.fail_amt_sat.high >>> 0).toNumber() : message.fail_amt_sat;
            if (message.fail_amt_msat != null && $Object.hasOwnProperty.call(message, "fail_amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fail_amt_msat = typeof message.fail_amt_msat === "number" ? $BigInt(message.fail_amt_msat) : $util.Long.fromBits(message.fail_amt_msat.low >>> 0, message.fail_amt_msat.high >>> 0, false).toBigInt();
                else if (typeof message.fail_amt_msat === "number")
                    object.fail_amt_msat = options.longs === $String ? $String(message.fail_amt_msat) : message.fail_amt_msat;
                else
                    object.fail_amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.fail_amt_msat) : options.longs === $Number ? new $util.LongBits(message.fail_amt_msat.low >>> 0, message.fail_amt_msat.high >>> 0).toNumber() : message.fail_amt_msat;
            if (message.success_time != null && $Object.hasOwnProperty.call(message, "success_time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.success_time = typeof message.success_time === "number" ? $BigInt(message.success_time) : $util.Long.fromBits(message.success_time.low >>> 0, message.success_time.high >>> 0, false).toBigInt();
                else if (typeof message.success_time === "number")
                    object.success_time = options.longs === $String ? $String(message.success_time) : message.success_time;
                else
                    object.success_time = options.longs === $String ? $util.Long.prototype.toString.call(message.success_time) : options.longs === $Number ? new $util.LongBits(message.success_time.low >>> 0, message.success_time.high >>> 0).toNumber() : message.success_time;
            if (message.success_amt_sat != null && $Object.hasOwnProperty.call(message, "success_amt_sat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.success_amt_sat = typeof message.success_amt_sat === "number" ? $BigInt(message.success_amt_sat) : $util.Long.fromBits(message.success_amt_sat.low >>> 0, message.success_amt_sat.high >>> 0, false).toBigInt();
                else if (typeof message.success_amt_sat === "number")
                    object.success_amt_sat = options.longs === $String ? $String(message.success_amt_sat) : message.success_amt_sat;
                else
                    object.success_amt_sat = options.longs === $String ? $util.Long.prototype.toString.call(message.success_amt_sat) : options.longs === $Number ? new $util.LongBits(message.success_amt_sat.low >>> 0, message.success_amt_sat.high >>> 0).toNumber() : message.success_amt_sat;
            if (message.success_amt_msat != null && $Object.hasOwnProperty.call(message, "success_amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.success_amt_msat = typeof message.success_amt_msat === "number" ? $BigInt(message.success_amt_msat) : $util.Long.fromBits(message.success_amt_msat.low >>> 0, message.success_amt_msat.high >>> 0, false).toBigInt();
                else if (typeof message.success_amt_msat === "number")
                    object.success_amt_msat = options.longs === $String ? $String(message.success_amt_msat) : message.success_amt_msat;
                else
                    object.success_amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.success_amt_msat) : options.longs === $Number ? new $util.LongBits(message.success_amt_msat.low >>> 0, message.success_amt_msat.high >>> 0).toNumber() : message.success_amt_msat;
            return object;
        };

        /**
         * Converts this PairData to JSON.
         * @function toJSON
         * @memberof routerrpc.PairData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PairData.prototype.toJSON = function() {
            return PairData.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PairData
         * @function getTypeUrl
         * @memberof routerrpc.PairData
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PairData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.PairData";
        };

        return PairData;
    })();

    routerrpc.GetMissionControlConfigRequest = (function() {

        /**
         * Properties of a GetMissionControlConfigRequest.
         * @typedef {Object} routerrpc.GetMissionControlConfigRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetMissionControlConfigRequest.
         * @memberof routerrpc
         * @interface IGetMissionControlConfigRequest
         * @augments routerrpc.GetMissionControlConfigRequest.$Properties
         * @deprecated Use routerrpc.GetMissionControlConfigRequest.$Properties instead.
         */

        /**
         * Shape of a GetMissionControlConfigRequest.
         * @typedef {routerrpc.GetMissionControlConfigRequest.$Properties} routerrpc.GetMissionControlConfigRequest.$Shape
         */

        /**
         * Constructs a new GetMissionControlConfigRequest.
         * @memberof routerrpc
         * @classdesc Represents a GetMissionControlConfigRequest.
         * @constructor
         * @param {routerrpc.GetMissionControlConfigRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetMissionControlConfigRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new GetMissionControlConfigRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @static
         * @param {routerrpc.GetMissionControlConfigRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.GetMissionControlConfigRequest} GetMissionControlConfigRequest instance
         * @type {{
         *   (properties: routerrpc.GetMissionControlConfigRequest.$Shape): routerrpc.GetMissionControlConfigRequest & routerrpc.GetMissionControlConfigRequest.$Shape;
         *   (properties?: routerrpc.GetMissionControlConfigRequest.$Properties): routerrpc.GetMissionControlConfigRequest;
         * }}
         */
        GetMissionControlConfigRequest.create = function(properties) {
            return new GetMissionControlConfigRequest(properties);
        };

        /**
         * Encodes the specified GetMissionControlConfigRequest message. Does not implicitly {@link routerrpc.GetMissionControlConfigRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @static
         * @param {routerrpc.GetMissionControlConfigRequest.$Properties} message GetMissionControlConfigRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetMissionControlConfigRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetMissionControlConfigRequest message, length delimited. Does not implicitly {@link routerrpc.GetMissionControlConfigRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @static
         * @param {routerrpc.GetMissionControlConfigRequest.$Properties} message GetMissionControlConfigRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetMissionControlConfigRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetMissionControlConfigRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.GetMissionControlConfigRequest & routerrpc.GetMissionControlConfigRequest.$Shape} GetMissionControlConfigRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetMissionControlConfigRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.GetMissionControlConfigRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetMissionControlConfigRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.GetMissionControlConfigRequest & routerrpc.GetMissionControlConfigRequest.$Shape} GetMissionControlConfigRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetMissionControlConfigRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetMissionControlConfigRequest message.
         * @function verify
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetMissionControlConfigRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a GetMissionControlConfigRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.GetMissionControlConfigRequest} GetMissionControlConfigRequest
         */
        GetMissionControlConfigRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.GetMissionControlConfigRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.GetMissionControlConfigRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.GetMissionControlConfigRequest();
        };

        /**
         * Creates a plain object from a GetMissionControlConfigRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @static
         * @param {routerrpc.GetMissionControlConfigRequest} message GetMissionControlConfigRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetMissionControlConfigRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this GetMissionControlConfigRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetMissionControlConfigRequest.prototype.toJSON = function() {
            return GetMissionControlConfigRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetMissionControlConfigRequest
         * @function getTypeUrl
         * @memberof routerrpc.GetMissionControlConfigRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetMissionControlConfigRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.GetMissionControlConfigRequest";
        };

        return GetMissionControlConfigRequest;
    })();

    routerrpc.GetMissionControlConfigResponse = (function() {

        /**
         * Properties of a GetMissionControlConfigResponse.
         * @typedef {Object} routerrpc.GetMissionControlConfigResponse.$Properties
         * @property {routerrpc.MissionControlConfig.$Properties|null} [config] GetMissionControlConfigResponse config
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetMissionControlConfigResponse.
         * @memberof routerrpc
         * @interface IGetMissionControlConfigResponse
         * @augments routerrpc.GetMissionControlConfigResponse.$Properties
         * @deprecated Use routerrpc.GetMissionControlConfigResponse.$Properties instead.
         */

        /**
         * Shape of a GetMissionControlConfigResponse.
         * @typedef {{
         *   config?: routerrpc.MissionControlConfig.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} routerrpc.GetMissionControlConfigResponse.$Shape
         */

        /**
         * Constructs a new GetMissionControlConfigResponse.
         * @memberof routerrpc
         * @classdesc Represents a GetMissionControlConfigResponse.
         * @constructor
         * @param {routerrpc.GetMissionControlConfigResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetMissionControlConfigResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetMissionControlConfigResponse config.
         * @member {routerrpc.MissionControlConfig.$Properties|null|undefined} config
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @instance
         */
        GetMissionControlConfigResponse.prototype.config = null;

        /**
         * Creates a new GetMissionControlConfigResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @static
         * @param {routerrpc.GetMissionControlConfigResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.GetMissionControlConfigResponse} GetMissionControlConfigResponse instance
         * @type {{
         *   (properties: routerrpc.GetMissionControlConfigResponse.$Shape): routerrpc.GetMissionControlConfigResponse & routerrpc.GetMissionControlConfigResponse.$Shape;
         *   (properties?: routerrpc.GetMissionControlConfigResponse.$Properties): routerrpc.GetMissionControlConfigResponse;
         * }}
         */
        GetMissionControlConfigResponse.create = function(properties) {
            return new GetMissionControlConfigResponse(properties);
        };

        /**
         * Encodes the specified GetMissionControlConfigResponse message. Does not implicitly {@link routerrpc.GetMissionControlConfigResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @static
         * @param {routerrpc.GetMissionControlConfigResponse.$Properties} message GetMissionControlConfigResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetMissionControlConfigResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                $root.routerrpc.MissionControlConfig.encode(message.config, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetMissionControlConfigResponse message, length delimited. Does not implicitly {@link routerrpc.GetMissionControlConfigResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @static
         * @param {routerrpc.GetMissionControlConfigResponse.$Properties} message GetMissionControlConfigResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetMissionControlConfigResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetMissionControlConfigResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.GetMissionControlConfigResponse & routerrpc.GetMissionControlConfigResponse.$Shape} GetMissionControlConfigResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetMissionControlConfigResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.GetMissionControlConfigResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.config = $root.routerrpc.MissionControlConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetMissionControlConfigResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.GetMissionControlConfigResponse & routerrpc.GetMissionControlConfigResponse.$Shape} GetMissionControlConfigResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetMissionControlConfigResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetMissionControlConfigResponse message.
         * @function verify
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetMissionControlConfigResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                let error = $root.routerrpc.MissionControlConfig.verify(message.config, _depth + 1);
                if (error)
                    return "config." + error;
            }
            return null;
        };

        /**
         * Creates a GetMissionControlConfigResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.GetMissionControlConfigResponse} GetMissionControlConfigResponse
         */
        GetMissionControlConfigResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.GetMissionControlConfigResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.GetMissionControlConfigResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.GetMissionControlConfigResponse();
            if (object.config != null) {
                if (!$util.isObject(object.config))
                    throw $TypeError(".routerrpc.GetMissionControlConfigResponse.config: object expected");
                message.config = $root.routerrpc.MissionControlConfig.fromObject(object.config, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetMissionControlConfigResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @static
         * @param {routerrpc.GetMissionControlConfigResponse} message GetMissionControlConfigResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetMissionControlConfigResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.config = null;
            if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                object.config = $root.routerrpc.MissionControlConfig.toObject(message.config, options, _depth + 1);
            return object;
        };

        /**
         * Converts this GetMissionControlConfigResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetMissionControlConfigResponse.prototype.toJSON = function() {
            return GetMissionControlConfigResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetMissionControlConfigResponse
         * @function getTypeUrl
         * @memberof routerrpc.GetMissionControlConfigResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetMissionControlConfigResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.GetMissionControlConfigResponse";
        };

        return GetMissionControlConfigResponse;
    })();

    routerrpc.SetMissionControlConfigRequest = (function() {

        /**
         * Properties of a SetMissionControlConfigRequest.
         * @typedef {Object} routerrpc.SetMissionControlConfigRequest.$Properties
         * @property {routerrpc.MissionControlConfig.$Properties|null} [config] SetMissionControlConfigRequest config
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SetMissionControlConfigRequest.
         * @memberof routerrpc
         * @interface ISetMissionControlConfigRequest
         * @augments routerrpc.SetMissionControlConfigRequest.$Properties
         * @deprecated Use routerrpc.SetMissionControlConfigRequest.$Properties instead.
         */

        /**
         * Shape of a SetMissionControlConfigRequest.
         * @typedef {{
         *   config?: routerrpc.MissionControlConfig.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} routerrpc.SetMissionControlConfigRequest.$Shape
         */

        /**
         * Constructs a new SetMissionControlConfigRequest.
         * @memberof routerrpc
         * @classdesc Represents a SetMissionControlConfigRequest.
         * @constructor
         * @param {routerrpc.SetMissionControlConfigRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SetMissionControlConfigRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SetMissionControlConfigRequest config.
         * @member {routerrpc.MissionControlConfig.$Properties|null|undefined} config
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @instance
         */
        SetMissionControlConfigRequest.prototype.config = null;

        /**
         * Creates a new SetMissionControlConfigRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @static
         * @param {routerrpc.SetMissionControlConfigRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.SetMissionControlConfigRequest} SetMissionControlConfigRequest instance
         * @type {{
         *   (properties: routerrpc.SetMissionControlConfigRequest.$Shape): routerrpc.SetMissionControlConfigRequest & routerrpc.SetMissionControlConfigRequest.$Shape;
         *   (properties?: routerrpc.SetMissionControlConfigRequest.$Properties): routerrpc.SetMissionControlConfigRequest;
         * }}
         */
        SetMissionControlConfigRequest.create = function(properties) {
            return new SetMissionControlConfigRequest(properties);
        };

        /**
         * Encodes the specified SetMissionControlConfigRequest message. Does not implicitly {@link routerrpc.SetMissionControlConfigRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @static
         * @param {routerrpc.SetMissionControlConfigRequest.$Properties} message SetMissionControlConfigRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetMissionControlConfigRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                $root.routerrpc.MissionControlConfig.encode(message.config, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SetMissionControlConfigRequest message, length delimited. Does not implicitly {@link routerrpc.SetMissionControlConfigRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @static
         * @param {routerrpc.SetMissionControlConfigRequest.$Properties} message SetMissionControlConfigRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetMissionControlConfigRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SetMissionControlConfigRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.SetMissionControlConfigRequest & routerrpc.SetMissionControlConfigRequest.$Shape} SetMissionControlConfigRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetMissionControlConfigRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.SetMissionControlConfigRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.config = $root.routerrpc.MissionControlConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SetMissionControlConfigRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.SetMissionControlConfigRequest & routerrpc.SetMissionControlConfigRequest.$Shape} SetMissionControlConfigRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetMissionControlConfigRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SetMissionControlConfigRequest message.
         * @function verify
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SetMissionControlConfigRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                let error = $root.routerrpc.MissionControlConfig.verify(message.config, _depth + 1);
                if (error)
                    return "config." + error;
            }
            return null;
        };

        /**
         * Creates a SetMissionControlConfigRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.SetMissionControlConfigRequest} SetMissionControlConfigRequest
         */
        SetMissionControlConfigRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.SetMissionControlConfigRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.SetMissionControlConfigRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.SetMissionControlConfigRequest();
            if (object.config != null) {
                if (!$util.isObject(object.config))
                    throw $TypeError(".routerrpc.SetMissionControlConfigRequest.config: object expected");
                message.config = $root.routerrpc.MissionControlConfig.fromObject(object.config, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SetMissionControlConfigRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @static
         * @param {routerrpc.SetMissionControlConfigRequest} message SetMissionControlConfigRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetMissionControlConfigRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.config = null;
            if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                object.config = $root.routerrpc.MissionControlConfig.toObject(message.config, options, _depth + 1);
            return object;
        };

        /**
         * Converts this SetMissionControlConfigRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetMissionControlConfigRequest.prototype.toJSON = function() {
            return SetMissionControlConfigRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SetMissionControlConfigRequest
         * @function getTypeUrl
         * @memberof routerrpc.SetMissionControlConfigRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SetMissionControlConfigRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.SetMissionControlConfigRequest";
        };

        return SetMissionControlConfigRequest;
    })();

    routerrpc.SetMissionControlConfigResponse = (function() {

        /**
         * Properties of a SetMissionControlConfigResponse.
         * @typedef {Object} routerrpc.SetMissionControlConfigResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SetMissionControlConfigResponse.
         * @memberof routerrpc
         * @interface ISetMissionControlConfigResponse
         * @augments routerrpc.SetMissionControlConfigResponse.$Properties
         * @deprecated Use routerrpc.SetMissionControlConfigResponse.$Properties instead.
         */

        /**
         * Shape of a SetMissionControlConfigResponse.
         * @typedef {routerrpc.SetMissionControlConfigResponse.$Properties} routerrpc.SetMissionControlConfigResponse.$Shape
         */

        /**
         * Constructs a new SetMissionControlConfigResponse.
         * @memberof routerrpc
         * @classdesc Represents a SetMissionControlConfigResponse.
         * @constructor
         * @param {routerrpc.SetMissionControlConfigResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SetMissionControlConfigResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new SetMissionControlConfigResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @static
         * @param {routerrpc.SetMissionControlConfigResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.SetMissionControlConfigResponse} SetMissionControlConfigResponse instance
         * @type {{
         *   (properties: routerrpc.SetMissionControlConfigResponse.$Shape): routerrpc.SetMissionControlConfigResponse & routerrpc.SetMissionControlConfigResponse.$Shape;
         *   (properties?: routerrpc.SetMissionControlConfigResponse.$Properties): routerrpc.SetMissionControlConfigResponse;
         * }}
         */
        SetMissionControlConfigResponse.create = function(properties) {
            return new SetMissionControlConfigResponse(properties);
        };

        /**
         * Encodes the specified SetMissionControlConfigResponse message. Does not implicitly {@link routerrpc.SetMissionControlConfigResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @static
         * @param {routerrpc.SetMissionControlConfigResponse.$Properties} message SetMissionControlConfigResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetMissionControlConfigResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SetMissionControlConfigResponse message, length delimited. Does not implicitly {@link routerrpc.SetMissionControlConfigResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @static
         * @param {routerrpc.SetMissionControlConfigResponse.$Properties} message SetMissionControlConfigResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetMissionControlConfigResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SetMissionControlConfigResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.SetMissionControlConfigResponse & routerrpc.SetMissionControlConfigResponse.$Shape} SetMissionControlConfigResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetMissionControlConfigResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.SetMissionControlConfigResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SetMissionControlConfigResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.SetMissionControlConfigResponse & routerrpc.SetMissionControlConfigResponse.$Shape} SetMissionControlConfigResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetMissionControlConfigResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SetMissionControlConfigResponse message.
         * @function verify
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SetMissionControlConfigResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a SetMissionControlConfigResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.SetMissionControlConfigResponse} SetMissionControlConfigResponse
         */
        SetMissionControlConfigResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.SetMissionControlConfigResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.SetMissionControlConfigResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.SetMissionControlConfigResponse();
        };

        /**
         * Creates a plain object from a SetMissionControlConfigResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @static
         * @param {routerrpc.SetMissionControlConfigResponse} message SetMissionControlConfigResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetMissionControlConfigResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this SetMissionControlConfigResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetMissionControlConfigResponse.prototype.toJSON = function() {
            return SetMissionControlConfigResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SetMissionControlConfigResponse
         * @function getTypeUrl
         * @memberof routerrpc.SetMissionControlConfigResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SetMissionControlConfigResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.SetMissionControlConfigResponse";
        };

        return SetMissionControlConfigResponse;
    })();

    routerrpc.MissionControlConfig = (function() {

        /**
         * Properties of a MissionControlConfig.
         * @typedef {Object} routerrpc.MissionControlConfig.$Properties
         * @property {Long|null} [half_life_seconds] MissionControlConfig half_life_seconds
         * @property {number|null} [hop_probability] MissionControlConfig hop_probability
         * @property {number|null} [weight] MissionControlConfig weight
         * @property {number|null} [maximum_payment_results] MissionControlConfig maximum_payment_results
         * @property {Long|null} [minimum_failure_relax_interval] MissionControlConfig minimum_failure_relax_interval
         * @property {routerrpc.MissionControlConfig.ProbabilityModel|null} [model] MissionControlConfig model
         * @property {routerrpc.AprioriParameters.$Properties|null} [apriori] MissionControlConfig apriori
         * @property {routerrpc.BimodalParameters.$Properties|null} [bimodal] MissionControlConfig bimodal
         * @property {"apriori"|"bimodal"} [EstimatorConfig] MissionControlConfig EstimatorConfig
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MissionControlConfig.
         * @memberof routerrpc
         * @interface IMissionControlConfig
         * @augments routerrpc.MissionControlConfig.$Properties
         * @deprecated Use routerrpc.MissionControlConfig.$Properties instead.
         */

        /**
         * Narrowed shape of a MissionControlConfig.
         * @typedef {{
         *   half_life_seconds?: Long|null;
         *   hop_probability?: number|null;
         *   weight?: number|null;
         *   maximum_payment_results?: number|null;
         *   minimum_failure_relax_interval?: Long|null;
         *   model?: routerrpc.MissionControlConfig.ProbabilityModel|null;
         *   apriori?: routerrpc.AprioriParameters.$Shape|null;
         *   bimodal?: routerrpc.BimodalParameters.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ EstimatorConfig?: undefined; apriori?: null; bimodal?: null }|{ EstimatorConfig?: "apriori"; apriori: routerrpc.AprioriParameters.$Shape; bimodal?: null }|{ EstimatorConfig?: "bimodal"; apriori?: null; bimodal: routerrpc.BimodalParameters.$Shape })
         * )} routerrpc.MissionControlConfig.$Shape
         */

        /**
         * Constructs a new MissionControlConfig.
         * @memberof routerrpc
         * @classdesc Represents a MissionControlConfig.
         * @constructor
         * @param {routerrpc.MissionControlConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MissionControlConfig = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MissionControlConfig half_life_seconds.
         * @member {Long} half_life_seconds
         * @memberof routerrpc.MissionControlConfig
         * @instance
         */
        MissionControlConfig.prototype.half_life_seconds = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * MissionControlConfig hop_probability.
         * @member {number} hop_probability
         * @memberof routerrpc.MissionControlConfig
         * @instance
         */
        MissionControlConfig.prototype.hop_probability = 0;

        /**
         * MissionControlConfig weight.
         * @member {number} weight
         * @memberof routerrpc.MissionControlConfig
         * @instance
         */
        MissionControlConfig.prototype.weight = 0;

        /**
         * MissionControlConfig maximum_payment_results.
         * @member {number} maximum_payment_results
         * @memberof routerrpc.MissionControlConfig
         * @instance
         */
        MissionControlConfig.prototype.maximum_payment_results = 0;

        /**
         * MissionControlConfig minimum_failure_relax_interval.
         * @member {Long} minimum_failure_relax_interval
         * @memberof routerrpc.MissionControlConfig
         * @instance
         */
        MissionControlConfig.prototype.minimum_failure_relax_interval = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * MissionControlConfig model.
         * @member {routerrpc.MissionControlConfig.ProbabilityModel} model
         * @memberof routerrpc.MissionControlConfig
         * @instance
         */
        MissionControlConfig.prototype.model = 0;

        /**
         * MissionControlConfig apriori.
         * @member {routerrpc.AprioriParameters.$Properties|null|undefined} apriori
         * @memberof routerrpc.MissionControlConfig
         * @instance
         */
        MissionControlConfig.prototype.apriori = null;

        /**
         * MissionControlConfig bimodal.
         * @member {routerrpc.BimodalParameters.$Properties|null|undefined} bimodal
         * @memberof routerrpc.MissionControlConfig
         * @instance
         */
        MissionControlConfig.prototype.bimodal = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * MissionControlConfig EstimatorConfig.
         * @member {"apriori"|"bimodal"|undefined} EstimatorConfig
         * @memberof routerrpc.MissionControlConfig
         * @instance
         */
        $Object.defineProperty(MissionControlConfig.prototype, "EstimatorConfig", {
            get: $util.oneOfGetter($oneOfFields = ["apriori", "bimodal"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MissionControlConfig instance using the specified properties.
         * @function create
         * @memberof routerrpc.MissionControlConfig
         * @static
         * @param {routerrpc.MissionControlConfig.$Properties=} [properties] Properties to set
         * @returns {routerrpc.MissionControlConfig} MissionControlConfig instance
         * @type {{
         *   (properties: routerrpc.MissionControlConfig.$Shape): routerrpc.MissionControlConfig & routerrpc.MissionControlConfig.$Shape;
         *   (properties?: routerrpc.MissionControlConfig.$Properties): routerrpc.MissionControlConfig;
         * }}
         */
        MissionControlConfig.create = function(properties) {
            return new MissionControlConfig(properties);
        };

        /**
         * Encodes the specified MissionControlConfig message. Does not implicitly {@link routerrpc.MissionControlConfig.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.MissionControlConfig
         * @static
         * @param {routerrpc.MissionControlConfig.$Properties} message MissionControlConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MissionControlConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.half_life_seconds != null && $Object.hasOwnProperty.call(message, "half_life_seconds") && (typeof message.half_life_seconds === "object" ? message.half_life_seconds.low || message.half_life_seconds.high : message.half_life_seconds !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.half_life_seconds);
            if (message.hop_probability != null && $Object.hasOwnProperty.call(message, "hop_probability") && !$Object.is(message.hop_probability, 0))
                writer.uint32(/* id 2, wireType 5 =*/21).float(message.hop_probability);
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && !$Object.is(message.weight, 0))
                writer.uint32(/* id 3, wireType 5 =*/29).float(message.weight);
            if (message.maximum_payment_results != null && $Object.hasOwnProperty.call(message, "maximum_payment_results") && message.maximum_payment_results !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.maximum_payment_results);
            if (message.minimum_failure_relax_interval != null && $Object.hasOwnProperty.call(message, "minimum_failure_relax_interval") && (typeof message.minimum_failure_relax_interval === "object" ? message.minimum_failure_relax_interval.low || message.minimum_failure_relax_interval.high : message.minimum_failure_relax_interval !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.minimum_failure_relax_interval);
            if (message.model != null && $Object.hasOwnProperty.call(message, "model") && message.model !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.model);
            if (message.apriori != null && $Object.hasOwnProperty.call(message, "apriori"))
                $root.routerrpc.AprioriParameters.encode(message.apriori, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.bimodal != null && $Object.hasOwnProperty.call(message, "bimodal"))
                $root.routerrpc.BimodalParameters.encode(message.bimodal, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MissionControlConfig message, length delimited. Does not implicitly {@link routerrpc.MissionControlConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.MissionControlConfig
         * @static
         * @param {routerrpc.MissionControlConfig.$Properties} message MissionControlConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MissionControlConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MissionControlConfig message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.MissionControlConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.MissionControlConfig & routerrpc.MissionControlConfig.$Shape} MissionControlConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MissionControlConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.MissionControlConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.half_life_seconds = value;
                        else
                            delete message.half_life_seconds;
                        continue;
                    }
                case 2: {
                        if (wireType !== 5)
                            break;
                        if (!$Object.is(value = reader.float(), 0))
                            message.hop_probability = value;
                        else
                            delete message.hop_probability;
                        continue;
                    }
                case 3: {
                        if (wireType !== 5)
                            break;
                        if (!$Object.is(value = reader.float(), 0))
                            message.weight = value;
                        else
                            delete message.weight;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.maximum_payment_results = value;
                        else
                            delete message.maximum_payment_results;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.minimum_failure_relax_interval = value;
                        else
                            delete message.minimum_failure_relax_interval;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.model = value;
                        else
                            delete message.model;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.apriori = $root.routerrpc.AprioriParameters.decode(reader, reader.uint32(), $undefined, _depth + 1, message.apriori);
                        message.EstimatorConfig = "apriori";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.bimodal = $root.routerrpc.BimodalParameters.decode(reader, reader.uint32(), $undefined, _depth + 1, message.bimodal);
                        message.EstimatorConfig = "bimodal";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MissionControlConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.MissionControlConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.MissionControlConfig & routerrpc.MissionControlConfig.$Shape} MissionControlConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MissionControlConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MissionControlConfig message.
         * @function verify
         * @memberof routerrpc.MissionControlConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MissionControlConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.half_life_seconds != null && $Object.hasOwnProperty.call(message, "half_life_seconds"))
                if (!$util.isInteger(message.half_life_seconds) && !(message.half_life_seconds && $util.isInteger(message.half_life_seconds.low) && $util.isInteger(message.half_life_seconds.high)))
                    return "half_life_seconds: integer|Long expected";
            if (message.hop_probability != null && $Object.hasOwnProperty.call(message, "hop_probability"))
                if (typeof message.hop_probability !== "number")
                    return "hop_probability: number expected";
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                if (typeof message.weight !== "number")
                    return "weight: number expected";
            if (message.maximum_payment_results != null && $Object.hasOwnProperty.call(message, "maximum_payment_results"))
                if (!$util.isInteger(message.maximum_payment_results))
                    return "maximum_payment_results: integer expected";
            if (message.minimum_failure_relax_interval != null && $Object.hasOwnProperty.call(message, "minimum_failure_relax_interval"))
                if (!$util.isInteger(message.minimum_failure_relax_interval) && !(message.minimum_failure_relax_interval && $util.isInteger(message.minimum_failure_relax_interval.low) && $util.isInteger(message.minimum_failure_relax_interval.high)))
                    return "minimum_failure_relax_interval: integer|Long expected";
            if (message.model != null && $Object.hasOwnProperty.call(message, "model"))
                if (typeof message.model !== "number" || (message.model | 0) !== message.model)
                    return "model: enum value expected";
            if (message.apriori != null && $Object.hasOwnProperty.call(message, "apriori")) {
                properties.EstimatorConfig = 1;
                {
                    let error = $root.routerrpc.AprioriParameters.verify(message.apriori, _depth + 1);
                    if (error)
                        return "apriori." + error;
                }
            }
            if (message.bimodal != null && $Object.hasOwnProperty.call(message, "bimodal")) {
                if (properties.EstimatorConfig === 1)
                    return "EstimatorConfig: multiple values";
                properties.EstimatorConfig = 1;
                {
                    let error = $root.routerrpc.BimodalParameters.verify(message.bimodal, _depth + 1);
                    if (error)
                        return "bimodal." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MissionControlConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.MissionControlConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.MissionControlConfig} MissionControlConfig
         */
        MissionControlConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.MissionControlConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.MissionControlConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.MissionControlConfig();
            if (object.half_life_seconds != null)
                if (typeof object.half_life_seconds === "object" ? object.half_life_seconds.low || object.half_life_seconds.high : $Number(object.half_life_seconds) !== 0)
                    if ($util.Long)
                        message.half_life_seconds = $util.Long.fromValue(object.half_life_seconds, true);
                    else if (typeof object.half_life_seconds === "string")
                        message.half_life_seconds = $parseInt(object.half_life_seconds, 10);
                    else if (typeof object.half_life_seconds === "number")
                        message.half_life_seconds = object.half_life_seconds;
                    else if (typeof object.half_life_seconds === "object")
                        message.half_life_seconds = new $util.LongBits(object.half_life_seconds.low >>> 0, object.half_life_seconds.high >>> 0).toNumber(true);
            if (object.hop_probability != null)
                if (!$Object.is($Number(object.hop_probability), 0))
                    message.hop_probability = $Number(object.hop_probability);
            if (object.weight != null)
                if (!$Object.is($Number(object.weight), 0))
                    message.weight = $Number(object.weight);
            if (object.maximum_payment_results != null)
                if ($Number(object.maximum_payment_results) !== 0)
                    message.maximum_payment_results = object.maximum_payment_results >>> 0;
            if (object.minimum_failure_relax_interval != null)
                if (typeof object.minimum_failure_relax_interval === "object" ? object.minimum_failure_relax_interval.low || object.minimum_failure_relax_interval.high : $Number(object.minimum_failure_relax_interval) !== 0)
                    if ($util.Long)
                        message.minimum_failure_relax_interval = $util.Long.fromValue(object.minimum_failure_relax_interval, true);
                    else if (typeof object.minimum_failure_relax_interval === "string")
                        message.minimum_failure_relax_interval = $parseInt(object.minimum_failure_relax_interval, 10);
                    else if (typeof object.minimum_failure_relax_interval === "number")
                        message.minimum_failure_relax_interval = object.minimum_failure_relax_interval;
                    else if (typeof object.minimum_failure_relax_interval === "object")
                        message.minimum_failure_relax_interval = new $util.LongBits(object.minimum_failure_relax_interval.low >>> 0, object.minimum_failure_relax_interval.high >>> 0).toNumber(true);
            if (object.model !== 0 && (typeof object.model !== "string" || $root.routerrpc.MissionControlConfig.ProbabilityModel[object.model] !== 0))
                switch (object.model) {
                case "APRIORI":
                case 0:
                    message.model = 0;
                    break;
                case "BIMODAL":
                case 1:
                    message.model = 1;
                    break;
                default:
                    if (typeof object.model === "number" && (object.model | 0) === object.model)
                        message.model = object.model;
                }
            if (object.apriori != null) {
                if (!$util.isObject(object.apriori))
                    throw $TypeError(".routerrpc.MissionControlConfig.apriori: object expected");
                message.apriori = $root.routerrpc.AprioriParameters.fromObject(object.apriori, _depth + 1);
            }
            if (object.bimodal != null) {
                if (!$util.isObject(object.bimodal))
                    throw $TypeError(".routerrpc.MissionControlConfig.bimodal: object expected");
                message.bimodal = $root.routerrpc.BimodalParameters.fromObject(object.bimodal, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a MissionControlConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.MissionControlConfig
         * @static
         * @param {routerrpc.MissionControlConfig} message MissionControlConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MissionControlConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.half_life_seconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.half_life_seconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.hop_probability = 0;
                object.weight = 0;
                object.maximum_payment_results = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.minimum_failure_relax_interval = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.minimum_failure_relax_interval = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.model = options.enums === $String ? "APRIORI" : 0;
            }
            if (message.half_life_seconds != null && $Object.hasOwnProperty.call(message, "half_life_seconds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.half_life_seconds = typeof message.half_life_seconds === "number" ? $BigInt(message.half_life_seconds) : $util.Long.fromBits(message.half_life_seconds.low >>> 0, message.half_life_seconds.high >>> 0, true).toBigInt();
                else if (typeof message.half_life_seconds === "number")
                    object.half_life_seconds = options.longs === $String ? $String(message.half_life_seconds) : message.half_life_seconds;
                else
                    object.half_life_seconds = options.longs === $String ? $util.Long.prototype.toString.call(message.half_life_seconds) : options.longs === $Number ? new $util.LongBits(message.half_life_seconds.low >>> 0, message.half_life_seconds.high >>> 0).toNumber(true) : message.half_life_seconds;
            if (message.hop_probability != null && $Object.hasOwnProperty.call(message, "hop_probability"))
                object.hop_probability = options.json && !$isFinite(message.hop_probability) ? $String(message.hop_probability) : message.hop_probability;
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                object.weight = options.json && !$isFinite(message.weight) ? $String(message.weight) : message.weight;
            if (message.maximum_payment_results != null && $Object.hasOwnProperty.call(message, "maximum_payment_results"))
                object.maximum_payment_results = message.maximum_payment_results;
            if (message.minimum_failure_relax_interval != null && $Object.hasOwnProperty.call(message, "minimum_failure_relax_interval"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.minimum_failure_relax_interval = typeof message.minimum_failure_relax_interval === "number" ? $BigInt(message.minimum_failure_relax_interval) : $util.Long.fromBits(message.minimum_failure_relax_interval.low >>> 0, message.minimum_failure_relax_interval.high >>> 0, true).toBigInt();
                else if (typeof message.minimum_failure_relax_interval === "number")
                    object.minimum_failure_relax_interval = options.longs === $String ? $String(message.minimum_failure_relax_interval) : message.minimum_failure_relax_interval;
                else
                    object.minimum_failure_relax_interval = options.longs === $String ? $util.Long.prototype.toString.call(message.minimum_failure_relax_interval) : options.longs === $Number ? new $util.LongBits(message.minimum_failure_relax_interval.low >>> 0, message.minimum_failure_relax_interval.high >>> 0).toNumber(true) : message.minimum_failure_relax_interval;
            if (message.model != null && $Object.hasOwnProperty.call(message, "model"))
                object.model = options.enums === $String ? $root.routerrpc.MissionControlConfig.ProbabilityModel[message.model] === $undefined ? message.model : $root.routerrpc.MissionControlConfig.ProbabilityModel[message.model] : message.model;
            if (message.apriori != null && $Object.hasOwnProperty.call(message, "apriori")) {
                object.apriori = $root.routerrpc.AprioriParameters.toObject(message.apriori, options, _depth + 1);
                if (options.oneofs)
                    object.EstimatorConfig = "apriori";
            }
            if (message.bimodal != null && $Object.hasOwnProperty.call(message, "bimodal")) {
                object.bimodal = $root.routerrpc.BimodalParameters.toObject(message.bimodal, options, _depth + 1);
                if (options.oneofs)
                    object.EstimatorConfig = "bimodal";
            }
            return object;
        };

        /**
         * Converts this MissionControlConfig to JSON.
         * @function toJSON
         * @memberof routerrpc.MissionControlConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MissionControlConfig.prototype.toJSON = function() {
            return MissionControlConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MissionControlConfig
         * @function getTypeUrl
         * @memberof routerrpc.MissionControlConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MissionControlConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.MissionControlConfig";
        };

        /**
         * ProbabilityModel enum.
         * @name routerrpc.MissionControlConfig.ProbabilityModel
         * @enum {number}
         * @property {number} APRIORI=0 APRIORI value
         * @property {number} BIMODAL=1 BIMODAL value
         */
        MissionControlConfig.ProbabilityModel = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "APRIORI"] = 0;
            values[valuesById[1] = "BIMODAL"] = 1;
            return values;
        })();

        return MissionControlConfig;
    })();

    routerrpc.BimodalParameters = (function() {

        /**
         * Properties of a BimodalParameters.
         * @typedef {Object} routerrpc.BimodalParameters.$Properties
         * @property {number|null} [node_weight] BimodalParameters node_weight
         * @property {Long|null} [scale_msat] BimodalParameters scale_msat
         * @property {Long|null} [decay_time] BimodalParameters decay_time
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BimodalParameters.
         * @memberof routerrpc
         * @interface IBimodalParameters
         * @augments routerrpc.BimodalParameters.$Properties
         * @deprecated Use routerrpc.BimodalParameters.$Properties instead.
         */

        /**
         * Shape of a BimodalParameters.
         * @typedef {routerrpc.BimodalParameters.$Properties} routerrpc.BimodalParameters.$Shape
         */

        /**
         * Constructs a new BimodalParameters.
         * @memberof routerrpc
         * @classdesc Represents a BimodalParameters.
         * @constructor
         * @param {routerrpc.BimodalParameters.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BimodalParameters = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BimodalParameters node_weight.
         * @member {number} node_weight
         * @memberof routerrpc.BimodalParameters
         * @instance
         */
        BimodalParameters.prototype.node_weight = 0;

        /**
         * BimodalParameters scale_msat.
         * @member {Long} scale_msat
         * @memberof routerrpc.BimodalParameters
         * @instance
         */
        BimodalParameters.prototype.scale_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BimodalParameters decay_time.
         * @member {Long} decay_time
         * @memberof routerrpc.BimodalParameters
         * @instance
         */
        BimodalParameters.prototype.decay_time = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new BimodalParameters instance using the specified properties.
         * @function create
         * @memberof routerrpc.BimodalParameters
         * @static
         * @param {routerrpc.BimodalParameters.$Properties=} [properties] Properties to set
         * @returns {routerrpc.BimodalParameters} BimodalParameters instance
         * @type {{
         *   (properties: routerrpc.BimodalParameters.$Shape): routerrpc.BimodalParameters & routerrpc.BimodalParameters.$Shape;
         *   (properties?: routerrpc.BimodalParameters.$Properties): routerrpc.BimodalParameters;
         * }}
         */
        BimodalParameters.create = function(properties) {
            return new BimodalParameters(properties);
        };

        /**
         * Encodes the specified BimodalParameters message. Does not implicitly {@link routerrpc.BimodalParameters.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.BimodalParameters
         * @static
         * @param {routerrpc.BimodalParameters.$Properties} message BimodalParameters message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BimodalParameters.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.node_weight != null && $Object.hasOwnProperty.call(message, "node_weight") && !$Object.is(message.node_weight, 0))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.node_weight);
            if (message.scale_msat != null && $Object.hasOwnProperty.call(message, "scale_msat") && (typeof message.scale_msat === "object" ? message.scale_msat.low || message.scale_msat.high : message.scale_msat !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.scale_msat);
            if (message.decay_time != null && $Object.hasOwnProperty.call(message, "decay_time") && (typeof message.decay_time === "object" ? message.decay_time.low || message.decay_time.high : message.decay_time !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.decay_time);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BimodalParameters message, length delimited. Does not implicitly {@link routerrpc.BimodalParameters.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.BimodalParameters
         * @static
         * @param {routerrpc.BimodalParameters.$Properties} message BimodalParameters message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BimodalParameters.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BimodalParameters message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.BimodalParameters
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.BimodalParameters & routerrpc.BimodalParameters.$Shape} BimodalParameters
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BimodalParameters.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.BimodalParameters(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.node_weight = value;
                        else
                            delete message.node_weight;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.scale_msat = value;
                        else
                            delete message.scale_msat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.decay_time = value;
                        else
                            delete message.decay_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BimodalParameters message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.BimodalParameters
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.BimodalParameters & routerrpc.BimodalParameters.$Shape} BimodalParameters
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BimodalParameters.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BimodalParameters message.
         * @function verify
         * @memberof routerrpc.BimodalParameters
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BimodalParameters.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.node_weight != null && $Object.hasOwnProperty.call(message, "node_weight"))
                if (typeof message.node_weight !== "number")
                    return "node_weight: number expected";
            if (message.scale_msat != null && $Object.hasOwnProperty.call(message, "scale_msat"))
                if (!$util.isInteger(message.scale_msat) && !(message.scale_msat && $util.isInteger(message.scale_msat.low) && $util.isInteger(message.scale_msat.high)))
                    return "scale_msat: integer|Long expected";
            if (message.decay_time != null && $Object.hasOwnProperty.call(message, "decay_time"))
                if (!$util.isInteger(message.decay_time) && !(message.decay_time && $util.isInteger(message.decay_time.low) && $util.isInteger(message.decay_time.high)))
                    return "decay_time: integer|Long expected";
            return null;
        };

        /**
         * Creates a BimodalParameters message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.BimodalParameters
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.BimodalParameters} BimodalParameters
         */
        BimodalParameters.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.BimodalParameters)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.BimodalParameters: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.BimodalParameters();
            if (object.node_weight != null)
                if (!$Object.is($Number(object.node_weight), 0))
                    message.node_weight = $Number(object.node_weight);
            if (object.scale_msat != null)
                if (typeof object.scale_msat === "object" ? object.scale_msat.low || object.scale_msat.high : $Number(object.scale_msat) !== 0)
                    if ($util.Long)
                        message.scale_msat = $util.Long.fromValue(object.scale_msat, true);
                    else if (typeof object.scale_msat === "string")
                        message.scale_msat = $parseInt(object.scale_msat, 10);
                    else if (typeof object.scale_msat === "number")
                        message.scale_msat = object.scale_msat;
                    else if (typeof object.scale_msat === "object")
                        message.scale_msat = new $util.LongBits(object.scale_msat.low >>> 0, object.scale_msat.high >>> 0).toNumber(true);
            if (object.decay_time != null)
                if (typeof object.decay_time === "object" ? object.decay_time.low || object.decay_time.high : $Number(object.decay_time) !== 0)
                    if ($util.Long)
                        message.decay_time = $util.Long.fromValue(object.decay_time, true);
                    else if (typeof object.decay_time === "string")
                        message.decay_time = $parseInt(object.decay_time, 10);
                    else if (typeof object.decay_time === "number")
                        message.decay_time = object.decay_time;
                    else if (typeof object.decay_time === "object")
                        message.decay_time = new $util.LongBits(object.decay_time.low >>> 0, object.decay_time.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a BimodalParameters message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.BimodalParameters
         * @static
         * @param {routerrpc.BimodalParameters} message BimodalParameters
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BimodalParameters.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.node_weight = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.scale_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.scale_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.decay_time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.decay_time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.node_weight != null && $Object.hasOwnProperty.call(message, "node_weight"))
                object.node_weight = options.json && !$isFinite(message.node_weight) ? $String(message.node_weight) : message.node_weight;
            if (message.scale_msat != null && $Object.hasOwnProperty.call(message, "scale_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.scale_msat = typeof message.scale_msat === "number" ? $BigInt(message.scale_msat) : $util.Long.fromBits(message.scale_msat.low >>> 0, message.scale_msat.high >>> 0, true).toBigInt();
                else if (typeof message.scale_msat === "number")
                    object.scale_msat = options.longs === $String ? $String(message.scale_msat) : message.scale_msat;
                else
                    object.scale_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.scale_msat) : options.longs === $Number ? new $util.LongBits(message.scale_msat.low >>> 0, message.scale_msat.high >>> 0).toNumber(true) : message.scale_msat;
            if (message.decay_time != null && $Object.hasOwnProperty.call(message, "decay_time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.decay_time = typeof message.decay_time === "number" ? $BigInt(message.decay_time) : $util.Long.fromBits(message.decay_time.low >>> 0, message.decay_time.high >>> 0, true).toBigInt();
                else if (typeof message.decay_time === "number")
                    object.decay_time = options.longs === $String ? $String(message.decay_time) : message.decay_time;
                else
                    object.decay_time = options.longs === $String ? $util.Long.prototype.toString.call(message.decay_time) : options.longs === $Number ? new $util.LongBits(message.decay_time.low >>> 0, message.decay_time.high >>> 0).toNumber(true) : message.decay_time;
            return object;
        };

        /**
         * Converts this BimodalParameters to JSON.
         * @function toJSON
         * @memberof routerrpc.BimodalParameters
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BimodalParameters.prototype.toJSON = function() {
            return BimodalParameters.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BimodalParameters
         * @function getTypeUrl
         * @memberof routerrpc.BimodalParameters
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BimodalParameters.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.BimodalParameters";
        };

        return BimodalParameters;
    })();

    routerrpc.AprioriParameters = (function() {

        /**
         * Properties of an AprioriParameters.
         * @typedef {Object} routerrpc.AprioriParameters.$Properties
         * @property {Long|null} [half_life_seconds] AprioriParameters half_life_seconds
         * @property {number|null} [hop_probability] AprioriParameters hop_probability
         * @property {number|null} [weight] AprioriParameters weight
         * @property {number|null} [capacity_fraction] AprioriParameters capacity_fraction
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AprioriParameters.
         * @memberof routerrpc
         * @interface IAprioriParameters
         * @augments routerrpc.AprioriParameters.$Properties
         * @deprecated Use routerrpc.AprioriParameters.$Properties instead.
         */

        /**
         * Shape of an AprioriParameters.
         * @typedef {routerrpc.AprioriParameters.$Properties} routerrpc.AprioriParameters.$Shape
         */

        /**
         * Constructs a new AprioriParameters.
         * @memberof routerrpc
         * @classdesc Represents an AprioriParameters.
         * @constructor
         * @param {routerrpc.AprioriParameters.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AprioriParameters = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AprioriParameters half_life_seconds.
         * @member {Long} half_life_seconds
         * @memberof routerrpc.AprioriParameters
         * @instance
         */
        AprioriParameters.prototype.half_life_seconds = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * AprioriParameters hop_probability.
         * @member {number} hop_probability
         * @memberof routerrpc.AprioriParameters
         * @instance
         */
        AprioriParameters.prototype.hop_probability = 0;

        /**
         * AprioriParameters weight.
         * @member {number} weight
         * @memberof routerrpc.AprioriParameters
         * @instance
         */
        AprioriParameters.prototype.weight = 0;

        /**
         * AprioriParameters capacity_fraction.
         * @member {number} capacity_fraction
         * @memberof routerrpc.AprioriParameters
         * @instance
         */
        AprioriParameters.prototype.capacity_fraction = 0;

        /**
         * Creates a new AprioriParameters instance using the specified properties.
         * @function create
         * @memberof routerrpc.AprioriParameters
         * @static
         * @param {routerrpc.AprioriParameters.$Properties=} [properties] Properties to set
         * @returns {routerrpc.AprioriParameters} AprioriParameters instance
         * @type {{
         *   (properties: routerrpc.AprioriParameters.$Shape): routerrpc.AprioriParameters & routerrpc.AprioriParameters.$Shape;
         *   (properties?: routerrpc.AprioriParameters.$Properties): routerrpc.AprioriParameters;
         * }}
         */
        AprioriParameters.create = function(properties) {
            return new AprioriParameters(properties);
        };

        /**
         * Encodes the specified AprioriParameters message. Does not implicitly {@link routerrpc.AprioriParameters.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.AprioriParameters
         * @static
         * @param {routerrpc.AprioriParameters.$Properties} message AprioriParameters message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AprioriParameters.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.half_life_seconds != null && $Object.hasOwnProperty.call(message, "half_life_seconds") && (typeof message.half_life_seconds === "object" ? message.half_life_seconds.low || message.half_life_seconds.high : message.half_life_seconds !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.half_life_seconds);
            if (message.hop_probability != null && $Object.hasOwnProperty.call(message, "hop_probability") && !$Object.is(message.hop_probability, 0))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.hop_probability);
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && !$Object.is(message.weight, 0))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.weight);
            if (message.capacity_fraction != null && $Object.hasOwnProperty.call(message, "capacity_fraction") && !$Object.is(message.capacity_fraction, 0))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.capacity_fraction);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AprioriParameters message, length delimited. Does not implicitly {@link routerrpc.AprioriParameters.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.AprioriParameters
         * @static
         * @param {routerrpc.AprioriParameters.$Properties} message AprioriParameters message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AprioriParameters.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AprioriParameters message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.AprioriParameters
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.AprioriParameters & routerrpc.AprioriParameters.$Shape} AprioriParameters
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AprioriParameters.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.AprioriParameters(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.half_life_seconds = value;
                        else
                            delete message.half_life_seconds;
                        continue;
                    }
                case 2: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.hop_probability = value;
                        else
                            delete message.hop_probability;
                        continue;
                    }
                case 3: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.weight = value;
                        else
                            delete message.weight;
                        continue;
                    }
                case 4: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.capacity_fraction = value;
                        else
                            delete message.capacity_fraction;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AprioriParameters message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.AprioriParameters
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.AprioriParameters & routerrpc.AprioriParameters.$Shape} AprioriParameters
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AprioriParameters.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AprioriParameters message.
         * @function verify
         * @memberof routerrpc.AprioriParameters
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AprioriParameters.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.half_life_seconds != null && $Object.hasOwnProperty.call(message, "half_life_seconds"))
                if (!$util.isInteger(message.half_life_seconds) && !(message.half_life_seconds && $util.isInteger(message.half_life_seconds.low) && $util.isInteger(message.half_life_seconds.high)))
                    return "half_life_seconds: integer|Long expected";
            if (message.hop_probability != null && $Object.hasOwnProperty.call(message, "hop_probability"))
                if (typeof message.hop_probability !== "number")
                    return "hop_probability: number expected";
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                if (typeof message.weight !== "number")
                    return "weight: number expected";
            if (message.capacity_fraction != null && $Object.hasOwnProperty.call(message, "capacity_fraction"))
                if (typeof message.capacity_fraction !== "number")
                    return "capacity_fraction: number expected";
            return null;
        };

        /**
         * Creates an AprioriParameters message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.AprioriParameters
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.AprioriParameters} AprioriParameters
         */
        AprioriParameters.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.AprioriParameters)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.AprioriParameters: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.AprioriParameters();
            if (object.half_life_seconds != null)
                if (typeof object.half_life_seconds === "object" ? object.half_life_seconds.low || object.half_life_seconds.high : $Number(object.half_life_seconds) !== 0)
                    if ($util.Long)
                        message.half_life_seconds = $util.Long.fromValue(object.half_life_seconds, true);
                    else if (typeof object.half_life_seconds === "string")
                        message.half_life_seconds = $parseInt(object.half_life_seconds, 10);
                    else if (typeof object.half_life_seconds === "number")
                        message.half_life_seconds = object.half_life_seconds;
                    else if (typeof object.half_life_seconds === "object")
                        message.half_life_seconds = new $util.LongBits(object.half_life_seconds.low >>> 0, object.half_life_seconds.high >>> 0).toNumber(true);
            if (object.hop_probability != null)
                if (!$Object.is($Number(object.hop_probability), 0))
                    message.hop_probability = $Number(object.hop_probability);
            if (object.weight != null)
                if (!$Object.is($Number(object.weight), 0))
                    message.weight = $Number(object.weight);
            if (object.capacity_fraction != null)
                if (!$Object.is($Number(object.capacity_fraction), 0))
                    message.capacity_fraction = $Number(object.capacity_fraction);
            return message;
        };

        /**
         * Creates a plain object from an AprioriParameters message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.AprioriParameters
         * @static
         * @param {routerrpc.AprioriParameters} message AprioriParameters
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AprioriParameters.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.half_life_seconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.half_life_seconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.hop_probability = 0;
                object.weight = 0;
                object.capacity_fraction = 0;
            }
            if (message.half_life_seconds != null && $Object.hasOwnProperty.call(message, "half_life_seconds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.half_life_seconds = typeof message.half_life_seconds === "number" ? $BigInt(message.half_life_seconds) : $util.Long.fromBits(message.half_life_seconds.low >>> 0, message.half_life_seconds.high >>> 0, true).toBigInt();
                else if (typeof message.half_life_seconds === "number")
                    object.half_life_seconds = options.longs === $String ? $String(message.half_life_seconds) : message.half_life_seconds;
                else
                    object.half_life_seconds = options.longs === $String ? $util.Long.prototype.toString.call(message.half_life_seconds) : options.longs === $Number ? new $util.LongBits(message.half_life_seconds.low >>> 0, message.half_life_seconds.high >>> 0).toNumber(true) : message.half_life_seconds;
            if (message.hop_probability != null && $Object.hasOwnProperty.call(message, "hop_probability"))
                object.hop_probability = options.json && !$isFinite(message.hop_probability) ? $String(message.hop_probability) : message.hop_probability;
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                object.weight = options.json && !$isFinite(message.weight) ? $String(message.weight) : message.weight;
            if (message.capacity_fraction != null && $Object.hasOwnProperty.call(message, "capacity_fraction"))
                object.capacity_fraction = options.json && !$isFinite(message.capacity_fraction) ? $String(message.capacity_fraction) : message.capacity_fraction;
            return object;
        };

        /**
         * Converts this AprioriParameters to JSON.
         * @function toJSON
         * @memberof routerrpc.AprioriParameters
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AprioriParameters.prototype.toJSON = function() {
            return AprioriParameters.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AprioriParameters
         * @function getTypeUrl
         * @memberof routerrpc.AprioriParameters
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AprioriParameters.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.AprioriParameters";
        };

        return AprioriParameters;
    })();

    routerrpc.QueryProbabilityRequest = (function() {

        /**
         * Properties of a QueryProbabilityRequest.
         * @typedef {Object} routerrpc.QueryProbabilityRequest.$Properties
         * @property {Uint8Array|null} [from_node] QueryProbabilityRequest from_node
         * @property {Uint8Array|null} [to_node] QueryProbabilityRequest to_node
         * @property {Long|null} [amt_msat] QueryProbabilityRequest amt_msat
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a QueryProbabilityRequest.
         * @memberof routerrpc
         * @interface IQueryProbabilityRequest
         * @augments routerrpc.QueryProbabilityRequest.$Properties
         * @deprecated Use routerrpc.QueryProbabilityRequest.$Properties instead.
         */

        /**
         * Shape of a QueryProbabilityRequest.
         * @typedef {routerrpc.QueryProbabilityRequest.$Properties} routerrpc.QueryProbabilityRequest.$Shape
         */

        /**
         * Constructs a new QueryProbabilityRequest.
         * @memberof routerrpc
         * @classdesc Represents a QueryProbabilityRequest.
         * @constructor
         * @param {routerrpc.QueryProbabilityRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const QueryProbabilityRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * QueryProbabilityRequest from_node.
         * @member {Uint8Array} from_node
         * @memberof routerrpc.QueryProbabilityRequest
         * @instance
         */
        QueryProbabilityRequest.prototype.from_node = $util.newBuffer([]);

        /**
         * QueryProbabilityRequest to_node.
         * @member {Uint8Array} to_node
         * @memberof routerrpc.QueryProbabilityRequest
         * @instance
         */
        QueryProbabilityRequest.prototype.to_node = $util.newBuffer([]);

        /**
         * QueryProbabilityRequest amt_msat.
         * @member {Long} amt_msat
         * @memberof routerrpc.QueryProbabilityRequest
         * @instance
         */
        QueryProbabilityRequest.prototype.amt_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new QueryProbabilityRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.QueryProbabilityRequest
         * @static
         * @param {routerrpc.QueryProbabilityRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.QueryProbabilityRequest} QueryProbabilityRequest instance
         * @type {{
         *   (properties: routerrpc.QueryProbabilityRequest.$Shape): routerrpc.QueryProbabilityRequest & routerrpc.QueryProbabilityRequest.$Shape;
         *   (properties?: routerrpc.QueryProbabilityRequest.$Properties): routerrpc.QueryProbabilityRequest;
         * }}
         */
        QueryProbabilityRequest.create = function(properties) {
            return new QueryProbabilityRequest(properties);
        };

        /**
         * Encodes the specified QueryProbabilityRequest message. Does not implicitly {@link routerrpc.QueryProbabilityRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.QueryProbabilityRequest
         * @static
         * @param {routerrpc.QueryProbabilityRequest.$Properties} message QueryProbabilityRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryProbabilityRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.from_node != null && $Object.hasOwnProperty.call(message, "from_node") && message.from_node.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.from_node);
            if (message.to_node != null && $Object.hasOwnProperty.call(message, "to_node") && message.to_node.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.to_node);
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat") && (typeof message.amt_msat === "object" ? message.amt_msat.low || message.amt_msat.high : message.amt_msat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.amt_msat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QueryProbabilityRequest message, length delimited. Does not implicitly {@link routerrpc.QueryProbabilityRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.QueryProbabilityRequest
         * @static
         * @param {routerrpc.QueryProbabilityRequest.$Properties} message QueryProbabilityRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryProbabilityRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryProbabilityRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.QueryProbabilityRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.QueryProbabilityRequest & routerrpc.QueryProbabilityRequest.$Shape} QueryProbabilityRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryProbabilityRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.QueryProbabilityRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.from_node = value;
                        else
                            delete message.from_node;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.to_node = value;
                        else
                            delete message.to_node;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_msat = value;
                        else
                            delete message.amt_msat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QueryProbabilityRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.QueryProbabilityRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.QueryProbabilityRequest & routerrpc.QueryProbabilityRequest.$Shape} QueryProbabilityRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryProbabilityRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryProbabilityRequest message.
         * @function verify
         * @memberof routerrpc.QueryProbabilityRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryProbabilityRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.from_node != null && $Object.hasOwnProperty.call(message, "from_node"))
                if (!(message.from_node && typeof message.from_node.length === "number" || $util.isString(message.from_node)))
                    return "from_node: buffer expected";
            if (message.to_node != null && $Object.hasOwnProperty.call(message, "to_node"))
                if (!(message.to_node && typeof message.to_node.length === "number" || $util.isString(message.to_node)))
                    return "to_node: buffer expected";
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (!$util.isInteger(message.amt_msat) && !(message.amt_msat && $util.isInteger(message.amt_msat.low) && $util.isInteger(message.amt_msat.high)))
                    return "amt_msat: integer|Long expected";
            return null;
        };

        /**
         * Creates a QueryProbabilityRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.QueryProbabilityRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.QueryProbabilityRequest} QueryProbabilityRequest
         */
        QueryProbabilityRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.QueryProbabilityRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.QueryProbabilityRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.QueryProbabilityRequest();
            if (object.from_node != null)
                if (object.from_node.length)
                    if (typeof object.from_node === "string")
                        $util.base64.decode(object.from_node, message.from_node = $util.newBuffer($util.base64.length(object.from_node)), 0);
                    else if (object.from_node.length >= 0)
                        message.from_node = object.from_node;
            if (object.to_node != null)
                if (object.to_node.length)
                    if (typeof object.to_node === "string")
                        $util.base64.decode(object.to_node, message.to_node = $util.newBuffer($util.base64.length(object.to_node)), 0);
                    else if (object.to_node.length >= 0)
                        message.to_node = object.to_node;
            if (object.amt_msat != null)
                if (typeof object.amt_msat === "object" ? object.amt_msat.low || object.amt_msat.high : $Number(object.amt_msat) !== 0)
                    if ($util.Long)
                        message.amt_msat = $util.Long.fromValue(object.amt_msat, false);
                    else if (typeof object.amt_msat === "string")
                        message.amt_msat = $parseInt(object.amt_msat, 10);
                    else if (typeof object.amt_msat === "number")
                        message.amt_msat = object.amt_msat;
                    else if (typeof object.amt_msat === "object")
                        message.amt_msat = new $util.LongBits(object.amt_msat.low >>> 0, object.amt_msat.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a QueryProbabilityRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.QueryProbabilityRequest
         * @static
         * @param {routerrpc.QueryProbabilityRequest} message QueryProbabilityRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryProbabilityRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.from_node = "";
                else {
                    object.from_node = [];
                    if (options.bytes !== $Array)
                        object.from_node = $util.newBuffer(object.from_node);
                }
                if (options.bytes === $String)
                    object.to_node = "";
                else {
                    object.to_node = [];
                    if (options.bytes !== $Array)
                        object.to_node = $util.newBuffer(object.to_node);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.from_node != null && $Object.hasOwnProperty.call(message, "from_node"))
                object.from_node = options.bytes === $String ? $util.base64.encode(message.from_node, 0, message.from_node.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.from_node) : message.from_node;
            if (message.to_node != null && $Object.hasOwnProperty.call(message, "to_node"))
                object.to_node = options.bytes === $String ? $util.base64.encode(message.to_node, 0, message.to_node.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.to_node) : message.to_node;
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_msat = typeof message.amt_msat === "number" ? $BigInt(message.amt_msat) : $util.Long.fromBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_msat === "number")
                    object.amt_msat = options.longs === $String ? $String(message.amt_msat) : message.amt_msat;
                else
                    object.amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_msat) : options.longs === $Number ? new $util.LongBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0).toNumber() : message.amt_msat;
            return object;
        };

        /**
         * Converts this QueryProbabilityRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.QueryProbabilityRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryProbabilityRequest.prototype.toJSON = function() {
            return QueryProbabilityRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QueryProbabilityRequest
         * @function getTypeUrl
         * @memberof routerrpc.QueryProbabilityRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QueryProbabilityRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.QueryProbabilityRequest";
        };

        return QueryProbabilityRequest;
    })();

    routerrpc.QueryProbabilityResponse = (function() {

        /**
         * Properties of a QueryProbabilityResponse.
         * @typedef {Object} routerrpc.QueryProbabilityResponse.$Properties
         * @property {number|null} [probability] QueryProbabilityResponse probability
         * @property {routerrpc.PairData.$Properties|null} [history] QueryProbabilityResponse history
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a QueryProbabilityResponse.
         * @memberof routerrpc
         * @interface IQueryProbabilityResponse
         * @augments routerrpc.QueryProbabilityResponse.$Properties
         * @deprecated Use routerrpc.QueryProbabilityResponse.$Properties instead.
         */

        /**
         * Shape of a QueryProbabilityResponse.
         * @typedef {routerrpc.QueryProbabilityResponse.$Properties} routerrpc.QueryProbabilityResponse.$Shape
         */

        /**
         * Constructs a new QueryProbabilityResponse.
         * @memberof routerrpc
         * @classdesc Represents a QueryProbabilityResponse.
         * @constructor
         * @param {routerrpc.QueryProbabilityResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const QueryProbabilityResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * QueryProbabilityResponse probability.
         * @member {number} probability
         * @memberof routerrpc.QueryProbabilityResponse
         * @instance
         */
        QueryProbabilityResponse.prototype.probability = 0;

        /**
         * QueryProbabilityResponse history.
         * @member {routerrpc.PairData.$Properties|null|undefined} history
         * @memberof routerrpc.QueryProbabilityResponse
         * @instance
         */
        QueryProbabilityResponse.prototype.history = null;

        /**
         * Creates a new QueryProbabilityResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.QueryProbabilityResponse
         * @static
         * @param {routerrpc.QueryProbabilityResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.QueryProbabilityResponse} QueryProbabilityResponse instance
         * @type {{
         *   (properties: routerrpc.QueryProbabilityResponse.$Shape): routerrpc.QueryProbabilityResponse & routerrpc.QueryProbabilityResponse.$Shape;
         *   (properties?: routerrpc.QueryProbabilityResponse.$Properties): routerrpc.QueryProbabilityResponse;
         * }}
         */
        QueryProbabilityResponse.create = function(properties) {
            return new QueryProbabilityResponse(properties);
        };

        /**
         * Encodes the specified QueryProbabilityResponse message. Does not implicitly {@link routerrpc.QueryProbabilityResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.QueryProbabilityResponse
         * @static
         * @param {routerrpc.QueryProbabilityResponse.$Properties} message QueryProbabilityResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryProbabilityResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.probability != null && $Object.hasOwnProperty.call(message, "probability") && !$Object.is(message.probability, 0))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.probability);
            if (message.history != null && $Object.hasOwnProperty.call(message, "history"))
                $root.routerrpc.PairData.encode(message.history, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QueryProbabilityResponse message, length delimited. Does not implicitly {@link routerrpc.QueryProbabilityResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.QueryProbabilityResponse
         * @static
         * @param {routerrpc.QueryProbabilityResponse.$Properties} message QueryProbabilityResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryProbabilityResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryProbabilityResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.QueryProbabilityResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.QueryProbabilityResponse & routerrpc.QueryProbabilityResponse.$Shape} QueryProbabilityResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryProbabilityResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.QueryProbabilityResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.probability = value;
                        else
                            delete message.probability;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.history = $root.routerrpc.PairData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.history);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QueryProbabilityResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.QueryProbabilityResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.QueryProbabilityResponse & routerrpc.QueryProbabilityResponse.$Shape} QueryProbabilityResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryProbabilityResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryProbabilityResponse message.
         * @function verify
         * @memberof routerrpc.QueryProbabilityResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryProbabilityResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.probability != null && $Object.hasOwnProperty.call(message, "probability"))
                if (typeof message.probability !== "number")
                    return "probability: number expected";
            if (message.history != null && $Object.hasOwnProperty.call(message, "history")) {
                let error = $root.routerrpc.PairData.verify(message.history, _depth + 1);
                if (error)
                    return "history." + error;
            }
            return null;
        };

        /**
         * Creates a QueryProbabilityResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.QueryProbabilityResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.QueryProbabilityResponse} QueryProbabilityResponse
         */
        QueryProbabilityResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.QueryProbabilityResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.QueryProbabilityResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.QueryProbabilityResponse();
            if (object.probability != null)
                if (!$Object.is($Number(object.probability), 0))
                    message.probability = $Number(object.probability);
            if (object.history != null) {
                if (!$util.isObject(object.history))
                    throw $TypeError(".routerrpc.QueryProbabilityResponse.history: object expected");
                message.history = $root.routerrpc.PairData.fromObject(object.history, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a QueryProbabilityResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.QueryProbabilityResponse
         * @static
         * @param {routerrpc.QueryProbabilityResponse} message QueryProbabilityResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryProbabilityResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.probability = 0;
                object.history = null;
            }
            if (message.probability != null && $Object.hasOwnProperty.call(message, "probability"))
                object.probability = options.json && !$isFinite(message.probability) ? $String(message.probability) : message.probability;
            if (message.history != null && $Object.hasOwnProperty.call(message, "history"))
                object.history = $root.routerrpc.PairData.toObject(message.history, options, _depth + 1);
            return object;
        };

        /**
         * Converts this QueryProbabilityResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.QueryProbabilityResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryProbabilityResponse.prototype.toJSON = function() {
            return QueryProbabilityResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QueryProbabilityResponse
         * @function getTypeUrl
         * @memberof routerrpc.QueryProbabilityResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QueryProbabilityResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.QueryProbabilityResponse";
        };

        return QueryProbabilityResponse;
    })();

    routerrpc.BuildRouteRequest = (function() {

        /**
         * Properties of a BuildRouteRequest.
         * @typedef {Object} routerrpc.BuildRouteRequest.$Properties
         * @property {Long|null} [amt_msat] BuildRouteRequest amt_msat
         * @property {number|null} [final_cltv_delta] BuildRouteRequest final_cltv_delta
         * @property {Long|null} [outgoing_chan_id] BuildRouteRequest outgoing_chan_id
         * @property {Array.<Uint8Array>|null} [hop_pubkeys] BuildRouteRequest hop_pubkeys
         * @property {Uint8Array|null} [payment_addr] BuildRouteRequest payment_addr
         * @property {Object.<string,Uint8Array>|null} [first_hop_custom_records] BuildRouteRequest first_hop_custom_records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BuildRouteRequest.
         * @memberof routerrpc
         * @interface IBuildRouteRequest
         * @augments routerrpc.BuildRouteRequest.$Properties
         * @deprecated Use routerrpc.BuildRouteRequest.$Properties instead.
         */

        /**
         * Shape of a BuildRouteRequest.
         * @typedef {routerrpc.BuildRouteRequest.$Properties} routerrpc.BuildRouteRequest.$Shape
         */

        /**
         * Constructs a new BuildRouteRequest.
         * @memberof routerrpc
         * @classdesc Represents a BuildRouteRequest.
         * @constructor
         * @param {routerrpc.BuildRouteRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BuildRouteRequest = function (properties) {
            this.hop_pubkeys = [];
            this.first_hop_custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BuildRouteRequest amt_msat.
         * @member {Long} amt_msat
         * @memberof routerrpc.BuildRouteRequest
         * @instance
         */
        BuildRouteRequest.prototype.amt_msat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BuildRouteRequest final_cltv_delta.
         * @member {number} final_cltv_delta
         * @memberof routerrpc.BuildRouteRequest
         * @instance
         */
        BuildRouteRequest.prototype.final_cltv_delta = 0;

        /**
         * BuildRouteRequest outgoing_chan_id.
         * @member {Long} outgoing_chan_id
         * @memberof routerrpc.BuildRouteRequest
         * @instance
         */
        BuildRouteRequest.prototype.outgoing_chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BuildRouteRequest hop_pubkeys.
         * @member {Array.<Uint8Array>} hop_pubkeys
         * @memberof routerrpc.BuildRouteRequest
         * @instance
         */
        BuildRouteRequest.prototype.hop_pubkeys = $util.emptyArray;

        /**
         * BuildRouteRequest payment_addr.
         * @member {Uint8Array} payment_addr
         * @memberof routerrpc.BuildRouteRequest
         * @instance
         */
        BuildRouteRequest.prototype.payment_addr = $util.newBuffer([]);

        /**
         * BuildRouteRequest first_hop_custom_records.
         * @member {Object.<string,Uint8Array>} first_hop_custom_records
         * @memberof routerrpc.BuildRouteRequest
         * @instance
         */
        BuildRouteRequest.prototype.first_hop_custom_records = $util.emptyObject;

        /**
         * Creates a new BuildRouteRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.BuildRouteRequest
         * @static
         * @param {routerrpc.BuildRouteRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.BuildRouteRequest} BuildRouteRequest instance
         * @type {{
         *   (properties: routerrpc.BuildRouteRequest.$Shape): routerrpc.BuildRouteRequest & routerrpc.BuildRouteRequest.$Shape;
         *   (properties?: routerrpc.BuildRouteRequest.$Properties): routerrpc.BuildRouteRequest;
         * }}
         */
        BuildRouteRequest.create = function(properties) {
            return new BuildRouteRequest(properties);
        };

        /**
         * Encodes the specified BuildRouteRequest message. Does not implicitly {@link routerrpc.BuildRouteRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.BuildRouteRequest
         * @static
         * @param {routerrpc.BuildRouteRequest.$Properties} message BuildRouteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuildRouteRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat") && (typeof message.amt_msat === "object" ? message.amt_msat.low || message.amt_msat.high : message.amt_msat !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.amt_msat);
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta") && message.final_cltv_delta !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.final_cltv_delta);
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id") && (typeof message.outgoing_chan_id === "object" ? message.outgoing_chan_id.low || message.outgoing_chan_id.high : message.outgoing_chan_id !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.outgoing_chan_id);
            if (message.hop_pubkeys != null && message.hop_pubkeys.length)
                for (let i = 0; i < message.hop_pubkeys.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.hop_pubkeys[i]);
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr") && message.payment_addr.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.payment_addr);
            if (message.first_hop_custom_records != null && $Object.hasOwnProperty.call(message, "first_hop_custom_records"))
                for (let keys = $Object.keys(message.first_hop_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.first_hop_custom_records[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BuildRouteRequest message, length delimited. Does not implicitly {@link routerrpc.BuildRouteRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.BuildRouteRequest
         * @static
         * @param {routerrpc.BuildRouteRequest.$Properties} message BuildRouteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuildRouteRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BuildRouteRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.BuildRouteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.BuildRouteRequest & routerrpc.BuildRouteRequest.$Shape} BuildRouteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuildRouteRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.BuildRouteRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amt_msat = value;
                        else
                            delete message.amt_msat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.final_cltv_delta = value;
                        else
                            delete message.final_cltv_delta;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.outgoing_chan_id = value;
                        else
                            delete message.outgoing_chan_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hop_pubkeys && message.hop_pubkeys.length))
                            message.hop_pubkeys = [];
                        message.hop_pubkeys.push(reader.bytes());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_addr = value;
                        else
                            delete message.payment_addr;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (message.first_hop_custom_records === $util.emptyObject)
                            message.first_hop_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.first_hop_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BuildRouteRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.BuildRouteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.BuildRouteRequest & routerrpc.BuildRouteRequest.$Shape} BuildRouteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuildRouteRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuildRouteRequest message.
         * @function verify
         * @memberof routerrpc.BuildRouteRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuildRouteRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (!$util.isInteger(message.amt_msat) && !(message.amt_msat && $util.isInteger(message.amt_msat.low) && $util.isInteger(message.amt_msat.high)))
                    return "amt_msat: integer|Long expected";
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta"))
                if (!$util.isInteger(message.final_cltv_delta))
                    return "final_cltv_delta: integer expected";
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id"))
                if (!$util.isInteger(message.outgoing_chan_id) && !(message.outgoing_chan_id && $util.isInteger(message.outgoing_chan_id.low) && $util.isInteger(message.outgoing_chan_id.high)))
                    return "outgoing_chan_id: integer|Long expected";
            if (message.hop_pubkeys != null && $Object.hasOwnProperty.call(message, "hop_pubkeys")) {
                if (!$Array.isArray(message.hop_pubkeys))
                    return "hop_pubkeys: array expected";
                for (let i = 0; i < message.hop_pubkeys.length; ++i)
                    if (!(message.hop_pubkeys[i] && typeof message.hop_pubkeys[i].length === "number" || $util.isString(message.hop_pubkeys[i])))
                        return "hop_pubkeys: buffer[] expected";
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                if (!(message.payment_addr && typeof message.payment_addr.length === "number" || $util.isString(message.payment_addr)))
                    return "payment_addr: buffer expected";
            if (message.first_hop_custom_records != null && $Object.hasOwnProperty.call(message, "first_hop_custom_records")) {
                if (!$util.isObject(message.first_hop_custom_records))
                    return "first_hop_custom_records: object expected";
                let key = $Object.keys(message.first_hop_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "first_hop_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.first_hop_custom_records[key[i]] && typeof message.first_hop_custom_records[key[i]].length === "number" || $util.isString(message.first_hop_custom_records[key[i]])))
                        return "first_hop_custom_records: buffer{k:uint64} expected";
                }
            }
            return null;
        };

        /**
         * Creates a BuildRouteRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.BuildRouteRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.BuildRouteRequest} BuildRouteRequest
         */
        BuildRouteRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.BuildRouteRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.BuildRouteRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.BuildRouteRequest();
            if (object.amt_msat != null)
                if (typeof object.amt_msat === "object" ? object.amt_msat.low || object.amt_msat.high : $Number(object.amt_msat) !== 0)
                    if ($util.Long)
                        message.amt_msat = $util.Long.fromValue(object.amt_msat, false);
                    else if (typeof object.amt_msat === "string")
                        message.amt_msat = $parseInt(object.amt_msat, 10);
                    else if (typeof object.amt_msat === "number")
                        message.amt_msat = object.amt_msat;
                    else if (typeof object.amt_msat === "object")
                        message.amt_msat = new $util.LongBits(object.amt_msat.low >>> 0, object.amt_msat.high >>> 0).toNumber();
            if (object.final_cltv_delta != null)
                if ($Number(object.final_cltv_delta) !== 0)
                    message.final_cltv_delta = object.final_cltv_delta | 0;
            if (object.outgoing_chan_id != null)
                if (typeof object.outgoing_chan_id === "object" ? object.outgoing_chan_id.low || object.outgoing_chan_id.high : $Number(object.outgoing_chan_id) !== 0)
                    if ($util.Long)
                        message.outgoing_chan_id = $util.Long.fromValue(object.outgoing_chan_id, true);
                    else if (typeof object.outgoing_chan_id === "string")
                        message.outgoing_chan_id = $parseInt(object.outgoing_chan_id, 10);
                    else if (typeof object.outgoing_chan_id === "number")
                        message.outgoing_chan_id = object.outgoing_chan_id;
                    else if (typeof object.outgoing_chan_id === "object")
                        message.outgoing_chan_id = new $util.LongBits(object.outgoing_chan_id.low >>> 0, object.outgoing_chan_id.high >>> 0).toNumber(true);
            if (object.hop_pubkeys) {
                if (!$Array.isArray(object.hop_pubkeys))
                    throw $TypeError(".routerrpc.BuildRouteRequest.hop_pubkeys: array expected");
                message.hop_pubkeys = $Array(object.hop_pubkeys.length);
                for (let i = 0; i < object.hop_pubkeys.length; ++i)
                    if (typeof object.hop_pubkeys[i] === "string")
                        $util.base64.decode(object.hop_pubkeys[i], message.hop_pubkeys[i] = $util.newBuffer($util.base64.length(object.hop_pubkeys[i])), 0);
                    else if (object.hop_pubkeys[i].length >= 0)
                        message.hop_pubkeys[i] = object.hop_pubkeys[i];
            }
            if (object.payment_addr != null)
                if (object.payment_addr.length)
                    if (typeof object.payment_addr === "string")
                        $util.base64.decode(object.payment_addr, message.payment_addr = $util.newBuffer($util.base64.length(object.payment_addr)), 0);
                    else if (object.payment_addr.length >= 0)
                        message.payment_addr = object.payment_addr;
            if (object.first_hop_custom_records) {
                if (!$util.isObject(object.first_hop_custom_records))
                    throw $TypeError(".routerrpc.BuildRouteRequest.first_hop_custom_records: object expected");
                message.first_hop_custom_records = {};
                for (let keys = $Object.keys(object.first_hop_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.first_hop_custom_records, keys[i]);
                    if (typeof object.first_hop_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.first_hop_custom_records[keys[i]], message.first_hop_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.first_hop_custom_records[keys[i]])), 0);
                    else if (object.first_hop_custom_records[keys[i]].length >= 0)
                        message.first_hop_custom_records[keys[i]] = object.first_hop_custom_records[keys[i]];
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BuildRouteRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.BuildRouteRequest
         * @static
         * @param {routerrpc.BuildRouteRequest} message BuildRouteRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuildRouteRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.hop_pubkeys = [];
            if (options.objects || options.defaults)
                object.first_hop_custom_records = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.final_cltv_delta = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.outgoing_chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.outgoing_chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.payment_addr = "";
                else {
                    object.payment_addr = [];
                    if (options.bytes !== $Array)
                        object.payment_addr = $util.newBuffer(object.payment_addr);
                }
            }
            if (message.amt_msat != null && $Object.hasOwnProperty.call(message, "amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amt_msat = typeof message.amt_msat === "number" ? $BigInt(message.amt_msat) : $util.Long.fromBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0, false).toBigInt();
                else if (typeof message.amt_msat === "number")
                    object.amt_msat = options.longs === $String ? $String(message.amt_msat) : message.amt_msat;
                else
                    object.amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.amt_msat) : options.longs === $Number ? new $util.LongBits(message.amt_msat.low >>> 0, message.amt_msat.high >>> 0).toNumber() : message.amt_msat;
            if (message.final_cltv_delta != null && $Object.hasOwnProperty.call(message, "final_cltv_delta"))
                object.final_cltv_delta = message.final_cltv_delta;
            if (message.outgoing_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_chan_id = typeof message.outgoing_chan_id === "number" ? $BigInt(message.outgoing_chan_id) : $util.Long.fromBits(message.outgoing_chan_id.low >>> 0, message.outgoing_chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_chan_id === "number")
                    object.outgoing_chan_id = options.longs === $String ? $String(message.outgoing_chan_id) : message.outgoing_chan_id;
                else
                    object.outgoing_chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_chan_id) : options.longs === $Number ? new $util.LongBits(message.outgoing_chan_id.low >>> 0, message.outgoing_chan_id.high >>> 0).toNumber(true) : message.outgoing_chan_id;
            if (message.hop_pubkeys && message.hop_pubkeys.length) {
                object.hop_pubkeys = $Array(message.hop_pubkeys.length);
                for (let j = 0; j < message.hop_pubkeys.length; ++j)
                    object.hop_pubkeys[j] = options.bytes === $String ? $util.base64.encode(message.hop_pubkeys[j], 0, message.hop_pubkeys[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hop_pubkeys[j]) : message.hop_pubkeys[j];
            }
            if (message.payment_addr != null && $Object.hasOwnProperty.call(message, "payment_addr"))
                object.payment_addr = options.bytes === $String ? $util.base64.encode(message.payment_addr, 0, message.payment_addr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_addr) : message.payment_addr;
            let keys2;
            if (message.first_hop_custom_records && (keys2 = $Object.keys(message.first_hop_custom_records)).length) {
                object.first_hop_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.first_hop_custom_records, keys2[j]);
                    object.first_hop_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.first_hop_custom_records[keys2[j]], 0, message.first_hop_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.first_hop_custom_records[keys2[j]]) : message.first_hop_custom_records[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this BuildRouteRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.BuildRouteRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuildRouteRequest.prototype.toJSON = function() {
            return BuildRouteRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BuildRouteRequest
         * @function getTypeUrl
         * @memberof routerrpc.BuildRouteRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BuildRouteRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.BuildRouteRequest";
        };

        return BuildRouteRequest;
    })();

    routerrpc.BuildRouteResponse = (function() {

        /**
         * Properties of a BuildRouteResponse.
         * @typedef {Object} routerrpc.BuildRouteResponse.$Properties
         * @property {lnrpc.Route.$Properties|null} [route] BuildRouteResponse route
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BuildRouteResponse.
         * @memberof routerrpc
         * @interface IBuildRouteResponse
         * @augments routerrpc.BuildRouteResponse.$Properties
         * @deprecated Use routerrpc.BuildRouteResponse.$Properties instead.
         */

        /**
         * Shape of a BuildRouteResponse.
         * @typedef {routerrpc.BuildRouteResponse.$Properties} routerrpc.BuildRouteResponse.$Shape
         */

        /**
         * Constructs a new BuildRouteResponse.
         * @memberof routerrpc
         * @classdesc Represents a BuildRouteResponse.
         * @constructor
         * @param {routerrpc.BuildRouteResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BuildRouteResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BuildRouteResponse route.
         * @member {lnrpc.Route.$Properties|null|undefined} route
         * @memberof routerrpc.BuildRouteResponse
         * @instance
         */
        BuildRouteResponse.prototype.route = null;

        /**
         * Creates a new BuildRouteResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.BuildRouteResponse
         * @static
         * @param {routerrpc.BuildRouteResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.BuildRouteResponse} BuildRouteResponse instance
         * @type {{
         *   (properties: routerrpc.BuildRouteResponse.$Shape): routerrpc.BuildRouteResponse & routerrpc.BuildRouteResponse.$Shape;
         *   (properties?: routerrpc.BuildRouteResponse.$Properties): routerrpc.BuildRouteResponse;
         * }}
         */
        BuildRouteResponse.create = function(properties) {
            return new BuildRouteResponse(properties);
        };

        /**
         * Encodes the specified BuildRouteResponse message. Does not implicitly {@link routerrpc.BuildRouteResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.BuildRouteResponse
         * @static
         * @param {routerrpc.BuildRouteResponse.$Properties} message BuildRouteResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuildRouteResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.route != null && $Object.hasOwnProperty.call(message, "route"))
                $root.lnrpc.Route.encode(message.route, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BuildRouteResponse message, length delimited. Does not implicitly {@link routerrpc.BuildRouteResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.BuildRouteResponse
         * @static
         * @param {routerrpc.BuildRouteResponse.$Properties} message BuildRouteResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuildRouteResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BuildRouteResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.BuildRouteResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.BuildRouteResponse & routerrpc.BuildRouteResponse.$Shape} BuildRouteResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuildRouteResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.BuildRouteResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.route = $root.lnrpc.Route.decode(reader, reader.uint32(), $undefined, _depth + 1, message.route);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BuildRouteResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.BuildRouteResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.BuildRouteResponse & routerrpc.BuildRouteResponse.$Shape} BuildRouteResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuildRouteResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuildRouteResponse message.
         * @function verify
         * @memberof routerrpc.BuildRouteResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuildRouteResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.route != null && $Object.hasOwnProperty.call(message, "route")) {
                let error = $root.lnrpc.Route.verify(message.route, _depth + 1);
                if (error)
                    return "route." + error;
            }
            return null;
        };

        /**
         * Creates a BuildRouteResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.BuildRouteResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.BuildRouteResponse} BuildRouteResponse
         */
        BuildRouteResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.BuildRouteResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.BuildRouteResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.BuildRouteResponse();
            if (object.route != null) {
                if (!$util.isObject(object.route))
                    throw $TypeError(".routerrpc.BuildRouteResponse.route: object expected");
                message.route = $root.lnrpc.Route.fromObject(object.route, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a BuildRouteResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.BuildRouteResponse
         * @static
         * @param {routerrpc.BuildRouteResponse} message BuildRouteResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuildRouteResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.route = null;
            if (message.route != null && $Object.hasOwnProperty.call(message, "route"))
                object.route = $root.lnrpc.Route.toObject(message.route, options, _depth + 1);
            return object;
        };

        /**
         * Converts this BuildRouteResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.BuildRouteResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuildRouteResponse.prototype.toJSON = function() {
            return BuildRouteResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BuildRouteResponse
         * @function getTypeUrl
         * @memberof routerrpc.BuildRouteResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BuildRouteResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.BuildRouteResponse";
        };

        return BuildRouteResponse;
    })();

    routerrpc.SubscribeHtlcEventsRequest = (function() {

        /**
         * Properties of a SubscribeHtlcEventsRequest.
         * @typedef {Object} routerrpc.SubscribeHtlcEventsRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SubscribeHtlcEventsRequest.
         * @memberof routerrpc
         * @interface ISubscribeHtlcEventsRequest
         * @augments routerrpc.SubscribeHtlcEventsRequest.$Properties
         * @deprecated Use routerrpc.SubscribeHtlcEventsRequest.$Properties instead.
         */

        /**
         * Shape of a SubscribeHtlcEventsRequest.
         * @typedef {routerrpc.SubscribeHtlcEventsRequest.$Properties} routerrpc.SubscribeHtlcEventsRequest.$Shape
         */

        /**
         * Constructs a new SubscribeHtlcEventsRequest.
         * @memberof routerrpc
         * @classdesc Represents a SubscribeHtlcEventsRequest.
         * @constructor
         * @param {routerrpc.SubscribeHtlcEventsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SubscribeHtlcEventsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new SubscribeHtlcEventsRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @static
         * @param {routerrpc.SubscribeHtlcEventsRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.SubscribeHtlcEventsRequest} SubscribeHtlcEventsRequest instance
         * @type {{
         *   (properties: routerrpc.SubscribeHtlcEventsRequest.$Shape): routerrpc.SubscribeHtlcEventsRequest & routerrpc.SubscribeHtlcEventsRequest.$Shape;
         *   (properties?: routerrpc.SubscribeHtlcEventsRequest.$Properties): routerrpc.SubscribeHtlcEventsRequest;
         * }}
         */
        SubscribeHtlcEventsRequest.create = function(properties) {
            return new SubscribeHtlcEventsRequest(properties);
        };

        /**
         * Encodes the specified SubscribeHtlcEventsRequest message. Does not implicitly {@link routerrpc.SubscribeHtlcEventsRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @static
         * @param {routerrpc.SubscribeHtlcEventsRequest.$Properties} message SubscribeHtlcEventsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeHtlcEventsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SubscribeHtlcEventsRequest message, length delimited. Does not implicitly {@link routerrpc.SubscribeHtlcEventsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @static
         * @param {routerrpc.SubscribeHtlcEventsRequest.$Properties} message SubscribeHtlcEventsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeHtlcEventsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SubscribeHtlcEventsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.SubscribeHtlcEventsRequest & routerrpc.SubscribeHtlcEventsRequest.$Shape} SubscribeHtlcEventsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeHtlcEventsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.SubscribeHtlcEventsRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SubscribeHtlcEventsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.SubscribeHtlcEventsRequest & routerrpc.SubscribeHtlcEventsRequest.$Shape} SubscribeHtlcEventsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeHtlcEventsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SubscribeHtlcEventsRequest message.
         * @function verify
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SubscribeHtlcEventsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a SubscribeHtlcEventsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.SubscribeHtlcEventsRequest} SubscribeHtlcEventsRequest
         */
        SubscribeHtlcEventsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.SubscribeHtlcEventsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.SubscribeHtlcEventsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.SubscribeHtlcEventsRequest();
        };

        /**
         * Creates a plain object from a SubscribeHtlcEventsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @static
         * @param {routerrpc.SubscribeHtlcEventsRequest} message SubscribeHtlcEventsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SubscribeHtlcEventsRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this SubscribeHtlcEventsRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SubscribeHtlcEventsRequest.prototype.toJSON = function() {
            return SubscribeHtlcEventsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SubscribeHtlcEventsRequest
         * @function getTypeUrl
         * @memberof routerrpc.SubscribeHtlcEventsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SubscribeHtlcEventsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.SubscribeHtlcEventsRequest";
        };

        return SubscribeHtlcEventsRequest;
    })();

    routerrpc.HtlcEvent = (function() {

        /**
         * Properties of a HtlcEvent.
         * @typedef {Object} routerrpc.HtlcEvent.$Properties
         * @property {Long|null} [incoming_channel_id] HtlcEvent incoming_channel_id
         * @property {Long|null} [outgoing_channel_id] HtlcEvent outgoing_channel_id
         * @property {Long|null} [incoming_htlc_id] HtlcEvent incoming_htlc_id
         * @property {Long|null} [outgoing_htlc_id] HtlcEvent outgoing_htlc_id
         * @property {Long|null} [timestamp_ns] HtlcEvent timestamp_ns
         * @property {routerrpc.HtlcEvent.EventType|null} [event_type] HtlcEvent event_type
         * @property {routerrpc.ForwardEvent.$Properties|null} [forward_event] HtlcEvent forward_event
         * @property {routerrpc.ForwardFailEvent.$Properties|null} [forward_fail_event] HtlcEvent forward_fail_event
         * @property {routerrpc.SettleEvent.$Properties|null} [settle_event] HtlcEvent settle_event
         * @property {routerrpc.LinkFailEvent.$Properties|null} [link_fail_event] HtlcEvent link_fail_event
         * @property {routerrpc.SubscribedEvent.$Properties|null} [subscribed_event] HtlcEvent subscribed_event
         * @property {routerrpc.FinalHtlcEvent.$Properties|null} [final_htlc_event] HtlcEvent final_htlc_event
         * @property {"forward_event"|"forward_fail_event"|"settle_event"|"link_fail_event"|"subscribed_event"|"final_htlc_event"} [event] HtlcEvent event
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a HtlcEvent.
         * @memberof routerrpc
         * @interface IHtlcEvent
         * @augments routerrpc.HtlcEvent.$Properties
         * @deprecated Use routerrpc.HtlcEvent.$Properties instead.
         */

        /**
         * Narrowed shape of a HtlcEvent.
         * @typedef {{
         *   incoming_channel_id?: Long|null;
         *   outgoing_channel_id?: Long|null;
         *   incoming_htlc_id?: Long|null;
         *   outgoing_htlc_id?: Long|null;
         *   timestamp_ns?: Long|null;
         *   event_type?: routerrpc.HtlcEvent.EventType|null;
         *   forward_event?: routerrpc.ForwardEvent.$Shape|null;
         *   forward_fail_event?: routerrpc.ForwardFailEvent.$Shape|null;
         *   settle_event?: routerrpc.SettleEvent.$Shape|null;
         *   link_fail_event?: routerrpc.LinkFailEvent.$Shape|null;
         *   subscribed_event?: routerrpc.SubscribedEvent.$Shape|null;
         *   final_htlc_event?: routerrpc.FinalHtlcEvent.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ event?: undefined; forward_event?: null; forward_fail_event?: null; settle_event?: null; link_fail_event?: null; subscribed_event?: null; final_htlc_event?: null }|{ event?: "forward_event"; forward_event: routerrpc.ForwardEvent.$Shape; forward_fail_event?: null; settle_event?: null; link_fail_event?: null; subscribed_event?: null; final_htlc_event?: null }|{ event?: "forward_fail_event"; forward_event?: null; forward_fail_event: routerrpc.ForwardFailEvent.$Shape; settle_event?: null; link_fail_event?: null; subscribed_event?: null; final_htlc_event?: null }|{ event?: "settle_event"; forward_event?: null; forward_fail_event?: null; settle_event: routerrpc.SettleEvent.$Shape; link_fail_event?: null; subscribed_event?: null; final_htlc_event?: null }|{ event?: "link_fail_event"; forward_event?: null; forward_fail_event?: null; settle_event?: null; link_fail_event: routerrpc.LinkFailEvent.$Shape; subscribed_event?: null; final_htlc_event?: null }|{ event?: "subscribed_event"; forward_event?: null; forward_fail_event?: null; settle_event?: null; link_fail_event?: null; subscribed_event: routerrpc.SubscribedEvent.$Shape; final_htlc_event?: null }|{ event?: "final_htlc_event"; forward_event?: null; forward_fail_event?: null; settle_event?: null; link_fail_event?: null; subscribed_event?: null; final_htlc_event: routerrpc.FinalHtlcEvent.$Shape })
         * )} routerrpc.HtlcEvent.$Shape
         */

        /**
         * Constructs a new HtlcEvent.
         * @memberof routerrpc
         * @classdesc Represents a HtlcEvent.
         * @constructor
         * @param {routerrpc.HtlcEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const HtlcEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HtlcEvent incoming_channel_id.
         * @member {Long} incoming_channel_id
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.incoming_channel_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HtlcEvent outgoing_channel_id.
         * @member {Long} outgoing_channel_id
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.outgoing_channel_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HtlcEvent incoming_htlc_id.
         * @member {Long} incoming_htlc_id
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.incoming_htlc_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HtlcEvent outgoing_htlc_id.
         * @member {Long} outgoing_htlc_id
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.outgoing_htlc_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HtlcEvent timestamp_ns.
         * @member {Long} timestamp_ns
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.timestamp_ns = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HtlcEvent event_type.
         * @member {routerrpc.HtlcEvent.EventType} event_type
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.event_type = 0;

        /**
         * HtlcEvent forward_event.
         * @member {routerrpc.ForwardEvent.$Properties|null|undefined} forward_event
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.forward_event = null;

        /**
         * HtlcEvent forward_fail_event.
         * @member {routerrpc.ForwardFailEvent.$Properties|null|undefined} forward_fail_event
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.forward_fail_event = null;

        /**
         * HtlcEvent settle_event.
         * @member {routerrpc.SettleEvent.$Properties|null|undefined} settle_event
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.settle_event = null;

        /**
         * HtlcEvent link_fail_event.
         * @member {routerrpc.LinkFailEvent.$Properties|null|undefined} link_fail_event
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.link_fail_event = null;

        /**
         * HtlcEvent subscribed_event.
         * @member {routerrpc.SubscribedEvent.$Properties|null|undefined} subscribed_event
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.subscribed_event = null;

        /**
         * HtlcEvent final_htlc_event.
         * @member {routerrpc.FinalHtlcEvent.$Properties|null|undefined} final_htlc_event
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        HtlcEvent.prototype.final_htlc_event = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * HtlcEvent event.
         * @member {"forward_event"|"forward_fail_event"|"settle_event"|"link_fail_event"|"subscribed_event"|"final_htlc_event"|undefined} event
         * @memberof routerrpc.HtlcEvent
         * @instance
         */
        $Object.defineProperty(HtlcEvent.prototype, "event", {
            get: $util.oneOfGetter($oneOfFields = ["forward_event", "forward_fail_event", "settle_event", "link_fail_event", "subscribed_event", "final_htlc_event"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new HtlcEvent instance using the specified properties.
         * @function create
         * @memberof routerrpc.HtlcEvent
         * @static
         * @param {routerrpc.HtlcEvent.$Properties=} [properties] Properties to set
         * @returns {routerrpc.HtlcEvent} HtlcEvent instance
         * @type {{
         *   (properties: routerrpc.HtlcEvent.$Shape): routerrpc.HtlcEvent & routerrpc.HtlcEvent.$Shape;
         *   (properties?: routerrpc.HtlcEvent.$Properties): routerrpc.HtlcEvent;
         * }}
         */
        HtlcEvent.create = function(properties) {
            return new HtlcEvent(properties);
        };

        /**
         * Encodes the specified HtlcEvent message. Does not implicitly {@link routerrpc.HtlcEvent.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.HtlcEvent
         * @static
         * @param {routerrpc.HtlcEvent.$Properties} message HtlcEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HtlcEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.incoming_channel_id != null && $Object.hasOwnProperty.call(message, "incoming_channel_id") && (typeof message.incoming_channel_id === "object" ? message.incoming_channel_id.low || message.incoming_channel_id.high : message.incoming_channel_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.incoming_channel_id);
            if (message.outgoing_channel_id != null && $Object.hasOwnProperty.call(message, "outgoing_channel_id") && (typeof message.outgoing_channel_id === "object" ? message.outgoing_channel_id.low || message.outgoing_channel_id.high : message.outgoing_channel_id !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.outgoing_channel_id);
            if (message.incoming_htlc_id != null && $Object.hasOwnProperty.call(message, "incoming_htlc_id") && (typeof message.incoming_htlc_id === "object" ? message.incoming_htlc_id.low || message.incoming_htlc_id.high : message.incoming_htlc_id !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.incoming_htlc_id);
            if (message.outgoing_htlc_id != null && $Object.hasOwnProperty.call(message, "outgoing_htlc_id") && (typeof message.outgoing_htlc_id === "object" ? message.outgoing_htlc_id.low || message.outgoing_htlc_id.high : message.outgoing_htlc_id !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.outgoing_htlc_id);
            if (message.timestamp_ns != null && $Object.hasOwnProperty.call(message, "timestamp_ns") && (typeof message.timestamp_ns === "object" ? message.timestamp_ns.low || message.timestamp_ns.high : message.timestamp_ns !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.timestamp_ns);
            if (message.event_type != null && $Object.hasOwnProperty.call(message, "event_type") && message.event_type !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.event_type);
            if (message.forward_event != null && $Object.hasOwnProperty.call(message, "forward_event"))
                $root.routerrpc.ForwardEvent.encode(message.forward_event, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.forward_fail_event != null && $Object.hasOwnProperty.call(message, "forward_fail_event"))
                $root.routerrpc.ForwardFailEvent.encode(message.forward_fail_event, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.settle_event != null && $Object.hasOwnProperty.call(message, "settle_event"))
                $root.routerrpc.SettleEvent.encode(message.settle_event, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.link_fail_event != null && $Object.hasOwnProperty.call(message, "link_fail_event"))
                $root.routerrpc.LinkFailEvent.encode(message.link_fail_event, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.subscribed_event != null && $Object.hasOwnProperty.call(message, "subscribed_event"))
                $root.routerrpc.SubscribedEvent.encode(message.subscribed_event, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.final_htlc_event != null && $Object.hasOwnProperty.call(message, "final_htlc_event"))
                $root.routerrpc.FinalHtlcEvent.encode(message.final_htlc_event, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HtlcEvent message, length delimited. Does not implicitly {@link routerrpc.HtlcEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.HtlcEvent
         * @static
         * @param {routerrpc.HtlcEvent.$Properties} message HtlcEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HtlcEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HtlcEvent message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.HtlcEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.HtlcEvent & routerrpc.HtlcEvent.$Shape} HtlcEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HtlcEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.HtlcEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.incoming_channel_id = value;
                        else
                            delete message.incoming_channel_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.outgoing_channel_id = value;
                        else
                            delete message.outgoing_channel_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.incoming_htlc_id = value;
                        else
                            delete message.incoming_htlc_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.outgoing_htlc_id = value;
                        else
                            delete message.outgoing_htlc_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.timestamp_ns = value;
                        else
                            delete message.timestamp_ns;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.event_type = value;
                        else
                            delete message.event_type;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.forward_event = $root.routerrpc.ForwardEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.forward_event);
                        message.event = "forward_event";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.forward_fail_event = $root.routerrpc.ForwardFailEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.forward_fail_event);
                        message.event = "forward_fail_event";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.settle_event = $root.routerrpc.SettleEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.settle_event);
                        message.event = "settle_event";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.link_fail_event = $root.routerrpc.LinkFailEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.link_fail_event);
                        message.event = "link_fail_event";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.subscribed_event = $root.routerrpc.SubscribedEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.subscribed_event);
                        message.event = "subscribed_event";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.final_htlc_event = $root.routerrpc.FinalHtlcEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.final_htlc_event);
                        message.event = "final_htlc_event";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HtlcEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.HtlcEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.HtlcEvent & routerrpc.HtlcEvent.$Shape} HtlcEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HtlcEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HtlcEvent message.
         * @function verify
         * @memberof routerrpc.HtlcEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HtlcEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.incoming_channel_id != null && $Object.hasOwnProperty.call(message, "incoming_channel_id"))
                if (!$util.isInteger(message.incoming_channel_id) && !(message.incoming_channel_id && $util.isInteger(message.incoming_channel_id.low) && $util.isInteger(message.incoming_channel_id.high)))
                    return "incoming_channel_id: integer|Long expected";
            if (message.outgoing_channel_id != null && $Object.hasOwnProperty.call(message, "outgoing_channel_id"))
                if (!$util.isInteger(message.outgoing_channel_id) && !(message.outgoing_channel_id && $util.isInteger(message.outgoing_channel_id.low) && $util.isInteger(message.outgoing_channel_id.high)))
                    return "outgoing_channel_id: integer|Long expected";
            if (message.incoming_htlc_id != null && $Object.hasOwnProperty.call(message, "incoming_htlc_id"))
                if (!$util.isInteger(message.incoming_htlc_id) && !(message.incoming_htlc_id && $util.isInteger(message.incoming_htlc_id.low) && $util.isInteger(message.incoming_htlc_id.high)))
                    return "incoming_htlc_id: integer|Long expected";
            if (message.outgoing_htlc_id != null && $Object.hasOwnProperty.call(message, "outgoing_htlc_id"))
                if (!$util.isInteger(message.outgoing_htlc_id) && !(message.outgoing_htlc_id && $util.isInteger(message.outgoing_htlc_id.low) && $util.isInteger(message.outgoing_htlc_id.high)))
                    return "outgoing_htlc_id: integer|Long expected";
            if (message.timestamp_ns != null && $Object.hasOwnProperty.call(message, "timestamp_ns"))
                if (!$util.isInteger(message.timestamp_ns) && !(message.timestamp_ns && $util.isInteger(message.timestamp_ns.low) && $util.isInteger(message.timestamp_ns.high)))
                    return "timestamp_ns: integer|Long expected";
            if (message.event_type != null && $Object.hasOwnProperty.call(message, "event_type"))
                if (typeof message.event_type !== "number" || (message.event_type | 0) !== message.event_type)
                    return "event_type: enum value expected";
            if (message.forward_event != null && $Object.hasOwnProperty.call(message, "forward_event")) {
                properties.event = 1;
                {
                    let error = $root.routerrpc.ForwardEvent.verify(message.forward_event, _depth + 1);
                    if (error)
                        return "forward_event." + error;
                }
            }
            if (message.forward_fail_event != null && $Object.hasOwnProperty.call(message, "forward_fail_event")) {
                if (properties.event === 1)
                    return "event: multiple values";
                properties.event = 1;
                {
                    let error = $root.routerrpc.ForwardFailEvent.verify(message.forward_fail_event, _depth + 1);
                    if (error)
                        return "forward_fail_event." + error;
                }
            }
            if (message.settle_event != null && $Object.hasOwnProperty.call(message, "settle_event")) {
                if (properties.event === 1)
                    return "event: multiple values";
                properties.event = 1;
                {
                    let error = $root.routerrpc.SettleEvent.verify(message.settle_event, _depth + 1);
                    if (error)
                        return "settle_event." + error;
                }
            }
            if (message.link_fail_event != null && $Object.hasOwnProperty.call(message, "link_fail_event")) {
                if (properties.event === 1)
                    return "event: multiple values";
                properties.event = 1;
                {
                    let error = $root.routerrpc.LinkFailEvent.verify(message.link_fail_event, _depth + 1);
                    if (error)
                        return "link_fail_event." + error;
                }
            }
            if (message.subscribed_event != null && $Object.hasOwnProperty.call(message, "subscribed_event")) {
                if (properties.event === 1)
                    return "event: multiple values";
                properties.event = 1;
                {
                    let error = $root.routerrpc.SubscribedEvent.verify(message.subscribed_event, _depth + 1);
                    if (error)
                        return "subscribed_event." + error;
                }
            }
            if (message.final_htlc_event != null && $Object.hasOwnProperty.call(message, "final_htlc_event")) {
                if (properties.event === 1)
                    return "event: multiple values";
                properties.event = 1;
                {
                    let error = $root.routerrpc.FinalHtlcEvent.verify(message.final_htlc_event, _depth + 1);
                    if (error)
                        return "final_htlc_event." + error;
                }
            }
            return null;
        };

        /**
         * Creates a HtlcEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.HtlcEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.HtlcEvent} HtlcEvent
         */
        HtlcEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.HtlcEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.HtlcEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.HtlcEvent();
            if (object.incoming_channel_id != null)
                if (typeof object.incoming_channel_id === "object" ? object.incoming_channel_id.low || object.incoming_channel_id.high : $Number(object.incoming_channel_id) !== 0)
                    if ($util.Long)
                        message.incoming_channel_id = $util.Long.fromValue(object.incoming_channel_id, true);
                    else if (typeof object.incoming_channel_id === "string")
                        message.incoming_channel_id = $parseInt(object.incoming_channel_id, 10);
                    else if (typeof object.incoming_channel_id === "number")
                        message.incoming_channel_id = object.incoming_channel_id;
                    else if (typeof object.incoming_channel_id === "object")
                        message.incoming_channel_id = new $util.LongBits(object.incoming_channel_id.low >>> 0, object.incoming_channel_id.high >>> 0).toNumber(true);
            if (object.outgoing_channel_id != null)
                if (typeof object.outgoing_channel_id === "object" ? object.outgoing_channel_id.low || object.outgoing_channel_id.high : $Number(object.outgoing_channel_id) !== 0)
                    if ($util.Long)
                        message.outgoing_channel_id = $util.Long.fromValue(object.outgoing_channel_id, true);
                    else if (typeof object.outgoing_channel_id === "string")
                        message.outgoing_channel_id = $parseInt(object.outgoing_channel_id, 10);
                    else if (typeof object.outgoing_channel_id === "number")
                        message.outgoing_channel_id = object.outgoing_channel_id;
                    else if (typeof object.outgoing_channel_id === "object")
                        message.outgoing_channel_id = new $util.LongBits(object.outgoing_channel_id.low >>> 0, object.outgoing_channel_id.high >>> 0).toNumber(true);
            if (object.incoming_htlc_id != null)
                if (typeof object.incoming_htlc_id === "object" ? object.incoming_htlc_id.low || object.incoming_htlc_id.high : $Number(object.incoming_htlc_id) !== 0)
                    if ($util.Long)
                        message.incoming_htlc_id = $util.Long.fromValue(object.incoming_htlc_id, true);
                    else if (typeof object.incoming_htlc_id === "string")
                        message.incoming_htlc_id = $parseInt(object.incoming_htlc_id, 10);
                    else if (typeof object.incoming_htlc_id === "number")
                        message.incoming_htlc_id = object.incoming_htlc_id;
                    else if (typeof object.incoming_htlc_id === "object")
                        message.incoming_htlc_id = new $util.LongBits(object.incoming_htlc_id.low >>> 0, object.incoming_htlc_id.high >>> 0).toNumber(true);
            if (object.outgoing_htlc_id != null)
                if (typeof object.outgoing_htlc_id === "object" ? object.outgoing_htlc_id.low || object.outgoing_htlc_id.high : $Number(object.outgoing_htlc_id) !== 0)
                    if ($util.Long)
                        message.outgoing_htlc_id = $util.Long.fromValue(object.outgoing_htlc_id, true);
                    else if (typeof object.outgoing_htlc_id === "string")
                        message.outgoing_htlc_id = $parseInt(object.outgoing_htlc_id, 10);
                    else if (typeof object.outgoing_htlc_id === "number")
                        message.outgoing_htlc_id = object.outgoing_htlc_id;
                    else if (typeof object.outgoing_htlc_id === "object")
                        message.outgoing_htlc_id = new $util.LongBits(object.outgoing_htlc_id.low >>> 0, object.outgoing_htlc_id.high >>> 0).toNumber(true);
            if (object.timestamp_ns != null)
                if (typeof object.timestamp_ns === "object" ? object.timestamp_ns.low || object.timestamp_ns.high : $Number(object.timestamp_ns) !== 0)
                    if ($util.Long)
                        message.timestamp_ns = $util.Long.fromValue(object.timestamp_ns, true);
                    else if (typeof object.timestamp_ns === "string")
                        message.timestamp_ns = $parseInt(object.timestamp_ns, 10);
                    else if (typeof object.timestamp_ns === "number")
                        message.timestamp_ns = object.timestamp_ns;
                    else if (typeof object.timestamp_ns === "object")
                        message.timestamp_ns = new $util.LongBits(object.timestamp_ns.low >>> 0, object.timestamp_ns.high >>> 0).toNumber(true);
            if (object.event_type !== 0 && (typeof object.event_type !== "string" || $root.routerrpc.HtlcEvent.EventType[object.event_type] !== 0))
                switch (object.event_type) {
                case "UNKNOWN":
                case 0:
                    message.event_type = 0;
                    break;
                case "SEND":
                case 1:
                    message.event_type = 1;
                    break;
                case "RECEIVE":
                case 2:
                    message.event_type = 2;
                    break;
                case "FORWARD":
                case 3:
                    message.event_type = 3;
                    break;
                default:
                    if (typeof object.event_type === "number" && (object.event_type | 0) === object.event_type)
                        message.event_type = object.event_type;
                }
            if (object.forward_event != null) {
                if (!$util.isObject(object.forward_event))
                    throw $TypeError(".routerrpc.HtlcEvent.forward_event: object expected");
                message.forward_event = $root.routerrpc.ForwardEvent.fromObject(object.forward_event, _depth + 1);
            }
            if (object.forward_fail_event != null) {
                if (!$util.isObject(object.forward_fail_event))
                    throw $TypeError(".routerrpc.HtlcEvent.forward_fail_event: object expected");
                message.forward_fail_event = $root.routerrpc.ForwardFailEvent.fromObject(object.forward_fail_event, _depth + 1);
            }
            if (object.settle_event != null) {
                if (!$util.isObject(object.settle_event))
                    throw $TypeError(".routerrpc.HtlcEvent.settle_event: object expected");
                message.settle_event = $root.routerrpc.SettleEvent.fromObject(object.settle_event, _depth + 1);
            }
            if (object.link_fail_event != null) {
                if (!$util.isObject(object.link_fail_event))
                    throw $TypeError(".routerrpc.HtlcEvent.link_fail_event: object expected");
                message.link_fail_event = $root.routerrpc.LinkFailEvent.fromObject(object.link_fail_event, _depth + 1);
            }
            if (object.subscribed_event != null) {
                if (!$util.isObject(object.subscribed_event))
                    throw $TypeError(".routerrpc.HtlcEvent.subscribed_event: object expected");
                message.subscribed_event = $root.routerrpc.SubscribedEvent.fromObject(object.subscribed_event, _depth + 1);
            }
            if (object.final_htlc_event != null) {
                if (!$util.isObject(object.final_htlc_event))
                    throw $TypeError(".routerrpc.HtlcEvent.final_htlc_event: object expected");
                message.final_htlc_event = $root.routerrpc.FinalHtlcEvent.fromObject(object.final_htlc_event, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a HtlcEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.HtlcEvent
         * @static
         * @param {routerrpc.HtlcEvent} message HtlcEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HtlcEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.incoming_channel_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.incoming_channel_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.outgoing_channel_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.outgoing_channel_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.incoming_htlc_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.incoming_htlc_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.outgoing_htlc_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.outgoing_htlc_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.timestamp_ns = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.timestamp_ns = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.event_type = options.enums === $String ? "UNKNOWN" : 0;
            }
            if (message.incoming_channel_id != null && $Object.hasOwnProperty.call(message, "incoming_channel_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.incoming_channel_id = typeof message.incoming_channel_id === "number" ? $BigInt(message.incoming_channel_id) : $util.Long.fromBits(message.incoming_channel_id.low >>> 0, message.incoming_channel_id.high >>> 0, true).toBigInt();
                else if (typeof message.incoming_channel_id === "number")
                    object.incoming_channel_id = options.longs === $String ? $String(message.incoming_channel_id) : message.incoming_channel_id;
                else
                    object.incoming_channel_id = options.longs === $String ? $util.Long.prototype.toString.call(message.incoming_channel_id) : options.longs === $Number ? new $util.LongBits(message.incoming_channel_id.low >>> 0, message.incoming_channel_id.high >>> 0).toNumber(true) : message.incoming_channel_id;
            if (message.outgoing_channel_id != null && $Object.hasOwnProperty.call(message, "outgoing_channel_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_channel_id = typeof message.outgoing_channel_id === "number" ? $BigInt(message.outgoing_channel_id) : $util.Long.fromBits(message.outgoing_channel_id.low >>> 0, message.outgoing_channel_id.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_channel_id === "number")
                    object.outgoing_channel_id = options.longs === $String ? $String(message.outgoing_channel_id) : message.outgoing_channel_id;
                else
                    object.outgoing_channel_id = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_channel_id) : options.longs === $Number ? new $util.LongBits(message.outgoing_channel_id.low >>> 0, message.outgoing_channel_id.high >>> 0).toNumber(true) : message.outgoing_channel_id;
            if (message.incoming_htlc_id != null && $Object.hasOwnProperty.call(message, "incoming_htlc_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.incoming_htlc_id = typeof message.incoming_htlc_id === "number" ? $BigInt(message.incoming_htlc_id) : $util.Long.fromBits(message.incoming_htlc_id.low >>> 0, message.incoming_htlc_id.high >>> 0, true).toBigInt();
                else if (typeof message.incoming_htlc_id === "number")
                    object.incoming_htlc_id = options.longs === $String ? $String(message.incoming_htlc_id) : message.incoming_htlc_id;
                else
                    object.incoming_htlc_id = options.longs === $String ? $util.Long.prototype.toString.call(message.incoming_htlc_id) : options.longs === $Number ? new $util.LongBits(message.incoming_htlc_id.low >>> 0, message.incoming_htlc_id.high >>> 0).toNumber(true) : message.incoming_htlc_id;
            if (message.outgoing_htlc_id != null && $Object.hasOwnProperty.call(message, "outgoing_htlc_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_htlc_id = typeof message.outgoing_htlc_id === "number" ? $BigInt(message.outgoing_htlc_id) : $util.Long.fromBits(message.outgoing_htlc_id.low >>> 0, message.outgoing_htlc_id.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_htlc_id === "number")
                    object.outgoing_htlc_id = options.longs === $String ? $String(message.outgoing_htlc_id) : message.outgoing_htlc_id;
                else
                    object.outgoing_htlc_id = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_htlc_id) : options.longs === $Number ? new $util.LongBits(message.outgoing_htlc_id.low >>> 0, message.outgoing_htlc_id.high >>> 0).toNumber(true) : message.outgoing_htlc_id;
            if (message.timestamp_ns != null && $Object.hasOwnProperty.call(message, "timestamp_ns"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.timestamp_ns = typeof message.timestamp_ns === "number" ? $BigInt(message.timestamp_ns) : $util.Long.fromBits(message.timestamp_ns.low >>> 0, message.timestamp_ns.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp_ns === "number")
                    object.timestamp_ns = options.longs === $String ? $String(message.timestamp_ns) : message.timestamp_ns;
                else
                    object.timestamp_ns = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp_ns) : options.longs === $Number ? new $util.LongBits(message.timestamp_ns.low >>> 0, message.timestamp_ns.high >>> 0).toNumber(true) : message.timestamp_ns;
            if (message.event_type != null && $Object.hasOwnProperty.call(message, "event_type"))
                object.event_type = options.enums === $String ? $root.routerrpc.HtlcEvent.EventType[message.event_type] === $undefined ? message.event_type : $root.routerrpc.HtlcEvent.EventType[message.event_type] : message.event_type;
            if (message.forward_event != null && $Object.hasOwnProperty.call(message, "forward_event")) {
                object.forward_event = $root.routerrpc.ForwardEvent.toObject(message.forward_event, options, _depth + 1);
                if (options.oneofs)
                    object.event = "forward_event";
            }
            if (message.forward_fail_event != null && $Object.hasOwnProperty.call(message, "forward_fail_event")) {
                object.forward_fail_event = $root.routerrpc.ForwardFailEvent.toObject(message.forward_fail_event, options, _depth + 1);
                if (options.oneofs)
                    object.event = "forward_fail_event";
            }
            if (message.settle_event != null && $Object.hasOwnProperty.call(message, "settle_event")) {
                object.settle_event = $root.routerrpc.SettleEvent.toObject(message.settle_event, options, _depth + 1);
                if (options.oneofs)
                    object.event = "settle_event";
            }
            if (message.link_fail_event != null && $Object.hasOwnProperty.call(message, "link_fail_event")) {
                object.link_fail_event = $root.routerrpc.LinkFailEvent.toObject(message.link_fail_event, options, _depth + 1);
                if (options.oneofs)
                    object.event = "link_fail_event";
            }
            if (message.subscribed_event != null && $Object.hasOwnProperty.call(message, "subscribed_event")) {
                object.subscribed_event = $root.routerrpc.SubscribedEvent.toObject(message.subscribed_event, options, _depth + 1);
                if (options.oneofs)
                    object.event = "subscribed_event";
            }
            if (message.final_htlc_event != null && $Object.hasOwnProperty.call(message, "final_htlc_event")) {
                object.final_htlc_event = $root.routerrpc.FinalHtlcEvent.toObject(message.final_htlc_event, options, _depth + 1);
                if (options.oneofs)
                    object.event = "final_htlc_event";
            }
            return object;
        };

        /**
         * Converts this HtlcEvent to JSON.
         * @function toJSON
         * @memberof routerrpc.HtlcEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HtlcEvent.prototype.toJSON = function() {
            return HtlcEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HtlcEvent
         * @function getTypeUrl
         * @memberof routerrpc.HtlcEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HtlcEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.HtlcEvent";
        };

        /**
         * EventType enum.
         * @name routerrpc.HtlcEvent.EventType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} SEND=1 SEND value
         * @property {number} RECEIVE=2 RECEIVE value
         * @property {number} FORWARD=3 FORWARD value
         */
        HtlcEvent.EventType = (function() {
            const valuesById = {}, values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "SEND"] = 1;
            values[valuesById[2] = "RECEIVE"] = 2;
            values[valuesById[3] = "FORWARD"] = 3;
            return values;
        })();

        return HtlcEvent;
    })();

    routerrpc.HtlcInfo = (function() {

        /**
         * Properties of a HtlcInfo.
         * @typedef {Object} routerrpc.HtlcInfo.$Properties
         * @property {number|null} [incoming_timelock] HtlcInfo incoming_timelock
         * @property {number|null} [outgoing_timelock] HtlcInfo outgoing_timelock
         * @property {Long|null} [incoming_amt_msat] HtlcInfo incoming_amt_msat
         * @property {Long|null} [outgoing_amt_msat] HtlcInfo outgoing_amt_msat
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a HtlcInfo.
         * @memberof routerrpc
         * @interface IHtlcInfo
         * @augments routerrpc.HtlcInfo.$Properties
         * @deprecated Use routerrpc.HtlcInfo.$Properties instead.
         */

        /**
         * Shape of a HtlcInfo.
         * @typedef {routerrpc.HtlcInfo.$Properties} routerrpc.HtlcInfo.$Shape
         */

        /**
         * Constructs a new HtlcInfo.
         * @memberof routerrpc
         * @classdesc Represents a HtlcInfo.
         * @constructor
         * @param {routerrpc.HtlcInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const HtlcInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HtlcInfo incoming_timelock.
         * @member {number} incoming_timelock
         * @memberof routerrpc.HtlcInfo
         * @instance
         */
        HtlcInfo.prototype.incoming_timelock = 0;

        /**
         * HtlcInfo outgoing_timelock.
         * @member {number} outgoing_timelock
         * @memberof routerrpc.HtlcInfo
         * @instance
         */
        HtlcInfo.prototype.outgoing_timelock = 0;

        /**
         * HtlcInfo incoming_amt_msat.
         * @member {Long} incoming_amt_msat
         * @memberof routerrpc.HtlcInfo
         * @instance
         */
        HtlcInfo.prototype.incoming_amt_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HtlcInfo outgoing_amt_msat.
         * @member {Long} outgoing_amt_msat
         * @memberof routerrpc.HtlcInfo
         * @instance
         */
        HtlcInfo.prototype.outgoing_amt_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new HtlcInfo instance using the specified properties.
         * @function create
         * @memberof routerrpc.HtlcInfo
         * @static
         * @param {routerrpc.HtlcInfo.$Properties=} [properties] Properties to set
         * @returns {routerrpc.HtlcInfo} HtlcInfo instance
         * @type {{
         *   (properties: routerrpc.HtlcInfo.$Shape): routerrpc.HtlcInfo & routerrpc.HtlcInfo.$Shape;
         *   (properties?: routerrpc.HtlcInfo.$Properties): routerrpc.HtlcInfo;
         * }}
         */
        HtlcInfo.create = function(properties) {
            return new HtlcInfo(properties);
        };

        /**
         * Encodes the specified HtlcInfo message. Does not implicitly {@link routerrpc.HtlcInfo.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.HtlcInfo
         * @static
         * @param {routerrpc.HtlcInfo.$Properties} message HtlcInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HtlcInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.incoming_timelock != null && $Object.hasOwnProperty.call(message, "incoming_timelock") && message.incoming_timelock !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.incoming_timelock);
            if (message.outgoing_timelock != null && $Object.hasOwnProperty.call(message, "outgoing_timelock") && message.outgoing_timelock !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.outgoing_timelock);
            if (message.incoming_amt_msat != null && $Object.hasOwnProperty.call(message, "incoming_amt_msat") && (typeof message.incoming_amt_msat === "object" ? message.incoming_amt_msat.low || message.incoming_amt_msat.high : message.incoming_amt_msat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.incoming_amt_msat);
            if (message.outgoing_amt_msat != null && $Object.hasOwnProperty.call(message, "outgoing_amt_msat") && (typeof message.outgoing_amt_msat === "object" ? message.outgoing_amt_msat.low || message.outgoing_amt_msat.high : message.outgoing_amt_msat !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.outgoing_amt_msat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HtlcInfo message, length delimited. Does not implicitly {@link routerrpc.HtlcInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.HtlcInfo
         * @static
         * @param {routerrpc.HtlcInfo.$Properties} message HtlcInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HtlcInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HtlcInfo message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.HtlcInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.HtlcInfo & routerrpc.HtlcInfo.$Shape} HtlcInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HtlcInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.HtlcInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.incoming_timelock = value;
                        else
                            delete message.incoming_timelock;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.outgoing_timelock = value;
                        else
                            delete message.outgoing_timelock;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.incoming_amt_msat = value;
                        else
                            delete message.incoming_amt_msat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.outgoing_amt_msat = value;
                        else
                            delete message.outgoing_amt_msat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HtlcInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.HtlcInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.HtlcInfo & routerrpc.HtlcInfo.$Shape} HtlcInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HtlcInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HtlcInfo message.
         * @function verify
         * @memberof routerrpc.HtlcInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HtlcInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.incoming_timelock != null && $Object.hasOwnProperty.call(message, "incoming_timelock"))
                if (!$util.isInteger(message.incoming_timelock))
                    return "incoming_timelock: integer expected";
            if (message.outgoing_timelock != null && $Object.hasOwnProperty.call(message, "outgoing_timelock"))
                if (!$util.isInteger(message.outgoing_timelock))
                    return "outgoing_timelock: integer expected";
            if (message.incoming_amt_msat != null && $Object.hasOwnProperty.call(message, "incoming_amt_msat"))
                if (!$util.isInteger(message.incoming_amt_msat) && !(message.incoming_amt_msat && $util.isInteger(message.incoming_amt_msat.low) && $util.isInteger(message.incoming_amt_msat.high)))
                    return "incoming_amt_msat: integer|Long expected";
            if (message.outgoing_amt_msat != null && $Object.hasOwnProperty.call(message, "outgoing_amt_msat"))
                if (!$util.isInteger(message.outgoing_amt_msat) && !(message.outgoing_amt_msat && $util.isInteger(message.outgoing_amt_msat.low) && $util.isInteger(message.outgoing_amt_msat.high)))
                    return "outgoing_amt_msat: integer|Long expected";
            return null;
        };

        /**
         * Creates a HtlcInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.HtlcInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.HtlcInfo} HtlcInfo
         */
        HtlcInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.HtlcInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.HtlcInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.HtlcInfo();
            if (object.incoming_timelock != null)
                if ($Number(object.incoming_timelock) !== 0)
                    message.incoming_timelock = object.incoming_timelock >>> 0;
            if (object.outgoing_timelock != null)
                if ($Number(object.outgoing_timelock) !== 0)
                    message.outgoing_timelock = object.outgoing_timelock >>> 0;
            if (object.incoming_amt_msat != null)
                if (typeof object.incoming_amt_msat === "object" ? object.incoming_amt_msat.low || object.incoming_amt_msat.high : $Number(object.incoming_amt_msat) !== 0)
                    if ($util.Long)
                        message.incoming_amt_msat = $util.Long.fromValue(object.incoming_amt_msat, true);
                    else if (typeof object.incoming_amt_msat === "string")
                        message.incoming_amt_msat = $parseInt(object.incoming_amt_msat, 10);
                    else if (typeof object.incoming_amt_msat === "number")
                        message.incoming_amt_msat = object.incoming_amt_msat;
                    else if (typeof object.incoming_amt_msat === "object")
                        message.incoming_amt_msat = new $util.LongBits(object.incoming_amt_msat.low >>> 0, object.incoming_amt_msat.high >>> 0).toNumber(true);
            if (object.outgoing_amt_msat != null)
                if (typeof object.outgoing_amt_msat === "object" ? object.outgoing_amt_msat.low || object.outgoing_amt_msat.high : $Number(object.outgoing_amt_msat) !== 0)
                    if ($util.Long)
                        message.outgoing_amt_msat = $util.Long.fromValue(object.outgoing_amt_msat, true);
                    else if (typeof object.outgoing_amt_msat === "string")
                        message.outgoing_amt_msat = $parseInt(object.outgoing_amt_msat, 10);
                    else if (typeof object.outgoing_amt_msat === "number")
                        message.outgoing_amt_msat = object.outgoing_amt_msat;
                    else if (typeof object.outgoing_amt_msat === "object")
                        message.outgoing_amt_msat = new $util.LongBits(object.outgoing_amt_msat.low >>> 0, object.outgoing_amt_msat.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a HtlcInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.HtlcInfo
         * @static
         * @param {routerrpc.HtlcInfo} message HtlcInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HtlcInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.incoming_timelock = 0;
                object.outgoing_timelock = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.incoming_amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.incoming_amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.outgoing_amt_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.outgoing_amt_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.incoming_timelock != null && $Object.hasOwnProperty.call(message, "incoming_timelock"))
                object.incoming_timelock = message.incoming_timelock;
            if (message.outgoing_timelock != null && $Object.hasOwnProperty.call(message, "outgoing_timelock"))
                object.outgoing_timelock = message.outgoing_timelock;
            if (message.incoming_amt_msat != null && $Object.hasOwnProperty.call(message, "incoming_amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.incoming_amt_msat = typeof message.incoming_amt_msat === "number" ? $BigInt(message.incoming_amt_msat) : $util.Long.fromBits(message.incoming_amt_msat.low >>> 0, message.incoming_amt_msat.high >>> 0, true).toBigInt();
                else if (typeof message.incoming_amt_msat === "number")
                    object.incoming_amt_msat = options.longs === $String ? $String(message.incoming_amt_msat) : message.incoming_amt_msat;
                else
                    object.incoming_amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.incoming_amt_msat) : options.longs === $Number ? new $util.LongBits(message.incoming_amt_msat.low >>> 0, message.incoming_amt_msat.high >>> 0).toNumber(true) : message.incoming_amt_msat;
            if (message.outgoing_amt_msat != null && $Object.hasOwnProperty.call(message, "outgoing_amt_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_amt_msat = typeof message.outgoing_amt_msat === "number" ? $BigInt(message.outgoing_amt_msat) : $util.Long.fromBits(message.outgoing_amt_msat.low >>> 0, message.outgoing_amt_msat.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_amt_msat === "number")
                    object.outgoing_amt_msat = options.longs === $String ? $String(message.outgoing_amt_msat) : message.outgoing_amt_msat;
                else
                    object.outgoing_amt_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_amt_msat) : options.longs === $Number ? new $util.LongBits(message.outgoing_amt_msat.low >>> 0, message.outgoing_amt_msat.high >>> 0).toNumber(true) : message.outgoing_amt_msat;
            return object;
        };

        /**
         * Converts this HtlcInfo to JSON.
         * @function toJSON
         * @memberof routerrpc.HtlcInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HtlcInfo.prototype.toJSON = function() {
            return HtlcInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HtlcInfo
         * @function getTypeUrl
         * @memberof routerrpc.HtlcInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HtlcInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.HtlcInfo";
        };

        return HtlcInfo;
    })();

    routerrpc.ForwardEvent = (function() {

        /**
         * Properties of a ForwardEvent.
         * @typedef {Object} routerrpc.ForwardEvent.$Properties
         * @property {routerrpc.HtlcInfo.$Properties|null} [info] ForwardEvent info
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ForwardEvent.
         * @memberof routerrpc
         * @interface IForwardEvent
         * @augments routerrpc.ForwardEvent.$Properties
         * @deprecated Use routerrpc.ForwardEvent.$Properties instead.
         */

        /**
         * Shape of a ForwardEvent.
         * @typedef {routerrpc.ForwardEvent.$Properties} routerrpc.ForwardEvent.$Shape
         */

        /**
         * Constructs a new ForwardEvent.
         * @memberof routerrpc
         * @classdesc Represents a ForwardEvent.
         * @constructor
         * @param {routerrpc.ForwardEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ForwardEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ForwardEvent info.
         * @member {routerrpc.HtlcInfo.$Properties|null|undefined} info
         * @memberof routerrpc.ForwardEvent
         * @instance
         */
        ForwardEvent.prototype.info = null;

        /**
         * Creates a new ForwardEvent instance using the specified properties.
         * @function create
         * @memberof routerrpc.ForwardEvent
         * @static
         * @param {routerrpc.ForwardEvent.$Properties=} [properties] Properties to set
         * @returns {routerrpc.ForwardEvent} ForwardEvent instance
         * @type {{
         *   (properties: routerrpc.ForwardEvent.$Shape): routerrpc.ForwardEvent & routerrpc.ForwardEvent.$Shape;
         *   (properties?: routerrpc.ForwardEvent.$Properties): routerrpc.ForwardEvent;
         * }}
         */
        ForwardEvent.create = function(properties) {
            return new ForwardEvent(properties);
        };

        /**
         * Encodes the specified ForwardEvent message. Does not implicitly {@link routerrpc.ForwardEvent.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.ForwardEvent
         * @static
         * @param {routerrpc.ForwardEvent.$Properties} message ForwardEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.info != null && $Object.hasOwnProperty.call(message, "info"))
                $root.routerrpc.HtlcInfo.encode(message.info, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ForwardEvent message, length delimited. Does not implicitly {@link routerrpc.ForwardEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.ForwardEvent
         * @static
         * @param {routerrpc.ForwardEvent.$Properties} message ForwardEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ForwardEvent message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.ForwardEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.ForwardEvent & routerrpc.ForwardEvent.$Shape} ForwardEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.ForwardEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.info = $root.routerrpc.HtlcInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ForwardEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.ForwardEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.ForwardEvent & routerrpc.ForwardEvent.$Shape} ForwardEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForwardEvent message.
         * @function verify
         * @memberof routerrpc.ForwardEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForwardEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.info != null && $Object.hasOwnProperty.call(message, "info")) {
                let error = $root.routerrpc.HtlcInfo.verify(message.info, _depth + 1);
                if (error)
                    return "info." + error;
            }
            return null;
        };

        /**
         * Creates a ForwardEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.ForwardEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.ForwardEvent} ForwardEvent
         */
        ForwardEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.ForwardEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.ForwardEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.ForwardEvent();
            if (object.info != null) {
                if (!$util.isObject(object.info))
                    throw $TypeError(".routerrpc.ForwardEvent.info: object expected");
                message.info = $root.routerrpc.HtlcInfo.fromObject(object.info, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ForwardEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.ForwardEvent
         * @static
         * @param {routerrpc.ForwardEvent} message ForwardEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForwardEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.info = null;
            if (message.info != null && $Object.hasOwnProperty.call(message, "info"))
                object.info = $root.routerrpc.HtlcInfo.toObject(message.info, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ForwardEvent to JSON.
         * @function toJSON
         * @memberof routerrpc.ForwardEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForwardEvent.prototype.toJSON = function() {
            return ForwardEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ForwardEvent
         * @function getTypeUrl
         * @memberof routerrpc.ForwardEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ForwardEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.ForwardEvent";
        };

        return ForwardEvent;
    })();

    routerrpc.ForwardFailEvent = (function() {

        /**
         * Properties of a ForwardFailEvent.
         * @typedef {Object} routerrpc.ForwardFailEvent.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ForwardFailEvent.
         * @memberof routerrpc
         * @interface IForwardFailEvent
         * @augments routerrpc.ForwardFailEvent.$Properties
         * @deprecated Use routerrpc.ForwardFailEvent.$Properties instead.
         */

        /**
         * Shape of a ForwardFailEvent.
         * @typedef {routerrpc.ForwardFailEvent.$Properties} routerrpc.ForwardFailEvent.$Shape
         */

        /**
         * Constructs a new ForwardFailEvent.
         * @memberof routerrpc
         * @classdesc Represents a ForwardFailEvent.
         * @constructor
         * @param {routerrpc.ForwardFailEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ForwardFailEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ForwardFailEvent instance using the specified properties.
         * @function create
         * @memberof routerrpc.ForwardFailEvent
         * @static
         * @param {routerrpc.ForwardFailEvent.$Properties=} [properties] Properties to set
         * @returns {routerrpc.ForwardFailEvent} ForwardFailEvent instance
         * @type {{
         *   (properties: routerrpc.ForwardFailEvent.$Shape): routerrpc.ForwardFailEvent & routerrpc.ForwardFailEvent.$Shape;
         *   (properties?: routerrpc.ForwardFailEvent.$Properties): routerrpc.ForwardFailEvent;
         * }}
         */
        ForwardFailEvent.create = function(properties) {
            return new ForwardFailEvent(properties);
        };

        /**
         * Encodes the specified ForwardFailEvent message. Does not implicitly {@link routerrpc.ForwardFailEvent.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.ForwardFailEvent
         * @static
         * @param {routerrpc.ForwardFailEvent.$Properties} message ForwardFailEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardFailEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ForwardFailEvent message, length delimited. Does not implicitly {@link routerrpc.ForwardFailEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.ForwardFailEvent
         * @static
         * @param {routerrpc.ForwardFailEvent.$Properties} message ForwardFailEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardFailEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ForwardFailEvent message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.ForwardFailEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.ForwardFailEvent & routerrpc.ForwardFailEvent.$Shape} ForwardFailEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardFailEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.ForwardFailEvent();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ForwardFailEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.ForwardFailEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.ForwardFailEvent & routerrpc.ForwardFailEvent.$Shape} ForwardFailEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardFailEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForwardFailEvent message.
         * @function verify
         * @memberof routerrpc.ForwardFailEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForwardFailEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ForwardFailEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.ForwardFailEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.ForwardFailEvent} ForwardFailEvent
         */
        ForwardFailEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.ForwardFailEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.ForwardFailEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.ForwardFailEvent();
        };

        /**
         * Creates a plain object from a ForwardFailEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.ForwardFailEvent
         * @static
         * @param {routerrpc.ForwardFailEvent} message ForwardFailEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForwardFailEvent.toObject = function () {
            return {};
        };

        /**
         * Converts this ForwardFailEvent to JSON.
         * @function toJSON
         * @memberof routerrpc.ForwardFailEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForwardFailEvent.prototype.toJSON = function() {
            return ForwardFailEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ForwardFailEvent
         * @function getTypeUrl
         * @memberof routerrpc.ForwardFailEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ForwardFailEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.ForwardFailEvent";
        };

        return ForwardFailEvent;
    })();

    routerrpc.SettleEvent = (function() {

        /**
         * Properties of a SettleEvent.
         * @typedef {Object} routerrpc.SettleEvent.$Properties
         * @property {Uint8Array|null} [preimage] SettleEvent preimage
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SettleEvent.
         * @memberof routerrpc
         * @interface ISettleEvent
         * @augments routerrpc.SettleEvent.$Properties
         * @deprecated Use routerrpc.SettleEvent.$Properties instead.
         */

        /**
         * Shape of a SettleEvent.
         * @typedef {routerrpc.SettleEvent.$Properties} routerrpc.SettleEvent.$Shape
         */

        /**
         * Constructs a new SettleEvent.
         * @memberof routerrpc
         * @classdesc Represents a SettleEvent.
         * @constructor
         * @param {routerrpc.SettleEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SettleEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SettleEvent preimage.
         * @member {Uint8Array} preimage
         * @memberof routerrpc.SettleEvent
         * @instance
         */
        SettleEvent.prototype.preimage = $util.newBuffer([]);

        /**
         * Creates a new SettleEvent instance using the specified properties.
         * @function create
         * @memberof routerrpc.SettleEvent
         * @static
         * @param {routerrpc.SettleEvent.$Properties=} [properties] Properties to set
         * @returns {routerrpc.SettleEvent} SettleEvent instance
         * @type {{
         *   (properties: routerrpc.SettleEvent.$Shape): routerrpc.SettleEvent & routerrpc.SettleEvent.$Shape;
         *   (properties?: routerrpc.SettleEvent.$Properties): routerrpc.SettleEvent;
         * }}
         */
        SettleEvent.create = function(properties) {
            return new SettleEvent(properties);
        };

        /**
         * Encodes the specified SettleEvent message. Does not implicitly {@link routerrpc.SettleEvent.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.SettleEvent
         * @static
         * @param {routerrpc.SettleEvent.$Properties} message SettleEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SettleEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage") && message.preimage.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.preimage);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SettleEvent message, length delimited. Does not implicitly {@link routerrpc.SettleEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.SettleEvent
         * @static
         * @param {routerrpc.SettleEvent.$Properties} message SettleEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SettleEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SettleEvent message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.SettleEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.SettleEvent & routerrpc.SettleEvent.$Shape} SettleEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SettleEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.SettleEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.preimage = value;
                        else
                            delete message.preimage;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SettleEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.SettleEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.SettleEvent & routerrpc.SettleEvent.$Shape} SettleEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SettleEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SettleEvent message.
         * @function verify
         * @memberof routerrpc.SettleEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SettleEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                if (!(message.preimage && typeof message.preimage.length === "number" || $util.isString(message.preimage)))
                    return "preimage: buffer expected";
            return null;
        };

        /**
         * Creates a SettleEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.SettleEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.SettleEvent} SettleEvent
         */
        SettleEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.SettleEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.SettleEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.SettleEvent();
            if (object.preimage != null)
                if (object.preimage.length)
                    if (typeof object.preimage === "string")
                        $util.base64.decode(object.preimage, message.preimage = $util.newBuffer($util.base64.length(object.preimage)), 0);
                    else if (object.preimage.length >= 0)
                        message.preimage = object.preimage;
            return message;
        };

        /**
         * Creates a plain object from a SettleEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.SettleEvent
         * @static
         * @param {routerrpc.SettleEvent} message SettleEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SettleEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.preimage = "";
                else {
                    object.preimage = [];
                    if (options.bytes !== $Array)
                        object.preimage = $util.newBuffer(object.preimage);
                }
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                object.preimage = options.bytes === $String ? $util.base64.encode(message.preimage, 0, message.preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.preimage) : message.preimage;
            return object;
        };

        /**
         * Converts this SettleEvent to JSON.
         * @function toJSON
         * @memberof routerrpc.SettleEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SettleEvent.prototype.toJSON = function() {
            return SettleEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SettleEvent
         * @function getTypeUrl
         * @memberof routerrpc.SettleEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SettleEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.SettleEvent";
        };

        return SettleEvent;
    })();

    routerrpc.FinalHtlcEvent = (function() {

        /**
         * Properties of a FinalHtlcEvent.
         * @typedef {Object} routerrpc.FinalHtlcEvent.$Properties
         * @property {boolean|null} [settled] FinalHtlcEvent settled
         * @property {boolean|null} [offchain] FinalHtlcEvent offchain
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FinalHtlcEvent.
         * @memberof routerrpc
         * @interface IFinalHtlcEvent
         * @augments routerrpc.FinalHtlcEvent.$Properties
         * @deprecated Use routerrpc.FinalHtlcEvent.$Properties instead.
         */

        /**
         * Shape of a FinalHtlcEvent.
         * @typedef {routerrpc.FinalHtlcEvent.$Properties} routerrpc.FinalHtlcEvent.$Shape
         */

        /**
         * Constructs a new FinalHtlcEvent.
         * @memberof routerrpc
         * @classdesc Represents a FinalHtlcEvent.
         * @constructor
         * @param {routerrpc.FinalHtlcEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FinalHtlcEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FinalHtlcEvent settled.
         * @member {boolean} settled
         * @memberof routerrpc.FinalHtlcEvent
         * @instance
         */
        FinalHtlcEvent.prototype.settled = false;

        /**
         * FinalHtlcEvent offchain.
         * @member {boolean} offchain
         * @memberof routerrpc.FinalHtlcEvent
         * @instance
         */
        FinalHtlcEvent.prototype.offchain = false;

        /**
         * Creates a new FinalHtlcEvent instance using the specified properties.
         * @function create
         * @memberof routerrpc.FinalHtlcEvent
         * @static
         * @param {routerrpc.FinalHtlcEvent.$Properties=} [properties] Properties to set
         * @returns {routerrpc.FinalHtlcEvent} FinalHtlcEvent instance
         * @type {{
         *   (properties: routerrpc.FinalHtlcEvent.$Shape): routerrpc.FinalHtlcEvent & routerrpc.FinalHtlcEvent.$Shape;
         *   (properties?: routerrpc.FinalHtlcEvent.$Properties): routerrpc.FinalHtlcEvent;
         * }}
         */
        FinalHtlcEvent.create = function(properties) {
            return new FinalHtlcEvent(properties);
        };

        /**
         * Encodes the specified FinalHtlcEvent message. Does not implicitly {@link routerrpc.FinalHtlcEvent.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.FinalHtlcEvent
         * @static
         * @param {routerrpc.FinalHtlcEvent.$Properties} message FinalHtlcEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FinalHtlcEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.settled != null && $Object.hasOwnProperty.call(message, "settled") && message.settled !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.settled);
            if (message.offchain != null && $Object.hasOwnProperty.call(message, "offchain") && message.offchain !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.offchain);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FinalHtlcEvent message, length delimited. Does not implicitly {@link routerrpc.FinalHtlcEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.FinalHtlcEvent
         * @static
         * @param {routerrpc.FinalHtlcEvent.$Properties} message FinalHtlcEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FinalHtlcEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FinalHtlcEvent message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.FinalHtlcEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.FinalHtlcEvent & routerrpc.FinalHtlcEvent.$Shape} FinalHtlcEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FinalHtlcEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.FinalHtlcEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.settled = value;
                        else
                            delete message.settled;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.offchain = value;
                        else
                            delete message.offchain;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FinalHtlcEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.FinalHtlcEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.FinalHtlcEvent & routerrpc.FinalHtlcEvent.$Shape} FinalHtlcEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FinalHtlcEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FinalHtlcEvent message.
         * @function verify
         * @memberof routerrpc.FinalHtlcEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FinalHtlcEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.settled != null && $Object.hasOwnProperty.call(message, "settled"))
                if (typeof message.settled !== "boolean")
                    return "settled: boolean expected";
            if (message.offchain != null && $Object.hasOwnProperty.call(message, "offchain"))
                if (typeof message.offchain !== "boolean")
                    return "offchain: boolean expected";
            return null;
        };

        /**
         * Creates a FinalHtlcEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.FinalHtlcEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.FinalHtlcEvent} FinalHtlcEvent
         */
        FinalHtlcEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.FinalHtlcEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.FinalHtlcEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.FinalHtlcEvent();
            if (object.settled != null)
                if (object.settled)
                    message.settled = $Boolean(object.settled);
            if (object.offchain != null)
                if (object.offchain)
                    message.offchain = $Boolean(object.offchain);
            return message;
        };

        /**
         * Creates a plain object from a FinalHtlcEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.FinalHtlcEvent
         * @static
         * @param {routerrpc.FinalHtlcEvent} message FinalHtlcEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FinalHtlcEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.settled = false;
                object.offchain = false;
            }
            if (message.settled != null && $Object.hasOwnProperty.call(message, "settled"))
                object.settled = message.settled;
            if (message.offchain != null && $Object.hasOwnProperty.call(message, "offchain"))
                object.offchain = message.offchain;
            return object;
        };

        /**
         * Converts this FinalHtlcEvent to JSON.
         * @function toJSON
         * @memberof routerrpc.FinalHtlcEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FinalHtlcEvent.prototype.toJSON = function() {
            return FinalHtlcEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FinalHtlcEvent
         * @function getTypeUrl
         * @memberof routerrpc.FinalHtlcEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FinalHtlcEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.FinalHtlcEvent";
        };

        return FinalHtlcEvent;
    })();

    routerrpc.SubscribedEvent = (function() {

        /**
         * Properties of a SubscribedEvent.
         * @typedef {Object} routerrpc.SubscribedEvent.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SubscribedEvent.
         * @memberof routerrpc
         * @interface ISubscribedEvent
         * @augments routerrpc.SubscribedEvent.$Properties
         * @deprecated Use routerrpc.SubscribedEvent.$Properties instead.
         */

        /**
         * Shape of a SubscribedEvent.
         * @typedef {routerrpc.SubscribedEvent.$Properties} routerrpc.SubscribedEvent.$Shape
         */

        /**
         * Constructs a new SubscribedEvent.
         * @memberof routerrpc
         * @classdesc Represents a SubscribedEvent.
         * @constructor
         * @param {routerrpc.SubscribedEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SubscribedEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new SubscribedEvent instance using the specified properties.
         * @function create
         * @memberof routerrpc.SubscribedEvent
         * @static
         * @param {routerrpc.SubscribedEvent.$Properties=} [properties] Properties to set
         * @returns {routerrpc.SubscribedEvent} SubscribedEvent instance
         * @type {{
         *   (properties: routerrpc.SubscribedEvent.$Shape): routerrpc.SubscribedEvent & routerrpc.SubscribedEvent.$Shape;
         *   (properties?: routerrpc.SubscribedEvent.$Properties): routerrpc.SubscribedEvent;
         * }}
         */
        SubscribedEvent.create = function(properties) {
            return new SubscribedEvent(properties);
        };

        /**
         * Encodes the specified SubscribedEvent message. Does not implicitly {@link routerrpc.SubscribedEvent.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.SubscribedEvent
         * @static
         * @param {routerrpc.SubscribedEvent.$Properties} message SubscribedEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribedEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SubscribedEvent message, length delimited. Does not implicitly {@link routerrpc.SubscribedEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.SubscribedEvent
         * @static
         * @param {routerrpc.SubscribedEvent.$Properties} message SubscribedEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribedEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SubscribedEvent message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.SubscribedEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.SubscribedEvent & routerrpc.SubscribedEvent.$Shape} SubscribedEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribedEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.SubscribedEvent();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SubscribedEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.SubscribedEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.SubscribedEvent & routerrpc.SubscribedEvent.$Shape} SubscribedEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribedEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SubscribedEvent message.
         * @function verify
         * @memberof routerrpc.SubscribedEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SubscribedEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a SubscribedEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.SubscribedEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.SubscribedEvent} SubscribedEvent
         */
        SubscribedEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.SubscribedEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.SubscribedEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.SubscribedEvent();
        };

        /**
         * Creates a plain object from a SubscribedEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.SubscribedEvent
         * @static
         * @param {routerrpc.SubscribedEvent} message SubscribedEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SubscribedEvent.toObject = function () {
            return {};
        };

        /**
         * Converts this SubscribedEvent to JSON.
         * @function toJSON
         * @memberof routerrpc.SubscribedEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SubscribedEvent.prototype.toJSON = function() {
            return SubscribedEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SubscribedEvent
         * @function getTypeUrl
         * @memberof routerrpc.SubscribedEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SubscribedEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.SubscribedEvent";
        };

        return SubscribedEvent;
    })();

    routerrpc.LinkFailEvent = (function() {

        /**
         * Properties of a LinkFailEvent.
         * @typedef {Object} routerrpc.LinkFailEvent.$Properties
         * @property {routerrpc.HtlcInfo.$Properties|null} [info] LinkFailEvent info
         * @property {lnrpc.Failure.FailureCode|null} [wire_failure] LinkFailEvent wire_failure
         * @property {routerrpc.FailureDetail|null} [failure_detail] LinkFailEvent failure_detail
         * @property {string|null} [failure_string] LinkFailEvent failure_string
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LinkFailEvent.
         * @memberof routerrpc
         * @interface ILinkFailEvent
         * @augments routerrpc.LinkFailEvent.$Properties
         * @deprecated Use routerrpc.LinkFailEvent.$Properties instead.
         */

        /**
         * Shape of a LinkFailEvent.
         * @typedef {routerrpc.LinkFailEvent.$Properties} routerrpc.LinkFailEvent.$Shape
         */

        /**
         * Constructs a new LinkFailEvent.
         * @memberof routerrpc
         * @classdesc Represents a LinkFailEvent.
         * @constructor
         * @param {routerrpc.LinkFailEvent.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LinkFailEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LinkFailEvent info.
         * @member {routerrpc.HtlcInfo.$Properties|null|undefined} info
         * @memberof routerrpc.LinkFailEvent
         * @instance
         */
        LinkFailEvent.prototype.info = null;

        /**
         * LinkFailEvent wire_failure.
         * @member {lnrpc.Failure.FailureCode} wire_failure
         * @memberof routerrpc.LinkFailEvent
         * @instance
         */
        LinkFailEvent.prototype.wire_failure = 0;

        /**
         * LinkFailEvent failure_detail.
         * @member {routerrpc.FailureDetail} failure_detail
         * @memberof routerrpc.LinkFailEvent
         * @instance
         */
        LinkFailEvent.prototype.failure_detail = 0;

        /**
         * LinkFailEvent failure_string.
         * @member {string} failure_string
         * @memberof routerrpc.LinkFailEvent
         * @instance
         */
        LinkFailEvent.prototype.failure_string = "";

        /**
         * Creates a new LinkFailEvent instance using the specified properties.
         * @function create
         * @memberof routerrpc.LinkFailEvent
         * @static
         * @param {routerrpc.LinkFailEvent.$Properties=} [properties] Properties to set
         * @returns {routerrpc.LinkFailEvent} LinkFailEvent instance
         * @type {{
         *   (properties: routerrpc.LinkFailEvent.$Shape): routerrpc.LinkFailEvent & routerrpc.LinkFailEvent.$Shape;
         *   (properties?: routerrpc.LinkFailEvent.$Properties): routerrpc.LinkFailEvent;
         * }}
         */
        LinkFailEvent.create = function(properties) {
            return new LinkFailEvent(properties);
        };

        /**
         * Encodes the specified LinkFailEvent message. Does not implicitly {@link routerrpc.LinkFailEvent.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.LinkFailEvent
         * @static
         * @param {routerrpc.LinkFailEvent.$Properties} message LinkFailEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LinkFailEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.info != null && $Object.hasOwnProperty.call(message, "info"))
                $root.routerrpc.HtlcInfo.encode(message.info, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.wire_failure != null && $Object.hasOwnProperty.call(message, "wire_failure") && message.wire_failure !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.wire_failure);
            if (message.failure_detail != null && $Object.hasOwnProperty.call(message, "failure_detail") && message.failure_detail !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.failure_detail);
            if (message.failure_string != null && $Object.hasOwnProperty.call(message, "failure_string") && message.failure_string !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.failure_string);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LinkFailEvent message, length delimited. Does not implicitly {@link routerrpc.LinkFailEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.LinkFailEvent
         * @static
         * @param {routerrpc.LinkFailEvent.$Properties} message LinkFailEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LinkFailEvent.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LinkFailEvent message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.LinkFailEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.LinkFailEvent & routerrpc.LinkFailEvent.$Shape} LinkFailEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LinkFailEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.LinkFailEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.info = $root.routerrpc.HtlcInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.info);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.wire_failure = value;
                        else
                            delete message.wire_failure;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.failure_detail = value;
                        else
                            delete message.failure_detail;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.failure_string = value;
                        else
                            delete message.failure_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LinkFailEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.LinkFailEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.LinkFailEvent & routerrpc.LinkFailEvent.$Shape} LinkFailEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LinkFailEvent.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LinkFailEvent message.
         * @function verify
         * @memberof routerrpc.LinkFailEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LinkFailEvent.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.info != null && $Object.hasOwnProperty.call(message, "info")) {
                let error = $root.routerrpc.HtlcInfo.verify(message.info, _depth + 1);
                if (error)
                    return "info." + error;
            }
            if (message.wire_failure != null && $Object.hasOwnProperty.call(message, "wire_failure"))
                if (typeof message.wire_failure !== "number" || (message.wire_failure | 0) !== message.wire_failure)
                    return "wire_failure: enum value expected";
            if (message.failure_detail != null && $Object.hasOwnProperty.call(message, "failure_detail"))
                if (typeof message.failure_detail !== "number" || (message.failure_detail | 0) !== message.failure_detail)
                    return "failure_detail: enum value expected";
            if (message.failure_string != null && $Object.hasOwnProperty.call(message, "failure_string"))
                if (!$util.isString(message.failure_string))
                    return "failure_string: string expected";
            return null;
        };

        /**
         * Creates a LinkFailEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.LinkFailEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.LinkFailEvent} LinkFailEvent
         */
        LinkFailEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.LinkFailEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.LinkFailEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.LinkFailEvent();
            if (object.info != null) {
                if (!$util.isObject(object.info))
                    throw $TypeError(".routerrpc.LinkFailEvent.info: object expected");
                message.info = $root.routerrpc.HtlcInfo.fromObject(object.info, _depth + 1);
            }
            if (object.wire_failure !== 0 && (typeof object.wire_failure !== "string" || $root.lnrpc.Failure.FailureCode[object.wire_failure] !== 0))
                switch (object.wire_failure) {
                case "RESERVED":
                case 0:
                    message.wire_failure = 0;
                    break;
                case "INCORRECT_OR_UNKNOWN_PAYMENT_DETAILS":
                case 1:
                    message.wire_failure = 1;
                    break;
                case "INCORRECT_PAYMENT_AMOUNT":
                case 2:
                    message.wire_failure = 2;
                    break;
                case "FINAL_INCORRECT_CLTV_EXPIRY":
                case 3:
                    message.wire_failure = 3;
                    break;
                case "FINAL_INCORRECT_HTLC_AMOUNT":
                case 4:
                    message.wire_failure = 4;
                    break;
                case "FINAL_EXPIRY_TOO_SOON":
                case 5:
                    message.wire_failure = 5;
                    break;
                case "INVALID_REALM":
                case 6:
                    message.wire_failure = 6;
                    break;
                case "EXPIRY_TOO_SOON":
                case 7:
                    message.wire_failure = 7;
                    break;
                case "INVALID_ONION_VERSION":
                case 8:
                    message.wire_failure = 8;
                    break;
                case "INVALID_ONION_HMAC":
                case 9:
                    message.wire_failure = 9;
                    break;
                case "INVALID_ONION_KEY":
                case 10:
                    message.wire_failure = 10;
                    break;
                case "AMOUNT_BELOW_MINIMUM":
                case 11:
                    message.wire_failure = 11;
                    break;
                case "FEE_INSUFFICIENT":
                case 12:
                    message.wire_failure = 12;
                    break;
                case "INCORRECT_CLTV_EXPIRY":
                case 13:
                    message.wire_failure = 13;
                    break;
                case "CHANNEL_DISABLED":
                case 14:
                    message.wire_failure = 14;
                    break;
                case "TEMPORARY_CHANNEL_FAILURE":
                case 15:
                    message.wire_failure = 15;
                    break;
                case "REQUIRED_NODE_FEATURE_MISSING":
                case 16:
                    message.wire_failure = 16;
                    break;
                case "REQUIRED_CHANNEL_FEATURE_MISSING":
                case 17:
                    message.wire_failure = 17;
                    break;
                case "UNKNOWN_NEXT_PEER":
                case 18:
                    message.wire_failure = 18;
                    break;
                case "TEMPORARY_NODE_FAILURE":
                case 19:
                    message.wire_failure = 19;
                    break;
                case "PERMANENT_NODE_FAILURE":
                case 20:
                    message.wire_failure = 20;
                    break;
                case "PERMANENT_CHANNEL_FAILURE":
                case 21:
                    message.wire_failure = 21;
                    break;
                case "EXPIRY_TOO_FAR":
                case 22:
                    message.wire_failure = 22;
                    break;
                case "MPP_TIMEOUT":
                case 23:
                    message.wire_failure = 23;
                    break;
                case "INVALID_ONION_PAYLOAD":
                case 24:
                    message.wire_failure = 24;
                    break;
                case "INVALID_ONION_BLINDING":
                case 25:
                    message.wire_failure = 25;
                    break;
                case "INTERNAL_FAILURE":
                case 997:
                    message.wire_failure = 997;
                    break;
                case "UNKNOWN_FAILURE":
                case 998:
                    message.wire_failure = 998;
                    break;
                case "UNREADABLE_FAILURE":
                case 999:
                    message.wire_failure = 999;
                    break;
                default:
                    if (typeof object.wire_failure === "number" && (object.wire_failure | 0) === object.wire_failure)
                        message.wire_failure = object.wire_failure;
                }
            if (object.failure_detail !== 0 && (typeof object.failure_detail !== "string" || $root.routerrpc.FailureDetail[object.failure_detail] !== 0))
                switch (object.failure_detail) {
                case "UNKNOWN":
                case 0:
                    message.failure_detail = 0;
                    break;
                case "NO_DETAIL":
                case 1:
                    message.failure_detail = 1;
                    break;
                case "ONION_DECODE":
                case 2:
                    message.failure_detail = 2;
                    break;
                case "LINK_NOT_ELIGIBLE":
                case 3:
                    message.failure_detail = 3;
                    break;
                case "ON_CHAIN_TIMEOUT":
                case 4:
                    message.failure_detail = 4;
                    break;
                case "HTLC_EXCEEDS_MAX":
                case 5:
                    message.failure_detail = 5;
                    break;
                case "INSUFFICIENT_BALANCE":
                case 6:
                    message.failure_detail = 6;
                    break;
                case "INCOMPLETE_FORWARD":
                case 7:
                    message.failure_detail = 7;
                    break;
                case "HTLC_ADD_FAILED":
                case 8:
                    message.failure_detail = 8;
                    break;
                case "FORWARDS_DISABLED":
                case 9:
                    message.failure_detail = 9;
                    break;
                case "INVOICE_CANCELED":
                case 10:
                    message.failure_detail = 10;
                    break;
                case "INVOICE_UNDERPAID":
                case 11:
                    message.failure_detail = 11;
                    break;
                case "INVOICE_EXPIRY_TOO_SOON":
                case 12:
                    message.failure_detail = 12;
                    break;
                case "INVOICE_NOT_OPEN":
                case 13:
                    message.failure_detail = 13;
                    break;
                case "MPP_INVOICE_TIMEOUT":
                case 14:
                    message.failure_detail = 14;
                    break;
                case "ADDRESS_MISMATCH":
                case 15:
                    message.failure_detail = 15;
                    break;
                case "SET_TOTAL_MISMATCH":
                case 16:
                    message.failure_detail = 16;
                    break;
                case "SET_TOTAL_TOO_LOW":
                case 17:
                    message.failure_detail = 17;
                    break;
                case "SET_OVERPAID":
                case 18:
                    message.failure_detail = 18;
                    break;
                case "UNKNOWN_INVOICE":
                case 19:
                    message.failure_detail = 19;
                    break;
                case "INVALID_KEYSEND":
                case 20:
                    message.failure_detail = 20;
                    break;
                case "MPP_IN_PROGRESS":
                case 21:
                    message.failure_detail = 21;
                    break;
                case "CIRCULAR_ROUTE":
                case 22:
                    message.failure_detail = 22;
                    break;
                default:
                    if (typeof object.failure_detail === "number" && (object.failure_detail | 0) === object.failure_detail)
                        message.failure_detail = object.failure_detail;
                }
            if (object.failure_string != null)
                if (typeof object.failure_string !== "string" || object.failure_string.length)
                    message.failure_string = $String(object.failure_string);
            return message;
        };

        /**
         * Creates a plain object from a LinkFailEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.LinkFailEvent
         * @static
         * @param {routerrpc.LinkFailEvent} message LinkFailEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LinkFailEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.info = null;
                object.wire_failure = options.enums === $String ? "RESERVED" : 0;
                object.failure_detail = options.enums === $String ? "UNKNOWN" : 0;
                object.failure_string = "";
            }
            if (message.info != null && $Object.hasOwnProperty.call(message, "info"))
                object.info = $root.routerrpc.HtlcInfo.toObject(message.info, options, _depth + 1);
            if (message.wire_failure != null && $Object.hasOwnProperty.call(message, "wire_failure"))
                object.wire_failure = options.enums === $String ? $root.lnrpc.Failure.FailureCode[message.wire_failure] === $undefined ? message.wire_failure : $root.lnrpc.Failure.FailureCode[message.wire_failure] : message.wire_failure;
            if (message.failure_detail != null && $Object.hasOwnProperty.call(message, "failure_detail"))
                object.failure_detail = options.enums === $String ? $root.routerrpc.FailureDetail[message.failure_detail] === $undefined ? message.failure_detail : $root.routerrpc.FailureDetail[message.failure_detail] : message.failure_detail;
            if (message.failure_string != null && $Object.hasOwnProperty.call(message, "failure_string"))
                object.failure_string = message.failure_string;
            return object;
        };

        /**
         * Converts this LinkFailEvent to JSON.
         * @function toJSON
         * @memberof routerrpc.LinkFailEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LinkFailEvent.prototype.toJSON = function() {
            return LinkFailEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LinkFailEvent
         * @function getTypeUrl
         * @memberof routerrpc.LinkFailEvent
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LinkFailEvent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.LinkFailEvent";
        };

        return LinkFailEvent;
    })();

    /**
     * FailureDetail enum.
     * @name routerrpc.FailureDetail
     * @enum {number}
     * @property {number} UNKNOWN=0 UNKNOWN value
     * @property {number} NO_DETAIL=1 NO_DETAIL value
     * @property {number} ONION_DECODE=2 ONION_DECODE value
     * @property {number} LINK_NOT_ELIGIBLE=3 LINK_NOT_ELIGIBLE value
     * @property {number} ON_CHAIN_TIMEOUT=4 ON_CHAIN_TIMEOUT value
     * @property {number} HTLC_EXCEEDS_MAX=5 HTLC_EXCEEDS_MAX value
     * @property {number} INSUFFICIENT_BALANCE=6 INSUFFICIENT_BALANCE value
     * @property {number} INCOMPLETE_FORWARD=7 INCOMPLETE_FORWARD value
     * @property {number} HTLC_ADD_FAILED=8 HTLC_ADD_FAILED value
     * @property {number} FORWARDS_DISABLED=9 FORWARDS_DISABLED value
     * @property {number} INVOICE_CANCELED=10 INVOICE_CANCELED value
     * @property {number} INVOICE_UNDERPAID=11 INVOICE_UNDERPAID value
     * @property {number} INVOICE_EXPIRY_TOO_SOON=12 INVOICE_EXPIRY_TOO_SOON value
     * @property {number} INVOICE_NOT_OPEN=13 INVOICE_NOT_OPEN value
     * @property {number} MPP_INVOICE_TIMEOUT=14 MPP_INVOICE_TIMEOUT value
     * @property {number} ADDRESS_MISMATCH=15 ADDRESS_MISMATCH value
     * @property {number} SET_TOTAL_MISMATCH=16 SET_TOTAL_MISMATCH value
     * @property {number} SET_TOTAL_TOO_LOW=17 SET_TOTAL_TOO_LOW value
     * @property {number} SET_OVERPAID=18 SET_OVERPAID value
     * @property {number} UNKNOWN_INVOICE=19 UNKNOWN_INVOICE value
     * @property {number} INVALID_KEYSEND=20 INVALID_KEYSEND value
     * @property {number} MPP_IN_PROGRESS=21 MPP_IN_PROGRESS value
     * @property {number} CIRCULAR_ROUTE=22 CIRCULAR_ROUTE value
     */
    routerrpc.FailureDetail = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "NO_DETAIL"] = 1;
        values[valuesById[2] = "ONION_DECODE"] = 2;
        values[valuesById[3] = "LINK_NOT_ELIGIBLE"] = 3;
        values[valuesById[4] = "ON_CHAIN_TIMEOUT"] = 4;
        values[valuesById[5] = "HTLC_EXCEEDS_MAX"] = 5;
        values[valuesById[6] = "INSUFFICIENT_BALANCE"] = 6;
        values[valuesById[7] = "INCOMPLETE_FORWARD"] = 7;
        values[valuesById[8] = "HTLC_ADD_FAILED"] = 8;
        values[valuesById[9] = "FORWARDS_DISABLED"] = 9;
        values[valuesById[10] = "INVOICE_CANCELED"] = 10;
        values[valuesById[11] = "INVOICE_UNDERPAID"] = 11;
        values[valuesById[12] = "INVOICE_EXPIRY_TOO_SOON"] = 12;
        values[valuesById[13] = "INVOICE_NOT_OPEN"] = 13;
        values[valuesById[14] = "MPP_INVOICE_TIMEOUT"] = 14;
        values[valuesById[15] = "ADDRESS_MISMATCH"] = 15;
        values[valuesById[16] = "SET_TOTAL_MISMATCH"] = 16;
        values[valuesById[17] = "SET_TOTAL_TOO_LOW"] = 17;
        values[valuesById[18] = "SET_OVERPAID"] = 18;
        values[valuesById[19] = "UNKNOWN_INVOICE"] = 19;
        values[valuesById[20] = "INVALID_KEYSEND"] = 20;
        values[valuesById[21] = "MPP_IN_PROGRESS"] = 21;
        values[valuesById[22] = "CIRCULAR_ROUTE"] = 22;
        return values;
    })();

    /**
     * PaymentState enum.
     * @name routerrpc.PaymentState
     * @enum {number}
     * @property {number} IN_FLIGHT=0 IN_FLIGHT value
     * @property {number} SUCCEEDED=1 SUCCEEDED value
     * @property {number} FAILED_TIMEOUT=2 FAILED_TIMEOUT value
     * @property {number} FAILED_NO_ROUTE=3 FAILED_NO_ROUTE value
     * @property {number} FAILED_ERROR=4 FAILED_ERROR value
     * @property {number} FAILED_INCORRECT_PAYMENT_DETAILS=5 FAILED_INCORRECT_PAYMENT_DETAILS value
     * @property {number} FAILED_INSUFFICIENT_BALANCE=6 FAILED_INSUFFICIENT_BALANCE value
     */
    routerrpc.PaymentState = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "IN_FLIGHT"] = 0;
        values[valuesById[1] = "SUCCEEDED"] = 1;
        values[valuesById[2] = "FAILED_TIMEOUT"] = 2;
        values[valuesById[3] = "FAILED_NO_ROUTE"] = 3;
        values[valuesById[4] = "FAILED_ERROR"] = 4;
        values[valuesById[5] = "FAILED_INCORRECT_PAYMENT_DETAILS"] = 5;
        values[valuesById[6] = "FAILED_INSUFFICIENT_BALANCE"] = 6;
        return values;
    })();

    routerrpc.PaymentStatus = (function() {

        /**
         * Properties of a PaymentStatus.
         * @typedef {Object} routerrpc.PaymentStatus.$Properties
         * @property {routerrpc.PaymentState|null} [state] PaymentStatus state
         * @property {Uint8Array|null} [preimage] PaymentStatus preimage
         * @property {Array.<lnrpc.HTLCAttempt.$Properties>|null} [htlcs] PaymentStatus htlcs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PaymentStatus.
         * @memberof routerrpc
         * @interface IPaymentStatus
         * @augments routerrpc.PaymentStatus.$Properties
         * @deprecated Use routerrpc.PaymentStatus.$Properties instead.
         */

        /**
         * Shape of a PaymentStatus.
         * @typedef {routerrpc.PaymentStatus.$Properties} routerrpc.PaymentStatus.$Shape
         */

        /**
         * Constructs a new PaymentStatus.
         * @memberof routerrpc
         * @classdesc Represents a PaymentStatus.
         * @constructor
         * @param {routerrpc.PaymentStatus.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PaymentStatus = function (properties) {
            this.htlcs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PaymentStatus state.
         * @member {routerrpc.PaymentState} state
         * @memberof routerrpc.PaymentStatus
         * @instance
         */
        PaymentStatus.prototype.state = 0;

        /**
         * PaymentStatus preimage.
         * @member {Uint8Array} preimage
         * @memberof routerrpc.PaymentStatus
         * @instance
         */
        PaymentStatus.prototype.preimage = $util.newBuffer([]);

        /**
         * PaymentStatus htlcs.
         * @member {Array.<lnrpc.HTLCAttempt.$Properties>} htlcs
         * @memberof routerrpc.PaymentStatus
         * @instance
         */
        PaymentStatus.prototype.htlcs = $util.emptyArray;

        /**
         * Creates a new PaymentStatus instance using the specified properties.
         * @function create
         * @memberof routerrpc.PaymentStatus
         * @static
         * @param {routerrpc.PaymentStatus.$Properties=} [properties] Properties to set
         * @returns {routerrpc.PaymentStatus} PaymentStatus instance
         * @type {{
         *   (properties: routerrpc.PaymentStatus.$Shape): routerrpc.PaymentStatus & routerrpc.PaymentStatus.$Shape;
         *   (properties?: routerrpc.PaymentStatus.$Properties): routerrpc.PaymentStatus;
         * }}
         */
        PaymentStatus.create = function(properties) {
            return new PaymentStatus(properties);
        };

        /**
         * Encodes the specified PaymentStatus message. Does not implicitly {@link routerrpc.PaymentStatus.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.PaymentStatus
         * @static
         * @param {routerrpc.PaymentStatus.$Properties} message PaymentStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentStatus.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage") && message.preimage.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.preimage);
            if (message.htlcs != null && message.htlcs.length)
                for (let i = 0; i < message.htlcs.length; ++i)
                    $root.lnrpc.HTLCAttempt.encode(message.htlcs[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PaymentStatus message, length delimited. Does not implicitly {@link routerrpc.PaymentStatus.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.PaymentStatus
         * @static
         * @param {routerrpc.PaymentStatus.$Properties} message PaymentStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentStatus.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PaymentStatus message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.PaymentStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.PaymentStatus & routerrpc.PaymentStatus.$Shape} PaymentStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentStatus.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.PaymentStatus(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.preimage = value;
                        else
                            delete message.preimage;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.htlcs && message.htlcs.length))
                            message.htlcs = [];
                        message.htlcs.push($root.lnrpc.HTLCAttempt.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PaymentStatus message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.PaymentStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.PaymentStatus & routerrpc.PaymentStatus.$Shape} PaymentStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentStatus.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PaymentStatus message.
         * @function verify
         * @memberof routerrpc.PaymentStatus
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PaymentStatus.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                    return "state: enum value expected";
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                if (!(message.preimage && typeof message.preimage.length === "number" || $util.isString(message.preimage)))
                    return "preimage: buffer expected";
            if (message.htlcs != null && $Object.hasOwnProperty.call(message, "htlcs")) {
                if (!$Array.isArray(message.htlcs))
                    return "htlcs: array expected";
                for (let i = 0; i < message.htlcs.length; ++i) {
                    let error = $root.lnrpc.HTLCAttempt.verify(message.htlcs[i], _depth + 1);
                    if (error)
                        return "htlcs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PaymentStatus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.PaymentStatus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.PaymentStatus} PaymentStatus
         */
        PaymentStatus.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.PaymentStatus)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.PaymentStatus: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.PaymentStatus();
            if (object.state !== 0 && (typeof object.state !== "string" || $root.routerrpc.PaymentState[object.state] !== 0))
                switch (object.state) {
                case "IN_FLIGHT":
                case 0:
                    message.state = 0;
                    break;
                case "SUCCEEDED":
                case 1:
                    message.state = 1;
                    break;
                case "FAILED_TIMEOUT":
                case 2:
                    message.state = 2;
                    break;
                case "FAILED_NO_ROUTE":
                case 3:
                    message.state = 3;
                    break;
                case "FAILED_ERROR":
                case 4:
                    message.state = 4;
                    break;
                case "FAILED_INCORRECT_PAYMENT_DETAILS":
                case 5:
                    message.state = 5;
                    break;
                case "FAILED_INSUFFICIENT_BALANCE":
                case 6:
                    message.state = 6;
                    break;
                default:
                    if (typeof object.state === "number" && (object.state | 0) === object.state)
                        message.state = object.state;
                }
            if (object.preimage != null)
                if (object.preimage.length)
                    if (typeof object.preimage === "string")
                        $util.base64.decode(object.preimage, message.preimage = $util.newBuffer($util.base64.length(object.preimage)), 0);
                    else if (object.preimage.length >= 0)
                        message.preimage = object.preimage;
            if (object.htlcs) {
                if (!$Array.isArray(object.htlcs))
                    throw $TypeError(".routerrpc.PaymentStatus.htlcs: array expected");
                message.htlcs = $Array(object.htlcs.length);
                for (let i = 0; i < object.htlcs.length; ++i) {
                    if (!$util.isObject(object.htlcs[i]))
                        throw $TypeError(".routerrpc.PaymentStatus.htlcs: object expected");
                    message.htlcs[i] = $root.lnrpc.HTLCAttempt.fromObject(object.htlcs[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PaymentStatus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.PaymentStatus
         * @static
         * @param {routerrpc.PaymentStatus} message PaymentStatus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PaymentStatus.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.htlcs = [];
            if (options.defaults) {
                object.state = options.enums === $String ? "IN_FLIGHT" : 0;
                if (options.bytes === $String)
                    object.preimage = "";
                else {
                    object.preimage = [];
                    if (options.bytes !== $Array)
                        object.preimage = $util.newBuffer(object.preimage);
                }
            }
            if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                object.state = options.enums === $String ? $root.routerrpc.PaymentState[message.state] === $undefined ? message.state : $root.routerrpc.PaymentState[message.state] : message.state;
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                object.preimage = options.bytes === $String ? $util.base64.encode(message.preimage, 0, message.preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.preimage) : message.preimage;
            if (message.htlcs && message.htlcs.length) {
                object.htlcs = $Array(message.htlcs.length);
                for (let j = 0; j < message.htlcs.length; ++j)
                    object.htlcs[j] = $root.lnrpc.HTLCAttempt.toObject(message.htlcs[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this PaymentStatus to JSON.
         * @function toJSON
         * @memberof routerrpc.PaymentStatus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PaymentStatus.prototype.toJSON = function() {
            return PaymentStatus.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PaymentStatus
         * @function getTypeUrl
         * @memberof routerrpc.PaymentStatus
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PaymentStatus.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.PaymentStatus";
        };

        return PaymentStatus;
    })();

    routerrpc.CircuitKey = (function() {

        /**
         * Properties of a CircuitKey.
         * @typedef {Object} routerrpc.CircuitKey.$Properties
         * @property {Long|null} [chan_id] The id of the channel that the is part of this circuit.
         * @property {Long|null} [htlc_id] The index of the incoming htlc in the incoming channel.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a CircuitKey.
         * @memberof routerrpc
         * @interface ICircuitKey
         * @augments routerrpc.CircuitKey.$Properties
         * @deprecated Use routerrpc.CircuitKey.$Properties instead.
         */

        /**
         * Shape of a CircuitKey.
         * @typedef {routerrpc.CircuitKey.$Properties} routerrpc.CircuitKey.$Shape
         */

        /**
         * Constructs a new CircuitKey.
         * @memberof routerrpc
         * @classdesc Represents a CircuitKey.
         * @constructor
         * @param {routerrpc.CircuitKey.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const CircuitKey = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The id of the channel that the is part of this circuit.
         * @member {Long} chan_id
         * @memberof routerrpc.CircuitKey
         * @instance
         */
        CircuitKey.prototype.chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * The index of the incoming htlc in the incoming channel.
         * @member {Long} htlc_id
         * @memberof routerrpc.CircuitKey
         * @instance
         */
        CircuitKey.prototype.htlc_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new CircuitKey instance using the specified properties.
         * @function create
         * @memberof routerrpc.CircuitKey
         * @static
         * @param {routerrpc.CircuitKey.$Properties=} [properties] Properties to set
         * @returns {routerrpc.CircuitKey} CircuitKey instance
         * @type {{
         *   (properties: routerrpc.CircuitKey.$Shape): routerrpc.CircuitKey & routerrpc.CircuitKey.$Shape;
         *   (properties?: routerrpc.CircuitKey.$Properties): routerrpc.CircuitKey;
         * }}
         */
        CircuitKey.create = function(properties) {
            return new CircuitKey(properties);
        };

        /**
         * Encodes the specified CircuitKey message. Does not implicitly {@link routerrpc.CircuitKey.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.CircuitKey
         * @static
         * @param {routerrpc.CircuitKey.$Properties} message CircuitKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CircuitKey.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id") && (typeof message.chan_id === "object" ? message.chan_id.low || message.chan_id.high : message.chan_id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.chan_id);
            if (message.htlc_id != null && $Object.hasOwnProperty.call(message, "htlc_id") && (typeof message.htlc_id === "object" ? message.htlc_id.low || message.htlc_id.high : message.htlc_id !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.htlc_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CircuitKey message, length delimited. Does not implicitly {@link routerrpc.CircuitKey.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.CircuitKey
         * @static
         * @param {routerrpc.CircuitKey.$Properties} message CircuitKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CircuitKey.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CircuitKey message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.CircuitKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.CircuitKey & routerrpc.CircuitKey.$Shape} CircuitKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CircuitKey.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.CircuitKey(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.chan_id = value;
                        else
                            delete message.chan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.htlc_id = value;
                        else
                            delete message.htlc_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CircuitKey message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.CircuitKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.CircuitKey & routerrpc.CircuitKey.$Shape} CircuitKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CircuitKey.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CircuitKey message.
         * @function verify
         * @memberof routerrpc.CircuitKey
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CircuitKey.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (!$util.isInteger(message.chan_id) && !(message.chan_id && $util.isInteger(message.chan_id.low) && $util.isInteger(message.chan_id.high)))
                    return "chan_id: integer|Long expected";
            if (message.htlc_id != null && $Object.hasOwnProperty.call(message, "htlc_id"))
                if (!$util.isInteger(message.htlc_id) && !(message.htlc_id && $util.isInteger(message.htlc_id.low) && $util.isInteger(message.htlc_id.high)))
                    return "htlc_id: integer|Long expected";
            return null;
        };

        /**
         * Creates a CircuitKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.CircuitKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.CircuitKey} CircuitKey
         */
        CircuitKey.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.CircuitKey)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.CircuitKey: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.CircuitKey();
            if (object.chan_id != null)
                if (typeof object.chan_id === "object" ? object.chan_id.low || object.chan_id.high : $Number(object.chan_id) !== 0)
                    if ($util.Long)
                        message.chan_id = $util.Long.fromValue(object.chan_id, true);
                    else if (typeof object.chan_id === "string")
                        message.chan_id = $parseInt(object.chan_id, 10);
                    else if (typeof object.chan_id === "number")
                        message.chan_id = object.chan_id;
                    else if (typeof object.chan_id === "object")
                        message.chan_id = new $util.LongBits(object.chan_id.low >>> 0, object.chan_id.high >>> 0).toNumber(true);
            if (object.htlc_id != null)
                if (typeof object.htlc_id === "object" ? object.htlc_id.low || object.htlc_id.high : $Number(object.htlc_id) !== 0)
                    if ($util.Long)
                        message.htlc_id = $util.Long.fromValue(object.htlc_id, true);
                    else if (typeof object.htlc_id === "string")
                        message.htlc_id = $parseInt(object.htlc_id, 10);
                    else if (typeof object.htlc_id === "number")
                        message.htlc_id = object.htlc_id;
                    else if (typeof object.htlc_id === "object")
                        message.htlc_id = new $util.LongBits(object.htlc_id.low >>> 0, object.htlc_id.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a CircuitKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.CircuitKey
         * @static
         * @param {routerrpc.CircuitKey} message CircuitKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CircuitKey.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.htlc_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.htlc_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.chan_id != null && $Object.hasOwnProperty.call(message, "chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.chan_id = typeof message.chan_id === "number" ? $BigInt(message.chan_id) : $util.Long.fromBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.chan_id === "number")
                    object.chan_id = options.longs === $String ? $String(message.chan_id) : message.chan_id;
                else
                    object.chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.chan_id) : options.longs === $Number ? new $util.LongBits(message.chan_id.low >>> 0, message.chan_id.high >>> 0).toNumber(true) : message.chan_id;
            if (message.htlc_id != null && $Object.hasOwnProperty.call(message, "htlc_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.htlc_id = typeof message.htlc_id === "number" ? $BigInt(message.htlc_id) : $util.Long.fromBits(message.htlc_id.low >>> 0, message.htlc_id.high >>> 0, true).toBigInt();
                else if (typeof message.htlc_id === "number")
                    object.htlc_id = options.longs === $String ? $String(message.htlc_id) : message.htlc_id;
                else
                    object.htlc_id = options.longs === $String ? $util.Long.prototype.toString.call(message.htlc_id) : options.longs === $Number ? new $util.LongBits(message.htlc_id.low >>> 0, message.htlc_id.high >>> 0).toNumber(true) : message.htlc_id;
            return object;
        };

        /**
         * Converts this CircuitKey to JSON.
         * @function toJSON
         * @memberof routerrpc.CircuitKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CircuitKey.prototype.toJSON = function() {
            return CircuitKey.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CircuitKey
         * @function getTypeUrl
         * @memberof routerrpc.CircuitKey
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CircuitKey.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.CircuitKey";
        };

        return CircuitKey;
    })();

    routerrpc.ForwardHtlcInterceptRequest = (function() {

        /**
         * Properties of a ForwardHtlcInterceptRequest.
         * @typedef {Object} routerrpc.ForwardHtlcInterceptRequest.$Properties
         * @property {routerrpc.CircuitKey.$Properties|null} [incoming_circuit_key] ForwardHtlcInterceptRequest incoming_circuit_key
         * @property {Long|null} [incoming_amount_msat] ForwardHtlcInterceptRequest incoming_amount_msat
         * @property {number|null} [incoming_expiry] ForwardHtlcInterceptRequest incoming_expiry
         * @property {Uint8Array|null} [payment_hash] ForwardHtlcInterceptRequest payment_hash
         * @property {Long|null} [outgoing_requested_chan_id] ForwardHtlcInterceptRequest outgoing_requested_chan_id
         * @property {Long|null} [outgoing_amount_msat] ForwardHtlcInterceptRequest outgoing_amount_msat
         * @property {number|null} [outgoing_expiry] ForwardHtlcInterceptRequest outgoing_expiry
         * @property {Object.<string,Uint8Array>|null} [custom_records] ForwardHtlcInterceptRequest custom_records
         * @property {Uint8Array|null} [onion_blob] ForwardHtlcInterceptRequest onion_blob
         * @property {number|null} [auto_fail_height] ForwardHtlcInterceptRequest auto_fail_height
         * @property {Object.<string,Uint8Array>|null} [in_wire_custom_records] ForwardHtlcInterceptRequest in_wire_custom_records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ForwardHtlcInterceptRequest.
         * @memberof routerrpc
         * @interface IForwardHtlcInterceptRequest
         * @augments routerrpc.ForwardHtlcInterceptRequest.$Properties
         * @deprecated Use routerrpc.ForwardHtlcInterceptRequest.$Properties instead.
         */

        /**
         * Shape of a ForwardHtlcInterceptRequest.
         * @typedef {routerrpc.ForwardHtlcInterceptRequest.$Properties} routerrpc.ForwardHtlcInterceptRequest.$Shape
         */

        /**
         * Constructs a new ForwardHtlcInterceptRequest.
         * @memberof routerrpc
         * @classdesc Represents a ForwardHtlcInterceptRequest.
         * @constructor
         * @param {routerrpc.ForwardHtlcInterceptRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ForwardHtlcInterceptRequest = function (properties) {
            this.custom_records = {};
            this.in_wire_custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ForwardHtlcInterceptRequest incoming_circuit_key.
         * @member {routerrpc.CircuitKey.$Properties|null|undefined} incoming_circuit_key
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.incoming_circuit_key = null;

        /**
         * ForwardHtlcInterceptRequest incoming_amount_msat.
         * @member {Long} incoming_amount_msat
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.incoming_amount_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardHtlcInterceptRequest incoming_expiry.
         * @member {number} incoming_expiry
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.incoming_expiry = 0;

        /**
         * ForwardHtlcInterceptRequest payment_hash.
         * @member {Uint8Array} payment_hash
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.payment_hash = $util.newBuffer([]);

        /**
         * ForwardHtlcInterceptRequest outgoing_requested_chan_id.
         * @member {Long} outgoing_requested_chan_id
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.outgoing_requested_chan_id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardHtlcInterceptRequest outgoing_amount_msat.
         * @member {Long} outgoing_amount_msat
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.outgoing_amount_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardHtlcInterceptRequest outgoing_expiry.
         * @member {number} outgoing_expiry
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.outgoing_expiry = 0;

        /**
         * ForwardHtlcInterceptRequest custom_records.
         * @member {Object.<string,Uint8Array>} custom_records
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.custom_records = $util.emptyObject;

        /**
         * ForwardHtlcInterceptRequest onion_blob.
         * @member {Uint8Array} onion_blob
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.onion_blob = $util.newBuffer([]);

        /**
         * ForwardHtlcInterceptRequest auto_fail_height.
         * @member {number} auto_fail_height
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.auto_fail_height = 0;

        /**
         * ForwardHtlcInterceptRequest in_wire_custom_records.
         * @member {Object.<string,Uint8Array>} in_wire_custom_records
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         */
        ForwardHtlcInterceptRequest.prototype.in_wire_custom_records = $util.emptyObject;

        /**
         * Creates a new ForwardHtlcInterceptRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @static
         * @param {routerrpc.ForwardHtlcInterceptRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.ForwardHtlcInterceptRequest} ForwardHtlcInterceptRequest instance
         * @type {{
         *   (properties: routerrpc.ForwardHtlcInterceptRequest.$Shape): routerrpc.ForwardHtlcInterceptRequest & routerrpc.ForwardHtlcInterceptRequest.$Shape;
         *   (properties?: routerrpc.ForwardHtlcInterceptRequest.$Properties): routerrpc.ForwardHtlcInterceptRequest;
         * }}
         */
        ForwardHtlcInterceptRequest.create = function(properties) {
            return new ForwardHtlcInterceptRequest(properties);
        };

        /**
         * Encodes the specified ForwardHtlcInterceptRequest message. Does not implicitly {@link routerrpc.ForwardHtlcInterceptRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @static
         * @param {routerrpc.ForwardHtlcInterceptRequest.$Properties} message ForwardHtlcInterceptRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardHtlcInterceptRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.incoming_circuit_key != null && $Object.hasOwnProperty.call(message, "incoming_circuit_key"))
                $root.routerrpc.CircuitKey.encode(message.incoming_circuit_key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash") && message.payment_hash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.payment_hash);
            if (message.outgoing_amount_msat != null && $Object.hasOwnProperty.call(message, "outgoing_amount_msat") && (typeof message.outgoing_amount_msat === "object" ? message.outgoing_amount_msat.low || message.outgoing_amount_msat.high : message.outgoing_amount_msat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.outgoing_amount_msat);
            if (message.outgoing_expiry != null && $Object.hasOwnProperty.call(message, "outgoing_expiry") && message.outgoing_expiry !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.outgoing_expiry);
            if (message.incoming_amount_msat != null && $Object.hasOwnProperty.call(message, "incoming_amount_msat") && (typeof message.incoming_amount_msat === "object" ? message.incoming_amount_msat.low || message.incoming_amount_msat.high : message.incoming_amount_msat !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.incoming_amount_msat);
            if (message.incoming_expiry != null && $Object.hasOwnProperty.call(message, "incoming_expiry") && message.incoming_expiry !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.incoming_expiry);
            if (message.outgoing_requested_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_requested_chan_id") && (typeof message.outgoing_requested_chan_id === "object" ? message.outgoing_requested_chan_id.low || message.outgoing_requested_chan_id.high : message.outgoing_requested_chan_id !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.outgoing_requested_chan_id);
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records"))
                for (let keys = $Object.keys(message.custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.custom_records[keys[i]]).ldelim();
            if (message.onion_blob != null && $Object.hasOwnProperty.call(message, "onion_blob") && message.onion_blob.length)
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.onion_blob);
            if (message.auto_fail_height != null && $Object.hasOwnProperty.call(message, "auto_fail_height") && message.auto_fail_height !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.auto_fail_height);
            if (message.in_wire_custom_records != null && $Object.hasOwnProperty.call(message, "in_wire_custom_records"))
                for (let keys = $Object.keys(message.in_wire_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 11, wireType 2 =*/90).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.in_wire_custom_records[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ForwardHtlcInterceptRequest message, length delimited. Does not implicitly {@link routerrpc.ForwardHtlcInterceptRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @static
         * @param {routerrpc.ForwardHtlcInterceptRequest.$Properties} message ForwardHtlcInterceptRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardHtlcInterceptRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ForwardHtlcInterceptRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.ForwardHtlcInterceptRequest & routerrpc.ForwardHtlcInterceptRequest.$Shape} ForwardHtlcInterceptRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardHtlcInterceptRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.ForwardHtlcInterceptRequest(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.incoming_circuit_key = $root.routerrpc.CircuitKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.incoming_circuit_key);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.incoming_amount_msat = value;
                        else
                            delete message.incoming_amount_msat;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.incoming_expiry = value;
                        else
                            delete message.incoming_expiry;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payment_hash = value;
                        else
                            delete message.payment_hash;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.outgoing_requested_chan_id = value;
                        else
                            delete message.outgoing_requested_chan_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.outgoing_amount_msat = value;
                        else
                            delete message.outgoing_amount_msat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.outgoing_expiry = value;
                        else
                            delete message.outgoing_expiry;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (message.custom_records === $util.emptyObject)
                            message.custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.onion_blob = value;
                        else
                            delete message.onion_blob;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.auto_fail_height = value;
                        else
                            delete message.auto_fail_height;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (message.in_wire_custom_records === $util.emptyObject)
                            message.in_wire_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.in_wire_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ForwardHtlcInterceptRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.ForwardHtlcInterceptRequest & routerrpc.ForwardHtlcInterceptRequest.$Shape} ForwardHtlcInterceptRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardHtlcInterceptRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForwardHtlcInterceptRequest message.
         * @function verify
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForwardHtlcInterceptRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.incoming_circuit_key != null && $Object.hasOwnProperty.call(message, "incoming_circuit_key")) {
                let error = $root.routerrpc.CircuitKey.verify(message.incoming_circuit_key, _depth + 1);
                if (error)
                    return "incoming_circuit_key." + error;
            }
            if (message.incoming_amount_msat != null && $Object.hasOwnProperty.call(message, "incoming_amount_msat"))
                if (!$util.isInteger(message.incoming_amount_msat) && !(message.incoming_amount_msat && $util.isInteger(message.incoming_amount_msat.low) && $util.isInteger(message.incoming_amount_msat.high)))
                    return "incoming_amount_msat: integer|Long expected";
            if (message.incoming_expiry != null && $Object.hasOwnProperty.call(message, "incoming_expiry"))
                if (!$util.isInteger(message.incoming_expiry))
                    return "incoming_expiry: integer expected";
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                if (!(message.payment_hash && typeof message.payment_hash.length === "number" || $util.isString(message.payment_hash)))
                    return "payment_hash: buffer expected";
            if (message.outgoing_requested_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_requested_chan_id"))
                if (!$util.isInteger(message.outgoing_requested_chan_id) && !(message.outgoing_requested_chan_id && $util.isInteger(message.outgoing_requested_chan_id.low) && $util.isInteger(message.outgoing_requested_chan_id.high)))
                    return "outgoing_requested_chan_id: integer|Long expected";
            if (message.outgoing_amount_msat != null && $Object.hasOwnProperty.call(message, "outgoing_amount_msat"))
                if (!$util.isInteger(message.outgoing_amount_msat) && !(message.outgoing_amount_msat && $util.isInteger(message.outgoing_amount_msat.low) && $util.isInteger(message.outgoing_amount_msat.high)))
                    return "outgoing_amount_msat: integer|Long expected";
            if (message.outgoing_expiry != null && $Object.hasOwnProperty.call(message, "outgoing_expiry"))
                if (!$util.isInteger(message.outgoing_expiry))
                    return "outgoing_expiry: integer expected";
            if (message.custom_records != null && $Object.hasOwnProperty.call(message, "custom_records")) {
                if (!$util.isObject(message.custom_records))
                    return "custom_records: object expected";
                let key = $Object.keys(message.custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.custom_records[key[i]] && typeof message.custom_records[key[i]].length === "number" || $util.isString(message.custom_records[key[i]])))
                        return "custom_records: buffer{k:uint64} expected";
                }
            }
            if (message.onion_blob != null && $Object.hasOwnProperty.call(message, "onion_blob"))
                if (!(message.onion_blob && typeof message.onion_blob.length === "number" || $util.isString(message.onion_blob)))
                    return "onion_blob: buffer expected";
            if (message.auto_fail_height != null && $Object.hasOwnProperty.call(message, "auto_fail_height"))
                if (!$util.isInteger(message.auto_fail_height))
                    return "auto_fail_height: integer expected";
            if (message.in_wire_custom_records != null && $Object.hasOwnProperty.call(message, "in_wire_custom_records")) {
                if (!$util.isObject(message.in_wire_custom_records))
                    return "in_wire_custom_records: object expected";
                let key = $Object.keys(message.in_wire_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "in_wire_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.in_wire_custom_records[key[i]] && typeof message.in_wire_custom_records[key[i]].length === "number" || $util.isString(message.in_wire_custom_records[key[i]])))
                        return "in_wire_custom_records: buffer{k:uint64} expected";
                }
            }
            return null;
        };

        /**
         * Creates a ForwardHtlcInterceptRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.ForwardHtlcInterceptRequest} ForwardHtlcInterceptRequest
         */
        ForwardHtlcInterceptRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.ForwardHtlcInterceptRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.ForwardHtlcInterceptRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.ForwardHtlcInterceptRequest();
            if (object.incoming_circuit_key != null) {
                if (!$util.isObject(object.incoming_circuit_key))
                    throw $TypeError(".routerrpc.ForwardHtlcInterceptRequest.incoming_circuit_key: object expected");
                message.incoming_circuit_key = $root.routerrpc.CircuitKey.fromObject(object.incoming_circuit_key, _depth + 1);
            }
            if (object.incoming_amount_msat != null)
                if (typeof object.incoming_amount_msat === "object" ? object.incoming_amount_msat.low || object.incoming_amount_msat.high : $Number(object.incoming_amount_msat) !== 0)
                    if ($util.Long)
                        message.incoming_amount_msat = $util.Long.fromValue(object.incoming_amount_msat, true);
                    else if (typeof object.incoming_amount_msat === "string")
                        message.incoming_amount_msat = $parseInt(object.incoming_amount_msat, 10);
                    else if (typeof object.incoming_amount_msat === "number")
                        message.incoming_amount_msat = object.incoming_amount_msat;
                    else if (typeof object.incoming_amount_msat === "object")
                        message.incoming_amount_msat = new $util.LongBits(object.incoming_amount_msat.low >>> 0, object.incoming_amount_msat.high >>> 0).toNumber(true);
            if (object.incoming_expiry != null)
                if ($Number(object.incoming_expiry) !== 0)
                    message.incoming_expiry = object.incoming_expiry >>> 0;
            if (object.payment_hash != null)
                if (object.payment_hash.length)
                    if (typeof object.payment_hash === "string")
                        $util.base64.decode(object.payment_hash, message.payment_hash = $util.newBuffer($util.base64.length(object.payment_hash)), 0);
                    else if (object.payment_hash.length >= 0)
                        message.payment_hash = object.payment_hash;
            if (object.outgoing_requested_chan_id != null)
                if (typeof object.outgoing_requested_chan_id === "object" ? object.outgoing_requested_chan_id.low || object.outgoing_requested_chan_id.high : $Number(object.outgoing_requested_chan_id) !== 0)
                    if ($util.Long)
                        message.outgoing_requested_chan_id = $util.Long.fromValue(object.outgoing_requested_chan_id, true);
                    else if (typeof object.outgoing_requested_chan_id === "string")
                        message.outgoing_requested_chan_id = $parseInt(object.outgoing_requested_chan_id, 10);
                    else if (typeof object.outgoing_requested_chan_id === "number")
                        message.outgoing_requested_chan_id = object.outgoing_requested_chan_id;
                    else if (typeof object.outgoing_requested_chan_id === "object")
                        message.outgoing_requested_chan_id = new $util.LongBits(object.outgoing_requested_chan_id.low >>> 0, object.outgoing_requested_chan_id.high >>> 0).toNumber(true);
            if (object.outgoing_amount_msat != null)
                if (typeof object.outgoing_amount_msat === "object" ? object.outgoing_amount_msat.low || object.outgoing_amount_msat.high : $Number(object.outgoing_amount_msat) !== 0)
                    if ($util.Long)
                        message.outgoing_amount_msat = $util.Long.fromValue(object.outgoing_amount_msat, true);
                    else if (typeof object.outgoing_amount_msat === "string")
                        message.outgoing_amount_msat = $parseInt(object.outgoing_amount_msat, 10);
                    else if (typeof object.outgoing_amount_msat === "number")
                        message.outgoing_amount_msat = object.outgoing_amount_msat;
                    else if (typeof object.outgoing_amount_msat === "object")
                        message.outgoing_amount_msat = new $util.LongBits(object.outgoing_amount_msat.low >>> 0, object.outgoing_amount_msat.high >>> 0).toNumber(true);
            if (object.outgoing_expiry != null)
                if ($Number(object.outgoing_expiry) !== 0)
                    message.outgoing_expiry = object.outgoing_expiry >>> 0;
            if (object.custom_records) {
                if (!$util.isObject(object.custom_records))
                    throw $TypeError(".routerrpc.ForwardHtlcInterceptRequest.custom_records: object expected");
                message.custom_records = {};
                for (let keys = $Object.keys(object.custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.custom_records, keys[i]);
                    if (typeof object.custom_records[keys[i]] === "string")
                        $util.base64.decode(object.custom_records[keys[i]], message.custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.custom_records[keys[i]])), 0);
                    else if (object.custom_records[keys[i]].length >= 0)
                        message.custom_records[keys[i]] = object.custom_records[keys[i]];
                }
            }
            if (object.onion_blob != null)
                if (object.onion_blob.length)
                    if (typeof object.onion_blob === "string")
                        $util.base64.decode(object.onion_blob, message.onion_blob = $util.newBuffer($util.base64.length(object.onion_blob)), 0);
                    else if (object.onion_blob.length >= 0)
                        message.onion_blob = object.onion_blob;
            if (object.auto_fail_height != null)
                if ($Number(object.auto_fail_height) !== 0)
                    message.auto_fail_height = object.auto_fail_height | 0;
            if (object.in_wire_custom_records) {
                if (!$util.isObject(object.in_wire_custom_records))
                    throw $TypeError(".routerrpc.ForwardHtlcInterceptRequest.in_wire_custom_records: object expected");
                message.in_wire_custom_records = {};
                for (let keys = $Object.keys(object.in_wire_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.in_wire_custom_records, keys[i]);
                    if (typeof object.in_wire_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.in_wire_custom_records[keys[i]], message.in_wire_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.in_wire_custom_records[keys[i]])), 0);
                    else if (object.in_wire_custom_records[keys[i]].length >= 0)
                        message.in_wire_custom_records[keys[i]] = object.in_wire_custom_records[keys[i]];
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ForwardHtlcInterceptRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @static
         * @param {routerrpc.ForwardHtlcInterceptRequest} message ForwardHtlcInterceptRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForwardHtlcInterceptRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults) {
                object.custom_records = {};
                object.in_wire_custom_records = {};
            }
            if (options.defaults) {
                object.incoming_circuit_key = null;
                if (options.bytes === $String)
                    object.payment_hash = "";
                else {
                    object.payment_hash = [];
                    if (options.bytes !== $Array)
                        object.payment_hash = $util.newBuffer(object.payment_hash);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.outgoing_amount_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.outgoing_amount_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.outgoing_expiry = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.incoming_amount_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.incoming_amount_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.incoming_expiry = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.outgoing_requested_chan_id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.outgoing_requested_chan_id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.onion_blob = "";
                else {
                    object.onion_blob = [];
                    if (options.bytes !== $Array)
                        object.onion_blob = $util.newBuffer(object.onion_blob);
                }
                object.auto_fail_height = 0;
            }
            if (message.incoming_circuit_key != null && $Object.hasOwnProperty.call(message, "incoming_circuit_key"))
                object.incoming_circuit_key = $root.routerrpc.CircuitKey.toObject(message.incoming_circuit_key, options, _depth + 1);
            if (message.payment_hash != null && $Object.hasOwnProperty.call(message, "payment_hash"))
                object.payment_hash = options.bytes === $String ? $util.base64.encode(message.payment_hash, 0, message.payment_hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payment_hash) : message.payment_hash;
            if (message.outgoing_amount_msat != null && $Object.hasOwnProperty.call(message, "outgoing_amount_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_amount_msat = typeof message.outgoing_amount_msat === "number" ? $BigInt(message.outgoing_amount_msat) : $util.Long.fromBits(message.outgoing_amount_msat.low >>> 0, message.outgoing_amount_msat.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_amount_msat === "number")
                    object.outgoing_amount_msat = options.longs === $String ? $String(message.outgoing_amount_msat) : message.outgoing_amount_msat;
                else
                    object.outgoing_amount_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_amount_msat) : options.longs === $Number ? new $util.LongBits(message.outgoing_amount_msat.low >>> 0, message.outgoing_amount_msat.high >>> 0).toNumber(true) : message.outgoing_amount_msat;
            if (message.outgoing_expiry != null && $Object.hasOwnProperty.call(message, "outgoing_expiry"))
                object.outgoing_expiry = message.outgoing_expiry;
            if (message.incoming_amount_msat != null && $Object.hasOwnProperty.call(message, "incoming_amount_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.incoming_amount_msat = typeof message.incoming_amount_msat === "number" ? $BigInt(message.incoming_amount_msat) : $util.Long.fromBits(message.incoming_amount_msat.low >>> 0, message.incoming_amount_msat.high >>> 0, true).toBigInt();
                else if (typeof message.incoming_amount_msat === "number")
                    object.incoming_amount_msat = options.longs === $String ? $String(message.incoming_amount_msat) : message.incoming_amount_msat;
                else
                    object.incoming_amount_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.incoming_amount_msat) : options.longs === $Number ? new $util.LongBits(message.incoming_amount_msat.low >>> 0, message.incoming_amount_msat.high >>> 0).toNumber(true) : message.incoming_amount_msat;
            if (message.incoming_expiry != null && $Object.hasOwnProperty.call(message, "incoming_expiry"))
                object.incoming_expiry = message.incoming_expiry;
            if (message.outgoing_requested_chan_id != null && $Object.hasOwnProperty.call(message, "outgoing_requested_chan_id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.outgoing_requested_chan_id = typeof message.outgoing_requested_chan_id === "number" ? $BigInt(message.outgoing_requested_chan_id) : $util.Long.fromBits(message.outgoing_requested_chan_id.low >>> 0, message.outgoing_requested_chan_id.high >>> 0, true).toBigInt();
                else if (typeof message.outgoing_requested_chan_id === "number")
                    object.outgoing_requested_chan_id = options.longs === $String ? $String(message.outgoing_requested_chan_id) : message.outgoing_requested_chan_id;
                else
                    object.outgoing_requested_chan_id = options.longs === $String ? $util.Long.prototype.toString.call(message.outgoing_requested_chan_id) : options.longs === $Number ? new $util.LongBits(message.outgoing_requested_chan_id.low >>> 0, message.outgoing_requested_chan_id.high >>> 0).toNumber(true) : message.outgoing_requested_chan_id;
            let keys2;
            if (message.custom_records && (keys2 = $Object.keys(message.custom_records)).length) {
                object.custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.custom_records, keys2[j]);
                    object.custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.custom_records[keys2[j]], 0, message.custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_records[keys2[j]]) : message.custom_records[keys2[j]];
                }
            }
            if (message.onion_blob != null && $Object.hasOwnProperty.call(message, "onion_blob"))
                object.onion_blob = options.bytes === $String ? $util.base64.encode(message.onion_blob, 0, message.onion_blob.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.onion_blob) : message.onion_blob;
            if (message.auto_fail_height != null && $Object.hasOwnProperty.call(message, "auto_fail_height"))
                object.auto_fail_height = message.auto_fail_height;
            if (message.in_wire_custom_records && (keys2 = $Object.keys(message.in_wire_custom_records)).length) {
                object.in_wire_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.in_wire_custom_records, keys2[j]);
                    object.in_wire_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.in_wire_custom_records[keys2[j]], 0, message.in_wire_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.in_wire_custom_records[keys2[j]]) : message.in_wire_custom_records[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this ForwardHtlcInterceptRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForwardHtlcInterceptRequest.prototype.toJSON = function() {
            return ForwardHtlcInterceptRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ForwardHtlcInterceptRequest
         * @function getTypeUrl
         * @memberof routerrpc.ForwardHtlcInterceptRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ForwardHtlcInterceptRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.ForwardHtlcInterceptRequest";
        };

        return ForwardHtlcInterceptRequest;
    })();

    routerrpc.ForwardHtlcInterceptResponse = (function() {

        /**
         * Properties of a ForwardHtlcInterceptResponse.
         * @typedef {Object} routerrpc.ForwardHtlcInterceptResponse.$Properties
         * @property {routerrpc.CircuitKey.$Properties|null} [incoming_circuit_key] The key of this forwarded htlc. It defines the incoming channel id and
         * the index in this channel.
         * @property {routerrpc.ResolveHoldForwardAction|null} [action] ForwardHtlcInterceptResponse action
         * @property {Uint8Array|null} [preimage] ForwardHtlcInterceptResponse preimage
         * @property {Uint8Array|null} [failure_message] ForwardHtlcInterceptResponse failure_message
         * @property {lnrpc.Failure.FailureCode|null} [failure_code] ForwardHtlcInterceptResponse failure_code
         * @property {Long|null} [in_amount_msat] ForwardHtlcInterceptResponse in_amount_msat
         * @property {Long|null} [out_amount_msat] ForwardHtlcInterceptResponse out_amount_msat
         * @property {Object.<string,Uint8Array>|null} [out_wire_custom_records] ForwardHtlcInterceptResponse out_wire_custom_records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ForwardHtlcInterceptResponse.
         * @memberof routerrpc
         * @interface IForwardHtlcInterceptResponse
         * @augments routerrpc.ForwardHtlcInterceptResponse.$Properties
         * @deprecated Use routerrpc.ForwardHtlcInterceptResponse.$Properties instead.
         */

        /**
         * Shape of a ForwardHtlcInterceptResponse.
         * @typedef {routerrpc.ForwardHtlcInterceptResponse.$Properties} routerrpc.ForwardHtlcInterceptResponse.$Shape
         */

        /**
         * Constructs a new ForwardHtlcInterceptResponse.
         * @memberof routerrpc
         * @classdesc ForwardHtlcInterceptResponse enables the caller to resolve a previously hold
         * forward. The caller can choose either to:
         * - `Resume`: Execute the default behavior (usually forward).
         * - `ResumeModified`: Execute the default behavior (usually forward) with HTLC
         * field modifications.
         * - `Reject`: Fail the htlc backwards.
         * - `Settle`: Settle this htlc with a given preimage.
         * @constructor
         * @param {routerrpc.ForwardHtlcInterceptResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ForwardHtlcInterceptResponse = function (properties) {
            this.out_wire_custom_records = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The key of this forwarded htlc. It defines the incoming channel id and
         * the index in this channel.
         * @member {routerrpc.CircuitKey.$Properties|null|undefined} incoming_circuit_key
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @instance
         */
        ForwardHtlcInterceptResponse.prototype.incoming_circuit_key = null;

        /**
         * ForwardHtlcInterceptResponse action.
         * @member {routerrpc.ResolveHoldForwardAction} action
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @instance
         */
        ForwardHtlcInterceptResponse.prototype.action = 0;

        /**
         * ForwardHtlcInterceptResponse preimage.
         * @member {Uint8Array} preimage
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @instance
         */
        ForwardHtlcInterceptResponse.prototype.preimage = $util.newBuffer([]);

        /**
         * ForwardHtlcInterceptResponse failure_message.
         * @member {Uint8Array} failure_message
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @instance
         */
        ForwardHtlcInterceptResponse.prototype.failure_message = $util.newBuffer([]);

        /**
         * ForwardHtlcInterceptResponse failure_code.
         * @member {lnrpc.Failure.FailureCode} failure_code
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @instance
         */
        ForwardHtlcInterceptResponse.prototype.failure_code = 0;

        /**
         * ForwardHtlcInterceptResponse in_amount_msat.
         * @member {Long} in_amount_msat
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @instance
         */
        ForwardHtlcInterceptResponse.prototype.in_amount_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardHtlcInterceptResponse out_amount_msat.
         * @member {Long} out_amount_msat
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @instance
         */
        ForwardHtlcInterceptResponse.prototype.out_amount_msat = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ForwardHtlcInterceptResponse out_wire_custom_records.
         * @member {Object.<string,Uint8Array>} out_wire_custom_records
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @instance
         */
        ForwardHtlcInterceptResponse.prototype.out_wire_custom_records = $util.emptyObject;

        /**
         * Creates a new ForwardHtlcInterceptResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @static
         * @param {routerrpc.ForwardHtlcInterceptResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.ForwardHtlcInterceptResponse} ForwardHtlcInterceptResponse instance
         * @type {{
         *   (properties: routerrpc.ForwardHtlcInterceptResponse.$Shape): routerrpc.ForwardHtlcInterceptResponse & routerrpc.ForwardHtlcInterceptResponse.$Shape;
         *   (properties?: routerrpc.ForwardHtlcInterceptResponse.$Properties): routerrpc.ForwardHtlcInterceptResponse;
         * }}
         */
        ForwardHtlcInterceptResponse.create = function(properties) {
            return new ForwardHtlcInterceptResponse(properties);
        };

        /**
         * Encodes the specified ForwardHtlcInterceptResponse message. Does not implicitly {@link routerrpc.ForwardHtlcInterceptResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @static
         * @param {routerrpc.ForwardHtlcInterceptResponse.$Properties} message ForwardHtlcInterceptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardHtlcInterceptResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.incoming_circuit_key != null && $Object.hasOwnProperty.call(message, "incoming_circuit_key"))
                $root.routerrpc.CircuitKey.encode(message.incoming_circuit_key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.action != null && $Object.hasOwnProperty.call(message, "action") && message.action !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.action);
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage") && message.preimage.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.preimage);
            if (message.failure_message != null && $Object.hasOwnProperty.call(message, "failure_message") && message.failure_message.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.failure_message);
            if (message.failure_code != null && $Object.hasOwnProperty.call(message, "failure_code") && message.failure_code !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.failure_code);
            if (message.in_amount_msat != null && $Object.hasOwnProperty.call(message, "in_amount_msat") && (typeof message.in_amount_msat === "object" ? message.in_amount_msat.low || message.in_amount_msat.high : message.in_amount_msat !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.in_amount_msat);
            if (message.out_amount_msat != null && $Object.hasOwnProperty.call(message, "out_amount_msat") && (typeof message.out_amount_msat === "object" ? message.out_amount_msat.low || message.out_amount_msat.high : message.out_amount_msat !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.out_amount_msat);
            if (message.out_wire_custom_records != null && $Object.hasOwnProperty.call(message, "out_wire_custom_records"))
                for (let keys = $Object.keys(message.out_wire_custom_records), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).fork().uint32(/* id 1, wireType 0 =*/8).uint64($util.longFromKey(keys[i], true)).uint32(/* id 2, wireType 2 =*/18).bytes(message.out_wire_custom_records[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ForwardHtlcInterceptResponse message, length delimited. Does not implicitly {@link routerrpc.ForwardHtlcInterceptResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @static
         * @param {routerrpc.ForwardHtlcInterceptResponse.$Properties} message ForwardHtlcInterceptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardHtlcInterceptResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ForwardHtlcInterceptResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.ForwardHtlcInterceptResponse & routerrpc.ForwardHtlcInterceptResponse.$Shape} ForwardHtlcInterceptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardHtlcInterceptResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.ForwardHtlcInterceptResponse(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.incoming_circuit_key = $root.routerrpc.CircuitKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.incoming_circuit_key);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.action = value;
                        else
                            delete message.action;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.preimage = value;
                        else
                            delete message.preimage;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.failure_message = value;
                        else
                            delete message.failure_message;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.failure_code = value;
                        else
                            delete message.failure_code;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.in_amount_msat = value;
                        else
                            delete message.in_amount_msat;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.out_amount_msat = value;
                        else
                            delete message.out_amount_msat;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (message.out_wire_custom_records === $util.emptyObject)
                            message.out_wire_custom_records = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 0)
                                    break;
                                key = reader.uint64();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.bytes();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        message.out_wire_custom_records[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ForwardHtlcInterceptResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.ForwardHtlcInterceptResponse & routerrpc.ForwardHtlcInterceptResponse.$Shape} ForwardHtlcInterceptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardHtlcInterceptResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForwardHtlcInterceptResponse message.
         * @function verify
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForwardHtlcInterceptResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.incoming_circuit_key != null && $Object.hasOwnProperty.call(message, "incoming_circuit_key")) {
                let error = $root.routerrpc.CircuitKey.verify(message.incoming_circuit_key, _depth + 1);
                if (error)
                    return "incoming_circuit_key." + error;
            }
            if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
                if (typeof message.action !== "number" || (message.action | 0) !== message.action)
                    return "action: enum value expected";
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                if (!(message.preimage && typeof message.preimage.length === "number" || $util.isString(message.preimage)))
                    return "preimage: buffer expected";
            if (message.failure_message != null && $Object.hasOwnProperty.call(message, "failure_message"))
                if (!(message.failure_message && typeof message.failure_message.length === "number" || $util.isString(message.failure_message)))
                    return "failure_message: buffer expected";
            if (message.failure_code != null && $Object.hasOwnProperty.call(message, "failure_code"))
                if (typeof message.failure_code !== "number" || (message.failure_code | 0) !== message.failure_code)
                    return "failure_code: enum value expected";
            if (message.in_amount_msat != null && $Object.hasOwnProperty.call(message, "in_amount_msat"))
                if (!$util.isInteger(message.in_amount_msat) && !(message.in_amount_msat && $util.isInteger(message.in_amount_msat.low) && $util.isInteger(message.in_amount_msat.high)))
                    return "in_amount_msat: integer|Long expected";
            if (message.out_amount_msat != null && $Object.hasOwnProperty.call(message, "out_amount_msat"))
                if (!$util.isInteger(message.out_amount_msat) && !(message.out_amount_msat && $util.isInteger(message.out_amount_msat.low) && $util.isInteger(message.out_amount_msat.high)))
                    return "out_amount_msat: integer|Long expected";
            if (message.out_wire_custom_records != null && $Object.hasOwnProperty.call(message, "out_wire_custom_records")) {
                if (!$util.isObject(message.out_wire_custom_records))
                    return "out_wire_custom_records: object expected";
                let key = $Object.keys(message.out_wire_custom_records);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "out_wire_custom_records: integer|Long key{k:uint64} expected";
                    if (!(message.out_wire_custom_records[key[i]] && typeof message.out_wire_custom_records[key[i]].length === "number" || $util.isString(message.out_wire_custom_records[key[i]])))
                        return "out_wire_custom_records: buffer{k:uint64} expected";
                }
            }
            return null;
        };

        /**
         * Creates a ForwardHtlcInterceptResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.ForwardHtlcInterceptResponse} ForwardHtlcInterceptResponse
         */
        ForwardHtlcInterceptResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.ForwardHtlcInterceptResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.ForwardHtlcInterceptResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.ForwardHtlcInterceptResponse();
            if (object.incoming_circuit_key != null) {
                if (!$util.isObject(object.incoming_circuit_key))
                    throw $TypeError(".routerrpc.ForwardHtlcInterceptResponse.incoming_circuit_key: object expected");
                message.incoming_circuit_key = $root.routerrpc.CircuitKey.fromObject(object.incoming_circuit_key, _depth + 1);
            }
            if (object.action !== 0 && (typeof object.action !== "string" || $root.routerrpc.ResolveHoldForwardAction[object.action] !== 0))
                switch (object.action) {
                case "SETTLE":
                case 0:
                    message.action = 0;
                    break;
                case "FAIL":
                case 1:
                    message.action = 1;
                    break;
                case "RESUME":
                case 2:
                    message.action = 2;
                    break;
                case "RESUME_MODIFIED":
                case 3:
                    message.action = 3;
                    break;
                default:
                    if (typeof object.action === "number" && (object.action | 0) === object.action)
                        message.action = object.action;
                }
            if (object.preimage != null)
                if (object.preimage.length)
                    if (typeof object.preimage === "string")
                        $util.base64.decode(object.preimage, message.preimage = $util.newBuffer($util.base64.length(object.preimage)), 0);
                    else if (object.preimage.length >= 0)
                        message.preimage = object.preimage;
            if (object.failure_message != null)
                if (object.failure_message.length)
                    if (typeof object.failure_message === "string")
                        $util.base64.decode(object.failure_message, message.failure_message = $util.newBuffer($util.base64.length(object.failure_message)), 0);
                    else if (object.failure_message.length >= 0)
                        message.failure_message = object.failure_message;
            if (object.failure_code !== 0 && (typeof object.failure_code !== "string" || $root.lnrpc.Failure.FailureCode[object.failure_code] !== 0))
                switch (object.failure_code) {
                case "RESERVED":
                case 0:
                    message.failure_code = 0;
                    break;
                case "INCORRECT_OR_UNKNOWN_PAYMENT_DETAILS":
                case 1:
                    message.failure_code = 1;
                    break;
                case "INCORRECT_PAYMENT_AMOUNT":
                case 2:
                    message.failure_code = 2;
                    break;
                case "FINAL_INCORRECT_CLTV_EXPIRY":
                case 3:
                    message.failure_code = 3;
                    break;
                case "FINAL_INCORRECT_HTLC_AMOUNT":
                case 4:
                    message.failure_code = 4;
                    break;
                case "FINAL_EXPIRY_TOO_SOON":
                case 5:
                    message.failure_code = 5;
                    break;
                case "INVALID_REALM":
                case 6:
                    message.failure_code = 6;
                    break;
                case "EXPIRY_TOO_SOON":
                case 7:
                    message.failure_code = 7;
                    break;
                case "INVALID_ONION_VERSION":
                case 8:
                    message.failure_code = 8;
                    break;
                case "INVALID_ONION_HMAC":
                case 9:
                    message.failure_code = 9;
                    break;
                case "INVALID_ONION_KEY":
                case 10:
                    message.failure_code = 10;
                    break;
                case "AMOUNT_BELOW_MINIMUM":
                case 11:
                    message.failure_code = 11;
                    break;
                case "FEE_INSUFFICIENT":
                case 12:
                    message.failure_code = 12;
                    break;
                case "INCORRECT_CLTV_EXPIRY":
                case 13:
                    message.failure_code = 13;
                    break;
                case "CHANNEL_DISABLED":
                case 14:
                    message.failure_code = 14;
                    break;
                case "TEMPORARY_CHANNEL_FAILURE":
                case 15:
                    message.failure_code = 15;
                    break;
                case "REQUIRED_NODE_FEATURE_MISSING":
                case 16:
                    message.failure_code = 16;
                    break;
                case "REQUIRED_CHANNEL_FEATURE_MISSING":
                case 17:
                    message.failure_code = 17;
                    break;
                case "UNKNOWN_NEXT_PEER":
                case 18:
                    message.failure_code = 18;
                    break;
                case "TEMPORARY_NODE_FAILURE":
                case 19:
                    message.failure_code = 19;
                    break;
                case "PERMANENT_NODE_FAILURE":
                case 20:
                    message.failure_code = 20;
                    break;
                case "PERMANENT_CHANNEL_FAILURE":
                case 21:
                    message.failure_code = 21;
                    break;
                case "EXPIRY_TOO_FAR":
                case 22:
                    message.failure_code = 22;
                    break;
                case "MPP_TIMEOUT":
                case 23:
                    message.failure_code = 23;
                    break;
                case "INVALID_ONION_PAYLOAD":
                case 24:
                    message.failure_code = 24;
                    break;
                case "INVALID_ONION_BLINDING":
                case 25:
                    message.failure_code = 25;
                    break;
                case "INTERNAL_FAILURE":
                case 997:
                    message.failure_code = 997;
                    break;
                case "UNKNOWN_FAILURE":
                case 998:
                    message.failure_code = 998;
                    break;
                case "UNREADABLE_FAILURE":
                case 999:
                    message.failure_code = 999;
                    break;
                default:
                    if (typeof object.failure_code === "number" && (object.failure_code | 0) === object.failure_code)
                        message.failure_code = object.failure_code;
                }
            if (object.in_amount_msat != null)
                if (typeof object.in_amount_msat === "object" ? object.in_amount_msat.low || object.in_amount_msat.high : $Number(object.in_amount_msat) !== 0)
                    if ($util.Long)
                        message.in_amount_msat = $util.Long.fromValue(object.in_amount_msat, true);
                    else if (typeof object.in_amount_msat === "string")
                        message.in_amount_msat = $parseInt(object.in_amount_msat, 10);
                    else if (typeof object.in_amount_msat === "number")
                        message.in_amount_msat = object.in_amount_msat;
                    else if (typeof object.in_amount_msat === "object")
                        message.in_amount_msat = new $util.LongBits(object.in_amount_msat.low >>> 0, object.in_amount_msat.high >>> 0).toNumber(true);
            if (object.out_amount_msat != null)
                if (typeof object.out_amount_msat === "object" ? object.out_amount_msat.low || object.out_amount_msat.high : $Number(object.out_amount_msat) !== 0)
                    if ($util.Long)
                        message.out_amount_msat = $util.Long.fromValue(object.out_amount_msat, true);
                    else if (typeof object.out_amount_msat === "string")
                        message.out_amount_msat = $parseInt(object.out_amount_msat, 10);
                    else if (typeof object.out_amount_msat === "number")
                        message.out_amount_msat = object.out_amount_msat;
                    else if (typeof object.out_amount_msat === "object")
                        message.out_amount_msat = new $util.LongBits(object.out_amount_msat.low >>> 0, object.out_amount_msat.high >>> 0).toNumber(true);
            if (object.out_wire_custom_records) {
                if (!$util.isObject(object.out_wire_custom_records))
                    throw $TypeError(".routerrpc.ForwardHtlcInterceptResponse.out_wire_custom_records: object expected");
                message.out_wire_custom_records = {};
                for (let keys = $Object.keys(object.out_wire_custom_records), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.out_wire_custom_records, keys[i]);
                    if (typeof object.out_wire_custom_records[keys[i]] === "string")
                        $util.base64.decode(object.out_wire_custom_records[keys[i]], message.out_wire_custom_records[keys[i]] = $util.newBuffer($util.base64.length(object.out_wire_custom_records[keys[i]])), 0);
                    else if (object.out_wire_custom_records[keys[i]].length >= 0)
                        message.out_wire_custom_records[keys[i]] = object.out_wire_custom_records[keys[i]];
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ForwardHtlcInterceptResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @static
         * @param {routerrpc.ForwardHtlcInterceptResponse} message ForwardHtlcInterceptResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForwardHtlcInterceptResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.out_wire_custom_records = {};
            if (options.defaults) {
                object.incoming_circuit_key = null;
                object.action = options.enums === $String ? "SETTLE" : 0;
                if (options.bytes === $String)
                    object.preimage = "";
                else {
                    object.preimage = [];
                    if (options.bytes !== $Array)
                        object.preimage = $util.newBuffer(object.preimage);
                }
                if (options.bytes === $String)
                    object.failure_message = "";
                else {
                    object.failure_message = [];
                    if (options.bytes !== $Array)
                        object.failure_message = $util.newBuffer(object.failure_message);
                }
                object.failure_code = options.enums === $String ? "RESERVED" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.in_amount_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.in_amount_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.out_amount_msat = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.out_amount_msat = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.incoming_circuit_key != null && $Object.hasOwnProperty.call(message, "incoming_circuit_key"))
                object.incoming_circuit_key = $root.routerrpc.CircuitKey.toObject(message.incoming_circuit_key, options, _depth + 1);
            if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
                object.action = options.enums === $String ? $root.routerrpc.ResolveHoldForwardAction[message.action] === $undefined ? message.action : $root.routerrpc.ResolveHoldForwardAction[message.action] : message.action;
            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                object.preimage = options.bytes === $String ? $util.base64.encode(message.preimage, 0, message.preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.preimage) : message.preimage;
            if (message.failure_message != null && $Object.hasOwnProperty.call(message, "failure_message"))
                object.failure_message = options.bytes === $String ? $util.base64.encode(message.failure_message, 0, message.failure_message.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.failure_message) : message.failure_message;
            if (message.failure_code != null && $Object.hasOwnProperty.call(message, "failure_code"))
                object.failure_code = options.enums === $String ? $root.lnrpc.Failure.FailureCode[message.failure_code] === $undefined ? message.failure_code : $root.lnrpc.Failure.FailureCode[message.failure_code] : message.failure_code;
            if (message.in_amount_msat != null && $Object.hasOwnProperty.call(message, "in_amount_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.in_amount_msat = typeof message.in_amount_msat === "number" ? $BigInt(message.in_amount_msat) : $util.Long.fromBits(message.in_amount_msat.low >>> 0, message.in_amount_msat.high >>> 0, true).toBigInt();
                else if (typeof message.in_amount_msat === "number")
                    object.in_amount_msat = options.longs === $String ? $String(message.in_amount_msat) : message.in_amount_msat;
                else
                    object.in_amount_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.in_amount_msat) : options.longs === $Number ? new $util.LongBits(message.in_amount_msat.low >>> 0, message.in_amount_msat.high >>> 0).toNumber(true) : message.in_amount_msat;
            if (message.out_amount_msat != null && $Object.hasOwnProperty.call(message, "out_amount_msat"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.out_amount_msat = typeof message.out_amount_msat === "number" ? $BigInt(message.out_amount_msat) : $util.Long.fromBits(message.out_amount_msat.low >>> 0, message.out_amount_msat.high >>> 0, true).toBigInt();
                else if (typeof message.out_amount_msat === "number")
                    object.out_amount_msat = options.longs === $String ? $String(message.out_amount_msat) : message.out_amount_msat;
                else
                    object.out_amount_msat = options.longs === $String ? $util.Long.prototype.toString.call(message.out_amount_msat) : options.longs === $Number ? new $util.LongBits(message.out_amount_msat.low >>> 0, message.out_amount_msat.high >>> 0).toNumber(true) : message.out_amount_msat;
            let keys2;
            if (message.out_wire_custom_records && (keys2 = $Object.keys(message.out_wire_custom_records)).length) {
                object.out_wire_custom_records = {};
                for (let j = 0; j < keys2.length; ++j) {
                    let k2 = $util.longFromKey(keys2[j], true).toString();
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.out_wire_custom_records, keys2[j]);
                    object.out_wire_custom_records[k2] = options.bytes === $String ? $util.base64.encode(message.out_wire_custom_records[keys2[j]], 0, message.out_wire_custom_records[keys2[j]].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.out_wire_custom_records[keys2[j]]) : message.out_wire_custom_records[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this ForwardHtlcInterceptResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForwardHtlcInterceptResponse.prototype.toJSON = function() {
            return ForwardHtlcInterceptResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ForwardHtlcInterceptResponse
         * @function getTypeUrl
         * @memberof routerrpc.ForwardHtlcInterceptResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ForwardHtlcInterceptResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.ForwardHtlcInterceptResponse";
        };

        return ForwardHtlcInterceptResponse;
    })();

    /**
     * ResolveHoldForwardAction enum.
     * @name routerrpc.ResolveHoldForwardAction
     * @enum {number}
     * @property {number} SETTLE=0 SETTLE value
     * @property {number} FAIL=1 FAIL value
     * @property {number} RESUME=2 RESUME value
     * @property {number} RESUME_MODIFIED=3 RESUME_MODIFIED value
     */
    routerrpc.ResolveHoldForwardAction = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "SETTLE"] = 0;
        values[valuesById[1] = "FAIL"] = 1;
        values[valuesById[2] = "RESUME"] = 2;
        values[valuesById[3] = "RESUME_MODIFIED"] = 3;
        return values;
    })();

    routerrpc.UpdateChanStatusRequest = (function() {

        /**
         * Properties of an UpdateChanStatusRequest.
         * @typedef {Object} routerrpc.UpdateChanStatusRequest.$Properties
         * @property {lnrpc.ChannelPoint.$Properties|null} [chan_point] UpdateChanStatusRequest chan_point
         * @property {routerrpc.ChanStatusAction|null} [action] UpdateChanStatusRequest action
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an UpdateChanStatusRequest.
         * @memberof routerrpc
         * @interface IUpdateChanStatusRequest
         * @augments routerrpc.UpdateChanStatusRequest.$Properties
         * @deprecated Use routerrpc.UpdateChanStatusRequest.$Properties instead.
         */

        /**
         * Shape of an UpdateChanStatusRequest.
         * @typedef {{
         *   chan_point?: lnrpc.ChannelPoint.$Shape|null;
         *   action?: routerrpc.ChanStatusAction|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} routerrpc.UpdateChanStatusRequest.$Shape
         */

        /**
         * Constructs a new UpdateChanStatusRequest.
         * @memberof routerrpc
         * @classdesc Represents an UpdateChanStatusRequest.
         * @constructor
         * @param {routerrpc.UpdateChanStatusRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const UpdateChanStatusRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * UpdateChanStatusRequest chan_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} chan_point
         * @memberof routerrpc.UpdateChanStatusRequest
         * @instance
         */
        UpdateChanStatusRequest.prototype.chan_point = null;

        /**
         * UpdateChanStatusRequest action.
         * @member {routerrpc.ChanStatusAction} action
         * @memberof routerrpc.UpdateChanStatusRequest
         * @instance
         */
        UpdateChanStatusRequest.prototype.action = 0;

        /**
         * Creates a new UpdateChanStatusRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.UpdateChanStatusRequest
         * @static
         * @param {routerrpc.UpdateChanStatusRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.UpdateChanStatusRequest} UpdateChanStatusRequest instance
         * @type {{
         *   (properties: routerrpc.UpdateChanStatusRequest.$Shape): routerrpc.UpdateChanStatusRequest & routerrpc.UpdateChanStatusRequest.$Shape;
         *   (properties?: routerrpc.UpdateChanStatusRequest.$Properties): routerrpc.UpdateChanStatusRequest;
         * }}
         */
        UpdateChanStatusRequest.create = function(properties) {
            return new UpdateChanStatusRequest(properties);
        };

        /**
         * Encodes the specified UpdateChanStatusRequest message. Does not implicitly {@link routerrpc.UpdateChanStatusRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.UpdateChanStatusRequest
         * @static
         * @param {routerrpc.UpdateChanStatusRequest.$Properties} message UpdateChanStatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UpdateChanStatusRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                $root.lnrpc.ChannelPoint.encode(message.chan_point, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.action != null && $Object.hasOwnProperty.call(message, "action") && message.action !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.action);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UpdateChanStatusRequest message, length delimited. Does not implicitly {@link routerrpc.UpdateChanStatusRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.UpdateChanStatusRequest
         * @static
         * @param {routerrpc.UpdateChanStatusRequest.$Properties} message UpdateChanStatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UpdateChanStatusRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an UpdateChanStatusRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.UpdateChanStatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.UpdateChanStatusRequest & routerrpc.UpdateChanStatusRequest.$Shape} UpdateChanStatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UpdateChanStatusRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.UpdateChanStatusRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.chan_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_point);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.action = value;
                        else
                            delete message.action;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UpdateChanStatusRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.UpdateChanStatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.UpdateChanStatusRequest & routerrpc.UpdateChanStatusRequest.$Shape} UpdateChanStatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UpdateChanStatusRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UpdateChanStatusRequest message.
         * @function verify
         * @memberof routerrpc.UpdateChanStatusRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UpdateChanStatusRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.chan_point, _depth + 1);
                if (error)
                    return "chan_point." + error;
            }
            if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
                if (typeof message.action !== "number" || (message.action | 0) !== message.action)
                    return "action: enum value expected";
            return null;
        };

        /**
         * Creates an UpdateChanStatusRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.UpdateChanStatusRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.UpdateChanStatusRequest} UpdateChanStatusRequest
         */
        UpdateChanStatusRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.UpdateChanStatusRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.UpdateChanStatusRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.UpdateChanStatusRequest();
            if (object.chan_point != null) {
                if (!$util.isObject(object.chan_point))
                    throw $TypeError(".routerrpc.UpdateChanStatusRequest.chan_point: object expected");
                message.chan_point = $root.lnrpc.ChannelPoint.fromObject(object.chan_point, _depth + 1);
            }
            if (object.action !== 0 && (typeof object.action !== "string" || $root.routerrpc.ChanStatusAction[object.action] !== 0))
                switch (object.action) {
                case "ENABLE":
                case 0:
                    message.action = 0;
                    break;
                case "DISABLE":
                case 1:
                    message.action = 1;
                    break;
                case "AUTO":
                case 2:
                    message.action = 2;
                    break;
                default:
                    if (typeof object.action === "number" && (object.action | 0) === object.action)
                        message.action = object.action;
                }
            return message;
        };

        /**
         * Creates a plain object from an UpdateChanStatusRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.UpdateChanStatusRequest
         * @static
         * @param {routerrpc.UpdateChanStatusRequest} message UpdateChanStatusRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UpdateChanStatusRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.chan_point = null;
                object.action = options.enums === $String ? "ENABLE" : 0;
            }
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                object.chan_point = $root.lnrpc.ChannelPoint.toObject(message.chan_point, options, _depth + 1);
            if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
                object.action = options.enums === $String ? $root.routerrpc.ChanStatusAction[message.action] === $undefined ? message.action : $root.routerrpc.ChanStatusAction[message.action] : message.action;
            return object;
        };

        /**
         * Converts this UpdateChanStatusRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.UpdateChanStatusRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UpdateChanStatusRequest.prototype.toJSON = function() {
            return UpdateChanStatusRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UpdateChanStatusRequest
         * @function getTypeUrl
         * @memberof routerrpc.UpdateChanStatusRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UpdateChanStatusRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.UpdateChanStatusRequest";
        };

        return UpdateChanStatusRequest;
    })();

    /**
     * ChanStatusAction enum.
     * @name routerrpc.ChanStatusAction
     * @enum {number}
     * @property {number} ENABLE=0 ENABLE value
     * @property {number} DISABLE=1 DISABLE value
     * @property {number} AUTO=2 AUTO value
     */
    routerrpc.ChanStatusAction = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "ENABLE"] = 0;
        values[valuesById[1] = "DISABLE"] = 1;
        values[valuesById[2] = "AUTO"] = 2;
        return values;
    })();

    routerrpc.UpdateChanStatusResponse = (function() {

        /**
         * Properties of an UpdateChanStatusResponse.
         * @typedef {Object} routerrpc.UpdateChanStatusResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an UpdateChanStatusResponse.
         * @memberof routerrpc
         * @interface IUpdateChanStatusResponse
         * @augments routerrpc.UpdateChanStatusResponse.$Properties
         * @deprecated Use routerrpc.UpdateChanStatusResponse.$Properties instead.
         */

        /**
         * Shape of an UpdateChanStatusResponse.
         * @typedef {routerrpc.UpdateChanStatusResponse.$Properties} routerrpc.UpdateChanStatusResponse.$Shape
         */

        /**
         * Constructs a new UpdateChanStatusResponse.
         * @memberof routerrpc
         * @classdesc Represents an UpdateChanStatusResponse.
         * @constructor
         * @param {routerrpc.UpdateChanStatusResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const UpdateChanStatusResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new UpdateChanStatusResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.UpdateChanStatusResponse
         * @static
         * @param {routerrpc.UpdateChanStatusResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.UpdateChanStatusResponse} UpdateChanStatusResponse instance
         * @type {{
         *   (properties: routerrpc.UpdateChanStatusResponse.$Shape): routerrpc.UpdateChanStatusResponse & routerrpc.UpdateChanStatusResponse.$Shape;
         *   (properties?: routerrpc.UpdateChanStatusResponse.$Properties): routerrpc.UpdateChanStatusResponse;
         * }}
         */
        UpdateChanStatusResponse.create = function(properties) {
            return new UpdateChanStatusResponse(properties);
        };

        /**
         * Encodes the specified UpdateChanStatusResponse message. Does not implicitly {@link routerrpc.UpdateChanStatusResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.UpdateChanStatusResponse
         * @static
         * @param {routerrpc.UpdateChanStatusResponse.$Properties} message UpdateChanStatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UpdateChanStatusResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UpdateChanStatusResponse message, length delimited. Does not implicitly {@link routerrpc.UpdateChanStatusResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.UpdateChanStatusResponse
         * @static
         * @param {routerrpc.UpdateChanStatusResponse.$Properties} message UpdateChanStatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UpdateChanStatusResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an UpdateChanStatusResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.UpdateChanStatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.UpdateChanStatusResponse & routerrpc.UpdateChanStatusResponse.$Shape} UpdateChanStatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UpdateChanStatusResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.UpdateChanStatusResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UpdateChanStatusResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.UpdateChanStatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.UpdateChanStatusResponse & routerrpc.UpdateChanStatusResponse.$Shape} UpdateChanStatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UpdateChanStatusResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UpdateChanStatusResponse message.
         * @function verify
         * @memberof routerrpc.UpdateChanStatusResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UpdateChanStatusResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates an UpdateChanStatusResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.UpdateChanStatusResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.UpdateChanStatusResponse} UpdateChanStatusResponse
         */
        UpdateChanStatusResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.UpdateChanStatusResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.UpdateChanStatusResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.routerrpc.UpdateChanStatusResponse();
        };

        /**
         * Creates a plain object from an UpdateChanStatusResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.UpdateChanStatusResponse
         * @static
         * @param {routerrpc.UpdateChanStatusResponse} message UpdateChanStatusResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UpdateChanStatusResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this UpdateChanStatusResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.UpdateChanStatusResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UpdateChanStatusResponse.prototype.toJSON = function() {
            return UpdateChanStatusResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UpdateChanStatusResponse
         * @function getTypeUrl
         * @memberof routerrpc.UpdateChanStatusResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UpdateChanStatusResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.UpdateChanStatusResponse";
        };

        return UpdateChanStatusResponse;
    })();

    routerrpc.AddAliasesRequest = (function() {

        /**
         * Properties of an AddAliasesRequest.
         * @typedef {Object} routerrpc.AddAliasesRequest.$Properties
         * @property {Array.<lnrpc.AliasMap.$Properties>|null} [alias_maps] AddAliasesRequest alias_maps
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddAliasesRequest.
         * @memberof routerrpc
         * @interface IAddAliasesRequest
         * @augments routerrpc.AddAliasesRequest.$Properties
         * @deprecated Use routerrpc.AddAliasesRequest.$Properties instead.
         */

        /**
         * Shape of an AddAliasesRequest.
         * @typedef {routerrpc.AddAliasesRequest.$Properties} routerrpc.AddAliasesRequest.$Shape
         */

        /**
         * Constructs a new AddAliasesRequest.
         * @memberof routerrpc
         * @classdesc Represents an AddAliasesRequest.
         * @constructor
         * @param {routerrpc.AddAliasesRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddAliasesRequest = function (properties) {
            this.alias_maps = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddAliasesRequest alias_maps.
         * @member {Array.<lnrpc.AliasMap.$Properties>} alias_maps
         * @memberof routerrpc.AddAliasesRequest
         * @instance
         */
        AddAliasesRequest.prototype.alias_maps = $util.emptyArray;

        /**
         * Creates a new AddAliasesRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.AddAliasesRequest
         * @static
         * @param {routerrpc.AddAliasesRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.AddAliasesRequest} AddAliasesRequest instance
         * @type {{
         *   (properties: routerrpc.AddAliasesRequest.$Shape): routerrpc.AddAliasesRequest & routerrpc.AddAliasesRequest.$Shape;
         *   (properties?: routerrpc.AddAliasesRequest.$Properties): routerrpc.AddAliasesRequest;
         * }}
         */
        AddAliasesRequest.create = function(properties) {
            return new AddAliasesRequest(properties);
        };

        /**
         * Encodes the specified AddAliasesRequest message. Does not implicitly {@link routerrpc.AddAliasesRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.AddAliasesRequest
         * @static
         * @param {routerrpc.AddAliasesRequest.$Properties} message AddAliasesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddAliasesRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.alias_maps != null && message.alias_maps.length)
                for (let i = 0; i < message.alias_maps.length; ++i)
                    $root.lnrpc.AliasMap.encode(message.alias_maps[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddAliasesRequest message, length delimited. Does not implicitly {@link routerrpc.AddAliasesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.AddAliasesRequest
         * @static
         * @param {routerrpc.AddAliasesRequest.$Properties} message AddAliasesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddAliasesRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddAliasesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.AddAliasesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.AddAliasesRequest & routerrpc.AddAliasesRequest.$Shape} AddAliasesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddAliasesRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.AddAliasesRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.alias_maps && message.alias_maps.length))
                            message.alias_maps = [];
                        message.alias_maps.push($root.lnrpc.AliasMap.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddAliasesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.AddAliasesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.AddAliasesRequest & routerrpc.AddAliasesRequest.$Shape} AddAliasesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddAliasesRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddAliasesRequest message.
         * @function verify
         * @memberof routerrpc.AddAliasesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddAliasesRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.alias_maps != null && $Object.hasOwnProperty.call(message, "alias_maps")) {
                if (!$Array.isArray(message.alias_maps))
                    return "alias_maps: array expected";
                for (let i = 0; i < message.alias_maps.length; ++i) {
                    let error = $root.lnrpc.AliasMap.verify(message.alias_maps[i], _depth + 1);
                    if (error)
                        return "alias_maps." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AddAliasesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.AddAliasesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.AddAliasesRequest} AddAliasesRequest
         */
        AddAliasesRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.AddAliasesRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.AddAliasesRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.AddAliasesRequest();
            if (object.alias_maps) {
                if (!$Array.isArray(object.alias_maps))
                    throw $TypeError(".routerrpc.AddAliasesRequest.alias_maps: array expected");
                message.alias_maps = $Array(object.alias_maps.length);
                for (let i = 0; i < object.alias_maps.length; ++i) {
                    if (!$util.isObject(object.alias_maps[i]))
                        throw $TypeError(".routerrpc.AddAliasesRequest.alias_maps: object expected");
                    message.alias_maps[i] = $root.lnrpc.AliasMap.fromObject(object.alias_maps[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AddAliasesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.AddAliasesRequest
         * @static
         * @param {routerrpc.AddAliasesRequest} message AddAliasesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddAliasesRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.alias_maps = [];
            if (message.alias_maps && message.alias_maps.length) {
                object.alias_maps = $Array(message.alias_maps.length);
                for (let j = 0; j < message.alias_maps.length; ++j)
                    object.alias_maps[j] = $root.lnrpc.AliasMap.toObject(message.alias_maps[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this AddAliasesRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.AddAliasesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddAliasesRequest.prototype.toJSON = function() {
            return AddAliasesRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddAliasesRequest
         * @function getTypeUrl
         * @memberof routerrpc.AddAliasesRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddAliasesRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.AddAliasesRequest";
        };

        return AddAliasesRequest;
    })();

    routerrpc.AddAliasesResponse = (function() {

        /**
         * Properties of an AddAliasesResponse.
         * @typedef {Object} routerrpc.AddAliasesResponse.$Properties
         * @property {Array.<lnrpc.AliasMap.$Properties>|null} [alias_maps] AddAliasesResponse alias_maps
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddAliasesResponse.
         * @memberof routerrpc
         * @interface IAddAliasesResponse
         * @augments routerrpc.AddAliasesResponse.$Properties
         * @deprecated Use routerrpc.AddAliasesResponse.$Properties instead.
         */

        /**
         * Shape of an AddAliasesResponse.
         * @typedef {routerrpc.AddAliasesResponse.$Properties} routerrpc.AddAliasesResponse.$Shape
         */

        /**
         * Constructs a new AddAliasesResponse.
         * @memberof routerrpc
         * @classdesc Represents an AddAliasesResponse.
         * @constructor
         * @param {routerrpc.AddAliasesResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddAliasesResponse = function (properties) {
            this.alias_maps = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddAliasesResponse alias_maps.
         * @member {Array.<lnrpc.AliasMap.$Properties>} alias_maps
         * @memberof routerrpc.AddAliasesResponse
         * @instance
         */
        AddAliasesResponse.prototype.alias_maps = $util.emptyArray;

        /**
         * Creates a new AddAliasesResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.AddAliasesResponse
         * @static
         * @param {routerrpc.AddAliasesResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.AddAliasesResponse} AddAliasesResponse instance
         * @type {{
         *   (properties: routerrpc.AddAliasesResponse.$Shape): routerrpc.AddAliasesResponse & routerrpc.AddAliasesResponse.$Shape;
         *   (properties?: routerrpc.AddAliasesResponse.$Properties): routerrpc.AddAliasesResponse;
         * }}
         */
        AddAliasesResponse.create = function(properties) {
            return new AddAliasesResponse(properties);
        };

        /**
         * Encodes the specified AddAliasesResponse message. Does not implicitly {@link routerrpc.AddAliasesResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.AddAliasesResponse
         * @static
         * @param {routerrpc.AddAliasesResponse.$Properties} message AddAliasesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddAliasesResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.alias_maps != null && message.alias_maps.length)
                for (let i = 0; i < message.alias_maps.length; ++i)
                    $root.lnrpc.AliasMap.encode(message.alias_maps[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddAliasesResponse message, length delimited. Does not implicitly {@link routerrpc.AddAliasesResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.AddAliasesResponse
         * @static
         * @param {routerrpc.AddAliasesResponse.$Properties} message AddAliasesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddAliasesResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddAliasesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.AddAliasesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.AddAliasesResponse & routerrpc.AddAliasesResponse.$Shape} AddAliasesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddAliasesResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.AddAliasesResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.alias_maps && message.alias_maps.length))
                            message.alias_maps = [];
                        message.alias_maps.push($root.lnrpc.AliasMap.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddAliasesResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.AddAliasesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.AddAliasesResponse & routerrpc.AddAliasesResponse.$Shape} AddAliasesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddAliasesResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddAliasesResponse message.
         * @function verify
         * @memberof routerrpc.AddAliasesResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddAliasesResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.alias_maps != null && $Object.hasOwnProperty.call(message, "alias_maps")) {
                if (!$Array.isArray(message.alias_maps))
                    return "alias_maps: array expected";
                for (let i = 0; i < message.alias_maps.length; ++i) {
                    let error = $root.lnrpc.AliasMap.verify(message.alias_maps[i], _depth + 1);
                    if (error)
                        return "alias_maps." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AddAliasesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.AddAliasesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.AddAliasesResponse} AddAliasesResponse
         */
        AddAliasesResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.AddAliasesResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.AddAliasesResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.AddAliasesResponse();
            if (object.alias_maps) {
                if (!$Array.isArray(object.alias_maps))
                    throw $TypeError(".routerrpc.AddAliasesResponse.alias_maps: array expected");
                message.alias_maps = $Array(object.alias_maps.length);
                for (let i = 0; i < object.alias_maps.length; ++i) {
                    if (!$util.isObject(object.alias_maps[i]))
                        throw $TypeError(".routerrpc.AddAliasesResponse.alias_maps: object expected");
                    message.alias_maps[i] = $root.lnrpc.AliasMap.fromObject(object.alias_maps[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AddAliasesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.AddAliasesResponse
         * @static
         * @param {routerrpc.AddAliasesResponse} message AddAliasesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddAliasesResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.alias_maps = [];
            if (message.alias_maps && message.alias_maps.length) {
                object.alias_maps = $Array(message.alias_maps.length);
                for (let j = 0; j < message.alias_maps.length; ++j)
                    object.alias_maps[j] = $root.lnrpc.AliasMap.toObject(message.alias_maps[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this AddAliasesResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.AddAliasesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddAliasesResponse.prototype.toJSON = function() {
            return AddAliasesResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddAliasesResponse
         * @function getTypeUrl
         * @memberof routerrpc.AddAliasesResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddAliasesResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.AddAliasesResponse";
        };

        return AddAliasesResponse;
    })();

    routerrpc.DeleteAliasesRequest = (function() {

        /**
         * Properties of a DeleteAliasesRequest.
         * @typedef {Object} routerrpc.DeleteAliasesRequest.$Properties
         * @property {Array.<lnrpc.AliasMap.$Properties>|null} [alias_maps] DeleteAliasesRequest alias_maps
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeleteAliasesRequest.
         * @memberof routerrpc
         * @interface IDeleteAliasesRequest
         * @augments routerrpc.DeleteAliasesRequest.$Properties
         * @deprecated Use routerrpc.DeleteAliasesRequest.$Properties instead.
         */

        /**
         * Shape of a DeleteAliasesRequest.
         * @typedef {routerrpc.DeleteAliasesRequest.$Properties} routerrpc.DeleteAliasesRequest.$Shape
         */

        /**
         * Constructs a new DeleteAliasesRequest.
         * @memberof routerrpc
         * @classdesc Represents a DeleteAliasesRequest.
         * @constructor
         * @param {routerrpc.DeleteAliasesRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeleteAliasesRequest = function (properties) {
            this.alias_maps = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeleteAliasesRequest alias_maps.
         * @member {Array.<lnrpc.AliasMap.$Properties>} alias_maps
         * @memberof routerrpc.DeleteAliasesRequest
         * @instance
         */
        DeleteAliasesRequest.prototype.alias_maps = $util.emptyArray;

        /**
         * Creates a new DeleteAliasesRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.DeleteAliasesRequest
         * @static
         * @param {routerrpc.DeleteAliasesRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.DeleteAliasesRequest} DeleteAliasesRequest instance
         * @type {{
         *   (properties: routerrpc.DeleteAliasesRequest.$Shape): routerrpc.DeleteAliasesRequest & routerrpc.DeleteAliasesRequest.$Shape;
         *   (properties?: routerrpc.DeleteAliasesRequest.$Properties): routerrpc.DeleteAliasesRequest;
         * }}
         */
        DeleteAliasesRequest.create = function(properties) {
            return new DeleteAliasesRequest(properties);
        };

        /**
         * Encodes the specified DeleteAliasesRequest message. Does not implicitly {@link routerrpc.DeleteAliasesRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.DeleteAliasesRequest
         * @static
         * @param {routerrpc.DeleteAliasesRequest.$Properties} message DeleteAliasesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteAliasesRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.alias_maps != null && message.alias_maps.length)
                for (let i = 0; i < message.alias_maps.length; ++i)
                    $root.lnrpc.AliasMap.encode(message.alias_maps[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeleteAliasesRequest message, length delimited. Does not implicitly {@link routerrpc.DeleteAliasesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.DeleteAliasesRequest
         * @static
         * @param {routerrpc.DeleteAliasesRequest.$Properties} message DeleteAliasesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteAliasesRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeleteAliasesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.DeleteAliasesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.DeleteAliasesRequest & routerrpc.DeleteAliasesRequest.$Shape} DeleteAliasesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteAliasesRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.DeleteAliasesRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.alias_maps && message.alias_maps.length))
                            message.alias_maps = [];
                        message.alias_maps.push($root.lnrpc.AliasMap.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeleteAliasesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.DeleteAliasesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.DeleteAliasesRequest & routerrpc.DeleteAliasesRequest.$Shape} DeleteAliasesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteAliasesRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeleteAliasesRequest message.
         * @function verify
         * @memberof routerrpc.DeleteAliasesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeleteAliasesRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.alias_maps != null && $Object.hasOwnProperty.call(message, "alias_maps")) {
                if (!$Array.isArray(message.alias_maps))
                    return "alias_maps: array expected";
                for (let i = 0; i < message.alias_maps.length; ++i) {
                    let error = $root.lnrpc.AliasMap.verify(message.alias_maps[i], _depth + 1);
                    if (error)
                        return "alias_maps." + error;
                }
            }
            return null;
        };

        /**
         * Creates a DeleteAliasesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.DeleteAliasesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.DeleteAliasesRequest} DeleteAliasesRequest
         */
        DeleteAliasesRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.DeleteAliasesRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.DeleteAliasesRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.DeleteAliasesRequest();
            if (object.alias_maps) {
                if (!$Array.isArray(object.alias_maps))
                    throw $TypeError(".routerrpc.DeleteAliasesRequest.alias_maps: array expected");
                message.alias_maps = $Array(object.alias_maps.length);
                for (let i = 0; i < object.alias_maps.length; ++i) {
                    if (!$util.isObject(object.alias_maps[i]))
                        throw $TypeError(".routerrpc.DeleteAliasesRequest.alias_maps: object expected");
                    message.alias_maps[i] = $root.lnrpc.AliasMap.fromObject(object.alias_maps[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a DeleteAliasesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.DeleteAliasesRequest
         * @static
         * @param {routerrpc.DeleteAliasesRequest} message DeleteAliasesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeleteAliasesRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.alias_maps = [];
            if (message.alias_maps && message.alias_maps.length) {
                object.alias_maps = $Array(message.alias_maps.length);
                for (let j = 0; j < message.alias_maps.length; ++j)
                    object.alias_maps[j] = $root.lnrpc.AliasMap.toObject(message.alias_maps[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this DeleteAliasesRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.DeleteAliasesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeleteAliasesRequest.prototype.toJSON = function() {
            return DeleteAliasesRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeleteAliasesRequest
         * @function getTypeUrl
         * @memberof routerrpc.DeleteAliasesRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeleteAliasesRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.DeleteAliasesRequest";
        };

        return DeleteAliasesRequest;
    })();

    routerrpc.DeleteAliasesResponse = (function() {

        /**
         * Properties of a DeleteAliasesResponse.
         * @typedef {Object} routerrpc.DeleteAliasesResponse.$Properties
         * @property {Array.<lnrpc.AliasMap.$Properties>|null} [alias_maps] DeleteAliasesResponse alias_maps
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeleteAliasesResponse.
         * @memberof routerrpc
         * @interface IDeleteAliasesResponse
         * @augments routerrpc.DeleteAliasesResponse.$Properties
         * @deprecated Use routerrpc.DeleteAliasesResponse.$Properties instead.
         */

        /**
         * Shape of a DeleteAliasesResponse.
         * @typedef {routerrpc.DeleteAliasesResponse.$Properties} routerrpc.DeleteAliasesResponse.$Shape
         */

        /**
         * Constructs a new DeleteAliasesResponse.
         * @memberof routerrpc
         * @classdesc Represents a DeleteAliasesResponse.
         * @constructor
         * @param {routerrpc.DeleteAliasesResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeleteAliasesResponse = function (properties) {
            this.alias_maps = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeleteAliasesResponse alias_maps.
         * @member {Array.<lnrpc.AliasMap.$Properties>} alias_maps
         * @memberof routerrpc.DeleteAliasesResponse
         * @instance
         */
        DeleteAliasesResponse.prototype.alias_maps = $util.emptyArray;

        /**
         * Creates a new DeleteAliasesResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.DeleteAliasesResponse
         * @static
         * @param {routerrpc.DeleteAliasesResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.DeleteAliasesResponse} DeleteAliasesResponse instance
         * @type {{
         *   (properties: routerrpc.DeleteAliasesResponse.$Shape): routerrpc.DeleteAliasesResponse & routerrpc.DeleteAliasesResponse.$Shape;
         *   (properties?: routerrpc.DeleteAliasesResponse.$Properties): routerrpc.DeleteAliasesResponse;
         * }}
         */
        DeleteAliasesResponse.create = function(properties) {
            return new DeleteAliasesResponse(properties);
        };

        /**
         * Encodes the specified DeleteAliasesResponse message. Does not implicitly {@link routerrpc.DeleteAliasesResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.DeleteAliasesResponse
         * @static
         * @param {routerrpc.DeleteAliasesResponse.$Properties} message DeleteAliasesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteAliasesResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.alias_maps != null && message.alias_maps.length)
                for (let i = 0; i < message.alias_maps.length; ++i)
                    $root.lnrpc.AliasMap.encode(message.alias_maps[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeleteAliasesResponse message, length delimited. Does not implicitly {@link routerrpc.DeleteAliasesResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.DeleteAliasesResponse
         * @static
         * @param {routerrpc.DeleteAliasesResponse.$Properties} message DeleteAliasesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteAliasesResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeleteAliasesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.DeleteAliasesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.DeleteAliasesResponse & routerrpc.DeleteAliasesResponse.$Shape} DeleteAliasesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteAliasesResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.DeleteAliasesResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.alias_maps && message.alias_maps.length))
                            message.alias_maps = [];
                        message.alias_maps.push($root.lnrpc.AliasMap.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeleteAliasesResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.DeleteAliasesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.DeleteAliasesResponse & routerrpc.DeleteAliasesResponse.$Shape} DeleteAliasesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteAliasesResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeleteAliasesResponse message.
         * @function verify
         * @memberof routerrpc.DeleteAliasesResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeleteAliasesResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.alias_maps != null && $Object.hasOwnProperty.call(message, "alias_maps")) {
                if (!$Array.isArray(message.alias_maps))
                    return "alias_maps: array expected";
                for (let i = 0; i < message.alias_maps.length; ++i) {
                    let error = $root.lnrpc.AliasMap.verify(message.alias_maps[i], _depth + 1);
                    if (error)
                        return "alias_maps." + error;
                }
            }
            return null;
        };

        /**
         * Creates a DeleteAliasesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.DeleteAliasesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.DeleteAliasesResponse} DeleteAliasesResponse
         */
        DeleteAliasesResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.DeleteAliasesResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.DeleteAliasesResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.DeleteAliasesResponse();
            if (object.alias_maps) {
                if (!$Array.isArray(object.alias_maps))
                    throw $TypeError(".routerrpc.DeleteAliasesResponse.alias_maps: array expected");
                message.alias_maps = $Array(object.alias_maps.length);
                for (let i = 0; i < object.alias_maps.length; ++i) {
                    if (!$util.isObject(object.alias_maps[i]))
                        throw $TypeError(".routerrpc.DeleteAliasesResponse.alias_maps: object expected");
                    message.alias_maps[i] = $root.lnrpc.AliasMap.fromObject(object.alias_maps[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a DeleteAliasesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.DeleteAliasesResponse
         * @static
         * @param {routerrpc.DeleteAliasesResponse} message DeleteAliasesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeleteAliasesResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.alias_maps = [];
            if (message.alias_maps && message.alias_maps.length) {
                object.alias_maps = $Array(message.alias_maps.length);
                for (let j = 0; j < message.alias_maps.length; ++j)
                    object.alias_maps[j] = $root.lnrpc.AliasMap.toObject(message.alias_maps[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this DeleteAliasesResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.DeleteAliasesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeleteAliasesResponse.prototype.toJSON = function() {
            return DeleteAliasesResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeleteAliasesResponse
         * @function getTypeUrl
         * @memberof routerrpc.DeleteAliasesResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeleteAliasesResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.DeleteAliasesResponse";
        };

        return DeleteAliasesResponse;
    })();

    routerrpc.FindBaseAliasRequest = (function() {

        /**
         * Properties of a FindBaseAliasRequest.
         * @typedef {Object} routerrpc.FindBaseAliasRequest.$Properties
         * @property {Long|null} [alias] FindBaseAliasRequest alias
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FindBaseAliasRequest.
         * @memberof routerrpc
         * @interface IFindBaseAliasRequest
         * @augments routerrpc.FindBaseAliasRequest.$Properties
         * @deprecated Use routerrpc.FindBaseAliasRequest.$Properties instead.
         */

        /**
         * Shape of a FindBaseAliasRequest.
         * @typedef {routerrpc.FindBaseAliasRequest.$Properties} routerrpc.FindBaseAliasRequest.$Shape
         */

        /**
         * Constructs a new FindBaseAliasRequest.
         * @memberof routerrpc
         * @classdesc Represents a FindBaseAliasRequest.
         * @constructor
         * @param {routerrpc.FindBaseAliasRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FindBaseAliasRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FindBaseAliasRequest alias.
         * @member {Long} alias
         * @memberof routerrpc.FindBaseAliasRequest
         * @instance
         */
        FindBaseAliasRequest.prototype.alias = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new FindBaseAliasRequest instance using the specified properties.
         * @function create
         * @memberof routerrpc.FindBaseAliasRequest
         * @static
         * @param {routerrpc.FindBaseAliasRequest.$Properties=} [properties] Properties to set
         * @returns {routerrpc.FindBaseAliasRequest} FindBaseAliasRequest instance
         * @type {{
         *   (properties: routerrpc.FindBaseAliasRequest.$Shape): routerrpc.FindBaseAliasRequest & routerrpc.FindBaseAliasRequest.$Shape;
         *   (properties?: routerrpc.FindBaseAliasRequest.$Properties): routerrpc.FindBaseAliasRequest;
         * }}
         */
        FindBaseAliasRequest.create = function(properties) {
            return new FindBaseAliasRequest(properties);
        };

        /**
         * Encodes the specified FindBaseAliasRequest message. Does not implicitly {@link routerrpc.FindBaseAliasRequest.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.FindBaseAliasRequest
         * @static
         * @param {routerrpc.FindBaseAliasRequest.$Properties} message FindBaseAliasRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FindBaseAliasRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias") && (typeof message.alias === "object" ? message.alias.low || message.alias.high : message.alias !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.alias);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FindBaseAliasRequest message, length delimited. Does not implicitly {@link routerrpc.FindBaseAliasRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.FindBaseAliasRequest
         * @static
         * @param {routerrpc.FindBaseAliasRequest.$Properties} message FindBaseAliasRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FindBaseAliasRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FindBaseAliasRequest message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.FindBaseAliasRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.FindBaseAliasRequest & routerrpc.FindBaseAliasRequest.$Shape} FindBaseAliasRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FindBaseAliasRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.FindBaseAliasRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.alias = value;
                        else
                            delete message.alias;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FindBaseAliasRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.FindBaseAliasRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.FindBaseAliasRequest & routerrpc.FindBaseAliasRequest.$Shape} FindBaseAliasRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FindBaseAliasRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FindBaseAliasRequest message.
         * @function verify
         * @memberof routerrpc.FindBaseAliasRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FindBaseAliasRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                if (!$util.isInteger(message.alias) && !(message.alias && $util.isInteger(message.alias.low) && $util.isInteger(message.alias.high)))
                    return "alias: integer|Long expected";
            return null;
        };

        /**
         * Creates a FindBaseAliasRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.FindBaseAliasRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.FindBaseAliasRequest} FindBaseAliasRequest
         */
        FindBaseAliasRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.FindBaseAliasRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.FindBaseAliasRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.FindBaseAliasRequest();
            if (object.alias != null)
                if (typeof object.alias === "object" ? object.alias.low || object.alias.high : $Number(object.alias) !== 0)
                    if ($util.Long)
                        message.alias = $util.Long.fromValue(object.alias, true);
                    else if (typeof object.alias === "string")
                        message.alias = $parseInt(object.alias, 10);
                    else if (typeof object.alias === "number")
                        message.alias = object.alias;
                    else if (typeof object.alias === "object")
                        message.alias = new $util.LongBits(object.alias.low >>> 0, object.alias.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FindBaseAliasRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.FindBaseAliasRequest
         * @static
         * @param {routerrpc.FindBaseAliasRequest} message FindBaseAliasRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FindBaseAliasRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.alias = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.alias = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.alias = typeof message.alias === "number" ? $BigInt(message.alias) : $util.Long.fromBits(message.alias.low >>> 0, message.alias.high >>> 0, true).toBigInt();
                else if (typeof message.alias === "number")
                    object.alias = options.longs === $String ? $String(message.alias) : message.alias;
                else
                    object.alias = options.longs === $String ? $util.Long.prototype.toString.call(message.alias) : options.longs === $Number ? new $util.LongBits(message.alias.low >>> 0, message.alias.high >>> 0).toNumber(true) : message.alias;
            return object;
        };

        /**
         * Converts this FindBaseAliasRequest to JSON.
         * @function toJSON
         * @memberof routerrpc.FindBaseAliasRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FindBaseAliasRequest.prototype.toJSON = function() {
            return FindBaseAliasRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FindBaseAliasRequest
         * @function getTypeUrl
         * @memberof routerrpc.FindBaseAliasRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FindBaseAliasRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.FindBaseAliasRequest";
        };

        return FindBaseAliasRequest;
    })();

    routerrpc.FindBaseAliasResponse = (function() {

        /**
         * Properties of a FindBaseAliasResponse.
         * @typedef {Object} routerrpc.FindBaseAliasResponse.$Properties
         * @property {Long|null} [base] FindBaseAliasResponse base
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FindBaseAliasResponse.
         * @memberof routerrpc
         * @interface IFindBaseAliasResponse
         * @augments routerrpc.FindBaseAliasResponse.$Properties
         * @deprecated Use routerrpc.FindBaseAliasResponse.$Properties instead.
         */

        /**
         * Shape of a FindBaseAliasResponse.
         * @typedef {routerrpc.FindBaseAliasResponse.$Properties} routerrpc.FindBaseAliasResponse.$Shape
         */

        /**
         * Constructs a new FindBaseAliasResponse.
         * @memberof routerrpc
         * @classdesc Represents a FindBaseAliasResponse.
         * @constructor
         * @param {routerrpc.FindBaseAliasResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FindBaseAliasResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FindBaseAliasResponse base.
         * @member {Long} base
         * @memberof routerrpc.FindBaseAliasResponse
         * @instance
         */
        FindBaseAliasResponse.prototype.base = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new FindBaseAliasResponse instance using the specified properties.
         * @function create
         * @memberof routerrpc.FindBaseAliasResponse
         * @static
         * @param {routerrpc.FindBaseAliasResponse.$Properties=} [properties] Properties to set
         * @returns {routerrpc.FindBaseAliasResponse} FindBaseAliasResponse instance
         * @type {{
         *   (properties: routerrpc.FindBaseAliasResponse.$Shape): routerrpc.FindBaseAliasResponse & routerrpc.FindBaseAliasResponse.$Shape;
         *   (properties?: routerrpc.FindBaseAliasResponse.$Properties): routerrpc.FindBaseAliasResponse;
         * }}
         */
        FindBaseAliasResponse.create = function(properties) {
            return new FindBaseAliasResponse(properties);
        };

        /**
         * Encodes the specified FindBaseAliasResponse message. Does not implicitly {@link routerrpc.FindBaseAliasResponse.verify|verify} messages.
         * @function encode
         * @memberof routerrpc.FindBaseAliasResponse
         * @static
         * @param {routerrpc.FindBaseAliasResponse.$Properties} message FindBaseAliasResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FindBaseAliasResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.base != null && $Object.hasOwnProperty.call(message, "base") && (typeof message.base === "object" ? message.base.low || message.base.high : message.base !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.base);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FindBaseAliasResponse message, length delimited. Does not implicitly {@link routerrpc.FindBaseAliasResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof routerrpc.FindBaseAliasResponse
         * @static
         * @param {routerrpc.FindBaseAliasResponse.$Properties} message FindBaseAliasResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FindBaseAliasResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FindBaseAliasResponse message from the specified reader or buffer.
         * @function decode
         * @memberof routerrpc.FindBaseAliasResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {routerrpc.FindBaseAliasResponse & routerrpc.FindBaseAliasResponse.$Shape} FindBaseAliasResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FindBaseAliasResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.routerrpc.FindBaseAliasResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.base = value;
                        else
                            delete message.base;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FindBaseAliasResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof routerrpc.FindBaseAliasResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {routerrpc.FindBaseAliasResponse & routerrpc.FindBaseAliasResponse.$Shape} FindBaseAliasResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FindBaseAliasResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FindBaseAliasResponse message.
         * @function verify
         * @memberof routerrpc.FindBaseAliasResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FindBaseAliasResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.base != null && $Object.hasOwnProperty.call(message, "base"))
                if (!$util.isInteger(message.base) && !(message.base && $util.isInteger(message.base.low) && $util.isInteger(message.base.high)))
                    return "base: integer|Long expected";
            return null;
        };

        /**
         * Creates a FindBaseAliasResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof routerrpc.FindBaseAliasResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {routerrpc.FindBaseAliasResponse} FindBaseAliasResponse
         */
        FindBaseAliasResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.routerrpc.FindBaseAliasResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".routerrpc.FindBaseAliasResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.routerrpc.FindBaseAliasResponse();
            if (object.base != null)
                if (typeof object.base === "object" ? object.base.low || object.base.high : $Number(object.base) !== 0)
                    if ($util.Long)
                        message.base = $util.Long.fromValue(object.base, true);
                    else if (typeof object.base === "string")
                        message.base = $parseInt(object.base, 10);
                    else if (typeof object.base === "number")
                        message.base = object.base;
                    else if (typeof object.base === "object")
                        message.base = new $util.LongBits(object.base.low >>> 0, object.base.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FindBaseAliasResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof routerrpc.FindBaseAliasResponse
         * @static
         * @param {routerrpc.FindBaseAliasResponse} message FindBaseAliasResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FindBaseAliasResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.base = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.base = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.base != null && $Object.hasOwnProperty.call(message, "base"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.base = typeof message.base === "number" ? $BigInt(message.base) : $util.Long.fromBits(message.base.low >>> 0, message.base.high >>> 0, true).toBigInt();
                else if (typeof message.base === "number")
                    object.base = options.longs === $String ? $String(message.base) : message.base;
                else
                    object.base = options.longs === $String ? $util.Long.prototype.toString.call(message.base) : options.longs === $Number ? new $util.LongBits(message.base.low >>> 0, message.base.high >>> 0).toNumber(true) : message.base;
            return object;
        };

        /**
         * Converts this FindBaseAliasResponse to JSON.
         * @function toJSON
         * @memberof routerrpc.FindBaseAliasResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FindBaseAliasResponse.prototype.toJSON = function() {
            return FindBaseAliasResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FindBaseAliasResponse
         * @function getTypeUrl
         * @memberof routerrpc.FindBaseAliasResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FindBaseAliasResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/routerrpc.FindBaseAliasResponse";
        };

        return FindBaseAliasResponse;
    })();

    return routerrpc;
})();

export const signrpc = $root.signrpc = (() => {

    /**
     * Namespace signrpc.
     * @exports signrpc
     * @namespace
     */
    const signrpc = {};

    signrpc.Signer = (function() {

        /**
         * Constructs a new Signer service.
         * @memberof signrpc
         * @classdesc Represents a Signer
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const Signer = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (Signer.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = Signer;

        /**
         * Creates new Signer service using the specified rpc implementation.
         * @function create
         * @memberof signrpc.Signer
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {Signer} RPC service. Useful where requests and/or responses are streamed.
         */
        Signer.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link signrpc.Signer#signOutputRaw}.
         * @memberof signrpc.Signer
         * @typedef SignOutputRawCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.SignResp} [response] SignResp
         */

        /**
         * Calls SignOutputRaw.
         * @memberof signrpc.Signer
         * @typedef SignOutputRaw
         * @type {{
         *   (request: signrpc.ISignReq, callback: signrpc.Signer.SignOutputRawCallback): void;
         *   (request: signrpc.ISignReq): Promise<signrpc.SignResp>;
         *   readonly name: "SignOutputRaw";
         *   readonly path: "/signrpc.Signer/SignOutputRaw";
         *   readonly requestType: "SignReq";
         *   readonly responseType: "SignResp";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SignOutputRaw.
         * @name signrpc.Signer#signOutputRaw
         * @type {signrpc.Signer.SignOutputRaw}
         */
        $Object.defineProperties(Signer.prototype.signOutputRaw = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.signOutputRaw, $root.signrpc.SignReq, $root.signrpc.SignResp, request, callback);
        }, {
            name: { value: "SignOutputRaw" },
            path: { value: "/signrpc.Signer/SignOutputRaw" },
            requestType: { value: "SignReq" },
            responseType: { value: "SignResp" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#computeInputScript}.
         * @memberof signrpc.Signer
         * @typedef ComputeInputScriptCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.InputScriptResp} [response] InputScriptResp
         */

        /**
         * Calls ComputeInputScript.
         * @memberof signrpc.Signer
         * @typedef ComputeInputScript
         * @type {{
         *   (request: signrpc.ISignReq, callback: signrpc.Signer.ComputeInputScriptCallback): void;
         *   (request: signrpc.ISignReq): Promise<signrpc.InputScriptResp>;
         *   readonly name: "ComputeInputScript";
         *   readonly path: "/signrpc.Signer/ComputeInputScript";
         *   readonly requestType: "SignReq";
         *   readonly responseType: "InputScriptResp";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ComputeInputScript.
         * @name signrpc.Signer#computeInputScript
         * @type {signrpc.Signer.ComputeInputScript}
         */
        $Object.defineProperties(Signer.prototype.computeInputScript = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.computeInputScript, $root.signrpc.SignReq, $root.signrpc.InputScriptResp, request, callback);
        }, {
            name: { value: "ComputeInputScript" },
            path: { value: "/signrpc.Signer/ComputeInputScript" },
            requestType: { value: "SignReq" },
            responseType: { value: "InputScriptResp" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#signMessage}.
         * @memberof signrpc.Signer
         * @typedef SignMessageCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.SignMessageResp} [response] SignMessageResp
         */

        /**
         * Calls SignMessage.
         * @memberof signrpc.Signer
         * @typedef SignMessage
         * @type {{
         *   (request: signrpc.ISignMessageReq, callback: signrpc.Signer.SignMessageCallback): void;
         *   (request: signrpc.ISignMessageReq): Promise<signrpc.SignMessageResp>;
         *   readonly name: "SignMessage";
         *   readonly path: "/signrpc.Signer/SignMessage";
         *   readonly requestType: "SignMessageReq";
         *   readonly responseType: "SignMessageResp";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SignMessage.
         * @name signrpc.Signer#signMessage
         * @type {signrpc.Signer.SignMessage}
         */
        $Object.defineProperties(Signer.prototype.signMessage = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.signMessage, $root.signrpc.SignMessageReq, $root.signrpc.SignMessageResp, request, callback);
        }, {
            name: { value: "SignMessage" },
            path: { value: "/signrpc.Signer/SignMessage" },
            requestType: { value: "SignMessageReq" },
            responseType: { value: "SignMessageResp" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#verifyMessage}.
         * @memberof signrpc.Signer
         * @typedef VerifyMessageCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.VerifyMessageResp} [response] VerifyMessageResp
         */

        /**
         * Calls VerifyMessage.
         * @memberof signrpc.Signer
         * @typedef VerifyMessage
         * @type {{
         *   (request: signrpc.IVerifyMessageReq, callback: signrpc.Signer.VerifyMessageCallback): void;
         *   (request: signrpc.IVerifyMessageReq): Promise<signrpc.VerifyMessageResp>;
         *   readonly name: "VerifyMessage";
         *   readonly path: "/signrpc.Signer/VerifyMessage";
         *   readonly requestType: "VerifyMessageReq";
         *   readonly responseType: "VerifyMessageResp";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls VerifyMessage.
         * @name signrpc.Signer#verifyMessage
         * @type {signrpc.Signer.VerifyMessage}
         */
        $Object.defineProperties(Signer.prototype.verifyMessage = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.verifyMessage, $root.signrpc.VerifyMessageReq, $root.signrpc.VerifyMessageResp, request, callback);
        }, {
            name: { value: "VerifyMessage" },
            path: { value: "/signrpc.Signer/VerifyMessage" },
            requestType: { value: "VerifyMessageReq" },
            responseType: { value: "VerifyMessageResp" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#deriveSharedKey}.
         * @memberof signrpc.Signer
         * @typedef DeriveSharedKeyCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.SharedKeyResponse} [response] SharedKeyResponse
         */

        /**
         * Calls DeriveSharedKey.
         * @memberof signrpc.Signer
         * @typedef DeriveSharedKey
         * @type {{
         *   (request: signrpc.ISharedKeyRequest, callback: signrpc.Signer.DeriveSharedKeyCallback): void;
         *   (request: signrpc.ISharedKeyRequest): Promise<signrpc.SharedKeyResponse>;
         *   readonly name: "DeriveSharedKey";
         *   readonly path: "/signrpc.Signer/DeriveSharedKey";
         *   readonly requestType: "SharedKeyRequest";
         *   readonly responseType: "SharedKeyResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DeriveSharedKey.
         * @name signrpc.Signer#deriveSharedKey
         * @type {signrpc.Signer.DeriveSharedKey}
         */
        $Object.defineProperties(Signer.prototype.deriveSharedKey = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.deriveSharedKey, $root.signrpc.SharedKeyRequest, $root.signrpc.SharedKeyResponse, request, callback);
        }, {
            name: { value: "DeriveSharedKey" },
            path: { value: "/signrpc.Signer/DeriveSharedKey" },
            requestType: { value: "SharedKeyRequest" },
            responseType: { value: "SharedKeyResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#muSig2CombineKeys}.
         * @memberof signrpc.Signer
         * @typedef MuSig2CombineKeysCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.MuSig2CombineKeysResponse} [response] MuSig2CombineKeysResponse
         */

        /**
         * Calls MuSig2CombineKeys.
         * @memberof signrpc.Signer
         * @typedef MuSig2CombineKeys
         * @type {{
         *   (request: signrpc.IMuSig2CombineKeysRequest, callback: signrpc.Signer.MuSig2CombineKeysCallback): void;
         *   (request: signrpc.IMuSig2CombineKeysRequest): Promise<signrpc.MuSig2CombineKeysResponse>;
         *   readonly name: "MuSig2CombineKeys";
         *   readonly path: "/signrpc.Signer/MuSig2CombineKeys";
         *   readonly requestType: "MuSig2CombineKeysRequest";
         *   readonly responseType: "MuSig2CombineKeysResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls MuSig2CombineKeys.
         * @name signrpc.Signer#muSig2CombineKeys
         * @type {signrpc.Signer.MuSig2CombineKeys}
         */
        $Object.defineProperties(Signer.prototype.muSig2CombineKeys = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.muSig2CombineKeys, $root.signrpc.MuSig2CombineKeysRequest, $root.signrpc.MuSig2CombineKeysResponse, request, callback);
        }, {
            name: { value: "MuSig2CombineKeys" },
            path: { value: "/signrpc.Signer/MuSig2CombineKeys" },
            requestType: { value: "MuSig2CombineKeysRequest" },
            responseType: { value: "MuSig2CombineKeysResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#muSig2CreateSession}.
         * @memberof signrpc.Signer
         * @typedef MuSig2CreateSessionCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.MuSig2SessionResponse} [response] MuSig2SessionResponse
         */

        /**
         * Calls MuSig2CreateSession.
         * @memberof signrpc.Signer
         * @typedef MuSig2CreateSession
         * @type {{
         *   (request: signrpc.IMuSig2SessionRequest, callback: signrpc.Signer.MuSig2CreateSessionCallback): void;
         *   (request: signrpc.IMuSig2SessionRequest): Promise<signrpc.MuSig2SessionResponse>;
         *   readonly name: "MuSig2CreateSession";
         *   readonly path: "/signrpc.Signer/MuSig2CreateSession";
         *   readonly requestType: "MuSig2SessionRequest";
         *   readonly responseType: "MuSig2SessionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls MuSig2CreateSession.
         * @name signrpc.Signer#muSig2CreateSession
         * @type {signrpc.Signer.MuSig2CreateSession}
         */
        $Object.defineProperties(Signer.prototype.muSig2CreateSession = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.muSig2CreateSession, $root.signrpc.MuSig2SessionRequest, $root.signrpc.MuSig2SessionResponse, request, callback);
        }, {
            name: { value: "MuSig2CreateSession" },
            path: { value: "/signrpc.Signer/MuSig2CreateSession" },
            requestType: { value: "MuSig2SessionRequest" },
            responseType: { value: "MuSig2SessionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#muSig2RegisterNonces}.
         * @memberof signrpc.Signer
         * @typedef MuSig2RegisterNoncesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.MuSig2RegisterNoncesResponse} [response] MuSig2RegisterNoncesResponse
         */

        /**
         * Calls MuSig2RegisterNonces.
         * @memberof signrpc.Signer
         * @typedef MuSig2RegisterNonces
         * @type {{
         *   (request: signrpc.IMuSig2RegisterNoncesRequest, callback: signrpc.Signer.MuSig2RegisterNoncesCallback): void;
         *   (request: signrpc.IMuSig2RegisterNoncesRequest): Promise<signrpc.MuSig2RegisterNoncesResponse>;
         *   readonly name: "MuSig2RegisterNonces";
         *   readonly path: "/signrpc.Signer/MuSig2RegisterNonces";
         *   readonly requestType: "MuSig2RegisterNoncesRequest";
         *   readonly responseType: "MuSig2RegisterNoncesResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls MuSig2RegisterNonces.
         * @name signrpc.Signer#muSig2RegisterNonces
         * @type {signrpc.Signer.MuSig2RegisterNonces}
         */
        $Object.defineProperties(Signer.prototype.muSig2RegisterNonces = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.muSig2RegisterNonces, $root.signrpc.MuSig2RegisterNoncesRequest, $root.signrpc.MuSig2RegisterNoncesResponse, request, callback);
        }, {
            name: { value: "MuSig2RegisterNonces" },
            path: { value: "/signrpc.Signer/MuSig2RegisterNonces" },
            requestType: { value: "MuSig2RegisterNoncesRequest" },
            responseType: { value: "MuSig2RegisterNoncesResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#muSig2Sign}.
         * @memberof signrpc.Signer
         * @typedef MuSig2SignCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.MuSig2SignResponse} [response] MuSig2SignResponse
         */

        /**
         * Calls MuSig2Sign.
         * @memberof signrpc.Signer
         * @typedef MuSig2Sign
         * @type {{
         *   (request: signrpc.IMuSig2SignRequest, callback: signrpc.Signer.MuSig2SignCallback): void;
         *   (request: signrpc.IMuSig2SignRequest): Promise<signrpc.MuSig2SignResponse>;
         *   readonly name: "MuSig2Sign";
         *   readonly path: "/signrpc.Signer/MuSig2Sign";
         *   readonly requestType: "MuSig2SignRequest";
         *   readonly responseType: "MuSig2SignResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls MuSig2Sign.
         * @name signrpc.Signer#muSig2Sign
         * @type {signrpc.Signer.MuSig2Sign}
         */
        $Object.defineProperties(Signer.prototype.muSig2Sign = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.muSig2Sign, $root.signrpc.MuSig2SignRequest, $root.signrpc.MuSig2SignResponse, request, callback);
        }, {
            name: { value: "MuSig2Sign" },
            path: { value: "/signrpc.Signer/MuSig2Sign" },
            requestType: { value: "MuSig2SignRequest" },
            responseType: { value: "MuSig2SignResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#muSig2CombineSig}.
         * @memberof signrpc.Signer
         * @typedef MuSig2CombineSigCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.MuSig2CombineSigResponse} [response] MuSig2CombineSigResponse
         */

        /**
         * Calls MuSig2CombineSig.
         * @memberof signrpc.Signer
         * @typedef MuSig2CombineSig
         * @type {{
         *   (request: signrpc.IMuSig2CombineSigRequest, callback: signrpc.Signer.MuSig2CombineSigCallback): void;
         *   (request: signrpc.IMuSig2CombineSigRequest): Promise<signrpc.MuSig2CombineSigResponse>;
         *   readonly name: "MuSig2CombineSig";
         *   readonly path: "/signrpc.Signer/MuSig2CombineSig";
         *   readonly requestType: "MuSig2CombineSigRequest";
         *   readonly responseType: "MuSig2CombineSigResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls MuSig2CombineSig.
         * @name signrpc.Signer#muSig2CombineSig
         * @type {signrpc.Signer.MuSig2CombineSig}
         */
        $Object.defineProperties(Signer.prototype.muSig2CombineSig = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.muSig2CombineSig, $root.signrpc.MuSig2CombineSigRequest, $root.signrpc.MuSig2CombineSigResponse, request, callback);
        }, {
            name: { value: "MuSig2CombineSig" },
            path: { value: "/signrpc.Signer/MuSig2CombineSig" },
            requestType: { value: "MuSig2CombineSigRequest" },
            responseType: { value: "MuSig2CombineSigResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link signrpc.Signer#muSig2Cleanup}.
         * @memberof signrpc.Signer
         * @typedef MuSig2CleanupCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.MuSig2CleanupResponse} [response] MuSig2CleanupResponse
         */

        /**
         * Calls MuSig2Cleanup.
         * @memberof signrpc.Signer
         * @typedef MuSig2Cleanup
         * @type {{
         *   (request: signrpc.IMuSig2CleanupRequest, callback: signrpc.Signer.MuSig2CleanupCallback): void;
         *   (request: signrpc.IMuSig2CleanupRequest): Promise<signrpc.MuSig2CleanupResponse>;
         *   readonly name: "MuSig2Cleanup";
         *   readonly path: "/signrpc.Signer/MuSig2Cleanup";
         *   readonly requestType: "MuSig2CleanupRequest";
         *   readonly responseType: "MuSig2CleanupResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls MuSig2Cleanup.
         * @name signrpc.Signer#muSig2Cleanup
         * @type {signrpc.Signer.MuSig2Cleanup}
         */
        $Object.defineProperties(Signer.prototype.muSig2Cleanup = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, Signer.prototype.muSig2Cleanup, $root.signrpc.MuSig2CleanupRequest, $root.signrpc.MuSig2CleanupResponse, request, callback);
        }, {
            name: { value: "MuSig2Cleanup" },
            path: { value: "/signrpc.Signer/MuSig2Cleanup" },
            requestType: { value: "MuSig2CleanupRequest" },
            responseType: { value: "MuSig2CleanupResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return Signer;
    })();

    signrpc.KeyLocator = (function() {

        /**
         * Properties of a KeyLocator.
         * @typedef {Object} signrpc.KeyLocator.$Properties
         * @property {number|null} [key_family] KeyLocator key_family
         * @property {number|null} [key_index] KeyLocator key_index
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a KeyLocator.
         * @memberof signrpc
         * @interface IKeyLocator
         * @augments signrpc.KeyLocator.$Properties
         * @deprecated Use signrpc.KeyLocator.$Properties instead.
         */

        /**
         * Shape of a KeyLocator.
         * @typedef {signrpc.KeyLocator.$Properties} signrpc.KeyLocator.$Shape
         */

        /**
         * Constructs a new KeyLocator.
         * @memberof signrpc
         * @classdesc Represents a KeyLocator.
         * @constructor
         * @param {signrpc.KeyLocator.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const KeyLocator = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * KeyLocator key_family.
         * @member {number} key_family
         * @memberof signrpc.KeyLocator
         * @instance
         */
        KeyLocator.prototype.key_family = 0;

        /**
         * KeyLocator key_index.
         * @member {number} key_index
         * @memberof signrpc.KeyLocator
         * @instance
         */
        KeyLocator.prototype.key_index = 0;

        /**
         * Creates a new KeyLocator instance using the specified properties.
         * @function create
         * @memberof signrpc.KeyLocator
         * @static
         * @param {signrpc.KeyLocator.$Properties=} [properties] Properties to set
         * @returns {signrpc.KeyLocator} KeyLocator instance
         * @type {{
         *   (properties: signrpc.KeyLocator.$Shape): signrpc.KeyLocator & signrpc.KeyLocator.$Shape;
         *   (properties?: signrpc.KeyLocator.$Properties): signrpc.KeyLocator;
         * }}
         */
        KeyLocator.create = function(properties) {
            return new KeyLocator(properties);
        };

        /**
         * Encodes the specified KeyLocator message. Does not implicitly {@link signrpc.KeyLocator.verify|verify} messages.
         * @function encode
         * @memberof signrpc.KeyLocator
         * @static
         * @param {signrpc.KeyLocator.$Properties} message KeyLocator message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyLocator.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key_family != null && $Object.hasOwnProperty.call(message, "key_family") && message.key_family !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.key_family);
            if (message.key_index != null && $Object.hasOwnProperty.call(message, "key_index") && message.key_index !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.key_index);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified KeyLocator message, length delimited. Does not implicitly {@link signrpc.KeyLocator.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.KeyLocator
         * @static
         * @param {signrpc.KeyLocator.$Properties} message KeyLocator message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyLocator.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KeyLocator message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.KeyLocator
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.KeyLocator & signrpc.KeyLocator.$Shape} KeyLocator
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyLocator.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.KeyLocator(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.key_family = value;
                        else
                            delete message.key_family;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.key_index = value;
                        else
                            delete message.key_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a KeyLocator message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.KeyLocator
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.KeyLocator & signrpc.KeyLocator.$Shape} KeyLocator
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyLocator.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeyLocator message.
         * @function verify
         * @memberof signrpc.KeyLocator
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeyLocator.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key_family != null && $Object.hasOwnProperty.call(message, "key_family"))
                if (!$util.isInteger(message.key_family))
                    return "key_family: integer expected";
            if (message.key_index != null && $Object.hasOwnProperty.call(message, "key_index"))
                if (!$util.isInteger(message.key_index))
                    return "key_index: integer expected";
            return null;
        };

        /**
         * Creates a KeyLocator message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.KeyLocator
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.KeyLocator} KeyLocator
         */
        KeyLocator.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.KeyLocator)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.KeyLocator: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.KeyLocator();
            if (object.key_family != null)
                if ($Number(object.key_family) !== 0)
                    message.key_family = object.key_family | 0;
            if (object.key_index != null)
                if ($Number(object.key_index) !== 0)
                    message.key_index = object.key_index | 0;
            return message;
        };

        /**
         * Creates a plain object from a KeyLocator message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.KeyLocator
         * @static
         * @param {signrpc.KeyLocator} message KeyLocator
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeyLocator.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key_family = 0;
                object.key_index = 0;
            }
            if (message.key_family != null && $Object.hasOwnProperty.call(message, "key_family"))
                object.key_family = message.key_family;
            if (message.key_index != null && $Object.hasOwnProperty.call(message, "key_index"))
                object.key_index = message.key_index;
            return object;
        };

        /**
         * Converts this KeyLocator to JSON.
         * @function toJSON
         * @memberof signrpc.KeyLocator
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeyLocator.prototype.toJSON = function() {
            return KeyLocator.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for KeyLocator
         * @function getTypeUrl
         * @memberof signrpc.KeyLocator
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        KeyLocator.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.KeyLocator";
        };

        return KeyLocator;
    })();

    signrpc.KeyDescriptor = (function() {

        /**
         * Properties of a KeyDescriptor.
         * @typedef {Object} signrpc.KeyDescriptor.$Properties
         * @property {Uint8Array|null} [raw_key_bytes] KeyDescriptor raw_key_bytes
         * @property {signrpc.KeyLocator.$Properties|null} [key_loc] KeyDescriptor key_loc
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a KeyDescriptor.
         * @memberof signrpc
         * @interface IKeyDescriptor
         * @augments signrpc.KeyDescriptor.$Properties
         * @deprecated Use signrpc.KeyDescriptor.$Properties instead.
         */

        /**
         * Shape of a KeyDescriptor.
         * @typedef {signrpc.KeyDescriptor.$Properties} signrpc.KeyDescriptor.$Shape
         */

        /**
         * Constructs a new KeyDescriptor.
         * @memberof signrpc
         * @classdesc Represents a KeyDescriptor.
         * @constructor
         * @param {signrpc.KeyDescriptor.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const KeyDescriptor = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * KeyDescriptor raw_key_bytes.
         * @member {Uint8Array} raw_key_bytes
         * @memberof signrpc.KeyDescriptor
         * @instance
         */
        KeyDescriptor.prototype.raw_key_bytes = $util.newBuffer([]);

        /**
         * KeyDescriptor key_loc.
         * @member {signrpc.KeyLocator.$Properties|null|undefined} key_loc
         * @memberof signrpc.KeyDescriptor
         * @instance
         */
        KeyDescriptor.prototype.key_loc = null;

        /**
         * Creates a new KeyDescriptor instance using the specified properties.
         * @function create
         * @memberof signrpc.KeyDescriptor
         * @static
         * @param {signrpc.KeyDescriptor.$Properties=} [properties] Properties to set
         * @returns {signrpc.KeyDescriptor} KeyDescriptor instance
         * @type {{
         *   (properties: signrpc.KeyDescriptor.$Shape): signrpc.KeyDescriptor & signrpc.KeyDescriptor.$Shape;
         *   (properties?: signrpc.KeyDescriptor.$Properties): signrpc.KeyDescriptor;
         * }}
         */
        KeyDescriptor.create = function(properties) {
            return new KeyDescriptor(properties);
        };

        /**
         * Encodes the specified KeyDescriptor message. Does not implicitly {@link signrpc.KeyDescriptor.verify|verify} messages.
         * @function encode
         * @memberof signrpc.KeyDescriptor
         * @static
         * @param {signrpc.KeyDescriptor.$Properties} message KeyDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyDescriptor.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.raw_key_bytes != null && $Object.hasOwnProperty.call(message, "raw_key_bytes") && message.raw_key_bytes.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.raw_key_bytes);
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                $root.signrpc.KeyLocator.encode(message.key_loc, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified KeyDescriptor message, length delimited. Does not implicitly {@link signrpc.KeyDescriptor.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.KeyDescriptor
         * @static
         * @param {signrpc.KeyDescriptor.$Properties} message KeyDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyDescriptor.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KeyDescriptor message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.KeyDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.KeyDescriptor & signrpc.KeyDescriptor.$Shape} KeyDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyDescriptor.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.KeyDescriptor(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_key_bytes = value;
                        else
                            delete message.raw_key_bytes;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.key_loc = $root.signrpc.KeyLocator.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key_loc);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a KeyDescriptor message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.KeyDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.KeyDescriptor & signrpc.KeyDescriptor.$Shape} KeyDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyDescriptor.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeyDescriptor message.
         * @function verify
         * @memberof signrpc.KeyDescriptor
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeyDescriptor.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.raw_key_bytes != null && $Object.hasOwnProperty.call(message, "raw_key_bytes"))
                if (!(message.raw_key_bytes && typeof message.raw_key_bytes.length === "number" || $util.isString(message.raw_key_bytes)))
                    return "raw_key_bytes: buffer expected";
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc")) {
                let error = $root.signrpc.KeyLocator.verify(message.key_loc, _depth + 1);
                if (error)
                    return "key_loc." + error;
            }
            return null;
        };

        /**
         * Creates a KeyDescriptor message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.KeyDescriptor
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.KeyDescriptor} KeyDescriptor
         */
        KeyDescriptor.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.KeyDescriptor)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.KeyDescriptor: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.KeyDescriptor();
            if (object.raw_key_bytes != null)
                if (object.raw_key_bytes.length)
                    if (typeof object.raw_key_bytes === "string")
                        $util.base64.decode(object.raw_key_bytes, message.raw_key_bytes = $util.newBuffer($util.base64.length(object.raw_key_bytes)), 0);
                    else if (object.raw_key_bytes.length >= 0)
                        message.raw_key_bytes = object.raw_key_bytes;
            if (object.key_loc != null) {
                if (!$util.isObject(object.key_loc))
                    throw $TypeError(".signrpc.KeyDescriptor.key_loc: object expected");
                message.key_loc = $root.signrpc.KeyLocator.fromObject(object.key_loc, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a KeyDescriptor message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.KeyDescriptor
         * @static
         * @param {signrpc.KeyDescriptor} message KeyDescriptor
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeyDescriptor.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.raw_key_bytes = "";
                else {
                    object.raw_key_bytes = [];
                    if (options.bytes !== $Array)
                        object.raw_key_bytes = $util.newBuffer(object.raw_key_bytes);
                }
                object.key_loc = null;
            }
            if (message.raw_key_bytes != null && $Object.hasOwnProperty.call(message, "raw_key_bytes"))
                object.raw_key_bytes = options.bytes === $String ? $util.base64.encode(message.raw_key_bytes, 0, message.raw_key_bytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_key_bytes) : message.raw_key_bytes;
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                object.key_loc = $root.signrpc.KeyLocator.toObject(message.key_loc, options, _depth + 1);
            return object;
        };

        /**
         * Converts this KeyDescriptor to JSON.
         * @function toJSON
         * @memberof signrpc.KeyDescriptor
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeyDescriptor.prototype.toJSON = function() {
            return KeyDescriptor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for KeyDescriptor
         * @function getTypeUrl
         * @memberof signrpc.KeyDescriptor
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        KeyDescriptor.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.KeyDescriptor";
        };

        return KeyDescriptor;
    })();

    signrpc.TxOut = (function() {

        /**
         * Properties of a TxOut.
         * @typedef {Object} signrpc.TxOut.$Properties
         * @property {Long|null} [value] TxOut value
         * @property {Uint8Array|null} [pk_script] TxOut pk_script
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TxOut.
         * @memberof signrpc
         * @interface ITxOut
         * @augments signrpc.TxOut.$Properties
         * @deprecated Use signrpc.TxOut.$Properties instead.
         */

        /**
         * Shape of a TxOut.
         * @typedef {signrpc.TxOut.$Properties} signrpc.TxOut.$Shape
         */

        /**
         * Constructs a new TxOut.
         * @memberof signrpc
         * @classdesc Represents a TxOut.
         * @constructor
         * @param {signrpc.TxOut.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TxOut = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TxOut value.
         * @member {Long} value
         * @memberof signrpc.TxOut
         * @instance
         */
        TxOut.prototype.value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * TxOut pk_script.
         * @member {Uint8Array} pk_script
         * @memberof signrpc.TxOut
         * @instance
         */
        TxOut.prototype.pk_script = $util.newBuffer([]);

        /**
         * Creates a new TxOut instance using the specified properties.
         * @function create
         * @memberof signrpc.TxOut
         * @static
         * @param {signrpc.TxOut.$Properties=} [properties] Properties to set
         * @returns {signrpc.TxOut} TxOut instance
         * @type {{
         *   (properties: signrpc.TxOut.$Shape): signrpc.TxOut & signrpc.TxOut.$Shape;
         *   (properties?: signrpc.TxOut.$Properties): signrpc.TxOut;
         * }}
         */
        TxOut.create = function(properties) {
            return new TxOut(properties);
        };

        /**
         * Encodes the specified TxOut message. Does not implicitly {@link signrpc.TxOut.verify|verify} messages.
         * @function encode
         * @memberof signrpc.TxOut
         * @static
         * @param {signrpc.TxOut.$Properties} message TxOut message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxOut.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.value);
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script") && message.pk_script.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.pk_script);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TxOut message, length delimited. Does not implicitly {@link signrpc.TxOut.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.TxOut
         * @static
         * @param {signrpc.TxOut.$Properties} message TxOut message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxOut.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TxOut message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.TxOut
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.TxOut & signrpc.TxOut.$Shape} TxOut
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxOut.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.TxOut(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pk_script = value;
                        else
                            delete message.pk_script;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TxOut message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.TxOut
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.TxOut & signrpc.TxOut.$Shape} TxOut
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxOut.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TxOut message.
         * @function verify
         * @memberof signrpc.TxOut
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TxOut.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                    return "value: integer|Long expected";
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                if (!(message.pk_script && typeof message.pk_script.length === "number" || $util.isString(message.pk_script)))
                    return "pk_script: buffer expected";
            return null;
        };

        /**
         * Creates a TxOut message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.TxOut
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.TxOut} TxOut
         */
        TxOut.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.TxOut)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.TxOut: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.TxOut();
            if (object.value != null)
                if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                    if ($util.Long)
                        message.value = $util.Long.fromValue(object.value, false);
                    else if (typeof object.value === "string")
                        message.value = $parseInt(object.value, 10);
                    else if (typeof object.value === "number")
                        message.value = object.value;
                    else if (typeof object.value === "object")
                        message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber();
            if (object.pk_script != null)
                if (object.pk_script.length)
                    if (typeof object.pk_script === "string")
                        $util.base64.decode(object.pk_script, message.pk_script = $util.newBuffer($util.base64.length(object.pk_script)), 0);
                    else if (object.pk_script.length >= 0)
                        message.pk_script = object.pk_script;
            return message;
        };

        /**
         * Creates a plain object from a TxOut message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.TxOut
         * @static
         * @param {signrpc.TxOut} message TxOut
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TxOut.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.pk_script = "";
                else {
                    object.pk_script = [];
                    if (options.bytes !== $Array)
                        object.pk_script = $util.newBuffer(object.pk_script);
                }
            }
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, false).toBigInt();
                else if (typeof message.value === "number")
                    object.value = options.longs === $String ? $String(message.value) : message.value;
                else
                    object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber() : message.value;
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                object.pk_script = options.bytes === $String ? $util.base64.encode(message.pk_script, 0, message.pk_script.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pk_script) : message.pk_script;
            return object;
        };

        /**
         * Converts this TxOut to JSON.
         * @function toJSON
         * @memberof signrpc.TxOut
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TxOut.prototype.toJSON = function() {
            return TxOut.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TxOut
         * @function getTypeUrl
         * @memberof signrpc.TxOut
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TxOut.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.TxOut";
        };

        return TxOut;
    })();

    /**
     * SignMethod enum.
     * @name signrpc.SignMethod
     * @enum {number}
     * @property {number} SIGN_METHOD_WITNESS_V0=0 SIGN_METHOD_WITNESS_V0 value
     * @property {number} SIGN_METHOD_TAPROOT_KEY_SPEND_BIP0086=1 SIGN_METHOD_TAPROOT_KEY_SPEND_BIP0086 value
     * @property {number} SIGN_METHOD_TAPROOT_KEY_SPEND=2 SIGN_METHOD_TAPROOT_KEY_SPEND value
     * @property {number} SIGN_METHOD_TAPROOT_SCRIPT_SPEND=3 SIGN_METHOD_TAPROOT_SCRIPT_SPEND value
     */
    signrpc.SignMethod = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "SIGN_METHOD_WITNESS_V0"] = 0;
        values[valuesById[1] = "SIGN_METHOD_TAPROOT_KEY_SPEND_BIP0086"] = 1;
        values[valuesById[2] = "SIGN_METHOD_TAPROOT_KEY_SPEND"] = 2;
        values[valuesById[3] = "SIGN_METHOD_TAPROOT_SCRIPT_SPEND"] = 3;
        return values;
    })();

    signrpc.SignDescriptor = (function() {

        /**
         * Properties of a SignDescriptor.
         * @typedef {Object} signrpc.SignDescriptor.$Properties
         * @property {signrpc.KeyDescriptor.$Properties|null} [key_desc] SignDescriptor key_desc
         * @property {Uint8Array|null} [single_tweak] SignDescriptor single_tweak
         * @property {Uint8Array|null} [double_tweak] SignDescriptor double_tweak
         * @property {Uint8Array|null} [tap_tweak] SignDescriptor tap_tweak
         * @property {Uint8Array|null} [witness_script] SignDescriptor witness_script
         * @property {signrpc.TxOut.$Properties|null} [output] SignDescriptor output
         * @property {number|null} [sighash] SignDescriptor sighash
         * @property {number|null} [input_index] SignDescriptor input_index
         * @property {signrpc.SignMethod|null} [sign_method] SignDescriptor sign_method
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignDescriptor.
         * @memberof signrpc
         * @interface ISignDescriptor
         * @augments signrpc.SignDescriptor.$Properties
         * @deprecated Use signrpc.SignDescriptor.$Properties instead.
         */

        /**
         * Shape of a SignDescriptor.
         * @typedef {signrpc.SignDescriptor.$Properties} signrpc.SignDescriptor.$Shape
         */

        /**
         * Constructs a new SignDescriptor.
         * @memberof signrpc
         * @classdesc Represents a SignDescriptor.
         * @constructor
         * @param {signrpc.SignDescriptor.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignDescriptor = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignDescriptor key_desc.
         * @member {signrpc.KeyDescriptor.$Properties|null|undefined} key_desc
         * @memberof signrpc.SignDescriptor
         * @instance
         */
        SignDescriptor.prototype.key_desc = null;

        /**
         * SignDescriptor single_tweak.
         * @member {Uint8Array} single_tweak
         * @memberof signrpc.SignDescriptor
         * @instance
         */
        SignDescriptor.prototype.single_tweak = $util.newBuffer([]);

        /**
         * SignDescriptor double_tweak.
         * @member {Uint8Array} double_tweak
         * @memberof signrpc.SignDescriptor
         * @instance
         */
        SignDescriptor.prototype.double_tweak = $util.newBuffer([]);

        /**
         * SignDescriptor tap_tweak.
         * @member {Uint8Array} tap_tweak
         * @memberof signrpc.SignDescriptor
         * @instance
         */
        SignDescriptor.prototype.tap_tweak = $util.newBuffer([]);

        /**
         * SignDescriptor witness_script.
         * @member {Uint8Array} witness_script
         * @memberof signrpc.SignDescriptor
         * @instance
         */
        SignDescriptor.prototype.witness_script = $util.newBuffer([]);

        /**
         * SignDescriptor output.
         * @member {signrpc.TxOut.$Properties|null|undefined} output
         * @memberof signrpc.SignDescriptor
         * @instance
         */
        SignDescriptor.prototype.output = null;

        /**
         * SignDescriptor sighash.
         * @member {number} sighash
         * @memberof signrpc.SignDescriptor
         * @instance
         */
        SignDescriptor.prototype.sighash = 0;

        /**
         * SignDescriptor input_index.
         * @member {number} input_index
         * @memberof signrpc.SignDescriptor
         * @instance
         */
        SignDescriptor.prototype.input_index = 0;

        /**
         * SignDescriptor sign_method.
         * @member {signrpc.SignMethod} sign_method
         * @memberof signrpc.SignDescriptor
         * @instance
         */
        SignDescriptor.prototype.sign_method = 0;

        /**
         * Creates a new SignDescriptor instance using the specified properties.
         * @function create
         * @memberof signrpc.SignDescriptor
         * @static
         * @param {signrpc.SignDescriptor.$Properties=} [properties] Properties to set
         * @returns {signrpc.SignDescriptor} SignDescriptor instance
         * @type {{
         *   (properties: signrpc.SignDescriptor.$Shape): signrpc.SignDescriptor & signrpc.SignDescriptor.$Shape;
         *   (properties?: signrpc.SignDescriptor.$Properties): signrpc.SignDescriptor;
         * }}
         */
        SignDescriptor.create = function(properties) {
            return new SignDescriptor(properties);
        };

        /**
         * Encodes the specified SignDescriptor message. Does not implicitly {@link signrpc.SignDescriptor.verify|verify} messages.
         * @function encode
         * @memberof signrpc.SignDescriptor
         * @static
         * @param {signrpc.SignDescriptor.$Properties} message SignDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignDescriptor.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key_desc != null && $Object.hasOwnProperty.call(message, "key_desc"))
                $root.signrpc.KeyDescriptor.encode(message.key_desc, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.single_tweak != null && $Object.hasOwnProperty.call(message, "single_tweak") && message.single_tweak.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.single_tweak);
            if (message.double_tweak != null && $Object.hasOwnProperty.call(message, "double_tweak") && message.double_tweak.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.double_tweak);
            if (message.witness_script != null && $Object.hasOwnProperty.call(message, "witness_script") && message.witness_script.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.witness_script);
            if (message.output != null && $Object.hasOwnProperty.call(message, "output"))
                $root.signrpc.TxOut.encode(message.output, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.sighash != null && $Object.hasOwnProperty.call(message, "sighash") && message.sighash !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.sighash);
            if (message.input_index != null && $Object.hasOwnProperty.call(message, "input_index") && message.input_index !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.input_index);
            if (message.sign_method != null && $Object.hasOwnProperty.call(message, "sign_method") && message.sign_method !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.sign_method);
            if (message.tap_tweak != null && $Object.hasOwnProperty.call(message, "tap_tweak") && message.tap_tweak.length)
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.tap_tweak);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignDescriptor message, length delimited. Does not implicitly {@link signrpc.SignDescriptor.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.SignDescriptor
         * @static
         * @param {signrpc.SignDescriptor.$Properties} message SignDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignDescriptor.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignDescriptor message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.SignDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.SignDescriptor & signrpc.SignDescriptor.$Shape} SignDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignDescriptor.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.SignDescriptor(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.key_desc = $root.signrpc.KeyDescriptor.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key_desc);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.single_tweak = value;
                        else
                            delete message.single_tweak;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.double_tweak = value;
                        else
                            delete message.double_tweak;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.tap_tweak = value;
                        else
                            delete message.tap_tweak;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.witness_script = value;
                        else
                            delete message.witness_script;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.output = $root.signrpc.TxOut.decode(reader, reader.uint32(), $undefined, _depth + 1, message.output);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sighash = value;
                        else
                            delete message.sighash;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.input_index = value;
                        else
                            delete message.input_index;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.sign_method = value;
                        else
                            delete message.sign_method;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignDescriptor message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.SignDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.SignDescriptor & signrpc.SignDescriptor.$Shape} SignDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignDescriptor.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignDescriptor message.
         * @function verify
         * @memberof signrpc.SignDescriptor
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignDescriptor.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key_desc != null && $Object.hasOwnProperty.call(message, "key_desc")) {
                let error = $root.signrpc.KeyDescriptor.verify(message.key_desc, _depth + 1);
                if (error)
                    return "key_desc." + error;
            }
            if (message.single_tweak != null && $Object.hasOwnProperty.call(message, "single_tweak"))
                if (!(message.single_tweak && typeof message.single_tweak.length === "number" || $util.isString(message.single_tweak)))
                    return "single_tweak: buffer expected";
            if (message.double_tweak != null && $Object.hasOwnProperty.call(message, "double_tweak"))
                if (!(message.double_tweak && typeof message.double_tweak.length === "number" || $util.isString(message.double_tweak)))
                    return "double_tweak: buffer expected";
            if (message.tap_tweak != null && $Object.hasOwnProperty.call(message, "tap_tweak"))
                if (!(message.tap_tweak && typeof message.tap_tweak.length === "number" || $util.isString(message.tap_tweak)))
                    return "tap_tweak: buffer expected";
            if (message.witness_script != null && $Object.hasOwnProperty.call(message, "witness_script"))
                if (!(message.witness_script && typeof message.witness_script.length === "number" || $util.isString(message.witness_script)))
                    return "witness_script: buffer expected";
            if (message.output != null && $Object.hasOwnProperty.call(message, "output")) {
                let error = $root.signrpc.TxOut.verify(message.output, _depth + 1);
                if (error)
                    return "output." + error;
            }
            if (message.sighash != null && $Object.hasOwnProperty.call(message, "sighash"))
                if (!$util.isInteger(message.sighash))
                    return "sighash: integer expected";
            if (message.input_index != null && $Object.hasOwnProperty.call(message, "input_index"))
                if (!$util.isInteger(message.input_index))
                    return "input_index: integer expected";
            if (message.sign_method != null && $Object.hasOwnProperty.call(message, "sign_method"))
                if (typeof message.sign_method !== "number" || (message.sign_method | 0) !== message.sign_method)
                    return "sign_method: enum value expected";
            return null;
        };

        /**
         * Creates a SignDescriptor message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.SignDescriptor
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.SignDescriptor} SignDescriptor
         */
        SignDescriptor.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.SignDescriptor)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.SignDescriptor: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.SignDescriptor();
            if (object.key_desc != null) {
                if (!$util.isObject(object.key_desc))
                    throw $TypeError(".signrpc.SignDescriptor.key_desc: object expected");
                message.key_desc = $root.signrpc.KeyDescriptor.fromObject(object.key_desc, _depth + 1);
            }
            if (object.single_tweak != null)
                if (object.single_tweak.length)
                    if (typeof object.single_tweak === "string")
                        $util.base64.decode(object.single_tweak, message.single_tweak = $util.newBuffer($util.base64.length(object.single_tweak)), 0);
                    else if (object.single_tweak.length >= 0)
                        message.single_tweak = object.single_tweak;
            if (object.double_tweak != null)
                if (object.double_tweak.length)
                    if (typeof object.double_tweak === "string")
                        $util.base64.decode(object.double_tweak, message.double_tweak = $util.newBuffer($util.base64.length(object.double_tweak)), 0);
                    else if (object.double_tweak.length >= 0)
                        message.double_tweak = object.double_tweak;
            if (object.tap_tweak != null)
                if (object.tap_tweak.length)
                    if (typeof object.tap_tweak === "string")
                        $util.base64.decode(object.tap_tweak, message.tap_tweak = $util.newBuffer($util.base64.length(object.tap_tweak)), 0);
                    else if (object.tap_tweak.length >= 0)
                        message.tap_tweak = object.tap_tweak;
            if (object.witness_script != null)
                if (object.witness_script.length)
                    if (typeof object.witness_script === "string")
                        $util.base64.decode(object.witness_script, message.witness_script = $util.newBuffer($util.base64.length(object.witness_script)), 0);
                    else if (object.witness_script.length >= 0)
                        message.witness_script = object.witness_script;
            if (object.output != null) {
                if (!$util.isObject(object.output))
                    throw $TypeError(".signrpc.SignDescriptor.output: object expected");
                message.output = $root.signrpc.TxOut.fromObject(object.output, _depth + 1);
            }
            if (object.sighash != null)
                if ($Number(object.sighash) !== 0)
                    message.sighash = object.sighash >>> 0;
            if (object.input_index != null)
                if ($Number(object.input_index) !== 0)
                    message.input_index = object.input_index | 0;
            if (object.sign_method !== 0 && (typeof object.sign_method !== "string" || $root.signrpc.SignMethod[object.sign_method] !== 0))
                switch (object.sign_method) {
                case "SIGN_METHOD_WITNESS_V0":
                case 0:
                    message.sign_method = 0;
                    break;
                case "SIGN_METHOD_TAPROOT_KEY_SPEND_BIP0086":
                case 1:
                    message.sign_method = 1;
                    break;
                case "SIGN_METHOD_TAPROOT_KEY_SPEND":
                case 2:
                    message.sign_method = 2;
                    break;
                case "SIGN_METHOD_TAPROOT_SCRIPT_SPEND":
                case 3:
                    message.sign_method = 3;
                    break;
                default:
                    if (typeof object.sign_method === "number" && (object.sign_method | 0) === object.sign_method)
                        message.sign_method = object.sign_method;
                }
            return message;
        };

        /**
         * Creates a plain object from a SignDescriptor message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.SignDescriptor
         * @static
         * @param {signrpc.SignDescriptor} message SignDescriptor
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignDescriptor.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key_desc = null;
                if (options.bytes === $String)
                    object.single_tweak = "";
                else {
                    object.single_tweak = [];
                    if (options.bytes !== $Array)
                        object.single_tweak = $util.newBuffer(object.single_tweak);
                }
                if (options.bytes === $String)
                    object.double_tweak = "";
                else {
                    object.double_tweak = [];
                    if (options.bytes !== $Array)
                        object.double_tweak = $util.newBuffer(object.double_tweak);
                }
                if (options.bytes === $String)
                    object.witness_script = "";
                else {
                    object.witness_script = [];
                    if (options.bytes !== $Array)
                        object.witness_script = $util.newBuffer(object.witness_script);
                }
                object.output = null;
                object.sighash = 0;
                object.input_index = 0;
                object.sign_method = options.enums === $String ? "SIGN_METHOD_WITNESS_V0" : 0;
                if (options.bytes === $String)
                    object.tap_tweak = "";
                else {
                    object.tap_tweak = [];
                    if (options.bytes !== $Array)
                        object.tap_tweak = $util.newBuffer(object.tap_tweak);
                }
            }
            if (message.key_desc != null && $Object.hasOwnProperty.call(message, "key_desc"))
                object.key_desc = $root.signrpc.KeyDescriptor.toObject(message.key_desc, options, _depth + 1);
            if (message.single_tweak != null && $Object.hasOwnProperty.call(message, "single_tweak"))
                object.single_tweak = options.bytes === $String ? $util.base64.encode(message.single_tweak, 0, message.single_tweak.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.single_tweak) : message.single_tweak;
            if (message.double_tweak != null && $Object.hasOwnProperty.call(message, "double_tweak"))
                object.double_tweak = options.bytes === $String ? $util.base64.encode(message.double_tweak, 0, message.double_tweak.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.double_tweak) : message.double_tweak;
            if (message.witness_script != null && $Object.hasOwnProperty.call(message, "witness_script"))
                object.witness_script = options.bytes === $String ? $util.base64.encode(message.witness_script, 0, message.witness_script.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.witness_script) : message.witness_script;
            if (message.output != null && $Object.hasOwnProperty.call(message, "output"))
                object.output = $root.signrpc.TxOut.toObject(message.output, options, _depth + 1);
            if (message.sighash != null && $Object.hasOwnProperty.call(message, "sighash"))
                object.sighash = message.sighash;
            if (message.input_index != null && $Object.hasOwnProperty.call(message, "input_index"))
                object.input_index = message.input_index;
            if (message.sign_method != null && $Object.hasOwnProperty.call(message, "sign_method"))
                object.sign_method = options.enums === $String ? $root.signrpc.SignMethod[message.sign_method] === $undefined ? message.sign_method : $root.signrpc.SignMethod[message.sign_method] : message.sign_method;
            if (message.tap_tweak != null && $Object.hasOwnProperty.call(message, "tap_tweak"))
                object.tap_tweak = options.bytes === $String ? $util.base64.encode(message.tap_tweak, 0, message.tap_tweak.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.tap_tweak) : message.tap_tweak;
            return object;
        };

        /**
         * Converts this SignDescriptor to JSON.
         * @function toJSON
         * @memberof signrpc.SignDescriptor
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignDescriptor.prototype.toJSON = function() {
            return SignDescriptor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignDescriptor
         * @function getTypeUrl
         * @memberof signrpc.SignDescriptor
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignDescriptor.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.SignDescriptor";
        };

        return SignDescriptor;
    })();

    signrpc.SignReq = (function() {

        /**
         * Properties of a SignReq.
         * @typedef {Object} signrpc.SignReq.$Properties
         * @property {Uint8Array|null} [raw_tx_bytes] SignReq raw_tx_bytes
         * @property {Array.<signrpc.SignDescriptor.$Properties>|null} [sign_descs] SignReq sign_descs
         * @property {Array.<signrpc.TxOut.$Properties>|null} [prev_outputs] SignReq prev_outputs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignReq.
         * @memberof signrpc
         * @interface ISignReq
         * @augments signrpc.SignReq.$Properties
         * @deprecated Use signrpc.SignReq.$Properties instead.
         */

        /**
         * Shape of a SignReq.
         * @typedef {signrpc.SignReq.$Properties} signrpc.SignReq.$Shape
         */

        /**
         * Constructs a new SignReq.
         * @memberof signrpc
         * @classdesc Represents a SignReq.
         * @constructor
         * @param {signrpc.SignReq.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignReq = function (properties) {
            this.sign_descs = [];
            this.prev_outputs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignReq raw_tx_bytes.
         * @member {Uint8Array} raw_tx_bytes
         * @memberof signrpc.SignReq
         * @instance
         */
        SignReq.prototype.raw_tx_bytes = $util.newBuffer([]);

        /**
         * SignReq sign_descs.
         * @member {Array.<signrpc.SignDescriptor.$Properties>} sign_descs
         * @memberof signrpc.SignReq
         * @instance
         */
        SignReq.prototype.sign_descs = $util.emptyArray;

        /**
         * SignReq prev_outputs.
         * @member {Array.<signrpc.TxOut.$Properties>} prev_outputs
         * @memberof signrpc.SignReq
         * @instance
         */
        SignReq.prototype.prev_outputs = $util.emptyArray;

        /**
         * Creates a new SignReq instance using the specified properties.
         * @function create
         * @memberof signrpc.SignReq
         * @static
         * @param {signrpc.SignReq.$Properties=} [properties] Properties to set
         * @returns {signrpc.SignReq} SignReq instance
         * @type {{
         *   (properties: signrpc.SignReq.$Shape): signrpc.SignReq & signrpc.SignReq.$Shape;
         *   (properties?: signrpc.SignReq.$Properties): signrpc.SignReq;
         * }}
         */
        SignReq.create = function(properties) {
            return new SignReq(properties);
        };

        /**
         * Encodes the specified SignReq message. Does not implicitly {@link signrpc.SignReq.verify|verify} messages.
         * @function encode
         * @memberof signrpc.SignReq
         * @static
         * @param {signrpc.SignReq.$Properties} message SignReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignReq.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.raw_tx_bytes != null && $Object.hasOwnProperty.call(message, "raw_tx_bytes") && message.raw_tx_bytes.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.raw_tx_bytes);
            if (message.sign_descs != null && message.sign_descs.length)
                for (let i = 0; i < message.sign_descs.length; ++i)
                    $root.signrpc.SignDescriptor.encode(message.sign_descs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.prev_outputs != null && message.prev_outputs.length)
                for (let i = 0; i < message.prev_outputs.length; ++i)
                    $root.signrpc.TxOut.encode(message.prev_outputs[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignReq message, length delimited. Does not implicitly {@link signrpc.SignReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.SignReq
         * @static
         * @param {signrpc.SignReq.$Properties} message SignReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignReq.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignReq message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.SignReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.SignReq & signrpc.SignReq.$Shape} SignReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignReq.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.SignReq(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_tx_bytes = value;
                        else
                            delete message.raw_tx_bytes;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sign_descs && message.sign_descs.length))
                            message.sign_descs = [];
                        message.sign_descs.push($root.signrpc.SignDescriptor.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.prev_outputs && message.prev_outputs.length))
                            message.prev_outputs = [];
                        message.prev_outputs.push($root.signrpc.TxOut.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.SignReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.SignReq & signrpc.SignReq.$Shape} SignReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignReq.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignReq message.
         * @function verify
         * @memberof signrpc.SignReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignReq.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.raw_tx_bytes != null && $Object.hasOwnProperty.call(message, "raw_tx_bytes"))
                if (!(message.raw_tx_bytes && typeof message.raw_tx_bytes.length === "number" || $util.isString(message.raw_tx_bytes)))
                    return "raw_tx_bytes: buffer expected";
            if (message.sign_descs != null && $Object.hasOwnProperty.call(message, "sign_descs")) {
                if (!$Array.isArray(message.sign_descs))
                    return "sign_descs: array expected";
                for (let i = 0; i < message.sign_descs.length; ++i) {
                    let error = $root.signrpc.SignDescriptor.verify(message.sign_descs[i], _depth + 1);
                    if (error)
                        return "sign_descs." + error;
                }
            }
            if (message.prev_outputs != null && $Object.hasOwnProperty.call(message, "prev_outputs")) {
                if (!$Array.isArray(message.prev_outputs))
                    return "prev_outputs: array expected";
                for (let i = 0; i < message.prev_outputs.length; ++i) {
                    let error = $root.signrpc.TxOut.verify(message.prev_outputs[i], _depth + 1);
                    if (error)
                        return "prev_outputs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SignReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.SignReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.SignReq} SignReq
         */
        SignReq.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.SignReq)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.SignReq: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.SignReq();
            if (object.raw_tx_bytes != null)
                if (object.raw_tx_bytes.length)
                    if (typeof object.raw_tx_bytes === "string")
                        $util.base64.decode(object.raw_tx_bytes, message.raw_tx_bytes = $util.newBuffer($util.base64.length(object.raw_tx_bytes)), 0);
                    else if (object.raw_tx_bytes.length >= 0)
                        message.raw_tx_bytes = object.raw_tx_bytes;
            if (object.sign_descs) {
                if (!$Array.isArray(object.sign_descs))
                    throw $TypeError(".signrpc.SignReq.sign_descs: array expected");
                message.sign_descs = $Array(object.sign_descs.length);
                for (let i = 0; i < object.sign_descs.length; ++i) {
                    if (!$util.isObject(object.sign_descs[i]))
                        throw $TypeError(".signrpc.SignReq.sign_descs: object expected");
                    message.sign_descs[i] = $root.signrpc.SignDescriptor.fromObject(object.sign_descs[i], _depth + 1);
                }
            }
            if (object.prev_outputs) {
                if (!$Array.isArray(object.prev_outputs))
                    throw $TypeError(".signrpc.SignReq.prev_outputs: array expected");
                message.prev_outputs = $Array(object.prev_outputs.length);
                for (let i = 0; i < object.prev_outputs.length; ++i) {
                    if (!$util.isObject(object.prev_outputs[i]))
                        throw $TypeError(".signrpc.SignReq.prev_outputs: object expected");
                    message.prev_outputs[i] = $root.signrpc.TxOut.fromObject(object.prev_outputs[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SignReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.SignReq
         * @static
         * @param {signrpc.SignReq} message SignReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignReq.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.sign_descs = [];
                object.prev_outputs = [];
            }
            if (options.defaults)
                if (options.bytes === $String)
                    object.raw_tx_bytes = "";
                else {
                    object.raw_tx_bytes = [];
                    if (options.bytes !== $Array)
                        object.raw_tx_bytes = $util.newBuffer(object.raw_tx_bytes);
                }
            if (message.raw_tx_bytes != null && $Object.hasOwnProperty.call(message, "raw_tx_bytes"))
                object.raw_tx_bytes = options.bytes === $String ? $util.base64.encode(message.raw_tx_bytes, 0, message.raw_tx_bytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_tx_bytes) : message.raw_tx_bytes;
            if (message.sign_descs && message.sign_descs.length) {
                object.sign_descs = $Array(message.sign_descs.length);
                for (let j = 0; j < message.sign_descs.length; ++j)
                    object.sign_descs[j] = $root.signrpc.SignDescriptor.toObject(message.sign_descs[j], options, _depth + 1);
            }
            if (message.prev_outputs && message.prev_outputs.length) {
                object.prev_outputs = $Array(message.prev_outputs.length);
                for (let j = 0; j < message.prev_outputs.length; ++j)
                    object.prev_outputs[j] = $root.signrpc.TxOut.toObject(message.prev_outputs[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this SignReq to JSON.
         * @function toJSON
         * @memberof signrpc.SignReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignReq.prototype.toJSON = function() {
            return SignReq.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignReq
         * @function getTypeUrl
         * @memberof signrpc.SignReq
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignReq.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.SignReq";
        };

        return SignReq;
    })();

    signrpc.SignResp = (function() {

        /**
         * Properties of a SignResp.
         * @typedef {Object} signrpc.SignResp.$Properties
         * @property {Array.<Uint8Array>|null} [raw_sigs] SignResp raw_sigs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignResp.
         * @memberof signrpc
         * @interface ISignResp
         * @augments signrpc.SignResp.$Properties
         * @deprecated Use signrpc.SignResp.$Properties instead.
         */

        /**
         * Shape of a SignResp.
         * @typedef {signrpc.SignResp.$Properties} signrpc.SignResp.$Shape
         */

        /**
         * Constructs a new SignResp.
         * @memberof signrpc
         * @classdesc Represents a SignResp.
         * @constructor
         * @param {signrpc.SignResp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignResp = function (properties) {
            this.raw_sigs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignResp raw_sigs.
         * @member {Array.<Uint8Array>} raw_sigs
         * @memberof signrpc.SignResp
         * @instance
         */
        SignResp.prototype.raw_sigs = $util.emptyArray;

        /**
         * Creates a new SignResp instance using the specified properties.
         * @function create
         * @memberof signrpc.SignResp
         * @static
         * @param {signrpc.SignResp.$Properties=} [properties] Properties to set
         * @returns {signrpc.SignResp} SignResp instance
         * @type {{
         *   (properties: signrpc.SignResp.$Shape): signrpc.SignResp & signrpc.SignResp.$Shape;
         *   (properties?: signrpc.SignResp.$Properties): signrpc.SignResp;
         * }}
         */
        SignResp.create = function(properties) {
            return new SignResp(properties);
        };

        /**
         * Encodes the specified SignResp message. Does not implicitly {@link signrpc.SignResp.verify|verify} messages.
         * @function encode
         * @memberof signrpc.SignResp
         * @static
         * @param {signrpc.SignResp.$Properties} message SignResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignResp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.raw_sigs != null && message.raw_sigs.length)
                for (let i = 0; i < message.raw_sigs.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.raw_sigs[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignResp message, length delimited. Does not implicitly {@link signrpc.SignResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.SignResp
         * @static
         * @param {signrpc.SignResp.$Properties} message SignResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignResp.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignResp message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.SignResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.SignResp & signrpc.SignResp.$Shape} SignResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignResp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.SignResp();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.raw_sigs && message.raw_sigs.length))
                            message.raw_sigs = [];
                        message.raw_sigs.push(reader.bytes());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.SignResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.SignResp & signrpc.SignResp.$Shape} SignResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignResp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignResp message.
         * @function verify
         * @memberof signrpc.SignResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignResp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.raw_sigs != null && $Object.hasOwnProperty.call(message, "raw_sigs")) {
                if (!$Array.isArray(message.raw_sigs))
                    return "raw_sigs: array expected";
                for (let i = 0; i < message.raw_sigs.length; ++i)
                    if (!(message.raw_sigs[i] && typeof message.raw_sigs[i].length === "number" || $util.isString(message.raw_sigs[i])))
                        return "raw_sigs: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates a SignResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.SignResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.SignResp} SignResp
         */
        SignResp.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.SignResp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.SignResp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.SignResp();
            if (object.raw_sigs) {
                if (!$Array.isArray(object.raw_sigs))
                    throw $TypeError(".signrpc.SignResp.raw_sigs: array expected");
                message.raw_sigs = $Array(object.raw_sigs.length);
                for (let i = 0; i < object.raw_sigs.length; ++i)
                    if (typeof object.raw_sigs[i] === "string")
                        $util.base64.decode(object.raw_sigs[i], message.raw_sigs[i] = $util.newBuffer($util.base64.length(object.raw_sigs[i])), 0);
                    else if (object.raw_sigs[i].length >= 0)
                        message.raw_sigs[i] = object.raw_sigs[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a SignResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.SignResp
         * @static
         * @param {signrpc.SignResp} message SignResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignResp.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.raw_sigs = [];
            if (message.raw_sigs && message.raw_sigs.length) {
                object.raw_sigs = $Array(message.raw_sigs.length);
                for (let j = 0; j < message.raw_sigs.length; ++j)
                    object.raw_sigs[j] = options.bytes === $String ? $util.base64.encode(message.raw_sigs[j], 0, message.raw_sigs[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_sigs[j]) : message.raw_sigs[j];
            }
            return object;
        };

        /**
         * Converts this SignResp to JSON.
         * @function toJSON
         * @memberof signrpc.SignResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignResp.prototype.toJSON = function() {
            return SignResp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignResp
         * @function getTypeUrl
         * @memberof signrpc.SignResp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignResp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.SignResp";
        };

        return SignResp;
    })();

    signrpc.InputScript = (function() {

        /**
         * Properties of an InputScript.
         * @typedef {Object} signrpc.InputScript.$Properties
         * @property {Array.<Uint8Array>|null} [witness] InputScript witness
         * @property {Uint8Array|null} [sig_script] InputScript sig_script
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an InputScript.
         * @memberof signrpc
         * @interface IInputScript
         * @augments signrpc.InputScript.$Properties
         * @deprecated Use signrpc.InputScript.$Properties instead.
         */

        /**
         * Shape of an InputScript.
         * @typedef {signrpc.InputScript.$Properties} signrpc.InputScript.$Shape
         */

        /**
         * Constructs a new InputScript.
         * @memberof signrpc
         * @classdesc Represents an InputScript.
         * @constructor
         * @param {signrpc.InputScript.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const InputScript = function (properties) {
            this.witness = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InputScript witness.
         * @member {Array.<Uint8Array>} witness
         * @memberof signrpc.InputScript
         * @instance
         */
        InputScript.prototype.witness = $util.emptyArray;

        /**
         * InputScript sig_script.
         * @member {Uint8Array} sig_script
         * @memberof signrpc.InputScript
         * @instance
         */
        InputScript.prototype.sig_script = $util.newBuffer([]);

        /**
         * Creates a new InputScript instance using the specified properties.
         * @function create
         * @memberof signrpc.InputScript
         * @static
         * @param {signrpc.InputScript.$Properties=} [properties] Properties to set
         * @returns {signrpc.InputScript} InputScript instance
         * @type {{
         *   (properties: signrpc.InputScript.$Shape): signrpc.InputScript & signrpc.InputScript.$Shape;
         *   (properties?: signrpc.InputScript.$Properties): signrpc.InputScript;
         * }}
         */
        InputScript.create = function(properties) {
            return new InputScript(properties);
        };

        /**
         * Encodes the specified InputScript message. Does not implicitly {@link signrpc.InputScript.verify|verify} messages.
         * @function encode
         * @memberof signrpc.InputScript
         * @static
         * @param {signrpc.InputScript.$Properties} message InputScript message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InputScript.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.witness != null && message.witness.length)
                for (let i = 0; i < message.witness.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.witness[i]);
            if (message.sig_script != null && $Object.hasOwnProperty.call(message, "sig_script") && message.sig_script.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.sig_script);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InputScript message, length delimited. Does not implicitly {@link signrpc.InputScript.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.InputScript
         * @static
         * @param {signrpc.InputScript.$Properties} message InputScript message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InputScript.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InputScript message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.InputScript
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.InputScript & signrpc.InputScript.$Shape} InputScript
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InputScript.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.InputScript(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.witness && message.witness.length))
                            message.witness = [];
                        message.witness.push(reader.bytes());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.sig_script = value;
                        else
                            delete message.sig_script;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InputScript message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.InputScript
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.InputScript & signrpc.InputScript.$Shape} InputScript
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InputScript.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InputScript message.
         * @function verify
         * @memberof signrpc.InputScript
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InputScript.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.witness != null && $Object.hasOwnProperty.call(message, "witness")) {
                if (!$Array.isArray(message.witness))
                    return "witness: array expected";
                for (let i = 0; i < message.witness.length; ++i)
                    if (!(message.witness[i] && typeof message.witness[i].length === "number" || $util.isString(message.witness[i])))
                        return "witness: buffer[] expected";
            }
            if (message.sig_script != null && $Object.hasOwnProperty.call(message, "sig_script"))
                if (!(message.sig_script && typeof message.sig_script.length === "number" || $util.isString(message.sig_script)))
                    return "sig_script: buffer expected";
            return null;
        };

        /**
         * Creates an InputScript message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.InputScript
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.InputScript} InputScript
         */
        InputScript.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.InputScript)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.InputScript: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.InputScript();
            if (object.witness) {
                if (!$Array.isArray(object.witness))
                    throw $TypeError(".signrpc.InputScript.witness: array expected");
                message.witness = $Array(object.witness.length);
                for (let i = 0; i < object.witness.length; ++i)
                    if (typeof object.witness[i] === "string")
                        $util.base64.decode(object.witness[i], message.witness[i] = $util.newBuffer($util.base64.length(object.witness[i])), 0);
                    else if (object.witness[i].length >= 0)
                        message.witness[i] = object.witness[i];
            }
            if (object.sig_script != null)
                if (object.sig_script.length)
                    if (typeof object.sig_script === "string")
                        $util.base64.decode(object.sig_script, message.sig_script = $util.newBuffer($util.base64.length(object.sig_script)), 0);
                    else if (object.sig_script.length >= 0)
                        message.sig_script = object.sig_script;
            return message;
        };

        /**
         * Creates a plain object from an InputScript message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.InputScript
         * @static
         * @param {signrpc.InputScript} message InputScript
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InputScript.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.witness = [];
            if (options.defaults)
                if (options.bytes === $String)
                    object.sig_script = "";
                else {
                    object.sig_script = [];
                    if (options.bytes !== $Array)
                        object.sig_script = $util.newBuffer(object.sig_script);
                }
            if (message.witness && message.witness.length) {
                object.witness = $Array(message.witness.length);
                for (let j = 0; j < message.witness.length; ++j)
                    object.witness[j] = options.bytes === $String ? $util.base64.encode(message.witness[j], 0, message.witness[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.witness[j]) : message.witness[j];
            }
            if (message.sig_script != null && $Object.hasOwnProperty.call(message, "sig_script"))
                object.sig_script = options.bytes === $String ? $util.base64.encode(message.sig_script, 0, message.sig_script.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.sig_script) : message.sig_script;
            return object;
        };

        /**
         * Converts this InputScript to JSON.
         * @function toJSON
         * @memberof signrpc.InputScript
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InputScript.prototype.toJSON = function() {
            return InputScript.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InputScript
         * @function getTypeUrl
         * @memberof signrpc.InputScript
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InputScript.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.InputScript";
        };

        return InputScript;
    })();

    signrpc.InputScriptResp = (function() {

        /**
         * Properties of an InputScriptResp.
         * @typedef {Object} signrpc.InputScriptResp.$Properties
         * @property {Array.<signrpc.InputScript.$Properties>|null} [input_scripts] InputScriptResp input_scripts
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an InputScriptResp.
         * @memberof signrpc
         * @interface IInputScriptResp
         * @augments signrpc.InputScriptResp.$Properties
         * @deprecated Use signrpc.InputScriptResp.$Properties instead.
         */

        /**
         * Shape of an InputScriptResp.
         * @typedef {signrpc.InputScriptResp.$Properties} signrpc.InputScriptResp.$Shape
         */

        /**
         * Constructs a new InputScriptResp.
         * @memberof signrpc
         * @classdesc Represents an InputScriptResp.
         * @constructor
         * @param {signrpc.InputScriptResp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const InputScriptResp = function (properties) {
            this.input_scripts = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InputScriptResp input_scripts.
         * @member {Array.<signrpc.InputScript.$Properties>} input_scripts
         * @memberof signrpc.InputScriptResp
         * @instance
         */
        InputScriptResp.prototype.input_scripts = $util.emptyArray;

        /**
         * Creates a new InputScriptResp instance using the specified properties.
         * @function create
         * @memberof signrpc.InputScriptResp
         * @static
         * @param {signrpc.InputScriptResp.$Properties=} [properties] Properties to set
         * @returns {signrpc.InputScriptResp} InputScriptResp instance
         * @type {{
         *   (properties: signrpc.InputScriptResp.$Shape): signrpc.InputScriptResp & signrpc.InputScriptResp.$Shape;
         *   (properties?: signrpc.InputScriptResp.$Properties): signrpc.InputScriptResp;
         * }}
         */
        InputScriptResp.create = function(properties) {
            return new InputScriptResp(properties);
        };

        /**
         * Encodes the specified InputScriptResp message. Does not implicitly {@link signrpc.InputScriptResp.verify|verify} messages.
         * @function encode
         * @memberof signrpc.InputScriptResp
         * @static
         * @param {signrpc.InputScriptResp.$Properties} message InputScriptResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InputScriptResp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.input_scripts != null && message.input_scripts.length)
                for (let i = 0; i < message.input_scripts.length; ++i)
                    $root.signrpc.InputScript.encode(message.input_scripts[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InputScriptResp message, length delimited. Does not implicitly {@link signrpc.InputScriptResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.InputScriptResp
         * @static
         * @param {signrpc.InputScriptResp.$Properties} message InputScriptResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InputScriptResp.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InputScriptResp message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.InputScriptResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.InputScriptResp & signrpc.InputScriptResp.$Shape} InputScriptResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InputScriptResp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.InputScriptResp();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.input_scripts && message.input_scripts.length))
                            message.input_scripts = [];
                        message.input_scripts.push($root.signrpc.InputScript.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InputScriptResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.InputScriptResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.InputScriptResp & signrpc.InputScriptResp.$Shape} InputScriptResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InputScriptResp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InputScriptResp message.
         * @function verify
         * @memberof signrpc.InputScriptResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InputScriptResp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.input_scripts != null && $Object.hasOwnProperty.call(message, "input_scripts")) {
                if (!$Array.isArray(message.input_scripts))
                    return "input_scripts: array expected";
                for (let i = 0; i < message.input_scripts.length; ++i) {
                    let error = $root.signrpc.InputScript.verify(message.input_scripts[i], _depth + 1);
                    if (error)
                        return "input_scripts." + error;
                }
            }
            return null;
        };

        /**
         * Creates an InputScriptResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.InputScriptResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.InputScriptResp} InputScriptResp
         */
        InputScriptResp.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.InputScriptResp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.InputScriptResp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.InputScriptResp();
            if (object.input_scripts) {
                if (!$Array.isArray(object.input_scripts))
                    throw $TypeError(".signrpc.InputScriptResp.input_scripts: array expected");
                message.input_scripts = $Array(object.input_scripts.length);
                for (let i = 0; i < object.input_scripts.length; ++i) {
                    if (!$util.isObject(object.input_scripts[i]))
                        throw $TypeError(".signrpc.InputScriptResp.input_scripts: object expected");
                    message.input_scripts[i] = $root.signrpc.InputScript.fromObject(object.input_scripts[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an InputScriptResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.InputScriptResp
         * @static
         * @param {signrpc.InputScriptResp} message InputScriptResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InputScriptResp.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.input_scripts = [];
            if (message.input_scripts && message.input_scripts.length) {
                object.input_scripts = $Array(message.input_scripts.length);
                for (let j = 0; j < message.input_scripts.length; ++j)
                    object.input_scripts[j] = $root.signrpc.InputScript.toObject(message.input_scripts[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this InputScriptResp to JSON.
         * @function toJSON
         * @memberof signrpc.InputScriptResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InputScriptResp.prototype.toJSON = function() {
            return InputScriptResp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InputScriptResp
         * @function getTypeUrl
         * @memberof signrpc.InputScriptResp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InputScriptResp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.InputScriptResp";
        };

        return InputScriptResp;
    })();

    signrpc.SignMessageReq = (function() {

        /**
         * Properties of a SignMessageReq.
         * @typedef {Object} signrpc.SignMessageReq.$Properties
         * @property {Uint8Array|null} [msg] SignMessageReq msg
         * @property {signrpc.KeyLocator.$Properties|null} [key_loc] SignMessageReq key_loc
         * @property {boolean|null} [double_hash] SignMessageReq double_hash
         * @property {boolean|null} [compact_sig] SignMessageReq compact_sig
         * @property {boolean|null} [schnorr_sig] SignMessageReq schnorr_sig
         * @property {Uint8Array|null} [schnorr_sig_tap_tweak] SignMessageReq schnorr_sig_tap_tweak
         * @property {Uint8Array|null} [tag] SignMessageReq tag
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignMessageReq.
         * @memberof signrpc
         * @interface ISignMessageReq
         * @augments signrpc.SignMessageReq.$Properties
         * @deprecated Use signrpc.SignMessageReq.$Properties instead.
         */

        /**
         * Shape of a SignMessageReq.
         * @typedef {signrpc.SignMessageReq.$Properties} signrpc.SignMessageReq.$Shape
         */

        /**
         * Constructs a new SignMessageReq.
         * @memberof signrpc
         * @classdesc Represents a SignMessageReq.
         * @constructor
         * @param {signrpc.SignMessageReq.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignMessageReq = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignMessageReq msg.
         * @member {Uint8Array} msg
         * @memberof signrpc.SignMessageReq
         * @instance
         */
        SignMessageReq.prototype.msg = $util.newBuffer([]);

        /**
         * SignMessageReq key_loc.
         * @member {signrpc.KeyLocator.$Properties|null|undefined} key_loc
         * @memberof signrpc.SignMessageReq
         * @instance
         */
        SignMessageReq.prototype.key_loc = null;

        /**
         * SignMessageReq double_hash.
         * @member {boolean} double_hash
         * @memberof signrpc.SignMessageReq
         * @instance
         */
        SignMessageReq.prototype.double_hash = false;

        /**
         * SignMessageReq compact_sig.
         * @member {boolean} compact_sig
         * @memberof signrpc.SignMessageReq
         * @instance
         */
        SignMessageReq.prototype.compact_sig = false;

        /**
         * SignMessageReq schnorr_sig.
         * @member {boolean} schnorr_sig
         * @memberof signrpc.SignMessageReq
         * @instance
         */
        SignMessageReq.prototype.schnorr_sig = false;

        /**
         * SignMessageReq schnorr_sig_tap_tweak.
         * @member {Uint8Array} schnorr_sig_tap_tweak
         * @memberof signrpc.SignMessageReq
         * @instance
         */
        SignMessageReq.prototype.schnorr_sig_tap_tweak = $util.newBuffer([]);

        /**
         * SignMessageReq tag.
         * @member {Uint8Array} tag
         * @memberof signrpc.SignMessageReq
         * @instance
         */
        SignMessageReq.prototype.tag = $util.newBuffer([]);

        /**
         * Creates a new SignMessageReq instance using the specified properties.
         * @function create
         * @memberof signrpc.SignMessageReq
         * @static
         * @param {signrpc.SignMessageReq.$Properties=} [properties] Properties to set
         * @returns {signrpc.SignMessageReq} SignMessageReq instance
         * @type {{
         *   (properties: signrpc.SignMessageReq.$Shape): signrpc.SignMessageReq & signrpc.SignMessageReq.$Shape;
         *   (properties?: signrpc.SignMessageReq.$Properties): signrpc.SignMessageReq;
         * }}
         */
        SignMessageReq.create = function(properties) {
            return new SignMessageReq(properties);
        };

        /**
         * Encodes the specified SignMessageReq message. Does not implicitly {@link signrpc.SignMessageReq.verify|verify} messages.
         * @function encode
         * @memberof signrpc.SignMessageReq
         * @static
         * @param {signrpc.SignMessageReq.$Properties} message SignMessageReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageReq.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg") && message.msg.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.msg);
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                $root.signrpc.KeyLocator.encode(message.key_loc, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.double_hash != null && $Object.hasOwnProperty.call(message, "double_hash") && message.double_hash !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.double_hash);
            if (message.compact_sig != null && $Object.hasOwnProperty.call(message, "compact_sig") && message.compact_sig !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.compact_sig);
            if (message.schnorr_sig != null && $Object.hasOwnProperty.call(message, "schnorr_sig") && message.schnorr_sig !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.schnorr_sig);
            if (message.schnorr_sig_tap_tweak != null && $Object.hasOwnProperty.call(message, "schnorr_sig_tap_tweak") && message.schnorr_sig_tap_tweak.length)
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.schnorr_sig_tap_tweak);
            if (message.tag != null && $Object.hasOwnProperty.call(message, "tag") && message.tag.length)
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.tag);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignMessageReq message, length delimited. Does not implicitly {@link signrpc.SignMessageReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.SignMessageReq
         * @static
         * @param {signrpc.SignMessageReq.$Properties} message SignMessageReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageReq.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignMessageReq message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.SignMessageReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.SignMessageReq & signrpc.SignMessageReq.$Shape} SignMessageReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageReq.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.SignMessageReq(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.msg = value;
                        else
                            delete message.msg;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.key_loc = $root.signrpc.KeyLocator.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key_loc);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.double_hash = value;
                        else
                            delete message.double_hash;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.compact_sig = value;
                        else
                            delete message.compact_sig;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.schnorr_sig = value;
                        else
                            delete message.schnorr_sig;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.schnorr_sig_tap_tweak = value;
                        else
                            delete message.schnorr_sig_tap_tweak;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.tag = value;
                        else
                            delete message.tag;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignMessageReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.SignMessageReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.SignMessageReq & signrpc.SignMessageReq.$Shape} SignMessageReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageReq.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignMessageReq message.
         * @function verify
         * @memberof signrpc.SignMessageReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignMessageReq.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                if (!(message.msg && typeof message.msg.length === "number" || $util.isString(message.msg)))
                    return "msg: buffer expected";
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc")) {
                let error = $root.signrpc.KeyLocator.verify(message.key_loc, _depth + 1);
                if (error)
                    return "key_loc." + error;
            }
            if (message.double_hash != null && $Object.hasOwnProperty.call(message, "double_hash"))
                if (typeof message.double_hash !== "boolean")
                    return "double_hash: boolean expected";
            if (message.compact_sig != null && $Object.hasOwnProperty.call(message, "compact_sig"))
                if (typeof message.compact_sig !== "boolean")
                    return "compact_sig: boolean expected";
            if (message.schnorr_sig != null && $Object.hasOwnProperty.call(message, "schnorr_sig"))
                if (typeof message.schnorr_sig !== "boolean")
                    return "schnorr_sig: boolean expected";
            if (message.schnorr_sig_tap_tweak != null && $Object.hasOwnProperty.call(message, "schnorr_sig_tap_tweak"))
                if (!(message.schnorr_sig_tap_tweak && typeof message.schnorr_sig_tap_tweak.length === "number" || $util.isString(message.schnorr_sig_tap_tweak)))
                    return "schnorr_sig_tap_tweak: buffer expected";
            if (message.tag != null && $Object.hasOwnProperty.call(message, "tag"))
                if (!(message.tag && typeof message.tag.length === "number" || $util.isString(message.tag)))
                    return "tag: buffer expected";
            return null;
        };

        /**
         * Creates a SignMessageReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.SignMessageReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.SignMessageReq} SignMessageReq
         */
        SignMessageReq.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.SignMessageReq)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.SignMessageReq: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.SignMessageReq();
            if (object.msg != null)
                if (object.msg.length)
                    if (typeof object.msg === "string")
                        $util.base64.decode(object.msg, message.msg = $util.newBuffer($util.base64.length(object.msg)), 0);
                    else if (object.msg.length >= 0)
                        message.msg = object.msg;
            if (object.key_loc != null) {
                if (!$util.isObject(object.key_loc))
                    throw $TypeError(".signrpc.SignMessageReq.key_loc: object expected");
                message.key_loc = $root.signrpc.KeyLocator.fromObject(object.key_loc, _depth + 1);
            }
            if (object.double_hash != null)
                if (object.double_hash)
                    message.double_hash = $Boolean(object.double_hash);
            if (object.compact_sig != null)
                if (object.compact_sig)
                    message.compact_sig = $Boolean(object.compact_sig);
            if (object.schnorr_sig != null)
                if (object.schnorr_sig)
                    message.schnorr_sig = $Boolean(object.schnorr_sig);
            if (object.schnorr_sig_tap_tweak != null)
                if (object.schnorr_sig_tap_tweak.length)
                    if (typeof object.schnorr_sig_tap_tweak === "string")
                        $util.base64.decode(object.schnorr_sig_tap_tweak, message.schnorr_sig_tap_tweak = $util.newBuffer($util.base64.length(object.schnorr_sig_tap_tweak)), 0);
                    else if (object.schnorr_sig_tap_tweak.length >= 0)
                        message.schnorr_sig_tap_tweak = object.schnorr_sig_tap_tweak;
            if (object.tag != null)
                if (object.tag.length)
                    if (typeof object.tag === "string")
                        $util.base64.decode(object.tag, message.tag = $util.newBuffer($util.base64.length(object.tag)), 0);
                    else if (object.tag.length >= 0)
                        message.tag = object.tag;
            return message;
        };

        /**
         * Creates a plain object from a SignMessageReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.SignMessageReq
         * @static
         * @param {signrpc.SignMessageReq} message SignMessageReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignMessageReq.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.msg = "";
                else {
                    object.msg = [];
                    if (options.bytes !== $Array)
                        object.msg = $util.newBuffer(object.msg);
                }
                object.key_loc = null;
                object.double_hash = false;
                object.compact_sig = false;
                object.schnorr_sig = false;
                if (options.bytes === $String)
                    object.schnorr_sig_tap_tweak = "";
                else {
                    object.schnorr_sig_tap_tweak = [];
                    if (options.bytes !== $Array)
                        object.schnorr_sig_tap_tweak = $util.newBuffer(object.schnorr_sig_tap_tweak);
                }
                if (options.bytes === $String)
                    object.tag = "";
                else {
                    object.tag = [];
                    if (options.bytes !== $Array)
                        object.tag = $util.newBuffer(object.tag);
                }
            }
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                object.msg = options.bytes === $String ? $util.base64.encode(message.msg, 0, message.msg.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.msg) : message.msg;
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                object.key_loc = $root.signrpc.KeyLocator.toObject(message.key_loc, options, _depth + 1);
            if (message.double_hash != null && $Object.hasOwnProperty.call(message, "double_hash"))
                object.double_hash = message.double_hash;
            if (message.compact_sig != null && $Object.hasOwnProperty.call(message, "compact_sig"))
                object.compact_sig = message.compact_sig;
            if (message.schnorr_sig != null && $Object.hasOwnProperty.call(message, "schnorr_sig"))
                object.schnorr_sig = message.schnorr_sig;
            if (message.schnorr_sig_tap_tweak != null && $Object.hasOwnProperty.call(message, "schnorr_sig_tap_tweak"))
                object.schnorr_sig_tap_tweak = options.bytes === $String ? $util.base64.encode(message.schnorr_sig_tap_tweak, 0, message.schnorr_sig_tap_tweak.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.schnorr_sig_tap_tweak) : message.schnorr_sig_tap_tweak;
            if (message.tag != null && $Object.hasOwnProperty.call(message, "tag"))
                object.tag = options.bytes === $String ? $util.base64.encode(message.tag, 0, message.tag.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.tag) : message.tag;
            return object;
        };

        /**
         * Converts this SignMessageReq to JSON.
         * @function toJSON
         * @memberof signrpc.SignMessageReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignMessageReq.prototype.toJSON = function() {
            return SignMessageReq.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignMessageReq
         * @function getTypeUrl
         * @memberof signrpc.SignMessageReq
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignMessageReq.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.SignMessageReq";
        };

        return SignMessageReq;
    })();

    signrpc.SignMessageResp = (function() {

        /**
         * Properties of a SignMessageResp.
         * @typedef {Object} signrpc.SignMessageResp.$Properties
         * @property {Uint8Array|null} [signature] SignMessageResp signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignMessageResp.
         * @memberof signrpc
         * @interface ISignMessageResp
         * @augments signrpc.SignMessageResp.$Properties
         * @deprecated Use signrpc.SignMessageResp.$Properties instead.
         */

        /**
         * Shape of a SignMessageResp.
         * @typedef {signrpc.SignMessageResp.$Properties} signrpc.SignMessageResp.$Shape
         */

        /**
         * Constructs a new SignMessageResp.
         * @memberof signrpc
         * @classdesc Represents a SignMessageResp.
         * @constructor
         * @param {signrpc.SignMessageResp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignMessageResp = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignMessageResp signature.
         * @member {Uint8Array} signature
         * @memberof signrpc.SignMessageResp
         * @instance
         */
        SignMessageResp.prototype.signature = $util.newBuffer([]);

        /**
         * Creates a new SignMessageResp instance using the specified properties.
         * @function create
         * @memberof signrpc.SignMessageResp
         * @static
         * @param {signrpc.SignMessageResp.$Properties=} [properties] Properties to set
         * @returns {signrpc.SignMessageResp} SignMessageResp instance
         * @type {{
         *   (properties: signrpc.SignMessageResp.$Shape): signrpc.SignMessageResp & signrpc.SignMessageResp.$Shape;
         *   (properties?: signrpc.SignMessageResp.$Properties): signrpc.SignMessageResp;
         * }}
         */
        SignMessageResp.create = function(properties) {
            return new SignMessageResp(properties);
        };

        /**
         * Encodes the specified SignMessageResp message. Does not implicitly {@link signrpc.SignMessageResp.verify|verify} messages.
         * @function encode
         * @memberof signrpc.SignMessageResp
         * @static
         * @param {signrpc.SignMessageResp.$Properties} message SignMessageResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageResp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.signature);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignMessageResp message, length delimited. Does not implicitly {@link signrpc.SignMessageResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.SignMessageResp
         * @static
         * @param {signrpc.SignMessageResp.$Properties} message SignMessageResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageResp.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignMessageResp message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.SignMessageResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.SignMessageResp & signrpc.SignMessageResp.$Shape} SignMessageResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageResp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.SignMessageResp(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignMessageResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.SignMessageResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.SignMessageResp & signrpc.SignMessageResp.$Shape} SignMessageResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageResp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignMessageResp message.
         * @function verify
         * @memberof signrpc.SignMessageResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignMessageResp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            return null;
        };

        /**
         * Creates a SignMessageResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.SignMessageResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.SignMessageResp} SignMessageResp
         */
        SignMessageResp.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.SignMessageResp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.SignMessageResp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.SignMessageResp();
            if (object.signature != null)
                if (object.signature.length)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
            return message;
        };

        /**
         * Creates a plain object from a SignMessageResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.SignMessageResp
         * @static
         * @param {signrpc.SignMessageResp} message SignMessageResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignMessageResp.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== $Array)
                        object.signature = $util.newBuffer(object.signature);
                }
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                object.signature = options.bytes === $String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signature) : message.signature;
            return object;
        };

        /**
         * Converts this SignMessageResp to JSON.
         * @function toJSON
         * @memberof signrpc.SignMessageResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignMessageResp.prototype.toJSON = function() {
            return SignMessageResp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignMessageResp
         * @function getTypeUrl
         * @memberof signrpc.SignMessageResp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignMessageResp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.SignMessageResp";
        };

        return SignMessageResp;
    })();

    signrpc.VerifyMessageReq = (function() {

        /**
         * Properties of a VerifyMessageReq.
         * @typedef {Object} signrpc.VerifyMessageReq.$Properties
         * @property {Uint8Array|null} [msg] VerifyMessageReq msg
         * @property {Uint8Array|null} [signature] VerifyMessageReq signature
         * @property {Uint8Array|null} [pubkey] VerifyMessageReq pubkey
         * @property {boolean|null} [is_schnorr_sig] VerifyMessageReq is_schnorr_sig
         * @property {Uint8Array|null} [tag] VerifyMessageReq tag
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a VerifyMessageReq.
         * @memberof signrpc
         * @interface IVerifyMessageReq
         * @augments signrpc.VerifyMessageReq.$Properties
         * @deprecated Use signrpc.VerifyMessageReq.$Properties instead.
         */

        /**
         * Shape of a VerifyMessageReq.
         * @typedef {signrpc.VerifyMessageReq.$Properties} signrpc.VerifyMessageReq.$Shape
         */

        /**
         * Constructs a new VerifyMessageReq.
         * @memberof signrpc
         * @classdesc Represents a VerifyMessageReq.
         * @constructor
         * @param {signrpc.VerifyMessageReq.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const VerifyMessageReq = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * VerifyMessageReq msg.
         * @member {Uint8Array} msg
         * @memberof signrpc.VerifyMessageReq
         * @instance
         */
        VerifyMessageReq.prototype.msg = $util.newBuffer([]);

        /**
         * VerifyMessageReq signature.
         * @member {Uint8Array} signature
         * @memberof signrpc.VerifyMessageReq
         * @instance
         */
        VerifyMessageReq.prototype.signature = $util.newBuffer([]);

        /**
         * VerifyMessageReq pubkey.
         * @member {Uint8Array} pubkey
         * @memberof signrpc.VerifyMessageReq
         * @instance
         */
        VerifyMessageReq.prototype.pubkey = $util.newBuffer([]);

        /**
         * VerifyMessageReq is_schnorr_sig.
         * @member {boolean} is_schnorr_sig
         * @memberof signrpc.VerifyMessageReq
         * @instance
         */
        VerifyMessageReq.prototype.is_schnorr_sig = false;

        /**
         * VerifyMessageReq tag.
         * @member {Uint8Array} tag
         * @memberof signrpc.VerifyMessageReq
         * @instance
         */
        VerifyMessageReq.prototype.tag = $util.newBuffer([]);

        /**
         * Creates a new VerifyMessageReq instance using the specified properties.
         * @function create
         * @memberof signrpc.VerifyMessageReq
         * @static
         * @param {signrpc.VerifyMessageReq.$Properties=} [properties] Properties to set
         * @returns {signrpc.VerifyMessageReq} VerifyMessageReq instance
         * @type {{
         *   (properties: signrpc.VerifyMessageReq.$Shape): signrpc.VerifyMessageReq & signrpc.VerifyMessageReq.$Shape;
         *   (properties?: signrpc.VerifyMessageReq.$Properties): signrpc.VerifyMessageReq;
         * }}
         */
        VerifyMessageReq.create = function(properties) {
            return new VerifyMessageReq(properties);
        };

        /**
         * Encodes the specified VerifyMessageReq message. Does not implicitly {@link signrpc.VerifyMessageReq.verify|verify} messages.
         * @function encode
         * @memberof signrpc.VerifyMessageReq
         * @static
         * @param {signrpc.VerifyMessageReq.$Properties} message VerifyMessageReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageReq.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg") && message.msg.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.msg);
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.signature);
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey") && message.pubkey.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.pubkey);
            if (message.is_schnorr_sig != null && $Object.hasOwnProperty.call(message, "is_schnorr_sig") && message.is_schnorr_sig !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.is_schnorr_sig);
            if (message.tag != null && $Object.hasOwnProperty.call(message, "tag") && message.tag.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.tag);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VerifyMessageReq message, length delimited. Does not implicitly {@link signrpc.VerifyMessageReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.VerifyMessageReq
         * @static
         * @param {signrpc.VerifyMessageReq.$Properties} message VerifyMessageReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageReq.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VerifyMessageReq message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.VerifyMessageReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.VerifyMessageReq & signrpc.VerifyMessageReq.$Shape} VerifyMessageReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageReq.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.VerifyMessageReq(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.msg = value;
                        else
                            delete message.msg;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pubkey = value;
                        else
                            delete message.pubkey;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_schnorr_sig = value;
                        else
                            delete message.is_schnorr_sig;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.tag = value;
                        else
                            delete message.tag;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a VerifyMessageReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.VerifyMessageReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.VerifyMessageReq & signrpc.VerifyMessageReq.$Shape} VerifyMessageReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageReq.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VerifyMessageReq message.
         * @function verify
         * @memberof signrpc.VerifyMessageReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VerifyMessageReq.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                if (!(message.msg && typeof message.msg.length === "number" || $util.isString(message.msg)))
                    return "msg: buffer expected";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                if (!(message.pubkey && typeof message.pubkey.length === "number" || $util.isString(message.pubkey)))
                    return "pubkey: buffer expected";
            if (message.is_schnorr_sig != null && $Object.hasOwnProperty.call(message, "is_schnorr_sig"))
                if (typeof message.is_schnorr_sig !== "boolean")
                    return "is_schnorr_sig: boolean expected";
            if (message.tag != null && $Object.hasOwnProperty.call(message, "tag"))
                if (!(message.tag && typeof message.tag.length === "number" || $util.isString(message.tag)))
                    return "tag: buffer expected";
            return null;
        };

        /**
         * Creates a VerifyMessageReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.VerifyMessageReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.VerifyMessageReq} VerifyMessageReq
         */
        VerifyMessageReq.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.VerifyMessageReq)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.VerifyMessageReq: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.VerifyMessageReq();
            if (object.msg != null)
                if (object.msg.length)
                    if (typeof object.msg === "string")
                        $util.base64.decode(object.msg, message.msg = $util.newBuffer($util.base64.length(object.msg)), 0);
                    else if (object.msg.length >= 0)
                        message.msg = object.msg;
            if (object.signature != null)
                if (object.signature.length)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
            if (object.pubkey != null)
                if (object.pubkey.length)
                    if (typeof object.pubkey === "string")
                        $util.base64.decode(object.pubkey, message.pubkey = $util.newBuffer($util.base64.length(object.pubkey)), 0);
                    else if (object.pubkey.length >= 0)
                        message.pubkey = object.pubkey;
            if (object.is_schnorr_sig != null)
                if (object.is_schnorr_sig)
                    message.is_schnorr_sig = $Boolean(object.is_schnorr_sig);
            if (object.tag != null)
                if (object.tag.length)
                    if (typeof object.tag === "string")
                        $util.base64.decode(object.tag, message.tag = $util.newBuffer($util.base64.length(object.tag)), 0);
                    else if (object.tag.length >= 0)
                        message.tag = object.tag;
            return message;
        };

        /**
         * Creates a plain object from a VerifyMessageReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.VerifyMessageReq
         * @static
         * @param {signrpc.VerifyMessageReq} message VerifyMessageReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VerifyMessageReq.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.msg = "";
                else {
                    object.msg = [];
                    if (options.bytes !== $Array)
                        object.msg = $util.newBuffer(object.msg);
                }
                if (options.bytes === $String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== $Array)
                        object.signature = $util.newBuffer(object.signature);
                }
                if (options.bytes === $String)
                    object.pubkey = "";
                else {
                    object.pubkey = [];
                    if (options.bytes !== $Array)
                        object.pubkey = $util.newBuffer(object.pubkey);
                }
                object.is_schnorr_sig = false;
                if (options.bytes === $String)
                    object.tag = "";
                else {
                    object.tag = [];
                    if (options.bytes !== $Array)
                        object.tag = $util.newBuffer(object.tag);
                }
            }
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                object.msg = options.bytes === $String ? $util.base64.encode(message.msg, 0, message.msg.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.msg) : message.msg;
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                object.signature = options.bytes === $String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signature) : message.signature;
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                object.pubkey = options.bytes === $String ? $util.base64.encode(message.pubkey, 0, message.pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pubkey) : message.pubkey;
            if (message.is_schnorr_sig != null && $Object.hasOwnProperty.call(message, "is_schnorr_sig"))
                object.is_schnorr_sig = message.is_schnorr_sig;
            if (message.tag != null && $Object.hasOwnProperty.call(message, "tag"))
                object.tag = options.bytes === $String ? $util.base64.encode(message.tag, 0, message.tag.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.tag) : message.tag;
            return object;
        };

        /**
         * Converts this VerifyMessageReq to JSON.
         * @function toJSON
         * @memberof signrpc.VerifyMessageReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VerifyMessageReq.prototype.toJSON = function() {
            return VerifyMessageReq.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for VerifyMessageReq
         * @function getTypeUrl
         * @memberof signrpc.VerifyMessageReq
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        VerifyMessageReq.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.VerifyMessageReq";
        };

        return VerifyMessageReq;
    })();

    signrpc.VerifyMessageResp = (function() {

        /**
         * Properties of a VerifyMessageResp.
         * @typedef {Object} signrpc.VerifyMessageResp.$Properties
         * @property {boolean|null} [valid] VerifyMessageResp valid
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a VerifyMessageResp.
         * @memberof signrpc
         * @interface IVerifyMessageResp
         * @augments signrpc.VerifyMessageResp.$Properties
         * @deprecated Use signrpc.VerifyMessageResp.$Properties instead.
         */

        /**
         * Shape of a VerifyMessageResp.
         * @typedef {signrpc.VerifyMessageResp.$Properties} signrpc.VerifyMessageResp.$Shape
         */

        /**
         * Constructs a new VerifyMessageResp.
         * @memberof signrpc
         * @classdesc Represents a VerifyMessageResp.
         * @constructor
         * @param {signrpc.VerifyMessageResp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const VerifyMessageResp = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * VerifyMessageResp valid.
         * @member {boolean} valid
         * @memberof signrpc.VerifyMessageResp
         * @instance
         */
        VerifyMessageResp.prototype.valid = false;

        /**
         * Creates a new VerifyMessageResp instance using the specified properties.
         * @function create
         * @memberof signrpc.VerifyMessageResp
         * @static
         * @param {signrpc.VerifyMessageResp.$Properties=} [properties] Properties to set
         * @returns {signrpc.VerifyMessageResp} VerifyMessageResp instance
         * @type {{
         *   (properties: signrpc.VerifyMessageResp.$Shape): signrpc.VerifyMessageResp & signrpc.VerifyMessageResp.$Shape;
         *   (properties?: signrpc.VerifyMessageResp.$Properties): signrpc.VerifyMessageResp;
         * }}
         */
        VerifyMessageResp.create = function(properties) {
            return new VerifyMessageResp(properties);
        };

        /**
         * Encodes the specified VerifyMessageResp message. Does not implicitly {@link signrpc.VerifyMessageResp.verify|verify} messages.
         * @function encode
         * @memberof signrpc.VerifyMessageResp
         * @static
         * @param {signrpc.VerifyMessageResp.$Properties} message VerifyMessageResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageResp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid") && message.valid !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.valid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VerifyMessageResp message, length delimited. Does not implicitly {@link signrpc.VerifyMessageResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.VerifyMessageResp
         * @static
         * @param {signrpc.VerifyMessageResp.$Properties} message VerifyMessageResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageResp.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VerifyMessageResp message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.VerifyMessageResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.VerifyMessageResp & signrpc.VerifyMessageResp.$Shape} VerifyMessageResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageResp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.VerifyMessageResp(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.valid = value;
                        else
                            delete message.valid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a VerifyMessageResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.VerifyMessageResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.VerifyMessageResp & signrpc.VerifyMessageResp.$Shape} VerifyMessageResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageResp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VerifyMessageResp message.
         * @function verify
         * @memberof signrpc.VerifyMessageResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VerifyMessageResp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid"))
                if (typeof message.valid !== "boolean")
                    return "valid: boolean expected";
            return null;
        };

        /**
         * Creates a VerifyMessageResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.VerifyMessageResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.VerifyMessageResp} VerifyMessageResp
         */
        VerifyMessageResp.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.VerifyMessageResp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.VerifyMessageResp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.VerifyMessageResp();
            if (object.valid != null)
                if (object.valid)
                    message.valid = $Boolean(object.valid);
            return message;
        };

        /**
         * Creates a plain object from a VerifyMessageResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.VerifyMessageResp
         * @static
         * @param {signrpc.VerifyMessageResp} message VerifyMessageResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VerifyMessageResp.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.valid = false;
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid"))
                object.valid = message.valid;
            return object;
        };

        /**
         * Converts this VerifyMessageResp to JSON.
         * @function toJSON
         * @memberof signrpc.VerifyMessageResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VerifyMessageResp.prototype.toJSON = function() {
            return VerifyMessageResp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for VerifyMessageResp
         * @function getTypeUrl
         * @memberof signrpc.VerifyMessageResp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        VerifyMessageResp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.VerifyMessageResp";
        };

        return VerifyMessageResp;
    })();

    signrpc.SharedKeyRequest = (function() {

        /**
         * Properties of a SharedKeyRequest.
         * @typedef {Object} signrpc.SharedKeyRequest.$Properties
         * @property {Uint8Array|null} [ephemeral_pubkey] SharedKeyRequest ephemeral_pubkey
         * @property {signrpc.KeyLocator.$Properties|null} [key_loc] SharedKeyRequest key_loc
         * @property {signrpc.KeyDescriptor.$Properties|null} [key_desc] SharedKeyRequest key_desc
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SharedKeyRequest.
         * @memberof signrpc
         * @interface ISharedKeyRequest
         * @augments signrpc.SharedKeyRequest.$Properties
         * @deprecated Use signrpc.SharedKeyRequest.$Properties instead.
         */

        /**
         * Shape of a SharedKeyRequest.
         * @typedef {signrpc.SharedKeyRequest.$Properties} signrpc.SharedKeyRequest.$Shape
         */

        /**
         * Constructs a new SharedKeyRequest.
         * @memberof signrpc
         * @classdesc Represents a SharedKeyRequest.
         * @constructor
         * @param {signrpc.SharedKeyRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SharedKeyRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SharedKeyRequest ephemeral_pubkey.
         * @member {Uint8Array} ephemeral_pubkey
         * @memberof signrpc.SharedKeyRequest
         * @instance
         */
        SharedKeyRequest.prototype.ephemeral_pubkey = $util.newBuffer([]);

        /**
         * SharedKeyRequest key_loc.
         * @member {signrpc.KeyLocator.$Properties|null|undefined} key_loc
         * @memberof signrpc.SharedKeyRequest
         * @instance
         */
        SharedKeyRequest.prototype.key_loc = null;

        /**
         * SharedKeyRequest key_desc.
         * @member {signrpc.KeyDescriptor.$Properties|null|undefined} key_desc
         * @memberof signrpc.SharedKeyRequest
         * @instance
         */
        SharedKeyRequest.prototype.key_desc = null;

        /**
         * Creates a new SharedKeyRequest instance using the specified properties.
         * @function create
         * @memberof signrpc.SharedKeyRequest
         * @static
         * @param {signrpc.SharedKeyRequest.$Properties=} [properties] Properties to set
         * @returns {signrpc.SharedKeyRequest} SharedKeyRequest instance
         * @type {{
         *   (properties: signrpc.SharedKeyRequest.$Shape): signrpc.SharedKeyRequest & signrpc.SharedKeyRequest.$Shape;
         *   (properties?: signrpc.SharedKeyRequest.$Properties): signrpc.SharedKeyRequest;
         * }}
         */
        SharedKeyRequest.create = function(properties) {
            return new SharedKeyRequest(properties);
        };

        /**
         * Encodes the specified SharedKeyRequest message. Does not implicitly {@link signrpc.SharedKeyRequest.verify|verify} messages.
         * @function encode
         * @memberof signrpc.SharedKeyRequest
         * @static
         * @param {signrpc.SharedKeyRequest.$Properties} message SharedKeyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedKeyRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.ephemeral_pubkey != null && $Object.hasOwnProperty.call(message, "ephemeral_pubkey") && message.ephemeral_pubkey.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ephemeral_pubkey);
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                $root.signrpc.KeyLocator.encode(message.key_loc, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.key_desc != null && $Object.hasOwnProperty.call(message, "key_desc"))
                $root.signrpc.KeyDescriptor.encode(message.key_desc, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SharedKeyRequest message, length delimited. Does not implicitly {@link signrpc.SharedKeyRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.SharedKeyRequest
         * @static
         * @param {signrpc.SharedKeyRequest.$Properties} message SharedKeyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedKeyRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SharedKeyRequest message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.SharedKeyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.SharedKeyRequest & signrpc.SharedKeyRequest.$Shape} SharedKeyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedKeyRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.SharedKeyRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.ephemeral_pubkey = value;
                        else
                            delete message.ephemeral_pubkey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.key_loc = $root.signrpc.KeyLocator.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key_loc);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.key_desc = $root.signrpc.KeyDescriptor.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key_desc);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SharedKeyRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.SharedKeyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.SharedKeyRequest & signrpc.SharedKeyRequest.$Shape} SharedKeyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedKeyRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SharedKeyRequest message.
         * @function verify
         * @memberof signrpc.SharedKeyRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SharedKeyRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ephemeral_pubkey != null && $Object.hasOwnProperty.call(message, "ephemeral_pubkey"))
                if (!(message.ephemeral_pubkey && typeof message.ephemeral_pubkey.length === "number" || $util.isString(message.ephemeral_pubkey)))
                    return "ephemeral_pubkey: buffer expected";
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc")) {
                let error = $root.signrpc.KeyLocator.verify(message.key_loc, _depth + 1);
                if (error)
                    return "key_loc." + error;
            }
            if (message.key_desc != null && $Object.hasOwnProperty.call(message, "key_desc")) {
                let error = $root.signrpc.KeyDescriptor.verify(message.key_desc, _depth + 1);
                if (error)
                    return "key_desc." + error;
            }
            return null;
        };

        /**
         * Creates a SharedKeyRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.SharedKeyRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.SharedKeyRequest} SharedKeyRequest
         */
        SharedKeyRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.SharedKeyRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.SharedKeyRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.SharedKeyRequest();
            if (object.ephemeral_pubkey != null)
                if (object.ephemeral_pubkey.length)
                    if (typeof object.ephemeral_pubkey === "string")
                        $util.base64.decode(object.ephemeral_pubkey, message.ephemeral_pubkey = $util.newBuffer($util.base64.length(object.ephemeral_pubkey)), 0);
                    else if (object.ephemeral_pubkey.length >= 0)
                        message.ephemeral_pubkey = object.ephemeral_pubkey;
            if (object.key_loc != null) {
                if (!$util.isObject(object.key_loc))
                    throw $TypeError(".signrpc.SharedKeyRequest.key_loc: object expected");
                message.key_loc = $root.signrpc.KeyLocator.fromObject(object.key_loc, _depth + 1);
            }
            if (object.key_desc != null) {
                if (!$util.isObject(object.key_desc))
                    throw $TypeError(".signrpc.SharedKeyRequest.key_desc: object expected");
                message.key_desc = $root.signrpc.KeyDescriptor.fromObject(object.key_desc, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SharedKeyRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.SharedKeyRequest
         * @static
         * @param {signrpc.SharedKeyRequest} message SharedKeyRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SharedKeyRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.ephemeral_pubkey = "";
                else {
                    object.ephemeral_pubkey = [];
                    if (options.bytes !== $Array)
                        object.ephemeral_pubkey = $util.newBuffer(object.ephemeral_pubkey);
                }
                object.key_loc = null;
                object.key_desc = null;
            }
            if (message.ephemeral_pubkey != null && $Object.hasOwnProperty.call(message, "ephemeral_pubkey"))
                object.ephemeral_pubkey = options.bytes === $String ? $util.base64.encode(message.ephemeral_pubkey, 0, message.ephemeral_pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ephemeral_pubkey) : message.ephemeral_pubkey;
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                object.key_loc = $root.signrpc.KeyLocator.toObject(message.key_loc, options, _depth + 1);
            if (message.key_desc != null && $Object.hasOwnProperty.call(message, "key_desc"))
                object.key_desc = $root.signrpc.KeyDescriptor.toObject(message.key_desc, options, _depth + 1);
            return object;
        };

        /**
         * Converts this SharedKeyRequest to JSON.
         * @function toJSON
         * @memberof signrpc.SharedKeyRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SharedKeyRequest.prototype.toJSON = function() {
            return SharedKeyRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SharedKeyRequest
         * @function getTypeUrl
         * @memberof signrpc.SharedKeyRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SharedKeyRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.SharedKeyRequest";
        };

        return SharedKeyRequest;
    })();

    signrpc.SharedKeyResponse = (function() {

        /**
         * Properties of a SharedKeyResponse.
         * @typedef {Object} signrpc.SharedKeyResponse.$Properties
         * @property {Uint8Array|null} [shared_key] SharedKeyResponse shared_key
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SharedKeyResponse.
         * @memberof signrpc
         * @interface ISharedKeyResponse
         * @augments signrpc.SharedKeyResponse.$Properties
         * @deprecated Use signrpc.SharedKeyResponse.$Properties instead.
         */

        /**
         * Shape of a SharedKeyResponse.
         * @typedef {signrpc.SharedKeyResponse.$Properties} signrpc.SharedKeyResponse.$Shape
         */

        /**
         * Constructs a new SharedKeyResponse.
         * @memberof signrpc
         * @classdesc Represents a SharedKeyResponse.
         * @constructor
         * @param {signrpc.SharedKeyResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SharedKeyResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SharedKeyResponse shared_key.
         * @member {Uint8Array} shared_key
         * @memberof signrpc.SharedKeyResponse
         * @instance
         */
        SharedKeyResponse.prototype.shared_key = $util.newBuffer([]);

        /**
         * Creates a new SharedKeyResponse instance using the specified properties.
         * @function create
         * @memberof signrpc.SharedKeyResponse
         * @static
         * @param {signrpc.SharedKeyResponse.$Properties=} [properties] Properties to set
         * @returns {signrpc.SharedKeyResponse} SharedKeyResponse instance
         * @type {{
         *   (properties: signrpc.SharedKeyResponse.$Shape): signrpc.SharedKeyResponse & signrpc.SharedKeyResponse.$Shape;
         *   (properties?: signrpc.SharedKeyResponse.$Properties): signrpc.SharedKeyResponse;
         * }}
         */
        SharedKeyResponse.create = function(properties) {
            return new SharedKeyResponse(properties);
        };

        /**
         * Encodes the specified SharedKeyResponse message. Does not implicitly {@link signrpc.SharedKeyResponse.verify|verify} messages.
         * @function encode
         * @memberof signrpc.SharedKeyResponse
         * @static
         * @param {signrpc.SharedKeyResponse.$Properties} message SharedKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedKeyResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shared_key != null && $Object.hasOwnProperty.call(message, "shared_key") && message.shared_key.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.shared_key);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SharedKeyResponse message, length delimited. Does not implicitly {@link signrpc.SharedKeyResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.SharedKeyResponse
         * @static
         * @param {signrpc.SharedKeyResponse.$Properties} message SharedKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedKeyResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SharedKeyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.SharedKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.SharedKeyResponse & signrpc.SharedKeyResponse.$Shape} SharedKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedKeyResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.SharedKeyResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.shared_key = value;
                        else
                            delete message.shared_key;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SharedKeyResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.SharedKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.SharedKeyResponse & signrpc.SharedKeyResponse.$Shape} SharedKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedKeyResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SharedKeyResponse message.
         * @function verify
         * @memberof signrpc.SharedKeyResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SharedKeyResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.shared_key != null && $Object.hasOwnProperty.call(message, "shared_key"))
                if (!(message.shared_key && typeof message.shared_key.length === "number" || $util.isString(message.shared_key)))
                    return "shared_key: buffer expected";
            return null;
        };

        /**
         * Creates a SharedKeyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.SharedKeyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.SharedKeyResponse} SharedKeyResponse
         */
        SharedKeyResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.SharedKeyResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.SharedKeyResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.SharedKeyResponse();
            if (object.shared_key != null)
                if (object.shared_key.length)
                    if (typeof object.shared_key === "string")
                        $util.base64.decode(object.shared_key, message.shared_key = $util.newBuffer($util.base64.length(object.shared_key)), 0);
                    else if (object.shared_key.length >= 0)
                        message.shared_key = object.shared_key;
            return message;
        };

        /**
         * Creates a plain object from a SharedKeyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.SharedKeyResponse
         * @static
         * @param {signrpc.SharedKeyResponse} message SharedKeyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SharedKeyResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.shared_key = "";
                else {
                    object.shared_key = [];
                    if (options.bytes !== $Array)
                        object.shared_key = $util.newBuffer(object.shared_key);
                }
            if (message.shared_key != null && $Object.hasOwnProperty.call(message, "shared_key"))
                object.shared_key = options.bytes === $String ? $util.base64.encode(message.shared_key, 0, message.shared_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.shared_key) : message.shared_key;
            return object;
        };

        /**
         * Converts this SharedKeyResponse to JSON.
         * @function toJSON
         * @memberof signrpc.SharedKeyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SharedKeyResponse.prototype.toJSON = function() {
            return SharedKeyResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SharedKeyResponse
         * @function getTypeUrl
         * @memberof signrpc.SharedKeyResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SharedKeyResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.SharedKeyResponse";
        };

        return SharedKeyResponse;
    })();

    signrpc.TweakDesc = (function() {

        /**
         * Properties of a TweakDesc.
         * @typedef {Object} signrpc.TweakDesc.$Properties
         * @property {Uint8Array|null} [tweak] TweakDesc tweak
         * @property {boolean|null} [is_x_only] TweakDesc is_x_only
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TweakDesc.
         * @memberof signrpc
         * @interface ITweakDesc
         * @augments signrpc.TweakDesc.$Properties
         * @deprecated Use signrpc.TweakDesc.$Properties instead.
         */

        /**
         * Shape of a TweakDesc.
         * @typedef {signrpc.TweakDesc.$Properties} signrpc.TweakDesc.$Shape
         */

        /**
         * Constructs a new TweakDesc.
         * @memberof signrpc
         * @classdesc Represents a TweakDesc.
         * @constructor
         * @param {signrpc.TweakDesc.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TweakDesc = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TweakDesc tweak.
         * @member {Uint8Array} tweak
         * @memberof signrpc.TweakDesc
         * @instance
         */
        TweakDesc.prototype.tweak = $util.newBuffer([]);

        /**
         * TweakDesc is_x_only.
         * @member {boolean} is_x_only
         * @memberof signrpc.TweakDesc
         * @instance
         */
        TweakDesc.prototype.is_x_only = false;

        /**
         * Creates a new TweakDesc instance using the specified properties.
         * @function create
         * @memberof signrpc.TweakDesc
         * @static
         * @param {signrpc.TweakDesc.$Properties=} [properties] Properties to set
         * @returns {signrpc.TweakDesc} TweakDesc instance
         * @type {{
         *   (properties: signrpc.TweakDesc.$Shape): signrpc.TweakDesc & signrpc.TweakDesc.$Shape;
         *   (properties?: signrpc.TweakDesc.$Properties): signrpc.TweakDesc;
         * }}
         */
        TweakDesc.create = function(properties) {
            return new TweakDesc(properties);
        };

        /**
         * Encodes the specified TweakDesc message. Does not implicitly {@link signrpc.TweakDesc.verify|verify} messages.
         * @function encode
         * @memberof signrpc.TweakDesc
         * @static
         * @param {signrpc.TweakDesc.$Properties} message TweakDesc message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TweakDesc.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tweak != null && $Object.hasOwnProperty.call(message, "tweak") && message.tweak.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.tweak);
            if (message.is_x_only != null && $Object.hasOwnProperty.call(message, "is_x_only") && message.is_x_only !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.is_x_only);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TweakDesc message, length delimited. Does not implicitly {@link signrpc.TweakDesc.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.TweakDesc
         * @static
         * @param {signrpc.TweakDesc.$Properties} message TweakDesc message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TweakDesc.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TweakDesc message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.TweakDesc
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.TweakDesc & signrpc.TweakDesc.$Shape} TweakDesc
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TweakDesc.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.TweakDesc(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.tweak = value;
                        else
                            delete message.tweak;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_x_only = value;
                        else
                            delete message.is_x_only;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TweakDesc message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.TweakDesc
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.TweakDesc & signrpc.TweakDesc.$Shape} TweakDesc
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TweakDesc.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TweakDesc message.
         * @function verify
         * @memberof signrpc.TweakDesc
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TweakDesc.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tweak != null && $Object.hasOwnProperty.call(message, "tweak"))
                if (!(message.tweak && typeof message.tweak.length === "number" || $util.isString(message.tweak)))
                    return "tweak: buffer expected";
            if (message.is_x_only != null && $Object.hasOwnProperty.call(message, "is_x_only"))
                if (typeof message.is_x_only !== "boolean")
                    return "is_x_only: boolean expected";
            return null;
        };

        /**
         * Creates a TweakDesc message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.TweakDesc
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.TweakDesc} TweakDesc
         */
        TweakDesc.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.TweakDesc)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.TweakDesc: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.TweakDesc();
            if (object.tweak != null)
                if (object.tweak.length)
                    if (typeof object.tweak === "string")
                        $util.base64.decode(object.tweak, message.tweak = $util.newBuffer($util.base64.length(object.tweak)), 0);
                    else if (object.tweak.length >= 0)
                        message.tweak = object.tweak;
            if (object.is_x_only != null)
                if (object.is_x_only)
                    message.is_x_only = $Boolean(object.is_x_only);
            return message;
        };

        /**
         * Creates a plain object from a TweakDesc message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.TweakDesc
         * @static
         * @param {signrpc.TweakDesc} message TweakDesc
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TweakDesc.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.tweak = "";
                else {
                    object.tweak = [];
                    if (options.bytes !== $Array)
                        object.tweak = $util.newBuffer(object.tweak);
                }
                object.is_x_only = false;
            }
            if (message.tweak != null && $Object.hasOwnProperty.call(message, "tweak"))
                object.tweak = options.bytes === $String ? $util.base64.encode(message.tweak, 0, message.tweak.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.tweak) : message.tweak;
            if (message.is_x_only != null && $Object.hasOwnProperty.call(message, "is_x_only"))
                object.is_x_only = message.is_x_only;
            return object;
        };

        /**
         * Converts this TweakDesc to JSON.
         * @function toJSON
         * @memberof signrpc.TweakDesc
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TweakDesc.prototype.toJSON = function() {
            return TweakDesc.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TweakDesc
         * @function getTypeUrl
         * @memberof signrpc.TweakDesc
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TweakDesc.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.TweakDesc";
        };

        return TweakDesc;
    })();

    signrpc.TaprootTweakDesc = (function() {

        /**
         * Properties of a TaprootTweakDesc.
         * @typedef {Object} signrpc.TaprootTweakDesc.$Properties
         * @property {Uint8Array|null} [script_root] TaprootTweakDesc script_root
         * @property {boolean|null} [key_spend_only] TaprootTweakDesc key_spend_only
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TaprootTweakDesc.
         * @memberof signrpc
         * @interface ITaprootTweakDesc
         * @augments signrpc.TaprootTweakDesc.$Properties
         * @deprecated Use signrpc.TaprootTweakDesc.$Properties instead.
         */

        /**
         * Shape of a TaprootTweakDesc.
         * @typedef {signrpc.TaprootTweakDesc.$Properties} signrpc.TaprootTweakDesc.$Shape
         */

        /**
         * Constructs a new TaprootTweakDesc.
         * @memberof signrpc
         * @classdesc Represents a TaprootTweakDesc.
         * @constructor
         * @param {signrpc.TaprootTweakDesc.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TaprootTweakDesc = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TaprootTweakDesc script_root.
         * @member {Uint8Array} script_root
         * @memberof signrpc.TaprootTweakDesc
         * @instance
         */
        TaprootTweakDesc.prototype.script_root = $util.newBuffer([]);

        /**
         * TaprootTweakDesc key_spend_only.
         * @member {boolean} key_spend_only
         * @memberof signrpc.TaprootTweakDesc
         * @instance
         */
        TaprootTweakDesc.prototype.key_spend_only = false;

        /**
         * Creates a new TaprootTweakDesc instance using the specified properties.
         * @function create
         * @memberof signrpc.TaprootTweakDesc
         * @static
         * @param {signrpc.TaprootTweakDesc.$Properties=} [properties] Properties to set
         * @returns {signrpc.TaprootTweakDesc} TaprootTweakDesc instance
         * @type {{
         *   (properties: signrpc.TaprootTweakDesc.$Shape): signrpc.TaprootTweakDesc & signrpc.TaprootTweakDesc.$Shape;
         *   (properties?: signrpc.TaprootTweakDesc.$Properties): signrpc.TaprootTweakDesc;
         * }}
         */
        TaprootTweakDesc.create = function(properties) {
            return new TaprootTweakDesc(properties);
        };

        /**
         * Encodes the specified TaprootTweakDesc message. Does not implicitly {@link signrpc.TaprootTweakDesc.verify|verify} messages.
         * @function encode
         * @memberof signrpc.TaprootTweakDesc
         * @static
         * @param {signrpc.TaprootTweakDesc.$Properties} message TaprootTweakDesc message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TaprootTweakDesc.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.script_root != null && $Object.hasOwnProperty.call(message, "script_root") && message.script_root.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.script_root);
            if (message.key_spend_only != null && $Object.hasOwnProperty.call(message, "key_spend_only") && message.key_spend_only !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.key_spend_only);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TaprootTweakDesc message, length delimited. Does not implicitly {@link signrpc.TaprootTweakDesc.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.TaprootTweakDesc
         * @static
         * @param {signrpc.TaprootTweakDesc.$Properties} message TaprootTweakDesc message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TaprootTweakDesc.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TaprootTweakDesc message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.TaprootTweakDesc
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.TaprootTweakDesc & signrpc.TaprootTweakDesc.$Shape} TaprootTweakDesc
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TaprootTweakDesc.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.TaprootTweakDesc(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.script_root = value;
                        else
                            delete message.script_root;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.key_spend_only = value;
                        else
                            delete message.key_spend_only;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TaprootTweakDesc message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.TaprootTweakDesc
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.TaprootTweakDesc & signrpc.TaprootTweakDesc.$Shape} TaprootTweakDesc
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TaprootTweakDesc.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TaprootTweakDesc message.
         * @function verify
         * @memberof signrpc.TaprootTweakDesc
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TaprootTweakDesc.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.script_root != null && $Object.hasOwnProperty.call(message, "script_root"))
                if (!(message.script_root && typeof message.script_root.length === "number" || $util.isString(message.script_root)))
                    return "script_root: buffer expected";
            if (message.key_spend_only != null && $Object.hasOwnProperty.call(message, "key_spend_only"))
                if (typeof message.key_spend_only !== "boolean")
                    return "key_spend_only: boolean expected";
            return null;
        };

        /**
         * Creates a TaprootTweakDesc message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.TaprootTweakDesc
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.TaprootTweakDesc} TaprootTweakDesc
         */
        TaprootTweakDesc.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.TaprootTweakDesc)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.TaprootTweakDesc: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.TaprootTweakDesc();
            if (object.script_root != null)
                if (object.script_root.length)
                    if (typeof object.script_root === "string")
                        $util.base64.decode(object.script_root, message.script_root = $util.newBuffer($util.base64.length(object.script_root)), 0);
                    else if (object.script_root.length >= 0)
                        message.script_root = object.script_root;
            if (object.key_spend_only != null)
                if (object.key_spend_only)
                    message.key_spend_only = $Boolean(object.key_spend_only);
            return message;
        };

        /**
         * Creates a plain object from a TaprootTweakDesc message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.TaprootTweakDesc
         * @static
         * @param {signrpc.TaprootTweakDesc} message TaprootTweakDesc
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TaprootTweakDesc.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.script_root = "";
                else {
                    object.script_root = [];
                    if (options.bytes !== $Array)
                        object.script_root = $util.newBuffer(object.script_root);
                }
                object.key_spend_only = false;
            }
            if (message.script_root != null && $Object.hasOwnProperty.call(message, "script_root"))
                object.script_root = options.bytes === $String ? $util.base64.encode(message.script_root, 0, message.script_root.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.script_root) : message.script_root;
            if (message.key_spend_only != null && $Object.hasOwnProperty.call(message, "key_spend_only"))
                object.key_spend_only = message.key_spend_only;
            return object;
        };

        /**
         * Converts this TaprootTweakDesc to JSON.
         * @function toJSON
         * @memberof signrpc.TaprootTweakDesc
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TaprootTweakDesc.prototype.toJSON = function() {
            return TaprootTweakDesc.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TaprootTweakDesc
         * @function getTypeUrl
         * @memberof signrpc.TaprootTweakDesc
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TaprootTweakDesc.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.TaprootTweakDesc";
        };

        return TaprootTweakDesc;
    })();

    /**
     * MuSig2Version enum.
     * @name signrpc.MuSig2Version
     * @enum {number}
     * @property {number} MUSIG2_VERSION_UNDEFINED=0 MUSIG2_VERSION_UNDEFINED value
     * @property {number} MUSIG2_VERSION_V040=1 MUSIG2_VERSION_V040 value
     * @property {number} MUSIG2_VERSION_V100RC2=2 MUSIG2_VERSION_V100RC2 value
     */
    signrpc.MuSig2Version = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "MUSIG2_VERSION_UNDEFINED"] = 0;
        values[valuesById[1] = "MUSIG2_VERSION_V040"] = 1;
        values[valuesById[2] = "MUSIG2_VERSION_V100RC2"] = 2;
        return values;
    })();

    signrpc.MuSig2CombineKeysRequest = (function() {

        /**
         * Properties of a MuSig2CombineKeysRequest.
         * @typedef {Object} signrpc.MuSig2CombineKeysRequest.$Properties
         * @property {Array.<Uint8Array>|null} [all_signer_pubkeys] MuSig2CombineKeysRequest all_signer_pubkeys
         * @property {Array.<signrpc.TweakDesc.$Properties>|null} [tweaks] MuSig2CombineKeysRequest tweaks
         * @property {signrpc.TaprootTweakDesc.$Properties|null} [taproot_tweak] MuSig2CombineKeysRequest taproot_tweak
         * @property {signrpc.MuSig2Version|null} [version] MuSig2CombineKeysRequest version
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2CombineKeysRequest.
         * @memberof signrpc
         * @interface IMuSig2CombineKeysRequest
         * @augments signrpc.MuSig2CombineKeysRequest.$Properties
         * @deprecated Use signrpc.MuSig2CombineKeysRequest.$Properties instead.
         */

        /**
         * Shape of a MuSig2CombineKeysRequest.
         * @typedef {signrpc.MuSig2CombineKeysRequest.$Properties} signrpc.MuSig2CombineKeysRequest.$Shape
         */

        /**
         * Constructs a new MuSig2CombineKeysRequest.
         * @memberof signrpc
         * @classdesc Represents a MuSig2CombineKeysRequest.
         * @constructor
         * @param {signrpc.MuSig2CombineKeysRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2CombineKeysRequest = function (properties) {
            this.all_signer_pubkeys = [];
            this.tweaks = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2CombineKeysRequest all_signer_pubkeys.
         * @member {Array.<Uint8Array>} all_signer_pubkeys
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @instance
         */
        MuSig2CombineKeysRequest.prototype.all_signer_pubkeys = $util.emptyArray;

        /**
         * MuSig2CombineKeysRequest tweaks.
         * @member {Array.<signrpc.TweakDesc.$Properties>} tweaks
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @instance
         */
        MuSig2CombineKeysRequest.prototype.tweaks = $util.emptyArray;

        /**
         * MuSig2CombineKeysRequest taproot_tweak.
         * @member {signrpc.TaprootTweakDesc.$Properties|null|undefined} taproot_tweak
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @instance
         */
        MuSig2CombineKeysRequest.prototype.taproot_tweak = null;

        /**
         * MuSig2CombineKeysRequest version.
         * @member {signrpc.MuSig2Version} version
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @instance
         */
        MuSig2CombineKeysRequest.prototype.version = 0;

        /**
         * Creates a new MuSig2CombineKeysRequest instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @static
         * @param {signrpc.MuSig2CombineKeysRequest.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2CombineKeysRequest} MuSig2CombineKeysRequest instance
         * @type {{
         *   (properties: signrpc.MuSig2CombineKeysRequest.$Shape): signrpc.MuSig2CombineKeysRequest & signrpc.MuSig2CombineKeysRequest.$Shape;
         *   (properties?: signrpc.MuSig2CombineKeysRequest.$Properties): signrpc.MuSig2CombineKeysRequest;
         * }}
         */
        MuSig2CombineKeysRequest.create = function(properties) {
            return new MuSig2CombineKeysRequest(properties);
        };

        /**
         * Encodes the specified MuSig2CombineKeysRequest message. Does not implicitly {@link signrpc.MuSig2CombineKeysRequest.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @static
         * @param {signrpc.MuSig2CombineKeysRequest.$Properties} message MuSig2CombineKeysRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CombineKeysRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.all_signer_pubkeys != null && message.all_signer_pubkeys.length)
                for (let i = 0; i < message.all_signer_pubkeys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.all_signer_pubkeys[i]);
            if (message.tweaks != null && message.tweaks.length)
                for (let i = 0; i < message.tweaks.length; ++i)
                    $root.signrpc.TweakDesc.encode(message.tweaks[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.taproot_tweak != null && $Object.hasOwnProperty.call(message, "taproot_tweak"))
                $root.signrpc.TaprootTweakDesc.encode(message.taproot_tweak, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.version);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2CombineKeysRequest message, length delimited. Does not implicitly {@link signrpc.MuSig2CombineKeysRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @static
         * @param {signrpc.MuSig2CombineKeysRequest.$Properties} message MuSig2CombineKeysRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CombineKeysRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2CombineKeysRequest message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2CombineKeysRequest & signrpc.MuSig2CombineKeysRequest.$Shape} MuSig2CombineKeysRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CombineKeysRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2CombineKeysRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.all_signer_pubkeys && message.all_signer_pubkeys.length))
                            message.all_signer_pubkeys = [];
                        message.all_signer_pubkeys.push(reader.bytes());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tweaks && message.tweaks.length))
                            message.tweaks = [];
                        message.tweaks.push($root.signrpc.TweakDesc.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.taproot_tweak = $root.signrpc.TaprootTweakDesc.decode(reader, reader.uint32(), $undefined, _depth + 1, message.taproot_tweak);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2CombineKeysRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2CombineKeysRequest & signrpc.MuSig2CombineKeysRequest.$Shape} MuSig2CombineKeysRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CombineKeysRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2CombineKeysRequest message.
         * @function verify
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2CombineKeysRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.all_signer_pubkeys != null && $Object.hasOwnProperty.call(message, "all_signer_pubkeys")) {
                if (!$Array.isArray(message.all_signer_pubkeys))
                    return "all_signer_pubkeys: array expected";
                for (let i = 0; i < message.all_signer_pubkeys.length; ++i)
                    if (!(message.all_signer_pubkeys[i] && typeof message.all_signer_pubkeys[i].length === "number" || $util.isString(message.all_signer_pubkeys[i])))
                        return "all_signer_pubkeys: buffer[] expected";
            }
            if (message.tweaks != null && $Object.hasOwnProperty.call(message, "tweaks")) {
                if (!$Array.isArray(message.tweaks))
                    return "tweaks: array expected";
                for (let i = 0; i < message.tweaks.length; ++i) {
                    let error = $root.signrpc.TweakDesc.verify(message.tweaks[i], _depth + 1);
                    if (error)
                        return "tweaks." + error;
                }
            }
            if (message.taproot_tweak != null && $Object.hasOwnProperty.call(message, "taproot_tweak")) {
                let error = $root.signrpc.TaprootTweakDesc.verify(message.taproot_tweak, _depth + 1);
                if (error)
                    return "taproot_tweak." + error;
            }
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                if (typeof message.version !== "number" || (message.version | 0) !== message.version)
                    return "version: enum value expected";
            return null;
        };

        /**
         * Creates a MuSig2CombineKeysRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2CombineKeysRequest} MuSig2CombineKeysRequest
         */
        MuSig2CombineKeysRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2CombineKeysRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2CombineKeysRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2CombineKeysRequest();
            if (object.all_signer_pubkeys) {
                if (!$Array.isArray(object.all_signer_pubkeys))
                    throw $TypeError(".signrpc.MuSig2CombineKeysRequest.all_signer_pubkeys: array expected");
                message.all_signer_pubkeys = $Array(object.all_signer_pubkeys.length);
                for (let i = 0; i < object.all_signer_pubkeys.length; ++i)
                    if (typeof object.all_signer_pubkeys[i] === "string")
                        $util.base64.decode(object.all_signer_pubkeys[i], message.all_signer_pubkeys[i] = $util.newBuffer($util.base64.length(object.all_signer_pubkeys[i])), 0);
                    else if (object.all_signer_pubkeys[i].length >= 0)
                        message.all_signer_pubkeys[i] = object.all_signer_pubkeys[i];
            }
            if (object.tweaks) {
                if (!$Array.isArray(object.tweaks))
                    throw $TypeError(".signrpc.MuSig2CombineKeysRequest.tweaks: array expected");
                message.tweaks = $Array(object.tweaks.length);
                for (let i = 0; i < object.tweaks.length; ++i) {
                    if (!$util.isObject(object.tweaks[i]))
                        throw $TypeError(".signrpc.MuSig2CombineKeysRequest.tweaks: object expected");
                    message.tweaks[i] = $root.signrpc.TweakDesc.fromObject(object.tweaks[i], _depth + 1);
                }
            }
            if (object.taproot_tweak != null) {
                if (!$util.isObject(object.taproot_tweak))
                    throw $TypeError(".signrpc.MuSig2CombineKeysRequest.taproot_tweak: object expected");
                message.taproot_tweak = $root.signrpc.TaprootTweakDesc.fromObject(object.taproot_tweak, _depth + 1);
            }
            if (object.version !== 0 && (typeof object.version !== "string" || $root.signrpc.MuSig2Version[object.version] !== 0))
                switch (object.version) {
                case "MUSIG2_VERSION_UNDEFINED":
                case 0:
                    message.version = 0;
                    break;
                case "MUSIG2_VERSION_V040":
                case 1:
                    message.version = 1;
                    break;
                case "MUSIG2_VERSION_V100RC2":
                case 2:
                    message.version = 2;
                    break;
                default:
                    if (typeof object.version === "number" && (object.version | 0) === object.version)
                        message.version = object.version;
                }
            return message;
        };

        /**
         * Creates a plain object from a MuSig2CombineKeysRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @static
         * @param {signrpc.MuSig2CombineKeysRequest} message MuSig2CombineKeysRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2CombineKeysRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.all_signer_pubkeys = [];
                object.tweaks = [];
            }
            if (options.defaults) {
                object.taproot_tweak = null;
                object.version = options.enums === $String ? "MUSIG2_VERSION_UNDEFINED" : 0;
            }
            if (message.all_signer_pubkeys && message.all_signer_pubkeys.length) {
                object.all_signer_pubkeys = $Array(message.all_signer_pubkeys.length);
                for (let j = 0; j < message.all_signer_pubkeys.length; ++j)
                    object.all_signer_pubkeys[j] = options.bytes === $String ? $util.base64.encode(message.all_signer_pubkeys[j], 0, message.all_signer_pubkeys[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.all_signer_pubkeys[j]) : message.all_signer_pubkeys[j];
            }
            if (message.tweaks && message.tweaks.length) {
                object.tweaks = $Array(message.tweaks.length);
                for (let j = 0; j < message.tweaks.length; ++j)
                    object.tweaks[j] = $root.signrpc.TweakDesc.toObject(message.tweaks[j], options, _depth + 1);
            }
            if (message.taproot_tweak != null && $Object.hasOwnProperty.call(message, "taproot_tweak"))
                object.taproot_tweak = $root.signrpc.TaprootTweakDesc.toObject(message.taproot_tweak, options, _depth + 1);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = options.enums === $String ? $root.signrpc.MuSig2Version[message.version] === $undefined ? message.version : $root.signrpc.MuSig2Version[message.version] : message.version;
            return object;
        };

        /**
         * Converts this MuSig2CombineKeysRequest to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2CombineKeysRequest.prototype.toJSON = function() {
            return MuSig2CombineKeysRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2CombineKeysRequest
         * @function getTypeUrl
         * @memberof signrpc.MuSig2CombineKeysRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2CombineKeysRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2CombineKeysRequest";
        };

        return MuSig2CombineKeysRequest;
    })();

    signrpc.MuSig2CombineKeysResponse = (function() {

        /**
         * Properties of a MuSig2CombineKeysResponse.
         * @typedef {Object} signrpc.MuSig2CombineKeysResponse.$Properties
         * @property {Uint8Array|null} [combined_key] MuSig2CombineKeysResponse combined_key
         * @property {Uint8Array|null} [taproot_internal_key] MuSig2CombineKeysResponse taproot_internal_key
         * @property {signrpc.MuSig2Version|null} [version] MuSig2CombineKeysResponse version
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2CombineKeysResponse.
         * @memberof signrpc
         * @interface IMuSig2CombineKeysResponse
         * @augments signrpc.MuSig2CombineKeysResponse.$Properties
         * @deprecated Use signrpc.MuSig2CombineKeysResponse.$Properties instead.
         */

        /**
         * Shape of a MuSig2CombineKeysResponse.
         * @typedef {signrpc.MuSig2CombineKeysResponse.$Properties} signrpc.MuSig2CombineKeysResponse.$Shape
         */

        /**
         * Constructs a new MuSig2CombineKeysResponse.
         * @memberof signrpc
         * @classdesc Represents a MuSig2CombineKeysResponse.
         * @constructor
         * @param {signrpc.MuSig2CombineKeysResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2CombineKeysResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2CombineKeysResponse combined_key.
         * @member {Uint8Array} combined_key
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @instance
         */
        MuSig2CombineKeysResponse.prototype.combined_key = $util.newBuffer([]);

        /**
         * MuSig2CombineKeysResponse taproot_internal_key.
         * @member {Uint8Array} taproot_internal_key
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @instance
         */
        MuSig2CombineKeysResponse.prototype.taproot_internal_key = $util.newBuffer([]);

        /**
         * MuSig2CombineKeysResponse version.
         * @member {signrpc.MuSig2Version} version
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @instance
         */
        MuSig2CombineKeysResponse.prototype.version = 0;

        /**
         * Creates a new MuSig2CombineKeysResponse instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @static
         * @param {signrpc.MuSig2CombineKeysResponse.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2CombineKeysResponse} MuSig2CombineKeysResponse instance
         * @type {{
         *   (properties: signrpc.MuSig2CombineKeysResponse.$Shape): signrpc.MuSig2CombineKeysResponse & signrpc.MuSig2CombineKeysResponse.$Shape;
         *   (properties?: signrpc.MuSig2CombineKeysResponse.$Properties): signrpc.MuSig2CombineKeysResponse;
         * }}
         */
        MuSig2CombineKeysResponse.create = function(properties) {
            return new MuSig2CombineKeysResponse(properties);
        };

        /**
         * Encodes the specified MuSig2CombineKeysResponse message. Does not implicitly {@link signrpc.MuSig2CombineKeysResponse.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @static
         * @param {signrpc.MuSig2CombineKeysResponse.$Properties} message MuSig2CombineKeysResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CombineKeysResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.combined_key != null && $Object.hasOwnProperty.call(message, "combined_key") && message.combined_key.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.combined_key);
            if (message.taproot_internal_key != null && $Object.hasOwnProperty.call(message, "taproot_internal_key") && message.taproot_internal_key.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.taproot_internal_key);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.version);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2CombineKeysResponse message, length delimited. Does not implicitly {@link signrpc.MuSig2CombineKeysResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @static
         * @param {signrpc.MuSig2CombineKeysResponse.$Properties} message MuSig2CombineKeysResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CombineKeysResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2CombineKeysResponse message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2CombineKeysResponse & signrpc.MuSig2CombineKeysResponse.$Shape} MuSig2CombineKeysResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CombineKeysResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2CombineKeysResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.combined_key = value;
                        else
                            delete message.combined_key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.taproot_internal_key = value;
                        else
                            delete message.taproot_internal_key;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2CombineKeysResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2CombineKeysResponse & signrpc.MuSig2CombineKeysResponse.$Shape} MuSig2CombineKeysResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CombineKeysResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2CombineKeysResponse message.
         * @function verify
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2CombineKeysResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.combined_key != null && $Object.hasOwnProperty.call(message, "combined_key"))
                if (!(message.combined_key && typeof message.combined_key.length === "number" || $util.isString(message.combined_key)))
                    return "combined_key: buffer expected";
            if (message.taproot_internal_key != null && $Object.hasOwnProperty.call(message, "taproot_internal_key"))
                if (!(message.taproot_internal_key && typeof message.taproot_internal_key.length === "number" || $util.isString(message.taproot_internal_key)))
                    return "taproot_internal_key: buffer expected";
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                if (typeof message.version !== "number" || (message.version | 0) !== message.version)
                    return "version: enum value expected";
            return null;
        };

        /**
         * Creates a MuSig2CombineKeysResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2CombineKeysResponse} MuSig2CombineKeysResponse
         */
        MuSig2CombineKeysResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2CombineKeysResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2CombineKeysResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2CombineKeysResponse();
            if (object.combined_key != null)
                if (object.combined_key.length)
                    if (typeof object.combined_key === "string")
                        $util.base64.decode(object.combined_key, message.combined_key = $util.newBuffer($util.base64.length(object.combined_key)), 0);
                    else if (object.combined_key.length >= 0)
                        message.combined_key = object.combined_key;
            if (object.taproot_internal_key != null)
                if (object.taproot_internal_key.length)
                    if (typeof object.taproot_internal_key === "string")
                        $util.base64.decode(object.taproot_internal_key, message.taproot_internal_key = $util.newBuffer($util.base64.length(object.taproot_internal_key)), 0);
                    else if (object.taproot_internal_key.length >= 0)
                        message.taproot_internal_key = object.taproot_internal_key;
            if (object.version !== 0 && (typeof object.version !== "string" || $root.signrpc.MuSig2Version[object.version] !== 0))
                switch (object.version) {
                case "MUSIG2_VERSION_UNDEFINED":
                case 0:
                    message.version = 0;
                    break;
                case "MUSIG2_VERSION_V040":
                case 1:
                    message.version = 1;
                    break;
                case "MUSIG2_VERSION_V100RC2":
                case 2:
                    message.version = 2;
                    break;
                default:
                    if (typeof object.version === "number" && (object.version | 0) === object.version)
                        message.version = object.version;
                }
            return message;
        };

        /**
         * Creates a plain object from a MuSig2CombineKeysResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @static
         * @param {signrpc.MuSig2CombineKeysResponse} message MuSig2CombineKeysResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2CombineKeysResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.combined_key = "";
                else {
                    object.combined_key = [];
                    if (options.bytes !== $Array)
                        object.combined_key = $util.newBuffer(object.combined_key);
                }
                if (options.bytes === $String)
                    object.taproot_internal_key = "";
                else {
                    object.taproot_internal_key = [];
                    if (options.bytes !== $Array)
                        object.taproot_internal_key = $util.newBuffer(object.taproot_internal_key);
                }
                object.version = options.enums === $String ? "MUSIG2_VERSION_UNDEFINED" : 0;
            }
            if (message.combined_key != null && $Object.hasOwnProperty.call(message, "combined_key"))
                object.combined_key = options.bytes === $String ? $util.base64.encode(message.combined_key, 0, message.combined_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.combined_key) : message.combined_key;
            if (message.taproot_internal_key != null && $Object.hasOwnProperty.call(message, "taproot_internal_key"))
                object.taproot_internal_key = options.bytes === $String ? $util.base64.encode(message.taproot_internal_key, 0, message.taproot_internal_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.taproot_internal_key) : message.taproot_internal_key;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = options.enums === $String ? $root.signrpc.MuSig2Version[message.version] === $undefined ? message.version : $root.signrpc.MuSig2Version[message.version] : message.version;
            return object;
        };

        /**
         * Converts this MuSig2CombineKeysResponse to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2CombineKeysResponse.prototype.toJSON = function() {
            return MuSig2CombineKeysResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2CombineKeysResponse
         * @function getTypeUrl
         * @memberof signrpc.MuSig2CombineKeysResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2CombineKeysResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2CombineKeysResponse";
        };

        return MuSig2CombineKeysResponse;
    })();

    signrpc.MuSig2SessionRequest = (function() {

        /**
         * Properties of a MuSig2SessionRequest.
         * @typedef {Object} signrpc.MuSig2SessionRequest.$Properties
         * @property {signrpc.KeyLocator.$Properties|null} [key_loc] MuSig2SessionRequest key_loc
         * @property {Array.<Uint8Array>|null} [all_signer_pubkeys] MuSig2SessionRequest all_signer_pubkeys
         * @property {Array.<Uint8Array>|null} [other_signer_public_nonces] MuSig2SessionRequest other_signer_public_nonces
         * @property {Array.<signrpc.TweakDesc.$Properties>|null} [tweaks] MuSig2SessionRequest tweaks
         * @property {signrpc.TaprootTweakDesc.$Properties|null} [taproot_tweak] MuSig2SessionRequest taproot_tweak
         * @property {signrpc.MuSig2Version|null} [version] MuSig2SessionRequest version
         * @property {Uint8Array|null} [pregenerated_local_nonce] MuSig2SessionRequest pregenerated_local_nonce
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2SessionRequest.
         * @memberof signrpc
         * @interface IMuSig2SessionRequest
         * @augments signrpc.MuSig2SessionRequest.$Properties
         * @deprecated Use signrpc.MuSig2SessionRequest.$Properties instead.
         */

        /**
         * Shape of a MuSig2SessionRequest.
         * @typedef {signrpc.MuSig2SessionRequest.$Properties} signrpc.MuSig2SessionRequest.$Shape
         */

        /**
         * Constructs a new MuSig2SessionRequest.
         * @memberof signrpc
         * @classdesc Represents a MuSig2SessionRequest.
         * @constructor
         * @param {signrpc.MuSig2SessionRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2SessionRequest = function (properties) {
            this.all_signer_pubkeys = [];
            this.other_signer_public_nonces = [];
            this.tweaks = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2SessionRequest key_loc.
         * @member {signrpc.KeyLocator.$Properties|null|undefined} key_loc
         * @memberof signrpc.MuSig2SessionRequest
         * @instance
         */
        MuSig2SessionRequest.prototype.key_loc = null;

        /**
         * MuSig2SessionRequest all_signer_pubkeys.
         * @member {Array.<Uint8Array>} all_signer_pubkeys
         * @memberof signrpc.MuSig2SessionRequest
         * @instance
         */
        MuSig2SessionRequest.prototype.all_signer_pubkeys = $util.emptyArray;

        /**
         * MuSig2SessionRequest other_signer_public_nonces.
         * @member {Array.<Uint8Array>} other_signer_public_nonces
         * @memberof signrpc.MuSig2SessionRequest
         * @instance
         */
        MuSig2SessionRequest.prototype.other_signer_public_nonces = $util.emptyArray;

        /**
         * MuSig2SessionRequest tweaks.
         * @member {Array.<signrpc.TweakDesc.$Properties>} tweaks
         * @memberof signrpc.MuSig2SessionRequest
         * @instance
         */
        MuSig2SessionRequest.prototype.tweaks = $util.emptyArray;

        /**
         * MuSig2SessionRequest taproot_tweak.
         * @member {signrpc.TaprootTweakDesc.$Properties|null|undefined} taproot_tweak
         * @memberof signrpc.MuSig2SessionRequest
         * @instance
         */
        MuSig2SessionRequest.prototype.taproot_tweak = null;

        /**
         * MuSig2SessionRequest version.
         * @member {signrpc.MuSig2Version} version
         * @memberof signrpc.MuSig2SessionRequest
         * @instance
         */
        MuSig2SessionRequest.prototype.version = 0;

        /**
         * MuSig2SessionRequest pregenerated_local_nonce.
         * @member {Uint8Array} pregenerated_local_nonce
         * @memberof signrpc.MuSig2SessionRequest
         * @instance
         */
        MuSig2SessionRequest.prototype.pregenerated_local_nonce = $util.newBuffer([]);

        /**
         * Creates a new MuSig2SessionRequest instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2SessionRequest
         * @static
         * @param {signrpc.MuSig2SessionRequest.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2SessionRequest} MuSig2SessionRequest instance
         * @type {{
         *   (properties: signrpc.MuSig2SessionRequest.$Shape): signrpc.MuSig2SessionRequest & signrpc.MuSig2SessionRequest.$Shape;
         *   (properties?: signrpc.MuSig2SessionRequest.$Properties): signrpc.MuSig2SessionRequest;
         * }}
         */
        MuSig2SessionRequest.create = function(properties) {
            return new MuSig2SessionRequest(properties);
        };

        /**
         * Encodes the specified MuSig2SessionRequest message. Does not implicitly {@link signrpc.MuSig2SessionRequest.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2SessionRequest
         * @static
         * @param {signrpc.MuSig2SessionRequest.$Properties} message MuSig2SessionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2SessionRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                $root.signrpc.KeyLocator.encode(message.key_loc, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.all_signer_pubkeys != null && message.all_signer_pubkeys.length)
                for (let i = 0; i < message.all_signer_pubkeys.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.all_signer_pubkeys[i]);
            if (message.other_signer_public_nonces != null && message.other_signer_public_nonces.length)
                for (let i = 0; i < message.other_signer_public_nonces.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.other_signer_public_nonces[i]);
            if (message.tweaks != null && message.tweaks.length)
                for (let i = 0; i < message.tweaks.length; ++i)
                    $root.signrpc.TweakDesc.encode(message.tweaks[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.taproot_tweak != null && $Object.hasOwnProperty.call(message, "taproot_tweak"))
                $root.signrpc.TaprootTweakDesc.encode(message.taproot_tweak, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.version);
            if (message.pregenerated_local_nonce != null && $Object.hasOwnProperty.call(message, "pregenerated_local_nonce") && message.pregenerated_local_nonce.length)
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.pregenerated_local_nonce);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2SessionRequest message, length delimited. Does not implicitly {@link signrpc.MuSig2SessionRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2SessionRequest
         * @static
         * @param {signrpc.MuSig2SessionRequest.$Properties} message MuSig2SessionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2SessionRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2SessionRequest message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2SessionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2SessionRequest & signrpc.MuSig2SessionRequest.$Shape} MuSig2SessionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2SessionRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2SessionRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.key_loc = $root.signrpc.KeyLocator.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key_loc);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.all_signer_pubkeys && message.all_signer_pubkeys.length))
                            message.all_signer_pubkeys = [];
                        message.all_signer_pubkeys.push(reader.bytes());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.other_signer_public_nonces && message.other_signer_public_nonces.length))
                            message.other_signer_public_nonces = [];
                        message.other_signer_public_nonces.push(reader.bytes());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tweaks && message.tweaks.length))
                            message.tweaks = [];
                        message.tweaks.push($root.signrpc.TweakDesc.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.taproot_tweak = $root.signrpc.TaprootTweakDesc.decode(reader, reader.uint32(), $undefined, _depth + 1, message.taproot_tweak);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pregenerated_local_nonce = value;
                        else
                            delete message.pregenerated_local_nonce;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2SessionRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2SessionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2SessionRequest & signrpc.MuSig2SessionRequest.$Shape} MuSig2SessionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2SessionRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2SessionRequest message.
         * @function verify
         * @memberof signrpc.MuSig2SessionRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2SessionRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc")) {
                let error = $root.signrpc.KeyLocator.verify(message.key_loc, _depth + 1);
                if (error)
                    return "key_loc." + error;
            }
            if (message.all_signer_pubkeys != null && $Object.hasOwnProperty.call(message, "all_signer_pubkeys")) {
                if (!$Array.isArray(message.all_signer_pubkeys))
                    return "all_signer_pubkeys: array expected";
                for (let i = 0; i < message.all_signer_pubkeys.length; ++i)
                    if (!(message.all_signer_pubkeys[i] && typeof message.all_signer_pubkeys[i].length === "number" || $util.isString(message.all_signer_pubkeys[i])))
                        return "all_signer_pubkeys: buffer[] expected";
            }
            if (message.other_signer_public_nonces != null && $Object.hasOwnProperty.call(message, "other_signer_public_nonces")) {
                if (!$Array.isArray(message.other_signer_public_nonces))
                    return "other_signer_public_nonces: array expected";
                for (let i = 0; i < message.other_signer_public_nonces.length; ++i)
                    if (!(message.other_signer_public_nonces[i] && typeof message.other_signer_public_nonces[i].length === "number" || $util.isString(message.other_signer_public_nonces[i])))
                        return "other_signer_public_nonces: buffer[] expected";
            }
            if (message.tweaks != null && $Object.hasOwnProperty.call(message, "tweaks")) {
                if (!$Array.isArray(message.tweaks))
                    return "tweaks: array expected";
                for (let i = 0; i < message.tweaks.length; ++i) {
                    let error = $root.signrpc.TweakDesc.verify(message.tweaks[i], _depth + 1);
                    if (error)
                        return "tweaks." + error;
                }
            }
            if (message.taproot_tweak != null && $Object.hasOwnProperty.call(message, "taproot_tweak")) {
                let error = $root.signrpc.TaprootTweakDesc.verify(message.taproot_tweak, _depth + 1);
                if (error)
                    return "taproot_tweak." + error;
            }
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                if (typeof message.version !== "number" || (message.version | 0) !== message.version)
                    return "version: enum value expected";
            if (message.pregenerated_local_nonce != null && $Object.hasOwnProperty.call(message, "pregenerated_local_nonce"))
                if (!(message.pregenerated_local_nonce && typeof message.pregenerated_local_nonce.length === "number" || $util.isString(message.pregenerated_local_nonce)))
                    return "pregenerated_local_nonce: buffer expected";
            return null;
        };

        /**
         * Creates a MuSig2SessionRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2SessionRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2SessionRequest} MuSig2SessionRequest
         */
        MuSig2SessionRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2SessionRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2SessionRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2SessionRequest();
            if (object.key_loc != null) {
                if (!$util.isObject(object.key_loc))
                    throw $TypeError(".signrpc.MuSig2SessionRequest.key_loc: object expected");
                message.key_loc = $root.signrpc.KeyLocator.fromObject(object.key_loc, _depth + 1);
            }
            if (object.all_signer_pubkeys) {
                if (!$Array.isArray(object.all_signer_pubkeys))
                    throw $TypeError(".signrpc.MuSig2SessionRequest.all_signer_pubkeys: array expected");
                message.all_signer_pubkeys = $Array(object.all_signer_pubkeys.length);
                for (let i = 0; i < object.all_signer_pubkeys.length; ++i)
                    if (typeof object.all_signer_pubkeys[i] === "string")
                        $util.base64.decode(object.all_signer_pubkeys[i], message.all_signer_pubkeys[i] = $util.newBuffer($util.base64.length(object.all_signer_pubkeys[i])), 0);
                    else if (object.all_signer_pubkeys[i].length >= 0)
                        message.all_signer_pubkeys[i] = object.all_signer_pubkeys[i];
            }
            if (object.other_signer_public_nonces) {
                if (!$Array.isArray(object.other_signer_public_nonces))
                    throw $TypeError(".signrpc.MuSig2SessionRequest.other_signer_public_nonces: array expected");
                message.other_signer_public_nonces = $Array(object.other_signer_public_nonces.length);
                for (let i = 0; i < object.other_signer_public_nonces.length; ++i)
                    if (typeof object.other_signer_public_nonces[i] === "string")
                        $util.base64.decode(object.other_signer_public_nonces[i], message.other_signer_public_nonces[i] = $util.newBuffer($util.base64.length(object.other_signer_public_nonces[i])), 0);
                    else if (object.other_signer_public_nonces[i].length >= 0)
                        message.other_signer_public_nonces[i] = object.other_signer_public_nonces[i];
            }
            if (object.tweaks) {
                if (!$Array.isArray(object.tweaks))
                    throw $TypeError(".signrpc.MuSig2SessionRequest.tweaks: array expected");
                message.tweaks = $Array(object.tweaks.length);
                for (let i = 0; i < object.tweaks.length; ++i) {
                    if (!$util.isObject(object.tweaks[i]))
                        throw $TypeError(".signrpc.MuSig2SessionRequest.tweaks: object expected");
                    message.tweaks[i] = $root.signrpc.TweakDesc.fromObject(object.tweaks[i], _depth + 1);
                }
            }
            if (object.taproot_tweak != null) {
                if (!$util.isObject(object.taproot_tweak))
                    throw $TypeError(".signrpc.MuSig2SessionRequest.taproot_tweak: object expected");
                message.taproot_tweak = $root.signrpc.TaprootTweakDesc.fromObject(object.taproot_tweak, _depth + 1);
            }
            if (object.version !== 0 && (typeof object.version !== "string" || $root.signrpc.MuSig2Version[object.version] !== 0))
                switch (object.version) {
                case "MUSIG2_VERSION_UNDEFINED":
                case 0:
                    message.version = 0;
                    break;
                case "MUSIG2_VERSION_V040":
                case 1:
                    message.version = 1;
                    break;
                case "MUSIG2_VERSION_V100RC2":
                case 2:
                    message.version = 2;
                    break;
                default:
                    if (typeof object.version === "number" && (object.version | 0) === object.version)
                        message.version = object.version;
                }
            if (object.pregenerated_local_nonce != null)
                if (object.pregenerated_local_nonce.length)
                    if (typeof object.pregenerated_local_nonce === "string")
                        $util.base64.decode(object.pregenerated_local_nonce, message.pregenerated_local_nonce = $util.newBuffer($util.base64.length(object.pregenerated_local_nonce)), 0);
                    else if (object.pregenerated_local_nonce.length >= 0)
                        message.pregenerated_local_nonce = object.pregenerated_local_nonce;
            return message;
        };

        /**
         * Creates a plain object from a MuSig2SessionRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2SessionRequest
         * @static
         * @param {signrpc.MuSig2SessionRequest} message MuSig2SessionRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2SessionRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.all_signer_pubkeys = [];
                object.other_signer_public_nonces = [];
                object.tweaks = [];
            }
            if (options.defaults) {
                object.key_loc = null;
                object.taproot_tweak = null;
                object.version = options.enums === $String ? "MUSIG2_VERSION_UNDEFINED" : 0;
                if (options.bytes === $String)
                    object.pregenerated_local_nonce = "";
                else {
                    object.pregenerated_local_nonce = [];
                    if (options.bytes !== $Array)
                        object.pregenerated_local_nonce = $util.newBuffer(object.pregenerated_local_nonce);
                }
            }
            if (message.key_loc != null && $Object.hasOwnProperty.call(message, "key_loc"))
                object.key_loc = $root.signrpc.KeyLocator.toObject(message.key_loc, options, _depth + 1);
            if (message.all_signer_pubkeys && message.all_signer_pubkeys.length) {
                object.all_signer_pubkeys = $Array(message.all_signer_pubkeys.length);
                for (let j = 0; j < message.all_signer_pubkeys.length; ++j)
                    object.all_signer_pubkeys[j] = options.bytes === $String ? $util.base64.encode(message.all_signer_pubkeys[j], 0, message.all_signer_pubkeys[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.all_signer_pubkeys[j]) : message.all_signer_pubkeys[j];
            }
            if (message.other_signer_public_nonces && message.other_signer_public_nonces.length) {
                object.other_signer_public_nonces = $Array(message.other_signer_public_nonces.length);
                for (let j = 0; j < message.other_signer_public_nonces.length; ++j)
                    object.other_signer_public_nonces[j] = options.bytes === $String ? $util.base64.encode(message.other_signer_public_nonces[j], 0, message.other_signer_public_nonces[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.other_signer_public_nonces[j]) : message.other_signer_public_nonces[j];
            }
            if (message.tweaks && message.tweaks.length) {
                object.tweaks = $Array(message.tweaks.length);
                for (let j = 0; j < message.tweaks.length; ++j)
                    object.tweaks[j] = $root.signrpc.TweakDesc.toObject(message.tweaks[j], options, _depth + 1);
            }
            if (message.taproot_tweak != null && $Object.hasOwnProperty.call(message, "taproot_tweak"))
                object.taproot_tweak = $root.signrpc.TaprootTweakDesc.toObject(message.taproot_tweak, options, _depth + 1);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = options.enums === $String ? $root.signrpc.MuSig2Version[message.version] === $undefined ? message.version : $root.signrpc.MuSig2Version[message.version] : message.version;
            if (message.pregenerated_local_nonce != null && $Object.hasOwnProperty.call(message, "pregenerated_local_nonce"))
                object.pregenerated_local_nonce = options.bytes === $String ? $util.base64.encode(message.pregenerated_local_nonce, 0, message.pregenerated_local_nonce.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pregenerated_local_nonce) : message.pregenerated_local_nonce;
            return object;
        };

        /**
         * Converts this MuSig2SessionRequest to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2SessionRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2SessionRequest.prototype.toJSON = function() {
            return MuSig2SessionRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2SessionRequest
         * @function getTypeUrl
         * @memberof signrpc.MuSig2SessionRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2SessionRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2SessionRequest";
        };

        return MuSig2SessionRequest;
    })();

    signrpc.MuSig2SessionResponse = (function() {

        /**
         * Properties of a MuSig2SessionResponse.
         * @typedef {Object} signrpc.MuSig2SessionResponse.$Properties
         * @property {Uint8Array|null} [session_id] MuSig2SessionResponse session_id
         * @property {Uint8Array|null} [combined_key] MuSig2SessionResponse combined_key
         * @property {Uint8Array|null} [taproot_internal_key] MuSig2SessionResponse taproot_internal_key
         * @property {Uint8Array|null} [local_public_nonces] MuSig2SessionResponse local_public_nonces
         * @property {boolean|null} [have_all_nonces] MuSig2SessionResponse have_all_nonces
         * @property {signrpc.MuSig2Version|null} [version] MuSig2SessionResponse version
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2SessionResponse.
         * @memberof signrpc
         * @interface IMuSig2SessionResponse
         * @augments signrpc.MuSig2SessionResponse.$Properties
         * @deprecated Use signrpc.MuSig2SessionResponse.$Properties instead.
         */

        /**
         * Shape of a MuSig2SessionResponse.
         * @typedef {signrpc.MuSig2SessionResponse.$Properties} signrpc.MuSig2SessionResponse.$Shape
         */

        /**
         * Constructs a new MuSig2SessionResponse.
         * @memberof signrpc
         * @classdesc Represents a MuSig2SessionResponse.
         * @constructor
         * @param {signrpc.MuSig2SessionResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2SessionResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2SessionResponse session_id.
         * @member {Uint8Array} session_id
         * @memberof signrpc.MuSig2SessionResponse
         * @instance
         */
        MuSig2SessionResponse.prototype.session_id = $util.newBuffer([]);

        /**
         * MuSig2SessionResponse combined_key.
         * @member {Uint8Array} combined_key
         * @memberof signrpc.MuSig2SessionResponse
         * @instance
         */
        MuSig2SessionResponse.prototype.combined_key = $util.newBuffer([]);

        /**
         * MuSig2SessionResponse taproot_internal_key.
         * @member {Uint8Array} taproot_internal_key
         * @memberof signrpc.MuSig2SessionResponse
         * @instance
         */
        MuSig2SessionResponse.prototype.taproot_internal_key = $util.newBuffer([]);

        /**
         * MuSig2SessionResponse local_public_nonces.
         * @member {Uint8Array} local_public_nonces
         * @memberof signrpc.MuSig2SessionResponse
         * @instance
         */
        MuSig2SessionResponse.prototype.local_public_nonces = $util.newBuffer([]);

        /**
         * MuSig2SessionResponse have_all_nonces.
         * @member {boolean} have_all_nonces
         * @memberof signrpc.MuSig2SessionResponse
         * @instance
         */
        MuSig2SessionResponse.prototype.have_all_nonces = false;

        /**
         * MuSig2SessionResponse version.
         * @member {signrpc.MuSig2Version} version
         * @memberof signrpc.MuSig2SessionResponse
         * @instance
         */
        MuSig2SessionResponse.prototype.version = 0;

        /**
         * Creates a new MuSig2SessionResponse instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2SessionResponse
         * @static
         * @param {signrpc.MuSig2SessionResponse.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2SessionResponse} MuSig2SessionResponse instance
         * @type {{
         *   (properties: signrpc.MuSig2SessionResponse.$Shape): signrpc.MuSig2SessionResponse & signrpc.MuSig2SessionResponse.$Shape;
         *   (properties?: signrpc.MuSig2SessionResponse.$Properties): signrpc.MuSig2SessionResponse;
         * }}
         */
        MuSig2SessionResponse.create = function(properties) {
            return new MuSig2SessionResponse(properties);
        };

        /**
         * Encodes the specified MuSig2SessionResponse message. Does not implicitly {@link signrpc.MuSig2SessionResponse.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2SessionResponse
         * @static
         * @param {signrpc.MuSig2SessionResponse.$Properties} message MuSig2SessionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2SessionResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id") && message.session_id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.session_id);
            if (message.combined_key != null && $Object.hasOwnProperty.call(message, "combined_key") && message.combined_key.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.combined_key);
            if (message.taproot_internal_key != null && $Object.hasOwnProperty.call(message, "taproot_internal_key") && message.taproot_internal_key.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.taproot_internal_key);
            if (message.local_public_nonces != null && $Object.hasOwnProperty.call(message, "local_public_nonces") && message.local_public_nonces.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.local_public_nonces);
            if (message.have_all_nonces != null && $Object.hasOwnProperty.call(message, "have_all_nonces") && message.have_all_nonces !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.have_all_nonces);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.version);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2SessionResponse message, length delimited. Does not implicitly {@link signrpc.MuSig2SessionResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2SessionResponse
         * @static
         * @param {signrpc.MuSig2SessionResponse.$Properties} message MuSig2SessionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2SessionResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2SessionResponse message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2SessionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2SessionResponse & signrpc.MuSig2SessionResponse.$Shape} MuSig2SessionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2SessionResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2SessionResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.session_id = value;
                        else
                            delete message.session_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.combined_key = value;
                        else
                            delete message.combined_key;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.taproot_internal_key = value;
                        else
                            delete message.taproot_internal_key;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.local_public_nonces = value;
                        else
                            delete message.local_public_nonces;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_all_nonces = value;
                        else
                            delete message.have_all_nonces;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2SessionResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2SessionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2SessionResponse & signrpc.MuSig2SessionResponse.$Shape} MuSig2SessionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2SessionResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2SessionResponse message.
         * @function verify
         * @memberof signrpc.MuSig2SessionResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2SessionResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                if (!(message.session_id && typeof message.session_id.length === "number" || $util.isString(message.session_id)))
                    return "session_id: buffer expected";
            if (message.combined_key != null && $Object.hasOwnProperty.call(message, "combined_key"))
                if (!(message.combined_key && typeof message.combined_key.length === "number" || $util.isString(message.combined_key)))
                    return "combined_key: buffer expected";
            if (message.taproot_internal_key != null && $Object.hasOwnProperty.call(message, "taproot_internal_key"))
                if (!(message.taproot_internal_key && typeof message.taproot_internal_key.length === "number" || $util.isString(message.taproot_internal_key)))
                    return "taproot_internal_key: buffer expected";
            if (message.local_public_nonces != null && $Object.hasOwnProperty.call(message, "local_public_nonces"))
                if (!(message.local_public_nonces && typeof message.local_public_nonces.length === "number" || $util.isString(message.local_public_nonces)))
                    return "local_public_nonces: buffer expected";
            if (message.have_all_nonces != null && $Object.hasOwnProperty.call(message, "have_all_nonces"))
                if (typeof message.have_all_nonces !== "boolean")
                    return "have_all_nonces: boolean expected";
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                if (typeof message.version !== "number" || (message.version | 0) !== message.version)
                    return "version: enum value expected";
            return null;
        };

        /**
         * Creates a MuSig2SessionResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2SessionResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2SessionResponse} MuSig2SessionResponse
         */
        MuSig2SessionResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2SessionResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2SessionResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2SessionResponse();
            if (object.session_id != null)
                if (object.session_id.length)
                    if (typeof object.session_id === "string")
                        $util.base64.decode(object.session_id, message.session_id = $util.newBuffer($util.base64.length(object.session_id)), 0);
                    else if (object.session_id.length >= 0)
                        message.session_id = object.session_id;
            if (object.combined_key != null)
                if (object.combined_key.length)
                    if (typeof object.combined_key === "string")
                        $util.base64.decode(object.combined_key, message.combined_key = $util.newBuffer($util.base64.length(object.combined_key)), 0);
                    else if (object.combined_key.length >= 0)
                        message.combined_key = object.combined_key;
            if (object.taproot_internal_key != null)
                if (object.taproot_internal_key.length)
                    if (typeof object.taproot_internal_key === "string")
                        $util.base64.decode(object.taproot_internal_key, message.taproot_internal_key = $util.newBuffer($util.base64.length(object.taproot_internal_key)), 0);
                    else if (object.taproot_internal_key.length >= 0)
                        message.taproot_internal_key = object.taproot_internal_key;
            if (object.local_public_nonces != null)
                if (object.local_public_nonces.length)
                    if (typeof object.local_public_nonces === "string")
                        $util.base64.decode(object.local_public_nonces, message.local_public_nonces = $util.newBuffer($util.base64.length(object.local_public_nonces)), 0);
                    else if (object.local_public_nonces.length >= 0)
                        message.local_public_nonces = object.local_public_nonces;
            if (object.have_all_nonces != null)
                if (object.have_all_nonces)
                    message.have_all_nonces = $Boolean(object.have_all_nonces);
            if (object.version !== 0 && (typeof object.version !== "string" || $root.signrpc.MuSig2Version[object.version] !== 0))
                switch (object.version) {
                case "MUSIG2_VERSION_UNDEFINED":
                case 0:
                    message.version = 0;
                    break;
                case "MUSIG2_VERSION_V040":
                case 1:
                    message.version = 1;
                    break;
                case "MUSIG2_VERSION_V100RC2":
                case 2:
                    message.version = 2;
                    break;
                default:
                    if (typeof object.version === "number" && (object.version | 0) === object.version)
                        message.version = object.version;
                }
            return message;
        };

        /**
         * Creates a plain object from a MuSig2SessionResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2SessionResponse
         * @static
         * @param {signrpc.MuSig2SessionResponse} message MuSig2SessionResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2SessionResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.session_id = "";
                else {
                    object.session_id = [];
                    if (options.bytes !== $Array)
                        object.session_id = $util.newBuffer(object.session_id);
                }
                if (options.bytes === $String)
                    object.combined_key = "";
                else {
                    object.combined_key = [];
                    if (options.bytes !== $Array)
                        object.combined_key = $util.newBuffer(object.combined_key);
                }
                if (options.bytes === $String)
                    object.taproot_internal_key = "";
                else {
                    object.taproot_internal_key = [];
                    if (options.bytes !== $Array)
                        object.taproot_internal_key = $util.newBuffer(object.taproot_internal_key);
                }
                if (options.bytes === $String)
                    object.local_public_nonces = "";
                else {
                    object.local_public_nonces = [];
                    if (options.bytes !== $Array)
                        object.local_public_nonces = $util.newBuffer(object.local_public_nonces);
                }
                object.have_all_nonces = false;
                object.version = options.enums === $String ? "MUSIG2_VERSION_UNDEFINED" : 0;
            }
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                object.session_id = options.bytes === $String ? $util.base64.encode(message.session_id, 0, message.session_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.session_id) : message.session_id;
            if (message.combined_key != null && $Object.hasOwnProperty.call(message, "combined_key"))
                object.combined_key = options.bytes === $String ? $util.base64.encode(message.combined_key, 0, message.combined_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.combined_key) : message.combined_key;
            if (message.taproot_internal_key != null && $Object.hasOwnProperty.call(message, "taproot_internal_key"))
                object.taproot_internal_key = options.bytes === $String ? $util.base64.encode(message.taproot_internal_key, 0, message.taproot_internal_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.taproot_internal_key) : message.taproot_internal_key;
            if (message.local_public_nonces != null && $Object.hasOwnProperty.call(message, "local_public_nonces"))
                object.local_public_nonces = options.bytes === $String ? $util.base64.encode(message.local_public_nonces, 0, message.local_public_nonces.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.local_public_nonces) : message.local_public_nonces;
            if (message.have_all_nonces != null && $Object.hasOwnProperty.call(message, "have_all_nonces"))
                object.have_all_nonces = message.have_all_nonces;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = options.enums === $String ? $root.signrpc.MuSig2Version[message.version] === $undefined ? message.version : $root.signrpc.MuSig2Version[message.version] : message.version;
            return object;
        };

        /**
         * Converts this MuSig2SessionResponse to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2SessionResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2SessionResponse.prototype.toJSON = function() {
            return MuSig2SessionResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2SessionResponse
         * @function getTypeUrl
         * @memberof signrpc.MuSig2SessionResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2SessionResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2SessionResponse";
        };

        return MuSig2SessionResponse;
    })();

    signrpc.MuSig2RegisterNoncesRequest = (function() {

        /**
         * Properties of a MuSig2RegisterNoncesRequest.
         * @typedef {Object} signrpc.MuSig2RegisterNoncesRequest.$Properties
         * @property {Uint8Array|null} [session_id] MuSig2RegisterNoncesRequest session_id
         * @property {Array.<Uint8Array>|null} [other_signer_public_nonces] MuSig2RegisterNoncesRequest other_signer_public_nonces
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2RegisterNoncesRequest.
         * @memberof signrpc
         * @interface IMuSig2RegisterNoncesRequest
         * @augments signrpc.MuSig2RegisterNoncesRequest.$Properties
         * @deprecated Use signrpc.MuSig2RegisterNoncesRequest.$Properties instead.
         */

        /**
         * Shape of a MuSig2RegisterNoncesRequest.
         * @typedef {signrpc.MuSig2RegisterNoncesRequest.$Properties} signrpc.MuSig2RegisterNoncesRequest.$Shape
         */

        /**
         * Constructs a new MuSig2RegisterNoncesRequest.
         * @memberof signrpc
         * @classdesc Represents a MuSig2RegisterNoncesRequest.
         * @constructor
         * @param {signrpc.MuSig2RegisterNoncesRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2RegisterNoncesRequest = function (properties) {
            this.other_signer_public_nonces = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2RegisterNoncesRequest session_id.
         * @member {Uint8Array} session_id
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @instance
         */
        MuSig2RegisterNoncesRequest.prototype.session_id = $util.newBuffer([]);

        /**
         * MuSig2RegisterNoncesRequest other_signer_public_nonces.
         * @member {Array.<Uint8Array>} other_signer_public_nonces
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @instance
         */
        MuSig2RegisterNoncesRequest.prototype.other_signer_public_nonces = $util.emptyArray;

        /**
         * Creates a new MuSig2RegisterNoncesRequest instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @static
         * @param {signrpc.MuSig2RegisterNoncesRequest.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2RegisterNoncesRequest} MuSig2RegisterNoncesRequest instance
         * @type {{
         *   (properties: signrpc.MuSig2RegisterNoncesRequest.$Shape): signrpc.MuSig2RegisterNoncesRequest & signrpc.MuSig2RegisterNoncesRequest.$Shape;
         *   (properties?: signrpc.MuSig2RegisterNoncesRequest.$Properties): signrpc.MuSig2RegisterNoncesRequest;
         * }}
         */
        MuSig2RegisterNoncesRequest.create = function(properties) {
            return new MuSig2RegisterNoncesRequest(properties);
        };

        /**
         * Encodes the specified MuSig2RegisterNoncesRequest message. Does not implicitly {@link signrpc.MuSig2RegisterNoncesRequest.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @static
         * @param {signrpc.MuSig2RegisterNoncesRequest.$Properties} message MuSig2RegisterNoncesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2RegisterNoncesRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id") && message.session_id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.session_id);
            if (message.other_signer_public_nonces != null && message.other_signer_public_nonces.length)
                for (let i = 0; i < message.other_signer_public_nonces.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.other_signer_public_nonces[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2RegisterNoncesRequest message, length delimited. Does not implicitly {@link signrpc.MuSig2RegisterNoncesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @static
         * @param {signrpc.MuSig2RegisterNoncesRequest.$Properties} message MuSig2RegisterNoncesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2RegisterNoncesRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2RegisterNoncesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2RegisterNoncesRequest & signrpc.MuSig2RegisterNoncesRequest.$Shape} MuSig2RegisterNoncesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2RegisterNoncesRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2RegisterNoncesRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.session_id = value;
                        else
                            delete message.session_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.other_signer_public_nonces && message.other_signer_public_nonces.length))
                            message.other_signer_public_nonces = [];
                        message.other_signer_public_nonces.push(reader.bytes());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2RegisterNoncesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2RegisterNoncesRequest & signrpc.MuSig2RegisterNoncesRequest.$Shape} MuSig2RegisterNoncesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2RegisterNoncesRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2RegisterNoncesRequest message.
         * @function verify
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2RegisterNoncesRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                if (!(message.session_id && typeof message.session_id.length === "number" || $util.isString(message.session_id)))
                    return "session_id: buffer expected";
            if (message.other_signer_public_nonces != null && $Object.hasOwnProperty.call(message, "other_signer_public_nonces")) {
                if (!$Array.isArray(message.other_signer_public_nonces))
                    return "other_signer_public_nonces: array expected";
                for (let i = 0; i < message.other_signer_public_nonces.length; ++i)
                    if (!(message.other_signer_public_nonces[i] && typeof message.other_signer_public_nonces[i].length === "number" || $util.isString(message.other_signer_public_nonces[i])))
                        return "other_signer_public_nonces: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates a MuSig2RegisterNoncesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2RegisterNoncesRequest} MuSig2RegisterNoncesRequest
         */
        MuSig2RegisterNoncesRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2RegisterNoncesRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2RegisterNoncesRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2RegisterNoncesRequest();
            if (object.session_id != null)
                if (object.session_id.length)
                    if (typeof object.session_id === "string")
                        $util.base64.decode(object.session_id, message.session_id = $util.newBuffer($util.base64.length(object.session_id)), 0);
                    else if (object.session_id.length >= 0)
                        message.session_id = object.session_id;
            if (object.other_signer_public_nonces) {
                if (!$Array.isArray(object.other_signer_public_nonces))
                    throw $TypeError(".signrpc.MuSig2RegisterNoncesRequest.other_signer_public_nonces: array expected");
                message.other_signer_public_nonces = $Array(object.other_signer_public_nonces.length);
                for (let i = 0; i < object.other_signer_public_nonces.length; ++i)
                    if (typeof object.other_signer_public_nonces[i] === "string")
                        $util.base64.decode(object.other_signer_public_nonces[i], message.other_signer_public_nonces[i] = $util.newBuffer($util.base64.length(object.other_signer_public_nonces[i])), 0);
                    else if (object.other_signer_public_nonces[i].length >= 0)
                        message.other_signer_public_nonces[i] = object.other_signer_public_nonces[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a MuSig2RegisterNoncesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @static
         * @param {signrpc.MuSig2RegisterNoncesRequest} message MuSig2RegisterNoncesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2RegisterNoncesRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.other_signer_public_nonces = [];
            if (options.defaults)
                if (options.bytes === $String)
                    object.session_id = "";
                else {
                    object.session_id = [];
                    if (options.bytes !== $Array)
                        object.session_id = $util.newBuffer(object.session_id);
                }
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                object.session_id = options.bytes === $String ? $util.base64.encode(message.session_id, 0, message.session_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.session_id) : message.session_id;
            if (message.other_signer_public_nonces && message.other_signer_public_nonces.length) {
                object.other_signer_public_nonces = $Array(message.other_signer_public_nonces.length);
                for (let j = 0; j < message.other_signer_public_nonces.length; ++j)
                    object.other_signer_public_nonces[j] = options.bytes === $String ? $util.base64.encode(message.other_signer_public_nonces[j], 0, message.other_signer_public_nonces[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.other_signer_public_nonces[j]) : message.other_signer_public_nonces[j];
            }
            return object;
        };

        /**
         * Converts this MuSig2RegisterNoncesRequest to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2RegisterNoncesRequest.prototype.toJSON = function() {
            return MuSig2RegisterNoncesRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2RegisterNoncesRequest
         * @function getTypeUrl
         * @memberof signrpc.MuSig2RegisterNoncesRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2RegisterNoncesRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2RegisterNoncesRequest";
        };

        return MuSig2RegisterNoncesRequest;
    })();

    signrpc.MuSig2RegisterNoncesResponse = (function() {

        /**
         * Properties of a MuSig2RegisterNoncesResponse.
         * @typedef {Object} signrpc.MuSig2RegisterNoncesResponse.$Properties
         * @property {boolean|null} [have_all_nonces] MuSig2RegisterNoncesResponse have_all_nonces
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2RegisterNoncesResponse.
         * @memberof signrpc
         * @interface IMuSig2RegisterNoncesResponse
         * @augments signrpc.MuSig2RegisterNoncesResponse.$Properties
         * @deprecated Use signrpc.MuSig2RegisterNoncesResponse.$Properties instead.
         */

        /**
         * Shape of a MuSig2RegisterNoncesResponse.
         * @typedef {signrpc.MuSig2RegisterNoncesResponse.$Properties} signrpc.MuSig2RegisterNoncesResponse.$Shape
         */

        /**
         * Constructs a new MuSig2RegisterNoncesResponse.
         * @memberof signrpc
         * @classdesc Represents a MuSig2RegisterNoncesResponse.
         * @constructor
         * @param {signrpc.MuSig2RegisterNoncesResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2RegisterNoncesResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2RegisterNoncesResponse have_all_nonces.
         * @member {boolean} have_all_nonces
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @instance
         */
        MuSig2RegisterNoncesResponse.prototype.have_all_nonces = false;

        /**
         * Creates a new MuSig2RegisterNoncesResponse instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @static
         * @param {signrpc.MuSig2RegisterNoncesResponse.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2RegisterNoncesResponse} MuSig2RegisterNoncesResponse instance
         * @type {{
         *   (properties: signrpc.MuSig2RegisterNoncesResponse.$Shape): signrpc.MuSig2RegisterNoncesResponse & signrpc.MuSig2RegisterNoncesResponse.$Shape;
         *   (properties?: signrpc.MuSig2RegisterNoncesResponse.$Properties): signrpc.MuSig2RegisterNoncesResponse;
         * }}
         */
        MuSig2RegisterNoncesResponse.create = function(properties) {
            return new MuSig2RegisterNoncesResponse(properties);
        };

        /**
         * Encodes the specified MuSig2RegisterNoncesResponse message. Does not implicitly {@link signrpc.MuSig2RegisterNoncesResponse.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @static
         * @param {signrpc.MuSig2RegisterNoncesResponse.$Properties} message MuSig2RegisterNoncesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2RegisterNoncesResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.have_all_nonces != null && $Object.hasOwnProperty.call(message, "have_all_nonces") && message.have_all_nonces !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.have_all_nonces);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2RegisterNoncesResponse message, length delimited. Does not implicitly {@link signrpc.MuSig2RegisterNoncesResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @static
         * @param {signrpc.MuSig2RegisterNoncesResponse.$Properties} message MuSig2RegisterNoncesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2RegisterNoncesResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2RegisterNoncesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2RegisterNoncesResponse & signrpc.MuSig2RegisterNoncesResponse.$Shape} MuSig2RegisterNoncesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2RegisterNoncesResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2RegisterNoncesResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_all_nonces = value;
                        else
                            delete message.have_all_nonces;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2RegisterNoncesResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2RegisterNoncesResponse & signrpc.MuSig2RegisterNoncesResponse.$Shape} MuSig2RegisterNoncesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2RegisterNoncesResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2RegisterNoncesResponse message.
         * @function verify
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2RegisterNoncesResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.have_all_nonces != null && $Object.hasOwnProperty.call(message, "have_all_nonces"))
                if (typeof message.have_all_nonces !== "boolean")
                    return "have_all_nonces: boolean expected";
            return null;
        };

        /**
         * Creates a MuSig2RegisterNoncesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2RegisterNoncesResponse} MuSig2RegisterNoncesResponse
         */
        MuSig2RegisterNoncesResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2RegisterNoncesResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2RegisterNoncesResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2RegisterNoncesResponse();
            if (object.have_all_nonces != null)
                if (object.have_all_nonces)
                    message.have_all_nonces = $Boolean(object.have_all_nonces);
            return message;
        };

        /**
         * Creates a plain object from a MuSig2RegisterNoncesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @static
         * @param {signrpc.MuSig2RegisterNoncesResponse} message MuSig2RegisterNoncesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2RegisterNoncesResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.have_all_nonces = false;
            if (message.have_all_nonces != null && $Object.hasOwnProperty.call(message, "have_all_nonces"))
                object.have_all_nonces = message.have_all_nonces;
            return object;
        };

        /**
         * Converts this MuSig2RegisterNoncesResponse to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2RegisterNoncesResponse.prototype.toJSON = function() {
            return MuSig2RegisterNoncesResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2RegisterNoncesResponse
         * @function getTypeUrl
         * @memberof signrpc.MuSig2RegisterNoncesResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2RegisterNoncesResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2RegisterNoncesResponse";
        };

        return MuSig2RegisterNoncesResponse;
    })();

    signrpc.MuSig2SignRequest = (function() {

        /**
         * Properties of a MuSig2SignRequest.
         * @typedef {Object} signrpc.MuSig2SignRequest.$Properties
         * @property {Uint8Array|null} [session_id] MuSig2SignRequest session_id
         * @property {Uint8Array|null} [message_digest] MuSig2SignRequest message_digest
         * @property {boolean|null} [cleanup] MuSig2SignRequest cleanup
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2SignRequest.
         * @memberof signrpc
         * @interface IMuSig2SignRequest
         * @augments signrpc.MuSig2SignRequest.$Properties
         * @deprecated Use signrpc.MuSig2SignRequest.$Properties instead.
         */

        /**
         * Shape of a MuSig2SignRequest.
         * @typedef {signrpc.MuSig2SignRequest.$Properties} signrpc.MuSig2SignRequest.$Shape
         */

        /**
         * Constructs a new MuSig2SignRequest.
         * @memberof signrpc
         * @classdesc Represents a MuSig2SignRequest.
         * @constructor
         * @param {signrpc.MuSig2SignRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2SignRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2SignRequest session_id.
         * @member {Uint8Array} session_id
         * @memberof signrpc.MuSig2SignRequest
         * @instance
         */
        MuSig2SignRequest.prototype.session_id = $util.newBuffer([]);

        /**
         * MuSig2SignRequest message_digest.
         * @member {Uint8Array} message_digest
         * @memberof signrpc.MuSig2SignRequest
         * @instance
         */
        MuSig2SignRequest.prototype.message_digest = $util.newBuffer([]);

        /**
         * MuSig2SignRequest cleanup.
         * @member {boolean} cleanup
         * @memberof signrpc.MuSig2SignRequest
         * @instance
         */
        MuSig2SignRequest.prototype.cleanup = false;

        /**
         * Creates a new MuSig2SignRequest instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2SignRequest
         * @static
         * @param {signrpc.MuSig2SignRequest.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2SignRequest} MuSig2SignRequest instance
         * @type {{
         *   (properties: signrpc.MuSig2SignRequest.$Shape): signrpc.MuSig2SignRequest & signrpc.MuSig2SignRequest.$Shape;
         *   (properties?: signrpc.MuSig2SignRequest.$Properties): signrpc.MuSig2SignRequest;
         * }}
         */
        MuSig2SignRequest.create = function(properties) {
            return new MuSig2SignRequest(properties);
        };

        /**
         * Encodes the specified MuSig2SignRequest message. Does not implicitly {@link signrpc.MuSig2SignRequest.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2SignRequest
         * @static
         * @param {signrpc.MuSig2SignRequest.$Properties} message MuSig2SignRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2SignRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id") && message.session_id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.session_id);
            if (message.message_digest != null && $Object.hasOwnProperty.call(message, "message_digest") && message.message_digest.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.message_digest);
            if (message.cleanup != null && $Object.hasOwnProperty.call(message, "cleanup") && message.cleanup !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.cleanup);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2SignRequest message, length delimited. Does not implicitly {@link signrpc.MuSig2SignRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2SignRequest
         * @static
         * @param {signrpc.MuSig2SignRequest.$Properties} message MuSig2SignRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2SignRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2SignRequest message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2SignRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2SignRequest & signrpc.MuSig2SignRequest.$Shape} MuSig2SignRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2SignRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2SignRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.session_id = value;
                        else
                            delete message.session_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.message_digest = value;
                        else
                            delete message.message_digest;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cleanup = value;
                        else
                            delete message.cleanup;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2SignRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2SignRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2SignRequest & signrpc.MuSig2SignRequest.$Shape} MuSig2SignRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2SignRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2SignRequest message.
         * @function verify
         * @memberof signrpc.MuSig2SignRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2SignRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                if (!(message.session_id && typeof message.session_id.length === "number" || $util.isString(message.session_id)))
                    return "session_id: buffer expected";
            if (message.message_digest != null && $Object.hasOwnProperty.call(message, "message_digest"))
                if (!(message.message_digest && typeof message.message_digest.length === "number" || $util.isString(message.message_digest)))
                    return "message_digest: buffer expected";
            if (message.cleanup != null && $Object.hasOwnProperty.call(message, "cleanup"))
                if (typeof message.cleanup !== "boolean")
                    return "cleanup: boolean expected";
            return null;
        };

        /**
         * Creates a MuSig2SignRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2SignRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2SignRequest} MuSig2SignRequest
         */
        MuSig2SignRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2SignRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2SignRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2SignRequest();
            if (object.session_id != null)
                if (object.session_id.length)
                    if (typeof object.session_id === "string")
                        $util.base64.decode(object.session_id, message.session_id = $util.newBuffer($util.base64.length(object.session_id)), 0);
                    else if (object.session_id.length >= 0)
                        message.session_id = object.session_id;
            if (object.message_digest != null)
                if (object.message_digest.length)
                    if (typeof object.message_digest === "string")
                        $util.base64.decode(object.message_digest, message.message_digest = $util.newBuffer($util.base64.length(object.message_digest)), 0);
                    else if (object.message_digest.length >= 0)
                        message.message_digest = object.message_digest;
            if (object.cleanup != null)
                if (object.cleanup)
                    message.cleanup = $Boolean(object.cleanup);
            return message;
        };

        /**
         * Creates a plain object from a MuSig2SignRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2SignRequest
         * @static
         * @param {signrpc.MuSig2SignRequest} message MuSig2SignRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2SignRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.session_id = "";
                else {
                    object.session_id = [];
                    if (options.bytes !== $Array)
                        object.session_id = $util.newBuffer(object.session_id);
                }
                if (options.bytes === $String)
                    object.message_digest = "";
                else {
                    object.message_digest = [];
                    if (options.bytes !== $Array)
                        object.message_digest = $util.newBuffer(object.message_digest);
                }
                object.cleanup = false;
            }
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                object.session_id = options.bytes === $String ? $util.base64.encode(message.session_id, 0, message.session_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.session_id) : message.session_id;
            if (message.message_digest != null && $Object.hasOwnProperty.call(message, "message_digest"))
                object.message_digest = options.bytes === $String ? $util.base64.encode(message.message_digest, 0, message.message_digest.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.message_digest) : message.message_digest;
            if (message.cleanup != null && $Object.hasOwnProperty.call(message, "cleanup"))
                object.cleanup = message.cleanup;
            return object;
        };

        /**
         * Converts this MuSig2SignRequest to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2SignRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2SignRequest.prototype.toJSON = function() {
            return MuSig2SignRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2SignRequest
         * @function getTypeUrl
         * @memberof signrpc.MuSig2SignRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2SignRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2SignRequest";
        };

        return MuSig2SignRequest;
    })();

    signrpc.MuSig2SignResponse = (function() {

        /**
         * Properties of a MuSig2SignResponse.
         * @typedef {Object} signrpc.MuSig2SignResponse.$Properties
         * @property {Uint8Array|null} [local_partial_signature] MuSig2SignResponse local_partial_signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2SignResponse.
         * @memberof signrpc
         * @interface IMuSig2SignResponse
         * @augments signrpc.MuSig2SignResponse.$Properties
         * @deprecated Use signrpc.MuSig2SignResponse.$Properties instead.
         */

        /**
         * Shape of a MuSig2SignResponse.
         * @typedef {signrpc.MuSig2SignResponse.$Properties} signrpc.MuSig2SignResponse.$Shape
         */

        /**
         * Constructs a new MuSig2SignResponse.
         * @memberof signrpc
         * @classdesc Represents a MuSig2SignResponse.
         * @constructor
         * @param {signrpc.MuSig2SignResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2SignResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2SignResponse local_partial_signature.
         * @member {Uint8Array} local_partial_signature
         * @memberof signrpc.MuSig2SignResponse
         * @instance
         */
        MuSig2SignResponse.prototype.local_partial_signature = $util.newBuffer([]);

        /**
         * Creates a new MuSig2SignResponse instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2SignResponse
         * @static
         * @param {signrpc.MuSig2SignResponse.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2SignResponse} MuSig2SignResponse instance
         * @type {{
         *   (properties: signrpc.MuSig2SignResponse.$Shape): signrpc.MuSig2SignResponse & signrpc.MuSig2SignResponse.$Shape;
         *   (properties?: signrpc.MuSig2SignResponse.$Properties): signrpc.MuSig2SignResponse;
         * }}
         */
        MuSig2SignResponse.create = function(properties) {
            return new MuSig2SignResponse(properties);
        };

        /**
         * Encodes the specified MuSig2SignResponse message. Does not implicitly {@link signrpc.MuSig2SignResponse.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2SignResponse
         * @static
         * @param {signrpc.MuSig2SignResponse.$Properties} message MuSig2SignResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2SignResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.local_partial_signature != null && $Object.hasOwnProperty.call(message, "local_partial_signature") && message.local_partial_signature.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.local_partial_signature);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2SignResponse message, length delimited. Does not implicitly {@link signrpc.MuSig2SignResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2SignResponse
         * @static
         * @param {signrpc.MuSig2SignResponse.$Properties} message MuSig2SignResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2SignResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2SignResponse message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2SignResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2SignResponse & signrpc.MuSig2SignResponse.$Shape} MuSig2SignResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2SignResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2SignResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.local_partial_signature = value;
                        else
                            delete message.local_partial_signature;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2SignResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2SignResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2SignResponse & signrpc.MuSig2SignResponse.$Shape} MuSig2SignResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2SignResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2SignResponse message.
         * @function verify
         * @memberof signrpc.MuSig2SignResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2SignResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.local_partial_signature != null && $Object.hasOwnProperty.call(message, "local_partial_signature"))
                if (!(message.local_partial_signature && typeof message.local_partial_signature.length === "number" || $util.isString(message.local_partial_signature)))
                    return "local_partial_signature: buffer expected";
            return null;
        };

        /**
         * Creates a MuSig2SignResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2SignResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2SignResponse} MuSig2SignResponse
         */
        MuSig2SignResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2SignResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2SignResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2SignResponse();
            if (object.local_partial_signature != null)
                if (object.local_partial_signature.length)
                    if (typeof object.local_partial_signature === "string")
                        $util.base64.decode(object.local_partial_signature, message.local_partial_signature = $util.newBuffer($util.base64.length(object.local_partial_signature)), 0);
                    else if (object.local_partial_signature.length >= 0)
                        message.local_partial_signature = object.local_partial_signature;
            return message;
        };

        /**
         * Creates a plain object from a MuSig2SignResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2SignResponse
         * @static
         * @param {signrpc.MuSig2SignResponse} message MuSig2SignResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2SignResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.local_partial_signature = "";
                else {
                    object.local_partial_signature = [];
                    if (options.bytes !== $Array)
                        object.local_partial_signature = $util.newBuffer(object.local_partial_signature);
                }
            if (message.local_partial_signature != null && $Object.hasOwnProperty.call(message, "local_partial_signature"))
                object.local_partial_signature = options.bytes === $String ? $util.base64.encode(message.local_partial_signature, 0, message.local_partial_signature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.local_partial_signature) : message.local_partial_signature;
            return object;
        };

        /**
         * Converts this MuSig2SignResponse to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2SignResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2SignResponse.prototype.toJSON = function() {
            return MuSig2SignResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2SignResponse
         * @function getTypeUrl
         * @memberof signrpc.MuSig2SignResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2SignResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2SignResponse";
        };

        return MuSig2SignResponse;
    })();

    signrpc.MuSig2CombineSigRequest = (function() {

        /**
         * Properties of a MuSig2CombineSigRequest.
         * @typedef {Object} signrpc.MuSig2CombineSigRequest.$Properties
         * @property {Uint8Array|null} [session_id] MuSig2CombineSigRequest session_id
         * @property {Array.<Uint8Array>|null} [other_partial_signatures] MuSig2CombineSigRequest other_partial_signatures
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2CombineSigRequest.
         * @memberof signrpc
         * @interface IMuSig2CombineSigRequest
         * @augments signrpc.MuSig2CombineSigRequest.$Properties
         * @deprecated Use signrpc.MuSig2CombineSigRequest.$Properties instead.
         */

        /**
         * Shape of a MuSig2CombineSigRequest.
         * @typedef {signrpc.MuSig2CombineSigRequest.$Properties} signrpc.MuSig2CombineSigRequest.$Shape
         */

        /**
         * Constructs a new MuSig2CombineSigRequest.
         * @memberof signrpc
         * @classdesc Represents a MuSig2CombineSigRequest.
         * @constructor
         * @param {signrpc.MuSig2CombineSigRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2CombineSigRequest = function (properties) {
            this.other_partial_signatures = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2CombineSigRequest session_id.
         * @member {Uint8Array} session_id
         * @memberof signrpc.MuSig2CombineSigRequest
         * @instance
         */
        MuSig2CombineSigRequest.prototype.session_id = $util.newBuffer([]);

        /**
         * MuSig2CombineSigRequest other_partial_signatures.
         * @member {Array.<Uint8Array>} other_partial_signatures
         * @memberof signrpc.MuSig2CombineSigRequest
         * @instance
         */
        MuSig2CombineSigRequest.prototype.other_partial_signatures = $util.emptyArray;

        /**
         * Creates a new MuSig2CombineSigRequest instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2CombineSigRequest
         * @static
         * @param {signrpc.MuSig2CombineSigRequest.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2CombineSigRequest} MuSig2CombineSigRequest instance
         * @type {{
         *   (properties: signrpc.MuSig2CombineSigRequest.$Shape): signrpc.MuSig2CombineSigRequest & signrpc.MuSig2CombineSigRequest.$Shape;
         *   (properties?: signrpc.MuSig2CombineSigRequest.$Properties): signrpc.MuSig2CombineSigRequest;
         * }}
         */
        MuSig2CombineSigRequest.create = function(properties) {
            return new MuSig2CombineSigRequest(properties);
        };

        /**
         * Encodes the specified MuSig2CombineSigRequest message. Does not implicitly {@link signrpc.MuSig2CombineSigRequest.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2CombineSigRequest
         * @static
         * @param {signrpc.MuSig2CombineSigRequest.$Properties} message MuSig2CombineSigRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CombineSigRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id") && message.session_id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.session_id);
            if (message.other_partial_signatures != null && message.other_partial_signatures.length)
                for (let i = 0; i < message.other_partial_signatures.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.other_partial_signatures[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2CombineSigRequest message, length delimited. Does not implicitly {@link signrpc.MuSig2CombineSigRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2CombineSigRequest
         * @static
         * @param {signrpc.MuSig2CombineSigRequest.$Properties} message MuSig2CombineSigRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CombineSigRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2CombineSigRequest message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2CombineSigRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2CombineSigRequest & signrpc.MuSig2CombineSigRequest.$Shape} MuSig2CombineSigRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CombineSigRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2CombineSigRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.session_id = value;
                        else
                            delete message.session_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.other_partial_signatures && message.other_partial_signatures.length))
                            message.other_partial_signatures = [];
                        message.other_partial_signatures.push(reader.bytes());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2CombineSigRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2CombineSigRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2CombineSigRequest & signrpc.MuSig2CombineSigRequest.$Shape} MuSig2CombineSigRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CombineSigRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2CombineSigRequest message.
         * @function verify
         * @memberof signrpc.MuSig2CombineSigRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2CombineSigRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                if (!(message.session_id && typeof message.session_id.length === "number" || $util.isString(message.session_id)))
                    return "session_id: buffer expected";
            if (message.other_partial_signatures != null && $Object.hasOwnProperty.call(message, "other_partial_signatures")) {
                if (!$Array.isArray(message.other_partial_signatures))
                    return "other_partial_signatures: array expected";
                for (let i = 0; i < message.other_partial_signatures.length; ++i)
                    if (!(message.other_partial_signatures[i] && typeof message.other_partial_signatures[i].length === "number" || $util.isString(message.other_partial_signatures[i])))
                        return "other_partial_signatures: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates a MuSig2CombineSigRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2CombineSigRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2CombineSigRequest} MuSig2CombineSigRequest
         */
        MuSig2CombineSigRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2CombineSigRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2CombineSigRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2CombineSigRequest();
            if (object.session_id != null)
                if (object.session_id.length)
                    if (typeof object.session_id === "string")
                        $util.base64.decode(object.session_id, message.session_id = $util.newBuffer($util.base64.length(object.session_id)), 0);
                    else if (object.session_id.length >= 0)
                        message.session_id = object.session_id;
            if (object.other_partial_signatures) {
                if (!$Array.isArray(object.other_partial_signatures))
                    throw $TypeError(".signrpc.MuSig2CombineSigRequest.other_partial_signatures: array expected");
                message.other_partial_signatures = $Array(object.other_partial_signatures.length);
                for (let i = 0; i < object.other_partial_signatures.length; ++i)
                    if (typeof object.other_partial_signatures[i] === "string")
                        $util.base64.decode(object.other_partial_signatures[i], message.other_partial_signatures[i] = $util.newBuffer($util.base64.length(object.other_partial_signatures[i])), 0);
                    else if (object.other_partial_signatures[i].length >= 0)
                        message.other_partial_signatures[i] = object.other_partial_signatures[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a MuSig2CombineSigRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2CombineSigRequest
         * @static
         * @param {signrpc.MuSig2CombineSigRequest} message MuSig2CombineSigRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2CombineSigRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.other_partial_signatures = [];
            if (options.defaults)
                if (options.bytes === $String)
                    object.session_id = "";
                else {
                    object.session_id = [];
                    if (options.bytes !== $Array)
                        object.session_id = $util.newBuffer(object.session_id);
                }
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                object.session_id = options.bytes === $String ? $util.base64.encode(message.session_id, 0, message.session_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.session_id) : message.session_id;
            if (message.other_partial_signatures && message.other_partial_signatures.length) {
                object.other_partial_signatures = $Array(message.other_partial_signatures.length);
                for (let j = 0; j < message.other_partial_signatures.length; ++j)
                    object.other_partial_signatures[j] = options.bytes === $String ? $util.base64.encode(message.other_partial_signatures[j], 0, message.other_partial_signatures[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.other_partial_signatures[j]) : message.other_partial_signatures[j];
            }
            return object;
        };

        /**
         * Converts this MuSig2CombineSigRequest to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2CombineSigRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2CombineSigRequest.prototype.toJSON = function() {
            return MuSig2CombineSigRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2CombineSigRequest
         * @function getTypeUrl
         * @memberof signrpc.MuSig2CombineSigRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2CombineSigRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2CombineSigRequest";
        };

        return MuSig2CombineSigRequest;
    })();

    signrpc.MuSig2CombineSigResponse = (function() {

        /**
         * Properties of a MuSig2CombineSigResponse.
         * @typedef {Object} signrpc.MuSig2CombineSigResponse.$Properties
         * @property {boolean|null} [have_all_signatures] MuSig2CombineSigResponse have_all_signatures
         * @property {Uint8Array|null} [final_signature] MuSig2CombineSigResponse final_signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2CombineSigResponse.
         * @memberof signrpc
         * @interface IMuSig2CombineSigResponse
         * @augments signrpc.MuSig2CombineSigResponse.$Properties
         * @deprecated Use signrpc.MuSig2CombineSigResponse.$Properties instead.
         */

        /**
         * Shape of a MuSig2CombineSigResponse.
         * @typedef {signrpc.MuSig2CombineSigResponse.$Properties} signrpc.MuSig2CombineSigResponse.$Shape
         */

        /**
         * Constructs a new MuSig2CombineSigResponse.
         * @memberof signrpc
         * @classdesc Represents a MuSig2CombineSigResponse.
         * @constructor
         * @param {signrpc.MuSig2CombineSigResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2CombineSigResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2CombineSigResponse have_all_signatures.
         * @member {boolean} have_all_signatures
         * @memberof signrpc.MuSig2CombineSigResponse
         * @instance
         */
        MuSig2CombineSigResponse.prototype.have_all_signatures = false;

        /**
         * MuSig2CombineSigResponse final_signature.
         * @member {Uint8Array} final_signature
         * @memberof signrpc.MuSig2CombineSigResponse
         * @instance
         */
        MuSig2CombineSigResponse.prototype.final_signature = $util.newBuffer([]);

        /**
         * Creates a new MuSig2CombineSigResponse instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2CombineSigResponse
         * @static
         * @param {signrpc.MuSig2CombineSigResponse.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2CombineSigResponse} MuSig2CombineSigResponse instance
         * @type {{
         *   (properties: signrpc.MuSig2CombineSigResponse.$Shape): signrpc.MuSig2CombineSigResponse & signrpc.MuSig2CombineSigResponse.$Shape;
         *   (properties?: signrpc.MuSig2CombineSigResponse.$Properties): signrpc.MuSig2CombineSigResponse;
         * }}
         */
        MuSig2CombineSigResponse.create = function(properties) {
            return new MuSig2CombineSigResponse(properties);
        };

        /**
         * Encodes the specified MuSig2CombineSigResponse message. Does not implicitly {@link signrpc.MuSig2CombineSigResponse.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2CombineSigResponse
         * @static
         * @param {signrpc.MuSig2CombineSigResponse.$Properties} message MuSig2CombineSigResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CombineSigResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.have_all_signatures != null && $Object.hasOwnProperty.call(message, "have_all_signatures") && message.have_all_signatures !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.have_all_signatures);
            if (message.final_signature != null && $Object.hasOwnProperty.call(message, "final_signature") && message.final_signature.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.final_signature);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2CombineSigResponse message, length delimited. Does not implicitly {@link signrpc.MuSig2CombineSigResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2CombineSigResponse
         * @static
         * @param {signrpc.MuSig2CombineSigResponse.$Properties} message MuSig2CombineSigResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CombineSigResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2CombineSigResponse message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2CombineSigResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2CombineSigResponse & signrpc.MuSig2CombineSigResponse.$Shape} MuSig2CombineSigResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CombineSigResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2CombineSigResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_all_signatures = value;
                        else
                            delete message.have_all_signatures;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.final_signature = value;
                        else
                            delete message.final_signature;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2CombineSigResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2CombineSigResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2CombineSigResponse & signrpc.MuSig2CombineSigResponse.$Shape} MuSig2CombineSigResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CombineSigResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2CombineSigResponse message.
         * @function verify
         * @memberof signrpc.MuSig2CombineSigResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2CombineSigResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.have_all_signatures != null && $Object.hasOwnProperty.call(message, "have_all_signatures"))
                if (typeof message.have_all_signatures !== "boolean")
                    return "have_all_signatures: boolean expected";
            if (message.final_signature != null && $Object.hasOwnProperty.call(message, "final_signature"))
                if (!(message.final_signature && typeof message.final_signature.length === "number" || $util.isString(message.final_signature)))
                    return "final_signature: buffer expected";
            return null;
        };

        /**
         * Creates a MuSig2CombineSigResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2CombineSigResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2CombineSigResponse} MuSig2CombineSigResponse
         */
        MuSig2CombineSigResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2CombineSigResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2CombineSigResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2CombineSigResponse();
            if (object.have_all_signatures != null)
                if (object.have_all_signatures)
                    message.have_all_signatures = $Boolean(object.have_all_signatures);
            if (object.final_signature != null)
                if (object.final_signature.length)
                    if (typeof object.final_signature === "string")
                        $util.base64.decode(object.final_signature, message.final_signature = $util.newBuffer($util.base64.length(object.final_signature)), 0);
                    else if (object.final_signature.length >= 0)
                        message.final_signature = object.final_signature;
            return message;
        };

        /**
         * Creates a plain object from a MuSig2CombineSigResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2CombineSigResponse
         * @static
         * @param {signrpc.MuSig2CombineSigResponse} message MuSig2CombineSigResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2CombineSigResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.have_all_signatures = false;
                if (options.bytes === $String)
                    object.final_signature = "";
                else {
                    object.final_signature = [];
                    if (options.bytes !== $Array)
                        object.final_signature = $util.newBuffer(object.final_signature);
                }
            }
            if (message.have_all_signatures != null && $Object.hasOwnProperty.call(message, "have_all_signatures"))
                object.have_all_signatures = message.have_all_signatures;
            if (message.final_signature != null && $Object.hasOwnProperty.call(message, "final_signature"))
                object.final_signature = options.bytes === $String ? $util.base64.encode(message.final_signature, 0, message.final_signature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.final_signature) : message.final_signature;
            return object;
        };

        /**
         * Converts this MuSig2CombineSigResponse to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2CombineSigResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2CombineSigResponse.prototype.toJSON = function() {
            return MuSig2CombineSigResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2CombineSigResponse
         * @function getTypeUrl
         * @memberof signrpc.MuSig2CombineSigResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2CombineSigResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2CombineSigResponse";
        };

        return MuSig2CombineSigResponse;
    })();

    signrpc.MuSig2CleanupRequest = (function() {

        /**
         * Properties of a MuSig2CleanupRequest.
         * @typedef {Object} signrpc.MuSig2CleanupRequest.$Properties
         * @property {Uint8Array|null} [session_id] MuSig2CleanupRequest session_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2CleanupRequest.
         * @memberof signrpc
         * @interface IMuSig2CleanupRequest
         * @augments signrpc.MuSig2CleanupRequest.$Properties
         * @deprecated Use signrpc.MuSig2CleanupRequest.$Properties instead.
         */

        /**
         * Shape of a MuSig2CleanupRequest.
         * @typedef {signrpc.MuSig2CleanupRequest.$Properties} signrpc.MuSig2CleanupRequest.$Shape
         */

        /**
         * Constructs a new MuSig2CleanupRequest.
         * @memberof signrpc
         * @classdesc Represents a MuSig2CleanupRequest.
         * @constructor
         * @param {signrpc.MuSig2CleanupRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2CleanupRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MuSig2CleanupRequest session_id.
         * @member {Uint8Array} session_id
         * @memberof signrpc.MuSig2CleanupRequest
         * @instance
         */
        MuSig2CleanupRequest.prototype.session_id = $util.newBuffer([]);

        /**
         * Creates a new MuSig2CleanupRequest instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2CleanupRequest
         * @static
         * @param {signrpc.MuSig2CleanupRequest.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2CleanupRequest} MuSig2CleanupRequest instance
         * @type {{
         *   (properties: signrpc.MuSig2CleanupRequest.$Shape): signrpc.MuSig2CleanupRequest & signrpc.MuSig2CleanupRequest.$Shape;
         *   (properties?: signrpc.MuSig2CleanupRequest.$Properties): signrpc.MuSig2CleanupRequest;
         * }}
         */
        MuSig2CleanupRequest.create = function(properties) {
            return new MuSig2CleanupRequest(properties);
        };

        /**
         * Encodes the specified MuSig2CleanupRequest message. Does not implicitly {@link signrpc.MuSig2CleanupRequest.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2CleanupRequest
         * @static
         * @param {signrpc.MuSig2CleanupRequest.$Properties} message MuSig2CleanupRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CleanupRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id") && message.session_id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.session_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2CleanupRequest message, length delimited. Does not implicitly {@link signrpc.MuSig2CleanupRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2CleanupRequest
         * @static
         * @param {signrpc.MuSig2CleanupRequest.$Properties} message MuSig2CleanupRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CleanupRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2CleanupRequest message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2CleanupRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2CleanupRequest & signrpc.MuSig2CleanupRequest.$Shape} MuSig2CleanupRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CleanupRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2CleanupRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.session_id = value;
                        else
                            delete message.session_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2CleanupRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2CleanupRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2CleanupRequest & signrpc.MuSig2CleanupRequest.$Shape} MuSig2CleanupRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CleanupRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2CleanupRequest message.
         * @function verify
         * @memberof signrpc.MuSig2CleanupRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2CleanupRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                if (!(message.session_id && typeof message.session_id.length === "number" || $util.isString(message.session_id)))
                    return "session_id: buffer expected";
            return null;
        };

        /**
         * Creates a MuSig2CleanupRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2CleanupRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2CleanupRequest} MuSig2CleanupRequest
         */
        MuSig2CleanupRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2CleanupRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2CleanupRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.signrpc.MuSig2CleanupRequest();
            if (object.session_id != null)
                if (object.session_id.length)
                    if (typeof object.session_id === "string")
                        $util.base64.decode(object.session_id, message.session_id = $util.newBuffer($util.base64.length(object.session_id)), 0);
                    else if (object.session_id.length >= 0)
                        message.session_id = object.session_id;
            return message;
        };

        /**
         * Creates a plain object from a MuSig2CleanupRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2CleanupRequest
         * @static
         * @param {signrpc.MuSig2CleanupRequest} message MuSig2CleanupRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2CleanupRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.session_id = "";
                else {
                    object.session_id = [];
                    if (options.bytes !== $Array)
                        object.session_id = $util.newBuffer(object.session_id);
                }
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                object.session_id = options.bytes === $String ? $util.base64.encode(message.session_id, 0, message.session_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.session_id) : message.session_id;
            return object;
        };

        /**
         * Converts this MuSig2CleanupRequest to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2CleanupRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2CleanupRequest.prototype.toJSON = function() {
            return MuSig2CleanupRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2CleanupRequest
         * @function getTypeUrl
         * @memberof signrpc.MuSig2CleanupRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2CleanupRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2CleanupRequest";
        };

        return MuSig2CleanupRequest;
    })();

    signrpc.MuSig2CleanupResponse = (function() {

        /**
         * Properties of a MuSig2CleanupResponse.
         * @typedef {Object} signrpc.MuSig2CleanupResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a MuSig2CleanupResponse.
         * @memberof signrpc
         * @interface IMuSig2CleanupResponse
         * @augments signrpc.MuSig2CleanupResponse.$Properties
         * @deprecated Use signrpc.MuSig2CleanupResponse.$Properties instead.
         */

        /**
         * Shape of a MuSig2CleanupResponse.
         * @typedef {signrpc.MuSig2CleanupResponse.$Properties} signrpc.MuSig2CleanupResponse.$Shape
         */

        /**
         * Constructs a new MuSig2CleanupResponse.
         * @memberof signrpc
         * @classdesc Represents a MuSig2CleanupResponse.
         * @constructor
         * @param {signrpc.MuSig2CleanupResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const MuSig2CleanupResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new MuSig2CleanupResponse instance using the specified properties.
         * @function create
         * @memberof signrpc.MuSig2CleanupResponse
         * @static
         * @param {signrpc.MuSig2CleanupResponse.$Properties=} [properties] Properties to set
         * @returns {signrpc.MuSig2CleanupResponse} MuSig2CleanupResponse instance
         * @type {{
         *   (properties: signrpc.MuSig2CleanupResponse.$Shape): signrpc.MuSig2CleanupResponse & signrpc.MuSig2CleanupResponse.$Shape;
         *   (properties?: signrpc.MuSig2CleanupResponse.$Properties): signrpc.MuSig2CleanupResponse;
         * }}
         */
        MuSig2CleanupResponse.create = function(properties) {
            return new MuSig2CleanupResponse(properties);
        };

        /**
         * Encodes the specified MuSig2CleanupResponse message. Does not implicitly {@link signrpc.MuSig2CleanupResponse.verify|verify} messages.
         * @function encode
         * @memberof signrpc.MuSig2CleanupResponse
         * @static
         * @param {signrpc.MuSig2CleanupResponse.$Properties} message MuSig2CleanupResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CleanupResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MuSig2CleanupResponse message, length delimited. Does not implicitly {@link signrpc.MuSig2CleanupResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof signrpc.MuSig2CleanupResponse
         * @static
         * @param {signrpc.MuSig2CleanupResponse.$Properties} message MuSig2CleanupResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MuSig2CleanupResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MuSig2CleanupResponse message from the specified reader or buffer.
         * @function decode
         * @memberof signrpc.MuSig2CleanupResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {signrpc.MuSig2CleanupResponse & signrpc.MuSig2CleanupResponse.$Shape} MuSig2CleanupResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CleanupResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.signrpc.MuSig2CleanupResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MuSig2CleanupResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof signrpc.MuSig2CleanupResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {signrpc.MuSig2CleanupResponse & signrpc.MuSig2CleanupResponse.$Shape} MuSig2CleanupResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MuSig2CleanupResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MuSig2CleanupResponse message.
         * @function verify
         * @memberof signrpc.MuSig2CleanupResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MuSig2CleanupResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a MuSig2CleanupResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof signrpc.MuSig2CleanupResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {signrpc.MuSig2CleanupResponse} MuSig2CleanupResponse
         */
        MuSig2CleanupResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.signrpc.MuSig2CleanupResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".signrpc.MuSig2CleanupResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.signrpc.MuSig2CleanupResponse();
        };

        /**
         * Creates a plain object from a MuSig2CleanupResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof signrpc.MuSig2CleanupResponse
         * @static
         * @param {signrpc.MuSig2CleanupResponse} message MuSig2CleanupResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MuSig2CleanupResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this MuSig2CleanupResponse to JSON.
         * @function toJSON
         * @memberof signrpc.MuSig2CleanupResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MuSig2CleanupResponse.prototype.toJSON = function() {
            return MuSig2CleanupResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MuSig2CleanupResponse
         * @function getTypeUrl
         * @memberof signrpc.MuSig2CleanupResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MuSig2CleanupResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/signrpc.MuSig2CleanupResponse";
        };

        return MuSig2CleanupResponse;
    })();

    return signrpc;
})();

export const walletrpc = $root.walletrpc = (() => {

    /**
     * Namespace walletrpc.
     * @exports walletrpc
     * @namespace
     */
    const walletrpc = {};

    walletrpc.WalletKit = (function() {

        /**
         * Constructs a new WalletKit service.
         * @memberof walletrpc
         * @classdesc Represents a WalletKit
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const WalletKit = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (WalletKit.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = WalletKit;

        /**
         * Creates new WalletKit service using the specified rpc implementation.
         * @function create
         * @memberof walletrpc.WalletKit
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {WalletKit} RPC service. Useful where requests and/or responses are streamed.
         */
        WalletKit.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link walletrpc.WalletKit#listUnspent}.
         * @memberof walletrpc.WalletKit
         * @typedef ListUnspentCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.ListUnspentResponse} [response] ListUnspentResponse
         */

        /**
         * Calls ListUnspent.
         * @memberof walletrpc.WalletKit
         * @typedef ListUnspent
         * @type {{
         *   (request: walletrpc.IListUnspentRequest, callback: walletrpc.WalletKit.ListUnspentCallback): void;
         *   (request: walletrpc.IListUnspentRequest): Promise<walletrpc.ListUnspentResponse>;
         *   readonly name: "ListUnspent";
         *   readonly path: "/walletrpc.WalletKit/ListUnspent";
         *   readonly requestType: "ListUnspentRequest";
         *   readonly responseType: "ListUnspentResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListUnspent.
         * @name walletrpc.WalletKit#listUnspent
         * @type {walletrpc.WalletKit.ListUnspent}
         */
        $Object.defineProperties(WalletKit.prototype.listUnspent = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.listUnspent, $root.walletrpc.ListUnspentRequest, $root.walletrpc.ListUnspentResponse, request, callback);
        }, {
            name: { value: "ListUnspent" },
            path: { value: "/walletrpc.WalletKit/ListUnspent" },
            requestType: { value: "ListUnspentRequest" },
            responseType: { value: "ListUnspentResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#leaseOutput}.
         * @memberof walletrpc.WalletKit
         * @typedef LeaseOutputCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.LeaseOutputResponse} [response] LeaseOutputResponse
         */

        /**
         * Calls LeaseOutput.
         * @memberof walletrpc.WalletKit
         * @typedef LeaseOutput
         * @type {{
         *   (request: walletrpc.ILeaseOutputRequest, callback: walletrpc.WalletKit.LeaseOutputCallback): void;
         *   (request: walletrpc.ILeaseOutputRequest): Promise<walletrpc.LeaseOutputResponse>;
         *   readonly name: "LeaseOutput";
         *   readonly path: "/walletrpc.WalletKit/LeaseOutput";
         *   readonly requestType: "LeaseOutputRequest";
         *   readonly responseType: "LeaseOutputResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls LeaseOutput.
         * @name walletrpc.WalletKit#leaseOutput
         * @type {walletrpc.WalletKit.LeaseOutput}
         */
        $Object.defineProperties(WalletKit.prototype.leaseOutput = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.leaseOutput, $root.walletrpc.LeaseOutputRequest, $root.walletrpc.LeaseOutputResponse, request, callback);
        }, {
            name: { value: "LeaseOutput" },
            path: { value: "/walletrpc.WalletKit/LeaseOutput" },
            requestType: { value: "LeaseOutputRequest" },
            responseType: { value: "LeaseOutputResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#releaseOutput}.
         * @memberof walletrpc.WalletKit
         * @typedef ReleaseOutputCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.ReleaseOutputResponse} [response] ReleaseOutputResponse
         */

        /**
         * Calls ReleaseOutput.
         * @memberof walletrpc.WalletKit
         * @typedef ReleaseOutput
         * @type {{
         *   (request: walletrpc.IReleaseOutputRequest, callback: walletrpc.WalletKit.ReleaseOutputCallback): void;
         *   (request: walletrpc.IReleaseOutputRequest): Promise<walletrpc.ReleaseOutputResponse>;
         *   readonly name: "ReleaseOutput";
         *   readonly path: "/walletrpc.WalletKit/ReleaseOutput";
         *   readonly requestType: "ReleaseOutputRequest";
         *   readonly responseType: "ReleaseOutputResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ReleaseOutput.
         * @name walletrpc.WalletKit#releaseOutput
         * @type {walletrpc.WalletKit.ReleaseOutput}
         */
        $Object.defineProperties(WalletKit.prototype.releaseOutput = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.releaseOutput, $root.walletrpc.ReleaseOutputRequest, $root.walletrpc.ReleaseOutputResponse, request, callback);
        }, {
            name: { value: "ReleaseOutput" },
            path: { value: "/walletrpc.WalletKit/ReleaseOutput" },
            requestType: { value: "ReleaseOutputRequest" },
            responseType: { value: "ReleaseOutputResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#listLeases}.
         * @memberof walletrpc.WalletKit
         * @typedef ListLeasesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.ListLeasesResponse} [response] ListLeasesResponse
         */

        /**
         * Calls ListLeases.
         * @memberof walletrpc.WalletKit
         * @typedef ListLeases
         * @type {{
         *   (request: walletrpc.IListLeasesRequest, callback: walletrpc.WalletKit.ListLeasesCallback): void;
         *   (request: walletrpc.IListLeasesRequest): Promise<walletrpc.ListLeasesResponse>;
         *   readonly name: "ListLeases";
         *   readonly path: "/walletrpc.WalletKit/ListLeases";
         *   readonly requestType: "ListLeasesRequest";
         *   readonly responseType: "ListLeasesResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListLeases.
         * @name walletrpc.WalletKit#listLeases
         * @type {walletrpc.WalletKit.ListLeases}
         */
        $Object.defineProperties(WalletKit.prototype.listLeases = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.listLeases, $root.walletrpc.ListLeasesRequest, $root.walletrpc.ListLeasesResponse, request, callback);
        }, {
            name: { value: "ListLeases" },
            path: { value: "/walletrpc.WalletKit/ListLeases" },
            requestType: { value: "ListLeasesRequest" },
            responseType: { value: "ListLeasesResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#deriveNextKey}.
         * @memberof walletrpc.WalletKit
         * @typedef DeriveNextKeyCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.KeyDescriptor} [response] KeyDescriptor
         */

        /**
         * Calls DeriveNextKey.
         * @memberof walletrpc.WalletKit
         * @typedef DeriveNextKey
         * @type {{
         *   (request: walletrpc.IKeyReq, callback: walletrpc.WalletKit.DeriveNextKeyCallback): void;
         *   (request: walletrpc.IKeyReq): Promise<signrpc.KeyDescriptor>;
         *   readonly name: "DeriveNextKey";
         *   readonly path: "/walletrpc.WalletKit/DeriveNextKey";
         *   readonly requestType: "KeyReq";
         *   readonly responseType: "signrpc.KeyDescriptor";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DeriveNextKey.
         * @name walletrpc.WalletKit#deriveNextKey
         * @type {walletrpc.WalletKit.DeriveNextKey}
         */
        $Object.defineProperties(WalletKit.prototype.deriveNextKey = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.deriveNextKey, $root.walletrpc.KeyReq, $root.signrpc.KeyDescriptor, request, callback);
        }, {
            name: { value: "DeriveNextKey" },
            path: { value: "/walletrpc.WalletKit/DeriveNextKey" },
            requestType: { value: "KeyReq" },
            responseType: { value: "signrpc.KeyDescriptor" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#deriveKey}.
         * @memberof walletrpc.WalletKit
         * @typedef DeriveKeyCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {signrpc.KeyDescriptor} [response] KeyDescriptor
         */

        /**
         * Calls DeriveKey.
         * @memberof walletrpc.WalletKit
         * @typedef DeriveKey
         * @type {{
         *   (request: signrpc.IKeyLocator, callback: walletrpc.WalletKit.DeriveKeyCallback): void;
         *   (request: signrpc.IKeyLocator): Promise<signrpc.KeyDescriptor>;
         *   readonly name: "DeriveKey";
         *   readonly path: "/walletrpc.WalletKit/DeriveKey";
         *   readonly requestType: "signrpc.KeyLocator";
         *   readonly responseType: "signrpc.KeyDescriptor";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DeriveKey.
         * @name walletrpc.WalletKit#deriveKey
         * @type {walletrpc.WalletKit.DeriveKey}
         */
        $Object.defineProperties(WalletKit.prototype.deriveKey = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.deriveKey, $root.signrpc.KeyLocator, $root.signrpc.KeyDescriptor, request, callback);
        }, {
            name: { value: "DeriveKey" },
            path: { value: "/walletrpc.WalletKit/DeriveKey" },
            requestType: { value: "signrpc.KeyLocator" },
            responseType: { value: "signrpc.KeyDescriptor" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#nextAddr}.
         * @memberof walletrpc.WalletKit
         * @typedef NextAddrCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.AddrResponse} [response] AddrResponse
         */

        /**
         * Calls NextAddr.
         * @memberof walletrpc.WalletKit
         * @typedef NextAddr
         * @type {{
         *   (request: walletrpc.IAddrRequest, callback: walletrpc.WalletKit.NextAddrCallback): void;
         *   (request: walletrpc.IAddrRequest): Promise<walletrpc.AddrResponse>;
         *   readonly name: "NextAddr";
         *   readonly path: "/walletrpc.WalletKit/NextAddr";
         *   readonly requestType: "AddrRequest";
         *   readonly responseType: "AddrResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls NextAddr.
         * @name walletrpc.WalletKit#nextAddr
         * @type {walletrpc.WalletKit.NextAddr}
         */
        $Object.defineProperties(WalletKit.prototype.nextAddr = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.nextAddr, $root.walletrpc.AddrRequest, $root.walletrpc.AddrResponse, request, callback);
        }, {
            name: { value: "NextAddr" },
            path: { value: "/walletrpc.WalletKit/NextAddr" },
            requestType: { value: "AddrRequest" },
            responseType: { value: "AddrResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#getTransaction}.
         * @memberof walletrpc.WalletKit
         * @typedef GetTransactionCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {lnrpc.Transaction} [response] Transaction
         */

        /**
         * Calls GetTransaction.
         * @memberof walletrpc.WalletKit
         * @typedef GetTransaction
         * @type {{
         *   (request: walletrpc.IGetTransactionRequest, callback: walletrpc.WalletKit.GetTransactionCallback): void;
         *   (request: walletrpc.IGetTransactionRequest): Promise<lnrpc.Transaction>;
         *   readonly name: "GetTransaction";
         *   readonly path: "/walletrpc.WalletKit/GetTransaction";
         *   readonly requestType: "GetTransactionRequest";
         *   readonly responseType: "lnrpc.Transaction";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetTransaction.
         * @name walletrpc.WalletKit#getTransaction
         * @type {walletrpc.WalletKit.GetTransaction}
         */
        $Object.defineProperties(WalletKit.prototype.getTransaction = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.getTransaction, $root.walletrpc.GetTransactionRequest, $root.lnrpc.Transaction, request, callback);
        }, {
            name: { value: "GetTransaction" },
            path: { value: "/walletrpc.WalletKit/GetTransaction" },
            requestType: { value: "GetTransactionRequest" },
            responseType: { value: "lnrpc.Transaction" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#listAccounts}.
         * @memberof walletrpc.WalletKit
         * @typedef ListAccountsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.ListAccountsResponse} [response] ListAccountsResponse
         */

        /**
         * Calls ListAccounts.
         * @memberof walletrpc.WalletKit
         * @typedef ListAccounts
         * @type {{
         *   (request: walletrpc.IListAccountsRequest, callback: walletrpc.WalletKit.ListAccountsCallback): void;
         *   (request: walletrpc.IListAccountsRequest): Promise<walletrpc.ListAccountsResponse>;
         *   readonly name: "ListAccounts";
         *   readonly path: "/walletrpc.WalletKit/ListAccounts";
         *   readonly requestType: "ListAccountsRequest";
         *   readonly responseType: "ListAccountsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListAccounts.
         * @name walletrpc.WalletKit#listAccounts
         * @type {walletrpc.WalletKit.ListAccounts}
         */
        $Object.defineProperties(WalletKit.prototype.listAccounts = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.listAccounts, $root.walletrpc.ListAccountsRequest, $root.walletrpc.ListAccountsResponse, request, callback);
        }, {
            name: { value: "ListAccounts" },
            path: { value: "/walletrpc.WalletKit/ListAccounts" },
            requestType: { value: "ListAccountsRequest" },
            responseType: { value: "ListAccountsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#requiredReserve}.
         * @memberof walletrpc.WalletKit
         * @typedef RequiredReserveCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.RequiredReserveResponse} [response] RequiredReserveResponse
         */

        /**
         * Calls RequiredReserve.
         * @memberof walletrpc.WalletKit
         * @typedef RequiredReserve
         * @type {{
         *   (request: walletrpc.IRequiredReserveRequest, callback: walletrpc.WalletKit.RequiredReserveCallback): void;
         *   (request: walletrpc.IRequiredReserveRequest): Promise<walletrpc.RequiredReserveResponse>;
         *   readonly name: "RequiredReserve";
         *   readonly path: "/walletrpc.WalletKit/RequiredReserve";
         *   readonly requestType: "RequiredReserveRequest";
         *   readonly responseType: "RequiredReserveResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls RequiredReserve.
         * @name walletrpc.WalletKit#requiredReserve
         * @type {walletrpc.WalletKit.RequiredReserve}
         */
        $Object.defineProperties(WalletKit.prototype.requiredReserve = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.requiredReserve, $root.walletrpc.RequiredReserveRequest, $root.walletrpc.RequiredReserveResponse, request, callback);
        }, {
            name: { value: "RequiredReserve" },
            path: { value: "/walletrpc.WalletKit/RequiredReserve" },
            requestType: { value: "RequiredReserveRequest" },
            responseType: { value: "RequiredReserveResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#listAddresses}.
         * @memberof walletrpc.WalletKit
         * @typedef ListAddressesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.ListAddressesResponse} [response] ListAddressesResponse
         */

        /**
         * Calls ListAddresses.
         * @memberof walletrpc.WalletKit
         * @typedef ListAddresses
         * @type {{
         *   (request: walletrpc.IListAddressesRequest, callback: walletrpc.WalletKit.ListAddressesCallback): void;
         *   (request: walletrpc.IListAddressesRequest): Promise<walletrpc.ListAddressesResponse>;
         *   readonly name: "ListAddresses";
         *   readonly path: "/walletrpc.WalletKit/ListAddresses";
         *   readonly requestType: "ListAddressesRequest";
         *   readonly responseType: "ListAddressesResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListAddresses.
         * @name walletrpc.WalletKit#listAddresses
         * @type {walletrpc.WalletKit.ListAddresses}
         */
        $Object.defineProperties(WalletKit.prototype.listAddresses = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.listAddresses, $root.walletrpc.ListAddressesRequest, $root.walletrpc.ListAddressesResponse, request, callback);
        }, {
            name: { value: "ListAddresses" },
            path: { value: "/walletrpc.WalletKit/ListAddresses" },
            requestType: { value: "ListAddressesRequest" },
            responseType: { value: "ListAddressesResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#signMessageWithAddr}.
         * @memberof walletrpc.WalletKit
         * @typedef SignMessageWithAddrCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.SignMessageWithAddrResponse} [response] SignMessageWithAddrResponse
         */

        /**
         * Calls SignMessageWithAddr.
         * @memberof walletrpc.WalletKit
         * @typedef SignMessageWithAddr
         * @type {{
         *   (request: walletrpc.ISignMessageWithAddrRequest, callback: walletrpc.WalletKit.SignMessageWithAddrCallback): void;
         *   (request: walletrpc.ISignMessageWithAddrRequest): Promise<walletrpc.SignMessageWithAddrResponse>;
         *   readonly name: "SignMessageWithAddr";
         *   readonly path: "/walletrpc.WalletKit/SignMessageWithAddr";
         *   readonly requestType: "SignMessageWithAddrRequest";
         *   readonly responseType: "SignMessageWithAddrResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SignMessageWithAddr.
         * @name walletrpc.WalletKit#signMessageWithAddr
         * @type {walletrpc.WalletKit.SignMessageWithAddr}
         */
        $Object.defineProperties(WalletKit.prototype.signMessageWithAddr = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.signMessageWithAddr, $root.walletrpc.SignMessageWithAddrRequest, $root.walletrpc.SignMessageWithAddrResponse, request, callback);
        }, {
            name: { value: "SignMessageWithAddr" },
            path: { value: "/walletrpc.WalletKit/SignMessageWithAddr" },
            requestType: { value: "SignMessageWithAddrRequest" },
            responseType: { value: "SignMessageWithAddrResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#verifyMessageWithAddr}.
         * @memberof walletrpc.WalletKit
         * @typedef VerifyMessageWithAddrCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.VerifyMessageWithAddrResponse} [response] VerifyMessageWithAddrResponse
         */

        /**
         * Calls VerifyMessageWithAddr.
         * @memberof walletrpc.WalletKit
         * @typedef VerifyMessageWithAddr
         * @type {{
         *   (request: walletrpc.IVerifyMessageWithAddrRequest, callback: walletrpc.WalletKit.VerifyMessageWithAddrCallback): void;
         *   (request: walletrpc.IVerifyMessageWithAddrRequest): Promise<walletrpc.VerifyMessageWithAddrResponse>;
         *   readonly name: "VerifyMessageWithAddr";
         *   readonly path: "/walletrpc.WalletKit/VerifyMessageWithAddr";
         *   readonly requestType: "VerifyMessageWithAddrRequest";
         *   readonly responseType: "VerifyMessageWithAddrResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls VerifyMessageWithAddr.
         * @name walletrpc.WalletKit#verifyMessageWithAddr
         * @type {walletrpc.WalletKit.VerifyMessageWithAddr}
         */
        $Object.defineProperties(WalletKit.prototype.verifyMessageWithAddr = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.verifyMessageWithAddr, $root.walletrpc.VerifyMessageWithAddrRequest, $root.walletrpc.VerifyMessageWithAddrResponse, request, callback);
        }, {
            name: { value: "VerifyMessageWithAddr" },
            path: { value: "/walletrpc.WalletKit/VerifyMessageWithAddr" },
            requestType: { value: "VerifyMessageWithAddrRequest" },
            responseType: { value: "VerifyMessageWithAddrResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#importAccount}.
         * @memberof walletrpc.WalletKit
         * @typedef ImportAccountCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.ImportAccountResponse} [response] ImportAccountResponse
         */

        /**
         * Calls ImportAccount.
         * @memberof walletrpc.WalletKit
         * @typedef ImportAccount
         * @type {{
         *   (request: walletrpc.IImportAccountRequest, callback: walletrpc.WalletKit.ImportAccountCallback): void;
         *   (request: walletrpc.IImportAccountRequest): Promise<walletrpc.ImportAccountResponse>;
         *   readonly name: "ImportAccount";
         *   readonly path: "/walletrpc.WalletKit/ImportAccount";
         *   readonly requestType: "ImportAccountRequest";
         *   readonly responseType: "ImportAccountResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ImportAccount.
         * @name walletrpc.WalletKit#importAccount
         * @type {walletrpc.WalletKit.ImportAccount}
         */
        $Object.defineProperties(WalletKit.prototype.importAccount = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.importAccount, $root.walletrpc.ImportAccountRequest, $root.walletrpc.ImportAccountResponse, request, callback);
        }, {
            name: { value: "ImportAccount" },
            path: { value: "/walletrpc.WalletKit/ImportAccount" },
            requestType: { value: "ImportAccountRequest" },
            responseType: { value: "ImportAccountResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#importPublicKey}.
         * @memberof walletrpc.WalletKit
         * @typedef ImportPublicKeyCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.ImportPublicKeyResponse} [response] ImportPublicKeyResponse
         */

        /**
         * Calls ImportPublicKey.
         * @memberof walletrpc.WalletKit
         * @typedef ImportPublicKey
         * @type {{
         *   (request: walletrpc.IImportPublicKeyRequest, callback: walletrpc.WalletKit.ImportPublicKeyCallback): void;
         *   (request: walletrpc.IImportPublicKeyRequest): Promise<walletrpc.ImportPublicKeyResponse>;
         *   readonly name: "ImportPublicKey";
         *   readonly path: "/walletrpc.WalletKit/ImportPublicKey";
         *   readonly requestType: "ImportPublicKeyRequest";
         *   readonly responseType: "ImportPublicKeyResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ImportPublicKey.
         * @name walletrpc.WalletKit#importPublicKey
         * @type {walletrpc.WalletKit.ImportPublicKey}
         */
        $Object.defineProperties(WalletKit.prototype.importPublicKey = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.importPublicKey, $root.walletrpc.ImportPublicKeyRequest, $root.walletrpc.ImportPublicKeyResponse, request, callback);
        }, {
            name: { value: "ImportPublicKey" },
            path: { value: "/walletrpc.WalletKit/ImportPublicKey" },
            requestType: { value: "ImportPublicKeyRequest" },
            responseType: { value: "ImportPublicKeyResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#importTapscript}.
         * @memberof walletrpc.WalletKit
         * @typedef ImportTapscriptCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.ImportTapscriptResponse} [response] ImportTapscriptResponse
         */

        /**
         * Calls ImportTapscript.
         * @memberof walletrpc.WalletKit
         * @typedef ImportTapscript
         * @type {{
         *   (request: walletrpc.IImportTapscriptRequest, callback: walletrpc.WalletKit.ImportTapscriptCallback): void;
         *   (request: walletrpc.IImportTapscriptRequest): Promise<walletrpc.ImportTapscriptResponse>;
         *   readonly name: "ImportTapscript";
         *   readonly path: "/walletrpc.WalletKit/ImportTapscript";
         *   readonly requestType: "ImportTapscriptRequest";
         *   readonly responseType: "ImportTapscriptResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ImportTapscript.
         * @name walletrpc.WalletKit#importTapscript
         * @type {walletrpc.WalletKit.ImportTapscript}
         */
        $Object.defineProperties(WalletKit.prototype.importTapscript = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.importTapscript, $root.walletrpc.ImportTapscriptRequest, $root.walletrpc.ImportTapscriptResponse, request, callback);
        }, {
            name: { value: "ImportTapscript" },
            path: { value: "/walletrpc.WalletKit/ImportTapscript" },
            requestType: { value: "ImportTapscriptRequest" },
            responseType: { value: "ImportTapscriptResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#publishTransaction}.
         * @memberof walletrpc.WalletKit
         * @typedef PublishTransactionCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.PublishResponse} [response] PublishResponse
         */

        /**
         * Calls PublishTransaction.
         * @memberof walletrpc.WalletKit
         * @typedef PublishTransaction
         * @type {{
         *   (request: walletrpc.ITransaction, callback: walletrpc.WalletKit.PublishTransactionCallback): void;
         *   (request: walletrpc.ITransaction): Promise<walletrpc.PublishResponse>;
         *   readonly name: "PublishTransaction";
         *   readonly path: "/walletrpc.WalletKit/PublishTransaction";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "PublishResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls PublishTransaction.
         * @name walletrpc.WalletKit#publishTransaction
         * @type {walletrpc.WalletKit.PublishTransaction}
         */
        $Object.defineProperties(WalletKit.prototype.publishTransaction = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.publishTransaction, $root.walletrpc.Transaction, $root.walletrpc.PublishResponse, request, callback);
        }, {
            name: { value: "PublishTransaction" },
            path: { value: "/walletrpc.WalletKit/PublishTransaction" },
            requestType: { value: "Transaction" },
            responseType: { value: "PublishResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#removeTransaction}.
         * @memberof walletrpc.WalletKit
         * @typedef RemoveTransactionCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.RemoveTransactionResponse} [response] RemoveTransactionResponse
         */

        /**
         * Calls RemoveTransaction.
         * @memberof walletrpc.WalletKit
         * @typedef RemoveTransaction
         * @type {{
         *   (request: walletrpc.IGetTransactionRequest, callback: walletrpc.WalletKit.RemoveTransactionCallback): void;
         *   (request: walletrpc.IGetTransactionRequest): Promise<walletrpc.RemoveTransactionResponse>;
         *   readonly name: "RemoveTransaction";
         *   readonly path: "/walletrpc.WalletKit/RemoveTransaction";
         *   readonly requestType: "GetTransactionRequest";
         *   readonly responseType: "RemoveTransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls RemoveTransaction.
         * @name walletrpc.WalletKit#removeTransaction
         * @type {walletrpc.WalletKit.RemoveTransaction}
         */
        $Object.defineProperties(WalletKit.prototype.removeTransaction = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.removeTransaction, $root.walletrpc.GetTransactionRequest, $root.walletrpc.RemoveTransactionResponse, request, callback);
        }, {
            name: { value: "RemoveTransaction" },
            path: { value: "/walletrpc.WalletKit/RemoveTransaction" },
            requestType: { value: "GetTransactionRequest" },
            responseType: { value: "RemoveTransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#sendOutputs}.
         * @memberof walletrpc.WalletKit
         * @typedef SendOutputsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.SendOutputsResponse} [response] SendOutputsResponse
         */

        /**
         * Calls SendOutputs.
         * @memberof walletrpc.WalletKit
         * @typedef SendOutputs
         * @type {{
         *   (request: walletrpc.ISendOutputsRequest, callback: walletrpc.WalletKit.SendOutputsCallback): void;
         *   (request: walletrpc.ISendOutputsRequest): Promise<walletrpc.SendOutputsResponse>;
         *   readonly name: "SendOutputs";
         *   readonly path: "/walletrpc.WalletKit/SendOutputs";
         *   readonly requestType: "SendOutputsRequest";
         *   readonly responseType: "SendOutputsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SendOutputs.
         * @name walletrpc.WalletKit#sendOutputs
         * @type {walletrpc.WalletKit.SendOutputs}
         */
        $Object.defineProperties(WalletKit.prototype.sendOutputs = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.sendOutputs, $root.walletrpc.SendOutputsRequest, $root.walletrpc.SendOutputsResponse, request, callback);
        }, {
            name: { value: "SendOutputs" },
            path: { value: "/walletrpc.WalletKit/SendOutputs" },
            requestType: { value: "SendOutputsRequest" },
            responseType: { value: "SendOutputsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#estimateFee}.
         * @memberof walletrpc.WalletKit
         * @typedef EstimateFeeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.EstimateFeeResponse} [response] EstimateFeeResponse
         */

        /**
         * Calls EstimateFee.
         * @memberof walletrpc.WalletKit
         * @typedef EstimateFee
         * @type {{
         *   (request: walletrpc.IEstimateFeeRequest, callback: walletrpc.WalletKit.EstimateFeeCallback): void;
         *   (request: walletrpc.IEstimateFeeRequest): Promise<walletrpc.EstimateFeeResponse>;
         *   readonly name: "EstimateFee";
         *   readonly path: "/walletrpc.WalletKit/EstimateFee";
         *   readonly requestType: "EstimateFeeRequest";
         *   readonly responseType: "EstimateFeeResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls EstimateFee.
         * @name walletrpc.WalletKit#estimateFee
         * @type {walletrpc.WalletKit.EstimateFee}
         */
        $Object.defineProperties(WalletKit.prototype.estimateFee = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.estimateFee, $root.walletrpc.EstimateFeeRequest, $root.walletrpc.EstimateFeeResponse, request, callback);
        }, {
            name: { value: "EstimateFee" },
            path: { value: "/walletrpc.WalletKit/EstimateFee" },
            requestType: { value: "EstimateFeeRequest" },
            responseType: { value: "EstimateFeeResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#pendingSweeps}.
         * @memberof walletrpc.WalletKit
         * @typedef PendingSweepsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.PendingSweepsResponse} [response] PendingSweepsResponse
         */

        /**
         * Calls PendingSweeps.
         * @memberof walletrpc.WalletKit
         * @typedef PendingSweeps
         * @type {{
         *   (request: walletrpc.IPendingSweepsRequest, callback: walletrpc.WalletKit.PendingSweepsCallback): void;
         *   (request: walletrpc.IPendingSweepsRequest): Promise<walletrpc.PendingSweepsResponse>;
         *   readonly name: "PendingSweeps";
         *   readonly path: "/walletrpc.WalletKit/PendingSweeps";
         *   readonly requestType: "PendingSweepsRequest";
         *   readonly responseType: "PendingSweepsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls PendingSweeps.
         * @name walletrpc.WalletKit#pendingSweeps
         * @type {walletrpc.WalletKit.PendingSweeps}
         */
        $Object.defineProperties(WalletKit.prototype.pendingSweeps = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.pendingSweeps, $root.walletrpc.PendingSweepsRequest, $root.walletrpc.PendingSweepsResponse, request, callback);
        }, {
            name: { value: "PendingSweeps" },
            path: { value: "/walletrpc.WalletKit/PendingSweeps" },
            requestType: { value: "PendingSweepsRequest" },
            responseType: { value: "PendingSweepsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#bumpFee}.
         * @memberof walletrpc.WalletKit
         * @typedef BumpFeeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.BumpFeeResponse} [response] BumpFeeResponse
         */

        /**
         * Calls BumpFee.
         * @memberof walletrpc.WalletKit
         * @typedef BumpFee
         * @type {{
         *   (request: walletrpc.IBumpFeeRequest, callback: walletrpc.WalletKit.BumpFeeCallback): void;
         *   (request: walletrpc.IBumpFeeRequest): Promise<walletrpc.BumpFeeResponse>;
         *   readonly name: "BumpFee";
         *   readonly path: "/walletrpc.WalletKit/BumpFee";
         *   readonly requestType: "BumpFeeRequest";
         *   readonly responseType: "BumpFeeResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls BumpFee.
         * @name walletrpc.WalletKit#bumpFee
         * @type {walletrpc.WalletKit.BumpFee}
         */
        $Object.defineProperties(WalletKit.prototype.bumpFee = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.bumpFee, $root.walletrpc.BumpFeeRequest, $root.walletrpc.BumpFeeResponse, request, callback);
        }, {
            name: { value: "BumpFee" },
            path: { value: "/walletrpc.WalletKit/BumpFee" },
            requestType: { value: "BumpFeeRequest" },
            responseType: { value: "BumpFeeResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#bumpForceCloseFee}.
         * @memberof walletrpc.WalletKit
         * @typedef BumpForceCloseFeeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.BumpForceCloseFeeResponse} [response] BumpForceCloseFeeResponse
         */

        /**
         * Calls BumpForceCloseFee.
         * @memberof walletrpc.WalletKit
         * @typedef BumpForceCloseFee
         * @type {{
         *   (request: walletrpc.IBumpForceCloseFeeRequest, callback: walletrpc.WalletKit.BumpForceCloseFeeCallback): void;
         *   (request: walletrpc.IBumpForceCloseFeeRequest): Promise<walletrpc.BumpForceCloseFeeResponse>;
         *   readonly name: "BumpForceCloseFee";
         *   readonly path: "/walletrpc.WalletKit/BumpForceCloseFee";
         *   readonly requestType: "BumpForceCloseFeeRequest";
         *   readonly responseType: "BumpForceCloseFeeResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls BumpForceCloseFee.
         * @name walletrpc.WalletKit#bumpForceCloseFee
         * @type {walletrpc.WalletKit.BumpForceCloseFee}
         */
        $Object.defineProperties(WalletKit.prototype.bumpForceCloseFee = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.bumpForceCloseFee, $root.walletrpc.BumpForceCloseFeeRequest, $root.walletrpc.BumpForceCloseFeeResponse, request, callback);
        }, {
            name: { value: "BumpForceCloseFee" },
            path: { value: "/walletrpc.WalletKit/BumpForceCloseFee" },
            requestType: { value: "BumpForceCloseFeeRequest" },
            responseType: { value: "BumpForceCloseFeeResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#listSweeps}.
         * @memberof walletrpc.WalletKit
         * @typedef ListSweepsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.ListSweepsResponse} [response] ListSweepsResponse
         */

        /**
         * Calls ListSweeps.
         * @memberof walletrpc.WalletKit
         * @typedef ListSweeps
         * @type {{
         *   (request: walletrpc.IListSweepsRequest, callback: walletrpc.WalletKit.ListSweepsCallback): void;
         *   (request: walletrpc.IListSweepsRequest): Promise<walletrpc.ListSweepsResponse>;
         *   readonly name: "ListSweeps";
         *   readonly path: "/walletrpc.WalletKit/ListSweeps";
         *   readonly requestType: "ListSweepsRequest";
         *   readonly responseType: "ListSweepsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListSweeps.
         * @name walletrpc.WalletKit#listSweeps
         * @type {walletrpc.WalletKit.ListSweeps}
         */
        $Object.defineProperties(WalletKit.prototype.listSweeps = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.listSweeps, $root.walletrpc.ListSweepsRequest, $root.walletrpc.ListSweepsResponse, request, callback);
        }, {
            name: { value: "ListSweeps" },
            path: { value: "/walletrpc.WalletKit/ListSweeps" },
            requestType: { value: "ListSweepsRequest" },
            responseType: { value: "ListSweepsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#labelTransaction}.
         * @memberof walletrpc.WalletKit
         * @typedef LabelTransactionCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.LabelTransactionResponse} [response] LabelTransactionResponse
         */

        /**
         * Calls LabelTransaction.
         * @memberof walletrpc.WalletKit
         * @typedef LabelTransaction
         * @type {{
         *   (request: walletrpc.ILabelTransactionRequest, callback: walletrpc.WalletKit.LabelTransactionCallback): void;
         *   (request: walletrpc.ILabelTransactionRequest): Promise<walletrpc.LabelTransactionResponse>;
         *   readonly name: "LabelTransaction";
         *   readonly path: "/walletrpc.WalletKit/LabelTransaction";
         *   readonly requestType: "LabelTransactionRequest";
         *   readonly responseType: "LabelTransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls LabelTransaction.
         * @name walletrpc.WalletKit#labelTransaction
         * @type {walletrpc.WalletKit.LabelTransaction}
         */
        $Object.defineProperties(WalletKit.prototype.labelTransaction = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.labelTransaction, $root.walletrpc.LabelTransactionRequest, $root.walletrpc.LabelTransactionResponse, request, callback);
        }, {
            name: { value: "LabelTransaction" },
            path: { value: "/walletrpc.WalletKit/LabelTransaction" },
            requestType: { value: "LabelTransactionRequest" },
            responseType: { value: "LabelTransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#fundPsbt}.
         * @memberof walletrpc.WalletKit
         * @typedef FundPsbtCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.FundPsbtResponse} [response] FundPsbtResponse
         */

        /**
         * Calls FundPsbt.
         * @memberof walletrpc.WalletKit
         * @typedef FundPsbt
         * @type {{
         *   (request: walletrpc.IFundPsbtRequest, callback: walletrpc.WalletKit.FundPsbtCallback): void;
         *   (request: walletrpc.IFundPsbtRequest): Promise<walletrpc.FundPsbtResponse>;
         *   readonly name: "FundPsbt";
         *   readonly path: "/walletrpc.WalletKit/FundPsbt";
         *   readonly requestType: "FundPsbtRequest";
         *   readonly responseType: "FundPsbtResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls FundPsbt.
         * @name walletrpc.WalletKit#fundPsbt
         * @type {walletrpc.WalletKit.FundPsbt}
         */
        $Object.defineProperties(WalletKit.prototype.fundPsbt = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.fundPsbt, $root.walletrpc.FundPsbtRequest, $root.walletrpc.FundPsbtResponse, request, callback);
        }, {
            name: { value: "FundPsbt" },
            path: { value: "/walletrpc.WalletKit/FundPsbt" },
            requestType: { value: "FundPsbtRequest" },
            responseType: { value: "FundPsbtResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#signPsbt}.
         * @memberof walletrpc.WalletKit
         * @typedef SignPsbtCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.SignPsbtResponse} [response] SignPsbtResponse
         */

        /**
         * Calls SignPsbt.
         * @memberof walletrpc.WalletKit
         * @typedef SignPsbt
         * @type {{
         *   (request: walletrpc.ISignPsbtRequest, callback: walletrpc.WalletKit.SignPsbtCallback): void;
         *   (request: walletrpc.ISignPsbtRequest): Promise<walletrpc.SignPsbtResponse>;
         *   readonly name: "SignPsbt";
         *   readonly path: "/walletrpc.WalletKit/SignPsbt";
         *   readonly requestType: "SignPsbtRequest";
         *   readonly responseType: "SignPsbtResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls SignPsbt.
         * @name walletrpc.WalletKit#signPsbt
         * @type {walletrpc.WalletKit.SignPsbt}
         */
        $Object.defineProperties(WalletKit.prototype.signPsbt = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.signPsbt, $root.walletrpc.SignPsbtRequest, $root.walletrpc.SignPsbtResponse, request, callback);
        }, {
            name: { value: "SignPsbt" },
            path: { value: "/walletrpc.WalletKit/SignPsbt" },
            requestType: { value: "SignPsbtRequest" },
            responseType: { value: "SignPsbtResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#finalizePsbt}.
         * @memberof walletrpc.WalletKit
         * @typedef FinalizePsbtCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.FinalizePsbtResponse} [response] FinalizePsbtResponse
         */

        /**
         * Calls FinalizePsbt.
         * @memberof walletrpc.WalletKit
         * @typedef FinalizePsbt
         * @type {{
         *   (request: walletrpc.IFinalizePsbtRequest, callback: walletrpc.WalletKit.FinalizePsbtCallback): void;
         *   (request: walletrpc.IFinalizePsbtRequest): Promise<walletrpc.FinalizePsbtResponse>;
         *   readonly name: "FinalizePsbt";
         *   readonly path: "/walletrpc.WalletKit/FinalizePsbt";
         *   readonly requestType: "FinalizePsbtRequest";
         *   readonly responseType: "FinalizePsbtResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls FinalizePsbt.
         * @name walletrpc.WalletKit#finalizePsbt
         * @type {walletrpc.WalletKit.FinalizePsbt}
         */
        $Object.defineProperties(WalletKit.prototype.finalizePsbt = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.finalizePsbt, $root.walletrpc.FinalizePsbtRequest, $root.walletrpc.FinalizePsbtResponse, request, callback);
        }, {
            name: { value: "FinalizePsbt" },
            path: { value: "/walletrpc.WalletKit/FinalizePsbt" },
            requestType: { value: "FinalizePsbtRequest" },
            responseType: { value: "FinalizePsbtResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link walletrpc.WalletKit#rescan}.
         * @memberof walletrpc.WalletKit
         * @typedef RescanCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {walletrpc.RescanResponse} [response] RescanResponse
         */

        /**
         * Calls Rescan.
         * @memberof walletrpc.WalletKit
         * @typedef Rescan
         * @type {{
         *   (request: walletrpc.IRescanRequest, callback: walletrpc.WalletKit.RescanCallback): void;
         *   (request: walletrpc.IRescanRequest): Promise<walletrpc.RescanResponse>;
         *   readonly name: "Rescan";
         *   readonly path: "/walletrpc.WalletKit/Rescan";
         *   readonly requestType: "RescanRequest";
         *   readonly responseType: "RescanResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls Rescan.
         * @name walletrpc.WalletKit#rescan
         * @type {walletrpc.WalletKit.Rescan}
         */
        $Object.defineProperties(WalletKit.prototype.rescan = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WalletKit.prototype.rescan, $root.walletrpc.RescanRequest, $root.walletrpc.RescanResponse, request, callback);
        }, {
            name: { value: "Rescan" },
            path: { value: "/walletrpc.WalletKit/Rescan" },
            requestType: { value: "RescanRequest" },
            responseType: { value: "RescanResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return WalletKit;
    })();

    walletrpc.ListUnspentRequest = (function() {

        /**
         * Properties of a ListUnspentRequest.
         * @typedef {Object} walletrpc.ListUnspentRequest.$Properties
         * @property {number|null} [min_confs] ListUnspentRequest min_confs
         * @property {number|null} [max_confs] ListUnspentRequest max_confs
         * @property {string|null} [account] ListUnspentRequest account
         * @property {boolean|null} [unconfirmed_only] ListUnspentRequest unconfirmed_only
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListUnspentRequest.
         * @memberof walletrpc
         * @interface IListUnspentRequest
         * @augments walletrpc.ListUnspentRequest.$Properties
         * @deprecated Use walletrpc.ListUnspentRequest.$Properties instead.
         */

        /**
         * Shape of a ListUnspentRequest.
         * @typedef {walletrpc.ListUnspentRequest.$Properties} walletrpc.ListUnspentRequest.$Shape
         */

        /**
         * Constructs a new ListUnspentRequest.
         * @memberof walletrpc
         * @classdesc Represents a ListUnspentRequest.
         * @constructor
         * @param {walletrpc.ListUnspentRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListUnspentRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListUnspentRequest min_confs.
         * @member {number} min_confs
         * @memberof walletrpc.ListUnspentRequest
         * @instance
         */
        ListUnspentRequest.prototype.min_confs = 0;

        /**
         * ListUnspentRequest max_confs.
         * @member {number} max_confs
         * @memberof walletrpc.ListUnspentRequest
         * @instance
         */
        ListUnspentRequest.prototype.max_confs = 0;

        /**
         * ListUnspentRequest account.
         * @member {string} account
         * @memberof walletrpc.ListUnspentRequest
         * @instance
         */
        ListUnspentRequest.prototype.account = "";

        /**
         * ListUnspentRequest unconfirmed_only.
         * @member {boolean} unconfirmed_only
         * @memberof walletrpc.ListUnspentRequest
         * @instance
         */
        ListUnspentRequest.prototype.unconfirmed_only = false;

        /**
         * Creates a new ListUnspentRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListUnspentRequest
         * @static
         * @param {walletrpc.ListUnspentRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListUnspentRequest} ListUnspentRequest instance
         * @type {{
         *   (properties: walletrpc.ListUnspentRequest.$Shape): walletrpc.ListUnspentRequest & walletrpc.ListUnspentRequest.$Shape;
         *   (properties?: walletrpc.ListUnspentRequest.$Properties): walletrpc.ListUnspentRequest;
         * }}
         */
        ListUnspentRequest.create = function(properties) {
            return new ListUnspentRequest(properties);
        };

        /**
         * Encodes the specified ListUnspentRequest message. Does not implicitly {@link walletrpc.ListUnspentRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListUnspentRequest
         * @static
         * @param {walletrpc.ListUnspentRequest.$Properties} message ListUnspentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListUnspentRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.min_confs);
            if (message.max_confs != null && $Object.hasOwnProperty.call(message, "max_confs") && message.max_confs !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.max_confs);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account") && message.account !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.account);
            if (message.unconfirmed_only != null && $Object.hasOwnProperty.call(message, "unconfirmed_only") && message.unconfirmed_only !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.unconfirmed_only);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListUnspentRequest message, length delimited. Does not implicitly {@link walletrpc.ListUnspentRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListUnspentRequest
         * @static
         * @param {walletrpc.ListUnspentRequest.$Properties} message ListUnspentRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListUnspentRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListUnspentRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListUnspentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListUnspentRequest & walletrpc.ListUnspentRequest.$Shape} ListUnspentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListUnspentRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListUnspentRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.max_confs = value;
                        else
                            delete message.max_confs;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.unconfirmed_only = value;
                        else
                            delete message.unconfirmed_only;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListUnspentRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListUnspentRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListUnspentRequest & walletrpc.ListUnspentRequest.$Shape} ListUnspentRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListUnspentRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListUnspentRequest message.
         * @function verify
         * @memberof walletrpc.ListUnspentRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListUnspentRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            if (message.max_confs != null && $Object.hasOwnProperty.call(message, "max_confs"))
                if (!$util.isInteger(message.max_confs))
                    return "max_confs: integer expected";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            if (message.unconfirmed_only != null && $Object.hasOwnProperty.call(message, "unconfirmed_only"))
                if (typeof message.unconfirmed_only !== "boolean")
                    return "unconfirmed_only: boolean expected";
            return null;
        };

        /**
         * Creates a ListUnspentRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListUnspentRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListUnspentRequest} ListUnspentRequest
         */
        ListUnspentRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListUnspentRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListUnspentRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ListUnspentRequest();
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            if (object.max_confs != null)
                if ($Number(object.max_confs) !== 0)
                    message.max_confs = object.max_confs | 0;
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = $String(object.account);
            if (object.unconfirmed_only != null)
                if (object.unconfirmed_only)
                    message.unconfirmed_only = $Boolean(object.unconfirmed_only);
            return message;
        };

        /**
         * Creates a plain object from a ListUnspentRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListUnspentRequest
         * @static
         * @param {walletrpc.ListUnspentRequest} message ListUnspentRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListUnspentRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.min_confs = 0;
                object.max_confs = 0;
                object.account = "";
                object.unconfirmed_only = false;
            }
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            if (message.max_confs != null && $Object.hasOwnProperty.call(message, "max_confs"))
                object.max_confs = message.max_confs;
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = message.account;
            if (message.unconfirmed_only != null && $Object.hasOwnProperty.call(message, "unconfirmed_only"))
                object.unconfirmed_only = message.unconfirmed_only;
            return object;
        };

        /**
         * Converts this ListUnspentRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.ListUnspentRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListUnspentRequest.prototype.toJSON = function() {
            return ListUnspentRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListUnspentRequest
         * @function getTypeUrl
         * @memberof walletrpc.ListUnspentRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListUnspentRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListUnspentRequest";
        };

        return ListUnspentRequest;
    })();

    walletrpc.ListUnspentResponse = (function() {

        /**
         * Properties of a ListUnspentResponse.
         * @typedef {Object} walletrpc.ListUnspentResponse.$Properties
         * @property {Array.<lnrpc.Utxo.$Properties>|null} [utxos] ListUnspentResponse utxos
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListUnspentResponse.
         * @memberof walletrpc
         * @interface IListUnspentResponse
         * @augments walletrpc.ListUnspentResponse.$Properties
         * @deprecated Use walletrpc.ListUnspentResponse.$Properties instead.
         */

        /**
         * Shape of a ListUnspentResponse.
         * @typedef {walletrpc.ListUnspentResponse.$Properties} walletrpc.ListUnspentResponse.$Shape
         */

        /**
         * Constructs a new ListUnspentResponse.
         * @memberof walletrpc
         * @classdesc Represents a ListUnspentResponse.
         * @constructor
         * @param {walletrpc.ListUnspentResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListUnspentResponse = function (properties) {
            this.utxos = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListUnspentResponse utxos.
         * @member {Array.<lnrpc.Utxo.$Properties>} utxos
         * @memberof walletrpc.ListUnspentResponse
         * @instance
         */
        ListUnspentResponse.prototype.utxos = $util.emptyArray;

        /**
         * Creates a new ListUnspentResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListUnspentResponse
         * @static
         * @param {walletrpc.ListUnspentResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListUnspentResponse} ListUnspentResponse instance
         * @type {{
         *   (properties: walletrpc.ListUnspentResponse.$Shape): walletrpc.ListUnspentResponse & walletrpc.ListUnspentResponse.$Shape;
         *   (properties?: walletrpc.ListUnspentResponse.$Properties): walletrpc.ListUnspentResponse;
         * }}
         */
        ListUnspentResponse.create = function(properties) {
            return new ListUnspentResponse(properties);
        };

        /**
         * Encodes the specified ListUnspentResponse message. Does not implicitly {@link walletrpc.ListUnspentResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListUnspentResponse
         * @static
         * @param {walletrpc.ListUnspentResponse.$Properties} message ListUnspentResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListUnspentResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.utxos != null && message.utxos.length)
                for (let i = 0; i < message.utxos.length; ++i)
                    $root.lnrpc.Utxo.encode(message.utxos[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListUnspentResponse message, length delimited. Does not implicitly {@link walletrpc.ListUnspentResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListUnspentResponse
         * @static
         * @param {walletrpc.ListUnspentResponse.$Properties} message ListUnspentResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListUnspentResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListUnspentResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListUnspentResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListUnspentResponse & walletrpc.ListUnspentResponse.$Shape} ListUnspentResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListUnspentResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListUnspentResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.utxos && message.utxos.length))
                            message.utxos = [];
                        message.utxos.push($root.lnrpc.Utxo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListUnspentResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListUnspentResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListUnspentResponse & walletrpc.ListUnspentResponse.$Shape} ListUnspentResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListUnspentResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListUnspentResponse message.
         * @function verify
         * @memberof walletrpc.ListUnspentResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListUnspentResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.utxos != null && $Object.hasOwnProperty.call(message, "utxos")) {
                if (!$Array.isArray(message.utxos))
                    return "utxos: array expected";
                for (let i = 0; i < message.utxos.length; ++i) {
                    let error = $root.lnrpc.Utxo.verify(message.utxos[i], _depth + 1);
                    if (error)
                        return "utxos." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListUnspentResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListUnspentResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListUnspentResponse} ListUnspentResponse
         */
        ListUnspentResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListUnspentResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListUnspentResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ListUnspentResponse();
            if (object.utxos) {
                if (!$Array.isArray(object.utxos))
                    throw $TypeError(".walletrpc.ListUnspentResponse.utxos: array expected");
                message.utxos = $Array(object.utxos.length);
                for (let i = 0; i < object.utxos.length; ++i) {
                    if (!$util.isObject(object.utxos[i]))
                        throw $TypeError(".walletrpc.ListUnspentResponse.utxos: object expected");
                    message.utxos[i] = $root.lnrpc.Utxo.fromObject(object.utxos[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListUnspentResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListUnspentResponse
         * @static
         * @param {walletrpc.ListUnspentResponse} message ListUnspentResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListUnspentResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.utxos = [];
            if (message.utxos && message.utxos.length) {
                object.utxos = $Array(message.utxos.length);
                for (let j = 0; j < message.utxos.length; ++j)
                    object.utxos[j] = $root.lnrpc.Utxo.toObject(message.utxos[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ListUnspentResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.ListUnspentResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListUnspentResponse.prototype.toJSON = function() {
            return ListUnspentResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListUnspentResponse
         * @function getTypeUrl
         * @memberof walletrpc.ListUnspentResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListUnspentResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListUnspentResponse";
        };

        return ListUnspentResponse;
    })();

    walletrpc.LeaseOutputRequest = (function() {

        /**
         * Properties of a LeaseOutputRequest.
         * @typedef {Object} walletrpc.LeaseOutputRequest.$Properties
         * @property {Uint8Array|null} [id] LeaseOutputRequest id
         * @property {lnrpc.OutPoint.$Properties|null} [outpoint] LeaseOutputRequest outpoint
         * @property {Long|null} [expiration_seconds] LeaseOutputRequest expiration_seconds
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LeaseOutputRequest.
         * @memberof walletrpc
         * @interface ILeaseOutputRequest
         * @augments walletrpc.LeaseOutputRequest.$Properties
         * @deprecated Use walletrpc.LeaseOutputRequest.$Properties instead.
         */

        /**
         * Shape of a LeaseOutputRequest.
         * @typedef {walletrpc.LeaseOutputRequest.$Properties} walletrpc.LeaseOutputRequest.$Shape
         */

        /**
         * Constructs a new LeaseOutputRequest.
         * @memberof walletrpc
         * @classdesc Represents a LeaseOutputRequest.
         * @constructor
         * @param {walletrpc.LeaseOutputRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LeaseOutputRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LeaseOutputRequest id.
         * @member {Uint8Array} id
         * @memberof walletrpc.LeaseOutputRequest
         * @instance
         */
        LeaseOutputRequest.prototype.id = $util.newBuffer([]);

        /**
         * LeaseOutputRequest outpoint.
         * @member {lnrpc.OutPoint.$Properties|null|undefined} outpoint
         * @memberof walletrpc.LeaseOutputRequest
         * @instance
         */
        LeaseOutputRequest.prototype.outpoint = null;

        /**
         * LeaseOutputRequest expiration_seconds.
         * @member {Long} expiration_seconds
         * @memberof walletrpc.LeaseOutputRequest
         * @instance
         */
        LeaseOutputRequest.prototype.expiration_seconds = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new LeaseOutputRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.LeaseOutputRequest
         * @static
         * @param {walletrpc.LeaseOutputRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.LeaseOutputRequest} LeaseOutputRequest instance
         * @type {{
         *   (properties: walletrpc.LeaseOutputRequest.$Shape): walletrpc.LeaseOutputRequest & walletrpc.LeaseOutputRequest.$Shape;
         *   (properties?: walletrpc.LeaseOutputRequest.$Properties): walletrpc.LeaseOutputRequest;
         * }}
         */
        LeaseOutputRequest.create = function(properties) {
            return new LeaseOutputRequest(properties);
        };

        /**
         * Encodes the specified LeaseOutputRequest message. Does not implicitly {@link walletrpc.LeaseOutputRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.LeaseOutputRequest
         * @static
         * @param {walletrpc.LeaseOutputRequest.$Properties} message LeaseOutputRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LeaseOutputRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.id);
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                $root.lnrpc.OutPoint.encode(message.outpoint, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.expiration_seconds != null && $Object.hasOwnProperty.call(message, "expiration_seconds") && (typeof message.expiration_seconds === "object" ? message.expiration_seconds.low || message.expiration_seconds.high : message.expiration_seconds !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.expiration_seconds);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LeaseOutputRequest message, length delimited. Does not implicitly {@link walletrpc.LeaseOutputRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.LeaseOutputRequest
         * @static
         * @param {walletrpc.LeaseOutputRequest.$Properties} message LeaseOutputRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LeaseOutputRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LeaseOutputRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.LeaseOutputRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.LeaseOutputRequest & walletrpc.LeaseOutputRequest.$Shape} LeaseOutputRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LeaseOutputRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.LeaseOutputRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.outpoint = $root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.outpoint);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiration_seconds = value;
                        else
                            delete message.expiration_seconds;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LeaseOutputRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.LeaseOutputRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.LeaseOutputRequest & walletrpc.LeaseOutputRequest.$Shape} LeaseOutputRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LeaseOutputRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LeaseOutputRequest message.
         * @function verify
         * @memberof walletrpc.LeaseOutputRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LeaseOutputRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!(message.id && typeof message.id.length === "number" || $util.isString(message.id)))
                    return "id: buffer expected";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint")) {
                let error = $root.lnrpc.OutPoint.verify(message.outpoint, _depth + 1);
                if (error)
                    return "outpoint." + error;
            }
            if (message.expiration_seconds != null && $Object.hasOwnProperty.call(message, "expiration_seconds"))
                if (!$util.isInteger(message.expiration_seconds) && !(message.expiration_seconds && $util.isInteger(message.expiration_seconds.low) && $util.isInteger(message.expiration_seconds.high)))
                    return "expiration_seconds: integer|Long expected";
            return null;
        };

        /**
         * Creates a LeaseOutputRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.LeaseOutputRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.LeaseOutputRequest} LeaseOutputRequest
         */
        LeaseOutputRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.LeaseOutputRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.LeaseOutputRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.LeaseOutputRequest();
            if (object.id != null)
                if (object.id.length)
                    if (typeof object.id === "string")
                        $util.base64.decode(object.id, message.id = $util.newBuffer($util.base64.length(object.id)), 0);
                    else if (object.id.length >= 0)
                        message.id = object.id;
            if (object.outpoint != null) {
                if (!$util.isObject(object.outpoint))
                    throw $TypeError(".walletrpc.LeaseOutputRequest.outpoint: object expected");
                message.outpoint = $root.lnrpc.OutPoint.fromObject(object.outpoint, _depth + 1);
            }
            if (object.expiration_seconds != null)
                if (typeof object.expiration_seconds === "object" ? object.expiration_seconds.low || object.expiration_seconds.high : $Number(object.expiration_seconds) !== 0)
                    if ($util.Long)
                        message.expiration_seconds = $util.Long.fromValue(object.expiration_seconds, true);
                    else if (typeof object.expiration_seconds === "string")
                        message.expiration_seconds = $parseInt(object.expiration_seconds, 10);
                    else if (typeof object.expiration_seconds === "number")
                        message.expiration_seconds = object.expiration_seconds;
                    else if (typeof object.expiration_seconds === "object")
                        message.expiration_seconds = new $util.LongBits(object.expiration_seconds.low >>> 0, object.expiration_seconds.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a LeaseOutputRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.LeaseOutputRequest
         * @static
         * @param {walletrpc.LeaseOutputRequest} message LeaseOutputRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LeaseOutputRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.id = "";
                else {
                    object.id = [];
                    if (options.bytes !== $Array)
                        object.id = $util.newBuffer(object.id);
                }
                object.outpoint = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.expiration_seconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiration_seconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = options.bytes === $String ? $util.base64.encode(message.id, 0, message.id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.id) : message.id;
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = $root.lnrpc.OutPoint.toObject(message.outpoint, options, _depth + 1);
            if (message.expiration_seconds != null && $Object.hasOwnProperty.call(message, "expiration_seconds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiration_seconds = typeof message.expiration_seconds === "number" ? $BigInt(message.expiration_seconds) : $util.Long.fromBits(message.expiration_seconds.low >>> 0, message.expiration_seconds.high >>> 0, true).toBigInt();
                else if (typeof message.expiration_seconds === "number")
                    object.expiration_seconds = options.longs === $String ? $String(message.expiration_seconds) : message.expiration_seconds;
                else
                    object.expiration_seconds = options.longs === $String ? $util.Long.prototype.toString.call(message.expiration_seconds) : options.longs === $Number ? new $util.LongBits(message.expiration_seconds.low >>> 0, message.expiration_seconds.high >>> 0).toNumber(true) : message.expiration_seconds;
            return object;
        };

        /**
         * Converts this LeaseOutputRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.LeaseOutputRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LeaseOutputRequest.prototype.toJSON = function() {
            return LeaseOutputRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LeaseOutputRequest
         * @function getTypeUrl
         * @memberof walletrpc.LeaseOutputRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LeaseOutputRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.LeaseOutputRequest";
        };

        return LeaseOutputRequest;
    })();

    walletrpc.LeaseOutputResponse = (function() {

        /**
         * Properties of a LeaseOutputResponse.
         * @typedef {Object} walletrpc.LeaseOutputResponse.$Properties
         * @property {Long|null} [expiration] LeaseOutputResponse expiration
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LeaseOutputResponse.
         * @memberof walletrpc
         * @interface ILeaseOutputResponse
         * @augments walletrpc.LeaseOutputResponse.$Properties
         * @deprecated Use walletrpc.LeaseOutputResponse.$Properties instead.
         */

        /**
         * Shape of a LeaseOutputResponse.
         * @typedef {walletrpc.LeaseOutputResponse.$Properties} walletrpc.LeaseOutputResponse.$Shape
         */

        /**
         * Constructs a new LeaseOutputResponse.
         * @memberof walletrpc
         * @classdesc Represents a LeaseOutputResponse.
         * @constructor
         * @param {walletrpc.LeaseOutputResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LeaseOutputResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LeaseOutputResponse expiration.
         * @member {Long} expiration
         * @memberof walletrpc.LeaseOutputResponse
         * @instance
         */
        LeaseOutputResponse.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new LeaseOutputResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.LeaseOutputResponse
         * @static
         * @param {walletrpc.LeaseOutputResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.LeaseOutputResponse} LeaseOutputResponse instance
         * @type {{
         *   (properties: walletrpc.LeaseOutputResponse.$Shape): walletrpc.LeaseOutputResponse & walletrpc.LeaseOutputResponse.$Shape;
         *   (properties?: walletrpc.LeaseOutputResponse.$Properties): walletrpc.LeaseOutputResponse;
         * }}
         */
        LeaseOutputResponse.create = function(properties) {
            return new LeaseOutputResponse(properties);
        };

        /**
         * Encodes the specified LeaseOutputResponse message. Does not implicitly {@link walletrpc.LeaseOutputResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.LeaseOutputResponse
         * @static
         * @param {walletrpc.LeaseOutputResponse.$Properties} message LeaseOutputResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LeaseOutputResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.expiration != null && $Object.hasOwnProperty.call(message, "expiration") && (typeof message.expiration === "object" ? message.expiration.low || message.expiration.high : message.expiration !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.expiration);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LeaseOutputResponse message, length delimited. Does not implicitly {@link walletrpc.LeaseOutputResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.LeaseOutputResponse
         * @static
         * @param {walletrpc.LeaseOutputResponse.$Properties} message LeaseOutputResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LeaseOutputResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LeaseOutputResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.LeaseOutputResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.LeaseOutputResponse & walletrpc.LeaseOutputResponse.$Shape} LeaseOutputResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LeaseOutputResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.LeaseOutputResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiration = value;
                        else
                            delete message.expiration;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LeaseOutputResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.LeaseOutputResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.LeaseOutputResponse & walletrpc.LeaseOutputResponse.$Shape} LeaseOutputResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LeaseOutputResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LeaseOutputResponse message.
         * @function verify
         * @memberof walletrpc.LeaseOutputResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LeaseOutputResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.expiration != null && $Object.hasOwnProperty.call(message, "expiration"))
                if (!$util.isInteger(message.expiration) && !(message.expiration && $util.isInteger(message.expiration.low) && $util.isInteger(message.expiration.high)))
                    return "expiration: integer|Long expected";
            return null;
        };

        /**
         * Creates a LeaseOutputResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.LeaseOutputResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.LeaseOutputResponse} LeaseOutputResponse
         */
        LeaseOutputResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.LeaseOutputResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.LeaseOutputResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.LeaseOutputResponse();
            if (object.expiration != null)
                if (typeof object.expiration === "object" ? object.expiration.low || object.expiration.high : $Number(object.expiration) !== 0)
                    if ($util.Long)
                        message.expiration = $util.Long.fromValue(object.expiration, true);
                    else if (typeof object.expiration === "string")
                        message.expiration = $parseInt(object.expiration, 10);
                    else if (typeof object.expiration === "number")
                        message.expiration = object.expiration;
                    else if (typeof object.expiration === "object")
                        message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a LeaseOutputResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.LeaseOutputResponse
         * @static
         * @param {walletrpc.LeaseOutputResponse} message LeaseOutputResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LeaseOutputResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.expiration = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.expiration != null && $Object.hasOwnProperty.call(message, "expiration"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiration = typeof message.expiration === "number" ? $BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, true).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === $String ? $String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === $String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === $Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber(true) : message.expiration;
            return object;
        };

        /**
         * Converts this LeaseOutputResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.LeaseOutputResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LeaseOutputResponse.prototype.toJSON = function() {
            return LeaseOutputResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LeaseOutputResponse
         * @function getTypeUrl
         * @memberof walletrpc.LeaseOutputResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LeaseOutputResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.LeaseOutputResponse";
        };

        return LeaseOutputResponse;
    })();

    walletrpc.ReleaseOutputRequest = (function() {

        /**
         * Properties of a ReleaseOutputRequest.
         * @typedef {Object} walletrpc.ReleaseOutputRequest.$Properties
         * @property {Uint8Array|null} [id] ReleaseOutputRequest id
         * @property {lnrpc.OutPoint.$Properties|null} [outpoint] ReleaseOutputRequest outpoint
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ReleaseOutputRequest.
         * @memberof walletrpc
         * @interface IReleaseOutputRequest
         * @augments walletrpc.ReleaseOutputRequest.$Properties
         * @deprecated Use walletrpc.ReleaseOutputRequest.$Properties instead.
         */

        /**
         * Shape of a ReleaseOutputRequest.
         * @typedef {walletrpc.ReleaseOutputRequest.$Properties} walletrpc.ReleaseOutputRequest.$Shape
         */

        /**
         * Constructs a new ReleaseOutputRequest.
         * @memberof walletrpc
         * @classdesc Represents a ReleaseOutputRequest.
         * @constructor
         * @param {walletrpc.ReleaseOutputRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ReleaseOutputRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ReleaseOutputRequest id.
         * @member {Uint8Array} id
         * @memberof walletrpc.ReleaseOutputRequest
         * @instance
         */
        ReleaseOutputRequest.prototype.id = $util.newBuffer([]);

        /**
         * ReleaseOutputRequest outpoint.
         * @member {lnrpc.OutPoint.$Properties|null|undefined} outpoint
         * @memberof walletrpc.ReleaseOutputRequest
         * @instance
         */
        ReleaseOutputRequest.prototype.outpoint = null;

        /**
         * Creates a new ReleaseOutputRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.ReleaseOutputRequest
         * @static
         * @param {walletrpc.ReleaseOutputRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ReleaseOutputRequest} ReleaseOutputRequest instance
         * @type {{
         *   (properties: walletrpc.ReleaseOutputRequest.$Shape): walletrpc.ReleaseOutputRequest & walletrpc.ReleaseOutputRequest.$Shape;
         *   (properties?: walletrpc.ReleaseOutputRequest.$Properties): walletrpc.ReleaseOutputRequest;
         * }}
         */
        ReleaseOutputRequest.create = function(properties) {
            return new ReleaseOutputRequest(properties);
        };

        /**
         * Encodes the specified ReleaseOutputRequest message. Does not implicitly {@link walletrpc.ReleaseOutputRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ReleaseOutputRequest
         * @static
         * @param {walletrpc.ReleaseOutputRequest.$Properties} message ReleaseOutputRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReleaseOutputRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.id);
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                $root.lnrpc.OutPoint.encode(message.outpoint, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ReleaseOutputRequest message, length delimited. Does not implicitly {@link walletrpc.ReleaseOutputRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ReleaseOutputRequest
         * @static
         * @param {walletrpc.ReleaseOutputRequest.$Properties} message ReleaseOutputRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReleaseOutputRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReleaseOutputRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ReleaseOutputRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ReleaseOutputRequest & walletrpc.ReleaseOutputRequest.$Shape} ReleaseOutputRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReleaseOutputRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ReleaseOutputRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.outpoint = $root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.outpoint);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ReleaseOutputRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ReleaseOutputRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ReleaseOutputRequest & walletrpc.ReleaseOutputRequest.$Shape} ReleaseOutputRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReleaseOutputRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReleaseOutputRequest message.
         * @function verify
         * @memberof walletrpc.ReleaseOutputRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReleaseOutputRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!(message.id && typeof message.id.length === "number" || $util.isString(message.id)))
                    return "id: buffer expected";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint")) {
                let error = $root.lnrpc.OutPoint.verify(message.outpoint, _depth + 1);
                if (error)
                    return "outpoint." + error;
            }
            return null;
        };

        /**
         * Creates a ReleaseOutputRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ReleaseOutputRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ReleaseOutputRequest} ReleaseOutputRequest
         */
        ReleaseOutputRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ReleaseOutputRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ReleaseOutputRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ReleaseOutputRequest();
            if (object.id != null)
                if (object.id.length)
                    if (typeof object.id === "string")
                        $util.base64.decode(object.id, message.id = $util.newBuffer($util.base64.length(object.id)), 0);
                    else if (object.id.length >= 0)
                        message.id = object.id;
            if (object.outpoint != null) {
                if (!$util.isObject(object.outpoint))
                    throw $TypeError(".walletrpc.ReleaseOutputRequest.outpoint: object expected");
                message.outpoint = $root.lnrpc.OutPoint.fromObject(object.outpoint, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ReleaseOutputRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ReleaseOutputRequest
         * @static
         * @param {walletrpc.ReleaseOutputRequest} message ReleaseOutputRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReleaseOutputRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.id = "";
                else {
                    object.id = [];
                    if (options.bytes !== $Array)
                        object.id = $util.newBuffer(object.id);
                }
                object.outpoint = null;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = options.bytes === $String ? $util.base64.encode(message.id, 0, message.id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.id) : message.id;
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = $root.lnrpc.OutPoint.toObject(message.outpoint, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ReleaseOutputRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.ReleaseOutputRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReleaseOutputRequest.prototype.toJSON = function() {
            return ReleaseOutputRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ReleaseOutputRequest
         * @function getTypeUrl
         * @memberof walletrpc.ReleaseOutputRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ReleaseOutputRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ReleaseOutputRequest";
        };

        return ReleaseOutputRequest;
    })();

    walletrpc.ReleaseOutputResponse = (function() {

        /**
         * Properties of a ReleaseOutputResponse.
         * @typedef {Object} walletrpc.ReleaseOutputResponse.$Properties
         * @property {string|null} [status] ReleaseOutputResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ReleaseOutputResponse.
         * @memberof walletrpc
         * @interface IReleaseOutputResponse
         * @augments walletrpc.ReleaseOutputResponse.$Properties
         * @deprecated Use walletrpc.ReleaseOutputResponse.$Properties instead.
         */

        /**
         * Shape of a ReleaseOutputResponse.
         * @typedef {walletrpc.ReleaseOutputResponse.$Properties} walletrpc.ReleaseOutputResponse.$Shape
         */

        /**
         * Constructs a new ReleaseOutputResponse.
         * @memberof walletrpc
         * @classdesc Represents a ReleaseOutputResponse.
         * @constructor
         * @param {walletrpc.ReleaseOutputResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ReleaseOutputResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ReleaseOutputResponse status.
         * @member {string} status
         * @memberof walletrpc.ReleaseOutputResponse
         * @instance
         */
        ReleaseOutputResponse.prototype.status = "";

        /**
         * Creates a new ReleaseOutputResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.ReleaseOutputResponse
         * @static
         * @param {walletrpc.ReleaseOutputResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ReleaseOutputResponse} ReleaseOutputResponse instance
         * @type {{
         *   (properties: walletrpc.ReleaseOutputResponse.$Shape): walletrpc.ReleaseOutputResponse & walletrpc.ReleaseOutputResponse.$Shape;
         *   (properties?: walletrpc.ReleaseOutputResponse.$Properties): walletrpc.ReleaseOutputResponse;
         * }}
         */
        ReleaseOutputResponse.create = function(properties) {
            return new ReleaseOutputResponse(properties);
        };

        /**
         * Encodes the specified ReleaseOutputResponse message. Does not implicitly {@link walletrpc.ReleaseOutputResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ReleaseOutputResponse
         * @static
         * @param {walletrpc.ReleaseOutputResponse.$Properties} message ReleaseOutputResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReleaseOutputResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ReleaseOutputResponse message, length delimited. Does not implicitly {@link walletrpc.ReleaseOutputResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ReleaseOutputResponse
         * @static
         * @param {walletrpc.ReleaseOutputResponse.$Properties} message ReleaseOutputResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReleaseOutputResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReleaseOutputResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ReleaseOutputResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ReleaseOutputResponse & walletrpc.ReleaseOutputResponse.$Shape} ReleaseOutputResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReleaseOutputResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ReleaseOutputResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ReleaseOutputResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ReleaseOutputResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ReleaseOutputResponse & walletrpc.ReleaseOutputResponse.$Shape} ReleaseOutputResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReleaseOutputResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReleaseOutputResponse message.
         * @function verify
         * @memberof walletrpc.ReleaseOutputResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReleaseOutputResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a ReleaseOutputResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ReleaseOutputResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ReleaseOutputResponse} ReleaseOutputResponse
         */
        ReleaseOutputResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ReleaseOutputResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ReleaseOutputResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ReleaseOutputResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a ReleaseOutputResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ReleaseOutputResponse
         * @static
         * @param {walletrpc.ReleaseOutputResponse} message ReleaseOutputResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReleaseOutputResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this ReleaseOutputResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.ReleaseOutputResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReleaseOutputResponse.prototype.toJSON = function() {
            return ReleaseOutputResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ReleaseOutputResponse
         * @function getTypeUrl
         * @memberof walletrpc.ReleaseOutputResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ReleaseOutputResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ReleaseOutputResponse";
        };

        return ReleaseOutputResponse;
    })();

    walletrpc.KeyReq = (function() {

        /**
         * Properties of a KeyReq.
         * @typedef {Object} walletrpc.KeyReq.$Properties
         * @property {number|null} [key_finger_print] KeyReq key_finger_print
         * @property {number|null} [key_family] KeyReq key_family
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a KeyReq.
         * @memberof walletrpc
         * @interface IKeyReq
         * @augments walletrpc.KeyReq.$Properties
         * @deprecated Use walletrpc.KeyReq.$Properties instead.
         */

        /**
         * Shape of a KeyReq.
         * @typedef {walletrpc.KeyReq.$Properties} walletrpc.KeyReq.$Shape
         */

        /**
         * Constructs a new KeyReq.
         * @memberof walletrpc
         * @classdesc Represents a KeyReq.
         * @constructor
         * @param {walletrpc.KeyReq.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const KeyReq = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * KeyReq key_finger_print.
         * @member {number} key_finger_print
         * @memberof walletrpc.KeyReq
         * @instance
         */
        KeyReq.prototype.key_finger_print = 0;

        /**
         * KeyReq key_family.
         * @member {number} key_family
         * @memberof walletrpc.KeyReq
         * @instance
         */
        KeyReq.prototype.key_family = 0;

        /**
         * Creates a new KeyReq instance using the specified properties.
         * @function create
         * @memberof walletrpc.KeyReq
         * @static
         * @param {walletrpc.KeyReq.$Properties=} [properties] Properties to set
         * @returns {walletrpc.KeyReq} KeyReq instance
         * @type {{
         *   (properties: walletrpc.KeyReq.$Shape): walletrpc.KeyReq & walletrpc.KeyReq.$Shape;
         *   (properties?: walletrpc.KeyReq.$Properties): walletrpc.KeyReq;
         * }}
         */
        KeyReq.create = function(properties) {
            return new KeyReq(properties);
        };

        /**
         * Encodes the specified KeyReq message. Does not implicitly {@link walletrpc.KeyReq.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.KeyReq
         * @static
         * @param {walletrpc.KeyReq.$Properties} message KeyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyReq.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key_finger_print != null && $Object.hasOwnProperty.call(message, "key_finger_print") && message.key_finger_print !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.key_finger_print);
            if (message.key_family != null && $Object.hasOwnProperty.call(message, "key_family") && message.key_family !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.key_family);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified KeyReq message, length delimited. Does not implicitly {@link walletrpc.KeyReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.KeyReq
         * @static
         * @param {walletrpc.KeyReq.$Properties} message KeyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyReq.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KeyReq message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.KeyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.KeyReq & walletrpc.KeyReq.$Shape} KeyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyReq.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.KeyReq(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.key_finger_print = value;
                        else
                            delete message.key_finger_print;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.key_family = value;
                        else
                            delete message.key_family;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a KeyReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.KeyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.KeyReq & walletrpc.KeyReq.$Shape} KeyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyReq.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeyReq message.
         * @function verify
         * @memberof walletrpc.KeyReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeyReq.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key_finger_print != null && $Object.hasOwnProperty.call(message, "key_finger_print"))
                if (!$util.isInteger(message.key_finger_print))
                    return "key_finger_print: integer expected";
            if (message.key_family != null && $Object.hasOwnProperty.call(message, "key_family"))
                if (!$util.isInteger(message.key_family))
                    return "key_family: integer expected";
            return null;
        };

        /**
         * Creates a KeyReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.KeyReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.KeyReq} KeyReq
         */
        KeyReq.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.KeyReq)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.KeyReq: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.KeyReq();
            if (object.key_finger_print != null)
                if ($Number(object.key_finger_print) !== 0)
                    message.key_finger_print = object.key_finger_print | 0;
            if (object.key_family != null)
                if ($Number(object.key_family) !== 0)
                    message.key_family = object.key_family | 0;
            return message;
        };

        /**
         * Creates a plain object from a KeyReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.KeyReq
         * @static
         * @param {walletrpc.KeyReq} message KeyReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeyReq.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key_finger_print = 0;
                object.key_family = 0;
            }
            if (message.key_finger_print != null && $Object.hasOwnProperty.call(message, "key_finger_print"))
                object.key_finger_print = message.key_finger_print;
            if (message.key_family != null && $Object.hasOwnProperty.call(message, "key_family"))
                object.key_family = message.key_family;
            return object;
        };

        /**
         * Converts this KeyReq to JSON.
         * @function toJSON
         * @memberof walletrpc.KeyReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeyReq.prototype.toJSON = function() {
            return KeyReq.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for KeyReq
         * @function getTypeUrl
         * @memberof walletrpc.KeyReq
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        KeyReq.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.KeyReq";
        };

        return KeyReq;
    })();

    walletrpc.AddrRequest = (function() {

        /**
         * Properties of an AddrRequest.
         * @typedef {Object} walletrpc.AddrRequest.$Properties
         * @property {string|null} [account] AddrRequest account
         * @property {walletrpc.AddressType|null} [type] AddrRequest type
         * @property {boolean|null} [change] AddrRequest change
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddrRequest.
         * @memberof walletrpc
         * @interface IAddrRequest
         * @augments walletrpc.AddrRequest.$Properties
         * @deprecated Use walletrpc.AddrRequest.$Properties instead.
         */

        /**
         * Shape of an AddrRequest.
         * @typedef {walletrpc.AddrRequest.$Properties} walletrpc.AddrRequest.$Shape
         */

        /**
         * Constructs a new AddrRequest.
         * @memberof walletrpc
         * @classdesc Represents an AddrRequest.
         * @constructor
         * @param {walletrpc.AddrRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddrRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddrRequest account.
         * @member {string} account
         * @memberof walletrpc.AddrRequest
         * @instance
         */
        AddrRequest.prototype.account = "";

        /**
         * AddrRequest type.
         * @member {walletrpc.AddressType} type
         * @memberof walletrpc.AddrRequest
         * @instance
         */
        AddrRequest.prototype.type = 0;

        /**
         * AddrRequest change.
         * @member {boolean} change
         * @memberof walletrpc.AddrRequest
         * @instance
         */
        AddrRequest.prototype.change = false;

        /**
         * Creates a new AddrRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.AddrRequest
         * @static
         * @param {walletrpc.AddrRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.AddrRequest} AddrRequest instance
         * @type {{
         *   (properties: walletrpc.AddrRequest.$Shape): walletrpc.AddrRequest & walletrpc.AddrRequest.$Shape;
         *   (properties?: walletrpc.AddrRequest.$Properties): walletrpc.AddrRequest;
         * }}
         */
        AddrRequest.create = function(properties) {
            return new AddrRequest(properties);
        };

        /**
         * Encodes the specified AddrRequest message. Does not implicitly {@link walletrpc.AddrRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.AddrRequest
         * @static
         * @param {walletrpc.AddrRequest.$Properties} message AddrRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddrRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.account != null && $Object.hasOwnProperty.call(message, "account") && message.account !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.account);
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
            if (message.change != null && $Object.hasOwnProperty.call(message, "change") && message.change !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.change);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddrRequest message, length delimited. Does not implicitly {@link walletrpc.AddrRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.AddrRequest
         * @static
         * @param {walletrpc.AddrRequest.$Properties} message AddrRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddrRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddrRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.AddrRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.AddrRequest & walletrpc.AddrRequest.$Shape} AddrRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddrRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.AddrRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.change = value;
                        else
                            delete message.change;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddrRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.AddrRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.AddrRequest & walletrpc.AddrRequest.$Shape} AddrRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddrRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddrRequest message.
         * @function verify
         * @memberof walletrpc.AddrRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddrRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            if (message.change != null && $Object.hasOwnProperty.call(message, "change"))
                if (typeof message.change !== "boolean")
                    return "change: boolean expected";
            return null;
        };

        /**
         * Creates an AddrRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.AddrRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.AddrRequest} AddrRequest
         */
        AddrRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.AddrRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.AddrRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.AddrRequest();
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = $String(object.account);
            if (object.type !== 0 && (typeof object.type !== "string" || $root.walletrpc.AddressType[object.type] !== 0))
                switch (object.type) {
                case "UNKNOWN":
                case 0:
                    message.type = 0;
                    break;
                case "WITNESS_PUBKEY_HASH":
                case 1:
                    message.type = 1;
                    break;
                case "NESTED_WITNESS_PUBKEY_HASH":
                case 2:
                    message.type = 2;
                    break;
                case "HYBRID_NESTED_WITNESS_PUBKEY_HASH":
                case 3:
                    message.type = 3;
                    break;
                case "TAPROOT_PUBKEY":
                case 4:
                    message.type = 4;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
            if (object.change != null)
                if (object.change)
                    message.change = $Boolean(object.change);
            return message;
        };

        /**
         * Creates a plain object from an AddrRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.AddrRequest
         * @static
         * @param {walletrpc.AddrRequest} message AddrRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddrRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.account = "";
                object.type = options.enums === $String ? "UNKNOWN" : 0;
                object.change = false;
            }
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = message.account;
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === $String ? $root.walletrpc.AddressType[message.type] === $undefined ? message.type : $root.walletrpc.AddressType[message.type] : message.type;
            if (message.change != null && $Object.hasOwnProperty.call(message, "change"))
                object.change = message.change;
            return object;
        };

        /**
         * Converts this AddrRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.AddrRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddrRequest.prototype.toJSON = function() {
            return AddrRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddrRequest
         * @function getTypeUrl
         * @memberof walletrpc.AddrRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddrRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.AddrRequest";
        };

        return AddrRequest;
    })();

    walletrpc.AddrResponse = (function() {

        /**
         * Properties of an AddrResponse.
         * @typedef {Object} walletrpc.AddrResponse.$Properties
         * @property {string|null} [addr] AddrResponse addr
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddrResponse.
         * @memberof walletrpc
         * @interface IAddrResponse
         * @augments walletrpc.AddrResponse.$Properties
         * @deprecated Use walletrpc.AddrResponse.$Properties instead.
         */

        /**
         * Shape of an AddrResponse.
         * @typedef {walletrpc.AddrResponse.$Properties} walletrpc.AddrResponse.$Shape
         */

        /**
         * Constructs a new AddrResponse.
         * @memberof walletrpc
         * @classdesc Represents an AddrResponse.
         * @constructor
         * @param {walletrpc.AddrResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddrResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddrResponse addr.
         * @member {string} addr
         * @memberof walletrpc.AddrResponse
         * @instance
         */
        AddrResponse.prototype.addr = "";

        /**
         * Creates a new AddrResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.AddrResponse
         * @static
         * @param {walletrpc.AddrResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.AddrResponse} AddrResponse instance
         * @type {{
         *   (properties: walletrpc.AddrResponse.$Shape): walletrpc.AddrResponse & walletrpc.AddrResponse.$Shape;
         *   (properties?: walletrpc.AddrResponse.$Properties): walletrpc.AddrResponse;
         * }}
         */
        AddrResponse.create = function(properties) {
            return new AddrResponse(properties);
        };

        /**
         * Encodes the specified AddrResponse message. Does not implicitly {@link walletrpc.AddrResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.AddrResponse
         * @static
         * @param {walletrpc.AddrResponse.$Properties} message AddrResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddrResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr") && message.addr !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.addr);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddrResponse message, length delimited. Does not implicitly {@link walletrpc.AddrResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.AddrResponse
         * @static
         * @param {walletrpc.AddrResponse.$Properties} message AddrResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddrResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddrResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.AddrResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.AddrResponse & walletrpc.AddrResponse.$Shape} AddrResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddrResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.AddrResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.addr = value;
                        else
                            delete message.addr;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddrResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.AddrResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.AddrResponse & walletrpc.AddrResponse.$Shape} AddrResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddrResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddrResponse message.
         * @function verify
         * @memberof walletrpc.AddrResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddrResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                if (!$util.isString(message.addr))
                    return "addr: string expected";
            return null;
        };

        /**
         * Creates an AddrResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.AddrResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.AddrResponse} AddrResponse
         */
        AddrResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.AddrResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.AddrResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.AddrResponse();
            if (object.addr != null)
                if (typeof object.addr !== "string" || object.addr.length)
                    message.addr = $String(object.addr);
            return message;
        };

        /**
         * Creates a plain object from an AddrResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.AddrResponse
         * @static
         * @param {walletrpc.AddrResponse} message AddrResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddrResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.addr = "";
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                object.addr = message.addr;
            return object;
        };

        /**
         * Converts this AddrResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.AddrResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddrResponse.prototype.toJSON = function() {
            return AddrResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddrResponse
         * @function getTypeUrl
         * @memberof walletrpc.AddrResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddrResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.AddrResponse";
        };

        return AddrResponse;
    })();

    /**
     * AddressType enum.
     * @name walletrpc.AddressType
     * @enum {number}
     * @property {number} UNKNOWN=0 UNKNOWN value
     * @property {number} WITNESS_PUBKEY_HASH=1 WITNESS_PUBKEY_HASH value
     * @property {number} NESTED_WITNESS_PUBKEY_HASH=2 NESTED_WITNESS_PUBKEY_HASH value
     * @property {number} HYBRID_NESTED_WITNESS_PUBKEY_HASH=3 HYBRID_NESTED_WITNESS_PUBKEY_HASH value
     * @property {number} TAPROOT_PUBKEY=4 TAPROOT_PUBKEY value
     */
    walletrpc.AddressType = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "WITNESS_PUBKEY_HASH"] = 1;
        values[valuesById[2] = "NESTED_WITNESS_PUBKEY_HASH"] = 2;
        values[valuesById[3] = "HYBRID_NESTED_WITNESS_PUBKEY_HASH"] = 3;
        values[valuesById[4] = "TAPROOT_PUBKEY"] = 4;
        return values;
    })();

    walletrpc.Account = (function() {

        /**
         * Properties of an Account.
         * @typedef {Object} walletrpc.Account.$Properties
         * @property {string|null} [name] Account name
         * @property {walletrpc.AddressType|null} [address_type] Account address_type
         * @property {string|null} [extended_public_key] Account extended_public_key
         * @property {Uint8Array|null} [master_key_fingerprint] Account master_key_fingerprint
         * @property {string|null} [derivation_path] Account derivation_path
         * @property {number|null} [external_key_count] Account external_key_count
         * @property {number|null} [internal_key_count] Account internal_key_count
         * @property {boolean|null} [watch_only] Account watch_only
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an Account.
         * @memberof walletrpc
         * @interface IAccount
         * @augments walletrpc.Account.$Properties
         * @deprecated Use walletrpc.Account.$Properties instead.
         */

        /**
         * Shape of an Account.
         * @typedef {walletrpc.Account.$Properties} walletrpc.Account.$Shape
         */

        /**
         * Constructs a new Account.
         * @memberof walletrpc
         * @classdesc Represents an Account.
         * @constructor
         * @param {walletrpc.Account.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Account = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Account name.
         * @member {string} name
         * @memberof walletrpc.Account
         * @instance
         */
        Account.prototype.name = "";

        /**
         * Account address_type.
         * @member {walletrpc.AddressType} address_type
         * @memberof walletrpc.Account
         * @instance
         */
        Account.prototype.address_type = 0;

        /**
         * Account extended_public_key.
         * @member {string} extended_public_key
         * @memberof walletrpc.Account
         * @instance
         */
        Account.prototype.extended_public_key = "";

        /**
         * Account master_key_fingerprint.
         * @member {Uint8Array} master_key_fingerprint
         * @memberof walletrpc.Account
         * @instance
         */
        Account.prototype.master_key_fingerprint = $util.newBuffer([]);

        /**
         * Account derivation_path.
         * @member {string} derivation_path
         * @memberof walletrpc.Account
         * @instance
         */
        Account.prototype.derivation_path = "";

        /**
         * Account external_key_count.
         * @member {number} external_key_count
         * @memberof walletrpc.Account
         * @instance
         */
        Account.prototype.external_key_count = 0;

        /**
         * Account internal_key_count.
         * @member {number} internal_key_count
         * @memberof walletrpc.Account
         * @instance
         */
        Account.prototype.internal_key_count = 0;

        /**
         * Account watch_only.
         * @member {boolean} watch_only
         * @memberof walletrpc.Account
         * @instance
         */
        Account.prototype.watch_only = false;

        /**
         * Creates a new Account instance using the specified properties.
         * @function create
         * @memberof walletrpc.Account
         * @static
         * @param {walletrpc.Account.$Properties=} [properties] Properties to set
         * @returns {walletrpc.Account} Account instance
         * @type {{
         *   (properties: walletrpc.Account.$Shape): walletrpc.Account & walletrpc.Account.$Shape;
         *   (properties?: walletrpc.Account.$Properties): walletrpc.Account;
         * }}
         */
        Account.create = function(properties) {
            return new Account(properties);
        };

        /**
         * Encodes the specified Account message. Does not implicitly {@link walletrpc.Account.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.Account
         * @static
         * @param {walletrpc.Account.$Properties} message Account message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Account.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type") && message.address_type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.address_type);
            if (message.extended_public_key != null && $Object.hasOwnProperty.call(message, "extended_public_key") && message.extended_public_key !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.extended_public_key);
            if (message.master_key_fingerprint != null && $Object.hasOwnProperty.call(message, "master_key_fingerprint") && message.master_key_fingerprint.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.master_key_fingerprint);
            if (message.derivation_path != null && $Object.hasOwnProperty.call(message, "derivation_path") && message.derivation_path !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.derivation_path);
            if (message.external_key_count != null && $Object.hasOwnProperty.call(message, "external_key_count") && message.external_key_count !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.external_key_count);
            if (message.internal_key_count != null && $Object.hasOwnProperty.call(message, "internal_key_count") && message.internal_key_count !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.internal_key_count);
            if (message.watch_only != null && $Object.hasOwnProperty.call(message, "watch_only") && message.watch_only !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.watch_only);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Account message, length delimited. Does not implicitly {@link walletrpc.Account.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.Account
         * @static
         * @param {walletrpc.Account.$Properties} message Account message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Account.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Account message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.Account
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.Account & walletrpc.Account.$Shape} Account
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Account.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.Account(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.address_type = value;
                        else
                            delete message.address_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.extended_public_key = value;
                        else
                            delete message.extended_public_key;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.master_key_fingerprint = value;
                        else
                            delete message.master_key_fingerprint;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.derivation_path = value;
                        else
                            delete message.derivation_path;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.external_key_count = value;
                        else
                            delete message.external_key_count;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.internal_key_count = value;
                        else
                            delete message.internal_key_count;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.watch_only = value;
                        else
                            delete message.watch_only;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Account message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.Account
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.Account & walletrpc.Account.$Shape} Account
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Account.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Account message.
         * @function verify
         * @memberof walletrpc.Account
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Account.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                if (typeof message.address_type !== "number" || (message.address_type | 0) !== message.address_type)
                    return "address_type: enum value expected";
            if (message.extended_public_key != null && $Object.hasOwnProperty.call(message, "extended_public_key"))
                if (!$util.isString(message.extended_public_key))
                    return "extended_public_key: string expected";
            if (message.master_key_fingerprint != null && $Object.hasOwnProperty.call(message, "master_key_fingerprint"))
                if (!(message.master_key_fingerprint && typeof message.master_key_fingerprint.length === "number" || $util.isString(message.master_key_fingerprint)))
                    return "master_key_fingerprint: buffer expected";
            if (message.derivation_path != null && $Object.hasOwnProperty.call(message, "derivation_path"))
                if (!$util.isString(message.derivation_path))
                    return "derivation_path: string expected";
            if (message.external_key_count != null && $Object.hasOwnProperty.call(message, "external_key_count"))
                if (!$util.isInteger(message.external_key_count))
                    return "external_key_count: integer expected";
            if (message.internal_key_count != null && $Object.hasOwnProperty.call(message, "internal_key_count"))
                if (!$util.isInteger(message.internal_key_count))
                    return "internal_key_count: integer expected";
            if (message.watch_only != null && $Object.hasOwnProperty.call(message, "watch_only"))
                if (typeof message.watch_only !== "boolean")
                    return "watch_only: boolean expected";
            return null;
        };

        /**
         * Creates an Account message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.Account
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.Account} Account
         */
        Account.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.Account)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.Account: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.Account();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.address_type !== 0 && (typeof object.address_type !== "string" || $root.walletrpc.AddressType[object.address_type] !== 0))
                switch (object.address_type) {
                case "UNKNOWN":
                case 0:
                    message.address_type = 0;
                    break;
                case "WITNESS_PUBKEY_HASH":
                case 1:
                    message.address_type = 1;
                    break;
                case "NESTED_WITNESS_PUBKEY_HASH":
                case 2:
                    message.address_type = 2;
                    break;
                case "HYBRID_NESTED_WITNESS_PUBKEY_HASH":
                case 3:
                    message.address_type = 3;
                    break;
                case "TAPROOT_PUBKEY":
                case 4:
                    message.address_type = 4;
                    break;
                default:
                    if (typeof object.address_type === "number" && (object.address_type | 0) === object.address_type)
                        message.address_type = object.address_type;
                }
            if (object.extended_public_key != null)
                if (typeof object.extended_public_key !== "string" || object.extended_public_key.length)
                    message.extended_public_key = $String(object.extended_public_key);
            if (object.master_key_fingerprint != null)
                if (object.master_key_fingerprint.length)
                    if (typeof object.master_key_fingerprint === "string")
                        $util.base64.decode(object.master_key_fingerprint, message.master_key_fingerprint = $util.newBuffer($util.base64.length(object.master_key_fingerprint)), 0);
                    else if (object.master_key_fingerprint.length >= 0)
                        message.master_key_fingerprint = object.master_key_fingerprint;
            if (object.derivation_path != null)
                if (typeof object.derivation_path !== "string" || object.derivation_path.length)
                    message.derivation_path = $String(object.derivation_path);
            if (object.external_key_count != null)
                if ($Number(object.external_key_count) !== 0)
                    message.external_key_count = object.external_key_count >>> 0;
            if (object.internal_key_count != null)
                if ($Number(object.internal_key_count) !== 0)
                    message.internal_key_count = object.internal_key_count >>> 0;
            if (object.watch_only != null)
                if (object.watch_only)
                    message.watch_only = $Boolean(object.watch_only);
            return message;
        };

        /**
         * Creates a plain object from an Account message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.Account
         * @static
         * @param {walletrpc.Account} message Account
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Account.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.address_type = options.enums === $String ? "UNKNOWN" : 0;
                object.extended_public_key = "";
                if (options.bytes === $String)
                    object.master_key_fingerprint = "";
                else {
                    object.master_key_fingerprint = [];
                    if (options.bytes !== $Array)
                        object.master_key_fingerprint = $util.newBuffer(object.master_key_fingerprint);
                }
                object.derivation_path = "";
                object.external_key_count = 0;
                object.internal_key_count = 0;
                object.watch_only = false;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                object.address_type = options.enums === $String ? $root.walletrpc.AddressType[message.address_type] === $undefined ? message.address_type : $root.walletrpc.AddressType[message.address_type] : message.address_type;
            if (message.extended_public_key != null && $Object.hasOwnProperty.call(message, "extended_public_key"))
                object.extended_public_key = message.extended_public_key;
            if (message.master_key_fingerprint != null && $Object.hasOwnProperty.call(message, "master_key_fingerprint"))
                object.master_key_fingerprint = options.bytes === $String ? $util.base64.encode(message.master_key_fingerprint, 0, message.master_key_fingerprint.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.master_key_fingerprint) : message.master_key_fingerprint;
            if (message.derivation_path != null && $Object.hasOwnProperty.call(message, "derivation_path"))
                object.derivation_path = message.derivation_path;
            if (message.external_key_count != null && $Object.hasOwnProperty.call(message, "external_key_count"))
                object.external_key_count = message.external_key_count;
            if (message.internal_key_count != null && $Object.hasOwnProperty.call(message, "internal_key_count"))
                object.internal_key_count = message.internal_key_count;
            if (message.watch_only != null && $Object.hasOwnProperty.call(message, "watch_only"))
                object.watch_only = message.watch_only;
            return object;
        };

        /**
         * Converts this Account to JSON.
         * @function toJSON
         * @memberof walletrpc.Account
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Account.prototype.toJSON = function() {
            return Account.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Account
         * @function getTypeUrl
         * @memberof walletrpc.Account
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Account.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.Account";
        };

        return Account;
    })();

    walletrpc.AddressProperty = (function() {

        /**
         * Properties of an AddressProperty.
         * @typedef {Object} walletrpc.AddressProperty.$Properties
         * @property {string|null} [address] AddressProperty address
         * @property {boolean|null} [is_internal] AddressProperty is_internal
         * @property {Long|null} [balance] AddressProperty balance
         * @property {string|null} [derivation_path] AddressProperty derivation_path
         * @property {Uint8Array|null} [public_key] AddressProperty public_key
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddressProperty.
         * @memberof walletrpc
         * @interface IAddressProperty
         * @augments walletrpc.AddressProperty.$Properties
         * @deprecated Use walletrpc.AddressProperty.$Properties instead.
         */

        /**
         * Shape of an AddressProperty.
         * @typedef {walletrpc.AddressProperty.$Properties} walletrpc.AddressProperty.$Shape
         */

        /**
         * Constructs a new AddressProperty.
         * @memberof walletrpc
         * @classdesc Represents an AddressProperty.
         * @constructor
         * @param {walletrpc.AddressProperty.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddressProperty = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddressProperty address.
         * @member {string} address
         * @memberof walletrpc.AddressProperty
         * @instance
         */
        AddressProperty.prototype.address = "";

        /**
         * AddressProperty is_internal.
         * @member {boolean} is_internal
         * @memberof walletrpc.AddressProperty
         * @instance
         */
        AddressProperty.prototype.is_internal = false;

        /**
         * AddressProperty balance.
         * @member {Long} balance
         * @memberof walletrpc.AddressProperty
         * @instance
         */
        AddressProperty.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AddressProperty derivation_path.
         * @member {string} derivation_path
         * @memberof walletrpc.AddressProperty
         * @instance
         */
        AddressProperty.prototype.derivation_path = "";

        /**
         * AddressProperty public_key.
         * @member {Uint8Array} public_key
         * @memberof walletrpc.AddressProperty
         * @instance
         */
        AddressProperty.prototype.public_key = $util.newBuffer([]);

        /**
         * Creates a new AddressProperty instance using the specified properties.
         * @function create
         * @memberof walletrpc.AddressProperty
         * @static
         * @param {walletrpc.AddressProperty.$Properties=} [properties] Properties to set
         * @returns {walletrpc.AddressProperty} AddressProperty instance
         * @type {{
         *   (properties: walletrpc.AddressProperty.$Shape): walletrpc.AddressProperty & walletrpc.AddressProperty.$Shape;
         *   (properties?: walletrpc.AddressProperty.$Properties): walletrpc.AddressProperty;
         * }}
         */
        AddressProperty.create = function(properties) {
            return new AddressProperty(properties);
        };

        /**
         * Encodes the specified AddressProperty message. Does not implicitly {@link walletrpc.AddressProperty.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.AddressProperty
         * @static
         * @param {walletrpc.AddressProperty.$Properties} message AddressProperty message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddressProperty.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
            if (message.is_internal != null && $Object.hasOwnProperty.call(message, "is_internal") && message.is_internal !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.is_internal);
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.balance);
            if (message.derivation_path != null && $Object.hasOwnProperty.call(message, "derivation_path") && message.derivation_path !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.derivation_path);
            if (message.public_key != null && $Object.hasOwnProperty.call(message, "public_key") && message.public_key.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.public_key);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddressProperty message, length delimited. Does not implicitly {@link walletrpc.AddressProperty.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.AddressProperty
         * @static
         * @param {walletrpc.AddressProperty.$Properties} message AddressProperty message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddressProperty.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddressProperty message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.AddressProperty
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.AddressProperty & walletrpc.AddressProperty.$Shape} AddressProperty
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddressProperty.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.AddressProperty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.address = value;
                        else
                            delete message.address;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_internal = value;
                        else
                            delete message.is_internal;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.balance = value;
                        else
                            delete message.balance;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.derivation_path = value;
                        else
                            delete message.derivation_path;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.public_key = value;
                        else
                            delete message.public_key;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddressProperty message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.AddressProperty
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.AddressProperty & walletrpc.AddressProperty.$Shape} AddressProperty
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddressProperty.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddressProperty message.
         * @function verify
         * @memberof walletrpc.AddressProperty
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddressProperty.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.is_internal != null && $Object.hasOwnProperty.call(message, "is_internal"))
                if (typeof message.is_internal !== "boolean")
                    return "is_internal: boolean expected";
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                    return "balance: integer|Long expected";
            if (message.derivation_path != null && $Object.hasOwnProperty.call(message, "derivation_path"))
                if (!$util.isString(message.derivation_path))
                    return "derivation_path: string expected";
            if (message.public_key != null && $Object.hasOwnProperty.call(message, "public_key"))
                if (!(message.public_key && typeof message.public_key.length === "number" || $util.isString(message.public_key)))
                    return "public_key: buffer expected";
            return null;
        };

        /**
         * Creates an AddressProperty message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.AddressProperty
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.AddressProperty} AddressProperty
         */
        AddressProperty.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.AddressProperty)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.AddressProperty: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.AddressProperty();
            if (object.address != null)
                if (typeof object.address !== "string" || object.address.length)
                    message.address = $String(object.address);
            if (object.is_internal != null)
                if (object.is_internal)
                    message.is_internal = $Boolean(object.is_internal);
            if (object.balance != null)
                if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                    if ($util.Long)
                        message.balance = $util.Long.fromValue(object.balance, false);
                    else if (typeof object.balance === "string")
                        message.balance = $parseInt(object.balance, 10);
                    else if (typeof object.balance === "number")
                        message.balance = object.balance;
                    else if (typeof object.balance === "object")
                        message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber();
            if (object.derivation_path != null)
                if (typeof object.derivation_path !== "string" || object.derivation_path.length)
                    message.derivation_path = $String(object.derivation_path);
            if (object.public_key != null)
                if (object.public_key.length)
                    if (typeof object.public_key === "string")
                        $util.base64.decode(object.public_key, message.public_key = $util.newBuffer($util.base64.length(object.public_key)), 0);
                    else if (object.public_key.length >= 0)
                        message.public_key = object.public_key;
            return message;
        };

        /**
         * Creates a plain object from an AddressProperty message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.AddressProperty
         * @static
         * @param {walletrpc.AddressProperty} message AddressProperty
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddressProperty.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.address = "";
                object.is_internal = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.derivation_path = "";
                if (options.bytes === $String)
                    object.public_key = "";
                else {
                    object.public_key = [];
                    if (options.bytes !== $Array)
                        object.public_key = $util.newBuffer(object.public_key);
                }
            }
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                object.address = message.address;
            if (message.is_internal != null && $Object.hasOwnProperty.call(message, "is_internal"))
                object.is_internal = message.is_internal;
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, false).toBigInt();
                else if (typeof message.balance === "number")
                    object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                else
                    object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber() : message.balance;
            if (message.derivation_path != null && $Object.hasOwnProperty.call(message, "derivation_path"))
                object.derivation_path = message.derivation_path;
            if (message.public_key != null && $Object.hasOwnProperty.call(message, "public_key"))
                object.public_key = options.bytes === $String ? $util.base64.encode(message.public_key, 0, message.public_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.public_key) : message.public_key;
            return object;
        };

        /**
         * Converts this AddressProperty to JSON.
         * @function toJSON
         * @memberof walletrpc.AddressProperty
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddressProperty.prototype.toJSON = function() {
            return AddressProperty.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddressProperty
         * @function getTypeUrl
         * @memberof walletrpc.AddressProperty
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddressProperty.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.AddressProperty";
        };

        return AddressProperty;
    })();

    walletrpc.AccountWithAddresses = (function() {

        /**
         * Properties of an AccountWithAddresses.
         * @typedef {Object} walletrpc.AccountWithAddresses.$Properties
         * @property {string|null} [name] AccountWithAddresses name
         * @property {walletrpc.AddressType|null} [address_type] AccountWithAddresses address_type
         * @property {string|null} [derivation_path] AccountWithAddresses derivation_path
         * @property {Array.<walletrpc.AddressProperty.$Properties>|null} [addresses] AccountWithAddresses addresses
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AccountWithAddresses.
         * @memberof walletrpc
         * @interface IAccountWithAddresses
         * @augments walletrpc.AccountWithAddresses.$Properties
         * @deprecated Use walletrpc.AccountWithAddresses.$Properties instead.
         */

        /**
         * Shape of an AccountWithAddresses.
         * @typedef {walletrpc.AccountWithAddresses.$Properties} walletrpc.AccountWithAddresses.$Shape
         */

        /**
         * Constructs a new AccountWithAddresses.
         * @memberof walletrpc
         * @classdesc Represents an AccountWithAddresses.
         * @constructor
         * @param {walletrpc.AccountWithAddresses.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AccountWithAddresses = function (properties) {
            this.addresses = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AccountWithAddresses name.
         * @member {string} name
         * @memberof walletrpc.AccountWithAddresses
         * @instance
         */
        AccountWithAddresses.prototype.name = "";

        /**
         * AccountWithAddresses address_type.
         * @member {walletrpc.AddressType} address_type
         * @memberof walletrpc.AccountWithAddresses
         * @instance
         */
        AccountWithAddresses.prototype.address_type = 0;

        /**
         * AccountWithAddresses derivation_path.
         * @member {string} derivation_path
         * @memberof walletrpc.AccountWithAddresses
         * @instance
         */
        AccountWithAddresses.prototype.derivation_path = "";

        /**
         * AccountWithAddresses addresses.
         * @member {Array.<walletrpc.AddressProperty.$Properties>} addresses
         * @memberof walletrpc.AccountWithAddresses
         * @instance
         */
        AccountWithAddresses.prototype.addresses = $util.emptyArray;

        /**
         * Creates a new AccountWithAddresses instance using the specified properties.
         * @function create
         * @memberof walletrpc.AccountWithAddresses
         * @static
         * @param {walletrpc.AccountWithAddresses.$Properties=} [properties] Properties to set
         * @returns {walletrpc.AccountWithAddresses} AccountWithAddresses instance
         * @type {{
         *   (properties: walletrpc.AccountWithAddresses.$Shape): walletrpc.AccountWithAddresses & walletrpc.AccountWithAddresses.$Shape;
         *   (properties?: walletrpc.AccountWithAddresses.$Properties): walletrpc.AccountWithAddresses;
         * }}
         */
        AccountWithAddresses.create = function(properties) {
            return new AccountWithAddresses(properties);
        };

        /**
         * Encodes the specified AccountWithAddresses message. Does not implicitly {@link walletrpc.AccountWithAddresses.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.AccountWithAddresses
         * @static
         * @param {walletrpc.AccountWithAddresses.$Properties} message AccountWithAddresses message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountWithAddresses.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type") && message.address_type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.address_type);
            if (message.derivation_path != null && $Object.hasOwnProperty.call(message, "derivation_path") && message.derivation_path !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.derivation_path);
            if (message.addresses != null && message.addresses.length)
                for (let i = 0; i < message.addresses.length; ++i)
                    $root.walletrpc.AddressProperty.encode(message.addresses[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AccountWithAddresses message, length delimited. Does not implicitly {@link walletrpc.AccountWithAddresses.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.AccountWithAddresses
         * @static
         * @param {walletrpc.AccountWithAddresses.$Properties} message AccountWithAddresses message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountWithAddresses.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AccountWithAddresses message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.AccountWithAddresses
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.AccountWithAddresses & walletrpc.AccountWithAddresses.$Shape} AccountWithAddresses
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountWithAddresses.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.AccountWithAddresses(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.address_type = value;
                        else
                            delete message.address_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.derivation_path = value;
                        else
                            delete message.derivation_path;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.addresses && message.addresses.length))
                            message.addresses = [];
                        message.addresses.push($root.walletrpc.AddressProperty.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AccountWithAddresses message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.AccountWithAddresses
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.AccountWithAddresses & walletrpc.AccountWithAddresses.$Shape} AccountWithAddresses
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountWithAddresses.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccountWithAddresses message.
         * @function verify
         * @memberof walletrpc.AccountWithAddresses
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccountWithAddresses.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                if (typeof message.address_type !== "number" || (message.address_type | 0) !== message.address_type)
                    return "address_type: enum value expected";
            if (message.derivation_path != null && $Object.hasOwnProperty.call(message, "derivation_path"))
                if (!$util.isString(message.derivation_path))
                    return "derivation_path: string expected";
            if (message.addresses != null && $Object.hasOwnProperty.call(message, "addresses")) {
                if (!$Array.isArray(message.addresses))
                    return "addresses: array expected";
                for (let i = 0; i < message.addresses.length; ++i) {
                    let error = $root.walletrpc.AddressProperty.verify(message.addresses[i], _depth + 1);
                    if (error)
                        return "addresses." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AccountWithAddresses message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.AccountWithAddresses
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.AccountWithAddresses} AccountWithAddresses
         */
        AccountWithAddresses.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.AccountWithAddresses)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.AccountWithAddresses: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.AccountWithAddresses();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.address_type !== 0 && (typeof object.address_type !== "string" || $root.walletrpc.AddressType[object.address_type] !== 0))
                switch (object.address_type) {
                case "UNKNOWN":
                case 0:
                    message.address_type = 0;
                    break;
                case "WITNESS_PUBKEY_HASH":
                case 1:
                    message.address_type = 1;
                    break;
                case "NESTED_WITNESS_PUBKEY_HASH":
                case 2:
                    message.address_type = 2;
                    break;
                case "HYBRID_NESTED_WITNESS_PUBKEY_HASH":
                case 3:
                    message.address_type = 3;
                    break;
                case "TAPROOT_PUBKEY":
                case 4:
                    message.address_type = 4;
                    break;
                default:
                    if (typeof object.address_type === "number" && (object.address_type | 0) === object.address_type)
                        message.address_type = object.address_type;
                }
            if (object.derivation_path != null)
                if (typeof object.derivation_path !== "string" || object.derivation_path.length)
                    message.derivation_path = $String(object.derivation_path);
            if (object.addresses) {
                if (!$Array.isArray(object.addresses))
                    throw $TypeError(".walletrpc.AccountWithAddresses.addresses: array expected");
                message.addresses = $Array(object.addresses.length);
                for (let i = 0; i < object.addresses.length; ++i) {
                    if (!$util.isObject(object.addresses[i]))
                        throw $TypeError(".walletrpc.AccountWithAddresses.addresses: object expected");
                    message.addresses[i] = $root.walletrpc.AddressProperty.fromObject(object.addresses[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AccountWithAddresses message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.AccountWithAddresses
         * @static
         * @param {walletrpc.AccountWithAddresses} message AccountWithAddresses
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountWithAddresses.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.addresses = [];
            if (options.defaults) {
                object.name = "";
                object.address_type = options.enums === $String ? "UNKNOWN" : 0;
                object.derivation_path = "";
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                object.address_type = options.enums === $String ? $root.walletrpc.AddressType[message.address_type] === $undefined ? message.address_type : $root.walletrpc.AddressType[message.address_type] : message.address_type;
            if (message.derivation_path != null && $Object.hasOwnProperty.call(message, "derivation_path"))
                object.derivation_path = message.derivation_path;
            if (message.addresses && message.addresses.length) {
                object.addresses = $Array(message.addresses.length);
                for (let j = 0; j < message.addresses.length; ++j)
                    object.addresses[j] = $root.walletrpc.AddressProperty.toObject(message.addresses[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this AccountWithAddresses to JSON.
         * @function toJSON
         * @memberof walletrpc.AccountWithAddresses
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountWithAddresses.prototype.toJSON = function() {
            return AccountWithAddresses.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AccountWithAddresses
         * @function getTypeUrl
         * @memberof walletrpc.AccountWithAddresses
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AccountWithAddresses.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.AccountWithAddresses";
        };

        return AccountWithAddresses;
    })();

    walletrpc.ListAccountsRequest = (function() {

        /**
         * Properties of a ListAccountsRequest.
         * @typedef {Object} walletrpc.ListAccountsRequest.$Properties
         * @property {string|null} [name] ListAccountsRequest name
         * @property {walletrpc.AddressType|null} [address_type] ListAccountsRequest address_type
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListAccountsRequest.
         * @memberof walletrpc
         * @interface IListAccountsRequest
         * @augments walletrpc.ListAccountsRequest.$Properties
         * @deprecated Use walletrpc.ListAccountsRequest.$Properties instead.
         */

        /**
         * Shape of a ListAccountsRequest.
         * @typedef {walletrpc.ListAccountsRequest.$Properties} walletrpc.ListAccountsRequest.$Shape
         */

        /**
         * Constructs a new ListAccountsRequest.
         * @memberof walletrpc
         * @classdesc Represents a ListAccountsRequest.
         * @constructor
         * @param {walletrpc.ListAccountsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListAccountsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListAccountsRequest name.
         * @member {string} name
         * @memberof walletrpc.ListAccountsRequest
         * @instance
         */
        ListAccountsRequest.prototype.name = "";

        /**
         * ListAccountsRequest address_type.
         * @member {walletrpc.AddressType} address_type
         * @memberof walletrpc.ListAccountsRequest
         * @instance
         */
        ListAccountsRequest.prototype.address_type = 0;

        /**
         * Creates a new ListAccountsRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListAccountsRequest
         * @static
         * @param {walletrpc.ListAccountsRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListAccountsRequest} ListAccountsRequest instance
         * @type {{
         *   (properties: walletrpc.ListAccountsRequest.$Shape): walletrpc.ListAccountsRequest & walletrpc.ListAccountsRequest.$Shape;
         *   (properties?: walletrpc.ListAccountsRequest.$Properties): walletrpc.ListAccountsRequest;
         * }}
         */
        ListAccountsRequest.create = function(properties) {
            return new ListAccountsRequest(properties);
        };

        /**
         * Encodes the specified ListAccountsRequest message. Does not implicitly {@link walletrpc.ListAccountsRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListAccountsRequest
         * @static
         * @param {walletrpc.ListAccountsRequest.$Properties} message ListAccountsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAccountsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type") && message.address_type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.address_type);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListAccountsRequest message, length delimited. Does not implicitly {@link walletrpc.ListAccountsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListAccountsRequest
         * @static
         * @param {walletrpc.ListAccountsRequest.$Properties} message ListAccountsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAccountsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListAccountsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListAccountsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListAccountsRequest & walletrpc.ListAccountsRequest.$Shape} ListAccountsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAccountsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListAccountsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.address_type = value;
                        else
                            delete message.address_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListAccountsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListAccountsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListAccountsRequest & walletrpc.ListAccountsRequest.$Shape} ListAccountsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAccountsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListAccountsRequest message.
         * @function verify
         * @memberof walletrpc.ListAccountsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListAccountsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                if (typeof message.address_type !== "number" || (message.address_type | 0) !== message.address_type)
                    return "address_type: enum value expected";
            return null;
        };

        /**
         * Creates a ListAccountsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListAccountsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListAccountsRequest} ListAccountsRequest
         */
        ListAccountsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListAccountsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListAccountsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ListAccountsRequest();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.address_type !== 0 && (typeof object.address_type !== "string" || $root.walletrpc.AddressType[object.address_type] !== 0))
                switch (object.address_type) {
                case "UNKNOWN":
                case 0:
                    message.address_type = 0;
                    break;
                case "WITNESS_PUBKEY_HASH":
                case 1:
                    message.address_type = 1;
                    break;
                case "NESTED_WITNESS_PUBKEY_HASH":
                case 2:
                    message.address_type = 2;
                    break;
                case "HYBRID_NESTED_WITNESS_PUBKEY_HASH":
                case 3:
                    message.address_type = 3;
                    break;
                case "TAPROOT_PUBKEY":
                case 4:
                    message.address_type = 4;
                    break;
                default:
                    if (typeof object.address_type === "number" && (object.address_type | 0) === object.address_type)
                        message.address_type = object.address_type;
                }
            return message;
        };

        /**
         * Creates a plain object from a ListAccountsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListAccountsRequest
         * @static
         * @param {walletrpc.ListAccountsRequest} message ListAccountsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListAccountsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.address_type = options.enums === $String ? "UNKNOWN" : 0;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                object.address_type = options.enums === $String ? $root.walletrpc.AddressType[message.address_type] === $undefined ? message.address_type : $root.walletrpc.AddressType[message.address_type] : message.address_type;
            return object;
        };

        /**
         * Converts this ListAccountsRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.ListAccountsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListAccountsRequest.prototype.toJSON = function() {
            return ListAccountsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListAccountsRequest
         * @function getTypeUrl
         * @memberof walletrpc.ListAccountsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListAccountsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListAccountsRequest";
        };

        return ListAccountsRequest;
    })();

    walletrpc.ListAccountsResponse = (function() {

        /**
         * Properties of a ListAccountsResponse.
         * @typedef {Object} walletrpc.ListAccountsResponse.$Properties
         * @property {Array.<walletrpc.Account.$Properties>|null} [accounts] ListAccountsResponse accounts
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListAccountsResponse.
         * @memberof walletrpc
         * @interface IListAccountsResponse
         * @augments walletrpc.ListAccountsResponse.$Properties
         * @deprecated Use walletrpc.ListAccountsResponse.$Properties instead.
         */

        /**
         * Shape of a ListAccountsResponse.
         * @typedef {walletrpc.ListAccountsResponse.$Properties} walletrpc.ListAccountsResponse.$Shape
         */

        /**
         * Constructs a new ListAccountsResponse.
         * @memberof walletrpc
         * @classdesc Represents a ListAccountsResponse.
         * @constructor
         * @param {walletrpc.ListAccountsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListAccountsResponse = function (properties) {
            this.accounts = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListAccountsResponse accounts.
         * @member {Array.<walletrpc.Account.$Properties>} accounts
         * @memberof walletrpc.ListAccountsResponse
         * @instance
         */
        ListAccountsResponse.prototype.accounts = $util.emptyArray;

        /**
         * Creates a new ListAccountsResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListAccountsResponse
         * @static
         * @param {walletrpc.ListAccountsResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListAccountsResponse} ListAccountsResponse instance
         * @type {{
         *   (properties: walletrpc.ListAccountsResponse.$Shape): walletrpc.ListAccountsResponse & walletrpc.ListAccountsResponse.$Shape;
         *   (properties?: walletrpc.ListAccountsResponse.$Properties): walletrpc.ListAccountsResponse;
         * }}
         */
        ListAccountsResponse.create = function(properties) {
            return new ListAccountsResponse(properties);
        };

        /**
         * Encodes the specified ListAccountsResponse message. Does not implicitly {@link walletrpc.ListAccountsResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListAccountsResponse
         * @static
         * @param {walletrpc.ListAccountsResponse.$Properties} message ListAccountsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAccountsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accounts != null && message.accounts.length)
                for (let i = 0; i < message.accounts.length; ++i)
                    $root.walletrpc.Account.encode(message.accounts[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListAccountsResponse message, length delimited. Does not implicitly {@link walletrpc.ListAccountsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListAccountsResponse
         * @static
         * @param {walletrpc.ListAccountsResponse.$Properties} message ListAccountsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAccountsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListAccountsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListAccountsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListAccountsResponse & walletrpc.ListAccountsResponse.$Shape} ListAccountsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAccountsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListAccountsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.accounts && message.accounts.length))
                            message.accounts = [];
                        message.accounts.push($root.walletrpc.Account.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListAccountsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListAccountsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListAccountsResponse & walletrpc.ListAccountsResponse.$Shape} ListAccountsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAccountsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListAccountsResponse message.
         * @function verify
         * @memberof walletrpc.ListAccountsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListAccountsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accounts != null && $Object.hasOwnProperty.call(message, "accounts")) {
                if (!$Array.isArray(message.accounts))
                    return "accounts: array expected";
                for (let i = 0; i < message.accounts.length; ++i) {
                    let error = $root.walletrpc.Account.verify(message.accounts[i], _depth + 1);
                    if (error)
                        return "accounts." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListAccountsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListAccountsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListAccountsResponse} ListAccountsResponse
         */
        ListAccountsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListAccountsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListAccountsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ListAccountsResponse();
            if (object.accounts) {
                if (!$Array.isArray(object.accounts))
                    throw $TypeError(".walletrpc.ListAccountsResponse.accounts: array expected");
                message.accounts = $Array(object.accounts.length);
                for (let i = 0; i < object.accounts.length; ++i) {
                    if (!$util.isObject(object.accounts[i]))
                        throw $TypeError(".walletrpc.ListAccountsResponse.accounts: object expected");
                    message.accounts[i] = $root.walletrpc.Account.fromObject(object.accounts[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListAccountsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListAccountsResponse
         * @static
         * @param {walletrpc.ListAccountsResponse} message ListAccountsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListAccountsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.accounts = [];
            if (message.accounts && message.accounts.length) {
                object.accounts = $Array(message.accounts.length);
                for (let j = 0; j < message.accounts.length; ++j)
                    object.accounts[j] = $root.walletrpc.Account.toObject(message.accounts[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ListAccountsResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.ListAccountsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListAccountsResponse.prototype.toJSON = function() {
            return ListAccountsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListAccountsResponse
         * @function getTypeUrl
         * @memberof walletrpc.ListAccountsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListAccountsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListAccountsResponse";
        };

        return ListAccountsResponse;
    })();

    walletrpc.RequiredReserveRequest = (function() {

        /**
         * Properties of a RequiredReserveRequest.
         * @typedef {Object} walletrpc.RequiredReserveRequest.$Properties
         * @property {number|null} [additional_public_channels] RequiredReserveRequest additional_public_channels
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RequiredReserveRequest.
         * @memberof walletrpc
         * @interface IRequiredReserveRequest
         * @augments walletrpc.RequiredReserveRequest.$Properties
         * @deprecated Use walletrpc.RequiredReserveRequest.$Properties instead.
         */

        /**
         * Shape of a RequiredReserveRequest.
         * @typedef {walletrpc.RequiredReserveRequest.$Properties} walletrpc.RequiredReserveRequest.$Shape
         */

        /**
         * Constructs a new RequiredReserveRequest.
         * @memberof walletrpc
         * @classdesc Represents a RequiredReserveRequest.
         * @constructor
         * @param {walletrpc.RequiredReserveRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RequiredReserveRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RequiredReserveRequest additional_public_channels.
         * @member {number} additional_public_channels
         * @memberof walletrpc.RequiredReserveRequest
         * @instance
         */
        RequiredReserveRequest.prototype.additional_public_channels = 0;

        /**
         * Creates a new RequiredReserveRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.RequiredReserveRequest
         * @static
         * @param {walletrpc.RequiredReserveRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.RequiredReserveRequest} RequiredReserveRequest instance
         * @type {{
         *   (properties: walletrpc.RequiredReserveRequest.$Shape): walletrpc.RequiredReserveRequest & walletrpc.RequiredReserveRequest.$Shape;
         *   (properties?: walletrpc.RequiredReserveRequest.$Properties): walletrpc.RequiredReserveRequest;
         * }}
         */
        RequiredReserveRequest.create = function(properties) {
            return new RequiredReserveRequest(properties);
        };

        /**
         * Encodes the specified RequiredReserveRequest message. Does not implicitly {@link walletrpc.RequiredReserveRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.RequiredReserveRequest
         * @static
         * @param {walletrpc.RequiredReserveRequest.$Properties} message RequiredReserveRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RequiredReserveRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.additional_public_channels != null && $Object.hasOwnProperty.call(message, "additional_public_channels") && message.additional_public_channels !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.additional_public_channels);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RequiredReserveRequest message, length delimited. Does not implicitly {@link walletrpc.RequiredReserveRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.RequiredReserveRequest
         * @static
         * @param {walletrpc.RequiredReserveRequest.$Properties} message RequiredReserveRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RequiredReserveRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RequiredReserveRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.RequiredReserveRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.RequiredReserveRequest & walletrpc.RequiredReserveRequest.$Shape} RequiredReserveRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RequiredReserveRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.RequiredReserveRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.additional_public_channels = value;
                        else
                            delete message.additional_public_channels;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RequiredReserveRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.RequiredReserveRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.RequiredReserveRequest & walletrpc.RequiredReserveRequest.$Shape} RequiredReserveRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RequiredReserveRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RequiredReserveRequest message.
         * @function verify
         * @memberof walletrpc.RequiredReserveRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RequiredReserveRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.additional_public_channels != null && $Object.hasOwnProperty.call(message, "additional_public_channels"))
                if (!$util.isInteger(message.additional_public_channels))
                    return "additional_public_channels: integer expected";
            return null;
        };

        /**
         * Creates a RequiredReserveRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.RequiredReserveRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.RequiredReserveRequest} RequiredReserveRequest
         */
        RequiredReserveRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.RequiredReserveRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.RequiredReserveRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.RequiredReserveRequest();
            if (object.additional_public_channels != null)
                if ($Number(object.additional_public_channels) !== 0)
                    message.additional_public_channels = object.additional_public_channels >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a RequiredReserveRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.RequiredReserveRequest
         * @static
         * @param {walletrpc.RequiredReserveRequest} message RequiredReserveRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RequiredReserveRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.additional_public_channels = 0;
            if (message.additional_public_channels != null && $Object.hasOwnProperty.call(message, "additional_public_channels"))
                object.additional_public_channels = message.additional_public_channels;
            return object;
        };

        /**
         * Converts this RequiredReserveRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.RequiredReserveRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RequiredReserveRequest.prototype.toJSON = function() {
            return RequiredReserveRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RequiredReserveRequest
         * @function getTypeUrl
         * @memberof walletrpc.RequiredReserveRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RequiredReserveRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.RequiredReserveRequest";
        };

        return RequiredReserveRequest;
    })();

    walletrpc.RequiredReserveResponse = (function() {

        /**
         * Properties of a RequiredReserveResponse.
         * @typedef {Object} walletrpc.RequiredReserveResponse.$Properties
         * @property {Long|null} [required_reserve] RequiredReserveResponse required_reserve
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RequiredReserveResponse.
         * @memberof walletrpc
         * @interface IRequiredReserveResponse
         * @augments walletrpc.RequiredReserveResponse.$Properties
         * @deprecated Use walletrpc.RequiredReserveResponse.$Properties instead.
         */

        /**
         * Shape of a RequiredReserveResponse.
         * @typedef {walletrpc.RequiredReserveResponse.$Properties} walletrpc.RequiredReserveResponse.$Shape
         */

        /**
         * Constructs a new RequiredReserveResponse.
         * @memberof walletrpc
         * @classdesc Represents a RequiredReserveResponse.
         * @constructor
         * @param {walletrpc.RequiredReserveResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RequiredReserveResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RequiredReserveResponse required_reserve.
         * @member {Long} required_reserve
         * @memberof walletrpc.RequiredReserveResponse
         * @instance
         */
        RequiredReserveResponse.prototype.required_reserve = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RequiredReserveResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.RequiredReserveResponse
         * @static
         * @param {walletrpc.RequiredReserveResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.RequiredReserveResponse} RequiredReserveResponse instance
         * @type {{
         *   (properties: walletrpc.RequiredReserveResponse.$Shape): walletrpc.RequiredReserveResponse & walletrpc.RequiredReserveResponse.$Shape;
         *   (properties?: walletrpc.RequiredReserveResponse.$Properties): walletrpc.RequiredReserveResponse;
         * }}
         */
        RequiredReserveResponse.create = function(properties) {
            return new RequiredReserveResponse(properties);
        };

        /**
         * Encodes the specified RequiredReserveResponse message. Does not implicitly {@link walletrpc.RequiredReserveResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.RequiredReserveResponse
         * @static
         * @param {walletrpc.RequiredReserveResponse.$Properties} message RequiredReserveResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RequiredReserveResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.required_reserve != null && $Object.hasOwnProperty.call(message, "required_reserve") && (typeof message.required_reserve === "object" ? message.required_reserve.low || message.required_reserve.high : message.required_reserve !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.required_reserve);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RequiredReserveResponse message, length delimited. Does not implicitly {@link walletrpc.RequiredReserveResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.RequiredReserveResponse
         * @static
         * @param {walletrpc.RequiredReserveResponse.$Properties} message RequiredReserveResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RequiredReserveResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RequiredReserveResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.RequiredReserveResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.RequiredReserveResponse & walletrpc.RequiredReserveResponse.$Shape} RequiredReserveResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RequiredReserveResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.RequiredReserveResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.required_reserve = value;
                        else
                            delete message.required_reserve;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RequiredReserveResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.RequiredReserveResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.RequiredReserveResponse & walletrpc.RequiredReserveResponse.$Shape} RequiredReserveResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RequiredReserveResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RequiredReserveResponse message.
         * @function verify
         * @memberof walletrpc.RequiredReserveResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RequiredReserveResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.required_reserve != null && $Object.hasOwnProperty.call(message, "required_reserve"))
                if (!$util.isInteger(message.required_reserve) && !(message.required_reserve && $util.isInteger(message.required_reserve.low) && $util.isInteger(message.required_reserve.high)))
                    return "required_reserve: integer|Long expected";
            return null;
        };

        /**
         * Creates a RequiredReserveResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.RequiredReserveResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.RequiredReserveResponse} RequiredReserveResponse
         */
        RequiredReserveResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.RequiredReserveResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.RequiredReserveResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.RequiredReserveResponse();
            if (object.required_reserve != null)
                if (typeof object.required_reserve === "object" ? object.required_reserve.low || object.required_reserve.high : $Number(object.required_reserve) !== 0)
                    if ($util.Long)
                        message.required_reserve = $util.Long.fromValue(object.required_reserve, false);
                    else if (typeof object.required_reserve === "string")
                        message.required_reserve = $parseInt(object.required_reserve, 10);
                    else if (typeof object.required_reserve === "number")
                        message.required_reserve = object.required_reserve;
                    else if (typeof object.required_reserve === "object")
                        message.required_reserve = new $util.LongBits(object.required_reserve.low >>> 0, object.required_reserve.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RequiredReserveResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.RequiredReserveResponse
         * @static
         * @param {walletrpc.RequiredReserveResponse} message RequiredReserveResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RequiredReserveResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.required_reserve = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.required_reserve = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.required_reserve != null && $Object.hasOwnProperty.call(message, "required_reserve"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.required_reserve = typeof message.required_reserve === "number" ? $BigInt(message.required_reserve) : $util.Long.fromBits(message.required_reserve.low >>> 0, message.required_reserve.high >>> 0, false).toBigInt();
                else if (typeof message.required_reserve === "number")
                    object.required_reserve = options.longs === $String ? $String(message.required_reserve) : message.required_reserve;
                else
                    object.required_reserve = options.longs === $String ? $util.Long.prototype.toString.call(message.required_reserve) : options.longs === $Number ? new $util.LongBits(message.required_reserve.low >>> 0, message.required_reserve.high >>> 0).toNumber() : message.required_reserve;
            return object;
        };

        /**
         * Converts this RequiredReserveResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.RequiredReserveResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RequiredReserveResponse.prototype.toJSON = function() {
            return RequiredReserveResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RequiredReserveResponse
         * @function getTypeUrl
         * @memberof walletrpc.RequiredReserveResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RequiredReserveResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.RequiredReserveResponse";
        };

        return RequiredReserveResponse;
    })();

    walletrpc.ListAddressesRequest = (function() {

        /**
         * Properties of a ListAddressesRequest.
         * @typedef {Object} walletrpc.ListAddressesRequest.$Properties
         * @property {string|null} [account_name] ListAddressesRequest account_name
         * @property {boolean|null} [show_custom_accounts] ListAddressesRequest show_custom_accounts
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListAddressesRequest.
         * @memberof walletrpc
         * @interface IListAddressesRequest
         * @augments walletrpc.ListAddressesRequest.$Properties
         * @deprecated Use walletrpc.ListAddressesRequest.$Properties instead.
         */

        /**
         * Shape of a ListAddressesRequest.
         * @typedef {walletrpc.ListAddressesRequest.$Properties} walletrpc.ListAddressesRequest.$Shape
         */

        /**
         * Constructs a new ListAddressesRequest.
         * @memberof walletrpc
         * @classdesc Represents a ListAddressesRequest.
         * @constructor
         * @param {walletrpc.ListAddressesRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListAddressesRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListAddressesRequest account_name.
         * @member {string} account_name
         * @memberof walletrpc.ListAddressesRequest
         * @instance
         */
        ListAddressesRequest.prototype.account_name = "";

        /**
         * ListAddressesRequest show_custom_accounts.
         * @member {boolean} show_custom_accounts
         * @memberof walletrpc.ListAddressesRequest
         * @instance
         */
        ListAddressesRequest.prototype.show_custom_accounts = false;

        /**
         * Creates a new ListAddressesRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListAddressesRequest
         * @static
         * @param {walletrpc.ListAddressesRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListAddressesRequest} ListAddressesRequest instance
         * @type {{
         *   (properties: walletrpc.ListAddressesRequest.$Shape): walletrpc.ListAddressesRequest & walletrpc.ListAddressesRequest.$Shape;
         *   (properties?: walletrpc.ListAddressesRequest.$Properties): walletrpc.ListAddressesRequest;
         * }}
         */
        ListAddressesRequest.create = function(properties) {
            return new ListAddressesRequest(properties);
        };

        /**
         * Encodes the specified ListAddressesRequest message. Does not implicitly {@link walletrpc.ListAddressesRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListAddressesRequest
         * @static
         * @param {walletrpc.ListAddressesRequest.$Properties} message ListAddressesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAddressesRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.account_name != null && $Object.hasOwnProperty.call(message, "account_name") && message.account_name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.account_name);
            if (message.show_custom_accounts != null && $Object.hasOwnProperty.call(message, "show_custom_accounts") && message.show_custom_accounts !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.show_custom_accounts);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListAddressesRequest message, length delimited. Does not implicitly {@link walletrpc.ListAddressesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListAddressesRequest
         * @static
         * @param {walletrpc.ListAddressesRequest.$Properties} message ListAddressesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAddressesRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListAddressesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListAddressesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListAddressesRequest & walletrpc.ListAddressesRequest.$Shape} ListAddressesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAddressesRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListAddressesRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.account_name = value;
                        else
                            delete message.account_name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.show_custom_accounts = value;
                        else
                            delete message.show_custom_accounts;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListAddressesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListAddressesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListAddressesRequest & walletrpc.ListAddressesRequest.$Shape} ListAddressesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAddressesRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListAddressesRequest message.
         * @function verify
         * @memberof walletrpc.ListAddressesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListAddressesRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_name != null && $Object.hasOwnProperty.call(message, "account_name"))
                if (!$util.isString(message.account_name))
                    return "account_name: string expected";
            if (message.show_custom_accounts != null && $Object.hasOwnProperty.call(message, "show_custom_accounts"))
                if (typeof message.show_custom_accounts !== "boolean")
                    return "show_custom_accounts: boolean expected";
            return null;
        };

        /**
         * Creates a ListAddressesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListAddressesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListAddressesRequest} ListAddressesRequest
         */
        ListAddressesRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListAddressesRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListAddressesRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ListAddressesRequest();
            if (object.account_name != null)
                if (typeof object.account_name !== "string" || object.account_name.length)
                    message.account_name = $String(object.account_name);
            if (object.show_custom_accounts != null)
                if (object.show_custom_accounts)
                    message.show_custom_accounts = $Boolean(object.show_custom_accounts);
            return message;
        };

        /**
         * Creates a plain object from a ListAddressesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListAddressesRequest
         * @static
         * @param {walletrpc.ListAddressesRequest} message ListAddressesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListAddressesRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.account_name = "";
                object.show_custom_accounts = false;
            }
            if (message.account_name != null && $Object.hasOwnProperty.call(message, "account_name"))
                object.account_name = message.account_name;
            if (message.show_custom_accounts != null && $Object.hasOwnProperty.call(message, "show_custom_accounts"))
                object.show_custom_accounts = message.show_custom_accounts;
            return object;
        };

        /**
         * Converts this ListAddressesRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.ListAddressesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListAddressesRequest.prototype.toJSON = function() {
            return ListAddressesRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListAddressesRequest
         * @function getTypeUrl
         * @memberof walletrpc.ListAddressesRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListAddressesRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListAddressesRequest";
        };

        return ListAddressesRequest;
    })();

    walletrpc.ListAddressesResponse = (function() {

        /**
         * Properties of a ListAddressesResponse.
         * @typedef {Object} walletrpc.ListAddressesResponse.$Properties
         * @property {Array.<walletrpc.AccountWithAddresses.$Properties>|null} [account_with_addresses] ListAddressesResponse account_with_addresses
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListAddressesResponse.
         * @memberof walletrpc
         * @interface IListAddressesResponse
         * @augments walletrpc.ListAddressesResponse.$Properties
         * @deprecated Use walletrpc.ListAddressesResponse.$Properties instead.
         */

        /**
         * Shape of a ListAddressesResponse.
         * @typedef {walletrpc.ListAddressesResponse.$Properties} walletrpc.ListAddressesResponse.$Shape
         */

        /**
         * Constructs a new ListAddressesResponse.
         * @memberof walletrpc
         * @classdesc Represents a ListAddressesResponse.
         * @constructor
         * @param {walletrpc.ListAddressesResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListAddressesResponse = function (properties) {
            this.account_with_addresses = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListAddressesResponse account_with_addresses.
         * @member {Array.<walletrpc.AccountWithAddresses.$Properties>} account_with_addresses
         * @memberof walletrpc.ListAddressesResponse
         * @instance
         */
        ListAddressesResponse.prototype.account_with_addresses = $util.emptyArray;

        /**
         * Creates a new ListAddressesResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListAddressesResponse
         * @static
         * @param {walletrpc.ListAddressesResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListAddressesResponse} ListAddressesResponse instance
         * @type {{
         *   (properties: walletrpc.ListAddressesResponse.$Shape): walletrpc.ListAddressesResponse & walletrpc.ListAddressesResponse.$Shape;
         *   (properties?: walletrpc.ListAddressesResponse.$Properties): walletrpc.ListAddressesResponse;
         * }}
         */
        ListAddressesResponse.create = function(properties) {
            return new ListAddressesResponse(properties);
        };

        /**
         * Encodes the specified ListAddressesResponse message. Does not implicitly {@link walletrpc.ListAddressesResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListAddressesResponse
         * @static
         * @param {walletrpc.ListAddressesResponse.$Properties} message ListAddressesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAddressesResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.account_with_addresses != null && message.account_with_addresses.length)
                for (let i = 0; i < message.account_with_addresses.length; ++i)
                    $root.walletrpc.AccountWithAddresses.encode(message.account_with_addresses[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListAddressesResponse message, length delimited. Does not implicitly {@link walletrpc.ListAddressesResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListAddressesResponse
         * @static
         * @param {walletrpc.ListAddressesResponse.$Properties} message ListAddressesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListAddressesResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListAddressesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListAddressesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListAddressesResponse & walletrpc.ListAddressesResponse.$Shape} ListAddressesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAddressesResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListAddressesResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.account_with_addresses && message.account_with_addresses.length))
                            message.account_with_addresses = [];
                        message.account_with_addresses.push($root.walletrpc.AccountWithAddresses.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListAddressesResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListAddressesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListAddressesResponse & walletrpc.ListAddressesResponse.$Shape} ListAddressesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListAddressesResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListAddressesResponse message.
         * @function verify
         * @memberof walletrpc.ListAddressesResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListAddressesResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_with_addresses != null && $Object.hasOwnProperty.call(message, "account_with_addresses")) {
                if (!$Array.isArray(message.account_with_addresses))
                    return "account_with_addresses: array expected";
                for (let i = 0; i < message.account_with_addresses.length; ++i) {
                    let error = $root.walletrpc.AccountWithAddresses.verify(message.account_with_addresses[i], _depth + 1);
                    if (error)
                        return "account_with_addresses." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListAddressesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListAddressesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListAddressesResponse} ListAddressesResponse
         */
        ListAddressesResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListAddressesResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListAddressesResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ListAddressesResponse();
            if (object.account_with_addresses) {
                if (!$Array.isArray(object.account_with_addresses))
                    throw $TypeError(".walletrpc.ListAddressesResponse.account_with_addresses: array expected");
                message.account_with_addresses = $Array(object.account_with_addresses.length);
                for (let i = 0; i < object.account_with_addresses.length; ++i) {
                    if (!$util.isObject(object.account_with_addresses[i]))
                        throw $TypeError(".walletrpc.ListAddressesResponse.account_with_addresses: object expected");
                    message.account_with_addresses[i] = $root.walletrpc.AccountWithAddresses.fromObject(object.account_with_addresses[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListAddressesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListAddressesResponse
         * @static
         * @param {walletrpc.ListAddressesResponse} message ListAddressesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListAddressesResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.account_with_addresses = [];
            if (message.account_with_addresses && message.account_with_addresses.length) {
                object.account_with_addresses = $Array(message.account_with_addresses.length);
                for (let j = 0; j < message.account_with_addresses.length; ++j)
                    object.account_with_addresses[j] = $root.walletrpc.AccountWithAddresses.toObject(message.account_with_addresses[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ListAddressesResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.ListAddressesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListAddressesResponse.prototype.toJSON = function() {
            return ListAddressesResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListAddressesResponse
         * @function getTypeUrl
         * @memberof walletrpc.ListAddressesResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListAddressesResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListAddressesResponse";
        };

        return ListAddressesResponse;
    })();

    walletrpc.GetTransactionRequest = (function() {

        /**
         * Properties of a GetTransactionRequest.
         * @typedef {Object} walletrpc.GetTransactionRequest.$Properties
         * @property {string|null} [txid] GetTransactionRequest txid
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetTransactionRequest.
         * @memberof walletrpc
         * @interface IGetTransactionRequest
         * @augments walletrpc.GetTransactionRequest.$Properties
         * @deprecated Use walletrpc.GetTransactionRequest.$Properties instead.
         */

        /**
         * Shape of a GetTransactionRequest.
         * @typedef {walletrpc.GetTransactionRequest.$Properties} walletrpc.GetTransactionRequest.$Shape
         */

        /**
         * Constructs a new GetTransactionRequest.
         * @memberof walletrpc
         * @classdesc Represents a GetTransactionRequest.
         * @constructor
         * @param {walletrpc.GetTransactionRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetTransactionRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetTransactionRequest txid.
         * @member {string} txid
         * @memberof walletrpc.GetTransactionRequest
         * @instance
         */
        GetTransactionRequest.prototype.txid = "";

        /**
         * Creates a new GetTransactionRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.GetTransactionRequest
         * @static
         * @param {walletrpc.GetTransactionRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.GetTransactionRequest} GetTransactionRequest instance
         * @type {{
         *   (properties: walletrpc.GetTransactionRequest.$Shape): walletrpc.GetTransactionRequest & walletrpc.GetTransactionRequest.$Shape;
         *   (properties?: walletrpc.GetTransactionRequest.$Properties): walletrpc.GetTransactionRequest;
         * }}
         */
        GetTransactionRequest.create = function(properties) {
            return new GetTransactionRequest(properties);
        };

        /**
         * Encodes the specified GetTransactionRequest message. Does not implicitly {@link walletrpc.GetTransactionRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.GetTransactionRequest
         * @static
         * @param {walletrpc.GetTransactionRequest.$Properties} message GetTransactionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTransactionRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid") && message.txid !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.txid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetTransactionRequest message, length delimited. Does not implicitly {@link walletrpc.GetTransactionRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.GetTransactionRequest
         * @static
         * @param {walletrpc.GetTransactionRequest.$Properties} message GetTransactionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTransactionRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetTransactionRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.GetTransactionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.GetTransactionRequest & walletrpc.GetTransactionRequest.$Shape} GetTransactionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTransactionRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.GetTransactionRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.txid = value;
                        else
                            delete message.txid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetTransactionRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.GetTransactionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.GetTransactionRequest & walletrpc.GetTransactionRequest.$Shape} GetTransactionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTransactionRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetTransactionRequest message.
         * @function verify
         * @memberof walletrpc.GetTransactionRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetTransactionRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                if (!$util.isString(message.txid))
                    return "txid: string expected";
            return null;
        };

        /**
         * Creates a GetTransactionRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.GetTransactionRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.GetTransactionRequest} GetTransactionRequest
         */
        GetTransactionRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.GetTransactionRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.GetTransactionRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.GetTransactionRequest();
            if (object.txid != null)
                if (typeof object.txid !== "string" || object.txid.length)
                    message.txid = $String(object.txid);
            return message;
        };

        /**
         * Creates a plain object from a GetTransactionRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.GetTransactionRequest
         * @static
         * @param {walletrpc.GetTransactionRequest} message GetTransactionRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetTransactionRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.txid = "";
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                object.txid = message.txid;
            return object;
        };

        /**
         * Converts this GetTransactionRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.GetTransactionRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetTransactionRequest.prototype.toJSON = function() {
            return GetTransactionRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetTransactionRequest
         * @function getTypeUrl
         * @memberof walletrpc.GetTransactionRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetTransactionRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.GetTransactionRequest";
        };

        return GetTransactionRequest;
    })();

    walletrpc.SignMessageWithAddrRequest = (function() {

        /**
         * Properties of a SignMessageWithAddrRequest.
         * @typedef {Object} walletrpc.SignMessageWithAddrRequest.$Properties
         * @property {Uint8Array|null} [msg] SignMessageWithAddrRequest msg
         * @property {string|null} [addr] SignMessageWithAddrRequest addr
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignMessageWithAddrRequest.
         * @memberof walletrpc
         * @interface ISignMessageWithAddrRequest
         * @augments walletrpc.SignMessageWithAddrRequest.$Properties
         * @deprecated Use walletrpc.SignMessageWithAddrRequest.$Properties instead.
         */

        /**
         * Shape of a SignMessageWithAddrRequest.
         * @typedef {walletrpc.SignMessageWithAddrRequest.$Properties} walletrpc.SignMessageWithAddrRequest.$Shape
         */

        /**
         * Constructs a new SignMessageWithAddrRequest.
         * @memberof walletrpc
         * @classdesc Represents a SignMessageWithAddrRequest.
         * @constructor
         * @param {walletrpc.SignMessageWithAddrRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignMessageWithAddrRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignMessageWithAddrRequest msg.
         * @member {Uint8Array} msg
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @instance
         */
        SignMessageWithAddrRequest.prototype.msg = $util.newBuffer([]);

        /**
         * SignMessageWithAddrRequest addr.
         * @member {string} addr
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @instance
         */
        SignMessageWithAddrRequest.prototype.addr = "";

        /**
         * Creates a new SignMessageWithAddrRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @static
         * @param {walletrpc.SignMessageWithAddrRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.SignMessageWithAddrRequest} SignMessageWithAddrRequest instance
         * @type {{
         *   (properties: walletrpc.SignMessageWithAddrRequest.$Shape): walletrpc.SignMessageWithAddrRequest & walletrpc.SignMessageWithAddrRequest.$Shape;
         *   (properties?: walletrpc.SignMessageWithAddrRequest.$Properties): walletrpc.SignMessageWithAddrRequest;
         * }}
         */
        SignMessageWithAddrRequest.create = function(properties) {
            return new SignMessageWithAddrRequest(properties);
        };

        /**
         * Encodes the specified SignMessageWithAddrRequest message. Does not implicitly {@link walletrpc.SignMessageWithAddrRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @static
         * @param {walletrpc.SignMessageWithAddrRequest.$Properties} message SignMessageWithAddrRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageWithAddrRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg") && message.msg.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.msg);
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr") && message.addr !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.addr);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignMessageWithAddrRequest message, length delimited. Does not implicitly {@link walletrpc.SignMessageWithAddrRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @static
         * @param {walletrpc.SignMessageWithAddrRequest.$Properties} message SignMessageWithAddrRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageWithAddrRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignMessageWithAddrRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.SignMessageWithAddrRequest & walletrpc.SignMessageWithAddrRequest.$Shape} SignMessageWithAddrRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageWithAddrRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.SignMessageWithAddrRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.msg = value;
                        else
                            delete message.msg;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.addr = value;
                        else
                            delete message.addr;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignMessageWithAddrRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.SignMessageWithAddrRequest & walletrpc.SignMessageWithAddrRequest.$Shape} SignMessageWithAddrRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageWithAddrRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignMessageWithAddrRequest message.
         * @function verify
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignMessageWithAddrRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                if (!(message.msg && typeof message.msg.length === "number" || $util.isString(message.msg)))
                    return "msg: buffer expected";
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                if (!$util.isString(message.addr))
                    return "addr: string expected";
            return null;
        };

        /**
         * Creates a SignMessageWithAddrRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.SignMessageWithAddrRequest} SignMessageWithAddrRequest
         */
        SignMessageWithAddrRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.SignMessageWithAddrRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.SignMessageWithAddrRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.SignMessageWithAddrRequest();
            if (object.msg != null)
                if (object.msg.length)
                    if (typeof object.msg === "string")
                        $util.base64.decode(object.msg, message.msg = $util.newBuffer($util.base64.length(object.msg)), 0);
                    else if (object.msg.length >= 0)
                        message.msg = object.msg;
            if (object.addr != null)
                if (typeof object.addr !== "string" || object.addr.length)
                    message.addr = $String(object.addr);
            return message;
        };

        /**
         * Creates a plain object from a SignMessageWithAddrRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @static
         * @param {walletrpc.SignMessageWithAddrRequest} message SignMessageWithAddrRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignMessageWithAddrRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.msg = "";
                else {
                    object.msg = [];
                    if (options.bytes !== $Array)
                        object.msg = $util.newBuffer(object.msg);
                }
                object.addr = "";
            }
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                object.msg = options.bytes === $String ? $util.base64.encode(message.msg, 0, message.msg.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.msg) : message.msg;
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                object.addr = message.addr;
            return object;
        };

        /**
         * Converts this SignMessageWithAddrRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignMessageWithAddrRequest.prototype.toJSON = function() {
            return SignMessageWithAddrRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignMessageWithAddrRequest
         * @function getTypeUrl
         * @memberof walletrpc.SignMessageWithAddrRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignMessageWithAddrRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.SignMessageWithAddrRequest";
        };

        return SignMessageWithAddrRequest;
    })();

    walletrpc.SignMessageWithAddrResponse = (function() {

        /**
         * Properties of a SignMessageWithAddrResponse.
         * @typedef {Object} walletrpc.SignMessageWithAddrResponse.$Properties
         * @property {string|null} [signature] SignMessageWithAddrResponse signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignMessageWithAddrResponse.
         * @memberof walletrpc
         * @interface ISignMessageWithAddrResponse
         * @augments walletrpc.SignMessageWithAddrResponse.$Properties
         * @deprecated Use walletrpc.SignMessageWithAddrResponse.$Properties instead.
         */

        /**
         * Shape of a SignMessageWithAddrResponse.
         * @typedef {walletrpc.SignMessageWithAddrResponse.$Properties} walletrpc.SignMessageWithAddrResponse.$Shape
         */

        /**
         * Constructs a new SignMessageWithAddrResponse.
         * @memberof walletrpc
         * @classdesc Represents a SignMessageWithAddrResponse.
         * @constructor
         * @param {walletrpc.SignMessageWithAddrResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignMessageWithAddrResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignMessageWithAddrResponse signature.
         * @member {string} signature
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @instance
         */
        SignMessageWithAddrResponse.prototype.signature = "";

        /**
         * Creates a new SignMessageWithAddrResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @static
         * @param {walletrpc.SignMessageWithAddrResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.SignMessageWithAddrResponse} SignMessageWithAddrResponse instance
         * @type {{
         *   (properties: walletrpc.SignMessageWithAddrResponse.$Shape): walletrpc.SignMessageWithAddrResponse & walletrpc.SignMessageWithAddrResponse.$Shape;
         *   (properties?: walletrpc.SignMessageWithAddrResponse.$Properties): walletrpc.SignMessageWithAddrResponse;
         * }}
         */
        SignMessageWithAddrResponse.create = function(properties) {
            return new SignMessageWithAddrResponse(properties);
        };

        /**
         * Encodes the specified SignMessageWithAddrResponse message. Does not implicitly {@link walletrpc.SignMessageWithAddrResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @static
         * @param {walletrpc.SignMessageWithAddrResponse.$Properties} message SignMessageWithAddrResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageWithAddrResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.signature);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignMessageWithAddrResponse message, length delimited. Does not implicitly {@link walletrpc.SignMessageWithAddrResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @static
         * @param {walletrpc.SignMessageWithAddrResponse.$Properties} message SignMessageWithAddrResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignMessageWithAddrResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignMessageWithAddrResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.SignMessageWithAddrResponse & walletrpc.SignMessageWithAddrResponse.$Shape} SignMessageWithAddrResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageWithAddrResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.SignMessageWithAddrResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignMessageWithAddrResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.SignMessageWithAddrResponse & walletrpc.SignMessageWithAddrResponse.$Shape} SignMessageWithAddrResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignMessageWithAddrResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignMessageWithAddrResponse message.
         * @function verify
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignMessageWithAddrResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                if (!$util.isString(message.signature))
                    return "signature: string expected";
            return null;
        };

        /**
         * Creates a SignMessageWithAddrResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.SignMessageWithAddrResponse} SignMessageWithAddrResponse
         */
        SignMessageWithAddrResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.SignMessageWithAddrResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.SignMessageWithAddrResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.SignMessageWithAddrResponse();
            if (object.signature != null)
                if (typeof object.signature !== "string" || object.signature.length)
                    message.signature = $String(object.signature);
            return message;
        };

        /**
         * Creates a plain object from a SignMessageWithAddrResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @static
         * @param {walletrpc.SignMessageWithAddrResponse} message SignMessageWithAddrResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignMessageWithAddrResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.signature = "";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                object.signature = message.signature;
            return object;
        };

        /**
         * Converts this SignMessageWithAddrResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignMessageWithAddrResponse.prototype.toJSON = function() {
            return SignMessageWithAddrResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignMessageWithAddrResponse
         * @function getTypeUrl
         * @memberof walletrpc.SignMessageWithAddrResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignMessageWithAddrResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.SignMessageWithAddrResponse";
        };

        return SignMessageWithAddrResponse;
    })();

    walletrpc.VerifyMessageWithAddrRequest = (function() {

        /**
         * Properties of a VerifyMessageWithAddrRequest.
         * @typedef {Object} walletrpc.VerifyMessageWithAddrRequest.$Properties
         * @property {Uint8Array|null} [msg] VerifyMessageWithAddrRequest msg
         * @property {string|null} [signature] VerifyMessageWithAddrRequest signature
         * @property {string|null} [addr] VerifyMessageWithAddrRequest addr
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a VerifyMessageWithAddrRequest.
         * @memberof walletrpc
         * @interface IVerifyMessageWithAddrRequest
         * @augments walletrpc.VerifyMessageWithAddrRequest.$Properties
         * @deprecated Use walletrpc.VerifyMessageWithAddrRequest.$Properties instead.
         */

        /**
         * Shape of a VerifyMessageWithAddrRequest.
         * @typedef {walletrpc.VerifyMessageWithAddrRequest.$Properties} walletrpc.VerifyMessageWithAddrRequest.$Shape
         */

        /**
         * Constructs a new VerifyMessageWithAddrRequest.
         * @memberof walletrpc
         * @classdesc Represents a VerifyMessageWithAddrRequest.
         * @constructor
         * @param {walletrpc.VerifyMessageWithAddrRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const VerifyMessageWithAddrRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * VerifyMessageWithAddrRequest msg.
         * @member {Uint8Array} msg
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @instance
         */
        VerifyMessageWithAddrRequest.prototype.msg = $util.newBuffer([]);

        /**
         * VerifyMessageWithAddrRequest signature.
         * @member {string} signature
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @instance
         */
        VerifyMessageWithAddrRequest.prototype.signature = "";

        /**
         * VerifyMessageWithAddrRequest addr.
         * @member {string} addr
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @instance
         */
        VerifyMessageWithAddrRequest.prototype.addr = "";

        /**
         * Creates a new VerifyMessageWithAddrRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @static
         * @param {walletrpc.VerifyMessageWithAddrRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.VerifyMessageWithAddrRequest} VerifyMessageWithAddrRequest instance
         * @type {{
         *   (properties: walletrpc.VerifyMessageWithAddrRequest.$Shape): walletrpc.VerifyMessageWithAddrRequest & walletrpc.VerifyMessageWithAddrRequest.$Shape;
         *   (properties?: walletrpc.VerifyMessageWithAddrRequest.$Properties): walletrpc.VerifyMessageWithAddrRequest;
         * }}
         */
        VerifyMessageWithAddrRequest.create = function(properties) {
            return new VerifyMessageWithAddrRequest(properties);
        };

        /**
         * Encodes the specified VerifyMessageWithAddrRequest message. Does not implicitly {@link walletrpc.VerifyMessageWithAddrRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @static
         * @param {walletrpc.VerifyMessageWithAddrRequest.$Properties} message VerifyMessageWithAddrRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageWithAddrRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg") && message.msg.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.msg);
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.signature);
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr") && message.addr !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.addr);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VerifyMessageWithAddrRequest message, length delimited. Does not implicitly {@link walletrpc.VerifyMessageWithAddrRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @static
         * @param {walletrpc.VerifyMessageWithAddrRequest.$Properties} message VerifyMessageWithAddrRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageWithAddrRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VerifyMessageWithAddrRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.VerifyMessageWithAddrRequest & walletrpc.VerifyMessageWithAddrRequest.$Shape} VerifyMessageWithAddrRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageWithAddrRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.VerifyMessageWithAddrRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.msg = value;
                        else
                            delete message.msg;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.addr = value;
                        else
                            delete message.addr;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a VerifyMessageWithAddrRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.VerifyMessageWithAddrRequest & walletrpc.VerifyMessageWithAddrRequest.$Shape} VerifyMessageWithAddrRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageWithAddrRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VerifyMessageWithAddrRequest message.
         * @function verify
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VerifyMessageWithAddrRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                if (!(message.msg && typeof message.msg.length === "number" || $util.isString(message.msg)))
                    return "msg: buffer expected";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                if (!$util.isString(message.signature))
                    return "signature: string expected";
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                if (!$util.isString(message.addr))
                    return "addr: string expected";
            return null;
        };

        /**
         * Creates a VerifyMessageWithAddrRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.VerifyMessageWithAddrRequest} VerifyMessageWithAddrRequest
         */
        VerifyMessageWithAddrRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.VerifyMessageWithAddrRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.VerifyMessageWithAddrRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.VerifyMessageWithAddrRequest();
            if (object.msg != null)
                if (object.msg.length)
                    if (typeof object.msg === "string")
                        $util.base64.decode(object.msg, message.msg = $util.newBuffer($util.base64.length(object.msg)), 0);
                    else if (object.msg.length >= 0)
                        message.msg = object.msg;
            if (object.signature != null)
                if (typeof object.signature !== "string" || object.signature.length)
                    message.signature = $String(object.signature);
            if (object.addr != null)
                if (typeof object.addr !== "string" || object.addr.length)
                    message.addr = $String(object.addr);
            return message;
        };

        /**
         * Creates a plain object from a VerifyMessageWithAddrRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @static
         * @param {walletrpc.VerifyMessageWithAddrRequest} message VerifyMessageWithAddrRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VerifyMessageWithAddrRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.msg = "";
                else {
                    object.msg = [];
                    if (options.bytes !== $Array)
                        object.msg = $util.newBuffer(object.msg);
                }
                object.signature = "";
                object.addr = "";
            }
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                object.msg = options.bytes === $String ? $util.base64.encode(message.msg, 0, message.msg.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.msg) : message.msg;
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                object.signature = message.signature;
            if (message.addr != null && $Object.hasOwnProperty.call(message, "addr"))
                object.addr = message.addr;
            return object;
        };

        /**
         * Converts this VerifyMessageWithAddrRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VerifyMessageWithAddrRequest.prototype.toJSON = function() {
            return VerifyMessageWithAddrRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for VerifyMessageWithAddrRequest
         * @function getTypeUrl
         * @memberof walletrpc.VerifyMessageWithAddrRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        VerifyMessageWithAddrRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.VerifyMessageWithAddrRequest";
        };

        return VerifyMessageWithAddrRequest;
    })();

    walletrpc.VerifyMessageWithAddrResponse = (function() {

        /**
         * Properties of a VerifyMessageWithAddrResponse.
         * @typedef {Object} walletrpc.VerifyMessageWithAddrResponse.$Properties
         * @property {boolean|null} [valid] VerifyMessageWithAddrResponse valid
         * @property {Uint8Array|null} [pubkey] VerifyMessageWithAddrResponse pubkey
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a VerifyMessageWithAddrResponse.
         * @memberof walletrpc
         * @interface IVerifyMessageWithAddrResponse
         * @augments walletrpc.VerifyMessageWithAddrResponse.$Properties
         * @deprecated Use walletrpc.VerifyMessageWithAddrResponse.$Properties instead.
         */

        /**
         * Shape of a VerifyMessageWithAddrResponse.
         * @typedef {walletrpc.VerifyMessageWithAddrResponse.$Properties} walletrpc.VerifyMessageWithAddrResponse.$Shape
         */

        /**
         * Constructs a new VerifyMessageWithAddrResponse.
         * @memberof walletrpc
         * @classdesc Represents a VerifyMessageWithAddrResponse.
         * @constructor
         * @param {walletrpc.VerifyMessageWithAddrResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const VerifyMessageWithAddrResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * VerifyMessageWithAddrResponse valid.
         * @member {boolean} valid
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @instance
         */
        VerifyMessageWithAddrResponse.prototype.valid = false;

        /**
         * VerifyMessageWithAddrResponse pubkey.
         * @member {Uint8Array} pubkey
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @instance
         */
        VerifyMessageWithAddrResponse.prototype.pubkey = $util.newBuffer([]);

        /**
         * Creates a new VerifyMessageWithAddrResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @static
         * @param {walletrpc.VerifyMessageWithAddrResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.VerifyMessageWithAddrResponse} VerifyMessageWithAddrResponse instance
         * @type {{
         *   (properties: walletrpc.VerifyMessageWithAddrResponse.$Shape): walletrpc.VerifyMessageWithAddrResponse & walletrpc.VerifyMessageWithAddrResponse.$Shape;
         *   (properties?: walletrpc.VerifyMessageWithAddrResponse.$Properties): walletrpc.VerifyMessageWithAddrResponse;
         * }}
         */
        VerifyMessageWithAddrResponse.create = function(properties) {
            return new VerifyMessageWithAddrResponse(properties);
        };

        /**
         * Encodes the specified VerifyMessageWithAddrResponse message. Does not implicitly {@link walletrpc.VerifyMessageWithAddrResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @static
         * @param {walletrpc.VerifyMessageWithAddrResponse.$Properties} message VerifyMessageWithAddrResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageWithAddrResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid") && message.valid !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.valid);
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey") && message.pubkey.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.pubkey);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VerifyMessageWithAddrResponse message, length delimited. Does not implicitly {@link walletrpc.VerifyMessageWithAddrResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @static
         * @param {walletrpc.VerifyMessageWithAddrResponse.$Properties} message VerifyMessageWithAddrResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifyMessageWithAddrResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VerifyMessageWithAddrResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.VerifyMessageWithAddrResponse & walletrpc.VerifyMessageWithAddrResponse.$Shape} VerifyMessageWithAddrResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageWithAddrResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.VerifyMessageWithAddrResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.valid = value;
                        else
                            delete message.valid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pubkey = value;
                        else
                            delete message.pubkey;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a VerifyMessageWithAddrResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.VerifyMessageWithAddrResponse & walletrpc.VerifyMessageWithAddrResponse.$Shape} VerifyMessageWithAddrResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifyMessageWithAddrResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VerifyMessageWithAddrResponse message.
         * @function verify
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VerifyMessageWithAddrResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid"))
                if (typeof message.valid !== "boolean")
                    return "valid: boolean expected";
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                if (!(message.pubkey && typeof message.pubkey.length === "number" || $util.isString(message.pubkey)))
                    return "pubkey: buffer expected";
            return null;
        };

        /**
         * Creates a VerifyMessageWithAddrResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.VerifyMessageWithAddrResponse} VerifyMessageWithAddrResponse
         */
        VerifyMessageWithAddrResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.VerifyMessageWithAddrResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.VerifyMessageWithAddrResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.VerifyMessageWithAddrResponse();
            if (object.valid != null)
                if (object.valid)
                    message.valid = $Boolean(object.valid);
            if (object.pubkey != null)
                if (object.pubkey.length)
                    if (typeof object.pubkey === "string")
                        $util.base64.decode(object.pubkey, message.pubkey = $util.newBuffer($util.base64.length(object.pubkey)), 0);
                    else if (object.pubkey.length >= 0)
                        message.pubkey = object.pubkey;
            return message;
        };

        /**
         * Creates a plain object from a VerifyMessageWithAddrResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @static
         * @param {walletrpc.VerifyMessageWithAddrResponse} message VerifyMessageWithAddrResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VerifyMessageWithAddrResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.valid = false;
                if (options.bytes === $String)
                    object.pubkey = "";
                else {
                    object.pubkey = [];
                    if (options.bytes !== $Array)
                        object.pubkey = $util.newBuffer(object.pubkey);
                }
            }
            if (message.valid != null && $Object.hasOwnProperty.call(message, "valid"))
                object.valid = message.valid;
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                object.pubkey = options.bytes === $String ? $util.base64.encode(message.pubkey, 0, message.pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pubkey) : message.pubkey;
            return object;
        };

        /**
         * Converts this VerifyMessageWithAddrResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VerifyMessageWithAddrResponse.prototype.toJSON = function() {
            return VerifyMessageWithAddrResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for VerifyMessageWithAddrResponse
         * @function getTypeUrl
         * @memberof walletrpc.VerifyMessageWithAddrResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        VerifyMessageWithAddrResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.VerifyMessageWithAddrResponse";
        };

        return VerifyMessageWithAddrResponse;
    })();

    walletrpc.ImportAccountRequest = (function() {

        /**
         * Properties of an ImportAccountRequest.
         * @typedef {Object} walletrpc.ImportAccountRequest.$Properties
         * @property {string|null} [name] ImportAccountRequest name
         * @property {string|null} [extended_public_key] ImportAccountRequest extended_public_key
         * @property {Uint8Array|null} [master_key_fingerprint] ImportAccountRequest master_key_fingerprint
         * @property {walletrpc.AddressType|null} [address_type] ImportAccountRequest address_type
         * @property {boolean|null} [dry_run] ImportAccountRequest dry_run
         * @property {number|null} [birthday_height] ImportAccountRequest birthday_height
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an ImportAccountRequest.
         * @memberof walletrpc
         * @interface IImportAccountRequest
         * @augments walletrpc.ImportAccountRequest.$Properties
         * @deprecated Use walletrpc.ImportAccountRequest.$Properties instead.
         */

        /**
         * Shape of an ImportAccountRequest.
         * @typedef {walletrpc.ImportAccountRequest.$Properties} walletrpc.ImportAccountRequest.$Shape
         */

        /**
         * Constructs a new ImportAccountRequest.
         * @memberof walletrpc
         * @classdesc Represents an ImportAccountRequest.
         * @constructor
         * @param {walletrpc.ImportAccountRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ImportAccountRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ImportAccountRequest name.
         * @member {string} name
         * @memberof walletrpc.ImportAccountRequest
         * @instance
         */
        ImportAccountRequest.prototype.name = "";

        /**
         * ImportAccountRequest extended_public_key.
         * @member {string} extended_public_key
         * @memberof walletrpc.ImportAccountRequest
         * @instance
         */
        ImportAccountRequest.prototype.extended_public_key = "";

        /**
         * ImportAccountRequest master_key_fingerprint.
         * @member {Uint8Array} master_key_fingerprint
         * @memberof walletrpc.ImportAccountRequest
         * @instance
         */
        ImportAccountRequest.prototype.master_key_fingerprint = $util.newBuffer([]);

        /**
         * ImportAccountRequest address_type.
         * @member {walletrpc.AddressType} address_type
         * @memberof walletrpc.ImportAccountRequest
         * @instance
         */
        ImportAccountRequest.prototype.address_type = 0;

        /**
         * ImportAccountRequest dry_run.
         * @member {boolean} dry_run
         * @memberof walletrpc.ImportAccountRequest
         * @instance
         */
        ImportAccountRequest.prototype.dry_run = false;

        /**
         * ImportAccountRequest birthday_height.
         * @member {number} birthday_height
         * @memberof walletrpc.ImportAccountRequest
         * @instance
         */
        ImportAccountRequest.prototype.birthday_height = 0;

        /**
         * Creates a new ImportAccountRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.ImportAccountRequest
         * @static
         * @param {walletrpc.ImportAccountRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ImportAccountRequest} ImportAccountRequest instance
         * @type {{
         *   (properties: walletrpc.ImportAccountRequest.$Shape): walletrpc.ImportAccountRequest & walletrpc.ImportAccountRequest.$Shape;
         *   (properties?: walletrpc.ImportAccountRequest.$Properties): walletrpc.ImportAccountRequest;
         * }}
         */
        ImportAccountRequest.create = function(properties) {
            return new ImportAccountRequest(properties);
        };

        /**
         * Encodes the specified ImportAccountRequest message. Does not implicitly {@link walletrpc.ImportAccountRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ImportAccountRequest
         * @static
         * @param {walletrpc.ImportAccountRequest.$Properties} message ImportAccountRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportAccountRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.extended_public_key != null && $Object.hasOwnProperty.call(message, "extended_public_key") && message.extended_public_key !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.extended_public_key);
            if (message.master_key_fingerprint != null && $Object.hasOwnProperty.call(message, "master_key_fingerprint") && message.master_key_fingerprint.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.master_key_fingerprint);
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type") && message.address_type !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.address_type);
            if (message.dry_run != null && $Object.hasOwnProperty.call(message, "dry_run") && message.dry_run !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.dry_run);
            if (message.birthday_height != null && $Object.hasOwnProperty.call(message, "birthday_height") && message.birthday_height !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.birthday_height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ImportAccountRequest message, length delimited. Does not implicitly {@link walletrpc.ImportAccountRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ImportAccountRequest
         * @static
         * @param {walletrpc.ImportAccountRequest.$Properties} message ImportAccountRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportAccountRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ImportAccountRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ImportAccountRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ImportAccountRequest & walletrpc.ImportAccountRequest.$Shape} ImportAccountRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportAccountRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ImportAccountRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.extended_public_key = value;
                        else
                            delete message.extended_public_key;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.master_key_fingerprint = value;
                        else
                            delete message.master_key_fingerprint;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.address_type = value;
                        else
                            delete message.address_type;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dry_run = value;
                        else
                            delete message.dry_run;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.birthday_height = value;
                        else
                            delete message.birthday_height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ImportAccountRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ImportAccountRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ImportAccountRequest & walletrpc.ImportAccountRequest.$Shape} ImportAccountRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportAccountRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ImportAccountRequest message.
         * @function verify
         * @memberof walletrpc.ImportAccountRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ImportAccountRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.extended_public_key != null && $Object.hasOwnProperty.call(message, "extended_public_key"))
                if (!$util.isString(message.extended_public_key))
                    return "extended_public_key: string expected";
            if (message.master_key_fingerprint != null && $Object.hasOwnProperty.call(message, "master_key_fingerprint"))
                if (!(message.master_key_fingerprint && typeof message.master_key_fingerprint.length === "number" || $util.isString(message.master_key_fingerprint)))
                    return "master_key_fingerprint: buffer expected";
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                if (typeof message.address_type !== "number" || (message.address_type | 0) !== message.address_type)
                    return "address_type: enum value expected";
            if (message.dry_run != null && $Object.hasOwnProperty.call(message, "dry_run"))
                if (typeof message.dry_run !== "boolean")
                    return "dry_run: boolean expected";
            if (message.birthday_height != null && $Object.hasOwnProperty.call(message, "birthday_height"))
                if (!$util.isInteger(message.birthday_height))
                    return "birthday_height: integer expected";
            return null;
        };

        /**
         * Creates an ImportAccountRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ImportAccountRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ImportAccountRequest} ImportAccountRequest
         */
        ImportAccountRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ImportAccountRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ImportAccountRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ImportAccountRequest();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.extended_public_key != null)
                if (typeof object.extended_public_key !== "string" || object.extended_public_key.length)
                    message.extended_public_key = $String(object.extended_public_key);
            if (object.master_key_fingerprint != null)
                if (object.master_key_fingerprint.length)
                    if (typeof object.master_key_fingerprint === "string")
                        $util.base64.decode(object.master_key_fingerprint, message.master_key_fingerprint = $util.newBuffer($util.base64.length(object.master_key_fingerprint)), 0);
                    else if (object.master_key_fingerprint.length >= 0)
                        message.master_key_fingerprint = object.master_key_fingerprint;
            if (object.address_type !== 0 && (typeof object.address_type !== "string" || $root.walletrpc.AddressType[object.address_type] !== 0))
                switch (object.address_type) {
                case "UNKNOWN":
                case 0:
                    message.address_type = 0;
                    break;
                case "WITNESS_PUBKEY_HASH":
                case 1:
                    message.address_type = 1;
                    break;
                case "NESTED_WITNESS_PUBKEY_HASH":
                case 2:
                    message.address_type = 2;
                    break;
                case "HYBRID_NESTED_WITNESS_PUBKEY_HASH":
                case 3:
                    message.address_type = 3;
                    break;
                case "TAPROOT_PUBKEY":
                case 4:
                    message.address_type = 4;
                    break;
                default:
                    if (typeof object.address_type === "number" && (object.address_type | 0) === object.address_type)
                        message.address_type = object.address_type;
                }
            if (object.dry_run != null)
                if (object.dry_run)
                    message.dry_run = $Boolean(object.dry_run);
            if (object.birthday_height != null)
                if ($Number(object.birthday_height) !== 0)
                    message.birthday_height = object.birthday_height >>> 0;
            return message;
        };

        /**
         * Creates a plain object from an ImportAccountRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ImportAccountRequest
         * @static
         * @param {walletrpc.ImportAccountRequest} message ImportAccountRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ImportAccountRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.extended_public_key = "";
                if (options.bytes === $String)
                    object.master_key_fingerprint = "";
                else {
                    object.master_key_fingerprint = [];
                    if (options.bytes !== $Array)
                        object.master_key_fingerprint = $util.newBuffer(object.master_key_fingerprint);
                }
                object.address_type = options.enums === $String ? "UNKNOWN" : 0;
                object.dry_run = false;
                object.birthday_height = 0;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.extended_public_key != null && $Object.hasOwnProperty.call(message, "extended_public_key"))
                object.extended_public_key = message.extended_public_key;
            if (message.master_key_fingerprint != null && $Object.hasOwnProperty.call(message, "master_key_fingerprint"))
                object.master_key_fingerprint = options.bytes === $String ? $util.base64.encode(message.master_key_fingerprint, 0, message.master_key_fingerprint.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.master_key_fingerprint) : message.master_key_fingerprint;
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                object.address_type = options.enums === $String ? $root.walletrpc.AddressType[message.address_type] === $undefined ? message.address_type : $root.walletrpc.AddressType[message.address_type] : message.address_type;
            if (message.dry_run != null && $Object.hasOwnProperty.call(message, "dry_run"))
                object.dry_run = message.dry_run;
            if (message.birthday_height != null && $Object.hasOwnProperty.call(message, "birthday_height"))
                object.birthday_height = message.birthday_height;
            return object;
        };

        /**
         * Converts this ImportAccountRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.ImportAccountRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ImportAccountRequest.prototype.toJSON = function() {
            return ImportAccountRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ImportAccountRequest
         * @function getTypeUrl
         * @memberof walletrpc.ImportAccountRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ImportAccountRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ImportAccountRequest";
        };

        return ImportAccountRequest;
    })();

    walletrpc.ImportAccountResponse = (function() {

        /**
         * Properties of an ImportAccountResponse.
         * @typedef {Object} walletrpc.ImportAccountResponse.$Properties
         * @property {walletrpc.Account.$Properties|null} [account] ImportAccountResponse account
         * @property {Array.<string>|null} [dry_run_external_addrs] ImportAccountResponse dry_run_external_addrs
         * @property {Array.<string>|null} [dry_run_internal_addrs] ImportAccountResponse dry_run_internal_addrs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an ImportAccountResponse.
         * @memberof walletrpc
         * @interface IImportAccountResponse
         * @augments walletrpc.ImportAccountResponse.$Properties
         * @deprecated Use walletrpc.ImportAccountResponse.$Properties instead.
         */

        /**
         * Shape of an ImportAccountResponse.
         * @typedef {walletrpc.ImportAccountResponse.$Properties} walletrpc.ImportAccountResponse.$Shape
         */

        /**
         * Constructs a new ImportAccountResponse.
         * @memberof walletrpc
         * @classdesc Represents an ImportAccountResponse.
         * @constructor
         * @param {walletrpc.ImportAccountResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ImportAccountResponse = function (properties) {
            this.dry_run_external_addrs = [];
            this.dry_run_internal_addrs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ImportAccountResponse account.
         * @member {walletrpc.Account.$Properties|null|undefined} account
         * @memberof walletrpc.ImportAccountResponse
         * @instance
         */
        ImportAccountResponse.prototype.account = null;

        /**
         * ImportAccountResponse dry_run_external_addrs.
         * @member {Array.<string>} dry_run_external_addrs
         * @memberof walletrpc.ImportAccountResponse
         * @instance
         */
        ImportAccountResponse.prototype.dry_run_external_addrs = $util.emptyArray;

        /**
         * ImportAccountResponse dry_run_internal_addrs.
         * @member {Array.<string>} dry_run_internal_addrs
         * @memberof walletrpc.ImportAccountResponse
         * @instance
         */
        ImportAccountResponse.prototype.dry_run_internal_addrs = $util.emptyArray;

        /**
         * Creates a new ImportAccountResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.ImportAccountResponse
         * @static
         * @param {walletrpc.ImportAccountResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ImportAccountResponse} ImportAccountResponse instance
         * @type {{
         *   (properties: walletrpc.ImportAccountResponse.$Shape): walletrpc.ImportAccountResponse & walletrpc.ImportAccountResponse.$Shape;
         *   (properties?: walletrpc.ImportAccountResponse.$Properties): walletrpc.ImportAccountResponse;
         * }}
         */
        ImportAccountResponse.create = function(properties) {
            return new ImportAccountResponse(properties);
        };

        /**
         * Encodes the specified ImportAccountResponse message. Does not implicitly {@link walletrpc.ImportAccountResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ImportAccountResponse
         * @static
         * @param {walletrpc.ImportAccountResponse.$Properties} message ImportAccountResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportAccountResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                $root.walletrpc.Account.encode(message.account, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.dry_run_external_addrs != null && message.dry_run_external_addrs.length)
                for (let i = 0; i < message.dry_run_external_addrs.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dry_run_external_addrs[i]);
            if (message.dry_run_internal_addrs != null && message.dry_run_internal_addrs.length)
                for (let i = 0; i < message.dry_run_internal_addrs.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.dry_run_internal_addrs[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ImportAccountResponse message, length delimited. Does not implicitly {@link walletrpc.ImportAccountResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ImportAccountResponse
         * @static
         * @param {walletrpc.ImportAccountResponse.$Properties} message ImportAccountResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportAccountResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ImportAccountResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ImportAccountResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ImportAccountResponse & walletrpc.ImportAccountResponse.$Shape} ImportAccountResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportAccountResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ImportAccountResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.walletrpc.Account.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.dry_run_external_addrs && message.dry_run_external_addrs.length))
                            message.dry_run_external_addrs = [];
                        message.dry_run_external_addrs.push(reader.stringVerify());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.dry_run_internal_addrs && message.dry_run_internal_addrs.length))
                            message.dry_run_internal_addrs = [];
                        message.dry_run_internal_addrs.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ImportAccountResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ImportAccountResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ImportAccountResponse & walletrpc.ImportAccountResponse.$Shape} ImportAccountResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportAccountResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ImportAccountResponse message.
         * @function verify
         * @memberof walletrpc.ImportAccountResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ImportAccountResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
                let error = $root.walletrpc.Account.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            if (message.dry_run_external_addrs != null && $Object.hasOwnProperty.call(message, "dry_run_external_addrs")) {
                if (!$Array.isArray(message.dry_run_external_addrs))
                    return "dry_run_external_addrs: array expected";
                for (let i = 0; i < message.dry_run_external_addrs.length; ++i)
                    if (!$util.isString(message.dry_run_external_addrs[i]))
                        return "dry_run_external_addrs: string[] expected";
            }
            if (message.dry_run_internal_addrs != null && $Object.hasOwnProperty.call(message, "dry_run_internal_addrs")) {
                if (!$Array.isArray(message.dry_run_internal_addrs))
                    return "dry_run_internal_addrs: array expected";
                for (let i = 0; i < message.dry_run_internal_addrs.length; ++i)
                    if (!$util.isString(message.dry_run_internal_addrs[i]))
                        return "dry_run_internal_addrs: string[] expected";
            }
            return null;
        };

        /**
         * Creates an ImportAccountResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ImportAccountResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ImportAccountResponse} ImportAccountResponse
         */
        ImportAccountResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ImportAccountResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ImportAccountResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ImportAccountResponse();
            if (object.account != null) {
                if (!$util.isObject(object.account))
                    throw $TypeError(".walletrpc.ImportAccountResponse.account: object expected");
                message.account = $root.walletrpc.Account.fromObject(object.account, _depth + 1);
            }
            if (object.dry_run_external_addrs) {
                if (!$Array.isArray(object.dry_run_external_addrs))
                    throw $TypeError(".walletrpc.ImportAccountResponse.dry_run_external_addrs: array expected");
                message.dry_run_external_addrs = $Array(object.dry_run_external_addrs.length);
                for (let i = 0; i < object.dry_run_external_addrs.length; ++i)
                    message.dry_run_external_addrs[i] = $String(object.dry_run_external_addrs[i]);
            }
            if (object.dry_run_internal_addrs) {
                if (!$Array.isArray(object.dry_run_internal_addrs))
                    throw $TypeError(".walletrpc.ImportAccountResponse.dry_run_internal_addrs: array expected");
                message.dry_run_internal_addrs = $Array(object.dry_run_internal_addrs.length);
                for (let i = 0; i < object.dry_run_internal_addrs.length; ++i)
                    message.dry_run_internal_addrs[i] = $String(object.dry_run_internal_addrs[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from an ImportAccountResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ImportAccountResponse
         * @static
         * @param {walletrpc.ImportAccountResponse} message ImportAccountResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ImportAccountResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.dry_run_external_addrs = [];
                object.dry_run_internal_addrs = [];
            }
            if (options.defaults)
                object.account = null;
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = $root.walletrpc.Account.toObject(message.account, options, _depth + 1);
            if (message.dry_run_external_addrs && message.dry_run_external_addrs.length) {
                object.dry_run_external_addrs = $Array(message.dry_run_external_addrs.length);
                for (let j = 0; j < message.dry_run_external_addrs.length; ++j)
                    object.dry_run_external_addrs[j] = message.dry_run_external_addrs[j];
            }
            if (message.dry_run_internal_addrs && message.dry_run_internal_addrs.length) {
                object.dry_run_internal_addrs = $Array(message.dry_run_internal_addrs.length);
                for (let j = 0; j < message.dry_run_internal_addrs.length; ++j)
                    object.dry_run_internal_addrs[j] = message.dry_run_internal_addrs[j];
            }
            return object;
        };

        /**
         * Converts this ImportAccountResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.ImportAccountResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ImportAccountResponse.prototype.toJSON = function() {
            return ImportAccountResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ImportAccountResponse
         * @function getTypeUrl
         * @memberof walletrpc.ImportAccountResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ImportAccountResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ImportAccountResponse";
        };

        return ImportAccountResponse;
    })();

    walletrpc.RescanRequest = (function() {

        /**
         * Properties of a RescanRequest.
         * @typedef {Object} walletrpc.RescanRequest.$Properties
         * @property {number|null} [start_height] RescanRequest start_height
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RescanRequest.
         * @memberof walletrpc
         * @interface IRescanRequest
         * @augments walletrpc.RescanRequest.$Properties
         * @deprecated Use walletrpc.RescanRequest.$Properties instead.
         */

        /**
         * Shape of a RescanRequest.
         * @typedef {walletrpc.RescanRequest.$Properties} walletrpc.RescanRequest.$Shape
         */

        /**
         * Constructs a new RescanRequest.
         * @memberof walletrpc
         * @classdesc Represents a RescanRequest.
         * @constructor
         * @param {walletrpc.RescanRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RescanRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RescanRequest start_height.
         * @member {number} start_height
         * @memberof walletrpc.RescanRequest
         * @instance
         */
        RescanRequest.prototype.start_height = 0;

        /**
         * Creates a new RescanRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.RescanRequest
         * @static
         * @param {walletrpc.RescanRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.RescanRequest} RescanRequest instance
         * @type {{
         *   (properties: walletrpc.RescanRequest.$Shape): walletrpc.RescanRequest & walletrpc.RescanRequest.$Shape;
         *   (properties?: walletrpc.RescanRequest.$Properties): walletrpc.RescanRequest;
         * }}
         */
        RescanRequest.create = function(properties) {
            return new RescanRequest(properties);
        };

        /**
         * Encodes the specified RescanRequest message. Does not implicitly {@link walletrpc.RescanRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.RescanRequest
         * @static
         * @param {walletrpc.RescanRequest.$Properties} message RescanRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RescanRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.start_height != null && $Object.hasOwnProperty.call(message, "start_height") && message.start_height !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.start_height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RescanRequest message, length delimited. Does not implicitly {@link walletrpc.RescanRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.RescanRequest
         * @static
         * @param {walletrpc.RescanRequest.$Properties} message RescanRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RescanRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RescanRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.RescanRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.RescanRequest & walletrpc.RescanRequest.$Shape} RescanRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RescanRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.RescanRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.start_height = value;
                        else
                            delete message.start_height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RescanRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.RescanRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.RescanRequest & walletrpc.RescanRequest.$Shape} RescanRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RescanRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RescanRequest message.
         * @function verify
         * @memberof walletrpc.RescanRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RescanRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.start_height != null && $Object.hasOwnProperty.call(message, "start_height"))
                if (!$util.isInteger(message.start_height))
                    return "start_height: integer expected";
            return null;
        };

        /**
         * Creates a RescanRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.RescanRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.RescanRequest} RescanRequest
         */
        RescanRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.RescanRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.RescanRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.RescanRequest();
            if (object.start_height != null)
                if ($Number(object.start_height) !== 0)
                    message.start_height = object.start_height | 0;
            return message;
        };

        /**
         * Creates a plain object from a RescanRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.RescanRequest
         * @static
         * @param {walletrpc.RescanRequest} message RescanRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RescanRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.start_height = 0;
            if (message.start_height != null && $Object.hasOwnProperty.call(message, "start_height"))
                object.start_height = message.start_height;
            return object;
        };

        /**
         * Converts this RescanRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.RescanRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RescanRequest.prototype.toJSON = function() {
            return RescanRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RescanRequest
         * @function getTypeUrl
         * @memberof walletrpc.RescanRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RescanRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.RescanRequest";
        };

        return RescanRequest;
    })();

    walletrpc.RescanResponse = (function() {

        /**
         * Properties of a RescanResponse.
         * @typedef {Object} walletrpc.RescanResponse.$Properties
         * @property {string|null} [status] RescanResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RescanResponse.
         * @memberof walletrpc
         * @interface IRescanResponse
         * @augments walletrpc.RescanResponse.$Properties
         * @deprecated Use walletrpc.RescanResponse.$Properties instead.
         */

        /**
         * Shape of a RescanResponse.
         * @typedef {walletrpc.RescanResponse.$Properties} walletrpc.RescanResponse.$Shape
         */

        /**
         * Constructs a new RescanResponse.
         * @memberof walletrpc
         * @classdesc Represents a RescanResponse.
         * @constructor
         * @param {walletrpc.RescanResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RescanResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RescanResponse status.
         * @member {string} status
         * @memberof walletrpc.RescanResponse
         * @instance
         */
        RescanResponse.prototype.status = "";

        /**
         * Creates a new RescanResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.RescanResponse
         * @static
         * @param {walletrpc.RescanResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.RescanResponse} RescanResponse instance
         * @type {{
         *   (properties: walletrpc.RescanResponse.$Shape): walletrpc.RescanResponse & walletrpc.RescanResponse.$Shape;
         *   (properties?: walletrpc.RescanResponse.$Properties): walletrpc.RescanResponse;
         * }}
         */
        RescanResponse.create = function(properties) {
            return new RescanResponse(properties);
        };

        /**
         * Encodes the specified RescanResponse message. Does not implicitly {@link walletrpc.RescanResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.RescanResponse
         * @static
         * @param {walletrpc.RescanResponse.$Properties} message RescanResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RescanResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RescanResponse message, length delimited. Does not implicitly {@link walletrpc.RescanResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.RescanResponse
         * @static
         * @param {walletrpc.RescanResponse.$Properties} message RescanResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RescanResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RescanResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.RescanResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.RescanResponse & walletrpc.RescanResponse.$Shape} RescanResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RescanResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.RescanResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RescanResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.RescanResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.RescanResponse & walletrpc.RescanResponse.$Shape} RescanResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RescanResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RescanResponse message.
         * @function verify
         * @memberof walletrpc.RescanResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RescanResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a RescanResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.RescanResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.RescanResponse} RescanResponse
         */
        RescanResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.RescanResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.RescanResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.RescanResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a RescanResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.RescanResponse
         * @static
         * @param {walletrpc.RescanResponse} message RescanResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RescanResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this RescanResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.RescanResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RescanResponse.prototype.toJSON = function() {
            return RescanResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RescanResponse
         * @function getTypeUrl
         * @memberof walletrpc.RescanResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RescanResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.RescanResponse";
        };

        return RescanResponse;
    })();

    walletrpc.ImportPublicKeyRequest = (function() {

        /**
         * Properties of an ImportPublicKeyRequest.
         * @typedef {Object} walletrpc.ImportPublicKeyRequest.$Properties
         * @property {Uint8Array|null} [public_key] ImportPublicKeyRequest public_key
         * @property {walletrpc.AddressType|null} [address_type] ImportPublicKeyRequest address_type
         * @property {boolean|null} [rescan] ImportPublicKeyRequest rescan
         * @property {number|null} [birthday_height] ImportPublicKeyRequest birthday_height
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an ImportPublicKeyRequest.
         * @memberof walletrpc
         * @interface IImportPublicKeyRequest
         * @augments walletrpc.ImportPublicKeyRequest.$Properties
         * @deprecated Use walletrpc.ImportPublicKeyRequest.$Properties instead.
         */

        /**
         * Shape of an ImportPublicKeyRequest.
         * @typedef {walletrpc.ImportPublicKeyRequest.$Properties} walletrpc.ImportPublicKeyRequest.$Shape
         */

        /**
         * Constructs a new ImportPublicKeyRequest.
         * @memberof walletrpc
         * @classdesc Represents an ImportPublicKeyRequest.
         * @constructor
         * @param {walletrpc.ImportPublicKeyRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ImportPublicKeyRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ImportPublicKeyRequest public_key.
         * @member {Uint8Array} public_key
         * @memberof walletrpc.ImportPublicKeyRequest
         * @instance
         */
        ImportPublicKeyRequest.prototype.public_key = $util.newBuffer([]);

        /**
         * ImportPublicKeyRequest address_type.
         * @member {walletrpc.AddressType} address_type
         * @memberof walletrpc.ImportPublicKeyRequest
         * @instance
         */
        ImportPublicKeyRequest.prototype.address_type = 0;

        /**
         * ImportPublicKeyRequest rescan.
         * @member {boolean} rescan
         * @memberof walletrpc.ImportPublicKeyRequest
         * @instance
         */
        ImportPublicKeyRequest.prototype.rescan = false;

        /**
         * ImportPublicKeyRequest birthday_height.
         * @member {number} birthday_height
         * @memberof walletrpc.ImportPublicKeyRequest
         * @instance
         */
        ImportPublicKeyRequest.prototype.birthday_height = 0;

        /**
         * Creates a new ImportPublicKeyRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.ImportPublicKeyRequest
         * @static
         * @param {walletrpc.ImportPublicKeyRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ImportPublicKeyRequest} ImportPublicKeyRequest instance
         * @type {{
         *   (properties: walletrpc.ImportPublicKeyRequest.$Shape): walletrpc.ImportPublicKeyRequest & walletrpc.ImportPublicKeyRequest.$Shape;
         *   (properties?: walletrpc.ImportPublicKeyRequest.$Properties): walletrpc.ImportPublicKeyRequest;
         * }}
         */
        ImportPublicKeyRequest.create = function(properties) {
            return new ImportPublicKeyRequest(properties);
        };

        /**
         * Encodes the specified ImportPublicKeyRequest message. Does not implicitly {@link walletrpc.ImportPublicKeyRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ImportPublicKeyRequest
         * @static
         * @param {walletrpc.ImportPublicKeyRequest.$Properties} message ImportPublicKeyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportPublicKeyRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.public_key != null && $Object.hasOwnProperty.call(message, "public_key") && message.public_key.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.public_key);
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type") && message.address_type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.address_type);
            if (message.rescan != null && $Object.hasOwnProperty.call(message, "rescan") && message.rescan !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.rescan);
            if (message.birthday_height != null && $Object.hasOwnProperty.call(message, "birthday_height") && message.birthday_height !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.birthday_height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ImportPublicKeyRequest message, length delimited. Does not implicitly {@link walletrpc.ImportPublicKeyRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ImportPublicKeyRequest
         * @static
         * @param {walletrpc.ImportPublicKeyRequest.$Properties} message ImportPublicKeyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportPublicKeyRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ImportPublicKeyRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ImportPublicKeyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ImportPublicKeyRequest & walletrpc.ImportPublicKeyRequest.$Shape} ImportPublicKeyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportPublicKeyRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ImportPublicKeyRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.public_key = value;
                        else
                            delete message.public_key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.address_type = value;
                        else
                            delete message.address_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.rescan = value;
                        else
                            delete message.rescan;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.birthday_height = value;
                        else
                            delete message.birthday_height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ImportPublicKeyRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ImportPublicKeyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ImportPublicKeyRequest & walletrpc.ImportPublicKeyRequest.$Shape} ImportPublicKeyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportPublicKeyRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ImportPublicKeyRequest message.
         * @function verify
         * @memberof walletrpc.ImportPublicKeyRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ImportPublicKeyRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.public_key != null && $Object.hasOwnProperty.call(message, "public_key"))
                if (!(message.public_key && typeof message.public_key.length === "number" || $util.isString(message.public_key)))
                    return "public_key: buffer expected";
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                if (typeof message.address_type !== "number" || (message.address_type | 0) !== message.address_type)
                    return "address_type: enum value expected";
            if (message.rescan != null && $Object.hasOwnProperty.call(message, "rescan"))
                if (typeof message.rescan !== "boolean")
                    return "rescan: boolean expected";
            if (message.birthday_height != null && $Object.hasOwnProperty.call(message, "birthday_height"))
                if (!$util.isInteger(message.birthday_height))
                    return "birthday_height: integer expected";
            return null;
        };

        /**
         * Creates an ImportPublicKeyRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ImportPublicKeyRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ImportPublicKeyRequest} ImportPublicKeyRequest
         */
        ImportPublicKeyRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ImportPublicKeyRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ImportPublicKeyRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ImportPublicKeyRequest();
            if (object.public_key != null)
                if (object.public_key.length)
                    if (typeof object.public_key === "string")
                        $util.base64.decode(object.public_key, message.public_key = $util.newBuffer($util.base64.length(object.public_key)), 0);
                    else if (object.public_key.length >= 0)
                        message.public_key = object.public_key;
            if (object.address_type !== 0 && (typeof object.address_type !== "string" || $root.walletrpc.AddressType[object.address_type] !== 0))
                switch (object.address_type) {
                case "UNKNOWN":
                case 0:
                    message.address_type = 0;
                    break;
                case "WITNESS_PUBKEY_HASH":
                case 1:
                    message.address_type = 1;
                    break;
                case "NESTED_WITNESS_PUBKEY_HASH":
                case 2:
                    message.address_type = 2;
                    break;
                case "HYBRID_NESTED_WITNESS_PUBKEY_HASH":
                case 3:
                    message.address_type = 3;
                    break;
                case "TAPROOT_PUBKEY":
                case 4:
                    message.address_type = 4;
                    break;
                default:
                    if (typeof object.address_type === "number" && (object.address_type | 0) === object.address_type)
                        message.address_type = object.address_type;
                }
            if (object.rescan != null)
                if (object.rescan)
                    message.rescan = $Boolean(object.rescan);
            if (object.birthday_height != null)
                if ($Number(object.birthday_height) !== 0)
                    message.birthday_height = object.birthday_height | 0;
            return message;
        };

        /**
         * Creates a plain object from an ImportPublicKeyRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ImportPublicKeyRequest
         * @static
         * @param {walletrpc.ImportPublicKeyRequest} message ImportPublicKeyRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ImportPublicKeyRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.public_key = "";
                else {
                    object.public_key = [];
                    if (options.bytes !== $Array)
                        object.public_key = $util.newBuffer(object.public_key);
                }
                object.address_type = options.enums === $String ? "UNKNOWN" : 0;
                object.rescan = false;
                object.birthday_height = 0;
            }
            if (message.public_key != null && $Object.hasOwnProperty.call(message, "public_key"))
                object.public_key = options.bytes === $String ? $util.base64.encode(message.public_key, 0, message.public_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.public_key) : message.public_key;
            if (message.address_type != null && $Object.hasOwnProperty.call(message, "address_type"))
                object.address_type = options.enums === $String ? $root.walletrpc.AddressType[message.address_type] === $undefined ? message.address_type : $root.walletrpc.AddressType[message.address_type] : message.address_type;
            if (message.rescan != null && $Object.hasOwnProperty.call(message, "rescan"))
                object.rescan = message.rescan;
            if (message.birthday_height != null && $Object.hasOwnProperty.call(message, "birthday_height"))
                object.birthday_height = message.birthday_height;
            return object;
        };

        /**
         * Converts this ImportPublicKeyRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.ImportPublicKeyRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ImportPublicKeyRequest.prototype.toJSON = function() {
            return ImportPublicKeyRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ImportPublicKeyRequest
         * @function getTypeUrl
         * @memberof walletrpc.ImportPublicKeyRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ImportPublicKeyRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ImportPublicKeyRequest";
        };

        return ImportPublicKeyRequest;
    })();

    walletrpc.ImportPublicKeyResponse = (function() {

        /**
         * Properties of an ImportPublicKeyResponse.
         * @typedef {Object} walletrpc.ImportPublicKeyResponse.$Properties
         * @property {string|null} [status] ImportPublicKeyResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an ImportPublicKeyResponse.
         * @memberof walletrpc
         * @interface IImportPublicKeyResponse
         * @augments walletrpc.ImportPublicKeyResponse.$Properties
         * @deprecated Use walletrpc.ImportPublicKeyResponse.$Properties instead.
         */

        /**
         * Shape of an ImportPublicKeyResponse.
         * @typedef {walletrpc.ImportPublicKeyResponse.$Properties} walletrpc.ImportPublicKeyResponse.$Shape
         */

        /**
         * Constructs a new ImportPublicKeyResponse.
         * @memberof walletrpc
         * @classdesc Represents an ImportPublicKeyResponse.
         * @constructor
         * @param {walletrpc.ImportPublicKeyResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ImportPublicKeyResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ImportPublicKeyResponse status.
         * @member {string} status
         * @memberof walletrpc.ImportPublicKeyResponse
         * @instance
         */
        ImportPublicKeyResponse.prototype.status = "";

        /**
         * Creates a new ImportPublicKeyResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.ImportPublicKeyResponse
         * @static
         * @param {walletrpc.ImportPublicKeyResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ImportPublicKeyResponse} ImportPublicKeyResponse instance
         * @type {{
         *   (properties: walletrpc.ImportPublicKeyResponse.$Shape): walletrpc.ImportPublicKeyResponse & walletrpc.ImportPublicKeyResponse.$Shape;
         *   (properties?: walletrpc.ImportPublicKeyResponse.$Properties): walletrpc.ImportPublicKeyResponse;
         * }}
         */
        ImportPublicKeyResponse.create = function(properties) {
            return new ImportPublicKeyResponse(properties);
        };

        /**
         * Encodes the specified ImportPublicKeyResponse message. Does not implicitly {@link walletrpc.ImportPublicKeyResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ImportPublicKeyResponse
         * @static
         * @param {walletrpc.ImportPublicKeyResponse.$Properties} message ImportPublicKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportPublicKeyResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ImportPublicKeyResponse message, length delimited. Does not implicitly {@link walletrpc.ImportPublicKeyResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ImportPublicKeyResponse
         * @static
         * @param {walletrpc.ImportPublicKeyResponse.$Properties} message ImportPublicKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportPublicKeyResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ImportPublicKeyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ImportPublicKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ImportPublicKeyResponse & walletrpc.ImportPublicKeyResponse.$Shape} ImportPublicKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportPublicKeyResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ImportPublicKeyResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ImportPublicKeyResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ImportPublicKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ImportPublicKeyResponse & walletrpc.ImportPublicKeyResponse.$Shape} ImportPublicKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportPublicKeyResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ImportPublicKeyResponse message.
         * @function verify
         * @memberof walletrpc.ImportPublicKeyResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ImportPublicKeyResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates an ImportPublicKeyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ImportPublicKeyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ImportPublicKeyResponse} ImportPublicKeyResponse
         */
        ImportPublicKeyResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ImportPublicKeyResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ImportPublicKeyResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ImportPublicKeyResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from an ImportPublicKeyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ImportPublicKeyResponse
         * @static
         * @param {walletrpc.ImportPublicKeyResponse} message ImportPublicKeyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ImportPublicKeyResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this ImportPublicKeyResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.ImportPublicKeyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ImportPublicKeyResponse.prototype.toJSON = function() {
            return ImportPublicKeyResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ImportPublicKeyResponse
         * @function getTypeUrl
         * @memberof walletrpc.ImportPublicKeyResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ImportPublicKeyResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ImportPublicKeyResponse";
        };

        return ImportPublicKeyResponse;
    })();

    walletrpc.ImportTapscriptRequest = (function() {

        /**
         * Properties of an ImportTapscriptRequest.
         * @typedef {Object} walletrpc.ImportTapscriptRequest.$Properties
         * @property {Uint8Array|null} [internal_public_key] ImportTapscriptRequest internal_public_key
         * @property {walletrpc.TapscriptFullTree.$Properties|null} [full_tree] ImportTapscriptRequest full_tree
         * @property {walletrpc.TapscriptPartialReveal.$Properties|null} [partial_reveal] ImportTapscriptRequest partial_reveal
         * @property {Uint8Array|null} [root_hash_only] ImportTapscriptRequest root_hash_only
         * @property {boolean|null} [full_key_only] ImportTapscriptRequest full_key_only
         * @property {"full_tree"|"partial_reveal"|"root_hash_only"|"full_key_only"} [script] ImportTapscriptRequest script
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an ImportTapscriptRequest.
         * @memberof walletrpc
         * @interface IImportTapscriptRequest
         * @augments walletrpc.ImportTapscriptRequest.$Properties
         * @deprecated Use walletrpc.ImportTapscriptRequest.$Properties instead.
         */

        /**
         * Narrowed shape of an ImportTapscriptRequest.
         * @typedef {{
         *   internal_public_key?: Uint8Array|null;
         *   full_tree?: walletrpc.TapscriptFullTree.$Shape|null;
         *   partial_reveal?: walletrpc.TapscriptPartialReveal.$Shape|null;
         *   root_hash_only?: Uint8Array|null;
         *   full_key_only?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ script?: undefined; full_tree?: null; partial_reveal?: null; root_hash_only?: null; full_key_only?: null }|{ script?: "full_tree"; full_tree: walletrpc.TapscriptFullTree.$Shape; partial_reveal?: null; root_hash_only?: null; full_key_only?: null }|{ script?: "partial_reveal"; full_tree?: null; partial_reveal: walletrpc.TapscriptPartialReveal.$Shape; root_hash_only?: null; full_key_only?: null }|{ script?: "root_hash_only"; full_tree?: null; partial_reveal?: null; root_hash_only: Uint8Array; full_key_only?: null }|{ script?: "full_key_only"; full_tree?: null; partial_reveal?: null; root_hash_only?: null; full_key_only: boolean })
         * )} walletrpc.ImportTapscriptRequest.$Shape
         */

        /**
         * Constructs a new ImportTapscriptRequest.
         * @memberof walletrpc
         * @classdesc Represents an ImportTapscriptRequest.
         * @constructor
         * @param {walletrpc.ImportTapscriptRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ImportTapscriptRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ImportTapscriptRequest internal_public_key.
         * @member {Uint8Array} internal_public_key
         * @memberof walletrpc.ImportTapscriptRequest
         * @instance
         */
        ImportTapscriptRequest.prototype.internal_public_key = $util.newBuffer([]);

        /**
         * ImportTapscriptRequest full_tree.
         * @member {walletrpc.TapscriptFullTree.$Properties|null|undefined} full_tree
         * @memberof walletrpc.ImportTapscriptRequest
         * @instance
         */
        ImportTapscriptRequest.prototype.full_tree = null;

        /**
         * ImportTapscriptRequest partial_reveal.
         * @member {walletrpc.TapscriptPartialReveal.$Properties|null|undefined} partial_reveal
         * @memberof walletrpc.ImportTapscriptRequest
         * @instance
         */
        ImportTapscriptRequest.prototype.partial_reveal = null;

        /**
         * ImportTapscriptRequest root_hash_only.
         * @member {Uint8Array|null|undefined} root_hash_only
         * @memberof walletrpc.ImportTapscriptRequest
         * @instance
         */
        ImportTapscriptRequest.prototype.root_hash_only = null;

        /**
         * ImportTapscriptRequest full_key_only.
         * @member {boolean|null|undefined} full_key_only
         * @memberof walletrpc.ImportTapscriptRequest
         * @instance
         */
        ImportTapscriptRequest.prototype.full_key_only = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ImportTapscriptRequest script.
         * @member {"full_tree"|"partial_reveal"|"root_hash_only"|"full_key_only"|undefined} script
         * @memberof walletrpc.ImportTapscriptRequest
         * @instance
         */
        $Object.defineProperty(ImportTapscriptRequest.prototype, "script", {
            get: $util.oneOfGetter($oneOfFields = ["full_tree", "partial_reveal", "root_hash_only", "full_key_only"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ImportTapscriptRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.ImportTapscriptRequest
         * @static
         * @param {walletrpc.ImportTapscriptRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ImportTapscriptRequest} ImportTapscriptRequest instance
         * @type {{
         *   (properties: walletrpc.ImportTapscriptRequest.$Shape): walletrpc.ImportTapscriptRequest & walletrpc.ImportTapscriptRequest.$Shape;
         *   (properties?: walletrpc.ImportTapscriptRequest.$Properties): walletrpc.ImportTapscriptRequest;
         * }}
         */
        ImportTapscriptRequest.create = function(properties) {
            return new ImportTapscriptRequest(properties);
        };

        /**
         * Encodes the specified ImportTapscriptRequest message. Does not implicitly {@link walletrpc.ImportTapscriptRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ImportTapscriptRequest
         * @static
         * @param {walletrpc.ImportTapscriptRequest.$Properties} message ImportTapscriptRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportTapscriptRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.internal_public_key != null && $Object.hasOwnProperty.call(message, "internal_public_key") && message.internal_public_key.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.internal_public_key);
            if (message.full_tree != null && $Object.hasOwnProperty.call(message, "full_tree"))
                $root.walletrpc.TapscriptFullTree.encode(message.full_tree, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.partial_reveal != null && $Object.hasOwnProperty.call(message, "partial_reveal"))
                $root.walletrpc.TapscriptPartialReveal.encode(message.partial_reveal, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.root_hash_only != null && $Object.hasOwnProperty.call(message, "root_hash_only"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.root_hash_only);
            if (message.full_key_only != null && $Object.hasOwnProperty.call(message, "full_key_only"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.full_key_only);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ImportTapscriptRequest message, length delimited. Does not implicitly {@link walletrpc.ImportTapscriptRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ImportTapscriptRequest
         * @static
         * @param {walletrpc.ImportTapscriptRequest.$Properties} message ImportTapscriptRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportTapscriptRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ImportTapscriptRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ImportTapscriptRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ImportTapscriptRequest & walletrpc.ImportTapscriptRequest.$Shape} ImportTapscriptRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportTapscriptRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ImportTapscriptRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.internal_public_key = value;
                        else
                            delete message.internal_public_key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.full_tree = $root.walletrpc.TapscriptFullTree.decode(reader, reader.uint32(), $undefined, _depth + 1, message.full_tree);
                        message.script = "full_tree";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.partial_reveal = $root.walletrpc.TapscriptPartialReveal.decode(reader, reader.uint32(), $undefined, _depth + 1, message.partial_reveal);
                        message.script = "partial_reveal";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.root_hash_only = reader.bytes();
                        message.script = "root_hash_only";
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        message.full_key_only = reader.bool();
                        message.script = "full_key_only";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ImportTapscriptRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ImportTapscriptRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ImportTapscriptRequest & walletrpc.ImportTapscriptRequest.$Shape} ImportTapscriptRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportTapscriptRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ImportTapscriptRequest message.
         * @function verify
         * @memberof walletrpc.ImportTapscriptRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ImportTapscriptRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.internal_public_key != null && $Object.hasOwnProperty.call(message, "internal_public_key"))
                if (!(message.internal_public_key && typeof message.internal_public_key.length === "number" || $util.isString(message.internal_public_key)))
                    return "internal_public_key: buffer expected";
            if (message.full_tree != null && $Object.hasOwnProperty.call(message, "full_tree")) {
                properties.script = 1;
                {
                    let error = $root.walletrpc.TapscriptFullTree.verify(message.full_tree, _depth + 1);
                    if (error)
                        return "full_tree." + error;
                }
            }
            if (message.partial_reveal != null && $Object.hasOwnProperty.call(message, "partial_reveal")) {
                if (properties.script === 1)
                    return "script: multiple values";
                properties.script = 1;
                {
                    let error = $root.walletrpc.TapscriptPartialReveal.verify(message.partial_reveal, _depth + 1);
                    if (error)
                        return "partial_reveal." + error;
                }
            }
            if (message.root_hash_only != null && $Object.hasOwnProperty.call(message, "root_hash_only")) {
                if (properties.script === 1)
                    return "script: multiple values";
                properties.script = 1;
                if (!(message.root_hash_only && typeof message.root_hash_only.length === "number" || $util.isString(message.root_hash_only)))
                    return "root_hash_only: buffer expected";
            }
            if (message.full_key_only != null && $Object.hasOwnProperty.call(message, "full_key_only")) {
                if (properties.script === 1)
                    return "script: multiple values";
                properties.script = 1;
                if (typeof message.full_key_only !== "boolean")
                    return "full_key_only: boolean expected";
            }
            return null;
        };

        /**
         * Creates an ImportTapscriptRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ImportTapscriptRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ImportTapscriptRequest} ImportTapscriptRequest
         */
        ImportTapscriptRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ImportTapscriptRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ImportTapscriptRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ImportTapscriptRequest();
            if (object.internal_public_key != null)
                if (object.internal_public_key.length)
                    if (typeof object.internal_public_key === "string")
                        $util.base64.decode(object.internal_public_key, message.internal_public_key = $util.newBuffer($util.base64.length(object.internal_public_key)), 0);
                    else if (object.internal_public_key.length >= 0)
                        message.internal_public_key = object.internal_public_key;
            if (object.full_tree != null) {
                if (!$util.isObject(object.full_tree))
                    throw $TypeError(".walletrpc.ImportTapscriptRequest.full_tree: object expected");
                message.full_tree = $root.walletrpc.TapscriptFullTree.fromObject(object.full_tree, _depth + 1);
            }
            if (object.partial_reveal != null) {
                if (!$util.isObject(object.partial_reveal))
                    throw $TypeError(".walletrpc.ImportTapscriptRequest.partial_reveal: object expected");
                message.partial_reveal = $root.walletrpc.TapscriptPartialReveal.fromObject(object.partial_reveal, _depth + 1);
            }
            if (object.root_hash_only != null)
                if (typeof object.root_hash_only === "string")
                    $util.base64.decode(object.root_hash_only, message.root_hash_only = $util.newBuffer($util.base64.length(object.root_hash_only)), 0);
                else if (object.root_hash_only.length >= 0)
                    message.root_hash_only = object.root_hash_only;
            if (object.full_key_only != null)
                message.full_key_only = $Boolean(object.full_key_only);
            return message;
        };

        /**
         * Creates a plain object from an ImportTapscriptRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ImportTapscriptRequest
         * @static
         * @param {walletrpc.ImportTapscriptRequest} message ImportTapscriptRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ImportTapscriptRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.internal_public_key = "";
                else {
                    object.internal_public_key = [];
                    if (options.bytes !== $Array)
                        object.internal_public_key = $util.newBuffer(object.internal_public_key);
                }
            if (message.internal_public_key != null && $Object.hasOwnProperty.call(message, "internal_public_key"))
                object.internal_public_key = options.bytes === $String ? $util.base64.encode(message.internal_public_key, 0, message.internal_public_key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.internal_public_key) : message.internal_public_key;
            if (message.full_tree != null && $Object.hasOwnProperty.call(message, "full_tree")) {
                object.full_tree = $root.walletrpc.TapscriptFullTree.toObject(message.full_tree, options, _depth + 1);
                if (options.oneofs)
                    object.script = "full_tree";
            }
            if (message.partial_reveal != null && $Object.hasOwnProperty.call(message, "partial_reveal")) {
                object.partial_reveal = $root.walletrpc.TapscriptPartialReveal.toObject(message.partial_reveal, options, _depth + 1);
                if (options.oneofs)
                    object.script = "partial_reveal";
            }
            if (message.root_hash_only != null && $Object.hasOwnProperty.call(message, "root_hash_only")) {
                object.root_hash_only = options.bytes === $String ? $util.base64.encode(message.root_hash_only, 0, message.root_hash_only.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.root_hash_only) : message.root_hash_only;
                if (options.oneofs)
                    object.script = "root_hash_only";
            }
            if (message.full_key_only != null && $Object.hasOwnProperty.call(message, "full_key_only")) {
                object.full_key_only = message.full_key_only;
                if (options.oneofs)
                    object.script = "full_key_only";
            }
            return object;
        };

        /**
         * Converts this ImportTapscriptRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.ImportTapscriptRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ImportTapscriptRequest.prototype.toJSON = function() {
            return ImportTapscriptRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ImportTapscriptRequest
         * @function getTypeUrl
         * @memberof walletrpc.ImportTapscriptRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ImportTapscriptRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ImportTapscriptRequest";
        };

        return ImportTapscriptRequest;
    })();

    walletrpc.TapscriptFullTree = (function() {

        /**
         * Properties of a TapscriptFullTree.
         * @typedef {Object} walletrpc.TapscriptFullTree.$Properties
         * @property {Array.<walletrpc.TapLeaf.$Properties>|null} [all_leaves] TapscriptFullTree all_leaves
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TapscriptFullTree.
         * @memberof walletrpc
         * @interface ITapscriptFullTree
         * @augments walletrpc.TapscriptFullTree.$Properties
         * @deprecated Use walletrpc.TapscriptFullTree.$Properties instead.
         */

        /**
         * Shape of a TapscriptFullTree.
         * @typedef {walletrpc.TapscriptFullTree.$Properties} walletrpc.TapscriptFullTree.$Shape
         */

        /**
         * Constructs a new TapscriptFullTree.
         * @memberof walletrpc
         * @classdesc Represents a TapscriptFullTree.
         * @constructor
         * @param {walletrpc.TapscriptFullTree.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TapscriptFullTree = function (properties) {
            this.all_leaves = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TapscriptFullTree all_leaves.
         * @member {Array.<walletrpc.TapLeaf.$Properties>} all_leaves
         * @memberof walletrpc.TapscriptFullTree
         * @instance
         */
        TapscriptFullTree.prototype.all_leaves = $util.emptyArray;

        /**
         * Creates a new TapscriptFullTree instance using the specified properties.
         * @function create
         * @memberof walletrpc.TapscriptFullTree
         * @static
         * @param {walletrpc.TapscriptFullTree.$Properties=} [properties] Properties to set
         * @returns {walletrpc.TapscriptFullTree} TapscriptFullTree instance
         * @type {{
         *   (properties: walletrpc.TapscriptFullTree.$Shape): walletrpc.TapscriptFullTree & walletrpc.TapscriptFullTree.$Shape;
         *   (properties?: walletrpc.TapscriptFullTree.$Properties): walletrpc.TapscriptFullTree;
         * }}
         */
        TapscriptFullTree.create = function(properties) {
            return new TapscriptFullTree(properties);
        };

        /**
         * Encodes the specified TapscriptFullTree message. Does not implicitly {@link walletrpc.TapscriptFullTree.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.TapscriptFullTree
         * @static
         * @param {walletrpc.TapscriptFullTree.$Properties} message TapscriptFullTree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TapscriptFullTree.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.all_leaves != null && message.all_leaves.length)
                for (let i = 0; i < message.all_leaves.length; ++i)
                    $root.walletrpc.TapLeaf.encode(message.all_leaves[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TapscriptFullTree message, length delimited. Does not implicitly {@link walletrpc.TapscriptFullTree.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.TapscriptFullTree
         * @static
         * @param {walletrpc.TapscriptFullTree.$Properties} message TapscriptFullTree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TapscriptFullTree.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TapscriptFullTree message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.TapscriptFullTree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.TapscriptFullTree & walletrpc.TapscriptFullTree.$Shape} TapscriptFullTree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TapscriptFullTree.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.TapscriptFullTree();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.all_leaves && message.all_leaves.length))
                            message.all_leaves = [];
                        message.all_leaves.push($root.walletrpc.TapLeaf.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TapscriptFullTree message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.TapscriptFullTree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.TapscriptFullTree & walletrpc.TapscriptFullTree.$Shape} TapscriptFullTree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TapscriptFullTree.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TapscriptFullTree message.
         * @function verify
         * @memberof walletrpc.TapscriptFullTree
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TapscriptFullTree.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.all_leaves != null && $Object.hasOwnProperty.call(message, "all_leaves")) {
                if (!$Array.isArray(message.all_leaves))
                    return "all_leaves: array expected";
                for (let i = 0; i < message.all_leaves.length; ++i) {
                    let error = $root.walletrpc.TapLeaf.verify(message.all_leaves[i], _depth + 1);
                    if (error)
                        return "all_leaves." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TapscriptFullTree message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.TapscriptFullTree
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.TapscriptFullTree} TapscriptFullTree
         */
        TapscriptFullTree.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.TapscriptFullTree)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.TapscriptFullTree: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.TapscriptFullTree();
            if (object.all_leaves) {
                if (!$Array.isArray(object.all_leaves))
                    throw $TypeError(".walletrpc.TapscriptFullTree.all_leaves: array expected");
                message.all_leaves = $Array(object.all_leaves.length);
                for (let i = 0; i < object.all_leaves.length; ++i) {
                    if (!$util.isObject(object.all_leaves[i]))
                        throw $TypeError(".walletrpc.TapscriptFullTree.all_leaves: object expected");
                    message.all_leaves[i] = $root.walletrpc.TapLeaf.fromObject(object.all_leaves[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TapscriptFullTree message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.TapscriptFullTree
         * @static
         * @param {walletrpc.TapscriptFullTree} message TapscriptFullTree
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TapscriptFullTree.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.all_leaves = [];
            if (message.all_leaves && message.all_leaves.length) {
                object.all_leaves = $Array(message.all_leaves.length);
                for (let j = 0; j < message.all_leaves.length; ++j)
                    object.all_leaves[j] = $root.walletrpc.TapLeaf.toObject(message.all_leaves[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TapscriptFullTree to JSON.
         * @function toJSON
         * @memberof walletrpc.TapscriptFullTree
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TapscriptFullTree.prototype.toJSON = function() {
            return TapscriptFullTree.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TapscriptFullTree
         * @function getTypeUrl
         * @memberof walletrpc.TapscriptFullTree
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TapscriptFullTree.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.TapscriptFullTree";
        };

        return TapscriptFullTree;
    })();

    walletrpc.TapLeaf = (function() {

        /**
         * Properties of a TapLeaf.
         * @typedef {Object} walletrpc.TapLeaf.$Properties
         * @property {number|null} [leaf_version] TapLeaf leaf_version
         * @property {Uint8Array|null} [script] TapLeaf script
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TapLeaf.
         * @memberof walletrpc
         * @interface ITapLeaf
         * @augments walletrpc.TapLeaf.$Properties
         * @deprecated Use walletrpc.TapLeaf.$Properties instead.
         */

        /**
         * Shape of a TapLeaf.
         * @typedef {walletrpc.TapLeaf.$Properties} walletrpc.TapLeaf.$Shape
         */

        /**
         * Constructs a new TapLeaf.
         * @memberof walletrpc
         * @classdesc Represents a TapLeaf.
         * @constructor
         * @param {walletrpc.TapLeaf.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TapLeaf = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TapLeaf leaf_version.
         * @member {number} leaf_version
         * @memberof walletrpc.TapLeaf
         * @instance
         */
        TapLeaf.prototype.leaf_version = 0;

        /**
         * TapLeaf script.
         * @member {Uint8Array} script
         * @memberof walletrpc.TapLeaf
         * @instance
         */
        TapLeaf.prototype.script = $util.newBuffer([]);

        /**
         * Creates a new TapLeaf instance using the specified properties.
         * @function create
         * @memberof walletrpc.TapLeaf
         * @static
         * @param {walletrpc.TapLeaf.$Properties=} [properties] Properties to set
         * @returns {walletrpc.TapLeaf} TapLeaf instance
         * @type {{
         *   (properties: walletrpc.TapLeaf.$Shape): walletrpc.TapLeaf & walletrpc.TapLeaf.$Shape;
         *   (properties?: walletrpc.TapLeaf.$Properties): walletrpc.TapLeaf;
         * }}
         */
        TapLeaf.create = function(properties) {
            return new TapLeaf(properties);
        };

        /**
         * Encodes the specified TapLeaf message. Does not implicitly {@link walletrpc.TapLeaf.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.TapLeaf
         * @static
         * @param {walletrpc.TapLeaf.$Properties} message TapLeaf message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TapLeaf.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.leaf_version != null && $Object.hasOwnProperty.call(message, "leaf_version") && message.leaf_version !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.leaf_version);
            if (message.script != null && $Object.hasOwnProperty.call(message, "script") && message.script.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.script);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TapLeaf message, length delimited. Does not implicitly {@link walletrpc.TapLeaf.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.TapLeaf
         * @static
         * @param {walletrpc.TapLeaf.$Properties} message TapLeaf message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TapLeaf.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TapLeaf message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.TapLeaf
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.TapLeaf & walletrpc.TapLeaf.$Shape} TapLeaf
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TapLeaf.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.TapLeaf(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.leaf_version = value;
                        else
                            delete message.leaf_version;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.script = value;
                        else
                            delete message.script;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TapLeaf message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.TapLeaf
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.TapLeaf & walletrpc.TapLeaf.$Shape} TapLeaf
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TapLeaf.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TapLeaf message.
         * @function verify
         * @memberof walletrpc.TapLeaf
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TapLeaf.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.leaf_version != null && $Object.hasOwnProperty.call(message, "leaf_version"))
                if (!$util.isInteger(message.leaf_version))
                    return "leaf_version: integer expected";
            if (message.script != null && $Object.hasOwnProperty.call(message, "script"))
                if (!(message.script && typeof message.script.length === "number" || $util.isString(message.script)))
                    return "script: buffer expected";
            return null;
        };

        /**
         * Creates a TapLeaf message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.TapLeaf
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.TapLeaf} TapLeaf
         */
        TapLeaf.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.TapLeaf)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.TapLeaf: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.TapLeaf();
            if (object.leaf_version != null)
                if ($Number(object.leaf_version) !== 0)
                    message.leaf_version = object.leaf_version >>> 0;
            if (object.script != null)
                if (object.script.length)
                    if (typeof object.script === "string")
                        $util.base64.decode(object.script, message.script = $util.newBuffer($util.base64.length(object.script)), 0);
                    else if (object.script.length >= 0)
                        message.script = object.script;
            return message;
        };

        /**
         * Creates a plain object from a TapLeaf message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.TapLeaf
         * @static
         * @param {walletrpc.TapLeaf} message TapLeaf
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TapLeaf.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.leaf_version = 0;
                if (options.bytes === $String)
                    object.script = "";
                else {
                    object.script = [];
                    if (options.bytes !== $Array)
                        object.script = $util.newBuffer(object.script);
                }
            }
            if (message.leaf_version != null && $Object.hasOwnProperty.call(message, "leaf_version"))
                object.leaf_version = message.leaf_version;
            if (message.script != null && $Object.hasOwnProperty.call(message, "script"))
                object.script = options.bytes === $String ? $util.base64.encode(message.script, 0, message.script.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.script) : message.script;
            return object;
        };

        /**
         * Converts this TapLeaf to JSON.
         * @function toJSON
         * @memberof walletrpc.TapLeaf
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TapLeaf.prototype.toJSON = function() {
            return TapLeaf.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TapLeaf
         * @function getTypeUrl
         * @memberof walletrpc.TapLeaf
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TapLeaf.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.TapLeaf";
        };

        return TapLeaf;
    })();

    walletrpc.TapscriptPartialReveal = (function() {

        /**
         * Properties of a TapscriptPartialReveal.
         * @typedef {Object} walletrpc.TapscriptPartialReveal.$Properties
         * @property {walletrpc.TapLeaf.$Properties|null} [revealed_leaf] TapscriptPartialReveal revealed_leaf
         * @property {Uint8Array|null} [full_inclusion_proof] TapscriptPartialReveal full_inclusion_proof
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TapscriptPartialReveal.
         * @memberof walletrpc
         * @interface ITapscriptPartialReveal
         * @augments walletrpc.TapscriptPartialReveal.$Properties
         * @deprecated Use walletrpc.TapscriptPartialReveal.$Properties instead.
         */

        /**
         * Shape of a TapscriptPartialReveal.
         * @typedef {walletrpc.TapscriptPartialReveal.$Properties} walletrpc.TapscriptPartialReveal.$Shape
         */

        /**
         * Constructs a new TapscriptPartialReveal.
         * @memberof walletrpc
         * @classdesc Represents a TapscriptPartialReveal.
         * @constructor
         * @param {walletrpc.TapscriptPartialReveal.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TapscriptPartialReveal = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TapscriptPartialReveal revealed_leaf.
         * @member {walletrpc.TapLeaf.$Properties|null|undefined} revealed_leaf
         * @memberof walletrpc.TapscriptPartialReveal
         * @instance
         */
        TapscriptPartialReveal.prototype.revealed_leaf = null;

        /**
         * TapscriptPartialReveal full_inclusion_proof.
         * @member {Uint8Array} full_inclusion_proof
         * @memberof walletrpc.TapscriptPartialReveal
         * @instance
         */
        TapscriptPartialReveal.prototype.full_inclusion_proof = $util.newBuffer([]);

        /**
         * Creates a new TapscriptPartialReveal instance using the specified properties.
         * @function create
         * @memberof walletrpc.TapscriptPartialReveal
         * @static
         * @param {walletrpc.TapscriptPartialReveal.$Properties=} [properties] Properties to set
         * @returns {walletrpc.TapscriptPartialReveal} TapscriptPartialReveal instance
         * @type {{
         *   (properties: walletrpc.TapscriptPartialReveal.$Shape): walletrpc.TapscriptPartialReveal & walletrpc.TapscriptPartialReveal.$Shape;
         *   (properties?: walletrpc.TapscriptPartialReveal.$Properties): walletrpc.TapscriptPartialReveal;
         * }}
         */
        TapscriptPartialReveal.create = function(properties) {
            return new TapscriptPartialReveal(properties);
        };

        /**
         * Encodes the specified TapscriptPartialReveal message. Does not implicitly {@link walletrpc.TapscriptPartialReveal.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.TapscriptPartialReveal
         * @static
         * @param {walletrpc.TapscriptPartialReveal.$Properties} message TapscriptPartialReveal message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TapscriptPartialReveal.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.revealed_leaf != null && $Object.hasOwnProperty.call(message, "revealed_leaf"))
                $root.walletrpc.TapLeaf.encode(message.revealed_leaf, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.full_inclusion_proof != null && $Object.hasOwnProperty.call(message, "full_inclusion_proof") && message.full_inclusion_proof.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.full_inclusion_proof);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TapscriptPartialReveal message, length delimited. Does not implicitly {@link walletrpc.TapscriptPartialReveal.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.TapscriptPartialReveal
         * @static
         * @param {walletrpc.TapscriptPartialReveal.$Properties} message TapscriptPartialReveal message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TapscriptPartialReveal.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TapscriptPartialReveal message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.TapscriptPartialReveal
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.TapscriptPartialReveal & walletrpc.TapscriptPartialReveal.$Shape} TapscriptPartialReveal
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TapscriptPartialReveal.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.TapscriptPartialReveal(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.revealed_leaf = $root.walletrpc.TapLeaf.decode(reader, reader.uint32(), $undefined, _depth + 1, message.revealed_leaf);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.full_inclusion_proof = value;
                        else
                            delete message.full_inclusion_proof;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TapscriptPartialReveal message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.TapscriptPartialReveal
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.TapscriptPartialReveal & walletrpc.TapscriptPartialReveal.$Shape} TapscriptPartialReveal
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TapscriptPartialReveal.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TapscriptPartialReveal message.
         * @function verify
         * @memberof walletrpc.TapscriptPartialReveal
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TapscriptPartialReveal.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.revealed_leaf != null && $Object.hasOwnProperty.call(message, "revealed_leaf")) {
                let error = $root.walletrpc.TapLeaf.verify(message.revealed_leaf, _depth + 1);
                if (error)
                    return "revealed_leaf." + error;
            }
            if (message.full_inclusion_proof != null && $Object.hasOwnProperty.call(message, "full_inclusion_proof"))
                if (!(message.full_inclusion_proof && typeof message.full_inclusion_proof.length === "number" || $util.isString(message.full_inclusion_proof)))
                    return "full_inclusion_proof: buffer expected";
            return null;
        };

        /**
         * Creates a TapscriptPartialReveal message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.TapscriptPartialReveal
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.TapscriptPartialReveal} TapscriptPartialReveal
         */
        TapscriptPartialReveal.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.TapscriptPartialReveal)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.TapscriptPartialReveal: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.TapscriptPartialReveal();
            if (object.revealed_leaf != null) {
                if (!$util.isObject(object.revealed_leaf))
                    throw $TypeError(".walletrpc.TapscriptPartialReveal.revealed_leaf: object expected");
                message.revealed_leaf = $root.walletrpc.TapLeaf.fromObject(object.revealed_leaf, _depth + 1);
            }
            if (object.full_inclusion_proof != null)
                if (object.full_inclusion_proof.length)
                    if (typeof object.full_inclusion_proof === "string")
                        $util.base64.decode(object.full_inclusion_proof, message.full_inclusion_proof = $util.newBuffer($util.base64.length(object.full_inclusion_proof)), 0);
                    else if (object.full_inclusion_proof.length >= 0)
                        message.full_inclusion_proof = object.full_inclusion_proof;
            return message;
        };

        /**
         * Creates a plain object from a TapscriptPartialReveal message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.TapscriptPartialReveal
         * @static
         * @param {walletrpc.TapscriptPartialReveal} message TapscriptPartialReveal
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TapscriptPartialReveal.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.revealed_leaf = null;
                if (options.bytes === $String)
                    object.full_inclusion_proof = "";
                else {
                    object.full_inclusion_proof = [];
                    if (options.bytes !== $Array)
                        object.full_inclusion_proof = $util.newBuffer(object.full_inclusion_proof);
                }
            }
            if (message.revealed_leaf != null && $Object.hasOwnProperty.call(message, "revealed_leaf"))
                object.revealed_leaf = $root.walletrpc.TapLeaf.toObject(message.revealed_leaf, options, _depth + 1);
            if (message.full_inclusion_proof != null && $Object.hasOwnProperty.call(message, "full_inclusion_proof"))
                object.full_inclusion_proof = options.bytes === $String ? $util.base64.encode(message.full_inclusion_proof, 0, message.full_inclusion_proof.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.full_inclusion_proof) : message.full_inclusion_proof;
            return object;
        };

        /**
         * Converts this TapscriptPartialReveal to JSON.
         * @function toJSON
         * @memberof walletrpc.TapscriptPartialReveal
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TapscriptPartialReveal.prototype.toJSON = function() {
            return TapscriptPartialReveal.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TapscriptPartialReveal
         * @function getTypeUrl
         * @memberof walletrpc.TapscriptPartialReveal
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TapscriptPartialReveal.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.TapscriptPartialReveal";
        };

        return TapscriptPartialReveal;
    })();

    walletrpc.ImportTapscriptResponse = (function() {

        /**
         * Properties of an ImportTapscriptResponse.
         * @typedef {Object} walletrpc.ImportTapscriptResponse.$Properties
         * @property {string|null} [p2tr_address] ImportTapscriptResponse p2tr_address
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an ImportTapscriptResponse.
         * @memberof walletrpc
         * @interface IImportTapscriptResponse
         * @augments walletrpc.ImportTapscriptResponse.$Properties
         * @deprecated Use walletrpc.ImportTapscriptResponse.$Properties instead.
         */

        /**
         * Shape of an ImportTapscriptResponse.
         * @typedef {walletrpc.ImportTapscriptResponse.$Properties} walletrpc.ImportTapscriptResponse.$Shape
         */

        /**
         * Constructs a new ImportTapscriptResponse.
         * @memberof walletrpc
         * @classdesc Represents an ImportTapscriptResponse.
         * @constructor
         * @param {walletrpc.ImportTapscriptResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ImportTapscriptResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ImportTapscriptResponse p2tr_address.
         * @member {string} p2tr_address
         * @memberof walletrpc.ImportTapscriptResponse
         * @instance
         */
        ImportTapscriptResponse.prototype.p2tr_address = "";

        /**
         * Creates a new ImportTapscriptResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.ImportTapscriptResponse
         * @static
         * @param {walletrpc.ImportTapscriptResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ImportTapscriptResponse} ImportTapscriptResponse instance
         * @type {{
         *   (properties: walletrpc.ImportTapscriptResponse.$Shape): walletrpc.ImportTapscriptResponse & walletrpc.ImportTapscriptResponse.$Shape;
         *   (properties?: walletrpc.ImportTapscriptResponse.$Properties): walletrpc.ImportTapscriptResponse;
         * }}
         */
        ImportTapscriptResponse.create = function(properties) {
            return new ImportTapscriptResponse(properties);
        };

        /**
         * Encodes the specified ImportTapscriptResponse message. Does not implicitly {@link walletrpc.ImportTapscriptResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ImportTapscriptResponse
         * @static
         * @param {walletrpc.ImportTapscriptResponse.$Properties} message ImportTapscriptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportTapscriptResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.p2tr_address != null && $Object.hasOwnProperty.call(message, "p2tr_address") && message.p2tr_address !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.p2tr_address);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ImportTapscriptResponse message, length delimited. Does not implicitly {@link walletrpc.ImportTapscriptResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ImportTapscriptResponse
         * @static
         * @param {walletrpc.ImportTapscriptResponse.$Properties} message ImportTapscriptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImportTapscriptResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ImportTapscriptResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ImportTapscriptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ImportTapscriptResponse & walletrpc.ImportTapscriptResponse.$Shape} ImportTapscriptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportTapscriptResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ImportTapscriptResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.p2tr_address = value;
                        else
                            delete message.p2tr_address;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ImportTapscriptResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ImportTapscriptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ImportTapscriptResponse & walletrpc.ImportTapscriptResponse.$Shape} ImportTapscriptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImportTapscriptResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ImportTapscriptResponse message.
         * @function verify
         * @memberof walletrpc.ImportTapscriptResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ImportTapscriptResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.p2tr_address != null && $Object.hasOwnProperty.call(message, "p2tr_address"))
                if (!$util.isString(message.p2tr_address))
                    return "p2tr_address: string expected";
            return null;
        };

        /**
         * Creates an ImportTapscriptResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ImportTapscriptResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ImportTapscriptResponse} ImportTapscriptResponse
         */
        ImportTapscriptResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ImportTapscriptResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ImportTapscriptResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ImportTapscriptResponse();
            if (object.p2tr_address != null)
                if (typeof object.p2tr_address !== "string" || object.p2tr_address.length)
                    message.p2tr_address = $String(object.p2tr_address);
            return message;
        };

        /**
         * Creates a plain object from an ImportTapscriptResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ImportTapscriptResponse
         * @static
         * @param {walletrpc.ImportTapscriptResponse} message ImportTapscriptResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ImportTapscriptResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.p2tr_address = "";
            if (message.p2tr_address != null && $Object.hasOwnProperty.call(message, "p2tr_address"))
                object.p2tr_address = message.p2tr_address;
            return object;
        };

        /**
         * Converts this ImportTapscriptResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.ImportTapscriptResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ImportTapscriptResponse.prototype.toJSON = function() {
            return ImportTapscriptResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ImportTapscriptResponse
         * @function getTypeUrl
         * @memberof walletrpc.ImportTapscriptResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ImportTapscriptResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ImportTapscriptResponse";
        };

        return ImportTapscriptResponse;
    })();

    walletrpc.Transaction = (function() {

        /**
         * Properties of a Transaction.
         * @typedef {Object} walletrpc.Transaction.$Properties
         * @property {Uint8Array|null} [tx_hex] Transaction tx_hex
         * @property {string|null} [label] Transaction label
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Transaction.
         * @memberof walletrpc
         * @interface ITransaction
         * @augments walletrpc.Transaction.$Properties
         * @deprecated Use walletrpc.Transaction.$Properties instead.
         */

        /**
         * Shape of a Transaction.
         * @typedef {walletrpc.Transaction.$Properties} walletrpc.Transaction.$Shape
         */

        /**
         * Constructs a new Transaction.
         * @memberof walletrpc
         * @classdesc Represents a Transaction.
         * @constructor
         * @param {walletrpc.Transaction.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Transaction = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Transaction tx_hex.
         * @member {Uint8Array} tx_hex
         * @memberof walletrpc.Transaction
         * @instance
         */
        Transaction.prototype.tx_hex = $util.newBuffer([]);

        /**
         * Transaction label.
         * @member {string} label
         * @memberof walletrpc.Transaction
         * @instance
         */
        Transaction.prototype.label = "";

        /**
         * Creates a new Transaction instance using the specified properties.
         * @function create
         * @memberof walletrpc.Transaction
         * @static
         * @param {walletrpc.Transaction.$Properties=} [properties] Properties to set
         * @returns {walletrpc.Transaction} Transaction instance
         * @type {{
         *   (properties: walletrpc.Transaction.$Shape): walletrpc.Transaction & walletrpc.Transaction.$Shape;
         *   (properties?: walletrpc.Transaction.$Properties): walletrpc.Transaction;
         * }}
         */
        Transaction.create = function(properties) {
            return new Transaction(properties);
        };

        /**
         * Encodes the specified Transaction message. Does not implicitly {@link walletrpc.Transaction.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.Transaction
         * @static
         * @param {walletrpc.Transaction.$Properties} message Transaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Transaction.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tx_hex != null && $Object.hasOwnProperty.call(message, "tx_hex") && message.tx_hex.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.tx_hex);
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.label);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Transaction message, length delimited. Does not implicitly {@link walletrpc.Transaction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.Transaction
         * @static
         * @param {walletrpc.Transaction.$Properties} message Transaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Transaction.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Transaction message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.Transaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.Transaction & walletrpc.Transaction.$Shape} Transaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Transaction.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.Transaction(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.tx_hex = value;
                        else
                            delete message.tx_hex;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Transaction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.Transaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.Transaction & walletrpc.Transaction.$Shape} Transaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Transaction.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Transaction message.
         * @function verify
         * @memberof walletrpc.Transaction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Transaction.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tx_hex != null && $Object.hasOwnProperty.call(message, "tx_hex"))
                if (!(message.tx_hex && typeof message.tx_hex.length === "number" || $util.isString(message.tx_hex)))
                    return "tx_hex: buffer expected";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            return null;
        };

        /**
         * Creates a Transaction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.Transaction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.Transaction} Transaction
         */
        Transaction.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.Transaction)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.Transaction: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.Transaction();
            if (object.tx_hex != null)
                if (object.tx_hex.length)
                    if (typeof object.tx_hex === "string")
                        $util.base64.decode(object.tx_hex, message.tx_hex = $util.newBuffer($util.base64.length(object.tx_hex)), 0);
                    else if (object.tx_hex.length >= 0)
                        message.tx_hex = object.tx_hex;
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            return message;
        };

        /**
         * Creates a plain object from a Transaction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.Transaction
         * @static
         * @param {walletrpc.Transaction} message Transaction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Transaction.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.tx_hex = "";
                else {
                    object.tx_hex = [];
                    if (options.bytes !== $Array)
                        object.tx_hex = $util.newBuffer(object.tx_hex);
                }
                object.label = "";
            }
            if (message.tx_hex != null && $Object.hasOwnProperty.call(message, "tx_hex"))
                object.tx_hex = options.bytes === $String ? $util.base64.encode(message.tx_hex, 0, message.tx_hex.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.tx_hex) : message.tx_hex;
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            return object;
        };

        /**
         * Converts this Transaction to JSON.
         * @function toJSON
         * @memberof walletrpc.Transaction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Transaction.prototype.toJSON = function() {
            return Transaction.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Transaction
         * @function getTypeUrl
         * @memberof walletrpc.Transaction
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Transaction.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.Transaction";
        };

        return Transaction;
    })();

    walletrpc.PublishResponse = (function() {

        /**
         * Properties of a PublishResponse.
         * @typedef {Object} walletrpc.PublishResponse.$Properties
         * @property {string|null} [publish_error] PublishResponse publish_error
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PublishResponse.
         * @memberof walletrpc
         * @interface IPublishResponse
         * @augments walletrpc.PublishResponse.$Properties
         * @deprecated Use walletrpc.PublishResponse.$Properties instead.
         */

        /**
         * Shape of a PublishResponse.
         * @typedef {walletrpc.PublishResponse.$Properties} walletrpc.PublishResponse.$Shape
         */

        /**
         * Constructs a new PublishResponse.
         * @memberof walletrpc
         * @classdesc Represents a PublishResponse.
         * @constructor
         * @param {walletrpc.PublishResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PublishResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PublishResponse publish_error.
         * @member {string} publish_error
         * @memberof walletrpc.PublishResponse
         * @instance
         */
        PublishResponse.prototype.publish_error = "";

        /**
         * Creates a new PublishResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.PublishResponse
         * @static
         * @param {walletrpc.PublishResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.PublishResponse} PublishResponse instance
         * @type {{
         *   (properties: walletrpc.PublishResponse.$Shape): walletrpc.PublishResponse & walletrpc.PublishResponse.$Shape;
         *   (properties?: walletrpc.PublishResponse.$Properties): walletrpc.PublishResponse;
         * }}
         */
        PublishResponse.create = function(properties) {
            return new PublishResponse(properties);
        };

        /**
         * Encodes the specified PublishResponse message. Does not implicitly {@link walletrpc.PublishResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.PublishResponse
         * @static
         * @param {walletrpc.PublishResponse.$Properties} message PublishResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PublishResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.publish_error != null && $Object.hasOwnProperty.call(message, "publish_error") && message.publish_error !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.publish_error);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PublishResponse message, length delimited. Does not implicitly {@link walletrpc.PublishResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.PublishResponse
         * @static
         * @param {walletrpc.PublishResponse.$Properties} message PublishResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PublishResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PublishResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.PublishResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.PublishResponse & walletrpc.PublishResponse.$Shape} PublishResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PublishResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.PublishResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.publish_error = value;
                        else
                            delete message.publish_error;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PublishResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.PublishResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.PublishResponse & walletrpc.PublishResponse.$Shape} PublishResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PublishResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PublishResponse message.
         * @function verify
         * @memberof walletrpc.PublishResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PublishResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.publish_error != null && $Object.hasOwnProperty.call(message, "publish_error"))
                if (!$util.isString(message.publish_error))
                    return "publish_error: string expected";
            return null;
        };

        /**
         * Creates a PublishResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.PublishResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.PublishResponse} PublishResponse
         */
        PublishResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.PublishResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.PublishResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.PublishResponse();
            if (object.publish_error != null)
                if (typeof object.publish_error !== "string" || object.publish_error.length)
                    message.publish_error = $String(object.publish_error);
            return message;
        };

        /**
         * Creates a plain object from a PublishResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.PublishResponse
         * @static
         * @param {walletrpc.PublishResponse} message PublishResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PublishResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.publish_error = "";
            if (message.publish_error != null && $Object.hasOwnProperty.call(message, "publish_error"))
                object.publish_error = message.publish_error;
            return object;
        };

        /**
         * Converts this PublishResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.PublishResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PublishResponse.prototype.toJSON = function() {
            return PublishResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PublishResponse
         * @function getTypeUrl
         * @memberof walletrpc.PublishResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PublishResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.PublishResponse";
        };

        return PublishResponse;
    })();

    walletrpc.RemoveTransactionResponse = (function() {

        /**
         * Properties of a RemoveTransactionResponse.
         * @typedef {Object} walletrpc.RemoveTransactionResponse.$Properties
         * @property {string|null} [status] RemoveTransactionResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RemoveTransactionResponse.
         * @memberof walletrpc
         * @interface IRemoveTransactionResponse
         * @augments walletrpc.RemoveTransactionResponse.$Properties
         * @deprecated Use walletrpc.RemoveTransactionResponse.$Properties instead.
         */

        /**
         * Shape of a RemoveTransactionResponse.
         * @typedef {walletrpc.RemoveTransactionResponse.$Properties} walletrpc.RemoveTransactionResponse.$Shape
         */

        /**
         * Constructs a new RemoveTransactionResponse.
         * @memberof walletrpc
         * @classdesc Represents a RemoveTransactionResponse.
         * @constructor
         * @param {walletrpc.RemoveTransactionResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RemoveTransactionResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RemoveTransactionResponse status.
         * @member {string} status
         * @memberof walletrpc.RemoveTransactionResponse
         * @instance
         */
        RemoveTransactionResponse.prototype.status = "";

        /**
         * Creates a new RemoveTransactionResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.RemoveTransactionResponse
         * @static
         * @param {walletrpc.RemoveTransactionResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.RemoveTransactionResponse} RemoveTransactionResponse instance
         * @type {{
         *   (properties: walletrpc.RemoveTransactionResponse.$Shape): walletrpc.RemoveTransactionResponse & walletrpc.RemoveTransactionResponse.$Shape;
         *   (properties?: walletrpc.RemoveTransactionResponse.$Properties): walletrpc.RemoveTransactionResponse;
         * }}
         */
        RemoveTransactionResponse.create = function(properties) {
            return new RemoveTransactionResponse(properties);
        };

        /**
         * Encodes the specified RemoveTransactionResponse message. Does not implicitly {@link walletrpc.RemoveTransactionResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.RemoveTransactionResponse
         * @static
         * @param {walletrpc.RemoveTransactionResponse.$Properties} message RemoveTransactionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveTransactionResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RemoveTransactionResponse message, length delimited. Does not implicitly {@link walletrpc.RemoveTransactionResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.RemoveTransactionResponse
         * @static
         * @param {walletrpc.RemoveTransactionResponse.$Properties} message RemoveTransactionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveTransactionResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RemoveTransactionResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.RemoveTransactionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.RemoveTransactionResponse & walletrpc.RemoveTransactionResponse.$Shape} RemoveTransactionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveTransactionResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.RemoveTransactionResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RemoveTransactionResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.RemoveTransactionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.RemoveTransactionResponse & walletrpc.RemoveTransactionResponse.$Shape} RemoveTransactionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveTransactionResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RemoveTransactionResponse message.
         * @function verify
         * @memberof walletrpc.RemoveTransactionResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RemoveTransactionResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a RemoveTransactionResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.RemoveTransactionResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.RemoveTransactionResponse} RemoveTransactionResponse
         */
        RemoveTransactionResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.RemoveTransactionResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.RemoveTransactionResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.RemoveTransactionResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a RemoveTransactionResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.RemoveTransactionResponse
         * @static
         * @param {walletrpc.RemoveTransactionResponse} message RemoveTransactionResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RemoveTransactionResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this RemoveTransactionResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.RemoveTransactionResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RemoveTransactionResponse.prototype.toJSON = function() {
            return RemoveTransactionResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RemoveTransactionResponse
         * @function getTypeUrl
         * @memberof walletrpc.RemoveTransactionResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RemoveTransactionResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.RemoveTransactionResponse";
        };

        return RemoveTransactionResponse;
    })();

    walletrpc.SendOutputsRequest = (function() {

        /**
         * Properties of a SendOutputsRequest.
         * @typedef {Object} walletrpc.SendOutputsRequest.$Properties
         * @property {Long|null} [sat_per_kw] SendOutputsRequest sat_per_kw
         * @property {Array.<signrpc.TxOut.$Properties>|null} [outputs] SendOutputsRequest outputs
         * @property {string|null} [label] SendOutputsRequest label
         * @property {number|null} [min_confs] SendOutputsRequest min_confs
         * @property {boolean|null} [spend_unconfirmed] SendOutputsRequest spend_unconfirmed
         * @property {lnrpc.CoinSelectionStrategy|null} [coin_selection_strategy] SendOutputsRequest coin_selection_strategy
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendOutputsRequest.
         * @memberof walletrpc
         * @interface ISendOutputsRequest
         * @augments walletrpc.SendOutputsRequest.$Properties
         * @deprecated Use walletrpc.SendOutputsRequest.$Properties instead.
         */

        /**
         * Shape of a SendOutputsRequest.
         * @typedef {walletrpc.SendOutputsRequest.$Properties} walletrpc.SendOutputsRequest.$Shape
         */

        /**
         * Constructs a new SendOutputsRequest.
         * @memberof walletrpc
         * @classdesc Represents a SendOutputsRequest.
         * @constructor
         * @param {walletrpc.SendOutputsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendOutputsRequest = function (properties) {
            this.outputs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendOutputsRequest sat_per_kw.
         * @member {Long} sat_per_kw
         * @memberof walletrpc.SendOutputsRequest
         * @instance
         */
        SendOutputsRequest.prototype.sat_per_kw = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SendOutputsRequest outputs.
         * @member {Array.<signrpc.TxOut.$Properties>} outputs
         * @memberof walletrpc.SendOutputsRequest
         * @instance
         */
        SendOutputsRequest.prototype.outputs = $util.emptyArray;

        /**
         * SendOutputsRequest label.
         * @member {string} label
         * @memberof walletrpc.SendOutputsRequest
         * @instance
         */
        SendOutputsRequest.prototype.label = "";

        /**
         * SendOutputsRequest min_confs.
         * @member {number} min_confs
         * @memberof walletrpc.SendOutputsRequest
         * @instance
         */
        SendOutputsRequest.prototype.min_confs = 0;

        /**
         * SendOutputsRequest spend_unconfirmed.
         * @member {boolean} spend_unconfirmed
         * @memberof walletrpc.SendOutputsRequest
         * @instance
         */
        SendOutputsRequest.prototype.spend_unconfirmed = false;

        /**
         * SendOutputsRequest coin_selection_strategy.
         * @member {lnrpc.CoinSelectionStrategy} coin_selection_strategy
         * @memberof walletrpc.SendOutputsRequest
         * @instance
         */
        SendOutputsRequest.prototype.coin_selection_strategy = 0;

        /**
         * Creates a new SendOutputsRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.SendOutputsRequest
         * @static
         * @param {walletrpc.SendOutputsRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.SendOutputsRequest} SendOutputsRequest instance
         * @type {{
         *   (properties: walletrpc.SendOutputsRequest.$Shape): walletrpc.SendOutputsRequest & walletrpc.SendOutputsRequest.$Shape;
         *   (properties?: walletrpc.SendOutputsRequest.$Properties): walletrpc.SendOutputsRequest;
         * }}
         */
        SendOutputsRequest.create = function(properties) {
            return new SendOutputsRequest(properties);
        };

        /**
         * Encodes the specified SendOutputsRequest message. Does not implicitly {@link walletrpc.SendOutputsRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.SendOutputsRequest
         * @static
         * @param {walletrpc.SendOutputsRequest.$Properties} message SendOutputsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendOutputsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sat_per_kw != null && $Object.hasOwnProperty.call(message, "sat_per_kw") && (typeof message.sat_per_kw === "object" ? message.sat_per_kw.low || message.sat_per_kw.high : message.sat_per_kw !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.sat_per_kw);
            if (message.outputs != null && message.outputs.length)
                for (let i = 0; i < message.outputs.length; ++i)
                    $root.signrpc.TxOut.encode(message.outputs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.label);
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.min_confs);
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed") && message.spend_unconfirmed !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.spend_unconfirmed);
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy") && message.coin_selection_strategy !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.coin_selection_strategy);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendOutputsRequest message, length delimited. Does not implicitly {@link walletrpc.SendOutputsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.SendOutputsRequest
         * @static
         * @param {walletrpc.SendOutputsRequest.$Properties} message SendOutputsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendOutputsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendOutputsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.SendOutputsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.SendOutputsRequest & walletrpc.SendOutputsRequest.$Shape} SendOutputsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendOutputsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.SendOutputsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_kw = value;
                        else
                            delete message.sat_per_kw;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.outputs && message.outputs.length))
                            message.outputs = [];
                        message.outputs.push($root.signrpc.TxOut.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.spend_unconfirmed = value;
                        else
                            delete message.spend_unconfirmed;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.coin_selection_strategy = value;
                        else
                            delete message.coin_selection_strategy;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendOutputsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.SendOutputsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.SendOutputsRequest & walletrpc.SendOutputsRequest.$Shape} SendOutputsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendOutputsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendOutputsRequest message.
         * @function verify
         * @memberof walletrpc.SendOutputsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendOutputsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sat_per_kw != null && $Object.hasOwnProperty.call(message, "sat_per_kw"))
                if (!$util.isInteger(message.sat_per_kw) && !(message.sat_per_kw && $util.isInteger(message.sat_per_kw.low) && $util.isInteger(message.sat_per_kw.high)))
                    return "sat_per_kw: integer|Long expected";
            if (message.outputs != null && $Object.hasOwnProperty.call(message, "outputs")) {
                if (!$Array.isArray(message.outputs))
                    return "outputs: array expected";
                for (let i = 0; i < message.outputs.length; ++i) {
                    let error = $root.signrpc.TxOut.verify(message.outputs[i], _depth + 1);
                    if (error)
                        return "outputs." + error;
                }
            }
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                if (typeof message.spend_unconfirmed !== "boolean")
                    return "spend_unconfirmed: boolean expected";
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                if (typeof message.coin_selection_strategy !== "number" || (message.coin_selection_strategy | 0) !== message.coin_selection_strategy)
                    return "coin_selection_strategy: enum value expected";
            return null;
        };

        /**
         * Creates a SendOutputsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.SendOutputsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.SendOutputsRequest} SendOutputsRequest
         */
        SendOutputsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.SendOutputsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.SendOutputsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.SendOutputsRequest();
            if (object.sat_per_kw != null)
                if (typeof object.sat_per_kw === "object" ? object.sat_per_kw.low || object.sat_per_kw.high : $Number(object.sat_per_kw) !== 0)
                    if ($util.Long)
                        message.sat_per_kw = $util.Long.fromValue(object.sat_per_kw, false);
                    else if (typeof object.sat_per_kw === "string")
                        message.sat_per_kw = $parseInt(object.sat_per_kw, 10);
                    else if (typeof object.sat_per_kw === "number")
                        message.sat_per_kw = object.sat_per_kw;
                    else if (typeof object.sat_per_kw === "object")
                        message.sat_per_kw = new $util.LongBits(object.sat_per_kw.low >>> 0, object.sat_per_kw.high >>> 0).toNumber();
            if (object.outputs) {
                if (!$Array.isArray(object.outputs))
                    throw $TypeError(".walletrpc.SendOutputsRequest.outputs: array expected");
                message.outputs = $Array(object.outputs.length);
                for (let i = 0; i < object.outputs.length; ++i) {
                    if (!$util.isObject(object.outputs[i]))
                        throw $TypeError(".walletrpc.SendOutputsRequest.outputs: object expected");
                    message.outputs[i] = $root.signrpc.TxOut.fromObject(object.outputs[i], _depth + 1);
                }
            }
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            if (object.spend_unconfirmed != null)
                if (object.spend_unconfirmed)
                    message.spend_unconfirmed = $Boolean(object.spend_unconfirmed);
            if (object.coin_selection_strategy !== 0 && (typeof object.coin_selection_strategy !== "string" || $root.lnrpc.CoinSelectionStrategy[object.coin_selection_strategy] !== 0))
                switch (object.coin_selection_strategy) {
                case "STRATEGY_USE_GLOBAL_CONFIG":
                case 0:
                    message.coin_selection_strategy = 0;
                    break;
                case "STRATEGY_LARGEST":
                case 1:
                    message.coin_selection_strategy = 1;
                    break;
                case "STRATEGY_RANDOM":
                case 2:
                    message.coin_selection_strategy = 2;
                    break;
                default:
                    if (typeof object.coin_selection_strategy === "number" && (object.coin_selection_strategy | 0) === object.coin_selection_strategy)
                        message.coin_selection_strategy = object.coin_selection_strategy;
                }
            return message;
        };

        /**
         * Creates a plain object from a SendOutputsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.SendOutputsRequest
         * @static
         * @param {walletrpc.SendOutputsRequest} message SendOutputsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendOutputsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.outputs = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sat_per_kw = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_kw = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.label = "";
                object.min_confs = 0;
                object.spend_unconfirmed = false;
                object.coin_selection_strategy = options.enums === $String ? "STRATEGY_USE_GLOBAL_CONFIG" : 0;
            }
            if (message.sat_per_kw != null && $Object.hasOwnProperty.call(message, "sat_per_kw"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_kw = typeof message.sat_per_kw === "number" ? $BigInt(message.sat_per_kw) : $util.Long.fromBits(message.sat_per_kw.low >>> 0, message.sat_per_kw.high >>> 0, false).toBigInt();
                else if (typeof message.sat_per_kw === "number")
                    object.sat_per_kw = options.longs === $String ? $String(message.sat_per_kw) : message.sat_per_kw;
                else
                    object.sat_per_kw = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_kw) : options.longs === $Number ? new $util.LongBits(message.sat_per_kw.low >>> 0, message.sat_per_kw.high >>> 0).toNumber() : message.sat_per_kw;
            if (message.outputs && message.outputs.length) {
                object.outputs = $Array(message.outputs.length);
                for (let j = 0; j < message.outputs.length; ++j)
                    object.outputs[j] = $root.signrpc.TxOut.toObject(message.outputs[j], options, _depth + 1);
            }
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                object.spend_unconfirmed = message.spend_unconfirmed;
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                object.coin_selection_strategy = options.enums === $String ? $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] === $undefined ? message.coin_selection_strategy : $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] : message.coin_selection_strategy;
            return object;
        };

        /**
         * Converts this SendOutputsRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.SendOutputsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendOutputsRequest.prototype.toJSON = function() {
            return SendOutputsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendOutputsRequest
         * @function getTypeUrl
         * @memberof walletrpc.SendOutputsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendOutputsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.SendOutputsRequest";
        };

        return SendOutputsRequest;
    })();

    walletrpc.SendOutputsResponse = (function() {

        /**
         * Properties of a SendOutputsResponse.
         * @typedef {Object} walletrpc.SendOutputsResponse.$Properties
         * @property {Uint8Array|null} [raw_tx] SendOutputsResponse raw_tx
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SendOutputsResponse.
         * @memberof walletrpc
         * @interface ISendOutputsResponse
         * @augments walletrpc.SendOutputsResponse.$Properties
         * @deprecated Use walletrpc.SendOutputsResponse.$Properties instead.
         */

        /**
         * Shape of a SendOutputsResponse.
         * @typedef {walletrpc.SendOutputsResponse.$Properties} walletrpc.SendOutputsResponse.$Shape
         */

        /**
         * Constructs a new SendOutputsResponse.
         * @memberof walletrpc
         * @classdesc Represents a SendOutputsResponse.
         * @constructor
         * @param {walletrpc.SendOutputsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SendOutputsResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SendOutputsResponse raw_tx.
         * @member {Uint8Array} raw_tx
         * @memberof walletrpc.SendOutputsResponse
         * @instance
         */
        SendOutputsResponse.prototype.raw_tx = $util.newBuffer([]);

        /**
         * Creates a new SendOutputsResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.SendOutputsResponse
         * @static
         * @param {walletrpc.SendOutputsResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.SendOutputsResponse} SendOutputsResponse instance
         * @type {{
         *   (properties: walletrpc.SendOutputsResponse.$Shape): walletrpc.SendOutputsResponse & walletrpc.SendOutputsResponse.$Shape;
         *   (properties?: walletrpc.SendOutputsResponse.$Properties): walletrpc.SendOutputsResponse;
         * }}
         */
        SendOutputsResponse.create = function(properties) {
            return new SendOutputsResponse(properties);
        };

        /**
         * Encodes the specified SendOutputsResponse message. Does not implicitly {@link walletrpc.SendOutputsResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.SendOutputsResponse
         * @static
         * @param {walletrpc.SendOutputsResponse.$Properties} message SendOutputsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendOutputsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.raw_tx != null && $Object.hasOwnProperty.call(message, "raw_tx") && message.raw_tx.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.raw_tx);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SendOutputsResponse message, length delimited. Does not implicitly {@link walletrpc.SendOutputsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.SendOutputsResponse
         * @static
         * @param {walletrpc.SendOutputsResponse.$Properties} message SendOutputsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendOutputsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SendOutputsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.SendOutputsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.SendOutputsResponse & walletrpc.SendOutputsResponse.$Shape} SendOutputsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendOutputsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.SendOutputsResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_tx = value;
                        else
                            delete message.raw_tx;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SendOutputsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.SendOutputsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.SendOutputsResponse & walletrpc.SendOutputsResponse.$Shape} SendOutputsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendOutputsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SendOutputsResponse message.
         * @function verify
         * @memberof walletrpc.SendOutputsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SendOutputsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.raw_tx != null && $Object.hasOwnProperty.call(message, "raw_tx"))
                if (!(message.raw_tx && typeof message.raw_tx.length === "number" || $util.isString(message.raw_tx)))
                    return "raw_tx: buffer expected";
            return null;
        };

        /**
         * Creates a SendOutputsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.SendOutputsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.SendOutputsResponse} SendOutputsResponse
         */
        SendOutputsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.SendOutputsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.SendOutputsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.SendOutputsResponse();
            if (object.raw_tx != null)
                if (object.raw_tx.length)
                    if (typeof object.raw_tx === "string")
                        $util.base64.decode(object.raw_tx, message.raw_tx = $util.newBuffer($util.base64.length(object.raw_tx)), 0);
                    else if (object.raw_tx.length >= 0)
                        message.raw_tx = object.raw_tx;
            return message;
        };

        /**
         * Creates a plain object from a SendOutputsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.SendOutputsResponse
         * @static
         * @param {walletrpc.SendOutputsResponse} message SendOutputsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendOutputsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.raw_tx = "";
                else {
                    object.raw_tx = [];
                    if (options.bytes !== $Array)
                        object.raw_tx = $util.newBuffer(object.raw_tx);
                }
            if (message.raw_tx != null && $Object.hasOwnProperty.call(message, "raw_tx"))
                object.raw_tx = options.bytes === $String ? $util.base64.encode(message.raw_tx, 0, message.raw_tx.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_tx) : message.raw_tx;
            return object;
        };

        /**
         * Converts this SendOutputsResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.SendOutputsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendOutputsResponse.prototype.toJSON = function() {
            return SendOutputsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SendOutputsResponse
         * @function getTypeUrl
         * @memberof walletrpc.SendOutputsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SendOutputsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.SendOutputsResponse";
        };

        return SendOutputsResponse;
    })();

    walletrpc.EstimateFeeRequest = (function() {

        /**
         * Properties of an EstimateFeeRequest.
         * @typedef {Object} walletrpc.EstimateFeeRequest.$Properties
         * @property {number|null} [conf_target] EstimateFeeRequest conf_target
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an EstimateFeeRequest.
         * @memberof walletrpc
         * @interface IEstimateFeeRequest
         * @augments walletrpc.EstimateFeeRequest.$Properties
         * @deprecated Use walletrpc.EstimateFeeRequest.$Properties instead.
         */

        /**
         * Shape of an EstimateFeeRequest.
         * @typedef {walletrpc.EstimateFeeRequest.$Properties} walletrpc.EstimateFeeRequest.$Shape
         */

        /**
         * Constructs a new EstimateFeeRequest.
         * @memberof walletrpc
         * @classdesc Represents an EstimateFeeRequest.
         * @constructor
         * @param {walletrpc.EstimateFeeRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const EstimateFeeRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * EstimateFeeRequest conf_target.
         * @member {number} conf_target
         * @memberof walletrpc.EstimateFeeRequest
         * @instance
         */
        EstimateFeeRequest.prototype.conf_target = 0;

        /**
         * Creates a new EstimateFeeRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.EstimateFeeRequest
         * @static
         * @param {walletrpc.EstimateFeeRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.EstimateFeeRequest} EstimateFeeRequest instance
         * @type {{
         *   (properties: walletrpc.EstimateFeeRequest.$Shape): walletrpc.EstimateFeeRequest & walletrpc.EstimateFeeRequest.$Shape;
         *   (properties?: walletrpc.EstimateFeeRequest.$Properties): walletrpc.EstimateFeeRequest;
         * }}
         */
        EstimateFeeRequest.create = function(properties) {
            return new EstimateFeeRequest(properties);
        };

        /**
         * Encodes the specified EstimateFeeRequest message. Does not implicitly {@link walletrpc.EstimateFeeRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.EstimateFeeRequest
         * @static
         * @param {walletrpc.EstimateFeeRequest.$Properties} message EstimateFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EstimateFeeRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.conf_target != null && $Object.hasOwnProperty.call(message, "conf_target") && message.conf_target !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.conf_target);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EstimateFeeRequest message, length delimited. Does not implicitly {@link walletrpc.EstimateFeeRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.EstimateFeeRequest
         * @static
         * @param {walletrpc.EstimateFeeRequest.$Properties} message EstimateFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EstimateFeeRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EstimateFeeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.EstimateFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.EstimateFeeRequest & walletrpc.EstimateFeeRequest.$Shape} EstimateFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EstimateFeeRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.EstimateFeeRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.conf_target = value;
                        else
                            delete message.conf_target;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EstimateFeeRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.EstimateFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.EstimateFeeRequest & walletrpc.EstimateFeeRequest.$Shape} EstimateFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EstimateFeeRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EstimateFeeRequest message.
         * @function verify
         * @memberof walletrpc.EstimateFeeRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EstimateFeeRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.conf_target != null && $Object.hasOwnProperty.call(message, "conf_target"))
                if (!$util.isInteger(message.conf_target))
                    return "conf_target: integer expected";
            return null;
        };

        /**
         * Creates an EstimateFeeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.EstimateFeeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.EstimateFeeRequest} EstimateFeeRequest
         */
        EstimateFeeRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.EstimateFeeRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.EstimateFeeRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.EstimateFeeRequest();
            if (object.conf_target != null)
                if ($Number(object.conf_target) !== 0)
                    message.conf_target = object.conf_target | 0;
            return message;
        };

        /**
         * Creates a plain object from an EstimateFeeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.EstimateFeeRequest
         * @static
         * @param {walletrpc.EstimateFeeRequest} message EstimateFeeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EstimateFeeRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.conf_target = 0;
            if (message.conf_target != null && $Object.hasOwnProperty.call(message, "conf_target"))
                object.conf_target = message.conf_target;
            return object;
        };

        /**
         * Converts this EstimateFeeRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.EstimateFeeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EstimateFeeRequest.prototype.toJSON = function() {
            return EstimateFeeRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EstimateFeeRequest
         * @function getTypeUrl
         * @memberof walletrpc.EstimateFeeRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EstimateFeeRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.EstimateFeeRequest";
        };

        return EstimateFeeRequest;
    })();

    walletrpc.EstimateFeeResponse = (function() {

        /**
         * Properties of an EstimateFeeResponse.
         * @typedef {Object} walletrpc.EstimateFeeResponse.$Properties
         * @property {Long|null} [sat_per_kw] EstimateFeeResponse sat_per_kw
         * @property {Long|null} [min_relay_fee_sat_per_kw] EstimateFeeResponse min_relay_fee_sat_per_kw
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an EstimateFeeResponse.
         * @memberof walletrpc
         * @interface IEstimateFeeResponse
         * @augments walletrpc.EstimateFeeResponse.$Properties
         * @deprecated Use walletrpc.EstimateFeeResponse.$Properties instead.
         */

        /**
         * Shape of an EstimateFeeResponse.
         * @typedef {walletrpc.EstimateFeeResponse.$Properties} walletrpc.EstimateFeeResponse.$Shape
         */

        /**
         * Constructs a new EstimateFeeResponse.
         * @memberof walletrpc
         * @classdesc Represents an EstimateFeeResponse.
         * @constructor
         * @param {walletrpc.EstimateFeeResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const EstimateFeeResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * EstimateFeeResponse sat_per_kw.
         * @member {Long} sat_per_kw
         * @memberof walletrpc.EstimateFeeResponse
         * @instance
         */
        EstimateFeeResponse.prototype.sat_per_kw = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EstimateFeeResponse min_relay_fee_sat_per_kw.
         * @member {Long} min_relay_fee_sat_per_kw
         * @memberof walletrpc.EstimateFeeResponse
         * @instance
         */
        EstimateFeeResponse.prototype.min_relay_fee_sat_per_kw = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new EstimateFeeResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.EstimateFeeResponse
         * @static
         * @param {walletrpc.EstimateFeeResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.EstimateFeeResponse} EstimateFeeResponse instance
         * @type {{
         *   (properties: walletrpc.EstimateFeeResponse.$Shape): walletrpc.EstimateFeeResponse & walletrpc.EstimateFeeResponse.$Shape;
         *   (properties?: walletrpc.EstimateFeeResponse.$Properties): walletrpc.EstimateFeeResponse;
         * }}
         */
        EstimateFeeResponse.create = function(properties) {
            return new EstimateFeeResponse(properties);
        };

        /**
         * Encodes the specified EstimateFeeResponse message. Does not implicitly {@link walletrpc.EstimateFeeResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.EstimateFeeResponse
         * @static
         * @param {walletrpc.EstimateFeeResponse.$Properties} message EstimateFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EstimateFeeResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sat_per_kw != null && $Object.hasOwnProperty.call(message, "sat_per_kw") && (typeof message.sat_per_kw === "object" ? message.sat_per_kw.low || message.sat_per_kw.high : message.sat_per_kw !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.sat_per_kw);
            if (message.min_relay_fee_sat_per_kw != null && $Object.hasOwnProperty.call(message, "min_relay_fee_sat_per_kw") && (typeof message.min_relay_fee_sat_per_kw === "object" ? message.min_relay_fee_sat_per_kw.low || message.min_relay_fee_sat_per_kw.high : message.min_relay_fee_sat_per_kw !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.min_relay_fee_sat_per_kw);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EstimateFeeResponse message, length delimited. Does not implicitly {@link walletrpc.EstimateFeeResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.EstimateFeeResponse
         * @static
         * @param {walletrpc.EstimateFeeResponse.$Properties} message EstimateFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EstimateFeeResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EstimateFeeResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.EstimateFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.EstimateFeeResponse & walletrpc.EstimateFeeResponse.$Shape} EstimateFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EstimateFeeResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.EstimateFeeResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_kw = value;
                        else
                            delete message.sat_per_kw;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.min_relay_fee_sat_per_kw = value;
                        else
                            delete message.min_relay_fee_sat_per_kw;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EstimateFeeResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.EstimateFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.EstimateFeeResponse & walletrpc.EstimateFeeResponse.$Shape} EstimateFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EstimateFeeResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EstimateFeeResponse message.
         * @function verify
         * @memberof walletrpc.EstimateFeeResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EstimateFeeResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sat_per_kw != null && $Object.hasOwnProperty.call(message, "sat_per_kw"))
                if (!$util.isInteger(message.sat_per_kw) && !(message.sat_per_kw && $util.isInteger(message.sat_per_kw.low) && $util.isInteger(message.sat_per_kw.high)))
                    return "sat_per_kw: integer|Long expected";
            if (message.min_relay_fee_sat_per_kw != null && $Object.hasOwnProperty.call(message, "min_relay_fee_sat_per_kw"))
                if (!$util.isInteger(message.min_relay_fee_sat_per_kw) && !(message.min_relay_fee_sat_per_kw && $util.isInteger(message.min_relay_fee_sat_per_kw.low) && $util.isInteger(message.min_relay_fee_sat_per_kw.high)))
                    return "min_relay_fee_sat_per_kw: integer|Long expected";
            return null;
        };

        /**
         * Creates an EstimateFeeResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.EstimateFeeResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.EstimateFeeResponse} EstimateFeeResponse
         */
        EstimateFeeResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.EstimateFeeResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.EstimateFeeResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.EstimateFeeResponse();
            if (object.sat_per_kw != null)
                if (typeof object.sat_per_kw === "object" ? object.sat_per_kw.low || object.sat_per_kw.high : $Number(object.sat_per_kw) !== 0)
                    if ($util.Long)
                        message.sat_per_kw = $util.Long.fromValue(object.sat_per_kw, false);
                    else if (typeof object.sat_per_kw === "string")
                        message.sat_per_kw = $parseInt(object.sat_per_kw, 10);
                    else if (typeof object.sat_per_kw === "number")
                        message.sat_per_kw = object.sat_per_kw;
                    else if (typeof object.sat_per_kw === "object")
                        message.sat_per_kw = new $util.LongBits(object.sat_per_kw.low >>> 0, object.sat_per_kw.high >>> 0).toNumber();
            if (object.min_relay_fee_sat_per_kw != null)
                if (typeof object.min_relay_fee_sat_per_kw === "object" ? object.min_relay_fee_sat_per_kw.low || object.min_relay_fee_sat_per_kw.high : $Number(object.min_relay_fee_sat_per_kw) !== 0)
                    if ($util.Long)
                        message.min_relay_fee_sat_per_kw = $util.Long.fromValue(object.min_relay_fee_sat_per_kw, false);
                    else if (typeof object.min_relay_fee_sat_per_kw === "string")
                        message.min_relay_fee_sat_per_kw = $parseInt(object.min_relay_fee_sat_per_kw, 10);
                    else if (typeof object.min_relay_fee_sat_per_kw === "number")
                        message.min_relay_fee_sat_per_kw = object.min_relay_fee_sat_per_kw;
                    else if (typeof object.min_relay_fee_sat_per_kw === "object")
                        message.min_relay_fee_sat_per_kw = new $util.LongBits(object.min_relay_fee_sat_per_kw.low >>> 0, object.min_relay_fee_sat_per_kw.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an EstimateFeeResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.EstimateFeeResponse
         * @static
         * @param {walletrpc.EstimateFeeResponse} message EstimateFeeResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EstimateFeeResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sat_per_kw = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_kw = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.min_relay_fee_sat_per_kw = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min_relay_fee_sat_per_kw = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.sat_per_kw != null && $Object.hasOwnProperty.call(message, "sat_per_kw"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_kw = typeof message.sat_per_kw === "number" ? $BigInt(message.sat_per_kw) : $util.Long.fromBits(message.sat_per_kw.low >>> 0, message.sat_per_kw.high >>> 0, false).toBigInt();
                else if (typeof message.sat_per_kw === "number")
                    object.sat_per_kw = options.longs === $String ? $String(message.sat_per_kw) : message.sat_per_kw;
                else
                    object.sat_per_kw = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_kw) : options.longs === $Number ? new $util.LongBits(message.sat_per_kw.low >>> 0, message.sat_per_kw.high >>> 0).toNumber() : message.sat_per_kw;
            if (message.min_relay_fee_sat_per_kw != null && $Object.hasOwnProperty.call(message, "min_relay_fee_sat_per_kw"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min_relay_fee_sat_per_kw = typeof message.min_relay_fee_sat_per_kw === "number" ? $BigInt(message.min_relay_fee_sat_per_kw) : $util.Long.fromBits(message.min_relay_fee_sat_per_kw.low >>> 0, message.min_relay_fee_sat_per_kw.high >>> 0, false).toBigInt();
                else if (typeof message.min_relay_fee_sat_per_kw === "number")
                    object.min_relay_fee_sat_per_kw = options.longs === $String ? $String(message.min_relay_fee_sat_per_kw) : message.min_relay_fee_sat_per_kw;
                else
                    object.min_relay_fee_sat_per_kw = options.longs === $String ? $util.Long.prototype.toString.call(message.min_relay_fee_sat_per_kw) : options.longs === $Number ? new $util.LongBits(message.min_relay_fee_sat_per_kw.low >>> 0, message.min_relay_fee_sat_per_kw.high >>> 0).toNumber() : message.min_relay_fee_sat_per_kw;
            return object;
        };

        /**
         * Converts this EstimateFeeResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.EstimateFeeResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EstimateFeeResponse.prototype.toJSON = function() {
            return EstimateFeeResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EstimateFeeResponse
         * @function getTypeUrl
         * @memberof walletrpc.EstimateFeeResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EstimateFeeResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.EstimateFeeResponse";
        };

        return EstimateFeeResponse;
    })();

    /**
     * WitnessType enum.
     * @name walletrpc.WitnessType
     * @enum {number}
     * @property {number} UNKNOWN_WITNESS=0 UNKNOWN_WITNESS value
     * @property {number} COMMITMENT_TIME_LOCK=1 COMMITMENT_TIME_LOCK value
     * @property {number} COMMITMENT_NO_DELAY=2 COMMITMENT_NO_DELAY value
     * @property {number} COMMITMENT_REVOKE=3 COMMITMENT_REVOKE value
     * @property {number} HTLC_OFFERED_REVOKE=4 HTLC_OFFERED_REVOKE value
     * @property {number} HTLC_ACCEPTED_REVOKE=5 HTLC_ACCEPTED_REVOKE value
     * @property {number} HTLC_OFFERED_TIMEOUT_SECOND_LEVEL=6 HTLC_OFFERED_TIMEOUT_SECOND_LEVEL value
     * @property {number} HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL=7 HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL value
     * @property {number} HTLC_OFFERED_REMOTE_TIMEOUT=8 HTLC_OFFERED_REMOTE_TIMEOUT value
     * @property {number} HTLC_ACCEPTED_REMOTE_SUCCESS=9 HTLC_ACCEPTED_REMOTE_SUCCESS value
     * @property {number} HTLC_SECOND_LEVEL_REVOKE=10 HTLC_SECOND_LEVEL_REVOKE value
     * @property {number} WITNESS_KEY_HASH=11 WITNESS_KEY_HASH value
     * @property {number} NESTED_WITNESS_KEY_HASH=12 NESTED_WITNESS_KEY_HASH value
     * @property {number} COMMITMENT_ANCHOR=13 COMMITMENT_ANCHOR value
     * @property {number} COMMITMENT_NO_DELAY_TWEAKLESS=14 COMMITMENT_NO_DELAY_TWEAKLESS value
     * @property {number} COMMITMENT_TO_REMOTE_CONFIRMED=15 COMMITMENT_TO_REMOTE_CONFIRMED value
     * @property {number} HTLC_OFFERED_TIMEOUT_SECOND_LEVEL_INPUT_CONFIRMED=16 HTLC_OFFERED_TIMEOUT_SECOND_LEVEL_INPUT_CONFIRMED value
     * @property {number} HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL_INPUT_CONFIRMED=17 HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL_INPUT_CONFIRMED value
     * @property {number} LEASE_COMMITMENT_TIME_LOCK=18 LEASE_COMMITMENT_TIME_LOCK value
     * @property {number} LEASE_COMMITMENT_TO_REMOTE_CONFIRMED=19 LEASE_COMMITMENT_TO_REMOTE_CONFIRMED value
     * @property {number} LEASE_HTLC_OFFERED_TIMEOUT_SECOND_LEVEL=20 LEASE_HTLC_OFFERED_TIMEOUT_SECOND_LEVEL value
     * @property {number} LEASE_HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL=21 LEASE_HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL value
     * @property {number} TAPROOT_PUB_KEY_SPEND=22 TAPROOT_PUB_KEY_SPEND value
     * @property {number} TAPROOT_LOCAL_COMMIT_SPEND=23 TAPROOT_LOCAL_COMMIT_SPEND value
     * @property {number} TAPROOT_REMOTE_COMMIT_SPEND=24 TAPROOT_REMOTE_COMMIT_SPEND value
     * @property {number} TAPROOT_ANCHOR_SWEEP_SPEND=25 TAPROOT_ANCHOR_SWEEP_SPEND value
     * @property {number} TAPROOT_HTLC_OFFERED_TIMEOUT_SECOND_LEVEL=26 TAPROOT_HTLC_OFFERED_TIMEOUT_SECOND_LEVEL value
     * @property {number} TAPROOT_HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL=27 TAPROOT_HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL value
     * @property {number} TAPROOT_HTLC_SECOND_LEVEL_REVOKE=28 TAPROOT_HTLC_SECOND_LEVEL_REVOKE value
     * @property {number} TAPROOT_HTLC_ACCEPTED_REVOKE=29 TAPROOT_HTLC_ACCEPTED_REVOKE value
     * @property {number} TAPROOT_HTLC_OFFERED_REVOKE=30 TAPROOT_HTLC_OFFERED_REVOKE value
     * @property {number} TAPROOT_HTLC_OFFERED_REMOTE_TIMEOUT=31 TAPROOT_HTLC_OFFERED_REMOTE_TIMEOUT value
     * @property {number} TAPROOT_HTLC_LOCAL_OFFERED_TIMEOUT=32 TAPROOT_HTLC_LOCAL_OFFERED_TIMEOUT value
     * @property {number} TAPROOT_HTLC_ACCEPTED_REMOTE_SUCCESS=33 TAPROOT_HTLC_ACCEPTED_REMOTE_SUCCESS value
     * @property {number} TAPROOT_HTLC_ACCEPTED_LOCAL_SUCCESS=34 TAPROOT_HTLC_ACCEPTED_LOCAL_SUCCESS value
     * @property {number} TAPROOT_COMMITMENT_REVOKE=35 TAPROOT_COMMITMENT_REVOKE value
     */
    walletrpc.WitnessType = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN_WITNESS"] = 0;
        values[valuesById[1] = "COMMITMENT_TIME_LOCK"] = 1;
        values[valuesById[2] = "COMMITMENT_NO_DELAY"] = 2;
        values[valuesById[3] = "COMMITMENT_REVOKE"] = 3;
        values[valuesById[4] = "HTLC_OFFERED_REVOKE"] = 4;
        values[valuesById[5] = "HTLC_ACCEPTED_REVOKE"] = 5;
        values[valuesById[6] = "HTLC_OFFERED_TIMEOUT_SECOND_LEVEL"] = 6;
        values[valuesById[7] = "HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL"] = 7;
        values[valuesById[8] = "HTLC_OFFERED_REMOTE_TIMEOUT"] = 8;
        values[valuesById[9] = "HTLC_ACCEPTED_REMOTE_SUCCESS"] = 9;
        values[valuesById[10] = "HTLC_SECOND_LEVEL_REVOKE"] = 10;
        values[valuesById[11] = "WITNESS_KEY_HASH"] = 11;
        values[valuesById[12] = "NESTED_WITNESS_KEY_HASH"] = 12;
        values[valuesById[13] = "COMMITMENT_ANCHOR"] = 13;
        values[valuesById[14] = "COMMITMENT_NO_DELAY_TWEAKLESS"] = 14;
        values[valuesById[15] = "COMMITMENT_TO_REMOTE_CONFIRMED"] = 15;
        values[valuesById[16] = "HTLC_OFFERED_TIMEOUT_SECOND_LEVEL_INPUT_CONFIRMED"] = 16;
        values[valuesById[17] = "HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL_INPUT_CONFIRMED"] = 17;
        values[valuesById[18] = "LEASE_COMMITMENT_TIME_LOCK"] = 18;
        values[valuesById[19] = "LEASE_COMMITMENT_TO_REMOTE_CONFIRMED"] = 19;
        values[valuesById[20] = "LEASE_HTLC_OFFERED_TIMEOUT_SECOND_LEVEL"] = 20;
        values[valuesById[21] = "LEASE_HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL"] = 21;
        values[valuesById[22] = "TAPROOT_PUB_KEY_SPEND"] = 22;
        values[valuesById[23] = "TAPROOT_LOCAL_COMMIT_SPEND"] = 23;
        values[valuesById[24] = "TAPROOT_REMOTE_COMMIT_SPEND"] = 24;
        values[valuesById[25] = "TAPROOT_ANCHOR_SWEEP_SPEND"] = 25;
        values[valuesById[26] = "TAPROOT_HTLC_OFFERED_TIMEOUT_SECOND_LEVEL"] = 26;
        values[valuesById[27] = "TAPROOT_HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL"] = 27;
        values[valuesById[28] = "TAPROOT_HTLC_SECOND_LEVEL_REVOKE"] = 28;
        values[valuesById[29] = "TAPROOT_HTLC_ACCEPTED_REVOKE"] = 29;
        values[valuesById[30] = "TAPROOT_HTLC_OFFERED_REVOKE"] = 30;
        values[valuesById[31] = "TAPROOT_HTLC_OFFERED_REMOTE_TIMEOUT"] = 31;
        values[valuesById[32] = "TAPROOT_HTLC_LOCAL_OFFERED_TIMEOUT"] = 32;
        values[valuesById[33] = "TAPROOT_HTLC_ACCEPTED_REMOTE_SUCCESS"] = 33;
        values[valuesById[34] = "TAPROOT_HTLC_ACCEPTED_LOCAL_SUCCESS"] = 34;
        values[valuesById[35] = "TAPROOT_COMMITMENT_REVOKE"] = 35;
        return values;
    })();

    walletrpc.PendingSweep = (function() {

        /**
         * Properties of a PendingSweep.
         * @typedef {Object} walletrpc.PendingSweep.$Properties
         * @property {lnrpc.OutPoint.$Properties|null} [outpoint] PendingSweep outpoint
         * @property {walletrpc.WitnessType|null} [witness_type] PendingSweep witness_type
         * @property {number|null} [amount_sat] PendingSweep amount_sat
         * @property {number|null} [sat_per_byte] PendingSweep sat_per_byte
         * @property {number|null} [broadcast_attempts] PendingSweep broadcast_attempts
         * @property {number|null} [next_broadcast_height] PendingSweep next_broadcast_height
         * @property {boolean|null} [force] PendingSweep force
         * @property {number|null} [requested_conf_target] PendingSweep requested_conf_target
         * @property {number|null} [requested_sat_per_byte] PendingSweep requested_sat_per_byte
         * @property {Long|null} [sat_per_vbyte] PendingSweep sat_per_vbyte
         * @property {Long|null} [requested_sat_per_vbyte] PendingSweep requested_sat_per_vbyte
         * @property {boolean|null} [immediate] PendingSweep immediate
         * @property {Long|null} [budget] PendingSweep budget
         * @property {number|null} [deadline_height] PendingSweep deadline_height
         * @property {number|null} [maturity_height] PendingSweep maturity_height
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PendingSweep.
         * @memberof walletrpc
         * @interface IPendingSweep
         * @augments walletrpc.PendingSweep.$Properties
         * @deprecated Use walletrpc.PendingSweep.$Properties instead.
         */

        /**
         * Shape of a PendingSweep.
         * @typedef {walletrpc.PendingSweep.$Properties} walletrpc.PendingSweep.$Shape
         */

        /**
         * Constructs a new PendingSweep.
         * @memberof walletrpc
         * @classdesc Represents a PendingSweep.
         * @constructor
         * @param {walletrpc.PendingSweep.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PendingSweep = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PendingSweep outpoint.
         * @member {lnrpc.OutPoint.$Properties|null|undefined} outpoint
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.outpoint = null;

        /**
         * PendingSweep witness_type.
         * @member {walletrpc.WitnessType} witness_type
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.witness_type = 0;

        /**
         * PendingSweep amount_sat.
         * @member {number} amount_sat
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.amount_sat = 0;

        /**
         * PendingSweep sat_per_byte.
         * @member {number} sat_per_byte
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.sat_per_byte = 0;

        /**
         * PendingSweep broadcast_attempts.
         * @member {number} broadcast_attempts
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.broadcast_attempts = 0;

        /**
         * PendingSweep next_broadcast_height.
         * @member {number} next_broadcast_height
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.next_broadcast_height = 0;

        /**
         * PendingSweep force.
         * @member {boolean} force
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.force = false;

        /**
         * PendingSweep requested_conf_target.
         * @member {number} requested_conf_target
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.requested_conf_target = 0;

        /**
         * PendingSweep requested_sat_per_byte.
         * @member {number} requested_sat_per_byte
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.requested_sat_per_byte = 0;

        /**
         * PendingSweep sat_per_vbyte.
         * @member {Long} sat_per_vbyte
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.sat_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PendingSweep requested_sat_per_vbyte.
         * @member {Long} requested_sat_per_vbyte
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.requested_sat_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PendingSweep immediate.
         * @member {boolean} immediate
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.immediate = false;

        /**
         * PendingSweep budget.
         * @member {Long} budget
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.budget = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PendingSweep deadline_height.
         * @member {number} deadline_height
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.deadline_height = 0;

        /**
         * PendingSweep maturity_height.
         * @member {number} maturity_height
         * @memberof walletrpc.PendingSweep
         * @instance
         */
        PendingSweep.prototype.maturity_height = 0;

        /**
         * Creates a new PendingSweep instance using the specified properties.
         * @function create
         * @memberof walletrpc.PendingSweep
         * @static
         * @param {walletrpc.PendingSweep.$Properties=} [properties] Properties to set
         * @returns {walletrpc.PendingSweep} PendingSweep instance
         * @type {{
         *   (properties: walletrpc.PendingSweep.$Shape): walletrpc.PendingSweep & walletrpc.PendingSweep.$Shape;
         *   (properties?: walletrpc.PendingSweep.$Properties): walletrpc.PendingSweep;
         * }}
         */
        PendingSweep.create = function(properties) {
            return new PendingSweep(properties);
        };

        /**
         * Encodes the specified PendingSweep message. Does not implicitly {@link walletrpc.PendingSweep.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.PendingSweep
         * @static
         * @param {walletrpc.PendingSweep.$Properties} message PendingSweep message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingSweep.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                $root.lnrpc.OutPoint.encode(message.outpoint, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.witness_type != null && $Object.hasOwnProperty.call(message, "witness_type") && message.witness_type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.witness_type);
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat") && message.amount_sat !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.amount_sat);
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte") && message.sat_per_byte !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.sat_per_byte);
            if (message.broadcast_attempts != null && $Object.hasOwnProperty.call(message, "broadcast_attempts") && message.broadcast_attempts !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.broadcast_attempts);
            if (message.next_broadcast_height != null && $Object.hasOwnProperty.call(message, "next_broadcast_height") && message.next_broadcast_height !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.next_broadcast_height);
            if (message.force != null && $Object.hasOwnProperty.call(message, "force") && message.force !== false)
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.force);
            if (message.requested_conf_target != null && $Object.hasOwnProperty.call(message, "requested_conf_target") && message.requested_conf_target !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.requested_conf_target);
            if (message.requested_sat_per_byte != null && $Object.hasOwnProperty.call(message, "requested_sat_per_byte") && message.requested_sat_per_byte !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.requested_sat_per_byte);
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte") && (typeof message.sat_per_vbyte === "object" ? message.sat_per_vbyte.low || message.sat_per_vbyte.high : message.sat_per_vbyte !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.sat_per_vbyte);
            if (message.requested_sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "requested_sat_per_vbyte") && (typeof message.requested_sat_per_vbyte === "object" ? message.requested_sat_per_vbyte.low || message.requested_sat_per_vbyte.high : message.requested_sat_per_vbyte !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.requested_sat_per_vbyte);
            if (message.immediate != null && $Object.hasOwnProperty.call(message, "immediate") && message.immediate !== false)
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.immediate);
            if (message.budget != null && $Object.hasOwnProperty.call(message, "budget") && (typeof message.budget === "object" ? message.budget.low || message.budget.high : message.budget !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).uint64(message.budget);
            if (message.deadline_height != null && $Object.hasOwnProperty.call(message, "deadline_height") && message.deadline_height !== 0)
                writer.uint32(/* id 14, wireType 0 =*/112).uint32(message.deadline_height);
            if (message.maturity_height != null && $Object.hasOwnProperty.call(message, "maturity_height") && message.maturity_height !== 0)
                writer.uint32(/* id 15, wireType 0 =*/120).uint32(message.maturity_height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingSweep message, length delimited. Does not implicitly {@link walletrpc.PendingSweep.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.PendingSweep
         * @static
         * @param {walletrpc.PendingSweep.$Properties} message PendingSweep message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingSweep.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PendingSweep message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.PendingSweep
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.PendingSweep & walletrpc.PendingSweep.$Shape} PendingSweep
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingSweep.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.PendingSweep(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.outpoint = $root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.outpoint);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.witness_type = value;
                        else
                            delete message.witness_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.amount_sat = value;
                        else
                            delete message.amount_sat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sat_per_byte = value;
                        else
                            delete message.sat_per_byte;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.broadcast_attempts = value;
                        else
                            delete message.broadcast_attempts;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.next_broadcast_height = value;
                        else
                            delete message.next_broadcast_height;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.force = value;
                        else
                            delete message.force;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.requested_conf_target = value;
                        else
                            delete message.requested_conf_target;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.requested_sat_per_byte = value;
                        else
                            delete message.requested_sat_per_byte;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_vbyte = value;
                        else
                            delete message.sat_per_vbyte;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.requested_sat_per_vbyte = value;
                        else
                            delete message.requested_sat_per_vbyte;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.immediate = value;
                        else
                            delete message.immediate;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.budget = value;
                        else
                            delete message.budget;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.deadline_height = value;
                        else
                            delete message.deadline_height;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.maturity_height = value;
                        else
                            delete message.maturity_height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingSweep message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.PendingSweep
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.PendingSweep & walletrpc.PendingSweep.$Shape} PendingSweep
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingSweep.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingSweep message.
         * @function verify
         * @memberof walletrpc.PendingSweep
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingSweep.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint")) {
                let error = $root.lnrpc.OutPoint.verify(message.outpoint, _depth + 1);
                if (error)
                    return "outpoint." + error;
            }
            if (message.witness_type != null && $Object.hasOwnProperty.call(message, "witness_type"))
                if (typeof message.witness_type !== "number" || (message.witness_type | 0) !== message.witness_type)
                    return "witness_type: enum value expected";
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat"))
                if (!$util.isInteger(message.amount_sat))
                    return "amount_sat: integer expected";
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (!$util.isInteger(message.sat_per_byte))
                    return "sat_per_byte: integer expected";
            if (message.broadcast_attempts != null && $Object.hasOwnProperty.call(message, "broadcast_attempts"))
                if (!$util.isInteger(message.broadcast_attempts))
                    return "broadcast_attempts: integer expected";
            if (message.next_broadcast_height != null && $Object.hasOwnProperty.call(message, "next_broadcast_height"))
                if (!$util.isInteger(message.next_broadcast_height))
                    return "next_broadcast_height: integer expected";
            if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                if (typeof message.force !== "boolean")
                    return "force: boolean expected";
            if (message.requested_conf_target != null && $Object.hasOwnProperty.call(message, "requested_conf_target"))
                if (!$util.isInteger(message.requested_conf_target))
                    return "requested_conf_target: integer expected";
            if (message.requested_sat_per_byte != null && $Object.hasOwnProperty.call(message, "requested_sat_per_byte"))
                if (!$util.isInteger(message.requested_sat_per_byte))
                    return "requested_sat_per_byte: integer expected";
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (!$util.isInteger(message.sat_per_vbyte) && !(message.sat_per_vbyte && $util.isInteger(message.sat_per_vbyte.low) && $util.isInteger(message.sat_per_vbyte.high)))
                    return "sat_per_vbyte: integer|Long expected";
            if (message.requested_sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "requested_sat_per_vbyte"))
                if (!$util.isInteger(message.requested_sat_per_vbyte) && !(message.requested_sat_per_vbyte && $util.isInteger(message.requested_sat_per_vbyte.low) && $util.isInteger(message.requested_sat_per_vbyte.high)))
                    return "requested_sat_per_vbyte: integer|Long expected";
            if (message.immediate != null && $Object.hasOwnProperty.call(message, "immediate"))
                if (typeof message.immediate !== "boolean")
                    return "immediate: boolean expected";
            if (message.budget != null && $Object.hasOwnProperty.call(message, "budget"))
                if (!$util.isInteger(message.budget) && !(message.budget && $util.isInteger(message.budget.low) && $util.isInteger(message.budget.high)))
                    return "budget: integer|Long expected";
            if (message.deadline_height != null && $Object.hasOwnProperty.call(message, "deadline_height"))
                if (!$util.isInteger(message.deadline_height))
                    return "deadline_height: integer expected";
            if (message.maturity_height != null && $Object.hasOwnProperty.call(message, "maturity_height"))
                if (!$util.isInteger(message.maturity_height))
                    return "maturity_height: integer expected";
            return null;
        };

        /**
         * Creates a PendingSweep message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.PendingSweep
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.PendingSweep} PendingSweep
         */
        PendingSweep.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.PendingSweep)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.PendingSweep: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.PendingSweep();
            if (object.outpoint != null) {
                if (!$util.isObject(object.outpoint))
                    throw $TypeError(".walletrpc.PendingSweep.outpoint: object expected");
                message.outpoint = $root.lnrpc.OutPoint.fromObject(object.outpoint, _depth + 1);
            }
            if (object.witness_type !== 0 && (typeof object.witness_type !== "string" || $root.walletrpc.WitnessType[object.witness_type] !== 0))
                switch (object.witness_type) {
                case "UNKNOWN_WITNESS":
                case 0:
                    message.witness_type = 0;
                    break;
                case "COMMITMENT_TIME_LOCK":
                case 1:
                    message.witness_type = 1;
                    break;
                case "COMMITMENT_NO_DELAY":
                case 2:
                    message.witness_type = 2;
                    break;
                case "COMMITMENT_REVOKE":
                case 3:
                    message.witness_type = 3;
                    break;
                case "HTLC_OFFERED_REVOKE":
                case 4:
                    message.witness_type = 4;
                    break;
                case "HTLC_ACCEPTED_REVOKE":
                case 5:
                    message.witness_type = 5;
                    break;
                case "HTLC_OFFERED_TIMEOUT_SECOND_LEVEL":
                case 6:
                    message.witness_type = 6;
                    break;
                case "HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL":
                case 7:
                    message.witness_type = 7;
                    break;
                case "HTLC_OFFERED_REMOTE_TIMEOUT":
                case 8:
                    message.witness_type = 8;
                    break;
                case "HTLC_ACCEPTED_REMOTE_SUCCESS":
                case 9:
                    message.witness_type = 9;
                    break;
                case "HTLC_SECOND_LEVEL_REVOKE":
                case 10:
                    message.witness_type = 10;
                    break;
                case "WITNESS_KEY_HASH":
                case 11:
                    message.witness_type = 11;
                    break;
                case "NESTED_WITNESS_KEY_HASH":
                case 12:
                    message.witness_type = 12;
                    break;
                case "COMMITMENT_ANCHOR":
                case 13:
                    message.witness_type = 13;
                    break;
                case "COMMITMENT_NO_DELAY_TWEAKLESS":
                case 14:
                    message.witness_type = 14;
                    break;
                case "COMMITMENT_TO_REMOTE_CONFIRMED":
                case 15:
                    message.witness_type = 15;
                    break;
                case "HTLC_OFFERED_TIMEOUT_SECOND_LEVEL_INPUT_CONFIRMED":
                case 16:
                    message.witness_type = 16;
                    break;
                case "HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL_INPUT_CONFIRMED":
                case 17:
                    message.witness_type = 17;
                    break;
                case "LEASE_COMMITMENT_TIME_LOCK":
                case 18:
                    message.witness_type = 18;
                    break;
                case "LEASE_COMMITMENT_TO_REMOTE_CONFIRMED":
                case 19:
                    message.witness_type = 19;
                    break;
                case "LEASE_HTLC_OFFERED_TIMEOUT_SECOND_LEVEL":
                case 20:
                    message.witness_type = 20;
                    break;
                case "LEASE_HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL":
                case 21:
                    message.witness_type = 21;
                    break;
                case "TAPROOT_PUB_KEY_SPEND":
                case 22:
                    message.witness_type = 22;
                    break;
                case "TAPROOT_LOCAL_COMMIT_SPEND":
                case 23:
                    message.witness_type = 23;
                    break;
                case "TAPROOT_REMOTE_COMMIT_SPEND":
                case 24:
                    message.witness_type = 24;
                    break;
                case "TAPROOT_ANCHOR_SWEEP_SPEND":
                case 25:
                    message.witness_type = 25;
                    break;
                case "TAPROOT_HTLC_OFFERED_TIMEOUT_SECOND_LEVEL":
                case 26:
                    message.witness_type = 26;
                    break;
                case "TAPROOT_HTLC_ACCEPTED_SUCCESS_SECOND_LEVEL":
                case 27:
                    message.witness_type = 27;
                    break;
                case "TAPROOT_HTLC_SECOND_LEVEL_REVOKE":
                case 28:
                    message.witness_type = 28;
                    break;
                case "TAPROOT_HTLC_ACCEPTED_REVOKE":
                case 29:
                    message.witness_type = 29;
                    break;
                case "TAPROOT_HTLC_OFFERED_REVOKE":
                case 30:
                    message.witness_type = 30;
                    break;
                case "TAPROOT_HTLC_OFFERED_REMOTE_TIMEOUT":
                case 31:
                    message.witness_type = 31;
                    break;
                case "TAPROOT_HTLC_LOCAL_OFFERED_TIMEOUT":
                case 32:
                    message.witness_type = 32;
                    break;
                case "TAPROOT_HTLC_ACCEPTED_REMOTE_SUCCESS":
                case 33:
                    message.witness_type = 33;
                    break;
                case "TAPROOT_HTLC_ACCEPTED_LOCAL_SUCCESS":
                case 34:
                    message.witness_type = 34;
                    break;
                case "TAPROOT_COMMITMENT_REVOKE":
                case 35:
                    message.witness_type = 35;
                    break;
                default:
                    if (typeof object.witness_type === "number" && (object.witness_type | 0) === object.witness_type)
                        message.witness_type = object.witness_type;
                }
            if (object.amount_sat != null)
                if ($Number(object.amount_sat) !== 0)
                    message.amount_sat = object.amount_sat >>> 0;
            if (object.sat_per_byte != null)
                if ($Number(object.sat_per_byte) !== 0)
                    message.sat_per_byte = object.sat_per_byte >>> 0;
            if (object.broadcast_attempts != null)
                if ($Number(object.broadcast_attempts) !== 0)
                    message.broadcast_attempts = object.broadcast_attempts >>> 0;
            if (object.next_broadcast_height != null)
                if ($Number(object.next_broadcast_height) !== 0)
                    message.next_broadcast_height = object.next_broadcast_height >>> 0;
            if (object.force != null)
                if (object.force)
                    message.force = $Boolean(object.force);
            if (object.requested_conf_target != null)
                if ($Number(object.requested_conf_target) !== 0)
                    message.requested_conf_target = object.requested_conf_target >>> 0;
            if (object.requested_sat_per_byte != null)
                if ($Number(object.requested_sat_per_byte) !== 0)
                    message.requested_sat_per_byte = object.requested_sat_per_byte >>> 0;
            if (object.sat_per_vbyte != null)
                if (typeof object.sat_per_vbyte === "object" ? object.sat_per_vbyte.low || object.sat_per_vbyte.high : $Number(object.sat_per_vbyte) !== 0)
                    if ($util.Long)
                        message.sat_per_vbyte = $util.Long.fromValue(object.sat_per_vbyte, true);
                    else if (typeof object.sat_per_vbyte === "string")
                        message.sat_per_vbyte = $parseInt(object.sat_per_vbyte, 10);
                    else if (typeof object.sat_per_vbyte === "number")
                        message.sat_per_vbyte = object.sat_per_vbyte;
                    else if (typeof object.sat_per_vbyte === "object")
                        message.sat_per_vbyte = new $util.LongBits(object.sat_per_vbyte.low >>> 0, object.sat_per_vbyte.high >>> 0).toNumber(true);
            if (object.requested_sat_per_vbyte != null)
                if (typeof object.requested_sat_per_vbyte === "object" ? object.requested_sat_per_vbyte.low || object.requested_sat_per_vbyte.high : $Number(object.requested_sat_per_vbyte) !== 0)
                    if ($util.Long)
                        message.requested_sat_per_vbyte = $util.Long.fromValue(object.requested_sat_per_vbyte, true);
                    else if (typeof object.requested_sat_per_vbyte === "string")
                        message.requested_sat_per_vbyte = $parseInt(object.requested_sat_per_vbyte, 10);
                    else if (typeof object.requested_sat_per_vbyte === "number")
                        message.requested_sat_per_vbyte = object.requested_sat_per_vbyte;
                    else if (typeof object.requested_sat_per_vbyte === "object")
                        message.requested_sat_per_vbyte = new $util.LongBits(object.requested_sat_per_vbyte.low >>> 0, object.requested_sat_per_vbyte.high >>> 0).toNumber(true);
            if (object.immediate != null)
                if (object.immediate)
                    message.immediate = $Boolean(object.immediate);
            if (object.budget != null)
                if (typeof object.budget === "object" ? object.budget.low || object.budget.high : $Number(object.budget) !== 0)
                    if ($util.Long)
                        message.budget = $util.Long.fromValue(object.budget, true);
                    else if (typeof object.budget === "string")
                        message.budget = $parseInt(object.budget, 10);
                    else if (typeof object.budget === "number")
                        message.budget = object.budget;
                    else if (typeof object.budget === "object")
                        message.budget = new $util.LongBits(object.budget.low >>> 0, object.budget.high >>> 0).toNumber(true);
            if (object.deadline_height != null)
                if ($Number(object.deadline_height) !== 0)
                    message.deadline_height = object.deadline_height >>> 0;
            if (object.maturity_height != null)
                if ($Number(object.maturity_height) !== 0)
                    message.maturity_height = object.maturity_height >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a PendingSweep message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.PendingSweep
         * @static
         * @param {walletrpc.PendingSweep} message PendingSweep
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingSweep.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.outpoint = null;
                object.witness_type = options.enums === $String ? "UNKNOWN_WITNESS" : 0;
                object.amount_sat = 0;
                object.sat_per_byte = 0;
                object.broadcast_attempts = 0;
                object.next_broadcast_height = 0;
                object.force = false;
                object.requested_conf_target = 0;
                object.requested_sat_per_byte = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sat_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.requested_sat_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.requested_sat_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.immediate = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.budget = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.budget = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.deadline_height = 0;
                object.maturity_height = 0;
            }
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = $root.lnrpc.OutPoint.toObject(message.outpoint, options, _depth + 1);
            if (message.witness_type != null && $Object.hasOwnProperty.call(message, "witness_type"))
                object.witness_type = options.enums === $String ? $root.walletrpc.WitnessType[message.witness_type] === $undefined ? message.witness_type : $root.walletrpc.WitnessType[message.witness_type] : message.witness_type;
            if (message.amount_sat != null && $Object.hasOwnProperty.call(message, "amount_sat"))
                object.amount_sat = message.amount_sat;
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                object.sat_per_byte = message.sat_per_byte;
            if (message.broadcast_attempts != null && $Object.hasOwnProperty.call(message, "broadcast_attempts"))
                object.broadcast_attempts = message.broadcast_attempts;
            if (message.next_broadcast_height != null && $Object.hasOwnProperty.call(message, "next_broadcast_height"))
                object.next_broadcast_height = message.next_broadcast_height;
            if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                object.force = message.force;
            if (message.requested_conf_target != null && $Object.hasOwnProperty.call(message, "requested_conf_target"))
                object.requested_conf_target = message.requested_conf_target;
            if (message.requested_sat_per_byte != null && $Object.hasOwnProperty.call(message, "requested_sat_per_byte"))
                object.requested_sat_per_byte = message.requested_sat_per_byte;
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_vbyte = typeof message.sat_per_vbyte === "number" ? $BigInt(message.sat_per_vbyte) : $util.Long.fromBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.sat_per_vbyte === "number")
                    object.sat_per_vbyte = options.longs === $String ? $String(message.sat_per_vbyte) : message.sat_per_vbyte;
                else
                    object.sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0).toNumber(true) : message.sat_per_vbyte;
            if (message.requested_sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "requested_sat_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.requested_sat_per_vbyte = typeof message.requested_sat_per_vbyte === "number" ? $BigInt(message.requested_sat_per_vbyte) : $util.Long.fromBits(message.requested_sat_per_vbyte.low >>> 0, message.requested_sat_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.requested_sat_per_vbyte === "number")
                    object.requested_sat_per_vbyte = options.longs === $String ? $String(message.requested_sat_per_vbyte) : message.requested_sat_per_vbyte;
                else
                    object.requested_sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.requested_sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.requested_sat_per_vbyte.low >>> 0, message.requested_sat_per_vbyte.high >>> 0).toNumber(true) : message.requested_sat_per_vbyte;
            if (message.immediate != null && $Object.hasOwnProperty.call(message, "immediate"))
                object.immediate = message.immediate;
            if (message.budget != null && $Object.hasOwnProperty.call(message, "budget"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.budget = typeof message.budget === "number" ? $BigInt(message.budget) : $util.Long.fromBits(message.budget.low >>> 0, message.budget.high >>> 0, true).toBigInt();
                else if (typeof message.budget === "number")
                    object.budget = options.longs === $String ? $String(message.budget) : message.budget;
                else
                    object.budget = options.longs === $String ? $util.Long.prototype.toString.call(message.budget) : options.longs === $Number ? new $util.LongBits(message.budget.low >>> 0, message.budget.high >>> 0).toNumber(true) : message.budget;
            if (message.deadline_height != null && $Object.hasOwnProperty.call(message, "deadline_height"))
                object.deadline_height = message.deadline_height;
            if (message.maturity_height != null && $Object.hasOwnProperty.call(message, "maturity_height"))
                object.maturity_height = message.maturity_height;
            return object;
        };

        /**
         * Converts this PendingSweep to JSON.
         * @function toJSON
         * @memberof walletrpc.PendingSweep
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingSweep.prototype.toJSON = function() {
            return PendingSweep.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingSweep
         * @function getTypeUrl
         * @memberof walletrpc.PendingSweep
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingSweep.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.PendingSweep";
        };

        return PendingSweep;
    })();

    walletrpc.PendingSweepsRequest = (function() {

        /**
         * Properties of a PendingSweepsRequest.
         * @typedef {Object} walletrpc.PendingSweepsRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PendingSweepsRequest.
         * @memberof walletrpc
         * @interface IPendingSweepsRequest
         * @augments walletrpc.PendingSweepsRequest.$Properties
         * @deprecated Use walletrpc.PendingSweepsRequest.$Properties instead.
         */

        /**
         * Shape of a PendingSweepsRequest.
         * @typedef {walletrpc.PendingSweepsRequest.$Properties} walletrpc.PendingSweepsRequest.$Shape
         */

        /**
         * Constructs a new PendingSweepsRequest.
         * @memberof walletrpc
         * @classdesc Represents a PendingSweepsRequest.
         * @constructor
         * @param {walletrpc.PendingSweepsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PendingSweepsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new PendingSweepsRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.PendingSweepsRequest
         * @static
         * @param {walletrpc.PendingSweepsRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.PendingSweepsRequest} PendingSweepsRequest instance
         * @type {{
         *   (properties: walletrpc.PendingSweepsRequest.$Shape): walletrpc.PendingSweepsRequest & walletrpc.PendingSweepsRequest.$Shape;
         *   (properties?: walletrpc.PendingSweepsRequest.$Properties): walletrpc.PendingSweepsRequest;
         * }}
         */
        PendingSweepsRequest.create = function(properties) {
            return new PendingSweepsRequest(properties);
        };

        /**
         * Encodes the specified PendingSweepsRequest message. Does not implicitly {@link walletrpc.PendingSweepsRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.PendingSweepsRequest
         * @static
         * @param {walletrpc.PendingSweepsRequest.$Properties} message PendingSweepsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingSweepsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingSweepsRequest message, length delimited. Does not implicitly {@link walletrpc.PendingSweepsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.PendingSweepsRequest
         * @static
         * @param {walletrpc.PendingSweepsRequest.$Properties} message PendingSweepsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingSweepsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PendingSweepsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.PendingSweepsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.PendingSweepsRequest & walletrpc.PendingSweepsRequest.$Shape} PendingSweepsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingSweepsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.PendingSweepsRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingSweepsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.PendingSweepsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.PendingSweepsRequest & walletrpc.PendingSweepsRequest.$Shape} PendingSweepsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingSweepsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingSweepsRequest message.
         * @function verify
         * @memberof walletrpc.PendingSweepsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingSweepsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a PendingSweepsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.PendingSweepsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.PendingSweepsRequest} PendingSweepsRequest
         */
        PendingSweepsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.PendingSweepsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.PendingSweepsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.walletrpc.PendingSweepsRequest();
        };

        /**
         * Creates a plain object from a PendingSweepsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.PendingSweepsRequest
         * @static
         * @param {walletrpc.PendingSweepsRequest} message PendingSweepsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingSweepsRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this PendingSweepsRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.PendingSweepsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingSweepsRequest.prototype.toJSON = function() {
            return PendingSweepsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingSweepsRequest
         * @function getTypeUrl
         * @memberof walletrpc.PendingSweepsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingSweepsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.PendingSweepsRequest";
        };

        return PendingSweepsRequest;
    })();

    walletrpc.PendingSweepsResponse = (function() {

        /**
         * Properties of a PendingSweepsResponse.
         * @typedef {Object} walletrpc.PendingSweepsResponse.$Properties
         * @property {Array.<walletrpc.PendingSweep.$Properties>|null} [pending_sweeps] PendingSweepsResponse pending_sweeps
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PendingSweepsResponse.
         * @memberof walletrpc
         * @interface IPendingSweepsResponse
         * @augments walletrpc.PendingSweepsResponse.$Properties
         * @deprecated Use walletrpc.PendingSweepsResponse.$Properties instead.
         */

        /**
         * Shape of a PendingSweepsResponse.
         * @typedef {walletrpc.PendingSweepsResponse.$Properties} walletrpc.PendingSweepsResponse.$Shape
         */

        /**
         * Constructs a new PendingSweepsResponse.
         * @memberof walletrpc
         * @classdesc Represents a PendingSweepsResponse.
         * @constructor
         * @param {walletrpc.PendingSweepsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PendingSweepsResponse = function (properties) {
            this.pending_sweeps = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PendingSweepsResponse pending_sweeps.
         * @member {Array.<walletrpc.PendingSweep.$Properties>} pending_sweeps
         * @memberof walletrpc.PendingSweepsResponse
         * @instance
         */
        PendingSweepsResponse.prototype.pending_sweeps = $util.emptyArray;

        /**
         * Creates a new PendingSweepsResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.PendingSweepsResponse
         * @static
         * @param {walletrpc.PendingSweepsResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.PendingSweepsResponse} PendingSweepsResponse instance
         * @type {{
         *   (properties: walletrpc.PendingSweepsResponse.$Shape): walletrpc.PendingSweepsResponse & walletrpc.PendingSweepsResponse.$Shape;
         *   (properties?: walletrpc.PendingSweepsResponse.$Properties): walletrpc.PendingSweepsResponse;
         * }}
         */
        PendingSweepsResponse.create = function(properties) {
            return new PendingSweepsResponse(properties);
        };

        /**
         * Encodes the specified PendingSweepsResponse message. Does not implicitly {@link walletrpc.PendingSweepsResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.PendingSweepsResponse
         * @static
         * @param {walletrpc.PendingSweepsResponse.$Properties} message PendingSweepsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingSweepsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pending_sweeps != null && message.pending_sweeps.length)
                for (let i = 0; i < message.pending_sweeps.length; ++i)
                    $root.walletrpc.PendingSweep.encode(message.pending_sweeps[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingSweepsResponse message, length delimited. Does not implicitly {@link walletrpc.PendingSweepsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.PendingSweepsResponse
         * @static
         * @param {walletrpc.PendingSweepsResponse.$Properties} message PendingSweepsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingSweepsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PendingSweepsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.PendingSweepsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.PendingSweepsResponse & walletrpc.PendingSweepsResponse.$Shape} PendingSweepsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingSweepsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.PendingSweepsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pending_sweeps && message.pending_sweeps.length))
                            message.pending_sweeps = [];
                        message.pending_sweeps.push($root.walletrpc.PendingSweep.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingSweepsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.PendingSweepsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.PendingSweepsResponse & walletrpc.PendingSweepsResponse.$Shape} PendingSweepsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingSweepsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingSweepsResponse message.
         * @function verify
         * @memberof walletrpc.PendingSweepsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingSweepsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pending_sweeps != null && $Object.hasOwnProperty.call(message, "pending_sweeps")) {
                if (!$Array.isArray(message.pending_sweeps))
                    return "pending_sweeps: array expected";
                for (let i = 0; i < message.pending_sweeps.length; ++i) {
                    let error = $root.walletrpc.PendingSweep.verify(message.pending_sweeps[i], _depth + 1);
                    if (error)
                        return "pending_sweeps." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PendingSweepsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.PendingSweepsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.PendingSweepsResponse} PendingSweepsResponse
         */
        PendingSweepsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.PendingSweepsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.PendingSweepsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.PendingSweepsResponse();
            if (object.pending_sweeps) {
                if (!$Array.isArray(object.pending_sweeps))
                    throw $TypeError(".walletrpc.PendingSweepsResponse.pending_sweeps: array expected");
                message.pending_sweeps = $Array(object.pending_sweeps.length);
                for (let i = 0; i < object.pending_sweeps.length; ++i) {
                    if (!$util.isObject(object.pending_sweeps[i]))
                        throw $TypeError(".walletrpc.PendingSweepsResponse.pending_sweeps: object expected");
                    message.pending_sweeps[i] = $root.walletrpc.PendingSweep.fromObject(object.pending_sweeps[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PendingSweepsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.PendingSweepsResponse
         * @static
         * @param {walletrpc.PendingSweepsResponse} message PendingSweepsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingSweepsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.pending_sweeps = [];
            if (message.pending_sweeps && message.pending_sweeps.length) {
                object.pending_sweeps = $Array(message.pending_sweeps.length);
                for (let j = 0; j < message.pending_sweeps.length; ++j)
                    object.pending_sweeps[j] = $root.walletrpc.PendingSweep.toObject(message.pending_sweeps[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this PendingSweepsResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.PendingSweepsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingSweepsResponse.prototype.toJSON = function() {
            return PendingSweepsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingSweepsResponse
         * @function getTypeUrl
         * @memberof walletrpc.PendingSweepsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingSweepsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.PendingSweepsResponse";
        };

        return PendingSweepsResponse;
    })();

    walletrpc.BumpFeeRequest = (function() {

        /**
         * Properties of a BumpFeeRequest.
         * @typedef {Object} walletrpc.BumpFeeRequest.$Properties
         * @property {lnrpc.OutPoint.$Properties|null} [outpoint] BumpFeeRequest outpoint
         * @property {number|null} [target_conf] BumpFeeRequest target_conf
         * @property {number|null} [sat_per_byte] BumpFeeRequest sat_per_byte
         * @property {boolean|null} [force] BumpFeeRequest force
         * @property {Long|null} [sat_per_vbyte] BumpFeeRequest sat_per_vbyte
         * @property {boolean|null} [immediate] BumpFeeRequest immediate
         * @property {Long|null} [budget] BumpFeeRequest budget
         * @property {number|null} [deadline_delta] BumpFeeRequest deadline_delta
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BumpFeeRequest.
         * @memberof walletrpc
         * @interface IBumpFeeRequest
         * @augments walletrpc.BumpFeeRequest.$Properties
         * @deprecated Use walletrpc.BumpFeeRequest.$Properties instead.
         */

        /**
         * Shape of a BumpFeeRequest.
         * @typedef {walletrpc.BumpFeeRequest.$Properties} walletrpc.BumpFeeRequest.$Shape
         */

        /**
         * Constructs a new BumpFeeRequest.
         * @memberof walletrpc
         * @classdesc Represents a BumpFeeRequest.
         * @constructor
         * @param {walletrpc.BumpFeeRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BumpFeeRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BumpFeeRequest outpoint.
         * @member {lnrpc.OutPoint.$Properties|null|undefined} outpoint
         * @memberof walletrpc.BumpFeeRequest
         * @instance
         */
        BumpFeeRequest.prototype.outpoint = null;

        /**
         * BumpFeeRequest target_conf.
         * @member {number} target_conf
         * @memberof walletrpc.BumpFeeRequest
         * @instance
         */
        BumpFeeRequest.prototype.target_conf = 0;

        /**
         * BumpFeeRequest sat_per_byte.
         * @member {number} sat_per_byte
         * @memberof walletrpc.BumpFeeRequest
         * @instance
         */
        BumpFeeRequest.prototype.sat_per_byte = 0;

        /**
         * BumpFeeRequest force.
         * @member {boolean} force
         * @memberof walletrpc.BumpFeeRequest
         * @instance
         */
        BumpFeeRequest.prototype.force = false;

        /**
         * BumpFeeRequest sat_per_vbyte.
         * @member {Long} sat_per_vbyte
         * @memberof walletrpc.BumpFeeRequest
         * @instance
         */
        BumpFeeRequest.prototype.sat_per_vbyte = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BumpFeeRequest immediate.
         * @member {boolean} immediate
         * @memberof walletrpc.BumpFeeRequest
         * @instance
         */
        BumpFeeRequest.prototype.immediate = false;

        /**
         * BumpFeeRequest budget.
         * @member {Long} budget
         * @memberof walletrpc.BumpFeeRequest
         * @instance
         */
        BumpFeeRequest.prototype.budget = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BumpFeeRequest deadline_delta.
         * @member {number} deadline_delta
         * @memberof walletrpc.BumpFeeRequest
         * @instance
         */
        BumpFeeRequest.prototype.deadline_delta = 0;

        /**
         * Creates a new BumpFeeRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.BumpFeeRequest
         * @static
         * @param {walletrpc.BumpFeeRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.BumpFeeRequest} BumpFeeRequest instance
         * @type {{
         *   (properties: walletrpc.BumpFeeRequest.$Shape): walletrpc.BumpFeeRequest & walletrpc.BumpFeeRequest.$Shape;
         *   (properties?: walletrpc.BumpFeeRequest.$Properties): walletrpc.BumpFeeRequest;
         * }}
         */
        BumpFeeRequest.create = function(properties) {
            return new BumpFeeRequest(properties);
        };

        /**
         * Encodes the specified BumpFeeRequest message. Does not implicitly {@link walletrpc.BumpFeeRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.BumpFeeRequest
         * @static
         * @param {walletrpc.BumpFeeRequest.$Properties} message BumpFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BumpFeeRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                $root.lnrpc.OutPoint.encode(message.outpoint, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf") && message.target_conf !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.target_conf);
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte") && message.sat_per_byte !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.sat_per_byte);
            if (message.force != null && $Object.hasOwnProperty.call(message, "force") && message.force !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.force);
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte") && (typeof message.sat_per_vbyte === "object" ? message.sat_per_vbyte.low || message.sat_per_vbyte.high : message.sat_per_vbyte !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.sat_per_vbyte);
            if (message.immediate != null && $Object.hasOwnProperty.call(message, "immediate") && message.immediate !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.immediate);
            if (message.budget != null && $Object.hasOwnProperty.call(message, "budget") && (typeof message.budget === "object" ? message.budget.low || message.budget.high : message.budget !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.budget);
            if (message.deadline_delta != null && $Object.hasOwnProperty.call(message, "deadline_delta") && message.deadline_delta !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.deadline_delta);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BumpFeeRequest message, length delimited. Does not implicitly {@link walletrpc.BumpFeeRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.BumpFeeRequest
         * @static
         * @param {walletrpc.BumpFeeRequest.$Properties} message BumpFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BumpFeeRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BumpFeeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.BumpFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.BumpFeeRequest & walletrpc.BumpFeeRequest.$Shape} BumpFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BumpFeeRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.BumpFeeRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.outpoint = $root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.outpoint);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_conf = value;
                        else
                            delete message.target_conf;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sat_per_byte = value;
                        else
                            delete message.sat_per_byte;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.force = value;
                        else
                            delete message.force;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sat_per_vbyte = value;
                        else
                            delete message.sat_per_vbyte;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.immediate = value;
                        else
                            delete message.immediate;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.budget = value;
                        else
                            delete message.budget;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.deadline_delta = value;
                        else
                            delete message.deadline_delta;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BumpFeeRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.BumpFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.BumpFeeRequest & walletrpc.BumpFeeRequest.$Shape} BumpFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BumpFeeRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BumpFeeRequest message.
         * @function verify
         * @memberof walletrpc.BumpFeeRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BumpFeeRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint")) {
                let error = $root.lnrpc.OutPoint.verify(message.outpoint, _depth + 1);
                if (error)
                    return "outpoint." + error;
            }
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                if (!$util.isInteger(message.target_conf))
                    return "target_conf: integer expected";
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                if (!$util.isInteger(message.sat_per_byte))
                    return "sat_per_byte: integer expected";
            if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                if (typeof message.force !== "boolean")
                    return "force: boolean expected";
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (!$util.isInteger(message.sat_per_vbyte) && !(message.sat_per_vbyte && $util.isInteger(message.sat_per_vbyte.low) && $util.isInteger(message.sat_per_vbyte.high)))
                    return "sat_per_vbyte: integer|Long expected";
            if (message.immediate != null && $Object.hasOwnProperty.call(message, "immediate"))
                if (typeof message.immediate !== "boolean")
                    return "immediate: boolean expected";
            if (message.budget != null && $Object.hasOwnProperty.call(message, "budget"))
                if (!$util.isInteger(message.budget) && !(message.budget && $util.isInteger(message.budget.low) && $util.isInteger(message.budget.high)))
                    return "budget: integer|Long expected";
            if (message.deadline_delta != null && $Object.hasOwnProperty.call(message, "deadline_delta"))
                if (!$util.isInteger(message.deadline_delta))
                    return "deadline_delta: integer expected";
            return null;
        };

        /**
         * Creates a BumpFeeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.BumpFeeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.BumpFeeRequest} BumpFeeRequest
         */
        BumpFeeRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.BumpFeeRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.BumpFeeRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.BumpFeeRequest();
            if (object.outpoint != null) {
                if (!$util.isObject(object.outpoint))
                    throw $TypeError(".walletrpc.BumpFeeRequest.outpoint: object expected");
                message.outpoint = $root.lnrpc.OutPoint.fromObject(object.outpoint, _depth + 1);
            }
            if (object.target_conf != null)
                if ($Number(object.target_conf) !== 0)
                    message.target_conf = object.target_conf >>> 0;
            if (object.sat_per_byte != null)
                if ($Number(object.sat_per_byte) !== 0)
                    message.sat_per_byte = object.sat_per_byte >>> 0;
            if (object.force != null)
                if (object.force)
                    message.force = $Boolean(object.force);
            if (object.sat_per_vbyte != null)
                if (typeof object.sat_per_vbyte === "object" ? object.sat_per_vbyte.low || object.sat_per_vbyte.high : $Number(object.sat_per_vbyte) !== 0)
                    if ($util.Long)
                        message.sat_per_vbyte = $util.Long.fromValue(object.sat_per_vbyte, true);
                    else if (typeof object.sat_per_vbyte === "string")
                        message.sat_per_vbyte = $parseInt(object.sat_per_vbyte, 10);
                    else if (typeof object.sat_per_vbyte === "number")
                        message.sat_per_vbyte = object.sat_per_vbyte;
                    else if (typeof object.sat_per_vbyte === "object")
                        message.sat_per_vbyte = new $util.LongBits(object.sat_per_vbyte.low >>> 0, object.sat_per_vbyte.high >>> 0).toNumber(true);
            if (object.immediate != null)
                if (object.immediate)
                    message.immediate = $Boolean(object.immediate);
            if (object.budget != null)
                if (typeof object.budget === "object" ? object.budget.low || object.budget.high : $Number(object.budget) !== 0)
                    if ($util.Long)
                        message.budget = $util.Long.fromValue(object.budget, true);
                    else if (typeof object.budget === "string")
                        message.budget = $parseInt(object.budget, 10);
                    else if (typeof object.budget === "number")
                        message.budget = object.budget;
                    else if (typeof object.budget === "object")
                        message.budget = new $util.LongBits(object.budget.low >>> 0, object.budget.high >>> 0).toNumber(true);
            if (object.deadline_delta != null)
                if ($Number(object.deadline_delta) !== 0)
                    message.deadline_delta = object.deadline_delta >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a BumpFeeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.BumpFeeRequest
         * @static
         * @param {walletrpc.BumpFeeRequest} message BumpFeeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BumpFeeRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.outpoint = null;
                object.target_conf = 0;
                object.sat_per_byte = 0;
                object.force = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sat_per_vbyte = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sat_per_vbyte = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.immediate = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.budget = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.budget = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.deadline_delta = 0;
            }
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = $root.lnrpc.OutPoint.toObject(message.outpoint, options, _depth + 1);
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                object.target_conf = message.target_conf;
            if (message.sat_per_byte != null && $Object.hasOwnProperty.call(message, "sat_per_byte"))
                object.sat_per_byte = message.sat_per_byte;
            if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                object.force = message.force;
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_vbyte = typeof message.sat_per_vbyte === "number" ? $BigInt(message.sat_per_vbyte) : $util.Long.fromBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.sat_per_vbyte === "number")
                    object.sat_per_vbyte = options.longs === $String ? $String(message.sat_per_vbyte) : message.sat_per_vbyte;
                else
                    object.sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0).toNumber(true) : message.sat_per_vbyte;
            if (message.immediate != null && $Object.hasOwnProperty.call(message, "immediate"))
                object.immediate = message.immediate;
            if (message.budget != null && $Object.hasOwnProperty.call(message, "budget"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.budget = typeof message.budget === "number" ? $BigInt(message.budget) : $util.Long.fromBits(message.budget.low >>> 0, message.budget.high >>> 0, true).toBigInt();
                else if (typeof message.budget === "number")
                    object.budget = options.longs === $String ? $String(message.budget) : message.budget;
                else
                    object.budget = options.longs === $String ? $util.Long.prototype.toString.call(message.budget) : options.longs === $Number ? new $util.LongBits(message.budget.low >>> 0, message.budget.high >>> 0).toNumber(true) : message.budget;
            if (message.deadline_delta != null && $Object.hasOwnProperty.call(message, "deadline_delta"))
                object.deadline_delta = message.deadline_delta;
            return object;
        };

        /**
         * Converts this BumpFeeRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.BumpFeeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BumpFeeRequest.prototype.toJSON = function() {
            return BumpFeeRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BumpFeeRequest
         * @function getTypeUrl
         * @memberof walletrpc.BumpFeeRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BumpFeeRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.BumpFeeRequest";
        };

        return BumpFeeRequest;
    })();

    walletrpc.BumpFeeResponse = (function() {

        /**
         * Properties of a BumpFeeResponse.
         * @typedef {Object} walletrpc.BumpFeeResponse.$Properties
         * @property {string|null} [status] BumpFeeResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BumpFeeResponse.
         * @memberof walletrpc
         * @interface IBumpFeeResponse
         * @augments walletrpc.BumpFeeResponse.$Properties
         * @deprecated Use walletrpc.BumpFeeResponse.$Properties instead.
         */

        /**
         * Shape of a BumpFeeResponse.
         * @typedef {walletrpc.BumpFeeResponse.$Properties} walletrpc.BumpFeeResponse.$Shape
         */

        /**
         * Constructs a new BumpFeeResponse.
         * @memberof walletrpc
         * @classdesc Represents a BumpFeeResponse.
         * @constructor
         * @param {walletrpc.BumpFeeResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BumpFeeResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BumpFeeResponse status.
         * @member {string} status
         * @memberof walletrpc.BumpFeeResponse
         * @instance
         */
        BumpFeeResponse.prototype.status = "";

        /**
         * Creates a new BumpFeeResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.BumpFeeResponse
         * @static
         * @param {walletrpc.BumpFeeResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.BumpFeeResponse} BumpFeeResponse instance
         * @type {{
         *   (properties: walletrpc.BumpFeeResponse.$Shape): walletrpc.BumpFeeResponse & walletrpc.BumpFeeResponse.$Shape;
         *   (properties?: walletrpc.BumpFeeResponse.$Properties): walletrpc.BumpFeeResponse;
         * }}
         */
        BumpFeeResponse.create = function(properties) {
            return new BumpFeeResponse(properties);
        };

        /**
         * Encodes the specified BumpFeeResponse message. Does not implicitly {@link walletrpc.BumpFeeResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.BumpFeeResponse
         * @static
         * @param {walletrpc.BumpFeeResponse.$Properties} message BumpFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BumpFeeResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BumpFeeResponse message, length delimited. Does not implicitly {@link walletrpc.BumpFeeResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.BumpFeeResponse
         * @static
         * @param {walletrpc.BumpFeeResponse.$Properties} message BumpFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BumpFeeResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BumpFeeResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.BumpFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.BumpFeeResponse & walletrpc.BumpFeeResponse.$Shape} BumpFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BumpFeeResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.BumpFeeResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BumpFeeResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.BumpFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.BumpFeeResponse & walletrpc.BumpFeeResponse.$Shape} BumpFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BumpFeeResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BumpFeeResponse message.
         * @function verify
         * @memberof walletrpc.BumpFeeResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BumpFeeResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a BumpFeeResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.BumpFeeResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.BumpFeeResponse} BumpFeeResponse
         */
        BumpFeeResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.BumpFeeResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.BumpFeeResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.BumpFeeResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a BumpFeeResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.BumpFeeResponse
         * @static
         * @param {walletrpc.BumpFeeResponse} message BumpFeeResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BumpFeeResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this BumpFeeResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.BumpFeeResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BumpFeeResponse.prototype.toJSON = function() {
            return BumpFeeResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BumpFeeResponse
         * @function getTypeUrl
         * @memberof walletrpc.BumpFeeResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BumpFeeResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.BumpFeeResponse";
        };

        return BumpFeeResponse;
    })();

    walletrpc.BumpForceCloseFeeRequest = (function() {

        /**
         * Properties of a BumpForceCloseFeeRequest.
         * @typedef {Object} walletrpc.BumpForceCloseFeeRequest.$Properties
         * @property {lnrpc.ChannelPoint.$Properties|null} [chan_point] BumpForceCloseFeeRequest chan_point
         * @property {number|null} [deadline_delta] BumpForceCloseFeeRequest deadline_delta
         * @property {Long|null} [starting_feerate] BumpForceCloseFeeRequest starting_feerate
         * @property {boolean|null} [immediate] BumpForceCloseFeeRequest immediate
         * @property {Long|null} [budget] BumpForceCloseFeeRequest budget
         * @property {number|null} [target_conf] BumpForceCloseFeeRequest target_conf
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BumpForceCloseFeeRequest.
         * @memberof walletrpc
         * @interface IBumpForceCloseFeeRequest
         * @augments walletrpc.BumpForceCloseFeeRequest.$Properties
         * @deprecated Use walletrpc.BumpForceCloseFeeRequest.$Properties instead.
         */

        /**
         * Shape of a BumpForceCloseFeeRequest.
         * @typedef {{
         *   chan_point?: lnrpc.ChannelPoint.$Shape|null;
         *   deadline_delta?: number|null;
         *   starting_feerate?: Long|null;
         *   immediate?: boolean|null;
         *   budget?: Long|null;
         *   target_conf?: number|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} walletrpc.BumpForceCloseFeeRequest.$Shape
         */

        /**
         * Constructs a new BumpForceCloseFeeRequest.
         * @memberof walletrpc
         * @classdesc Represents a BumpForceCloseFeeRequest.
         * @constructor
         * @param {walletrpc.BumpForceCloseFeeRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BumpForceCloseFeeRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BumpForceCloseFeeRequest chan_point.
         * @member {lnrpc.ChannelPoint.$Properties|null|undefined} chan_point
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @instance
         */
        BumpForceCloseFeeRequest.prototype.chan_point = null;

        /**
         * BumpForceCloseFeeRequest deadline_delta.
         * @member {number} deadline_delta
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @instance
         */
        BumpForceCloseFeeRequest.prototype.deadline_delta = 0;

        /**
         * BumpForceCloseFeeRequest starting_feerate.
         * @member {Long} starting_feerate
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @instance
         */
        BumpForceCloseFeeRequest.prototype.starting_feerate = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BumpForceCloseFeeRequest immediate.
         * @member {boolean} immediate
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @instance
         */
        BumpForceCloseFeeRequest.prototype.immediate = false;

        /**
         * BumpForceCloseFeeRequest budget.
         * @member {Long} budget
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @instance
         */
        BumpForceCloseFeeRequest.prototype.budget = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BumpForceCloseFeeRequest target_conf.
         * @member {number} target_conf
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @instance
         */
        BumpForceCloseFeeRequest.prototype.target_conf = 0;

        /**
         * Creates a new BumpForceCloseFeeRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @static
         * @param {walletrpc.BumpForceCloseFeeRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.BumpForceCloseFeeRequest} BumpForceCloseFeeRequest instance
         * @type {{
         *   (properties: walletrpc.BumpForceCloseFeeRequest.$Shape): walletrpc.BumpForceCloseFeeRequest & walletrpc.BumpForceCloseFeeRequest.$Shape;
         *   (properties?: walletrpc.BumpForceCloseFeeRequest.$Properties): walletrpc.BumpForceCloseFeeRequest;
         * }}
         */
        BumpForceCloseFeeRequest.create = function(properties) {
            return new BumpForceCloseFeeRequest(properties);
        };

        /**
         * Encodes the specified BumpForceCloseFeeRequest message. Does not implicitly {@link walletrpc.BumpForceCloseFeeRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @static
         * @param {walletrpc.BumpForceCloseFeeRequest.$Properties} message BumpForceCloseFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BumpForceCloseFeeRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                $root.lnrpc.ChannelPoint.encode(message.chan_point, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.deadline_delta != null && $Object.hasOwnProperty.call(message, "deadline_delta") && message.deadline_delta !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.deadline_delta);
            if (message.starting_feerate != null && $Object.hasOwnProperty.call(message, "starting_feerate") && (typeof message.starting_feerate === "object" ? message.starting_feerate.low || message.starting_feerate.high : message.starting_feerate !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.starting_feerate);
            if (message.immediate != null && $Object.hasOwnProperty.call(message, "immediate") && message.immediate !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.immediate);
            if (message.budget != null && $Object.hasOwnProperty.call(message, "budget") && (typeof message.budget === "object" ? message.budget.low || message.budget.high : message.budget !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.budget);
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf") && message.target_conf !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.target_conf);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BumpForceCloseFeeRequest message, length delimited. Does not implicitly {@link walletrpc.BumpForceCloseFeeRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @static
         * @param {walletrpc.BumpForceCloseFeeRequest.$Properties} message BumpForceCloseFeeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BumpForceCloseFeeRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BumpForceCloseFeeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.BumpForceCloseFeeRequest & walletrpc.BumpForceCloseFeeRequest.$Shape} BumpForceCloseFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BumpForceCloseFeeRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.BumpForceCloseFeeRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.chan_point = $root.lnrpc.ChannelPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chan_point);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.deadline_delta = value;
                        else
                            delete message.deadline_delta;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.starting_feerate = value;
                        else
                            delete message.starting_feerate;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.immediate = value;
                        else
                            delete message.immediate;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.budget = value;
                        else
                            delete message.budget;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_conf = value;
                        else
                            delete message.target_conf;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BumpForceCloseFeeRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.BumpForceCloseFeeRequest & walletrpc.BumpForceCloseFeeRequest.$Shape} BumpForceCloseFeeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BumpForceCloseFeeRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BumpForceCloseFeeRequest message.
         * @function verify
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BumpForceCloseFeeRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point")) {
                let error = $root.lnrpc.ChannelPoint.verify(message.chan_point, _depth + 1);
                if (error)
                    return "chan_point." + error;
            }
            if (message.deadline_delta != null && $Object.hasOwnProperty.call(message, "deadline_delta"))
                if (!$util.isInteger(message.deadline_delta))
                    return "deadline_delta: integer expected";
            if (message.starting_feerate != null && $Object.hasOwnProperty.call(message, "starting_feerate"))
                if (!$util.isInteger(message.starting_feerate) && !(message.starting_feerate && $util.isInteger(message.starting_feerate.low) && $util.isInteger(message.starting_feerate.high)))
                    return "starting_feerate: integer|Long expected";
            if (message.immediate != null && $Object.hasOwnProperty.call(message, "immediate"))
                if (typeof message.immediate !== "boolean")
                    return "immediate: boolean expected";
            if (message.budget != null && $Object.hasOwnProperty.call(message, "budget"))
                if (!$util.isInteger(message.budget) && !(message.budget && $util.isInteger(message.budget.low) && $util.isInteger(message.budget.high)))
                    return "budget: integer|Long expected";
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                if (!$util.isInteger(message.target_conf))
                    return "target_conf: integer expected";
            return null;
        };

        /**
         * Creates a BumpForceCloseFeeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.BumpForceCloseFeeRequest} BumpForceCloseFeeRequest
         */
        BumpForceCloseFeeRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.BumpForceCloseFeeRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.BumpForceCloseFeeRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.BumpForceCloseFeeRequest();
            if (object.chan_point != null) {
                if (!$util.isObject(object.chan_point))
                    throw $TypeError(".walletrpc.BumpForceCloseFeeRequest.chan_point: object expected");
                message.chan_point = $root.lnrpc.ChannelPoint.fromObject(object.chan_point, _depth + 1);
            }
            if (object.deadline_delta != null)
                if ($Number(object.deadline_delta) !== 0)
                    message.deadline_delta = object.deadline_delta >>> 0;
            if (object.starting_feerate != null)
                if (typeof object.starting_feerate === "object" ? object.starting_feerate.low || object.starting_feerate.high : $Number(object.starting_feerate) !== 0)
                    if ($util.Long)
                        message.starting_feerate = $util.Long.fromValue(object.starting_feerate, true);
                    else if (typeof object.starting_feerate === "string")
                        message.starting_feerate = $parseInt(object.starting_feerate, 10);
                    else if (typeof object.starting_feerate === "number")
                        message.starting_feerate = object.starting_feerate;
                    else if (typeof object.starting_feerate === "object")
                        message.starting_feerate = new $util.LongBits(object.starting_feerate.low >>> 0, object.starting_feerate.high >>> 0).toNumber(true);
            if (object.immediate != null)
                if (object.immediate)
                    message.immediate = $Boolean(object.immediate);
            if (object.budget != null)
                if (typeof object.budget === "object" ? object.budget.low || object.budget.high : $Number(object.budget) !== 0)
                    if ($util.Long)
                        message.budget = $util.Long.fromValue(object.budget, true);
                    else if (typeof object.budget === "string")
                        message.budget = $parseInt(object.budget, 10);
                    else if (typeof object.budget === "number")
                        message.budget = object.budget;
                    else if (typeof object.budget === "object")
                        message.budget = new $util.LongBits(object.budget.low >>> 0, object.budget.high >>> 0).toNumber(true);
            if (object.target_conf != null)
                if ($Number(object.target_conf) !== 0)
                    message.target_conf = object.target_conf >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a BumpForceCloseFeeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @static
         * @param {walletrpc.BumpForceCloseFeeRequest} message BumpForceCloseFeeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BumpForceCloseFeeRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.chan_point = null;
                object.deadline_delta = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.starting_feerate = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.starting_feerate = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.immediate = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.budget = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.budget = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.target_conf = 0;
            }
            if (message.chan_point != null && $Object.hasOwnProperty.call(message, "chan_point"))
                object.chan_point = $root.lnrpc.ChannelPoint.toObject(message.chan_point, options, _depth + 1);
            if (message.deadline_delta != null && $Object.hasOwnProperty.call(message, "deadline_delta"))
                object.deadline_delta = message.deadline_delta;
            if (message.starting_feerate != null && $Object.hasOwnProperty.call(message, "starting_feerate"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.starting_feerate = typeof message.starting_feerate === "number" ? $BigInt(message.starting_feerate) : $util.Long.fromBits(message.starting_feerate.low >>> 0, message.starting_feerate.high >>> 0, true).toBigInt();
                else if (typeof message.starting_feerate === "number")
                    object.starting_feerate = options.longs === $String ? $String(message.starting_feerate) : message.starting_feerate;
                else
                    object.starting_feerate = options.longs === $String ? $util.Long.prototype.toString.call(message.starting_feerate) : options.longs === $Number ? new $util.LongBits(message.starting_feerate.low >>> 0, message.starting_feerate.high >>> 0).toNumber(true) : message.starting_feerate;
            if (message.immediate != null && $Object.hasOwnProperty.call(message, "immediate"))
                object.immediate = message.immediate;
            if (message.budget != null && $Object.hasOwnProperty.call(message, "budget"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.budget = typeof message.budget === "number" ? $BigInt(message.budget) : $util.Long.fromBits(message.budget.low >>> 0, message.budget.high >>> 0, true).toBigInt();
                else if (typeof message.budget === "number")
                    object.budget = options.longs === $String ? $String(message.budget) : message.budget;
                else
                    object.budget = options.longs === $String ? $util.Long.prototype.toString.call(message.budget) : options.longs === $Number ? new $util.LongBits(message.budget.low >>> 0, message.budget.high >>> 0).toNumber(true) : message.budget;
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                object.target_conf = message.target_conf;
            return object;
        };

        /**
         * Converts this BumpForceCloseFeeRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BumpForceCloseFeeRequest.prototype.toJSON = function() {
            return BumpForceCloseFeeRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BumpForceCloseFeeRequest
         * @function getTypeUrl
         * @memberof walletrpc.BumpForceCloseFeeRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BumpForceCloseFeeRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.BumpForceCloseFeeRequest";
        };

        return BumpForceCloseFeeRequest;
    })();

    walletrpc.BumpForceCloseFeeResponse = (function() {

        /**
         * Properties of a BumpForceCloseFeeResponse.
         * @typedef {Object} walletrpc.BumpForceCloseFeeResponse.$Properties
         * @property {string|null} [status] BumpForceCloseFeeResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a BumpForceCloseFeeResponse.
         * @memberof walletrpc
         * @interface IBumpForceCloseFeeResponse
         * @augments walletrpc.BumpForceCloseFeeResponse.$Properties
         * @deprecated Use walletrpc.BumpForceCloseFeeResponse.$Properties instead.
         */

        /**
         * Shape of a BumpForceCloseFeeResponse.
         * @typedef {walletrpc.BumpForceCloseFeeResponse.$Properties} walletrpc.BumpForceCloseFeeResponse.$Shape
         */

        /**
         * Constructs a new BumpForceCloseFeeResponse.
         * @memberof walletrpc
         * @classdesc Represents a BumpForceCloseFeeResponse.
         * @constructor
         * @param {walletrpc.BumpForceCloseFeeResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const BumpForceCloseFeeResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BumpForceCloseFeeResponse status.
         * @member {string} status
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @instance
         */
        BumpForceCloseFeeResponse.prototype.status = "";

        /**
         * Creates a new BumpForceCloseFeeResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @static
         * @param {walletrpc.BumpForceCloseFeeResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.BumpForceCloseFeeResponse} BumpForceCloseFeeResponse instance
         * @type {{
         *   (properties: walletrpc.BumpForceCloseFeeResponse.$Shape): walletrpc.BumpForceCloseFeeResponse & walletrpc.BumpForceCloseFeeResponse.$Shape;
         *   (properties?: walletrpc.BumpForceCloseFeeResponse.$Properties): walletrpc.BumpForceCloseFeeResponse;
         * }}
         */
        BumpForceCloseFeeResponse.create = function(properties) {
            return new BumpForceCloseFeeResponse(properties);
        };

        /**
         * Encodes the specified BumpForceCloseFeeResponse message. Does not implicitly {@link walletrpc.BumpForceCloseFeeResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @static
         * @param {walletrpc.BumpForceCloseFeeResponse.$Properties} message BumpForceCloseFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BumpForceCloseFeeResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BumpForceCloseFeeResponse message, length delimited. Does not implicitly {@link walletrpc.BumpForceCloseFeeResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @static
         * @param {walletrpc.BumpForceCloseFeeResponse.$Properties} message BumpForceCloseFeeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BumpForceCloseFeeResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BumpForceCloseFeeResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.BumpForceCloseFeeResponse & walletrpc.BumpForceCloseFeeResponse.$Shape} BumpForceCloseFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BumpForceCloseFeeResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.BumpForceCloseFeeResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BumpForceCloseFeeResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.BumpForceCloseFeeResponse & walletrpc.BumpForceCloseFeeResponse.$Shape} BumpForceCloseFeeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BumpForceCloseFeeResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BumpForceCloseFeeResponse message.
         * @function verify
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BumpForceCloseFeeResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a BumpForceCloseFeeResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.BumpForceCloseFeeResponse} BumpForceCloseFeeResponse
         */
        BumpForceCloseFeeResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.BumpForceCloseFeeResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.BumpForceCloseFeeResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.BumpForceCloseFeeResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a BumpForceCloseFeeResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @static
         * @param {walletrpc.BumpForceCloseFeeResponse} message BumpForceCloseFeeResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BumpForceCloseFeeResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this BumpForceCloseFeeResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BumpForceCloseFeeResponse.prototype.toJSON = function() {
            return BumpForceCloseFeeResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BumpForceCloseFeeResponse
         * @function getTypeUrl
         * @memberof walletrpc.BumpForceCloseFeeResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BumpForceCloseFeeResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.BumpForceCloseFeeResponse";
        };

        return BumpForceCloseFeeResponse;
    })();

    walletrpc.ListSweepsRequest = (function() {

        /**
         * Properties of a ListSweepsRequest.
         * @typedef {Object} walletrpc.ListSweepsRequest.$Properties
         * @property {boolean|null} [verbose] ListSweepsRequest verbose
         * @property {number|null} [start_height] ListSweepsRequest start_height
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListSweepsRequest.
         * @memberof walletrpc
         * @interface IListSweepsRequest
         * @augments walletrpc.ListSweepsRequest.$Properties
         * @deprecated Use walletrpc.ListSweepsRequest.$Properties instead.
         */

        /**
         * Shape of a ListSweepsRequest.
         * @typedef {walletrpc.ListSweepsRequest.$Properties} walletrpc.ListSweepsRequest.$Shape
         */

        /**
         * Constructs a new ListSweepsRequest.
         * @memberof walletrpc
         * @classdesc Represents a ListSweepsRequest.
         * @constructor
         * @param {walletrpc.ListSweepsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListSweepsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListSweepsRequest verbose.
         * @member {boolean} verbose
         * @memberof walletrpc.ListSweepsRequest
         * @instance
         */
        ListSweepsRequest.prototype.verbose = false;

        /**
         * ListSweepsRequest start_height.
         * @member {number} start_height
         * @memberof walletrpc.ListSweepsRequest
         * @instance
         */
        ListSweepsRequest.prototype.start_height = 0;

        /**
         * Creates a new ListSweepsRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListSweepsRequest
         * @static
         * @param {walletrpc.ListSweepsRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListSweepsRequest} ListSweepsRequest instance
         * @type {{
         *   (properties: walletrpc.ListSweepsRequest.$Shape): walletrpc.ListSweepsRequest & walletrpc.ListSweepsRequest.$Shape;
         *   (properties?: walletrpc.ListSweepsRequest.$Properties): walletrpc.ListSweepsRequest;
         * }}
         */
        ListSweepsRequest.create = function(properties) {
            return new ListSweepsRequest(properties);
        };

        /**
         * Encodes the specified ListSweepsRequest message. Does not implicitly {@link walletrpc.ListSweepsRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListSweepsRequest
         * @static
         * @param {walletrpc.ListSweepsRequest.$Properties} message ListSweepsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListSweepsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.verbose != null && $Object.hasOwnProperty.call(message, "verbose") && message.verbose !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.verbose);
            if (message.start_height != null && $Object.hasOwnProperty.call(message, "start_height") && message.start_height !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.start_height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListSweepsRequest message, length delimited. Does not implicitly {@link walletrpc.ListSweepsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListSweepsRequest
         * @static
         * @param {walletrpc.ListSweepsRequest.$Properties} message ListSweepsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListSweepsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListSweepsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListSweepsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListSweepsRequest & walletrpc.ListSweepsRequest.$Shape} ListSweepsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListSweepsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListSweepsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.verbose = value;
                        else
                            delete message.verbose;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.start_height = value;
                        else
                            delete message.start_height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListSweepsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListSweepsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListSweepsRequest & walletrpc.ListSweepsRequest.$Shape} ListSweepsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListSweepsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListSweepsRequest message.
         * @function verify
         * @memberof walletrpc.ListSweepsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListSweepsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.verbose != null && $Object.hasOwnProperty.call(message, "verbose"))
                if (typeof message.verbose !== "boolean")
                    return "verbose: boolean expected";
            if (message.start_height != null && $Object.hasOwnProperty.call(message, "start_height"))
                if (!$util.isInteger(message.start_height))
                    return "start_height: integer expected";
            return null;
        };

        /**
         * Creates a ListSweepsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListSweepsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListSweepsRequest} ListSweepsRequest
         */
        ListSweepsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListSweepsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListSweepsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ListSweepsRequest();
            if (object.verbose != null)
                if (object.verbose)
                    message.verbose = $Boolean(object.verbose);
            if (object.start_height != null)
                if ($Number(object.start_height) !== 0)
                    message.start_height = object.start_height | 0;
            return message;
        };

        /**
         * Creates a plain object from a ListSweepsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListSweepsRequest
         * @static
         * @param {walletrpc.ListSweepsRequest} message ListSweepsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListSweepsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.verbose = false;
                object.start_height = 0;
            }
            if (message.verbose != null && $Object.hasOwnProperty.call(message, "verbose"))
                object.verbose = message.verbose;
            if (message.start_height != null && $Object.hasOwnProperty.call(message, "start_height"))
                object.start_height = message.start_height;
            return object;
        };

        /**
         * Converts this ListSweepsRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.ListSweepsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListSweepsRequest.prototype.toJSON = function() {
            return ListSweepsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListSweepsRequest
         * @function getTypeUrl
         * @memberof walletrpc.ListSweepsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListSweepsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListSweepsRequest";
        };

        return ListSweepsRequest;
    })();

    walletrpc.ListSweepsResponse = (function() {

        /**
         * Properties of a ListSweepsResponse.
         * @typedef {Object} walletrpc.ListSweepsResponse.$Properties
         * @property {lnrpc.TransactionDetails.$Properties|null} [transaction_details] ListSweepsResponse transaction_details
         * @property {walletrpc.ListSweepsResponse.TransactionIDs.$Properties|null} [transaction_ids] ListSweepsResponse transaction_ids
         * @property {"transaction_details"|"transaction_ids"} [sweeps] ListSweepsResponse sweeps
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListSweepsResponse.
         * @memberof walletrpc
         * @interface IListSweepsResponse
         * @augments walletrpc.ListSweepsResponse.$Properties
         * @deprecated Use walletrpc.ListSweepsResponse.$Properties instead.
         */

        /**
         * Narrowed shape of a ListSweepsResponse.
         * @typedef {{
         *   transaction_details?: lnrpc.TransactionDetails.$Shape|null;
         *   transaction_ids?: walletrpc.ListSweepsResponse.TransactionIDs.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ sweeps?: undefined; transaction_details?: null; transaction_ids?: null }|{ sweeps?: "transaction_details"; transaction_details: lnrpc.TransactionDetails.$Shape; transaction_ids?: null }|{ sweeps?: "transaction_ids"; transaction_details?: null; transaction_ids: walletrpc.ListSweepsResponse.TransactionIDs.$Shape })
         * )} walletrpc.ListSweepsResponse.$Shape
         */

        /**
         * Constructs a new ListSweepsResponse.
         * @memberof walletrpc
         * @classdesc Represents a ListSweepsResponse.
         * @constructor
         * @param {walletrpc.ListSweepsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListSweepsResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListSweepsResponse transaction_details.
         * @member {lnrpc.TransactionDetails.$Properties|null|undefined} transaction_details
         * @memberof walletrpc.ListSweepsResponse
         * @instance
         */
        ListSweepsResponse.prototype.transaction_details = null;

        /**
         * ListSweepsResponse transaction_ids.
         * @member {walletrpc.ListSweepsResponse.TransactionIDs.$Properties|null|undefined} transaction_ids
         * @memberof walletrpc.ListSweepsResponse
         * @instance
         */
        ListSweepsResponse.prototype.transaction_ids = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ListSweepsResponse sweeps.
         * @member {"transaction_details"|"transaction_ids"|undefined} sweeps
         * @memberof walletrpc.ListSweepsResponse
         * @instance
         */
        $Object.defineProperty(ListSweepsResponse.prototype, "sweeps", {
            get: $util.oneOfGetter($oneOfFields = ["transaction_details", "transaction_ids"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ListSweepsResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListSweepsResponse
         * @static
         * @param {walletrpc.ListSweepsResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListSweepsResponse} ListSweepsResponse instance
         * @type {{
         *   (properties: walletrpc.ListSweepsResponse.$Shape): walletrpc.ListSweepsResponse & walletrpc.ListSweepsResponse.$Shape;
         *   (properties?: walletrpc.ListSweepsResponse.$Properties): walletrpc.ListSweepsResponse;
         * }}
         */
        ListSweepsResponse.create = function(properties) {
            return new ListSweepsResponse(properties);
        };

        /**
         * Encodes the specified ListSweepsResponse message. Does not implicitly {@link walletrpc.ListSweepsResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListSweepsResponse
         * @static
         * @param {walletrpc.ListSweepsResponse.$Properties} message ListSweepsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListSweepsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transaction_details != null && $Object.hasOwnProperty.call(message, "transaction_details"))
                $root.lnrpc.TransactionDetails.encode(message.transaction_details, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transaction_ids != null && $Object.hasOwnProperty.call(message, "transaction_ids"))
                $root.walletrpc.ListSweepsResponse.TransactionIDs.encode(message.transaction_ids, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListSweepsResponse message, length delimited. Does not implicitly {@link walletrpc.ListSweepsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListSweepsResponse
         * @static
         * @param {walletrpc.ListSweepsResponse.$Properties} message ListSweepsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListSweepsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListSweepsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListSweepsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListSweepsResponse & walletrpc.ListSweepsResponse.$Shape} ListSweepsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListSweepsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListSweepsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.transaction_details = $root.lnrpc.TransactionDetails.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transaction_details);
                        message.sweeps = "transaction_details";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.transaction_ids = $root.walletrpc.ListSweepsResponse.TransactionIDs.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transaction_ids);
                        message.sweeps = "transaction_ids";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListSweepsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListSweepsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListSweepsResponse & walletrpc.ListSweepsResponse.$Shape} ListSweepsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListSweepsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListSweepsResponse message.
         * @function verify
         * @memberof walletrpc.ListSweepsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListSweepsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.transaction_details != null && $Object.hasOwnProperty.call(message, "transaction_details")) {
                properties.sweeps = 1;
                {
                    let error = $root.lnrpc.TransactionDetails.verify(message.transaction_details, _depth + 1);
                    if (error)
                        return "transaction_details." + error;
                }
            }
            if (message.transaction_ids != null && $Object.hasOwnProperty.call(message, "transaction_ids")) {
                if (properties.sweeps === 1)
                    return "sweeps: multiple values";
                properties.sweeps = 1;
                {
                    let error = $root.walletrpc.ListSweepsResponse.TransactionIDs.verify(message.transaction_ids, _depth + 1);
                    if (error)
                        return "transaction_ids." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListSweepsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListSweepsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListSweepsResponse} ListSweepsResponse
         */
        ListSweepsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListSweepsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListSweepsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ListSweepsResponse();
            if (object.transaction_details != null) {
                if (!$util.isObject(object.transaction_details))
                    throw $TypeError(".walletrpc.ListSweepsResponse.transaction_details: object expected");
                message.transaction_details = $root.lnrpc.TransactionDetails.fromObject(object.transaction_details, _depth + 1);
            }
            if (object.transaction_ids != null) {
                if (!$util.isObject(object.transaction_ids))
                    throw $TypeError(".walletrpc.ListSweepsResponse.transaction_ids: object expected");
                message.transaction_ids = $root.walletrpc.ListSweepsResponse.TransactionIDs.fromObject(object.transaction_ids, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ListSweepsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListSweepsResponse
         * @static
         * @param {walletrpc.ListSweepsResponse} message ListSweepsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListSweepsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.transaction_details != null && $Object.hasOwnProperty.call(message, "transaction_details")) {
                object.transaction_details = $root.lnrpc.TransactionDetails.toObject(message.transaction_details, options, _depth + 1);
                if (options.oneofs)
                    object.sweeps = "transaction_details";
            }
            if (message.transaction_ids != null && $Object.hasOwnProperty.call(message, "transaction_ids")) {
                object.transaction_ids = $root.walletrpc.ListSweepsResponse.TransactionIDs.toObject(message.transaction_ids, options, _depth + 1);
                if (options.oneofs)
                    object.sweeps = "transaction_ids";
            }
            return object;
        };

        /**
         * Converts this ListSweepsResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.ListSweepsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListSweepsResponse.prototype.toJSON = function() {
            return ListSweepsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListSweepsResponse
         * @function getTypeUrl
         * @memberof walletrpc.ListSweepsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListSweepsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListSweepsResponse";
        };

        ListSweepsResponse.TransactionIDs = (function() {

            /**
             * Properties of a TransactionIDs.
             * @typedef {Object} walletrpc.ListSweepsResponse.TransactionIDs.$Properties
             * @property {Array.<string>|null} [transaction_ids] TransactionIDs transaction_ids
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */

            /**
             * Properties of a TransactionIDs.
             * @memberof walletrpc.ListSweepsResponse
             * @interface ITransactionIDs
             * @augments walletrpc.ListSweepsResponse.TransactionIDs.$Properties
             * @deprecated Use walletrpc.ListSweepsResponse.TransactionIDs.$Properties instead.
             */

            /**
             * Shape of a TransactionIDs.
             * @typedef {walletrpc.ListSweepsResponse.TransactionIDs.$Properties} walletrpc.ListSweepsResponse.TransactionIDs.$Shape
             */

            /**
             * Constructs a new TransactionIDs.
             * @memberof walletrpc.ListSweepsResponse
             * @classdesc Represents a TransactionIDs.
             * @constructor
             * @param {walletrpc.ListSweepsResponse.TransactionIDs.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
             */
            const TransactionIDs = function (properties) {
                this.transaction_ids = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * TransactionIDs transaction_ids.
             * @member {Array.<string>} transaction_ids
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @instance
             */
            TransactionIDs.prototype.transaction_ids = $util.emptyArray;

            /**
             * Creates a new TransactionIDs instance using the specified properties.
             * @function create
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @static
             * @param {walletrpc.ListSweepsResponse.TransactionIDs.$Properties=} [properties] Properties to set
             * @returns {walletrpc.ListSweepsResponse.TransactionIDs} TransactionIDs instance
             * @type {{
             *   (properties: walletrpc.ListSweepsResponse.TransactionIDs.$Shape): walletrpc.ListSweepsResponse.TransactionIDs & walletrpc.ListSweepsResponse.TransactionIDs.$Shape;
             *   (properties?: walletrpc.ListSweepsResponse.TransactionIDs.$Properties): walletrpc.ListSweepsResponse.TransactionIDs;
             * }}
             */
            TransactionIDs.create = function(properties) {
                return new TransactionIDs(properties);
            };

            /**
             * Encodes the specified TransactionIDs message. Does not implicitly {@link walletrpc.ListSweepsResponse.TransactionIDs.verify|verify} messages.
             * @function encode
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @static
             * @param {walletrpc.ListSweepsResponse.TransactionIDs.$Properties} message TransactionIDs message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TransactionIDs.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.transaction_ids != null && message.transaction_ids.length)
                    for (let i = 0; i < message.transaction_ids.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.transaction_ids[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified TransactionIDs message, length delimited. Does not implicitly {@link walletrpc.ListSweepsResponse.TransactionIDs.verify|verify} messages.
             * @function encodeDelimited
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @static
             * @param {walletrpc.ListSweepsResponse.TransactionIDs.$Properties} message TransactionIDs message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TransactionIDs.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a TransactionIDs message from the specified reader or buffer.
             * @function decode
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {walletrpc.ListSweepsResponse.TransactionIDs & walletrpc.ListSweepsResponse.TransactionIDs.$Shape} TransactionIDs
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TransactionIDs.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListSweepsResponse.TransactionIDs();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.transaction_ids && message.transaction_ids.length))
                                message.transaction_ids = [];
                            message.transaction_ids.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a TransactionIDs message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {walletrpc.ListSweepsResponse.TransactionIDs & walletrpc.ListSweepsResponse.TransactionIDs.$Shape} TransactionIDs
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TransactionIDs.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TransactionIDs message.
             * @function verify
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TransactionIDs.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.transaction_ids != null && $Object.hasOwnProperty.call(message, "transaction_ids")) {
                    if (!$Array.isArray(message.transaction_ids))
                        return "transaction_ids: array expected";
                    for (let i = 0; i < message.transaction_ids.length; ++i)
                        if (!$util.isString(message.transaction_ids[i]))
                            return "transaction_ids: string[] expected";
                }
                return null;
            };

            /**
             * Creates a TransactionIDs message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {walletrpc.ListSweepsResponse.TransactionIDs} TransactionIDs
             */
            TransactionIDs.fromObject = function (object, _depth) {
                if (object instanceof $root.walletrpc.ListSweepsResponse.TransactionIDs)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".walletrpc.ListSweepsResponse.TransactionIDs: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.walletrpc.ListSweepsResponse.TransactionIDs();
                if (object.transaction_ids) {
                    if (!$Array.isArray(object.transaction_ids))
                        throw $TypeError(".walletrpc.ListSweepsResponse.TransactionIDs.transaction_ids: array expected");
                    message.transaction_ids = $Array(object.transaction_ids.length);
                    for (let i = 0; i < object.transaction_ids.length; ++i)
                        message.transaction_ids[i] = $String(object.transaction_ids[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a TransactionIDs message. Also converts values to other types if specified.
             * @function toObject
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @static
             * @param {walletrpc.ListSweepsResponse.TransactionIDs} message TransactionIDs
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TransactionIDs.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.transaction_ids = [];
                if (message.transaction_ids && message.transaction_ids.length) {
                    object.transaction_ids = $Array(message.transaction_ids.length);
                    for (let j = 0; j < message.transaction_ids.length; ++j)
                        object.transaction_ids[j] = message.transaction_ids[j];
                }
                return object;
            };

            /**
             * Converts this TransactionIDs to JSON.
             * @function toJSON
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TransactionIDs.prototype.toJSON = function() {
                return TransactionIDs.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for TransactionIDs
             * @function getTypeUrl
             * @memberof walletrpc.ListSweepsResponse.TransactionIDs
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            TransactionIDs.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/walletrpc.ListSweepsResponse.TransactionIDs";
            };

            return TransactionIDs;
        })();

        return ListSweepsResponse;
    })();

    walletrpc.LabelTransactionRequest = (function() {

        /**
         * Properties of a LabelTransactionRequest.
         * @typedef {Object} walletrpc.LabelTransactionRequest.$Properties
         * @property {Uint8Array|null} [txid] LabelTransactionRequest txid
         * @property {string|null} [label] LabelTransactionRequest label
         * @property {boolean|null} [overwrite] LabelTransactionRequest overwrite
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LabelTransactionRequest.
         * @memberof walletrpc
         * @interface ILabelTransactionRequest
         * @augments walletrpc.LabelTransactionRequest.$Properties
         * @deprecated Use walletrpc.LabelTransactionRequest.$Properties instead.
         */

        /**
         * Shape of a LabelTransactionRequest.
         * @typedef {walletrpc.LabelTransactionRequest.$Properties} walletrpc.LabelTransactionRequest.$Shape
         */

        /**
         * Constructs a new LabelTransactionRequest.
         * @memberof walletrpc
         * @classdesc Represents a LabelTransactionRequest.
         * @constructor
         * @param {walletrpc.LabelTransactionRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LabelTransactionRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LabelTransactionRequest txid.
         * @member {Uint8Array} txid
         * @memberof walletrpc.LabelTransactionRequest
         * @instance
         */
        LabelTransactionRequest.prototype.txid = $util.newBuffer([]);

        /**
         * LabelTransactionRequest label.
         * @member {string} label
         * @memberof walletrpc.LabelTransactionRequest
         * @instance
         */
        LabelTransactionRequest.prototype.label = "";

        /**
         * LabelTransactionRequest overwrite.
         * @member {boolean} overwrite
         * @memberof walletrpc.LabelTransactionRequest
         * @instance
         */
        LabelTransactionRequest.prototype.overwrite = false;

        /**
         * Creates a new LabelTransactionRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.LabelTransactionRequest
         * @static
         * @param {walletrpc.LabelTransactionRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.LabelTransactionRequest} LabelTransactionRequest instance
         * @type {{
         *   (properties: walletrpc.LabelTransactionRequest.$Shape): walletrpc.LabelTransactionRequest & walletrpc.LabelTransactionRequest.$Shape;
         *   (properties?: walletrpc.LabelTransactionRequest.$Properties): walletrpc.LabelTransactionRequest;
         * }}
         */
        LabelTransactionRequest.create = function(properties) {
            return new LabelTransactionRequest(properties);
        };

        /**
         * Encodes the specified LabelTransactionRequest message. Does not implicitly {@link walletrpc.LabelTransactionRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.LabelTransactionRequest
         * @static
         * @param {walletrpc.LabelTransactionRequest.$Properties} message LabelTransactionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LabelTransactionRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid") && message.txid.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.txid);
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.label);
            if (message.overwrite != null && $Object.hasOwnProperty.call(message, "overwrite") && message.overwrite !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.overwrite);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LabelTransactionRequest message, length delimited. Does not implicitly {@link walletrpc.LabelTransactionRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.LabelTransactionRequest
         * @static
         * @param {walletrpc.LabelTransactionRequest.$Properties} message LabelTransactionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LabelTransactionRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LabelTransactionRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.LabelTransactionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.LabelTransactionRequest & walletrpc.LabelTransactionRequest.$Shape} LabelTransactionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LabelTransactionRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.LabelTransactionRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.txid = value;
                        else
                            delete message.txid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.overwrite = value;
                        else
                            delete message.overwrite;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LabelTransactionRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.LabelTransactionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.LabelTransactionRequest & walletrpc.LabelTransactionRequest.$Shape} LabelTransactionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LabelTransactionRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LabelTransactionRequest message.
         * @function verify
         * @memberof walletrpc.LabelTransactionRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LabelTransactionRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                if (!(message.txid && typeof message.txid.length === "number" || $util.isString(message.txid)))
                    return "txid: buffer expected";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.overwrite != null && $Object.hasOwnProperty.call(message, "overwrite"))
                if (typeof message.overwrite !== "boolean")
                    return "overwrite: boolean expected";
            return null;
        };

        /**
         * Creates a LabelTransactionRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.LabelTransactionRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.LabelTransactionRequest} LabelTransactionRequest
         */
        LabelTransactionRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.LabelTransactionRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.LabelTransactionRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.LabelTransactionRequest();
            if (object.txid != null)
                if (object.txid.length)
                    if (typeof object.txid === "string")
                        $util.base64.decode(object.txid, message.txid = $util.newBuffer($util.base64.length(object.txid)), 0);
                    else if (object.txid.length >= 0)
                        message.txid = object.txid;
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.overwrite != null)
                if (object.overwrite)
                    message.overwrite = $Boolean(object.overwrite);
            return message;
        };

        /**
         * Creates a plain object from a LabelTransactionRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.LabelTransactionRequest
         * @static
         * @param {walletrpc.LabelTransactionRequest} message LabelTransactionRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LabelTransactionRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.txid = "";
                else {
                    object.txid = [];
                    if (options.bytes !== $Array)
                        object.txid = $util.newBuffer(object.txid);
                }
                object.label = "";
                object.overwrite = false;
            }
            if (message.txid != null && $Object.hasOwnProperty.call(message, "txid"))
                object.txid = options.bytes === $String ? $util.base64.encode(message.txid, 0, message.txid.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.txid) : message.txid;
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.overwrite != null && $Object.hasOwnProperty.call(message, "overwrite"))
                object.overwrite = message.overwrite;
            return object;
        };

        /**
         * Converts this LabelTransactionRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.LabelTransactionRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LabelTransactionRequest.prototype.toJSON = function() {
            return LabelTransactionRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LabelTransactionRequest
         * @function getTypeUrl
         * @memberof walletrpc.LabelTransactionRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LabelTransactionRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.LabelTransactionRequest";
        };

        return LabelTransactionRequest;
    })();

    walletrpc.LabelTransactionResponse = (function() {

        /**
         * Properties of a LabelTransactionResponse.
         * @typedef {Object} walletrpc.LabelTransactionResponse.$Properties
         * @property {string|null} [status] LabelTransactionResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a LabelTransactionResponse.
         * @memberof walletrpc
         * @interface ILabelTransactionResponse
         * @augments walletrpc.LabelTransactionResponse.$Properties
         * @deprecated Use walletrpc.LabelTransactionResponse.$Properties instead.
         */

        /**
         * Shape of a LabelTransactionResponse.
         * @typedef {walletrpc.LabelTransactionResponse.$Properties} walletrpc.LabelTransactionResponse.$Shape
         */

        /**
         * Constructs a new LabelTransactionResponse.
         * @memberof walletrpc
         * @classdesc Represents a LabelTransactionResponse.
         * @constructor
         * @param {walletrpc.LabelTransactionResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const LabelTransactionResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LabelTransactionResponse status.
         * @member {string} status
         * @memberof walletrpc.LabelTransactionResponse
         * @instance
         */
        LabelTransactionResponse.prototype.status = "";

        /**
         * Creates a new LabelTransactionResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.LabelTransactionResponse
         * @static
         * @param {walletrpc.LabelTransactionResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.LabelTransactionResponse} LabelTransactionResponse instance
         * @type {{
         *   (properties: walletrpc.LabelTransactionResponse.$Shape): walletrpc.LabelTransactionResponse & walletrpc.LabelTransactionResponse.$Shape;
         *   (properties?: walletrpc.LabelTransactionResponse.$Properties): walletrpc.LabelTransactionResponse;
         * }}
         */
        LabelTransactionResponse.create = function(properties) {
            return new LabelTransactionResponse(properties);
        };

        /**
         * Encodes the specified LabelTransactionResponse message. Does not implicitly {@link walletrpc.LabelTransactionResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.LabelTransactionResponse
         * @static
         * @param {walletrpc.LabelTransactionResponse.$Properties} message LabelTransactionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LabelTransactionResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LabelTransactionResponse message, length delimited. Does not implicitly {@link walletrpc.LabelTransactionResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.LabelTransactionResponse
         * @static
         * @param {walletrpc.LabelTransactionResponse.$Properties} message LabelTransactionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LabelTransactionResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LabelTransactionResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.LabelTransactionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.LabelTransactionResponse & walletrpc.LabelTransactionResponse.$Shape} LabelTransactionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LabelTransactionResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.LabelTransactionResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LabelTransactionResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.LabelTransactionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.LabelTransactionResponse & walletrpc.LabelTransactionResponse.$Shape} LabelTransactionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LabelTransactionResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LabelTransactionResponse message.
         * @function verify
         * @memberof walletrpc.LabelTransactionResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LabelTransactionResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a LabelTransactionResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.LabelTransactionResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.LabelTransactionResponse} LabelTransactionResponse
         */
        LabelTransactionResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.LabelTransactionResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.LabelTransactionResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.LabelTransactionResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a LabelTransactionResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.LabelTransactionResponse
         * @static
         * @param {walletrpc.LabelTransactionResponse} message LabelTransactionResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LabelTransactionResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this LabelTransactionResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.LabelTransactionResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LabelTransactionResponse.prototype.toJSON = function() {
            return LabelTransactionResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LabelTransactionResponse
         * @function getTypeUrl
         * @memberof walletrpc.LabelTransactionResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LabelTransactionResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.LabelTransactionResponse";
        };

        return LabelTransactionResponse;
    })();

    /**
     * ChangeAddressType enum.
     * @name walletrpc.ChangeAddressType
     * @enum {number}
     * @property {number} CHANGE_ADDRESS_TYPE_UNSPECIFIED=0 CHANGE_ADDRESS_TYPE_UNSPECIFIED value
     * @property {number} CHANGE_ADDRESS_TYPE_P2TR=1 CHANGE_ADDRESS_TYPE_P2TR value
     */
    walletrpc.ChangeAddressType = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "CHANGE_ADDRESS_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "CHANGE_ADDRESS_TYPE_P2TR"] = 1;
        return values;
    })();

    walletrpc.FundPsbtRequest = (function() {

        /**
         * Properties of a FundPsbtRequest.
         * @typedef {Object} walletrpc.FundPsbtRequest.$Properties
         * @property {Uint8Array|null} [psbt] FundPsbtRequest psbt
         * @property {walletrpc.TxTemplate.$Properties|null} [raw] FundPsbtRequest raw
         * @property {walletrpc.PsbtCoinSelect.$Properties|null} [coin_select] FundPsbtRequest coin_select
         * @property {number|null} [target_conf] FundPsbtRequest target_conf
         * @property {Long|null} [sat_per_vbyte] FundPsbtRequest sat_per_vbyte
         * @property {Long|null} [sat_per_kw] FundPsbtRequest sat_per_kw
         * @property {string|null} [account] FundPsbtRequest account
         * @property {number|null} [min_confs] FundPsbtRequest min_confs
         * @property {boolean|null} [spend_unconfirmed] FundPsbtRequest spend_unconfirmed
         * @property {walletrpc.ChangeAddressType|null} [change_type] FundPsbtRequest change_type
         * @property {lnrpc.CoinSelectionStrategy|null} [coin_selection_strategy] FundPsbtRequest coin_selection_strategy
         * @property {number|null} [max_fee_ratio] FundPsbtRequest max_fee_ratio
         * @property {Uint8Array|null} [custom_lock_id] FundPsbtRequest custom_lock_id
         * @property {Long|null} [lock_expiration_seconds] FundPsbtRequest lock_expiration_seconds
         * @property {"psbt"|"raw"|"coin_select"} [template] FundPsbtRequest template
         * @property {"target_conf"|"sat_per_vbyte"|"sat_per_kw"} [fees] FundPsbtRequest fees
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FundPsbtRequest.
         * @memberof walletrpc
         * @interface IFundPsbtRequest
         * @augments walletrpc.FundPsbtRequest.$Properties
         * @deprecated Use walletrpc.FundPsbtRequest.$Properties instead.
         */

        /**
         * Narrowed shape of a FundPsbtRequest.
         * @typedef {{
         *   psbt?: Uint8Array|null;
         *   raw?: walletrpc.TxTemplate.$Shape|null;
         *   coin_select?: walletrpc.PsbtCoinSelect.$Shape|null;
         *   target_conf?: number|null;
         *   sat_per_vbyte?: Long|null;
         *   sat_per_kw?: Long|null;
         *   account?: string|null;
         *   min_confs?: number|null;
         *   spend_unconfirmed?: boolean|null;
         *   change_type?: walletrpc.ChangeAddressType|null;
         *   coin_selection_strategy?: lnrpc.CoinSelectionStrategy|null;
         *   max_fee_ratio?: number|null;
         *   custom_lock_id?: Uint8Array|null;
         *   lock_expiration_seconds?: Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ template?: undefined; psbt?: null; raw?: null; coin_select?: null }|{ template?: "psbt"; psbt: Uint8Array; raw?: null; coin_select?: null }|{ template?: "raw"; psbt?: null; raw: walletrpc.TxTemplate.$Shape; coin_select?: null }|{ template?: "coin_select"; psbt?: null; raw?: null; coin_select: walletrpc.PsbtCoinSelect.$Shape })
         * ) & (
         *   ({ fees?: undefined; target_conf?: null; sat_per_vbyte?: null; sat_per_kw?: null }|{ fees?: "target_conf"; target_conf: number; sat_per_vbyte?: null; sat_per_kw?: null }|{ fees?: "sat_per_vbyte"; target_conf?: null; sat_per_vbyte: Long; sat_per_kw?: null }|{ fees?: "sat_per_kw"; target_conf?: null; sat_per_vbyte?: null; sat_per_kw: Long })
         * )} walletrpc.FundPsbtRequest.$Shape
         */

        /**
         * Constructs a new FundPsbtRequest.
         * @memberof walletrpc
         * @classdesc Represents a FundPsbtRequest.
         * @constructor
         * @param {walletrpc.FundPsbtRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FundPsbtRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FundPsbtRequest psbt.
         * @member {Uint8Array|null|undefined} psbt
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.psbt = null;

        /**
         * FundPsbtRequest raw.
         * @member {walletrpc.TxTemplate.$Properties|null|undefined} raw
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.raw = null;

        /**
         * FundPsbtRequest coin_select.
         * @member {walletrpc.PsbtCoinSelect.$Properties|null|undefined} coin_select
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.coin_select = null;

        /**
         * FundPsbtRequest target_conf.
         * @member {number|null|undefined} target_conf
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.target_conf = null;

        /**
         * FundPsbtRequest sat_per_vbyte.
         * @member {Long|null|undefined} sat_per_vbyte
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.sat_per_vbyte = null;

        /**
         * FundPsbtRequest sat_per_kw.
         * @member {Long|null|undefined} sat_per_kw
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.sat_per_kw = null;

        /**
         * FundPsbtRequest account.
         * @member {string} account
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.account = "";

        /**
         * FundPsbtRequest min_confs.
         * @member {number} min_confs
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.min_confs = 0;

        /**
         * FundPsbtRequest spend_unconfirmed.
         * @member {boolean} spend_unconfirmed
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.spend_unconfirmed = false;

        /**
         * FundPsbtRequest change_type.
         * @member {walletrpc.ChangeAddressType} change_type
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.change_type = 0;

        /**
         * FundPsbtRequest coin_selection_strategy.
         * @member {lnrpc.CoinSelectionStrategy} coin_selection_strategy
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.coin_selection_strategy = 0;

        /**
         * FundPsbtRequest max_fee_ratio.
         * @member {number} max_fee_ratio
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.max_fee_ratio = 0;

        /**
         * FundPsbtRequest custom_lock_id.
         * @member {Uint8Array} custom_lock_id
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.custom_lock_id = $util.newBuffer([]);

        /**
         * FundPsbtRequest lock_expiration_seconds.
         * @member {Long} lock_expiration_seconds
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        FundPsbtRequest.prototype.lock_expiration_seconds = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * FundPsbtRequest template.
         * @member {"psbt"|"raw"|"coin_select"|undefined} template
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        $Object.defineProperty(FundPsbtRequest.prototype, "template", {
            get: $util.oneOfGetter($oneOfFields = ["psbt", "raw", "coin_select"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * FundPsbtRequest fees.
         * @member {"target_conf"|"sat_per_vbyte"|"sat_per_kw"|undefined} fees
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         */
        $Object.defineProperty(FundPsbtRequest.prototype, "fees", {
            get: $util.oneOfGetter($oneOfFields = ["target_conf", "sat_per_vbyte", "sat_per_kw"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new FundPsbtRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.FundPsbtRequest
         * @static
         * @param {walletrpc.FundPsbtRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.FundPsbtRequest} FundPsbtRequest instance
         * @type {{
         *   (properties: walletrpc.FundPsbtRequest.$Shape): walletrpc.FundPsbtRequest & walletrpc.FundPsbtRequest.$Shape;
         *   (properties?: walletrpc.FundPsbtRequest.$Properties): walletrpc.FundPsbtRequest;
         * }}
         */
        FundPsbtRequest.create = function(properties) {
            return new FundPsbtRequest(properties);
        };

        /**
         * Encodes the specified FundPsbtRequest message. Does not implicitly {@link walletrpc.FundPsbtRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.FundPsbtRequest
         * @static
         * @param {walletrpc.FundPsbtRequest.$Properties} message FundPsbtRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundPsbtRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.psbt != null && $Object.hasOwnProperty.call(message, "psbt"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.psbt);
            if (message.raw != null && $Object.hasOwnProperty.call(message, "raw"))
                $root.walletrpc.TxTemplate.encode(message.raw, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.target_conf);
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.sat_per_vbyte);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account") && message.account !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.account);
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs") && message.min_confs !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.min_confs);
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed") && message.spend_unconfirmed !== false)
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.spend_unconfirmed);
            if (message.change_type != null && $Object.hasOwnProperty.call(message, "change_type") && message.change_type !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.change_type);
            if (message.coin_select != null && $Object.hasOwnProperty.call(message, "coin_select"))
                $root.walletrpc.PsbtCoinSelect.encode(message.coin_select, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy") && message.coin_selection_strategy !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.coin_selection_strategy);
            if (message.sat_per_kw != null && $Object.hasOwnProperty.call(message, "sat_per_kw"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.sat_per_kw);
            if (message.max_fee_ratio != null && $Object.hasOwnProperty.call(message, "max_fee_ratio") && !$Object.is(message.max_fee_ratio, 0))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.max_fee_ratio);
            if (message.custom_lock_id != null && $Object.hasOwnProperty.call(message, "custom_lock_id") && message.custom_lock_id.length)
                writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.custom_lock_id);
            if (message.lock_expiration_seconds != null && $Object.hasOwnProperty.call(message, "lock_expiration_seconds") && (typeof message.lock_expiration_seconds === "object" ? message.lock_expiration_seconds.low || message.lock_expiration_seconds.high : message.lock_expiration_seconds !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.lock_expiration_seconds);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FundPsbtRequest message, length delimited. Does not implicitly {@link walletrpc.FundPsbtRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.FundPsbtRequest
         * @static
         * @param {walletrpc.FundPsbtRequest.$Properties} message FundPsbtRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundPsbtRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FundPsbtRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.FundPsbtRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.FundPsbtRequest & walletrpc.FundPsbtRequest.$Shape} FundPsbtRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundPsbtRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.FundPsbtRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.psbt = reader.bytes();
                        message.template = "psbt";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.raw = $root.walletrpc.TxTemplate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.raw);
                        message.template = "raw";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.coin_select = $root.walletrpc.PsbtCoinSelect.decode(reader, reader.uint32(), $undefined, _depth + 1, message.coin_select);
                        message.template = "coin_select";
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        message.target_conf = reader.uint32();
                        message.fees = "target_conf";
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        message.sat_per_vbyte = reader.uint64();
                        message.fees = "sat_per_vbyte";
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        message.sat_per_kw = reader.uint64();
                        message.fees = "sat_per_kw";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min_confs = value;
                        else
                            delete message.min_confs;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.spend_unconfirmed = value;
                        else
                            delete message.spend_unconfirmed;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.change_type = value;
                        else
                            delete message.change_type;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.coin_selection_strategy = value;
                        else
                            delete message.coin_selection_strategy;
                        continue;
                    }
                case 12: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.max_fee_ratio = value;
                        else
                            delete message.max_fee_ratio;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.custom_lock_id = value;
                        else
                            delete message.custom_lock_id;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.lock_expiration_seconds = value;
                        else
                            delete message.lock_expiration_seconds;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FundPsbtRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.FundPsbtRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.FundPsbtRequest & walletrpc.FundPsbtRequest.$Shape} FundPsbtRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundPsbtRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FundPsbtRequest message.
         * @function verify
         * @memberof walletrpc.FundPsbtRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FundPsbtRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.psbt != null && $Object.hasOwnProperty.call(message, "psbt")) {
                properties.template = 1;
                if (!(message.psbt && typeof message.psbt.length === "number" || $util.isString(message.psbt)))
                    return "psbt: buffer expected";
            }
            if (message.raw != null && $Object.hasOwnProperty.call(message, "raw")) {
                if (properties.template === 1)
                    return "template: multiple values";
                properties.template = 1;
                {
                    let error = $root.walletrpc.TxTemplate.verify(message.raw, _depth + 1);
                    if (error)
                        return "raw." + error;
                }
            }
            if (message.coin_select != null && $Object.hasOwnProperty.call(message, "coin_select")) {
                if (properties.template === 1)
                    return "template: multiple values";
                properties.template = 1;
                {
                    let error = $root.walletrpc.PsbtCoinSelect.verify(message.coin_select, _depth + 1);
                    if (error)
                        return "coin_select." + error;
                }
            }
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf")) {
                properties.fees = 1;
                if (!$util.isInteger(message.target_conf))
                    return "target_conf: integer expected";
            }
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte")) {
                if (properties.fees === 1)
                    return "fees: multiple values";
                properties.fees = 1;
                if (!$util.isInteger(message.sat_per_vbyte) && !(message.sat_per_vbyte && $util.isInteger(message.sat_per_vbyte.low) && $util.isInteger(message.sat_per_vbyte.high)))
                    return "sat_per_vbyte: integer|Long expected";
            }
            if (message.sat_per_kw != null && $Object.hasOwnProperty.call(message, "sat_per_kw")) {
                if (properties.fees === 1)
                    return "fees: multiple values";
                properties.fees = 1;
                if (!$util.isInteger(message.sat_per_kw) && !(message.sat_per_kw && $util.isInteger(message.sat_per_kw.low) && $util.isInteger(message.sat_per_kw.high)))
                    return "sat_per_kw: integer|Long expected";
            }
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                if (!$util.isInteger(message.min_confs))
                    return "min_confs: integer expected";
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                if (typeof message.spend_unconfirmed !== "boolean")
                    return "spend_unconfirmed: boolean expected";
            if (message.change_type != null && $Object.hasOwnProperty.call(message, "change_type"))
                if (typeof message.change_type !== "number" || (message.change_type | 0) !== message.change_type)
                    return "change_type: enum value expected";
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                if (typeof message.coin_selection_strategy !== "number" || (message.coin_selection_strategy | 0) !== message.coin_selection_strategy)
                    return "coin_selection_strategy: enum value expected";
            if (message.max_fee_ratio != null && $Object.hasOwnProperty.call(message, "max_fee_ratio"))
                if (typeof message.max_fee_ratio !== "number")
                    return "max_fee_ratio: number expected";
            if (message.custom_lock_id != null && $Object.hasOwnProperty.call(message, "custom_lock_id"))
                if (!(message.custom_lock_id && typeof message.custom_lock_id.length === "number" || $util.isString(message.custom_lock_id)))
                    return "custom_lock_id: buffer expected";
            if (message.lock_expiration_seconds != null && $Object.hasOwnProperty.call(message, "lock_expiration_seconds"))
                if (!$util.isInteger(message.lock_expiration_seconds) && !(message.lock_expiration_seconds && $util.isInteger(message.lock_expiration_seconds.low) && $util.isInteger(message.lock_expiration_seconds.high)))
                    return "lock_expiration_seconds: integer|Long expected";
            return null;
        };

        /**
         * Creates a FundPsbtRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.FundPsbtRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.FundPsbtRequest} FundPsbtRequest
         */
        FundPsbtRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.FundPsbtRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.FundPsbtRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.FundPsbtRequest();
            if (object.psbt != null)
                if (typeof object.psbt === "string")
                    $util.base64.decode(object.psbt, message.psbt = $util.newBuffer($util.base64.length(object.psbt)), 0);
                else if (object.psbt.length >= 0)
                    message.psbt = object.psbt;
            if (object.raw != null) {
                if (!$util.isObject(object.raw))
                    throw $TypeError(".walletrpc.FundPsbtRequest.raw: object expected");
                message.raw = $root.walletrpc.TxTemplate.fromObject(object.raw, _depth + 1);
            }
            if (object.coin_select != null) {
                if (!$util.isObject(object.coin_select))
                    throw $TypeError(".walletrpc.FundPsbtRequest.coin_select: object expected");
                message.coin_select = $root.walletrpc.PsbtCoinSelect.fromObject(object.coin_select, _depth + 1);
            }
            if (object.target_conf != null)
                message.target_conf = object.target_conf >>> 0;
            if (object.sat_per_vbyte != null)
                if ($util.Long)
                    message.sat_per_vbyte = $util.Long.fromValue(object.sat_per_vbyte, true);
                else if (typeof object.sat_per_vbyte === "string")
                    message.sat_per_vbyte = $parseInt(object.sat_per_vbyte, 10);
                else if (typeof object.sat_per_vbyte === "number")
                    message.sat_per_vbyte = object.sat_per_vbyte;
                else if (typeof object.sat_per_vbyte === "object")
                    message.sat_per_vbyte = new $util.LongBits(object.sat_per_vbyte.low >>> 0, object.sat_per_vbyte.high >>> 0).toNumber(true);
            if (object.sat_per_kw != null)
                if ($util.Long)
                    message.sat_per_kw = $util.Long.fromValue(object.sat_per_kw, true);
                else if (typeof object.sat_per_kw === "string")
                    message.sat_per_kw = $parseInt(object.sat_per_kw, 10);
                else if (typeof object.sat_per_kw === "number")
                    message.sat_per_kw = object.sat_per_kw;
                else if (typeof object.sat_per_kw === "object")
                    message.sat_per_kw = new $util.LongBits(object.sat_per_kw.low >>> 0, object.sat_per_kw.high >>> 0).toNumber(true);
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = $String(object.account);
            if (object.min_confs != null)
                if ($Number(object.min_confs) !== 0)
                    message.min_confs = object.min_confs | 0;
            if (object.spend_unconfirmed != null)
                if (object.spend_unconfirmed)
                    message.spend_unconfirmed = $Boolean(object.spend_unconfirmed);
            if (object.change_type !== 0 && (typeof object.change_type !== "string" || $root.walletrpc.ChangeAddressType[object.change_type] !== 0))
                switch (object.change_type) {
                case "CHANGE_ADDRESS_TYPE_UNSPECIFIED":
                case 0:
                    message.change_type = 0;
                    break;
                case "CHANGE_ADDRESS_TYPE_P2TR":
                case 1:
                    message.change_type = 1;
                    break;
                default:
                    if (typeof object.change_type === "number" && (object.change_type | 0) === object.change_type)
                        message.change_type = object.change_type;
                }
            if (object.coin_selection_strategy !== 0 && (typeof object.coin_selection_strategy !== "string" || $root.lnrpc.CoinSelectionStrategy[object.coin_selection_strategy] !== 0))
                switch (object.coin_selection_strategy) {
                case "STRATEGY_USE_GLOBAL_CONFIG":
                case 0:
                    message.coin_selection_strategy = 0;
                    break;
                case "STRATEGY_LARGEST":
                case 1:
                    message.coin_selection_strategy = 1;
                    break;
                case "STRATEGY_RANDOM":
                case 2:
                    message.coin_selection_strategy = 2;
                    break;
                default:
                    if (typeof object.coin_selection_strategy === "number" && (object.coin_selection_strategy | 0) === object.coin_selection_strategy)
                        message.coin_selection_strategy = object.coin_selection_strategy;
                }
            if (object.max_fee_ratio != null)
                if (!$Object.is($Number(object.max_fee_ratio), 0))
                    message.max_fee_ratio = $Number(object.max_fee_ratio);
            if (object.custom_lock_id != null)
                if (object.custom_lock_id.length)
                    if (typeof object.custom_lock_id === "string")
                        $util.base64.decode(object.custom_lock_id, message.custom_lock_id = $util.newBuffer($util.base64.length(object.custom_lock_id)), 0);
                    else if (object.custom_lock_id.length >= 0)
                        message.custom_lock_id = object.custom_lock_id;
            if (object.lock_expiration_seconds != null)
                if (typeof object.lock_expiration_seconds === "object" ? object.lock_expiration_seconds.low || object.lock_expiration_seconds.high : $Number(object.lock_expiration_seconds) !== 0)
                    if ($util.Long)
                        message.lock_expiration_seconds = $util.Long.fromValue(object.lock_expiration_seconds, true);
                    else if (typeof object.lock_expiration_seconds === "string")
                        message.lock_expiration_seconds = $parseInt(object.lock_expiration_seconds, 10);
                    else if (typeof object.lock_expiration_seconds === "number")
                        message.lock_expiration_seconds = object.lock_expiration_seconds;
                    else if (typeof object.lock_expiration_seconds === "object")
                        message.lock_expiration_seconds = new $util.LongBits(object.lock_expiration_seconds.low >>> 0, object.lock_expiration_seconds.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FundPsbtRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.FundPsbtRequest
         * @static
         * @param {walletrpc.FundPsbtRequest} message FundPsbtRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FundPsbtRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.account = "";
                object.min_confs = 0;
                object.spend_unconfirmed = false;
                object.change_type = options.enums === $String ? "CHANGE_ADDRESS_TYPE_UNSPECIFIED" : 0;
                object.coin_selection_strategy = options.enums === $String ? "STRATEGY_USE_GLOBAL_CONFIG" : 0;
                object.max_fee_ratio = 0;
                if (options.bytes === $String)
                    object.custom_lock_id = "";
                else {
                    object.custom_lock_id = [];
                    if (options.bytes !== $Array)
                        object.custom_lock_id = $util.newBuffer(object.custom_lock_id);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.lock_expiration_seconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.lock_expiration_seconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.psbt != null && $Object.hasOwnProperty.call(message, "psbt")) {
                object.psbt = options.bytes === $String ? $util.base64.encode(message.psbt, 0, message.psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.psbt) : message.psbt;
                if (options.oneofs)
                    object.template = "psbt";
            }
            if (message.raw != null && $Object.hasOwnProperty.call(message, "raw")) {
                object.raw = $root.walletrpc.TxTemplate.toObject(message.raw, options, _depth + 1);
                if (options.oneofs)
                    object.template = "raw";
            }
            if (message.target_conf != null && $Object.hasOwnProperty.call(message, "target_conf")) {
                object.target_conf = message.target_conf;
                if (options.oneofs)
                    object.fees = "target_conf";
            }
            if (message.sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sat_per_vbyte")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_vbyte = typeof message.sat_per_vbyte === "number" ? $BigInt(message.sat_per_vbyte) : $util.Long.fromBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0, true).toBigInt();
                else if (typeof message.sat_per_vbyte === "number")
                    object.sat_per_vbyte = options.longs === $String ? $String(message.sat_per_vbyte) : message.sat_per_vbyte;
                else
                    object.sat_per_vbyte = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_vbyte) : options.longs === $Number ? new $util.LongBits(message.sat_per_vbyte.low >>> 0, message.sat_per_vbyte.high >>> 0).toNumber(true) : message.sat_per_vbyte;
                if (options.oneofs)
                    object.fees = "sat_per_vbyte";
            }
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = message.account;
            if (message.min_confs != null && $Object.hasOwnProperty.call(message, "min_confs"))
                object.min_confs = message.min_confs;
            if (message.spend_unconfirmed != null && $Object.hasOwnProperty.call(message, "spend_unconfirmed"))
                object.spend_unconfirmed = message.spend_unconfirmed;
            if (message.change_type != null && $Object.hasOwnProperty.call(message, "change_type"))
                object.change_type = options.enums === $String ? $root.walletrpc.ChangeAddressType[message.change_type] === $undefined ? message.change_type : $root.walletrpc.ChangeAddressType[message.change_type] : message.change_type;
            if (message.coin_select != null && $Object.hasOwnProperty.call(message, "coin_select")) {
                object.coin_select = $root.walletrpc.PsbtCoinSelect.toObject(message.coin_select, options, _depth + 1);
                if (options.oneofs)
                    object.template = "coin_select";
            }
            if (message.coin_selection_strategy != null && $Object.hasOwnProperty.call(message, "coin_selection_strategy"))
                object.coin_selection_strategy = options.enums === $String ? $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] === $undefined ? message.coin_selection_strategy : $root.lnrpc.CoinSelectionStrategy[message.coin_selection_strategy] : message.coin_selection_strategy;
            if (message.sat_per_kw != null && $Object.hasOwnProperty.call(message, "sat_per_kw")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sat_per_kw = typeof message.sat_per_kw === "number" ? $BigInt(message.sat_per_kw) : $util.Long.fromBits(message.sat_per_kw.low >>> 0, message.sat_per_kw.high >>> 0, true).toBigInt();
                else if (typeof message.sat_per_kw === "number")
                    object.sat_per_kw = options.longs === $String ? $String(message.sat_per_kw) : message.sat_per_kw;
                else
                    object.sat_per_kw = options.longs === $String ? $util.Long.prototype.toString.call(message.sat_per_kw) : options.longs === $Number ? new $util.LongBits(message.sat_per_kw.low >>> 0, message.sat_per_kw.high >>> 0).toNumber(true) : message.sat_per_kw;
                if (options.oneofs)
                    object.fees = "sat_per_kw";
            }
            if (message.max_fee_ratio != null && $Object.hasOwnProperty.call(message, "max_fee_ratio"))
                object.max_fee_ratio = options.json && !$isFinite(message.max_fee_ratio) ? $String(message.max_fee_ratio) : message.max_fee_ratio;
            if (message.custom_lock_id != null && $Object.hasOwnProperty.call(message, "custom_lock_id"))
                object.custom_lock_id = options.bytes === $String ? $util.base64.encode(message.custom_lock_id, 0, message.custom_lock_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.custom_lock_id) : message.custom_lock_id;
            if (message.lock_expiration_seconds != null && $Object.hasOwnProperty.call(message, "lock_expiration_seconds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.lock_expiration_seconds = typeof message.lock_expiration_seconds === "number" ? $BigInt(message.lock_expiration_seconds) : $util.Long.fromBits(message.lock_expiration_seconds.low >>> 0, message.lock_expiration_seconds.high >>> 0, true).toBigInt();
                else if (typeof message.lock_expiration_seconds === "number")
                    object.lock_expiration_seconds = options.longs === $String ? $String(message.lock_expiration_seconds) : message.lock_expiration_seconds;
                else
                    object.lock_expiration_seconds = options.longs === $String ? $util.Long.prototype.toString.call(message.lock_expiration_seconds) : options.longs === $Number ? new $util.LongBits(message.lock_expiration_seconds.low >>> 0, message.lock_expiration_seconds.high >>> 0).toNumber(true) : message.lock_expiration_seconds;
            return object;
        };

        /**
         * Converts this FundPsbtRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.FundPsbtRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FundPsbtRequest.prototype.toJSON = function() {
            return FundPsbtRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FundPsbtRequest
         * @function getTypeUrl
         * @memberof walletrpc.FundPsbtRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FundPsbtRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.FundPsbtRequest";
        };

        return FundPsbtRequest;
    })();

    walletrpc.FundPsbtResponse = (function() {

        /**
         * Properties of a FundPsbtResponse.
         * @typedef {Object} walletrpc.FundPsbtResponse.$Properties
         * @property {Uint8Array|null} [funded_psbt] FundPsbtResponse funded_psbt
         * @property {number|null} [change_output_index] FundPsbtResponse change_output_index
         * @property {Array.<walletrpc.UtxoLease.$Properties>|null} [locked_utxos] FundPsbtResponse locked_utxos
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FundPsbtResponse.
         * @memberof walletrpc
         * @interface IFundPsbtResponse
         * @augments walletrpc.FundPsbtResponse.$Properties
         * @deprecated Use walletrpc.FundPsbtResponse.$Properties instead.
         */

        /**
         * Shape of a FundPsbtResponse.
         * @typedef {walletrpc.FundPsbtResponse.$Properties} walletrpc.FundPsbtResponse.$Shape
         */

        /**
         * Constructs a new FundPsbtResponse.
         * @memberof walletrpc
         * @classdesc Represents a FundPsbtResponse.
         * @constructor
         * @param {walletrpc.FundPsbtResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FundPsbtResponse = function (properties) {
            this.locked_utxos = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FundPsbtResponse funded_psbt.
         * @member {Uint8Array} funded_psbt
         * @memberof walletrpc.FundPsbtResponse
         * @instance
         */
        FundPsbtResponse.prototype.funded_psbt = $util.newBuffer([]);

        /**
         * FundPsbtResponse change_output_index.
         * @member {number} change_output_index
         * @memberof walletrpc.FundPsbtResponse
         * @instance
         */
        FundPsbtResponse.prototype.change_output_index = 0;

        /**
         * FundPsbtResponse locked_utxos.
         * @member {Array.<walletrpc.UtxoLease.$Properties>} locked_utxos
         * @memberof walletrpc.FundPsbtResponse
         * @instance
         */
        FundPsbtResponse.prototype.locked_utxos = $util.emptyArray;

        /**
         * Creates a new FundPsbtResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.FundPsbtResponse
         * @static
         * @param {walletrpc.FundPsbtResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.FundPsbtResponse} FundPsbtResponse instance
         * @type {{
         *   (properties: walletrpc.FundPsbtResponse.$Shape): walletrpc.FundPsbtResponse & walletrpc.FundPsbtResponse.$Shape;
         *   (properties?: walletrpc.FundPsbtResponse.$Properties): walletrpc.FundPsbtResponse;
         * }}
         */
        FundPsbtResponse.create = function(properties) {
            return new FundPsbtResponse(properties);
        };

        /**
         * Encodes the specified FundPsbtResponse message. Does not implicitly {@link walletrpc.FundPsbtResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.FundPsbtResponse
         * @static
         * @param {walletrpc.FundPsbtResponse.$Properties} message FundPsbtResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundPsbtResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt") && message.funded_psbt.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.funded_psbt);
            if (message.change_output_index != null && $Object.hasOwnProperty.call(message, "change_output_index") && message.change_output_index !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.change_output_index);
            if (message.locked_utxos != null && message.locked_utxos.length)
                for (let i = 0; i < message.locked_utxos.length; ++i)
                    $root.walletrpc.UtxoLease.encode(message.locked_utxos[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FundPsbtResponse message, length delimited. Does not implicitly {@link walletrpc.FundPsbtResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.FundPsbtResponse
         * @static
         * @param {walletrpc.FundPsbtResponse.$Properties} message FundPsbtResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FundPsbtResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FundPsbtResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.FundPsbtResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.FundPsbtResponse & walletrpc.FundPsbtResponse.$Shape} FundPsbtResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundPsbtResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.FundPsbtResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.funded_psbt = value;
                        else
                            delete message.funded_psbt;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.change_output_index = value;
                        else
                            delete message.change_output_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.locked_utxos && message.locked_utxos.length))
                            message.locked_utxos = [];
                        message.locked_utxos.push($root.walletrpc.UtxoLease.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FundPsbtResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.FundPsbtResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.FundPsbtResponse & walletrpc.FundPsbtResponse.$Shape} FundPsbtResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FundPsbtResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FundPsbtResponse message.
         * @function verify
         * @memberof walletrpc.FundPsbtResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FundPsbtResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt"))
                if (!(message.funded_psbt && typeof message.funded_psbt.length === "number" || $util.isString(message.funded_psbt)))
                    return "funded_psbt: buffer expected";
            if (message.change_output_index != null && $Object.hasOwnProperty.call(message, "change_output_index"))
                if (!$util.isInteger(message.change_output_index))
                    return "change_output_index: integer expected";
            if (message.locked_utxos != null && $Object.hasOwnProperty.call(message, "locked_utxos")) {
                if (!$Array.isArray(message.locked_utxos))
                    return "locked_utxos: array expected";
                for (let i = 0; i < message.locked_utxos.length; ++i) {
                    let error = $root.walletrpc.UtxoLease.verify(message.locked_utxos[i], _depth + 1);
                    if (error)
                        return "locked_utxos." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FundPsbtResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.FundPsbtResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.FundPsbtResponse} FundPsbtResponse
         */
        FundPsbtResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.FundPsbtResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.FundPsbtResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.FundPsbtResponse();
            if (object.funded_psbt != null)
                if (object.funded_psbt.length)
                    if (typeof object.funded_psbt === "string")
                        $util.base64.decode(object.funded_psbt, message.funded_psbt = $util.newBuffer($util.base64.length(object.funded_psbt)), 0);
                    else if (object.funded_psbt.length >= 0)
                        message.funded_psbt = object.funded_psbt;
            if (object.change_output_index != null)
                if ($Number(object.change_output_index) !== 0)
                    message.change_output_index = object.change_output_index | 0;
            if (object.locked_utxos) {
                if (!$Array.isArray(object.locked_utxos))
                    throw $TypeError(".walletrpc.FundPsbtResponse.locked_utxos: array expected");
                message.locked_utxos = $Array(object.locked_utxos.length);
                for (let i = 0; i < object.locked_utxos.length; ++i) {
                    if (!$util.isObject(object.locked_utxos[i]))
                        throw $TypeError(".walletrpc.FundPsbtResponse.locked_utxos: object expected");
                    message.locked_utxos[i] = $root.walletrpc.UtxoLease.fromObject(object.locked_utxos[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FundPsbtResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.FundPsbtResponse
         * @static
         * @param {walletrpc.FundPsbtResponse} message FundPsbtResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FundPsbtResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.locked_utxos = [];
            if (options.defaults) {
                if (options.bytes === $String)
                    object.funded_psbt = "";
                else {
                    object.funded_psbt = [];
                    if (options.bytes !== $Array)
                        object.funded_psbt = $util.newBuffer(object.funded_psbt);
                }
                object.change_output_index = 0;
            }
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt"))
                object.funded_psbt = options.bytes === $String ? $util.base64.encode(message.funded_psbt, 0, message.funded_psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.funded_psbt) : message.funded_psbt;
            if (message.change_output_index != null && $Object.hasOwnProperty.call(message, "change_output_index"))
                object.change_output_index = message.change_output_index;
            if (message.locked_utxos && message.locked_utxos.length) {
                object.locked_utxos = $Array(message.locked_utxos.length);
                for (let j = 0; j < message.locked_utxos.length; ++j)
                    object.locked_utxos[j] = $root.walletrpc.UtxoLease.toObject(message.locked_utxos[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this FundPsbtResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.FundPsbtResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FundPsbtResponse.prototype.toJSON = function() {
            return FundPsbtResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FundPsbtResponse
         * @function getTypeUrl
         * @memberof walletrpc.FundPsbtResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FundPsbtResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.FundPsbtResponse";
        };

        return FundPsbtResponse;
    })();

    walletrpc.TxTemplate = (function() {

        /**
         * Properties of a TxTemplate.
         * @typedef {Object} walletrpc.TxTemplate.$Properties
         * @property {Array.<lnrpc.OutPoint.$Properties>|null} [inputs] TxTemplate inputs
         * @property {Object.<string,Long>|null} [outputs] TxTemplate outputs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TxTemplate.
         * @memberof walletrpc
         * @interface ITxTemplate
         * @augments walletrpc.TxTemplate.$Properties
         * @deprecated Use walletrpc.TxTemplate.$Properties instead.
         */

        /**
         * Shape of a TxTemplate.
         * @typedef {walletrpc.TxTemplate.$Properties} walletrpc.TxTemplate.$Shape
         */

        /**
         * Constructs a new TxTemplate.
         * @memberof walletrpc
         * @classdesc Represents a TxTemplate.
         * @constructor
         * @param {walletrpc.TxTemplate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TxTemplate = function (properties) {
            this.inputs = [];
            this.outputs = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TxTemplate inputs.
         * @member {Array.<lnrpc.OutPoint.$Properties>} inputs
         * @memberof walletrpc.TxTemplate
         * @instance
         */
        TxTemplate.prototype.inputs = $util.emptyArray;

        /**
         * TxTemplate outputs.
         * @member {Object.<string,Long>} outputs
         * @memberof walletrpc.TxTemplate
         * @instance
         */
        TxTemplate.prototype.outputs = $util.emptyObject;

        /**
         * Creates a new TxTemplate instance using the specified properties.
         * @function create
         * @memberof walletrpc.TxTemplate
         * @static
         * @param {walletrpc.TxTemplate.$Properties=} [properties] Properties to set
         * @returns {walletrpc.TxTemplate} TxTemplate instance
         * @type {{
         *   (properties: walletrpc.TxTemplate.$Shape): walletrpc.TxTemplate & walletrpc.TxTemplate.$Shape;
         *   (properties?: walletrpc.TxTemplate.$Properties): walletrpc.TxTemplate;
         * }}
         */
        TxTemplate.create = function(properties) {
            return new TxTemplate(properties);
        };

        /**
         * Encodes the specified TxTemplate message. Does not implicitly {@link walletrpc.TxTemplate.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.TxTemplate
         * @static
         * @param {walletrpc.TxTemplate.$Properties} message TxTemplate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxTemplate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.inputs != null && message.inputs.length)
                for (let i = 0; i < message.inputs.length; ++i)
                    $root.lnrpc.OutPoint.encode(message.inputs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.outputs != null && $Object.hasOwnProperty.call(message, "outputs"))
                for (let keys = $Object.keys(message.outputs), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).uint64(message.outputs[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TxTemplate message, length delimited. Does not implicitly {@link walletrpc.TxTemplate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.TxTemplate
         * @static
         * @param {walletrpc.TxTemplate.$Properties} message TxTemplate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxTemplate.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TxTemplate message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.TxTemplate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.TxTemplate & walletrpc.TxTemplate.$Shape} TxTemplate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxTemplate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.TxTemplate(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.inputs && message.inputs.length))
                            message.inputs = [];
                        message.inputs.push($root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (message.outputs === $util.emptyObject)
                            message.outputs = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, true) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.uint64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.outputs, key);
                        message.outputs[key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TxTemplate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.TxTemplate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.TxTemplate & walletrpc.TxTemplate.$Shape} TxTemplate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxTemplate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TxTemplate message.
         * @function verify
         * @memberof walletrpc.TxTemplate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TxTemplate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.inputs != null && $Object.hasOwnProperty.call(message, "inputs")) {
                if (!$Array.isArray(message.inputs))
                    return "inputs: array expected";
                for (let i = 0; i < message.inputs.length; ++i) {
                    let error = $root.lnrpc.OutPoint.verify(message.inputs[i], _depth + 1);
                    if (error)
                        return "inputs." + error;
                }
            }
            if (message.outputs != null && $Object.hasOwnProperty.call(message, "outputs")) {
                if (!$util.isObject(message.outputs))
                    return "outputs: object expected";
                let key = $Object.keys(message.outputs);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.outputs[key[i]]) && !(message.outputs[key[i]] && $util.isInteger(message.outputs[key[i]].low) && $util.isInteger(message.outputs[key[i]].high)))
                        return "outputs: integer|Long{k:string} expected";
            }
            return null;
        };

        /**
         * Creates a TxTemplate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.TxTemplate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.TxTemplate} TxTemplate
         */
        TxTemplate.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.TxTemplate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.TxTemplate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.TxTemplate();
            if (object.inputs) {
                if (!$Array.isArray(object.inputs))
                    throw $TypeError(".walletrpc.TxTemplate.inputs: array expected");
                message.inputs = $Array(object.inputs.length);
                for (let i = 0; i < object.inputs.length; ++i) {
                    if (!$util.isObject(object.inputs[i]))
                        throw $TypeError(".walletrpc.TxTemplate.inputs: object expected");
                    message.inputs[i] = $root.lnrpc.OutPoint.fromObject(object.inputs[i], _depth + 1);
                }
            }
            if (object.outputs) {
                if (!$util.isObject(object.outputs))
                    throw $TypeError(".walletrpc.TxTemplate.outputs: object expected");
                message.outputs = {};
                for (let keys = $Object.keys(object.outputs), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.outputs, keys[i]);
                    if ($util.Long)
                        message.outputs[keys[i]] = $util.Long.fromValue(object.outputs[keys[i]], true);
                    else if (typeof object.outputs[keys[i]] === "string")
                        message.outputs[keys[i]] = $parseInt(object.outputs[keys[i]], 10);
                    else if (typeof object.outputs[keys[i]] === "number")
                        message.outputs[keys[i]] = object.outputs[keys[i]];
                    else if (typeof object.outputs[keys[i]] === "object")
                        message.outputs[keys[i]] = new $util.LongBits(object.outputs[keys[i]].low >>> 0, object.outputs[keys[i]].high >>> 0).toNumber(true);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TxTemplate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.TxTemplate
         * @static
         * @param {walletrpc.TxTemplate} message TxTemplate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TxTemplate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.inputs = [];
            if (options.objects || options.defaults)
                object.outputs = {};
            if (message.inputs && message.inputs.length) {
                object.inputs = $Array(message.inputs.length);
                for (let j = 0; j < message.inputs.length; ++j)
                    object.inputs[j] = $root.lnrpc.OutPoint.toObject(message.inputs[j], options, _depth + 1);
            }
            let keys2;
            if (message.outputs && (keys2 = $Object.keys(message.outputs)).length) {
                object.outputs = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.outputs, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.outputs[keys2[j]] = typeof message.outputs[keys2[j]] === "number" ? $BigInt(message.outputs[keys2[j]]) : $util.Long.fromBits(message.outputs[keys2[j]].low >>> 0, message.outputs[keys2[j]].high >>> 0, true).toBigInt();
                    else if (typeof message.outputs[keys2[j]] === "number")
                        object.outputs[keys2[j]] = options.longs === $String ? $String(message.outputs[keys2[j]]) : message.outputs[keys2[j]];
                    else
                        object.outputs[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.outputs[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.outputs[keys2[j]].low >>> 0, message.outputs[keys2[j]].high >>> 0).toNumber(true) : message.outputs[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this TxTemplate to JSON.
         * @function toJSON
         * @memberof walletrpc.TxTemplate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TxTemplate.prototype.toJSON = function() {
            return TxTemplate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TxTemplate
         * @function getTypeUrl
         * @memberof walletrpc.TxTemplate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TxTemplate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.TxTemplate";
        };

        return TxTemplate;
    })();

    walletrpc.PsbtCoinSelect = (function() {

        /**
         * Properties of a PsbtCoinSelect.
         * @typedef {Object} walletrpc.PsbtCoinSelect.$Properties
         * @property {Uint8Array|null} [psbt] PsbtCoinSelect psbt
         * @property {number|null} [existing_output_index] PsbtCoinSelect existing_output_index
         * @property {boolean|null} [add] PsbtCoinSelect add
         * @property {"existing_output_index"|"add"} [change_output] PsbtCoinSelect change_output
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PsbtCoinSelect.
         * @memberof walletrpc
         * @interface IPsbtCoinSelect
         * @augments walletrpc.PsbtCoinSelect.$Properties
         * @deprecated Use walletrpc.PsbtCoinSelect.$Properties instead.
         */

        /**
         * Narrowed shape of a PsbtCoinSelect.
         * @typedef {{
         *   psbt?: Uint8Array|null;
         *   existing_output_index?: number|null;
         *   add?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ change_output?: undefined; existing_output_index?: null; add?: null }|{ change_output?: "existing_output_index"; existing_output_index: number; add?: null }|{ change_output?: "add"; existing_output_index?: null; add: boolean })
         * )} walletrpc.PsbtCoinSelect.$Shape
         */

        /**
         * Constructs a new PsbtCoinSelect.
         * @memberof walletrpc
         * @classdesc Represents a PsbtCoinSelect.
         * @constructor
         * @param {walletrpc.PsbtCoinSelect.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PsbtCoinSelect = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PsbtCoinSelect psbt.
         * @member {Uint8Array} psbt
         * @memberof walletrpc.PsbtCoinSelect
         * @instance
         */
        PsbtCoinSelect.prototype.psbt = $util.newBuffer([]);

        /**
         * PsbtCoinSelect existing_output_index.
         * @member {number|null|undefined} existing_output_index
         * @memberof walletrpc.PsbtCoinSelect
         * @instance
         */
        PsbtCoinSelect.prototype.existing_output_index = null;

        /**
         * PsbtCoinSelect add.
         * @member {boolean|null|undefined} add
         * @memberof walletrpc.PsbtCoinSelect
         * @instance
         */
        PsbtCoinSelect.prototype.add = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * PsbtCoinSelect change_output.
         * @member {"existing_output_index"|"add"|undefined} change_output
         * @memberof walletrpc.PsbtCoinSelect
         * @instance
         */
        $Object.defineProperty(PsbtCoinSelect.prototype, "change_output", {
            get: $util.oneOfGetter($oneOfFields = ["existing_output_index", "add"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PsbtCoinSelect instance using the specified properties.
         * @function create
         * @memberof walletrpc.PsbtCoinSelect
         * @static
         * @param {walletrpc.PsbtCoinSelect.$Properties=} [properties] Properties to set
         * @returns {walletrpc.PsbtCoinSelect} PsbtCoinSelect instance
         * @type {{
         *   (properties: walletrpc.PsbtCoinSelect.$Shape): walletrpc.PsbtCoinSelect & walletrpc.PsbtCoinSelect.$Shape;
         *   (properties?: walletrpc.PsbtCoinSelect.$Properties): walletrpc.PsbtCoinSelect;
         * }}
         */
        PsbtCoinSelect.create = function(properties) {
            return new PsbtCoinSelect(properties);
        };

        /**
         * Encodes the specified PsbtCoinSelect message. Does not implicitly {@link walletrpc.PsbtCoinSelect.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.PsbtCoinSelect
         * @static
         * @param {walletrpc.PsbtCoinSelect.$Properties} message PsbtCoinSelect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PsbtCoinSelect.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.psbt != null && $Object.hasOwnProperty.call(message, "psbt") && message.psbt.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.psbt);
            if (message.existing_output_index != null && $Object.hasOwnProperty.call(message, "existing_output_index"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.existing_output_index);
            if (message.add != null && $Object.hasOwnProperty.call(message, "add"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.add);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PsbtCoinSelect message, length delimited. Does not implicitly {@link walletrpc.PsbtCoinSelect.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.PsbtCoinSelect
         * @static
         * @param {walletrpc.PsbtCoinSelect.$Properties} message PsbtCoinSelect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PsbtCoinSelect.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PsbtCoinSelect message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.PsbtCoinSelect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.PsbtCoinSelect & walletrpc.PsbtCoinSelect.$Shape} PsbtCoinSelect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PsbtCoinSelect.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.PsbtCoinSelect(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.psbt = value;
                        else
                            delete message.psbt;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        message.existing_output_index = reader.int32();
                        message.change_output = "existing_output_index";
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        message.add = reader.bool();
                        message.change_output = "add";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PsbtCoinSelect message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.PsbtCoinSelect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.PsbtCoinSelect & walletrpc.PsbtCoinSelect.$Shape} PsbtCoinSelect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PsbtCoinSelect.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PsbtCoinSelect message.
         * @function verify
         * @memberof walletrpc.PsbtCoinSelect
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PsbtCoinSelect.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.psbt != null && $Object.hasOwnProperty.call(message, "psbt"))
                if (!(message.psbt && typeof message.psbt.length === "number" || $util.isString(message.psbt)))
                    return "psbt: buffer expected";
            if (message.existing_output_index != null && $Object.hasOwnProperty.call(message, "existing_output_index")) {
                properties.change_output = 1;
                if (!$util.isInteger(message.existing_output_index))
                    return "existing_output_index: integer expected";
            }
            if (message.add != null && $Object.hasOwnProperty.call(message, "add")) {
                if (properties.change_output === 1)
                    return "change_output: multiple values";
                properties.change_output = 1;
                if (typeof message.add !== "boolean")
                    return "add: boolean expected";
            }
            return null;
        };

        /**
         * Creates a PsbtCoinSelect message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.PsbtCoinSelect
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.PsbtCoinSelect} PsbtCoinSelect
         */
        PsbtCoinSelect.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.PsbtCoinSelect)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.PsbtCoinSelect: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.PsbtCoinSelect();
            if (object.psbt != null)
                if (object.psbt.length)
                    if (typeof object.psbt === "string")
                        $util.base64.decode(object.psbt, message.psbt = $util.newBuffer($util.base64.length(object.psbt)), 0);
                    else if (object.psbt.length >= 0)
                        message.psbt = object.psbt;
            if (object.existing_output_index != null)
                message.existing_output_index = object.existing_output_index | 0;
            if (object.add != null)
                message.add = $Boolean(object.add);
            return message;
        };

        /**
         * Creates a plain object from a PsbtCoinSelect message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.PsbtCoinSelect
         * @static
         * @param {walletrpc.PsbtCoinSelect} message PsbtCoinSelect
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PsbtCoinSelect.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.psbt = "";
                else {
                    object.psbt = [];
                    if (options.bytes !== $Array)
                        object.psbt = $util.newBuffer(object.psbt);
                }
            if (message.psbt != null && $Object.hasOwnProperty.call(message, "psbt"))
                object.psbt = options.bytes === $String ? $util.base64.encode(message.psbt, 0, message.psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.psbt) : message.psbt;
            if (message.existing_output_index != null && $Object.hasOwnProperty.call(message, "existing_output_index")) {
                object.existing_output_index = message.existing_output_index;
                if (options.oneofs)
                    object.change_output = "existing_output_index";
            }
            if (message.add != null && $Object.hasOwnProperty.call(message, "add")) {
                object.add = message.add;
                if (options.oneofs)
                    object.change_output = "add";
            }
            return object;
        };

        /**
         * Converts this PsbtCoinSelect to JSON.
         * @function toJSON
         * @memberof walletrpc.PsbtCoinSelect
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PsbtCoinSelect.prototype.toJSON = function() {
            return PsbtCoinSelect.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PsbtCoinSelect
         * @function getTypeUrl
         * @memberof walletrpc.PsbtCoinSelect
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PsbtCoinSelect.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.PsbtCoinSelect";
        };

        return PsbtCoinSelect;
    })();

    walletrpc.UtxoLease = (function() {

        /**
         * Properties of an UtxoLease.
         * @typedef {Object} walletrpc.UtxoLease.$Properties
         * @property {Uint8Array|null} [id] UtxoLease id
         * @property {lnrpc.OutPoint.$Properties|null} [outpoint] UtxoLease outpoint
         * @property {Long|null} [expiration] UtxoLease expiration
         * @property {Uint8Array|null} [pk_script] UtxoLease pk_script
         * @property {Long|null} [value] UtxoLease value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an UtxoLease.
         * @memberof walletrpc
         * @interface IUtxoLease
         * @augments walletrpc.UtxoLease.$Properties
         * @deprecated Use walletrpc.UtxoLease.$Properties instead.
         */

        /**
         * Shape of an UtxoLease.
         * @typedef {walletrpc.UtxoLease.$Properties} walletrpc.UtxoLease.$Shape
         */

        /**
         * Constructs a new UtxoLease.
         * @memberof walletrpc
         * @classdesc Represents an UtxoLease.
         * @constructor
         * @param {walletrpc.UtxoLease.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const UtxoLease = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * UtxoLease id.
         * @member {Uint8Array} id
         * @memberof walletrpc.UtxoLease
         * @instance
         */
        UtxoLease.prototype.id = $util.newBuffer([]);

        /**
         * UtxoLease outpoint.
         * @member {lnrpc.OutPoint.$Properties|null|undefined} outpoint
         * @memberof walletrpc.UtxoLease
         * @instance
         */
        UtxoLease.prototype.outpoint = null;

        /**
         * UtxoLease expiration.
         * @member {Long} expiration
         * @memberof walletrpc.UtxoLease
         * @instance
         */
        UtxoLease.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * UtxoLease pk_script.
         * @member {Uint8Array} pk_script
         * @memberof walletrpc.UtxoLease
         * @instance
         */
        UtxoLease.prototype.pk_script = $util.newBuffer([]);

        /**
         * UtxoLease value.
         * @member {Long} value
         * @memberof walletrpc.UtxoLease
         * @instance
         */
        UtxoLease.prototype.value = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new UtxoLease instance using the specified properties.
         * @function create
         * @memberof walletrpc.UtxoLease
         * @static
         * @param {walletrpc.UtxoLease.$Properties=} [properties] Properties to set
         * @returns {walletrpc.UtxoLease} UtxoLease instance
         * @type {{
         *   (properties: walletrpc.UtxoLease.$Shape): walletrpc.UtxoLease & walletrpc.UtxoLease.$Shape;
         *   (properties?: walletrpc.UtxoLease.$Properties): walletrpc.UtxoLease;
         * }}
         */
        UtxoLease.create = function(properties) {
            return new UtxoLease(properties);
        };

        /**
         * Encodes the specified UtxoLease message. Does not implicitly {@link walletrpc.UtxoLease.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.UtxoLease
         * @static
         * @param {walletrpc.UtxoLease.$Properties} message UtxoLease message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UtxoLease.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.id);
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                $root.lnrpc.OutPoint.encode(message.outpoint, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.expiration != null && $Object.hasOwnProperty.call(message, "expiration") && (typeof message.expiration === "object" ? message.expiration.low || message.expiration.high : message.expiration !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.expiration);
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script") && message.pk_script.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.pk_script);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UtxoLease message, length delimited. Does not implicitly {@link walletrpc.UtxoLease.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.UtxoLease
         * @static
         * @param {walletrpc.UtxoLease.$Properties} message UtxoLease message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UtxoLease.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an UtxoLease message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.UtxoLease
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.UtxoLease & walletrpc.UtxoLease.$Shape} UtxoLease
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UtxoLease.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.UtxoLease(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.outpoint = $root.lnrpc.OutPoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.outpoint);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiration = value;
                        else
                            delete message.expiration;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pk_script = value;
                        else
                            delete message.pk_script;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UtxoLease message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.UtxoLease
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.UtxoLease & walletrpc.UtxoLease.$Shape} UtxoLease
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UtxoLease.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UtxoLease message.
         * @function verify
         * @memberof walletrpc.UtxoLease
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UtxoLease.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!(message.id && typeof message.id.length === "number" || $util.isString(message.id)))
                    return "id: buffer expected";
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint")) {
                let error = $root.lnrpc.OutPoint.verify(message.outpoint, _depth + 1);
                if (error)
                    return "outpoint." + error;
            }
            if (message.expiration != null && $Object.hasOwnProperty.call(message, "expiration"))
                if (!$util.isInteger(message.expiration) && !(message.expiration && $util.isInteger(message.expiration.low) && $util.isInteger(message.expiration.high)))
                    return "expiration: integer|Long expected";
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                if (!(message.pk_script && typeof message.pk_script.length === "number" || $util.isString(message.pk_script)))
                    return "pk_script: buffer expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                    return "value: integer|Long expected";
            return null;
        };

        /**
         * Creates an UtxoLease message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.UtxoLease
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.UtxoLease} UtxoLease
         */
        UtxoLease.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.UtxoLease)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.UtxoLease: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.UtxoLease();
            if (object.id != null)
                if (object.id.length)
                    if (typeof object.id === "string")
                        $util.base64.decode(object.id, message.id = $util.newBuffer($util.base64.length(object.id)), 0);
                    else if (object.id.length >= 0)
                        message.id = object.id;
            if (object.outpoint != null) {
                if (!$util.isObject(object.outpoint))
                    throw $TypeError(".walletrpc.UtxoLease.outpoint: object expected");
                message.outpoint = $root.lnrpc.OutPoint.fromObject(object.outpoint, _depth + 1);
            }
            if (object.expiration != null)
                if (typeof object.expiration === "object" ? object.expiration.low || object.expiration.high : $Number(object.expiration) !== 0)
                    if ($util.Long)
                        message.expiration = $util.Long.fromValue(object.expiration, true);
                    else if (typeof object.expiration === "string")
                        message.expiration = $parseInt(object.expiration, 10);
                    else if (typeof object.expiration === "number")
                        message.expiration = object.expiration;
                    else if (typeof object.expiration === "object")
                        message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber(true);
            if (object.pk_script != null)
                if (object.pk_script.length)
                    if (typeof object.pk_script === "string")
                        $util.base64.decode(object.pk_script, message.pk_script = $util.newBuffer($util.base64.length(object.pk_script)), 0);
                    else if (object.pk_script.length >= 0)
                        message.pk_script = object.pk_script;
            if (object.value != null)
                if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                    if ($util.Long)
                        message.value = $util.Long.fromValue(object.value, true);
                    else if (typeof object.value === "string")
                        message.value = $parseInt(object.value, 10);
                    else if (typeof object.value === "number")
                        message.value = object.value;
                    else if (typeof object.value === "object")
                        message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from an UtxoLease message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.UtxoLease
         * @static
         * @param {walletrpc.UtxoLease} message UtxoLease
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UtxoLease.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.id = "";
                else {
                    object.id = [];
                    if (options.bytes !== $Array)
                        object.id = $util.newBuffer(object.id);
                }
                object.outpoint = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.expiration = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.pk_script = "";
                else {
                    object.pk_script = [];
                    if (options.bytes !== $Array)
                        object.pk_script = $util.newBuffer(object.pk_script);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = options.bytes === $String ? $util.base64.encode(message.id, 0, message.id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.id) : message.id;
            if (message.outpoint != null && $Object.hasOwnProperty.call(message, "outpoint"))
                object.outpoint = $root.lnrpc.OutPoint.toObject(message.outpoint, options, _depth + 1);
            if (message.expiration != null && $Object.hasOwnProperty.call(message, "expiration"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiration = typeof message.expiration === "number" ? $BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, true).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === $String ? $String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === $String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === $Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber(true) : message.expiration;
            if (message.pk_script != null && $Object.hasOwnProperty.call(message, "pk_script"))
                object.pk_script = options.bytes === $String ? $util.base64.encode(message.pk_script, 0, message.pk_script.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pk_script) : message.pk_script;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, true).toBigInt();
                else if (typeof message.value === "number")
                    object.value = options.longs === $String ? $String(message.value) : message.value;
                else
                    object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber(true) : message.value;
            return object;
        };

        /**
         * Converts this UtxoLease to JSON.
         * @function toJSON
         * @memberof walletrpc.UtxoLease
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UtxoLease.prototype.toJSON = function() {
            return UtxoLease.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UtxoLease
         * @function getTypeUrl
         * @memberof walletrpc.UtxoLease
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UtxoLease.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.UtxoLease";
        };

        return UtxoLease;
    })();

    walletrpc.SignPsbtRequest = (function() {

        /**
         * Properties of a SignPsbtRequest.
         * @typedef {Object} walletrpc.SignPsbtRequest.$Properties
         * @property {Uint8Array|null} [funded_psbt] SignPsbtRequest funded_psbt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignPsbtRequest.
         * @memberof walletrpc
         * @interface ISignPsbtRequest
         * @augments walletrpc.SignPsbtRequest.$Properties
         * @deprecated Use walletrpc.SignPsbtRequest.$Properties instead.
         */

        /**
         * Shape of a SignPsbtRequest.
         * @typedef {walletrpc.SignPsbtRequest.$Properties} walletrpc.SignPsbtRequest.$Shape
         */

        /**
         * Constructs a new SignPsbtRequest.
         * @memberof walletrpc
         * @classdesc Represents a SignPsbtRequest.
         * @constructor
         * @param {walletrpc.SignPsbtRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignPsbtRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignPsbtRequest funded_psbt.
         * @member {Uint8Array} funded_psbt
         * @memberof walletrpc.SignPsbtRequest
         * @instance
         */
        SignPsbtRequest.prototype.funded_psbt = $util.newBuffer([]);

        /**
         * Creates a new SignPsbtRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.SignPsbtRequest
         * @static
         * @param {walletrpc.SignPsbtRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.SignPsbtRequest} SignPsbtRequest instance
         * @type {{
         *   (properties: walletrpc.SignPsbtRequest.$Shape): walletrpc.SignPsbtRequest & walletrpc.SignPsbtRequest.$Shape;
         *   (properties?: walletrpc.SignPsbtRequest.$Properties): walletrpc.SignPsbtRequest;
         * }}
         */
        SignPsbtRequest.create = function(properties) {
            return new SignPsbtRequest(properties);
        };

        /**
         * Encodes the specified SignPsbtRequest message. Does not implicitly {@link walletrpc.SignPsbtRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.SignPsbtRequest
         * @static
         * @param {walletrpc.SignPsbtRequest.$Properties} message SignPsbtRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignPsbtRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt") && message.funded_psbt.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.funded_psbt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignPsbtRequest message, length delimited. Does not implicitly {@link walletrpc.SignPsbtRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.SignPsbtRequest
         * @static
         * @param {walletrpc.SignPsbtRequest.$Properties} message SignPsbtRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignPsbtRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignPsbtRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.SignPsbtRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.SignPsbtRequest & walletrpc.SignPsbtRequest.$Shape} SignPsbtRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignPsbtRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.SignPsbtRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.funded_psbt = value;
                        else
                            delete message.funded_psbt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignPsbtRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.SignPsbtRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.SignPsbtRequest & walletrpc.SignPsbtRequest.$Shape} SignPsbtRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignPsbtRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignPsbtRequest message.
         * @function verify
         * @memberof walletrpc.SignPsbtRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignPsbtRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt"))
                if (!(message.funded_psbt && typeof message.funded_psbt.length === "number" || $util.isString(message.funded_psbt)))
                    return "funded_psbt: buffer expected";
            return null;
        };

        /**
         * Creates a SignPsbtRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.SignPsbtRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.SignPsbtRequest} SignPsbtRequest
         */
        SignPsbtRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.SignPsbtRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.SignPsbtRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.SignPsbtRequest();
            if (object.funded_psbt != null)
                if (object.funded_psbt.length)
                    if (typeof object.funded_psbt === "string")
                        $util.base64.decode(object.funded_psbt, message.funded_psbt = $util.newBuffer($util.base64.length(object.funded_psbt)), 0);
                    else if (object.funded_psbt.length >= 0)
                        message.funded_psbt = object.funded_psbt;
            return message;
        };

        /**
         * Creates a plain object from a SignPsbtRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.SignPsbtRequest
         * @static
         * @param {walletrpc.SignPsbtRequest} message SignPsbtRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignPsbtRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.funded_psbt = "";
                else {
                    object.funded_psbt = [];
                    if (options.bytes !== $Array)
                        object.funded_psbt = $util.newBuffer(object.funded_psbt);
                }
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt"))
                object.funded_psbt = options.bytes === $String ? $util.base64.encode(message.funded_psbt, 0, message.funded_psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.funded_psbt) : message.funded_psbt;
            return object;
        };

        /**
         * Converts this SignPsbtRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.SignPsbtRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignPsbtRequest.prototype.toJSON = function() {
            return SignPsbtRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignPsbtRequest
         * @function getTypeUrl
         * @memberof walletrpc.SignPsbtRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignPsbtRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.SignPsbtRequest";
        };

        return SignPsbtRequest;
    })();

    walletrpc.SignPsbtResponse = (function() {

        /**
         * Properties of a SignPsbtResponse.
         * @typedef {Object} walletrpc.SignPsbtResponse.$Properties
         * @property {Uint8Array|null} [signed_psbt] SignPsbtResponse signed_psbt
         * @property {Array.<number>|null} [signed_inputs] SignPsbtResponse signed_inputs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a SignPsbtResponse.
         * @memberof walletrpc
         * @interface ISignPsbtResponse
         * @augments walletrpc.SignPsbtResponse.$Properties
         * @deprecated Use walletrpc.SignPsbtResponse.$Properties instead.
         */

        /**
         * Shape of a SignPsbtResponse.
         * @typedef {walletrpc.SignPsbtResponse.$Properties} walletrpc.SignPsbtResponse.$Shape
         */

        /**
         * Constructs a new SignPsbtResponse.
         * @memberof walletrpc
         * @classdesc Represents a SignPsbtResponse.
         * @constructor
         * @param {walletrpc.SignPsbtResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const SignPsbtResponse = function (properties) {
            this.signed_inputs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SignPsbtResponse signed_psbt.
         * @member {Uint8Array} signed_psbt
         * @memberof walletrpc.SignPsbtResponse
         * @instance
         */
        SignPsbtResponse.prototype.signed_psbt = $util.newBuffer([]);

        /**
         * SignPsbtResponse signed_inputs.
         * @member {Array.<number>} signed_inputs
         * @memberof walletrpc.SignPsbtResponse
         * @instance
         */
        SignPsbtResponse.prototype.signed_inputs = $util.emptyArray;

        /**
         * Creates a new SignPsbtResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.SignPsbtResponse
         * @static
         * @param {walletrpc.SignPsbtResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.SignPsbtResponse} SignPsbtResponse instance
         * @type {{
         *   (properties: walletrpc.SignPsbtResponse.$Shape): walletrpc.SignPsbtResponse & walletrpc.SignPsbtResponse.$Shape;
         *   (properties?: walletrpc.SignPsbtResponse.$Properties): walletrpc.SignPsbtResponse;
         * }}
         */
        SignPsbtResponse.create = function(properties) {
            return new SignPsbtResponse(properties);
        };

        /**
         * Encodes the specified SignPsbtResponse message. Does not implicitly {@link walletrpc.SignPsbtResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.SignPsbtResponse
         * @static
         * @param {walletrpc.SignPsbtResponse.$Properties} message SignPsbtResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignPsbtResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.signed_psbt != null && $Object.hasOwnProperty.call(message, "signed_psbt") && message.signed_psbt.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.signed_psbt);
            if (message.signed_inputs != null && message.signed_inputs.length)
                writer.uint32(/* id 2, wireType 2 =*/18).uint32s(message.signed_inputs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignPsbtResponse message, length delimited. Does not implicitly {@link walletrpc.SignPsbtResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.SignPsbtResponse
         * @static
         * @param {walletrpc.SignPsbtResponse.$Properties} message SignPsbtResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignPsbtResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignPsbtResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.SignPsbtResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.SignPsbtResponse & walletrpc.SignPsbtResponse.$Shape} SignPsbtResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignPsbtResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.SignPsbtResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.signed_psbt = value;
                        else
                            delete message.signed_psbt;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.signed_inputs && message.signed_inputs.length))
                                message.signed_inputs = [];
                            reader.uint32s(message.signed_inputs);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.signed_inputs && message.signed_inputs.length))
                            message.signed_inputs = [];
                        message.signed_inputs.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignPsbtResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.SignPsbtResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.SignPsbtResponse & walletrpc.SignPsbtResponse.$Shape} SignPsbtResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignPsbtResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignPsbtResponse message.
         * @function verify
         * @memberof walletrpc.SignPsbtResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignPsbtResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.signed_psbt != null && $Object.hasOwnProperty.call(message, "signed_psbt"))
                if (!(message.signed_psbt && typeof message.signed_psbt.length === "number" || $util.isString(message.signed_psbt)))
                    return "signed_psbt: buffer expected";
            if (message.signed_inputs != null && $Object.hasOwnProperty.call(message, "signed_inputs")) {
                if (!$Array.isArray(message.signed_inputs))
                    return "signed_inputs: array expected";
                for (let i = 0; i < message.signed_inputs.length; ++i)
                    if (!$util.isInteger(message.signed_inputs[i]))
                        return "signed_inputs: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SignPsbtResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.SignPsbtResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.SignPsbtResponse} SignPsbtResponse
         */
        SignPsbtResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.SignPsbtResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.SignPsbtResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.SignPsbtResponse();
            if (object.signed_psbt != null)
                if (object.signed_psbt.length)
                    if (typeof object.signed_psbt === "string")
                        $util.base64.decode(object.signed_psbt, message.signed_psbt = $util.newBuffer($util.base64.length(object.signed_psbt)), 0);
                    else if (object.signed_psbt.length >= 0)
                        message.signed_psbt = object.signed_psbt;
            if (object.signed_inputs) {
                if (!$Array.isArray(object.signed_inputs))
                    throw $TypeError(".walletrpc.SignPsbtResponse.signed_inputs: array expected");
                message.signed_inputs = $Array(object.signed_inputs.length);
                for (let i = 0; i < object.signed_inputs.length; ++i)
                    message.signed_inputs[i] = object.signed_inputs[i] >>> 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SignPsbtResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.SignPsbtResponse
         * @static
         * @param {walletrpc.SignPsbtResponse} message SignPsbtResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignPsbtResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.signed_inputs = [];
            if (options.defaults)
                if (options.bytes === $String)
                    object.signed_psbt = "";
                else {
                    object.signed_psbt = [];
                    if (options.bytes !== $Array)
                        object.signed_psbt = $util.newBuffer(object.signed_psbt);
                }
            if (message.signed_psbt != null && $Object.hasOwnProperty.call(message, "signed_psbt"))
                object.signed_psbt = options.bytes === $String ? $util.base64.encode(message.signed_psbt, 0, message.signed_psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signed_psbt) : message.signed_psbt;
            if (message.signed_inputs && message.signed_inputs.length) {
                object.signed_inputs = $Array(message.signed_inputs.length);
                for (let j = 0; j < message.signed_inputs.length; ++j)
                    object.signed_inputs[j] = message.signed_inputs[j];
            }
            return object;
        };

        /**
         * Converts this SignPsbtResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.SignPsbtResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignPsbtResponse.prototype.toJSON = function() {
            return SignPsbtResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignPsbtResponse
         * @function getTypeUrl
         * @memberof walletrpc.SignPsbtResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignPsbtResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.SignPsbtResponse";
        };

        return SignPsbtResponse;
    })();

    walletrpc.FinalizePsbtRequest = (function() {

        /**
         * Properties of a FinalizePsbtRequest.
         * @typedef {Object} walletrpc.FinalizePsbtRequest.$Properties
         * @property {Uint8Array|null} [funded_psbt] FinalizePsbtRequest funded_psbt
         * @property {string|null} [account] FinalizePsbtRequest account
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FinalizePsbtRequest.
         * @memberof walletrpc
         * @interface IFinalizePsbtRequest
         * @augments walletrpc.FinalizePsbtRequest.$Properties
         * @deprecated Use walletrpc.FinalizePsbtRequest.$Properties instead.
         */

        /**
         * Shape of a FinalizePsbtRequest.
         * @typedef {walletrpc.FinalizePsbtRequest.$Properties} walletrpc.FinalizePsbtRequest.$Shape
         */

        /**
         * Constructs a new FinalizePsbtRequest.
         * @memberof walletrpc
         * @classdesc Represents a FinalizePsbtRequest.
         * @constructor
         * @param {walletrpc.FinalizePsbtRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FinalizePsbtRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FinalizePsbtRequest funded_psbt.
         * @member {Uint8Array} funded_psbt
         * @memberof walletrpc.FinalizePsbtRequest
         * @instance
         */
        FinalizePsbtRequest.prototype.funded_psbt = $util.newBuffer([]);

        /**
         * FinalizePsbtRequest account.
         * @member {string} account
         * @memberof walletrpc.FinalizePsbtRequest
         * @instance
         */
        FinalizePsbtRequest.prototype.account = "";

        /**
         * Creates a new FinalizePsbtRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.FinalizePsbtRequest
         * @static
         * @param {walletrpc.FinalizePsbtRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.FinalizePsbtRequest} FinalizePsbtRequest instance
         * @type {{
         *   (properties: walletrpc.FinalizePsbtRequest.$Shape): walletrpc.FinalizePsbtRequest & walletrpc.FinalizePsbtRequest.$Shape;
         *   (properties?: walletrpc.FinalizePsbtRequest.$Properties): walletrpc.FinalizePsbtRequest;
         * }}
         */
        FinalizePsbtRequest.create = function(properties) {
            return new FinalizePsbtRequest(properties);
        };

        /**
         * Encodes the specified FinalizePsbtRequest message. Does not implicitly {@link walletrpc.FinalizePsbtRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.FinalizePsbtRequest
         * @static
         * @param {walletrpc.FinalizePsbtRequest.$Properties} message FinalizePsbtRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FinalizePsbtRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt") && message.funded_psbt.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.funded_psbt);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account") && message.account !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.account);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FinalizePsbtRequest message, length delimited. Does not implicitly {@link walletrpc.FinalizePsbtRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.FinalizePsbtRequest
         * @static
         * @param {walletrpc.FinalizePsbtRequest.$Properties} message FinalizePsbtRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FinalizePsbtRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FinalizePsbtRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.FinalizePsbtRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.FinalizePsbtRequest & walletrpc.FinalizePsbtRequest.$Shape} FinalizePsbtRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FinalizePsbtRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.FinalizePsbtRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.funded_psbt = value;
                        else
                            delete message.funded_psbt;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FinalizePsbtRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.FinalizePsbtRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.FinalizePsbtRequest & walletrpc.FinalizePsbtRequest.$Shape} FinalizePsbtRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FinalizePsbtRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FinalizePsbtRequest message.
         * @function verify
         * @memberof walletrpc.FinalizePsbtRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FinalizePsbtRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt"))
                if (!(message.funded_psbt && typeof message.funded_psbt.length === "number" || $util.isString(message.funded_psbt)))
                    return "funded_psbt: buffer expected";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            return null;
        };

        /**
         * Creates a FinalizePsbtRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.FinalizePsbtRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.FinalizePsbtRequest} FinalizePsbtRequest
         */
        FinalizePsbtRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.FinalizePsbtRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.FinalizePsbtRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.FinalizePsbtRequest();
            if (object.funded_psbt != null)
                if (object.funded_psbt.length)
                    if (typeof object.funded_psbt === "string")
                        $util.base64.decode(object.funded_psbt, message.funded_psbt = $util.newBuffer($util.base64.length(object.funded_psbt)), 0);
                    else if (object.funded_psbt.length >= 0)
                        message.funded_psbt = object.funded_psbt;
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = $String(object.account);
            return message;
        };

        /**
         * Creates a plain object from a FinalizePsbtRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.FinalizePsbtRequest
         * @static
         * @param {walletrpc.FinalizePsbtRequest} message FinalizePsbtRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FinalizePsbtRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.funded_psbt = "";
                else {
                    object.funded_psbt = [];
                    if (options.bytes !== $Array)
                        object.funded_psbt = $util.newBuffer(object.funded_psbt);
                }
                object.account = "";
            }
            if (message.funded_psbt != null && $Object.hasOwnProperty.call(message, "funded_psbt"))
                object.funded_psbt = options.bytes === $String ? $util.base64.encode(message.funded_psbt, 0, message.funded_psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.funded_psbt) : message.funded_psbt;
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = message.account;
            return object;
        };

        /**
         * Converts this FinalizePsbtRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.FinalizePsbtRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FinalizePsbtRequest.prototype.toJSON = function() {
            return FinalizePsbtRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FinalizePsbtRequest
         * @function getTypeUrl
         * @memberof walletrpc.FinalizePsbtRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FinalizePsbtRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.FinalizePsbtRequest";
        };

        return FinalizePsbtRequest;
    })();

    walletrpc.FinalizePsbtResponse = (function() {

        /**
         * Properties of a FinalizePsbtResponse.
         * @typedef {Object} walletrpc.FinalizePsbtResponse.$Properties
         * @property {Uint8Array|null} [signed_psbt] FinalizePsbtResponse signed_psbt
         * @property {Uint8Array|null} [raw_final_tx] FinalizePsbtResponse raw_final_tx
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a FinalizePsbtResponse.
         * @memberof walletrpc
         * @interface IFinalizePsbtResponse
         * @augments walletrpc.FinalizePsbtResponse.$Properties
         * @deprecated Use walletrpc.FinalizePsbtResponse.$Properties instead.
         */

        /**
         * Shape of a FinalizePsbtResponse.
         * @typedef {walletrpc.FinalizePsbtResponse.$Properties} walletrpc.FinalizePsbtResponse.$Shape
         */

        /**
         * Constructs a new FinalizePsbtResponse.
         * @memberof walletrpc
         * @classdesc Represents a FinalizePsbtResponse.
         * @constructor
         * @param {walletrpc.FinalizePsbtResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const FinalizePsbtResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FinalizePsbtResponse signed_psbt.
         * @member {Uint8Array} signed_psbt
         * @memberof walletrpc.FinalizePsbtResponse
         * @instance
         */
        FinalizePsbtResponse.prototype.signed_psbt = $util.newBuffer([]);

        /**
         * FinalizePsbtResponse raw_final_tx.
         * @member {Uint8Array} raw_final_tx
         * @memberof walletrpc.FinalizePsbtResponse
         * @instance
         */
        FinalizePsbtResponse.prototype.raw_final_tx = $util.newBuffer([]);

        /**
         * Creates a new FinalizePsbtResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.FinalizePsbtResponse
         * @static
         * @param {walletrpc.FinalizePsbtResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.FinalizePsbtResponse} FinalizePsbtResponse instance
         * @type {{
         *   (properties: walletrpc.FinalizePsbtResponse.$Shape): walletrpc.FinalizePsbtResponse & walletrpc.FinalizePsbtResponse.$Shape;
         *   (properties?: walletrpc.FinalizePsbtResponse.$Properties): walletrpc.FinalizePsbtResponse;
         * }}
         */
        FinalizePsbtResponse.create = function(properties) {
            return new FinalizePsbtResponse(properties);
        };

        /**
         * Encodes the specified FinalizePsbtResponse message. Does not implicitly {@link walletrpc.FinalizePsbtResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.FinalizePsbtResponse
         * @static
         * @param {walletrpc.FinalizePsbtResponse.$Properties} message FinalizePsbtResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FinalizePsbtResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.signed_psbt != null && $Object.hasOwnProperty.call(message, "signed_psbt") && message.signed_psbt.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.signed_psbt);
            if (message.raw_final_tx != null && $Object.hasOwnProperty.call(message, "raw_final_tx") && message.raw_final_tx.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.raw_final_tx);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FinalizePsbtResponse message, length delimited. Does not implicitly {@link walletrpc.FinalizePsbtResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.FinalizePsbtResponse
         * @static
         * @param {walletrpc.FinalizePsbtResponse.$Properties} message FinalizePsbtResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FinalizePsbtResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FinalizePsbtResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.FinalizePsbtResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.FinalizePsbtResponse & walletrpc.FinalizePsbtResponse.$Shape} FinalizePsbtResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FinalizePsbtResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.FinalizePsbtResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.signed_psbt = value;
                        else
                            delete message.signed_psbt;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.raw_final_tx = value;
                        else
                            delete message.raw_final_tx;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FinalizePsbtResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.FinalizePsbtResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.FinalizePsbtResponse & walletrpc.FinalizePsbtResponse.$Shape} FinalizePsbtResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FinalizePsbtResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FinalizePsbtResponse message.
         * @function verify
         * @memberof walletrpc.FinalizePsbtResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FinalizePsbtResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.signed_psbt != null && $Object.hasOwnProperty.call(message, "signed_psbt"))
                if (!(message.signed_psbt && typeof message.signed_psbt.length === "number" || $util.isString(message.signed_psbt)))
                    return "signed_psbt: buffer expected";
            if (message.raw_final_tx != null && $Object.hasOwnProperty.call(message, "raw_final_tx"))
                if (!(message.raw_final_tx && typeof message.raw_final_tx.length === "number" || $util.isString(message.raw_final_tx)))
                    return "raw_final_tx: buffer expected";
            return null;
        };

        /**
         * Creates a FinalizePsbtResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.FinalizePsbtResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.FinalizePsbtResponse} FinalizePsbtResponse
         */
        FinalizePsbtResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.FinalizePsbtResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.FinalizePsbtResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.FinalizePsbtResponse();
            if (object.signed_psbt != null)
                if (object.signed_psbt.length)
                    if (typeof object.signed_psbt === "string")
                        $util.base64.decode(object.signed_psbt, message.signed_psbt = $util.newBuffer($util.base64.length(object.signed_psbt)), 0);
                    else if (object.signed_psbt.length >= 0)
                        message.signed_psbt = object.signed_psbt;
            if (object.raw_final_tx != null)
                if (object.raw_final_tx.length)
                    if (typeof object.raw_final_tx === "string")
                        $util.base64.decode(object.raw_final_tx, message.raw_final_tx = $util.newBuffer($util.base64.length(object.raw_final_tx)), 0);
                    else if (object.raw_final_tx.length >= 0)
                        message.raw_final_tx = object.raw_final_tx;
            return message;
        };

        /**
         * Creates a plain object from a FinalizePsbtResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.FinalizePsbtResponse
         * @static
         * @param {walletrpc.FinalizePsbtResponse} message FinalizePsbtResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FinalizePsbtResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.signed_psbt = "";
                else {
                    object.signed_psbt = [];
                    if (options.bytes !== $Array)
                        object.signed_psbt = $util.newBuffer(object.signed_psbt);
                }
                if (options.bytes === $String)
                    object.raw_final_tx = "";
                else {
                    object.raw_final_tx = [];
                    if (options.bytes !== $Array)
                        object.raw_final_tx = $util.newBuffer(object.raw_final_tx);
                }
            }
            if (message.signed_psbt != null && $Object.hasOwnProperty.call(message, "signed_psbt"))
                object.signed_psbt = options.bytes === $String ? $util.base64.encode(message.signed_psbt, 0, message.signed_psbt.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signed_psbt) : message.signed_psbt;
            if (message.raw_final_tx != null && $Object.hasOwnProperty.call(message, "raw_final_tx"))
                object.raw_final_tx = options.bytes === $String ? $util.base64.encode(message.raw_final_tx, 0, message.raw_final_tx.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.raw_final_tx) : message.raw_final_tx;
            return object;
        };

        /**
         * Converts this FinalizePsbtResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.FinalizePsbtResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FinalizePsbtResponse.prototype.toJSON = function() {
            return FinalizePsbtResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FinalizePsbtResponse
         * @function getTypeUrl
         * @memberof walletrpc.FinalizePsbtResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FinalizePsbtResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.FinalizePsbtResponse";
        };

        return FinalizePsbtResponse;
    })();

    walletrpc.ListLeasesRequest = (function() {

        /**
         * Properties of a ListLeasesRequest.
         * @typedef {Object} walletrpc.ListLeasesRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListLeasesRequest.
         * @memberof walletrpc
         * @interface IListLeasesRequest
         * @augments walletrpc.ListLeasesRequest.$Properties
         * @deprecated Use walletrpc.ListLeasesRequest.$Properties instead.
         */

        /**
         * Shape of a ListLeasesRequest.
         * @typedef {walletrpc.ListLeasesRequest.$Properties} walletrpc.ListLeasesRequest.$Shape
         */

        /**
         * Constructs a new ListLeasesRequest.
         * @memberof walletrpc
         * @classdesc Represents a ListLeasesRequest.
         * @constructor
         * @param {walletrpc.ListLeasesRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListLeasesRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ListLeasesRequest instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListLeasesRequest
         * @static
         * @param {walletrpc.ListLeasesRequest.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListLeasesRequest} ListLeasesRequest instance
         * @type {{
         *   (properties: walletrpc.ListLeasesRequest.$Shape): walletrpc.ListLeasesRequest & walletrpc.ListLeasesRequest.$Shape;
         *   (properties?: walletrpc.ListLeasesRequest.$Properties): walletrpc.ListLeasesRequest;
         * }}
         */
        ListLeasesRequest.create = function(properties) {
            return new ListLeasesRequest(properties);
        };

        /**
         * Encodes the specified ListLeasesRequest message. Does not implicitly {@link walletrpc.ListLeasesRequest.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListLeasesRequest
         * @static
         * @param {walletrpc.ListLeasesRequest.$Properties} message ListLeasesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListLeasesRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListLeasesRequest message, length delimited. Does not implicitly {@link walletrpc.ListLeasesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListLeasesRequest
         * @static
         * @param {walletrpc.ListLeasesRequest.$Properties} message ListLeasesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListLeasesRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListLeasesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListLeasesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListLeasesRequest & walletrpc.ListLeasesRequest.$Shape} ListLeasesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListLeasesRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListLeasesRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListLeasesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListLeasesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListLeasesRequest & walletrpc.ListLeasesRequest.$Shape} ListLeasesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListLeasesRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListLeasesRequest message.
         * @function verify
         * @memberof walletrpc.ListLeasesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListLeasesRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ListLeasesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListLeasesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListLeasesRequest} ListLeasesRequest
         */
        ListLeasesRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListLeasesRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListLeasesRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.walletrpc.ListLeasesRequest();
        };

        /**
         * Creates a plain object from a ListLeasesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListLeasesRequest
         * @static
         * @param {walletrpc.ListLeasesRequest} message ListLeasesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListLeasesRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this ListLeasesRequest to JSON.
         * @function toJSON
         * @memberof walletrpc.ListLeasesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListLeasesRequest.prototype.toJSON = function() {
            return ListLeasesRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListLeasesRequest
         * @function getTypeUrl
         * @memberof walletrpc.ListLeasesRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListLeasesRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListLeasesRequest";
        };

        return ListLeasesRequest;
    })();

    walletrpc.ListLeasesResponse = (function() {

        /**
         * Properties of a ListLeasesResponse.
         * @typedef {Object} walletrpc.ListLeasesResponse.$Properties
         * @property {Array.<walletrpc.UtxoLease.$Properties>|null} [locked_utxos] ListLeasesResponse locked_utxos
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListLeasesResponse.
         * @memberof walletrpc
         * @interface IListLeasesResponse
         * @augments walletrpc.ListLeasesResponse.$Properties
         * @deprecated Use walletrpc.ListLeasesResponse.$Properties instead.
         */

        /**
         * Shape of a ListLeasesResponse.
         * @typedef {walletrpc.ListLeasesResponse.$Properties} walletrpc.ListLeasesResponse.$Shape
         */

        /**
         * Constructs a new ListLeasesResponse.
         * @memberof walletrpc
         * @classdesc Represents a ListLeasesResponse.
         * @constructor
         * @param {walletrpc.ListLeasesResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListLeasesResponse = function (properties) {
            this.locked_utxos = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListLeasesResponse locked_utxos.
         * @member {Array.<walletrpc.UtxoLease.$Properties>} locked_utxos
         * @memberof walletrpc.ListLeasesResponse
         * @instance
         */
        ListLeasesResponse.prototype.locked_utxos = $util.emptyArray;

        /**
         * Creates a new ListLeasesResponse instance using the specified properties.
         * @function create
         * @memberof walletrpc.ListLeasesResponse
         * @static
         * @param {walletrpc.ListLeasesResponse.$Properties=} [properties] Properties to set
         * @returns {walletrpc.ListLeasesResponse} ListLeasesResponse instance
         * @type {{
         *   (properties: walletrpc.ListLeasesResponse.$Shape): walletrpc.ListLeasesResponse & walletrpc.ListLeasesResponse.$Shape;
         *   (properties?: walletrpc.ListLeasesResponse.$Properties): walletrpc.ListLeasesResponse;
         * }}
         */
        ListLeasesResponse.create = function(properties) {
            return new ListLeasesResponse(properties);
        };

        /**
         * Encodes the specified ListLeasesResponse message. Does not implicitly {@link walletrpc.ListLeasesResponse.verify|verify} messages.
         * @function encode
         * @memberof walletrpc.ListLeasesResponse
         * @static
         * @param {walletrpc.ListLeasesResponse.$Properties} message ListLeasesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListLeasesResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.locked_utxos != null && message.locked_utxos.length)
                for (let i = 0; i < message.locked_utxos.length; ++i)
                    $root.walletrpc.UtxoLease.encode(message.locked_utxos[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListLeasesResponse message, length delimited. Does not implicitly {@link walletrpc.ListLeasesResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof walletrpc.ListLeasesResponse
         * @static
         * @param {walletrpc.ListLeasesResponse.$Properties} message ListLeasesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListLeasesResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListLeasesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof walletrpc.ListLeasesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {walletrpc.ListLeasesResponse & walletrpc.ListLeasesResponse.$Shape} ListLeasesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListLeasesResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.walletrpc.ListLeasesResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.locked_utxos && message.locked_utxos.length))
                            message.locked_utxos = [];
                        message.locked_utxos.push($root.walletrpc.UtxoLease.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListLeasesResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof walletrpc.ListLeasesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {walletrpc.ListLeasesResponse & walletrpc.ListLeasesResponse.$Shape} ListLeasesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListLeasesResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListLeasesResponse message.
         * @function verify
         * @memberof walletrpc.ListLeasesResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListLeasesResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.locked_utxos != null && $Object.hasOwnProperty.call(message, "locked_utxos")) {
                if (!$Array.isArray(message.locked_utxos))
                    return "locked_utxos: array expected";
                for (let i = 0; i < message.locked_utxos.length; ++i) {
                    let error = $root.walletrpc.UtxoLease.verify(message.locked_utxos[i], _depth + 1);
                    if (error)
                        return "locked_utxos." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListLeasesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof walletrpc.ListLeasesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {walletrpc.ListLeasesResponse} ListLeasesResponse
         */
        ListLeasesResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.walletrpc.ListLeasesResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".walletrpc.ListLeasesResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.walletrpc.ListLeasesResponse();
            if (object.locked_utxos) {
                if (!$Array.isArray(object.locked_utxos))
                    throw $TypeError(".walletrpc.ListLeasesResponse.locked_utxos: array expected");
                message.locked_utxos = $Array(object.locked_utxos.length);
                for (let i = 0; i < object.locked_utxos.length; ++i) {
                    if (!$util.isObject(object.locked_utxos[i]))
                        throw $TypeError(".walletrpc.ListLeasesResponse.locked_utxos: object expected");
                    message.locked_utxos[i] = $root.walletrpc.UtxoLease.fromObject(object.locked_utxos[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListLeasesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof walletrpc.ListLeasesResponse
         * @static
         * @param {walletrpc.ListLeasesResponse} message ListLeasesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListLeasesResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.locked_utxos = [];
            if (message.locked_utxos && message.locked_utxos.length) {
                object.locked_utxos = $Array(message.locked_utxos.length);
                for (let j = 0; j < message.locked_utxos.length; ++j)
                    object.locked_utxos[j] = $root.walletrpc.UtxoLease.toObject(message.locked_utxos[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ListLeasesResponse to JSON.
         * @function toJSON
         * @memberof walletrpc.ListLeasesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListLeasesResponse.prototype.toJSON = function() {
            return ListLeasesResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListLeasesResponse
         * @function getTypeUrl
         * @memberof walletrpc.ListLeasesResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListLeasesResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/walletrpc.ListLeasesResponse";
        };

        return ListLeasesResponse;
    })();

    return walletrpc;
})();

export const wtclientrpc = $root.wtclientrpc = (() => {

    /**
     * Namespace wtclientrpc.
     * @exports wtclientrpc
     * @namespace
     */
    const wtclientrpc = {};

    wtclientrpc.WatchtowerClient = (function() {

        /**
         * Constructs a new WatchtowerClient service.
         * @memberof wtclientrpc
         * @classdesc Represents a WatchtowerClient
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const WatchtowerClient = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        (WatchtowerClient.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = WatchtowerClient;

        /**
         * Creates new WatchtowerClient service using the specified rpc implementation.
         * @function create
         * @memberof wtclientrpc.WatchtowerClient
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {WatchtowerClient} RPC service. Useful where requests and/or responses are streamed.
         */
        WatchtowerClient.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link wtclientrpc.WatchtowerClient#addTower}.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef AddTowerCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {wtclientrpc.AddTowerResponse} [response] AddTowerResponse
         */

        /**
         * Calls AddTower.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef AddTower
         * @type {{
         *   (request: wtclientrpc.IAddTowerRequest, callback: wtclientrpc.WatchtowerClient.AddTowerCallback): void;
         *   (request: wtclientrpc.IAddTowerRequest): Promise<wtclientrpc.AddTowerResponse>;
         *   readonly name: "AddTower";
         *   readonly path: "/wtclientrpc.WatchtowerClient/AddTower";
         *   readonly requestType: "AddTowerRequest";
         *   readonly responseType: "AddTowerResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls AddTower.
         * @name wtclientrpc.WatchtowerClient#addTower
         * @type {wtclientrpc.WatchtowerClient.AddTower}
         */
        $Object.defineProperties(WatchtowerClient.prototype.addTower = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WatchtowerClient.prototype.addTower, $root.wtclientrpc.AddTowerRequest, $root.wtclientrpc.AddTowerResponse, request, callback);
        }, {
            name: { value: "AddTower" },
            path: { value: "/wtclientrpc.WatchtowerClient/AddTower" },
            requestType: { value: "AddTowerRequest" },
            responseType: { value: "AddTowerResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link wtclientrpc.WatchtowerClient#removeTower}.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef RemoveTowerCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {wtclientrpc.RemoveTowerResponse} [response] RemoveTowerResponse
         */

        /**
         * Calls RemoveTower.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef RemoveTower
         * @type {{
         *   (request: wtclientrpc.IRemoveTowerRequest, callback: wtclientrpc.WatchtowerClient.RemoveTowerCallback): void;
         *   (request: wtclientrpc.IRemoveTowerRequest): Promise<wtclientrpc.RemoveTowerResponse>;
         *   readonly name: "RemoveTower";
         *   readonly path: "/wtclientrpc.WatchtowerClient/RemoveTower";
         *   readonly requestType: "RemoveTowerRequest";
         *   readonly responseType: "RemoveTowerResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls RemoveTower.
         * @name wtclientrpc.WatchtowerClient#removeTower
         * @type {wtclientrpc.WatchtowerClient.RemoveTower}
         */
        $Object.defineProperties(WatchtowerClient.prototype.removeTower = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WatchtowerClient.prototype.removeTower, $root.wtclientrpc.RemoveTowerRequest, $root.wtclientrpc.RemoveTowerResponse, request, callback);
        }, {
            name: { value: "RemoveTower" },
            path: { value: "/wtclientrpc.WatchtowerClient/RemoveTower" },
            requestType: { value: "RemoveTowerRequest" },
            responseType: { value: "RemoveTowerResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link wtclientrpc.WatchtowerClient#deactivateTower}.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef DeactivateTowerCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {wtclientrpc.DeactivateTowerResponse} [response] DeactivateTowerResponse
         */

        /**
         * Calls DeactivateTower.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef DeactivateTower
         * @type {{
         *   (request: wtclientrpc.IDeactivateTowerRequest, callback: wtclientrpc.WatchtowerClient.DeactivateTowerCallback): void;
         *   (request: wtclientrpc.IDeactivateTowerRequest): Promise<wtclientrpc.DeactivateTowerResponse>;
         *   readonly name: "DeactivateTower";
         *   readonly path: "/wtclientrpc.WatchtowerClient/DeactivateTower";
         *   readonly requestType: "DeactivateTowerRequest";
         *   readonly responseType: "DeactivateTowerResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls DeactivateTower.
         * @name wtclientrpc.WatchtowerClient#deactivateTower
         * @type {wtclientrpc.WatchtowerClient.DeactivateTower}
         */
        $Object.defineProperties(WatchtowerClient.prototype.deactivateTower = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WatchtowerClient.prototype.deactivateTower, $root.wtclientrpc.DeactivateTowerRequest, $root.wtclientrpc.DeactivateTowerResponse, request, callback);
        }, {
            name: { value: "DeactivateTower" },
            path: { value: "/wtclientrpc.WatchtowerClient/DeactivateTower" },
            requestType: { value: "DeactivateTowerRequest" },
            responseType: { value: "DeactivateTowerResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link wtclientrpc.WatchtowerClient#terminateSession}.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef TerminateSessionCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {wtclientrpc.TerminateSessionResponse} [response] TerminateSessionResponse
         */

        /**
         * Calls TerminateSession.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef TerminateSession
         * @type {{
         *   (request: wtclientrpc.ITerminateSessionRequest, callback: wtclientrpc.WatchtowerClient.TerminateSessionCallback): void;
         *   (request: wtclientrpc.ITerminateSessionRequest): Promise<wtclientrpc.TerminateSessionResponse>;
         *   readonly name: "TerminateSession";
         *   readonly path: "/wtclientrpc.WatchtowerClient/TerminateSession";
         *   readonly requestType: "TerminateSessionRequest";
         *   readonly responseType: "TerminateSessionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls TerminateSession.
         * @name wtclientrpc.WatchtowerClient#terminateSession
         * @type {wtclientrpc.WatchtowerClient.TerminateSession}
         */
        $Object.defineProperties(WatchtowerClient.prototype.terminateSession = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WatchtowerClient.prototype.terminateSession, $root.wtclientrpc.TerminateSessionRequest, $root.wtclientrpc.TerminateSessionResponse, request, callback);
        }, {
            name: { value: "TerminateSession" },
            path: { value: "/wtclientrpc.WatchtowerClient/TerminateSession" },
            requestType: { value: "TerminateSessionRequest" },
            responseType: { value: "TerminateSessionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link wtclientrpc.WatchtowerClient#listTowers}.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef ListTowersCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {wtclientrpc.ListTowersResponse} [response] ListTowersResponse
         */

        /**
         * Calls ListTowers.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef ListTowers
         * @type {{
         *   (request: wtclientrpc.IListTowersRequest, callback: wtclientrpc.WatchtowerClient.ListTowersCallback): void;
         *   (request: wtclientrpc.IListTowersRequest): Promise<wtclientrpc.ListTowersResponse>;
         *   readonly name: "ListTowers";
         *   readonly path: "/wtclientrpc.WatchtowerClient/ListTowers";
         *   readonly requestType: "ListTowersRequest";
         *   readonly responseType: "ListTowersResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls ListTowers.
         * @name wtclientrpc.WatchtowerClient#listTowers
         * @type {wtclientrpc.WatchtowerClient.ListTowers}
         */
        $Object.defineProperties(WatchtowerClient.prototype.listTowers = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WatchtowerClient.prototype.listTowers, $root.wtclientrpc.ListTowersRequest, $root.wtclientrpc.ListTowersResponse, request, callback);
        }, {
            name: { value: "ListTowers" },
            path: { value: "/wtclientrpc.WatchtowerClient/ListTowers" },
            requestType: { value: "ListTowersRequest" },
            responseType: { value: "ListTowersResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link wtclientrpc.WatchtowerClient#getTowerInfo}.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef GetTowerInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {wtclientrpc.Tower} [response] Tower
         */

        /**
         * Calls GetTowerInfo.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef GetTowerInfo
         * @type {{
         *   (request: wtclientrpc.IGetTowerInfoRequest, callback: wtclientrpc.WatchtowerClient.GetTowerInfoCallback): void;
         *   (request: wtclientrpc.IGetTowerInfoRequest): Promise<wtclientrpc.Tower>;
         *   readonly name: "GetTowerInfo";
         *   readonly path: "/wtclientrpc.WatchtowerClient/GetTowerInfo";
         *   readonly requestType: "GetTowerInfoRequest";
         *   readonly responseType: "Tower";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls GetTowerInfo.
         * @name wtclientrpc.WatchtowerClient#getTowerInfo
         * @type {wtclientrpc.WatchtowerClient.GetTowerInfo}
         */
        $Object.defineProperties(WatchtowerClient.prototype.getTowerInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WatchtowerClient.prototype.getTowerInfo, $root.wtclientrpc.GetTowerInfoRequest, $root.wtclientrpc.Tower, request, callback);
        }, {
            name: { value: "GetTowerInfo" },
            path: { value: "/wtclientrpc.WatchtowerClient/GetTowerInfo" },
            requestType: { value: "GetTowerInfoRequest" },
            responseType: { value: "Tower" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link wtclientrpc.WatchtowerClient#stats}.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef StatsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {wtclientrpc.StatsResponse} [response] StatsResponse
         */

        /**
         * Calls Stats.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef Stats
         * @type {{
         *   (request: wtclientrpc.IStatsRequest, callback: wtclientrpc.WatchtowerClient.StatsCallback): void;
         *   (request: wtclientrpc.IStatsRequest): Promise<wtclientrpc.StatsResponse>;
         *   readonly name: "Stats";
         *   readonly path: "/wtclientrpc.WatchtowerClient/Stats";
         *   readonly requestType: "StatsRequest";
         *   readonly responseType: "StatsResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls Stats.
         * @name wtclientrpc.WatchtowerClient#stats
         * @type {wtclientrpc.WatchtowerClient.Stats}
         */
        $Object.defineProperties(WatchtowerClient.prototype.stats = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WatchtowerClient.prototype.stats, $root.wtclientrpc.StatsRequest, $root.wtclientrpc.StatsResponse, request, callback);
        }, {
            name: { value: "Stats" },
            path: { value: "/wtclientrpc.WatchtowerClient/Stats" },
            requestType: { value: "StatsRequest" },
            responseType: { value: "StatsResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link wtclientrpc.WatchtowerClient#policy}.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef PolicyCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {wtclientrpc.PolicyResponse} [response] PolicyResponse
         */

        /**
         * Calls Policy.
         * @memberof wtclientrpc.WatchtowerClient
         * @typedef Policy
         * @type {{
         *   (request: wtclientrpc.IPolicyRequest, callback: wtclientrpc.WatchtowerClient.PolicyCallback): void;
         *   (request: wtclientrpc.IPolicyRequest): Promise<wtclientrpc.PolicyResponse>;
         *   readonly name: "Policy";
         *   readonly path: "/wtclientrpc.WatchtowerClient/Policy";
         *   readonly requestType: "PolicyRequest";
         *   readonly responseType: "PolicyResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Calls Policy.
         * @name wtclientrpc.WatchtowerClient#policy
         * @type {wtclientrpc.WatchtowerClient.Policy}
         */
        $Object.defineProperties(WatchtowerClient.prototype.policy = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, WatchtowerClient.prototype.policy, $root.wtclientrpc.PolicyRequest, $root.wtclientrpc.PolicyResponse, request, callback);
        }, {
            name: { value: "Policy" },
            path: { value: "/wtclientrpc.WatchtowerClient/Policy" },
            requestType: { value: "PolicyRequest" },
            responseType: { value: "PolicyResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return WatchtowerClient;
    })();

    wtclientrpc.AddTowerRequest = (function() {

        /**
         * Properties of an AddTowerRequest.
         * @typedef {Object} wtclientrpc.AddTowerRequest.$Properties
         * @property {Uint8Array|null} [pubkey] AddTowerRequest pubkey
         * @property {string|null} [address] AddTowerRequest address
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddTowerRequest.
         * @memberof wtclientrpc
         * @interface IAddTowerRequest
         * @augments wtclientrpc.AddTowerRequest.$Properties
         * @deprecated Use wtclientrpc.AddTowerRequest.$Properties instead.
         */

        /**
         * Shape of an AddTowerRequest.
         * @typedef {wtclientrpc.AddTowerRequest.$Properties} wtclientrpc.AddTowerRequest.$Shape
         */

        /**
         * Constructs a new AddTowerRequest.
         * @memberof wtclientrpc
         * @classdesc Represents an AddTowerRequest.
         * @constructor
         * @param {wtclientrpc.AddTowerRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddTowerRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AddTowerRequest pubkey.
         * @member {Uint8Array} pubkey
         * @memberof wtclientrpc.AddTowerRequest
         * @instance
         */
        AddTowerRequest.prototype.pubkey = $util.newBuffer([]);

        /**
         * AddTowerRequest address.
         * @member {string} address
         * @memberof wtclientrpc.AddTowerRequest
         * @instance
         */
        AddTowerRequest.prototype.address = "";

        /**
         * Creates a new AddTowerRequest instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.AddTowerRequest
         * @static
         * @param {wtclientrpc.AddTowerRequest.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.AddTowerRequest} AddTowerRequest instance
         * @type {{
         *   (properties: wtclientrpc.AddTowerRequest.$Shape): wtclientrpc.AddTowerRequest & wtclientrpc.AddTowerRequest.$Shape;
         *   (properties?: wtclientrpc.AddTowerRequest.$Properties): wtclientrpc.AddTowerRequest;
         * }}
         */
        AddTowerRequest.create = function(properties) {
            return new AddTowerRequest(properties);
        };

        /**
         * Encodes the specified AddTowerRequest message. Does not implicitly {@link wtclientrpc.AddTowerRequest.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.AddTowerRequest
         * @static
         * @param {wtclientrpc.AddTowerRequest.$Properties} message AddTowerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddTowerRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey") && message.pubkey.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pubkey);
            if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddTowerRequest message, length delimited. Does not implicitly {@link wtclientrpc.AddTowerRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.AddTowerRequest
         * @static
         * @param {wtclientrpc.AddTowerRequest.$Properties} message AddTowerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddTowerRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddTowerRequest message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.AddTowerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.AddTowerRequest & wtclientrpc.AddTowerRequest.$Shape} AddTowerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddTowerRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.AddTowerRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pubkey = value;
                        else
                            delete message.pubkey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.address = value;
                        else
                            delete message.address;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddTowerRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.AddTowerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.AddTowerRequest & wtclientrpc.AddTowerRequest.$Shape} AddTowerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddTowerRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddTowerRequest message.
         * @function verify
         * @memberof wtclientrpc.AddTowerRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddTowerRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                if (!(message.pubkey && typeof message.pubkey.length === "number" || $util.isString(message.pubkey)))
                    return "pubkey: buffer expected";
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            return null;
        };

        /**
         * Creates an AddTowerRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.AddTowerRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.AddTowerRequest} AddTowerRequest
         */
        AddTowerRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.AddTowerRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.AddTowerRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.AddTowerRequest();
            if (object.pubkey != null)
                if (object.pubkey.length)
                    if (typeof object.pubkey === "string")
                        $util.base64.decode(object.pubkey, message.pubkey = $util.newBuffer($util.base64.length(object.pubkey)), 0);
                    else if (object.pubkey.length >= 0)
                        message.pubkey = object.pubkey;
            if (object.address != null)
                if (typeof object.address !== "string" || object.address.length)
                    message.address = $String(object.address);
            return message;
        };

        /**
         * Creates a plain object from an AddTowerRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.AddTowerRequest
         * @static
         * @param {wtclientrpc.AddTowerRequest} message AddTowerRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddTowerRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.pubkey = "";
                else {
                    object.pubkey = [];
                    if (options.bytes !== $Array)
                        object.pubkey = $util.newBuffer(object.pubkey);
                }
                object.address = "";
            }
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                object.pubkey = options.bytes === $String ? $util.base64.encode(message.pubkey, 0, message.pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pubkey) : message.pubkey;
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                object.address = message.address;
            return object;
        };

        /**
         * Converts this AddTowerRequest to JSON.
         * @function toJSON
         * @memberof wtclientrpc.AddTowerRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddTowerRequest.prototype.toJSON = function() {
            return AddTowerRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddTowerRequest
         * @function getTypeUrl
         * @memberof wtclientrpc.AddTowerRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddTowerRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.AddTowerRequest";
        };

        return AddTowerRequest;
    })();

    wtclientrpc.AddTowerResponse = (function() {

        /**
         * Properties of an AddTowerResponse.
         * @typedef {Object} wtclientrpc.AddTowerResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of an AddTowerResponse.
         * @memberof wtclientrpc
         * @interface IAddTowerResponse
         * @augments wtclientrpc.AddTowerResponse.$Properties
         * @deprecated Use wtclientrpc.AddTowerResponse.$Properties instead.
         */

        /**
         * Shape of an AddTowerResponse.
         * @typedef {wtclientrpc.AddTowerResponse.$Properties} wtclientrpc.AddTowerResponse.$Shape
         */

        /**
         * Constructs a new AddTowerResponse.
         * @memberof wtclientrpc
         * @classdesc Represents an AddTowerResponse.
         * @constructor
         * @param {wtclientrpc.AddTowerResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const AddTowerResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new AddTowerResponse instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.AddTowerResponse
         * @static
         * @param {wtclientrpc.AddTowerResponse.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.AddTowerResponse} AddTowerResponse instance
         * @type {{
         *   (properties: wtclientrpc.AddTowerResponse.$Shape): wtclientrpc.AddTowerResponse & wtclientrpc.AddTowerResponse.$Shape;
         *   (properties?: wtclientrpc.AddTowerResponse.$Properties): wtclientrpc.AddTowerResponse;
         * }}
         */
        AddTowerResponse.create = function(properties) {
            return new AddTowerResponse(properties);
        };

        /**
         * Encodes the specified AddTowerResponse message. Does not implicitly {@link wtclientrpc.AddTowerResponse.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.AddTowerResponse
         * @static
         * @param {wtclientrpc.AddTowerResponse.$Properties} message AddTowerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddTowerResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AddTowerResponse message, length delimited. Does not implicitly {@link wtclientrpc.AddTowerResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.AddTowerResponse
         * @static
         * @param {wtclientrpc.AddTowerResponse.$Properties} message AddTowerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddTowerResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AddTowerResponse message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.AddTowerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.AddTowerResponse & wtclientrpc.AddTowerResponse.$Shape} AddTowerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddTowerResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.AddTowerResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AddTowerResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.AddTowerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.AddTowerResponse & wtclientrpc.AddTowerResponse.$Shape} AddTowerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddTowerResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AddTowerResponse message.
         * @function verify
         * @memberof wtclientrpc.AddTowerResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AddTowerResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates an AddTowerResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.AddTowerResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.AddTowerResponse} AddTowerResponse
         */
        AddTowerResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.AddTowerResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.AddTowerResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.wtclientrpc.AddTowerResponse();
        };

        /**
         * Creates a plain object from an AddTowerResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.AddTowerResponse
         * @static
         * @param {wtclientrpc.AddTowerResponse} message AddTowerResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddTowerResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this AddTowerResponse to JSON.
         * @function toJSON
         * @memberof wtclientrpc.AddTowerResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddTowerResponse.prototype.toJSON = function() {
            return AddTowerResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AddTowerResponse
         * @function getTypeUrl
         * @memberof wtclientrpc.AddTowerResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AddTowerResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.AddTowerResponse";
        };

        return AddTowerResponse;
    })();

    wtclientrpc.RemoveTowerRequest = (function() {

        /**
         * Properties of a RemoveTowerRequest.
         * @typedef {Object} wtclientrpc.RemoveTowerRequest.$Properties
         * @property {Uint8Array|null} [pubkey] RemoveTowerRequest pubkey
         * @property {string|null} [address] RemoveTowerRequest address
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RemoveTowerRequest.
         * @memberof wtclientrpc
         * @interface IRemoveTowerRequest
         * @augments wtclientrpc.RemoveTowerRequest.$Properties
         * @deprecated Use wtclientrpc.RemoveTowerRequest.$Properties instead.
         */

        /**
         * Shape of a RemoveTowerRequest.
         * @typedef {wtclientrpc.RemoveTowerRequest.$Properties} wtclientrpc.RemoveTowerRequest.$Shape
         */

        /**
         * Constructs a new RemoveTowerRequest.
         * @memberof wtclientrpc
         * @classdesc Represents a RemoveTowerRequest.
         * @constructor
         * @param {wtclientrpc.RemoveTowerRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RemoveTowerRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RemoveTowerRequest pubkey.
         * @member {Uint8Array} pubkey
         * @memberof wtclientrpc.RemoveTowerRequest
         * @instance
         */
        RemoveTowerRequest.prototype.pubkey = $util.newBuffer([]);

        /**
         * RemoveTowerRequest address.
         * @member {string} address
         * @memberof wtclientrpc.RemoveTowerRequest
         * @instance
         */
        RemoveTowerRequest.prototype.address = "";

        /**
         * Creates a new RemoveTowerRequest instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.RemoveTowerRequest
         * @static
         * @param {wtclientrpc.RemoveTowerRequest.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.RemoveTowerRequest} RemoveTowerRequest instance
         * @type {{
         *   (properties: wtclientrpc.RemoveTowerRequest.$Shape): wtclientrpc.RemoveTowerRequest & wtclientrpc.RemoveTowerRequest.$Shape;
         *   (properties?: wtclientrpc.RemoveTowerRequest.$Properties): wtclientrpc.RemoveTowerRequest;
         * }}
         */
        RemoveTowerRequest.create = function(properties) {
            return new RemoveTowerRequest(properties);
        };

        /**
         * Encodes the specified RemoveTowerRequest message. Does not implicitly {@link wtclientrpc.RemoveTowerRequest.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.RemoveTowerRequest
         * @static
         * @param {wtclientrpc.RemoveTowerRequest.$Properties} message RemoveTowerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveTowerRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey") && message.pubkey.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pubkey);
            if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RemoveTowerRequest message, length delimited. Does not implicitly {@link wtclientrpc.RemoveTowerRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.RemoveTowerRequest
         * @static
         * @param {wtclientrpc.RemoveTowerRequest.$Properties} message RemoveTowerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveTowerRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RemoveTowerRequest message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.RemoveTowerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.RemoveTowerRequest & wtclientrpc.RemoveTowerRequest.$Shape} RemoveTowerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveTowerRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.RemoveTowerRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pubkey = value;
                        else
                            delete message.pubkey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.address = value;
                        else
                            delete message.address;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RemoveTowerRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.RemoveTowerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.RemoveTowerRequest & wtclientrpc.RemoveTowerRequest.$Shape} RemoveTowerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveTowerRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RemoveTowerRequest message.
         * @function verify
         * @memberof wtclientrpc.RemoveTowerRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RemoveTowerRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                if (!(message.pubkey && typeof message.pubkey.length === "number" || $util.isString(message.pubkey)))
                    return "pubkey: buffer expected";
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            return null;
        };

        /**
         * Creates a RemoveTowerRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.RemoveTowerRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.RemoveTowerRequest} RemoveTowerRequest
         */
        RemoveTowerRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.RemoveTowerRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.RemoveTowerRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.RemoveTowerRequest();
            if (object.pubkey != null)
                if (object.pubkey.length)
                    if (typeof object.pubkey === "string")
                        $util.base64.decode(object.pubkey, message.pubkey = $util.newBuffer($util.base64.length(object.pubkey)), 0);
                    else if (object.pubkey.length >= 0)
                        message.pubkey = object.pubkey;
            if (object.address != null)
                if (typeof object.address !== "string" || object.address.length)
                    message.address = $String(object.address);
            return message;
        };

        /**
         * Creates a plain object from a RemoveTowerRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.RemoveTowerRequest
         * @static
         * @param {wtclientrpc.RemoveTowerRequest} message RemoveTowerRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RemoveTowerRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.pubkey = "";
                else {
                    object.pubkey = [];
                    if (options.bytes !== $Array)
                        object.pubkey = $util.newBuffer(object.pubkey);
                }
                object.address = "";
            }
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                object.pubkey = options.bytes === $String ? $util.base64.encode(message.pubkey, 0, message.pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pubkey) : message.pubkey;
            if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                object.address = message.address;
            return object;
        };

        /**
         * Converts this RemoveTowerRequest to JSON.
         * @function toJSON
         * @memberof wtclientrpc.RemoveTowerRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RemoveTowerRequest.prototype.toJSON = function() {
            return RemoveTowerRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RemoveTowerRequest
         * @function getTypeUrl
         * @memberof wtclientrpc.RemoveTowerRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RemoveTowerRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.RemoveTowerRequest";
        };

        return RemoveTowerRequest;
    })();

    wtclientrpc.RemoveTowerResponse = (function() {

        /**
         * Properties of a RemoveTowerResponse.
         * @typedef {Object} wtclientrpc.RemoveTowerResponse.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a RemoveTowerResponse.
         * @memberof wtclientrpc
         * @interface IRemoveTowerResponse
         * @augments wtclientrpc.RemoveTowerResponse.$Properties
         * @deprecated Use wtclientrpc.RemoveTowerResponse.$Properties instead.
         */

        /**
         * Shape of a RemoveTowerResponse.
         * @typedef {wtclientrpc.RemoveTowerResponse.$Properties} wtclientrpc.RemoveTowerResponse.$Shape
         */

        /**
         * Constructs a new RemoveTowerResponse.
         * @memberof wtclientrpc
         * @classdesc Represents a RemoveTowerResponse.
         * @constructor
         * @param {wtclientrpc.RemoveTowerResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const RemoveTowerResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new RemoveTowerResponse instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.RemoveTowerResponse
         * @static
         * @param {wtclientrpc.RemoveTowerResponse.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.RemoveTowerResponse} RemoveTowerResponse instance
         * @type {{
         *   (properties: wtclientrpc.RemoveTowerResponse.$Shape): wtclientrpc.RemoveTowerResponse & wtclientrpc.RemoveTowerResponse.$Shape;
         *   (properties?: wtclientrpc.RemoveTowerResponse.$Properties): wtclientrpc.RemoveTowerResponse;
         * }}
         */
        RemoveTowerResponse.create = function(properties) {
            return new RemoveTowerResponse(properties);
        };

        /**
         * Encodes the specified RemoveTowerResponse message. Does not implicitly {@link wtclientrpc.RemoveTowerResponse.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.RemoveTowerResponse
         * @static
         * @param {wtclientrpc.RemoveTowerResponse.$Properties} message RemoveTowerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveTowerResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RemoveTowerResponse message, length delimited. Does not implicitly {@link wtclientrpc.RemoveTowerResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.RemoveTowerResponse
         * @static
         * @param {wtclientrpc.RemoveTowerResponse.$Properties} message RemoveTowerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveTowerResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RemoveTowerResponse message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.RemoveTowerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.RemoveTowerResponse & wtclientrpc.RemoveTowerResponse.$Shape} RemoveTowerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveTowerResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.RemoveTowerResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RemoveTowerResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.RemoveTowerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.RemoveTowerResponse & wtclientrpc.RemoveTowerResponse.$Shape} RemoveTowerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveTowerResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RemoveTowerResponse message.
         * @function verify
         * @memberof wtclientrpc.RemoveTowerResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RemoveTowerResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a RemoveTowerResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.RemoveTowerResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.RemoveTowerResponse} RemoveTowerResponse
         */
        RemoveTowerResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.RemoveTowerResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.RemoveTowerResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.wtclientrpc.RemoveTowerResponse();
        };

        /**
         * Creates a plain object from a RemoveTowerResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.RemoveTowerResponse
         * @static
         * @param {wtclientrpc.RemoveTowerResponse} message RemoveTowerResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RemoveTowerResponse.toObject = function () {
            return {};
        };

        /**
         * Converts this RemoveTowerResponse to JSON.
         * @function toJSON
         * @memberof wtclientrpc.RemoveTowerResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RemoveTowerResponse.prototype.toJSON = function() {
            return RemoveTowerResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RemoveTowerResponse
         * @function getTypeUrl
         * @memberof wtclientrpc.RemoveTowerResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RemoveTowerResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.RemoveTowerResponse";
        };

        return RemoveTowerResponse;
    })();

    wtclientrpc.DeactivateTowerRequest = (function() {

        /**
         * Properties of a DeactivateTowerRequest.
         * @typedef {Object} wtclientrpc.DeactivateTowerRequest.$Properties
         * @property {Uint8Array|null} [pubkey] DeactivateTowerRequest pubkey
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeactivateTowerRequest.
         * @memberof wtclientrpc
         * @interface IDeactivateTowerRequest
         * @augments wtclientrpc.DeactivateTowerRequest.$Properties
         * @deprecated Use wtclientrpc.DeactivateTowerRequest.$Properties instead.
         */

        /**
         * Shape of a DeactivateTowerRequest.
         * @typedef {wtclientrpc.DeactivateTowerRequest.$Properties} wtclientrpc.DeactivateTowerRequest.$Shape
         */

        /**
         * Constructs a new DeactivateTowerRequest.
         * @memberof wtclientrpc
         * @classdesc Represents a DeactivateTowerRequest.
         * @constructor
         * @param {wtclientrpc.DeactivateTowerRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeactivateTowerRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeactivateTowerRequest pubkey.
         * @member {Uint8Array} pubkey
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @instance
         */
        DeactivateTowerRequest.prototype.pubkey = $util.newBuffer([]);

        /**
         * Creates a new DeactivateTowerRequest instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @static
         * @param {wtclientrpc.DeactivateTowerRequest.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.DeactivateTowerRequest} DeactivateTowerRequest instance
         * @type {{
         *   (properties: wtclientrpc.DeactivateTowerRequest.$Shape): wtclientrpc.DeactivateTowerRequest & wtclientrpc.DeactivateTowerRequest.$Shape;
         *   (properties?: wtclientrpc.DeactivateTowerRequest.$Properties): wtclientrpc.DeactivateTowerRequest;
         * }}
         */
        DeactivateTowerRequest.create = function(properties) {
            return new DeactivateTowerRequest(properties);
        };

        /**
         * Encodes the specified DeactivateTowerRequest message. Does not implicitly {@link wtclientrpc.DeactivateTowerRequest.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @static
         * @param {wtclientrpc.DeactivateTowerRequest.$Properties} message DeactivateTowerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeactivateTowerRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey") && message.pubkey.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pubkey);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeactivateTowerRequest message, length delimited. Does not implicitly {@link wtclientrpc.DeactivateTowerRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @static
         * @param {wtclientrpc.DeactivateTowerRequest.$Properties} message DeactivateTowerRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeactivateTowerRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeactivateTowerRequest message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.DeactivateTowerRequest & wtclientrpc.DeactivateTowerRequest.$Shape} DeactivateTowerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeactivateTowerRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.DeactivateTowerRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pubkey = value;
                        else
                            delete message.pubkey;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeactivateTowerRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.DeactivateTowerRequest & wtclientrpc.DeactivateTowerRequest.$Shape} DeactivateTowerRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeactivateTowerRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeactivateTowerRequest message.
         * @function verify
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeactivateTowerRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                if (!(message.pubkey && typeof message.pubkey.length === "number" || $util.isString(message.pubkey)))
                    return "pubkey: buffer expected";
            return null;
        };

        /**
         * Creates a DeactivateTowerRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.DeactivateTowerRequest} DeactivateTowerRequest
         */
        DeactivateTowerRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.DeactivateTowerRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.DeactivateTowerRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.DeactivateTowerRequest();
            if (object.pubkey != null)
                if (object.pubkey.length)
                    if (typeof object.pubkey === "string")
                        $util.base64.decode(object.pubkey, message.pubkey = $util.newBuffer($util.base64.length(object.pubkey)), 0);
                    else if (object.pubkey.length >= 0)
                        message.pubkey = object.pubkey;
            return message;
        };

        /**
         * Creates a plain object from a DeactivateTowerRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @static
         * @param {wtclientrpc.DeactivateTowerRequest} message DeactivateTowerRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeactivateTowerRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.pubkey = "";
                else {
                    object.pubkey = [];
                    if (options.bytes !== $Array)
                        object.pubkey = $util.newBuffer(object.pubkey);
                }
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                object.pubkey = options.bytes === $String ? $util.base64.encode(message.pubkey, 0, message.pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pubkey) : message.pubkey;
            return object;
        };

        /**
         * Converts this DeactivateTowerRequest to JSON.
         * @function toJSON
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeactivateTowerRequest.prototype.toJSON = function() {
            return DeactivateTowerRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeactivateTowerRequest
         * @function getTypeUrl
         * @memberof wtclientrpc.DeactivateTowerRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeactivateTowerRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.DeactivateTowerRequest";
        };

        return DeactivateTowerRequest;
    })();

    wtclientrpc.DeactivateTowerResponse = (function() {

        /**
         * Properties of a DeactivateTowerResponse.
         * @typedef {Object} wtclientrpc.DeactivateTowerResponse.$Properties
         * @property {string|null} [status] DeactivateTowerResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a DeactivateTowerResponse.
         * @memberof wtclientrpc
         * @interface IDeactivateTowerResponse
         * @augments wtclientrpc.DeactivateTowerResponse.$Properties
         * @deprecated Use wtclientrpc.DeactivateTowerResponse.$Properties instead.
         */

        /**
         * Shape of a DeactivateTowerResponse.
         * @typedef {wtclientrpc.DeactivateTowerResponse.$Properties} wtclientrpc.DeactivateTowerResponse.$Shape
         */

        /**
         * Constructs a new DeactivateTowerResponse.
         * @memberof wtclientrpc
         * @classdesc Represents a DeactivateTowerResponse.
         * @constructor
         * @param {wtclientrpc.DeactivateTowerResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const DeactivateTowerResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DeactivateTowerResponse status.
         * @member {string} status
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @instance
         */
        DeactivateTowerResponse.prototype.status = "";

        /**
         * Creates a new DeactivateTowerResponse instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @static
         * @param {wtclientrpc.DeactivateTowerResponse.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.DeactivateTowerResponse} DeactivateTowerResponse instance
         * @type {{
         *   (properties: wtclientrpc.DeactivateTowerResponse.$Shape): wtclientrpc.DeactivateTowerResponse & wtclientrpc.DeactivateTowerResponse.$Shape;
         *   (properties?: wtclientrpc.DeactivateTowerResponse.$Properties): wtclientrpc.DeactivateTowerResponse;
         * }}
         */
        DeactivateTowerResponse.create = function(properties) {
            return new DeactivateTowerResponse(properties);
        };

        /**
         * Encodes the specified DeactivateTowerResponse message. Does not implicitly {@link wtclientrpc.DeactivateTowerResponse.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @static
         * @param {wtclientrpc.DeactivateTowerResponse.$Properties} message DeactivateTowerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeactivateTowerResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeactivateTowerResponse message, length delimited. Does not implicitly {@link wtclientrpc.DeactivateTowerResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @static
         * @param {wtclientrpc.DeactivateTowerResponse.$Properties} message DeactivateTowerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeactivateTowerResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeactivateTowerResponse message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.DeactivateTowerResponse & wtclientrpc.DeactivateTowerResponse.$Shape} DeactivateTowerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeactivateTowerResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.DeactivateTowerResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeactivateTowerResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.DeactivateTowerResponse & wtclientrpc.DeactivateTowerResponse.$Shape} DeactivateTowerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeactivateTowerResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeactivateTowerResponse message.
         * @function verify
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeactivateTowerResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a DeactivateTowerResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.DeactivateTowerResponse} DeactivateTowerResponse
         */
        DeactivateTowerResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.DeactivateTowerResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.DeactivateTowerResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.DeactivateTowerResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a DeactivateTowerResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @static
         * @param {wtclientrpc.DeactivateTowerResponse} message DeactivateTowerResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeactivateTowerResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this DeactivateTowerResponse to JSON.
         * @function toJSON
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeactivateTowerResponse.prototype.toJSON = function() {
            return DeactivateTowerResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DeactivateTowerResponse
         * @function getTypeUrl
         * @memberof wtclientrpc.DeactivateTowerResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DeactivateTowerResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.DeactivateTowerResponse";
        };

        return DeactivateTowerResponse;
    })();

    wtclientrpc.TerminateSessionRequest = (function() {

        /**
         * Properties of a TerminateSessionRequest.
         * @typedef {Object} wtclientrpc.TerminateSessionRequest.$Properties
         * @property {Uint8Array|null} [session_id] TerminateSessionRequest session_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TerminateSessionRequest.
         * @memberof wtclientrpc
         * @interface ITerminateSessionRequest
         * @augments wtclientrpc.TerminateSessionRequest.$Properties
         * @deprecated Use wtclientrpc.TerminateSessionRequest.$Properties instead.
         */

        /**
         * Shape of a TerminateSessionRequest.
         * @typedef {wtclientrpc.TerminateSessionRequest.$Properties} wtclientrpc.TerminateSessionRequest.$Shape
         */

        /**
         * Constructs a new TerminateSessionRequest.
         * @memberof wtclientrpc
         * @classdesc Represents a TerminateSessionRequest.
         * @constructor
         * @param {wtclientrpc.TerminateSessionRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TerminateSessionRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TerminateSessionRequest session_id.
         * @member {Uint8Array} session_id
         * @memberof wtclientrpc.TerminateSessionRequest
         * @instance
         */
        TerminateSessionRequest.prototype.session_id = $util.newBuffer([]);

        /**
         * Creates a new TerminateSessionRequest instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.TerminateSessionRequest
         * @static
         * @param {wtclientrpc.TerminateSessionRequest.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.TerminateSessionRequest} TerminateSessionRequest instance
         * @type {{
         *   (properties: wtclientrpc.TerminateSessionRequest.$Shape): wtclientrpc.TerminateSessionRequest & wtclientrpc.TerminateSessionRequest.$Shape;
         *   (properties?: wtclientrpc.TerminateSessionRequest.$Properties): wtclientrpc.TerminateSessionRequest;
         * }}
         */
        TerminateSessionRequest.create = function(properties) {
            return new TerminateSessionRequest(properties);
        };

        /**
         * Encodes the specified TerminateSessionRequest message. Does not implicitly {@link wtclientrpc.TerminateSessionRequest.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.TerminateSessionRequest
         * @static
         * @param {wtclientrpc.TerminateSessionRequest.$Properties} message TerminateSessionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TerminateSessionRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id") && message.session_id.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.session_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TerminateSessionRequest message, length delimited. Does not implicitly {@link wtclientrpc.TerminateSessionRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.TerminateSessionRequest
         * @static
         * @param {wtclientrpc.TerminateSessionRequest.$Properties} message TerminateSessionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TerminateSessionRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TerminateSessionRequest message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.TerminateSessionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.TerminateSessionRequest & wtclientrpc.TerminateSessionRequest.$Shape} TerminateSessionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TerminateSessionRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.TerminateSessionRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.session_id = value;
                        else
                            delete message.session_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TerminateSessionRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.TerminateSessionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.TerminateSessionRequest & wtclientrpc.TerminateSessionRequest.$Shape} TerminateSessionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TerminateSessionRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TerminateSessionRequest message.
         * @function verify
         * @memberof wtclientrpc.TerminateSessionRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TerminateSessionRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                if (!(message.session_id && typeof message.session_id.length === "number" || $util.isString(message.session_id)))
                    return "session_id: buffer expected";
            return null;
        };

        /**
         * Creates a TerminateSessionRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.TerminateSessionRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.TerminateSessionRequest} TerminateSessionRequest
         */
        TerminateSessionRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.TerminateSessionRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.TerminateSessionRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.TerminateSessionRequest();
            if (object.session_id != null)
                if (object.session_id.length)
                    if (typeof object.session_id === "string")
                        $util.base64.decode(object.session_id, message.session_id = $util.newBuffer($util.base64.length(object.session_id)), 0);
                    else if (object.session_id.length >= 0)
                        message.session_id = object.session_id;
            return message;
        };

        /**
         * Creates a plain object from a TerminateSessionRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.TerminateSessionRequest
         * @static
         * @param {wtclientrpc.TerminateSessionRequest} message TerminateSessionRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TerminateSessionRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.session_id = "";
                else {
                    object.session_id = [];
                    if (options.bytes !== $Array)
                        object.session_id = $util.newBuffer(object.session_id);
                }
            if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                object.session_id = options.bytes === $String ? $util.base64.encode(message.session_id, 0, message.session_id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.session_id) : message.session_id;
            return object;
        };

        /**
         * Converts this TerminateSessionRequest to JSON.
         * @function toJSON
         * @memberof wtclientrpc.TerminateSessionRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TerminateSessionRequest.prototype.toJSON = function() {
            return TerminateSessionRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TerminateSessionRequest
         * @function getTypeUrl
         * @memberof wtclientrpc.TerminateSessionRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TerminateSessionRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.TerminateSessionRequest";
        };

        return TerminateSessionRequest;
    })();

    wtclientrpc.TerminateSessionResponse = (function() {

        /**
         * Properties of a TerminateSessionResponse.
         * @typedef {Object} wtclientrpc.TerminateSessionResponse.$Properties
         * @property {string|null} [status] TerminateSessionResponse status
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TerminateSessionResponse.
         * @memberof wtclientrpc
         * @interface ITerminateSessionResponse
         * @augments wtclientrpc.TerminateSessionResponse.$Properties
         * @deprecated Use wtclientrpc.TerminateSessionResponse.$Properties instead.
         */

        /**
         * Shape of a TerminateSessionResponse.
         * @typedef {wtclientrpc.TerminateSessionResponse.$Properties} wtclientrpc.TerminateSessionResponse.$Shape
         */

        /**
         * Constructs a new TerminateSessionResponse.
         * @memberof wtclientrpc
         * @classdesc Represents a TerminateSessionResponse.
         * @constructor
         * @param {wtclientrpc.TerminateSessionResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TerminateSessionResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TerminateSessionResponse status.
         * @member {string} status
         * @memberof wtclientrpc.TerminateSessionResponse
         * @instance
         */
        TerminateSessionResponse.prototype.status = "";

        /**
         * Creates a new TerminateSessionResponse instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.TerminateSessionResponse
         * @static
         * @param {wtclientrpc.TerminateSessionResponse.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.TerminateSessionResponse} TerminateSessionResponse instance
         * @type {{
         *   (properties: wtclientrpc.TerminateSessionResponse.$Shape): wtclientrpc.TerminateSessionResponse & wtclientrpc.TerminateSessionResponse.$Shape;
         *   (properties?: wtclientrpc.TerminateSessionResponse.$Properties): wtclientrpc.TerminateSessionResponse;
         * }}
         */
        TerminateSessionResponse.create = function(properties) {
            return new TerminateSessionResponse(properties);
        };

        /**
         * Encodes the specified TerminateSessionResponse message. Does not implicitly {@link wtclientrpc.TerminateSessionResponse.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.TerminateSessionResponse
         * @static
         * @param {wtclientrpc.TerminateSessionResponse.$Properties} message TerminateSessionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TerminateSessionResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TerminateSessionResponse message, length delimited. Does not implicitly {@link wtclientrpc.TerminateSessionResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.TerminateSessionResponse
         * @static
         * @param {wtclientrpc.TerminateSessionResponse.$Properties} message TerminateSessionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TerminateSessionResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TerminateSessionResponse message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.TerminateSessionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.TerminateSessionResponse & wtclientrpc.TerminateSessionResponse.$Shape} TerminateSessionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TerminateSessionResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.TerminateSessionResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TerminateSessionResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.TerminateSessionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.TerminateSessionResponse & wtclientrpc.TerminateSessionResponse.$Shape} TerminateSessionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TerminateSessionResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TerminateSessionResponse message.
         * @function verify
         * @memberof wtclientrpc.TerminateSessionResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TerminateSessionResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a TerminateSessionResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.TerminateSessionResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.TerminateSessionResponse} TerminateSessionResponse
         */
        TerminateSessionResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.TerminateSessionResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.TerminateSessionResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.TerminateSessionResponse();
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a TerminateSessionResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.TerminateSessionResponse
         * @static
         * @param {wtclientrpc.TerminateSessionResponse} message TerminateSessionResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TerminateSessionResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this TerminateSessionResponse to JSON.
         * @function toJSON
         * @memberof wtclientrpc.TerminateSessionResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TerminateSessionResponse.prototype.toJSON = function() {
            return TerminateSessionResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TerminateSessionResponse
         * @function getTypeUrl
         * @memberof wtclientrpc.TerminateSessionResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TerminateSessionResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.TerminateSessionResponse";
        };

        return TerminateSessionResponse;
    })();

    wtclientrpc.GetTowerInfoRequest = (function() {

        /**
         * Properties of a GetTowerInfoRequest.
         * @typedef {Object} wtclientrpc.GetTowerInfoRequest.$Properties
         * @property {Uint8Array|null} [pubkey] GetTowerInfoRequest pubkey
         * @property {boolean|null} [include_sessions] GetTowerInfoRequest include_sessions
         * @property {boolean|null} [exclude_exhausted_sessions] GetTowerInfoRequest exclude_exhausted_sessions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a GetTowerInfoRequest.
         * @memberof wtclientrpc
         * @interface IGetTowerInfoRequest
         * @augments wtclientrpc.GetTowerInfoRequest.$Properties
         * @deprecated Use wtclientrpc.GetTowerInfoRequest.$Properties instead.
         */

        /**
         * Shape of a GetTowerInfoRequest.
         * @typedef {wtclientrpc.GetTowerInfoRequest.$Properties} wtclientrpc.GetTowerInfoRequest.$Shape
         */

        /**
         * Constructs a new GetTowerInfoRequest.
         * @memberof wtclientrpc
         * @classdesc Represents a GetTowerInfoRequest.
         * @constructor
         * @param {wtclientrpc.GetTowerInfoRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const GetTowerInfoRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetTowerInfoRequest pubkey.
         * @member {Uint8Array} pubkey
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @instance
         */
        GetTowerInfoRequest.prototype.pubkey = $util.newBuffer([]);

        /**
         * GetTowerInfoRequest include_sessions.
         * @member {boolean} include_sessions
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @instance
         */
        GetTowerInfoRequest.prototype.include_sessions = false;

        /**
         * GetTowerInfoRequest exclude_exhausted_sessions.
         * @member {boolean} exclude_exhausted_sessions
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @instance
         */
        GetTowerInfoRequest.prototype.exclude_exhausted_sessions = false;

        /**
         * Creates a new GetTowerInfoRequest instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @static
         * @param {wtclientrpc.GetTowerInfoRequest.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.GetTowerInfoRequest} GetTowerInfoRequest instance
         * @type {{
         *   (properties: wtclientrpc.GetTowerInfoRequest.$Shape): wtclientrpc.GetTowerInfoRequest & wtclientrpc.GetTowerInfoRequest.$Shape;
         *   (properties?: wtclientrpc.GetTowerInfoRequest.$Properties): wtclientrpc.GetTowerInfoRequest;
         * }}
         */
        GetTowerInfoRequest.create = function(properties) {
            return new GetTowerInfoRequest(properties);
        };

        /**
         * Encodes the specified GetTowerInfoRequest message. Does not implicitly {@link wtclientrpc.GetTowerInfoRequest.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @static
         * @param {wtclientrpc.GetTowerInfoRequest.$Properties} message GetTowerInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTowerInfoRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey") && message.pubkey.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pubkey);
            if (message.include_sessions != null && $Object.hasOwnProperty.call(message, "include_sessions") && message.include_sessions !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.include_sessions);
            if (message.exclude_exhausted_sessions != null && $Object.hasOwnProperty.call(message, "exclude_exhausted_sessions") && message.exclude_exhausted_sessions !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.exclude_exhausted_sessions);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetTowerInfoRequest message, length delimited. Does not implicitly {@link wtclientrpc.GetTowerInfoRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @static
         * @param {wtclientrpc.GetTowerInfoRequest.$Properties} message GetTowerInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTowerInfoRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetTowerInfoRequest message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.GetTowerInfoRequest & wtclientrpc.GetTowerInfoRequest.$Shape} GetTowerInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTowerInfoRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.GetTowerInfoRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pubkey = value;
                        else
                            delete message.pubkey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_sessions = value;
                        else
                            delete message.include_sessions;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.exclude_exhausted_sessions = value;
                        else
                            delete message.exclude_exhausted_sessions;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetTowerInfoRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.GetTowerInfoRequest & wtclientrpc.GetTowerInfoRequest.$Shape} GetTowerInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTowerInfoRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetTowerInfoRequest message.
         * @function verify
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetTowerInfoRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                if (!(message.pubkey && typeof message.pubkey.length === "number" || $util.isString(message.pubkey)))
                    return "pubkey: buffer expected";
            if (message.include_sessions != null && $Object.hasOwnProperty.call(message, "include_sessions"))
                if (typeof message.include_sessions !== "boolean")
                    return "include_sessions: boolean expected";
            if (message.exclude_exhausted_sessions != null && $Object.hasOwnProperty.call(message, "exclude_exhausted_sessions"))
                if (typeof message.exclude_exhausted_sessions !== "boolean")
                    return "exclude_exhausted_sessions: boolean expected";
            return null;
        };

        /**
         * Creates a GetTowerInfoRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.GetTowerInfoRequest} GetTowerInfoRequest
         */
        GetTowerInfoRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.GetTowerInfoRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.GetTowerInfoRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.GetTowerInfoRequest();
            if (object.pubkey != null)
                if (object.pubkey.length)
                    if (typeof object.pubkey === "string")
                        $util.base64.decode(object.pubkey, message.pubkey = $util.newBuffer($util.base64.length(object.pubkey)), 0);
                    else if (object.pubkey.length >= 0)
                        message.pubkey = object.pubkey;
            if (object.include_sessions != null)
                if (object.include_sessions)
                    message.include_sessions = $Boolean(object.include_sessions);
            if (object.exclude_exhausted_sessions != null)
                if (object.exclude_exhausted_sessions)
                    message.exclude_exhausted_sessions = $Boolean(object.exclude_exhausted_sessions);
            return message;
        };

        /**
         * Creates a plain object from a GetTowerInfoRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @static
         * @param {wtclientrpc.GetTowerInfoRequest} message GetTowerInfoRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetTowerInfoRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.pubkey = "";
                else {
                    object.pubkey = [];
                    if (options.bytes !== $Array)
                        object.pubkey = $util.newBuffer(object.pubkey);
                }
                object.include_sessions = false;
                object.exclude_exhausted_sessions = false;
            }
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                object.pubkey = options.bytes === $String ? $util.base64.encode(message.pubkey, 0, message.pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pubkey) : message.pubkey;
            if (message.include_sessions != null && $Object.hasOwnProperty.call(message, "include_sessions"))
                object.include_sessions = message.include_sessions;
            if (message.exclude_exhausted_sessions != null && $Object.hasOwnProperty.call(message, "exclude_exhausted_sessions"))
                object.exclude_exhausted_sessions = message.exclude_exhausted_sessions;
            return object;
        };

        /**
         * Converts this GetTowerInfoRequest to JSON.
         * @function toJSON
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetTowerInfoRequest.prototype.toJSON = function() {
            return GetTowerInfoRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetTowerInfoRequest
         * @function getTypeUrl
         * @memberof wtclientrpc.GetTowerInfoRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetTowerInfoRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.GetTowerInfoRequest";
        };

        return GetTowerInfoRequest;
    })();

    wtclientrpc.TowerSession = (function() {

        /**
         * Properties of a TowerSession.
         * @typedef {Object} wtclientrpc.TowerSession.$Properties
         * @property {number|null} [num_backups] TowerSession num_backups
         * @property {number|null} [num_pending_backups] TowerSession num_pending_backups
         * @property {number|null} [max_backups] TowerSession max_backups
         * @property {number|null} [sweep_sat_per_byte] TowerSession sweep_sat_per_byte
         * @property {number|null} [sweep_sat_per_vbyte] TowerSession sweep_sat_per_vbyte
         * @property {Uint8Array|null} [id] TowerSession id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TowerSession.
         * @memberof wtclientrpc
         * @interface ITowerSession
         * @augments wtclientrpc.TowerSession.$Properties
         * @deprecated Use wtclientrpc.TowerSession.$Properties instead.
         */

        /**
         * Shape of a TowerSession.
         * @typedef {wtclientrpc.TowerSession.$Properties} wtclientrpc.TowerSession.$Shape
         */

        /**
         * Constructs a new TowerSession.
         * @memberof wtclientrpc
         * @classdesc Represents a TowerSession.
         * @constructor
         * @param {wtclientrpc.TowerSession.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TowerSession = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TowerSession num_backups.
         * @member {number} num_backups
         * @memberof wtclientrpc.TowerSession
         * @instance
         */
        TowerSession.prototype.num_backups = 0;

        /**
         * TowerSession num_pending_backups.
         * @member {number} num_pending_backups
         * @memberof wtclientrpc.TowerSession
         * @instance
         */
        TowerSession.prototype.num_pending_backups = 0;

        /**
         * TowerSession max_backups.
         * @member {number} max_backups
         * @memberof wtclientrpc.TowerSession
         * @instance
         */
        TowerSession.prototype.max_backups = 0;

        /**
         * TowerSession sweep_sat_per_byte.
         * @member {number} sweep_sat_per_byte
         * @memberof wtclientrpc.TowerSession
         * @instance
         */
        TowerSession.prototype.sweep_sat_per_byte = 0;

        /**
         * TowerSession sweep_sat_per_vbyte.
         * @member {number} sweep_sat_per_vbyte
         * @memberof wtclientrpc.TowerSession
         * @instance
         */
        TowerSession.prototype.sweep_sat_per_vbyte = 0;

        /**
         * TowerSession id.
         * @member {Uint8Array} id
         * @memberof wtclientrpc.TowerSession
         * @instance
         */
        TowerSession.prototype.id = $util.newBuffer([]);

        /**
         * Creates a new TowerSession instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.TowerSession
         * @static
         * @param {wtclientrpc.TowerSession.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.TowerSession} TowerSession instance
         * @type {{
         *   (properties: wtclientrpc.TowerSession.$Shape): wtclientrpc.TowerSession & wtclientrpc.TowerSession.$Shape;
         *   (properties?: wtclientrpc.TowerSession.$Properties): wtclientrpc.TowerSession;
         * }}
         */
        TowerSession.create = function(properties) {
            return new TowerSession(properties);
        };

        /**
         * Encodes the specified TowerSession message. Does not implicitly {@link wtclientrpc.TowerSession.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.TowerSession
         * @static
         * @param {wtclientrpc.TowerSession.$Properties} message TowerSession message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TowerSession.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.num_backups != null && $Object.hasOwnProperty.call(message, "num_backups") && message.num_backups !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.num_backups);
            if (message.num_pending_backups != null && $Object.hasOwnProperty.call(message, "num_pending_backups") && message.num_pending_backups !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.num_pending_backups);
            if (message.max_backups != null && $Object.hasOwnProperty.call(message, "max_backups") && message.max_backups !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.max_backups);
            if (message.sweep_sat_per_byte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_byte") && message.sweep_sat_per_byte !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.sweep_sat_per_byte);
            if (message.sweep_sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_vbyte") && message.sweep_sat_per_vbyte !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.sweep_sat_per_vbyte);
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id.length)
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TowerSession message, length delimited. Does not implicitly {@link wtclientrpc.TowerSession.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.TowerSession
         * @static
         * @param {wtclientrpc.TowerSession.$Properties} message TowerSession message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TowerSession.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TowerSession message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.TowerSession
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.TowerSession & wtclientrpc.TowerSession.$Shape} TowerSession
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TowerSession.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.TowerSession(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_backups = value;
                        else
                            delete message.num_backups;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_pending_backups = value;
                        else
                            delete message.num_pending_backups;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_backups = value;
                        else
                            delete message.max_backups;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sweep_sat_per_byte = value;
                        else
                            delete message.sweep_sat_per_byte;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sweep_sat_per_vbyte = value;
                        else
                            delete message.sweep_sat_per_vbyte;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TowerSession message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.TowerSession
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.TowerSession & wtclientrpc.TowerSession.$Shape} TowerSession
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TowerSession.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TowerSession message.
         * @function verify
         * @memberof wtclientrpc.TowerSession
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TowerSession.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.num_backups != null && $Object.hasOwnProperty.call(message, "num_backups"))
                if (!$util.isInteger(message.num_backups))
                    return "num_backups: integer expected";
            if (message.num_pending_backups != null && $Object.hasOwnProperty.call(message, "num_pending_backups"))
                if (!$util.isInteger(message.num_pending_backups))
                    return "num_pending_backups: integer expected";
            if (message.max_backups != null && $Object.hasOwnProperty.call(message, "max_backups"))
                if (!$util.isInteger(message.max_backups))
                    return "max_backups: integer expected";
            if (message.sweep_sat_per_byte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_byte"))
                if (!$util.isInteger(message.sweep_sat_per_byte))
                    return "sweep_sat_per_byte: integer expected";
            if (message.sweep_sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_vbyte"))
                if (!$util.isInteger(message.sweep_sat_per_vbyte))
                    return "sweep_sat_per_vbyte: integer expected";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!(message.id && typeof message.id.length === "number" || $util.isString(message.id)))
                    return "id: buffer expected";
            return null;
        };

        /**
         * Creates a TowerSession message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.TowerSession
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.TowerSession} TowerSession
         */
        TowerSession.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.TowerSession)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.TowerSession: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.TowerSession();
            if (object.num_backups != null)
                if ($Number(object.num_backups) !== 0)
                    message.num_backups = object.num_backups >>> 0;
            if (object.num_pending_backups != null)
                if ($Number(object.num_pending_backups) !== 0)
                    message.num_pending_backups = object.num_pending_backups >>> 0;
            if (object.max_backups != null)
                if ($Number(object.max_backups) !== 0)
                    message.max_backups = object.max_backups >>> 0;
            if (object.sweep_sat_per_byte != null)
                if ($Number(object.sweep_sat_per_byte) !== 0)
                    message.sweep_sat_per_byte = object.sweep_sat_per_byte >>> 0;
            if (object.sweep_sat_per_vbyte != null)
                if ($Number(object.sweep_sat_per_vbyte) !== 0)
                    message.sweep_sat_per_vbyte = object.sweep_sat_per_vbyte >>> 0;
            if (object.id != null)
                if (object.id.length)
                    if (typeof object.id === "string")
                        $util.base64.decode(object.id, message.id = $util.newBuffer($util.base64.length(object.id)), 0);
                    else if (object.id.length >= 0)
                        message.id = object.id;
            return message;
        };

        /**
         * Creates a plain object from a TowerSession message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.TowerSession
         * @static
         * @param {wtclientrpc.TowerSession} message TowerSession
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TowerSession.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.num_backups = 0;
                object.num_pending_backups = 0;
                object.max_backups = 0;
                object.sweep_sat_per_byte = 0;
                object.sweep_sat_per_vbyte = 0;
                if (options.bytes === $String)
                    object.id = "";
                else {
                    object.id = [];
                    if (options.bytes !== $Array)
                        object.id = $util.newBuffer(object.id);
                }
            }
            if (message.num_backups != null && $Object.hasOwnProperty.call(message, "num_backups"))
                object.num_backups = message.num_backups;
            if (message.num_pending_backups != null && $Object.hasOwnProperty.call(message, "num_pending_backups"))
                object.num_pending_backups = message.num_pending_backups;
            if (message.max_backups != null && $Object.hasOwnProperty.call(message, "max_backups"))
                object.max_backups = message.max_backups;
            if (message.sweep_sat_per_byte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_byte"))
                object.sweep_sat_per_byte = message.sweep_sat_per_byte;
            if (message.sweep_sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_vbyte"))
                object.sweep_sat_per_vbyte = message.sweep_sat_per_vbyte;
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = options.bytes === $String ? $util.base64.encode(message.id, 0, message.id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.id) : message.id;
            return object;
        };

        /**
         * Converts this TowerSession to JSON.
         * @function toJSON
         * @memberof wtclientrpc.TowerSession
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TowerSession.prototype.toJSON = function() {
            return TowerSession.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TowerSession
         * @function getTypeUrl
         * @memberof wtclientrpc.TowerSession
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TowerSession.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.TowerSession";
        };

        return TowerSession;
    })();

    wtclientrpc.Tower = (function() {

        /**
         * Properties of a Tower.
         * @typedef {Object} wtclientrpc.Tower.$Properties
         * @property {Uint8Array|null} [pubkey] Tower pubkey
         * @property {Array.<string>|null} [addresses] Tower addresses
         * @property {boolean|null} [active_session_candidate] Tower active_session_candidate
         * @property {number|null} [num_sessions] Tower num_sessions
         * @property {Array.<wtclientrpc.TowerSession.$Properties>|null} [sessions] Tower sessions
         * @property {Array.<wtclientrpc.TowerSessionInfo.$Properties>|null} [session_info] Tower session_info
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a Tower.
         * @memberof wtclientrpc
         * @interface ITower
         * @augments wtclientrpc.Tower.$Properties
         * @deprecated Use wtclientrpc.Tower.$Properties instead.
         */

        /**
         * Shape of a Tower.
         * @typedef {wtclientrpc.Tower.$Properties} wtclientrpc.Tower.$Shape
         */

        /**
         * Constructs a new Tower.
         * @memberof wtclientrpc
         * @classdesc Represents a Tower.
         * @constructor
         * @param {wtclientrpc.Tower.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const Tower = function (properties) {
            this.addresses = [];
            this.sessions = [];
            this.session_info = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Tower pubkey.
         * @member {Uint8Array} pubkey
         * @memberof wtclientrpc.Tower
         * @instance
         */
        Tower.prototype.pubkey = $util.newBuffer([]);

        /**
         * Tower addresses.
         * @member {Array.<string>} addresses
         * @memberof wtclientrpc.Tower
         * @instance
         */
        Tower.prototype.addresses = $util.emptyArray;

        /**
         * Tower active_session_candidate.
         * @member {boolean} active_session_candidate
         * @memberof wtclientrpc.Tower
         * @instance
         */
        Tower.prototype.active_session_candidate = false;

        /**
         * Tower num_sessions.
         * @member {number} num_sessions
         * @memberof wtclientrpc.Tower
         * @instance
         */
        Tower.prototype.num_sessions = 0;

        /**
         * Tower sessions.
         * @member {Array.<wtclientrpc.TowerSession.$Properties>} sessions
         * @memberof wtclientrpc.Tower
         * @instance
         */
        Tower.prototype.sessions = $util.emptyArray;

        /**
         * Tower session_info.
         * @member {Array.<wtclientrpc.TowerSessionInfo.$Properties>} session_info
         * @memberof wtclientrpc.Tower
         * @instance
         */
        Tower.prototype.session_info = $util.emptyArray;

        /**
         * Creates a new Tower instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.Tower
         * @static
         * @param {wtclientrpc.Tower.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.Tower} Tower instance
         * @type {{
         *   (properties: wtclientrpc.Tower.$Shape): wtclientrpc.Tower & wtclientrpc.Tower.$Shape;
         *   (properties?: wtclientrpc.Tower.$Properties): wtclientrpc.Tower;
         * }}
         */
        Tower.create = function(properties) {
            return new Tower(properties);
        };

        /**
         * Encodes the specified Tower message. Does not implicitly {@link wtclientrpc.Tower.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.Tower
         * @static
         * @param {wtclientrpc.Tower.$Properties} message Tower message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Tower.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey") && message.pubkey.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pubkey);
            if (message.addresses != null && message.addresses.length)
                for (let i = 0; i < message.addresses.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.addresses[i]);
            if (message.active_session_candidate != null && $Object.hasOwnProperty.call(message, "active_session_candidate") && message.active_session_candidate !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.active_session_candidate);
            if (message.num_sessions != null && $Object.hasOwnProperty.call(message, "num_sessions") && message.num_sessions !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.num_sessions);
            if (message.sessions != null && message.sessions.length)
                for (let i = 0; i < message.sessions.length; ++i)
                    $root.wtclientrpc.TowerSession.encode(message.sessions[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.session_info != null && message.session_info.length)
                for (let i = 0; i < message.session_info.length; ++i)
                    $root.wtclientrpc.TowerSessionInfo.encode(message.session_info[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Tower message, length delimited. Does not implicitly {@link wtclientrpc.Tower.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.Tower
         * @static
         * @param {wtclientrpc.Tower.$Properties} message Tower message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Tower.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Tower message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.Tower
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.Tower & wtclientrpc.Tower.$Shape} Tower
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Tower.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.Tower(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pubkey = value;
                        else
                            delete message.pubkey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.addresses && message.addresses.length))
                            message.addresses = [];
                        message.addresses.push(reader.stringVerify());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.active_session_candidate = value;
                        else
                            delete message.active_session_candidate;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_sessions = value;
                        else
                            delete message.num_sessions;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sessions && message.sessions.length))
                            message.sessions = [];
                        message.sessions.push($root.wtclientrpc.TowerSession.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.session_info && message.session_info.length))
                            message.session_info = [];
                        message.session_info.push($root.wtclientrpc.TowerSessionInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Tower message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.Tower
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.Tower & wtclientrpc.Tower.$Shape} Tower
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Tower.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Tower message.
         * @function verify
         * @memberof wtclientrpc.Tower
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Tower.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                if (!(message.pubkey && typeof message.pubkey.length === "number" || $util.isString(message.pubkey)))
                    return "pubkey: buffer expected";
            if (message.addresses != null && $Object.hasOwnProperty.call(message, "addresses")) {
                if (!$Array.isArray(message.addresses))
                    return "addresses: array expected";
                for (let i = 0; i < message.addresses.length; ++i)
                    if (!$util.isString(message.addresses[i]))
                        return "addresses: string[] expected";
            }
            if (message.active_session_candidate != null && $Object.hasOwnProperty.call(message, "active_session_candidate"))
                if (typeof message.active_session_candidate !== "boolean")
                    return "active_session_candidate: boolean expected";
            if (message.num_sessions != null && $Object.hasOwnProperty.call(message, "num_sessions"))
                if (!$util.isInteger(message.num_sessions))
                    return "num_sessions: integer expected";
            if (message.sessions != null && $Object.hasOwnProperty.call(message, "sessions")) {
                if (!$Array.isArray(message.sessions))
                    return "sessions: array expected";
                for (let i = 0; i < message.sessions.length; ++i) {
                    let error = $root.wtclientrpc.TowerSession.verify(message.sessions[i], _depth + 1);
                    if (error)
                        return "sessions." + error;
                }
            }
            if (message.session_info != null && $Object.hasOwnProperty.call(message, "session_info")) {
                if (!$Array.isArray(message.session_info))
                    return "session_info: array expected";
                for (let i = 0; i < message.session_info.length; ++i) {
                    let error = $root.wtclientrpc.TowerSessionInfo.verify(message.session_info[i], _depth + 1);
                    if (error)
                        return "session_info." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Tower message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.Tower
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.Tower} Tower
         */
        Tower.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.Tower)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.Tower: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.Tower();
            if (object.pubkey != null)
                if (object.pubkey.length)
                    if (typeof object.pubkey === "string")
                        $util.base64.decode(object.pubkey, message.pubkey = $util.newBuffer($util.base64.length(object.pubkey)), 0);
                    else if (object.pubkey.length >= 0)
                        message.pubkey = object.pubkey;
            if (object.addresses) {
                if (!$Array.isArray(object.addresses))
                    throw $TypeError(".wtclientrpc.Tower.addresses: array expected");
                message.addresses = $Array(object.addresses.length);
                for (let i = 0; i < object.addresses.length; ++i)
                    message.addresses[i] = $String(object.addresses[i]);
            }
            if (object.active_session_candidate != null)
                if (object.active_session_candidate)
                    message.active_session_candidate = $Boolean(object.active_session_candidate);
            if (object.num_sessions != null)
                if ($Number(object.num_sessions) !== 0)
                    message.num_sessions = object.num_sessions >>> 0;
            if (object.sessions) {
                if (!$Array.isArray(object.sessions))
                    throw $TypeError(".wtclientrpc.Tower.sessions: array expected");
                message.sessions = $Array(object.sessions.length);
                for (let i = 0; i < object.sessions.length; ++i) {
                    if (!$util.isObject(object.sessions[i]))
                        throw $TypeError(".wtclientrpc.Tower.sessions: object expected");
                    message.sessions[i] = $root.wtclientrpc.TowerSession.fromObject(object.sessions[i], _depth + 1);
                }
            }
            if (object.session_info) {
                if (!$Array.isArray(object.session_info))
                    throw $TypeError(".wtclientrpc.Tower.session_info: array expected");
                message.session_info = $Array(object.session_info.length);
                for (let i = 0; i < object.session_info.length; ++i) {
                    if (!$util.isObject(object.session_info[i]))
                        throw $TypeError(".wtclientrpc.Tower.session_info: object expected");
                    message.session_info[i] = $root.wtclientrpc.TowerSessionInfo.fromObject(object.session_info[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Tower message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.Tower
         * @static
         * @param {wtclientrpc.Tower} message Tower
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Tower.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.addresses = [];
                object.sessions = [];
                object.session_info = [];
            }
            if (options.defaults) {
                if (options.bytes === $String)
                    object.pubkey = "";
                else {
                    object.pubkey = [];
                    if (options.bytes !== $Array)
                        object.pubkey = $util.newBuffer(object.pubkey);
                }
                object.active_session_candidate = false;
                object.num_sessions = 0;
            }
            if (message.pubkey != null && $Object.hasOwnProperty.call(message, "pubkey"))
                object.pubkey = options.bytes === $String ? $util.base64.encode(message.pubkey, 0, message.pubkey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pubkey) : message.pubkey;
            if (message.addresses && message.addresses.length) {
                object.addresses = $Array(message.addresses.length);
                for (let j = 0; j < message.addresses.length; ++j)
                    object.addresses[j] = message.addresses[j];
            }
            if (message.active_session_candidate != null && $Object.hasOwnProperty.call(message, "active_session_candidate"))
                object.active_session_candidate = message.active_session_candidate;
            if (message.num_sessions != null && $Object.hasOwnProperty.call(message, "num_sessions"))
                object.num_sessions = message.num_sessions;
            if (message.sessions && message.sessions.length) {
                object.sessions = $Array(message.sessions.length);
                for (let j = 0; j < message.sessions.length; ++j)
                    object.sessions[j] = $root.wtclientrpc.TowerSession.toObject(message.sessions[j], options, _depth + 1);
            }
            if (message.session_info && message.session_info.length) {
                object.session_info = $Array(message.session_info.length);
                for (let j = 0; j < message.session_info.length; ++j)
                    object.session_info[j] = $root.wtclientrpc.TowerSessionInfo.toObject(message.session_info[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this Tower to JSON.
         * @function toJSON
         * @memberof wtclientrpc.Tower
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Tower.prototype.toJSON = function() {
            return Tower.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Tower
         * @function getTypeUrl
         * @memberof wtclientrpc.Tower
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Tower.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.Tower";
        };

        return Tower;
    })();

    wtclientrpc.TowerSessionInfo = (function() {

        /**
         * Properties of a TowerSessionInfo.
         * @typedef {Object} wtclientrpc.TowerSessionInfo.$Properties
         * @property {boolean|null} [active_session_candidate] TowerSessionInfo active_session_candidate
         * @property {number|null} [num_sessions] TowerSessionInfo num_sessions
         * @property {Array.<wtclientrpc.TowerSession.$Properties>|null} [sessions] TowerSessionInfo sessions
         * @property {wtclientrpc.PolicyType|null} [policy_type] TowerSessionInfo policy_type
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a TowerSessionInfo.
         * @memberof wtclientrpc
         * @interface ITowerSessionInfo
         * @augments wtclientrpc.TowerSessionInfo.$Properties
         * @deprecated Use wtclientrpc.TowerSessionInfo.$Properties instead.
         */

        /**
         * Shape of a TowerSessionInfo.
         * @typedef {wtclientrpc.TowerSessionInfo.$Properties} wtclientrpc.TowerSessionInfo.$Shape
         */

        /**
         * Constructs a new TowerSessionInfo.
         * @memberof wtclientrpc
         * @classdesc Represents a TowerSessionInfo.
         * @constructor
         * @param {wtclientrpc.TowerSessionInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const TowerSessionInfo = function (properties) {
            this.sessions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TowerSessionInfo active_session_candidate.
         * @member {boolean} active_session_candidate
         * @memberof wtclientrpc.TowerSessionInfo
         * @instance
         */
        TowerSessionInfo.prototype.active_session_candidate = false;

        /**
         * TowerSessionInfo num_sessions.
         * @member {number} num_sessions
         * @memberof wtclientrpc.TowerSessionInfo
         * @instance
         */
        TowerSessionInfo.prototype.num_sessions = 0;

        /**
         * TowerSessionInfo sessions.
         * @member {Array.<wtclientrpc.TowerSession.$Properties>} sessions
         * @memberof wtclientrpc.TowerSessionInfo
         * @instance
         */
        TowerSessionInfo.prototype.sessions = $util.emptyArray;

        /**
         * TowerSessionInfo policy_type.
         * @member {wtclientrpc.PolicyType} policy_type
         * @memberof wtclientrpc.TowerSessionInfo
         * @instance
         */
        TowerSessionInfo.prototype.policy_type = 0;

        /**
         * Creates a new TowerSessionInfo instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.TowerSessionInfo
         * @static
         * @param {wtclientrpc.TowerSessionInfo.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.TowerSessionInfo} TowerSessionInfo instance
         * @type {{
         *   (properties: wtclientrpc.TowerSessionInfo.$Shape): wtclientrpc.TowerSessionInfo & wtclientrpc.TowerSessionInfo.$Shape;
         *   (properties?: wtclientrpc.TowerSessionInfo.$Properties): wtclientrpc.TowerSessionInfo;
         * }}
         */
        TowerSessionInfo.create = function(properties) {
            return new TowerSessionInfo(properties);
        };

        /**
         * Encodes the specified TowerSessionInfo message. Does not implicitly {@link wtclientrpc.TowerSessionInfo.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.TowerSessionInfo
         * @static
         * @param {wtclientrpc.TowerSessionInfo.$Properties} message TowerSessionInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TowerSessionInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.active_session_candidate != null && $Object.hasOwnProperty.call(message, "active_session_candidate") && message.active_session_candidate !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.active_session_candidate);
            if (message.num_sessions != null && $Object.hasOwnProperty.call(message, "num_sessions") && message.num_sessions !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.num_sessions);
            if (message.sessions != null && message.sessions.length)
                for (let i = 0; i < message.sessions.length; ++i)
                    $root.wtclientrpc.TowerSession.encode(message.sessions[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.policy_type != null && $Object.hasOwnProperty.call(message, "policy_type") && message.policy_type !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.policy_type);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TowerSessionInfo message, length delimited. Does not implicitly {@link wtclientrpc.TowerSessionInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.TowerSessionInfo
         * @static
         * @param {wtclientrpc.TowerSessionInfo.$Properties} message TowerSessionInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TowerSessionInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TowerSessionInfo message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.TowerSessionInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.TowerSessionInfo & wtclientrpc.TowerSessionInfo.$Shape} TowerSessionInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TowerSessionInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.TowerSessionInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.active_session_candidate = value;
                        else
                            delete message.active_session_candidate;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_sessions = value;
                        else
                            delete message.num_sessions;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sessions && message.sessions.length))
                            message.sessions = [];
                        message.sessions.push($root.wtclientrpc.TowerSession.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.policy_type = value;
                        else
                            delete message.policy_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TowerSessionInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.TowerSessionInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.TowerSessionInfo & wtclientrpc.TowerSessionInfo.$Shape} TowerSessionInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TowerSessionInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TowerSessionInfo message.
         * @function verify
         * @memberof wtclientrpc.TowerSessionInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TowerSessionInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.active_session_candidate != null && $Object.hasOwnProperty.call(message, "active_session_candidate"))
                if (typeof message.active_session_candidate !== "boolean")
                    return "active_session_candidate: boolean expected";
            if (message.num_sessions != null && $Object.hasOwnProperty.call(message, "num_sessions"))
                if (!$util.isInteger(message.num_sessions))
                    return "num_sessions: integer expected";
            if (message.sessions != null && $Object.hasOwnProperty.call(message, "sessions")) {
                if (!$Array.isArray(message.sessions))
                    return "sessions: array expected";
                for (let i = 0; i < message.sessions.length; ++i) {
                    let error = $root.wtclientrpc.TowerSession.verify(message.sessions[i], _depth + 1);
                    if (error)
                        return "sessions." + error;
                }
            }
            if (message.policy_type != null && $Object.hasOwnProperty.call(message, "policy_type"))
                if (typeof message.policy_type !== "number" || (message.policy_type | 0) !== message.policy_type)
                    return "policy_type: enum value expected";
            return null;
        };

        /**
         * Creates a TowerSessionInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.TowerSessionInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.TowerSessionInfo} TowerSessionInfo
         */
        TowerSessionInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.TowerSessionInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.TowerSessionInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.TowerSessionInfo();
            if (object.active_session_candidate != null)
                if (object.active_session_candidate)
                    message.active_session_candidate = $Boolean(object.active_session_candidate);
            if (object.num_sessions != null)
                if ($Number(object.num_sessions) !== 0)
                    message.num_sessions = object.num_sessions >>> 0;
            if (object.sessions) {
                if (!$Array.isArray(object.sessions))
                    throw $TypeError(".wtclientrpc.TowerSessionInfo.sessions: array expected");
                message.sessions = $Array(object.sessions.length);
                for (let i = 0; i < object.sessions.length; ++i) {
                    if (!$util.isObject(object.sessions[i]))
                        throw $TypeError(".wtclientrpc.TowerSessionInfo.sessions: object expected");
                    message.sessions[i] = $root.wtclientrpc.TowerSession.fromObject(object.sessions[i], _depth + 1);
                }
            }
            if (object.policy_type !== 0 && (typeof object.policy_type !== "string" || $root.wtclientrpc.PolicyType[object.policy_type] !== 0))
                switch (object.policy_type) {
                case "LEGACY":
                case 0:
                    message.policy_type = 0;
                    break;
                case "ANCHOR":
                case 1:
                    message.policy_type = 1;
                    break;
                case "TAPROOT":
                case 2:
                    message.policy_type = 2;
                    break;
                default:
                    if (typeof object.policy_type === "number" && (object.policy_type | 0) === object.policy_type)
                        message.policy_type = object.policy_type;
                }
            return message;
        };

        /**
         * Creates a plain object from a TowerSessionInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.TowerSessionInfo
         * @static
         * @param {wtclientrpc.TowerSessionInfo} message TowerSessionInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TowerSessionInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.sessions = [];
            if (options.defaults) {
                object.active_session_candidate = false;
                object.num_sessions = 0;
                object.policy_type = options.enums === $String ? "LEGACY" : 0;
            }
            if (message.active_session_candidate != null && $Object.hasOwnProperty.call(message, "active_session_candidate"))
                object.active_session_candidate = message.active_session_candidate;
            if (message.num_sessions != null && $Object.hasOwnProperty.call(message, "num_sessions"))
                object.num_sessions = message.num_sessions;
            if (message.sessions && message.sessions.length) {
                object.sessions = $Array(message.sessions.length);
                for (let j = 0; j < message.sessions.length; ++j)
                    object.sessions[j] = $root.wtclientrpc.TowerSession.toObject(message.sessions[j], options, _depth + 1);
            }
            if (message.policy_type != null && $Object.hasOwnProperty.call(message, "policy_type"))
                object.policy_type = options.enums === $String ? $root.wtclientrpc.PolicyType[message.policy_type] === $undefined ? message.policy_type : $root.wtclientrpc.PolicyType[message.policy_type] : message.policy_type;
            return object;
        };

        /**
         * Converts this TowerSessionInfo to JSON.
         * @function toJSON
         * @memberof wtclientrpc.TowerSessionInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TowerSessionInfo.prototype.toJSON = function() {
            return TowerSessionInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TowerSessionInfo
         * @function getTypeUrl
         * @memberof wtclientrpc.TowerSessionInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TowerSessionInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.TowerSessionInfo";
        };

        return TowerSessionInfo;
    })();

    wtclientrpc.ListTowersRequest = (function() {

        /**
         * Properties of a ListTowersRequest.
         * @typedef {Object} wtclientrpc.ListTowersRequest.$Properties
         * @property {boolean|null} [include_sessions] ListTowersRequest include_sessions
         * @property {boolean|null} [exclude_exhausted_sessions] ListTowersRequest exclude_exhausted_sessions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListTowersRequest.
         * @memberof wtclientrpc
         * @interface IListTowersRequest
         * @augments wtclientrpc.ListTowersRequest.$Properties
         * @deprecated Use wtclientrpc.ListTowersRequest.$Properties instead.
         */

        /**
         * Shape of a ListTowersRequest.
         * @typedef {wtclientrpc.ListTowersRequest.$Properties} wtclientrpc.ListTowersRequest.$Shape
         */

        /**
         * Constructs a new ListTowersRequest.
         * @memberof wtclientrpc
         * @classdesc Represents a ListTowersRequest.
         * @constructor
         * @param {wtclientrpc.ListTowersRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListTowersRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListTowersRequest include_sessions.
         * @member {boolean} include_sessions
         * @memberof wtclientrpc.ListTowersRequest
         * @instance
         */
        ListTowersRequest.prototype.include_sessions = false;

        /**
         * ListTowersRequest exclude_exhausted_sessions.
         * @member {boolean} exclude_exhausted_sessions
         * @memberof wtclientrpc.ListTowersRequest
         * @instance
         */
        ListTowersRequest.prototype.exclude_exhausted_sessions = false;

        /**
         * Creates a new ListTowersRequest instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.ListTowersRequest
         * @static
         * @param {wtclientrpc.ListTowersRequest.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.ListTowersRequest} ListTowersRequest instance
         * @type {{
         *   (properties: wtclientrpc.ListTowersRequest.$Shape): wtclientrpc.ListTowersRequest & wtclientrpc.ListTowersRequest.$Shape;
         *   (properties?: wtclientrpc.ListTowersRequest.$Properties): wtclientrpc.ListTowersRequest;
         * }}
         */
        ListTowersRequest.create = function(properties) {
            return new ListTowersRequest(properties);
        };

        /**
         * Encodes the specified ListTowersRequest message. Does not implicitly {@link wtclientrpc.ListTowersRequest.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.ListTowersRequest
         * @static
         * @param {wtclientrpc.ListTowersRequest.$Properties} message ListTowersRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListTowersRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.include_sessions != null && $Object.hasOwnProperty.call(message, "include_sessions") && message.include_sessions !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.include_sessions);
            if (message.exclude_exhausted_sessions != null && $Object.hasOwnProperty.call(message, "exclude_exhausted_sessions") && message.exclude_exhausted_sessions !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.exclude_exhausted_sessions);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListTowersRequest message, length delimited. Does not implicitly {@link wtclientrpc.ListTowersRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.ListTowersRequest
         * @static
         * @param {wtclientrpc.ListTowersRequest.$Properties} message ListTowersRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListTowersRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListTowersRequest message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.ListTowersRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.ListTowersRequest & wtclientrpc.ListTowersRequest.$Shape} ListTowersRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListTowersRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.ListTowersRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.include_sessions = value;
                        else
                            delete message.include_sessions;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.exclude_exhausted_sessions = value;
                        else
                            delete message.exclude_exhausted_sessions;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListTowersRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.ListTowersRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.ListTowersRequest & wtclientrpc.ListTowersRequest.$Shape} ListTowersRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListTowersRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListTowersRequest message.
         * @function verify
         * @memberof wtclientrpc.ListTowersRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListTowersRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.include_sessions != null && $Object.hasOwnProperty.call(message, "include_sessions"))
                if (typeof message.include_sessions !== "boolean")
                    return "include_sessions: boolean expected";
            if (message.exclude_exhausted_sessions != null && $Object.hasOwnProperty.call(message, "exclude_exhausted_sessions"))
                if (typeof message.exclude_exhausted_sessions !== "boolean")
                    return "exclude_exhausted_sessions: boolean expected";
            return null;
        };

        /**
         * Creates a ListTowersRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.ListTowersRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.ListTowersRequest} ListTowersRequest
         */
        ListTowersRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.ListTowersRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.ListTowersRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.ListTowersRequest();
            if (object.include_sessions != null)
                if (object.include_sessions)
                    message.include_sessions = $Boolean(object.include_sessions);
            if (object.exclude_exhausted_sessions != null)
                if (object.exclude_exhausted_sessions)
                    message.exclude_exhausted_sessions = $Boolean(object.exclude_exhausted_sessions);
            return message;
        };

        /**
         * Creates a plain object from a ListTowersRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.ListTowersRequest
         * @static
         * @param {wtclientrpc.ListTowersRequest} message ListTowersRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListTowersRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.include_sessions = false;
                object.exclude_exhausted_sessions = false;
            }
            if (message.include_sessions != null && $Object.hasOwnProperty.call(message, "include_sessions"))
                object.include_sessions = message.include_sessions;
            if (message.exclude_exhausted_sessions != null && $Object.hasOwnProperty.call(message, "exclude_exhausted_sessions"))
                object.exclude_exhausted_sessions = message.exclude_exhausted_sessions;
            return object;
        };

        /**
         * Converts this ListTowersRequest to JSON.
         * @function toJSON
         * @memberof wtclientrpc.ListTowersRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListTowersRequest.prototype.toJSON = function() {
            return ListTowersRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListTowersRequest
         * @function getTypeUrl
         * @memberof wtclientrpc.ListTowersRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListTowersRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.ListTowersRequest";
        };

        return ListTowersRequest;
    })();

    wtclientrpc.ListTowersResponse = (function() {

        /**
         * Properties of a ListTowersResponse.
         * @typedef {Object} wtclientrpc.ListTowersResponse.$Properties
         * @property {Array.<wtclientrpc.Tower.$Properties>|null} [towers] ListTowersResponse towers
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a ListTowersResponse.
         * @memberof wtclientrpc
         * @interface IListTowersResponse
         * @augments wtclientrpc.ListTowersResponse.$Properties
         * @deprecated Use wtclientrpc.ListTowersResponse.$Properties instead.
         */

        /**
         * Shape of a ListTowersResponse.
         * @typedef {wtclientrpc.ListTowersResponse.$Properties} wtclientrpc.ListTowersResponse.$Shape
         */

        /**
         * Constructs a new ListTowersResponse.
         * @memberof wtclientrpc
         * @classdesc Represents a ListTowersResponse.
         * @constructor
         * @param {wtclientrpc.ListTowersResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const ListTowersResponse = function (properties) {
            this.towers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ListTowersResponse towers.
         * @member {Array.<wtclientrpc.Tower.$Properties>} towers
         * @memberof wtclientrpc.ListTowersResponse
         * @instance
         */
        ListTowersResponse.prototype.towers = $util.emptyArray;

        /**
         * Creates a new ListTowersResponse instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.ListTowersResponse
         * @static
         * @param {wtclientrpc.ListTowersResponse.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.ListTowersResponse} ListTowersResponse instance
         * @type {{
         *   (properties: wtclientrpc.ListTowersResponse.$Shape): wtclientrpc.ListTowersResponse & wtclientrpc.ListTowersResponse.$Shape;
         *   (properties?: wtclientrpc.ListTowersResponse.$Properties): wtclientrpc.ListTowersResponse;
         * }}
         */
        ListTowersResponse.create = function(properties) {
            return new ListTowersResponse(properties);
        };

        /**
         * Encodes the specified ListTowersResponse message. Does not implicitly {@link wtclientrpc.ListTowersResponse.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.ListTowersResponse
         * @static
         * @param {wtclientrpc.ListTowersResponse.$Properties} message ListTowersResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListTowersResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.towers != null && message.towers.length)
                for (let i = 0; i < message.towers.length; ++i)
                    $root.wtclientrpc.Tower.encode(message.towers[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ListTowersResponse message, length delimited. Does not implicitly {@link wtclientrpc.ListTowersResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.ListTowersResponse
         * @static
         * @param {wtclientrpc.ListTowersResponse.$Properties} message ListTowersResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListTowersResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListTowersResponse message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.ListTowersResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.ListTowersResponse & wtclientrpc.ListTowersResponse.$Shape} ListTowersResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListTowersResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.ListTowersResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.towers && message.towers.length))
                            message.towers = [];
                        message.towers.push($root.wtclientrpc.Tower.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ListTowersResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.ListTowersResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.ListTowersResponse & wtclientrpc.ListTowersResponse.$Shape} ListTowersResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListTowersResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListTowersResponse message.
         * @function verify
         * @memberof wtclientrpc.ListTowersResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListTowersResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.towers != null && $Object.hasOwnProperty.call(message, "towers")) {
                if (!$Array.isArray(message.towers))
                    return "towers: array expected";
                for (let i = 0; i < message.towers.length; ++i) {
                    let error = $root.wtclientrpc.Tower.verify(message.towers[i], _depth + 1);
                    if (error)
                        return "towers." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListTowersResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.ListTowersResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.ListTowersResponse} ListTowersResponse
         */
        ListTowersResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.ListTowersResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.ListTowersResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.ListTowersResponse();
            if (object.towers) {
                if (!$Array.isArray(object.towers))
                    throw $TypeError(".wtclientrpc.ListTowersResponse.towers: array expected");
                message.towers = $Array(object.towers.length);
                for (let i = 0; i < object.towers.length; ++i) {
                    if (!$util.isObject(object.towers[i]))
                        throw $TypeError(".wtclientrpc.ListTowersResponse.towers: object expected");
                    message.towers[i] = $root.wtclientrpc.Tower.fromObject(object.towers[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListTowersResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.ListTowersResponse
         * @static
         * @param {wtclientrpc.ListTowersResponse} message ListTowersResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListTowersResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.towers = [];
            if (message.towers && message.towers.length) {
                object.towers = $Array(message.towers.length);
                for (let j = 0; j < message.towers.length; ++j)
                    object.towers[j] = $root.wtclientrpc.Tower.toObject(message.towers[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ListTowersResponse to JSON.
         * @function toJSON
         * @memberof wtclientrpc.ListTowersResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListTowersResponse.prototype.toJSON = function() {
            return ListTowersResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ListTowersResponse
         * @function getTypeUrl
         * @memberof wtclientrpc.ListTowersResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ListTowersResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.ListTowersResponse";
        };

        return ListTowersResponse;
    })();

    wtclientrpc.StatsRequest = (function() {

        /**
         * Properties of a StatsRequest.
         * @typedef {Object} wtclientrpc.StatsRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a StatsRequest.
         * @memberof wtclientrpc
         * @interface IStatsRequest
         * @augments wtclientrpc.StatsRequest.$Properties
         * @deprecated Use wtclientrpc.StatsRequest.$Properties instead.
         */

        /**
         * Shape of a StatsRequest.
         * @typedef {wtclientrpc.StatsRequest.$Properties} wtclientrpc.StatsRequest.$Shape
         */

        /**
         * Constructs a new StatsRequest.
         * @memberof wtclientrpc
         * @classdesc Represents a StatsRequest.
         * @constructor
         * @param {wtclientrpc.StatsRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const StatsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new StatsRequest instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.StatsRequest
         * @static
         * @param {wtclientrpc.StatsRequest.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.StatsRequest} StatsRequest instance
         * @type {{
         *   (properties: wtclientrpc.StatsRequest.$Shape): wtclientrpc.StatsRequest & wtclientrpc.StatsRequest.$Shape;
         *   (properties?: wtclientrpc.StatsRequest.$Properties): wtclientrpc.StatsRequest;
         * }}
         */
        StatsRequest.create = function(properties) {
            return new StatsRequest(properties);
        };

        /**
         * Encodes the specified StatsRequest message. Does not implicitly {@link wtclientrpc.StatsRequest.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.StatsRequest
         * @static
         * @param {wtclientrpc.StatsRequest.$Properties} message StatsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StatsRequest message, length delimited. Does not implicitly {@link wtclientrpc.StatsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.StatsRequest
         * @static
         * @param {wtclientrpc.StatsRequest.$Properties} message StatsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatsRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StatsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.StatsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.StatsRequest & wtclientrpc.StatsRequest.$Shape} StatsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.StatsRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StatsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.StatsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.StatsRequest & wtclientrpc.StatsRequest.$Shape} StatsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatsRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatsRequest message.
         * @function verify
         * @memberof wtclientrpc.StatsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatsRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a StatsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.StatsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.StatsRequest} StatsRequest
         */
        StatsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.StatsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.StatsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.wtclientrpc.StatsRequest();
        };

        /**
         * Creates a plain object from a StatsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.StatsRequest
         * @static
         * @param {wtclientrpc.StatsRequest} message StatsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatsRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this StatsRequest to JSON.
         * @function toJSON
         * @memberof wtclientrpc.StatsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatsRequest.prototype.toJSON = function() {
            return StatsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StatsRequest
         * @function getTypeUrl
         * @memberof wtclientrpc.StatsRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StatsRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.StatsRequest";
        };

        return StatsRequest;
    })();

    wtclientrpc.StatsResponse = (function() {

        /**
         * Properties of a StatsResponse.
         * @typedef {Object} wtclientrpc.StatsResponse.$Properties
         * @property {number|null} [num_backups] StatsResponse num_backups
         * @property {number|null} [num_pending_backups] StatsResponse num_pending_backups
         * @property {number|null} [num_failed_backups] StatsResponse num_failed_backups
         * @property {number|null} [num_sessions_acquired] StatsResponse num_sessions_acquired
         * @property {number|null} [num_sessions_exhausted] StatsResponse num_sessions_exhausted
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a StatsResponse.
         * @memberof wtclientrpc
         * @interface IStatsResponse
         * @augments wtclientrpc.StatsResponse.$Properties
         * @deprecated Use wtclientrpc.StatsResponse.$Properties instead.
         */

        /**
         * Shape of a StatsResponse.
         * @typedef {wtclientrpc.StatsResponse.$Properties} wtclientrpc.StatsResponse.$Shape
         */

        /**
         * Constructs a new StatsResponse.
         * @memberof wtclientrpc
         * @classdesc Represents a StatsResponse.
         * @constructor
         * @param {wtclientrpc.StatsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const StatsResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StatsResponse num_backups.
         * @member {number} num_backups
         * @memberof wtclientrpc.StatsResponse
         * @instance
         */
        StatsResponse.prototype.num_backups = 0;

        /**
         * StatsResponse num_pending_backups.
         * @member {number} num_pending_backups
         * @memberof wtclientrpc.StatsResponse
         * @instance
         */
        StatsResponse.prototype.num_pending_backups = 0;

        /**
         * StatsResponse num_failed_backups.
         * @member {number} num_failed_backups
         * @memberof wtclientrpc.StatsResponse
         * @instance
         */
        StatsResponse.prototype.num_failed_backups = 0;

        /**
         * StatsResponse num_sessions_acquired.
         * @member {number} num_sessions_acquired
         * @memberof wtclientrpc.StatsResponse
         * @instance
         */
        StatsResponse.prototype.num_sessions_acquired = 0;

        /**
         * StatsResponse num_sessions_exhausted.
         * @member {number} num_sessions_exhausted
         * @memberof wtclientrpc.StatsResponse
         * @instance
         */
        StatsResponse.prototype.num_sessions_exhausted = 0;

        /**
         * Creates a new StatsResponse instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.StatsResponse
         * @static
         * @param {wtclientrpc.StatsResponse.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.StatsResponse} StatsResponse instance
         * @type {{
         *   (properties: wtclientrpc.StatsResponse.$Shape): wtclientrpc.StatsResponse & wtclientrpc.StatsResponse.$Shape;
         *   (properties?: wtclientrpc.StatsResponse.$Properties): wtclientrpc.StatsResponse;
         * }}
         */
        StatsResponse.create = function(properties) {
            return new StatsResponse(properties);
        };

        /**
         * Encodes the specified StatsResponse message. Does not implicitly {@link wtclientrpc.StatsResponse.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.StatsResponse
         * @static
         * @param {wtclientrpc.StatsResponse.$Properties} message StatsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.num_backups != null && $Object.hasOwnProperty.call(message, "num_backups") && message.num_backups !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.num_backups);
            if (message.num_pending_backups != null && $Object.hasOwnProperty.call(message, "num_pending_backups") && message.num_pending_backups !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.num_pending_backups);
            if (message.num_failed_backups != null && $Object.hasOwnProperty.call(message, "num_failed_backups") && message.num_failed_backups !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.num_failed_backups);
            if (message.num_sessions_acquired != null && $Object.hasOwnProperty.call(message, "num_sessions_acquired") && message.num_sessions_acquired !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.num_sessions_acquired);
            if (message.num_sessions_exhausted != null && $Object.hasOwnProperty.call(message, "num_sessions_exhausted") && message.num_sessions_exhausted !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.num_sessions_exhausted);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StatsResponse message, length delimited. Does not implicitly {@link wtclientrpc.StatsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.StatsResponse
         * @static
         * @param {wtclientrpc.StatsResponse.$Properties} message StatsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StatsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.StatsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.StatsResponse & wtclientrpc.StatsResponse.$Shape} StatsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.StatsResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_backups = value;
                        else
                            delete message.num_backups;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_pending_backups = value;
                        else
                            delete message.num_pending_backups;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_failed_backups = value;
                        else
                            delete message.num_failed_backups;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_sessions_acquired = value;
                        else
                            delete message.num_sessions_acquired;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.num_sessions_exhausted = value;
                        else
                            delete message.num_sessions_exhausted;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StatsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.StatsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.StatsResponse & wtclientrpc.StatsResponse.$Shape} StatsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatsResponse message.
         * @function verify
         * @memberof wtclientrpc.StatsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.num_backups != null && $Object.hasOwnProperty.call(message, "num_backups"))
                if (!$util.isInteger(message.num_backups))
                    return "num_backups: integer expected";
            if (message.num_pending_backups != null && $Object.hasOwnProperty.call(message, "num_pending_backups"))
                if (!$util.isInteger(message.num_pending_backups))
                    return "num_pending_backups: integer expected";
            if (message.num_failed_backups != null && $Object.hasOwnProperty.call(message, "num_failed_backups"))
                if (!$util.isInteger(message.num_failed_backups))
                    return "num_failed_backups: integer expected";
            if (message.num_sessions_acquired != null && $Object.hasOwnProperty.call(message, "num_sessions_acquired"))
                if (!$util.isInteger(message.num_sessions_acquired))
                    return "num_sessions_acquired: integer expected";
            if (message.num_sessions_exhausted != null && $Object.hasOwnProperty.call(message, "num_sessions_exhausted"))
                if (!$util.isInteger(message.num_sessions_exhausted))
                    return "num_sessions_exhausted: integer expected";
            return null;
        };

        /**
         * Creates a StatsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.StatsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.StatsResponse} StatsResponse
         */
        StatsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.StatsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.StatsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.StatsResponse();
            if (object.num_backups != null)
                if ($Number(object.num_backups) !== 0)
                    message.num_backups = object.num_backups >>> 0;
            if (object.num_pending_backups != null)
                if ($Number(object.num_pending_backups) !== 0)
                    message.num_pending_backups = object.num_pending_backups >>> 0;
            if (object.num_failed_backups != null)
                if ($Number(object.num_failed_backups) !== 0)
                    message.num_failed_backups = object.num_failed_backups >>> 0;
            if (object.num_sessions_acquired != null)
                if ($Number(object.num_sessions_acquired) !== 0)
                    message.num_sessions_acquired = object.num_sessions_acquired >>> 0;
            if (object.num_sessions_exhausted != null)
                if ($Number(object.num_sessions_exhausted) !== 0)
                    message.num_sessions_exhausted = object.num_sessions_exhausted >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a StatsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.StatsResponse
         * @static
         * @param {wtclientrpc.StatsResponse} message StatsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.num_backups = 0;
                object.num_pending_backups = 0;
                object.num_failed_backups = 0;
                object.num_sessions_acquired = 0;
                object.num_sessions_exhausted = 0;
            }
            if (message.num_backups != null && $Object.hasOwnProperty.call(message, "num_backups"))
                object.num_backups = message.num_backups;
            if (message.num_pending_backups != null && $Object.hasOwnProperty.call(message, "num_pending_backups"))
                object.num_pending_backups = message.num_pending_backups;
            if (message.num_failed_backups != null && $Object.hasOwnProperty.call(message, "num_failed_backups"))
                object.num_failed_backups = message.num_failed_backups;
            if (message.num_sessions_acquired != null && $Object.hasOwnProperty.call(message, "num_sessions_acquired"))
                object.num_sessions_acquired = message.num_sessions_acquired;
            if (message.num_sessions_exhausted != null && $Object.hasOwnProperty.call(message, "num_sessions_exhausted"))
                object.num_sessions_exhausted = message.num_sessions_exhausted;
            return object;
        };

        /**
         * Converts this StatsResponse to JSON.
         * @function toJSON
         * @memberof wtclientrpc.StatsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatsResponse.prototype.toJSON = function() {
            return StatsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StatsResponse
         * @function getTypeUrl
         * @memberof wtclientrpc.StatsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StatsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.StatsResponse";
        };

        return StatsResponse;
    })();

    /**
     * PolicyType enum.
     * @name wtclientrpc.PolicyType
     * @enum {number}
     * @property {number} LEGACY=0 LEGACY value
     * @property {number} ANCHOR=1 ANCHOR value
     * @property {number} TAPROOT=2 TAPROOT value
     */
    wtclientrpc.PolicyType = (function() {
        const valuesById = {}, values = $Object.create(valuesById);
        values[valuesById[0] = "LEGACY"] = 0;
        values[valuesById[1] = "ANCHOR"] = 1;
        values[valuesById[2] = "TAPROOT"] = 2;
        return values;
    })();

    wtclientrpc.PolicyRequest = (function() {

        /**
         * Properties of a PolicyRequest.
         * @typedef {Object} wtclientrpc.PolicyRequest.$Properties
         * @property {wtclientrpc.PolicyType|null} [policy_type] PolicyRequest policy_type
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PolicyRequest.
         * @memberof wtclientrpc
         * @interface IPolicyRequest
         * @augments wtclientrpc.PolicyRequest.$Properties
         * @deprecated Use wtclientrpc.PolicyRequest.$Properties instead.
         */

        /**
         * Shape of a PolicyRequest.
         * @typedef {wtclientrpc.PolicyRequest.$Properties} wtclientrpc.PolicyRequest.$Shape
         */

        /**
         * Constructs a new PolicyRequest.
         * @memberof wtclientrpc
         * @classdesc Represents a PolicyRequest.
         * @constructor
         * @param {wtclientrpc.PolicyRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PolicyRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PolicyRequest policy_type.
         * @member {wtclientrpc.PolicyType} policy_type
         * @memberof wtclientrpc.PolicyRequest
         * @instance
         */
        PolicyRequest.prototype.policy_type = 0;

        /**
         * Creates a new PolicyRequest instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.PolicyRequest
         * @static
         * @param {wtclientrpc.PolicyRequest.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.PolicyRequest} PolicyRequest instance
         * @type {{
         *   (properties: wtclientrpc.PolicyRequest.$Shape): wtclientrpc.PolicyRequest & wtclientrpc.PolicyRequest.$Shape;
         *   (properties?: wtclientrpc.PolicyRequest.$Properties): wtclientrpc.PolicyRequest;
         * }}
         */
        PolicyRequest.create = function(properties) {
            return new PolicyRequest(properties);
        };

        /**
         * Encodes the specified PolicyRequest message. Does not implicitly {@link wtclientrpc.PolicyRequest.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.PolicyRequest
         * @static
         * @param {wtclientrpc.PolicyRequest.$Properties} message PolicyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PolicyRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.policy_type != null && $Object.hasOwnProperty.call(message, "policy_type") && message.policy_type !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.policy_type);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PolicyRequest message, length delimited. Does not implicitly {@link wtclientrpc.PolicyRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.PolicyRequest
         * @static
         * @param {wtclientrpc.PolicyRequest.$Properties} message PolicyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PolicyRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PolicyRequest message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.PolicyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.PolicyRequest & wtclientrpc.PolicyRequest.$Shape} PolicyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PolicyRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.PolicyRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.policy_type = value;
                        else
                            delete message.policy_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PolicyRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.PolicyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.PolicyRequest & wtclientrpc.PolicyRequest.$Shape} PolicyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PolicyRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PolicyRequest message.
         * @function verify
         * @memberof wtclientrpc.PolicyRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PolicyRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.policy_type != null && $Object.hasOwnProperty.call(message, "policy_type"))
                if (typeof message.policy_type !== "number" || (message.policy_type | 0) !== message.policy_type)
                    return "policy_type: enum value expected";
            return null;
        };

        /**
         * Creates a PolicyRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.PolicyRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.PolicyRequest} PolicyRequest
         */
        PolicyRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.PolicyRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.PolicyRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.PolicyRequest();
            if (object.policy_type !== 0 && (typeof object.policy_type !== "string" || $root.wtclientrpc.PolicyType[object.policy_type] !== 0))
                switch (object.policy_type) {
                case "LEGACY":
                case 0:
                    message.policy_type = 0;
                    break;
                case "ANCHOR":
                case 1:
                    message.policy_type = 1;
                    break;
                case "TAPROOT":
                case 2:
                    message.policy_type = 2;
                    break;
                default:
                    if (typeof object.policy_type === "number" && (object.policy_type | 0) === object.policy_type)
                        message.policy_type = object.policy_type;
                }
            return message;
        };

        /**
         * Creates a plain object from a PolicyRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.PolicyRequest
         * @static
         * @param {wtclientrpc.PolicyRequest} message PolicyRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PolicyRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.policy_type = options.enums === $String ? "LEGACY" : 0;
            if (message.policy_type != null && $Object.hasOwnProperty.call(message, "policy_type"))
                object.policy_type = options.enums === $String ? $root.wtclientrpc.PolicyType[message.policy_type] === $undefined ? message.policy_type : $root.wtclientrpc.PolicyType[message.policy_type] : message.policy_type;
            return object;
        };

        /**
         * Converts this PolicyRequest to JSON.
         * @function toJSON
         * @memberof wtclientrpc.PolicyRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PolicyRequest.prototype.toJSON = function() {
            return PolicyRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PolicyRequest
         * @function getTypeUrl
         * @memberof wtclientrpc.PolicyRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PolicyRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.PolicyRequest";
        };

        return PolicyRequest;
    })();

    wtclientrpc.PolicyResponse = (function() {

        /**
         * Properties of a PolicyResponse.
         * @typedef {Object} wtclientrpc.PolicyResponse.$Properties
         * @property {number|null} [max_updates] PolicyResponse max_updates
         * @property {number|null} [sweep_sat_per_byte] PolicyResponse sweep_sat_per_byte
         * @property {number|null} [sweep_sat_per_vbyte] PolicyResponse sweep_sat_per_vbyte
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */

        /**
         * Properties of a PolicyResponse.
         * @memberof wtclientrpc
         * @interface IPolicyResponse
         * @augments wtclientrpc.PolicyResponse.$Properties
         * @deprecated Use wtclientrpc.PolicyResponse.$Properties instead.
         */

        /**
         * Shape of a PolicyResponse.
         * @typedef {wtclientrpc.PolicyResponse.$Properties} wtclientrpc.PolicyResponse.$Shape
         */

        /**
         * Constructs a new PolicyResponse.
         * @memberof wtclientrpc
         * @classdesc Represents a PolicyResponse.
         * @constructor
         * @param {wtclientrpc.PolicyResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding
         */
        const PolicyResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PolicyResponse max_updates.
         * @member {number} max_updates
         * @memberof wtclientrpc.PolicyResponse
         * @instance
         */
        PolicyResponse.prototype.max_updates = 0;

        /**
         * PolicyResponse sweep_sat_per_byte.
         * @member {number} sweep_sat_per_byte
         * @memberof wtclientrpc.PolicyResponse
         * @instance
         */
        PolicyResponse.prototype.sweep_sat_per_byte = 0;

        /**
         * PolicyResponse sweep_sat_per_vbyte.
         * @member {number} sweep_sat_per_vbyte
         * @memberof wtclientrpc.PolicyResponse
         * @instance
         */
        PolicyResponse.prototype.sweep_sat_per_vbyte = 0;

        /**
         * Creates a new PolicyResponse instance using the specified properties.
         * @function create
         * @memberof wtclientrpc.PolicyResponse
         * @static
         * @param {wtclientrpc.PolicyResponse.$Properties=} [properties] Properties to set
         * @returns {wtclientrpc.PolicyResponse} PolicyResponse instance
         * @type {{
         *   (properties: wtclientrpc.PolicyResponse.$Shape): wtclientrpc.PolicyResponse & wtclientrpc.PolicyResponse.$Shape;
         *   (properties?: wtclientrpc.PolicyResponse.$Properties): wtclientrpc.PolicyResponse;
         * }}
         */
        PolicyResponse.create = function(properties) {
            return new PolicyResponse(properties);
        };

        /**
         * Encodes the specified PolicyResponse message. Does not implicitly {@link wtclientrpc.PolicyResponse.verify|verify} messages.
         * @function encode
         * @memberof wtclientrpc.PolicyResponse
         * @static
         * @param {wtclientrpc.PolicyResponse.$Properties} message PolicyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PolicyResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.max_updates != null && $Object.hasOwnProperty.call(message, "max_updates") && message.max_updates !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.max_updates);
            if (message.sweep_sat_per_byte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_byte") && message.sweep_sat_per_byte !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.sweep_sat_per_byte);
            if (message.sweep_sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_vbyte") && message.sweep_sat_per_vbyte !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.sweep_sat_per_vbyte);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PolicyResponse message, length delimited. Does not implicitly {@link wtclientrpc.PolicyResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wtclientrpc.PolicyResponse
         * @static
         * @param {wtclientrpc.PolicyResponse.$Properties} message PolicyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PolicyResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PolicyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof wtclientrpc.PolicyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wtclientrpc.PolicyResponse & wtclientrpc.PolicyResponse.$Shape} PolicyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PolicyResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wtclientrpc.PolicyResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_updates = value;
                        else
                            delete message.max_updates;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sweep_sat_per_byte = value;
                        else
                            delete message.sweep_sat_per_byte;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sweep_sat_per_vbyte = value;
                        else
                            delete message.sweep_sat_per_vbyte;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PolicyResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wtclientrpc.PolicyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wtclientrpc.PolicyResponse & wtclientrpc.PolicyResponse.$Shape} PolicyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PolicyResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PolicyResponse message.
         * @function verify
         * @memberof wtclientrpc.PolicyResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PolicyResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.max_updates != null && $Object.hasOwnProperty.call(message, "max_updates"))
                if (!$util.isInteger(message.max_updates))
                    return "max_updates: integer expected";
            if (message.sweep_sat_per_byte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_byte"))
                if (!$util.isInteger(message.sweep_sat_per_byte))
                    return "sweep_sat_per_byte: integer expected";
            if (message.sweep_sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_vbyte"))
                if (!$util.isInteger(message.sweep_sat_per_vbyte))
                    return "sweep_sat_per_vbyte: integer expected";
            return null;
        };

        /**
         * Creates a PolicyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wtclientrpc.PolicyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wtclientrpc.PolicyResponse} PolicyResponse
         */
        PolicyResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.wtclientrpc.PolicyResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wtclientrpc.PolicyResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wtclientrpc.PolicyResponse();
            if (object.max_updates != null)
                if ($Number(object.max_updates) !== 0)
                    message.max_updates = object.max_updates >>> 0;
            if (object.sweep_sat_per_byte != null)
                if ($Number(object.sweep_sat_per_byte) !== 0)
                    message.sweep_sat_per_byte = object.sweep_sat_per_byte >>> 0;
            if (object.sweep_sat_per_vbyte != null)
                if ($Number(object.sweep_sat_per_vbyte) !== 0)
                    message.sweep_sat_per_vbyte = object.sweep_sat_per_vbyte >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a PolicyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wtclientrpc.PolicyResponse
         * @static
         * @param {wtclientrpc.PolicyResponse} message PolicyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PolicyResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.max_updates = 0;
                object.sweep_sat_per_byte = 0;
                object.sweep_sat_per_vbyte = 0;
            }
            if (message.max_updates != null && $Object.hasOwnProperty.call(message, "max_updates"))
                object.max_updates = message.max_updates;
            if (message.sweep_sat_per_byte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_byte"))
                object.sweep_sat_per_byte = message.sweep_sat_per_byte;
            if (message.sweep_sat_per_vbyte != null && $Object.hasOwnProperty.call(message, "sweep_sat_per_vbyte"))
                object.sweep_sat_per_vbyte = message.sweep_sat_per_vbyte;
            return object;
        };

        /**
         * Converts this PolicyResponse to JSON.
         * @function toJSON
         * @memberof wtclientrpc.PolicyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PolicyResponse.prototype.toJSON = function() {
            return PolicyResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PolicyResponse
         * @function getTypeUrl
         * @memberof wtclientrpc.PolicyResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PolicyResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wtclientrpc.PolicyResponse";
        };

        return PolicyResponse;
    })();

    return wtclientrpc;
})();

export {
  $root as default
};
